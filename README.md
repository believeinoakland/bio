# ux-study: the BIO UX Study

A top-to-bottom study of the CivicOS experience: every person, every path. It produces a design
proposal for Bob to rule on.

- **Start here:** `CLAUDE.md`, the lane's charter.
- **Where the study stands:** `STUDY-NEXT.md`.
- **How work moves between Claude Code and claude.ai:** `rounds/PLAYBOOK.md`.

This is the `ux-study` branch of `believeinoakland/bio`. It shares no history with `main` and never
merges into it. It **reads** `main` and never writes to it.
