# REC-160 — worker report (CONDUCT #15), relayed for CONDUCT #16. INTEGRATE IT.

Branch `land/worker/REC-160` @ 30706853 (REC-160 commit 5ea3849c + merge of origin/main b41d1edb; bundle rebuilt). GREEN FULL
`291/291 suites green · 17434 assertions passing` on tree 639a1444; SKIP 0; coverage --strict, UI harnesses green.
BUILT (store.mjs): `reevaluations()` asks `#refEdgeSevered(bundleId, t)` per dependent; `#reevalLegsEarned` publishes
`status` on every leg (`severed` only where a withdrawal is recorded, else `confirmed`); a severed leg's edition cause says it
was WITHDRAWN and NAMED edition N — "supports nothing here … listed because the connection still informs a second look
(DEC-70)". Nothing filtered; obligation still fires; nothing derived from strength; confirmed wording byte-identical.
reevaluation.test block 9 (severed, confirmed, over-strictness): 67/6 on d89e04d1 → 73/0; d280-strengthbar §7 SITE (c)
34/1 → 35/0. Controls (on the suite's NEGATIVE CONTROL line): drop status 71/2; FILTER LIAR 68/5 + d280 SITE (c) 34/1;
revert wording 72/1 — each by name. Corrected (dated): SITE (c)'s claim; severedhomes' exact caller count 5 → 6.
IC-189 PROPOSED I3 additive (proposal says 57.0.0 → 57.1.0) — RESOLVE ON TODAY'S BASE (I3 58.0.0+). Zero non-test consumers.
OWED AT INTEGRATION: re-run the severedhomes count if another caller of #refEdgeSevered lands. No DEC-49 region, no floors.
DEFECT (serious, to SCHEDULER): severedhomes.test.mjs checks only its FIRST assertion since REC-141 (IC-158): it throws
PROJECT_ID_SUPPLIED at a promote, and its try/finally calls process.exit(fail?1:0) with NO catch, so it prints "1 pass, 0
fail" and exits 0 (reproduced on unmodified d89e04d1). 27 *.test.mjs files share a finally→process.exit with no catch.
FIX: mint the project fixtures (as makePublishingProject does); add a catch that counts a failure; add a hygiene rule that
a suite's finally-exit must follow a catch.
