# M0-100 — worker report (CONDUCT #14, wave 6), relayed for CONDUCT #15. INTEGRATE IT SOON (see "already happening").

Branch `worktree-agent-a08c137666a63d52b` @ 69925f59 (merged origin/main cdfaea39; no push to land/*). Gates: GREEN FULL on
tree 647808fa `284/284 suites green · 17143 assertions passing`; the pushed tip (after the 2nd merge) GREEN SINCE 118/118 ·
8,270 on tree 3bb869b4 — the tip has no FULL of its own. mintid --audit 0 breaks; M-109 minted.
BUILT: each NEW entry is its own file — `docs/development/measurements/M-<n>.md`, `docs/development/interface-changes/IC-<n>.md`,
named by its minted id and opening with the ledger's own heading; MEASUREMENTS.md and INTERFACE-CHANGES.md FROZEN (one head
line each). ONE reader `tools/entries.mjs` (read/entries/find/view/corpus/kindOf; `audit()` checks freeze + layout;
`carry` moves an entry appended to a frozen file into its own file verbatim). Readers changed: mintid (M/IC corpora and the
allocation corpus now see entry files — else ids could be issued twice), `ledger.mjs find` (answers M-/IC- via the reader —
it answered NEITHER before), plancheck §2e runs audit(); tree-walkers unchanged and asserted. Suite entries.test.mjs 58/0;
acceptance driven in a throwaway remote: two land/* branches each adding a measurement land in ONE train, ONE gate, no
conflict; the frozen-file tail-append contrast conflicts; the merge=union liar is named by audit. Control 4/4 AS DECLARED
(arm 1: mintid's M corpus back at MEASUREMENTS.md alone fails by name). Reader counts over frozen history unchanged (M-109).
ALREADY HAPPENING: M-110 (M0-121) and M-111 (your leak fix) were appended to the frozen file after the cut; the worker moved
both out with `carry` (cmp-verified, Dropped-from-branch trailer). UNTIL THIS LANDS, any lane appending to MEASUREMENTS.md or
INTERFACE-CHANGES.md will be failed by plancheck §2e after merge — the fix is `node tools/entries.mjs carry`.
FOR SCHEDULER: pipeline-readers.control.mjs is broken ON MAIN (49/12, same on 30475ca6): its PLANT arms write into the scratch
BACKLOG.md pointer; fix: plant full content in the scratch tree or point BIO_COORD_REF at a planted ref. Not detected: a state
line appended to a frozen IC entry without its own heading (stated in entries.mjs). ledger.control.mjs not completed (killed
at T7 by the worker's own timeout; tools/mintid.mjs restored from HEAD, cmp-verified).
