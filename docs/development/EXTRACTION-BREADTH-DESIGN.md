# Extraction breadth — Part II §18 piece 4

**Status** · v0.1 DRAFT design, written 2026-09-14 by session BOB #11 under Bob's standing delegation (mechanism is the architect's). Not yet reviewed by Bob. **§4's production table is UNCHANGED as a design and its THIRD row was BUILT on 2026-09-14 by SK-8: the `ai(function, version)` step has its first producer, inside DEC-62's run per `BIO_Assistant_and_AI_Roles_v0_1.md` §7.3; the other four productions in that table remain unbuilt and §4 says which and why.** Complete as a design of five of piece 4's six parts — readers beyond three, tables and images as content, the extraction half of the AI EXTRACT role, read-time re-extraction to tier 3 (D-319), the per-page tier-2 rule (D-283); the sixth, the external OCR tier, is DEC-74, answered 2026-09-14 at Bob's delegation (not funded); §6 records the decision and the trigger that reopens it. Every "as built" statement below is Part II §15–§16's, measured 2026-09-14 at the shas those sections name; nothing here re-measures the plane. **THE PER-PAGE TIER-2 RULE IS NO LONGER "COMPLETE AS WRITTEN" AND THE CLAIM ABOVE IS QUALIFIED RATHER THAN LEFT STANDING: it is MEASURED-AND-CORRECTED** — CPDF-20 falsified §5.2's rule against 203 pages of 28 real documents and the corrected rule is what shipped, and REC-98 then WIRED it into the plane's two call sites (2026-09-15) and found a limit the fixture cannot exercise. Both are stated in §5.2 and in Incomplete sections; the other four of the five stand as written. **AND §5.2 GAINED ITS ORDER-OF-MERGES HALF ON 2026-09-16 (REC-102, closing D-372): the per-page statement is what the LAST merge composes FROM, not only what the tier-2 merge records** — §5.1's tier-3 merge rebuilt it from one document-level tier and now takes it from `perPageTier`, driven against a SYNTHESISED fixture for the one class that reaches both merges, the only real instance of it being a private record. **§3.2's `{part}` FENCE WAS BUILT ON 2026-09-23 BY D-440 (C-45.11; the capture's format projected onto `readings.capture_format`), stated in §3.2's own paragraph.** **§3.2 AND §3.3 ITEM 1 WERE BUILT ON 2026-09-18 BY FW-19 (§7 row 3; IC-124 on I2, IC-125 on I5/I3):** the `sheet-range` and `doc-table` arms and the `image` reference exist in IC-1's union, the six office entries emit them (`tables`, per-sheet `range`, `images`), the acquire wire persists the two new bounds, and the content table admits all three with `covers` per arm and the `cited_as` column, driven end to end in `bio-plane/test/fw19-extent-arms.test.mjs`. What §3.3 item 1 names and was NOT built then — a workbook's defined tables and named ranges as units — **WAS BUILT ON 2026-09-25 BY D-415**, stated in §3.3 item 1's own paragraph. **§5.1 IS BUILT (CPDF-19, 2026-09-18, IC-126): `op=pdfstructure&ocr=1` re-reads a held capture to tier 3 through the SAME seam function the acquire path calls, writes through promote's own per-capture writer, and refuses by name with no member bound; §5.1 records what it does and the two ways the build departs from this text.** **§3.3 ITEMS 2 AND 3 WERE TAKEN ON 2026-09-18 BY CPDF-18 (§7 row 4):** item 2 is BUILT — the structure op emits `image {page, rect}` for every image a page PAINTS (`pdfstructure.mjs`, `q`/`Q`/`cm`/`Do` and Form `/Matrix` interpreted, inline images reported), a row mints from that reference as bytes, and its crop is a derived rendition (`pdf-worker/src/imagecrop.mjs`) driven in `bio-plane/test/cpdf18-pdf-images.test.mjs`; the crop was reachable by no op until D-419. **THE CROP CAN BE ASKED FOR since 2026-09-25 (D-419, C-99): the PDF member serves `POST /crop` and the plane's read-only `op=contentcrop` resolves one content row through op=content's own viewer-stamped read and returns the crop of the image its `{page, rect}` names, labelled derived and checked against the row's capture hash, driven end to end in `bio-plane/test/d419-content-crop.test.mjs`; and a member can SEE it since 2026-09-25 (UI-120): the inquiry page's leg row offers the crop on a leg citing an image with a rectangle, and on no other, asked when used and never on load, driven in `civicos-ui/test/content-crop-surface.test.mjs` — §3.4 records both.** **THE PAGE FORM IS BOUNDED BY WHAT THE PAGE PAINTS since 2026-09-23 (D-420, IC-203 on I1, IC-204 on I3):** acquire persists the structure op's placements as `container_extent.images` for a PDF, and `covers` refuses a `{page, rect}` no placement equals — and a `{page}` alone on a page painting none — `CONTENT_EXTENT_NO_IMAGE_PAINTED` (C-45.12) by name, with the crop's own equality (1/1000 pt); a PDF acquired before D-420 holds no list (not backfilled) and is admitted with that absence stated (`image_bound`), driven in `bio-plane/test/d420-image-page.test.mjs`. Item 3 is MEASURED **NO-GO** (`MEASUREMENTS.md` M-55): the only account-free table step the runtime can run, a geometric one over pdf.js positions, reproduced 15/15 pages across workerd and node — which for deterministic code costs nothing — and recovered the unruled table exactly and the RULED one not at all. No `table(engine)` step exists. §3.2 now states that the `image` arm's `{part}` form addresses a container member only (D-55's design, 2026-09-21); the fence that enforces it is D-440, not built. §2 gained Bob's fifth named type, budget or dataset, as OWED and UNMEASURED (D-66). **§7 ROW 2 IS COMPLETE THROUGH §2's FOURTH CLASS (FW-20, 2026-09-23): the STAFF DIRECTORY type is written, and the premise it had been withheld on (D-376, "a tier-3 gap") was re-taken and did not hold — the directories are TIER-2 documents (fonts with no Unicode map), and 56 of 57 name-matched documents read from text with the fleet bound (`measurements/M-121.md`). §2's row 5 (a budget or dataset) was COUNTED on 2026-09-24 (D-66, `measurements/M-126.md`): ~520 ± 238, tied with directories at the bottom of the order; a read sample says what the 18 held, and that the office entries read `.xlsx` datasets but no `.csv` or `.xls`. No reader is written; §2's row 5 now owes only its reader, the definitional question having been RULED on 2026-09-24 by BOB #32 (a budget is a plan; an audited financial statement reports actuals and is its own FINANCIAL REPORT type). **THAT RULING IS NOW COUNTED (FW-22, 2026-09-24, `measurements/M-143.md`): the census instrument gained a SEVENTH class, `financial_report`, and D-66's own 1,000-document sample was RE-WALKED with it — the budget count is recounted at 16 of 1,000 (~463 ± 225) from D-66's 18, and FINANCIAL REPORT is counted for the first time at 2 of 1,000 (~58 ± 80), the smallest class in the order. §2 rows 5 and 6 carry both. No reader is written for either.** **§6 GAINED THE TIER-3 PATH'S GAP AND ITS CLOSURE ON 2026-09-25 (D-320): the tesseract member refused every `DCTDecode` page — 17 of CPDF-12's 24 image-only pages — because nothing in the isolate decoded a JPEG; a baseline decoder bit-exact with libjpeg now decodes them, upright, and progressive or arithmetic-coded files are refused by name (`measurements/M-163.md`).** **§2's ORDER WAS RE-TAKEN OVER THE WHOLE CORPUS, both halves, on 2026-09-25 (FW-24, `measurements/M-176.md`, n = 2,000) and did not move: staff reports and minutes are still not separated (z = 1.56), nor are directories and budget or dataset (z = 0.22).** as of 2026-09-25.

**Place in the system** · Level 2. Serves construct 5 (the document profile and the extraction substrate — `BIO_Content_Framework_v0_10.md` Part I, approved 2026-07-30) and construct 4 (content, Part II). Depends on `OFFICE-FORMATS.md` (what the office axis extracts and does not), `DOCUMENT-PROFILES.md` (the recogniser engine and the content-type axis), `CONTENT-EXTENT-DESIGN-SPACE.md` §6 (the content row and IC-1's extent grammar this widens) and `OBSERVATION-LOG-DESIGN.md` §4.2 (the content-axis frontier that lists what is worth re-extracting). The AI EXTRACT role's whole design belongs to construct 11's level-1 document, `BIO_Assistant_and_AI_Roles_v0_1.md`; §4 here names only its extraction half. Supersedes nothing; it gives D-319 and D-283 their design and the two GESTURED rows of Part II §15 (tables, images) their extent.

**Incomplete sections** ·
- §2 row 6 — **FINANCIAL REPORT is COUNTED and has no reader**, like rows 1-5 above it. What it also lacks is a bound on what it MISSES: 2 of the 13 documents FW-22 calibrated against — `2024-Single-Audit-Report-PDF.pdf` and `CAFR-2020.pdf`, both named by the publisher as what they are — carry NO TEXT LAYER and are invisible to the instrument, so the count is a count of financial reports that can be READ and the scanned ones are not bounded (FW-22, 2026-09-24, `measurements/M-143.md`). *(The bullet this replaces asked whether the budget type admits audited financial statements; BOB #32 RULED it on 2026-09-24 — it does not — and FW-22 counted the answer.)*
- §3.3 — table recognition on a PDF page is MEASURED NO-GO (CPDF-18, `MEASUREMENTS.md` M-55, 2026-09-18: a geometric step over pdf.js positions, deterministic, reproducible, and blind to a RULED table); no table reader on PDF is designed past the measurement, and what would reopen it — a lattice step over path geometry, measured on a larger hand-read set with ruled tables — is named in M-55 and not designed here.
- §6 — **the DCT decode's cost on the DEPLOYED member is unmeasured** (D-320, 2026-09-25): M-163 gives node and workerd WALL time and a memory ARITHMETIC (~55 MB transient for a 3300x2550 4:2:0 page, released before the 33.7 MB RGBA frame), not the platform's CPU or a refusal-located ceiling as CPDF-15 located the frame's. A progressive, arithmetic-coded or CMYK scan is refused by name and has no tier-3 path; none was in CPDF-12's census, so how many exist is UNMEASURED.
- §6 — the external OCR tier is not designed; DEC-74 is answered (not funded) **[audited 2026-09-17: STILL TRUE, DEC-74 answered and unfunded]** and the section states only the trigger that would reopen it.
- §5.2 — **the per-page rule's first condition is not yet a COMPARISON, and the section must eventually say what it becomes when it is.** Measured by CPDF-20 over 203 pages of 28 real council documents and 15 fixture pages: **tier 2 reported a non-zero undetermined-character count on 0 of 203 and 0 of 15** — it has no vocabulary for them at all — so `u2 < u1` tests whether TIER 1 FLAGGED THE PAGE rather than comparing two decodes. As written the rule awarded tier 2 145 of 203 pages and DEGRADED 23 of them by −692 characters, which are precisely the pages §8's own control requires it to keep; the landed rule adds a one-directional second condition that can only ever WITHHOLD an award, giving 122 pages and 0 degraded. **The residue is that condition (1) is currently equivalent to a flag test, and if tier 2 ever learns to report undetermined characters the rule changes meaning with no edit** — that is the sentence this section owes and has not written. **AND THE FIXTURE CANNOT EXERCISE THE GUARD THROUGH THE WIRE** (REC-98, 2026-09-15): the committed fixture holds exactly one rule-disagreement page and that document does not escalate at all (`needsTier2` reads 1,925 characters against one marker), so the guard is driven IN THE RULE by name against the real bytes while the routing half never hands it to the rule. How many of the 23 the guard saves through the wire is UNMEASURED and unclaimed, because measuring it needs the network.

**Contents**
- [1. The frontier, as Part II measures it](#1-the-frontier-as-part-ii-measures-it)
- [2. Readers beyond three — the rule, and the order](#2-readers-beyond-three-the-rule-and-the-order)
- [3. Tables and images as content](#3-tables-and-images-as-content)
  - [3.1 What "as content" means, and what differs between the two](#31-what-as-content-means-and-what-differs-between-the-two)
  - [3.2 The extent grammar grows two arms and one reference](#32-the-extent-grammar-grows-two-arms-and-one-reference)
  - [3.3 Producers, in order](#33-producers-in-order)
  - [3.4 What a cited image resolves to](#34-what-a-cited-image-resolves-to)
- [4. The AI EXTRACT role — its extraction half](#4-the-ai-extract-role-its-extraction-half)
- [5. Read-time re-extraction, and the per-page rule](#5-read-time-re-extraction-and-the-per-page-rule)
  - [5.1 D-319: the tier-3 seam on the read path — opt-in, recorded, and listed](#51-d-319-the-tier-3-seam-on-the-read-path-opt-in-recorded-and-listed)
  - [5.2 D-283: the per-page rule for two decodes of one layer](#52-d-283-the-per-page-rule-for-two-decodes-of-one-layer)
- [6. The external OCR tier — DEC-74, decided on its narrowed premise](#6-the-external-ocr-tier-dec-74-decided-on-its-narrowed-premise)
- [7. The decomposition](#7-the-decomposition)
- [8. Negative controls the discipline demands](#8-negative-controls-the-discipline-demands)

---

## 1. The frontier, as Part II measures it

Part II §16's closing table names the ABSENT row in one cell: *extraction of tables or images; the AI EXTRACT role; read-time re-extraction reaching tier 3; a per-page rule for tier-2 replacement; an enumeration of the content-axis frontier.* And §15 carries two forms as GESTURED — **tables** and **images** — the only two rows in that table with nothing in the grain column. Everything else is built or specified. This document takes those cells one at a time; the frontier enumeration is `OBSERVATION-LOG-DESIGN.md` §4.2's and is consumed here, not redesigned.

## 2. Readers beyond three — the rule, and the order

Three content types are registered (`docprofile/doctypes/registry.mjs`): `meeting_calendar` and `meeting_agenda`, both measured, and `generic`, the honest fallback that reports change without describing it. The rule that governs a fourth is the registry header's own and it is kept: **a type is written from a page that was actually fetched and read**, because a type invented from what a document probably looks like reassures people about things it has not understood.

The candidates the design already names and none has measured: **meeting minutes**, a **regulation** (an ordinance or resolution's text), a **staff report**, a **staff directory**. The argument for minutes first is structural — an agenda names what a body WILL consider and the minutes what it DID, sharing the meeting identifier that earns a B connection (Part I §8.1), so the agenda→minutes pair is the first progression (Part I §8.2) the record could close from its own readings. But a count decides, not an argument: the order is set by the census of document classes over COFF-6's corpus (§7 item 1), and the count is recorded before the second reader is written.

**THE CENSUS IS TAKEN, and the order is below** (M0-32, 2026-09-14, `MEASUREMENTS.md` M-18; instrument `tools/m032-class-census.py`; 44,076 items, of which 29,626 are text-bearing, and a 600-document fixed-seed body sample of that stratum). **The count returned a PARTIAL order, and that is the result rather than a shortfall in it.** Scaled to the text-bearing stratum: staff reports ~3,012 (±716), minutes ~2,617 (±673), ordinances and resolutions ~1,629 (±540), directories ~395 (±272) — with agendas ~3,456 (±761) counted as a CONTROL, since `meeting_agenda` is already registered and a census that could not find the class the plane already reads would be measuring nothing. Because all five counts come from one sample, the comparison that settles the order is PAIRED rather than a reading of overlapping intervals; it separates minutes from ordinances (z = 2.27) and ordinances from directories (z = 3.95), and it **does NOT separate staff reports from minutes (z = 0.78)**.

**So the order FW-18 writes, and why:**

| # | class | measured | why it sits here |
| --- | --- | --- | --- |
| 1 | **meeting minutes** | ~2,617 (±673) | The count does not separate it from staff reports, so the rule this section already stated — *minutes first unless the count says otherwise* — is not overturned, and the structural argument above decides: minutes close the agenda→minutes progression against the reader the plane already has. |
| 2 | **staff report** | ~3,012 (±716) | Statistically tied with minutes for the largest unregistered class. It is second only because the tie leaves the structural argument standing, not because it is rarer. |
| 3 | **ordinance or resolution** | ~1,629 (±540) | Separated from minutes by the paired comparison. |
| 4 | **staff directory** | ~395 (±272) | Separated from ordinances, and the smallest class by a wide margin. **Written by FW-20 (2026-09-23)** from directories read at TIER 2 — not tier 3, as D-376 had it (`measurements/M-121.md`). |
| 5 | **budget or dataset** | ~520 (±238), MEASURED 2026-09-24 (D-66, `measurements/M-126.md`): 18 of a 1,000-document body sample of the bucket's text-bearing stratum (seed 20260924; Legistar refused); by eye 13 of the 18 plainly the class | Bob named it (D-66; BOB #24, 2026-09-21). NOT separated from directories (paired z = 0.48), below ordinances. Two arms: 8 budgets (money density necessary), 10 datasets (a table of distinct, value-carrying records) — 6 of the 10 are ONE series, the parcel-exemption status CSVs. **The office entries read an `.xlsx` dataset (24 of 25 sampled) and read NO `.csv` or `.xls` at all** (no registry entry; 15 and 6 sampled, `undetermined`), and 10 of the 15 CSVs are datasets. Two of the 8 budgets are audited financial statements, and THAT QUESTION IS NOW ANSWERED. **RULED 2026-09-24 by BOB #32 (D-66's question):** the budget type does NOT admit audited financial statements (an ACFR/CAFR, the Redevelopment Agency's statements). A budget is a plan and a statement reports actuals, so those are a separate FINANCIAL REPORT type, counted apart and read by their own reader. **RECOUNTED 2026-09-24 (FW-22, `measurements/M-143.md`) on D-66's OWN sample re-walked, not a fresh draw: 16 of 1,000, ~463 ± 225, the budget arm falling from 8 documents to 6 and the dataset arm unmoved** — a dataset is a FORM and the ruling is about a subject, so a workbook of audited figures is honestly both and is reported multi-class. The figure is not separated from directories, and the ordering above is unchanged by the recount. No reader is written from this; a reader is its own row. |
| 6 | **financial report** | ~58 (± 80), MEASURED 2026-09-24 (FW-22, `measurements/M-143.md`): 2 of the same 1,000-document body sample (seed 20260924, the bucket half's 28,915 text-bearing items), judged from bodies | **BOB #32 ruled it into existence on 2026-09-24** out of D-66's row above: a budget is a PLAN for money not yet spent, an audited statement REPORTS what a closed period did, so the two are different types. The recogniser is money density joined by one of — the document naming itself an ACFR/CAFR or audited statements, an auditor's opinion in the body, or the GAAP statement spine together with A PERIOD THAT HAS ENDED; the last conjunct is the plan/actuals line itself and is what keeps a budget book's fund-balance schedules out. The paired comparison SEPARATES it from budget_dataset (z = 3.32), so it is the smallest class this census has found and the order puts it last; and the name layer and the body layer agree on ZERO documents here (body 2, name 1, both 0), which is the sharpest instance yet of D-66's rule that a filename is not evidence of a document's kind. No reader is written from this; a reader is its own row. |

**THE ORDER WAS RE-TAKEN OVER THE WHOLE CORPUS AND DID NOT MOVE** (FW-24, 2026-09-25, `measurements/M-176.md`; the same instrument, classification path `161d2ff9…`). The Legistar half answered again, so the census was run over both halves: a fresh 2,000-document draw of all 29,607 text-bearing items (seed 20260924). Scaled figures: agenda (control) ~3,982 ± 443, staff reports ~3,198 ± 403, minutes ~2,768 ± 378, ordinances and resolutions ~1,850 ± 314, directories ~607 ± 184, budget or dataset ~577 ± 179, financial reports ~89 ± 71. The paired comparisons reproduce every separation above: minutes > ordinances z = 3.72, ordinances > directories z = 6.67, budget > financial report z = 4.95. They also reproduce both ties: **staff reports > minutes z = 1.56, NOT separated**, so minutes stay first on the structural argument; directories > budget z = 0.22. The table is therefore not reordered. The Legistar half's 48 sampled attachments hold no minutes and no directories, and 21 of them are ordinances or resolutions, which is why that class rose the most.

Two facts from the census bear on how item 2 is BUILT, not merely on its order. **A filename is not evidence of a document's kind**: the same census run over key paths alone recovers only 13% of the staff reports its bodies find and 0% of the directories, so a recogniser that reads names rather than pages would rank this table wrongly and would have ranked staff reports fourth. And **52 of 600 sampled documents satisfied more than one class at once** — Oakland publishes agenda packets that genuinely contain an agenda, its staff reports and its draft resolutions — so a content type that assumes one document is one kind will mis-describe roughly one document in twelve. Neither fact is a reason to reorder the table; both are reasons the readers this order produces must admit a document being more than one thing.

Each new reader emits references as the two existing ones do, and — once FW-17 lands reading position on I2 — with WHERE the reference was read, so that the connections it earns are content-grain from the start rather than document-grain to be narrowed later (Bob's 5.4, second pass: specificity is worked for).

## 3. Tables and images as content

### 3.1 What "as content" means, and what differs between the two

A thing is content when an edge can point at it and it carries an extent and an extraction chain (Part II §14.2). For text that is settled. Tables and images differ from text and from each other in one respect each, and the differences are the design:

- **A table's TEXT keeps its container's chain; a table's STRUCTURE is either the container's own or a derivation.** In an office container the cells ARE the structure — the reader walks them from the XML — so a table there is a range of cells under the `layer` step with the document's cap. On a PDF page a table is pixels arranged as rows and columns, and recovering the structure is a machine step: `table(engine)`, a derivation that weakens like every other and is calibrated like OCR. The cells' text keeps the page's chain; the structure carries its own step.
- **An image cited AS ITSELF is bytes, not text.** A map, a signature, a photograph, a chart cited for what it shows rather than for words read off it has NO extraction chain: its fidelity is the capture's (the bytes are the record's own, grade A-able on the provenance chain), and any TEXT read off it is a separate content — OCR, tier 3, cap C — with its own extent over the same rectangle. So an image row carries no chain, and that null must not be read as *undetermined*: IC-83's rule that an extent with no chain is refused gains a stated exception, expressed as a column rather than an overloaded NULL (D-129's rule that an absence is never read as a value) — `cited_as: text | bytes`. A `bytes` row has no chain and no derivation cap; a `text` row has both or states them undetermined.

### 3.2 The extent grammar grows two arms and one reference

IC-1's union (`pdf-page` · `sheet-cell` · `slide-shape` · `doc-para` · `dom`) is I2's, FRAMEWORK's; the content table's `extent_kind` admits the same set (I5, RECORD's). Both grow together, in one IC per interface:

| arm | fields | what it addresses | producer |
| --- | --- | --- | --- |
| `sheet-range` | `{sheet, range}` — A1-style, a whole sheet when the range is the sheet's dimension | a table in a workbook, or a sheet as a unit (`CONTENT-SEARCH-DESIGN.md` §4.1's workbook unit) | the XLSX/ODS entries already know the sheet dimensions (the out-of-range refusal REC-85 builds uses them) |
| `doc-table` | `{table, cell?}` — the table's ordinal in the document, optionally one cell | a table in a word-processing document | the DOCX/ODT entries walk tables already; today they flatten them into paragraphs |
| `image` (a reference, not an arm of the union) | `{part}` (the content hash of the embedded media part, for a container) or `{page, rect}` (for a PDF page, the same fields as `pdf-page`) plus `mime` | a figure, map, signature or photograph as itself | office entries enumerate media parts; the PDF structure op reports image objects with their rectangles |

`covers` per new arm follows REC-85's shape: a `sheet-range` outside the sheet's dimensions, a `doc-table` ordinal past the table count, an `image` part not in the container, each refused from the container's own extent, by name.

**`{part}` is a member of a CONTAINER's own bytes, and nothing else (2026-09-21, D-55's design, `CLIENT-RENDERED.md` "DESIGNED 2026-09-21").** An image served beside a web page is not in the page's bytes. Nothing in §3.4's resolution covers it, because the capture's hash does not reach a subresource's bytes, and those bytes may have been REUSED from an earlier fetch (CAP-4). When it is evidence it is its own document: acquired at its address, or registered from the held bytes with its `site_asset_refs` row as its hop's evidence. So a `{part}` on a capture that is not an office container is refused by name. An office capture acquired before its image list was recorded keeps its undetermined admission, stated. **As built today the checker admits any 64-hex `{part}` whenever no image list is held, which includes every HTML capture; D-440 is that fence.** **BUILT 2026-09-23 (D-440), the sentence above kept as the record of what it closed:** `contentContextFor` answers whether the capture is an office container, three-valued, from the capture's FORMAT (the provenance profile's `format`, projected at promote onto `readings.capture_format`, else the reading's own `text_container`) and the format registry's own property that the entry walks parts. A `{part}` on a capture held as any other format is refused `CONTENT_EXTENT_NOT_A_CONTAINER` (C-45.11), naming the format and saying the image is its own document to acquire and cite whole. An office capture with no image list, and a capture whose format the record does not hold, are admitted and the answer carries `undetermined` naming which. The residue: a capture promoted before the projection whose reading names no container is in the second population until its bundle is next promoted.

### 3.3 Producers, in order

1. **Office (COFF):** `doc-table` from the DOCX/ODT table walk; `sheet-range` from the sheet dimensions and, where a defined table or a named range exists in the workbook, from it; `image` from the media parts, each hashed. No engine, no measurement owed; the evidence is in the container.
   **NAMED UNITS BUILT 2026-09-25 (D-415).** Beside each sheet's whole-sheet unit, `text()` emits `rangeUnits` — an `.xlsx` defined name (`workbook.xml`) and table part (`xl/tables/`, reached through the sheet's own rels), an `.ods` named range and database range — each a `sheet-range` unit through the one builder, carrying the name its author gave it, its scope, and the file's own hidden flag. A name that is not ONE rectangle on ONE sheet of that workbook — several areas, a formula or constant, `#REF!`, a whole row or column, another workbook, a sheet the workbook lacks, past the `.xlsx` grid — is emitted in `rangeUnitsSkipped` with its reason, never widened. What is NOT built: no op persists or indexes these units (nor the whole-sheet unit — `store.mjs` states it), so a caller reaches them only by citing the address, which C-45.1 bounds; driven in `bio-plane/test/fw19-extent-arms.test.mjs`.
2. **PDF images (CONTENT-PDF):** `image` as `{page, rect}` from the structure op's image objects; the citation resolves to the capture's bytes plus the rectangle, and a crop for display is a derived rendition that says it is derived (the render companion's rule).
   **BOUNDED 2026-09-23 (D-420).** The placements are the record's own extent for the `{page, rect}` form: persisted at acquire, and a citation naming a rectangle no placement equals is refused from them by name (§3.2's `covers` rule, on the page form); with no list held the citation is admitted and the absence stated, never refused.
   **BUILT 2026-09-18 (CPDF-18).** The rectangle is the image's PAINTED extent in default user space (not what survives clipping; a rotated placement gives its bounding box and says `axis_aligned: false`), and the crop is the image the file stores at that rectangle, not a render of the page region — no rasteriser exists (CPDF-12). A crop asked with the rect dropped is refused `RECT_REQUIRED` by name.
3. **PDF tables — measure first.** **MEASURED 2026-09-18: NO-GO** (CPDF-18, `MEASUREMENTS.md` M-55) — the paragraph below is kept as the design the measurement answered. No table-recognition engine had been measured on the runtime. The measurement (§7 item 4) is GO/NO-GO on the same discipline CPDF-14 used for vision OCR: does the STRUCTURE reproduce across runs on identical bytes (the anchor rule), and what does the step cost. NO-GO until measured; a `table(engine)` step is minted by the measurement, not by this document. Until then a table on a PDF page is a `pdf-page` rectangle whose text is the page's — content, honestly, without the structure claim.

### 3.4 What a cited image resolves to

The capture's bytes and the extent, verified as the capture is. The viewer shows the crop; the crop is not the evidence. Nothing here touches the provenance chain, which is why an image row can reach A: it claims exactly what the capture claims and no more.

**BUILT 2026-09-25 (D-419, C-99).** `op=contentcrop&id=<content_id>` is a read: it resolves the row through `op=content`'s fixed-key, viewer-stamped store read (so a row the caller cannot see answers exactly as an absent one, and a predicate is refused FIXED_KEY_ONLY), sends the row's capture sha, namespace and extent to the PDF member's `POST /crop`, and hands back `cropImage`'s answer — `derived: true`, `rendition: "crop"`, the bytes as base64 — only when the sha256 of the bytes the member cropped equals the row's capture. "Verified as the capture is" is that check. Each way it cannot is refused by name: no PDF member bound, a row that is not an image on a PDF page (a document, page or passage row, or a container `{part}`), an extent the member cannot crop (its own reason beside it: no rectangle, no image there, inline, undecodable, over the envelope), a member that gives no answer, and a crop of other bytes. It writes nothing. What the crop IS stays `imagecrop.mjs`'s: the image as the file stores it, marks drawn over it not composited and a rotated or flipped placement not applied.

**RENDERED 2026-09-25 (UI-120).** "The viewer shows the crop" has its viewer: the inquiry page's leg row (`basisLegRow` in `civicos-ui/app.html`, the one surface that shows a member a cited content row) offers *Show the cited image* on a leg whose standing is an `image` extent carrying a rectangle, and on no other — a passage, a page, a whole document, a container `{part}` and a page-form image with no rectangle get no control, since the plane could only refuse them. It is ASKED when used and never on load, with a credential or without one. What it draws is the plane's answer byte for byte (PNG or JPEG only; any other media type is stated and not drawn), labelled by the answer's own `why` — DERIVED, the evidence is the capture plus the extent — beside the capture the plane checked it against; a C-99 refusal renders in the catalogue's translation and code, with the PDF member's own reason beside C-99.3 as its translation promises. Where the answer does not say `upright: true`, the orientation is drawn as UNDETERMINED. **What is not settled, stated rather than filled:** the answer carries `upright` as true-or-null with no sentence, so that one line of the rendering is the surface's own words rather than the record's.

## 4. The AI EXTRACT role — its extraction half

DEC-24 names EXTRACT as the role that makes everything else addressable; Bob's 5.7 rules that the assistant may mark passages as citable on its own. The role's whole design — which credential, which surface, which loop, how its work is shown — is construct 11's and lives in `BIO_Assistant_and_AI_Roles_v0_1.md`, and **its §7.3 answered D-358 on 2026-09-14: the role runs in DEC-62's RUN, not on the pilot's credential, whose exclusion is CORRECTED rather than lifted (the pilot is read-only and writes nothing). Part II §14.5's "excluded from the pilot by name, with no item" is superseded by that answer and by SK-8's landing the same day.** Mints are a bound on the run and an uncited machine-minted row is a proposal rather than coverage — both §7.3's, neither this section's. What belongs here is unchanged and is what the role may PRODUCE on the content axis and under which chain:

| production | what it is | its chain and label |
| --- | --- | --- |
| a proposed citable passage (SK-7, rowed) | a content row over an existing extent and the CURRENT chain — no new text | `minted_by` a machine credential; the row's chain is the capture's; labelled everywhere it is shown; never attested by it |
| an on-point candidate for a document-grain edge (Bob's 5.4 second pass; REC-86's candidate list) | the same row shape, proposed against a specific edge | as above; the member's choice is the authored act (NARROW) |
| a proposed reading — entities and facts the registered readers did not find (SK-8, **BUILT 2026-09-14**) | a reading with an `ai(function)` step in its chain — **the designed step's FIRST producer**, `op=extractpropose` inside DEC-62's run; the step's performer travels in I2's existing `engine` field, which is what `checkChain` already refuses an unnamed `ai` step for | a derivation that weakens: the reading's basis is `ai(function, version)`, appended through `textchain`'s `appendStep` so rule 2 is enforced by the module that owns it; its grade COMPUTED from what it names (an identifier in the text earns B as today; a name C), never A, never offered by the caller; labelled on every surface; held in its own table so it is never counted as extraction coverage |
| a proposed table structure on a PDF page (§3.3 once GO) | a `table(engine)` step | a derivation; calibrated like OCR |
| cleaned or normalised text | a derivation step over the machine's output | weakens; never raises a cap (Part II §14.2); NOT a transcription — a person supplies text, a machine only transforms it |

What the role may never do is unchanged and enforced where it always was: it never attests (C-35.10), never touches the provenance chain, never chooses the question, and its rows are part of a finding only when a member cites them.

## 5. Read-time re-extraction, and the per-page rule

### 5.1 D-319: the tier-3 seam on the read path — opt-in, recorded, and listed

The gap, as CPDF-10 measured and asserted it: `op=acquire` reaches tier 3; the read-time structure op stops at tier 2, so an instance that installs the OCR member later has no route to the text of what it captured before, short of re-acquiring. The consumer code all exists and is driven; what is missing is the call site — and one decision, which D-319 correctly refused to make as a patch: a read that grows a ~10 s engine call must not do so unasked.

**Decided:** the seam is lifted into the read op behind the same `needsTier3`/`tier3Pages` predicates and the same page-wise merge, **opt-in by an explicit flag on the request**, never automatic, and:

- it produces a NEW chain for the capture, so the capture's text units are replaced (`CONTENT-SEARCH-DESIGN.md` §4.1) and its content rows go `stale` by REC-82's mechanism — a member who cited page 14 under `layer` sees the flag, and nothing moves under them (Bob's 5.8);
- it writes a content-level observation with `authority_kind = extract` and the member as `actor` (`OBSERVATION-LOG-DESIGN.md` §4.2), so *this document was re-read on this date at this member's request under this engine* is a fact the record holds;
- the LIST of what is worth re-reading — every capture whose latest content-level state is below what the fleet can now do (`tier3_candidate`; a chain older than its engine's calibration) — is the content-axis frontier read, so re-extraction is a choice made from a list, informed once at the act (DEC-69), and never a silent sweep. A bulk re-read is that list worked one document at a time by a member's act, each row-recorded.

The cost is stated at the affordance (≈10 s per image-only page on the deployed member, CPDF-10's measurement) and the honest branch stays: an instance with no OCR member bound refuses the flag by name rather than pretending.

**AS BUILT, 2026-09-18 (CPDF-19, IC-126).** The flag is `ocr=1` on `op=pdfstructure`. Without it the answer is byte-identical to the pre-item read — a digest taken by running the pre-item build is pinned in `reextract.test.mjs`. With it: the seam, the page-wise merge, the chain rule and the reading rule are the acquire path's own, because each is now ONE function both call sites use (`tier3Extend`, `textUnitsFor`, `readingFromWire` in `index.mjs`) — so a capture re-read here records exactly what it would have recorded had the member been installed at capture. The new reading goes through promote's own per-capture writer (`Store.#writeOneReading`, via the DO path `reextract`), which is what replaces the text units, marks the old chain's content rows stale and writes the content-level observation with the member as `actor`; the capture then leaves the frontier's candidate list, which the answer points at. Five refusals, C-51.1–.5, each before a byte is read or an engine called: a malformed flag, an agent credential, a session without `contribute`, **no OCR member bound**, and a capture the record holds no visible reading of. A document with no image-only page is answered `performed: false` with the reason and the engine is not called. **Two departures from the text above, each recorded in Incomplete sections:** the re-read mints no bundle version (so the bundle's `data/provenance.json` keeps the acquire-time reading, guarded against a revision undoing the re-read), and §8's *exactly one observation* is three rows, because the writer it goes through also writes the index row and the reader-run row. **Two defects found and closed while lifting the seam:** D-417 (the calibration join never fired) and D-418 (a transcribed document was filed as still wanting OCR, so it could never leave the candidate list this section says a re-read is chosen from).

### 5.2 D-283: the per-page rule for two decodes of one layer

Tier 1 and tier 2 are both `layer` derivations of the same source under the same null cap, so no cap is overclaimed when one replaces the other; what is unbounded is TEXT LOSS, because the escalation predicate is document-level and the replacement is wholesale — a document where tier 1 read page 0 well and failed page 1 escalates whole, and if tier 2 recovers page 1 and does worse on page 0 the plane keeps the worse page silently.

**Decided, AND CORRECTED 2026-09-14 BY THE MEASUREMENT THIS SECTION ASKED FOR — the original rule is kept below because what it got wrong is the useful part.** As written: *the decode with fewer undetermined characters on that page wins; a tie keeps tier 1*. **CPDF-20 measured it over 203 census pages and 15 fixture pages and found that TIER 2 REPORTS ZERO UNDETERMINED CHARACTERS ON EVERY PAGE** — it has no vocabulary for them — so the comparison performs NO COMPARISON: it reduces to *did tier 1 flag this page*, awards tier 2 **145 of 203** pages, and on **23** of those tier 1 had decoded MORE text (−692 characters). Those 23 are exactly the page §8's own control requires the rule to KEEP, so **§5.2 as written fails §8's control** — the arm that ships it unmodified fails by name on a real page, and that arm is in the suite. **WHY THE RULE READ AS ALREADY-CHECKED WHEN IT WAS WRITTEN, which is the part worth carrying: BOTH PRODUCERS PUBLISH A FIELD CALLED `undetermined` AND THEY COUNT DIFFERENT THINGS.** Raised by BOB #11 on reading CPDF-20's falsification, and CONFIRMED BY CONDUCT AGAINST BOTH SOURCES rather than accepted — a falsification deserves the scrutiny a claim gets, and the code is sharper than either of us said. Both sides push markers into an array named `undetermined`, so the name matches at a glance. What differs is the QUANTITY inside: the plane's reader (`pdfstructure.mjs`) emits one marker PER RUN carrying `count` = the undetermined CHARACTERS in that run (`bytes.length`, or `bytes.length / width`), while `pdf-worker` emits PAGE-LEVEL markers for pages it could not produce at all (`over_envelope`, `tier2_extraction_error`) whose `counts.undetermined` is `undetermined.length` — the number of MARKERS — **and whose markers carry `count: 0` explicitly.** So one side counts characters inside text it decoded and the other counts pages it failed to decode: same name, different quantity, different grain. **This is `CLAUDE.md`'s own rule arriving in a design rather than in a suite** — *an equality or an outcome that costs nothing to produce is not evidence* — and its design-time form does not look like its test-time form: in a suite it catches a check that cannot fail, in a design it catches a rule whose two sides cannot disagree. **A same-named field is the likeliest place it fails, because the name is what makes the comparison look already checked.** **THE SHIPPED RULE keeps this section's award axis and adds a ONE-DIRECTIONAL GUARD that can only WITHHOLD:** character count never PROMOTES a page, it only refuses to demote one — which stays inside D-283's own warning that a character count is not a quality measure. Measured result: **122 pages moved, 0 degraded, +186,242 characters, nothing left behind.** The original sentence stands here as the thing that was falsified; the guard is the correction. A tie still keeps tier 1 (fewer steps, the same cap, the same producer marker carried forward); the mixed-document chain the plane already composes (Part II §16, "The chain, composed as the path walks") records which tier produced each page, so the document's chain is honest about being a merge. **Measured first, not shipped first:** D-283 is stated as unmeasured because no fixture of partially-decodable PDFs exists; the item builds that fixture from the census sample (documents whose tier-1 reading carried undetermined pages), measures both decodes page-wise, and lands the rule behind the measurement with its negative control (a page tier 1 read well and tier 2 read worse is kept from tier 1).

**AND THE STATEMENT HAS TO SURVIVE §5.1's MERGE, WHICH IS A FACT ABOUT THE ORDER OF TWO MERGES RATHER THAN ABOUT EITHER OF THEM** (D-372, raised by REC-98 as its own residue and closed 2026-09-16 by REC-102). A document that escalates per page under this section and THEN re-extracts to tier 3 under §5.1 meets a SECOND merge, and that merge composes the chain's layer part too. Composing it from a single document-level tier — whatever tier the document happens to be wired at — rebuilds exactly the sentence this section exists to replace, one merge later: every page the tier-2 merge deliberately KEPT at tier 1 recorded as tier 2. **So the rule this section owes is not "record the winner per page at the tier-2 merge"; it is that the per-page statement is what the LAST merge composes FROM.** The tier-3 merge takes its layer parts from `perPageTier` — three parts rather than two when both merges fired — and falls back to the single wired tier, in the same position and byte-identically, for a document that reached only one. **It was never a regression, and that is measured rather than assumed:** before the per-page rule the escalation assigned tier 2 wholesale, so those pages read tier 2 anyway. It is stated here as a property of the COMPOSITION rather than as a defect in the rule, because that is what it is, and because the next merge added below either inherits the property or repeats the mistake.

**THE CLASS IS REACHABLE AND IS NOW DRIVEN, WHICH D-372 COULD NOT PROMISE.** Reaching both merges needs a `no_text_layer` marker to SURVIVE the tier-2 merge, and a page tier 2 WINS cannot supply one — a page with no text never satisfies the shipped rule's second condition — so the marker must come from a page tier 1 KEPT: a text-layer document carrying BOTH an unmappable-font page AND a fontless image page. CPDF-20's census found exactly one such document in fifty and rightly did not commit it, because it is a private individual's resume. **So the fixture is SYNTHESISED rather than captured** — the three page shapes `pdfstructure.mjs` actually reads, in the structure CPDF-9 measured, in one file — and the trade is stated rather than buried: a synthesised corpus says NOTHING about how common the class is (that remains CPDF-20's census, unchanged), and it buys the reachability question, which is the one a record-honesty defect turns on. No personal record is taken to answer it.

## 6. The external OCR tier — DEC-74, decided on its narrowed premise

DEC-74 (open, for Bob, raised 2026-09-10) asked whether to fund an external OCR tier, direct the tesseract runtime probe first, or accept that image-only pages have no tier-3 path. Since it was raised, CPDF-10 delivered tesseract-wasm as the `ocr-worker` fleet member, GO on the deployed runtime, connected and taken on the project's own instance at 0.58.0 with a chain of `pixels → ocr(tesseract-wasm 0.11.0)` capped at C (Part II §16). So the recommendation the item carried — run the probe before funding anything — has been taken, and its second and third options are closed by measurement. What remained was narrower than the entry said: whether to fund a tier ABOVE C for the image-only class (13 of 1,458 censused pages, in two documents). **DECIDED 2026-09-14 by BOB #11 at Bob's delegation: not funded.** The tesseract member at cap C is the tier-3 path; the external tier is reconsidered only when an image-only document is LOAD-BEARING in a real case and C is below that project's bar — a funding request with the document and the bar attached, brought to Bob then, since DEC-35's word still governs spending. Nothing is designed or rowed.

**THE TIER-3 PATH HAD A HOLE UNDER IT, AND IT WAS THE LARGER SHARE OF ITS CLASS (D-320, stated and closed 2026-09-25).** The decision above rests on "the tesseract member at cap C is the tier-3 path" — true only for pages whose pixels the member could READ. CPDF-12's renderer answers a `DCTDecode` page by handing on the publisher's own JPEG (`passthrough-dct`, the strongest provenance position), and nothing in workerd decodes a JPEG — no canvas, no `createImageBitmap` — so `ocr-worker`'s `transcribe.mjs` refused every such page `PIXELS_UNREADABLE`. **That was 17 of CPDF-12's 24 image-only pages** (`MEASUREMENTS.md`, 2026-08-08: 14 at `/Rotate 0`, 3 at `/Rotate 270`); the path the decision names reached the other 7, the CCITT ones. D-320 builds the missing piece in the member rather than moving it out: `pdf-worker/src/dctdecode.mjs`, a baseline JPEG decoder whose every integer step is libjpeg's (ISLOW IDCT, fancy upsampling, the fixed-point YCbCr tables, the colour-transform rule), so the pixels it produces are Pillow/libjpeg-turbo's to the bit and `pixels_sha256` stays a digest an independent decoder reproduces. The renderer takes it only when asked (`decodeDct`, route `decoded-dct`), the page's `/Rotate` is applied as the samples are written, and the member asks. **The pass-through route is unchanged and remains the default**: a decoded page is a transform of ours and its route says so. What stays unread, by name: a progressive, arithmetic-coded, lossless, hierarchical, 12-bit or CMYK JPEG (`UNSUPPORTED_JPEG_PROCESS` / `UNSUPPORTED_SAMPLES`), and one whose data ends early (`TRUNCATED_IMAGE_DATA`) — a baseline parse of a progressive file is a plausible smear, which an OCR engine turns into fluent invention. Measured in `measurements/M-163.md`; the deployed cost is in Incomplete sections.

## 7. The decomposition

Seven items, in dependency order, handed through the BOB INBOX (CONDUCT mints the ids). Milestones: M2 for readers and the tier rules, M4 for the extent arms, M5 for the frontier's consumers.

| # | owner | item | depends on | interface | design |
| --- | --- | --- | --- | --- | --- |
| 1 | M0 / FRAMEWORK | **the census of document classes** over COFF-6's corpus — agendas, minutes, staff reports, ordinances and resolutions, directories, other — by count, recorded in `MEASUREMENTS.md`; sets item 2's order | — | — | §2 |
| 2 | FRAMEWORK | **one content type per measured class**, in the census order (minutes first unless the count says otherwise), each written from a fetched and read page, each emitting references with position once FW-17 lands | item 1; FW-17 for position | none unless a reader changes the I2 shape | §2 |
| 3 | FRAMEWORK + COFF + RECORD | **the two arms and the image reference** — IC-1 gains `sheet-range` and `doc-table` and the `image` reference (I2, additive); the office entries emit them; the content table admits them with `covers` per arm and the `cited_as` column (I5, additive) | REC-85 (the three-arm `covers` this extends) | I2 (an IC; FRAMEWORK dormant — CONDUCT answers-for in writing or activates), I5 (an IC) | §3.1–3.3 |
| 4 | CONTENT-PDF | **PDF images as content, and the table measurement** — `image` as `{page, rect}` from the structure op; the crop as a derived rendition; and the GO/NO-GO measurement of a table-recognition step on the runtime (reproducibility of the structure across runs, cost) recorded before any table reader is rowed | item 3 | — | §3.3, §3.4 |
| 5 | CONTENT-PDF + RECORD | **D-319: read-time re-extraction to tier 3, opt-in** — the seam on the read op behind the flag and the existing predicates; the new chain replacing the text units and staling the content rows; the content-level observation; the frontier read as the candidate list; refused by name with no member bound; the negative control | `OBSERVATION-LOG-DESIGN.md` item 2 (the writer and the read); `CONTENT-SEARCH-DESIGN.md` item 4 (the units it replaces — ships without them if not yet landed, replacing nothing and saying so) | I3 (the flag, additive, an IC) | §5.1 |
| 6 | CONTENT-PDF | **D-283: the fixture, the measurement and the per-page rule** — a fixture of partially-decodable PDFs from the census sample; both decodes measured page-wise; the rule landed behind the measurement with the chain recording the winner per page | — | — | §5.2 |
| 7 | SKILL + RECORD | **AI-proposed readings** — the `ai(function)` step emitted for the first time, by the assistant's EXTRACT on a member's objective; graded by what it names; labelled; after the assistant's level-1 document places the role | SK-7; `BIO_Assistant_and_AI_Roles_v0_1.md` landed | I2 (the step is DESIGNED; its first producer confirms at IC-2) | §4 |

## 8. Negative controls the discipline demands

- a `sheet-range` past the sheet's dimensions, a `doc-table` ordinal past the count, an `image` part absent from the container — each refused by name from the container's own extent;
- an `image` row with `cited_as = bytes` carries no chain and no cap and is not refused; the same row with `cited_as = text` and no chain is refused (the two nulls are different facts);
- a table on a PDF page before the engine is measured is a `pdf-page` rectangle and no `table(engine)` step exists in any chain;
- re-extraction without the flag does not run the engine (the read's timing is the control); with the flag and no member bound it is refused by name; with both it writes exactly one observation and stales exactly the capture's content rows;
- the per-page rule keeps a page tier 1 decoded well when tier 2 decoded it worse; invert the comparison and the fixture's control page flips;
- a proposed reading carries an `ai(function, version)` step and never a grade above B; strip the step and the row is refused;
- the census count is recorded before the second reader's row is opened (plancheck's §4.7 arm: the row names §2 and the measurement).
