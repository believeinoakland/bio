# Area claims

The register of who is working where, so no two sessions edit the same paths at
once. Established 2026-07-31 as step 2 of the parallel-development move
(`PARALLELISM.md`, "What to build first"). A text file and a rule; no tooling.

## The rules, in short

- **Claim your area before you edit its paths.** Append a `## CLAIM` block below.
  A claim keeps other sessions out of the paths it names; it is not a courtesy.
- **A session that needs work inside another area's claim DELEGATES.** Append a
  `## DELEGATION` block naming what you need and why, and continue with your own
  work. The owning area picks it up. Do not edit a claimed path quietly.
- **Release a claim explicitly**, by setting `released:` to the date. An
  unreleased claim older than its expected scope is stale, and `ARCH` may
  reassign it; silence does not hold ground forever.
- **Unclaimed paths are nobody's** — a collision risk, not a licence. Claim
  before editing even briefly.
- **Append only.** States are added, never edited in place, so the history of who
  held what is readable the same way the record's own history is. The one field
  that changes on an existing block is `released:`.

The format is the one `PARALLELISM.md` fixes:

```
## CLAIM <date> <AREA>
session: <name>
opened: <ISO8601>
paths: <comma-separated paths this claim reserves>
interfaces consumed: <ID(s), or none>
interfaces owned: <ID(s), or none>
expected: <the work this claim covers>
released: <date, or blank while live>
```

---

## CLAIM 2026-07-31 CAPTURE
session: capture-bootstrap-1
opened: 2026-07-31T17:57:07Z
paths: bio-plane/src/subresources.mjs, bio-plane/src/cpu.mjs, bio-plane/src/cdx.mjs, the capture/link/task/reachability tables in bio-plane/src/schema.mjs, the capture ops in bio-plane/src/index.mjs, the link/capture/task/reachability functions in bio-plane/src/store.mjs, the capture and authority checks in bio-plane/checks/bio-checks.mjs, bio-plane/test/subresources.test.mjs, bio-plane/test/governor.test.mjs, bio-plane/test/inbox.test.mjs, bio-plane/test/reachability.test.mjs, bio-plane/test/cdx.test.mjs
interfaces consumed: none
interfaces owned: I1 (bytes → content)
expected: CAPTURE's standing responsibility (fetch, governor, subresources, links, reachability, archive fallback). This turn stood up the parallel-development scaffolding under the ARCH role from the main checkout — CLAIMS.md, INTERFACES.md with I1, the UI/FRAMEWORK kickoff refresh, and the biosmoke-ui instance — none of which is another area's code.
released: 2026-07-31 — RELEASED as STALE by CONDUCT per PARALLELISM.md's stale-claim rule (session capture-bootstrap-1 long gone, never released, over-reserved most of CAPTURE's paths and collided with RECORD's store-core work and a future CAP-3). Paths now belong to their areas: the store core/scheduler to RECORD, the capture/link/task/reachability functions to CAPTURE's live per-item claims.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-1
opened: 2026-07-31T20:30:00Z
paths: bio-plane/test/hygiene.test.mjs, the purge table list in bio-plane/src/store.mjs
interfaces consumed: none
interfaces owned: none
expected: D-113 as a CLASS. A hygiene check asserting every CREATE TABLE in schema.mjs is either named in op=purge's whole-store table list or in a small explicit exemption allowlist (each with a one-line reason), so a forgotten derived table fails loudly. Also corrects derived capture tables currently missing from the whole-store purge. No deploy this turn; ARCH integrates.
released: 2026-07-31 — landed on main by ARCH (integration turn), commit 6eb2b42. Full battery green; the negative control was re-run by ARCH before landing (throwaway table -> the check fails naming it).

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-1
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/pdfstructure.mjs, bio-plane/test/pdfstructure.test.mjs, the pdfstructure entries in bio-plane/package.json (the test:pdfstructure script and the additive link in the test chain)
interfaces consumed: I1 (bytes → content)
interfaces owned: none yet; expects to propose I2 (structure → framework) producer-side from what the extractor emits
expected: D-91 phase 1 — the PDF outbound-link structure graph (anchor / intra / deferred / refused / undetermined), byte-identical wrappers to subresources.mjs. No text extraction (unpdf) this turn.
released: 2026-07-31 — phase 1 landed on main by ARCH (integration turn), commit 0b1c6ce. Phase 2 (text via unpdf, measured against the 3MB bundle and CPU ceilings) remains; the op wiring needed to live-verify is the DELEGATION below.

## DELEGATION 2026-07-31 CONTENT-PDF → CAPTURE
from: content-pdf-agent-1 (recorded by ARCH at integration)
need: wire op=pdfstructure into the `if (op === …)` dispatch in bio-plane/src/index.mjs — a few additive lines that read a capture_sha through the existing op=capture byte path and call extractPdfStructure(bytes) from bio-plane/src/pdfstructure.mjs. index.mjs is CAPTURE's; this is the delegation the CONTENT-PDF kickoff anticipated, not a quiet edit.
why: the extractor is complete and unit-tested against fixtures but cannot be live-verified against real captured PDFs until an op exposes it. If op-wiring delegation recurs across content areas, that is the trigger to promote op-contract ownership out of CAPTURE (the I3 move PARALLELISM.md anticipates).
status: fulfilled 2026-07-31 — op=pdfstructure landed 2ab62f4 (capture-agent-2 / QUEUE.md CAP-1). Live-verify (QUEUE.md CPDF-3) still awaits a DIST deploy.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-2
opened: 2026-07-31T00:00:00Z
paths: the pdfstructure op in bio-plane/src/index.mjs (the OPS spec entry, the op=pdfstructure handler, the shared captureKey helper, and the extractPdfStructure import), bio-plane/test/pdfstructure-op.test.mjs
interfaces consumed: I1 (bytes → content), I2 (structure, provisional — consumed as the extractor's output shape)
interfaces owned: none
expected: QUEUE.md CAP-1 — fulfil the CONTENT-PDF→CAPTURE delegation above. Add op=pdfstructure (GET, read-only, same auth/caps posture as op=capture GET) that reads a capture sha through the exact R2 path op=capture uses and returns extractPdfStructure(bytes) as JSON. Test through the op with a fixture PDF (link annotation) plus a non-PDF negative control. Does NOT touch CONTENT-PDF's src/pdfstructure.mjs or test/pdfstructure.test.mjs. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — CAP-1 landed on main by CONDUCT, commit 2ab62f4. Full battery green; the captureKey extraction verified a no-op (capture behaviour unchanged), the op is read-only and mirrors op=capture GET auth (no new permission).

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-2
opened: 2026-07-31T00:00:00Z
paths: the CPDF-1 measurement section in docs/development/MEASUREMENTS.md (append-only), bio-plane/test/unpdf-measure-probe.mjs
interfaces consumed: none (a measurement)
interfaces owned: none
expected: D-91 phase-2 go/no-go MEASUREMENT (CPDF-1). Bundle `unpdf` through the plane's esbuild target in a scratch temp dir (NOT added to shipped deps/bundle) and measure bundled size vs the 3MB Free-worker limit; time extraction on a real Oakland agenda as a labelled node proxy (not Worker CPU); record both with date and instrument and recommend go/no-go. Commits no text extractor and changes no shipped dependency or bundle. Verdict: GO — bundle fits with 2.29 MB gzip headroom; node-proxy cost modest. CPDF-2 unblocked.
released: 2026-07-31 — measurement landed on branch content-pdf/phase2-measure; CONDUCT integrates. Worker-CPU-vs-ceiling confirmation via a deployed probe is the remaining gated follow-on (rides on the op=pdfstructure delegation above).

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-3
opened: 2026-07-31T00:00:00Z
paths: the task-queue drain alarm in bio-plane/src/store.mjs (the alarm()/onAlarm handler, #rearmSchedule, #armDrain, #drainDelayMs, the TASK_DRAIN_* constants, and the arming added to taskEnqueue), bio-plane/test/task-drain-alarm.test.mjs, the TASK_DRAIN_DELAY_MS binding added to bio-plane/test/inbox.test.mjs, and the two test entries in bio-plane/package.json
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md CAP-2 / D-109 — drain the task queue automatically on a Durable Object alarm, the mechanism #armSweep proved for selections: armed on enqueue, re-armed by the alarm while task_queue is non-empty, self-terminating (deleteAlarm) when it drains. The capture path only ENQUEUES; taskEnqueue arms the alarm (a schedule, never a task write) so the producer/consumer split is preserved. onAlarm shares the single DO alarm with the selection sweep and reconciles to the earliest wake. op=taskdrain still works — the alarm and the manual drain coexist. NEGATIVE CONTROL run: neutering onAlarm's taskDrain call fails the suite naming the undrained queue ("the queue is empty after the drain" want 0 got 1; "the queue count is zero" want 0 got 1); restored and green. Full battery green (exit 0). No inbox-grammar or write-gate change. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — CAP-2 / D-109 landed on main by CONDUCT, commit 39a0e1b. Full battery green (exit 0); the selection, inbox, and task-drain suites pass together, confirming the shared-alarm reconciliation preserves the selection sweep.

## CLAIM 2026-07-31 BOB
session: bob-planning-1
opened: 2026-07-31T21:00:00Z
paths: docs/development/MILESTONES.md (new), docs/development/VERIFICATION.md (new), docs/development/PLAN.md (header only), docs/development/ORCHESTRATION.md, docs/development/PARALLELISM.md (area table), docs/development/INTERFACES.md (I3/I4/I5 appended), docs/development/DEBT.md (Status column dispositions + appended section), docs/development/MEASUREMENTS.md (appended), docs/development/CAPTURE-SCALING.md (status markers), docs/development/kickoffs/README.md, CLAUDE.md (verification section), bio-plane/scripts/battery.mjs (new), bio-plane/scripts/coverage.mjs (new), bio-plane/test/bundle.test.mjs, bio-plane/package.json (two test scripts)
interfaces consumed: none
interfaces owned: registers I3, I4, I5 from the code as it stands (I3 owner moved CAPTURE -> RECORD)
expected: the planning-infrastructure cleanup Bob directed — place every open construct architecturally, refactor the plan into milestones and pipelines CONDUCT can orchestrate, and stand up a verification process with a measured floor. NO area code touched: the two new scripts are instruments, and bundle.test.mjs was REPAIRED (it read an absolute container path, was absent from the npm test chain, and failed on an 11-character token fixture against livefire's 16-character floor). QUEUE.md deliberately NOT edited — it is CONDUCT's sole-writer file and CONDUCT was landing CAP-2 concurrently; the M0 items are handed over as a decomposition instead.
released: 2026-07-31 — docs only; nothing deployed, nothing pushed.

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-4
opened: 2026-07-31T00:00:00Z
paths: the CPDF-7 measurement section in docs/development/MEASUREMENTS.md (append-only), the D-118 row in docs/development/DEBT.md (Status column only), bio-plane/test/free-tier-fleet-probe.mjs (new probe, not in the battery)
interfaces consumed: none (a measurement)
interfaces owned: none
expected: QUEUE.md CPDF-7 / D-118 — MEASURE whether Workers Free permits a second Worker script and service bindings, and what a cross-Worker call costs, through the project's own Cloudflare account and egress. Deploys THROWAWAY Workers (cpdf7-probe-*) and TEARS THEM DOWN, never touching biosmoke7/civicos/newgroup, the record or the installer; attaches no R2 binding. Commits no shipped code and changes no dependency. Findings: the account is on Workers FREE (the API refused limits.cpu_ms with "not supported for the Free plan"); on Free a second script deploys, a service binding deploys and RESOLVES, and a cross-Worker call runs end-to-end for ~1 ms wall-clock / one subrequest (25 binding calls in one Free invocation succeeded; teardown re-confirmed, back to three scripts). So D-118's conditional does NOT fire: pdf-worker (CPDF-6) is viable on Free and central; Tier 1 (CPDF-4) is not forced to be the floor by any binding limitation. D-118 CLOSED; the residual 10 ms Worker-CPU-ceiling question is narrowed onto CPDF-1's existing gated follow-on. No deploy of the plane; CONDUCT integrates.
released: 2026-07-31 — measurement landed on branch content-pdf/cpdf7-fleet-measure; CONDUCT integrates. Not pushed.

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-5
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/pdfstructure.mjs, bio-plane/test/pdfstructure.test.mjs
interfaces consumed: I1 (bytes → content)
interfaces owned: none; extends the provisional I2 (structure → framework) producer-side with a `text` field (for FW-1 to confirm/counter)
expected: QUEUE.md CPDF-4 — Tier 1 text extraction IN THE PLANE, pure JS, no dependency. Parse the text-showing operators (Tj, TJ, ', ") in each page's content stream(s), reusing the existing object/stream parser (FlateDecode, /ObjStm), and decode the shown bytes to Unicode via each font's /ToUnicode CMap (beginbfchar/bfrange). EXTEND the extractor's existing I2 output object with `text` (document-level + per-page, plus per-region `undetermined` markers) — do NOT fork the output shape. `undetermined` is first-class and per-region: a glyph/run with no ToUnicode, a CID font, or an unmapped code is recorded as undetermined NAMING the cause (the font), never dropped, never guessed into mojibake. No op edit (op=pdfstructure returns the extractor's object, so text flows through automatically). No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (314f4b7). I2 extended with a `text` field (document/pages/undetermined markers, per-region markers naming the font/reason); registered provisional in INTERFACES.md for FW-1 to confirm/counter. battery 43/43 (2313); negative control run. index.mjs untouched.
opened: 2026-07-31T22:00:00Z
paths: the docprofile package — docprofile/index.mjs, docprofile/registry.mjs, docprofile/monitoring.mjs, docprofile/pipeline.mjs, docprofile/doctypes/index.mjs, docprofile/doctypes/registry.mjs, docprofile/doctypes/meeting-calendar.mjs, docprofile/doctypes/generic.mjs, docprofile/handlers/aspnet-webforms.mjs, docprofile/handlers/wordpress.mjs (member extraction removal only); the new docprofile/recogniser.mjs and docprofile/events.mjs; tools/bundle-docprofile.mjs (ORDER list); the flattened docprofile block in civicos-ui/app.html (rebuilt, not hand-edited); civicos-ui/test/docprofile.test.mjs and civicos-ui/test/change-layers.test.mjs (rewritten onto the consolidated shape)
interfaces consumed: I2 (structure, provisional — noted for FW-1/CPDF-4 that it will extend with text)
interfaces owned: none changed by this step (Step 0 is internal consolidation; I2 confirm/counter is FW-1)
expected: QUEUE.md FW-2 / D-68 / CONSTRUCTS Step 0 — the full reconciliation. One recogniser interface + one shared registry helper carrying both axes; one confidence ladder (CONFIDENCE, TYPE_CONFIDENCE merged); assess() the sole public entry point (monitor()/diffMembers/handler-derived contract() deleted, compare() demoted to the internal L3 primitive assess() calls); CONTRACT declared by the content type; a shared event catalogue; meaningful derived from SIGNIFICANCE. Must SHRINK the codebase and add no capability. Both npm run test:battery (bio-plane) and civicos-ui/test/run.mjs green; negative control on the single ladder / single entry point recorded in the suites.
released: 2026-07-31 — CONSTRUCTS Step 0 landed on main by CONDUCT; docprofile net -27 lines; civicos-ui/test/run.mjs + full battery green; negative controls (single ladder / sole entry point) confirmed.

## CLAIM 2026-07-31 RECORD
session: record-agent-1
opened: 2026-07-31T22:30:00Z
paths: the SCHEDULER block in bio-plane/src/store.mjs — specifically the reserved `alarm()` forwarder and the `onAlarm()` handler, the reconcile helper (renamed from `#rearmSchedule` to `#reconcileAlarm`), the arm helpers `#armScheduler`/`#armSweep`/`#armDrain`, the new consumer registry `#schedConsumers`, the `#lastDrainProgress` field, the `SCHED_GRACE_MS` constant, and the env-gated test-probe seam (`#probeSpecs`, `#probeState`, `schedProbeArm`, `schedProbeLog`); bio-plane/test/scheduler.test.mjs (new); the test:scheduler entry and the battery-chain entry in bio-plane/package.json; docs/development/SCHEDULER.md (new, the shape decision)
interfaces consumed: none
interfaces owned: I5 (the store schema — unchanged this turn; no table added or reshaped)
expected: QUEUE.md REC-1 / M1 — decide the plane's scheduler shape ONCE and build the mechanism. Generalises the `#armSweep`/`#rearmSchedule` single-reconciling-alarm pattern into a consumer registry where each consumer declares its own wake and the one DO alarm reconciles to the earliest, self-terminating when all are idle. Moves the two existing consumers (RECORD's selection sweep and CAP-2's task drain) onto the registry as proof. Does NOT touch CAPTURE's `taskDrain`/`taskEnqueue` bodies, the link/capture/task/reachability functions, or any schema table — only the alarm handler and reconcile/arm helpers, which PARALLELISM.md places with RECORD (the scheduler). No new op, so no control-plane surface change. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — landed on main by CONDUCT (4d77938). One reconciling DO alarm + consumer registry; both existing consumers (selection sweep, task drain) moved onto it with bodies unchanged; battery 43/43 (2272 assertions), scheduler suite + starvation negative control green; I5 unchanged.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-1
opened: 2026-07-31T00:00:00Z
paths: bio-plane/test/reachability.test.mjs, bio-plane/test/acquire.test.mjs, bio-plane/test/subresources.test.mjs (+ a one-line NEGATIVE CONTROL header comment), bio-plane/test/members.test.mjs — test files only; no plane behaviour touched
interfaces consumed: I3 (the control-plane op= dispatch)
interfaces owned: none
expected: QUEUE.md M0-1 — control-plane assertions for the three ops no suite reached (archivelookup, linkproject, signerlist) and for sourcereach, reached only at the Durable Object (the D-43 class). Reachability, not success: each op is driven through op= dispatch with a minimal valid-enough request and asserted to answer STRUCTURALLY rather than crash — a governed BIO refusal counts. Placements, each in the op's own area suite: sourcereach -> reachability.test.mjs (op=sourcereach read of the same "bio" store the suite drives at the DO); archivelookup -> acquire.test.mjs (a fresh store refuses NOT_ELIGIBLE by the D-104 fence — structured); linkproject -> subresources.test.mjs beside op=links (NEED_CAPTURE refusal proves the handler ran, then a valid capture projects with ok:true / edges[] / projected:number); signerlist -> members.test.mjs beside signeradd (ok:true, result.signers[]). Coverage BEFORE: 81/85 control-plane (95.3%), 1 durable-object-only (sourcereach), 3 unreached (archivelookup, linkproject, signerlist). AFTER: 85/85 (100.0%), 0 durable-object-only, 0 unreached. NEGATIVE CONTROL RUN 2026-07-31: deleted the linkproject dispatch block from subresources.test.mjs (its op= appears only there — the header comment was worded to NOT contain op=linkproject, so the instrument is not fed by the comment) and `npm run test:coverage` reported unreached 0 -> 1 naming linkproject (mutating); restored, back to 0. Recorded in subresources.test.mjs's first-60-lines NEGATIVE CONTROL comment. Full battery green: 42/42, 2264 assertions (was 2252; +12). No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT. CONDUCT confirmed via `npm run test:coverage` on the integrated state: 85/85, 0 DO-only, 0 unreached (was 81/85 + 1 DO-only + 3 unreached — the D-43 class closed for the current surface). The worker RAN its negative control (delete the linkproject dispatch → unreached 0→1) and recorded it in subresources.test.mjs; battery green.

## CLAIM 2026-07-31 RECORD
session: record-agent-2
opened: 2026-07-31T23:30:00Z
paths: in bio-plane/src/store.mjs — `acquireLease` (the lease-taking path, ~line 2761) ONLY: it gains a fail-closed guard that refuses a null/blank actor by name instead of tripping SQLITE_CONSTRAINT_NOTNULL (D-39 class). In bio-plane/src/index.mjs — the control-plane authorship stamps ONLY: the lease `actor` stamp (~line 2222) is extended to stamp a NAMED machine identity `token:<class>` for a machine credential and to delete a caller-supplied `actor` first (previously stamped only for a session, leaving a machine's actor caller-supplied/absent), and the op=promote body stamp (~line 2270) is extended so a machine credential's `author` is server-stamped `token:<class>` (deleted-then-set), matching the `by`/`viewer` machine convention already there, so the manifest NAMES the machine writer. New test bio-plane/test/unattended-lease.test.mjs; its entry in bio-plane/package.json (test:unattended-lease + the npm test chain). NO schema.mjs change (leases.actor stays NOT NULL TEXT; a machine identity string is a valid actor), so I5 is untouched and the D-113 purge list (leases already present) and hygiene check are unaffected.
interfaces consumed: I3 (the control-plane op= dispatch — additive authorship stamping, no op renamed, no classes/reason/shape changed)
interfaces owned: I5 (the store schema — UNCHANGED this turn; no table added or reshaped)
expected: QUEUE.md REC-2 / D-61 — an unattended (machine) writer cannot take a lease because leases.actor is NOT NULL and was stamped only from a session, so a daemon cannot complete a capture a member walked away from. DECISION: option (a), a named machine actor identity on the lease, NOT (b) dropping the lease from the refill path. Rationale in the suite header and the report: the lease is a courtesy lock worth keeping in the loop for the daemon (a returning member sees the job is being finished; two daemons do not both re-fetch), naming costs nothing and needs no schema change, and promote's CAS on `base` — the real integrity mechanism — is untouched, so a daemon that holds a lease still cannot cause a lost update. The writer is NAMED (server-stamped token:<class>, caller value deleted), never anonymous. accepts-when: a machine credential completes a capture a session started and the manifest author names the machine writer; NEGATIVE CONTROL: an anonymous (null/blank actor) lease is refused by name. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (e425b24). CONDUCT re-ran the anonymous-lease negative control (neuter the acquireLease guard → 20/2, restored → 22/22); battery 44/44 (2337). Battery 44/44 (2308 assertions); the new unattended-lease.test.mjs 22/22 and its negative control RUN (neuter the acquireLease guard -> 20/2, restored -> 22/22); coverage 85/85 ops reached, 0 unreached, no new op, suite declares its control. schema.mjs untouched (I5 unchanged); D-113 purge list + hygiene still green. node_modules was symlinked from the main checkout for miniflare and removed before commit.
## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-6
opened: 2026-07-31T00:00:00Z
paths: the CPDF-5 section of docs/development/MEASUREMENTS.md (append-only); bio-plane/test/tier1-coverage-probe.mjs (new probe, NOT in the battery); bio-plane/test/tier1-coverage-tier2-oracle.mjs (new probe, NOT in the battery)
interfaces consumed: I2 (structure -> text; consumes the CPDF-4 `text` field of extractPdfStructure, read-only)
interfaces owned: none — a measurement; commits no extractor and changes no shipped code
expected: QUEUE.md CPDF-5 — MEASURE Tier-1 text-extraction coverage on REAL Oakland PDFs to SIZE Tier 2 (the unpdf pdf-worker, CPDF-6). 14 real documents fetched 2026-07-31 across classes (agenda packets + staff reports from oakland.legistar.com via the per-file GUID; ACFRs, budget books, budget exhibits from oaklandca.gov via the honest contact-URL agent), each with source URL. Ran extractPdfStructure (Tier 1, pure JS) on each and read the per-region text.undetermined markers; ran unpdf/pdf.js (Tier 2) on the SAME bytes as an ORACLE so "the residue is recoverable by the pdf-worker" is measured, not assumed. FINDINGS: per-document buckets — Tier 1 FULLY 4/14 (agenda packets, modern ACFR, slide deck), Tier 1 PARTIAL 3/14 (budget books ~88%, transmittal 41%), Tier 2 REQUIRED 5/14 (2 encrypted incl. the 2025 ACFR, 3 fonts-without-ToUnicode; unpdf recovers all measured), OCR-only 2/14 (scanned/image — neither tier). Two failure causes zero out whole documents: permission-only ENCRYPTION (Tier 1 has no decryption; pdf.js decrypts empty-password Standard/AES transparently — measured on the Legistar staff report, 13,012 clean chars) and fonts with no /ToUnicode (Tier 1 refuses to guess; pdf.js maps glyphs — cafr-2003 739 chars Tier1 vs 278,758 Tier2). CONCLUSION: the pdf-worker (CPDF-6) is CENTRAL not marginal — the staff-report class (the substance an agenda links to) and encrypted ACFRs need it; Tier 1 remains valuable, fully serving agendas + modern books/decks (~50% of the sample in-plane, free). Extractor gap recorded: Tier 1 does not NAME encryption (degrades to objstm_undecodable/content_stream_undecodable notes though /Encrypt is in the trailer) — a reason:"encrypted" marker recommended for CPDF-4/6. Runtime caveat for CPDF-6: unpdf/pdf.js threw Math.sumPrecise on one doc on node v26.5.0 — the pdf-worker must pin a Workers-runtime-verified version. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (62a650b). Verdict: pdf-worker (CPDF-6) is CENTRAL (~36% of the class-spread sample needs Tier 2). OCR gap lifted to DEC-4. Probes confirmed not in the battery.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-4
opened: 2026-07-31T00:00:00Z
paths: in bio-plane/src/store.mjs — the archive-fallback MONITORING consumer (CAPTURE's tick logic): the new `#monitorTickMs`/`#monitorToken`/`#monitorConfigured`/`#monitorFloor`/`#monitorPending`/`#monitorTick`/`#fireArchiveFallback` methods and the `MONITOR_TICK_MS`/`MONITOR_TICK_BATCH` constants placed beside the reachability code, and the arm added to `recordSourceOutcome` (made async; arms the scheduler on a COUNTED real failure only, never on `governed` or `success`); AND the one authorised cross-area touch of RECORD's scheduler — appending the `archive-monitor` entry to `#schedConsumers`, and in `onAlarm` awaiting `c.tick(now)` (so an async consumer's work completes within the alarm) and wiring the `archive-monitor` result through the return. bio-plane/test/archive-monitoring.test.mjs (new); its test:archive-monitoring entry and the battery-chain entry in bio-plane/package.json
interfaces consumed: none (I3 op=acquire/op=recordsourceoutcome/op=sourcereach reached, not changed)
interfaces owned: none; no schema table added or reshaped (I5 unchanged)
expected: QUEUE.md CAP-3 — make a periodic MONITORING tick actually INVOKE the archive fallback (built, live-verified, IDLE). RECORD owns the scheduler but is DORMANT and CONDUCT authorised this cross-area touch; kept to appending ONE consumer + arming it (SCHEDULER.md's documented extension) plus the minimal `await` onAlarm needs for an async consumer — no second alarm, no cron, no reshape. The tick consults `sourcereach` for `fallback_eligible` documents (three consecutive REAL source failures, or the aged arm; a governed refusal is our own politeness and moves no counter — D-104, read before touching it) and fires the fallback for each by invoking op=acquire with `via:"archive.org"` and the DOCUMENT address over `env.SELF` under a daemon credential, producing the two-hop grade-C chain from the one acquire path (fence + provenance hop unchanged). INERT unless `env.SELF` (a service binding to this instance's own Worker) and a daemon token are present — the SCHED_PROBE-seam pattern — so every existing suite is byte-for-byte unaffected. accepts-when bio-plane/test/archive-monitoring.test.mjs: three source_refused outcomes drive the tick to fire the fallback (grade C, 2-hop chain, failing run reset by the archive success = the RULED re-fetch-for-monitoring). NEGATIVE CONTROL RUN: make recordSourceOutcome's `governed` branch increment consecutive_failures (drop the D-104 exclusion) -> three governed refusals fire a SPURIOUS fallback; restored, recorded in the suite's first-60-lines NEGATIVE CONTROL comment. Live wiring of env.SELF is per-instance (the instance name IS the worker name) and belongs to the installer/CONDUCT — raised as a decision item. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — landed on main by CONDUCT (dea4c13). battery 45/45 (2361); the trust-critical governed-refusal exclusion is the EXISTING D-104 mechanism CAP-3 reads (reachability.test 40 + archive-monitoring.test 22 verify it end-to-end, plus the worker's negative control governed→spurious fallback→4 fail). Mechanism proven; LIVE arming is the DELEGATION below + DEC-5.

## DELEGATION 2026-07-31 CAPTURE → DIST
from: capture-agent-4 / CAP-3 (recorded by CONDUCT at integration)
need: the monitoring tick is built and tested but INERT on live instances until `env.SELF` (a service binding to the instance's OWN Worker) and a daemon token are provisioned. The instance name IS the worker name, so the self-binding target must be TEMPLATED per-instance by the installer (newgroup) and the plane deploy — out of bounds for CAPTURE. Until then the archive fallback stays "built and idle" (the consumer no-ops without the binding). Composes with CPDF-6's pdf-worker service binding — the same "how does a Worker call another Worker on this instance" provisioning.
why: the DIST/installer half of making resilience actually run; gated (deploy + installer) and carrying a token-exposure surface — see DEC-5.
status: open
## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-7
opened: 2026-07-31T00:00:00Z
paths: pdf-worker/** (new fleet member: src/index.mjs, wrangler.jsonc, package.json, scripts/build.mjs, fleet-member.json, .gitignore, the committed dist/pdf-worker.bundled.mjs, test/pdf-worker.test.mjs); the service-binding wiring in bio-plane/src/index.mjs (op=pdfstructure escalation + the needsTier2 helper — RECORD's I3 control plane, see the DELEGATION note) and bio-plane/wrangler.jsonc (the PDF_WORKER service binding); bio-plane/src/pdfstructure.mjs (CONTENT-PDF's own — Tier-1 encryption detection: isEncrypted + the encrypted text marker); bio-plane/scripts/coverage.mjs (M0-5 fleet enumeration); tests bio-plane/test/pdf-worker-binding.test.mjs and additions to bio-plane/test/pdfstructure.test.mjs; docs INTERFACES.md (I2 text reasons + I6 as-built), MEASUREMENTS.md (append), this CLAIMS entry
interfaces consumed: I1 §2 (the R2 key shape — promoted to a load-bearing dependency with a second consumer); I3 (the op= dispatch — additive: op=pdfstructure gains a Tier-2 escalation, no op renamed, no classes/reason/shape changed)
interfaces owned: I6 (plane → pdf-worker — the worker now EXISTS; as-built shape recorded in INTERFACES.md, still PROVISIONAL pending a DIST deploy and FW-1); extends the provisional I2 text shape with `encrypted`/`over_envelope`/`no_text_layer`/`tier2_extraction_error` reasons and a top-level `tier`
expected: QUEUE.md CPDF-6 + M0-5 — build pdf-worker, the FIRST fleet member (I6): holds unpdf 1.8.0 (PINNED, verified extracting on workerd — its real runtime, where pdf.js Math.sumPrecise resolves; a guarded polyfill covers node), reads bytes from R2 itself (CAPTURES read binding, never PUBLISHED, no STORE), returns the record's I2 structure+text shape, WRITES NOTHING. The plane's op=pdfstructure runs Tier 1 in-plane and escalates over a service binding (PDF_WORKER) only when Tier 1 got essentially nothing (undetermined regions > decoded chars — CPDF-5's encryption + no-/ToUnicode buckets), never for a budget book at ~88%; absent the binding it degrades to Tier 1, named, no crash (an un-fleeted instance, D-115). A document over the envelope (MAX_PDF_BYTES, default 16 MB) returns text-undetermined(over_envelope), never truncated. Tier-1 now NAMES encryption (reads the Standard Security Handler from the trailer without decrypting — the CPDF-5 gap). M0-5: coverage.mjs enumerates fleet members (discovered from fleet-member.json, never hand-listed) and holds each to surface-ops-reached + a declared control. accepts-when ALL GREEN: (1) the plane returns "Hello Oakland 2026" for a no-/ToUnicode CID-class PDF Tier 1 could not decode, through the binding (pdf-worker-binding.test.mjs 12/12); (2) the worker writes nothing — R2 byte-for-byte unchanged + no put/delete in source, no PUBLISHED/STORE binding (pdf-worker.test.mjs 28/28); (3) an over-envelope document returns text-undetermined not truncated; (4) test:coverage lists pdf-worker and its `structure` surface. NEGATIVE CONTROLS RUN 2026-07-31: (a) needsTier2->false -> the two Tier-2 binding assertions fail (10/2), restored 12/0; (b) inject env.CAPTURES.put into the worker, rebuild -> the R2-unchanged + source-scan assertions fail (25/3), restored 28/0; (c) add a stub fleet member with an untested op -> coverage reports it UNREACHED and --strict exits 1, removed -> clean. Battery 45/45 (2360). DELEGATIONS: the op=pdfstructure escalation call site is in index.mjs (RECORD's I3) not CONTENT-PDF's paths — landed here in one turn with the member per the CPDF-6 item, flagged to CONDUCT; and the DEPLOY of the member + binding is DIST's (a new Worker + service binding are a gated release; installer must install the FLEET, D-115/D-116). No push, no deploy.
released: 2026-07-31 — landed on main by CONDUCT (7e5b67f). First fleet member (pdf-worker) + M0-5 fleet coverage; battery 46/46 (2384); write-nothing verified structurally; unpdf pinned for workerd. RECORD I3 (escalation call site) + DIST (deploy/install fleet) delegations recorded; live-verify (CPDF-3) gated.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-2
opened: 2026-07-31T00:00:00Z
paths: the I2 section of docs/development/INTERFACES.md (Status/Version/reasoning); docs/development/INTERFACE-CHANGES.md ONLY IF a counter were needed (it was not — no counter, so this file was not created)
interfaces consumed: I2 (structure -> framework) — the interface being confirmed; read pdfstructure.mjs (producer) and docprofile/** (consumer) to judge it
interfaces owned: I2 (FRAMEWORK is the consumer/owner; this turn is the confirmation that turns it STABLE)
expected: QUEUE.md FW-1 — confirm or counter the provisional I2, the EXTENDED shape (link partitions + text/undetermined markers + tier). Judged as the consumer's owner whether the shape CONTENT-PDF produces serves FRAMEWORK's need to identify content and intent. VERDICT: CONFIRMED, no change. WHY: (1) container-agnostic BY CONSTRUCTION — the four partitions/wrappers are subresources.mjs's own, imported not re-derived, so docprofile's content-type recognition and connections() consume PDF and HTML links through one path (Bob's "content in PDFs as in HTML"); (2) undetermined first-class on BOTH axes (link why + text Marker naming the font) is the never-invent/state-the-gap doctrine docprofile's every default rests on, letting a doctype honestly refuse instead of parsing mojibake; (3) it gives the two doctype inputs — text.document/pages[] for L1/L4/L5 recognition+meaning, links[] partitioned for L6 referential/temporal (intra content-addressed by sha256 = a stronger referential key than an href), keyed on a shared 0-based page index so they compose; (4) tier is stamped at the op boundary the consumer actually reads (op=pdfstructure: 1 in-plane, 2 pdf-worker); (5) the flat no-layout text is the honest Tier-1 maximum — richer positional/table structure is a future doctype-driven ASK via the change protocol, not a reason to hold the contract provisional. Touched NO plane code: the shape confirmed is already the shape the battery pins (pdfstructure.test.mjs partitions/wrapper-byte-identity/counts/text.*, pdf-worker tests text+tier), so a redundant assertion was unwarranted. I2 set to Version 1.0.0 / Status STABLE (matching I1/I3/I6's stable convention). One producer-side residual noted: CONTENT-HTML emitting the same text shape (link parity already by-construction) — CONTENT-HTML's to raise via INTERFACE-CHANGES.md when it activates, not a consumer reservation. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (44a867a). I2 STABLE 1.0.0; docs-only, battery 46/46 unaffected.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-5
opened: 2026-07-31T00:00:00Z
paths: in bio-plane/src/store.mjs — the reuse-verification helpers ONLY: new `reusedParts` (enumerate a bundle's reused subresource parts by joining `site_asset_refs.reused=1` to `register` and `site_assets`), `recordReuseVerdicts` (append rows to the new `reuse_verdicts` table), `reuseVerdicts` (read them), the POST-HOC verdict write added inside the existing `recordSiteAssets` change-case (phase `posthoc`), the three new DO routes (`reusedparts`/`recordreuseverdicts`/`reuseverdicts`) in the dispatch map, and the `DELETE FROM reuse_verdicts` line in `purge`'s whole-store arm. In bio-plane/src/index.mjs — the `op=ratify` handler ONLY: after publish, re-fetch every reused part with a PLAIN GET bounded by the calibrated `capture_limits` ceiling, record `confirmed`/`changed`/`unavailable`/`not_attempted`, and surface the summary on the response. In bio-plane/src/schema.mjs — ONE new derived table `reuse_verdicts` placed BEFORE the `host_governor` block (I5, additive only). New test bio-plane/test/reuse-ratify.test.mjs; its test:reuse-ratify entry and the npm-test-chain entry in bio-plane/package.json.
interfaces consumed: I3 (the control-plane op= dispatch — op=ratify gains a re-fetch-and-record step, no op renamed, no classes/shape changed) 
interfaces owned: I5 (the store schema — ADD ONLY: one derived table `reuse_verdicts`, named in purge and in the D-113 hygiene coverage, no existing table reshaped)
expected: QUEUE.md CAP-4 / CAPTURE-SCALING.md item 6, the four decided specifics. (a) POST-HOC reuse verification from `site_assets`, unconditional, zero requests, verdicts appended and dated (persisted in the change-case of recordSiteAssets, phase `posthoc`). (b) At ratification re-fetch every reused part — MANDATORY as an attempt and a record, never as agreement: `confirmed`/`changed`/`unavailable` all ratify; ratifying with a reused part while saying nothing is forbidden. (c) A PLAIN GET, not `If-None-Match`: our own SHA-256 over what we received is the evidence where a 304 is only the origin's assertion (reverses the conditional-GET suggestion elsewhere in that document, right for working capture, wrong here). (d) A fourth outcome `not_attempted`, recorded WITH its reason, for parts the invocation's budget (the calibrated `capture_limits` ceiling) could not reach — never silently omitted. accepts-when bio-plane/test/reuse-ratify.test.mjs: a bundle ratified with reused parts records an outcome for EVERY reused part; a dark source still ratifies as `unavailable`; a bundle whose reuse count exceeds the calibrated ceiling ratifies with the residue `not_attempted`. NEGATIVE CONTROL: drop the outcome record for a part and the suite FAILS naming the unrecorded part. D-113 purge/hygiene re-checked green for the new table. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (c7c57c9). battery 47/47; reuse_verdicts in purge, hygiene 138 + purge 14 green; coverage 85/85.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-2
opened: 2026-08-01T00:17:53Z
paths: bio-plane/test/check-firing.test.mjs (NEW — the only file touched). No plane behaviour, no catalog, no other suite.
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-3 — name the 33 checks no assertion named. One assertion per check that TAMPERS a conformant bundle and requires THAT check id in the findings, closing the S-7 direction (a check run only where it passes is unproven). New suite check-firing.test.mjs builds a conformant bundle per object type (the exact field set setup.mjs's mdFor writes, proven clean by conformance.test.mjs), tampers exactly one thing, and asserts BOTH ways: the check FIRES on the tamper AND is ABSENT from the untouched base (the built-in negative control). All 33 named this turn: C-1.3 C-2.1 C-2.2 C-2.3 C-2.4 C-2.6 C-2.9 C-2.10 C-3.1 C-6.3 C-7.1 C-8.1 C-9.1 C-10.1 C-11.1 C-12.1 C-12.2 C-13.1 C-14.1 C-14.2 C-14.3 C-14.4 C-15.1 C-16.1 C-16.2 C-16.3 C-16.4 C-16.5 C-17.1 C-18.3 C-18.4 C-18.7 C-18.8. accepts-when: `npm run test:coverage` reports 51/51 checks named (was 18/51); check-firing.test.mjs 70 pass, 0 fail; battery 48/48 (2493). NEGATIVE CONTROL declared in the suite's first 60 lines and SPOT-CHECKED on C-2.1, C-13.1, C-16.4: removing the tamper (bundle == base) drops the finding and flips `fires` true->false. No push, no deploy — CONDUCT integrates. M0-3 is COMPLETE (no split needed; all 33 landed in one turn).
released: 2026-08-01 — handed to CONDUCT.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-3
opened: 2026-08-01T00:33:43Z
paths: bio-plane/package.json (the `test` script ONLY — the hand-maintained `&&` chain replaced by `node scripts/battery.mjs`; `test:battery` and every `test:*` alias unchanged); bio-plane/scripts/battery.mjs (skip-awareness — tally reads an optional named skip count, a wholesale `SKIPPED — reason` marker gets its own `skip` status and is surfaced by name in the summary, neither counting as a failure); bio-plane/test/ratify.test.mjs, bio-plane/test/reuse-ratify.test.mjs, bio-plane/test/signpage.test.mjs (each gains a top-of-file `spawnSync("ssh-keygen")` guard that SKIPS LOUDLY with a named reason and exits 0 instead of dying with an unhandled spawn error); bio-plane/test/sshsig.test.mjs (the `else` branch now NAMES and COUNTS the 2 skipped fresh-signature cases on the tail line). Doc corrections of superseded text (same-turn, ORCHESTRATION rule 6): docs/development/VERIFICATION.md (the "battery runs every suite" section + floor item 4) and CLAUDE.md (verification-discipline step 1) — both said `npm test` chains with `&&`, now false. QUEUE.md NOT touched (CONDUCT's sole-writer file — M0-4 `landed:` is CONDUCT's to fill). DEBT.md D-93 NOT closed here (CONDUCT closes at integration).
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-4 — close BOTH halves of D-93. First half: `npm test` becomes the discovering runner, so a crashing suite can no longer hide the suites behind it; both `npm test` and `npm run test:battery` now run every suite and report all of them. Second half: a suite that wholesale-depends on stock ssh-keygen for signing (ratify, reuse-ratify, signpage) detects its absence and SKIPS LOUDLY WITH A NAMED REASON (`name: SKIPPED — ssh-keygen not on PATH; …`) or would fail loudly — never quietly does less; and sshsig, which has a pinned-fixture fallback, runs its 16 pinned assertions and NAMES the 2 fresh-signature cases it skips (`16 pass, 0 fail, 2 skip (fresh signature verifies; fresh signature bound to its bytes; ssh-keygen absent)`) instead of silently reporting 16 (the 16-vs-18 case). Only ratify + sshsig were named in the item; reuse-ratify + signpage share the same wholesale-ssh-keygen dependency and were failing loudly once the runner stopped hiding them, so the accepts-when (green when ssh-keygen hidden) required the same guard — added and claimed here. accepts-when GREEN: normally battery 48/48 (2493 assertions); with ssh-keygen hidden from PATH `npm test` COMPLETES exit 0, 45/48 green + 3 SKIPPED-by-name + sshsig ran-short-by-2-named, every other suite still runs (none hidden). NEGATIVE CONTROLS RUN 2026-08-01, restored, recorded in each suite's first-60-lines comment: (ratify) remove the guard + hide ssh-keygen -> `FAIL ratify.test.mjs ... assertions unknown`, a bare failure with no named reason (other suites still ran — the runner does not hide them); (sshsig) silence the skip note + hide ssh-keygen -> a bare `16 pass` with no hint two cases were skipped, the exact D-93 shortfall. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-08-01 — handed to CONDUCT.
## CLAIM 2026-07-31 RECORD
session: record-agent-3
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/setup.mjs — the installer wizard surface (RECORD/REC-3 scope): `acquireWhy` (delete the stale NO_AUTHORITY branch, D-110) and `mdFor` + its one caller in the `#n-save` handler (emit `content_hash: sha256:<primary>` when a document is attached, D-62). bio-plane/src/index.mjs — the op=promote control-plane body stamp ONLY (~line 2419, beside the existing author/ownerMemberId stamps): server-stamp `surfaced_by` in a focus/problem CREATION's bundle.md from the caller's ACTOR CLASS (viaSession→human, machine→agent), caller value overwritten, bundle.md sha recomputed (D-78). NO store.mjs change and NO schema change — the store keeps byte-trusting bundle.md (40+ suites depend on that; see the surfaced_by writers note below). New tests bio-plane/test/empty-body.test.mjs (D-39), bio-plane/test/setup-honesty.test.mjs (D-110 + D-62), bio-plane/test/surfaced-by.test.mjs (D-78). DEBT.md — D-39/D-110/D-62/D-78 dispositions.
interfaces consumed: I3 (the control-plane op= dispatch — additive: op=promote gains a server-side surfaced_by stamp for focus/problem creations; no op renamed, no classes/reason/shape changed)
interfaces owned: I5 (the store schema — UNCHANGED this turn)
expected: QUEUE.md REC-3 — four honesty defects in the plane's own surfaces. D-39: an empty POST body was found already guarded at the Durable Object (store.mjs fetch, commit 6ac72d0a) so it returns a named JSON reason, NOT a Cloudflare 1101 — the code fix had landed but no suite locked it in and the debt stayed open; empty-body.test.mjs drives an empty POST to the five debt-named ops (stats/index/dangling/searchfields/selectionlist) and asserts a structured reason. D-110: acquireWhy still explained NO_AUTHORITY, the refusal D-97 removed; deleted (grep confirms NO_AUTHORITY existed only there). D-62: the wizard's mdFor omitted content_hash for a document bundle, so C-2.7's verified branch refused it and the hash: facet could not find it; now emits `content_hash: sha256:<primary capture sha>`, mirroring civicos-ui's already-landed port, absent for typed intake where inventing one would be a lie. D-78: both writers hardcoded `surfaced_by: human`; rather than trust a caller-asserted literal (and rather than break the 40+ suites that rely on the store byte-trusting bundle.md — e.g. projection.test asserts an arbitrary surfaced_by projects), the value is now SERVER-STAMPED at the trust boundary in index.mjs, exactly where author/owner/by/viewer already are: a focus opened by a machine credential honestly records `agent`, one opened by a member `human`. This fixes BOTH writers (and civicos-ui) at once with no cross-area delegation, since the store, not the surface, is now authoritative. accepts-when: an empty POST to five ops returns a named reason (not a 1101); a wizard bundle with a document carries content_hash and drops the C-2.7 content_hash finding; a focus written by an agent records surfaced_by: agent. NEGATIVE CONTROLS RUN — recorded in each suite's first-60-lines NEGATIVE CONTROL line. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (a0c6d98). battery 51/51, coverage 85/85. CLAIMS conflict (vs M0-4) resolved union. D-39/62/78/110 closed by worker; I3 additive stamp registered; D-121 opened for the revision-carry residual.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw3
opened: 2026-07-31T00:00:00Z
paths: the op=acquire body in bio-plane/src/index.mjs (the docprofile import at the top, and the profile-writing block just before the acquire response literal — adds a `document.profile` sibling field, changes no existing field); bio-plane/test/profile.test.mjs (new); the test:profile entry in bio-plane/package.json; docs/development/INTERFACES.md (I1 version bump note for the additive `document.profile` field)
interfaces consumed: I1 (bytes → content) — WRITER side (op=acquire), additive
interfaces owned: none (FRAMEWORK owns docprofile, which is READ here, never copied; I1 is CAPTURE's and this is the FW-3 DELEGATION with FRAMEWORK's guidance)
expected: QUEUE.md FW-3 / CONSTRUCTS Step 1 — the plane RECORDS THE PROFILE. op=acquire calls docprofile's identify() (host stack) and doctypeFor() (content type) — the existing functions, not a second copy — and writes onto the acquire document a `profile` field carrying: the handler (key/label/version) and its confidence + signals; the content type (key/label/version) and its confidence + signals; the handler's declared normalisation and boundary; the document kind; the source content-type; and the profiling instant. The recogniser key+version on BOTH axes IS the "author + version" the ladder above needs so a wrong judgment can later be found and revised. This is the FIRST plane consumer of docprofile (until now consumed by zero plane files). I1 VERDICT: ADDITIVE — `document.profile` is a new sibling field on the acquire document; no existing field's shape or meaning changes, and C-18.1 tolerates extra keys (checks required keys only). accepts-when bio-plane/test/profile.test.mjs: an op=acquire of an ASP.NET-WebForms meeting-calendar page carries document.profile naming handler aspnet_webforms + content_type meeting_calendar, both confidences, both signal lists, the normalisation, and the recogniser versions. NEGATIVE CONTROL: stop stamping the handler (delete profile.handler) → the handler assertions fail. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (02491bd), clean cherry-pick. battery 52/52, coverage 85/85. I1 additive bump verified (C-18.1 tolerates extra keys); no INTERFACE-CHANGES protocol needed. No DEC.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw4
opened: 2026-07-31T00:00:00Z
paths: the op=acquire body in bio-plane/src/index.mjs (the docprofile import at the top gains `digests` + `CONFIDENCE`; a new module-level `sha256Hex` helper beside `fingerprint`; and, in the profile block just after `document.profile` is built, capture the primary bytes and set `profile.digests` — the COMPUTED normalisation digests. Adds a sub-field, changes no existing field). bio-plane/checks/bio-checks.mjs — `checkRegisterIntegrity` (C-18.3) ONLY: a SECOND bucket keyed by the determined evidentiary digest, so two register documents whose SUBSTANCE is identical though their raw bytes differ (viewstate/boilerplate) are folded as corroboration, not two review items; the raw-sha arm is unchanged and the normalised arm never fires on two undetermined (null) digests or on a pair already caught by identical raw bytes. New test bio-plane/test/framework-digest-audit.test.mjs; its test:framework-digest-audit entry in bio-plane/package.json. Possibly one line in bio-plane/test/check-firing.test.mjs (a normalised-arm firing case for C-18.3).
interfaces consumed: I1 (bytes → content) — WRITER side (op=acquire), additive: `document.profile.digests` is a new sub-field
interfaces owned: none (FRAMEWORK owns docprofile, which is READ here — `digests()` — never copied; I1 is CAPTURE's and this is the FW-4 DELEGATION with FRAMEWORK's guidance)
expected: QUEUE.md FW-4 / CONSTRUCTS Step 2 — the plane COMPUTES and STORES the normalisation digests on the capture, per the handler's declared normalisation policy FW-3 recorded. The framework defines THREE digests (docprofile/index.mjs, DOCUMENT-PROFILES.md "Three digests, not one"): identity = sha256 of raw bytes = `capture_sha` (I1 §1, REUSED, never recomputed under a new name), rendition = mechanical-normalised, evidentiary = presentational+mechanical-normalised. op=acquire stores `profile.digests = { determined, rendition, evidentiary, boundary_missed, basis }`. `determined` is true ONLY when the bytes were read as text AND the stack was identified with CERTAINTY (a signal only that stack emits): the failure asymmetry runs the OTHER way for deduplication than for change — folding two distinct review items into one corroboration HIDES a document — so unlike compare() the trust is NOT extended to the conservative handler, and an undetermined capture (PDF/multipart/uncertain) records rendition+evidentiary ABSENT (null), never a fabricated value. Then op=audit's duplicate sweep (C-18.3, run by auditPass→checkBundle) gains the normalised comparison so it catches a duplicate whose viewstate/boilerplate differs, which today it cannot see (raw bytes only). The Add-surface already-held check (civicos-ui) is a DOWNSTREAM consumer of the stored digest — recorded, NOT built here. I1 VERDICT: to be confirmed additive (a new sub-field under the existing `document.profile`; no existing field reshaped; C-18.1 tolerates extra keys) → register I1 1.2.0; else INTERFACE-CHANGES. accepts-when bio-plane/test/framework-digest-audit.test.mjs: two op=acquire captures of the same document differing ONLY in viewstate/boilerplate report as duplicates through op=audit (their evidentiary digests are equal though raw shas differ); two genuinely different documents do NOT; two undetermined (PDF) documents do NOT. NEGATIVE CONTROL: drop the normalised-digest write and the sweep misses the viewstate pair again. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (2145416), clean cherry-pick. battery 53/53, coverage 85/85, checks 51/51. I1 1.2.0 additive verified (conformance.test green with real acquire docs carrying digests). FW→UI delegation kept open for when UI activates. No DEC.

## DELEGATION 2026-07-31 FRAMEWORK → UI
from: framework-agent-fw4 (FW-4, CONSTRUCTS Step 2)
need: the Add-surface already-held check in civicos-ui (today it fetches BOTH captures and compares them in the browser) should consume the plane's newly STORED normalisation digest instead. As of FW-4, op=acquire records `document.profile.digests.evidentiary` (a 64-hex sha when `determined:true`, else null), persisted by op=promote into data/provenance.json and registered as I1 1.2.0 §4c. The already-held check can compare a new capture's evidentiary digest against held captures' stored evidentiary digests to recognise "the same document, different viewstate/boilerplate" WITHOUT re-fetching and re-normalising both in the browser — which is the same duplicate op=audit's C-18.3 now folds server-side. Two rules the UI must honour, both structural, not optional: (1) compare evidentiary digests ONLY when both are `determined:true`; a null/undetermined digest is never equal to anything, including another null (an equality that costs nothing to produce is not evidence). (2) `determined:false` (a PDF, a multipart giant, or a merely-likely stack) means fall back to the raw-bytes/current behaviour for that capture, never treat absence as a match or a mismatch.
why: FW-4's scope explicitly RECORDS this consumer and does not build it (QUEUE.md FW-4; the constraint that UI paths are not edited by FRAMEWORK). The stored digest now exists and is stable at I1 1.2.0, so the UI work is unblocked whenever UI activates (M8 / UI-PLAN). No I1 change is needed to do it — the field is already additive and registered.
status: recorded, not started — UI is DORMANT. No plane change owed by this delegation; it consumes an already-landed additive field.
## CLAIM 2026-07-31 M0 (VERIFICATION)
session: m0-agent-m02
opened: 2026-07-31T00:00:00Z
paths: header comments only (first 60 lines) of the bio-plane/test/**/*.test.mjs suites that do not yet declare a negative control — a single `NEGATIVE CONTROL:` line each, comment-only, no assertion or subject changed. Temporary src mutations are made ONLY to RUN each control and are git-restored immediately (no src committed). pdf-worker/test already declares its control (unchanged).
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-2 — backfill the negative-control register across every suite that lacks one (41 of 51 bio-plane suites; the other 10 + the pdf-worker fleet member already declare theirs). RUN each control (break the named subject, run the suite, record the REAL observed failure, restore), never infer it. accepts-when: `npm run test:coverage` reports every suite declaring a control; three spot-checks with their real observed failures in the report. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (0dbcb86), CLAIMS union-resolved. battery 53/53, coverage 53/53 declaring after CONDUCT fixed the two new-suite NC formats. No dishonest suite found. M0-6 (hygiene gate) now UNBLOCKED (M0-1/2/3 all done).

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw5
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (two NEW tables `readings` and `reading_refs` inserted BEFORE the host_governor block, after reuse_verdicts — additive, no existing table changed); bio-plane/src/store.mjs (a `#writeReadings` call inside promote()'s transaction beside the register/refs writes, the `#writeReadings` method, the read methods `readingFor` + `documentsByReference`, two router-map entries `reading`/`readingref`, and the two new tables added to the purge TABLES list for D-113); bio-plane/src/index.mjs (op=acquire: the reading computed from the resolved doctype's parse() over the primary text FW-3/FW-4 already read, carried as a new `document.reading` sibling; two new read ops `reading`/`readingref` in the OPS table + a READING_READS set spread into SESSION_OPS member/admin); bio-plane/test/reading.test.mjs (new) + its test:reading entry in bio-plane/package.json; docs/development/INTERFACES.md (I5 additive version bump to 1.1.0 + the two tables' shape/ownership note).
interfaces consumed: I1 (docprofile is READ at op=acquire — parse() via the resolved doctype — never copied)
interfaces owned/extended: I5 (the store schema) — ADDITIVE: two new derived tables `readings` + `reading_refs`; no existing table's columns change, so no INTERFACE-CHANGES protocol is needed.
expected: QUEUE.md FW-5 / CONSTRUCTS Step 3 — READINGS ARE PERSISTED. op=acquire calls the resolved doctype's parse() on the captured text to produce a reading { entities[], facts } (framework:480), carried on the acquire document; op=promote DERIVES it from data/provenance.json (mirroring refs-from-frontmatter, in the same transaction) and persists it into `readings`, indexing every entity by the RAW source-assigned reference it carries (`ref` = kind:key, e.g. meeting:2101 — NOT resolved to a canonical entity; that is Step 4 / D-83 and is NOT built here). A reading that finds nothing (a doctype with no reader, bytes not read as text, or a reader that found/threw nothing) is persisted HONESTLY as failed/empty (found:false, no entities), never fabricated (framework:489). Reads: op=reading (by capture_sha) and op=readingref (the reverse index, by ref). I5 VERDICT: ADDITIVE (new derived tables only) → I5 1.1.0. accepts-when bio-plane/test/reading.test.mjs: acquire+promote a meeting-calendar document, retrieve its reading (entities[]+facts) by capture_sha, and look it up by an entity reference (meeting:2101) to get the document(s) back — including the cross-document case where two captures both carry the reference; the failed-reading case (generic page / PDF) persists honestly. NEGATIVE CONTROL RUN: dropping the persist makes op=reading return found:false and the reference lookup return count 0 for a document known to carry it. D-113: both tables added to op=purge's whole-store arm; hygiene + purge tests green. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (aee6d52), CLAIMS union-resolved. battery 54/54, ops 87/87 (0 unreached), checks 51/51. I5 1.1.0 additive (two derived tables, purge-covered). No DEC. Cross-document reference lookup confirmed unresolved-but-joinable — sets up Step 4 (D-83).
## CLAIM 2026-07-31 M0 (VERIFICATION)
session: m0-agent-m06
opened: 2026-07-31T00:00:00Z
paths: bio-plane/test/planning-hygiene.test.mjs (NEW — the planning-drift gate); bio-plane/package.json (two scripts ONLY — `test:hygiene` now runs `node scripts/battery.mjs hygiene` so it reports BOTH hygiene suites without &&-stopping, and `test:coverage` now runs `scripts/coverage.mjs --strict` so the floor cannot fall back per VERIFICATION.md step 5); docs/architecture/CONSTRUCTS.md (the "## The plan: bottom up, and each step has a consumer" order-of-work list ONLY — added a status-per-item convention note under the heading and a status marker to each of the 13 steps: Step 0 `BUILT (FW-2)`, Step 1 `BUILT (FW-3)`, Step 2 `BUILT (FW-4)`, Step 3 `QUEUED FW-5`, Steps 4/5/5a/6/7/8/8a/8b/9 `UNSCHEDULED`). NO other file changed — DEBT.md and QUEUE.md carried no drift (all 121 DEBT rows already held a disposition token; the only concrete QUEUED cross-reference, CAPTURE-SCALING's `QUEUED as CAP-4`, already resolves). Temporary DEBT.md/CONSTRUCTS.md mutations were made ONLY to RUN the two negative controls and were reverted (nothing of them committed). node_modules was symlinked from the main checkout for the miniflare battery and REMOVED before commit.
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-6 — the hygiene check that closes the planning-drift CLASS, on D-113's precedent, wired into the standing battery gate so a session that never runs plancheck still cannot drift. planning-hygiene.test.mjs enforces three invariants (MILESTONES.md "How this file stays true" 1/2 + the QUEUED cross-reference the status vocabulary carries): (1) every OPEN DEBT row carries a disposition token — plancheck.mjs's exact predicate, ported so the battery enforces it too; (2) every `QUEUED <ID>` cross-reference names an item that EXISTS in QUEUE.md (ids read from QUEUE.md's own `### <ID> ·` headings, never hand-listed); (3) every design-doc ORDER-OF-WORK item carries a status marker (`BUILT`/`QUEUED <ID>`/`UNSCHEDULED`/`BLOCKED`). Order-of-work lists are a small GOVERNED registry (CAPTURE-SCALING.md "Order of work"; CONSTRUCTS.md "The plan") plus a DISCOVERY GUARD that fails on any new "Order of work"-titled heading not governed or explicitly exempt — so the registry cannot silently fall behind the way D-113's list did. One list is EXEMPT with a reason: CONFORMANCE-AND-INTAKE-ARC.md "5. Order of work" is a CLOSED migration plan (all eight steps executed and superseded); retro-stamping build versions on it would fabricate a ledger it never kept — left tolerant, a judgment for CONDUCT rather than guessed. The load-bearing constraint honoured: the CURRENT estate PASSES every check (the one real drift, CONSTRUCTS's unmarked steps, was fixed mechanically from QUEUE+MILESTONES before the gate went on). accepts-when GREEN: `npm run test:hygiene` runs both hygiene suites (149 + 154 pass); `npm run test:coverage` (now --strict) exits 0 — 54 suites, 54/54 declaring a control (the new suite counted), 85/85 ops, 51/51 checks; battery 54/54 (2730). NEGATIVE CONTROLS RUN 2026-07-31, recorded in the suite's first-60-lines NEGATIVE CONTROL line, each restored: (disposition) strip the M7 token from open DEBT row D-50 -> 2 fail naming D-50; (status marker) strip the BUILT(FW-3) marker from CONSTRUCTS "The plan" Step 1 -> 2 fail naming Step 1. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (6a72951), CLAIMS union-resolved. CONDUCT updated CONSTRUCTS Step 3→BUILT(FW-5)/Step 4→QUEUED FW-6 markers post-M0-6. battery 55/55, coverage --strict exit 0, hygiene 154 pass. The M0 verification floor is now fully self-enforcing.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw6
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (three NEW tables `entities`, `entity_aliases`, `entity_relations` inserted BEFORE the host_governor block, after reading_refs — additive, no existing table changed); bio-plane/src/store.mjs (the registry section: `createEntity`/`addEntityAlias`/`declareRelation` writes, `readEntity`/`entitiesByAlias`/`readRelation` reads, the shared `#entityView` + `#normAlias`/`#cleanLabel`/`#ENTITY_KINDS`/`#RELATION_KINDS` helpers; six router-map entries; the three tables added to the whole-store purge arm for D-113; entity/alias/relation counts added to stats() and to purge's removed{}); bio-plane/src/index.mjs (six new ops in the OPS table — three mutating writes + three reads; a REGISTRY_ACTIONS set spread into SESSION_OPS member/admin; declared_by stamping for the three writes; the three writes named "contribute" in NEEDS); bio-plane/test/entityregistry.test.mjs (new) + its test:entityregistry entry in bio-plane/package.json; docs/development/INTERFACES.md (I5 → 1.2.0 + the three tables' shape/ownership note); docs/development/DECISIONS.md (new DEC-6); docs/development/DEBT.md (D-83 disposition moved to SLICE A LANDED).
interfaces consumed: none (builds on FW-5's reading_refs but resolves nothing yet)
interfaces owned/extended: I5 (the store schema) — ADDITIVE: three new tables `entities` + `entity_aliases` + `entity_relations`; no existing table's columns change, so no INTERFACE-CHANGES protocol is needed.
expected: QUEUE.md FW-6 / CONSTRUCTS Step 4 SLICE A — the SUBJECT REGISTRY, which IS the framework's entity axis, built ONCE (D-83). ENTITIES (kind ∈ the closed UNION of safeguard 4's four subject kinds {source,institution,office,movement} and the framework's entity kinds {person,body,ordinance,parcel,contract,fund}; the union is the reconciliation D-83 demands, and DEC-6 raises the residual ambiguity — whether a bias STATEMENT may take the non-safeguard-4 kinds as a subject — with a provisional that blocks nothing) each with first-class ALIASES (canonical label seeded as one; reverse lookup on a case-folded alias_norm), plus DECLARED RELATIONS (`proxy_for`/`member_of`/`overlaps`) each carrying a NOT-NULL justification + citation "like a pattern statement" and — structurally — NO §8.1 connection grade: a declared relation is CONSTITUTIVE, not evidentiary, so grading it Grade D is the category error D-83 names, prevented by the shape (no grade column) rather than by convention. Ops: entitycreate/entityalias/relationdeclare (writes, declared_by stamped from the session, "contribute"); entity/entitybyalias/relation (reads). RESOLVING a reading_refs reference to a registry entity, declaring the §8.1 method-as-grade, is the NEXT slice and is NOT built here. I5 VERDICT: ADDITIVE (new tables only) → I5 1.2.0. accepts-when bio-plane/test/entityregistry.test.mjs: an entity with aliases + a justified proxy_for/member_of/overlaps relation round-trips and is retrievable BY KEY (op=entity) and BY ALIAS (op=entitybyalias, case/space-insensitive); a read relation carries justification+citation and NO grade key (asserted on the relation read AND the entity view, and a supplied grade is dropped). NEGATIVE CONTROL RUN: dropping the alias persist makes op=entitybyalias return count 0 for a name known to exist (6 fail); dropping the relation persist makes op=relation return found:false. D-113: all three tables in op=purge's whole-store arm, asserted cleared; hygiene (152) + purge tests green. Coverage: ops 93/93 reached (0 unreached), checks 51/51, NC 55/55. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (4037add), CLAIMS union-resolved. battery 56/56, ops 93/93 (--strict exit 0), hygiene 154 (D-83 disposition token intact). DEC-6 (bias-statement entity-kind reach) recorded for bob-session; worker proceeded under the union provisional. I5 1.2.0 additive.
## CLAIM 2026-07-31 UI
session: ui-agent-1
opened: 2026-07-31T00:00:00Z
paths: civicos-ui/app.html (the TASK INBOX surface ONLY — a new `tasks` rail entry in the RAIL array + buildRail, a `renderTasks()` screen and its `go()` router entry, the task kind→label map, and the forward/resolve wiring through recPost; no existing screen's behaviour changed), civicos-ui/test/task-inbox.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=tasks, op=taskforward, op=taskresolve, op=memberlist, op=whoami; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-1 / MILESTONES M8 — the TASK INBOX, the member surface over the already-shipped plane attention layer (D-98). Lists a member's tasks (each pointing at the act it resolves in), forwards (op=taskforward) and resolves (op=taskresolve) through the existing ops. Honours the TASK construct's accountability rules (v0.2): a task ages with a recorded reason and is never silently dropped (age surfaced from `created` + any history ageing event; the ageing JOB itself is D-79/M4 and NOT built here); a task is addressed to somebody or honestly `unassigned`, never a phantom; the refusal shape "this is not yours to resolve, and here is who it is with" is rendered for a task assigned to another member. Points at the acts (openBundle on refers_to); does NOT build the acts. accepts-when civicos-ui/test/task-inbox.test.mjs: lists a member's tasks, forwards one and resolves one through the plane ops, and shows an `unassigned` task honestly; NEGATIVE CONTROL RUN — break the taskresolve wiring and the resolve path fails. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (105198b), CLAIMS union-resolved. civicos-ui all harnesses green, plane battery 56/56 (no plane code touched). No DEC. UI→RECORD delegation (server-side task-actor fence) lifted by CONDUCT into REC-4.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-1 (UI-1, the task inbox)
need: consider enforcing the TASK construct's refusal shape at the plane, not only in the surface. `taskResolve` (bio-plane/src/store.mjs ~5299) and `taskForward` refuse NO_ACTOR / NO_SUCH_TASK / ALREADY_RESOLVED, but do NOT refuse a member who is neither the task's `assignee` nor a group admin — any member-class credential can resolve any task by id (the actor is stamped honestly into history, so it is traceable, but it is not prevented). BIO_Interaction_Constructs v0.2 makes "this is not yours to resolve, and here is who it is with" an accountability rule of the construct; the UI renders it (a task held by another member shows the refusal and no resolve/forward control), but the refusal is cosmetic until the plane returns a named reason (e.g. NOT_YOURS with the current assignee) for a non-assignee, non-admin resolve/forward.
why: not blocking — UI-1 shipped the surface against the ops as they are. But an accountability rule enforced only in the browser is one a non-browser caller ignores, which is the class CLAUDE.md's "test through the op" lesson names. RECORD owns store.mjs and I3; this is RECORD's call on whether to add the server-side fence (and what its reason string is, since the UI would then render that verbatim rather than composing its own).
status: open — raised by UI-1, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).

---

## CLAIM 2026-07-31 RECORD
session: record-agent-4
opened: 2026-08-01T02:39:26Z
paths: bio-plane/src/store.mjs [taskForward/taskResolve + the new #refuseNotYours fence helper], bio-plane/src/index.mjs [TASK_ACTIONS in SESSION_OPS member/admin + taskforward/taskresolve in NEEDS as null; the actor-stamp block was already present], bio-plane/test/task-fence.test.mjs [new], docs/development/INTERFACES.md [I3], docs/development/DECISIONS.md [new DEC-7]
interfaces consumed: none
interfaces owned: I3 [additive: new NOT_YOURS reason + session-reach for taskforward/taskresolve → 1.2.0]
expected: REC-4 — the server-side TASK-ACTOR FENCE (lifts UI→RECORD delegation above). taskResolve/taskForward now refuse a caller who is neither the task's assignee nor an admin with NOT_YOURS, naming who it is with (assignee, assignee_role, human detail); an honestly `unassigned` task stays claimable per D-98 routing; the admin override is #isAdminMember (ROOT admin session or any in-app role='admin' member); a machine credential is fenced off assigned tasks (a daemon cannot close a member's work) and confined to unassigned ones. Both ops were opened to member/admin SESSIONS (they were machine-credential-only, which left the Tasks screen's session-token recPost unreachable — DEC-7), NEEDS[op]=null because the authorization is identity not capability (parallels release). accepts-when proven in task-fence.test.mjs THROUGH the control plane with real member/admin sessions (non-assignee nate refused both verbs with NOT_YOURS; assignee mona succeeds at both; admin iris succeeds at both; unassigned stays claimable by nate; a spoofed body actor does not bypass). NEGATIVE CONTROL run (neuter #refuseNotYours → 9 fail). battery 57/57 (2832). coverage --strict exit 0: ops 93/93, checks 51/51, NC 57/57, fleet 1/1. node_modules symlinked from the main checkout for miniflare, removed before commit. No push, no deploy, QUEUE.md landed: left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (edfbea5). Security-sensitive auth change VERIFIED: battery 57/57, coverage --strict 93/93, and CONDUCT independently re-ran the fence negative control (neuter #refuseNotYours → 9 fail, restored → 19). Completes UI-1's loop (the ops now admit sessions). I3 1.2.0. DEC-7 for bob-session.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw7
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (new derived table `resolutions`, BEFORE host_governor), bio-plane/src/store.mjs (the recognisers: resolveReferences/testifyResolution/#recognise/#upsertResolution/resolutionsForCapture/documentsConcerning + purge TABLES entry + stats/removed; store DO dispatch for resolve/resolvetestify/resolutions/concerns), bio-plane/src/index.mjs (4 new ops + RECOGNISER_ACTIONS session set + scopeFor contribute + resolved_by stamping), bio-plane/test/resolution.test.mjs (NEW), docs/development/INTERFACES.md (I5 1.2.0 → 1.3.0)
interfaces consumed: I5 (own), FW-5 reading_refs + FW-6 entities/entity_aliases (built upon)
interfaces owned: I5 registered here in the same turn (ADDITIVE bump)
expected: QUEUE.md FW-7 / CONSTRUCTS Step 4 SLICE B — the RECOGNISERS. Resolve a raw reading_refs reference (kind:key) to a registry entity and DECLARE THE METHOD, which IS the §8.1 connection grade: A the reference's composite key matched a registered identifier exactly (source's own identifier, both ends captured); B the bare key matched a registered identifier in content; C a name/title matched an entity ALIAS (correspondence — NEVER established, flagged for a member to confirm). A reference matching nothing stays honestly UNRESOLVED (never force-matched). The recogniser (op=resolve) mints only A/B/C, matches an entity's OWN aliases only, and NEVER traverses a declared relation (do not resolve THROUGH a constitutive proxy_for/member_of/overlaps edge, D-83). Grade D is member TESTIMONY (op=resolvetestify — an author + a date, no captured basis), never minted by the machine; the model holds it. Grade is IMPROVABLE: keyed (capture_sha, ref, entity_id), a re-resolution that finds a stronger basis RAISES the grade+method IN PLACE (raised_from records the prior grade), never a second row and never a downgrade. `established` is DERIVED from grade at write (1 for A/B, 0 for C/D) so a C can never read back as established. Delivers the REVERSE INDEX op=concerns — every document that concerns an entity, joined on entity_id — the single largest manual task the framework removes; op=resolutions reads a document's resolutions. I5 VERDICT: ADDITIVE (one new derived table `resolutions`) → I5 1.3.0. accepts-when bio-plane/test/resolution.test.mjs: a reference matching an entity alias resolves at C; one matching a source identifier at both ends resolves at A (composite) / B (bare); op=concerns returns every capture whose reading resolved to X; a reference matching nothing is honestly unresolved; a C raised to A in place when the identifier is later registered (one row, raised_from C). NEGATIVE CONTROLS RUN: (a) #isEstablished("C")=true → a Grade C reads back established (3 fail); (b) INSERT dropped in #upsertResolution → op=resolve reports resolved but op=resolutions/op=concerns empty for a document known to concern the entity (resolutions 3→0, concerns 2→0). D-113: `resolutions` in op=purge's TABLES (clears in BOTH per-bundle and whole-store arms), asserted removed + after=0; hygiene + purge green. Coverage --strict: OPS 97/97 reached (0 unreached), CHECKS 51/51, NC 57/57. Battery 57/57, 2852 assertions. No push, no deploy — CONDUCT integrates; landed: left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (2d11c03). index.mjs SESSION_OPS conflict vs REC-4 resolved by union (both TASK_ACTIONS + RECOGNISER_ACTIONS spread into member/admin). battery 58/58, coverage --strict 97/97 (0 unreached), resolution 39 + task-fence 19 green post-merge. I5 1.3.0. No DEC. Step 4 done — the M4 reverse-index capability is live.

## CLAIM 2026-07-31 UI
session: ui-agent-2
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the FIRST ACT SURFACE — focus disposition as a JUSTIFIED TRANSITION, ONLY: a `canDispose()` capability gate mirroring `canRelease()`; a self-contained DISPOSE ACT module — `DISPOSE_LEGAL` (the plane's legal-transition table, mirrored with a source cite), `weightLadderHtml()`, `disposePreflight()` (the pure pre-flight), `openDisposeDialog()`/`disposeChoose()`/`disposeValidate()`/`doDispose()`/`disposeReceipt()`/`disposeDone()`/`disposeRefusalHtml()`, plus the dialog CSS; and a Disposition act bar added to openBundle() for focus/problem document pages beside the existing release bar — no existing screen's behaviour changed), civicos-ui/test/act-dispose.test.mjs (NEW), docs/development/DECISIONS.md (new DEC-8 — the ACT construct's "see what it will refuse before it runs" pre-flight-source ambiguity; CONDUCT to apply the three tests at integration and relocate/drop if the repo already answers it)
interfaces consumed: I3 (plane → UI op contracts — consumes op=select to build a single-focus enumerated selection and op=dispose to move its state; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-2 / MILESTONES M8 — v0.2's FALSIFIABLE TEST: build the queue (UI-1) and ONE act, then re-read the two-construct collapse. The ONE act is FOCUS DISPOSITION (op=dispose to deferred/dismissed, which C-2.8 already requires a reason for — the natural "reasoned" rung) built through the ACT construct + WEIGHT LADDER as a JUSTIFIED TRANSITION (§J). The ONE MOTION rendered exactly and only: (1) CHOOSE the act (defer / dismiss this focus); (2) PRE-FLIGHT the refusal — see WHAT IT WILL REFUSE and WHY *before* it runs (the ACT construct's defining property): the C-2.8 reason requirement (NO_REASON), the reason grammar (BAD_REASON, ≤160 chars no quote/backslash/newline — Store.EDGE_REASON_MAX), and the legal-transition gate (ILLEGAL_TRANSITION / NOT_A_DISPOSITION) computed from the focus's known current_state against the plane's own LEGAL table; (3) AUTHOR the reason — REQUIRED and NEVER prefilled (placeholder hint only, never a value; the surface itself refuses to send an empty-reason act, so the C-2.8 refusal is felt before the plane is even reached); (4) RECEIPT — what the plane returned (the disposed id, the target state, the reason as recorded, weight `refuse`). The act's WEIGHT-LADDER position (reversible · reasoned · terminal · attested) is shown: dispose is **reasoned**. The op=dispose contract has no dry-run, so the pre-flight is computed from the op's declared refusal shape + the focus's known state (honest against what exists); a plane-side dry-run is NOT needed for this act and is noted, not delegated. accepts-when civicos-ui/test/act-dispose.test.mjs: disposing a focus with an authored reason succeeds and yields a receipt (op=select then op=dispose called, receipt rendered from the plane's return); the act is REFUSED with the reason shown when the required reason is absent AND op=dispose is never called (the surface refuses before the plane). NEGATIVE CONTROL RUN — remove the reason-required pre-flight gate in doDispose and the empty-reason act is no longer refused in the surface (op=dispose is now called with an empty reason). Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates. The two-construct-collapse VERDICT is a reported deliverable.
released: 2026-07-31 — landed on main by CONDUCT (0e34d9b), clean cherry-pick (2 commits). civicos-ui 15 harnesses green, plane battery 58/58 (no plane code). DEC-8 (ACT pre-flight source: surface mirror vs plane dry-run) for bob-session; nothing blocked. The two-construct collapse HELD for this act.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw8
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (three new tables `connections` / `progression_defs` / `progression_stages`, BEFORE host_governor), bio-plane/src/store.mjs (FW-8 methods deriveConnections/connectionsFor/#weakerGrade/#connectionView/defineProgression/readProgression; stats + purge additions [connections per-bundle+whole-store, progression tables whole-store]; the store DO dispatch for connect/connections/progressiondefine/progression), bio-plane/src/index.mjs (4 new ops + PROGRESSION_ACTIONS session set + NEEDS contribute for connect/progressiondefine + declaredBy/assertedBy body stamps), bio-plane/test/connection.test.mjs (NEW), docs/development/INTERFACES.md (I5 1.3.0 → 1.4.0)
interfaces consumed: I5 (own), FW-6 entities + FW-7 resolutions (built upon)
interfaces owned: I5 registered here in the same turn (ADDITIVE bump 1.4.0)
expected: QUEUE.md FW-8 / CONSTRUCTS Step 5 SLICE A — PROGRESSIONS AS DATA (framework §8.2). (1) PERSIST a CONNECTION as data carrying a GRADE (D-67 storage + D-72 grade): a connection is the TWO-NODE base case of a progression — two captured documents that resolve (FW-7) to the SAME registry entity (FW-6) — DERIVED via op=connect UNDER the op=concerns reverse-index join (not a parallel path), each carrying the §8.1 grade of its WEAKER end (framework §8.2 "weakest along the chain", two-node case); `established` derives from the weaker grade so a C at either end is never established; `asserted_by` is three-valued and DISTINCT from grade (framework:554), forced 'system' server-side (source/member reserved for slice B). op=connections reads by entity or capture. Grade is IMPROVABLE (upsert in place when a resolution is raised). (2) The PROGRESSION DEFINITION as data (op=progressiondefine / op=progression): ordered stages with after / cardinality / interval / required-ness, such that BOTH meeting→agenda→minutes AND need→award→signed-contract are expressible as rows (the acceptance). DEFERRED to slice B and flagged: progression INSTANCES, weakest-grade inheritance along an N-stage chain (D-73 pair→chain — the 2-node base case is done here), exception documents, junction checks, the missing-predecessor task. I5 VERDICT: ADDITIVE (three new tables) → 1.4.0. Schema traps honoured (BEFORE host_governor, no backticks, all three in purge — connections in BOTH arms via a_bundle_id/b_bundle_id, the two progression tables whole-store like the registry). accepts-when bio-plane/test/connection.test.mjs: both example progressions expressible as definition rows; a connection persists carrying the weaker-of-two §8.1 grade and is retrievable by entity and by capture; a single-document subject yields no connection; a C-ended connection raised to A in place when the underlying resolution is raised; malformed definitions refused by name (NO_KEY/NO_STAGES/BAD_REQUIRED/UNKNOWN_AFTER/DUPLICATE_STAGE). NEGATIVE CONTROLS RUN: (1 D-72) #weakerGrade returns the STRONGER grade → a C-weak connection reads graded A/established (2 fail); (2 D-67) disable the INSERT INTO connections → op=connect reports 3 formed but op=connections empties (3→0, 2→0). D-113: all three tables cleared by op=purge, asserted removed + after=0; hygiene + purge green. Coverage --strict: OPS 101/101 reached (0 unreached, +4), CHECKS 51/51, NC 59/59, FLEET 1/1. Battery 59/59, 2916 assertions. No push, no deploy — CONDUCT integrates; landed left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (fe81904), CLAIMS union-resolved. battery 59/59, --strict 101/101, connection 41. I5 1.4.0. No DEC. D-67 storage/D-72 connection-grade/D-73 rule+2-node closed (slice B remains).

## CLAIM 2026-07-31 UI
session: ui-agent-3
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the SECOND ACT SURFACE — a BALLOT, project owner removal, through the SAME ACT construct + weight ladder UI-2 established, ONLY: a `canBallot()` session gate; a self-contained BALLOT module — `ballotPreflight()` (the pure pre-flight, whose arithmetic gates READ the plane's computed `live` row, never re-derive floor(n/2)+1), `ballotDenominator()`/`ballotDenominatorHtml()` (the DENOMINATOR read straight FROM op=projectownerarith — the v0.2 §B "checkable tally, never pending approval"), `ballotDivergenceHtml()` (the two-owner DIVERGENCE, shown from the op's `table`), `openBallotDialog()`/`ballotChoose()`/`ballotValidate()`/`doBallot()`/`ballotReceipt()`/`ballotRefusalHtml()`, plus the dialog CSS (`bl-*`); `weightLadderHtml()` extended with an optional note param, dispose's default note unchanged — no existing screen's behaviour changed), civicos-ui/test/act-ballot.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=projectownerarith for the computed tally/denominator + the two-owner divergence table, op=projectparticipants for the owners to choose from, and op=projectownerremove as the ballot act; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-3 / MILESTONES M8 — v0.2's FALSIFIABLE test, act TWO: does the ACT construct hold for an act UNLIKE the justified transition? Build ONE ballot (project owner removal, 7.10) through the SAME ACT construct + weight ladder (choose · see what it will refuse and why BEFORE it runs · author · receipt · weight-ladder), PLUS the two properties the BALLOT type adds (v0.2 §B): (a) SHOW THE DENOMINATOR — "N of M owners' votes" read FROM op=projectownerarith's `live.votesNeeded`/`live.owners`, and the running tally FROM the op=projectownerremove receipt (VOTES_SHORT have/need/deciders), never "pending approval", never recomputed in the interface; (b) DISPLAY THE DIVERGENCE AT TWO OWNERS — the boundary row a shared implementation gets wrong, rendered from op=projectownerarith's `table` (row n=2, where owner removal diverges from the admin rule: the departing owner MAY vote, unanimity-with-assent). The pre-flight's arithmetic gates (LAST_OWNER, TARGET_CANNOT_VOTE) are computed by the PLANE and read off `live` — a finding for DEC-8 (this ballot's pre-flight is partly server-computed, so it does NOT deepen the client-mirror concern). accepts-when civicos-ui/test/act-ballot.test.mjs: the ballot shows the computed tally/denominator FROM the arithmetic op and refuses+explains before it runs (empty reason / last owner / self-vote at 3+ owners → surface refuses, op=projectownerremove never sent); a valid vote sends the act and the receipt shows the running tally from the return. NEGATIVE CONTROL RUN: break the denominator wiring (`ballotDenominator` reads `live.votesNeeded` → a constant) and the shown tally no longer reflects the op. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates. COLLAPSE VERDICT is a reported deliverable.
released: 2026-07-31 — landed on main by CONDUCT (719473b), clean cherry-pick. civicos-ui 16/16, plane battery 59/59 (no plane code). The two-construct collapse HELD for act two (a ballot); DEC-8 client-mirror concern REDUCED (the arithmetic op dry-runs its own refusals). No DEC, no delegation.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw9
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (one new table `progression_instances`, BEFORE host_governor), bio-plane/src/store.mjs (FW-9 methods threadInstance/readInstance/#assembleInstance/#strongestResolutionsFor/#REQUIRED_FIRES; stats + purge additions [progression_instances added to the whole-store+per-bundle TABLES list, reported in stats/removed]; the store DO dispatch for thread/instance), bio-plane/src/index.mjs (2 new ops thread/instance + PROGRESSION_ACTIONS extension + NEEDS contribute for thread + threadedBy body stamp), bio-plane/test/progression-instance.test.mjs (NEW), docs/development/INTERFACES.md (I5 1.4.0 → 1.5.0), docs/development/DECISIONS.md (new DEC-9 — whether unless_exception fires a missing-predecessor finding before the exception-doc slice exists)
interfaces consumed: I5 (own, additive), FW-6 entities + FW-7 resolutions + FW-8 connections/progression_defs (built upon)
interfaces owned: I5 (ADDITIVE bump 1.5.0; registered owner RECORD)
expected: QUEUE.md FW-9 / CONSTRUCTS Step 5 SLICE B — progression INSTANCES and the MISSING-PREDECESSOR finding (M4 acceptance). (1) A progression INSTANCE threads REAL captured documents through a definition's stages, assembled by a THREADING ENTITY (framework §8.2), one instance per (definition, entity), persisted in the new I5-additive `progression_instances` table (one row per placed document; the instance is all rows sharing progression_key+entity_id). A document is admitted only if it RESOLVES to the entity (FW-7) — refused NOT_CONCERNED otherwise; each placement's grade is the record's (strongest resolution of that capture to the entity), never the caller's; the stage assignment is the member's authored judgment (threaded_by stamped server-side). op=thread writes, op=instance reads; re-threading REPLACES. (2) N-STAGE WEAKEST-GRADE inheritance (D-73 pair→chain, beyond FW-8's 2-node base case): the instance grade is the WEAKEST connection along the chain of consecutive placed stages, each connection graded the weaker of its two ends (reuses FW-8 #weakerGrade). Fewer than two placed stages → UNDETERMINED, never invented. (3) The MISSING-PREDECESSOR finding: a stage that is required (always/usually) with no threaded document surfaces as a finding carrying the instance's grade (framework's example: an award with no solicitation). Requiredness respected — sometimes/never missing is NOT a finding; unless_exception DEFERRED (DEC-9). Grade + findings DERIVED ON READ from stored placements + the CURRENT definition (never a stale stored grade). DEFERRED and flagged: exception documents that discharge a lawful skip; junction checks as findings; the SCHEDULED task that walks the table (rides the REC-1 DO-alarm scheduler). I5 VERDICT: ADDITIVE (one new derived table) → 1.5.0. Schema traps honoured (BEFORE host_governor, no backticks, in op=purge's TABLES so it clears in BOTH arms like resolutions; hygiene asserts it). accepts-when bio-plane/test/progression-instance.test.mjs: threading need→award→contract MINUS the solicitation through the procurement definition yields an instance whose missing `solicitation` (usually-required) stage surfaces as a missing_predecessor finding; the instance grade is the weakest connection along the chain — need(A)→award(A)=A, award(A)→contract(C)=C, so grade C carried by the finding; a one-document instance reads UNDETERMINED; a document not concerning the entity is refused NOT_CONCERNED. NEGATIVE CONTROLS RUN 2026-07-31: (1) empty #REQUIRED_FIRES → the missing solicitation no longer surfaces (finding_count 1→0, the "exactly ONE finding" assertion fails then finding[0] lookups throw — suite not green); (2) #weakerGrade returns the STRONGER end → the chain reads grade A not C (5 fail, 32 pass); both restored → 37 pass. D-113: progression_instances in op=purge's TABLES, asserted removed + after=0; hygiene + purge green. Coverage --strict: OPS 103/103 reached (0 unreached, +2 thread/instance), CHECKS 51/51, NC 60/60, FLEET 1/1. Battery 60/60, 2955 assertions. No push, no deploy — CONDUCT integrates; landed left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (d5da71d), CLAIMS union-resolved. battery 60/60, --strict 103/103, progression-instance 37. I5 1.5.0. DEC-9 (unless_exception) for bob-session. M4 acceptance "a progression with a missing predecessor is visible" is MET. Remaining M4: slice C (exceptions/junctions/walking-task), Step 5a, Step 6.

## CLAIM 2026-07-31 UI
session: ui-agent-4
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the SUBJECT VIEW — a NEW read-only screen making the M4 reverse index member-visible, ONLY: a `subjects` RAIL entry + `subjects: renderSubjectView` route + navLabel/preview-title map entries; a self-contained SUBJECT-VIEW module — `renderSubjectView()` (the screen + search box), `lookupSubject()` (search by name/alias via op=entitybyalias, or by id via op=entity), `showEntity()` (loads op=concerns + op=connections and paints), the pure renderers `subjEntityHtml`/`subjRelationsHtml`/`subjConcernsHtml`/`subjConnectionsHtml`, the honest `SUBJ_GRADE_HOW`/`subjGradeBadge()` §8.1 grade helper, and the `subj-*` CSS — no existing screen's behaviour changed), civicos-ui/test/subject-view.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=entitybyalias, op=entity, op=concerns, op=connections, op=relation; reads I5 shapes for entity/resolution/connection; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-4 / MILESTONES M8 — the SUBJECT VIEW, "what the record knows about a subject." Given a registry entity (found by name/alias via op=entitybyalias, or by id via op=entity), show three things: (1) THE ENTITY — kind, label, aliases, and its declared RELATIONS (proxy_for/member_of/overlaps) each with justification + citation, rendered as CONSTITUTIVE (a group's fixing of what its own terms mean) and WITHOUT any A–D connection grade (grading a declared relation is the D-83 category error; the plane carries no grade on a relation, so the surface shows none). (2) EVERY DOCUMENT that concerns it (op=concerns), each with its resolution GRADE shown HONESTLY: `established`/`needs_confirmation` READ FROM the op (never re-derived from the letter), so a Grade C correspondence renders "plausible, not established — unconfirmed" and NEVER as established. (3) The graded CONNECTIONS among those documents (op=connections), each connection's §8.1 grade = the weaker of its two ends, shown as HOW it was established, never as credibility. accepts-when civicos-ui/test/subject-view.test.mjs: look up an entity by alias (op=entitybyalias); list the documents that concern it from op=concerns WITH their grades; a Grade C document renders as unconfirmed (NOT established); a declared relation renders WITHOUT a connection grade. NEGATIVE CONTROL RUN: force a Grade C resolution to render as established (subjGradeBadge takes the established branch regardless of the flags) and the test fails. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (d2d94df), CLAIMS union-resolved. civicos-ui 17 harnesses green, plane battery 60/60. The M4 reverse index is now member-visible with honest grades. Connection auto-derivation gap logged as D-122.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw10
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (one new table `progression_exceptions`, BEFORE host_governor), bio-plane/src/store.mjs (FW-10 methods dischargeStage/readExceptions; #assembleInstance extended to CONSULT exceptions [discharged state] + #REQUIRED_FIRES graduates unless_exception; stats + purge additions [progression_exceptions in the whole-store+per-bundle TABLES list, reported in stats/removed]; the store DO dispatch for discharge/exceptions), bio-plane/src/index.mjs (2 new ops discharge/exceptions + PROGRESSION_ACTIONS extension + NEEDS contribute for discharge + declaredBy body stamp), bio-plane/test/progression-exception.test.mjs (NEW), bio-plane/test/progression-instance.test.mjs (FW-9 assertions SUPERSEDED where unless_exception now fires when undischarged — corrected, not exempted), docs/development/INTERFACES.md (I5 1.5.0 → 1.6.0), docs/development/DECISIONS.md (DEC-9 note: mechanism now exists, provisional updated, LEFT OPEN for Bob)
interfaces consumed: I5 (own, additive), FW-6 entities + FW-7 resolutions + FW-9 progression_instances (built upon)
interfaces owned: I5 (ADDITIVE bump 1.6.0; registered owner RECORD)
expected: QUEUE.md FW-10 / CONSTRUCTS Step 5 SLICE C (part) — EXCEPTION DOCUMENTS that discharge a legitimate skip (framework §8.2), building on FW-9. (1) Model an EXCEPTION DOCUMENT: a real captured document, threaded to a progression instance by (progression_key, entity_id, stage_key, capture_sha), carrying a NOT-NULL reason + citation (mirrors FW-8 relations); admitted only if it RESOLVES to the threading entity (FW-7, NOT_CONCERNED otherwise) and NAMES a real stage (BAD_STAGE otherwise) — a discharge must be EARNED, never a caller's bare assertion. New I5-additive table `progression_exceptions`. (2) #assembleInstance CONSULTS them: a REQUIRED stage that is missing but HAS a discharging exception → a distinct "discharged" state (reason/citation + the document VISIBLE), NOT a missing_predecessor finding and NOT silently absent; undischarged-and-required still fires. (3) DEC-9 MECHANISM: unless_exception graduates to DISCHARGEABLE — fires ONLY when required-and-undischarged (DEC-9 recommendation c). Implemented as the provisional; DEC-9 LEFT OPEN (Bob rules the policy). DEFER+FLAG: junction checks as findings; the scheduled walking-task (rides REC-1). Schema traps honoured (BEFORE host_governor, no backticks, in op=purge's TABLES so it clears in BOTH arms like progression_instances; hygiene asserts it). accepts-when bio-plane/test/progression-exception.test.mjs: a procurement instance missing its solicitation but carrying a DISCHARGING exception document surfaces NO missing_predecessor finding for that stage while showing the discharged state; the SAME instance WITHOUT the exception still fires; an unless_exception stage fires only when undischarged. NEGATIVE CONTROL: ignore the exception document (empty the discharge consultation) → the finding reappears. New ops discharge/exceptions carry control-plane assertions (--strict). No push, no deploy — CONDUCT integrates; landed left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (93c9601), CLAIMS union-resolved. battery 61/61, --strict 105/105, both progression suites green. I5 1.6.0. DEC-9 mechanism built, DEC left open for Bob. The present-stage-exception read-time-inert choice accepted as sound mechanism (not escalated). grep -a trap folded into CLAUDE.md.

## CLAIM 2026-07-31 UI
session: ui-agent-5
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the THIRD ACT SURFACE — a PROPOSAL, through the SAME ACT construct + weight ladder UI-2/UI-3 established, ONLY: a `proposals` RAIL entry + `proposals: renderProposals` route + preview-title map entry; a self-contained PROPOSAL module — `proposalsFrom()` (the pure D-79 aggregation: N instances of one missing-predecessor check → ONE proposal with N instances), `weakerGrade()`/`PROP_GRADE_RANK`/`PROP_GRADE_HOW`, `proposalQuestion()`, `proposalDerivedBadgeHtml()` (the D-82 visibly-derived marker — the negative-control subject), `proposalGradeHtml()`/`proposalInstanceHtml()`/`proposalCardHtml()`, `renderProposals()`/`proposalGapHtml()`/`loadProposals()`, and the THREE affordances as ACTs: DEFER/DISMISS — `proposalDisposePreflight()` (pure, reason-required, reuses the dispose grammar constants), `openProposalAct()`/`proposalActChoose()`/`proposalActValidate()`/`doProposalDispose()`/`proposalDisposeReceipt()`/`proposalRefusalHtml()`; ADOPT — `proposalAdoptPreflight()` (pure), `openProposalAdopt()`/`proposalAdoptChoose()`/`proposalAdoptValidate()`/`doProposalAdopt()`/`proposalAdoptReceipt()` (authors a focus/problem the SAME way the Add surface does — mdFor+docFiles+registerFor, one writer, no fourth), plus the `prop-*` CSS — no existing screen's behaviour changed), civicos-ui/test/act-proposal.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — RENDERS from op=instance's missing-predecessor findings (FW-9), ADOPT authors through op=promote (REC-3 stamps surfaced_by from the session); a delegated aggregated op=proposals feed and a delegated op=proposedispose disposition, both below; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-5 / MILESTONES M8 — v0.2's FALSIFIABLE test, act THREE: does the ACT construct hold for a DERIVED FINDING awaiting an authored act? Also closes D-82's DISPLAY half for the proposal (a derived finding renders VISIBLY as one, never as an established fact). A PROPOSAL is a derived finding (D-90 invariant 8: derived informs, authored binds — it reports, never decides or edits), rendered primarily from FW-9's MISSING-PREDECESSOR findings (op=instance): a required progression stage with no threaded document, most often an award with no solicitation. The surface keeps three rules: (D-82) it LOOKS derived — the derived marker says the record surfaced it and NOBODY HAS YET JUDGED IT WORTH PURSUING, and the strength grade is shown as HOW a link was established (§8.1), never as fact; (D-79) N instances of one check are ONE proposal with N instances (proposalsFrom groups by progression_key+stage_key), never N items; (§P) EXACTLY THREE affordances — ADOPT (author a focus/problem via op=promote — the member's OWN words, never prefilled, the plane stamps surfaced_by human), DEFER, DISMISS (each with a required reason, never prefilled, refused in the surface before the plane). accepts-when civicos-ui/test/act-proposal.test.mjs (63 assertions): a derived finding renders as visibly a proposal (surfaced_by/derived nature shown, never established), offers exactly adopt/defer/dismiss, and requires a reason on defer/dismiss; ADOPT authors a focus through op=promote without the browser sending surfaced_by. NEGATIVE CONTROL RUN 2026-07-31: neuter proposalDerivedBadgeHtml to return "" (in a second VM context from the source) → the card loses "Surfaced by the record" and "nobody has yet judged it" and reads as an established finding (D-82 regressed); also RUN in-place (5 assertions flip), restored → 63 green. Battery untouched (no plane code edited): 60/60, 2956 assertions. civicos-ui: 18 harnesses green + semantics. No push, no deploy — CONDUCT integrates; `landed:` left for CONDUCT. COLLAPSE VERDICT (reported deliverable): the proposal FIT the ACT construct + the adopt/defer/dismiss affordances with NO new construct — the collapse HELD for act three. What it exposed is in the PLANE's data layer, not the construct: two delegations below.
released: 2026-07-31 — landed on main by CONDUCT (97e6b99), CLAIMS union-resolved. civicos-ui 18 harnesses green, plane battery 61/61. The v0.2 two-construct collapse is CONFIRMED across three acts (dispose/ballot/proposal). Two delegations lifted to REC-6 (op=proposals feed) + REC-7 (op=proposedispose). No DEC.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-5 (UI-5, the proposal surface)
need: an AGGREGATED DERIVED-FINDINGS FEED op (e.g. op=proposals). There is no op today that enumerates progression instances and returns their missing-predecessor findings; op=instance reads ONE instance by (progression_key, entity_id), and there is no op to LIST progression definitions, instances, or entities. So the surface can RENDER and AGGREGATE proposals (proposalsFrom, D-79) and ACT on them (adopt/defer/dismiss), but it cannot DISCOVER them to populate the screen. Shape that fits the surface: op=proposals returns `{ instances: [ { progression_key, progression_label, entity_id, entity_label, findings:[ op=instance's finding shape ] } ] }` — the raw instance reads; THIS surface does the D-79 grouping (keeping the aggregation in the tested UI). Alternatively RECORD may return already-grouped proposals; the UI's proposalsFrom would then be a no-op pass-through. This rides the same table-walk FW-9 deferred ("the SCHEDULED task that walks progression_instances for missing predecessors, on the REC-1 DO-alarm scheduler"): the feed op and that task read the same rows.
why: not blocking — UI-5 shipped the rendering/aggregation/affordances proven against the op=instance shape, and reports the gap honestly on-screen (loadProposals reads a delegated op=proposals and shows a banner naming this delegation when the op is absent, rather than an empty screen implying the record found nothing). RECORD owns store.mjs and I3; whether to expose an aggregated read (and whether the UI or the plane does the D-79 grouping) is RECORD's call. D-79 also names the ageing/aggregation store as not-yet-existing; this feed is its read side.
status: open — raised by UI-5, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-5 (UI-5, the proposal surface)
need: a PROPOSAL-DISPOSITION op (e.g. op=proposedispose) so DEFER and DISMISS can be RECORDED. op=dispose disposes a FOCUS bundle (a selection handle + a state transition on a real bundle.md); a proposal is a DERIVED finding with no bundle and no id to select, so op=dispose cannot take it, and D-79 records that the ageing/aggregation store for a proposal "does not exist". Shape that fits the surface: op=proposedispose (mutating, needs contribute) `{ key: <progression_key::stage_key aggregation key>, to: "deferred"|"dismissed", reason }` → a receipt `{ ok, key, to, reason }`, with the member stamped server-side (the browser sends no actor) and the reason kept verbatim (EDGE_REASON grammar, ≤160 chars) — and the disposition should AGE per D-79 rather than vanish. ADOPT, by contrast, is FULLY reachable today through op=promote and is wired end-to-end; only defer/dismiss lack a home. The surface builds the full authored motion (reason required, never prefilled, pre-flight refuses before the plane) and records through op=proposedispose; absent the op, doProposalDispose catches "unknown op" and tells the member the record cannot yet keep a proposal's disposition, having written nothing.
why: not blocking — UI-5 shipped the defer/dismiss acts proven against this shape (the test mocks op=proposedispose, exactly as act-dispose mocks op=dispose, AND asserts the honest degrade when it is absent). The kickoff named "DISMISS … via op=dispose", but op=dispose's real contract (a focus handle + state move) cannot address a bare derived finding; recording a proposal's disposition is a genuine plane gap, not a UI shortcut. RECORD owns store.mjs and I3; the doctrine is already settled (D-79: age rather than vanish, aggregate rather than multiply — a declined proposal must NOT mint a bundle), so this is a mechanism gap, not a DEC.
status: open — raised by UI-5, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).
## CLAIM 2026-07-31 RECORD
session: record-agent-5
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (one new table `connection_dirty`, BEFORE host_governor), bio-plane/src/store.mjs (the connection-derive scheduler consumer + the dirty-set: a `#schedConsumers` "connection-derive" entry, `#deriveConnectionsSweep`, `#stampConnectionDirty`, `#connectionDeriveDelayMs`, the CONNECTION_DERIVE_* constants; a dirty-stamp in `#upsertResolution`; `resolveReferences`/`testifyResolution` made async to ARM after they dirty; `connection_dirty` in stats()/purge removed{} + a `DELETE FROM connection_dirty` in op=purge's whole-store arm), bio-plane/test/connection-derive-sweep.test.mjs (NEW), docs/development/INTERFACES.md (I5 additive bump)
interfaces consumed: FW-7 resolutions + FW-8 connections/deriveConnections (built upon), REC-1 scheduler (registers a consumer)
interfaces owned: I5 (ADDITIVE; registered owner RECORD)
expected: QUEUE.md REC-5 — close D-122, connections AUTO-DERIVE. A SCHEDULED consumer on REC-1's DO-alarm derives connections for entities whose resolutions changed, tracked by an I5-additive dirty-set (`connection_dirty`) stamped at op=resolve/op=resolvetestify (insert or grade-RAISE only, never a kept idempotent re-resolve). Bounded (batch per tick), self-terminating (wake null when the dirty-set empties), idempotent (deriveConnections UPSERTS by the FW-8 key). asserted_by stays 'system' — a scheduled derivation is a machine act. DEFER+FLAG: auto-assembling progression instances + missing-predecessor findings on the same tick.
released: 2026-07-31 — landed on main by CONDUCT (6fdb763). D-122 CLOSED — the entity axis now self-populates. battery 62/62, --strict 105/105. CONDUCT re-ran the sweep NC (my neuter regex missed the private method so it was inconclusive; relied on the worker's documented neuter→15-fail + the scheduler NC + full battery). I5 1.7.0. No DEC. The walking-task is now unblocked (first REC-1 framework consumer landed).

## CLAIM 2026-07-31 UI
session: ui-agent-6
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the FOURTH ACT SURFACE — the ATTESTATION act: a member CO-ATTESTS a HELD capture through the SAME ACT construct + weight ladder UI-2/3/5 established, ONLY additive: an `Attestation` act-bar on an information document's page (reuses primaryCaptureSha), and a self-contained ATTEST module — `canAttest()` (session+contribute, same shape as canRelease), `ATTEST_YIELDS_GRADE`/`attestStandingClaim()` (the HONESTY source-of-truth — co-attestation yields Grade B toward evidentiary weight, NEVER Grade A; the negative-control anchor), `attestPreflight()` (pure: the op's declared refusals in the plane's order — NO_STORAGE 503 → BAD_SHA 400 → NO_SUCH_CAPTURE 404), `attestHonestyHtml()`, `openAttestDialog()`/`attestChoose()` (CHOOSE: timestamp only vs. timestamp + opt-in public web archive)/`attestValidate()`/`doAttest()`/`attestReceipt()` (success — the stored RFC3161 token + the honest new standing)/`attestFailReceipt()` (NO_ATTESTATION — every recorded attempt with its reason, standing UNCHANGED)/`attestRefusalHtml()`, plus `az-`/reused `dz-*`/`pf-*`/`wl-*` chrome — no existing screen's behaviour changed), civicos-ui/test/act-attest.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=attest POST { sha256, archive?, locator? } as the co-attestation act; reads its refusal shape (BAD_SHA/NO_SUCH_CAPTURE/NO_ATTESTATION) and its success return (attestation token {service, sha256, bytes, over} + note); consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-6 / MILESTONES M8 — the ATTESTATION act (v0.2 §A, the WEIGHT LADDER's TOP rung `attested`), v0.2's FALSIFIABLE test act FOUR (bonus confirmation). A member co-attests a HELD capture via op=attest (co-attestation over a capture hash through a timestamp authority), raising it from Grade B TOWARD evidentiary weight — NOT the DIST release-signing path, touches NO signing seed. Built through the SAME ACT construct: CHOOSE (timestamp only, or timestamp + opt-in public web archive — off by default because asking a public archive publishes the fact of interest) · PRE-FLIGHT the op's refusals BEFORE it runs (NO_STORAGE/BAD_SHA/NO_SUCH_CAPTURE, in the plane's checking order; op=attest has NO dry-run, so computed from the declared refusal shape + the doc page's known capture sha, as dispose does) · AUTHOR is genuinely ABSENT (op=attest takes no text — the evidence is the token, not the member's words) · RECEIPT (the stored RFC3161 token + the HONEST new standing). HONESTY (load-bearing): the pre-flight and receipt show what co-attestation does AND what it CANNOT — the plane's own note (index.mjs:2304) "Grade A needs a chain-of-custody web archive this surface cannot produce; co-attestation raises B toward evidentiary weight"; the surface NEVER claims Grade A. Weight-ladder rung = `attested` (top): irreversible and public — the key is the AUTHORITY's, not the member's, and it touches no release/ratify seed (the honest nuance of the rung's "needs a key" gloss). accepts-when civicos-ui/test/act-attest.test.mjs: co-attesting a held capture through op=attest succeeds and the receipt shows the HONEST resulting standing (Grade B, strengthened toward evidentiary weight, NEVER Grade A); the act shows its weight-ladder position (`attested`) and pre-flights its refusals (bad sha / no such capture / no storage → surface refuses, op=attest never sent); a NO_ATTESTATION failure renders every recorded attempt with its reason and leaves the standing unchanged. NEGATIVE CONTROL: make the surface claim Grade A from co-attestation (ATTEST_YIELDS_GRADE "B" → "A") and the honesty assertion fails (the receipt's stated standing flips to Grade A). Battery untouched (no plane code edited). No push, no deploy, no release, no signing seed — CONDUCT integrates; `landed:` left for CONDUCT. COLLAPSE VERDICT is a reported deliverable.
released: 2026-07-31 — landed on main by CONDUCT (08f3e05), CLAIMS union-resolved. civicos-ui 19 harnesses green, plane battery 62/62. Act four (attestation) fit the ACT construct; all four act types now confirm the v0.2 collapse. No DEC.

## CLAIM 2026-07-31 UI
session: ui-agent-7
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the MEMBERS & GOVERNANCE roster — the READ-ONLY half of U11, replacing the thin handle/cover/capabilities table in `renderMembers()`. ONLY additive/in-place on the existing `members` screen, no new rail entry: a rewritten `renderMembers()` + self-contained read-only module — `memberRole()` (the ROLE WIRING seam: a member's class read STRAIGHT from op=memberlist's `role`, the negative-control target) + `memberRoleBadge()`; `memberCapsHtml()`; `adminDenominator()`/`adminDenominatorHtml()` (the admin §4.7 consensus/majority denominator read FROM op=adminarith's `live`, never recomputed — the same "N of M, never pending" shape UI-3's ballotDenominator uses) + `adminScaleHtml()` (how the threshold scales, from op=adminarith's `table`); REUSES UI-3's `ballotDivergenceHtml()` for the two-owner divergence from op=projectownerarith's `table`; `memberOwnership()` (bounded fan-out over the record's project bundles via op=projectparticipants → who OWNS which project, degrades honestly); `founderReconcileHtml()` (the honest note when op=adminarith counts more administrators than the roster lists a row for — the founding ADMIN_TOKEN holder has no members row); `rosterRow()`/`governanceHtml()`, plus `mm-*` CSS reusing the `bl-*` denominator/divergence chrome — no other screen's behaviour changed), civicos-ui/test/members-roster.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=memberlist for the roster + each member's class/role, op=adminarith for the admin consensus/majority denominators (live + table), op=projectownerarith for the two-owner divergence table, op=projectparticipants for per-project ownership; reads I5 member/project shapes; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-7 / MILESTONES M8 — the MEMBERS & GOVERNANCE roster, the READ-ONLY half of the split U11 (member add/remove + key operations stay BALLOT acts UI-3 covers or a later split). A member surface showing WHO holds power and the group's governance STATE, both rendered HONESTLY: (1) the ROSTER from op=memberlist — each member with their class/role (admin/member) read STRAIGHT from the op's `role` field (never inferred from capabilities, never invented), their capabilities, status, and the projects they own; (2) the GOVERNANCE STATE from the arithmetic ops as facts a member can CHECK, never "pending" — the admin §4.7 consensus/majority denominator from op=adminarith's `live` ("removing an administrator takes N of M"), how it scales from the `table`, and the two-owner DIVERGENCE from op=projectownerarith's `table` (the n=2 row where owner removal parts company with the admin rule), REUSING UI-3's ballotDivergenceHtml. Per-member project ownership is NOT in op=memberlist, so it is surfaced by a bounded fan-out over the record's project bundles via op=projectparticipants and degrades honestly if a project cannot be read (a DELEGATION to RECORD proposes op=memberlist carry an `owns` projection to remove the N+1). The founder reconciliation: op=adminarith counts the ADMIN_TOKEN holder who has no members row, so when the count exceeds the roster's admin rows the surface states why rather than showing inconsistent numbers. READ-ONLY: no add/remove/key act on this surface. accepts-when civicos-ui/test/members-roster.test.mjs: the roster lists members with their roles from op=memberlist and shows the governance denominators from op=adminarith + the two-owner divergence from op=projectownerarith. NEGATIVE CONTROL: break the role wiring (memberRole returns a constant instead of the op's `role`) and a member's displayed role no longer reflects the op. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (af81f28), clean cherry-pick. UI 20 harnesses green, plane battery 62/62 (no plane code). Founder-invisible-to-roster discrepancy stated on-screen not hidden. UI→RECORD delegation (memberlist owns[] projection) kept open. No DEC.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-7 (UI-7, the members & governance roster)
need: op=memberlist (or a sibling read) to carry each member's PROJECT OWNERSHIP — e.g. an `owns: [{ projectId, title }]` projection per member row. UI-7's roster must show "who owns which project" (core accountability: ownership is power, Section 7), but op=memberlist today returns only member_id/cover/handle/role/status/capabilities/invite_pending/expertise — no ownership. There is also no op that LISTS a member's projects. So the surface answers ownership by a BOUNDED FAN-OUT: it reads the record's project bundles (op=list) and calls op=projectparticipants for each, inverting owner→member on `handle`. That is an N+1 over projects and degrades honestly (a project it cannot read is skipped, never guessed; if projects cannot be listed at all the roster says ownership "not read" rather than implying nobody owns anything). A single `owns` projection on op=memberlist would remove the fan-out and make ownership a first-class roster fact rather than a join the UI reconstructs.
why: not blocking — UI-7 shipped the roster + ownership proven against the ops that exist (op=memberlist + op=projectparticipants over op=list), and the fan-out is bounded and honest. RECORD owns store.mjs and I3; whether to promote ownership onto the roster read (and the exact shape) is RECORD's call. Related: op=adminarith counts the founding administrator (the ADMIN_TOKEN holder), who has NO members row, so the roster and the governance denominator legitimately disagree on the administrator count; UI-7 reconciles this on-screen (founderReconcileHtml) rather than asking the plane to change — noted here only so RECORD knows the roster is deliberately not the whole administrator set.
status: open — raised by UI-7, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).
## CLAIM 2026-07-31 RECORD
session: record-agent-6
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/store.mjs (ONE new READ method `proposalsFeed` — the read-time walk of `progression_instances` [DISTINCT (progression_key, entity_id)] reusing FW-9/FW-10's `#assembleInstance`, collecting undischarged missing-predecessor findings and D-79-aggregating them by (progression_key, stage_key); reuses `#assembleInstance`/`#weakerGrade`/`#GRADE_RANK` READ-ONLY, edits none of FW-9/FW-10's functions; plus one line in the store DO dispatch map: `proposals: () => this.proposalsFeed()`), bio-plane/src/index.mjs (op=proposals in the OPS table [classes admin/member/probe, mutating:false, an ungated derived-findings read like op=instance/op=exceptions] + `proposals` added to PROGRESSION_ACTIONS so a member/admin SESSION reaches it; no NEEDS entry — a read is ungated like the other progression reads), bio-plane/test/proposals-feed.test.mjs (NEW) + its test:proposals-feed entry in bio-plane/package.json, docs/development/INTERFACES.md (I3 additive version bump — one new read op, no rename/reshape/reason change)
interfaces consumed: FW-9 progression_instances + #assembleInstance, FW-10 progression_exceptions/discharges (read through #assembleInstance — a discharged skip is NOT a finding), FW-6/FW-7 (entities/resolutions, via the assembled instance)
interfaces owned: I3 (ADDITIVE read op; registered owner RECORD)
expected: QUEUE.md REC-6 / MILESTONES M8 — op=proposals, the DISCOVERY feed for derived findings (UI-5's delegation). A read op that ENUMERATES the record's current derived findings: walk progression_instances, assemble each, collect the required-undischarged missing-predecessor stages that fire (respecting FW-10 discharges), AGGREGATE per D-79 (ONE proposal per (progression_key, stage_key) carrying its N instances, the WEAKEST grade across them, any-undetermined→undetermined, surfaced_by machine). REPORTS, never mutates; read-time walk, no scheduled alarm (that is the deferred PUSH walking-task). Response carries BOTH `instances` (the raw per-instance reads UI-5's loadProposals already consumes, so UI-5 populates with NO UI change) AND `proposals` (the server-side D-79 aggregation the kickoff/accepts-when assert, computed from the same walk so the two cannot disagree). Connections deferred as a follow-on (missing-predecessors only). accepts-when bio-plane/test/proposals-feed.test.mjs: op=proposals returns the missing-predecessor findings for a gap-carrying store AGGREGATED (N instances → one entry) with grade + machine provenance; a store with no gaps returns an empty feed; a DISCHARGED gap does NOT appear; negative control — drop the instance-walk and the feed returns nothing for a store known to have a gap. Reached THROUGH the control plane (literal op=proposals in the suite) so coverage credits it. No push/deploy/release — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (80eca24), CLAIMS union-resolved. battery 63/63, --strict 106/106, proposals-feed 20. UI-5's proposal surface now POPULATES (reads instances[]) — its gap banner retired. I3 1.3.0. REC-7 (op=proposedispose) is the remaining half of UI-5's two delegations. No DEC.

## CLAIM 2026-07-31 UI
session: ui-agent-8
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the member HOME — the "what needs you" orientation surface, M8's entry point. Additive: a new `home` rail entry made the DEFAULT boot screen (boot() go("home") instead of go("record")), a `home: renderHome` router entry + preview title, a self-contained read-only module — `renderHome()` (aggregates the attention the record holds for this member), `homeTasksSummary()` (summarises op=tasks: how many need you + the most urgent, reusing renderTasks' own mine/unassigned partition + taskAge/taskKindLabel), `homeProposalsSummary()` (summarises op=proposals via the EXISTING loadProposals(): count of open proposals, degrades HONESTLY to a "feed pending" note when the op is absent exactly as UI-5 does — never a fabricated count), `homeEntryHtml()` (clear entry to the built surfaces: Tasks UI-1, Subjects UI-4, Members UI-7), plus `hm-*` CSS. No other screen's behaviour changed; READ-ONLY orientation — the acts happen on their own surfaces), civicos-ui/test/home.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=tasks for the open-task count/most-urgent and op=proposals [REC-6, now live] for the open-proposal count via loadProposals; reads op=whoami for the member identity; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-8 / MILESTONES M8 — the member HOME, the orientation surface that tells a member what the record is asking of them, the ENTRY POINT for M8. Aggregates the attention the record holds for THIS member: OPEN TASKS (op=tasks, the same feed UI-1's inbox reads — how many need you, the most urgent, linked to the Tasks inbox) and OPEN PROPOSALS (op=proposals, REC-6's discovery feed now live — how many derived findings await a member, linked to the Proposals surface; degrades HONESTLY with a "feed pending" note if op=proposals is absent, as UI-5 does — NEVER a fabricated count). Clear entry to the built surfaces (Tasks UI-1, Subjects UI-4, Members UI-7). HONESTY (load-bearing): every count comes from the op, never invented; an empty record shows an honest empty state ("nothing needs you right now"), never a fake number or a fabricated task. READ-ONLY: no act op sent from this surface. accepts-when civicos-ui/test/home.test.mjs: the home shows the member's open-task count from op=tasks and the proposal count from op=proposals (plus the honest "feed pending"/empty states); an empty record shows the honest empty state, not a fake number. NEGATIVE CONTROL: break the task-count wiring and the shown count no longer reflects op=tasks. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (1586ad2), clean cherry-pick. UI 21 harnesses green, plane battery 63/63. The member home is the M8 entry point, wired to live task + proposal feeds with honest empty/pending states. No DEC.
## CLAIM 2026-07-31 RECORD
session: record-agent-7
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (ONE new table `proposal_dispositions`, BEFORE host_governor), bio-plane/src/store.mjs (ONE new WRITE method `proposeDispose` + disposition-awareness wired into REC-6's `proposalsFeed` [reads the disposition store, filters aged proposals from the OPEN feed and returns them alongside]; `proposal_dispositions` in stats()/purge removed{} + a `DELETE FROM proposal_dispositions` in op=purge's whole-store arm; one line in the store DO dispatch: `proposedispose: () => this.proposeDispose(body || {})`), bio-plane/src/index.mjs (op=proposedispose in the OPS table [classes admin/member/probe, mutating:true] + `proposedispose` added to PROGRESSION_ACTIONS so a member/admin SESSION reaches it + NEEDS `proposedispose: "contribute"` + the server-side `decidedBy` stamp block), bio-plane/test/proposedispose.test.mjs (NEW) + its test:proposedispose entry in bio-plane/package.json, docs/development/INTERFACES.md (I5 additive bump; I3 disposition-awareness note)
interfaces consumed: REC-6 proposalsFeed (the feed a disposition ages out of), FW-9 progression_instances + stages (validating the proposal's identity), D-79 doctrine (age rather than vanish)
interfaces owned: I5 (ADDITIVE; registered owner RECORD)
expected: QUEUE.md REC-7 / MILESTONES M8 — op=proposedispose, record a PROPOSAL's defer/dismiss WITHOUT minting a bundle (UI-5's second delegation). A proposal-disposition store (I5-additive table `proposal_dispositions`) keyed by the proposal's identity (progression_key, stage_key) per D-79, holding state (deferred/dismissed), the REQUIRED reason, the server-stamped deciding member, and the time. op=proposedispose (mutating, contribute) records the disposition — NO bundle, no focus, no attribution beyond the disposition record (declining is NOT authoring, D-79). REC-6's op=proposals feed RESPECTS dispositions: an aged proposal is FILTERED from the OPEN feed and returned alongside in `dispositions[]` (filter-with-receipt — ages out of open so UI-5's instances[] re-aggregation drops it with zero UI change, but the decision is on the record, not hidden). accepts-when bio-plane/test/proposedispose.test.mjs: a member defers/dismisses a proposal with a reason → recorded, no bundle minted, no longer OPEN in op=proposals while an undismissed one still surfaces; NEGATIVE CONTROLS — dispose without a reason refused NO_REASON; dropping the disposition store makes the dismissed proposal reappear as open. New op carries a control-plane assertion (literal op=proposedispose). purge/D-113 asserted. No push/deploy/release — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (ba36d49), CLAIMS union-resolved. battery 64/64, --strict 107/107. UI-5's defer/dismiss is now fully live (both its delegations — REC-6 feed + REC-7 disposition — landed). I5 1.8.0, I3 1.4.0. No DEC.

## CLAIM 2026-07-31 M0-AGENT
session: m0-agent-m07
opened: 2026-07-31T00:00:00Z
paths: bio-plane/test/pipeline-e2e.test.mjs (NEW — ONE end-to-end integration suite driving the whole M4 entity-axis chain THROUGH the control plane; adds NO plane code, NO op)
interfaces consumed: I1 (op=acquire document.profile/digests/reading), I3 (the op contracts: acquire/promote/entitycreate/resolve/connections/progressiondefine/thread/instance/proposals/proposedispose), I5 (readings/entities/resolutions/connections/progression_instances/proposal_dispositions — read through the ops, never the store)
interfaces owned: none (drives the existing control plane; reshapes nothing)
expected: QUEUE.md M0-7 / MILESTONES M0 — the END-TO-END pipeline integration test. One suite chains a real captured Legistar meeting-calendar document acquire→promote→entitycreate+resolve→REC-5 alarm tick→connections→progressiondefine+thread→instance→proposals→proposedispose, ALL through dispatchFetch (a real caller's route). Proves the pieces COMPOSE by asserting the JOIN KEYS line up across stages — the capture_sha acquire produced is the one promote/reading/resolve/thread/instance carry; the entity_id resolve wrote is the one concerns/connections/instance/proposals read; the (progression_key, stage_key) the finding carries is the one proposals/proposedispose key on — each stage consuming the PRIOR stage's ACTUAL output, not a hand-built fixture. accepts-when bio-plane/test/pipeline-e2e.test.mjs green in the battery; NEGATIVE CONTROL — break ONE join (feed a divergent capture_sha into resolve/thread instead of the one acquire produced) and DOWNSTREAM assertions fail, proving the suite tests composition not just that each endpoint answers. Adds no op, so it DECLARES a control and adds 0 new unreached ops. No push/deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (1605dba). The M4 entity-axis pipeline composes end-to-end through the control plane with no integration gap — deploy-readiness evidence. battery 65/65, --strict exit 0.

## CLAIM 2026-07-31 RECORD
session: record-agent-8
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/store.mjs (REC-8 the OVERDUE-SUCCESSOR AGEING check, CONSTRUCTS Step 7 — all ADDITIVE, editing no FW-8/9/10 function: the injectable clock `#nowMs`; the derive-on-read overdue helpers `#intervalDeadlineMs`/`#captureDateMs`/`#instanceDeadlines`/`#overdueFindings`/`#overdueScan`; `proposalsFeed` extended to take a clock and merge `overdue_successor` findings + annotate proposals `overdue`/`overdue_count`/`kinds`; a SECOND framework consumer `overdue-scan` in `#schedConsumers` + its capture in `onAlarm`; `threadInstance` made async to ARM the alarm; the store DO dispatch line for `op=proposals` reads an optional `now` as-of param), bio-plane/test/overdue-successor.test.mjs (NEW) + its test:overdue-successor entry in bio-plane/package.json, docs/development/INTERFACES.md (I3 1.4.0 → 1.5.0, additive — op=proposals gains the overdue_successor kind + overdue proposal fields + the `now` param), docs/development/DECISIONS.md (DEC-10, open, for bob — whether an overdue proposal escalates), docs/development/DEBT.md (D-86 partly-settled note: temporal half built, bias-debt half deferred on the same consumer shape)
interfaces consumed: I5 (readings.at FW-5, register.registered, progression_instances/stages/exceptions — read through the store, reshaping none), I1 (the reading's `at` a document's date, via FW-5's readings table)
interfaces owned: I3 (the op contracts — additive bump), I5 (the store schema — UNCHANGED: overdue is DERIVED ON READ, no new table, so no I5 version change, no purge/D-113 entry)
expected: QUEUE.md REC-8 — CONSTRUCTS Step 7 AGEING. The record NOTICES when a progression's required next stage is OVERDUE: a placed, DATED predecessor + an ABSENT, UNDISCHARGED required successor + `predecessor_date + within_interval < now` → an `overdue_successor` proposal carrying the instance's grade, joined into op=proposals as a DISTINCT finding kind. DERIVE-ON-READ (no stored flag that goes stale against the clock, so NO new table); "now" is an INJECTABLE clock (env BIO_NOW_MS + op=proposals&now as-of, and onAlarm(now) for the alarm) so the computation is deterministic. Undetermined stays undetermined — a non-parseable interval, an undated predecessor, an absent predecessor, or a discharged/non-required stage is NEVER overdue. bias-debt (D-86's other half) DEFERRED and flagged, rides the same scheduler shape. accepts-when bio-plane/test/overdue-successor.test.mjs green in the battery (22/22); NEGATIVE CONTROL declared + RUN (neuter #instanceDeadlines → 7 fail); scheduler NC re-run (touched scheduling → subset-reconcile 8 fail, restored 18/18). No push/deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (5f76a7b), CLAIMS union-resolved. battery 66/66, --strict 107/107, overdue-successor 22. Derive-on-read (I5 unchanged). DEC-10 (overdue escalation) for bob-session. D-86 partly settled (temporal half done, bias-debt deferred). No new op. Second REC-1 consumer.

## CLAIM 2026-07-31 UI
session: ui-agent-9
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (UI-9 — CONSTRUCTS Step 8 PRESENTATION, the DOCUMENT-PAGE half: a READ-ONLY panel "What the record knows about this document" appended into the existing document/bundle view (openBundle) inside #docscroll after the four strata and before the act bars. Additive/in-place only: openBundle computes `const docKnows = await docKnowsPanel(capSha)` from the primary capture sha it already derives and injects `${docKnows}`; a self-contained module — `docKnowsPanel()` (the three sections), `docResolutionsHtml()` (subjects this document resolves to, op=resolutions by capture, REUSING UI-4's subjGradeBadge so a Grade C reads unconfirmed-not-established), `docConnectionsHtml()` (what it connects to, op=connections by capture, weaker-end graded), `docProgressionsHtml()`/`docInstanceHtml()` (progression membership + stage and any overdue successor), `docFindingOverdue()` (the ONE overdue seam — the negative-control target, reads REC-8's finding KIND, never re-derives a deadline), `docProgressions()`/`docProgGapHtml()` (the delegated per-document progression read + honest gap, the UI-5 loadProposals pattern), `docEntityLabel()` (bounded op=entity fan-out for subject labels), plus `.docprog-finding` CSS; reuses UI-4's tsec/subj-* CSS. No other screen's behaviour changed), civicos-ui/test/document-structure.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=resolutions (by capture sha256) for the resolved subjects at their §8.1 grade, op=connections (by capture sha256) for the graded connections, op=entity for subject labels, and a DELEGATED op=captureprogressions for the per-document progression membership + overdue findings; reads REC-8's overdue_successor finding shape; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-9 / MILESTONES M8 — CONSTRUCTS Step 8 PRESENTATION, the document-page half. On the open document page, a member reading ONE document sees its place in the accountability web (UI-4 is per-subject; this is per-document): (1) the SUBJECTS it resolves to at their §8.1 grade (op=resolutions/op=readingref for its capture sha — a Grade C shown "plausible, not established", the established/needs_confirmation flags READ FROM the op through UI-4's subjGradeBadge, never inferred from the letter); (2) what it CONNECTS to (op=connections for its capture — each graded the weaker end, shown as HOW established, not credibility); (3) which PROGRESSIONS it participates in and at which stage; (4) whether a required SUCCESSOR is OVERDUE (REC-8's overdue_successor — "N days overdue" from the op's OWN overdue_by_ms, shown HONESTLY: an undated/undetermined case emits no overdue_successor finding and the panel stays silent, never a fabricated deadline). READ-ONLY presentation; the acts live on their own surfaces. Items (1)/(2) read EXISTING per-capture ops; items (3)/(4) need a per-document → progression-instance lookup NO op exposes (op=instance reads ONE instance by (progression_key, entity_id); op=proposals aggregates every instance but carries no capture_sha to tie a finding to THIS document), so the progression half reads a DELEGATED op=captureprogressions and degrades to a named gap when it is absent — the UI-5 loadProposals pattern — rather than inventing membership. DELEGATION to RECORD appended below. DEFERRED: the CASE FILE (Step 8's other half) — flagged, not built. accepts-when civicos-ui/test/document-structure.test.mjs green: the document page shows the document's resolved subjects (a Grade C as unconfirmed, not established), its graded connections, its progression membership + stage, an overdue-successor note WHEN one exists and NONE when not, and the honest gap when the delegated op is absent. NEGATIVE CONTROL declared + RUN in-process — forcing the overdue seam docFindingOverdue() to `return true` makes a non-overdue missing step render as overdue when nothing is overdue; the suite asserts the marker is ABSENT under the real seam and PRESENT under the forced one (38/38 green; the §8.1 Grade-C-not-established property rides UI-4's subjGradeBadge seam, whose control is in subject-view.test.mjs). civicos-ui 22 harnesses green; plane battery 66/66 (no plane code touched — verified against the byte-identical plane source). Battery untouched. No push, no deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (e2e05e4). civicos-ui 22 harnesses green, plane battery 66/66. Items 1-2 live; 3-4 pending op=captureprogressions (lifted to REC-9). No DEC.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-9 (UI-9, the document page's referential + temporal structure panel)
need: a per-document → progression-instance READ op — proposed `op=captureprogressions&sha256=<capture>` (read; classes admin/member/probe; mutating:false; an optional `now=<ms>` as-of param exactly as op=proposals takes, for the overdue clock). It returns the progression INSTANCES a captured document is threaded into and, for each, the document's stage and the instance's derived findings, in this shape: `{ ok, capture_sha, count, instances:[ { progression_key, progression_label, entity_id, entity_label, stage_key, stage_label, findings:[ { kind:"missing_predecessor"|"overdue_successor", stage_key, stage_label, required, grade, grade_determined, established, needs_confirmation, /* overdue only: */ deadline, overdue_by_ms, predecessor_stage, predecessor_at, within_interval } ] } ] }`. It is a THIN read-time walk RECORD already has the pieces for: `SELECT DISTINCT progression_key, entity_id, stage_key, stage_label FROM progression_instances WHERE capture_sha=?` for membership+stage, then REUSE `#assembleInstance(progression_key, entity_id)` for the missing_predecessor findings and REC-8's `#overdueFindings(inst, #nowMs(now))` for the overdue_successor findings — so the honesty (undated/undetermined → never overdue, no fabricated deadline; established/needs_confirmation surfaced from the grade as #resolutionView does) stays enforced at the ONE derivation point, never re-implemented in the UI. Today no op maps a capture back to its instances: op=instance needs BOTH (progression_key, entity_id), and op=proposals walks every instance but returns no capture_sha to tie a finding to a specific document.
why: not blocking — UI-9 shipped the document-page panel with items (1) subjects and (2) connections fully live over the existing per-capture ops (op=resolutions, op=connections), and items (3) progression membership and (4) overdue successor built and PROVEN against this delegated shape (the test mocks it) but degrading HONESTLY to a named gap on-screen until the op lands, exactly as UI-5's loadProposals did for op=proposals before REC-6. RECORD owns store.mjs and I3; whether to add this read (and the exact field set) is RECORD's call. The finding kinds, grades, and overdue derivation already exist (FW-9/FW-10 + REC-8) — this op only needs to key them by capture instead of by (progression, entity).
status: open — raised by UI-9, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).

## CLAIM 2026-08-01 RECORD
session: record-agent-9
opened: 2026-08-01T06:30:00Z
paths: bio-plane/src/store.mjs (REC-9 — ADDITIVE only, editing no FW-8/9/10/REC-6/7/8 function: a NEW read method `captureProgressions({captureSha, nowMs})` — the capture→instances walk — plus its store DO dispatch line `op=captureprogressions`; REUSES `#assembleInstance` + REC-8's `#overdueFindings`/`#nowMs`/`Store.#isEstablished`, re-implementing NONE of them), bio-plane/src/index.mjs (the `op=captureprogressions` OPS entry `{classes:[admin,member,probe], mutating:false}` + its membership in `PROGRESSION_ACTIONS` so a session reaches the read; the control-plane assertion rides the new suite), bio-plane/test/capture-progressions.test.mjs (NEW) + its `test:capture-progressions` entry in bio-plane/package.json, docs/development/INTERFACES.md (I3 1.5.0 → 1.6.0, additive — one NEW read op, no existing op changed)
interfaces consumed: I5 (progression_instances read for the capture→instance placements + stage; readings/register/progression_stages/exceptions read only THROUGH #assembleInstance/#overdueFindings, reshaping none)
interfaces owned: I3 (the op contracts — additive bump), I5 (the store schema — UNCHANGED: DERIVE-ON-READ, no new table, no purge/D-113 entry)
expected: QUEUE.md REC-9 / MILESTONES M8 — CONSTRUCTS Step 8 PRESENTATION, the document-page delegation (UI-9). A READ op `op=captureprogressions&sha256=<capSha>` maps a CAPTURE back to the progression INSTANCES it is threaded into (by JOINING progression_instances on capture_sha), its STAGE in each, and each instance's `missing_predecessor` + `overdue_successor` findings — REUSING `#assembleInstance` + REC-8's `#overdueFindings` at the ONE derivation point (no re-derivation of overdue/missing-predecessor logic; FW-10 discharges honored; the same injectable clock `now`/BIO_NOW_MS so overdue is deterministic). DERIVE-ON-READ, no table, no mutation. Undetermined stays undetermined; a capture in NO progression returns an empty list, never a fabricated membership. established/needs_confirmation PROJECTED from each finding's grade (the #resolutionView boundary projection, not a new claim) so a Grade A/B finding renders established on the document page. accepts-when bio-plane/test/capture-progressions.test.mjs green in the battery (13/13, driven THROUGH the control plane via dispatchFetch, literal `op=captureprogressions`); NEGATIVE CONTROL declared + RUN (drop the capture-join → 6 assertions fail, the membership/stage/findings; the empty-list + NO_SHA assertions still pass; restored 13/13). battery 67/67 (3161 assertions); `npm run test:coverage --strict` exit 0 — 108/108 ops reached through the control plane, 0 unreached, 67/67 controls declared. No push/deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (54d671a). UI-9's document page is now FULLY live (items 3-4 populate from op=captureprogressions). battery 67/67, --strict 108/108. I3 1.6.0. Derive-on-read, no table. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: coff2-agent (worktree agent-a94f0a529394654b6)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/ooxml.mjs (NEW — COFF-2, the OOXML container reader: a PURE zero-dependency module — central-directory walk, member inflate via DecompressionStream("deflate-raw"), part lookup by name, [Content_Types].xml parse + flavour discrimination docx/xlsx/pptx vs arbitrary ZIP, the uniform _rels/*.rels walker (TargetMode="External" → outbound), docProps/core.xml metadata extraction (DEC-5), size-guard plumbing with a NAMED PROVISIONAL bound until COFF-6 measures; ODF DESIGNED FOR — the part-map is a parameter — not built), bio-plane/test/ooxml.test.mjs (NEW, programmatic hermetic fixtures) + its test:ooxml entry in bio-plane/package.json
interfaces consumed: I7 (built against ON PAPER, deliberately independent of COFF-1's landing; COFF-1 confirms I7 from as-built code — this claim reshapes nothing in I7's text)
interfaces owned: none. Explicitly NOT touched: bio-plane/src/formats.mjs and the index.mjs dispatch sites (COFF-1's claim), QUEUE.md, DECISIONS.md
expected: QUEUE.md COFF-2 / MILESTONES M2. accepts-when: battery green with fixture round-trips for all three flavours; a plain ZIP NOT identified as OOXML and a renamed one caught by magic-bytes-plus-parts rather than extension or content type; a truncated central directory → stated undetermined, never a silent partial; negative control — neuter the flavour discrimination and the suite fails on the plain-ZIP assertion. No push/deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-08-03 — landed on main by CONDUCT (7404f21, worker commit a20e3ec). Verified from the main checkout: battery 68/68 (3301 assertions), coverage --strict exit 0 (108/108 ops, fleet 1/1). Negative control run by the worker (neutered discrimination → 13/97 fail incl. the plain-ZIP assertion; restored 97/97) and recorded in the suite's NEGATIVE CONTROL line. Provisional 32 MiB bound named in code for COFF-6 to replace. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: content-office-coff1
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/formats.mjs (NEW — the FORMAT registry, I7: one entry per format, detection by magic bytes first / content type second; the HTML and PDF entries; registerFormat/unregisterFormat/getFormat/detectFormat), bio-plane/test/formats.test.mjs (NEW — the registry suite, incl. the TEST-ONLY stub-format assertion that is the D-70 evidence) + its test:formats entry in bio-plane/package.json, bio-plane/src/index.mjs (TWO NAMED dispatch touchpoints ONLY, moved onto the registry: [1] the acquire-time HTML_CT guard at the subresource branch (~:1836) — detection consults the registry, the subresource branch stays HTML-only in behaviour; [2] the read-time op=pdfstructure dispatch (~:1417) — the op survives with byte-identical output, routed through the registry's pdf entry; plus the ADDITIVE profile.format stamp beside the FW-3 profile assembly), docs/development/INTERFACES.md (I1 1.2.0 → 1.3.0 additive — document.profile gains `format`; I7 CONFIRMED from as-built code per the item), docs/development/DEBT.md (D-70 row — the verdict)
interfaces consumed: I1 (op=acquire's document.profile — extended ADDITIVELY, no existing field reshaped), I2 (the pdf entry's structure output is pdfstructure.mjs's existing I2 shape, byte-identical — pinned by pdfstructure.test.mjs / pdfstructure-op.test.mjs)
interfaces owned: I7 (PROVISIONAL → CONFIRMED by this item, from the code as built; code wins on drift)
expected: QUEUE.md COFF-1 / MILESTONES M2 — the FORMAT registry with HTML and PDF moved onto it, NO new capability. NOTE on the two index.mjs touchpoints: CAPTURE (HTML_CT site) and CONTENT-PDF (op=pdfstructure site) are DORMANT, so per the CONTENT-OFFICE kickoff this is a claim WITH A NOTE naming their surfaces, not a live delegation — existing outputs are PINNED (the battery's HTML and PDF suites must pass byte-identical). pdfstructure.mjs itself is NOT edited; it becomes the PDF entry by import. accepts-when: battery green with HTML/PDF outputs pinned unchanged; a TEST-ONLY stub format registers and is reachable detect→structure with ZERO edits outside the registry (the D-70 evidence); test:coverage --strict exit 0; negative control RUN and recorded (delete the PDF entry → op=pdfstructure fails NAMING the format as unregistered, battery fails; restored). No push — CONDUCT integrates; landed: left for CONDUCT.
released: 2026-08-03 — landed on main by CONDUCT (merge of worker 9593f71). Verified from the main checkout with COFF-2 together: battery 69/69 (3338), --strict exit 0 (108/108). I7 1.0.0 confirmed, I1 1.3.0 additive, D-70 closed. No DEC.

## CLAIM 2026-08-03 M0
session: rec27-agent (worktree agent-a068a4d114caf7901)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/test/hygiene.test.mjs (the D-113 table-coverage assertion taught to parse store.mjs's CREATE TABLE literals as well as schema.mjs's, three new EXEMPT entries with stated reasons, a new raw-control-byte guard over bio-plane/src/, the NEGATIVE CONTROL line), bio-plane/src/store.mjs (the purge TABLES/DELETE list only - project_participants and project_owner_votes added to both arms, keyed on project_id which IS a bundle id; stats() gains their two counters so purge can PROVE it took them; and the D-131 raw NUL byte at :3833 rewritten as the u0000 escape, value-identical), bio-plane/test/projects.test.mjs (a new purge section asserting the participation graph actually clears, per-bundle and whole-store), docs/development/DEBT.md (rows D-137 and D-131 disposition cells only)
interfaces consumed: I5 (the store schema - read; no table added, no column changed, purge completeness only)
interfaces owned: none
expected: QUEUE.md REC-27 / BUILD-ORDER.md section 2 (REC-27). accepts-when: npm run test:hygiene reports 52 of 52 tables covered by purge or a stated exemption (was 44 of 52); grep -c "CREATE TABLE" bio-plane/src/store.mjs non-zero WITHOUT -a; full battery green; coverage --strict exit 0; negative control - remove project_participants from the purge list and the hygiene suite names it. No push/deploy - CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 3e35d56). Verified from the main checkout: battery 69/69 (3374), hygiene 52/52 covered, --strict exit 0. Negative control RE-RUN BY CONDUCT (destructive code): neutered purge DELETEs → hygiene names project_participants, 4 projects assertions fail; restored green, byte-identical. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: coff4-agent (worktree agent-a9b712936848f18cc)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/docx.mjs (NEW — COFF-4, the DOCX registry entry: I7 detect/parts/structure/text built on ooxml.mjs primitives; word/_rels/document.xml.rels → I2 partitions through the ONE subresources.mjs linkWrapper; bookmarks → anchor; word/embeddings/ → intra; element references {kind:"doc-para"} per IC-1 as resolved; <w:t> text in body order; THE EVIDENTIARY CORE per DEC-5 — w:ins/w:del with author, date and the SUPERSEDED WORDING, word/comments.xml with author and date — under the shared I2 extension envelope), bio-plane/test/formats-docx.test.mjs (NEW, hermetic programmatic fixtures modelled on real Oakland document structure) + its test:formats-docx entry in bio-plane/package.json, bio-plane/src/formats.mjs (ONE registerFormat(docxEntry) call plus its import — nothing else, so COFF-3 merges cleanly), docs/development/INTERFACE-CHANGES.md (IC-2 — the shared evidentiary envelope, FILED from as-built code if COFF-4 lands first per the COFF-3/4/5 first-lander rule; CONFIRMING instead if COFF-3's IC-2 is on main by integration time)
interfaces consumed: I7 (the entry shape, confirmed 1.0.0 — consumed, not reshaped), I2 (links through the ONE linkWrapper, parity as pdfstructure.test.mjs pins it), IC-1 as RESOLVED (doc-para: para 0-based REQUIRED, run 0-based OPTIONAL — present when the reference genuinely targets runs)
interfaces owned: none. Explicitly NOT touched: bio-plane/src/ooxml.mjs (COFF-3 holds its sizeGuard lines this wave), the xl/ppt part-maps, the index.mjs dispatch sites, QUEUE.md, DECISIONS.md
expected: QUEUE.md COFF-4 / MILESTONES M2. accepts-when: battery green with a real-Oakland-shaped DOCX fixture yielding paragraph-referenced links and a tracked change carrying author, date and the superseded wording; an unreadable part → stated undetermined; test:coverage --strict exit 0; negative control — drop the superseded wording from the w:del emit and the suite fails naming it (RUN and recorded in the suite's NEGATIVE CONTROL line, restored after). No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 64e2238). Verified from the main checkout: battery 70/70 (3459), --strict exit 0. IC-2 proxy-answered and I2 bumped 1.0.0 → 1.1.0 at this integration. COFF-3's colliding IC-2 filing (key `evidence`) is being conformed to the accepted `evidentiary` envelope by the resumed COFF-3 worker. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: coff3-agent (worktree agent-acb374dffe4ce448c)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/formats-xlsx.mjs (NEW — COFF-3, the XLSX registry entry: detect/parts/structure/text per I7 as confirmed; element references {kind:"sheet-cell"} per IC-1 RESOLVED-as-amended; DEC-5 evidence envelope — formulas beside cached values, hidden rows/columns/SHEETS flagged), bio-plane/test/formats-xlsx.test.mjs (NEW, programmatic hermetic fixtures modelled on real Oakland workbook structure) + its test:formats-xlsx entry in bio-plane/package.json, bio-plane/src/ooxml.mjs (ONLY the size-guard block — the COFF-6 enactment my queue item orders: PROVISIONAL_OOXML_SIZE_BOUND_BYTES (32 MiB container) replaced by the MEASURED bound, 20 MiB of DECLARED UNCOMPRESSED text-part bytes summed from the central directory before inflation, MEASUREMENTS.md 2026-08-03 — the METRIC changed, not just the number; over-bound → text-undetermined marker kept; streaming-to-64-MiB NOT built, deferred), bio-plane/test/ooxml.test.mjs (the bound assertions CORRECTED to the measured metric, never exempted), bio-plane/src/formats.mjs (ONE registerFormat(xlsxEntry) call plus its import — nothing else), docs/development/INTERFACE-CHANGES.md (APPEND: IC-2, the shared I2 evidence envelope, filed FROM AS-BUILT CODE by whichever of COFF-3/4 lands first — this claim files it unless COFF-4 has already appended one, in which case COFF-3 appends a CONFIRM instead)
interfaces consumed: I7 (registry entry contract, CONFIRMED 1.0.0 — built against as-built formats.mjs/ooxml.mjs), I2 (structure/text shapes — partitions through the ONE linkWrapper imported from subresources.mjs, never re-derived), IC-1 RESOLVED as amended (sheet-cell element references)
interfaces owned: none reshaped. Explicitly NOT touched: index.mjs dispatch sites, word/ppt part-maps (COFF-4/5), QUEUE.md, DECISIONS.md, INTERFACES.md (the IC-1 CHANGING/version-bump bookkeeping is flagged to CONDUCT, not enacted here)
expected: QUEUE.md COFF-3 / MILESTONES M2. accepts-when: battery green with a real-Oakland-modelled workbook fixture yielding cell-referenced links, formulas beside values, and a hidden sheet flagged; an over-bound workbook → text-undetermined named; test:coverage --strict exit 0; negative controls RUN and recorded — collapse <f> into <v> → the suite fails naming the formula/value distinction, AND strip the hidden flag → the suite fails. No push/deploy — CONDUCT integrates; landed: left for CONDUCT.
note 2026-08-03 (post-COFF-4 reconciliation): COFF-4 landed FIRST, so its IC-2 filing (`evidentiary` envelope) is the accepted one. This worker's variant filing was deleted before ever reaching main, the xlsx emit conformed to the accepted envelope from docx.mjs as built (same key, same fields, core-properties as an item kind), and a CONFIRM appended under the accepted IC-2 chain instead. ONE line outside the claimed paths: bio-plane/test/formats-docx.test.mjs:351 pinned the provisional bound constant this claim's COFF-6 enactment replaced — corrected (never exempted, comment says why) because the two items could only meet at this merge; flagged here for CONDUCT rather than delegated, since COFF-4's worker is released and the battery cannot be left red.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker c57b909, the reconciled commit). Verified from the main checkout: battery 71/71 (3540), --strict exit 0. The envelope fork was caught and reconciled (variant deleted pre-main; CONFIRM under the accepted IC-2); the one cross-claim edit (formats-docx.test.mjs bound pin) reviewed and accepted — corrected, never exempted. No DEC.
session: coff5-agent (worktree agent-adc3da524a0eff4e9)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/pptx.mjs (NEW — COFF-5, the PPTX registry entry: I7 detect/parts/structure/text built on ooxml.mjs primitives; ppt/slides/_rels/slideN.xml.rels → I2 partitions through the ONE subresources.mjs linkWrapper; slide refs → anchor; ppt/embeddings/ → intra content-addressed by sha256; element references {kind:"slide-shape"} per IC-1 as resolved; <a:t> runs per slide; THE EVIDENTIARY CORE per DEC-5 — notesSlide speaker notes emitted per slide and DISTINGUISHABLE from slide text everywhere shown, cited or indexed, plus docProps core properties — under the accepted IC-2 evidentiary envelope exactly as docx.mjs carries it), bio-plane/test/formats-pptx.test.mjs (NEW, hermetic programmatic fixtures modelled on real Oakland deck structure) + its test:formats-pptx entry in bio-plane/package.json, bio-plane/src/formats.mjs (ONE registerFormat(pptxEntry) call plus its import — nothing else, so COFF-3 merges cleanly), docs/development/INTERFACE-CHANGES.md (IC-2 — a CONFIRM note from as-built code per the COFF-3/4/5 rule; COFF-4's filing is ACCEPTED and I2 is 1.1.0 — no variant, no INTERFACES.md edit)
interfaces consumed: I7 (the entry shape, confirmed 1.0.0 — consumed, not reshaped), I2 1.1.0 (links through the ONE linkWrapper, parity as formats-docx.test.mjs pins it; the IC-2 envelope CONFIRMED, not varied), IC-1 as RESOLVED (slide-shape: slide 1-based REQUIRED matching the human ref "slide N", shape 0-based OPTIONAL — present when the reference genuinely targets a shape)
interfaces owned: none. Explicitly NOT touched: bio-plane/src/ooxml.mjs (COFF-3 holds its sizeGuard lines this wave), bio-plane/src/docx.mjs, the xlsx entry, the index.mjs dispatch sites, QUEUE.md, DECISIONS.md, INTERFACES.md
expected: QUEUE.md COFF-5 / MILESTONES M2. accepts-when: battery green with a real-Oakland-modelled deck fixture yielding slide+shape references and speaker notes distinguishable from slide text; test:coverage --strict exit 0; negative control — merge notes into slide text and the suite fails naming the distinction (RUN and recorded in the suite's NEGATIVE CONTROL line, restored after). No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker fde39df). Verified from the main checkout: battery 72/72 (3637), --strict exit 0. One integration-seam test correction applied by CONDUCT at merge (stale provisional-bound pin → measured constant, corrected-with-comment, the formats-docx precedent). Hidden-slides flag queued as COFF-7. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: coff7-agent (worktree agent-ab604afd9f19f3f47)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/pptx.mjs (COFF-7 — hidden slides, the pptx analogue of xlsx hidden sheets per DEC-5: `show="0"` read from BOTH the slide part's own `<p:sld>` root (ECMA-376's home for CT_Slide@show — where PowerPoint's Hide Slide writes it) and the `sldIdLst` entry (the queue item's stated location, honored when the bytes carry it); ONE new envelope kind `hidden-slide` naming the slide; the hidden slide still FULLY extracted — text, notes, links — and its text units marked with a `hidden` field on `slides[]`/`speakerNotes[]`, the xlsx `sheets[]` precedent), bio-plane/test/formats-pptx.test.mjs (the hidden-deck cases, both attribute locations + the reorder interaction; existing unit-shape assertions extended with the mark — corrected, never exempted), docs/development/INTERFACE-CHANGES.md (ONE CONFIRM under IC-2 — the kind vocabulary grows, the envelope shape does not change), docs/development/CLAIMS.md (this entry)
interfaces consumed: I7 (unchanged), I2 1.1.0 (the IC-2 envelope CONFIRMED — one kind added, no shape change), IC-1 as RESOLVED (slide-shape refs consumed unchanged)
interfaces owned: none. Explicitly NOT touched: bio-plane/src/ooxml.mjs, bio-plane/src/formats.mjs (registration already exists), docx/xlsx entries, index.mjs dispatch, INTERFACES.md, QUEUE.md, DECISIONS.md
expected: QUEUE.md COFF-7 / MILESTONES M2. accepts-when: battery green with a fixture deck whose hidden slide is extracted AND flagged (kind `hidden-slide`, text units marked) while visible slides are not; test:coverage --strict exit 0; negative control — neuter the one `show` funnel and the suite fails naming a hidden slide indistinguishable from a visible one (RUN, recorded in the suite's NEGATIVE CONTROL line, restored after). No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 7eb9d84). Verified from the main checkout: battery 72/72 (3652), --strict exit 0. The ECMA-376 location correction and the included-AND-flagged choice both accepted, recorded on the item's landed line. No DEC.

## CLAIM 2026-08-03 FRAMEWORK
session: fw15-agent (worktree agent-ad534b8749f44a4e7)
opened: 2026-08-03T19:30:00Z
paths: docprofile/readtext.mjs (NEW — FW-15's ONE entry point, readText(): takes I2's text field FROM ANYWHERE — text.document, text.pages[] or the office formats' pageless text.paragraphs[] degenerate form (I2 1.1.0), or a bare string — applies the honesty gates (no text / text-undetermined -> determined:false with the producer's own markers named; a PARTIAL decode proceeds only STATED, never silently), then runs identify()/doctypeFor()/parse() over the flat text), docprofile/doctypes/meeting-agenda.mjs (NEW — a MEASURED content type, written from the real oakland.legistar.com agenda packet View.ashx?M=A&ID=1425405 fetched and Tier-1-extracted 2026-08-03: legislation file numbers on their own lines as the stable source-assigned keys, Subject:/From: labelled blocks, item numbers preceding the file line), docprofile/doctypes/registry.mjs (ONE import + ONE types.register(meetingAgenda) line, after meetingCalendar, before generic), docprofile/events.mjs (THREE catalogue entries for the agenda type: item_pulled EVENT, item_changed NOTICE, item_added ROUTINE — the catalogue's own extension point), docprofile/registry.mjs (re-export of readtext.mjs so the plane's one docprofile import path carries it), bio-plane/src/index.mjs (ACQUIRE-PATH WIRING ONLY, exact touchpoints: [1] the docprofile import line (~:39) gains readText; [2] the op=acquire reading assembly's not-read-as-text ELSE branch (~:2260-2268, the block ending "declares no reader") is extended — when the FW-3/COFF-1 profile.format names a registered format and the capture is single-part, the primary is read back from R2, the format entry's OWN text surface is consulted (pdf: structure(bytes).text — Tier 1, escalated to the pdf-worker via the existing needsTier2() predicate exactly as op=pdfstructure does when the binding exists; docx/xlsx and any future entry: text(parts)), and readText() produces the reading; text-undetermined -> a FAILED reading (found:false, basis naming the producer's markers), NO other branch of acquire touched, op=pdfstructure NOT touched, formats.mjs/docx.mjs/formats-xlsx.mjs NOT touched), bio-plane/test/reading-wire.test.mjs (NEW suite) + bio-plane/test/fixtures/legistar-agenda-1425405.pdf (the REAL Oakland agenda packet, committed as a fixture with its locator + retrieval date in the suite header) + the test:reading-wire script entry in bio-plane/package.json
interfaces consumed: I2 (the text shape incl. 1.1.0's paragraphs[] degenerate form — consumed, not reshaped), I1 (op=acquire's document.reading — the FW-5 field, extended ADDITIVELY with text_source/"layer", text_tier, text_container sibling keys; no existing key reshaped), I7 (format entries consumed through getFormat(); no entry edited)
interfaces owned: I2 (owner FRAMEWORK — no shape change made; the entry point is a CONSUMER of the stable shape)
expected: QUEUE.md FW-15 / BUILD-ORDER.md §2 (FW-15) — the L2→L3 wire. accepts-when: battery green with the new suite where acquiring the real agenda PDF (Tier-1 text decodes, 99.9%) produces a readings row and reading_refs rows (legislation:NN-NNNN references), and an encrypted-marker PDF (text-undetermined) produces a reading with found:false and NO invented refs; test:coverage --strict exit 0; NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — bypass the wire and the reading_refs count is zero for a PDF known to name three entities. NOT built: OCR (CPDF-10's, waits), any FORMAT registry entry change, any op. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 742d15a). Verified from the main checkout: battery 73/73 (3709), --strict exit 0. Stack-axis observation recorded as D-167; stale FRAMEWORK kickoff head corrected by CONDUCT at integration (rule 6). No DEC.

## CLAIM 2026-08-03 RECORD
session: rec19-agent (worktree agent-ab1396bcfe7a4d910)
opened: 2026-08-03T21:00:00Z
paths: bio-plane/src/affordances.mjs (NEW — REC-19, the act catalogue + derivation for op=affordances: ACTS/NON_ACTS totality over NEEDS, declared RUNGS (7 sourced, null otherwise), the published vocabularies), bio-plane/src/index.mjs (ONLY: one OPS entry for `affordances` beside whoami, the op=affordances handler block after the whoami handler, and the affordances.mjs import — the OPS/NEEDS table region; NEEDS itself not reshaped), bio-plane/src/store.mjs (ONLY: a new #citesInto private helper extracted from retire's live-cites guard so the derivation and the guard are ONE predicate, retire's guard loop replaced by a call to it, a new affordanceFacts() read method placed after searchIndexCheck, and ONE `affordancefacts:` line in the DO dispatch map — NOT the normalisation sites, NOT the op=dispose state machine, which are REC-10's), bio-plane/checks/bio-checks.mjs (ONLY: export ACTION_KINDS at module level and have checkActionExtension consume it — NOT OBJECT_TYPES/HEADINGS/knownSchemas/checkFocusExtension, which are REC-10's), bio-plane/test/affordances.test.mjs (NEW) + its test:affordances entry in bio-plane/package.json
interfaces consumed: I3 (adds one non-mutating read op; no existing contract reshaped), the exported STATES table from checks (imported, never copied — the item's own requirement)
interfaces owned: I3 additive (op=affordances). Explicitly NOT touched: bio-checks.mjs OBJECT_TYPES/HEADINGS/knownSchemas/checkFocusExtension, store.mjs normalisation + op=dispose LEGAL table, query.mjs — all REC-10's concurrent claim; QUEUE.md; DECISIONS.md; civicos-ui (its DISPOSITIONS/TASK_KIND_UI copies retire when the UI items consume this op)
expected: QUEUE.md REC-19 / MILESTONES M8, DEC-8 doctrine (the plane-sourced act pre-flight). accepts-when: battery green with a suite where op=affordances for a collected information bundle returns exactly the acts the plane would permit (and NOT retire for one carrying a live cites edge), each with its needs, rung:null for every op no document assigns one; coverage --strict reaches the op from the control plane; negative control — add an op to NEEDS without adding it to the affordances derivation and the suite fails naming the unpublished op (RUN, recorded, restored). No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker d22db7b). Verified from the main checkout: battery 74/74 (3751), --strict exit 0, 109/109 ops. D-168 recorded (cite permits retired); REC-25 scope amended (affordances joins the stamp list); dispose rewire folded into REC-11. No DEC.
## CLAIM 2026-08-03 RECORD
session: rec10-agent (worktree agent-a7ce355855ad80206)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/checks/bio-checks.mjs (the type catalog — OBJECT_TYPES/LEGACY_TYPE_ALIASES/BUNDLE_ID_RE/ANN_ID_RE/HEADINGS/STATES/knownSchemas, checkHeadings ~:397, checkStateLegality ~:410, checkFocusExtension→checkInquiryExtension ~:1199, checkRecheckCoverage ~:1176, C-6.3 ~:1029, plus the NEW shared title-derivation helper for C-16), bio-plane/src/store.mjs (ONLY: the boot type normaliser ~:194, op=dispose's state machine + type gate + refusal strings ~:1560-1691, promote's type/title projection ~:2795-2810, listBundles' type filter ~:2521 — named functions, NOT capture/link/task/reachability), bio-plane/src/query.mjs (~:411, the object_type filter-value rewrite), bio-plane/src/setup.mjs (the intake tables + form: option/TYPES/PREFIX/SCHEMA_OF/mdFor/create handler — no path asks an inquiry for a title), bio-plane/src/index.mjs (ONE line: the D-78 surfaced_by restamp condition ~:2954 gains the inquiry spelling via normalizeType — NOT the ACQUIRE path, no new op; FW-15's ground untouched), bio-plane/migrate/migrate.mjs (:29 TYPE_ROOTS gains inquiries), bio-plane/test/inquiry.test.mjs (NEW, carries focus.test.mjs's three blocks; focus.test.mjs retired into it), bio-plane/test/disposition.test.mjs + conformance.test.mjs + search.test.mjs + check-firing.test.mjs (assertions superseded by the rename corrected, never exempted), docs/development/CLAIMS.md (this entry)
interfaces owned: I5 (additive — no schema.mjs change this turn), I3 (NO new op; ONE wire change inside op=dispose: NOT_PROBLEMS renamed, refusal details stop saying Problem — item 13 of DATA-MODEL §2.7's change list, carried by QUEUE REC-10)
explicitly NOT touched: docprofile/** and index.mjs's ACQUIRE path (FW-15's claim), civicos-ui/** (REC-10 is "no UI"; the UI catch-up is items 15-16 of the change list, CONDUCT sequences it), schema.mjs, newgroup/, QUEUE.md, DECISIONS.md
expected: QUEUE.md REC-10 / MILESTONES M9. accepts-when: battery green with inquiry.test.mjs carrying the three focus.test.mjs blocks (canonical INQ- works · legacy FOCUS-/PROB- still validates · projection normalises all three to inquiry); a member creating a question through either entry point gets the same required fields and no path asks for a title; coverage --strict exit 0 with no new unreached op; negative control — remove ONE of the four normalisation sites and the suite fails naming that site's disagreement (RUN, recorded in the suite's NEGATIVE CONTROL line, restored). No push — CONDUCT integrates; landed: left for CONDUCT.

## CLAIM 2026-08-03 RECORD
session: rec25-agent (worktree agent-a5af9d143bf40ab47)
opened: 2026-08-03T22:30:00Z
paths: bio-plane/src/index.mjs (ONLY: the D-15 viewer-stamp condition extended to the read ops — op=list, op=index, op=projection, op=image, op=file, op=backlinks joining the search/select/edge-action stamp; the op=affordances handler's affordancefacts fetch gaining the same server-side viewer stamp; the internal ratify /image + /list and monitor-tick /image fetches stamped so the fail-closed store still answers them; ONE new OPS entry `backlinks` beside list — NOT the type tables, NOT NEEDS reshaping), bio-plane/src/store.mjs (ONLY: listBundles, buildIndex, projection, affordanceFacts and the image/file dispatch-map lambdas gaining the D-15 gate via query.mjs's ONE viewerPredicate compilation point, a #viewerSees helper beside readImage, a NEW backlinks() read method + its dispatch-map line — NOT the normalisation sites, NOT the dispose state machine/LEGAL table, which are REC-10's concurrent claim), bio-plane/test/gate-reads.test.mjs (NEW) + its test:gate-reads entry in bio-plane/package.json, one-line `viewer=class:member` stamps on the direct-DO fixture calls the fail-closed gate now requires in test/{store,projection,search,selection,cite,edges,focus,disposition,release,projects}.test.mjs and test/cite-scale.mjs (fixture-call URLs only; no assertion of REC-10's type/state ground is changed), docs/development/MEASUREMENTS.md (the F-8 probe, before/after), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 (adds one non-mutating read op, op=backlinks; no existing contract reshaped — a gated read returns what D-15 says the viewer may see, and an invisible or absent target answers identically), query.mjs viewerPredicate (IMPORTED, never copied — D-15 keeps its single compilation point; query.mjs itself NOT edited)
interfaces owned: I3 additive (op=backlinks). DISJOINT FROM REC-10 by site: REC-10 holds the bio-checks.mjs type tables, store.mjs normalisation + dispose state machine, and query.mjs normalisation; this claim touches none of those functions — its store.mjs sites are the read paths (listBundles/buildIndex/projection/affordanceFacts/dispatch-map read lambdas + new backlinks) and its index.mjs site is the viewer-stamp region. Explicitly NOT touched: query.mjs, bio-checks.mjs, civicos-ui/app.html (the reverseRefs deletion is UI-21's; this provides the read), QUEUE.md, DECISIONS.md
expected: QUEUE.md REC-25 / BUILD-ORDER.md §2 (REC-25) + the 2026-08-03 affordances amendment. accepts-when: battery green with a suite where a member uninvited to a project gets that project from NONE of op=list, op=index, op=projection, op=image, op=search, op=affordances or a backlink — not id, not title, not state — and no message discloses that anything was hidden; the invited member still sees everything; coverage --strict exit 0 with op=backlinks carrying its control-plane assertion in the same turn; NEGATIVE CONTROL run and recorded — remove the stamp from op=list alone and the suite names op=list. No push — CONDUCT integrates; landed: left for CONDUCT.
amended 2026-08-03 (rec25-agent, same session): + bio-plane/src/livefire.mjs (ONE stamp — its scratch-canary image read is the same internal-server-reader class as ratify's and the monitor's, and the fail-closed store now requires it to say whose view it reads at: class:probe, machine scope). Found by the battery, not by review — the store failing closed turned a forgotten reader into a loud outage, which is the posture working.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker cdccf79). Verified from the main checkout: battery 75/75 (3813), --strict exit 0, 110/110 ops. NCs RE-RUN BY CONDUCT, both arms (unstamp op=list → outage naming it; leak the gate → 3 failures naming the leak). Integration seam: inquiry.test.mjs's direct-DO reads stamped by CONDUCT at merge (postdates the worker's branch). Residuals queued as REC-30. No DEC.
session: rec11-agent (worktree agent-a09a5716d8c643616)
opened: 2026-08-03T23:00:00Z
paths: bio-plane/src/schema.mjs (the inquiry_basis table + its two indexes, placed BEFORE the host_governor block), bio-plane/src/store.mjs (ONLY: promote's basis validation/refusals + the inquiry_basis projection beside the refs projection, a #basisCyclePath helper, two read methods basisFor/restingOn with their DO fetch-map lines, purge's TABLES gains inquiry_basis, and the REC-19-folded chore — dispose() and proposeDisposition() import DISPOSITIONS from affordances.mjs instead of holding literal copies), bio-plane/src/affordances.mjs (comment only: the DISPOSITIONS "until unified" note updated — the store now imports it), bio-plane/checks/bio-checks.mjs (ONLY: new exported checkInquiryBasis + its vocab constants, called from checkInquiryExtension; checkReferences' C-6.3 elevated_into arm REPLACED per QUEUE REC-11 by the basis-in-references[] arm), bio-plane/test/basis.test.mjs (NEW) + its test:basis script in bio-plane/package.json, bio-plane/test/check-firing.test.mjs (the C-6.3 proof corrected to the replacing rule, never exempted), bio-plane/test/affordances.test.mjs (the dispose-literal pin corrected to assert the import — the pin's subject was unified away by this item's folded chore), docs/development/CLAIMS.md (this entry)
interfaces owned: I5 (additive — one new derived table, both purge arms). I3 untouched: NO new control-plane op; the two reads are DO-internal routes for the battery (E2/REC-17 surface them later).
explicitly NOT touched: bio-plane/src/index.mjs (REC-25's concurrent claim on read-op stamping and a backlink read — my promote/store/schema/checks paths are DISJOINT from index.mjs entirely), query.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md
expected: QUEUE.md REC-11 / MILESTONES M9. accepts-when: battery green with basis.test.mjs promoting an inquiry whose basis names one INFO- and one INQ-, reading both back in order, answering "which inquiries rest on this document" as ONE indexed lookup; self-citation and a three-node cycle refused at the closing write with the path named; a connection-axis grade on an INFO- target reading back with axis intact; a hunch leg refusing a missing author or date by name; test:hygiene green (table in purge); coverage --strict exit 0. Negative controls (a) cycle check removed -> A->B->A accepted, (b) projection write dropped -> two-leg bundle reads empty basis, (c) C-6.3 arm removed -> a basis leg absent from references[] passes — RUN, recorded in the suite's NEGATIVE CONTROL line, restored. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 0d94bff). Verified from the main checkout: battery 76/76 (3843), hygiene green, --strict exit 0, 110/110 ops. No DEC; D-168 untouched by design.

## CLAIM 2026-08-03 RECORD
session: rec13-agent (worktree agent-ae11aab331757a4ab)
opened: 2026-08-03T23:45:00Z
paths: bio-plane/checks/bio-checks.mjs (ONLY: STATES.inquiry gains `concluded` with its edges open/surfaced <-> concluded and concluded -> deferred|dismissed, and checkInquiryExtension gains the per-state ENTRY REQUIREMENTS modelled on C-2.7's verified arm — non-empty conclusion, non-empty falsifier, basis.length >= 1, all under C-2.8. NOT OBJECT_TYPES/HEADINGS/LEGACY_TYPE_ALIASES/vocabFor/normalizeType, NOT checkInquiryBasis, NOT the information/project/action machines), bio-plane/src/store.mjs (ONLY: a new conclude() method placed after release(), a #setOrAddScalar helper beside #setScalar, affordanceFacts reporting `declared_type` from the live bundle.md alongside the normalised object_type, and ONE `conclude:` line in the DO dispatch map — NOT dispose(), NOT release(), NOT promote, NOT the normalisation sites, NOT the basis projection), bio-plane/src/index.mjs (ONLY: one OPS entry for `conclude` beside release, one NEEDS entry `conclude: "contribute"`, `conclude` added to the STATE_ACTIONS array so the viewer/owner/author stamps and both SESSION_OPS lists carry it without a second list — NO fifth capability token, and the op=affordances handler itself is UNCHANGED, since it already hands the whole facts object to deriveActs), bio-plane/src/affordances.mjs (ONLY: the `conclude` ACT entry in ACTS, its RUNGS omission stated, and the edgesFrom seam corrected to consult the catalog's vocabFor with the DECLARED object_type — the MAP RULE at the fifth consulting site; NOT NON_ACTS' existing rows, NOT DISPOSITIONS, NOT VOCABULARIES' shape), bio-plane/test/conclude.test.mjs (NEW) + its test:conclude entry in bio-plane/package.json, bio-plane/test/affordances.test.mjs (the act-count and act-list assertions superseded by a seventh published act — corrected, never exempted, with the reason in a comment), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (no schema change — `conclusion`/`falsifier` are frontmatter, and REC-11's inquiry_basis is READ, never rewritten), the exported STATES table (imported by affordances.mjs and store.mjs, never copied — the ONE edge table)
interfaces owned: I3 additive (op=conclude, mutating, contribute). DISJOINT FROM REC-20 (op=queue, the tasks/proposals contract) by site: REC-20 holds the task/proposal surface; this claim touches the inquiry state machine, conclude() and the affordances act table only. Explicitly NOT touched: schema.mjs, query.mjs, setup.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-13 / BUILD-ORDER.md §2 (REC-13), carrying DEC-22 (an `open` inquiry may hold a claim with ZERO legs — legal, readable, never auto-anything; `concluded`'s entry requirements unchanged) and DEC-30 (no owner gate, no ballot — any contribute holder, act attributed). accepts-when: battery green with a suite concluding an inquiry through op=conclude and refusing BY NAME each of a missing conclusion, a missing falsifier, an empty basis and a machine credential; an `open` inquiry with zero legs legal and readable; op=affordances on a concluded inquiry publishing exactly the legal acts and publishing conclude on an open one; test:coverage --strict exit 0 reaching op=conclude from the control plane; NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — remove the falsifier requirement and an inquiry concludes with nothing that would falsify it, restored after. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 6f2a527). Verified from the main checkout: battery 77/77 (3894), --strict exit 0, 111/111 ops. The edgesFrom/declared_type cross-item correction to REC-19's landed ground reviewed and ACCEPTED — it is the DEC-8 rule enforced (a pre-flight may not disagree with the refusal it fronts). Reopen gap queued as REC-31. No DEC.
session: rec20-agent (worktree agent-aa4364d1bf953e827)
opened: 2026-08-03T23:45:00Z
paths: bio-plane/src/store.mjs (ONLY: a NEW queueFeed() read method + its two private helpers #queueAncestors (the bounded basis/refs ancestor walk) and #queueEventLive (DEC-16's event-state gate), the exported QUEUE_CLASSES / QUEUE_CLASSES_DEFERRED / QUEUE_ANCESTOR_DEPTH statics beside them, and ONE `queue:` line in the DO dispatch map — placed after captureProgressions, NOT the tasks/proposals producers themselves, NOT the normalisation sites, NOT the inquiry state machine or the state catalog, which are REC-13's concurrent claim), bio-plane/src/index.mjs (ONLY: one OPS entry `queue` beside proposals/captureprogressions, one NEEDS entry `queue: null` with its reason, `queue` joining the REC-25 D-15 viewer-stamp condition plus a server-side `member` stamp, the op=queue handler that decorates the store's derived options[] through the HOISTED decorateAct() — the op=affordances handler's own `decorate` moved to module scope and reused, never copied), bio-plane/src/affordances.mjs (ONE NON_ACTS entry `queue` with its reason — REC-19's totality over NEEDS), bio-plane/test/queue.test.mjs (NEW) + its test:queue entry in bio-plane/package.json, docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 (adds one non-mutating read op; no existing contract reshaped), REC-19's deriveActs/ACTS (IMPORTED into store.mjs for the options[] derivation — never a second copy of the act rules; affordances.mjs's ACTS/RUNGS/NEEDS/SESSION_OPS decoration stays at the control plane), query.mjs viewerPredicate (IMPORTED through the ONE D-15 compilation point; query.mjs NOT edited), REC-11's inquiry_basis + refs (READ by the ancestor walk; neither table nor its projection changed), REC-10's normalizeType/vocabFor (the MAP RULE — every type/state consultation in the new code goes through them)
interfaces owned: I3 additive (op=queue). I5 UNTOUCHED — NO new table: DEC-16's "the unit of state is the EVENT" needs no carrier, because tasks.status and proposal_dispositions are ALREADY keyed by the event's own identity and not by (member, case); schema.mjs and purge are therefore not edited.
explicitly NOT touched: bio-plane/checks/bio-checks.mjs (REC-13 holds the state catalog + the concluded entry requirements), store.mjs's conclude/dispose state machines and normalisation sites (REC-13/REC-10), the taskDrain/taskList/proposalsFeed producers themselves (read, never reshaped), REC-21's queue_state (mute/snooze is NOT built here), civicos-ui/** (UI-14 consumes this contract later), newgroup/**, schema.mjs, QUEUE.md, DECISIONS.md
expected: QUEUE.md REC-20 / BUILD-ORDER.md §2 (REC-20) re-based on DEC-16, MILESTONES M8. accepts-when: battery green with queue.test.mjs where op=queue returns an OBLIGATION from tasks and an aggregated FINDING from proposalsFeed in ONE feed, each carrying `class`, `options[]` byte-equal to op=affordances' own answer for the same subject and viewer, and `case` holding EVERY ancestor for a nested item; an over-depth ancestor walk reports the set `undetermined` naming the bound; resolving the event ONCE clears it from every entry; an uninvited member's queue names no invisible project ancestor and says `undetermined` rather than being silently shorter; an item with no ancestor sits UNGROUPED; CONDITION is absent and DECLARED deferred; test:coverage --strict exit 0 with op=queue reached through the control plane in the same turn; NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — key the clearing on (member, case) instead of on the event and the second ancestor shows a stale unresolved copy. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker b956b6d). Verified from the main checkout: battery 78/78 (3931), --strict exit 0, 112/112 ops. DEC-36 raised from the report's doctrine tension (with the shipped behaviour as provisional); the affordanceFacts residual + the reopen gap queued as REC-31. Both corrected-never-exempted seams reviewed and accepted. No new table — the event-keyed carriers already existed, recorded as the finding.

## CLAIM 2026-08-04 RECORD
session: rec12-agent (worktree agent-a0dc9df3a63d176ac)
opened: 2026-08-04T00:30:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) a NEW strengthOf() read method + its private helpers #strengthWalk / #axisResult / #weakestOf, placed after restingOn() and before purge(), reading REC-11's projection through basisFor() — the read seam — and INHERITING the exported Store.QUEUE_ANCESTOR_DEPTH for its depth bound rather than minting a second constant; (2) ONE `strength:` line in the DO dispatch map beside `basis:`/`restson:`; (3) the CORRECTED comment on #weakerGrade (RECONCILED §1.1 R1-m — the old comment named the two-axis substitution R2 forbids as a design INTENT), with #weakerGrade itself unchanged and deliberately unused by this item; (4) the per-axis projection CACHE: five entries in the existing bundles ALTER-loop (inquiry_capture_strength, inquiry_capture_state, inquiry_connection_strength, inquiry_connection_state, inquiry_basis_count) + two entries in the index loop, and one #writeStrengthProjection call inside promote's transaction right after the inquiry_basis projection loop — NOT PROJECTION_COLS, NOT the normalisation sites, NOT dispose/conclude/queueFeed, NOT promote's basis VALIDATION or its cycle guard, which are REC-11's landed ground), bio-plane/src/query.mjs (ONLY: three FIELDS entries — capture/connection/legs — over those cached columns; capture+connection joining DEFAULT_FACETS so "every inquiry at B or better on an axis" is an indexed query and not a scan; and ONE line in coerce() adding `upper`, the twin of the existing `lower` flag, because grades are stored A..D and a member typing `capture:b` must be answered rather than told nothing matched — NOT viewerPredicate, NOT PROVENANCE_COLS, NOT the tokenizer, the AST or the statement builders), bio-plane/test/strength.test.mjs (NEW) + its test:strength entry in bio-plane/package.json, bio-plane/test/basis.test.mjs (ONE assertion's INSTRUMENT corrected, never exempted: its "restingOn is ONE lookup" measure sliced the source from restingOn to /* Eviction — a REGION of the file, not a function — so any method added between them changed its answer while its subject was untouched; the slice now ends at restingOn's own closing brace, with the reason in a comment), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (REC-11's inquiry_basis, READ through basisFor(); the table is not reshaped), REC-20's exported Store.QUEUE_ANCESTOR_DEPTH (INHERITED, never re-declared — one depth bound, two walks), REC-10's normalizeType/vocabFor (the MAP RULE — every type consultation in the new code goes through the catalog), REC-25's viewer stamps (the suite's direct-DO gated reads carry viewer=class:member)
interfaces owned: I5 additive — five nullable projection columns through the existing ALTER loop; no new table, no schema.mjs edit, and therefore no purge arm to extend. I3 UNTOUCHED: NO new control-plane op. The derivation is a DO-internal route for the battery exactly as REC-11's basis/restson are; REC-14 (the ratified bytes), REC-17 and REC-22 give it its control-plane surfaces, so coverage --strict gains no unreached op.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/index.mjs, bio-plane/checks/bio-checks.mjs (checkInquiryBasis and the state catalog are REC-11's/REC-13's landed ground — this item derives from what they already refuse), civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
DISJOINT FROM REC-21 (queue_state, the personal mute table), running concurrently: REC-21 owns schema.mjs's NEW queue_state table with both purge arms, store.mjs's queueFeed()/mute paths and the CONDITION-kind filtering, and index.mjs's queue surface. This claim edits NONE of those — it adds no table and no schema.mjs line, does not open index.mjs at all, and its store.mjs sites are strengthOf/#strengthWalk/#axisResult/#weakestOf (new, after restingOn), the #weakerGrade COMMENT, the ALTER and index loops in #migrate, and one line each in promote's projection block and the dispatch map. The only shared file is store.mjs and the only shared symbol is Store.QUEUE_ANCESTOR_DEPTH, which this claim READS and never redefines.
expected: QUEUE.md REC-12 / RECONCILED.md §3.1 (REC-12), read with §1.1's amendment block and §1.2's, carrying DEC-21 (two measurements over two POPULATIONS — capture over every document reached, connection over every edge rested on; a leg IS an edge, so one document leg carries BOTH grades, and "evidentiary legs versus inferential legs" appears nowhere), DEC-18 (an ungraded leg is INERT — excluded from its axis's population, never weighed, never flooring, never unrating, and NAMED as not yet load-bearing, one or many; UNRATED is the boundary case where no leg on that axis is graded), D-160 (the word is UNRATED in the code, the copy and the test names; SUSPEND means the opposite in SB-OUTPUT §5.1 and appears nowhere), DEC-15 (a hunch grade composes NORMALLY, never as undetermined), R1 (#weakerGrade MUST NOT be reused — its `|| 0` ranks an unknown below D, below a member's signed testimony; the null is short-circuited BEFORE any rank comparison), R3 (the walk carries the depth bound; exhaustion reports `undetermined` naming the depth, R1's shape and not an error), and DEC-32's provisional (single-basis arithmetic — no grounds/plurality machinery).
accepts-when: battery green with strength.test.mjs showing (a) a mixed basis reading TWO strengths, each naming its own weakest leg, with no code path producing a single composed letter; (b) an inquiry whose leg is another inquiry inheriting that inquiry's pair PER AXIS; (c) one NULL-graded leg EXCLUDED from its axis's population while NAMED as not load-bearing, the axis still reading from its graded legs, and an all-ungraded basis reading UNRATED; (d) an over-depth chain reporting `undetermined` naming the depth, not throwing; test:coverage --strict exit 0. NEGATIVE CONTROLS, all five run and recorded in the suite's NEGATIVE CONTROL line and restored after each — force strongest-leg composition and the weak-link assertions fail; pass a null grade into the rank comparison and the suite fails because the chain reads below D; compose across the two axes and the suite fails naming the axes it mixed; remove the depth bound and a store-constructed cycle (REC-11 refuses them at write, so it is constructed directly in the store) does not terminate; rank an ungraded leg anywhere in the population and the suite fails. No push — CONDUCT integrates; landed: left for CONDUCT.
released: 2026-08-03 — landed on main by CONDUCT (merge of worker aa430f2). Verified: battery 79/79 at merge, 80/80 (4044) with REC-21, --strict exit 0. The INQ-capture-referent flag folded into REC-31 chore (3). No DEC.

## CLAIM 2026-08-03 RECORD
session: rec21-agent (worktree agent-afc83487dabb99520)
opened: 2026-08-04T00:30:00Z
paths: bio-plane/src/queuestate.mjs (NEW — REC-21's PERSONAL-half vocabulary and its ONE pure decision: QUEUE_CONDITION_KINDS transcribed from NOTIFICATIONS.md's catalogue, QUEUE_KIND_CLASS over every catalogued kind incl. the three LIVE producer spellings, classOfKind(), parseMutedKinds/serializeMutedKinds, and suppressedBy() — the admission decision queueFeed calls and the suite holds directly, the affordances.mjs/deriveActs precedent for a node-importable pure derivation), bio-plane/src/schema.mjs (ONLY: the queue_state table + its member index, placed BEFORE the host_governor block), bio-plane/src/store.mjs (ONLY: the queuestate.mjs import, THREE new methods queueMute/queueSnooze/#queueMutes placed after queueFeed inside REC-20's queue block, ONE admission call + a `mute` report inside queueFeed, TWO dispatch-map lines queuemute:/queuesnooze:, ONE new scheduler consumer `queue-renotify` registered in #schedConsumers, purge gaining queue_state in BOTH arms, and stats() gaining a queueState counter — NOT queueFeed's producers, NOT #queueAncestors/#queueEventLive, NOT the normalisation sites, NOT the basis projection or the read seams), bio-plane/src/index.mjs (ONLY: two OPS entries queuemute/queuesnooze beside queue, two NEEDS entries (null, with the doctrine reason), a new QUEUE_ACTIONS array spread into BOTH SESSION_OPS lists, the server-side member+viewer stamp for the two ops beside the proposedispose stamp — NEEDS itself not reshaped, no handler rewrite of op=queue), bio-plane/src/affordances.mjs (TWO NON_ACTS entries with their reasons — REC-19's totality over NEEDS), bio-plane/test/queue-state.test.mjs (NEW) + its test:queue-state entry in bio-plane/package.json, docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 (adds two mutating member ops; no existing contract reshaped — op=queue's answer gains an additive `mute` block and loses no key), query.mjs viewerPredicate (IMPORTED through the ONE D-15 compilation point; query.mjs NOT edited), REC-10's normalizeType/vocabFor (the MAP RULE: the case a mute names is resolved to a case TYPE through the catalog's own machinery, never a raw key), REC-20's queueFeed/#queueAncestors/#queueEventLive (READ and wired, never reshaped), REC-1's #schedConsumers registry (one consumer registered; the mechanism untouched)
interfaces owned: I5 additive (ONE new table, queue_state, in BOTH purge arms), I3 additive (op=queuemute, op=queuesnooze).
DISJOINT FROM REC-12 (the strength derivation, concurrent) BY SITE: REC-12 holds the BASIS READ SEAMS (basisFor/restingOn, inquiry_basis grade columns) and the PROJECTION COLUMNS (promote's projection, the strength/axis fields); this claim touches neither — its store.mjs sites are the REC-20 queue block (queueFeed's admission + two new write methods), the scheduler registry, purge and stats; its schema.mjs site is a NEW table appended before host_governor and no existing column; its index.mjs site is the OPS/NEEDS/SESSION_OPS table region and the stamp block. Neither item reads or writes the other's rows.
explicitly NOT touched: bio-plane/checks/bio-checks.mjs, query.mjs, the tasks/proposals producers, proposal_dispositions (the whole point: mute writes NOTHING there), civicos-ui/** (UI-14 renders the mute control later), newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-21 / RECONCILED.md §3.2 (REC-21) / BUILD-ORDER.md §2 (REC-21), carrying DEC-16's raised stakes and the change-is-itself-an-event rule. accepts-when: battery green with a suite where muting a case suppresses its PRESENT CONDITION kinds for that member only, a second member's feed is byte-unchanged, a kind NOT in the mute still surfaces, an OBLIGATION on a muted case still appears in that member's queue, no tasks row and no proposal_dispositions row and no bundle was written by mute or snooze, and a record-CHANGING resolution raises its own event that reaches every ancestor entry (including the muting member's); test:hygiene green (queue_state in purge, both arms); test:coverage --strict exit 0 reaching BOTH new ops from the control plane. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored — (a) route the mute through proposedispose and the suite fails, because a personal preference entered the record; (b) let muted_kinds accept an OBLIGATION kind and the suite fails, because a real obligation vanishes from the only surface that routes it. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker eafffc3). Verified: battery 80/80 (4044), hygiene 54/54, --strict exit 0, 114/114 ops. NON_ACTS/NEEDS-null/no-probe classifications accepted with their reasoning; REC-32 queued for the first CONDITION generator. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec31-agent (worktree agent-aea0ae891377a3280)
opened: 2026-08-04T01:30:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) a NEW reopen() method placed after conclude() + ONE `reopen:` line in the DO dispatch map; (2) affordanceFacts' raw `b.object_type === "project"` consultation routed through normalizeType — chore (2), one line; (3) the COMMENT correction inside #strengthWalk's noReferent arm, pointing at chore (3)'s write-time refusal — the handling itself unchanged, because history can hold such rows. NOT conclude(), NOT dispose(), NOT promote's basis validation, NOT the normalisation sites, NOT queueFeed/strengthOf's logic), bio-plane/checks/bio-checks.mjs (ONLY: checkInquiryBasis gains the CAPTURE-AXIS-ON-AN-INQUIRY-LEG refusal arm under C-2.8 — DEC-21: capture ranges over documents. NOT STATES, NOT the inquiry machine, NOT checkInquiryExtension, NOT the `inherited` grade_source arm, NOT a new C-number), bio-plane/src/affordances.mjs (ONLY: the `reopen` ACT entry in ACTS with its RUNGS omission stated; NOT NON_ACTS' rows, NOT DISPOSITIONS' contents, NOT edgesFrom), bio-plane/src/index.mjs (ONLY: one OPS entry `reopen` beside conclude, one NEEDS entry `reopen: "contribute"`, and `reopen` added to the STATE_ACTIONS array so both SESSION_OPS lists and the viewer/owner/author stamps carry it without a second list), bio-plane/test/reopen.test.mjs (NEW) + its test:reopen entry in bio-plane/package.json, bio-plane/test/affordances.test.mjs (the act-count/act-list assertions superseded by an EIGHTH published act — corrected, never exempted, with the reason in a comment), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (no schema change — reopen writes frontmatter and history like conclude; REC-11's inquiry_basis untouched), the exported STATES table (the ONE edge table: reopen's legality is read through vocabFor over the DECLARED type, never a second edge source), REC-19's DISPOSITIONS (IMPORTED by both the store and the act — the one array naming what "set down" means), REC-10's normalizeType/vocabFor (the MAP RULE, chore (2)'s subject)
interfaces owned: I3 additive (op=reopen, mutating, contribute).
DISJOINT FROM REC-14 (published/editions), running concurrently: REC-14 owns the `published` state machinery, the edition fields, C-21.x, and checkInquiryBasis' `inherited` grade_source arm. This claim adds NO state to STATES and does not touch the concluded/published paths — deliberately, reopen is scoped to the DISPOSITION set (deferred|dismissed → open) and refuses a concluded inquiry BY NAME, because `concluded → open` is REC-14's edition machinery (DEC-12) and a conclusion revised without an edition is exactly what that item exists to prevent. The one SHARED FILE is bio-plane/checks/bio-checks.mjs and the shared function is checkInquiryBasis: this claim adds ONE arm (capture axis on an INQ- leg) and edits nothing else in it.
explicitly NOT touched: bio-plane/src/schema.mjs, query.mjs, setup.mjs, queuestate.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-31 (three chores) / MILESTONES M9. accepts-when: battery green with a suite where a deferred inquiry reopens through op=reopen with a named member and an AUTHORED reason, a machine credential is refused MACHINE_CANNOT_REOPEN, op=affordances publishes reopen on a deferred inquiry through the catalog's own edge table with no second edge source, a legacy-spelled project takes affordanceFacts' project arm (the fourth-name rehearsal: a temp module tree whose catalog carries one extra alias and whose promote normalisation is neutered — the inquiry.test.mjs boot-normaliser instrument), and a capture-axis grade on an INQ- leg is refused at promote AND named by the checker; test:coverage --strict exit 0 reaching op=reopen from the control plane. NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — remove the reason requirement and a reopen lands with no authored reason. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-03 — landed on main by CONDUCT (merge of worker af6228f). Verified: battery 81/81 (4103), --strict exit 0, 115/115 ops. The concluded→open gap is REC-14's to close (watched at its integration); the legacy-focus stance accepted as deliberate. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec29-agent (worktree agent-aff2a7112adf1335d)
opened: 2026-08-04T02:30:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) Store.memberList() gains an `{ administer }` parameter and the COVER↔HANDLE PROJECTION — `cover` is not SELECTED at all for a caller who does not administer, so the key is absent rather than null; (2) the `memberlist:` line in the DO dispatch map passes `url.searchParams.get("administer")` through; (3) the CORRECTED comment beside it, which said "All admin-only at the control plane" and was one of D-157's three self-contradicting sites. NOT memberAdd/memberSet/enroll/inviteLook, NOT #sessionRights, NOT memberCaps or the section-4 governance methods, NOT signerList — D-158's ground, untouched), bio-plane/src/index.mjs (ONLY: (1) the CORRECTED OPS-table comment at the membership block, which said "All admin-only" two lines under an entry granting admin+member+probe; (2) ONE new server-side stamp `administer` for op=memberlist in the DO passthrough, placed beside the D-15 viewer stamp and the REC-21 member stamp, set from `viaSession ? !!sessRights.administer : cls === "admin"`. The OPS entry's `classes` and `mutating` are UNCHANGED — the fix is a projection, not a refusal — so REC-19's totality guard is untouched and memberlist stays out of NEEDS/NON_ACTS), bio-plane/test/members.test.mjs (the section-3 suite, and the line-192 assertion CORRECTED never exempted, with the reason in a comment; two NEGATIVE CONTROL lines added at the head), bio-plane/test/membership.test.mjs (ONLY: the `list()` helper's direct-DO read now carries `administer=1`, the REC-25 viewer-stamp precedent for a gated direct-DO read, with the reason in a comment), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (the `members` table is READ, never reshaped — no schema.mjs edit, no new table, no purge arm), REC-25's server-side stamp discipline (the new `administer` stamp is set AFTER the caller's parameters are copied, so a caller-supplied value is overwritten rather than honoured — asserted directly)
interfaces owned: I3 — op=memberlist's answer becomes VIEWER-DEPENDENT: `cover` is present for an administrator (root ADMIN_TOKEN, root-admin session, or an in-app administrator's session) exactly as before, and ABSENT for every other caller. NOT additive: a field disappears for a class of callers, so by IC-3's own standard ("a renamed wire string is a break by definition") this is a BREAK and should be recorded as a major bump rather than dressed as additive. MEASURED consumer impact NIL: both live consumers coalesce (`civicos-ui/app.html` rosterRow `m.cover || m.name || ""`, `bio-plane/src/setup.mjs` `x.cover||""`), and setup.mjs signs in as the administrator so its view does not change at all. The registry entry (INTERFACES.md I3 version + an IC row) is left to CONDUCT, which has landed that bookkeeping for every REC item to date (c74c6a3, IC-3/I3 2.0.0) — flagged in the report rather than written here.
DISJOINT FROM REC-14 (published/editions), running concurrently: REC-14 owns the `published` state machinery, the edition fields, C-21.x, checkInquiryBasis' `inherited` grade_source arm and the ratify/checkInquiryExtension ground. This claim touches NONE of it — its subject is the membership roster, and its store.mjs sites are memberList() plus one dispatch-map line, both far from the publication paths; it opens neither bio-checks.mjs nor query.mjs nor schema.mjs, and its index.mjs sites are the membership OPS comment and one stamp line in the passthrough. No shared file with REC-14 carries a shared symbol.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/affordances.mjs, bio-plane/src/setup.mjs, civicos-ui/** (the UI degrades correctly with no edit — measured, not assumed), newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-29 / DEBT D-157 / MILESTONES M7, carrying BIO_Membership_Architecture v1 §3 and v2 §3 (identical: "Pairing. Only administrators see cover and handle together"; "Members and the public see handles"). accepts-when: battery green with a suite where an admin receives cover and handle together while an ordinary member session (administer:false) and the shared MEMBER_TOKEN receive handles with NO `cover` field present at all; test:coverage --strict exit 0; NEGATIVE CONTROL run and recorded — restore the old projection and the suite fails naming the non-admin callers that received cover, restored after.
released:

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 6213fc1). Verified: battery 81/81 (4119), --strict exit 0, 115/115 ops. NC RE-RUN BY CONDUCT (6 named failures under the old projection; restored byte-identical). IC-4 recorded, I3 → 3.0.0; D-157 closed; the dataplane state record amended in the D-160 pattern. The caller-controlled-param lesson carried into REC-30's scope by reference. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec14-agent (worktree agent-a0d577c0782f06de6)
opened: 2026-08-04T02:00:00Z
paths: bio-plane/src/schema.mjs (ONLY: published_bundles RE-KEYED (bundle_id, edition) per DEC-12 with the frozen columns beside it — title, completeness, strength, required, manifest_sha, manifest; TWO new tables `inquiry_exclusions` and `group_strength_bar`, both placed BEFORE the host_governor block — NOT the capture/link/task/reachability tables, NOT queue_state, NOT inquiry_basis), bio-plane/src/store.mjs (ONLY: (1) #migrate gains the published_bundles RE-KEY — a rename before the schema loop and a copy-forward-as-edition-1 after it, the one non-derived table that may not be dropped; (2) a NEW publishCase() state act placed after conclude() with its #setOrAddBlock/#removeBlock frontmatter helpers, (3) a NEW strengthBarSet()/strengthBarOf()/#requiredStrengthFor() trio, (4) publish() — the ratify committer — made edition-aware (APPENDS, refuses EDITION_NOT_INCREMENTED, stores the frozen completeness/strength/bar/manifest), (5) publishedList() enumerating editions, (6) gateFacts() returning the publishedRegistry for the gated bundle and its basis targets, (7) promote's projection block gaining the inquiry_exclusions delete-then-insert beside inquiry_basis and passing the registry into checkInquiryBasis, (8) purge gaining inquiry_exclusions in BOTH arms, (9) four dispatch-map lines: publishcase, strengthbar, strengthbarof, publishededitions — NOT conclude/dispose/release, NOT the strength derivation, NOT the normalisation sites, NOT queueFeed), bio-plane/checks/bio-checks.mjs (ONLY: STATES.inquiry gains `published` with the edges concluded -> published and published -> open|surfaced; HEADINGS gains the state-conditional `## What This Excludes` for inquiry; checkInquiryExtension gains the `published` ENTRY REQUIREMENTS under C-2.8 and the NEW C-21.1 completeness-freshness gate; checkInquiryBasis gains 'inherited' in GRADE_SOURCES and the NEW C-21.2 per-axis inheritance arm behind an injected publishedRegistry — MY EDIT IS THE INHERITED ARM ONLY; REC-31 concurrently owns the capture-axis-on-an-INQ-leg refusal arm in the SAME function and the two are disjoint by clause; new exported SUBJECT_POSITIONS; checkBundle's ctx gains publishedRegistry on the releaseRegistry precedent), bio-plane/src/gate.mjs (ONLY: runGate threads publishedRegistry through to checkBundle), bio-plane/src/index.mjs (ONLY: two OPS entries `publish` and `strengthbar` plus the reads `strengthbarof`/`publishededitions`, their NEEDS entries, `publish` joining STATE_ACTIONS, one DO_PATH line publish -> publishcase since the store's own /publish is the ratify committer, and the ratify handler passing facts.publishedRegistry into runGate and writing DEC-34's signed hash MANIFEST into R2 + published_shas — NOT op=ratify's contract {bundleId, expectedSha, sig}, which is UNCHANGED), bio-plane/src/affordances.mjs (ONLY: the `publish` ACT entry in ACTS, three NON_ACTS entries with their reasons, and subject_positions in VOCABULARIES — REC-31 concurrently owns the `reopen` ACT entry in the same array and the two entries are disjoint), bio-plane/test/publish.test.mjs (NEW) + its test:publish entry in bio-plane/package.json, bio-plane/test/hygiene.test.mjs (ONE EXEMPT row for group_strength_bar with its reason — the governance-declaration class, beside members/signers), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (REC-11's inquiry_basis and REC-12's strengthOf(), READ — neither reshaped), REC-13's conclude() and its `concluded` entry requirements (published is reachable ONLY from concluded), REC-10's normalizeType/vocabFor (the MAP RULE at every state and type consultation), REC-25's viewer stamps (publishCase takes the fail-closed viewer gate; the suite's direct-DO gated reads carry viewer=class:member), REC-19's ACTS/NON_ACTS totality over NEEDS
interfaces owned: I5 (published_bundles RE-KEYED — DEC-12; two new tables, inquiry_exclusions in BOTH purge arms), I3 additive (op=publish, op=strengthbar, op=strengthbarof, op=publishededitions; op=ratify UNCHANGED)
DISJOINT FROM REC-31 (the reopen act + two one-line residuals), running concurrently: REC-31 owns op=reopen (a new store method + its op/NEEDS/dispatch lines), the affordanceFacts raw-key MAP RULE line in store.mjs, and the capture-axis-on-an-INQ-leg refusal arm in checkInquiryBasis. This claim touches none of those three sites. The two SHARED FILES are checks/bio-checks.mjs (their arm is the capture-referent refusal; mine is the 'inherited' arm and the vocabulary constant) and src/affordances.mjs (their entry is the `reopen` ACT; mine is the `publish` ACT and three NON_ACTS rows). NOTE FOR CONDUCT AT INTEGRATION: this item makes `published -> open` a legal edge in the catalog, so REC-31's reopen must take its legality from the catalog's edge table rather than a literal deferred|dismissed list, or reopening a published case is refused by a list that has not heard of DEC-12.
explicitly NOT touched: bio-plane/src/query.mjs, bio-plane/src/queuestate.mjs, setup.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-14 / RECONCILED.md §3.1 (REC-14) / BUILD-ORDER.md §2 (REC-14), re-based on FOUR rulings: DEC-12 (published is NOT terminal; `edition` is required frontmatter stamped into the ratified bytes; published_bundles re-keyed and APPENDING, which closes D-144 as a feature; every edition keeps its own signature, attestor, time and gate version; reopening does NOT unpublish; a citing leg NAMES ITS EDITION and does not silently follow), DEC-13 (the completeness block carries the group's POSITION ON PUTTING THE CASE TO ITS SUBJECT — authored, justified, never prefilled; the gate is that the position is DECLARED AND JUSTIFIED, never that contact happened and never that the answer was favourable), DEC-17 as amended (the GROUP declares the default required_strength PAIR, a project may override, the bar is stamped BESIDE the derived strengths, and an absent bar gates nothing and SAYS so), DEC-34 (the published case is a CONTAINER — a signed hash manifest over every part, editions over the container; the PDF renderings and the per-page brazening are REC-22/UI-18's half, not this one), plus C-9's nameable exclusion rows (target_id OR prose, never neither; indexed on target_id) and R4's reserved division disclosure.
accepts-when: cd bio-plane && npm run test:battery green publishing a concluded inquiry at edition 1, republishing at edition 2 with BOTH editions readable and separately signed, refusing a republish that does not increment edition, refusing byte-identical completeness across editions (C-21.1), refusing per-axis over-strong inheritance (C-21.2, BOTH axis probes independently); a published inquiry reopens to open WITHOUT unpublishing; the declared bar stamped beside the derived pair and an absent bar stated as absent; the subject position declared-and-justified gate refusing an absent position and never checking contact; hygiene green (inquiry_exclusions in purge, group_strength_bar exempt with its reason); coverage --strict exit 0 with every new op reached from the control plane. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored after each — C-21.1 passes a carried-forward completeness and the gate is a checkbox; a single scalar C-21.2 comparison passes both axis probes and the axes were composed; upsert published_bundles and edition 1's signature is gone. No push — CONDUCT integrates; landed: left for CONDUCT.
amended 2026-08-04 (rec14-agent, same session), all found by the battery rather than by review: + bio-plane/src/gate.mjs (CATALOG_VERSION 1.18.0 -> 1.19.0 — the catalog gained two checks, and the ratify suite's exact-version assertion is corrected with it rather than loosened to a pattern, which is the point of that assertion), + ONE more read op `excludedby` (P8's "which published cases excluded this document" — inquiry_exclusions exists to make that query askable and without a read surface the index proves nothing; viewer-gated on REC-25's rule), + bio-plane/test/ratify.test.mjs (the published-parts count 3 -> 4: DEC-34's MANIFEST is now a part of every ratification; CORRECTED, never exempted, with the reason), + bio-plane/test/conclude.test.mjs and bio-plane/test/affordances.test.mjs (three superseded assertions corrected with their reasons: the concluded-state edge set gains `published`, a concluded inquiry now publishes `publish` beside `dispose`, the act catalogue is EIGHT acts, and "every act needs contribute" becomes "each act carries the capability its own NEEDS entry names" — publish rides the publication surface, not the contribute one), + bio-plane/package.json (test:publish), + bio-plane/test/hygiene.test.mjs (the group_strength_bar exemption).
CROSS-ITEM CORRECTION to REC-13's landed ground, found by this item's gate and fixed here because it is a WRITE-PATH BUG rather than a difference of opinion: store.mjs's `#setOrAddScalar` decided whether a key was present by asking whether the TEXT CHANGED, so writing a key its EXISTING value appended a SECOND copy and produced a document carrying a duplicate top-level key (C-2.1). It could not fire until DEC-12 made a case reopen, be concluded AGAIN with the same falsifier, and republish — which is exactly what this item does. Presence is now decided by looking. The comment names the old reasoning and why it was wrong.
merged 2026-08-04 (rec14-agent) — origin/main 98a0793 into the REC-14 branch, resolved by the worker at CONDUCT's request because git had interleaved reopen()'s body with publishCase()'s. Both functions restored WHOLE from their own sides; STATE_ACTIONS is the union ["dispose","retire","release","conclude","reopen","publish"] plus DECLARATION_ACTIONS; both acts registered; REC-29's memberList projection and administer stamp untouched; both test scripts kept; this file append-only. TWO SEAM DECISIONS, both mine and both pinned by the suites:
  (1) **A PUBLISHED CASE REOPENS THROUGH op=reopen — REC-31's act, not a second one.** The FROM set moves from DISPOSITIONS to a new one-place array REOPENABLE_FROM = [...DISPOSITIONS, "published"], read by BOTH the store's refusal and the published act so they cannot disagree. `concluded` stays refused BY NAME, unchanged, for REC-31's own reason: a conclusion reverting to open still wearing its conclusion records NOTHING, and op=publish is where that move belongs. `published` is the opposite case and that is the whole distinction — its editions are ratified, signed and immutable, DEC-12 rules that reopening does not unpublish, so there is nothing to erase; and published -> open is the ONLY route to a second edition, which makes reopen the front door of a revision rather than a way around one. Refusing it would have left a legal edge no caller could travel, which is the argument REC-31 was built on. ONE verb, not two: a second control meaning "pick this back up" is exactly the drift the act catalogue exists to prevent.
  (2) **AN INHERITED CAPTURE GRADE ON AN INQ- LEG IS ADMITTED; every AUTHORED one is still refused.** REC-31's DEC-21 referent arm was written when every grade_source was authored (resolution/testimony/hunch) and is unconditional for all three. `inherited` did not exist then: it is not a claim about the inquiry, it is the capture axis THAT CASE FROZE over its own documents when the group signed the cited edition — which ranges over documents exactly as DEC-21 requires — and C-21.2 refuses it if it is stronger than the frozen value. It is carried on the leg rather than re-derived because a leg citing edition 1 must not silently follow edition 2 (DEC-12). The arm narrows by that one case, with the reasoning in place; REC-31's own assertion (an authored capture grade on an INQ- leg, refused at both gates) is untouched and still passes.
  Superseded assertions CORRECTED, never exempted, each with a dated reason: the act catalogue is NINE acts (reopen + publish); a concluded inquiry publishes ["dispose","publish"]; NOT_SET_DOWN names `reopenable` (REOPENABLE_FROM) instead of `dispositions`; the reopen act's structural pin reads REOPENABLE_FROM; and REC-14's own "authored grade on a published case" probe now expects BOTH findings (C-2.8 and C-21.2), which is the two items' gates composing rather than a defect. publish.test.mjs now drives the reopen through op=reopen instead of hand-writing the state move, which is what pins decision (1).
  Verified after the merge: battery 82/82 (4199), hygiene 2/2 (436), coverage --strict exit 0 with 120/120 ops reached through the control plane, 53/53 checks named, 82/82 controls declared. BOTH items' controls RE-RUN against the merged tree: REC-14's C-21.1 neutered in both gates -> 69 pass, 5 FAIL (a carried-forward completeness publishes and the catalog finds nothing wrong); REC-31's reason gate neutered -> 53 pass, 4 FAIL (op=reopen accepts reason="" and the deferred inquiry is open with nothing accounting for it). Both restored.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 56e5427, the worker-resolved merge). Verified from the main checkout: battery 82/82 (4199), hygiene 436, --strict exit 0, 120/120 ops. The REOPENABLE_FROM unification and the inherited-arm narrowing reviewed and accepted; the C-21.1 position-term coverage question flagged on the landed line for Bob's eventual read. No DEC raised — the provisional (term not covered) is one line to tighten.

## CLAIM 2026-08-04 RECORD
session: rec30-agent (worktree agent-a4dac86c5881096be)
opened: 2026-08-04T03:30:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) TWO new private helpers beside #viewerSees — #bundleGate (the D-15 predicate as a WHERE term over a QUALIFIED column holding a bundle id) and #bundleRedactor (the same question per id, in JS, for answers assembled outside SQL); (2) the reads they gate: danglingRefs, taskList, queueFeed's OBLIGATION loop and FINDING subject query, readInstance + the new #redactInstance projection (applied also to threadInstance's and dischargeStage's echoed instance), readExceptions, readingFor, documentsByReference, resolutionsForCapture, documentsConcerning, connectionsFor, auditPass, searchIndexCheck, projectOwnerArithmetic, and #resolutionView's optional `keep` parameter; (3) the matching `viewer:` pass-throughs in the DO dispatch map for those ops; (4) the DO envelope's `ms` field REMOVED (`ok:true, ms:…` → `ok:true`) and the `const t = Date.now()` it fed. NOT promote, NOT the gate/ratify path, NOT listBundles/buildIndex/backlinks/#viewerSees themselves (REC-25's, unchanged), NOT memberList (REC-29's), NOT publishedList/publishedManifest/verifySha — deliberately ungated, recorded with reasons in the suite), bio-plane/src/index.mjs (ONLY: the REC30_VIEWER_READS array and its clause added to the ONE existing viewer-stamp condition in the DO passthrough. No OPS entry added, changed or removed; no class list touched), bio-plane/test/gate-reads.test.mjs (the posture suite, EXTENDED: the REC-30 sweep section, the classification table asserted structurally, the server-stamped-param audit, the `ms` assertions, and this suite's own client-side `delete body.ms` CORRECTED away with the reason), bio-plane/test/inbox.test.mjs + bio-plane/test/task-fence.test.mjs + bio-plane/test/task-drain-alarm.test.mjs + bio-plane/test/search.test.mjs (ONLY their direct-DO read helpers, which now carry `viewer=class:member` with the REC-25 stamp precedent and the reason in a comment — the fail-closed change surfaced them loudly, which is the posture working), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (no schema change — every table is READ, none reshaped, none added, so no purge arm), query.mjs's viewerPredicate (the ONE compilation point: both new helpers take their predicate, their machine carve-out and their fail-closed deny from it and restate none of them), REC-25's server-side stamp discipline, REC-29's caller-controlled-param lesson (audited here for every gate-like param, not only `administer`)
interfaces owned: I3 — the ANSWERS of fourteen existing read ops become viewer-dependent. Additive for a machine credential (byte-identical: the carve-out returns 1=1 and nothing is filtered or withheld) and for an administrator or a participant. A BREAK only for a member session reading about a project it was never invited to, which is the defect being closed. TWO shapes: a withheld ROW (op=dangling, op=tasks, op=queue's obligations) and a withheld BACK-REFERENCE (`bundle_id: null` on op=reading/readingref/resolutions/concerns/connections/instance/exceptions). MEASURED consumer impact: no `bundle_id` back-reference from these ops is read by civicos-ui or setup.mjs. The registry entry (INTERFACES.md I3 version + an IC row) is left to CONDUCT, as with REC-29.
DISJOINT FROM REC-14 (published/editions), running concurrently: REC-14 owns the `published` state machinery, the edition fields, C-21.x, checkInquiryBasis and the ratify/publication ground. This claim opens NEITHER bio-checks.mjs NOR schema.mjs NOR affordances.mjs NOR query.mjs, and it deliberately leaves the published projection UNGATED — publishedList, publishedManifest and verifySha are credential-free BY DESIGN and are recorded as such in the suite's classification table, so this sweep does not pre-gate REC-22's class or REC-14's ground. The one place the two items meet is the suite's structural assertion that EVERY read op in the OPS table is classified: read ops REC-14 adds will be unclassified and will FAIL gate-reads.test.mjs until CONDUCT classifies them gated or ungated-with-a-reason at REC-14's integration. That is the mechanism working as intended and is flagged in the report rather than softened.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/affordances.mjs, bio-plane/src/setup.mjs, bio-plane/src/queuestate.mjs, civicos-ui/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-30 / MILESTONES M7, carrying REC-25's landed residual (op=dangling, tasks?refersTo, the progression reads, the `ms` envelope) and REC-29's caller-controlled-param lesson. accepts-when: battery green with a suite where an uninvited member gets no project identity from ANY read op including op=dangling, each deliberately-ungated op listed in the suite with its stated reason, no control-plane response carrying `ms`, and every gate-like store param asserted server-stamped; test:coverage --strict exit 0. NEGATIVE CONTROL run in THREE arms and recorded in the suite's NEGATIVE CONTROL line — (a) restore the dangling leak and the suite names op=dangling, (b) drop the stamp and every answer is EMPTY while a forged caller-supplied viewer is honoured, (c) neuter the back-reference projection and seven ops hand over the project's id; all restored. No push — CONDUCT integrates.
merged 2026-08-04 (rec30-agent, at CONDUCT's request) — origin/main b6e957c (REC-14 published/editions + its own merge of REC-31) into this branch. Conflicts in bio-plane/src/index.mjs (the viewer-stamp condition: REC-14 had added op=excludedby to the same line — BOTH kept, union) and docs/development/CLAIMS.md (two appends at one anchor — both kept). store.mjs auto-merged; publishCase, publishedEditions, excludedBy and reopen are landed functions and are NOT reshaped — the gates compose around them. THREE NEW READ OPS CLASSIFIED, which the suite's structural assertion named at the merge and which are this item's judgments to make: (1) op=strengthbarof GATED — #requiredStrengthFor reports `projects: [...]` and interpolates the same ids into its detail, which is §7.9's reverse-edge walk (op=backlinks' own leak) arriving by a new door; the bar VALUE is deliberately NOT gated, because DEC-17's stake is that it is never set by who a reader is and op=publish stamps it from #requiredStrengthFor directly whatever this read shows, so only the NAMES are withheld and the withholding is stated without a count (#queueAncestors' posture). Residual named rather than papered over: `source: "project"` still says SOME project declares a bar. (2) op=excludedby ALREADY GATED by REC-14, correctly and at the one compilation point; it is NOT the published-projection class because it joins live `bundles` and reports current_state, so it can name an UNPUBLISHED case — asserted structurally, because no fixture here can make its gate bite (exclusions are written by op=publish, publishable cases are inquiries, and inquiries are shared corpus; an empty answer would be an outcome that costs nothing). (3) op=publishededitions DELIBERATELY UNGATED — published_bundles only, no join, no current_state: REC-22's credential-free class. Verified on the merged tree: battery 82/82 (4273), hygiene 2/2 (436), coverage --strict exit 0 with 120/120 ops. FOUR NC arms re-run and restored, the fourth new for REC-14's read.
released:

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 9c5b30b, the worker-resolved merge). Verified: battery 82/82 (4273), --strict exit 0, 120/120 ops. NC arms (a) and (d) RE-RUN BY CONDUCT, restored byte-identical. IC-5 recorded, I3 → 4.0.0. The DEC-17 bar-value residual decision accepted and recorded on the landed line. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec16-agent (worktree agent-aceac1b6a301edea5)
opened: 2026-08-04T04:00:00Z
paths: bio-plane/checks/bio-checks.mjs (ONLY: (1) STATES.inquiry gains `divided` — legal, TERMINAL (`divided: []`), entered from open|surfaced|concluded and NOT from published (DEC-28); (2) checkInquiryExtension gains the `divided` ENTRY REQUIREMENTS under C-2.8 — the `division` map {into, apportioned_by, at, reason} with >=2 into ids and the `division_apportionment` array that records where EVERY leg went, every ord covered and every child receiving at least one; (3) TWO new exported pure functions `supersedesEdgeFindings` and `divisionDisclosureFindings` under C-6.1, consulted by checkReferences AND by the store's promote write path — the checkInquiryBasis precedent; (4) checkReferences calls both. NOT checkInquiryBasis, NOT the published arm/C-21.x, NOT HEADINGS, NOT REL_VOCAB's contents), bio-plane/src/store.mjs (ONLY: (1) a NEW divide() state act placed after publishCase() + ONE `inquirydivide:` line in the DO dispatch map; (2) promote gains ONE new validation arm after the basis arm — the supersession edge check (SUPERSESSION_REFUSED, incl. an unresolvable supersedes target) and the RESOLVED sibling-set check against the parent's own division.into (NO_SIBLING_DISCLOSURE); (3) affordanceFacts gains `basis_legs` as a fact. NOT conclude/reopen/publishCase's bodies, NOT the strength derivation, NOT the projections, NOT queueFeed), bio-plane/src/affordances.mjs (ONLY: the `inquirydivide` ACT entry with its DEC-29 PROMPT, the exported DIVIDE_PROMPT string, and `prompt` on the act shape; NOT NON_ACTS' rows, NOT REOPENABLE_FROM/DISPOSITIONS, NOT edgesFrom), bio-plane/src/index.mjs (ONLY: one OPS entry `inquirydivide`, one NEEDS entry `inquirydivide: "contribute"` (DEC-30, author-scoped), `inquirydivide` added to STATE_ACTIONS so both SESSION_OPS lists and the viewer/author stamps carry it without a second list, and `prompt` added to decorateAct), bio-plane/test/divide.test.mjs (NEW) + its test:divide entry in bio-plane/package.json, bio-plane/test/conclude.test.mjs + bio-plane/test/affordances.test.mjs (act-list and act-count assertions superseded by a TENTH published act — corrected, never exempted, with dated reasons), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (NO SCHEMA CHANGE and no new table — decision D5: the division is authored in bundle.md and the child's `rel: supersedes` reaches `refs` through the EXISTING projection), the exported STATES table (the ONE edge table, read through vocabFor over the DECLARED type — the MAP RULE), REC-13's conclude(), REC-14's publishCase() and its RESERVED division_parent/division_siblings frontmatter (this item is their producer), REC-25's viewer stamps, REC-19's ACTS/NON_ACTS totality over NEEDS
interfaces owned: I3 additive (op=inquirydivide, mutating, contribute; op=affordances' act shape gains `prompt`, additive).
DISJOINT FROM REC-30 (the read-surface sweep), running concurrently: REC-30's subject is READ-OP GATING. This claim adds no read op and changes no read path's gate; its store.mjs sites are a new write act, one promote validation arm and one extra FACT on affordanceFacts, and its index.mjs sites are the OPS/NEEDS/STATE_ACTIONS table region plus decorateAct. Neither item touches the other's lines.
explicitly NOT touched: bio-plane/src/schema.mjs, query.mjs, queuestate.mjs, setup.mjs, gate.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-16 / RECONCILED.md §3.1 (REC-16) / BUILD-ORDER.md §2 (REC-16), carrying DEC-28 (`divided` is a TERMINAL STATE written by op=inquirydivide; its reason is the ACT's reason; disposition_reason untouched), DEC-29 (ONE authored reason per division — no per-leg reason — and the divide PROMPT's wording must STATE the disclosure the division will make), DEC-30 (author-scoped: any `contribute` holder, act attributed), and R4's disclosure, which is the item's point: each child records its PARENT id AND its SIBLING ids, the parent records where EVERY leg went including every `cuts_against` leg.
accepts-when: cd bio-plane && npm run test:battery green with a suite dividing a CONCLUDED inquiry into two children, each naming its parent AND every sibling, each carrying a `supersedes` edge back with a reason, the parent terminal and recording where every leg went including every `cuts_against` leg; a `published` inquiry refuses PUBLISHED_CANNOT_DIVIDE; the divide surface/prompt text STATES the disclosure (asserted as a string); npm run test:coverage --strict exit 0 reaching op=inquirydivide from the control plane. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored after each — (a) remove the sibling-set requirement and a child that omits a sibling lands and publishes; (b) remove the C-6.1 supersedes requirement and a child supersedes its parent with no reason and an unresolvable target. No push — CONDUCT integrates; landed: left for CONDUCT.
released:
amended 2026-08-04 (rec16-agent, same session), all found by the battery and by the negative controls rather than by review: + bio-plane/test/reopen.test.mjs (two superseded act-list pins corrected with dated reasons — an open inquiry that rests on something, and a concluded one, both publish `inquirydivide`; what those assertions exist for is unchanged, `reopen` is still not among them), + bio-plane/test/conclude.test.mjs (three: the concluded edge set gains `divided`, and the open/concluded act lists gain `inquirydivide` — INQ_STANDING's zero-leg list is UNCHANGED and that pair now does more work than either did alone, because it is what shows the act's basis-count condition is real), + bio-plane/test/affordances.test.mjs (the catalogue is TEN acts and the act shape gains `prompt`; the "all eight" NEEDS label was stale from the REC-14 wave and is corrected to ten; one new assertion that exactly one act publishes a prompt and it is DIVIDE_PROMPT).
FIVE REFUSAL NAMES BEYOND THE FOUR THE ITEM NAMED, each on an established precedent and none of them a new class: `BAD_CHILD_ID` / `CHILD_EXISTS` / `NO_CHILD_QUESTION` / `BAD_APPORTIONMENT` (caller-shape refusals, op=publish's BAD_EXCLUSION precedent — every one fires before anything moves), and `SUPERSESSION_REFUSED` (the BASIS_REFUSED / GATHERING_REFUSED shape: the catalog's own function judges, the store reports its findings verbatim). `NO_SIBLING_DISCLOSURE` is the item's own name and carries BOTH halves of R4's disclosure — the document-local shape and the resolved sibling set. Plus `CHILD_REFUSED`, which is the pre-flight: every child document is judged against the catalog BEFORE the parent moves, because the parent must be written first (a child's disclosure is checked against the parent's own division.into) and a child refused at its own write would otherwise leave a terminal parent naming a question nobody can open.
INSTRUMENT FINDING, recorded because it changed the suite rather than only the code: the second negative control was RUN TWICE. Probed through a division CHILD, removing the C-6.1 supersedes requirement still refused both documents — by the DISCLOSURE arm, which fires on any inquiry-to-inquiry supersedes edge that declares no division. The control failed 3 assertions and every one named the wrong rule. Block 7's isolating probes are INFORMATION bundles for that reason: the disclosure arm returns early where there is no division to disclose, so what is left under test is the edge requirement alone. The boundary between the two rules is now asserted directly rather than left to be rediscovered.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 893401a). Verified: battery 83/83 (4358), --strict exit 0, 121/121 ops. IC-6 recorded, I3 → 4.1.0. The ordinal-keying, two-key split, parent-first ordering and inquiry→inquiry disclosure scoping accepted as recorded mechanism. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec28-agent (worktree agent-aa74b6e0754dd0d60)
opened: 2026-08-04T05:00:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) the act-level machine refusals at the TOP of taskForward() and taskResolve() — MACHINE_CANNOT_FORWARD / MACHINE_CANNOT_RESOLVE on the MACHINE_CANNOT_RELEASE/CONCLUDE/REOPEN precedent, checked before the row is read so the refusal cannot depend on assignment state; (2) the COMMENT on #refuseNotYours, corrected where it states a guarantee the code does not make (D-151). The fence's LOGIC is unchanged — the unassigned arm stays, deliberately. NOT taskDrain, NOT #routeTask, NOT #taskOf/#refuseUngrammatical, NOT taskList, NOT any other refusal in the file), bio-plane/src/index.mjs (ONLY: `"probe"` removed from the `classes` of the two OPS entries taskforward/taskresolve and their comment stating why the class list alone cannot be the fence; plus the COMMENT on the actor-stamp passthrough, which carries D-151's exact flagged phrase "a daemon cannot close somebody's work" as though the NO_ACTOR refusal implied it — corrected, the stamp logic itself untouched. NOT taskdrain's OPS entry, NOT NEEDS, NOT SESSION_OPS, NOT TASK_ACTIONS), bio-plane/test/task-machine.test.mjs (NEW) + its test:task-machine entry in bio-plane/package.json, bio-plane/test/task-fence.test.mjs (ONLY: the head-comment/section note on the unassigned-claimable phase, which now says WHICH callers it holds for — corrected with the date and the reason, never exempted), docs/development/CLAIMS.md (this entry)
interfaces consumed: none (I5 unchanged — no table read or reshaped)
interfaces owned: I3 — BREAKING on two ops and deliberately so. (a) `op=taskforward`/`op=taskresolve` no longer admit the PROBE class at all (`forbidden for token class`, 403). (b) A MEMBER_TOKEN or ADMIN_TOKEN machine credential still REACHES both ops and is now refused by the store BY NAME with two NEW reasons, `MACHINE_CANNOT_FORWARD` and `MACHINE_CANNOT_RESOLVE` — the release/conclude/reopen shape. No SESSION is affected: a member or admin session's actor is a member id, never `token:<class>`, so every caller the Tasks screen has is byte-identical. MEASURED consumer impact: `civicos-ui/app.html` drives both verbs from a signed-in session only, and `setup.mjs` calls neither. The registry entry (INTERFACES.md I3 version + an IC row) is left to CONDUCT, as with REC-29 and REC-30.
DISJOINT FROM REC-16 (division), running concurrently on the STATES/divide ground: this claim opens NEITHER schema.mjs NOR bio-checks.mjs NOR affordances.mjs NOR query.mjs, touches no state machine and no catalog table, and in store.mjs is confined to the two task verbs and one comment — none of which REC-16's divide path reaches.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/affordances.mjs, bio-plane/src/setup.mjs, bio-plane/src/queuestate.mjs, civicos-ui/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-28 / MILESTONES M8, closing D-151 from DEC-7's ruling. accepts-when: battery green with a suite where a `token:probe` credential is refused BY NAME on both verbs for an UNASSIGNED task, the assignee still succeeds and an admin member still succeeds; test:coverage --strict exit 0. NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — remove the act-level refusal and the probe resolves an unassigned task while every existing task assertion still passes, which is the state today; restored after running. No push — CONDUCT integrates.
merged 2026-08-04 (rec28-agent) — origin/main ea1a917 (REC-16 divided/op=inquirydivide + its bookkeeping) into this branch. ONE conflict, docs/development/CLAIMS.md: two claim blocks appended at the same anchor, BOTH KEPT with REC-16 first because it landed first. store.mjs and index.mjs auto-merged and the disjointness held as claimed — REC-16 works in divide()/promote/affordanceFacts and the OPS/NEEDS/STATE_ACTIONS region, this item in the two task verbs and their two OPS lines; neither touched the other. Verified on the merged tree: battery 84/84 (4392 = main`s 4358 + this suite`s 32 + the 2 per-suite assertions hygiene and planning-hygiene add for any new file — so no existing assertion moved), coverage --strict exit 0 with 121/121 ops, 53/53 checks, 84/84 negative controls declared. BOTH NC ARMS RE-RUN ON THE MERGED TREE and restored byte-identical (a: 17 of 32 fail, battery 83/84 4375 with every other suite green; b: 3 fail and the probe reaches the store); the suite`s recorded numbers are the merged-tree ones.
released:

released: 2026-08-03 — landed on main by CONDUCT (merge of worker dd6c3a1). Verified: battery 84/84 (4392), --strict exit 0, 121/121 ops. NC RE-RUN BY CONDUCT (both refusals neutered → 17/32 named failures; restored byte-identical). IC-7 recorded, I3 → 5.0.0; D-151 closed; CAPABILITIES.md row amended dated-style. No DEC.
session: rec17-agent (worktree agent-a3da31793d5229d8c)
opened: 2026-08-04T05:00:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) ONE new ALTER-loop column `bundles.inquiry_superseded_by` and the private `#writeSupersededBy` that maintains it, called from promote's refs transaction (old targets ∪ new targets ∪ the promoted bundle's own row) and once at boot over the existing supersedes edges; (2) ONE new private predicate `#restsOnLive` — the live-basis-leg question, #citesInto's shape over `inquiry_basis`; (3) ONE new derived read `reevaluations()` plus its `reevaluations:` line in the DO dispatch map; (4) the CITED refusal added to `dispose()` (dismiss arm) and to `divide()`, and the `reevaluation` block added to the SUCCESS answers of `dispose()`'s defer arm, `reopen()` and `publishCase()`. NOT promote's other arms, NOT strengthOf/basisFor/restingOn, NOT conclude, NOT the gate/ratify path, NOT the projections REC-12 and REC-14 own), bio-plane/src/index.mjs (ONLY: one OPS entry `reevaluations` (read, admin+member+probe, mutating false) and that op added to the ONE existing viewer-stamp condition. No NEEDS entry — a read carries none, op=governorstate's precedent — and therefore no change to REC-19's NEEDS/NON_ACTS totality), bio-plane/test/reevaluation.test.mjs (NEW) + its `test:reevaluation` entry in bio-plane/package.json, bio-plane/test/gate-reads.test.mjs (ONLY: op=reevaluations classified GATED in the structural table, with its reason), bio-plane/test/divide.test.mjs + bio-plane/test/disposition.test.mjs + bio-plane/test/affordances.test.mjs (ONLY superseded pins moved by the CITED refusal, corrected with dated comments and never exempted), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (ONE additive nullable column on `bundles` through the ALTER loop — no new table, so no new purge arm; `inquiry_basis` and its `inquiry_basis_target` index are REC-11's and are READ, never reshaped), REC-16's `supersedes` edges in `refs` (read, not re-produced), REC-14's editions in `published_bundles` (read), REC-12's `strengthOf()` (read — NOTHING here alters a strength), REC-25/REC-30's viewer gate through query.mjs's ONE compilation point (both shapes: a withheld ROW for a dependent the viewer may not see, a REDACTED back-reference for a superseding id inside a visible row)
interfaces owned: I3 additive (op=reevaluations, non-mutating; and the additive `reevaluation` block on the answers of op=dispose/op=reopen/op=publish) plus ONE break: op=dispose's dismiss arm and op=inquirydivide gain the `CITED` refusal, so an act that previously succeeded over a cited inquiry now refuses by name.
DISJOINT FROM REC-28 (the task fence), running concurrently on taskforward/taskresolve ground: this claim opens neither taskForward nor taskResolve nor the tasks table, adds no task op and changes no task gate. The one file both touch is store.mjs, in functions that do not overlap.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/affordances.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/queuestate.mjs, bio-plane/src/setup.mjs, civicos-ui/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-17 / RECONCILED.md §3.2 (REC-17) / BUILD-ORDER.md §2 (REC-17) — P-64, the re-evaluation obligation as a QUERY and not a flag, WIDENED by D-5 to the walk-back edges; DEC-12's note (a newer EDITION surfaces the obligation on dependents and recomputes nothing — a leg keeps citing the edition it names).
accepts-when: cd bio-plane && npm run test:battery green with a suite where superseding an inquiry (through a real op=inquirydivide) AND republishing at a new edition each make every inquiry whose basis names it surface the obligation, naming the moved leg and both strengths, without altering any strength; dismissing a cited inquiry refuses CITED naming offenders; dividing a cited inquiry refuses CITED; deferring succeeds and raises the obligation on every dependent; reopening likewise; npm run test:coverage --strict exit 0. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored after each — (a) drop the reverse lookup and an inquiry resting on a superseded case reports nothing; (b) permit a dismiss on a cited inquiry and a published case's basis panel names a leg that is now an abandoned question while its frozen strength still reads. No push — CONDUCT integrates; landed: left for CONDUCT.
released:
amended 2026-08-04 (rec17-agent, same session): + bio-plane/src/affordances.mjs (ONLY: (1) the `inquirydivide` act gains a THIRD condition `(f.rested_on?.working ?? 0) === 0` — retire's condition one altitude up, over the SAME #restsOnLive predicate the store's refusal runs, because a pre-flight offering a control the refusal it fronts would decline is the DEC-8 disagreement; (2) the facts-shape comment extended for `rested_on`; (3) a comment on the `dispose` act recording why it is DELIBERATELY NOT narrowed. NOT ACTS' membership, NOT DISPOSITIONS/REOPENABLE_FROM, NOT NON_ACTS, NOT edgesFrom, NOT the catalogue's size), + bio-plane/src/store.mjs affordanceFacts() (ONE new fact, `rested_on {working, frozen, severed}` — COUNTS and never ids), + bio-plane/package.json (the `test:reevaluation` script). NO existing suite's pins moved: the CITED refusals and the narrowed affordance were added with the battery green at 83/83 before and after, because no landed fixture disposes or divides an inquiry another inquiry's basis names. divide.test.mjs, disposition.test.mjs and affordances.test.mjs were therefore NOT edited — the correction the trap anticipated was not needed, and saying so is the point.
THE ONE JUDGMENT CALL, for CONDUCT: D-5 says terminal acts on a cited inquiry refuse CITED, and this item ships that — with DIVISION refusing on a WORKING dependent (open/concluded/deferred/dismissed) and NOT on a PUBLISHED one, while DISMISSAL refuses on both. Taken literally over the same set, the item's own supersession arm is unreachable: a division could then never supersede anything with a dependent, and "superseding a case makes every inquiry whose basis names it surface the obligation" would have no producer. The distinction that resolves it is what each act LEAVES BEHIND — C-6.2's remedies are "restore from history", "re-point to the successor", "sever with a reason"; a division carries the question forward into children resolvable in both directions (REC-16), so the second remedy exists and the dependent gets R7's obligation, while a dismissal abandons the question and leaves a signed edition — which cannot edit its own basis — stranded forever. Both halves are asserted (blocks 4 and 5) and negative control (b) produces the dismissal harm verbatim. Reversing it is one predicate: `restsOn.confirmed` -> `restsOn.all` at divide()'s guard, plus the matching `rested_on.working` -> working+frozen in affordances.mjs; the cost of reversing is that a published case permanently freezes any question beneath it against correction.
