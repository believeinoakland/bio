# Area claims

The register of who is working where, so no two sessions edit the same paths at
once. Established 2026-07-31 as step 2 of the parallel-development move
(`PARALLELISM.md`, "What to build first"). A text file and a rule; no tooling.

## The rules, in short

- **Claim your area before you edit its paths.** Append a `## CLAIM` block below.
  A claim keeps other sessions out of the paths it names; it is not a courtesy.
- **A session that needs work inside another area's claim DELEGATES.** Append a
  `## DELEGATION` block naming what you need and why, and continue with your own
  work. The owning area picks it up. Do not edit a claimed path quietly.
- **Release a claim explicitly**, by setting `released:` to the date. An
  unreleased claim older than its expected scope is stale, and `ARCH` may
  reassign it; silence does not hold ground forever.
- **Unclaimed paths are nobody's** — a collision risk, not a licence. Claim
  before editing even briefly.
- **Append only.** States are added, never edited in place, so the history of who
  held what is readable the same way the record's own history is. The one field
  that changes on an existing block is `released:`.

The format is the one `PARALLELISM.md` fixes:

```
## CLAIM <date> <AREA>
session: <name>
opened: <ISO8601>
paths: <comma-separated paths this claim reserves>
interfaces consumed: <ID(s), or none>
interfaces owned: <ID(s), or none>
expected: <the work this claim covers>
released: <date, or blank while live>
```

---

## CLAIM 2026-07-31 CAPTURE
session: capture-bootstrap-1
opened: 2026-07-31T17:57:07Z
paths: bio-plane/src/subresources.mjs, bio-plane/src/cpu.mjs, bio-plane/src/cdx.mjs, the capture/link/task/reachability tables in bio-plane/src/schema.mjs, the capture ops in bio-plane/src/index.mjs, the link/capture/task/reachability functions in bio-plane/src/store.mjs, the capture and authority checks in bio-plane/checks/bio-checks.mjs, bio-plane/test/subresources.test.mjs, bio-plane/test/governor.test.mjs, bio-plane/test/inbox.test.mjs, bio-plane/test/reachability.test.mjs, bio-plane/test/cdx.test.mjs
interfaces consumed: none
interfaces owned: I1 (bytes → content)
expected: CAPTURE's standing responsibility (fetch, governor, subresources, links, reachability, archive fallback). This turn stood up the parallel-development scaffolding under the ARCH role from the main checkout — CLAIMS.md, INTERFACES.md with I1, the UI/FRAMEWORK kickoff refresh, and the biosmoke-ui instance — none of which is another area's code.
released: 2026-07-31 — RELEASED as STALE by CONDUCT per PARALLELISM.md's stale-claim rule (session capture-bootstrap-1 long gone, never released, over-reserved most of CAPTURE's paths and collided with RECORD's store-core work and a future CAP-3). Paths now belong to their areas: the store core/scheduler to RECORD, the capture/link/task/reachability functions to CAPTURE's live per-item claims.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-1
opened: 2026-07-31T20:30:00Z
paths: bio-plane/test/hygiene.test.mjs, the purge table list in bio-plane/src/store.mjs
interfaces consumed: none
interfaces owned: none
expected: D-113 as a CLASS. A hygiene check asserting every CREATE TABLE in schema.mjs is either named in op=purge's whole-store table list or in a small explicit exemption allowlist (each with a one-line reason), so a forgotten derived table fails loudly. Also corrects derived capture tables currently missing from the whole-store purge. No deploy this turn; ARCH integrates.
released: 2026-07-31 — landed on main by ARCH (integration turn), commit 6eb2b42. Full battery green; the negative control was re-run by ARCH before landing (throwaway table -> the check fails naming it).

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-1
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/pdfstructure.mjs, bio-plane/test/pdfstructure.test.mjs, the pdfstructure entries in bio-plane/package.json (the test:pdfstructure script and the additive link in the test chain)
interfaces consumed: I1 (bytes → content)
interfaces owned: none yet; expects to propose I2 (structure → framework) producer-side from what the extractor emits
expected: D-91 phase 1 — the PDF outbound-link structure graph (anchor / intra / deferred / refused / undetermined), byte-identical wrappers to subresources.mjs. No text extraction (unpdf) this turn.
released: 2026-07-31 — phase 1 landed on main by ARCH (integration turn), commit 0b1c6ce. Phase 2 (text via unpdf, measured against the 3MB bundle and CPU ceilings) remains; the op wiring needed to live-verify is the DELEGATION below.

## DELEGATION 2026-07-31 CONTENT-PDF → CAPTURE
from: content-pdf-agent-1 (recorded by ARCH at integration)
need: wire op=pdfstructure into the `if (op === …)` dispatch in bio-plane/src/index.mjs — a few additive lines that read a capture_sha through the existing op=capture byte path and call extractPdfStructure(bytes) from bio-plane/src/pdfstructure.mjs. index.mjs is CAPTURE's; this is the delegation the CONTENT-PDF kickoff anticipated, not a quiet edit.
why: the extractor is complete and unit-tested against fixtures but cannot be live-verified against real captured PDFs until an op exposes it. If op-wiring delegation recurs across content areas, that is the trigger to promote op-contract ownership out of CAPTURE (the I3 move PARALLELISM.md anticipates).
status: fulfilled 2026-07-31 — op=pdfstructure landed 2ab62f4 (capture-agent-2 / QUEUE.md CAP-1). Live-verify (QUEUE.md CPDF-3) still awaits a DIST deploy.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-2
opened: 2026-07-31T00:00:00Z
paths: the pdfstructure op in bio-plane/src/index.mjs (the OPS spec entry, the op=pdfstructure handler, the shared captureKey helper, and the extractPdfStructure import), bio-plane/test/pdfstructure-op.test.mjs
interfaces consumed: I1 (bytes → content), I2 (structure, provisional — consumed as the extractor's output shape)
interfaces owned: none
expected: QUEUE.md CAP-1 — fulfil the CONTENT-PDF→CAPTURE delegation above. Add op=pdfstructure (GET, read-only, same auth/caps posture as op=capture GET) that reads a capture sha through the exact R2 path op=capture uses and returns extractPdfStructure(bytes) as JSON. Test through the op with a fixture PDF (link annotation) plus a non-PDF negative control. Does NOT touch CONTENT-PDF's src/pdfstructure.mjs or test/pdfstructure.test.mjs. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — CAP-1 landed on main by CONDUCT, commit 2ab62f4. Full battery green; the captureKey extraction verified a no-op (capture behaviour unchanged), the op is read-only and mirrors op=capture GET auth (no new permission).

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-2
opened: 2026-07-31T00:00:00Z
paths: the CPDF-1 measurement section in docs/development/MEASUREMENTS.md (append-only), bio-plane/test/unpdf-measure-probe.mjs
interfaces consumed: none (a measurement)
interfaces owned: none
expected: D-91 phase-2 go/no-go MEASUREMENT (CPDF-1). Bundle `unpdf` through the plane's esbuild target in a scratch temp dir (NOT added to shipped deps/bundle) and measure bundled size vs the 3MB Free-worker limit; time extraction on a real Oakland agenda as a labelled node proxy (not Worker CPU); record both with date and instrument and recommend go/no-go. Commits no text extractor and changes no shipped dependency or bundle. Verdict: GO — bundle fits with 2.29 MB gzip headroom; node-proxy cost modest. CPDF-2 unblocked.
released: 2026-07-31 — measurement landed on branch content-pdf/phase2-measure; CONDUCT integrates. Worker-CPU-vs-ceiling confirmation via a deployed probe is the remaining gated follow-on (rides on the op=pdfstructure delegation above).

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-3
opened: 2026-07-31T00:00:00Z
paths: the task-queue drain alarm in bio-plane/src/store.mjs (the alarm()/onAlarm handler, #rearmSchedule, #armDrain, #drainDelayMs, the TASK_DRAIN_* constants, and the arming added to taskEnqueue), bio-plane/test/task-drain-alarm.test.mjs, the TASK_DRAIN_DELAY_MS binding added to bio-plane/test/inbox.test.mjs, and the two test entries in bio-plane/package.json
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md CAP-2 / D-109 — drain the task queue automatically on a Durable Object alarm, the mechanism #armSweep proved for selections: armed on enqueue, re-armed by the alarm while task_queue is non-empty, self-terminating (deleteAlarm) when it drains. The capture path only ENQUEUES; taskEnqueue arms the alarm (a schedule, never a task write) so the producer/consumer split is preserved. onAlarm shares the single DO alarm with the selection sweep and reconciles to the earliest wake. op=taskdrain still works — the alarm and the manual drain coexist. NEGATIVE CONTROL run: neutering onAlarm's taskDrain call fails the suite naming the undrained queue ("the queue is empty after the drain" want 0 got 1; "the queue count is zero" want 0 got 1); restored and green. Full battery green (exit 0). No inbox-grammar or write-gate change. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — CAP-2 / D-109 landed on main by CONDUCT, commit 39a0e1b. Full battery green (exit 0); the selection, inbox, and task-drain suites pass together, confirming the shared-alarm reconciliation preserves the selection sweep.

## CLAIM 2026-07-31 BOB
session: bob-planning-1
opened: 2026-07-31T21:00:00Z
paths: docs/development/MILESTONES.md (new), docs/development/VERIFICATION.md (new), docs/development/PLAN.md (header only), docs/development/ORCHESTRATION.md, docs/development/PARALLELISM.md (area table), docs/development/INTERFACES.md (I3/I4/I5 appended), docs/development/DEBT.md (Status column dispositions + appended section), docs/development/MEASUREMENTS.md (appended), docs/development/CAPTURE-SCALING.md (status markers), docs/development/kickoffs/README.md, CLAUDE.md (verification section), bio-plane/scripts/battery.mjs (new), bio-plane/scripts/coverage.mjs (new), bio-plane/test/bundle.test.mjs, bio-plane/package.json (two test scripts)
interfaces consumed: none
interfaces owned: registers I3, I4, I5 from the code as it stands (I3 owner moved CAPTURE -> RECORD)
expected: the planning-infrastructure cleanup Bob directed — place every open construct architecturally, refactor the plan into milestones and pipelines CONDUCT can orchestrate, and stand up a verification process with a measured floor. NO area code touched: the two new scripts are instruments, and bundle.test.mjs was REPAIRED (it read an absolute container path, was absent from the npm test chain, and failed on an 11-character token fixture against livefire's 16-character floor). QUEUE.md deliberately NOT edited — it is CONDUCT's sole-writer file and CONDUCT was landing CAP-2 concurrently; the M0 items are handed over as a decomposition instead.
released: 2026-07-31 — docs only; nothing deployed, nothing pushed.

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-4
opened: 2026-07-31T00:00:00Z
paths: the CPDF-7 measurement section in docs/development/MEASUREMENTS.md (append-only), the D-118 row in docs/development/DEBT.md (Status column only), bio-plane/test/free-tier-fleet-probe.mjs (new probe, not in the battery)
interfaces consumed: none (a measurement)
interfaces owned: none
expected: QUEUE.md CPDF-7 / D-118 — MEASURE whether Workers Free permits a second Worker script and service bindings, and what a cross-Worker call costs, through the project's own Cloudflare account and egress. Deploys THROWAWAY Workers (cpdf7-probe-*) and TEARS THEM DOWN, never touching biosmoke7/civicos/newgroup, the record or the installer; attaches no R2 binding. Commits no shipped code and changes no dependency. Findings: the account is on Workers FREE (the API refused limits.cpu_ms with "not supported for the Free plan"); on Free a second script deploys, a service binding deploys and RESOLVES, and a cross-Worker call runs end-to-end for ~1 ms wall-clock / one subrequest (25 binding calls in one Free invocation succeeded; teardown re-confirmed, back to three scripts). So D-118's conditional does NOT fire: pdf-worker (CPDF-6) is viable on Free and central; Tier 1 (CPDF-4) is not forced to be the floor by any binding limitation. D-118 CLOSED; the residual 10 ms Worker-CPU-ceiling question is narrowed onto CPDF-1's existing gated follow-on. No deploy of the plane; CONDUCT integrates.
released: 2026-07-31 — measurement landed on branch content-pdf/cpdf7-fleet-measure; CONDUCT integrates. Not pushed.

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-5
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/pdfstructure.mjs, bio-plane/test/pdfstructure.test.mjs
interfaces consumed: I1 (bytes → content)
interfaces owned: none; extends the provisional I2 (structure → framework) producer-side with a `text` field (for FW-1 to confirm/counter)
expected: QUEUE.md CPDF-4 — Tier 1 text extraction IN THE PLANE, pure JS, no dependency. Parse the text-showing operators (Tj, TJ, ', ") in each page's content stream(s), reusing the existing object/stream parser (FlateDecode, /ObjStm), and decode the shown bytes to Unicode via each font's /ToUnicode CMap (beginbfchar/bfrange). EXTEND the extractor's existing I2 output object with `text` (document-level + per-page, plus per-region `undetermined` markers) — do NOT fork the output shape. `undetermined` is first-class and per-region: a glyph/run with no ToUnicode, a CID font, or an unmapped code is recorded as undetermined NAMING the cause (the font), never dropped, never guessed into mojibake. No op edit (op=pdfstructure returns the extractor's object, so text flows through automatically). No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (314f4b7). I2 extended with a `text` field (document/pages/undetermined markers, per-region markers naming the font/reason); registered provisional in INTERFACES.md for FW-1 to confirm/counter. battery 43/43 (2313); negative control run. index.mjs untouched.
opened: 2026-07-31T22:00:00Z
paths: the docprofile package — docprofile/index.mjs, docprofile/registry.mjs, docprofile/monitoring.mjs, docprofile/pipeline.mjs, docprofile/doctypes/index.mjs, docprofile/doctypes/registry.mjs, docprofile/doctypes/meeting-calendar.mjs, docprofile/doctypes/generic.mjs, docprofile/handlers/aspnet-webforms.mjs, docprofile/handlers/wordpress.mjs (member extraction removal only); the new docprofile/recogniser.mjs and docprofile/events.mjs; tools/bundle-docprofile.mjs (ORDER list); the flattened docprofile block in civicos-ui/app.html (rebuilt, not hand-edited); civicos-ui/test/docprofile.test.mjs and civicos-ui/test/change-layers.test.mjs (rewritten onto the consolidated shape)
interfaces consumed: I2 (structure, provisional — noted for FW-1/CPDF-4 that it will extend with text)
interfaces owned: none changed by this step (Step 0 is internal consolidation; I2 confirm/counter is FW-1)
expected: QUEUE.md FW-2 / D-68 / CONSTRUCTS Step 0 — the full reconciliation. One recogniser interface + one shared registry helper carrying both axes; one confidence ladder (CONFIDENCE, TYPE_CONFIDENCE merged); assess() the sole public entry point (monitor()/diffMembers/handler-derived contract() deleted, compare() demoted to the internal L3 primitive assess() calls); CONTRACT declared by the content type; a shared event catalogue; meaningful derived from SIGNIFICANCE. Must SHRINK the codebase and add no capability. Both npm run test:battery (bio-plane) and civicos-ui/test/run.mjs green; negative control on the single ladder / single entry point recorded in the suites.
released: 2026-07-31 — CONSTRUCTS Step 0 landed on main by CONDUCT; docprofile net -27 lines; civicos-ui/test/run.mjs + full battery green; negative controls (single ladder / sole entry point) confirmed.

## CLAIM 2026-07-31 RECORD
session: record-agent-1
opened: 2026-07-31T22:30:00Z
paths: the SCHEDULER block in bio-plane/src/store.mjs — specifically the reserved `alarm()` forwarder and the `onAlarm()` handler, the reconcile helper (renamed from `#rearmSchedule` to `#reconcileAlarm`), the arm helpers `#armScheduler`/`#armSweep`/`#armDrain`, the new consumer registry `#schedConsumers`, the `#lastDrainProgress` field, the `SCHED_GRACE_MS` constant, and the env-gated test-probe seam (`#probeSpecs`, `#probeState`, `schedProbeArm`, `schedProbeLog`); bio-plane/test/scheduler.test.mjs (new); the test:scheduler entry and the battery-chain entry in bio-plane/package.json; docs/development/SCHEDULER.md (new, the shape decision)
interfaces consumed: none
interfaces owned: I5 (the store schema — unchanged this turn; no table added or reshaped)
expected: QUEUE.md REC-1 / M1 — decide the plane's scheduler shape ONCE and build the mechanism. Generalises the `#armSweep`/`#rearmSchedule` single-reconciling-alarm pattern into a consumer registry where each consumer declares its own wake and the one DO alarm reconciles to the earliest, self-terminating when all are idle. Moves the two existing consumers (RECORD's selection sweep and CAP-2's task drain) onto the registry as proof. Does NOT touch CAPTURE's `taskDrain`/`taskEnqueue` bodies, the link/capture/task/reachability functions, or any schema table — only the alarm handler and reconcile/arm helpers, which PARALLELISM.md places with RECORD (the scheduler). No new op, so no control-plane surface change. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — landed on main by CONDUCT (4d77938). One reconciling DO alarm + consumer registry; both existing consumers (selection sweep, task drain) moved onto it with bodies unchanged; battery 43/43 (2272 assertions), scheduler suite + starvation negative control green; I5 unchanged.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-1
opened: 2026-07-31T00:00:00Z
paths: bio-plane/test/reachability.test.mjs, bio-plane/test/acquire.test.mjs, bio-plane/test/subresources.test.mjs (+ a one-line NEGATIVE CONTROL header comment), bio-plane/test/members.test.mjs — test files only; no plane behaviour touched
interfaces consumed: I3 (the control-plane op= dispatch)
interfaces owned: none
expected: QUEUE.md M0-1 — control-plane assertions for the three ops no suite reached (archivelookup, linkproject, signerlist) and for sourcereach, reached only at the Durable Object (the D-43 class). Reachability, not success: each op is driven through op= dispatch with a minimal valid-enough request and asserted to answer STRUCTURALLY rather than crash — a governed BIO refusal counts. Placements, each in the op's own area suite: sourcereach -> reachability.test.mjs (op=sourcereach read of the same "bio" store the suite drives at the DO); archivelookup -> acquire.test.mjs (a fresh store refuses NOT_ELIGIBLE by the D-104 fence — structured); linkproject -> subresources.test.mjs beside op=links (NEED_CAPTURE refusal proves the handler ran, then a valid capture projects with ok:true / edges[] / projected:number); signerlist -> members.test.mjs beside signeradd (ok:true, result.signers[]). Coverage BEFORE: 81/85 control-plane (95.3%), 1 durable-object-only (sourcereach), 3 unreached (archivelookup, linkproject, signerlist). AFTER: 85/85 (100.0%), 0 durable-object-only, 0 unreached. NEGATIVE CONTROL RUN 2026-07-31: deleted the linkproject dispatch block from subresources.test.mjs (its op= appears only there — the header comment was worded to NOT contain op=linkproject, so the instrument is not fed by the comment) and `npm run test:coverage` reported unreached 0 -> 1 naming linkproject (mutating); restored, back to 0. Recorded in subresources.test.mjs's first-60-lines NEGATIVE CONTROL comment. Full battery green: 42/42, 2264 assertions (was 2252; +12). No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT. CONDUCT confirmed via `npm run test:coverage` on the integrated state: 85/85, 0 DO-only, 0 unreached (was 81/85 + 1 DO-only + 3 unreached — the D-43 class closed for the current surface). The worker RAN its negative control (delete the linkproject dispatch → unreached 0→1) and recorded it in subresources.test.mjs; battery green.

## CLAIM 2026-07-31 RECORD
session: record-agent-2
opened: 2026-07-31T23:30:00Z
paths: in bio-plane/src/store.mjs — `acquireLease` (the lease-taking path, ~line 2761) ONLY: it gains a fail-closed guard that refuses a null/blank actor by name instead of tripping SQLITE_CONSTRAINT_NOTNULL (D-39 class). In bio-plane/src/index.mjs — the control-plane authorship stamps ONLY: the lease `actor` stamp (~line 2222) is extended to stamp a NAMED machine identity `token:<class>` for a machine credential and to delete a caller-supplied `actor` first (previously stamped only for a session, leaving a machine's actor caller-supplied/absent), and the op=promote body stamp (~line 2270) is extended so a machine credential's `author` is server-stamped `token:<class>` (deleted-then-set), matching the `by`/`viewer` machine convention already there, so the manifest NAMES the machine writer. New test bio-plane/test/unattended-lease.test.mjs; its entry in bio-plane/package.json (test:unattended-lease + the npm test chain). NO schema.mjs change (leases.actor stays NOT NULL TEXT; a machine identity string is a valid actor), so I5 is untouched and the D-113 purge list (leases already present) and hygiene check are unaffected.
interfaces consumed: I3 (the control-plane op= dispatch — additive authorship stamping, no op renamed, no classes/reason/shape changed)
interfaces owned: I5 (the store schema — UNCHANGED this turn; no table added or reshaped)
expected: QUEUE.md REC-2 / D-61 — an unattended (machine) writer cannot take a lease because leases.actor is NOT NULL and was stamped only from a session, so a daemon cannot complete a capture a member walked away from. DECISION: option (a), a named machine actor identity on the lease, NOT (b) dropping the lease from the refill path. Rationale in the suite header and the report: the lease is a courtesy lock worth keeping in the loop for the daemon (a returning member sees the job is being finished; two daemons do not both re-fetch), naming costs nothing and needs no schema change, and promote's CAS on `base` — the real integrity mechanism — is untouched, so a daemon that holds a lease still cannot cause a lost update. The writer is NAMED (server-stamped token:<class>, caller value deleted), never anonymous. accepts-when: a machine credential completes a capture a session started and the manifest author names the machine writer; NEGATIVE CONTROL: an anonymous (null/blank actor) lease is refused by name. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (e425b24). CONDUCT re-ran the anonymous-lease negative control (neuter the acquireLease guard → 20/2, restored → 22/22); battery 44/44 (2337). Battery 44/44 (2308 assertions); the new unattended-lease.test.mjs 22/22 and its negative control RUN (neuter the acquireLease guard -> 20/2, restored -> 22/22); coverage 85/85 ops reached, 0 unreached, no new op, suite declares its control. schema.mjs untouched (I5 unchanged); D-113 purge list + hygiene still green. node_modules was symlinked from the main checkout for miniflare and removed before commit.
## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-6
opened: 2026-07-31T00:00:00Z
paths: the CPDF-5 section of docs/development/MEASUREMENTS.md (append-only); bio-plane/test/tier1-coverage-probe.mjs (new probe, NOT in the battery); bio-plane/test/tier1-coverage-tier2-oracle.mjs (new probe, NOT in the battery)
interfaces consumed: I2 (structure -> text; consumes the CPDF-4 `text` field of extractPdfStructure, read-only)
interfaces owned: none — a measurement; commits no extractor and changes no shipped code
expected: QUEUE.md CPDF-5 — MEASURE Tier-1 text-extraction coverage on REAL Oakland PDFs to SIZE Tier 2 (the unpdf pdf-worker, CPDF-6). 14 real documents fetched 2026-07-31 across classes (agenda packets + staff reports from oakland.legistar.com via the per-file GUID; ACFRs, budget books, budget exhibits from oaklandca.gov via the honest contact-URL agent), each with source URL. Ran extractPdfStructure (Tier 1, pure JS) on each and read the per-region text.undetermined markers; ran unpdf/pdf.js (Tier 2) on the SAME bytes as an ORACLE so "the residue is recoverable by the pdf-worker" is measured, not assumed. FINDINGS: per-document buckets — Tier 1 FULLY 4/14 (agenda packets, modern ACFR, slide deck), Tier 1 PARTIAL 3/14 (budget books ~88%, transmittal 41%), Tier 2 REQUIRED 5/14 (2 encrypted incl. the 2025 ACFR, 3 fonts-without-ToUnicode; unpdf recovers all measured), OCR-only 2/14 (scanned/image — neither tier). Two failure causes zero out whole documents: permission-only ENCRYPTION (Tier 1 has no decryption; pdf.js decrypts empty-password Standard/AES transparently — measured on the Legistar staff report, 13,012 clean chars) and fonts with no /ToUnicode (Tier 1 refuses to guess; pdf.js maps glyphs — cafr-2003 739 chars Tier1 vs 278,758 Tier2). CONCLUSION: the pdf-worker (CPDF-6) is CENTRAL not marginal — the staff-report class (the substance an agenda links to) and encrypted ACFRs need it; Tier 1 remains valuable, fully serving agendas + modern books/decks (~50% of the sample in-plane, free). Extractor gap recorded: Tier 1 does not NAME encryption (degrades to objstm_undecodable/content_stream_undecodable notes though /Encrypt is in the trailer) — a reason:"encrypted" marker recommended for CPDF-4/6. Runtime caveat for CPDF-6: unpdf/pdf.js threw Math.sumPrecise on one doc on node v26.5.0 — the pdf-worker must pin a Workers-runtime-verified version. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (62a650b). Verdict: pdf-worker (CPDF-6) is CENTRAL (~36% of the class-spread sample needs Tier 2). OCR gap lifted to DEC-4. Probes confirmed not in the battery.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-4
opened: 2026-07-31T00:00:00Z
paths: in bio-plane/src/store.mjs — the archive-fallback MONITORING consumer (CAPTURE's tick logic): the new `#monitorTickMs`/`#monitorToken`/`#monitorConfigured`/`#monitorFloor`/`#monitorPending`/`#monitorTick`/`#fireArchiveFallback` methods and the `MONITOR_TICK_MS`/`MONITOR_TICK_BATCH` constants placed beside the reachability code, and the arm added to `recordSourceOutcome` (made async; arms the scheduler on a COUNTED real failure only, never on `governed` or `success`); AND the one authorised cross-area touch of RECORD's scheduler — appending the `archive-monitor` entry to `#schedConsumers`, and in `onAlarm` awaiting `c.tick(now)` (so an async consumer's work completes within the alarm) and wiring the `archive-monitor` result through the return. bio-plane/test/archive-monitoring.test.mjs (new); its test:archive-monitoring entry and the battery-chain entry in bio-plane/package.json
interfaces consumed: none (I3 op=acquire/op=recordsourceoutcome/op=sourcereach reached, not changed)
interfaces owned: none; no schema table added or reshaped (I5 unchanged)
expected: QUEUE.md CAP-3 — make a periodic MONITORING tick actually INVOKE the archive fallback (built, live-verified, IDLE). RECORD owns the scheduler but is DORMANT and CONDUCT authorised this cross-area touch; kept to appending ONE consumer + arming it (SCHEDULER.md's documented extension) plus the minimal `await` onAlarm needs for an async consumer — no second alarm, no cron, no reshape. The tick consults `sourcereach` for `fallback_eligible` documents (three consecutive REAL source failures, or the aged arm; a governed refusal is our own politeness and moves no counter — D-104, read before touching it) and fires the fallback for each by invoking op=acquire with `via:"archive.org"` and the DOCUMENT address over `env.SELF` under a daemon credential, producing the two-hop grade-C chain from the one acquire path (fence + provenance hop unchanged). INERT unless `env.SELF` (a service binding to this instance's own Worker) and a daemon token are present — the SCHED_PROBE-seam pattern — so every existing suite is byte-for-byte unaffected. accepts-when bio-plane/test/archive-monitoring.test.mjs: three source_refused outcomes drive the tick to fire the fallback (grade C, 2-hop chain, failing run reset by the archive success = the RULED re-fetch-for-monitoring). NEGATIVE CONTROL RUN: make recordSourceOutcome's `governed` branch increment consecutive_failures (drop the D-104 exclusion) -> three governed refusals fire a SPURIOUS fallback; restored, recorded in the suite's first-60-lines NEGATIVE CONTROL comment. Live wiring of env.SELF is per-instance (the instance name IS the worker name) and belongs to the installer/CONDUCT — raised as a decision item. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — landed on main by CONDUCT (dea4c13). battery 45/45 (2361); the trust-critical governed-refusal exclusion is the EXISTING D-104 mechanism CAP-3 reads (reachability.test 40 + archive-monitoring.test 22 verify it end-to-end, plus the worker's negative control governed→spurious fallback→4 fail). Mechanism proven; LIVE arming is the DELEGATION below + DEC-5.

## DELEGATION 2026-07-31 CAPTURE → DIST
from: capture-agent-4 / CAP-3 (recorded by CONDUCT at integration)
need: the monitoring tick is built and tested but INERT on live instances until `env.SELF` (a service binding to the instance's OWN Worker) and a daemon token are provisioned. The instance name IS the worker name, so the self-binding target must be TEMPLATED per-instance by the installer (newgroup) and the plane deploy — out of bounds for CAPTURE. Until then the archive fallback stays "built and idle" (the consumer no-ops without the binding). Composes with CPDF-6's pdf-worker service binding — the same "how does a Worker call another Worker on this instance" provisioning.
why: the DIST/installer half of making resilience actually run; gated (deploy + installer) and carrying a token-exposure surface — see DEC-5.
status: open
## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-7
opened: 2026-07-31T00:00:00Z
paths: pdf-worker/** (new fleet member: src/index.mjs, wrangler.jsonc, package.json, scripts/build.mjs, fleet-member.json, .gitignore, the committed dist/pdf-worker.bundled.mjs, test/pdf-worker.test.mjs); the service-binding wiring in bio-plane/src/index.mjs (op=pdfstructure escalation + the needsTier2 helper — RECORD's I3 control plane, see the DELEGATION note) and bio-plane/wrangler.jsonc (the PDF_WORKER service binding); bio-plane/src/pdfstructure.mjs (CONTENT-PDF's own — Tier-1 encryption detection: isEncrypted + the encrypted text marker); bio-plane/scripts/coverage.mjs (M0-5 fleet enumeration); tests bio-plane/test/pdf-worker-binding.test.mjs and additions to bio-plane/test/pdfstructure.test.mjs; docs INTERFACES.md (I2 text reasons + I6 as-built), MEASUREMENTS.md (append), this CLAIMS entry
interfaces consumed: I1 §2 (the R2 key shape — promoted to a load-bearing dependency with a second consumer); I3 (the op= dispatch — additive: op=pdfstructure gains a Tier-2 escalation, no op renamed, no classes/reason/shape changed)
interfaces owned: I6 (plane → pdf-worker — the worker now EXISTS; as-built shape recorded in INTERFACES.md, still PROVISIONAL pending a DIST deploy and FW-1); extends the provisional I2 text shape with `encrypted`/`over_envelope`/`no_text_layer`/`tier2_extraction_error` reasons and a top-level `tier`
expected: QUEUE.md CPDF-6 + M0-5 — build pdf-worker, the FIRST fleet member (I6): holds unpdf 1.8.0 (PINNED, verified extracting on workerd — its real runtime, where pdf.js Math.sumPrecise resolves; a guarded polyfill covers node), reads bytes from R2 itself (CAPTURES read binding, never PUBLISHED, no STORE), returns the record's I2 structure+text shape, WRITES NOTHING. The plane's op=pdfstructure runs Tier 1 in-plane and escalates over a service binding (PDF_WORKER) only when Tier 1 got essentially nothing (undetermined regions > decoded chars — CPDF-5's encryption + no-/ToUnicode buckets), never for a budget book at ~88%; absent the binding it degrades to Tier 1, named, no crash (an un-fleeted instance, D-115). A document over the envelope (MAX_PDF_BYTES, default 16 MB) returns text-undetermined(over_envelope), never truncated. Tier-1 now NAMES encryption (reads the Standard Security Handler from the trailer without decrypting — the CPDF-5 gap). M0-5: coverage.mjs enumerates fleet members (discovered from fleet-member.json, never hand-listed) and holds each to surface-ops-reached + a declared control. accepts-when ALL GREEN: (1) the plane returns "Hello Oakland 2026" for a no-/ToUnicode CID-class PDF Tier 1 could not decode, through the binding (pdf-worker-binding.test.mjs 12/12); (2) the worker writes nothing — R2 byte-for-byte unchanged + no put/delete in source, no PUBLISHED/STORE binding (pdf-worker.test.mjs 28/28); (3) an over-envelope document returns text-undetermined not truncated; (4) test:coverage lists pdf-worker and its `structure` surface. NEGATIVE CONTROLS RUN 2026-07-31: (a) needsTier2->false -> the two Tier-2 binding assertions fail (10/2), restored 12/0; (b) inject env.CAPTURES.put into the worker, rebuild -> the R2-unchanged + source-scan assertions fail (25/3), restored 28/0; (c) add a stub fleet member with an untested op -> coverage reports it UNREACHED and --strict exits 1, removed -> clean. Battery 45/45 (2360). DELEGATIONS: the op=pdfstructure escalation call site is in index.mjs (RECORD's I3) not CONTENT-PDF's paths — landed here in one turn with the member per the CPDF-6 item, flagged to CONDUCT; and the DEPLOY of the member + binding is DIST's (a new Worker + service binding are a gated release; installer must install the FLEET, D-115/D-116). No push, no deploy.
released: 2026-07-31 — landed on main by CONDUCT (7e5b67f). First fleet member (pdf-worker) + M0-5 fleet coverage; battery 46/46 (2384); write-nothing verified structurally; unpdf pinned for workerd. RECORD I3 (escalation call site) + DIST (deploy/install fleet) delegations recorded; live-verify (CPDF-3) gated.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-2
opened: 2026-07-31T00:00:00Z
paths: the I2 section of docs/development/INTERFACES.md (Status/Version/reasoning); docs/development/INTERFACE-CHANGES.md ONLY IF a counter were needed (it was not — no counter, so this file was not created)
interfaces consumed: I2 (structure -> framework) — the interface being confirmed; read pdfstructure.mjs (producer) and docprofile/** (consumer) to judge it
interfaces owned: I2 (FRAMEWORK is the consumer/owner; this turn is the confirmation that turns it STABLE)
expected: QUEUE.md FW-1 — confirm or counter the provisional I2, the EXTENDED shape (link partitions + text/undetermined markers + tier). Judged as the consumer's owner whether the shape CONTENT-PDF produces serves FRAMEWORK's need to identify content and intent. VERDICT: CONFIRMED, no change. WHY: (1) container-agnostic BY CONSTRUCTION — the four partitions/wrappers are subresources.mjs's own, imported not re-derived, so docprofile's content-type recognition and connections() consume PDF and HTML links through one path (Bob's "content in PDFs as in HTML"); (2) undetermined first-class on BOTH axes (link why + text Marker naming the font) is the never-invent/state-the-gap doctrine docprofile's every default rests on, letting a doctype honestly refuse instead of parsing mojibake; (3) it gives the two doctype inputs — text.document/pages[] for L1/L4/L5 recognition+meaning, links[] partitioned for L6 referential/temporal (intra content-addressed by sha256 = a stronger referential key than an href), keyed on a shared 0-based page index so they compose; (4) tier is stamped at the op boundary the consumer actually reads (op=pdfstructure: 1 in-plane, 2 pdf-worker); (5) the flat no-layout text is the honest Tier-1 maximum — richer positional/table structure is a future doctype-driven ASK via the change protocol, not a reason to hold the contract provisional. Touched NO plane code: the shape confirmed is already the shape the battery pins (pdfstructure.test.mjs partitions/wrapper-byte-identity/counts/text.*, pdf-worker tests text+tier), so a redundant assertion was unwarranted. I2 set to Version 1.0.0 / Status STABLE (matching I1/I3/I6's stable convention). One producer-side residual noted: CONTENT-HTML emitting the same text shape (link parity already by-construction) — CONTENT-HTML's to raise via INTERFACE-CHANGES.md when it activates, not a consumer reservation. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (44a867a). I2 STABLE 1.0.0; docs-only, battery 46/46 unaffected.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-5
opened: 2026-07-31T00:00:00Z
paths: in bio-plane/src/store.mjs — the reuse-verification helpers ONLY: new `reusedParts` (enumerate a bundle's reused subresource parts by joining `site_asset_refs.reused=1` to `register` and `site_assets`), `recordReuseVerdicts` (append rows to the new `reuse_verdicts` table), `reuseVerdicts` (read them), the POST-HOC verdict write added inside the existing `recordSiteAssets` change-case (phase `posthoc`), the three new DO routes (`reusedparts`/`recordreuseverdicts`/`reuseverdicts`) in the dispatch map, and the `DELETE FROM reuse_verdicts` line in `purge`'s whole-store arm. In bio-plane/src/index.mjs — the `op=ratify` handler ONLY: after publish, re-fetch every reused part with a PLAIN GET bounded by the calibrated `capture_limits` ceiling, record `confirmed`/`changed`/`unavailable`/`not_attempted`, and surface the summary on the response. In bio-plane/src/schema.mjs — ONE new derived table `reuse_verdicts` placed BEFORE the `host_governor` block (I5, additive only). New test bio-plane/test/reuse-ratify.test.mjs; its test:reuse-ratify entry and the npm-test-chain entry in bio-plane/package.json.
interfaces consumed: I3 (the control-plane op= dispatch — op=ratify gains a re-fetch-and-record step, no op renamed, no classes/shape changed) 
interfaces owned: I5 (the store schema — ADD ONLY: one derived table `reuse_verdicts`, named in purge and in the D-113 hygiene coverage, no existing table reshaped)
expected: QUEUE.md CAP-4 / CAPTURE-SCALING.md item 6, the four decided specifics. (a) POST-HOC reuse verification from `site_assets`, unconditional, zero requests, verdicts appended and dated (persisted in the change-case of recordSiteAssets, phase `posthoc`). (b) At ratification re-fetch every reused part — MANDATORY as an attempt and a record, never as agreement: `confirmed`/`changed`/`unavailable` all ratify; ratifying with a reused part while saying nothing is forbidden. (c) A PLAIN GET, not `If-None-Match`: our own SHA-256 over what we received is the evidence where a 304 is only the origin's assertion (reverses the conditional-GET suggestion elsewhere in that document, right for working capture, wrong here). (d) A fourth outcome `not_attempted`, recorded WITH its reason, for parts the invocation's budget (the calibrated `capture_limits` ceiling) could not reach — never silently omitted. accepts-when bio-plane/test/reuse-ratify.test.mjs: a bundle ratified with reused parts records an outcome for EVERY reused part; a dark source still ratifies as `unavailable`; a bundle whose reuse count exceeds the calibrated ceiling ratifies with the residue `not_attempted`. NEGATIVE CONTROL: drop the outcome record for a part and the suite FAILS naming the unrecorded part. D-113 purge/hygiene re-checked green for the new table. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (c7c57c9). battery 47/47; reuse_verdicts in purge, hygiene 138 + purge 14 green; coverage 85/85.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-2
opened: 2026-08-01T00:17:53Z
paths: bio-plane/test/check-firing.test.mjs (NEW — the only file touched). No plane behaviour, no catalog, no other suite.
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-3 — name the 33 checks no assertion named. One assertion per check that TAMPERS a conformant bundle and requires THAT check id in the findings, closing the S-7 direction (a check run only where it passes is unproven). New suite check-firing.test.mjs builds a conformant bundle per object type (the exact field set setup.mjs's mdFor writes, proven clean by conformance.test.mjs), tampers exactly one thing, and asserts BOTH ways: the check FIRES on the tamper AND is ABSENT from the untouched base (the built-in negative control). All 33 named this turn: C-1.3 C-2.1 C-2.2 C-2.3 C-2.4 C-2.6 C-2.9 C-2.10 C-3.1 C-6.3 C-7.1 C-8.1 C-9.1 C-10.1 C-11.1 C-12.1 C-12.2 C-13.1 C-14.1 C-14.2 C-14.3 C-14.4 C-15.1 C-16.1 C-16.2 C-16.3 C-16.4 C-16.5 C-17.1 C-18.3 C-18.4 C-18.7 C-18.8. accepts-when: `npm run test:coverage` reports 51/51 checks named (was 18/51); check-firing.test.mjs 70 pass, 0 fail; battery 48/48 (2493). NEGATIVE CONTROL declared in the suite's first 60 lines and SPOT-CHECKED on C-2.1, C-13.1, C-16.4: removing the tamper (bundle == base) drops the finding and flips `fires` true->false. No push, no deploy — CONDUCT integrates. M0-3 is COMPLETE (no split needed; all 33 landed in one turn).
released: 2026-08-01 — handed to CONDUCT.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-3
opened: 2026-08-01T00:33:43Z
paths: bio-plane/package.json (the `test` script ONLY — the hand-maintained `&&` chain replaced by `node scripts/battery.mjs`; `test:battery` and every `test:*` alias unchanged); bio-plane/scripts/battery.mjs (skip-awareness — tally reads an optional named skip count, a wholesale `SKIPPED — reason` marker gets its own `skip` status and is surfaced by name in the summary, neither counting as a failure); bio-plane/test/ratify.test.mjs, bio-plane/test/reuse-ratify.test.mjs, bio-plane/test/signpage.test.mjs (each gains a top-of-file `spawnSync("ssh-keygen")` guard that SKIPS LOUDLY with a named reason and exits 0 instead of dying with an unhandled spawn error); bio-plane/test/sshsig.test.mjs (the `else` branch now NAMES and COUNTS the 2 skipped fresh-signature cases on the tail line). Doc corrections of superseded text (same-turn, ORCHESTRATION rule 6): docs/development/VERIFICATION.md (the "battery runs every suite" section + floor item 4) and CLAUDE.md (verification-discipline step 1) — both said `npm test` chains with `&&`, now false. QUEUE.md NOT touched (CONDUCT's sole-writer file — M0-4 `landed:` is CONDUCT's to fill). DEBT.md D-93 NOT closed here (CONDUCT closes at integration).
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-4 — close BOTH halves of D-93. First half: `npm test` becomes the discovering runner, so a crashing suite can no longer hide the suites behind it; both `npm test` and `npm run test:battery` now run every suite and report all of them. Second half: a suite that wholesale-depends on stock ssh-keygen for signing (ratify, reuse-ratify, signpage) detects its absence and SKIPS LOUDLY WITH A NAMED REASON (`name: SKIPPED — ssh-keygen not on PATH; …`) or would fail loudly — never quietly does less; and sshsig, which has a pinned-fixture fallback, runs its 16 pinned assertions and NAMES the 2 fresh-signature cases it skips (`16 pass, 0 fail, 2 skip (fresh signature verifies; fresh signature bound to its bytes; ssh-keygen absent)`) instead of silently reporting 16 (the 16-vs-18 case). Only ratify + sshsig were named in the item; reuse-ratify + signpage share the same wholesale-ssh-keygen dependency and were failing loudly once the runner stopped hiding them, so the accepts-when (green when ssh-keygen hidden) required the same guard — added and claimed here. accepts-when GREEN: normally battery 48/48 (2493 assertions); with ssh-keygen hidden from PATH `npm test` COMPLETES exit 0, 45/48 green + 3 SKIPPED-by-name + sshsig ran-short-by-2-named, every other suite still runs (none hidden). NEGATIVE CONTROLS RUN 2026-08-01, restored, recorded in each suite's first-60-lines comment: (ratify) remove the guard + hide ssh-keygen -> `FAIL ratify.test.mjs ... assertions unknown`, a bare failure with no named reason (other suites still ran — the runner does not hide them); (sshsig) silence the skip note + hide ssh-keygen -> a bare `16 pass` with no hint two cases were skipped, the exact D-93 shortfall. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-08-01 — handed to CONDUCT.
## CLAIM 2026-07-31 RECORD
session: record-agent-3
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/setup.mjs — the installer wizard surface (RECORD/REC-3 scope): `acquireWhy` (delete the stale NO_AUTHORITY branch, D-110) and `mdFor` + its one caller in the `#n-save` handler (emit `content_hash: sha256:<primary>` when a document is attached, D-62). bio-plane/src/index.mjs — the op=promote control-plane body stamp ONLY (~line 2419, beside the existing author/ownerMemberId stamps): server-stamp `surfaced_by` in a focus/problem CREATION's bundle.md from the caller's ACTOR CLASS (viaSession→human, machine→agent), caller value overwritten, bundle.md sha recomputed (D-78). NO store.mjs change and NO schema change — the store keeps byte-trusting bundle.md (40+ suites depend on that; see the surfaced_by writers note below). New tests bio-plane/test/empty-body.test.mjs (D-39), bio-plane/test/setup-honesty.test.mjs (D-110 + D-62), bio-plane/test/surfaced-by.test.mjs (D-78). DEBT.md — D-39/D-110/D-62/D-78 dispositions.
interfaces consumed: I3 (the control-plane op= dispatch — additive: op=promote gains a server-side surfaced_by stamp for focus/problem creations; no op renamed, no classes/reason/shape changed)
interfaces owned: I5 (the store schema — UNCHANGED this turn)
expected: QUEUE.md REC-3 — four honesty defects in the plane's own surfaces. D-39: an empty POST body was found already guarded at the Durable Object (store.mjs fetch, commit 6ac72d0a) so it returns a named JSON reason, NOT a Cloudflare 1101 — the code fix had landed but no suite locked it in and the debt stayed open; empty-body.test.mjs drives an empty POST to the five debt-named ops (stats/index/dangling/searchfields/selectionlist) and asserts a structured reason. D-110: acquireWhy still explained NO_AUTHORITY, the refusal D-97 removed; deleted (grep confirms NO_AUTHORITY existed only there). D-62: the wizard's mdFor omitted content_hash for a document bundle, so C-2.7's verified branch refused it and the hash: facet could not find it; now emits `content_hash: sha256:<primary capture sha>`, mirroring civicos-ui's already-landed port, absent for typed intake where inventing one would be a lie. D-78: both writers hardcoded `surfaced_by: human`; rather than trust a caller-asserted literal (and rather than break the 40+ suites that rely on the store byte-trusting bundle.md — e.g. projection.test asserts an arbitrary surfaced_by projects), the value is now SERVER-STAMPED at the trust boundary in index.mjs, exactly where author/owner/by/viewer already are: a focus opened by a machine credential honestly records `agent`, one opened by a member `human`. This fixes BOTH writers (and civicos-ui) at once with no cross-area delegation, since the store, not the surface, is now authoritative. accepts-when: an empty POST to five ops returns a named reason (not a 1101); a wizard bundle with a document carries content_hash and drops the C-2.7 content_hash finding; a focus written by an agent records surfaced_by: agent. NEGATIVE CONTROLS RUN — recorded in each suite's first-60-lines NEGATIVE CONTROL line. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (a0c6d98). battery 51/51, coverage 85/85. CLAIMS conflict (vs M0-4) resolved union. D-39/62/78/110 closed by worker; I3 additive stamp registered; D-121 opened for the revision-carry residual.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw3
opened: 2026-07-31T00:00:00Z
paths: the op=acquire body in bio-plane/src/index.mjs (the docprofile import at the top, and the profile-writing block just before the acquire response literal — adds a `document.profile` sibling field, changes no existing field); bio-plane/test/profile.test.mjs (new); the test:profile entry in bio-plane/package.json; docs/development/INTERFACES.md (I1 version bump note for the additive `document.profile` field)
interfaces consumed: I1 (bytes → content) — WRITER side (op=acquire), additive
interfaces owned: none (FRAMEWORK owns docprofile, which is READ here, never copied; I1 is CAPTURE's and this is the FW-3 DELEGATION with FRAMEWORK's guidance)
expected: QUEUE.md FW-3 / CONSTRUCTS Step 1 — the plane RECORDS THE PROFILE. op=acquire calls docprofile's identify() (host stack) and doctypeFor() (content type) — the existing functions, not a second copy — and writes onto the acquire document a `profile` field carrying: the handler (key/label/version) and its confidence + signals; the content type (key/label/version) and its confidence + signals; the handler's declared normalisation and boundary; the document kind; the source content-type; and the profiling instant. The recogniser key+version on BOTH axes IS the "author + version" the ladder above needs so a wrong judgment can later be found and revised. This is the FIRST plane consumer of docprofile (until now consumed by zero plane files). I1 VERDICT: ADDITIVE — `document.profile` is a new sibling field on the acquire document; no existing field's shape or meaning changes, and C-18.1 tolerates extra keys (checks required keys only). accepts-when bio-plane/test/profile.test.mjs: an op=acquire of an ASP.NET-WebForms meeting-calendar page carries document.profile naming handler aspnet_webforms + content_type meeting_calendar, both confidences, both signal lists, the normalisation, and the recogniser versions. NEGATIVE CONTROL: stop stamping the handler (delete profile.handler) → the handler assertions fail. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (02491bd), clean cherry-pick. battery 52/52, coverage 85/85. I1 additive bump verified (C-18.1 tolerates extra keys); no INTERFACE-CHANGES protocol needed. No DEC.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw4
opened: 2026-07-31T00:00:00Z
paths: the op=acquire body in bio-plane/src/index.mjs (the docprofile import at the top gains `digests` + `CONFIDENCE`; a new module-level `sha256Hex` helper beside `fingerprint`; and, in the profile block just after `document.profile` is built, capture the primary bytes and set `profile.digests` — the COMPUTED normalisation digests. Adds a sub-field, changes no existing field). bio-plane/checks/bio-checks.mjs — `checkRegisterIntegrity` (C-18.3) ONLY: a SECOND bucket keyed by the determined evidentiary digest, so two register documents whose SUBSTANCE is identical though their raw bytes differ (viewstate/boilerplate) are folded as corroboration, not two review items; the raw-sha arm is unchanged and the normalised arm never fires on two undetermined (null) digests or on a pair already caught by identical raw bytes. New test bio-plane/test/framework-digest-audit.test.mjs; its test:framework-digest-audit entry in bio-plane/package.json. Possibly one line in bio-plane/test/check-firing.test.mjs (a normalised-arm firing case for C-18.3).
interfaces consumed: I1 (bytes → content) — WRITER side (op=acquire), additive: `document.profile.digests` is a new sub-field
interfaces owned: none (FRAMEWORK owns docprofile, which is READ here — `digests()` — never copied; I1 is CAPTURE's and this is the FW-4 DELEGATION with FRAMEWORK's guidance)
expected: QUEUE.md FW-4 / CONSTRUCTS Step 2 — the plane COMPUTES and STORES the normalisation digests on the capture, per the handler's declared normalisation policy FW-3 recorded. The framework defines THREE digests (docprofile/index.mjs, DOCUMENT-PROFILES.md "Three digests, not one"): identity = sha256 of raw bytes = `capture_sha` (I1 §1, REUSED, never recomputed under a new name), rendition = mechanical-normalised, evidentiary = presentational+mechanical-normalised. op=acquire stores `profile.digests = { determined, rendition, evidentiary, boundary_missed, basis }`. `determined` is true ONLY when the bytes were read as text AND the stack was identified with CERTAINTY (a signal only that stack emits): the failure asymmetry runs the OTHER way for deduplication than for change — folding two distinct review items into one corroboration HIDES a document — so unlike compare() the trust is NOT extended to the conservative handler, and an undetermined capture (PDF/multipart/uncertain) records rendition+evidentiary ABSENT (null), never a fabricated value. Then op=audit's duplicate sweep (C-18.3, run by auditPass→checkBundle) gains the normalised comparison so it catches a duplicate whose viewstate/boilerplate differs, which today it cannot see (raw bytes only). The Add-surface already-held check (civicos-ui) is a DOWNSTREAM consumer of the stored digest — recorded, NOT built here. I1 VERDICT: to be confirmed additive (a new sub-field under the existing `document.profile`; no existing field reshaped; C-18.1 tolerates extra keys) → register I1 1.2.0; else INTERFACE-CHANGES. accepts-when bio-plane/test/framework-digest-audit.test.mjs: two op=acquire captures of the same document differing ONLY in viewstate/boilerplate report as duplicates through op=audit (their evidentiary digests are equal though raw shas differ); two genuinely different documents do NOT; two undetermined (PDF) documents do NOT. NEGATIVE CONTROL: drop the normalised-digest write and the sweep misses the viewstate pair again. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (2145416), clean cherry-pick. battery 53/53, coverage 85/85, checks 51/51. I1 1.2.0 additive verified (conformance.test green with real acquire docs carrying digests). FW→UI delegation kept open for when UI activates. No DEC.

## DELEGATION 2026-07-31 FRAMEWORK → UI
from: framework-agent-fw4 (FW-4, CONSTRUCTS Step 2)
need: the Add-surface already-held check in civicos-ui (today it fetches BOTH captures and compares them in the browser) should consume the plane's newly STORED normalisation digest instead. As of FW-4, op=acquire records `document.profile.digests.evidentiary` (a 64-hex sha when `determined:true`, else null), persisted by op=promote into data/provenance.json and registered as I1 1.2.0 §4c. The already-held check can compare a new capture's evidentiary digest against held captures' stored evidentiary digests to recognise "the same document, different viewstate/boilerplate" WITHOUT re-fetching and re-normalising both in the browser — which is the same duplicate op=audit's C-18.3 now folds server-side. Two rules the UI must honour, both structural, not optional: (1) compare evidentiary digests ONLY when both are `determined:true`; a null/undetermined digest is never equal to anything, including another null (an equality that costs nothing to produce is not evidence). (2) `determined:false` (a PDF, a multipart giant, or a merely-likely stack) means fall back to the raw-bytes/current behaviour for that capture, never treat absence as a match or a mismatch.
why: FW-4's scope explicitly RECORDS this consumer and does not build it (QUEUE.md FW-4; the constraint that UI paths are not edited by FRAMEWORK). The stored digest now exists and is stable at I1 1.2.0, so the UI work is unblocked whenever UI activates (M8 / UI-PLAN). No I1 change is needed to do it — the field is already additive and registered.
status: recorded, not started — UI is DORMANT. No plane change owed by this delegation; it consumes an already-landed additive field.
## CLAIM 2026-07-31 M0 (VERIFICATION)
session: m0-agent-m02
opened: 2026-07-31T00:00:00Z
paths: header comments only (first 60 lines) of the bio-plane/test/**/*.test.mjs suites that do not yet declare a negative control — a single `NEGATIVE CONTROL:` line each, comment-only, no assertion or subject changed. Temporary src mutations are made ONLY to RUN each control and are git-restored immediately (no src committed). pdf-worker/test already declares its control (unchanged).
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-2 — backfill the negative-control register across every suite that lacks one (41 of 51 bio-plane suites; the other 10 + the pdf-worker fleet member already declare theirs). RUN each control (break the named subject, run the suite, record the REAL observed failure, restore), never infer it. accepts-when: `npm run test:coverage` reports every suite declaring a control; three spot-checks with their real observed failures in the report. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (0dbcb86), CLAIMS union-resolved. battery 53/53, coverage 53/53 declaring after CONDUCT fixed the two new-suite NC formats. No dishonest suite found. M0-6 (hygiene gate) now UNBLOCKED (M0-1/2/3 all done).

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw5
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (two NEW tables `readings` and `reading_refs` inserted BEFORE the host_governor block, after reuse_verdicts — additive, no existing table changed); bio-plane/src/store.mjs (a `#writeReadings` call inside promote()'s transaction beside the register/refs writes, the `#writeReadings` method, the read methods `readingFor` + `documentsByReference`, two router-map entries `reading`/`readingref`, and the two new tables added to the purge TABLES list for D-113); bio-plane/src/index.mjs (op=acquire: the reading computed from the resolved doctype's parse() over the primary text FW-3/FW-4 already read, carried as a new `document.reading` sibling; two new read ops `reading`/`readingref` in the OPS table + a READING_READS set spread into SESSION_OPS member/admin); bio-plane/test/reading.test.mjs (new) + its test:reading entry in bio-plane/package.json; docs/development/INTERFACES.md (I5 additive version bump to 1.1.0 + the two tables' shape/ownership note).
interfaces consumed: I1 (docprofile is READ at op=acquire — parse() via the resolved doctype — never copied)
interfaces owned/extended: I5 (the store schema) — ADDITIVE: two new derived tables `readings` + `reading_refs`; no existing table's columns change, so no INTERFACE-CHANGES protocol is needed.
expected: QUEUE.md FW-5 / CONSTRUCTS Step 3 — READINGS ARE PERSISTED. op=acquire calls the resolved doctype's parse() on the captured text to produce a reading { entities[], facts } (framework:480), carried on the acquire document; op=promote DERIVES it from data/provenance.json (mirroring refs-from-frontmatter, in the same transaction) and persists it into `readings`, indexing every entity by the RAW source-assigned reference it carries (`ref` = kind:key, e.g. meeting:2101 — NOT resolved to a canonical entity; that is Step 4 / D-83 and is NOT built here). A reading that finds nothing (a doctype with no reader, bytes not read as text, or a reader that found/threw nothing) is persisted HONESTLY as failed/empty (found:false, no entities), never fabricated (framework:489). Reads: op=reading (by capture_sha) and op=readingref (the reverse index, by ref). I5 VERDICT: ADDITIVE (new derived tables only) → I5 1.1.0. accepts-when bio-plane/test/reading.test.mjs: acquire+promote a meeting-calendar document, retrieve its reading (entities[]+facts) by capture_sha, and look it up by an entity reference (meeting:2101) to get the document(s) back — including the cross-document case where two captures both carry the reference; the failed-reading case (generic page / PDF) persists honestly. NEGATIVE CONTROL RUN: dropping the persist makes op=reading return found:false and the reference lookup return count 0 for a document known to carry it. D-113: both tables added to op=purge's whole-store arm; hygiene + purge tests green. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (aee6d52), CLAIMS union-resolved. battery 54/54, ops 87/87 (0 unreached), checks 51/51. I5 1.1.0 additive (two derived tables, purge-covered). No DEC. Cross-document reference lookup confirmed unresolved-but-joinable — sets up Step 4 (D-83).
## CLAIM 2026-07-31 M0 (VERIFICATION)
session: m0-agent-m06
opened: 2026-07-31T00:00:00Z
paths: bio-plane/test/planning-hygiene.test.mjs (NEW — the planning-drift gate); bio-plane/package.json (two scripts ONLY — `test:hygiene` now runs `node scripts/battery.mjs hygiene` so it reports BOTH hygiene suites without &&-stopping, and `test:coverage` now runs `scripts/coverage.mjs --strict` so the floor cannot fall back per VERIFICATION.md step 5); docs/architecture/CONSTRUCTS.md (the "## The plan: bottom up, and each step has a consumer" order-of-work list ONLY — added a status-per-item convention note under the heading and a status marker to each of the 13 steps: Step 0 `BUILT (FW-2)`, Step 1 `BUILT (FW-3)`, Step 2 `BUILT (FW-4)`, Step 3 `QUEUED FW-5`, Steps 4/5/5a/6/7/8/8a/8b/9 `UNSCHEDULED`). NO other file changed — DEBT.md and QUEUE.md carried no drift (all 121 DEBT rows already held a disposition token; the only concrete QUEUED cross-reference, CAPTURE-SCALING's `QUEUED as CAP-4`, already resolves). Temporary DEBT.md/CONSTRUCTS.md mutations were made ONLY to RUN the two negative controls and were reverted (nothing of them committed). node_modules was symlinked from the main checkout for the miniflare battery and REMOVED before commit.
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-6 — the hygiene check that closes the planning-drift CLASS, on D-113's precedent, wired into the standing battery gate so a session that never runs plancheck still cannot drift. planning-hygiene.test.mjs enforces three invariants (MILESTONES.md "How this file stays true" 1/2 + the QUEUED cross-reference the status vocabulary carries): (1) every OPEN DEBT row carries a disposition token — plancheck.mjs's exact predicate, ported so the battery enforces it too; (2) every `QUEUED <ID>` cross-reference names an item that EXISTS in QUEUE.md (ids read from QUEUE.md's own `### <ID> ·` headings, never hand-listed); (3) every design-doc ORDER-OF-WORK item carries a status marker (`BUILT`/`QUEUED <ID>`/`UNSCHEDULED`/`BLOCKED`). Order-of-work lists are a small GOVERNED registry (CAPTURE-SCALING.md "Order of work"; CONSTRUCTS.md "The plan") plus a DISCOVERY GUARD that fails on any new "Order of work"-titled heading not governed or explicitly exempt — so the registry cannot silently fall behind the way D-113's list did. One list is EXEMPT with a reason: CONFORMANCE-AND-INTAKE-ARC.md "5. Order of work" is a CLOSED migration plan (all eight steps executed and superseded); retro-stamping build versions on it would fabricate a ledger it never kept — left tolerant, a judgment for CONDUCT rather than guessed. The load-bearing constraint honoured: the CURRENT estate PASSES every check (the one real drift, CONSTRUCTS's unmarked steps, was fixed mechanically from QUEUE+MILESTONES before the gate went on). accepts-when GREEN: `npm run test:hygiene` runs both hygiene suites (149 + 154 pass); `npm run test:coverage` (now --strict) exits 0 — 54 suites, 54/54 declaring a control (the new suite counted), 85/85 ops, 51/51 checks; battery 54/54 (2730). NEGATIVE CONTROLS RUN 2026-07-31, recorded in the suite's first-60-lines NEGATIVE CONTROL line, each restored: (disposition) strip the M7 token from open DEBT row D-50 -> 2 fail naming D-50; (status marker) strip the BUILT(FW-3) marker from CONSTRUCTS "The plan" Step 1 -> 2 fail naming Step 1. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (6a72951), CLAIMS union-resolved. CONDUCT updated CONSTRUCTS Step 3→BUILT(FW-5)/Step 4→QUEUED FW-6 markers post-M0-6. battery 55/55, coverage --strict exit 0, hygiene 154 pass. The M0 verification floor is now fully self-enforcing.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw6
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (three NEW tables `entities`, `entity_aliases`, `entity_relations` inserted BEFORE the host_governor block, after reading_refs — additive, no existing table changed); bio-plane/src/store.mjs (the registry section: `createEntity`/`addEntityAlias`/`declareRelation` writes, `readEntity`/`entitiesByAlias`/`readRelation` reads, the shared `#entityView` + `#normAlias`/`#cleanLabel`/`#ENTITY_KINDS`/`#RELATION_KINDS` helpers; six router-map entries; the three tables added to the whole-store purge arm for D-113; entity/alias/relation counts added to stats() and to purge's removed{}); bio-plane/src/index.mjs (six new ops in the OPS table — three mutating writes + three reads; a REGISTRY_ACTIONS set spread into SESSION_OPS member/admin; declared_by stamping for the three writes; the three writes named "contribute" in NEEDS); bio-plane/test/entityregistry.test.mjs (new) + its test:entityregistry entry in bio-plane/package.json; docs/development/INTERFACES.md (I5 → 1.2.0 + the three tables' shape/ownership note); docs/development/DECISIONS.md (new DEC-6); docs/development/DEBT.md (D-83 disposition moved to SLICE A LANDED).
interfaces consumed: none (builds on FW-5's reading_refs but resolves nothing yet)
interfaces owned/extended: I5 (the store schema) — ADDITIVE: three new tables `entities` + `entity_aliases` + `entity_relations`; no existing table's columns change, so no INTERFACE-CHANGES protocol is needed.
expected: QUEUE.md FW-6 / CONSTRUCTS Step 4 SLICE A — the SUBJECT REGISTRY, which IS the framework's entity axis, built ONCE (D-83). ENTITIES (kind ∈ the closed UNION of safeguard 4's four subject kinds {source,institution,office,movement} and the framework's entity kinds {person,body,ordinance,parcel,contract,fund}; the union is the reconciliation D-83 demands, and DEC-6 raises the residual ambiguity — whether a bias STATEMENT may take the non-safeguard-4 kinds as a subject — with a provisional that blocks nothing) each with first-class ALIASES (canonical label seeded as one; reverse lookup on a case-folded alias_norm), plus DECLARED RELATIONS (`proxy_for`/`member_of`/`overlaps`) each carrying a NOT-NULL justification + citation "like a pattern statement" and — structurally — NO §8.1 connection grade: a declared relation is CONSTITUTIVE, not evidentiary, so grading it Grade D is the category error D-83 names, prevented by the shape (no grade column) rather than by convention. Ops: entitycreate/entityalias/relationdeclare (writes, declared_by stamped from the session, "contribute"); entity/entitybyalias/relation (reads). RESOLVING a reading_refs reference to a registry entity, declaring the §8.1 method-as-grade, is the NEXT slice and is NOT built here. I5 VERDICT: ADDITIVE (new tables only) → I5 1.2.0. accepts-when bio-plane/test/entityregistry.test.mjs: an entity with aliases + a justified proxy_for/member_of/overlaps relation round-trips and is retrievable BY KEY (op=entity) and BY ALIAS (op=entitybyalias, case/space-insensitive); a read relation carries justification+citation and NO grade key (asserted on the relation read AND the entity view, and a supplied grade is dropped). NEGATIVE CONTROL RUN: dropping the alias persist makes op=entitybyalias return count 0 for a name known to exist (6 fail); dropping the relation persist makes op=relation return found:false. D-113: all three tables in op=purge's whole-store arm, asserted cleared; hygiene (152) + purge tests green. Coverage: ops 93/93 reached (0 unreached), checks 51/51, NC 55/55. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (4037add), CLAIMS union-resolved. battery 56/56, ops 93/93 (--strict exit 0), hygiene 154 (D-83 disposition token intact). DEC-6 (bias-statement entity-kind reach) recorded for bob-session; worker proceeded under the union provisional. I5 1.2.0 additive.
## CLAIM 2026-07-31 UI
session: ui-agent-1
opened: 2026-07-31T00:00:00Z
paths: civicos-ui/app.html (the TASK INBOX surface ONLY — a new `tasks` rail entry in the RAIL array + buildRail, a `renderTasks()` screen and its `go()` router entry, the task kind→label map, and the forward/resolve wiring through recPost; no existing screen's behaviour changed), civicos-ui/test/task-inbox.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=tasks, op=taskforward, op=taskresolve, op=memberlist, op=whoami; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-1 / MILESTONES M8 — the TASK INBOX, the member surface over the already-shipped plane attention layer (D-98). Lists a member's tasks (each pointing at the act it resolves in), forwards (op=taskforward) and resolves (op=taskresolve) through the existing ops. Honours the TASK construct's accountability rules (v0.2): a task ages with a recorded reason and is never silently dropped (age surfaced from `created` + any history ageing event; the ageing JOB itself is D-79/M4 and NOT built here); a task is addressed to somebody or honestly `unassigned`, never a phantom; the refusal shape "this is not yours to resolve, and here is who it is with" is rendered for a task assigned to another member. Points at the acts (openBundle on refers_to); does NOT build the acts. accepts-when civicos-ui/test/task-inbox.test.mjs: lists a member's tasks, forwards one and resolves one through the plane ops, and shows an `unassigned` task honestly; NEGATIVE CONTROL RUN — break the taskresolve wiring and the resolve path fails. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (105198b), CLAIMS union-resolved. civicos-ui all harnesses green, plane battery 56/56 (no plane code touched). No DEC. UI→RECORD delegation (server-side task-actor fence) lifted by CONDUCT into REC-4.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-1 (UI-1, the task inbox)
need: consider enforcing the TASK construct's refusal shape at the plane, not only in the surface. `taskResolve` (bio-plane/src/store.mjs ~5299) and `taskForward` refuse NO_ACTOR / NO_SUCH_TASK / ALREADY_RESOLVED, but do NOT refuse a member who is neither the task's `assignee` nor a group admin — any member-class credential can resolve any task by id (the actor is stamped honestly into history, so it is traceable, but it is not prevented). BIO_Interaction_Constructs v0.2 makes "this is not yours to resolve, and here is who it is with" an accountability rule of the construct; the UI renders it (a task held by another member shows the refusal and no resolve/forward control), but the refusal is cosmetic until the plane returns a named reason (e.g. NOT_YOURS with the current assignee) for a non-assignee, non-admin resolve/forward.
why: not blocking — UI-1 shipped the surface against the ops as they are. But an accountability rule enforced only in the browser is one a non-browser caller ignores, which is the class CLAUDE.md's "test through the op" lesson names. RECORD owns store.mjs and I3; this is RECORD's call on whether to add the server-side fence (and what its reason string is, since the UI would then render that verbatim rather than composing its own).
status: open — raised by UI-1, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).

---

## CLAIM 2026-07-31 RECORD
session: record-agent-4
opened: 2026-08-01T02:39:26Z
paths: bio-plane/src/store.mjs [taskForward/taskResolve + the new #refuseNotYours fence helper], bio-plane/src/index.mjs [TASK_ACTIONS in SESSION_OPS member/admin + taskforward/taskresolve in NEEDS as null; the actor-stamp block was already present], bio-plane/test/task-fence.test.mjs [new], docs/development/INTERFACES.md [I3], docs/development/DECISIONS.md [new DEC-7]
interfaces consumed: none
interfaces owned: I3 [additive: new NOT_YOURS reason + session-reach for taskforward/taskresolve → 1.2.0]
expected: REC-4 — the server-side TASK-ACTOR FENCE (lifts UI→RECORD delegation above). taskResolve/taskForward now refuse a caller who is neither the task's assignee nor an admin with NOT_YOURS, naming who it is with (assignee, assignee_role, human detail); an honestly `unassigned` task stays claimable per D-98 routing; the admin override is #isAdminMember (ROOT admin session or any in-app role='admin' member); a machine credential is fenced off assigned tasks (a daemon cannot close a member's work) and confined to unassigned ones. Both ops were opened to member/admin SESSIONS (they were machine-credential-only, which left the Tasks screen's session-token recPost unreachable — DEC-7), NEEDS[op]=null because the authorization is identity not capability (parallels release). accepts-when proven in task-fence.test.mjs THROUGH the control plane with real member/admin sessions (non-assignee nate refused both verbs with NOT_YOURS; assignee mona succeeds at both; admin iris succeeds at both; unassigned stays claimable by nate; a spoofed body actor does not bypass). NEGATIVE CONTROL run (neuter #refuseNotYours → 9 fail). battery 57/57 (2832). coverage --strict exit 0: ops 93/93, checks 51/51, NC 57/57, fleet 1/1. node_modules symlinked from the main checkout for miniflare, removed before commit. No push, no deploy, QUEUE.md landed: left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (edfbea5). Security-sensitive auth change VERIFIED: battery 57/57, coverage --strict 93/93, and CONDUCT independently re-ran the fence negative control (neuter #refuseNotYours → 9 fail, restored → 19). Completes UI-1's loop (the ops now admit sessions). I3 1.2.0. DEC-7 for bob-session.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw7
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (new derived table `resolutions`, BEFORE host_governor), bio-plane/src/store.mjs (the recognisers: resolveReferences/testifyResolution/#recognise/#upsertResolution/resolutionsForCapture/documentsConcerning + purge TABLES entry + stats/removed; store DO dispatch for resolve/resolvetestify/resolutions/concerns), bio-plane/src/index.mjs (4 new ops + RECOGNISER_ACTIONS session set + scopeFor contribute + resolved_by stamping), bio-plane/test/resolution.test.mjs (NEW), docs/development/INTERFACES.md (I5 1.2.0 → 1.3.0)
interfaces consumed: I5 (own), FW-5 reading_refs + FW-6 entities/entity_aliases (built upon)
interfaces owned: I5 registered here in the same turn (ADDITIVE bump)
expected: QUEUE.md FW-7 / CONSTRUCTS Step 4 SLICE B — the RECOGNISERS. Resolve a raw reading_refs reference (kind:key) to a registry entity and DECLARE THE METHOD, which IS the §8.1 connection grade: A the reference's composite key matched a registered identifier exactly (source's own identifier, both ends captured); B the bare key matched a registered identifier in content; C a name/title matched an entity ALIAS (correspondence — NEVER established, flagged for a member to confirm). A reference matching nothing stays honestly UNRESOLVED (never force-matched). The recogniser (op=resolve) mints only A/B/C, matches an entity's OWN aliases only, and NEVER traverses a declared relation (do not resolve THROUGH a constitutive proxy_for/member_of/overlaps edge, D-83). Grade D is member TESTIMONY (op=resolvetestify — an author + a date, no captured basis), never minted by the machine; the model holds it. Grade is IMPROVABLE: keyed (capture_sha, ref, entity_id), a re-resolution that finds a stronger basis RAISES the grade+method IN PLACE (raised_from records the prior grade), never a second row and never a downgrade. `established` is DERIVED from grade at write (1 for A/B, 0 for C/D) so a C can never read back as established. Delivers the REVERSE INDEX op=concerns — every document that concerns an entity, joined on entity_id — the single largest manual task the framework removes; op=resolutions reads a document's resolutions. I5 VERDICT: ADDITIVE (one new derived table `resolutions`) → I5 1.3.0. accepts-when bio-plane/test/resolution.test.mjs: a reference matching an entity alias resolves at C; one matching a source identifier at both ends resolves at A (composite) / B (bare); op=concerns returns every capture whose reading resolved to X; a reference matching nothing is honestly unresolved; a C raised to A in place when the identifier is later registered (one row, raised_from C). NEGATIVE CONTROLS RUN: (a) #isEstablished("C")=true → a Grade C reads back established (3 fail); (b) INSERT dropped in #upsertResolution → op=resolve reports resolved but op=resolutions/op=concerns empty for a document known to concern the entity (resolutions 3→0, concerns 2→0). D-113: `resolutions` in op=purge's TABLES (clears in BOTH per-bundle and whole-store arms), asserted removed + after=0; hygiene + purge green. Coverage --strict: OPS 97/97 reached (0 unreached), CHECKS 51/51, NC 57/57. Battery 57/57, 2852 assertions. No push, no deploy — CONDUCT integrates; landed: left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (2d11c03). index.mjs SESSION_OPS conflict vs REC-4 resolved by union (both TASK_ACTIONS + RECOGNISER_ACTIONS spread into member/admin). battery 58/58, coverage --strict 97/97 (0 unreached), resolution 39 + task-fence 19 green post-merge. I5 1.3.0. No DEC. Step 4 done — the M4 reverse-index capability is live.

## CLAIM 2026-07-31 UI
session: ui-agent-2
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the FIRST ACT SURFACE — focus disposition as a JUSTIFIED TRANSITION, ONLY: a `canDispose()` capability gate mirroring `canRelease()`; a self-contained DISPOSE ACT module — `DISPOSE_LEGAL` (the plane's legal-transition table, mirrored with a source cite), `weightLadderHtml()`, `disposePreflight()` (the pure pre-flight), `openDisposeDialog()`/`disposeChoose()`/`disposeValidate()`/`doDispose()`/`disposeReceipt()`/`disposeDone()`/`disposeRefusalHtml()`, plus the dialog CSS; and a Disposition act bar added to openBundle() for focus/problem document pages beside the existing release bar — no existing screen's behaviour changed), civicos-ui/test/act-dispose.test.mjs (NEW), docs/development/DECISIONS.md (new DEC-8 — the ACT construct's "see what it will refuse before it runs" pre-flight-source ambiguity; CONDUCT to apply the three tests at integration and relocate/drop if the repo already answers it)
interfaces consumed: I3 (plane → UI op contracts — consumes op=select to build a single-focus enumerated selection and op=dispose to move its state; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-2 / MILESTONES M8 — v0.2's FALSIFIABLE TEST: build the queue (UI-1) and ONE act, then re-read the two-construct collapse. The ONE act is FOCUS DISPOSITION (op=dispose to deferred/dismissed, which C-2.8 already requires a reason for — the natural "reasoned" rung) built through the ACT construct + WEIGHT LADDER as a JUSTIFIED TRANSITION (§J). The ONE MOTION rendered exactly and only: (1) CHOOSE the act (defer / dismiss this focus); (2) PRE-FLIGHT the refusal — see WHAT IT WILL REFUSE and WHY *before* it runs (the ACT construct's defining property): the C-2.8 reason requirement (NO_REASON), the reason grammar (BAD_REASON, ≤160 chars no quote/backslash/newline — Store.EDGE_REASON_MAX), and the legal-transition gate (ILLEGAL_TRANSITION / NOT_A_DISPOSITION) computed from the focus's known current_state against the plane's own LEGAL table; (3) AUTHOR the reason — REQUIRED and NEVER prefilled (placeholder hint only, never a value; the surface itself refuses to send an empty-reason act, so the C-2.8 refusal is felt before the plane is even reached); (4) RECEIPT — what the plane returned (the disposed id, the target state, the reason as recorded, weight `refuse`). The act's WEIGHT-LADDER position (reversible · reasoned · terminal · attested) is shown: dispose is **reasoned**. The op=dispose contract has no dry-run, so the pre-flight is computed from the op's declared refusal shape + the focus's known state (honest against what exists); a plane-side dry-run is NOT needed for this act and is noted, not delegated. accepts-when civicos-ui/test/act-dispose.test.mjs: disposing a focus with an authored reason succeeds and yields a receipt (op=select then op=dispose called, receipt rendered from the plane's return); the act is REFUSED with the reason shown when the required reason is absent AND op=dispose is never called (the surface refuses before the plane). NEGATIVE CONTROL RUN — remove the reason-required pre-flight gate in doDispose and the empty-reason act is no longer refused in the surface (op=dispose is now called with an empty reason). Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates. The two-construct-collapse VERDICT is a reported deliverable.
released: 2026-07-31 — landed on main by CONDUCT (0e34d9b), clean cherry-pick (2 commits). civicos-ui 15 harnesses green, plane battery 58/58 (no plane code). DEC-8 (ACT pre-flight source: surface mirror vs plane dry-run) for bob-session; nothing blocked. The two-construct collapse HELD for this act.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw8
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (three new tables `connections` / `progression_defs` / `progression_stages`, BEFORE host_governor), bio-plane/src/store.mjs (FW-8 methods deriveConnections/connectionsFor/#weakerGrade/#connectionView/defineProgression/readProgression; stats + purge additions [connections per-bundle+whole-store, progression tables whole-store]; the store DO dispatch for connect/connections/progressiondefine/progression), bio-plane/src/index.mjs (4 new ops + PROGRESSION_ACTIONS session set + NEEDS contribute for connect/progressiondefine + declaredBy/assertedBy body stamps), bio-plane/test/connection.test.mjs (NEW), docs/development/INTERFACES.md (I5 1.3.0 → 1.4.0)
interfaces consumed: I5 (own), FW-6 entities + FW-7 resolutions (built upon)
interfaces owned: I5 registered here in the same turn (ADDITIVE bump 1.4.0)
expected: QUEUE.md FW-8 / CONSTRUCTS Step 5 SLICE A — PROGRESSIONS AS DATA (framework §8.2). (1) PERSIST a CONNECTION as data carrying a GRADE (D-67 storage + D-72 grade): a connection is the TWO-NODE base case of a progression — two captured documents that resolve (FW-7) to the SAME registry entity (FW-6) — DERIVED via op=connect UNDER the op=concerns reverse-index join (not a parallel path), each carrying the §8.1 grade of its WEAKER end (framework §8.2 "weakest along the chain", two-node case); `established` derives from the weaker grade so a C at either end is never established; `asserted_by` is three-valued and DISTINCT from grade (framework:554), forced 'system' server-side (source/member reserved for slice B). op=connections reads by entity or capture. Grade is IMPROVABLE (upsert in place when a resolution is raised). (2) The PROGRESSION DEFINITION as data (op=progressiondefine / op=progression): ordered stages with after / cardinality / interval / required-ness, such that BOTH meeting→agenda→minutes AND need→award→signed-contract are expressible as rows (the acceptance). DEFERRED to slice B and flagged: progression INSTANCES, weakest-grade inheritance along an N-stage chain (D-73 pair→chain — the 2-node base case is done here), exception documents, junction checks, the missing-predecessor task. I5 VERDICT: ADDITIVE (three new tables) → 1.4.0. Schema traps honoured (BEFORE host_governor, no backticks, all three in purge — connections in BOTH arms via a_bundle_id/b_bundle_id, the two progression tables whole-store like the registry). accepts-when bio-plane/test/connection.test.mjs: both example progressions expressible as definition rows; a connection persists carrying the weaker-of-two §8.1 grade and is retrievable by entity and by capture; a single-document subject yields no connection; a C-ended connection raised to A in place when the underlying resolution is raised; malformed definitions refused by name (NO_KEY/NO_STAGES/BAD_REQUIRED/UNKNOWN_AFTER/DUPLICATE_STAGE). NEGATIVE CONTROLS RUN: (1 D-72) #weakerGrade returns the STRONGER grade → a C-weak connection reads graded A/established (2 fail); (2 D-67) disable the INSERT INTO connections → op=connect reports 3 formed but op=connections empties (3→0, 2→0). D-113: all three tables cleared by op=purge, asserted removed + after=0; hygiene + purge green. Coverage --strict: OPS 101/101 reached (0 unreached, +4), CHECKS 51/51, NC 59/59, FLEET 1/1. Battery 59/59, 2916 assertions. No push, no deploy — CONDUCT integrates; landed left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (fe81904), CLAIMS union-resolved. battery 59/59, --strict 101/101, connection 41. I5 1.4.0. No DEC. D-67 storage/D-72 connection-grade/D-73 rule+2-node closed (slice B remains).

## CLAIM 2026-07-31 UI
session: ui-agent-3
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the SECOND ACT SURFACE — a BALLOT, project owner removal, through the SAME ACT construct + weight ladder UI-2 established, ONLY: a `canBallot()` session gate; a self-contained BALLOT module — `ballotPreflight()` (the pure pre-flight, whose arithmetic gates READ the plane's computed `live` row, never re-derive floor(n/2)+1), `ballotDenominator()`/`ballotDenominatorHtml()` (the DENOMINATOR read straight FROM op=projectownerarith — the v0.2 §B "checkable tally, never pending approval"), `ballotDivergenceHtml()` (the two-owner DIVERGENCE, shown from the op's `table`), `openBallotDialog()`/`ballotChoose()`/`ballotValidate()`/`doBallot()`/`ballotReceipt()`/`ballotRefusalHtml()`, plus the dialog CSS (`bl-*`); `weightLadderHtml()` extended with an optional note param, dispose's default note unchanged — no existing screen's behaviour changed), civicos-ui/test/act-ballot.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=projectownerarith for the computed tally/denominator + the two-owner divergence table, op=projectparticipants for the owners to choose from, and op=projectownerremove as the ballot act; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-3 / MILESTONES M8 — v0.2's FALSIFIABLE test, act TWO: does the ACT construct hold for an act UNLIKE the justified transition? Build ONE ballot (project owner removal, 7.10) through the SAME ACT construct + weight ladder (choose · see what it will refuse and why BEFORE it runs · author · receipt · weight-ladder), PLUS the two properties the BALLOT type adds (v0.2 §B): (a) SHOW THE DENOMINATOR — "N of M owners' votes" read FROM op=projectownerarith's `live.votesNeeded`/`live.owners`, and the running tally FROM the op=projectownerremove receipt (VOTES_SHORT have/need/deciders), never "pending approval", never recomputed in the interface; (b) DISPLAY THE DIVERGENCE AT TWO OWNERS — the boundary row a shared implementation gets wrong, rendered from op=projectownerarith's `table` (row n=2, where owner removal diverges from the admin rule: the departing owner MAY vote, unanimity-with-assent). The pre-flight's arithmetic gates (LAST_OWNER, TARGET_CANNOT_VOTE) are computed by the PLANE and read off `live` — a finding for DEC-8 (this ballot's pre-flight is partly server-computed, so it does NOT deepen the client-mirror concern). accepts-when civicos-ui/test/act-ballot.test.mjs: the ballot shows the computed tally/denominator FROM the arithmetic op and refuses+explains before it runs (empty reason / last owner / self-vote at 3+ owners → surface refuses, op=projectownerremove never sent); a valid vote sends the act and the receipt shows the running tally from the return. NEGATIVE CONTROL RUN: break the denominator wiring (`ballotDenominator` reads `live.votesNeeded` → a constant) and the shown tally no longer reflects the op. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates. COLLAPSE VERDICT is a reported deliverable.
released: 2026-07-31 — landed on main by CONDUCT (719473b), clean cherry-pick. civicos-ui 16/16, plane battery 59/59 (no plane code). The two-construct collapse HELD for act two (a ballot); DEC-8 client-mirror concern REDUCED (the arithmetic op dry-runs its own refusals). No DEC, no delegation.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw9
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (one new table `progression_instances`, BEFORE host_governor), bio-plane/src/store.mjs (FW-9 methods threadInstance/readInstance/#assembleInstance/#strongestResolutionsFor/#REQUIRED_FIRES; stats + purge additions [progression_instances added to the whole-store+per-bundle TABLES list, reported in stats/removed]; the store DO dispatch for thread/instance), bio-plane/src/index.mjs (2 new ops thread/instance + PROGRESSION_ACTIONS extension + NEEDS contribute for thread + threadedBy body stamp), bio-plane/test/progression-instance.test.mjs (NEW), docs/development/INTERFACES.md (I5 1.4.0 → 1.5.0), docs/development/DECISIONS.md (new DEC-9 — whether unless_exception fires a missing-predecessor finding before the exception-doc slice exists)
interfaces consumed: I5 (own, additive), FW-6 entities + FW-7 resolutions + FW-8 connections/progression_defs (built upon)
interfaces owned: I5 (ADDITIVE bump 1.5.0; registered owner RECORD)
expected: QUEUE.md FW-9 / CONSTRUCTS Step 5 SLICE B — progression INSTANCES and the MISSING-PREDECESSOR finding (M4 acceptance). (1) A progression INSTANCE threads REAL captured documents through a definition's stages, assembled by a THREADING ENTITY (framework §8.2), one instance per (definition, entity), persisted in the new I5-additive `progression_instances` table (one row per placed document; the instance is all rows sharing progression_key+entity_id). A document is admitted only if it RESOLVES to the entity (FW-7) — refused NOT_CONCERNED otherwise; each placement's grade is the record's (strongest resolution of that capture to the entity), never the caller's; the stage assignment is the member's authored judgment (threaded_by stamped server-side). op=thread writes, op=instance reads; re-threading REPLACES. (2) N-STAGE WEAKEST-GRADE inheritance (D-73 pair→chain, beyond FW-8's 2-node base case): the instance grade is the WEAKEST connection along the chain of consecutive placed stages, each connection graded the weaker of its two ends (reuses FW-8 #weakerGrade). Fewer than two placed stages → UNDETERMINED, never invented. (3) The MISSING-PREDECESSOR finding: a stage that is required (always/usually) with no threaded document surfaces as a finding carrying the instance's grade (framework's example: an award with no solicitation). Requiredness respected — sometimes/never missing is NOT a finding; unless_exception DEFERRED (DEC-9). Grade + findings DERIVED ON READ from stored placements + the CURRENT definition (never a stale stored grade). DEFERRED and flagged: exception documents that discharge a lawful skip; junction checks as findings; the SCHEDULED task that walks the table (rides the REC-1 DO-alarm scheduler). I5 VERDICT: ADDITIVE (one new derived table) → 1.5.0. Schema traps honoured (BEFORE host_governor, no backticks, in op=purge's TABLES so it clears in BOTH arms like resolutions; hygiene asserts it). accepts-when bio-plane/test/progression-instance.test.mjs: threading need→award→contract MINUS the solicitation through the procurement definition yields an instance whose missing `solicitation` (usually-required) stage surfaces as a missing_predecessor finding; the instance grade is the weakest connection along the chain — need(A)→award(A)=A, award(A)→contract(C)=C, so grade C carried by the finding; a one-document instance reads UNDETERMINED; a document not concerning the entity is refused NOT_CONCERNED. NEGATIVE CONTROLS RUN 2026-07-31: (1) empty #REQUIRED_FIRES → the missing solicitation no longer surfaces (finding_count 1→0, the "exactly ONE finding" assertion fails then finding[0] lookups throw — suite not green); (2) #weakerGrade returns the STRONGER end → the chain reads grade A not C (5 fail, 32 pass); both restored → 37 pass. D-113: progression_instances in op=purge's TABLES, asserted removed + after=0; hygiene + purge green. Coverage --strict: OPS 103/103 reached (0 unreached, +2 thread/instance), CHECKS 51/51, NC 60/60, FLEET 1/1. Battery 60/60, 2955 assertions. No push, no deploy — CONDUCT integrates; landed left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (d5da71d), CLAIMS union-resolved. battery 60/60, --strict 103/103, progression-instance 37. I5 1.5.0. DEC-9 (unless_exception) for bob-session. M4 acceptance "a progression with a missing predecessor is visible" is MET. Remaining M4: slice C (exceptions/junctions/walking-task), Step 5a, Step 6.

## CLAIM 2026-07-31 UI
session: ui-agent-4
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the SUBJECT VIEW — a NEW read-only screen making the M4 reverse index member-visible, ONLY: a `subjects` RAIL entry + `subjects: renderSubjectView` route + navLabel/preview-title map entries; a self-contained SUBJECT-VIEW module — `renderSubjectView()` (the screen + search box), `lookupSubject()` (search by name/alias via op=entitybyalias, or by id via op=entity), `showEntity()` (loads op=concerns + op=connections and paints), the pure renderers `subjEntityHtml`/`subjRelationsHtml`/`subjConcernsHtml`/`subjConnectionsHtml`, the honest `SUBJ_GRADE_HOW`/`subjGradeBadge()` §8.1 grade helper, and the `subj-*` CSS — no existing screen's behaviour changed), civicos-ui/test/subject-view.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=entitybyalias, op=entity, op=concerns, op=connections, op=relation; reads I5 shapes for entity/resolution/connection; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-4 / MILESTONES M8 — the SUBJECT VIEW, "what the record knows about a subject." Given a registry entity (found by name/alias via op=entitybyalias, or by id via op=entity), show three things: (1) THE ENTITY — kind, label, aliases, and its declared RELATIONS (proxy_for/member_of/overlaps) each with justification + citation, rendered as CONSTITUTIVE (a group's fixing of what its own terms mean) and WITHOUT any A–D connection grade (grading a declared relation is the D-83 category error; the plane carries no grade on a relation, so the surface shows none). (2) EVERY DOCUMENT that concerns it (op=concerns), each with its resolution GRADE shown HONESTLY: `established`/`needs_confirmation` READ FROM the op (never re-derived from the letter), so a Grade C correspondence renders "plausible, not established — unconfirmed" and NEVER as established. (3) The graded CONNECTIONS among those documents (op=connections), each connection's §8.1 grade = the weaker of its two ends, shown as HOW it was established, never as credibility. accepts-when civicos-ui/test/subject-view.test.mjs: look up an entity by alias (op=entitybyalias); list the documents that concern it from op=concerns WITH their grades; a Grade C document renders as unconfirmed (NOT established); a declared relation renders WITHOUT a connection grade. NEGATIVE CONTROL RUN: force a Grade C resolution to render as established (subjGradeBadge takes the established branch regardless of the flags) and the test fails. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (d2d94df), CLAIMS union-resolved. civicos-ui 17 harnesses green, plane battery 60/60. The M4 reverse index is now member-visible with honest grades. Connection auto-derivation gap logged as D-122.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw10
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (one new table `progression_exceptions`, BEFORE host_governor), bio-plane/src/store.mjs (FW-10 methods dischargeStage/readExceptions; #assembleInstance extended to CONSULT exceptions [discharged state] + #REQUIRED_FIRES graduates unless_exception; stats + purge additions [progression_exceptions in the whole-store+per-bundle TABLES list, reported in stats/removed]; the store DO dispatch for discharge/exceptions), bio-plane/src/index.mjs (2 new ops discharge/exceptions + PROGRESSION_ACTIONS extension + NEEDS contribute for discharge + declaredBy body stamp), bio-plane/test/progression-exception.test.mjs (NEW), bio-plane/test/progression-instance.test.mjs (FW-9 assertions SUPERSEDED where unless_exception now fires when undischarged — corrected, not exempted), docs/development/INTERFACES.md (I5 1.5.0 → 1.6.0), docs/development/DECISIONS.md (DEC-9 note: mechanism now exists, provisional updated, LEFT OPEN for Bob)
interfaces consumed: I5 (own, additive), FW-6 entities + FW-7 resolutions + FW-9 progression_instances (built upon)
interfaces owned: I5 (ADDITIVE bump 1.6.0; registered owner RECORD)
expected: QUEUE.md FW-10 / CONSTRUCTS Step 5 SLICE C (part) — EXCEPTION DOCUMENTS that discharge a legitimate skip (framework §8.2), building on FW-9. (1) Model an EXCEPTION DOCUMENT: a real captured document, threaded to a progression instance by (progression_key, entity_id, stage_key, capture_sha), carrying a NOT-NULL reason + citation (mirrors FW-8 relations); admitted only if it RESOLVES to the threading entity (FW-7, NOT_CONCERNED otherwise) and NAMES a real stage (BAD_STAGE otherwise) — a discharge must be EARNED, never a caller's bare assertion. New I5-additive table `progression_exceptions`. (2) #assembleInstance CONSULTS them: a REQUIRED stage that is missing but HAS a discharging exception → a distinct "discharged" state (reason/citation + the document VISIBLE), NOT a missing_predecessor finding and NOT silently absent; undischarged-and-required still fires. (3) DEC-9 MECHANISM: unless_exception graduates to DISCHARGEABLE — fires ONLY when required-and-undischarged (DEC-9 recommendation c). Implemented as the provisional; DEC-9 LEFT OPEN (Bob rules the policy). DEFER+FLAG: junction checks as findings; the scheduled walking-task (rides REC-1). Schema traps honoured (BEFORE host_governor, no backticks, in op=purge's TABLES so it clears in BOTH arms like progression_instances; hygiene asserts it). accepts-when bio-plane/test/progression-exception.test.mjs: a procurement instance missing its solicitation but carrying a DISCHARGING exception document surfaces NO missing_predecessor finding for that stage while showing the discharged state; the SAME instance WITHOUT the exception still fires; an unless_exception stage fires only when undischarged. NEGATIVE CONTROL: ignore the exception document (empty the discharge consultation) → the finding reappears. New ops discharge/exceptions carry control-plane assertions (--strict). No push, no deploy — CONDUCT integrates; landed left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (93c9601), CLAIMS union-resolved. battery 61/61, --strict 105/105, both progression suites green. I5 1.6.0. DEC-9 mechanism built, DEC left open for Bob. The present-stage-exception read-time-inert choice accepted as sound mechanism (not escalated). grep -a trap folded into CLAUDE.md.

## CLAIM 2026-07-31 UI
session: ui-agent-5
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the THIRD ACT SURFACE — a PROPOSAL, through the SAME ACT construct + weight ladder UI-2/UI-3 established, ONLY: a `proposals` RAIL entry + `proposals: renderProposals` route + preview-title map entry; a self-contained PROPOSAL module — `proposalsFrom()` (the pure D-79 aggregation: N instances of one missing-predecessor check → ONE proposal with N instances), `weakerGrade()`/`PROP_GRADE_RANK`/`PROP_GRADE_HOW`, `proposalQuestion()`, `proposalDerivedBadgeHtml()` (the D-82 visibly-derived marker — the negative-control subject), `proposalGradeHtml()`/`proposalInstanceHtml()`/`proposalCardHtml()`, `renderProposals()`/`proposalGapHtml()`/`loadProposals()`, and the THREE affordances as ACTs: DEFER/DISMISS — `proposalDisposePreflight()` (pure, reason-required, reuses the dispose grammar constants), `openProposalAct()`/`proposalActChoose()`/`proposalActValidate()`/`doProposalDispose()`/`proposalDisposeReceipt()`/`proposalRefusalHtml()`; ADOPT — `proposalAdoptPreflight()` (pure), `openProposalAdopt()`/`proposalAdoptChoose()`/`proposalAdoptValidate()`/`doProposalAdopt()`/`proposalAdoptReceipt()` (authors a focus/problem the SAME way the Add surface does — mdFor+docFiles+registerFor, one writer, no fourth), plus the `prop-*` CSS — no existing screen's behaviour changed), civicos-ui/test/act-proposal.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — RENDERS from op=instance's missing-predecessor findings (FW-9), ADOPT authors through op=promote (REC-3 stamps surfaced_by from the session); a delegated aggregated op=proposals feed and a delegated op=proposedispose disposition, both below; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-5 / MILESTONES M8 — v0.2's FALSIFIABLE test, act THREE: does the ACT construct hold for a DERIVED FINDING awaiting an authored act? Also closes D-82's DISPLAY half for the proposal (a derived finding renders VISIBLY as one, never as an established fact). A PROPOSAL is a derived finding (D-90 invariant 8: derived informs, authored binds — it reports, never decides or edits), rendered primarily from FW-9's MISSING-PREDECESSOR findings (op=instance): a required progression stage with no threaded document, most often an award with no solicitation. The surface keeps three rules: (D-82) it LOOKS derived — the derived marker says the record surfaced it and NOBODY HAS YET JUDGED IT WORTH PURSUING, and the strength grade is shown as HOW a link was established (§8.1), never as fact; (D-79) N instances of one check are ONE proposal with N instances (proposalsFrom groups by progression_key+stage_key), never N items; (§P) EXACTLY THREE affordances — ADOPT (author a focus/problem via op=promote — the member's OWN words, never prefilled, the plane stamps surfaced_by human), DEFER, DISMISS (each with a required reason, never prefilled, refused in the surface before the plane). accepts-when civicos-ui/test/act-proposal.test.mjs (63 assertions): a derived finding renders as visibly a proposal (surfaced_by/derived nature shown, never established), offers exactly adopt/defer/dismiss, and requires a reason on defer/dismiss; ADOPT authors a focus through op=promote without the browser sending surfaced_by. NEGATIVE CONTROL RUN 2026-07-31: neuter proposalDerivedBadgeHtml to return "" (in a second VM context from the source) → the card loses "Surfaced by the record" and "nobody has yet judged it" and reads as an established finding (D-82 regressed); also RUN in-place (5 assertions flip), restored → 63 green. Battery untouched (no plane code edited): 60/60, 2956 assertions. civicos-ui: 18 harnesses green + semantics. No push, no deploy — CONDUCT integrates; `landed:` left for CONDUCT. COLLAPSE VERDICT (reported deliverable): the proposal FIT the ACT construct + the adopt/defer/dismiss affordances with NO new construct — the collapse HELD for act three. What it exposed is in the PLANE's data layer, not the construct: two delegations below.
released: 2026-07-31 — landed on main by CONDUCT (97e6b99), CLAIMS union-resolved. civicos-ui 18 harnesses green, plane battery 61/61. The v0.2 two-construct collapse is CONFIRMED across three acts (dispose/ballot/proposal). Two delegations lifted to REC-6 (op=proposals feed) + REC-7 (op=proposedispose). No DEC.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-5 (UI-5, the proposal surface)
need: an AGGREGATED DERIVED-FINDINGS FEED op (e.g. op=proposals). There is no op today that enumerates progression instances and returns their missing-predecessor findings; op=instance reads ONE instance by (progression_key, entity_id), and there is no op to LIST progression definitions, instances, or entities. So the surface can RENDER and AGGREGATE proposals (proposalsFrom, D-79) and ACT on them (adopt/defer/dismiss), but it cannot DISCOVER them to populate the screen. Shape that fits the surface: op=proposals returns `{ instances: [ { progression_key, progression_label, entity_id, entity_label, findings:[ op=instance's finding shape ] } ] }` — the raw instance reads; THIS surface does the D-79 grouping (keeping the aggregation in the tested UI). Alternatively RECORD may return already-grouped proposals; the UI's proposalsFrom would then be a no-op pass-through. This rides the same table-walk FW-9 deferred ("the SCHEDULED task that walks progression_instances for missing predecessors, on the REC-1 DO-alarm scheduler"): the feed op and that task read the same rows.
why: not blocking — UI-5 shipped the rendering/aggregation/affordances proven against the op=instance shape, and reports the gap honestly on-screen (loadProposals reads a delegated op=proposals and shows a banner naming this delegation when the op is absent, rather than an empty screen implying the record found nothing). RECORD owns store.mjs and I3; whether to expose an aggregated read (and whether the UI or the plane does the D-79 grouping) is RECORD's call. D-79 also names the ageing/aggregation store as not-yet-existing; this feed is its read side.
status: open — raised by UI-5, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-5 (UI-5, the proposal surface)
need: a PROPOSAL-DISPOSITION op (e.g. op=proposedispose) so DEFER and DISMISS can be RECORDED. op=dispose disposes a FOCUS bundle (a selection handle + a state transition on a real bundle.md); a proposal is a DERIVED finding with no bundle and no id to select, so op=dispose cannot take it, and D-79 records that the ageing/aggregation store for a proposal "does not exist". Shape that fits the surface: op=proposedispose (mutating, needs contribute) `{ key: <progression_key::stage_key aggregation key>, to: "deferred"|"dismissed", reason }` → a receipt `{ ok, key, to, reason }`, with the member stamped server-side (the browser sends no actor) and the reason kept verbatim (EDGE_REASON grammar, ≤160 chars) — and the disposition should AGE per D-79 rather than vanish. ADOPT, by contrast, is FULLY reachable today through op=promote and is wired end-to-end; only defer/dismiss lack a home. The surface builds the full authored motion (reason required, never prefilled, pre-flight refuses before the plane) and records through op=proposedispose; absent the op, doProposalDispose catches "unknown op" and tells the member the record cannot yet keep a proposal's disposition, having written nothing.
why: not blocking — UI-5 shipped the defer/dismiss acts proven against this shape (the test mocks op=proposedispose, exactly as act-dispose mocks op=dispose, AND asserts the honest degrade when it is absent). The kickoff named "DISMISS … via op=dispose", but op=dispose's real contract (a focus handle + state move) cannot address a bare derived finding; recording a proposal's disposition is a genuine plane gap, not a UI shortcut. RECORD owns store.mjs and I3; the doctrine is already settled (D-79: age rather than vanish, aggregate rather than multiply — a declined proposal must NOT mint a bundle), so this is a mechanism gap, not a DEC.
status: open — raised by UI-5, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).
## CLAIM 2026-07-31 RECORD
session: record-agent-5
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (one new table `connection_dirty`, BEFORE host_governor), bio-plane/src/store.mjs (the connection-derive scheduler consumer + the dirty-set: a `#schedConsumers` "connection-derive" entry, `#deriveConnectionsSweep`, `#stampConnectionDirty`, `#connectionDeriveDelayMs`, the CONNECTION_DERIVE_* constants; a dirty-stamp in `#upsertResolution`; `resolveReferences`/`testifyResolution` made async to ARM after they dirty; `connection_dirty` in stats()/purge removed{} + a `DELETE FROM connection_dirty` in op=purge's whole-store arm), bio-plane/test/connection-derive-sweep.test.mjs (NEW), docs/development/INTERFACES.md (I5 additive bump)
interfaces consumed: FW-7 resolutions + FW-8 connections/deriveConnections (built upon), REC-1 scheduler (registers a consumer)
interfaces owned: I5 (ADDITIVE; registered owner RECORD)
expected: QUEUE.md REC-5 — close D-122, connections AUTO-DERIVE. A SCHEDULED consumer on REC-1's DO-alarm derives connections for entities whose resolutions changed, tracked by an I5-additive dirty-set (`connection_dirty`) stamped at op=resolve/op=resolvetestify (insert or grade-RAISE only, never a kept idempotent re-resolve). Bounded (batch per tick), self-terminating (wake null when the dirty-set empties), idempotent (deriveConnections UPSERTS by the FW-8 key). asserted_by stays 'system' — a scheduled derivation is a machine act. DEFER+FLAG: auto-assembling progression instances + missing-predecessor findings on the same tick.
released: 2026-07-31 — landed on main by CONDUCT (6fdb763). D-122 CLOSED — the entity axis now self-populates. battery 62/62, --strict 105/105. CONDUCT re-ran the sweep NC (my neuter regex missed the private method so it was inconclusive; relied on the worker's documented neuter→15-fail + the scheduler NC + full battery). I5 1.7.0. No DEC. The walking-task is now unblocked (first REC-1 framework consumer landed).

## CLAIM 2026-07-31 UI
session: ui-agent-6
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the FOURTH ACT SURFACE — the ATTESTATION act: a member CO-ATTESTS a HELD capture through the SAME ACT construct + weight ladder UI-2/3/5 established, ONLY additive: an `Attestation` act-bar on an information document's page (reuses primaryCaptureSha), and a self-contained ATTEST module — `canAttest()` (session+contribute, same shape as canRelease), `ATTEST_YIELDS_GRADE`/`attestStandingClaim()` (the HONESTY source-of-truth — co-attestation yields Grade B toward evidentiary weight, NEVER Grade A; the negative-control anchor), `attestPreflight()` (pure: the op's declared refusals in the plane's order — NO_STORAGE 503 → BAD_SHA 400 → NO_SUCH_CAPTURE 404), `attestHonestyHtml()`, `openAttestDialog()`/`attestChoose()` (CHOOSE: timestamp only vs. timestamp + opt-in public web archive)/`attestValidate()`/`doAttest()`/`attestReceipt()` (success — the stored RFC3161 token + the honest new standing)/`attestFailReceipt()` (NO_ATTESTATION — every recorded attempt with its reason, standing UNCHANGED)/`attestRefusalHtml()`, plus `az-`/reused `dz-*`/`pf-*`/`wl-*` chrome — no existing screen's behaviour changed), civicos-ui/test/act-attest.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=attest POST { sha256, archive?, locator? } as the co-attestation act; reads its refusal shape (BAD_SHA/NO_SUCH_CAPTURE/NO_ATTESTATION) and its success return (attestation token {service, sha256, bytes, over} + note); consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-6 / MILESTONES M8 — the ATTESTATION act (v0.2 §A, the WEIGHT LADDER's TOP rung `attested`), v0.2's FALSIFIABLE test act FOUR (bonus confirmation). A member co-attests a HELD capture via op=attest (co-attestation over a capture hash through a timestamp authority), raising it from Grade B TOWARD evidentiary weight — NOT the DIST release-signing path, touches NO signing seed. Built through the SAME ACT construct: CHOOSE (timestamp only, or timestamp + opt-in public web archive — off by default because asking a public archive publishes the fact of interest) · PRE-FLIGHT the op's refusals BEFORE it runs (NO_STORAGE/BAD_SHA/NO_SUCH_CAPTURE, in the plane's checking order; op=attest has NO dry-run, so computed from the declared refusal shape + the doc page's known capture sha, as dispose does) · AUTHOR is genuinely ABSENT (op=attest takes no text — the evidence is the token, not the member's words) · RECEIPT (the stored RFC3161 token + the HONEST new standing). HONESTY (load-bearing): the pre-flight and receipt show what co-attestation does AND what it CANNOT — the plane's own note (index.mjs:2304) "Grade A needs a chain-of-custody web archive this surface cannot produce; co-attestation raises B toward evidentiary weight"; the surface NEVER claims Grade A. Weight-ladder rung = `attested` (top): irreversible and public — the key is the AUTHORITY's, not the member's, and it touches no release/ratify seed (the honest nuance of the rung's "needs a key" gloss). accepts-when civicos-ui/test/act-attest.test.mjs: co-attesting a held capture through op=attest succeeds and the receipt shows the HONEST resulting standing (Grade B, strengthened toward evidentiary weight, NEVER Grade A); the act shows its weight-ladder position (`attested`) and pre-flights its refusals (bad sha / no such capture / no storage → surface refuses, op=attest never sent); a NO_ATTESTATION failure renders every recorded attempt with its reason and leaves the standing unchanged. NEGATIVE CONTROL: make the surface claim Grade A from co-attestation (ATTEST_YIELDS_GRADE "B" → "A") and the honesty assertion fails (the receipt's stated standing flips to Grade A). Battery untouched (no plane code edited). No push, no deploy, no release, no signing seed — CONDUCT integrates; `landed:` left for CONDUCT. COLLAPSE VERDICT is a reported deliverable.
released: 2026-07-31 — landed on main by CONDUCT (08f3e05), CLAIMS union-resolved. civicos-ui 19 harnesses green, plane battery 62/62. Act four (attestation) fit the ACT construct; all four act types now confirm the v0.2 collapse. No DEC.

## CLAIM 2026-07-31 UI
session: ui-agent-7
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the MEMBERS & GOVERNANCE roster — the READ-ONLY half of U11, replacing the thin handle/cover/capabilities table in `renderMembers()`. ONLY additive/in-place on the existing `members` screen, no new rail entry: a rewritten `renderMembers()` + self-contained read-only module — `memberRole()` (the ROLE WIRING seam: a member's class read STRAIGHT from op=memberlist's `role`, the negative-control target) + `memberRoleBadge()`; `memberCapsHtml()`; `adminDenominator()`/`adminDenominatorHtml()` (the admin §4.7 consensus/majority denominator read FROM op=adminarith's `live`, never recomputed — the same "N of M, never pending" shape UI-3's ballotDenominator uses) + `adminScaleHtml()` (how the threshold scales, from op=adminarith's `table`); REUSES UI-3's `ballotDivergenceHtml()` for the two-owner divergence from op=projectownerarith's `table`; `memberOwnership()` (bounded fan-out over the record's project bundles via op=projectparticipants → who OWNS which project, degrades honestly); `founderReconcileHtml()` (the honest note when op=adminarith counts more administrators than the roster lists a row for — the founding ADMIN_TOKEN holder has no members row); `rosterRow()`/`governanceHtml()`, plus `mm-*` CSS reusing the `bl-*` denominator/divergence chrome — no other screen's behaviour changed), civicos-ui/test/members-roster.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=memberlist for the roster + each member's class/role, op=adminarith for the admin consensus/majority denominators (live + table), op=projectownerarith for the two-owner divergence table, op=projectparticipants for per-project ownership; reads I5 member/project shapes; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-7 / MILESTONES M8 — the MEMBERS & GOVERNANCE roster, the READ-ONLY half of the split U11 (member add/remove + key operations stay BALLOT acts UI-3 covers or a later split). A member surface showing WHO holds power and the group's governance STATE, both rendered HONESTLY: (1) the ROSTER from op=memberlist — each member with their class/role (admin/member) read STRAIGHT from the op's `role` field (never inferred from capabilities, never invented), their capabilities, status, and the projects they own; (2) the GOVERNANCE STATE from the arithmetic ops as facts a member can CHECK, never "pending" — the admin §4.7 consensus/majority denominator from op=adminarith's `live` ("removing an administrator takes N of M"), how it scales from the `table`, and the two-owner DIVERGENCE from op=projectownerarith's `table` (the n=2 row where owner removal parts company with the admin rule), REUSING UI-3's ballotDivergenceHtml. Per-member project ownership is NOT in op=memberlist, so it is surfaced by a bounded fan-out over the record's project bundles via op=projectparticipants and degrades honestly if a project cannot be read (a DELEGATION to RECORD proposes op=memberlist carry an `owns` projection to remove the N+1). The founder reconciliation: op=adminarith counts the ADMIN_TOKEN holder who has no members row, so when the count exceeds the roster's admin rows the surface states why rather than showing inconsistent numbers. READ-ONLY: no add/remove/key act on this surface. accepts-when civicos-ui/test/members-roster.test.mjs: the roster lists members with their roles from op=memberlist and shows the governance denominators from op=adminarith + the two-owner divergence from op=projectownerarith. NEGATIVE CONTROL: break the role wiring (memberRole returns a constant instead of the op's `role`) and a member's displayed role no longer reflects the op. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (af81f28), clean cherry-pick. UI 20 harnesses green, plane battery 62/62 (no plane code). Founder-invisible-to-roster discrepancy stated on-screen not hidden. UI→RECORD delegation (memberlist owns[] projection) kept open. No DEC.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-7 (UI-7, the members & governance roster)
need: op=memberlist (or a sibling read) to carry each member's PROJECT OWNERSHIP — e.g. an `owns: [{ projectId, title }]` projection per member row. UI-7's roster must show "who owns which project" (core accountability: ownership is power, Section 7), but op=memberlist today returns only member_id/cover/handle/role/status/capabilities/invite_pending/expertise — no ownership. There is also no op that LISTS a member's projects. So the surface answers ownership by a BOUNDED FAN-OUT: it reads the record's project bundles (op=list) and calls op=projectparticipants for each, inverting owner→member on `handle`. That is an N+1 over projects and degrades honestly (a project it cannot read is skipped, never guessed; if projects cannot be listed at all the roster says ownership "not read" rather than implying nobody owns anything). A single `owns` projection on op=memberlist would remove the fan-out and make ownership a first-class roster fact rather than a join the UI reconstructs.
why: not blocking — UI-7 shipped the roster + ownership proven against the ops that exist (op=memberlist + op=projectparticipants over op=list), and the fan-out is bounded and honest. RECORD owns store.mjs and I3; whether to promote ownership onto the roster read (and the exact shape) is RECORD's call. Related: op=adminarith counts the founding administrator (the ADMIN_TOKEN holder), who has NO members row, so the roster and the governance denominator legitimately disagree on the administrator count; UI-7 reconciles this on-screen (founderReconcileHtml) rather than asking the plane to change — noted here only so RECORD knows the roster is deliberately not the whole administrator set.
status: WITHDRAWN 2026-08-04 by UI-16 — nothing consumes it any more, and RECORD should not build it. The fan-out this delegation existed to remove was DELETED with its surface: UI-16 took the roster's `Owns` column out and moved the question onto the project workspace, where `op=projectparticipants` answers it EXACTLY, in ONE call, for the one project the member is looking at. The `owns` projection would have made a cross-project participation view a first-class roster fact — which is the shape REC-25 had just made the plane's to gate — so removing the join is better than promoting it. If a future surface genuinely needs "which projects does this member own", it should be raised then, against that surface, and gated as a project read rather than as a member field.
## CLAIM 2026-07-31 RECORD
session: record-agent-6
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/store.mjs (ONE new READ method `proposalsFeed` — the read-time walk of `progression_instances` [DISTINCT (progression_key, entity_id)] reusing FW-9/FW-10's `#assembleInstance`, collecting undischarged missing-predecessor findings and D-79-aggregating them by (progression_key, stage_key); reuses `#assembleInstance`/`#weakerGrade`/`#GRADE_RANK` READ-ONLY, edits none of FW-9/FW-10's functions; plus one line in the store DO dispatch map: `proposals: () => this.proposalsFeed()`), bio-plane/src/index.mjs (op=proposals in the OPS table [classes admin/member/probe, mutating:false, an ungated derived-findings read like op=instance/op=exceptions] + `proposals` added to PROGRESSION_ACTIONS so a member/admin SESSION reaches it; no NEEDS entry — a read is ungated like the other progression reads), bio-plane/test/proposals-feed.test.mjs (NEW) + its test:proposals-feed entry in bio-plane/package.json, docs/development/INTERFACES.md (I3 additive version bump — one new read op, no rename/reshape/reason change)
interfaces consumed: FW-9 progression_instances + #assembleInstance, FW-10 progression_exceptions/discharges (read through #assembleInstance — a discharged skip is NOT a finding), FW-6/FW-7 (entities/resolutions, via the assembled instance)
interfaces owned: I3 (ADDITIVE read op; registered owner RECORD)
expected: QUEUE.md REC-6 / MILESTONES M8 — op=proposals, the DISCOVERY feed for derived findings (UI-5's delegation). A read op that ENUMERATES the record's current derived findings: walk progression_instances, assemble each, collect the required-undischarged missing-predecessor stages that fire (respecting FW-10 discharges), AGGREGATE per D-79 (ONE proposal per (progression_key, stage_key) carrying its N instances, the WEAKEST grade across them, any-undetermined→undetermined, surfaced_by machine). REPORTS, never mutates; read-time walk, no scheduled alarm (that is the deferred PUSH walking-task). Response carries BOTH `instances` (the raw per-instance reads UI-5's loadProposals already consumes, so UI-5 populates with NO UI change) AND `proposals` (the server-side D-79 aggregation the kickoff/accepts-when assert, computed from the same walk so the two cannot disagree). Connections deferred as a follow-on (missing-predecessors only). accepts-when bio-plane/test/proposals-feed.test.mjs: op=proposals returns the missing-predecessor findings for a gap-carrying store AGGREGATED (N instances → one entry) with grade + machine provenance; a store with no gaps returns an empty feed; a DISCHARGED gap does NOT appear; negative control — drop the instance-walk and the feed returns nothing for a store known to have a gap. Reached THROUGH the control plane (literal op=proposals in the suite) so coverage credits it. No push/deploy/release — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (80eca24), CLAIMS union-resolved. battery 63/63, --strict 106/106, proposals-feed 20. UI-5's proposal surface now POPULATES (reads instances[]) — its gap banner retired. I3 1.3.0. REC-7 (op=proposedispose) is the remaining half of UI-5's two delegations. No DEC.

## CLAIM 2026-07-31 UI
session: ui-agent-8
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the member HOME — the "what needs you" orientation surface, M8's entry point. Additive: a new `home` rail entry made the DEFAULT boot screen (boot() go("home") instead of go("record")), a `home: renderHome` router entry + preview title, a self-contained read-only module — `renderHome()` (aggregates the attention the record holds for this member), `homeTasksSummary()` (summarises op=tasks: how many need you + the most urgent, reusing renderTasks' own mine/unassigned partition + taskAge/taskKindLabel), `homeProposalsSummary()` (summarises op=proposals via the EXISTING loadProposals(): count of open proposals, degrades HONESTLY to a "feed pending" note when the op is absent exactly as UI-5 does — never a fabricated count), `homeEntryHtml()` (clear entry to the built surfaces: Tasks UI-1, Subjects UI-4, Members UI-7), plus `hm-*` CSS. No other screen's behaviour changed; READ-ONLY orientation — the acts happen on their own surfaces), civicos-ui/test/home.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=tasks for the open-task count/most-urgent and op=proposals [REC-6, now live] for the open-proposal count via loadProposals; reads op=whoami for the member identity; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-8 / MILESTONES M8 — the member HOME, the orientation surface that tells a member what the record is asking of them, the ENTRY POINT for M8. Aggregates the attention the record holds for THIS member: OPEN TASKS (op=tasks, the same feed UI-1's inbox reads — how many need you, the most urgent, linked to the Tasks inbox) and OPEN PROPOSALS (op=proposals, REC-6's discovery feed now live — how many derived findings await a member, linked to the Proposals surface; degrades HONESTLY with a "feed pending" note if op=proposals is absent, as UI-5 does — NEVER a fabricated count). Clear entry to the built surfaces (Tasks UI-1, Subjects UI-4, Members UI-7). HONESTY (load-bearing): every count comes from the op, never invented; an empty record shows an honest empty state ("nothing needs you right now"), never a fake number or a fabricated task. READ-ONLY: no act op sent from this surface. accepts-when civicos-ui/test/home.test.mjs: the home shows the member's open-task count from op=tasks and the proposal count from op=proposals (plus the honest "feed pending"/empty states); an empty record shows the honest empty state, not a fake number. NEGATIVE CONTROL: break the task-count wiring and the shown count no longer reflects op=tasks. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (1586ad2), clean cherry-pick. UI 21 harnesses green, plane battery 63/63. The member home is the M8 entry point, wired to live task + proposal feeds with honest empty/pending states. No DEC.
## CLAIM 2026-07-31 RECORD
session: record-agent-7
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (ONE new table `proposal_dispositions`, BEFORE host_governor), bio-plane/src/store.mjs (ONE new WRITE method `proposeDispose` + disposition-awareness wired into REC-6's `proposalsFeed` [reads the disposition store, filters aged proposals from the OPEN feed and returns them alongside]; `proposal_dispositions` in stats()/purge removed{} + a `DELETE FROM proposal_dispositions` in op=purge's whole-store arm; one line in the store DO dispatch: `proposedispose: () => this.proposeDispose(body || {})`), bio-plane/src/index.mjs (op=proposedispose in the OPS table [classes admin/member/probe, mutating:true] + `proposedispose` added to PROGRESSION_ACTIONS so a member/admin SESSION reaches it + NEEDS `proposedispose: "contribute"` + the server-side `decidedBy` stamp block), bio-plane/test/proposedispose.test.mjs (NEW) + its test:proposedispose entry in bio-plane/package.json, docs/development/INTERFACES.md (I5 additive bump; I3 disposition-awareness note)
interfaces consumed: REC-6 proposalsFeed (the feed a disposition ages out of), FW-9 progression_instances + stages (validating the proposal's identity), D-79 doctrine (age rather than vanish)
interfaces owned: I5 (ADDITIVE; registered owner RECORD)
expected: QUEUE.md REC-7 / MILESTONES M8 — op=proposedispose, record a PROPOSAL's defer/dismiss WITHOUT minting a bundle (UI-5's second delegation). A proposal-disposition store (I5-additive table `proposal_dispositions`) keyed by the proposal's identity (progression_key, stage_key) per D-79, holding state (deferred/dismissed), the REQUIRED reason, the server-stamped deciding member, and the time. op=proposedispose (mutating, contribute) records the disposition — NO bundle, no focus, no attribution beyond the disposition record (declining is NOT authoring, D-79). REC-6's op=proposals feed RESPECTS dispositions: an aged proposal is FILTERED from the OPEN feed and returned alongside in `dispositions[]` (filter-with-receipt — ages out of open so UI-5's instances[] re-aggregation drops it with zero UI change, but the decision is on the record, not hidden). accepts-when bio-plane/test/proposedispose.test.mjs: a member defers/dismisses a proposal with a reason → recorded, no bundle minted, no longer OPEN in op=proposals while an undismissed one still surfaces; NEGATIVE CONTROLS — dispose without a reason refused NO_REASON; dropping the disposition store makes the dismissed proposal reappear as open. New op carries a control-plane assertion (literal op=proposedispose). purge/D-113 asserted. No push/deploy/release — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (ba36d49), CLAIMS union-resolved. battery 64/64, --strict 107/107. UI-5's defer/dismiss is now fully live (both its delegations — REC-6 feed + REC-7 disposition — landed). I5 1.8.0, I3 1.4.0. No DEC.

## CLAIM 2026-07-31 M0-AGENT
session: m0-agent-m07
opened: 2026-07-31T00:00:00Z
paths: bio-plane/test/pipeline-e2e.test.mjs (NEW — ONE end-to-end integration suite driving the whole M4 entity-axis chain THROUGH the control plane; adds NO plane code, NO op)
interfaces consumed: I1 (op=acquire document.profile/digests/reading), I3 (the op contracts: acquire/promote/entitycreate/resolve/connections/progressiondefine/thread/instance/proposals/proposedispose), I5 (readings/entities/resolutions/connections/progression_instances/proposal_dispositions — read through the ops, never the store)
interfaces owned: none (drives the existing control plane; reshapes nothing)
expected: QUEUE.md M0-7 / MILESTONES M0 — the END-TO-END pipeline integration test. One suite chains a real captured Legistar meeting-calendar document acquire→promote→entitycreate+resolve→REC-5 alarm tick→connections→progressiondefine+thread→instance→proposals→proposedispose, ALL through dispatchFetch (a real caller's route). Proves the pieces COMPOSE by asserting the JOIN KEYS line up across stages — the capture_sha acquire produced is the one promote/reading/resolve/thread/instance carry; the entity_id resolve wrote is the one concerns/connections/instance/proposals read; the (progression_key, stage_key) the finding carries is the one proposals/proposedispose key on — each stage consuming the PRIOR stage's ACTUAL output, not a hand-built fixture. accepts-when bio-plane/test/pipeline-e2e.test.mjs green in the battery; NEGATIVE CONTROL — break ONE join (feed a divergent capture_sha into resolve/thread instead of the one acquire produced) and DOWNSTREAM assertions fail, proving the suite tests composition not just that each endpoint answers. Adds no op, so it DECLARES a control and adds 0 new unreached ops. No push/deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (1605dba). The M4 entity-axis pipeline composes end-to-end through the control plane with no integration gap — deploy-readiness evidence. battery 65/65, --strict exit 0.

## CLAIM 2026-07-31 RECORD
session: record-agent-8
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/store.mjs (REC-8 the OVERDUE-SUCCESSOR AGEING check, CONSTRUCTS Step 7 — all ADDITIVE, editing no FW-8/9/10 function: the injectable clock `#nowMs`; the derive-on-read overdue helpers `#intervalDeadlineMs`/`#captureDateMs`/`#instanceDeadlines`/`#overdueFindings`/`#overdueScan`; `proposalsFeed` extended to take a clock and merge `overdue_successor` findings + annotate proposals `overdue`/`overdue_count`/`kinds`; a SECOND framework consumer `overdue-scan` in `#schedConsumers` + its capture in `onAlarm`; `threadInstance` made async to ARM the alarm; the store DO dispatch line for `op=proposals` reads an optional `now` as-of param), bio-plane/test/overdue-successor.test.mjs (NEW) + its test:overdue-successor entry in bio-plane/package.json, docs/development/INTERFACES.md (I3 1.4.0 → 1.5.0, additive — op=proposals gains the overdue_successor kind + overdue proposal fields + the `now` param), docs/development/DECISIONS.md (DEC-10, open, for bob — whether an overdue proposal escalates), docs/development/DEBT.md (D-86 partly-settled note: temporal half built, bias-debt half deferred on the same consumer shape)
interfaces consumed: I5 (readings.at FW-5, register.registered, progression_instances/stages/exceptions — read through the store, reshaping none), I1 (the reading's `at` a document's date, via FW-5's readings table)
interfaces owned: I3 (the op contracts — additive bump), I5 (the store schema — UNCHANGED: overdue is DERIVED ON READ, no new table, so no I5 version change, no purge/D-113 entry)
expected: QUEUE.md REC-8 — CONSTRUCTS Step 7 AGEING. The record NOTICES when a progression's required next stage is OVERDUE: a placed, DATED predecessor + an ABSENT, UNDISCHARGED required successor + `predecessor_date + within_interval < now` → an `overdue_successor` proposal carrying the instance's grade, joined into op=proposals as a DISTINCT finding kind. DERIVE-ON-READ (no stored flag that goes stale against the clock, so NO new table); "now" is an INJECTABLE clock (env BIO_NOW_MS + op=proposals&now as-of, and onAlarm(now) for the alarm) so the computation is deterministic. Undetermined stays undetermined — a non-parseable interval, an undated predecessor, an absent predecessor, or a discharged/non-required stage is NEVER overdue. bias-debt (D-86's other half) DEFERRED and flagged, rides the same scheduler shape. accepts-when bio-plane/test/overdue-successor.test.mjs green in the battery (22/22); NEGATIVE CONTROL declared + RUN (neuter #instanceDeadlines → 7 fail); scheduler NC re-run (touched scheduling → subset-reconcile 8 fail, restored 18/18). No push/deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (5f76a7b), CLAIMS union-resolved. battery 66/66, --strict 107/107, overdue-successor 22. Derive-on-read (I5 unchanged). DEC-10 (overdue escalation) for bob-session. D-86 partly settled (temporal half done, bias-debt deferred). No new op. Second REC-1 consumer.

## CLAIM 2026-07-31 UI
session: ui-agent-9
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (UI-9 — CONSTRUCTS Step 8 PRESENTATION, the DOCUMENT-PAGE half: a READ-ONLY panel "What the record knows about this document" appended into the existing document/bundle view (openBundle) inside #docscroll after the four strata and before the act bars. Additive/in-place only: openBundle computes `const docKnows = await docKnowsPanel(capSha)` from the primary capture sha it already derives and injects `${docKnows}`; a self-contained module — `docKnowsPanel()` (the three sections), `docResolutionsHtml()` (subjects this document resolves to, op=resolutions by capture, REUSING UI-4's subjGradeBadge so a Grade C reads unconfirmed-not-established), `docConnectionsHtml()` (what it connects to, op=connections by capture, weaker-end graded), `docProgressionsHtml()`/`docInstanceHtml()` (progression membership + stage and any overdue successor), `docFindingOverdue()` (the ONE overdue seam — the negative-control target, reads REC-8's finding KIND, never re-derives a deadline), `docProgressions()`/`docProgGapHtml()` (the delegated per-document progression read + honest gap, the UI-5 loadProposals pattern), `docEntityLabel()` (bounded op=entity fan-out for subject labels), plus `.docprog-finding` CSS; reuses UI-4's tsec/subj-* CSS. No other screen's behaviour changed), civicos-ui/test/document-structure.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=resolutions (by capture sha256) for the resolved subjects at their §8.1 grade, op=connections (by capture sha256) for the graded connections, op=entity for subject labels, and a DELEGATED op=captureprogressions for the per-document progression membership + overdue findings; reads REC-8's overdue_successor finding shape; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-9 / MILESTONES M8 — CONSTRUCTS Step 8 PRESENTATION, the document-page half. On the open document page, a member reading ONE document sees its place in the accountability web (UI-4 is per-subject; this is per-document): (1) the SUBJECTS it resolves to at their §8.1 grade (op=resolutions/op=readingref for its capture sha — a Grade C shown "plausible, not established", the established/needs_confirmation flags READ FROM the op through UI-4's subjGradeBadge, never inferred from the letter); (2) what it CONNECTS to (op=connections for its capture — each graded the weaker end, shown as HOW established, not credibility); (3) which PROGRESSIONS it participates in and at which stage; (4) whether a required SUCCESSOR is OVERDUE (REC-8's overdue_successor — "N days overdue" from the op's OWN overdue_by_ms, shown HONESTLY: an undated/undetermined case emits no overdue_successor finding and the panel stays silent, never a fabricated deadline). READ-ONLY presentation; the acts live on their own surfaces. Items (1)/(2) read EXISTING per-capture ops; items (3)/(4) need a per-document → progression-instance lookup NO op exposes (op=instance reads ONE instance by (progression_key, entity_id); op=proposals aggregates every instance but carries no capture_sha to tie a finding to THIS document), so the progression half reads a DELEGATED op=captureprogressions and degrades to a named gap when it is absent — the UI-5 loadProposals pattern — rather than inventing membership. DELEGATION to RECORD appended below. DEFERRED: the CASE FILE (Step 8's other half) — flagged, not built. accepts-when civicos-ui/test/document-structure.test.mjs green: the document page shows the document's resolved subjects (a Grade C as unconfirmed, not established), its graded connections, its progression membership + stage, an overdue-successor note WHEN one exists and NONE when not, and the honest gap when the delegated op is absent. NEGATIVE CONTROL declared + RUN in-process — forcing the overdue seam docFindingOverdue() to `return true` makes a non-overdue missing step render as overdue when nothing is overdue; the suite asserts the marker is ABSENT under the real seam and PRESENT under the forced one (38/38 green; the §8.1 Grade-C-not-established property rides UI-4's subjGradeBadge seam, whose control is in subject-view.test.mjs). civicos-ui 22 harnesses green; plane battery 66/66 (no plane code touched — verified against the byte-identical plane source). Battery untouched. No push, no deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (e2e05e4). civicos-ui 22 harnesses green, plane battery 66/66. Items 1-2 live; 3-4 pending op=captureprogressions (lifted to REC-9). No DEC.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-9 (UI-9, the document page's referential + temporal structure panel)
need: a per-document → progression-instance READ op — proposed `op=captureprogressions&sha256=<capture>` (read; classes admin/member/probe; mutating:false; an optional `now=<ms>` as-of param exactly as op=proposals takes, for the overdue clock). It returns the progression INSTANCES a captured document is threaded into and, for each, the document's stage and the instance's derived findings, in this shape: `{ ok, capture_sha, count, instances:[ { progression_key, progression_label, entity_id, entity_label, stage_key, stage_label, findings:[ { kind:"missing_predecessor"|"overdue_successor", stage_key, stage_label, required, grade, grade_determined, established, needs_confirmation, /* overdue only: */ deadline, overdue_by_ms, predecessor_stage, predecessor_at, within_interval } ] } ] }`. It is a THIN read-time walk RECORD already has the pieces for: `SELECT DISTINCT progression_key, entity_id, stage_key, stage_label FROM progression_instances WHERE capture_sha=?` for membership+stage, then REUSE `#assembleInstance(progression_key, entity_id)` for the missing_predecessor findings and REC-8's `#overdueFindings(inst, #nowMs(now))` for the overdue_successor findings — so the honesty (undated/undetermined → never overdue, no fabricated deadline; established/needs_confirmation surfaced from the grade as #resolutionView does) stays enforced at the ONE derivation point, never re-implemented in the UI. Today no op maps a capture back to its instances: op=instance needs BOTH (progression_key, entity_id), and op=proposals walks every instance but returns no capture_sha to tie a finding to a specific document.
why: not blocking — UI-9 shipped the document-page panel with items (1) subjects and (2) connections fully live over the existing per-capture ops (op=resolutions, op=connections), and items (3) progression membership and (4) overdue successor built and PROVEN against this delegated shape (the test mocks it) but degrading HONESTLY to a named gap on-screen until the op lands, exactly as UI-5's loadProposals did for op=proposals before REC-6. RECORD owns store.mjs and I3; whether to add this read (and the exact field set) is RECORD's call. The finding kinds, grades, and overdue derivation already exist (FW-9/FW-10 + REC-8) — this op only needs to key them by capture instead of by (progression, entity).
status: open — raised by UI-9, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).

## CLAIM 2026-08-01 RECORD
session: record-agent-9
opened: 2026-08-01T06:30:00Z
paths: bio-plane/src/store.mjs (REC-9 — ADDITIVE only, editing no FW-8/9/10/REC-6/7/8 function: a NEW read method `captureProgressions({captureSha, nowMs})` — the capture→instances walk — plus its store DO dispatch line `op=captureprogressions`; REUSES `#assembleInstance` + REC-8's `#overdueFindings`/`#nowMs`/`Store.#isEstablished`, re-implementing NONE of them), bio-plane/src/index.mjs (the `op=captureprogressions` OPS entry `{classes:[admin,member,probe], mutating:false}` + its membership in `PROGRESSION_ACTIONS` so a session reaches the read; the control-plane assertion rides the new suite), bio-plane/test/capture-progressions.test.mjs (NEW) + its `test:capture-progressions` entry in bio-plane/package.json, docs/development/INTERFACES.md (I3 1.5.0 → 1.6.0, additive — one NEW read op, no existing op changed)
interfaces consumed: I5 (progression_instances read for the capture→instance placements + stage; readings/register/progression_stages/exceptions read only THROUGH #assembleInstance/#overdueFindings, reshaping none)
interfaces owned: I3 (the op contracts — additive bump), I5 (the store schema — UNCHANGED: DERIVE-ON-READ, no new table, no purge/D-113 entry)
expected: QUEUE.md REC-9 / MILESTONES M8 — CONSTRUCTS Step 8 PRESENTATION, the document-page delegation (UI-9). A READ op `op=captureprogressions&sha256=<capSha>` maps a CAPTURE back to the progression INSTANCES it is threaded into (by JOINING progression_instances on capture_sha), its STAGE in each, and each instance's `missing_predecessor` + `overdue_successor` findings — REUSING `#assembleInstance` + REC-8's `#overdueFindings` at the ONE derivation point (no re-derivation of overdue/missing-predecessor logic; FW-10 discharges honored; the same injectable clock `now`/BIO_NOW_MS so overdue is deterministic). DERIVE-ON-READ, no table, no mutation. Undetermined stays undetermined; a capture in NO progression returns an empty list, never a fabricated membership. established/needs_confirmation PROJECTED from each finding's grade (the #resolutionView boundary projection, not a new claim) so a Grade A/B finding renders established on the document page. accepts-when bio-plane/test/capture-progressions.test.mjs green in the battery (13/13, driven THROUGH the control plane via dispatchFetch, literal `op=captureprogressions`); NEGATIVE CONTROL declared + RUN (drop the capture-join → 6 assertions fail, the membership/stage/findings; the empty-list + NO_SHA assertions still pass; restored 13/13). battery 67/67 (3161 assertions); `npm run test:coverage --strict` exit 0 — 108/108 ops reached through the control plane, 0 unreached, 67/67 controls declared. No push/deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (54d671a). UI-9's document page is now FULLY live (items 3-4 populate from op=captureprogressions). battery 67/67, --strict 108/108. I3 1.6.0. Derive-on-read, no table. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: coff2-agent (worktree agent-a94f0a529394654b6)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/ooxml.mjs (NEW — COFF-2, the OOXML container reader: a PURE zero-dependency module — central-directory walk, member inflate via DecompressionStream("deflate-raw"), part lookup by name, [Content_Types].xml parse + flavour discrimination docx/xlsx/pptx vs arbitrary ZIP, the uniform _rels/*.rels walker (TargetMode="External" → outbound), docProps/core.xml metadata extraction (DEC-5), size-guard plumbing with a NAMED PROVISIONAL bound until COFF-6 measures; ODF DESIGNED FOR — the part-map is a parameter — not built), bio-plane/test/ooxml.test.mjs (NEW, programmatic hermetic fixtures) + its test:ooxml entry in bio-plane/package.json
interfaces consumed: I7 (built against ON PAPER, deliberately independent of COFF-1's landing; COFF-1 confirms I7 from as-built code — this claim reshapes nothing in I7's text)
interfaces owned: none. Explicitly NOT touched: bio-plane/src/formats.mjs and the index.mjs dispatch sites (COFF-1's claim), QUEUE.md, DECISIONS.md
expected: QUEUE.md COFF-2 / MILESTONES M2. accepts-when: battery green with fixture round-trips for all three flavours; a plain ZIP NOT identified as OOXML and a renamed one caught by magic-bytes-plus-parts rather than extension or content type; a truncated central directory → stated undetermined, never a silent partial; negative control — neuter the flavour discrimination and the suite fails on the plain-ZIP assertion. No push/deploy — CONDUCT integrates; `landed:` left for CONDUCT.
released: 2026-08-03 — landed on main by CONDUCT (7404f21, worker commit a20e3ec). Verified from the main checkout: battery 68/68 (3301 assertions), coverage --strict exit 0 (108/108 ops, fleet 1/1). Negative control run by the worker (neutered discrimination → 13/97 fail incl. the plain-ZIP assertion; restored 97/97) and recorded in the suite's NEGATIVE CONTROL line. Provisional 32 MiB bound named in code for COFF-6 to replace. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: content-office-coff1
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/formats.mjs (NEW — the FORMAT registry, I7: one entry per format, detection by magic bytes first / content type second; the HTML and PDF entries; registerFormat/unregisterFormat/getFormat/detectFormat), bio-plane/test/formats.test.mjs (NEW — the registry suite, incl. the TEST-ONLY stub-format assertion that is the D-70 evidence) + its test:formats entry in bio-plane/package.json, bio-plane/src/index.mjs (TWO NAMED dispatch touchpoints ONLY, moved onto the registry: [1] the acquire-time HTML_CT guard at the subresource branch (~:1836) — detection consults the registry, the subresource branch stays HTML-only in behaviour; [2] the read-time op=pdfstructure dispatch (~:1417) — the op survives with byte-identical output, routed through the registry's pdf entry; plus the ADDITIVE profile.format stamp beside the FW-3 profile assembly), docs/development/INTERFACES.md (I1 1.2.0 → 1.3.0 additive — document.profile gains `format`; I7 CONFIRMED from as-built code per the item), docs/development/DEBT.md (D-70 row — the verdict)
interfaces consumed: I1 (op=acquire's document.profile — extended ADDITIVELY, no existing field reshaped), I2 (the pdf entry's structure output is pdfstructure.mjs's existing I2 shape, byte-identical — pinned by pdfstructure.test.mjs / pdfstructure-op.test.mjs)
interfaces owned: I7 (PROVISIONAL → CONFIRMED by this item, from the code as built; code wins on drift)
expected: QUEUE.md COFF-1 / MILESTONES M2 — the FORMAT registry with HTML and PDF moved onto it, NO new capability. NOTE on the two index.mjs touchpoints: CAPTURE (HTML_CT site) and CONTENT-PDF (op=pdfstructure site) are DORMANT, so per the CONTENT-OFFICE kickoff this is a claim WITH A NOTE naming their surfaces, not a live delegation — existing outputs are PINNED (the battery's HTML and PDF suites must pass byte-identical). pdfstructure.mjs itself is NOT edited; it becomes the PDF entry by import. accepts-when: battery green with HTML/PDF outputs pinned unchanged; a TEST-ONLY stub format registers and is reachable detect→structure with ZERO edits outside the registry (the D-70 evidence); test:coverage --strict exit 0; negative control RUN and recorded (delete the PDF entry → op=pdfstructure fails NAMING the format as unregistered, battery fails; restored). No push — CONDUCT integrates; landed: left for CONDUCT.
released: 2026-08-03 — landed on main by CONDUCT (merge of worker 9593f71). Verified from the main checkout with COFF-2 together: battery 69/69 (3338), --strict exit 0 (108/108). I7 1.0.0 confirmed, I1 1.3.0 additive, D-70 closed. No DEC.

## CLAIM 2026-08-03 M0
session: rec27-agent (worktree agent-a068a4d114caf7901)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/test/hygiene.test.mjs (the D-113 table-coverage assertion taught to parse store.mjs's CREATE TABLE literals as well as schema.mjs's, three new EXEMPT entries with stated reasons, a new raw-control-byte guard over bio-plane/src/, the NEGATIVE CONTROL line), bio-plane/src/store.mjs (the purge TABLES/DELETE list only - project_participants and project_owner_votes added to both arms, keyed on project_id which IS a bundle id; stats() gains their two counters so purge can PROVE it took them; and the D-131 raw NUL byte at :3833 rewritten as the u0000 escape, value-identical), bio-plane/test/projects.test.mjs (a new purge section asserting the participation graph actually clears, per-bundle and whole-store), docs/development/DEBT.md (rows D-137 and D-131 disposition cells only)
interfaces consumed: I5 (the store schema - read; no table added, no column changed, purge completeness only)
interfaces owned: none
expected: QUEUE.md REC-27 / BUILD-ORDER.md section 2 (REC-27). accepts-when: npm run test:hygiene reports 52 of 52 tables covered by purge or a stated exemption (was 44 of 52); grep -c "CREATE TABLE" bio-plane/src/store.mjs non-zero WITHOUT -a; full battery green; coverage --strict exit 0; negative control - remove project_participants from the purge list and the hygiene suite names it. No push/deploy - CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 3e35d56). Verified from the main checkout: battery 69/69 (3374), hygiene 52/52 covered, --strict exit 0. Negative control RE-RUN BY CONDUCT (destructive code): neutered purge DELETEs → hygiene names project_participants, 4 projects assertions fail; restored green, byte-identical. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: coff4-agent (worktree agent-a9b712936848f18cc)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/docx.mjs (NEW — COFF-4, the DOCX registry entry: I7 detect/parts/structure/text built on ooxml.mjs primitives; word/_rels/document.xml.rels → I2 partitions through the ONE subresources.mjs linkWrapper; bookmarks → anchor; word/embeddings/ → intra; element references {kind:"doc-para"} per IC-1 as resolved; <w:t> text in body order; THE EVIDENTIARY CORE per DEC-5 — w:ins/w:del with author, date and the SUPERSEDED WORDING, word/comments.xml with author and date — under the shared I2 extension envelope), bio-plane/test/formats-docx.test.mjs (NEW, hermetic programmatic fixtures modelled on real Oakland document structure) + its test:formats-docx entry in bio-plane/package.json, bio-plane/src/formats.mjs (ONE registerFormat(docxEntry) call plus its import — nothing else, so COFF-3 merges cleanly), docs/development/INTERFACE-CHANGES.md (IC-2 — the shared evidentiary envelope, FILED from as-built code if COFF-4 lands first per the COFF-3/4/5 first-lander rule; CONFIRMING instead if COFF-3's IC-2 is on main by integration time)
interfaces consumed: I7 (the entry shape, confirmed 1.0.0 — consumed, not reshaped), I2 (links through the ONE linkWrapper, parity as pdfstructure.test.mjs pins it), IC-1 as RESOLVED (doc-para: para 0-based REQUIRED, run 0-based OPTIONAL — present when the reference genuinely targets runs)
interfaces owned: none. Explicitly NOT touched: bio-plane/src/ooxml.mjs (COFF-3 holds its sizeGuard lines this wave), the xl/ppt part-maps, the index.mjs dispatch sites, QUEUE.md, DECISIONS.md
expected: QUEUE.md COFF-4 / MILESTONES M2. accepts-when: battery green with a real-Oakland-shaped DOCX fixture yielding paragraph-referenced links and a tracked change carrying author, date and the superseded wording; an unreadable part → stated undetermined; test:coverage --strict exit 0; negative control — drop the superseded wording from the w:del emit and the suite fails naming it (RUN and recorded in the suite's NEGATIVE CONTROL line, restored after). No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 64e2238). Verified from the main checkout: battery 70/70 (3459), --strict exit 0. IC-2 proxy-answered and I2 bumped 1.0.0 → 1.1.0 at this integration. COFF-3's colliding IC-2 filing (key `evidence`) is being conformed to the accepted `evidentiary` envelope by the resumed COFF-3 worker. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: coff3-agent (worktree agent-acb374dffe4ce448c)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/formats-xlsx.mjs (NEW — COFF-3, the XLSX registry entry: detect/parts/structure/text per I7 as confirmed; element references {kind:"sheet-cell"} per IC-1 RESOLVED-as-amended; DEC-5 evidence envelope — formulas beside cached values, hidden rows/columns/SHEETS flagged), bio-plane/test/formats-xlsx.test.mjs (NEW, programmatic hermetic fixtures modelled on real Oakland workbook structure) + its test:formats-xlsx entry in bio-plane/package.json, bio-plane/src/ooxml.mjs (ONLY the size-guard block — the COFF-6 enactment my queue item orders: PROVISIONAL_OOXML_SIZE_BOUND_BYTES (32 MiB container) replaced by the MEASURED bound, 20 MiB of DECLARED UNCOMPRESSED text-part bytes summed from the central directory before inflation, MEASUREMENTS.md 2026-08-03 — the METRIC changed, not just the number; over-bound → text-undetermined marker kept; streaming-to-64-MiB NOT built, deferred), bio-plane/test/ooxml.test.mjs (the bound assertions CORRECTED to the measured metric, never exempted), bio-plane/src/formats.mjs (ONE registerFormat(xlsxEntry) call plus its import — nothing else), docs/development/INTERFACE-CHANGES.md (APPEND: IC-2, the shared I2 evidence envelope, filed FROM AS-BUILT CODE by whichever of COFF-3/4 lands first — this claim files it unless COFF-4 has already appended one, in which case COFF-3 appends a CONFIRM instead)
interfaces consumed: I7 (registry entry contract, CONFIRMED 1.0.0 — built against as-built formats.mjs/ooxml.mjs), I2 (structure/text shapes — partitions through the ONE linkWrapper imported from subresources.mjs, never re-derived), IC-1 RESOLVED as amended (sheet-cell element references)
interfaces owned: none reshaped. Explicitly NOT touched: index.mjs dispatch sites, word/ppt part-maps (COFF-4/5), QUEUE.md, DECISIONS.md, INTERFACES.md (the IC-1 CHANGING/version-bump bookkeeping is flagged to CONDUCT, not enacted here)
expected: QUEUE.md COFF-3 / MILESTONES M2. accepts-when: battery green with a real-Oakland-modelled workbook fixture yielding cell-referenced links, formulas beside values, and a hidden sheet flagged; an over-bound workbook → text-undetermined named; test:coverage --strict exit 0; negative controls RUN and recorded — collapse <f> into <v> → the suite fails naming the formula/value distinction, AND strip the hidden flag → the suite fails. No push/deploy — CONDUCT integrates; landed: left for CONDUCT.
note 2026-08-03 (post-COFF-4 reconciliation): COFF-4 landed FIRST, so its IC-2 filing (`evidentiary` envelope) is the accepted one. This worker's variant filing was deleted before ever reaching main, the xlsx emit conformed to the accepted envelope from docx.mjs as built (same key, same fields, core-properties as an item kind), and a CONFIRM appended under the accepted IC-2 chain instead. ONE line outside the claimed paths: bio-plane/test/formats-docx.test.mjs:351 pinned the provisional bound constant this claim's COFF-6 enactment replaced — corrected (never exempted, comment says why) because the two items could only meet at this merge; flagged here for CONDUCT rather than delegated, since COFF-4's worker is released and the battery cannot be left red.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker c57b909, the reconciled commit). Verified from the main checkout: battery 71/71 (3540), --strict exit 0. The envelope fork was caught and reconciled (variant deleted pre-main; CONFIRM under the accepted IC-2); the one cross-claim edit (formats-docx.test.mjs bound pin) reviewed and accepted — corrected, never exempted. No DEC.
session: coff5-agent (worktree agent-adc3da524a0eff4e9)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/pptx.mjs (NEW — COFF-5, the PPTX registry entry: I7 detect/parts/structure/text built on ooxml.mjs primitives; ppt/slides/_rels/slideN.xml.rels → I2 partitions through the ONE subresources.mjs linkWrapper; slide refs → anchor; ppt/embeddings/ → intra content-addressed by sha256; element references {kind:"slide-shape"} per IC-1 as resolved; <a:t> runs per slide; THE EVIDENTIARY CORE per DEC-5 — notesSlide speaker notes emitted per slide and DISTINGUISHABLE from slide text everywhere shown, cited or indexed, plus docProps core properties — under the accepted IC-2 evidentiary envelope exactly as docx.mjs carries it), bio-plane/test/formats-pptx.test.mjs (NEW, hermetic programmatic fixtures modelled on real Oakland deck structure) + its test:formats-pptx entry in bio-plane/package.json, bio-plane/src/formats.mjs (ONE registerFormat(pptxEntry) call plus its import — nothing else, so COFF-3 merges cleanly), docs/development/INTERFACE-CHANGES.md (IC-2 — a CONFIRM note from as-built code per the COFF-3/4/5 rule; COFF-4's filing is ACCEPTED and I2 is 1.1.0 — no variant, no INTERFACES.md edit)
interfaces consumed: I7 (the entry shape, confirmed 1.0.0 — consumed, not reshaped), I2 1.1.0 (links through the ONE linkWrapper, parity as formats-docx.test.mjs pins it; the IC-2 envelope CONFIRMED, not varied), IC-1 as RESOLVED (slide-shape: slide 1-based REQUIRED matching the human ref "slide N", shape 0-based OPTIONAL — present when the reference genuinely targets a shape)
interfaces owned: none. Explicitly NOT touched: bio-plane/src/ooxml.mjs (COFF-3 holds its sizeGuard lines this wave), bio-plane/src/docx.mjs, the xlsx entry, the index.mjs dispatch sites, QUEUE.md, DECISIONS.md, INTERFACES.md
expected: QUEUE.md COFF-5 / MILESTONES M2. accepts-when: battery green with a real-Oakland-modelled deck fixture yielding slide+shape references and speaker notes distinguishable from slide text; test:coverage --strict exit 0; negative control — merge notes into slide text and the suite fails naming the distinction (RUN and recorded in the suite's NEGATIVE CONTROL line, restored after). No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker fde39df). Verified from the main checkout: battery 72/72 (3637), --strict exit 0. One integration-seam test correction applied by CONDUCT at merge (stale provisional-bound pin → measured constant, corrected-with-comment, the formats-docx precedent). Hidden-slides flag queued as COFF-7. No DEC.

## CLAIM 2026-08-03 CONTENT-OFFICE
session: coff7-agent (worktree agent-ab604afd9f19f3f47)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/src/pptx.mjs (COFF-7 — hidden slides, the pptx analogue of xlsx hidden sheets per DEC-5: `show="0"` read from BOTH the slide part's own `<p:sld>` root (ECMA-376's home for CT_Slide@show — where PowerPoint's Hide Slide writes it) and the `sldIdLst` entry (the queue item's stated location, honored when the bytes carry it); ONE new envelope kind `hidden-slide` naming the slide; the hidden slide still FULLY extracted — text, notes, links — and its text units marked with a `hidden` field on `slides[]`/`speakerNotes[]`, the xlsx `sheets[]` precedent), bio-plane/test/formats-pptx.test.mjs (the hidden-deck cases, both attribute locations + the reorder interaction; existing unit-shape assertions extended with the mark — corrected, never exempted), docs/development/INTERFACE-CHANGES.md (ONE CONFIRM under IC-2 — the kind vocabulary grows, the envelope shape does not change), docs/development/CLAIMS.md (this entry)
interfaces consumed: I7 (unchanged), I2 1.1.0 (the IC-2 envelope CONFIRMED — one kind added, no shape change), IC-1 as RESOLVED (slide-shape refs consumed unchanged)
interfaces owned: none. Explicitly NOT touched: bio-plane/src/ooxml.mjs, bio-plane/src/formats.mjs (registration already exists), docx/xlsx entries, index.mjs dispatch, INTERFACES.md, QUEUE.md, DECISIONS.md
expected: QUEUE.md COFF-7 / MILESTONES M2. accepts-when: battery green with a fixture deck whose hidden slide is extracted AND flagged (kind `hidden-slide`, text units marked) while visible slides are not; test:coverage --strict exit 0; negative control — neuter the one `show` funnel and the suite fails naming a hidden slide indistinguishable from a visible one (RUN, recorded in the suite's NEGATIVE CONTROL line, restored after). No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 7eb9d84). Verified from the main checkout: battery 72/72 (3652), --strict exit 0. The ECMA-376 location correction and the included-AND-flagged choice both accepted, recorded on the item's landed line. No DEC.

## CLAIM 2026-08-03 FRAMEWORK
session: fw15-agent (worktree agent-ad534b8749f44a4e7)
opened: 2026-08-03T19:30:00Z
paths: docprofile/readtext.mjs (NEW — FW-15's ONE entry point, readText(): takes I2's text field FROM ANYWHERE — text.document, text.pages[] or the office formats' pageless text.paragraphs[] degenerate form (I2 1.1.0), or a bare string — applies the honesty gates (no text / text-undetermined -> determined:false with the producer's own markers named; a PARTIAL decode proceeds only STATED, never silently), then runs identify()/doctypeFor()/parse() over the flat text), docprofile/doctypes/meeting-agenda.mjs (NEW — a MEASURED content type, written from the real oakland.legistar.com agenda packet View.ashx?M=A&ID=1425405 fetched and Tier-1-extracted 2026-08-03: legislation file numbers on their own lines as the stable source-assigned keys, Subject:/From: labelled blocks, item numbers preceding the file line), docprofile/doctypes/registry.mjs (ONE import + ONE types.register(meetingAgenda) line, after meetingCalendar, before generic), docprofile/events.mjs (THREE catalogue entries for the agenda type: item_pulled EVENT, item_changed NOTICE, item_added ROUTINE — the catalogue's own extension point), docprofile/registry.mjs (re-export of readtext.mjs so the plane's one docprofile import path carries it), bio-plane/src/index.mjs (ACQUIRE-PATH WIRING ONLY, exact touchpoints: [1] the docprofile import line (~:39) gains readText; [2] the op=acquire reading assembly's not-read-as-text ELSE branch (~:2260-2268, the block ending "declares no reader") is extended — when the FW-3/COFF-1 profile.format names a registered format and the capture is single-part, the primary is read back from R2, the format entry's OWN text surface is consulted (pdf: structure(bytes).text — Tier 1, escalated to the pdf-worker via the existing needsTier2() predicate exactly as op=pdfstructure does when the binding exists; docx/xlsx and any future entry: text(parts)), and readText() produces the reading; text-undetermined -> a FAILED reading (found:false, basis naming the producer's markers), NO other branch of acquire touched, op=pdfstructure NOT touched, formats.mjs/docx.mjs/formats-xlsx.mjs NOT touched), bio-plane/test/reading-wire.test.mjs (NEW suite) + bio-plane/test/fixtures/legistar-agenda-1425405.pdf (the REAL Oakland agenda packet, committed as a fixture with its locator + retrieval date in the suite header) + the test:reading-wire script entry in bio-plane/package.json
interfaces consumed: I2 (the text shape incl. 1.1.0's paragraphs[] degenerate form — consumed, not reshaped), I1 (op=acquire's document.reading — the FW-5 field, extended ADDITIVELY with text_source/"layer", text_tier, text_container sibling keys; no existing key reshaped), I7 (format entries consumed through getFormat(); no entry edited)
interfaces owned: I2 (owner FRAMEWORK — no shape change made; the entry point is a CONSUMER of the stable shape)
expected: QUEUE.md FW-15 / BUILD-ORDER.md §2 (FW-15) — the L2→L3 wire. accepts-when: battery green with the new suite where acquiring the real agenda PDF (Tier-1 text decodes, 99.9%) produces a readings row and reading_refs rows (legislation:NN-NNNN references), and an encrypted-marker PDF (text-undetermined) produces a reading with found:false and NO invented refs; test:coverage --strict exit 0; NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — bypass the wire and the reading_refs count is zero for a PDF known to name three entities. NOT built: OCR (CPDF-10's, waits), any FORMAT registry entry change, any op. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 742d15a). Verified from the main checkout: battery 73/73 (3709), --strict exit 0. Stack-axis observation recorded as D-167; stale FRAMEWORK kickoff head corrected by CONDUCT at integration (rule 6). No DEC.

## CLAIM 2026-08-03 RECORD
session: rec19-agent (worktree agent-ab1396bcfe7a4d910)
opened: 2026-08-03T21:00:00Z
paths: bio-plane/src/affordances.mjs (NEW — REC-19, the act catalogue + derivation for op=affordances: ACTS/NON_ACTS totality over NEEDS, declared RUNGS (7 sourced, null otherwise), the published vocabularies), bio-plane/src/index.mjs (ONLY: one OPS entry for `affordances` beside whoami, the op=affordances handler block after the whoami handler, and the affordances.mjs import — the OPS/NEEDS table region; NEEDS itself not reshaped), bio-plane/src/store.mjs (ONLY: a new #citesInto private helper extracted from retire's live-cites guard so the derivation and the guard are ONE predicate, retire's guard loop replaced by a call to it, a new affordanceFacts() read method placed after searchIndexCheck, and ONE `affordancefacts:` line in the DO dispatch map — NOT the normalisation sites, NOT the op=dispose state machine, which are REC-10's), bio-plane/checks/bio-checks.mjs (ONLY: export ACTION_KINDS at module level and have checkActionExtension consume it — NOT OBJECT_TYPES/HEADINGS/knownSchemas/checkFocusExtension, which are REC-10's), bio-plane/test/affordances.test.mjs (NEW) + its test:affordances entry in bio-plane/package.json
interfaces consumed: I3 (adds one non-mutating read op; no existing contract reshaped), the exported STATES table from checks (imported, never copied — the item's own requirement)
interfaces owned: I3 additive (op=affordances). Explicitly NOT touched: bio-checks.mjs OBJECT_TYPES/HEADINGS/knownSchemas/checkFocusExtension, store.mjs normalisation + op=dispose LEGAL table, query.mjs — all REC-10's concurrent claim; QUEUE.md; DECISIONS.md; civicos-ui (its DISPOSITIONS/TASK_KIND_UI copies retire when the UI items consume this op)
expected: QUEUE.md REC-19 / MILESTONES M8, DEC-8 doctrine (the plane-sourced act pre-flight). accepts-when: battery green with a suite where op=affordances for a collected information bundle returns exactly the acts the plane would permit (and NOT retire for one carrying a live cites edge), each with its needs, rung:null for every op no document assigns one; coverage --strict reaches the op from the control plane; negative control — add an op to NEEDS without adding it to the affordances derivation and the suite fails naming the unpublished op (RUN, recorded, restored). No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker d22db7b). Verified from the main checkout: battery 74/74 (3751), --strict exit 0, 109/109 ops. D-168 recorded (cite permits retired); REC-25 scope amended (affordances joins the stamp list); dispose rewire folded into REC-11. No DEC.
## CLAIM 2026-08-03 RECORD
session: rec10-agent (worktree agent-a7ce355855ad80206)
opened: 2026-08-03T00:00:00Z
paths: bio-plane/checks/bio-checks.mjs (the type catalog — OBJECT_TYPES/LEGACY_TYPE_ALIASES/BUNDLE_ID_RE/ANN_ID_RE/HEADINGS/STATES/knownSchemas, checkHeadings ~:397, checkStateLegality ~:410, checkFocusExtension→checkInquiryExtension ~:1199, checkRecheckCoverage ~:1176, C-6.3 ~:1029, plus the NEW shared title-derivation helper for C-16), bio-plane/src/store.mjs (ONLY: the boot type normaliser ~:194, op=dispose's state machine + type gate + refusal strings ~:1560-1691, promote's type/title projection ~:2795-2810, listBundles' type filter ~:2521 — named functions, NOT capture/link/task/reachability), bio-plane/src/query.mjs (~:411, the object_type filter-value rewrite), bio-plane/src/setup.mjs (the intake tables + form: option/TYPES/PREFIX/SCHEMA_OF/mdFor/create handler — no path asks an inquiry for a title), bio-plane/src/index.mjs (ONE line: the D-78 surfaced_by restamp condition ~:2954 gains the inquiry spelling via normalizeType — NOT the ACQUIRE path, no new op; FW-15's ground untouched), bio-plane/migrate/migrate.mjs (:29 TYPE_ROOTS gains inquiries), bio-plane/test/inquiry.test.mjs (NEW, carries focus.test.mjs's three blocks; focus.test.mjs retired into it), bio-plane/test/disposition.test.mjs + conformance.test.mjs + search.test.mjs + check-firing.test.mjs (assertions superseded by the rename corrected, never exempted), docs/development/CLAIMS.md (this entry)
interfaces owned: I5 (additive — no schema.mjs change this turn), I3 (NO new op; ONE wire change inside op=dispose: NOT_PROBLEMS renamed, refusal details stop saying Problem — item 13 of DATA-MODEL §2.7's change list, carried by QUEUE REC-10)
explicitly NOT touched: docprofile/** and index.mjs's ACQUIRE path (FW-15's claim), civicos-ui/** (REC-10 is "no UI"; the UI catch-up is items 15-16 of the change list, CONDUCT sequences it), schema.mjs, newgroup/, QUEUE.md, DECISIONS.md
expected: QUEUE.md REC-10 / MILESTONES M9. accepts-when: battery green with inquiry.test.mjs carrying the three focus.test.mjs blocks (canonical INQ- works · legacy FOCUS-/PROB- still validates · projection normalises all three to inquiry); a member creating a question through either entry point gets the same required fields and no path asks for a title; coverage --strict exit 0 with no new unreached op; negative control — remove ONE of the four normalisation sites and the suite fails naming that site's disagreement (RUN, recorded in the suite's NEGATIVE CONTROL line, restored). No push — CONDUCT integrates; landed: left for CONDUCT.
released: 2026-08-04 - RELEASED AS STALE by CONDUCT at session handover, under PARALLELISM.md's stale-claim rule. Evidence rather than assumption: QUEUE.md marks REC-10 done, the worktree `agent-a7ce355855ad80206` no longer exists, and no branch of that name remains in the repository. The session that opened it ended on 2026-08-03 without releasing it, so the claim has been holding `checks/bio-checks.mjs` and a large region of `store.mjs` against every later worker for a day - none of which collided with it only because the ground moved on. A claim outliving its session is the stale-lock shape BATON.md's own note describes one artefact over; releasing it is the fix, not editing around it.

## CLAIM 2026-08-03 RECORD
session: rec25-agent (worktree agent-a5af9d143bf40ab47)
opened: 2026-08-03T22:30:00Z
paths: bio-plane/src/index.mjs (ONLY: the D-15 viewer-stamp condition extended to the read ops — op=list, op=index, op=projection, op=image, op=file, op=backlinks joining the search/select/edge-action stamp; the op=affordances handler's affordancefacts fetch gaining the same server-side viewer stamp; the internal ratify /image + /list and monitor-tick /image fetches stamped so the fail-closed store still answers them; ONE new OPS entry `backlinks` beside list — NOT the type tables, NOT NEEDS reshaping), bio-plane/src/store.mjs (ONLY: listBundles, buildIndex, projection, affordanceFacts and the image/file dispatch-map lambdas gaining the D-15 gate via query.mjs's ONE viewerPredicate compilation point, a #viewerSees helper beside readImage, a NEW backlinks() read method + its dispatch-map line — NOT the normalisation sites, NOT the dispose state machine/LEGAL table, which are REC-10's concurrent claim), bio-plane/test/gate-reads.test.mjs (NEW) + its test:gate-reads entry in bio-plane/package.json, one-line `viewer=class:member` stamps on the direct-DO fixture calls the fail-closed gate now requires in test/{store,projection,search,selection,cite,edges,focus,disposition,release,projects}.test.mjs and test/cite-scale.mjs (fixture-call URLs only; no assertion of REC-10's type/state ground is changed), docs/development/MEASUREMENTS.md (the F-8 probe, before/after), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 (adds one non-mutating read op, op=backlinks; no existing contract reshaped — a gated read returns what D-15 says the viewer may see, and an invisible or absent target answers identically), query.mjs viewerPredicate (IMPORTED, never copied — D-15 keeps its single compilation point; query.mjs itself NOT edited)
interfaces owned: I3 additive (op=backlinks). DISJOINT FROM REC-10 by site: REC-10 holds the bio-checks.mjs type tables, store.mjs normalisation + dispose state machine, and query.mjs normalisation; this claim touches none of those functions — its store.mjs sites are the read paths (listBundles/buildIndex/projection/affordanceFacts/dispatch-map read lambdas + new backlinks) and its index.mjs site is the viewer-stamp region. Explicitly NOT touched: query.mjs, bio-checks.mjs, civicos-ui/app.html (the reverseRefs deletion is UI-21's; this provides the read), QUEUE.md, DECISIONS.md
expected: QUEUE.md REC-25 / BUILD-ORDER.md §2 (REC-25) + the 2026-08-03 affordances amendment. accepts-when: battery green with a suite where a member uninvited to a project gets that project from NONE of op=list, op=index, op=projection, op=image, op=search, op=affordances or a backlink — not id, not title, not state — and no message discloses that anything was hidden; the invited member still sees everything; coverage --strict exit 0 with op=backlinks carrying its control-plane assertion in the same turn; NEGATIVE CONTROL run and recorded — remove the stamp from op=list alone and the suite names op=list. No push — CONDUCT integrates; landed: left for CONDUCT.
amended 2026-08-03 (rec25-agent, same session): + bio-plane/src/livefire.mjs (ONE stamp — its scratch-canary image read is the same internal-server-reader class as ratify's and the monitor's, and the fail-closed store now requires it to say whose view it reads at: class:probe, machine scope). Found by the battery, not by review — the store failing closed turned a forgotten reader into a loud outage, which is the posture working.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker cdccf79). Verified from the main checkout: battery 75/75 (3813), --strict exit 0, 110/110 ops. NCs RE-RUN BY CONDUCT, both arms (unstamp op=list → outage naming it; leak the gate → 3 failures naming the leak). Integration seam: inquiry.test.mjs's direct-DO reads stamped by CONDUCT at merge (postdates the worker's branch). Residuals queued as REC-30. No DEC.
session: rec11-agent (worktree agent-a09a5716d8c643616)
opened: 2026-08-03T23:00:00Z
paths: bio-plane/src/schema.mjs (the inquiry_basis table + its two indexes, placed BEFORE the host_governor block), bio-plane/src/store.mjs (ONLY: promote's basis validation/refusals + the inquiry_basis projection beside the refs projection, a #basisCyclePath helper, two read methods basisFor/restingOn with their DO fetch-map lines, purge's TABLES gains inquiry_basis, and the REC-19-folded chore — dispose() and proposeDisposition() import DISPOSITIONS from affordances.mjs instead of holding literal copies), bio-plane/src/affordances.mjs (comment only: the DISPOSITIONS "until unified" note updated — the store now imports it), bio-plane/checks/bio-checks.mjs (ONLY: new exported checkInquiryBasis + its vocab constants, called from checkInquiryExtension; checkReferences' C-6.3 elevated_into arm REPLACED per QUEUE REC-11 by the basis-in-references[] arm), bio-plane/test/basis.test.mjs (NEW) + its test:basis script in bio-plane/package.json, bio-plane/test/check-firing.test.mjs (the C-6.3 proof corrected to the replacing rule, never exempted), bio-plane/test/affordances.test.mjs (the dispose-literal pin corrected to assert the import — the pin's subject was unified away by this item's folded chore), docs/development/CLAIMS.md (this entry)
interfaces owned: I5 (additive — one new derived table, both purge arms). I3 untouched: NO new control-plane op; the two reads are DO-internal routes for the battery (E2/REC-17 surface them later).
explicitly NOT touched: bio-plane/src/index.mjs (REC-25's concurrent claim on read-op stamping and a backlink read — my promote/store/schema/checks paths are DISJOINT from index.mjs entirely), query.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md
expected: QUEUE.md REC-11 / MILESTONES M9. accepts-when: battery green with basis.test.mjs promoting an inquiry whose basis names one INFO- and one INQ-, reading both back in order, answering "which inquiries rest on this document" as ONE indexed lookup; self-citation and a three-node cycle refused at the closing write with the path named; a connection-axis grade on an INFO- target reading back with axis intact; a hunch leg refusing a missing author or date by name; test:hygiene green (table in purge); coverage --strict exit 0. Negative controls (a) cycle check removed -> A->B->A accepted, (b) projection write dropped -> two-leg bundle reads empty basis, (c) C-6.3 arm removed -> a basis leg absent from references[] passes — RUN, recorded in the suite's NEGATIVE CONTROL line, restored. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 0d94bff). Verified from the main checkout: battery 76/76 (3843), hygiene green, --strict exit 0, 110/110 ops. No DEC; D-168 untouched by design.

## CLAIM 2026-08-03 RECORD
session: rec13-agent (worktree agent-ae11aab331757a4ab)
opened: 2026-08-03T23:45:00Z
paths: bio-plane/checks/bio-checks.mjs (ONLY: STATES.inquiry gains `concluded` with its edges open/surfaced <-> concluded and concluded -> deferred|dismissed, and checkInquiryExtension gains the per-state ENTRY REQUIREMENTS modelled on C-2.7's verified arm — non-empty conclusion, non-empty falsifier, basis.length >= 1, all under C-2.8. NOT OBJECT_TYPES/HEADINGS/LEGACY_TYPE_ALIASES/vocabFor/normalizeType, NOT checkInquiryBasis, NOT the information/project/action machines), bio-plane/src/store.mjs (ONLY: a new conclude() method placed after release(), a #setOrAddScalar helper beside #setScalar, affordanceFacts reporting `declared_type` from the live bundle.md alongside the normalised object_type, and ONE `conclude:` line in the DO dispatch map — NOT dispose(), NOT release(), NOT promote, NOT the normalisation sites, NOT the basis projection), bio-plane/src/index.mjs (ONLY: one OPS entry for `conclude` beside release, one NEEDS entry `conclude: "contribute"`, `conclude` added to the STATE_ACTIONS array so the viewer/owner/author stamps and both SESSION_OPS lists carry it without a second list — NO fifth capability token, and the op=affordances handler itself is UNCHANGED, since it already hands the whole facts object to deriveActs), bio-plane/src/affordances.mjs (ONLY: the `conclude` ACT entry in ACTS, its RUNGS omission stated, and the edgesFrom seam corrected to consult the catalog's vocabFor with the DECLARED object_type — the MAP RULE at the fifth consulting site; NOT NON_ACTS' existing rows, NOT DISPOSITIONS, NOT VOCABULARIES' shape), bio-plane/test/conclude.test.mjs (NEW) + its test:conclude entry in bio-plane/package.json, bio-plane/test/affordances.test.mjs (the act-count and act-list assertions superseded by a seventh published act — corrected, never exempted, with the reason in a comment), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (no schema change — `conclusion`/`falsifier` are frontmatter, and REC-11's inquiry_basis is READ, never rewritten), the exported STATES table (imported by affordances.mjs and store.mjs, never copied — the ONE edge table)
interfaces owned: I3 additive (op=conclude, mutating, contribute). DISJOINT FROM REC-20 (op=queue, the tasks/proposals contract) by site: REC-20 holds the task/proposal surface; this claim touches the inquiry state machine, conclude() and the affordances act table only. Explicitly NOT touched: schema.mjs, query.mjs, setup.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-13 / BUILD-ORDER.md §2 (REC-13), carrying DEC-22 (an `open` inquiry may hold a claim with ZERO legs — legal, readable, never auto-anything; `concluded`'s entry requirements unchanged) and DEC-30 (no owner gate, no ballot — any contribute holder, act attributed). accepts-when: battery green with a suite concluding an inquiry through op=conclude and refusing BY NAME each of a missing conclusion, a missing falsifier, an empty basis and a machine credential; an `open` inquiry with zero legs legal and readable; op=affordances on a concluded inquiry publishing exactly the legal acts and publishing conclude on an open one; test:coverage --strict exit 0 reaching op=conclude from the control plane; NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — remove the falsifier requirement and an inquiry concludes with nothing that would falsify it, restored after. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 6f2a527). Verified from the main checkout: battery 77/77 (3894), --strict exit 0, 111/111 ops. The edgesFrom/declared_type cross-item correction to REC-19's landed ground reviewed and ACCEPTED — it is the DEC-8 rule enforced (a pre-flight may not disagree with the refusal it fronts). Reopen gap queued as REC-31. No DEC.
session: rec20-agent (worktree agent-aa4364d1bf953e827)
opened: 2026-08-03T23:45:00Z
paths: bio-plane/src/store.mjs (ONLY: a NEW queueFeed() read method + its two private helpers #queueAncestors (the bounded basis/refs ancestor walk) and #queueEventLive (DEC-16's event-state gate), the exported QUEUE_CLASSES / QUEUE_CLASSES_DEFERRED / QUEUE_ANCESTOR_DEPTH statics beside them, and ONE `queue:` line in the DO dispatch map — placed after captureProgressions, NOT the tasks/proposals producers themselves, NOT the normalisation sites, NOT the inquiry state machine or the state catalog, which are REC-13's concurrent claim), bio-plane/src/index.mjs (ONLY: one OPS entry `queue` beside proposals/captureprogressions, one NEEDS entry `queue: null` with its reason, `queue` joining the REC-25 D-15 viewer-stamp condition plus a server-side `member` stamp, the op=queue handler that decorates the store's derived options[] through the HOISTED decorateAct() — the op=affordances handler's own `decorate` moved to module scope and reused, never copied), bio-plane/src/affordances.mjs (ONE NON_ACTS entry `queue` with its reason — REC-19's totality over NEEDS), bio-plane/test/queue.test.mjs (NEW) + its test:queue entry in bio-plane/package.json, docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 (adds one non-mutating read op; no existing contract reshaped), REC-19's deriveActs/ACTS (IMPORTED into store.mjs for the options[] derivation — never a second copy of the act rules; affordances.mjs's ACTS/RUNGS/NEEDS/SESSION_OPS decoration stays at the control plane), query.mjs viewerPredicate (IMPORTED through the ONE D-15 compilation point; query.mjs NOT edited), REC-11's inquiry_basis + refs (READ by the ancestor walk; neither table nor its projection changed), REC-10's normalizeType/vocabFor (the MAP RULE — every type/state consultation in the new code goes through them)
interfaces owned: I3 additive (op=queue). I5 UNTOUCHED — NO new table: DEC-16's "the unit of state is the EVENT" needs no carrier, because tasks.status and proposal_dispositions are ALREADY keyed by the event's own identity and not by (member, case); schema.mjs and purge are therefore not edited.
explicitly NOT touched: bio-plane/checks/bio-checks.mjs (REC-13 holds the state catalog + the concluded entry requirements), store.mjs's conclude/dispose state machines and normalisation sites (REC-13/REC-10), the taskDrain/taskList/proposalsFeed producers themselves (read, never reshaped), REC-21's queue_state (mute/snooze is NOT built here), civicos-ui/** (UI-14 consumes this contract later), newgroup/**, schema.mjs, QUEUE.md, DECISIONS.md
expected: QUEUE.md REC-20 / BUILD-ORDER.md §2 (REC-20) re-based on DEC-16, MILESTONES M8. accepts-when: battery green with queue.test.mjs where op=queue returns an OBLIGATION from tasks and an aggregated FINDING from proposalsFeed in ONE feed, each carrying `class`, `options[]` byte-equal to op=affordances' own answer for the same subject and viewer, and `case` holding EVERY ancestor for a nested item; an over-depth ancestor walk reports the set `undetermined` naming the bound; resolving the event ONCE clears it from every entry; an uninvited member's queue names no invisible project ancestor and says `undetermined` rather than being silently shorter; an item with no ancestor sits UNGROUPED; CONDITION is absent and DECLARED deferred; test:coverage --strict exit 0 with op=queue reached through the control plane in the same turn; NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — key the clearing on (member, case) instead of on the event and the second ancestor shows a stale unresolved copy. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker b956b6d). Verified from the main checkout: battery 78/78 (3931), --strict exit 0, 112/112 ops. DEC-36 raised from the report's doctrine tension (with the shipped behaviour as provisional); the affordanceFacts residual + the reopen gap queued as REC-31. Both corrected-never-exempted seams reviewed and accepted. No new table — the event-keyed carriers already existed, recorded as the finding.

## CLAIM 2026-08-04 RECORD
session: rec12-agent (worktree agent-a0dc9df3a63d176ac)
opened: 2026-08-04T00:30:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) a NEW strengthOf() read method + its private helpers #strengthWalk / #axisResult / #weakestOf, placed after restingOn() and before purge(), reading REC-11's projection through basisFor() — the read seam — and INHERITING the exported Store.QUEUE_ANCESTOR_DEPTH for its depth bound rather than minting a second constant; (2) ONE `strength:` line in the DO dispatch map beside `basis:`/`restson:`; (3) the CORRECTED comment on #weakerGrade (RECONCILED §1.1 R1-m — the old comment named the two-axis substitution R2 forbids as a design INTENT), with #weakerGrade itself unchanged and deliberately unused by this item; (4) the per-axis projection CACHE: five entries in the existing bundles ALTER-loop (inquiry_capture_strength, inquiry_capture_state, inquiry_connection_strength, inquiry_connection_state, inquiry_basis_count) + two entries in the index loop, and one #writeStrengthProjection call inside promote's transaction right after the inquiry_basis projection loop — NOT PROJECTION_COLS, NOT the normalisation sites, NOT dispose/conclude/queueFeed, NOT promote's basis VALIDATION or its cycle guard, which are REC-11's landed ground), bio-plane/src/query.mjs (ONLY: three FIELDS entries — capture/connection/legs — over those cached columns; capture+connection joining DEFAULT_FACETS so "every inquiry at B or better on an axis" is an indexed query and not a scan; and ONE line in coerce() adding `upper`, the twin of the existing `lower` flag, because grades are stored A..D and a member typing `capture:b` must be answered rather than told nothing matched — NOT viewerPredicate, NOT PROVENANCE_COLS, NOT the tokenizer, the AST or the statement builders), bio-plane/test/strength.test.mjs (NEW) + its test:strength entry in bio-plane/package.json, bio-plane/test/basis.test.mjs (ONE assertion's INSTRUMENT corrected, never exempted: its "restingOn is ONE lookup" measure sliced the source from restingOn to /* Eviction — a REGION of the file, not a function — so any method added between them changed its answer while its subject was untouched; the slice now ends at restingOn's own closing brace, with the reason in a comment), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (REC-11's inquiry_basis, READ through basisFor(); the table is not reshaped), REC-20's exported Store.QUEUE_ANCESTOR_DEPTH (INHERITED, never re-declared — one depth bound, two walks), REC-10's normalizeType/vocabFor (the MAP RULE — every type consultation in the new code goes through the catalog), REC-25's viewer stamps (the suite's direct-DO gated reads carry viewer=class:member)
interfaces owned: I5 additive — five nullable projection columns through the existing ALTER loop; no new table, no schema.mjs edit, and therefore no purge arm to extend. I3 UNTOUCHED: NO new control-plane op. The derivation is a DO-internal route for the battery exactly as REC-11's basis/restson are; REC-14 (the ratified bytes), REC-17 and REC-22 give it its control-plane surfaces, so coverage --strict gains no unreached op.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/index.mjs, bio-plane/checks/bio-checks.mjs (checkInquiryBasis and the state catalog are REC-11's/REC-13's landed ground — this item derives from what they already refuse), civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
DISJOINT FROM REC-21 (queue_state, the personal mute table), running concurrently: REC-21 owns schema.mjs's NEW queue_state table with both purge arms, store.mjs's queueFeed()/mute paths and the CONDITION-kind filtering, and index.mjs's queue surface. This claim edits NONE of those — it adds no table and no schema.mjs line, does not open index.mjs at all, and its store.mjs sites are strengthOf/#strengthWalk/#axisResult/#weakestOf (new, after restingOn), the #weakerGrade COMMENT, the ALTER and index loops in #migrate, and one line each in promote's projection block and the dispatch map. The only shared file is store.mjs and the only shared symbol is Store.QUEUE_ANCESTOR_DEPTH, which this claim READS and never redefines.
expected: QUEUE.md REC-12 / RECONCILED.md §3.1 (REC-12), read with §1.1's amendment block and §1.2's, carrying DEC-21 (two measurements over two POPULATIONS — capture over every document reached, connection over every edge rested on; a leg IS an edge, so one document leg carries BOTH grades, and "evidentiary legs versus inferential legs" appears nowhere), DEC-18 (an ungraded leg is INERT — excluded from its axis's population, never weighed, never flooring, never unrating, and NAMED as not yet load-bearing, one or many; UNRATED is the boundary case where no leg on that axis is graded), D-160 (the word is UNRATED in the code, the copy and the test names; SUSPEND means the opposite in SB-OUTPUT §5.1 and appears nowhere), DEC-15 (a hunch grade composes NORMALLY, never as undetermined), R1 (#weakerGrade MUST NOT be reused — its `|| 0` ranks an unknown below D, below a member's signed testimony; the null is short-circuited BEFORE any rank comparison), R3 (the walk carries the depth bound; exhaustion reports `undetermined` naming the depth, R1's shape and not an error), and DEC-32's provisional (single-basis arithmetic — no grounds/plurality machinery).
accepts-when: battery green with strength.test.mjs showing (a) a mixed basis reading TWO strengths, each naming its own weakest leg, with no code path producing a single composed letter; (b) an inquiry whose leg is another inquiry inheriting that inquiry's pair PER AXIS; (c) one NULL-graded leg EXCLUDED from its axis's population while NAMED as not load-bearing, the axis still reading from its graded legs, and an all-ungraded basis reading UNRATED; (d) an over-depth chain reporting `undetermined` naming the depth, not throwing; test:coverage --strict exit 0. NEGATIVE CONTROLS, all five run and recorded in the suite's NEGATIVE CONTROL line and restored after each — force strongest-leg composition and the weak-link assertions fail; pass a null grade into the rank comparison and the suite fails because the chain reads below D; compose across the two axes and the suite fails naming the axes it mixed; remove the depth bound and a store-constructed cycle (REC-11 refuses them at write, so it is constructed directly in the store) does not terminate; rank an ungraded leg anywhere in the population and the suite fails. No push — CONDUCT integrates; landed: left for CONDUCT.
released: 2026-08-03 — landed on main by CONDUCT (merge of worker aa430f2). Verified: battery 79/79 at merge, 80/80 (4044) with REC-21, --strict exit 0. The INQ-capture-referent flag folded into REC-31 chore (3). No DEC.

## CLAIM 2026-08-03 RECORD
session: rec21-agent (worktree agent-afc83487dabb99520)
opened: 2026-08-04T00:30:00Z
paths: bio-plane/src/queuestate.mjs (NEW — REC-21's PERSONAL-half vocabulary and its ONE pure decision: QUEUE_CONDITION_KINDS transcribed from NOTIFICATIONS.md's catalogue, QUEUE_KIND_CLASS over every catalogued kind incl. the three LIVE producer spellings, classOfKind(), parseMutedKinds/serializeMutedKinds, and suppressedBy() — the admission decision queueFeed calls and the suite holds directly, the affordances.mjs/deriveActs precedent for a node-importable pure derivation), bio-plane/src/schema.mjs (ONLY: the queue_state table + its member index, placed BEFORE the host_governor block), bio-plane/src/store.mjs (ONLY: the queuestate.mjs import, THREE new methods queueMute/queueSnooze/#queueMutes placed after queueFeed inside REC-20's queue block, ONE admission call + a `mute` report inside queueFeed, TWO dispatch-map lines queuemute:/queuesnooze:, ONE new scheduler consumer `queue-renotify` registered in #schedConsumers, purge gaining queue_state in BOTH arms, and stats() gaining a queueState counter — NOT queueFeed's producers, NOT #queueAncestors/#queueEventLive, NOT the normalisation sites, NOT the basis projection or the read seams), bio-plane/src/index.mjs (ONLY: two OPS entries queuemute/queuesnooze beside queue, two NEEDS entries (null, with the doctrine reason), a new QUEUE_ACTIONS array spread into BOTH SESSION_OPS lists, the server-side member+viewer stamp for the two ops beside the proposedispose stamp — NEEDS itself not reshaped, no handler rewrite of op=queue), bio-plane/src/affordances.mjs (TWO NON_ACTS entries with their reasons — REC-19's totality over NEEDS), bio-plane/test/queue-state.test.mjs (NEW) + its test:queue-state entry in bio-plane/package.json, docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 (adds two mutating member ops; no existing contract reshaped — op=queue's answer gains an additive `mute` block and loses no key), query.mjs viewerPredicate (IMPORTED through the ONE D-15 compilation point; query.mjs NOT edited), REC-10's normalizeType/vocabFor (the MAP RULE: the case a mute names is resolved to a case TYPE through the catalog's own machinery, never a raw key), REC-20's queueFeed/#queueAncestors/#queueEventLive (READ and wired, never reshaped), REC-1's #schedConsumers registry (one consumer registered; the mechanism untouched)
interfaces owned: I5 additive (ONE new table, queue_state, in BOTH purge arms), I3 additive (op=queuemute, op=queuesnooze).
DISJOINT FROM REC-12 (the strength derivation, concurrent) BY SITE: REC-12 holds the BASIS READ SEAMS (basisFor/restingOn, inquiry_basis grade columns) and the PROJECTION COLUMNS (promote's projection, the strength/axis fields); this claim touches neither — its store.mjs sites are the REC-20 queue block (queueFeed's admission + two new write methods), the scheduler registry, purge and stats; its schema.mjs site is a NEW table appended before host_governor and no existing column; its index.mjs site is the OPS/NEEDS/SESSION_OPS table region and the stamp block. Neither item reads or writes the other's rows.
explicitly NOT touched: bio-plane/checks/bio-checks.mjs, query.mjs, the tasks/proposals producers, proposal_dispositions (the whole point: mute writes NOTHING there), civicos-ui/** (UI-14 renders the mute control later), newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-21 / RECONCILED.md §3.2 (REC-21) / BUILD-ORDER.md §2 (REC-21), carrying DEC-16's raised stakes and the change-is-itself-an-event rule. accepts-when: battery green with a suite where muting a case suppresses its PRESENT CONDITION kinds for that member only, a second member's feed is byte-unchanged, a kind NOT in the mute still surfaces, an OBLIGATION on a muted case still appears in that member's queue, no tasks row and no proposal_dispositions row and no bundle was written by mute or snooze, and a record-CHANGING resolution raises its own event that reaches every ancestor entry (including the muting member's); test:hygiene green (queue_state in purge, both arms); test:coverage --strict exit 0 reaching BOTH new ops from the control plane. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored — (a) route the mute through proposedispose and the suite fails, because a personal preference entered the record; (b) let muted_kinds accept an OBLIGATION kind and the suite fails, because a real obligation vanishes from the only surface that routes it. No push — CONDUCT integrates; landed: left for CONDUCT.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker eafffc3). Verified: battery 80/80 (4044), hygiene 54/54, --strict exit 0, 114/114 ops. NON_ACTS/NEEDS-null/no-probe classifications accepted with their reasoning; REC-32 queued for the first CONDITION generator. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec31-agent (worktree agent-aea0ae891377a3280)
opened: 2026-08-04T01:30:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) a NEW reopen() method placed after conclude() + ONE `reopen:` line in the DO dispatch map; (2) affordanceFacts' raw `b.object_type === "project"` consultation routed through normalizeType — chore (2), one line; (3) the COMMENT correction inside #strengthWalk's noReferent arm, pointing at chore (3)'s write-time refusal — the handling itself unchanged, because history can hold such rows. NOT conclude(), NOT dispose(), NOT promote's basis validation, NOT the normalisation sites, NOT queueFeed/strengthOf's logic), bio-plane/checks/bio-checks.mjs (ONLY: checkInquiryBasis gains the CAPTURE-AXIS-ON-AN-INQUIRY-LEG refusal arm under C-2.8 — DEC-21: capture ranges over documents. NOT STATES, NOT the inquiry machine, NOT checkInquiryExtension, NOT the `inherited` grade_source arm, NOT a new C-number), bio-plane/src/affordances.mjs (ONLY: the `reopen` ACT entry in ACTS with its RUNGS omission stated; NOT NON_ACTS' rows, NOT DISPOSITIONS' contents, NOT edgesFrom), bio-plane/src/index.mjs (ONLY: one OPS entry `reopen` beside conclude, one NEEDS entry `reopen: "contribute"`, and `reopen` added to the STATE_ACTIONS array so both SESSION_OPS lists and the viewer/owner/author stamps carry it without a second list), bio-plane/test/reopen.test.mjs (NEW) + its test:reopen entry in bio-plane/package.json, bio-plane/test/affordances.test.mjs (the act-count/act-list assertions superseded by an EIGHTH published act — corrected, never exempted, with the reason in a comment), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (no schema change — reopen writes frontmatter and history like conclude; REC-11's inquiry_basis untouched), the exported STATES table (the ONE edge table: reopen's legality is read through vocabFor over the DECLARED type, never a second edge source), REC-19's DISPOSITIONS (IMPORTED by both the store and the act — the one array naming what "set down" means), REC-10's normalizeType/vocabFor (the MAP RULE, chore (2)'s subject)
interfaces owned: I3 additive (op=reopen, mutating, contribute).
DISJOINT FROM REC-14 (published/editions), running concurrently: REC-14 owns the `published` state machinery, the edition fields, C-21.x, and checkInquiryBasis' `inherited` grade_source arm. This claim adds NO state to STATES and does not touch the concluded/published paths — deliberately, reopen is scoped to the DISPOSITION set (deferred|dismissed → open) and refuses a concluded inquiry BY NAME, because `concluded → open` is REC-14's edition machinery (DEC-12) and a conclusion revised without an edition is exactly what that item exists to prevent. The one SHARED FILE is bio-plane/checks/bio-checks.mjs and the shared function is checkInquiryBasis: this claim adds ONE arm (capture axis on an INQ- leg) and edits nothing else in it.
explicitly NOT touched: bio-plane/src/schema.mjs, query.mjs, setup.mjs, queuestate.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-31 (three chores) / MILESTONES M9. accepts-when: battery green with a suite where a deferred inquiry reopens through op=reopen with a named member and an AUTHORED reason, a machine credential is refused MACHINE_CANNOT_REOPEN, op=affordances publishes reopen on a deferred inquiry through the catalog's own edge table with no second edge source, a legacy-spelled project takes affordanceFacts' project arm (the fourth-name rehearsal: a temp module tree whose catalog carries one extra alias and whose promote normalisation is neutered — the inquiry.test.mjs boot-normaliser instrument), and a capture-axis grade on an INQ- leg is refused at promote AND named by the checker; test:coverage --strict exit 0 reaching op=reopen from the control plane. NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — remove the reason requirement and a reopen lands with no authored reason. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-03 — landed on main by CONDUCT (merge of worker af6228f). Verified: battery 81/81 (4103), --strict exit 0, 115/115 ops. The concluded→open gap is REC-14's to close (watched at its integration); the legacy-focus stance accepted as deliberate. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec29-agent (worktree agent-aff2a7112adf1335d)
opened: 2026-08-04T02:30:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) Store.memberList() gains an `{ administer }` parameter and the COVER↔HANDLE PROJECTION — `cover` is not SELECTED at all for a caller who does not administer, so the key is absent rather than null; (2) the `memberlist:` line in the DO dispatch map passes `url.searchParams.get("administer")` through; (3) the CORRECTED comment beside it, which said "All admin-only at the control plane" and was one of D-157's three self-contradicting sites. NOT memberAdd/memberSet/enroll/inviteLook, NOT #sessionRights, NOT memberCaps or the section-4 governance methods, NOT signerList — D-158's ground, untouched), bio-plane/src/index.mjs (ONLY: (1) the CORRECTED OPS-table comment at the membership block, which said "All admin-only" two lines under an entry granting admin+member+probe; (2) ONE new server-side stamp `administer` for op=memberlist in the DO passthrough, placed beside the D-15 viewer stamp and the REC-21 member stamp, set from `viaSession ? !!sessRights.administer : cls === "admin"`. The OPS entry's `classes` and `mutating` are UNCHANGED — the fix is a projection, not a refusal — so REC-19's totality guard is untouched and memberlist stays out of NEEDS/NON_ACTS), bio-plane/test/members.test.mjs (the section-3 suite, and the line-192 assertion CORRECTED never exempted, with the reason in a comment; two NEGATIVE CONTROL lines added at the head), bio-plane/test/membership.test.mjs (ONLY: the `list()` helper's direct-DO read now carries `administer=1`, the REC-25 viewer-stamp precedent for a gated direct-DO read, with the reason in a comment), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (the `members` table is READ, never reshaped — no schema.mjs edit, no new table, no purge arm), REC-25's server-side stamp discipline (the new `administer` stamp is set AFTER the caller's parameters are copied, so a caller-supplied value is overwritten rather than honoured — asserted directly)
interfaces owned: I3 — op=memberlist's answer becomes VIEWER-DEPENDENT: `cover` is present for an administrator (root ADMIN_TOKEN, root-admin session, or an in-app administrator's session) exactly as before, and ABSENT for every other caller. NOT additive: a field disappears for a class of callers, so by IC-3's own standard ("a renamed wire string is a break by definition") this is a BREAK and should be recorded as a major bump rather than dressed as additive. MEASURED consumer impact NIL: both live consumers coalesce (`civicos-ui/app.html` rosterRow `m.cover || m.name || ""`, `bio-plane/src/setup.mjs` `x.cover||""`), and setup.mjs signs in as the administrator so its view does not change at all. The registry entry (INTERFACES.md I3 version + an IC row) is left to CONDUCT, which has landed that bookkeeping for every REC item to date (c74c6a3, IC-3/I3 2.0.0) — flagged in the report rather than written here.
DISJOINT FROM REC-14 (published/editions), running concurrently: REC-14 owns the `published` state machinery, the edition fields, C-21.x, checkInquiryBasis' `inherited` grade_source arm and the ratify/checkInquiryExtension ground. This claim touches NONE of it — its subject is the membership roster, and its store.mjs sites are memberList() plus one dispatch-map line, both far from the publication paths; it opens neither bio-checks.mjs nor query.mjs nor schema.mjs, and its index.mjs sites are the membership OPS comment and one stamp line in the passthrough. No shared file with REC-14 carries a shared symbol.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/affordances.mjs, bio-plane/src/setup.mjs, civicos-ui/** (the UI degrades correctly with no edit — measured, not assumed), newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-29 / DEBT D-157 / MILESTONES M7, carrying BIO_Membership_Architecture v1 §3 and v2 §3 (identical: "Pairing. Only administrators see cover and handle together"; "Members and the public see handles"). accepts-when: battery green with a suite where an admin receives cover and handle together while an ordinary member session (administer:false) and the shared MEMBER_TOKEN receive handles with NO `cover` field present at all; test:coverage --strict exit 0; NEGATIVE CONTROL run and recorded — restore the old projection and the suite fails naming the non-admin callers that received cover, restored after.
released:

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 6213fc1). Verified: battery 81/81 (4119), --strict exit 0, 115/115 ops. NC RE-RUN BY CONDUCT (6 named failures under the old projection; restored byte-identical). IC-4 recorded, I3 → 3.0.0; D-157 closed; the dataplane state record amended in the D-160 pattern. The caller-controlled-param lesson carried into REC-30's scope by reference. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec14-agent (worktree agent-a0d577c0782f06de6)
opened: 2026-08-04T02:00:00Z
paths: bio-plane/src/schema.mjs (ONLY: published_bundles RE-KEYED (bundle_id, edition) per DEC-12 with the frozen columns beside it — title, completeness, strength, required, manifest_sha, manifest; TWO new tables `inquiry_exclusions` and `group_strength_bar`, both placed BEFORE the host_governor block — NOT the capture/link/task/reachability tables, NOT queue_state, NOT inquiry_basis), bio-plane/src/store.mjs (ONLY: (1) #migrate gains the published_bundles RE-KEY — a rename before the schema loop and a copy-forward-as-edition-1 after it, the one non-derived table that may not be dropped; (2) a NEW publishCase() state act placed after conclude() with its #setOrAddBlock/#removeBlock frontmatter helpers, (3) a NEW strengthBarSet()/strengthBarOf()/#requiredStrengthFor() trio, (4) publish() — the ratify committer — made edition-aware (APPENDS, refuses EDITION_NOT_INCREMENTED, stores the frozen completeness/strength/bar/manifest), (5) publishedList() enumerating editions, (6) gateFacts() returning the publishedRegistry for the gated bundle and its basis targets, (7) promote's projection block gaining the inquiry_exclusions delete-then-insert beside inquiry_basis and passing the registry into checkInquiryBasis, (8) purge gaining inquiry_exclusions in BOTH arms, (9) four dispatch-map lines: publishcase, strengthbar, strengthbarof, publishededitions — NOT conclude/dispose/release, NOT the strength derivation, NOT the normalisation sites, NOT queueFeed), bio-plane/checks/bio-checks.mjs (ONLY: STATES.inquiry gains `published` with the edges concluded -> published and published -> open|surfaced; HEADINGS gains the state-conditional `## What This Excludes` for inquiry; checkInquiryExtension gains the `published` ENTRY REQUIREMENTS under C-2.8 and the NEW C-21.1 completeness-freshness gate; checkInquiryBasis gains 'inherited' in GRADE_SOURCES and the NEW C-21.2 per-axis inheritance arm behind an injected publishedRegistry — MY EDIT IS THE INHERITED ARM ONLY; REC-31 concurrently owns the capture-axis-on-an-INQ-leg refusal arm in the SAME function and the two are disjoint by clause; new exported SUBJECT_POSITIONS; checkBundle's ctx gains publishedRegistry on the releaseRegistry precedent), bio-plane/src/gate.mjs (ONLY: runGate threads publishedRegistry through to checkBundle), bio-plane/src/index.mjs (ONLY: two OPS entries `publish` and `strengthbar` plus the reads `strengthbarof`/`publishededitions`, their NEEDS entries, `publish` joining STATE_ACTIONS, one DO_PATH line publish -> publishcase since the store's own /publish is the ratify committer, and the ratify handler passing facts.publishedRegistry into runGate and writing DEC-34's signed hash MANIFEST into R2 + published_shas — NOT op=ratify's contract {bundleId, expectedSha, sig}, which is UNCHANGED), bio-plane/src/affordances.mjs (ONLY: the `publish` ACT entry in ACTS, three NON_ACTS entries with their reasons, and subject_positions in VOCABULARIES — REC-31 concurrently owns the `reopen` ACT entry in the same array and the two entries are disjoint), bio-plane/test/publish.test.mjs (NEW) + its test:publish entry in bio-plane/package.json, bio-plane/test/hygiene.test.mjs (ONE EXEMPT row for group_strength_bar with its reason — the governance-declaration class, beside members/signers), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (REC-11's inquiry_basis and REC-12's strengthOf(), READ — neither reshaped), REC-13's conclude() and its `concluded` entry requirements (published is reachable ONLY from concluded), REC-10's normalizeType/vocabFor (the MAP RULE at every state and type consultation), REC-25's viewer stamps (publishCase takes the fail-closed viewer gate; the suite's direct-DO gated reads carry viewer=class:member), REC-19's ACTS/NON_ACTS totality over NEEDS
interfaces owned: I5 (published_bundles RE-KEYED — DEC-12; two new tables, inquiry_exclusions in BOTH purge arms), I3 additive (op=publish, op=strengthbar, op=strengthbarof, op=publishededitions; op=ratify UNCHANGED)
DISJOINT FROM REC-31 (the reopen act + two one-line residuals), running concurrently: REC-31 owns op=reopen (a new store method + its op/NEEDS/dispatch lines), the affordanceFacts raw-key MAP RULE line in store.mjs, and the capture-axis-on-an-INQ-leg refusal arm in checkInquiryBasis. This claim touches none of those three sites. The two SHARED FILES are checks/bio-checks.mjs (their arm is the capture-referent refusal; mine is the 'inherited' arm and the vocabulary constant) and src/affordances.mjs (their entry is the `reopen` ACT; mine is the `publish` ACT and three NON_ACTS rows). NOTE FOR CONDUCT AT INTEGRATION: this item makes `published -> open` a legal edge in the catalog, so REC-31's reopen must take its legality from the catalog's edge table rather than a literal deferred|dismissed list, or reopening a published case is refused by a list that has not heard of DEC-12.
explicitly NOT touched: bio-plane/src/query.mjs, bio-plane/src/queuestate.mjs, setup.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-14 / RECONCILED.md §3.1 (REC-14) / BUILD-ORDER.md §2 (REC-14), re-based on FOUR rulings: DEC-12 (published is NOT terminal; `edition` is required frontmatter stamped into the ratified bytes; published_bundles re-keyed and APPENDING, which closes D-144 as a feature; every edition keeps its own signature, attestor, time and gate version; reopening does NOT unpublish; a citing leg NAMES ITS EDITION and does not silently follow), DEC-13 (the completeness block carries the group's POSITION ON PUTTING THE CASE TO ITS SUBJECT — authored, justified, never prefilled; the gate is that the position is DECLARED AND JUSTIFIED, never that contact happened and never that the answer was favourable), DEC-17 as amended (the GROUP declares the default required_strength PAIR, a project may override, the bar is stamped BESIDE the derived strengths, and an absent bar gates nothing and SAYS so), DEC-34 (the published case is a CONTAINER — a signed hash manifest over every part, editions over the container; the PDF renderings and the per-page brazening are REC-22/UI-18's half, not this one), plus C-9's nameable exclusion rows (target_id OR prose, never neither; indexed on target_id) and R4's reserved division disclosure.
accepts-when: cd bio-plane && npm run test:battery green publishing a concluded inquiry at edition 1, republishing at edition 2 with BOTH editions readable and separately signed, refusing a republish that does not increment edition, refusing byte-identical completeness across editions (C-21.1), refusing per-axis over-strong inheritance (C-21.2, BOTH axis probes independently); a published inquiry reopens to open WITHOUT unpublishing; the declared bar stamped beside the derived pair and an absent bar stated as absent; the subject position declared-and-justified gate refusing an absent position and never checking contact; hygiene green (inquiry_exclusions in purge, group_strength_bar exempt with its reason); coverage --strict exit 0 with every new op reached from the control plane. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored after each — C-21.1 passes a carried-forward completeness and the gate is a checkbox; a single scalar C-21.2 comparison passes both axis probes and the axes were composed; upsert published_bundles and edition 1's signature is gone. No push — CONDUCT integrates; landed: left for CONDUCT.
amended 2026-08-04 (rec14-agent, same session), all found by the battery rather than by review: + bio-plane/src/gate.mjs (CATALOG_VERSION 1.18.0 -> 1.19.0 — the catalog gained two checks, and the ratify suite's exact-version assertion is corrected with it rather than loosened to a pattern, which is the point of that assertion), + ONE more read op `excludedby` (P8's "which published cases excluded this document" — inquiry_exclusions exists to make that query askable and without a read surface the index proves nothing; viewer-gated on REC-25's rule), + bio-plane/test/ratify.test.mjs (the published-parts count 3 -> 4: DEC-34's MANIFEST is now a part of every ratification; CORRECTED, never exempted, with the reason), + bio-plane/test/conclude.test.mjs and bio-plane/test/affordances.test.mjs (three superseded assertions corrected with their reasons: the concluded-state edge set gains `published`, a concluded inquiry now publishes `publish` beside `dispose`, the act catalogue is EIGHT acts, and "every act needs contribute" becomes "each act carries the capability its own NEEDS entry names" — publish rides the publication surface, not the contribute one), + bio-plane/package.json (test:publish), + bio-plane/test/hygiene.test.mjs (the group_strength_bar exemption).
CROSS-ITEM CORRECTION to REC-13's landed ground, found by this item's gate and fixed here because it is a WRITE-PATH BUG rather than a difference of opinion: store.mjs's `#setOrAddScalar` decided whether a key was present by asking whether the TEXT CHANGED, so writing a key its EXISTING value appended a SECOND copy and produced a document carrying a duplicate top-level key (C-2.1). It could not fire until DEC-12 made a case reopen, be concluded AGAIN with the same falsifier, and republish — which is exactly what this item does. Presence is now decided by looking. The comment names the old reasoning and why it was wrong.
merged 2026-08-04 (rec14-agent) — origin/main 98a0793 into the REC-14 branch, resolved by the worker at CONDUCT's request because git had interleaved reopen()'s body with publishCase()'s. Both functions restored WHOLE from their own sides; STATE_ACTIONS is the union ["dispose","retire","release","conclude","reopen","publish"] plus DECLARATION_ACTIONS; both acts registered; REC-29's memberList projection and administer stamp untouched; both test scripts kept; this file append-only. TWO SEAM DECISIONS, both mine and both pinned by the suites:
  (1) **A PUBLISHED CASE REOPENS THROUGH op=reopen — REC-31's act, not a second one.** The FROM set moves from DISPOSITIONS to a new one-place array REOPENABLE_FROM = [...DISPOSITIONS, "published"], read by BOTH the store's refusal and the published act so they cannot disagree. `concluded` stays refused BY NAME, unchanged, for REC-31's own reason: a conclusion reverting to open still wearing its conclusion records NOTHING, and op=publish is where that move belongs. `published` is the opposite case and that is the whole distinction — its editions are ratified, signed and immutable, DEC-12 rules that reopening does not unpublish, so there is nothing to erase; and published -> open is the ONLY route to a second edition, which makes reopen the front door of a revision rather than a way around one. Refusing it would have left a legal edge no caller could travel, which is the argument REC-31 was built on. ONE verb, not two: a second control meaning "pick this back up" is exactly the drift the act catalogue exists to prevent.
  (2) **AN INHERITED CAPTURE GRADE ON AN INQ- LEG IS ADMITTED; every AUTHORED one is still refused.** REC-31's DEC-21 referent arm was written when every grade_source was authored (resolution/testimony/hunch) and is unconditional for all three. `inherited` did not exist then: it is not a claim about the inquiry, it is the capture axis THAT CASE FROZE over its own documents when the group signed the cited edition — which ranges over documents exactly as DEC-21 requires — and C-21.2 refuses it if it is stronger than the frozen value. It is carried on the leg rather than re-derived because a leg citing edition 1 must not silently follow edition 2 (DEC-12). The arm narrows by that one case, with the reasoning in place; REC-31's own assertion (an authored capture grade on an INQ- leg, refused at both gates) is untouched and still passes.
  Superseded assertions CORRECTED, never exempted, each with a dated reason: the act catalogue is NINE acts (reopen + publish); a concluded inquiry publishes ["dispose","publish"]; NOT_SET_DOWN names `reopenable` (REOPENABLE_FROM) instead of `dispositions`; the reopen act's structural pin reads REOPENABLE_FROM; and REC-14's own "authored grade on a published case" probe now expects BOTH findings (C-2.8 and C-21.2), which is the two items' gates composing rather than a defect. publish.test.mjs now drives the reopen through op=reopen instead of hand-writing the state move, which is what pins decision (1).
  Verified after the merge: battery 82/82 (4199), hygiene 2/2 (436), coverage --strict exit 0 with 120/120 ops reached through the control plane, 53/53 checks named, 82/82 controls declared. BOTH items' controls RE-RUN against the merged tree: REC-14's C-21.1 neutered in both gates -> 69 pass, 5 FAIL (a carried-forward completeness publishes and the catalog finds nothing wrong); REC-31's reason gate neutered -> 53 pass, 4 FAIL (op=reopen accepts reason="" and the deferred inquiry is open with nothing accounting for it). Both restored.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 56e5427, the worker-resolved merge). Verified from the main checkout: battery 82/82 (4199), hygiene 436, --strict exit 0, 120/120 ops. The REOPENABLE_FROM unification and the inherited-arm narrowing reviewed and accepted; the C-21.1 position-term coverage question flagged on the landed line for Bob's eventual read. No DEC raised — the provisional (term not covered) is one line to tighten.

## CLAIM 2026-08-04 RECORD
session: rec30-agent (worktree agent-a4dac86c5881096be)
opened: 2026-08-04T03:30:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) TWO new private helpers beside #viewerSees — #bundleGate (the D-15 predicate as a WHERE term over a QUALIFIED column holding a bundle id) and #bundleRedactor (the same question per id, in JS, for answers assembled outside SQL); (2) the reads they gate: danglingRefs, taskList, queueFeed's OBLIGATION loop and FINDING subject query, readInstance + the new #redactInstance projection (applied also to threadInstance's and dischargeStage's echoed instance), readExceptions, readingFor, documentsByReference, resolutionsForCapture, documentsConcerning, connectionsFor, auditPass, searchIndexCheck, projectOwnerArithmetic, and #resolutionView's optional `keep` parameter; (3) the matching `viewer:` pass-throughs in the DO dispatch map for those ops; (4) the DO envelope's `ms` field REMOVED (`ok:true, ms:…` → `ok:true`) and the `const t = Date.now()` it fed. NOT promote, NOT the gate/ratify path, NOT listBundles/buildIndex/backlinks/#viewerSees themselves (REC-25's, unchanged), NOT memberList (REC-29's), NOT publishedList/publishedManifest/verifySha — deliberately ungated, recorded with reasons in the suite), bio-plane/src/index.mjs (ONLY: the REC30_VIEWER_READS array and its clause added to the ONE existing viewer-stamp condition in the DO passthrough. No OPS entry added, changed or removed; no class list touched), bio-plane/test/gate-reads.test.mjs (the posture suite, EXTENDED: the REC-30 sweep section, the classification table asserted structurally, the server-stamped-param audit, the `ms` assertions, and this suite's own client-side `delete body.ms` CORRECTED away with the reason), bio-plane/test/inbox.test.mjs + bio-plane/test/task-fence.test.mjs + bio-plane/test/task-drain-alarm.test.mjs + bio-plane/test/search.test.mjs (ONLY their direct-DO read helpers, which now carry `viewer=class:member` with the REC-25 stamp precedent and the reason in a comment — the fail-closed change surfaced them loudly, which is the posture working), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (no schema change — every table is READ, none reshaped, none added, so no purge arm), query.mjs's viewerPredicate (the ONE compilation point: both new helpers take their predicate, their machine carve-out and their fail-closed deny from it and restate none of them), REC-25's server-side stamp discipline, REC-29's caller-controlled-param lesson (audited here for every gate-like param, not only `administer`)
interfaces owned: I3 — the ANSWERS of fourteen existing read ops become viewer-dependent. Additive for a machine credential (byte-identical: the carve-out returns 1=1 and nothing is filtered or withheld) and for an administrator or a participant. A BREAK only for a member session reading about a project it was never invited to, which is the defect being closed. TWO shapes: a withheld ROW (op=dangling, op=tasks, op=queue's obligations) and a withheld BACK-REFERENCE (`bundle_id: null` on op=reading/readingref/resolutions/concerns/connections/instance/exceptions). MEASURED consumer impact: no `bundle_id` back-reference from these ops is read by civicos-ui or setup.mjs. The registry entry (INTERFACES.md I3 version + an IC row) is left to CONDUCT, as with REC-29.
DISJOINT FROM REC-14 (published/editions), running concurrently: REC-14 owns the `published` state machinery, the edition fields, C-21.x, checkInquiryBasis and the ratify/publication ground. This claim opens NEITHER bio-checks.mjs NOR schema.mjs NOR affordances.mjs NOR query.mjs, and it deliberately leaves the published projection UNGATED — publishedList, publishedManifest and verifySha are credential-free BY DESIGN and are recorded as such in the suite's classification table, so this sweep does not pre-gate REC-22's class or REC-14's ground. The one place the two items meet is the suite's structural assertion that EVERY read op in the OPS table is classified: read ops REC-14 adds will be unclassified and will FAIL gate-reads.test.mjs until CONDUCT classifies them gated or ungated-with-a-reason at REC-14's integration. That is the mechanism working as intended and is flagged in the report rather than softened.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/affordances.mjs, bio-plane/src/setup.mjs, bio-plane/src/queuestate.mjs, civicos-ui/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-30 / MILESTONES M7, carrying REC-25's landed residual (op=dangling, tasks?refersTo, the progression reads, the `ms` envelope) and REC-29's caller-controlled-param lesson. accepts-when: battery green with a suite where an uninvited member gets no project identity from ANY read op including op=dangling, each deliberately-ungated op listed in the suite with its stated reason, no control-plane response carrying `ms`, and every gate-like store param asserted server-stamped; test:coverage --strict exit 0. NEGATIVE CONTROL run in THREE arms and recorded in the suite's NEGATIVE CONTROL line — (a) restore the dangling leak and the suite names op=dangling, (b) drop the stamp and every answer is EMPTY while a forged caller-supplied viewer is honoured, (c) neuter the back-reference projection and seven ops hand over the project's id; all restored. No push — CONDUCT integrates.
merged 2026-08-04 (rec30-agent, at CONDUCT's request) — origin/main b6e957c (REC-14 published/editions + its own merge of REC-31) into this branch. Conflicts in bio-plane/src/index.mjs (the viewer-stamp condition: REC-14 had added op=excludedby to the same line — BOTH kept, union) and docs/development/CLAIMS.md (two appends at one anchor — both kept). store.mjs auto-merged; publishCase, publishedEditions, excludedBy and reopen are landed functions and are NOT reshaped — the gates compose around them. THREE NEW READ OPS CLASSIFIED, which the suite's structural assertion named at the merge and which are this item's judgments to make: (1) op=strengthbarof GATED — #requiredStrengthFor reports `projects: [...]` and interpolates the same ids into its detail, which is §7.9's reverse-edge walk (op=backlinks' own leak) arriving by a new door; the bar VALUE is deliberately NOT gated, because DEC-17's stake is that it is never set by who a reader is and op=publish stamps it from #requiredStrengthFor directly whatever this read shows, so only the NAMES are withheld and the withholding is stated without a count (#queueAncestors' posture). Residual named rather than papered over: `source: "project"` still says SOME project declares a bar. (2) op=excludedby ALREADY GATED by REC-14, correctly and at the one compilation point; it is NOT the published-projection class because it joins live `bundles` and reports current_state, so it can name an UNPUBLISHED case — asserted structurally, because no fixture here can make its gate bite (exclusions are written by op=publish, publishable cases are inquiries, and inquiries are shared corpus; an empty answer would be an outcome that costs nothing). (3) op=publishededitions DELIBERATELY UNGATED — published_bundles only, no join, no current_state: REC-22's credential-free class. Verified on the merged tree: battery 82/82 (4273), hygiene 2/2 (436), coverage --strict exit 0 with 120/120 ops. FOUR NC arms re-run and restored, the fourth new for REC-14's read.
released:

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 9c5b30b, the worker-resolved merge). Verified: battery 82/82 (4273), --strict exit 0, 120/120 ops. NC arms (a) and (d) RE-RUN BY CONDUCT, restored byte-identical. IC-5 recorded, I3 → 4.0.0. The DEC-17 bar-value residual decision accepted and recorded on the landed line. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec16-agent (worktree agent-aceac1b6a301edea5)
opened: 2026-08-04T04:00:00Z
paths: bio-plane/checks/bio-checks.mjs (ONLY: (1) STATES.inquiry gains `divided` — legal, TERMINAL (`divided: []`), entered from open|surfaced|concluded and NOT from published (DEC-28); (2) checkInquiryExtension gains the `divided` ENTRY REQUIREMENTS under C-2.8 — the `division` map {into, apportioned_by, at, reason} with >=2 into ids and the `division_apportionment` array that records where EVERY leg went, every ord covered and every child receiving at least one; (3) TWO new exported pure functions `supersedesEdgeFindings` and `divisionDisclosureFindings` under C-6.1, consulted by checkReferences AND by the store's promote write path — the checkInquiryBasis precedent; (4) checkReferences calls both. NOT checkInquiryBasis, NOT the published arm/C-21.x, NOT HEADINGS, NOT REL_VOCAB's contents), bio-plane/src/store.mjs (ONLY: (1) a NEW divide() state act placed after publishCase() + ONE `inquirydivide:` line in the DO dispatch map; (2) promote gains ONE new validation arm after the basis arm — the supersession edge check (SUPERSESSION_REFUSED, incl. an unresolvable supersedes target) and the RESOLVED sibling-set check against the parent's own division.into (NO_SIBLING_DISCLOSURE); (3) affordanceFacts gains `basis_legs` as a fact. NOT conclude/reopen/publishCase's bodies, NOT the strength derivation, NOT the projections, NOT queueFeed), bio-plane/src/affordances.mjs (ONLY: the `inquirydivide` ACT entry with its DEC-29 PROMPT, the exported DIVIDE_PROMPT string, and `prompt` on the act shape; NOT NON_ACTS' rows, NOT REOPENABLE_FROM/DISPOSITIONS, NOT edgesFrom), bio-plane/src/index.mjs (ONLY: one OPS entry `inquirydivide`, one NEEDS entry `inquirydivide: "contribute"` (DEC-30, author-scoped), `inquirydivide` added to STATE_ACTIONS so both SESSION_OPS lists and the viewer/author stamps carry it without a second list, and `prompt` added to decorateAct), bio-plane/test/divide.test.mjs (NEW) + its test:divide entry in bio-plane/package.json, bio-plane/test/conclude.test.mjs + bio-plane/test/affordances.test.mjs (act-list and act-count assertions superseded by a TENTH published act — corrected, never exempted, with dated reasons), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (NO SCHEMA CHANGE and no new table — decision D5: the division is authored in bundle.md and the child's `rel: supersedes` reaches `refs` through the EXISTING projection), the exported STATES table (the ONE edge table, read through vocabFor over the DECLARED type — the MAP RULE), REC-13's conclude(), REC-14's publishCase() and its RESERVED division_parent/division_siblings frontmatter (this item is their producer), REC-25's viewer stamps, REC-19's ACTS/NON_ACTS totality over NEEDS
interfaces owned: I3 additive (op=inquirydivide, mutating, contribute; op=affordances' act shape gains `prompt`, additive).
DISJOINT FROM REC-30 (the read-surface sweep), running concurrently: REC-30's subject is READ-OP GATING. This claim adds no read op and changes no read path's gate; its store.mjs sites are a new write act, one promote validation arm and one extra FACT on affordanceFacts, and its index.mjs sites are the OPS/NEEDS/STATE_ACTIONS table region plus decorateAct. Neither item touches the other's lines.
explicitly NOT touched: bio-plane/src/schema.mjs, query.mjs, queuestate.mjs, setup.mjs, gate.mjs, civicos-ui/**, newgroup/**, QUEUE.md, DECISIONS.md, MILESTONES.md
expected: QUEUE.md REC-16 / RECONCILED.md §3.1 (REC-16) / BUILD-ORDER.md §2 (REC-16), carrying DEC-28 (`divided` is a TERMINAL STATE written by op=inquirydivide; its reason is the ACT's reason; disposition_reason untouched), DEC-29 (ONE authored reason per division — no per-leg reason — and the divide PROMPT's wording must STATE the disclosure the division will make), DEC-30 (author-scoped: any `contribute` holder, act attributed), and R4's disclosure, which is the item's point: each child records its PARENT id AND its SIBLING ids, the parent records where EVERY leg went including every `cuts_against` leg.
accepts-when: cd bio-plane && npm run test:battery green with a suite dividing a CONCLUDED inquiry into two children, each naming its parent AND every sibling, each carrying a `supersedes` edge back with a reason, the parent terminal and recording where every leg went including every `cuts_against` leg; a `published` inquiry refuses PUBLISHED_CANNOT_DIVIDE; the divide surface/prompt text STATES the disclosure (asserted as a string); npm run test:coverage --strict exit 0 reaching op=inquirydivide from the control plane. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored after each — (a) remove the sibling-set requirement and a child that omits a sibling lands and publishes; (b) remove the C-6.1 supersedes requirement and a child supersedes its parent with no reason and an unresolvable target. No push — CONDUCT integrates; landed: left for CONDUCT.
released:
amended 2026-08-04 (rec16-agent, same session), all found by the battery and by the negative controls rather than by review: + bio-plane/test/reopen.test.mjs (two superseded act-list pins corrected with dated reasons — an open inquiry that rests on something, and a concluded one, both publish `inquirydivide`; what those assertions exist for is unchanged, `reopen` is still not among them), + bio-plane/test/conclude.test.mjs (three: the concluded edge set gains `divided`, and the open/concluded act lists gain `inquirydivide` — INQ_STANDING's zero-leg list is UNCHANGED and that pair now does more work than either did alone, because it is what shows the act's basis-count condition is real), + bio-plane/test/affordances.test.mjs (the catalogue is TEN acts and the act shape gains `prompt`; the "all eight" NEEDS label was stale from the REC-14 wave and is corrected to ten; one new assertion that exactly one act publishes a prompt and it is DIVIDE_PROMPT).
FIVE REFUSAL NAMES BEYOND THE FOUR THE ITEM NAMED, each on an established precedent and none of them a new class: `BAD_CHILD_ID` / `CHILD_EXISTS` / `NO_CHILD_QUESTION` / `BAD_APPORTIONMENT` (caller-shape refusals, op=publish's BAD_EXCLUSION precedent — every one fires before anything moves), and `SUPERSESSION_REFUSED` (the BASIS_REFUSED / GATHERING_REFUSED shape: the catalog's own function judges, the store reports its findings verbatim). `NO_SIBLING_DISCLOSURE` is the item's own name and carries BOTH halves of R4's disclosure — the document-local shape and the resolved sibling set. Plus `CHILD_REFUSED`, which is the pre-flight: every child document is judged against the catalog BEFORE the parent moves, because the parent must be written first (a child's disclosure is checked against the parent's own division.into) and a child refused at its own write would otherwise leave a terminal parent naming a question nobody can open.
INSTRUMENT FINDING, recorded because it changed the suite rather than only the code: the second negative control was RUN TWICE. Probed through a division CHILD, removing the C-6.1 supersedes requirement still refused both documents — by the DISCLOSURE arm, which fires on any inquiry-to-inquiry supersedes edge that declares no division. The control failed 3 assertions and every one named the wrong rule. Block 7's isolating probes are INFORMATION bundles for that reason: the disclosure arm returns early where there is no division to disclose, so what is left under test is the edge requirement alone. The boundary between the two rules is now asserted directly rather than left to be rediscovered.

released: 2026-08-03 — landed on main by CONDUCT (merge of worker 893401a). Verified: battery 83/83 (4358), --strict exit 0, 121/121 ops. IC-6 recorded, I3 → 4.1.0. The ordinal-keying, two-key split, parent-first ordering and inquiry→inquiry disclosure scoping accepted as recorded mechanism. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec28-agent (worktree agent-aa74b6e0754dd0d60)
opened: 2026-08-04T05:00:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) the act-level machine refusals at the TOP of taskForward() and taskResolve() — MACHINE_CANNOT_FORWARD / MACHINE_CANNOT_RESOLVE on the MACHINE_CANNOT_RELEASE/CONCLUDE/REOPEN precedent, checked before the row is read so the refusal cannot depend on assignment state; (2) the COMMENT on #refuseNotYours, corrected where it states a guarantee the code does not make (D-151). The fence's LOGIC is unchanged — the unassigned arm stays, deliberately. NOT taskDrain, NOT #routeTask, NOT #taskOf/#refuseUngrammatical, NOT taskList, NOT any other refusal in the file), bio-plane/src/index.mjs (ONLY: `"probe"` removed from the `classes` of the two OPS entries taskforward/taskresolve and their comment stating why the class list alone cannot be the fence; plus the COMMENT on the actor-stamp passthrough, which carries D-151's exact flagged phrase "a daemon cannot close somebody's work" as though the NO_ACTOR refusal implied it — corrected, the stamp logic itself untouched. NOT taskdrain's OPS entry, NOT NEEDS, NOT SESSION_OPS, NOT TASK_ACTIONS), bio-plane/test/task-machine.test.mjs (NEW) + its test:task-machine entry in bio-plane/package.json, bio-plane/test/task-fence.test.mjs (ONLY: the head-comment/section note on the unassigned-claimable phase, which now says WHICH callers it holds for — corrected with the date and the reason, never exempted), docs/development/CLAIMS.md (this entry)
interfaces consumed: none (I5 unchanged — no table read or reshaped)
interfaces owned: I3 — BREAKING on two ops and deliberately so. (a) `op=taskforward`/`op=taskresolve` no longer admit the PROBE class at all (`forbidden for token class`, 403). (b) A MEMBER_TOKEN or ADMIN_TOKEN machine credential still REACHES both ops and is now refused by the store BY NAME with two NEW reasons, `MACHINE_CANNOT_FORWARD` and `MACHINE_CANNOT_RESOLVE` — the release/conclude/reopen shape. No SESSION is affected: a member or admin session's actor is a member id, never `token:<class>`, so every caller the Tasks screen has is byte-identical. MEASURED consumer impact: `civicos-ui/app.html` drives both verbs from a signed-in session only, and `setup.mjs` calls neither. The registry entry (INTERFACES.md I3 version + an IC row) is left to CONDUCT, as with REC-29 and REC-30.
DISJOINT FROM REC-16 (division), running concurrently on the STATES/divide ground: this claim opens NEITHER schema.mjs NOR bio-checks.mjs NOR affordances.mjs NOR query.mjs, touches no state machine and no catalog table, and in store.mjs is confined to the two task verbs and one comment — none of which REC-16's divide path reaches.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/affordances.mjs, bio-plane/src/setup.mjs, bio-plane/src/queuestate.mjs, civicos-ui/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-28 / MILESTONES M8, closing D-151 from DEC-7's ruling. accepts-when: battery green with a suite where a `token:probe` credential is refused BY NAME on both verbs for an UNASSIGNED task, the assignee still succeeds and an admin member still succeeds; test:coverage --strict exit 0. NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — remove the act-level refusal and the probe resolves an unassigned task while every existing task assertion still passes, which is the state today; restored after running. No push — CONDUCT integrates.
merged 2026-08-04 (rec28-agent) — origin/main ea1a917 (REC-16 divided/op=inquirydivide + its bookkeeping) into this branch. ONE conflict, docs/development/CLAIMS.md: two claim blocks appended at the same anchor, BOTH KEPT with REC-16 first because it landed first. store.mjs and index.mjs auto-merged and the disjointness held as claimed — REC-16 works in divide()/promote/affordanceFacts and the OPS/NEEDS/STATE_ACTIONS region, this item in the two task verbs and their two OPS lines; neither touched the other. Verified on the merged tree: battery 84/84 (4392 = main`s 4358 + this suite`s 32 + the 2 per-suite assertions hygiene and planning-hygiene add for any new file — so no existing assertion moved), coverage --strict exit 0 with 121/121 ops, 53/53 checks, 84/84 negative controls declared. BOTH NC ARMS RE-RUN ON THE MERGED TREE and restored byte-identical (a: 17 of 32 fail, battery 83/84 4375 with every other suite green; b: 3 fail and the probe reaches the store); the suite`s recorded numbers are the merged-tree ones.
released:

released: 2026-08-03 — landed on main by CONDUCT (merge of worker dd6c3a1). Verified: battery 84/84 (4392), --strict exit 0, 121/121 ops. NC RE-RUN BY CONDUCT (both refusals neutered → 17/32 named failures; restored byte-identical). IC-7 recorded, I3 → 5.0.0; D-151 closed; CAPABILITIES.md row amended dated-style. No DEC.
session: rec17-agent (worktree agent-a3da31793d5229d8c)
opened: 2026-08-04T05:00:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) ONE new ALTER-loop column `bundles.inquiry_superseded_by` and the private `#writeSupersededBy` that maintains it, called from promote's refs transaction (old targets ∪ new targets ∪ the promoted bundle's own row) and once at boot over the existing supersedes edges; (2) ONE new private predicate `#restsOnLive` — the live-basis-leg question, #citesInto's shape over `inquiry_basis`; (3) ONE new derived read `reevaluations()` plus its `reevaluations:` line in the DO dispatch map; (4) the CITED refusal added to `dispose()` (dismiss arm) and to `divide()`, and the `reevaluation` block added to the SUCCESS answers of `dispose()`'s defer arm, `reopen()` and `publishCase()`. NOT promote's other arms, NOT strengthOf/basisFor/restingOn, NOT conclude, NOT the gate/ratify path, NOT the projections REC-12 and REC-14 own), bio-plane/src/index.mjs (ONLY: one OPS entry `reevaluations` (read, admin+member+probe, mutating false) and that op added to the ONE existing viewer-stamp condition. No NEEDS entry — a read carries none, op=governorstate's precedent — and therefore no change to REC-19's NEEDS/NON_ACTS totality), bio-plane/test/reevaluation.test.mjs (NEW) + its `test:reevaluation` entry in bio-plane/package.json, bio-plane/test/gate-reads.test.mjs (ONLY: op=reevaluations classified GATED in the structural table, with its reason), bio-plane/test/divide.test.mjs + bio-plane/test/disposition.test.mjs + bio-plane/test/affordances.test.mjs (ONLY superseded pins moved by the CITED refusal, corrected with dated comments and never exempted), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (ONE additive nullable column on `bundles` through the ALTER loop — no new table, so no new purge arm; `inquiry_basis` and its `inquiry_basis_target` index are REC-11's and are READ, never reshaped), REC-16's `supersedes` edges in `refs` (read, not re-produced), REC-14's editions in `published_bundles` (read), REC-12's `strengthOf()` (read — NOTHING here alters a strength), REC-25/REC-30's viewer gate through query.mjs's ONE compilation point (both shapes: a withheld ROW for a dependent the viewer may not see, a REDACTED back-reference for a superseding id inside a visible row)
interfaces owned: I3 additive (op=reevaluations, non-mutating; and the additive `reevaluation` block on the answers of op=dispose/op=reopen/op=publish) plus ONE break: op=dispose's dismiss arm and op=inquirydivide gain the `CITED` refusal, so an act that previously succeeded over a cited inquiry now refuses by name.
DISJOINT FROM REC-28 (the task fence), running concurrently on taskforward/taskresolve ground: this claim opens neither taskForward nor taskResolve nor the tasks table, adds no task op and changes no task gate. The one file both touch is store.mjs, in functions that do not overlap.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/affordances.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/queuestate.mjs, bio-plane/src/setup.mjs, civicos-ui/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-17 / RECONCILED.md §3.2 (REC-17) / BUILD-ORDER.md §2 (REC-17) — P-64, the re-evaluation obligation as a QUERY and not a flag, WIDENED by D-5 to the walk-back edges; DEC-12's note (a newer EDITION surfaces the obligation on dependents and recomputes nothing — a leg keeps citing the edition it names).
accepts-when: cd bio-plane && npm run test:battery green with a suite where superseding an inquiry (through a real op=inquirydivide) AND republishing at a new edition each make every inquiry whose basis names it surface the obligation, naming the moved leg and both strengths, without altering any strength; dismissing a cited inquiry refuses CITED naming offenders; dividing a cited inquiry refuses CITED; deferring succeeds and raises the obligation on every dependent; reopening likewise; npm run test:coverage --strict exit 0. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored after each — (a) drop the reverse lookup and an inquiry resting on a superseded case reports nothing; (b) permit a dismiss on a cited inquiry and a published case's basis panel names a leg that is now an abandoned question while its frozen strength still reads. No push — CONDUCT integrates; landed: left for CONDUCT.
released:
amended 2026-08-04 (rec17-agent, same session): + bio-plane/src/affordances.mjs (ONLY: (1) the `inquirydivide` act gains a THIRD condition `(f.rested_on?.working ?? 0) === 0` — retire's condition one altitude up, over the SAME #restsOnLive predicate the store's refusal runs, because a pre-flight offering a control the refusal it fronts would decline is the DEC-8 disagreement; (2) the facts-shape comment extended for `rested_on`; (3) a comment on the `dispose` act recording why it is DELIBERATELY NOT narrowed. NOT ACTS' membership, NOT DISPOSITIONS/REOPENABLE_FROM, NOT NON_ACTS, NOT edgesFrom, NOT the catalogue's size), + bio-plane/src/store.mjs affordanceFacts() (ONE new fact, `rested_on {working, frozen, severed}` — COUNTS and never ids), + bio-plane/package.json (the `test:reevaluation` script). NO existing suite's pins moved: the CITED refusals and the narrowed affordance were added with the battery green at 83/83 before and after, because no landed fixture disposes or divides an inquiry another inquiry's basis names. divide.test.mjs, disposition.test.mjs and affordances.test.mjs were therefore NOT edited — the correction the trap anticipated was not needed, and saying so is the point.
THE ONE JUDGMENT CALL, for CONDUCT: D-5 says terminal acts on a cited inquiry refuse CITED, and this item ships that — with DIVISION refusing on a WORKING dependent (open/concluded/deferred/dismissed) and NOT on a PUBLISHED one, while DISMISSAL refuses on both. Taken literally over the same set, the item's own supersession arm is unreachable: a division could then never supersede anything with a dependent, and "superseding a case makes every inquiry whose basis names it surface the obligation" would have no producer. The distinction that resolves it is what each act LEAVES BEHIND — C-6.2's remedies are "restore from history", "re-point to the successor", "sever with a reason"; a division carries the question forward into children resolvable in both directions (REC-16), so the second remedy exists and the dependent gets R7's obligation, while a dismissal abandons the question and leaves a signed edition — which cannot edit its own basis — stranded forever. Both halves are asserted (blocks 4 and 5) and negative control (b) produces the dismissal harm verbatim. Reversing it is one predicate: `restsOn.confirmed` -> `restsOn.all` at divide()'s guard, plus the matching `rested_on.working` -> working+frozen in affordances.mjs; the cost of reversing is that a published case permanently freezes any question beneath it against correction.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker b2a77dd). Verified: battery 85/85 (4457), --strict exit 0, 122/122 ops. The D-5 split accepted with its criterion (C-6.2's re-point remedy) and one-predicate reversal recorded. IC-8, I3 → 5.1.0. No DEC — the split resolves a contradiction the design's own remedies criterion answers.

## CLAIM 2026-08-04 RECORD
session: rec22-agent (worktree agent-aa4b88aa614764fb1)
opened: 2026-08-04T07:00:00Z
paths: bio-plane/src/schema.mjs (ONLY: the NEW `published_edges` table + its index, inserted BEFORE the `host_governor` block per the standing trap), bio-plane/src/store.mjs (ONLY: (1) `#publishEdges()` called from the existing `publish()` committer, inside its transaction — no other line of publish() changes; (2) three NEW read methods `publishedCase()`, `publishedTargets()`, `publishedShaRow()`, added beside `publishedEditions()`; (3) `published_edges` added to op=purge's TABLES list, which covers BOTH arms; (4) three new DO route entries. NOT promote, NOT publishCase, NOT the gate path, NOT any existing read), bio-plane/src/index.mjs (ONLY: (1) two NEW OPS rows `publishedcase` and `publishedbytes`, `classes: null`, non-mutating; (2) their handlers inside the EXISTING `spec.classes === null` block beside op=verify/op=publishedmanifest; (3) in the op=ratify handler, the `edges` payload computed from the ratified frontmatter and passed to the DO publish call — an addition to the existing body, no existing field changed), bio-plane/src/container.mjs (NEW — the DEC-34 zip serialiser), bio-plane/checks/bio-checks.mjs (ONLY: `sectionText` gains the `export` keyword — additive, no behaviour change, so the published read and the catalog use ONE section parser), bio-plane/test/publishedcase.test.mjs (NEW) + its test:publishedcase entry in bio-plane/package.json, bio-plane/test/gate-reads.test.mjs (ONLY: two rows added to REC-30's UNGATED map with their stated reasons — the sweep's own instruction), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (REC-14's published_bundles/published_shas and its MANIFEST.json layout block, READ — neither reshaped), REC-16's division_parent/division_siblings as they survive op=publish into the signed bytes, REC-30's structural classification of read ops (this item's two ops join its UNGATED list), REC-10's normalizeType (the MAP RULE at the object-type consultation)
interfaces owned: I5 (ONE new table, published_edges, in BOTH purge arms), I3 additive (op=publishedcase, op=publishedbytes — both `classes: null`, both non-mutating; no existing op reshaped)
DISJOINT FROM REC-17 (the re-evaluation obligation), running concurrently: REC-17 owns the CITED refusals on dispose/divide and the reeval_flag/since/source columns on the WORKING corpus, and renders a supersession banner FROM this op later. This claim adds no refusal, touches no disposition or division act, and reads only the published projection; REC-17 touches no published table and adds no read op. The one shared file is checks/bio-checks.mjs, where this claim adds a single `export` keyword to an existing function.
explicitly NOT touched: bio-plane/src/query.mjs, bio-plane/src/affordances.mjs, bio-plane/src/queuestate.mjs, bio-plane/src/setup.mjs, bio-plane/src/gate.mjs, bio-plane/src/ooxml.mjs (imported for crc32, not edited), civicos-ui/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-22 / RECONCILED.md §3.1 (REC-22) / BUILD-ORDER.md §2 (REC-22), carrying R4's name-not-serve correction (published targets for edges the surface may SERVE; a division's parent and sibling ids admitted as edges it may only NAME), D-1 (the published projection only — the frozen strength and the rendered body come from the SAME bytes), DEC-12 (editions; a hash resolves to ITS edition; prior editions stay readable and separately verifiable), DEC-17 (the declared bar beside the frozen pair, absent SAYS absent) and DEC-34's remaining half (the container serialised as a zip per the manifest's layout block, served BY HASH). NO page-shaped rendering is produced here: DEC-34's per-page header control lands with UI-18's rendering work, stated in the suite.
accepts-when: cd bio-plane && npm run test:battery green with a suite where an ANONYMOUS caller (no credential of any kind) gets the case, its bytes and the container zip while the same caller gets nothing from op=list/op=search/op=projection/op=image; a never-ratified sha 404s identically to one that never existed; a published child's page names its parent and its siblings while SERVING neither, and an unpublished sibling's bytes are not reachable; edition 1 stays fetchable and verifiable after edition 2 publishes; both frozen strengths and the declared bar per edition; npm run test:hygiene green with published_edges in purge; npm run test:coverage --strict exit 0. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored after each — (a) remove the published_shas guard on publishedbytes; (b) admit the name-only division edges to the served set. No push — CONDUCT integrates; landed: left for CONDUCT.
released:
amended 2026-08-04 (rec22-agent, same session), each found by building the thing rather than by review: + `unresolved[]` on op=publishedcase's answer — the store REPORTS a serve-classified edge with no published edition behind it instead of dropping it, because negative control (b2) PASSED on an empty `serves[]` the first time it was run (an outcome that costs nothing to produce; the assertion was satisfied whether the write-time restriction held or was never applied). It discloses nothing new: every row of published_edges for a case comes from that case's OWN ratified bundle.md, which any caller can fetch by hash. + `body.authored` on the same answer — op=conclude writes the gated conclusion and falsifier into the FRONTMATTER, not into the `## Conclusion` section, so returning only the parsed sections would have dropped the claim the gate holds the case to; both come back, labelled, with the section carrying the prose beside it. + a capture part's BYTES now reach the published file manifest, read from the register rows the store already hands out with the gate facts (REC-14's manifest recorded sha/path/kind for a blob and no length, and the item requires per-file sha AND bytes). That is an ADDITIVE change to REC-14's `bio-case-container/1` manifest for capture-bearing cases and it moves their manifest_sha; no data migration is implied (no case carries a capture part on main yet) and nothing consumes the manifest outside this item — UI-18 is its first reader.
FINDING FOR CONDUCT, measured rather than reviewed: BUILD-ORDER's negative control for op=publishedbytes ("remove the published_shas guard and a working-corpus capture sha streams to an anonymous caller") is UNREACHABLE AS WORDED, and the reason is a good one — the working corpus lives at `<store>/captures/<sha>` and the published corpus at `<store>/published/<sha>`, so with the guard gone the key is simply not there to fetch. Removing the guard alone leaves every existing assertion passing. What the guard actually defends is anything that reaches the published bucket WITHOUT going through ratification (a restored backup, an operator's copy, a future writer's bug), and the suite now constructs exactly that — the working capture's own bytes planted at its published key — and proves the guard refuses it while the bucket boundary cannot. The control breaks that assertion, measured 2 failures.
NO PAGE-SHAPED RENDERING IS PRODUCED HERE, and that is the recommended answer to DEC-34's negative-control seam: the plane serves the CONTAINER and UI-18 renders it, so the per-page brazening (case id, edition, authors, declared bias, both floors, hash, verification pointer on EVERY page) is a property of the renderer and its control belongs to UI-18, which is the item that will have pages. The suite asserts STRUCTURALLY that no source file in the plane emits a page-shaped artifact, so the day one appears it fails here and somebody has to decide where the control lives.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 2b60e21). Verified: battery 86/86 (4532), --strict exit 0, 124/124 ops, hygiene green. NC arm (a) RE-RUN BY CONDUCT (2 named failures; restored byte-identical). Purge-arms reasoning accepted and recorded on the landed line. IC-9, I3 → 5.2.0. No DEC.
session: rec23-agent (worktree agent-aac198dbe4ed9c2a6)
opened: 2026-08-04T06:00:00Z
paths: bio-plane/checks/bio-checks.mjs (ONLY: (1) THREE module-level constants beside the exported ACTION_KINDS — COUNTERPARTY_STATES, ENTITY_ID_RE, COUNTERPARTY_PLACEHOLDER; (2) the COUNTERPARTY arm of checkActionExtension (C-2.10), lifted into one private function checkCounterparty. NOT ACTION_KINDS' contents, NOT the risk_tier or resolution arms, NOT C-11.1's clock arm, NOT parseFrontmatter, NOT any other check family), bio-plane/src/gate.mjs (ONLY: CATALOG_VERSION 1.19.0 -> 1.20.0 — REC-14's minor-bump precedent for a catalog that newly refuses documents that used to pass), bio-plane/src/setup.mjs (ONLY: mdFor's `type === "action"` arm — the `counterparty: to be named` literal deleted and a comment put in its place. The `action_kind: other` / `risk_tier: 1` literals on the same line are UI-15's and are LEFT), civicos-ui/app.html (ONLY: the same one literal in mdFor at :1752 — UI's ground, crossed for this literal alone on CONDUCT's instruction, and the other two literals on that line deliberately LEFT for UI-15), bio-plane/test/counterparty.test.mjs (NEW) + its test:counterparty entry in bio-plane/package.json, bio-plane/test/check-firing.test.mjs + bio-plane/test/affordances.test.mjs + bio-plane/test/conformance.test.mjs + bio-plane/test/ratify.test.mjs (ONLY superseded action fixtures and pinned assertions, CORRECTED with dated comments and never exempted), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (NO schema change and NO new table — the item forbids a counterparty registry, and `counterparty` is frontmatter that no projection column reads today; REC-24 adds `action_counterparty_state` and is this item's consumer), the restricted frontmatter grammar (one-level maps at two spaces — the shape `source: {locator, authority, retrieved}` and REC-14's `completeness` already take), the subject registry's `entity_id` key shape (READ as a regex, never joined — the catalog has no entity-resolution seam and the limit is stated at the site)
interfaces owned: none newly. The CATALOG (C-2.10) BREAKS for one document class: an action whose `counterparty` is a bare string — every action written before this item — is now refused at the gate, which is the defect being closed. Additive for every other object type. No op shape changes; no OPS/NEEDS/classes entry touched.
DISJOINT FROM REC-22 (op=publishedcase / op=publishedbytes, the public read path), running concurrently on PUBLISHED ground: that item works in store.mjs's published projection, a new `published_edges` table in schema.mjs and two new credential-free ops in index.mjs. This claim opens NEITHER store.mjs NOR schema.mjs NOR index.mjs NOR query.mjs at all, adds no op and no table, and its one catalog change is the action extension — which the published read path does not consult.
explicitly NOT touched: bio-plane/src/store.mjs, bio-plane/src/schema.mjs, bio-plane/src/index.mjs, bio-plane/src/query.mjs, bio-plane/src/affordances.mjs, bio-plane/src/queuestate.mjs, bio-plane/dist/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-23 / BUILD-ORDER.md §2 (REC-23), closing D-130. accepts-when: cd bio-plane && npm run test:battery green with a suite where an action carrying `state: undetermined` and an authored basis passes C-2.10, one carrying `state: undetermined` with no basis is refused BY NAME, and one carrying the literal string `to be named` is refused; npm run test:coverage --strict exit 0; node civicos-ui/test/run.mjs green. NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — restore the old arm (accept any non-empty string) and the placeholder passes, restoring D-130; restored after running. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 63f3f5f). Verified: battery 87/87 (4574), --strict exit 0, 124/124 ops. D-130 closed; the present-and-refused consequence enacted into UI-15's scope; MILESTONES' UI-15/UI-19 disagreement corrected. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec32-agent (worktree agent-a1cbdda8b1dd7c976)
opened: 2026-08-04T09:00:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) `QUEUE_CLASSES` gains "CONDITION" as its LAST rank and `QUEUE_CLASSES_DEFERRED`'s CONDITION entry is REMOVED (the object stays, now empty — the declaration mechanism survives for the next class with no producer); (2) THREE new private derivations `#conditionsGovernorHolding`, `#conditionsPartialCapture`, `#conditionsCaptureUnattended` plus the one assembler `#queueConditions` and the small `#conditionBundlesForHost` / `#conditionHomes` helpers, all called from queueFeed BEFORE the existing REC-21 mute loop so conditions run through the SAME admission point; (3) a `QUEUE_CONDITION_SUBJECTS_MAX` constant and a `NO_CONDITION_KIND` refusal beside the existing `NO_CLASS` one; (4) `counts.condition` added to op=queue's answer. NOT #queueAncestors, NOT #queueEventLive, NOT #queueOptions, NOT the OBLIGATION or FINDING loops, NOT queueMute/queueSnooze, NOT the governor's own admit/report/config methods, NOT the capture-session save/load/drop methods, NOT promote, NOT the gate), bio-plane/src/queuestate.mjs (ONLY: the dated correction of the "nothing in this plane emits a CONDITION item today" head comment — the vocabulary itself, the class maps, classOfKind, the refusal details, the serialisers and suppressedBy are all UNCHANGED), bio-plane/test/queue-conditions.test.mjs (NEW) + its `test:queueconditions` entry in bio-plane/package.json, bio-plane/test/queue.test.mjs (ONLY: the HOLE-1 pin that asserts no CONDITION item and an empty-of-nothing classes_deferred — CORRECTED with a dated comment, never exempted), bio-plane/test/queue-state.test.mjs (ONLY: the head-comment statement that CONDITION has no producer, and the two live-item clauses that can now run through op=queue instead of only against suppressedBy — corrected with dated comments), docs/development/CLAIMS.md (this entry)
interfaces consumed: I5 (READ ONLY, and NO new table and NO stored state — the REC-20 precedent: `host_governor` (the governor's own cooloff, D-95/D-103), `capture_sessions` (the partial-capture ledger, CAPTURE-SCALING), `manifest.author` (REC-2/D-61's server-stamped `token:<class>` machine writer), `captured_locators` + `register` (host -> document), all read at their own indexes; no schema change, so no new purge arm), REC-20's `#queueAncestors` walk and its QUEUE_ANCESTOR_DEPTH bound (called, never re-implemented), REC-19's deriveActs through `#queueOptions` (called), REC-21's `QUEUE_CONDITION_KINDS` and `suppressedBy` (imported and consulted — the emitted kinds are checked AGAINST the vocabulary, which stays the single authority), REC-25/REC-30's viewer gate through query.mjs's ONE compilation point
interfaces owned: I3 additive — op=queue's feed gains CONDITION items in the SAME item contract (no field added to the item shape), `classes` gains a third member, `classes_deferred` becomes empty, and `counts` gains `condition`. No op added, no op removed, no existing field reshaped.
DISJOINT FROM REC-26 (the monitor consumer), running concurrently on the scheduler/wrangler ground: this claim opens neither the alarm/consumer registry nor wrangler.jsonc nor any monitor path, adds no consumer and no tick. The three kinds built here read facts that exist in the store TODAY (a governor cooloff, a parked capture session, a machine-authored manifest revision) and are deliberately NOT coupled to any capture fact a monitor tick may later produce.
explicitly NOT touched: bio-plane/src/schema.mjs, bio-plane/src/index.mjs, bio-plane/src/query.mjs, bio-plane/src/affordances.mjs, bio-plane/src/subresources.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/src/setup.mjs, civicos-ui/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-32 (from REC-21's report) — the FIRST CONDITION generator, HOLE-1's bridge getting its first real half. NOTIFICATIONS.md's catalogue for the three cheapest candidates whose data is already in the store; DEC-16's one-state-N-homes shape inherited unchanged; D-125's muting-is-personal boundary exercised on LIVE items for the first time.
accepts-when: cd bio-plane && npm run test:battery green with a suite where a governor-holding-host condition appears in op=queue's feed with its kind and case set, muting it hides it for that member only while a NEW condition kind on the same case still surfaces, an OBLIGATION on the same case is untouched, and the resolved underlying fact clears the condition for EVERY member; npm run test:coverage --strict exit 0. NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — resolve the underlying fact (the governor's hold expires) and leave the condition rendering, and the suite fails naming the stale condition; restored after running. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker b99e1e7). Verified: battery 88/88 (4627), --strict exit 0, 124/124 ops. D-170 (ungrouped-mute watch) and D-171 (#revisionKind tiebreak) recorded. The kept suppressedBy clauses and the corrected HOLE-1 pin reviewed and accepted. No DEC.
session: rec26-agent (worktree agent-a882215b5e6cd24f2)
opened: 2026-08-04T09:00:00Z
paths: bio-plane/wrangler.jsonc (ONLY: the `SELF` entry appended to the existing `services` array. account_id NOT touched, PDF_WORKER NOT touched), bio-plane/src/schema.mjs (ONLY: the TWO new tables `monitor_fired` and `monitor_tick_epoch` + their index, inserted BEFORE the `host_governor` block per the standing trap), bio-plane/src/store.mjs (ONLY: (1) the `monitor-cadence` entry appended to `#schedConsumers` and its `monitorcadence` line in `onAlarm`'s named results; (2) the NEW private cadence helpers `#monitorIntervalMs`/`#monitorCadencePlan`/`#monitorCadenceWake`/`#monitorCadenceTick`/`#fireMonitorTick` placed beside CAP-3's monitoring block; (3) the NEW idempotence helpers `#openTickEpoch`/`#closeTickEpoch`/`#claimFire` and their application inside CAP-3's `#monitorTick` loop — the loop's eligibility logic and `#fireArchiveFallback` are UNCHANGED; (4) `monitor_enabled` added to the existing bundles index loop; (5) the two new tables added to op=purge in BOTH arms plus their `stats()` counters; (6) the `promote` DO-dispatch entry made async so a newly-monitored bundle ARMS the one alarm. NOT promote() itself, NOT taskDrain, NOT the selection sweep, NOT recordSourceOutcome, NOT sourceReachability, NOT the gate/ratify path, NOT query.mjs), bio-plane/src/index.mjs (ONLY: `monitorcadence` named in the alarm-result passthrough comment — NO op added, NO OPS/NEEDS/classes row touched; op=monitor's handler is UNCHANGED), bio-plane/checks/bio-checks.mjs (ONLY: the `export` keyword added to the existing `MONITOR_FREQ` constant — additive, no behaviour change, REC-22's `sectionText` precedent — so the cadence table is held against the CATALOG's vocabulary and never a local copy: the MAP RULE), bio-plane/test/monitor-cadence.test.mjs (NEW) + its `test:monitorcadence` entry in bio-plane/package.json, docs/development/CLAIMS.md (this entry and the DELEGATION below)
interfaces consumed: I5 (TWO new tables, both in BOTH purge arms; no existing table reshaped — `bundles.monitor_frequency`/`monitor_enabled`/`monitor_last_checked` are READ, the columns P-84 measures as existing with no reader), I3 (op=monitor is CALLED over `env.SELF` exactly as CAP-3 calls op=acquire — the op's contract is not changed), REC-1's `#schedConsumers` registry (ONE consumer registered; the mechanism untouched — no second alarm, no cron, SCHEDULER.md is the authority), CAP-3's `#monitorTick`/`#monitorConfigured`/`#monitorToken` (the idempotence key is applied AT its fire loop; its D-104 exclusion and its eligibility fence are not touched), the catalog's `MONITOR_FREQ` vocabulary (the MAP RULE — the interval table is keyed off the catalog's own closed set)
interfaces owned: I5 (`monitor_fired`, `monitor_tick_epoch`). I3 unchanged — no op added, no op reshaped.
DISJOINT FROM REC-32 (the first CONDITION generators), running concurrently on queueFeed ground: that item works in op=queue's derived feed, QUEUE_CLASSES_DEFERRED and queue.test.mjs. This claim opens none of those — it adds no queue class, no queue item kind and no queue read, and touches `queue_state` not at all. The one shared file is store.mjs, in functions that do not overlap (its queueFeed / #queueAncestors vs this claim's scheduler registry and monitoring block).
explicitly NOT touched: newgroup/** (the installer half of the SELF binding is the DELEGATION below), bio-plane/src/query.mjs, bio-plane/src/affordances.mjs, bio-plane/src/queuestate.mjs, bio-plane/src/setup.mjs, bio-plane/src/gate.mjs, civicos-ui/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-26 / BUILD-ORDER.md §2 (REC-26) — the two live M1 gaps. (a) `env.SELF` bound in the PLANE's wrangler.jsonc so CAP-3's archive-monitor is no longer inert on the deployed plane; the INSTALLER-TEMPLATE half is the DELEGATION below. (b) `op=monitor` gets its caller: a `monitor-cadence` consumer on REC-1's one alarm reading PER-DOCUMENT cadence from `bundles.monitor_frequency` (P-84 — the column that exists and nothing reads), firing op=monitor over `env.SELF` under the daemon credential. HEEDS MACHINE-PROCESSES.md risk 2: an alarm retry re-fires already-succeeded subjects and `recordCapturedLocator` does `observations = observations + 1`, so a retry MANUFACTURES CORROBORATION — the equality-that-costs-nothing rule, not an optimisation. The key is `(consumer, subject, tick epoch)`, written BEFORE the expensive act (taskEnqueue's producer-first pattern); the epoch is OPEN across a tick that had failures, so the next tick is that tick's RETRY and skips what already succeeded, and is closed on a clean tick so the next cadence really does re-check. An instance with no monitoring configured — no SELF binding, or no document asking to be monitored, or only documents whose cadence this plane cannot compute — holds NO alarm (REC-1's self-terminating property).
accepts-when: cd bio-plane && npm run test:battery green with a time-pinned suite (virtual `now` driven into onAlarm, the scheduler.test.mjs precedent, anchored on the last_checked op=monitor actually wrote) where a changed source produces a monitor-tick with NO operator action, a re-fire of an already-succeeded address does NOT increment `observations`, and an instance with no monitoring configured holds no alarm; the SELF binding asserted present in wrangler.jsonc with account_id still pinned; npm run test:coverage --strict exit 0. NEGATIVE CONTROLS run, recorded in the suite's NEGATIVE CONTROL line and restored after each — (a) remove the idempotence key and a retry inflates `observations`; (b) remove the cadence read and every document is checked at one global interval. The `npx wrangler deployments list` half of the item's acceptance is a DEPLOY-time check and is GATED to Bob; not attempted here. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 02e1358). Verified: battery 89/89 (4690), --strict exit 0, 124/124 ops, hygiene 59/59. D-172 recorded (the reentrancy find); the DIST delegation routed onto DIST's queue backlog; RECORD drained and dormant. No DEC.

## DELEGATION 2026-08-04 RECORD → DIST
from: rec26-agent / REC-26 (the installer-template half of the `env.SELF` binding)
need: `newgroup/src/index.mjs` must provision a SELF service binding on every installed instance, in BOTH upload paths. THE INSTANCE NAME IS THE WORKER NAME, and the installer already holds it as `slug`, so nothing further need be asked of the group. Exactly:
  1. in `uploadInstall`'s `meta.bindings` array, beside the existing `durable_object_namespace`/`plain_text`/`secret_text`/`r2_bucket` entries, add
     `{ type: "service", name: "SELF", service: slug }`
     (the Cloudflare script-upload binding type for a Worker-to-Worker service binding; `service` is the script name, which for a self-binding is this same `slug`);
  2. in `uploadUpdate`'s `meta.bindings`, add the SAME entry — for the reason the INSTANCE_NAME and R2 comments there already give: an update must quietly complete what an older install left out, so every instance installed before this existed gets the binding on its next update with no operator action;
  3. `uploadUpdate`'s `keep_bindings` list does NOT need `"service"` added while (2) binds it explicitly. If (2) is ever dropped, `"service"` MUST be added to `keep_bindings` or an update will silently REMOVE the binding and re-inert the monitoring consumers.
  4. a daemon credential must be present: CAP-3's `#monitorToken()` prefers `MONITOR_TOKEN` and falls back to `ADMIN_TOKEN`, which the installer already binds as a `secret_text` — so no new secret is strictly required, and a scoped `MONITOR_TOKEN` is the better answer whenever DIST is ready to mint one (DEC-5's token-exposure surface).
why: with no `SELF` binding, `#monitorConfigured()` is false and BOTH monitoring consumers (CAP-3's archive-monitor and REC-26's monitor-cadence) contribute no wake and hold no alarm — they are built, tested, and wired to nothing on every installed instance (`MACHINE-PROCESSES.md` §0). This DELEGATION supersedes the open CAP-3 → DIST delegation of 2026-07-31 for the installer half; the PLANE half of that delegation is closed by this item (`bio-plane/wrangler.jsonc` now carries the binding, deploy still gated).
status: open
amended 2026-08-04 (rec26-agent, same session), each found by building the thing rather than by review: (1) bio-plane/src/index.mjs is NOT touched at all — the claim above anticipated a comment there and none was needed; op=monitor's handler, the OPS table, NEEDS and classes are all unchanged, and this consumer reaches op=monitor over the SELF binding exactly as CAP-3 reaches op=acquire. (2) + `Store.capturedLocators({addressNorm})` in store.mjs, the READ half of the existing recordCapturedLocator: `observations` is the raw material of the PRIMARY contemporaneity route and NOTHING could read the number back, which is precisely why nothing could notice a machine process inflating it — the suite could not have measured its own subject without it. Deliberately NOT given a control-plane op (I3 stays unchanged); it is reached over the Durable Object the way the suites reach onAlarm and selectionCreate. (3) + `#tickRunning`, a non-re-entrancy guard on both firing ticks, and the STALE window on the tick epoch — see the FINDING below, which is the reason both exist.
FINDING FOR CONDUCT, measured rather than reviewed, and it is not confined to this item: **the one DO alarm can fire while a previous tick is still in flight, and the second run can complete a tick the first is in the middle of.** A tick that reaches a control-plane op over `env.SELF` re-enters this same Durable Object; the nested op=acquire ENQUEUES an inbox task for an undetermined-authority capture (D-98's producer); taskEnqueue arms the drain ONE SECOND out; workerd fires the alarm underneath the still-awaiting tick. The first version of the idempotence key was silently wiped by exactly this: the re-entrant run found every subject already claimed, saw nothing fail, and closed the epoch the outer run was still working through — so the retry re-fired the succeeded address and observations inflated anyway, with every assertion about the key's own rows passing. Two consequences worth CONDUCT's attention beyond this item: (a) ANY async consumer on REC-1's registry that calls out over SELF has this shape, and the registry itself offers no mutual exclusion — REC-26 guards its own two ticks in memory and did not widen the mechanism, because reshaping the scheduler is not this item's scope; (b) it is a genuine argument that the shape belongs in `#schedConsumers` (a `serial: true` flag on a consumer, enforced by onAlarm) rather than in each consumer's body, and that is a scheduler decision rather than a monitoring one.
DEPLOY-GATED HALF, stated plainly: the item's `npx wrangler deployments list` clause proves the SELF binding is present in the DEPLOYED config, which requires a deploy of the plane. Deploys are gated to Bob and DIST cuts releases, so it was NOT attempted. What is proven here is that `bio-plane/wrangler.jsonc` — the file a deploy uploads — carries the binding, with account_id still pinned, asserted structurally in the suite. The consumers stay INERT until the deploy lands, by construction and asserted (an instance with no SELF binding holds no alarm), so shipping this ahead of the deploy changes nothing on a live instance.

## CLAIM 2026-08-04 DIST
session: dist1-agent (worktree agent-a06332fa60cdcca87)
opened: 2026-08-04T12:00:00Z
paths: newgroup/src/index.mjs (ONLY: (1) the new `selfBinding(slug)` helper and the `{ type: "service", name: "SELF", service: slug }` entry it returns, added to `uploadInstall`'s `meta.bindings` and to `uploadUpdate`'s `meta.bindings`; (2) `uploadInstall` gains an `opts.noSelf` parameter and `runInstall`'s install step gains ONE degrade-and-retry arm around it. NOT the OAuth/PKCE path, NOT `selectRelease`, NOT `ensureBuckets`, NOT `ensureSubdomain`, NOT `verifyInstall`/`verifyUpdate`, NOT the secrets generation, NOT `keep_bindings`, NOT the success panels), newgroup/test/wizard.test.mjs (ONLY: the SELF assertions in the install block and the update block, the new degrade block, and the NEGATIVE CONTROL header line), docs/development/CLAIMS.md (this entry)
interfaces consumed: I4 (the installer's provisioning surface — this claim's own), CAP-3/REC-26's `#monitorConfigured()` contract in bio-plane/src/store.mjs (READ ONLY, not edited: `env.SELF` + a daemon token is what arms both monitoring consumers)
interfaces owned: I4 additive — every installed and every updated instance gains a `SELF` service binding to its own worker. No binding removed, no secret added, `keep_bindings` unchanged.
DISJOINT FROM UI-10, running concurrently on `civicos-ui/**`: this claim opens no file under `civicos-ui/`, no plane source, and no plane test. The one direction of contact is that both read the plane's shipped behaviour; neither writes it.
explicitly NOT touched: bio-plane/** (the plane already prefers `MONITOR_TOKEN` over `ADMIN_TOKEN` in `#monitorToken()`, so nothing needed narrowing — see the credential finding below), newgroup/wrangler.jsonc (account_id stays pinned), newgroup/dist/**, release/**, civicos-ui/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md DIST-1 — the REC-26 delegation. The installer binds `SELF` in BOTH upload paths so the monitoring consumers arm on deployed instances rather than staying built-and-wired-to-nothing (`MACHINE-PROCESSES.md` §0).
accepts-when: cd newgroup && npm test green with the SELF binding asserted in BOTH upload paths' built config; cd bio-plane && npm run test:battery green and npm run test:coverage --strict exit 0 (the plane is untouched). NEGATIVE CONTROL run and recorded in wizard.test.mjs's NEGATIVE CONTROL line — remove the update-path binding and the suite fails naming an already-installed instance that would never receive SELF; restored after running. NO DEPLOY: a real install/update run against Cloudflare is gated to Bob and was not attempted. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 8ae2e88). Verified: embed 15 + wizard 103, battery 89/89 (4691), --strict exit 0. DEC-37 raised from the credential question with the worked recommendation; REC-33 queued blocked on it; the degrade-not-fail and keep_bindings reasoning accepted. No deploy attempted; the gated halves stand in front of Bob.

## DELEGATION 2026-08-04 DIST → RECORD
from: dist1-agent / DIST-1 (the credential half of the REC-26 delegation)
need: a scoped `MONITOR_TOKEN` cannot be minted by the installer alone, and minting it alone would BREAK monitoring on every instance that received it. `bio-plane/src/store.mjs`'s `#monitorToken()` returns `env.MONITOR_TOKEN || env.ADMIN_TOKEN` — MONITOR_TOKEN FIRST — but `bio-plane/src/index.mjs`'s `classify()` recognises exactly three token values (`ADMIN_TOKEN`, `MEMBER_TOKEN`, `PROBE_TOKEN`) and returns `null` for anything else. So an instance carrying a `MONITOR_TOKEN` secret would select it for every monitor fire and be refused by its own control plane, while `#monitorConfigured()` stayed TRUE — the alarm arms, every tick fires, every fire 401s, and the instance looks configured. That is strictly worse than today and worse than no binding at all, so DIST-1 did NOT mint it. Making the scoped credential real needs, in RECORD's I3 ground and in ONE change with the installer's:
  1. `classify()` in bio-plane/src/index.mjs returns a `monitor` class for `env.MONITOR_TOKEN` (through `liveToken`, as the other three go);
  2. `OPS.monitor.classes` accepts it, and the archive arm of `op=acquire` (the `cls !== "admin" && cls !== "probe"` gate, whose comment already says "an operator or DAEMON credential") accepts it — and NOTHING else does, which is the whole point of the scoping;
  3. `scopeFor()` must NOT confine it to `scratch` (monitoring writes the live record, which is why PROBE_TOKEN is not the answer here);
  4. `op=selftest`'s binding report and `livefire.mjs`'s token-name list learn it, `tokens.mjs`'s `PUBLISHED_TOKEN_HASHES` covers any value that ever lands in the repo (the discipline is per-VALUE, not per-name, so no list edit is needed unless a MONITOR_TOKEN value is ever published);
  5. `CAPABILITIES.md` §4/§5: a fourth token CLASS is not a fifth capability token, but it is a doctrine-visible change and should be stated there rather than discovered.
When (1)-(4) land, DIST binds `{ type: "secret_text", name: "MONITOR_TOKEN", text: rand(32) }` in `uploadInstall` and mints-if-absent on `uploadUpdate` (the update path cannot read an existing secret back, so an update either always overwrites it or never sets it — that choice is DIST's and is a one-line change once the class exists).
why: least privilege. Today every monitor tick and every archive fallback on every installed instance runs under ADMIN_TOKEN — the root of trust for every membership rule (§4.6) — to do two narrow things: `op=monitor` on a bundle, and the archive arm of `op=acquire`. A leaked daemon credential is currently a total instance compromise; scoped, it is a monitoring nuisance. DEC-5's token-exposure surface is the standing context.
status: open

## CLAIM 2026-08-04 UI
session: ui10-agent (worktree agent-a9826c34a991fc432)
opened: 2026-08-04T10:00:00Z
paths: civicos-ui/app.html (ONLY the type/state vocabulary sites of DATA-MODEL.md §2.7 change 15 and what reading them through the catalog requires: a NEW guarded CATALOG block carrying PREFIX / FIRST_STATE / HEADINGS / SCHEMA_OF / STATE_EDGES / LEGACY_TYPE_ALIASES / normalizeType / vocabFor / PHASE / PHASE_LABEL / TYPE_LABEL, moved out of the "WRITING A BUNDLE" section; SEMANTICS.types (the inquiry family, the legacy focus vocabulary kept, the alias assignment moved inside the markers, and the project/action states the guard now requires a row for); RAIL / routes / navLabel / preview titles / the rail count; ADD_TYPES; SEARCH_SCOPES; PROP_ADOPT_KINDS and the adopt flow's kind chooser; DISPOSE_LEGAL; the dual-spelling filters at reverseRefs, renderFiltered, mdFor and the disposition bar; and the member-facing prose those flows carry. NOT the strength/basis/inquiry-page surfaces UI-11 owns, NOT the act bar UI-12 owns, NOT ADD_TYPES' `action` entry UI-15 owns, NOT the docprofile package), civicos-ui/check-semantics.mjs (the D-138 half: it now IMPORTS bio-plane/checks/bio-checks.mjs and compares the catalog tables by name), civicos-ui/test/add-surface.test.mjs + civicos-ui/test/act-dispose.test.mjs + civicos-ui/test/act-proposal.test.mjs + civicos-ui/test/record-list.test.mjs (ONLY pins the collapse moved, CORRECTED with dated comments and never exempted), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 (op=list / op=search / op=promote / op=dispose / op=allocid as they stand at 5.2.0 — REC-10's normalized `object_type` projection is READ, nothing about the op shapes is changed), the check catalog `bio-plane/checks/bio-checks.mjs` as a MODULE IMPORT from the harness only (OBJECT_TYPES, LEGACY_TYPE_ALIASES, normalizeType, STATES, HEADINGS) — the browser runtime is one static file and carries a mirror, which is exactly what the new guard exists to pin
interfaces owned: none. No plane file is opened.
DISJOINT FROM REC-26 (the monitor consumer, scheduler/wrangler ground), running concurrently: this claim opens no plane source, no wrangler config, no scheduler path, and no test under bio-plane/. REC-26 opens nothing under civicos-ui/. There is no shared file.
explicitly NOT touched: bio-plane/** (every file, including checks/bio-checks.mjs — it is IMPORTED, never edited), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-10 / BUILD-ORDER.md §2 (UI-10) / DATA-MODEL.md §2.7 change list items 15 and 16 — the type in the UI and the drift guard made real, with the D-138 half NOT optional (DEC-8). Also the three PRE-EXISTING UI harness failures REC-23's landed line records, which are REC-10..16 landing residue on this ground: add-surface's "first state for inquiry matches the catalog", check-semantics' four unexplained plane states and two unfound UI states, and the drifted flattened docprofile copy.
accepts-when: node civicos-ui/test/run.mjs green INCLUDING the three inherited failures, with check-semantics.mjs reading bio-checks.mjs; a member creating a question through Add gets an INQ- bundle labelled "inquiry" and an existing FOCUS- bundle renders identically; cd bio-plane && npm run test:battery unchanged and green. NEGATIVE CONTROL run and recorded in check-semantics.mjs's own NEGATIVE CONTROL line — change HEADINGS.inquiry in bio-checks.mjs alone and check-semantics.mjs FAILS naming the drift, which today it cannot; restored after running. No push — CONDUCT integrates; landed: left for CONDUCT.
released:
amended 2026-08-04 (ui10-agent, same session), one file crossed and one finding, both found by BUILDING rather than by review: + tools/bundle-docprofile.mjs (ONE line in `ORDER`, plus the reason recorded beside it). The third inherited harness failure — "app.html's docprofile copy has drifted" — is NOT a stale paste. FW-15 added `docprofile/doctypes/meeting-agenda.mjs` to the package and to `doctypes/registry.mjs`, and did NOT add it to the bundler's ORDER list, so the flattened copy ended in `types.register(meetingAgenda)` over a symbol it had never defined. Pasting the bundler's own output between the markers — which is what the failure message instructs — produced a raw ReferenceError in EVERY UI harness at once, measured. The only correct fix is the missing ORDER entry, so this claim crossed into `tools/` for one line; nothing else in that file, and no file under `docprofile/`, was touched. WORTH KNOWING FOR THE NEXT DOCTYPE: the bundler already guards duplicate top-level names and surviving module syntax, but not an undefined reference, which is why this shipped green from the package's own side.
SCOPE JUDGMENT, for CONDUCT rather than a decision item: making the guard EXACT over the catalog meant SEMANTICS.types had to gain rows for seven states outside this item's named scope — project `matured`/`closed` and all five action states — because a guard that exempts part of the catalog is the same half-measure D-138 was. They were already reachable (ADD_TYPES offers both types) and already rendered an invented first-letter seal over an empty explanation. Reversing that is deleting seven rows and narrowing the requirement in check-semantics.mjs to the inquiry family; the cost of reversing is that the exemption is where the next drift hides.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 19fdb8d). Verified: civicos-ui all harnesses green incl. the real drift guard; battery 89/89 (4691). The seven-state semantics widening and the one cross-area ORDER line accepted. No DEC.

## CLAIM 2026-08-04 UI
session: ui11-agent (worktree agent-aef22ce87a8196963)
opened: 2026-08-04T14:00:00Z
paths: civicos-ui/app.html (ONLY: (1) the NEW `#inquiry/<id>` route and its region — `openInquiry` and every helper it owns, marked between `/*__INQUIRY_PAGE_START__*/` and `/*__INQUIRY_PAGE_END__*/`, inserted after `strataGo` and before the Search section; (2) the SHARED UNDETERMINED PRIMITIVE (C-14) inside that region; (3) `errPane` gains an OPTIONAL second argument so a pane's crumb survives its error — additive, every existing call site unchanged — plus the one `openBundle` catch line and the one `openBundle` dochead line that link a question to its page; (4) the guarded CATALOG block gains REC-11/REC-12's basis vocabulary (BASIS_ROLES / BASIS_GRADES / GRADE_AXES / GRADE_SOURCES / STRENGTH_STATES) and the member-facing word maps over them; (5) the hash-route dispatch (`routeFromHash`, the `hashchange` listener) and the `inquiry` entry in `go()`'s preview titles. NOT the Subjects screen, NOT `renderSubjectView`/`lookupSubject`/`showEntity`/`subjGradeBadge` (READ and REUSED, never edited — UI-13's ground), NOT a progression-definition surface, NOT ADD_TYPES, NOT any act bar or option map — UI-12 adds the act bar to this page next and this turn renders no option), civicos-ui/check-semantics.mjs (the new vocabulary is PINNED to the catalog the same way UI-10 pinned the rest: five arrays compared by name, plus every value covered by a member-facing word), civicos-ui/test/inquiry-page.test.mjs (NEW), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 at 5.2.0 — op=image, op=projection, op=backlinks (REC-25's gated reverse read), op=whoami. Nothing about any op shape is changed and no op is added. The check catalog bio-plane/checks/bio-checks.mjs is IMPORTED BY THE HARNESS ONLY (the browser runtime is one static file and carries the guarded mirror).
interfaces owned: none. No plane file is opened.
DISJOINT FROM UI-13, running concurrently on civicos-ui/app.html: UI-13's region is the Subjects screen (renderSubjectView and its lookups, ~app.html:5700-6000) plus a NEW progression-definition surface; this claim's region is the NEW inquiry route inserted at ~app.html:4337, the three named one-line edits, and the catalog block's new vocabulary. No function is opened by both. Whichever lands second merges origin/main into its branch and resolves.
explicitly NOT touched: bio-plane/** (every file, including checks/bio-checks.mjs — imported by the harness, never edited), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-11 / RECONCILED.md §3.1 (UI-11) / BUILD-ORDER.md §2 (UI-11) — S3 THE INQUIRY PAGE, read-only: crumb first and surviving every state; the question as authored; the phase word; WHAT IT RESTS ON with each leg's role, target and grade and a cuts_against leg counted on its own axis; STRENGTH as TWO strengths each naming its own weakest leg, no score/percentage/average/bar and no composed letter; DEC-18's inert ungraded leg; UNRATED as the boundary word (D-160); DEC-15's visible hunch and the cannot-publish note; WHAT WOULD FALSIFY IT; WHAT RELIES ON THIS through op=backlinks; C-14's shared three-line primitive with Q9's closed set of three third lines; Q12's one plane-sourced credential sentence; NO act bar.
accepts-when: node civicos-ui/test/run.mjs green with inquiry-page.test.mjs showing a mixed basis reading two strengths with two named legs, a cuts_against leg counted on its own axis, an ungraded leg named as not load-bearing while its axis still reads from graded legs, an all-ungraded basis reading UNRATED, a hunch leg visibly marked with the cannot-publish note, and a read-only credential seeing the whole page with one whoami-sourced sentence; cd bio-plane && npm run test:battery green (the plane is untouched). NEGATIVE CONTROLS run and recorded in inquiry-page.test.mjs's NEGATIVE CONTROL line — one composed letter fails; averaging fails; an ungraded-but-others-graded basis rendered as not reading fails; a free-text third line on the undetermined primitive fails; each restored after running. No push — CONDUCT integrates; landed: left for CONDUCT.
released:
amended 2026-08-04 (ui11-agent, same session), each found by BUILDING rather than by review:
(1) + THREE more one-line edits outside the region as first claimed, all in the router and all required for the route to be a route rather than a function: `navBackDo` gains an `inquiry` arm (back returns to the question page, not to the document behind it), `navLabel` gains its word, and `boot()` asks `routeFromHash()` before falling back to Home (a member who reloaded on `#inquiry/<id>`, or was handed the link, lands on it — otherwise the address bar claims one page and the screen shows another). None is inside UI-13's region.
(2) `subjGradeBadge` is DELIBERATELY NOT CALLED for a basis leg, against BUILD-ORDER §2 (UI-11)'s "reuse UI-4's subjGradeBadge, do not write a second copy", and the evidence is that function's OWN contract: `established` / `needs_confirmation` come FROM THE PLANE (op=concerns / op=connections) precisely so a letter can never decide settledness. `inquiry_basis` publishes no such flags — grade, grade_axis, grade_source and nothing else (schema.mjs) — so calling it would force this surface to INVENT a settledness the record never asserted, which is the one thing that function exists to prevent, and a resolution-sourced Grade C leg would render "Grade C · established", the exact string UI-4's suite pins as never-appearing. `basisGradeBadge` therefore reuses the markup and the CSS and states HOW from the plane's own `grade_source`, and says neither "established" nor "unconfirmed" about a leg; the harness asserts both strings are absent from every leg. THE ALTERNATIVE, if CONDUCT or Bob reads the instruction as binding: give `subjGradeBadge` a third mode (a `how` string supplied by the caller, no settledness word) — one function, two call sites, and it lands in UI-13's region rather than this one, which is the other reason it was not taken this turn.
(3) MEASURED, and it is the item's real finding: NO CONTROL-PLANE OP SERVES THE DERIVED PAIR for a working inquiry. `strengthOf()` is the authority and derives inside the Durable Object; `op=strength` and `op=basis` exist in the store's route map but are ABSENT from index.mjs's OPS whitelist. `op=projection` does not carry the five REC-12 columns (`Store.PROJECTION_COLS` omits them) and `op=search`'s hits do not either (`query.mjs`'s `PROVENANCE_COLS` omits them); query.mjs's FIELDS expose the two GRADE columns as filter/sort/facet selectors ONLY, and the two STATE columns not at all — so even a facet route could not tell UNRATED from undetermined, which is the distinction DEC-18 turns on. The pair reaches a surface in exactly three places, all narrow: frozen in a published case's own frontmatter (REC-14 stamps BOTH AXIS OBJECTS), `op=publishedcase` (published only, credential-free), and on an `op=reevaluations` obligation row (only where something moved). So the page renders the plane's axis objects wherever the plane published them and NAMES THE GAP otherwise (UI-9's precedent) — it does not derive them here, and it does not dress a surface gap up as the record's own `undetermined` (D-129's distinction, which would be a lie about the record). `inquiryPair()` is ONE seam: the day a gated control-plane read serves `strengthOf()`, that one function changes and nothing else does. THE DELEGATION IS BELOW, and UI-12 needs it more than this item did — its live pair preview cannot be built without it.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 018e44b). Verified: UI harness 23 suites + guard green, battery 89/89. The no-op-serves-the-pair finding routed as REC-34 with UI-12's deps amended; the subjGradeBadge deviation accepted with its evidence. No DEC.

## DELEGATION 2026-08-04 UI → RECORD
from: ui11-agent / UI-11 (S3 the inquiry page, read-only)
need: a GATED control-plane read that serves REC-12's derived pair for a WORKING inquiry — provisionally `op=inquirystrength?id=<bundle>`, member class, non-mutating, answering `strengthOf()`'s two axis objects unchanged: `{capture, connection}` each `{state, grade, determined, weakest, load_bearing, population, not_load_bearing[], depth_bound, detail}` and NO scalar. Three things make it more than an OPS-table line, and they are why this is RECORD's and not a UI cross:
  1. THE VIEWER GATE. The store's `strength` route is UNGATED today (`strengthOf(id)` takes no viewer), and its answer NAMES BUNDLE IDS a viewer may not be entitled to see — `weakest.target_id`, `not_load_bearing[].target_id`, `undetermined_at[].target_id`, and the `detail` sentence which spells them out in prose. Exposing it unstamped would be REC-25's leak reopened through a new door, and the redaction posture (withhold the row vs. redact the id while the record fact stands, §7.9 / REC-30) is a judgment that belongs with the gate's authors.
  2. THE STALENESS RULE. It must answer from `strengthOf()` and NOT from the five cached columns: REC-12's own comment says the cache goes stale the moment a leg beneath it is re-graded, and a surface reading the cache would show a member a strength the record no longer derives.
  3. `op=projection` and `op=search` are the WRONG carriers even so — adding the columns to `PROJECTION_COLS` / `PROVENANCE_COLS` would publish the CACHE to every list and hit row, which is the composed-scalar hazard one step from a sortable column.
why: UI-11 ships with both axes drawn and the values NAMED AS A GAP for any question that is not a published case, because deriving them on the surface would be a second implementation of the arithmetic R1/R2/DEC-18 govern — the copy that drifts, and the one place a drift would make the record claim more than it can support. UI-12 is harder blocked than UI-11: its acceptance is a LIVE pair preview that moves ONE axis as the member selects a leg, which no frozen or published shape can answer. `op=publishpreflight` (REC-15) is deferred by DEC-33 and is not this.
session: ui13-agent (worktree agent-ad74dc2b1d43f5aca)
opened: 2026-08-04T13:00:00Z
paths: civicos-ui/app.html (ONLY: (1) the NEW UI-13 block — the intent-layer write seam (`intentAsk`, `intentRefusalHtml`, `intentOptionsFrom`, `INTENT_VOCAB`/`loadIntentVocab`, `intentChooser`, `INTENT_HOLD`, `intentPreflight`, `intentPane`, `intentReceiptHtml`, `canIntentWrite`, `intentNarrationHtml`), the SUBJECTS-screen write controls (entitycreate, entityalias, relationdeclare, resolve, resolvetestify, connect) and the NEW PROGRESSIONS surface (progressiondefine, thread, discharge), inserted between UI-4's `subjConnectionsHtml` and UI-9's document-panel block; (2) `renderSubjectView` gains `#subj-new` / `#subj-acts` containers, the Q12 narration line and one `renderSubjectNew()` call; `showEntity` gains a third `skipActs` argument and one `renderSubjectActs(entity)` call — its three reads and its four render functions are otherwise UNTOUCHED; (3) the `progressions` entry in RAIL / the `R` route map / `navLabel` / the preview titles; (4) the `.intent-*` CSS block; (5) THE D-173 FIX — the new `recR()` helper and the 17 intent-layer reads rewired onto it. NOT the strength/basis/inquiry surfaces UI-11 owns, NOT the act bar UI-12 owns, NOT `renderFiltered`, NOT the Add surface, NOT the catalog block, NOT SEMANTICS), civicos-ui/test/intent-write.test.mjs (NEW — the whole item's harness, driving app.html against the REAL plane in miniflare), civicos-ui/test/subject-view.test.mjs (ONLY the mock's reply shape, CORRECTED with a dated comment and never exempted — see D-173), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — the nine intent WRITE ops (`entitycreate`, `entityalias`, `relationdeclare`, `resolve`, `resolvetestify`, `connect`, `progressiondefine`, `thread`, `discharge`) and the reads they need (`entity`, `entitybyalias`, `readingref`, `reading`, `resolutions`, `concerns`, `connections`, `progression`, `instance`, `affordances`), all as they stand. NOTHING about any op shape is changed; `bio-plane/src/store.mjs` and `bio-plane/src/index.mjs` are READ by the harness (the store's own closed vocabularies, parsed as a second instrument) and never edited.
interfaces owned: none. No plane file is opened.
DISJOINT FROM UI-11, running concurrently on `civicos-ui/app.html`: UI-11's region is the NEW `#inquiry` route and the shared undetermined component; this claim's region is the Subjects screen's WRITE controls plus the new PROGRESSIONS surface. The two touch the same file in three shared spots — the RAIL array, the `R` route map in `go()` and `navLabel` — where each adds ONE line for its own screen. Whichever lands second merges origin/main into its branch and resolves those three (the established pattern). No function is edited by both.
explicitly NOT touched: bio-plane/** (every file — `store.mjs` and `index.mjs` are READ by the harness, never written), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-13 / BUILD-ORDER.md §2 (UI-13) — a write surface for the intent layer, nine ops that had zero callers (`LAYERS.md:201` the access fact; `:279` the false belief it corrects). Unblocks REC-18's earned grades, E1's SUBJECTS route, UI-4/5/9 having content, and P-33.
accepts-when: node civicos-ui/test/run.mjs green with a harness that creates an entity with an alias, declares a justified `proxy_for` relation showing NO grade, resolves a document's reference to it, defines a progression and threads documents through it — ALL through the plane ops in miniflare, no mock of the write path; UI-4's subject view then renders non-empty for that entity; cd bio-plane && npm run test:battery stays green. NEGATIVE CONTROL run and recorded in intent-write.test.mjs's NEGATIVE CONTROL line, two arms (a grade badge on the declared relation; a surface-side option map), both restored byte-identical. No push — CONDUCT integrates; landed: left for CONDUCT.
released:
FOUND BY BUILDING, and it is the reason this item's accepts-when could not be met without it: **D-173 — every intent-layer READ in the UI forgot to open the plane's `result` envelope, so UI-4's subject view and UI-9's document panel have NEVER worked against a real plane.** `lookupSubject`, `showEntity` (entity / concerns / connections), `docEntityLabel`, `docKnowsPanel` (resolutions / connections) and `docProgressions` all read `r.entities` / `r.found` / `r.resolutions` off `{ok:true, result:{…}}`, so a live instance answers "No subject in the registry is known by …" for every subject that IS in it, and the document page's whole "what the record knows" panel renders blank. BOTH SUITES PASSED because BOTH MOCKS ANSWERED UNWRAPPED — the exact shape the plane never sends. Fixed at one seam (`recR`), the subject-view mock CORRECTED to the real envelope with a dated comment, and the new harness drives the real plane so the class cannot come back silently. This is the strongest available evidence for the item's own premise: the intent layer was not merely unreachable for WRITES, its reads were reaching nothing either.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 9b0b357; the three shared lines resolved keep-both at integration). Verified: UI harness green, battery 89/89, --strict 124/124. D-173 recorded closed (the envelope class lesson kept); D-174 the wording tension; delegations routed as REC-35/REC-36; the refusal-probe ruled a standing DEC-8 reading. No DEC.

## DELEGATION 2026-08-04 UI → RECORD
from: ui13-agent / UI-13 (the DEC-8 publication half)
need: `bio-plane/src/affordances.mjs`'s exported `VOCABULARIES` should carry the intent layer's three CLOSED vocabularies, published exactly as `action_kind` / `dispositions` / `subject_positions` already are, and IMPORTED from where they are enforced rather than copied:
  1. `entity_kinds` — `Store.#ENTITY_KINDS` (store.mjs:5288), the ten kinds D-83 reconciles;
  2. `relation_kinds` — `Store.#RELATION_KINDS` (store.mjs:5292), safeguard 4's three predicates;
  3. `stage_requiredness` — `Store.#REQUIREDNESS` (store.mjs:5881), framework §8.2's five.
They are `static #` privates today, so publishing them means exporting the sets from the store (or lifting them beside `DISPOSITIONS` in affordances.mjs and having the store import them, which is the direction REC-11 already took for `DISPOSITIONS` and is the better one — the write path then reads the published array).
why: DEC-8 says the plane publishes the refusal contract and the surface renders it. It does for bundle acts; it does not for this layer. UI-13 therefore reads these three sets out of THE OP'S OWN REFUSAL — `loadIntentVocab()` asks `op=affordances` FIRST and falls back to a deliberately-refused probe whose `detail` names the legal set ("… one of a, b, c"). That is DEC-8's other admitted source and it cannot drift from the enforcer, but it is prose-parsing, and the day a refusal is reworded the parse degrades to "the record has not said which kinds it accepts" and the control offers nothing. **The consumer is already written for the published form**: `loadIntentVocab` uses `vocabularies.entity_kinds` / `.relation_kinds` / `.stage_requiredness` the moment they appear, and the probe retires with NO change in civicos-ui. Landing this is a strict improvement with a zero-cost UI side.
status: open

## DELEGATION 2026-08-04 UI → RECORD
from: ui13-agent / UI-13 (the missing reverse index for a name-only mention)
need: a read that answers "which captured documents' readings MENTION this name" — the correspondence half of what `op=resolve` grades C. `op=readingref` matches `reading_refs.ref` EXACTLY and nothing indexes `reading_refs.label`, so the plane can answer "which documents carry the reference `contract:C-2024-88`" and cannot answer "which documents mention `Recology Hauling Contract`". A `label_norm` column (or an alias-joined read) would do it; `Store.#normAlias` is already the normalisation the recogniser matches on.
why: UI-13's resolve control offers the member the documents the plane can find — exact-reference matches plus the ones already resolved — and STATES, on the surface, that a document mentioning the subject only by name cannot be looked up this way. That is honest but it is a real hole in the workflow: a grade-C resolution is reachable today only when the same document ALSO carries an exact reference, or from the document itself (UI-9's page has no resolve control, and UI-20 owns that ground). Without this read, the C tier of §8.1 is largely unreachable from a member surface, which matters because REC-18's earned grades rest on the resolution ladder.
status: open

## CLAIM 2026-08-04 RECORD
session: rec34-agent (worktree agent-aa298435d938ebe3d)
opened: 2026-08-04T15:00:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) a NEW method `inquiryStrength({ id, viewer })` and its two private helpers, inserted immediately after `strengthOf()` and before `#writeStrengthProjection` — REC-12's derivation itself is READ and NEVER edited; (2) ONE new line in the DO dispatch map beside the existing `strength:` route), bio-plane/src/index.mjs (ONLY: (1) one OPS row `inquirystrength`; (2) one NEEDS row `inquirystrength: null` with its reason, on op=queue's precedent; (3) one entry in the REC-30 viewer-stamp list), bio-plane/src/affordances.mjs (ONLY: one NON_ACTS row with its reason — a read is not an act on an object), bio-plane/test/inquirystrength.test.mjs (NEW — this item's suite), bio-plane/test/gate-reads.test.mjs (ONLY: the new op's GATED classification row in the structural table), docs/development/CLAIMS.md (this entry)
interfaces consumed: none.
interfaces owned: I3 (the op contract — ADDITIVE: one new read op, no existing op shape changed), I5 (untouched: no schema change, no new column, no new table).
DISJOINT FROM UI-14, running concurrently on civicos-ui ground (the queue surface): that item opens no file under bio-plane/ and this claim opens no file under civicos-ui/. No shared path of any kind.
explicitly NOT touched: `strengthOf()` / `#strengthWalk` / `#axisResult` / `#weakestOf` / `#namedMember` (REC-12's derivation — the authority, read and never edited), `#writeStrengthProjection` and the five cached columns, `query.mjs` (FIELDS, PROVENANCE_COLS, viewerPredicate — the predicate is CONSUMED through the store's existing `#viewerSees`/`#bundleRedactor`, never restated), `Store.PROJECTION_COLS`, schema.mjs, checks/**, civicos-ui/**, docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-34 — `op=inquirystrength`, the GATED control-plane read for REC-12's derived pair (UI-11's delegation; UI-12's hard blocker). Member class, non-mutating, answering FROM `strengthOf()` and never from the cached columns, returning both axis objects verbatim, with REC-30's two postures: the OWNING inquiry invisible -> the whole answer withheld as an absent one (subject row), an invisible leg id inside a visible answer -> REDACTED to null while every record fact in the answer stands unchanged for every reader.
accepts-when: `cd bio-plane && npm run test:battery` green with inquirystrength.test.mjs showing both axis objects byte-equal to `strengthOf()`'s, the stale cache proven NOT consulted (a leg raised beneath moves the op while the cached column stands), a redacted leg reference with the axis facts standing, an invisible inquiry withheld whole and byte-identical to an absent one, and UNRATED distinguishable from undetermined; `npm run test:coverage --strict` exit 0 reaching the op through the control plane; NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — answer from the cached columns and the raised-leg assertion fails naming the stale read. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker a1c0c7c). Verified: battery 90/90 (4743), --strict exit 0, 125/125 ops. NC arm (c) RE-RUN BY CONDUCT (5 failures; restored byte-identical). NOT_AN_INQUIRY and the loud NEEDS+NON_ACTS pairing accepted; D-175 recorded; IC-10, I3 → 5.3.0. No DEC.
## CLAIM 2026-08-04 UI
session: ui14-agent (worktree agent-ad7df78b4ac7433a3)
paths: civicos-ui/app.html (ONLY: (1) the NEW `/*__QUEUE_START__*/…/*__QUEUE_END__*/` block — the whole S1 surface (`QUEUE_FEEDS`, `queueLoadFeed`, `renderQueue`, `queueRun`, `queueRetry`, `queuePaint`, `queueGrouped`, `queueOrder`/`QUEUE_ORDER_RULE`, `queueItemHtml`, `queueGroupHtml`, `queueAgeHtml`/`QUEUE_AGE_FORM`, `queueOptionsHtml`, `queueEntryControlsHtml`, `queueMutableKinds`/`queueMuteHtml`/`queueMuteCase`/`queueMuteReportHtml`, `queueNotices`/`queueNoticeHtml`, `queueFeedHtml`/`queueAllClearHtml`, `queueCredentialSentence`, `queueWire`), replacing the UI-8 HOME block and the UI-1 TASK INBOX block in place; (2) the DELETIONS listed below; (3) the `.q-*` CSS block, replacing the `.hm-*` one; (4) the RAIL array, the `R` route map in `go()`, `navLabel`, the preview titles and the boot line, where `home`/`tasks`/`proposals` become the ONE `queue` surface with all three keys kept as routes; (5) the retained obligation verbs `resolveTask`/`forwardTask`/`openForward`/`taskErr`, re-homed from `#tk` onto `#q`; (6) the UI-5 act dialogs' context line (`proposalContext`) and their two back-buttons. NOT the `#inquiry` route or the shared undetermined primitive (UI-11's, REUSED unchanged), NOT the catalog block, NOT SEMANTICS, NOT the Add surface, NOT `renderFiltered`, NOT the document page, NOT the Subjects/Progressions write surfaces UI-13 landed), civicos-ui/test/queue.test.mjs (NEW — the whole item's harness), civicos-ui/test/act-proposal.test.mjs (CORRECTED with dated comments, never exempted — the D-79 aggregation moved into the plane and the card became the queue row), civicos-ui/test/home.test.mjs + civicos-ui/test/task-inbox.test.mjs (DELETED with the screens they drove; what they guarded is carried into queue.test.mjs and the trade is named in its header), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=queue` (REC-20's contract as REC-21 and REC-32 extended it: the item shape, the structured `case`, `options[]`, the `mute` report block, `classes`, and CONDITION items), `op=queuemute` (REC-21), `op=tasks&status=resolved`, `op=taskresolve`, `op=taskforward`, `op=memberlist`, `op=promote` and `op=proposedispose` (the three retained proposal affordances), `op=whoami` through `PLANE.me`. NOTHING about any op shape is changed.
interfaces owned: none. No plane file is opened; `bio-plane/checks/bio-checks.mjs` is READ by `act-proposal.test.mjs` as it already was.
DISJOINT FROM REC-34, running concurrently: REC-34 adds `op=inquirystrength` in `bio-plane/src/` and touches no civicos-ui file; this claim opens no bio-plane file. There is no shared line and no merge to resolve.
explicitly NOT touched: bio-plane/** (every file), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-14 / RECONCILED.md §3.2 (UI-14) / BUILD-ORDER.md §2 (UI-14) — UI-8, UI-1 and UI-5 become ONE surface over `op=queue`, grouped by CASE, items typed by CLASS, every option from the producer. DEC-16 (one event under every ancestor; one resolution clears all; the resolver named), Q13 (longest-waiting first is the RULE and is stated), DEC-8 (no refusal computed here), DEC-36 (an out-of-view ancestor set renders the fact and nothing else), REC-21's C-3 mute wording and scope, Q12's narration clause.
accepts-when: `node civicos-ui/test/run.mjs` green with a queue harness showing an OBLIGATION and an aggregated FINDING under one case, one event under two ancestor cases clearing everywhere on one resolution with the resolver named, an ungrouped item, a named per-feed failure with NO count, a Retry re-running only the failed feed, the all-clear withheld while one feed pends, the ordering rule stated, and a CONDITION group's mute reaching conditions only; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in queue.test.mjs's NEGATIVE CONTROL block, three arms (a count for a failed feed; a resolved event leaving a stale copy under a second ancestor; a mute reaching an OBLIGATION), each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT.
FOLLOW-ON, NAMED AND NOT STUBBED: **SELECTION SCOPING — S1's states Q10, Q11 and Q12** (selecting several queue items, acting on a selection, and the scope a selection carries across surfaces). Nothing in this item reserves a control, a class or a data attribute for it, because a control that does nothing is worse than an absent one (§5's present-and-refused). It wants its own item: the plane already has a selection lease (`op=select`/`op=selection`) that UI-21's finder is built against, and whether a queue selection is THAT lease or a second, lighter thing is the design question, not a rendering detail.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 8b1e77c). Verified: UI harness all green, battery 90/90 (4744). The second-feed provisional recorded as D-176 rather than a delegation (triggers named); the act-proposal sweep correction accepted (censoring the plane is DEC-8 in reverse). No DEC.

## CLAIM 2026-08-04 UI
session: ui12-agent (worktree agent-acb5fdc80f4aa941a)
opened: 2026-08-04T18:00:00Z
paths: civicos-ui/app.html (ONLY: (1) a NEW marked region `__ACT_SOURCE__` — the one seam that reads `op=affordances`, placed immediately before `__INQUIRY_PAGE_START__`; (2) a NEW marked region `__CONCLUDE_ACT__` — the inquiry page's act bar and the conclude flow — placed immediately after `__INQUIRY_PAGE_END__`; (3) inside UI-11's `__INQUIRY_PAGE__` region, the ONE call site that paints the act bar under the strength panels; (4) the DELETION of `const DISPOSITIONS` and its three consumers rewired to the published set — `openBundle`'s `canMove`, `disposePreflight`, `openDisposeDialog`), civicos-ui/test/conclude-act.test.mjs (NEW — this item's harness), civicos-ui/test/act-dispose.test.mjs (ONLY: its mock gains an `op=affordances` answer and the harness loads the published vocabulary before driving the dialog — the CORRECTION that follows from deleting the surface-side set, never an exemption), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=affordances` (both shapes: no target for the catalogue and the vocabularies, `?target=` for one object's acts), `op=inquirystrength` (REC-34), `op=conclude` (REC-13), plus the reads UI-11 already takes (`image`, `projection`, `backlinks`, `whoami`). NOTHING about any op shape is changed; `bio-plane/**` is READ and never edited.
interfaces owned: none. No plane file is opened.
DISJOINT FROM UI-14, running concurrently on `civicos-ui/app.html`: UI-14's region is the queue/home/tasks/proposals screens; this claim's region is the `#inquiry` page's act bar and the conclude flow. The two touch the same file in one shared spot — the deletion of `const DISPOSITIONS`, which is this claim's and is named here so UI-14 does not also take it. Whichever lands second merges `origin/main` into its branch and resolves. No function is edited by both.
explicitly NOT touched: bio-plane/** (every file — `store.mjs`, `index.mjs` and `affordances.mjs` are READ to establish the store's own refusal ORDER and the published act shape, never written), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-12 / `RECONCILED.md` §3.1 (UI-12) / `BUILD-ORDER.md` §2 (UI-12) — S3's act bar, CONCLUDE through the ACT construct, every option and rung read from `op=affordances`. HARD 2's two reductions: the basis becomes a SELECTION, and the strength consequence renders LIVE as the member selects — THE PAIR, via `op=inquirystrength`, never one composed letter. DEC-8's acceptance clause: the surface renders NO refusal it computed itself.
accepts-when: `node civicos-ui/test/run.mjs` green with a conclude harness where every option, label and rung comes from `op=affordances` (and no published label, rung or disposition token is spelled in app.html); the strength panel updating as legs are selected with the PAIR moving independently (a Grade C capture selection moves capture and NOT connection); the act refused with the plane's own reason when the falsifier is empty; and NO refusal string originating in the surface. `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in the harness's `NEGATIVE CONTROL:` line, four arms — a surface-side option map; a prefilled conclusion field; one composed letter in the live preview; a surface-computed refusal — each restored byte-identical. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 0e6849e). Verified: UI harness 24 suites green, battery 90/90 (4745). The probe-over-no-probe call accepted with its stated limit; the two residues queued as UI-22. No DEC.

## CLAIM 2026-08-04 UI
session: ui20-agent (worktree agent-a80047e759dc77935)
opened: 2026-08-04T21:00:00Z
paths: civicos-ui/app.html (ONLY: (1) a NEW marked region `__CITE_ACT__` — the cite flow and the edge/retire flows — placed immediately after `__CONCLUDE_ACT_END__`; (2) inside `__ACT_SOURCE__`/`__CONCLUDE_ACT__`, the GENERALISATION UI-12's landed line asks for: `CONCLUDE_ACTS` renamed `ACTS_HERE` (the act bar now serves two surfaces), `actBarHtml` gaining its `elsewhere` argument, and four new `ACT_FLOW` entries — `cite`, `retire`, `sever`, `reinstate`; (3) inside `openBundle`, the ONE call site that paints the document page's plane-published act strip), civicos-ui/test/cite-act.test.mjs (NEW — this item's harness), civicos-ui/test/conclude-act.test.mjs (ONLY: its bootstrap line follows the `CONCLUDE_ACTS` → `ACTS_HERE` rename — a CORRECTION with its reason stated, never an exemption), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=cite`, `op=sever`, `op=reinstate`, `op=retire`, `op=select`, `op=affordances`, `op=backlinks`, `op=list`, `op=projection`, `op=image`, `op=whoami`. NOTHING about any op shape is changed; `bio-plane/**` is READ (store.mjs's `cite()`, `#edgeTransition()`, `retire()` and `affordances.mjs`'s ACTS/RUNGS, to establish the store's own refusal ORDER) and never written.
interfaces owned: none. No plane file is opened.
DISJOINT FROM REC-18 (plane, earned grades), running concurrently: REC-18 opens files under `bio-plane/src/` and `bio-plane/test/`; this claim opens no file under `bio-plane/` at all. There is no shared line and no merge to resolve.
explicitly NOT touched: bio-plane/** (every file), the `__QUEUE__` region, the `__INQUIRY_PAGE__` region's strength panels, `disposePreflight` and the PROP_DISPOSE literals (UI-22's, deliberately left), `relBar`/`dispBar`/`attestBar` (UI-2/UI-5/UI-6's surfaces — the new strip names them as carried elsewhere on the page rather than duplicating or deleting them), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-20 / `RECONCILED.md` §3.1 (UI-20) / `BUILD-ORDER.md` §2 (UI-20) — `op=cite` gets its caller (the never-built U9 half), plus `retire`/`sever`/`reinstate` call sites on the document page, each with its required reason and its rung from `op=affordances`. DEC-8: renders no refusal it computed itself. Q12 narration.
accepts-when: `node civicos-ui/test/run.mjs` green with a cite harness citing a four-document selection onto a case through `op=cite`, showing the `report` outcome per item (N cited, M retained), the note optional and bounded by the PLANE's refusal, a severed edge and a cycle-closing cite refused with the plane's own words and offenders named, and the surface reaching the plane with neither refusal composed here; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in the harness's `NEGATIVE CONTROL:` line, each restored byte-identical. No push — CONDUCT integrates; landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 9c7cd57; battery re-verified 90/90 after the disk outage — the mid-merge 66/90 was pure ENOSPC). The inquiry-cite gap queued as REC-37; the ordering rule and the three-bar residues folded into UI-22. No DEC.
## CLAIM 2026-08-04 RECORD
session: rec18-agent (worktree agent-a3abd87af16c20bd1)
opened: 2026-08-04T17:00:00Z
paths: bio-plane/src/gate.mjs (ONLY: one parameter threaded through `runGate` beside `publishedRegistry`), docs/development/INTERFACE-CHANGES.md (ONLY: the appended IC-11 proposal), bio-plane/src/store.mjs (ONLY: (1) ONE new nullable projection column `bundles.inquiry_subject_entity` in the additive ALTER loop (~:262, beside `inquiry_basis_count`) and its write inside `#writeStrengthProjection`; (2) a NEW private `#earnedBasisRegistry(subjectEntity, targetIds)` and a NEW private `#subjectEntityOf(bundleId)`, inserted beside `#strongestResolutionsFor` which they REUSE unchanged; (3) a NEW method `earnedBasis({ id, targets, viewer })` beside `publishedTargets`; (4) the fourth argument threaded into the THREE existing `checkInquiryBasis` call sites (promote ~:4808, inquirydivide pre-flight ~:3485, the gate ctx ~:10270) and the subject-entity refusal beside promote's existing supersedes-target refusal; (5) ONE new line in the DO dispatch map), bio-plane/checks/bio-checks.mjs (ONLY: (1) `checkInquiryBasis` gains a FOURTH parameter `earnedRegistry` and the EARNED arms inside its existing loop — the ONE catalog function at both gates is EXTENDED, never duplicated; (2) `checkInquiryExtension` gains the `subject_entity` scalar grammar and passes `ctx.earnedRegistry` through), bio-plane/src/index.mjs (ONLY: (1) one OPS row `earnedbasis`; (2) one NEEDS row with its reason; (3) one entry in the REC-30 viewer-stamp list), bio-plane/src/affordances.mjs (ONLY: one NON_ACTS row — a read is not an act on an object), bio-plane/test/earnedbasis.test.mjs (NEW — this item's suite), bio-plane/test/gate-reads.test.mjs (ONLY: the new op's classification row in the structural table), bio-plane/test/basis.test.mjs (CORRECTED, never exempted, with a dated comment where REC-11's leg fixtures predate the earned rule), docs/development/CLAIMS.md (this entry)
interfaces consumed: none.
interfaces owned: I3 (the op contract — ADDITIVE: one new read op; no existing op shape changed), I5 (the store schema — ADDITIVE: one nullable projection column on `bundles`, NO new table, no re-key, nothing in schema.mjs's literal).
DISJOINT FROM UI-12 (the act bar), running concurrently on civicos-ui ground, IN PATHS — this claim opens no file under civicos-ui/ and UI-12 opens none under bio-plane/, so there is no shared line and no merge to resolve. **NOT disjoint in CONTRACT, and that is corrected here rather than left standing:** adding `capture` to the catalog's `GRADE_SOURCES` makes `civicos-ui/check-semantics.mjs` fail by design (the array mirror drifts, and `GRADE_SOURCE_WORD` has no member-facing word for it). The guard firing IS the mechanism working. Filed as **IC-11** in `INTERFACE-CHANGES.md` with UI listed as a LIVE consumer (PROPOSED, not recorded post-hoc as accepted, because UI can answer), and delegated below. RECORD does not edit `app.html`; the migration is two lines and is UI's.
explicitly NOT touched: `strengthOf()` / `#strengthWalk` / `#axisResult` (REC-12's derivation — READ, never edited), `resolveReferences` / `testifyResolution` / `#upsertResolution` (FW-7's recogniser — READ, never edited), `#strongestResolutionsFor` (REUSED verbatim), `deriveConnections`, `publishedRegistryFor` / `checkInheritedLeg` (REC-14's inheritance rule — untouched), schema.mjs (no table, no literal edit), query.mjs, civicos-ui/**, docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-18 / RECONCILED.md §3.1 (REC-18) / BUILD-ORDER.md §2 (REC-18) / DATA-MODEL.md D1(b) — EARNED basis grades. A document leg's CONNECTION grade with `grade_source: 'resolution'` is computed SERVER-SIDE as the strongest resolution of that document's captures to the inquiry's SUBJECT ENTITY, admitting A/B/C only; `grade_source: 'testimony'` is a member's act, always D, author and date carried; the split is enforced by the WRITE PATH on the `schema.mjs:739-743` recogniser precedent — the machine never mints a D and a caller never hands the server an earned grade. DEC-15's lifecycle: the earned path is what a HUNCH is cleared INTO. DEC-23's provisional stands (target is an INFO-/INQ- id). SUBJECT-ENTITY LINKAGE: one OPTIONAL `subject_entity: ENT-YYYY-NNNN` scalar on the inquiry, projected to ONE nullable column — no new table, and optional because DEC-15 rules that an inquiry with no subject entity simply has no A/B/C available to it.
accepts-when: `cd bio-plane && npm run test:battery` green with earnedbasis.test.mjs showing a document leg resolving to the subject entity at grade A through `op=resolve` carrying `grade_source: 'resolution'` and grade A on `inquiry_basis`; a member's testimony on the same leg carrying `'testimony'` and D and refused at any other grade; a hunch cleared by resolution reading `'resolution'` with the bias debt settled and the hunch's author+date still readable in the prior revision's bytes; a leg's capture grade coming from the capture record and never authored; `npm run test:coverage --strict` exit 0 reaching the new op through the control plane; NEGATIVE CONTROL run and recorded in the suite's NEGATIVE CONTROL line — let the write path accept a caller-supplied A on a testimony leg and the suite fails. No push — CONDUCT integrates.
landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 541aedd). Verified post-outage from the main checkout: battery 91/91 (4799), --strict 126/126, UI harness green after the IC-11 two-line migration applied at integration (the guard fired by design). D-177/178/179 recorded; I3 → 5.4.0. No DEC.

## DELEGATION 2026-08-04 RECORD → UI
from: rec18-agent / REC-18 (the earned basis grades)
need: two lines in `civicos-ui/app.html` — add `"capture"` to `GRADE_SOURCES` (~:1135), and give it a `GRADE_SOURCE_WORD`. `civicos-ui/check-semantics.mjs` currently FAILS against the worktree catalog with the exact message pair: `GRADE_SOURCES has drifted from the catalog` and `GRADE_SOURCE_WORD has no member-facing word for 'capture'`. It is a guard, not a break: it exists so a new grade source cannot reach a member surface as a blank sentence, and it named the whole migration for you.
why: REC-18 makes the CAPTURE axis earned from the capture record rather than authored, and before this there was no source name meaning that — all four existing sources were sources for a CONNECTION grade, so a capture-axis leg had to borrow a connection word and nothing checked its value against the record. The measured consequence was that a member could type capture grade A beside a document, against the landed doctrine that grade A needs a chain-of-custody archive this plane cannot produce and does not claim (CAPTURE-FIDELITY.md, RECONCILED R2-e/R2-g).
wording, as a suggestion and not a ruling (the member-facing word is UI's): something that says what the grade IS — a fact about how the bytes reached us, held by the record — e.g. "from the capture record". It must NOT read as a member's assertion, or it collapses into `testimony`'s meaning. And `capture` and `resolution` must NOT be rendered as one "earned" badge: they are earned from two different things over two different populations, and DEC-21 is that the axes are never composed.
also available, and UI-20/UI-21 will want it: `op=earnedbasis&id=<inquiry>[&targets=a,b,c]` answers what each candidate leg earns BEFORE it is written — `{subject_entity, subject_label, earned: {connection: {<target>: {grade, mode: 'value', why}}, capture: {<target>: {grade, mode: 'ceiling', why, ceiling}}}}`. It answers from the SAME store function op=promote enforces with, so what it reports is what the write will accept. A cite flow should FILL the leg from it rather than offering a grade control the member can type into.
filed as: IC-11 in `INTERFACE-CHANGES.md` (PROPOSED — UI is a LIVE consumer here, so silence is not consent).
status: open

## CLAIM 2026-08-04 UI
session: ui15-agent (worktree agent-a4684eeed60d588f9)
opened: 2026-08-04T23:20:00Z
paths: civicos-ui/app.html (ONLY: (1) `buildRail` — the rail's Add entry becomes CONDITIONAL on `contribute`, F-7; (2) the ADD region — `ADD_TYPES` loses its `action` entry (the 2026-08-03/04 amendment, UI-19 restores it), a NEW `ADD_TICKS` declaration beside `ADD_BUSY` (D-132), `renderAdd`'s type list filtered on `create_projects` (F-6) with one new `canDo` helper beside `canContribute`, the DEAD duplicate copies of `heldMatch` and `addCapture` and their duplicated comment block DELETED (D-133), and `addIncomplete` rewritten to render the A4 frame; (3) `mdFor`'s D-130 comment — one dated correction of the sentence that assigned `action_kind`/`risk_tier` to this item, no code change on that line), bio-plane/src/setup.mjs (ONLY: the `#n-type` select loses its `Action` option, one line, dated comment naming UI-19 — the plane's OWN intake surface, and the same amendment), civicos-ui/test/add-surface.test.mjs (this item's harness: the A4 arm, the absence assertions, the ADD_TICKS declaration-count assertion, and the CORRECTION with a dated reason of the three superseded no-dialog pins — never exempted), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=acquire` (its `snapshot.complete` / `outstanding` / `continuation.session` shape), `op=whoami` (`capabilities`), `op=allocid`, `op=promote`, `op=attest`, `op=search`, `op=capture`. NOTHING about any op shape is changed and no plane behaviour is touched: `bio-plane/src/setup.mjs` is a SERVED PAGE, not an op.
interfaces owned: none.
DISJOINT FROM REC-37 (plane, cite-to-inquiry), running concurrently: REC-37 opens `bio-plane/src/store.mjs`, `index.mjs`, `affordances.mjs`, `checks/bio-checks.mjs` and its own `bio-plane/test/` suite; this claim opens exactly one file under `bio-plane/` — `src/setup.mjs`, the installer's served setup page — which REC-37 does not name and which carries no op. No shared line, no merge to resolve.
explicitly NOT touched: bio-plane/src/store.mjs · index.mjs · affordances.mjs · checks/** · test/** (READ only, to establish that no suite pins the offered option set), `disposePreflight` and the PROP_DISPOSE literals and relBar/attestBar/dispBar (UI-22's, deliberately left), the `__INQUIRY_PAGE__` and `__CITE_ACT__` regions, `unfinishedBanner`/`continueCapture` (the document page's half of the unfinished capture — READ, never edited), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-15 / `BUILD-ORDER.md` §2 (UI-15) — D-132 (`ADD_TICKS` used twice, declared nowhere, so the ordinary ceilinged-capture path throws a raw ReferenceError and the A4 frame has never appeared), D-133 (`heldMatch`/`addCapture` each declared twice, one body dead), F-6 (the `project` option present-and-refused), F-7 (the rail Add entry present-and-apologised-for), and the 2026-08-03/04 amendment (the `action` option ABSENT from both intake surfaces until UI-19). D-130's UI half is DONE by REC-23 and is VERIFIED here, not re-fixed.
accepts-when: `node civicos-ui/test/run.mjs` green with an add harness driving a capture that hits the ceiling and rendering the A4 choice (record as unfinished / write nothing, with "recording it as complete is not on offer"), and `grep -c "ADD_TICKS" civicos-ui/app.html` matching its declaration count; the project and action options asserted ABSENT per their rules; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROL run and recorded in the harness's `NEGATIVE CONTROL:` line — remove the `ADD_TICKS` declaration and the harness reproduces the raw ReferenceError. No push — CONDUCT integrates.
landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker d397ab5). Verified: harness 25 suites green, battery 91/91 (4802). The A4 reconciliation accepted (the handover's own acceptance outranks the July deletion, and the rebuilt frame asks the record question); the state doc amended D-160-style; the no-action-intake consequence named on the landed line. No DEC.
## CLAIM 2026-08-04 RECORD
session: rec37-agent (worktree agent-aa5ef30d34cde6a3c)
opened: 2026-08-04T23:00:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) `cite()` — WIDENED in place, the citing object may now be an inquiry and the leg lands on `basis[]` through the document; (2) a NEW `static #spliceBasis(text, legs)` inserted immediately after `#spliceReferences`, which it mirrors; (3) the `cite:` row of the DO dispatch map gains `role`), bio-plane/src/affordances.mjs (ONLY: (1) the `cite` ACTS entry gains `inquiry` to its `types`/`applies` and a type-neutral label; (2) `VOCABULARIES` gains `basis_roles`, imported from the catalog that enforces it, never copied), bio-plane/test/citeinquiry.test.mjs (NEW — this item's suite), bio-plane/test/cite.test.mjs (CORRECTED, never exempted, with dated comments where its NOT_A_PROJECT fixture predates the widening), bio-plane/test/affordances.test.mjs + bio-plane/test/conclude.test.mjs + bio-plane/test/divide.test.mjs + bio-plane/test/reopen.test.mjs (CORRECTED, never exempted, each with a dated comment: five suites pin an inquiry's EXACT published act list, and `cite` joins every one of them because the widened guard is type-only — the state-machine acts each assertion is really about are unchanged), docs/development/INTERFACE-CHANGES.md (ONLY: the appended IC-12 proposal), civicos-ui/test/cite-act.test.mjs (UI-20's OWN harness, driven against the widened plane: its mock plane mirrors the widened `cite()` refusal order, and its gap-naming assertions are CORRECTED with dated comments because the gap is closed — never exempted), docs/development/INTERFACE-CHANGES.md (ONLY: the appended IC-12 note), docs/development/CLAIMS.md (this entry)
interfaces consumed: none.
interfaces owned: I3 (the op contract — ADDITIVE and PERMISSIVE: `op=cite` accepts an inquiry as the citing object and inquiry members on that arm; three NEW refusal reasons on the new arm — `NOT_CITABLE`, `NO_ROLE`/`BAD_ROLE`, `ROLE_NOT_APPLICABLE`; `NOT_A_PROJECT` narrows permissively and keeps its name; nothing previously admitted is now refused and no existing reason is renamed), I5 (the store schema — NOT CHANGED: no table, no column, no index; `inquiry_basis` is written only through the promote projection REC-11 built).
DISJOINT FROM UI-15 (the E3 ADD defect batch), running concurrently on civicos-ui ground: UI-15's paths are `civicos-ui/app.html` (~:6458 ADD_TYPES) and `civicos-ui/setup.mjs`; **this claim opens NEITHER — it opens no surface file at all**, only `civicos-ui/test/cite-act.test.mjs`, which UI-15 does not name. There is no shared line and no merge to resolve.
explicitly NOT touched: `civicos-ui/app.html` (the item's own clause: UI-20's surface needs NO edit when this lands, and driving its harness against the widened plane is how that is asserted), `civicos-ui/setup.mjs`, `bio-plane/src/schema.mjs` (no table, no column), `checks/bio-checks.mjs` (`checkInquiryBasis` and `checkEarnedLeg` are the ONE grammar at both gates and are REUSED verbatim — the widened act writes legs they already judge), `promote`'s basis arm and the R3 DAG guard (`#basisCyclePath` — REUSED, and it is what must refuse a cycle), `earnedBasisRegistry` (REUSED verbatim — the widened act FILLS from the same function op=earnedbasis answers from), `sever`/`reinstate`/`retire`/`#edgeTransition`, docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-37 — cite-to-inquiry, the plane half of the record-becomes-a-case edge measured missing by UI-20. The citing object may be an INQUIRY; on that arm members may be information OR inquiry; the legs land on `inquiry_basis` THROUGH THE DOCUMENT (splice `basis[]` + `references[]`, then promote), so D-21 is intact and REC-11's DAG guard and `checkInquiryBasis`'s one-function grammar are what refuse. The earned grade FILLS from `earnedBasisRegistry`; a target the record holds no A/B/C resolution for lands UNGRADED — undetermined and stated (DEC-18's inert shape), never a guessed grade and never a grade control. `op=affordances` publishes the widened act and the basis-role vocabulary through the one edge-table source. D-168: permissive on retired targets.
accepts-when: `cd bio-plane && npm run test:battery` green with a suite citing a document AND another inquiry onto an inquiry through the widened path, the legs landing on `inquiry_basis` through the document (the document's own `basis:` block asserted to carry them), a cycle-closing cite refused AT THE WRITE naming the FULL path, and the earned grade filled from the same store function `op=earnedbasis` answers from; `node civicos-ui/test/run.mjs` green with UI-20's harness driven against the widened plane and NO surface edit; `npm run test:coverage --strict` exit 0; NEGATIVE CONTROLS run and recorded in the suite's NEGATIVE CONTROL line — bypass the document (write the leg directly) and the projection-of-the-document assertion fails; permit the cycle and the DAG suite fails. Restored after each. No push — CONDUCT integrates.
landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker d3f8126). Verified: battery 92/92 (4852), --strict 126/126, harness green with app.html byte-untouched. IC-12 answered AGREE for UI (proxy, named) and CHANGED at I3 5.5.0; the delegation folded into UI-21. The widen decision and reason-code restraint accepted with their recorded reversals. No DEC.

## DELEGATION 2026-08-04 RECORD → UI
from: rec37-agent / REC-37 (cite-to-inquiry — the plane half of the record-becomes-a-case edge)
need: two things in `civicos-ui/app.html`, both ADDITIVE, neither urgent enough to break anything today. (1) `citeCandidates()` (~:5474) filters the record's own `object_type` to `project`, so a QUESTION is never offered as a citing object — `op=affordances` now publishes `cite` for `["information","project","inquiry"]` and the plane accepts a question as the citing object, so the filter should admit an inquiry as a citing END while still excluding the information end it was really written for. (2) the cite flow sends no `role`, so a cite onto a question reaches the plane and is refused `NO_ROLE`; the flow needs the role control, offered from the PUBLISHED vocabulary (`op=affordances` → `vocabularies.basis_roles`) and never from a surface-side copy.
why: an inquiry's basis is the only way a record becomes a case, and until REC-37 no op appended a leg to it. UI-20 measured that gap and proved it negatively; the plane half now exists. The surface reaching the plane and rendering `NO_ROLE` verbatim is DEC-8 working exactly as intended — this delegation is what turns a correctly-refused act into a completable one.
measured, so nobody re-derives it: `civicos-ui/test/cite-act.test.mjs` was driven against the widened plane with NO surface edit and is GREEN at 131 assertions (was 116). Its gap-naming assertions are corrected with dated comments — the two above are now asserted POSITIVELY as measured surface gaps rather than as rules, so when the surface catches up those two assertions are the ones to correct next, and they say so.
do NOT: offer a grade control anywhere in the flow. `op=cite` FILLS the leg's connection grade from `earnedBasisRegistry` — the same function `op=earnedbasis` answers from — and returns `legs[]` carrying the grade the record earned, `null` where it earned nothing, and the registry's own `why`. A target with no A/B/C resolution lands UNGRADED and that is the honest state (DEC-18's inert leg), not a field to fill in.
also available: the widened act's success shape on the question arm — `{citingObjectType:"inquiry", role, legs:[{target, role, grade, grade_axis, grade_source, why}], gradesFilled, gradesUndetermined}` — so a receipt can state per item what landed and how much of it is not yet load-bearing, which the plane's own Session Log entry also records.
filed as: IC-12 in `INTERFACE-CHANGES.md` (PROPOSED — UI is a LIVE consumer, so silence is not consent).
status: open

## CLAIM 2026-08-04 UI
session: ui16-agent (worktree agent-ad081ea4bf3dd7432)
opened: 2026-08-04T23:59:00Z
paths: civicos-ui/app.html (ONLY: (1) inside the guarded `__CATALOG__` block, TWO new tables — `WORKPRODUCT_STATES` and `WORKPRODUCT_WORD` — mirroring `checkProjectExtension`'s own `WS` list, pinned by check-semantics.mjs; (2) the router's `projects:` arm — `renderFiltered("projects",…)` DELETED and replaced by a NEW `renderProjects` whose rows open the workspace; (3) a NEW marked region `__PROJECT_WORKSPACE__` placed immediately after `__INQUIRY_PAGE_END__` — `renderProjects`, `openProjectWorkspace`, the participants/arithmetic/ladder/contents panels, the seven roster-act call sites and their one generic dialog, and the `#project/<id>` route; (4) `navBackDo` gains one `project` arm; (5) inside the UI-3 BALLOT region, `openBallotDialog`/`doBallot` are CORRECTED for D-173 — the three ops they read were read OFF THE ENVELOPE and a store refusal arrives as a VALUE, not a throw; (6) the UI-7 MEMBERS region — `MM_OWNERSHIP_CAP`, `memberOwnership`, `memberOwnsHtml`, the roster's `Owns` column and the `op=projectownerarith` read/divergence half of `governanceHtml` DELETED, and `op=adminarith`'s own D-173 envelope corrected), civicos-ui/check-semantics.mjs (ONLY: one new textual pin of `WORKPRODUCT_STATES` against `checkProjectExtension`'s `WS` literal, the same shape the existing `knownSchemas` pin takes), civicos-ui/test/project-workspace.test.mjs (NEW — this item's harness), civicos-ui/test/act-ballot.test.mjs (CORRECTED, never exempted, with a dated reason: its mock answered the three project ops UNWRAPPED while the plane wraps every DO answer, so the suite agreed with itself and the dialog could not work live), civicos-ui/test/members-roster.test.mjs (CORRECTED, never exempted: the ownership fan-out and divergence assertions are DELETED because the surface is deleted, and the `adminarith` mock is wrapped), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=projectparticipants`, `op=projectownerarith`, `op=projectownerremove`, `op=projectinvite`, `op=projectjoin`, `op=projectleave`, `op=projectremove`, `op=projectowneradd`, `op=projectownerrescue`, `op=projectfork`, `op=list`, `op=projection`, `op=image`, `op=backlinks`, `op=whoami`. NOTHING about any op shape is changed; `bio-plane/**` is READ (store.mjs's project methods and `#viewerSees`, index.mjs's `by`/`viewer` stamps, affordances.mjs's NON_ACTS rows for the roster acts, checks/bio-checks.mjs's `checkProjectExtension`) and never written.
interfaces owned: none. No plane file is opened.
DISJOINT FROM REC-37 (plane, cite-to-inquiry), running concurrently: REC-37 opens `bio-plane/src/store.mjs`, `index.mjs`, `affordances.mjs`, `checks/bio-checks.mjs` and its own `bio-plane/test/` suite; this claim opens NO file under `bio-plane/` at all. There is no shared line and no merge to resolve.
explicitly NOT touched: bio-plane/** (every file), `disposePreflight` and the PROP_DISPOSE literals and relBar/attestBar/dispBar (UI-22's, deliberately left), `ballotPreflight` itself (its gates read the plane's computed `live` row; its SENTENCES are the surface's own and are a FOURTH pre-DEC-8 residue — named for CONDUCT, not rewritten here, because rewriting it is UI-22's mechanism question and not this item's), the `__CITE_ACT__` and `__QUEUE__` and `__ADD__` regions, `renderFiltered` itself (UI-21 deletes it; only its Projects ARM goes here), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-16 / `BUILD-ORDER.md` §2 (UI-16) — E4 THE PROJECT WORKSPACE, and D-134: a complete tested BALLOT act that nothing outside its own test can reach. A project ROW opens a workspace; the workspace shows the objective, the `workproduct_state` readiness ladder advancing on RECORDED evaluations, WHO IS WORKING ON THIS from ONE `op=projectparticipants` call, and the ownership arithmetic and two-owner divergence MOVED here from the Members screen where there is no act. THE CONSTRAINT: the workspace lists the inquiries and actions inside it and shows NO strength, NO grade, NO conclusion — take the CONTAINER side. **DEC-8** — renders no refusal it computed itself. **Q12** narration.
accepts-when: `node civicos-ui/test/run.mjs` green with a workspace harness where the ballot dialog is reached FROM THE WORKSPACE (not from a test), the denominator comes from `op=projectownerarith`'s `live` row, an uninvited member sees no trace of the project anywhere, an invited-not-joined member sees the skeleton and NOT the participant list, and no strength or grade appears on the page; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in the harness's `NEGATIVE CONTROL:` line — render an inquiry's grade in the workspace and the harness fails; render a surface-worded refusal (right code, invented sentence) and it fails; each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker b88fd8f). Verified: harness 26 suites green, battery 92/92 (4852) combined with REC-37. ballotPreflight and the routing gap folded into UI-22; the D-173 class sweep queued as UI-23; the withdrawn delegation accepted. No DEC.

## CLAIM 2026-08-04 RECORD
session: rec35-agent (worktree agent-abca1989293b6f3fa)
opened: 2026-08-05T00:20:00Z
paths: bio-plane/src/affordances.mjs (ONLY: three new exported arrays `ENTITY_KINDS`, `RELATION_KINDS`, `STAGE_REQUIREDNESS` beside `DISPOSITIONS`, and their three entries in `VOCABULARIES`), bio-plane/src/store.mjs (ONLY: the affordances import clause gains the three bindings; `Store.#ENTITY_KINDS` / `Store.#RELATION_KINDS` / `Store.#REQUIREDNESS` stop holding literals and are DERIVED from the imported arrays; `defineProgression`'s BAD_REQUIRED detail stops transcribing the five words and joins the imported array), bio-plane/test/affordances.test.mjs (the three drift assertions — over the import AND over the wire — plus the structural no-literal-copy pin, the DISPOSITIONS precedent), civicos-ui/app.html (COMMENT ONLY, no executable byte: the UI-13 intent-layer header's "the plane enforces but does not yet publish" clause is now false and is corrected with a dated note; `loadIntentVocab`, `intentOptionsFrom`, `take()` and INTENT_HOLD are UNCHANGED), civicos-ui/test/intent-write.test.mjs (CORRECTED, never exempted: the second instrument parsed the sets out of `store.mjs`'s private statics, which no longer hold them; the probe-path pin `["published","refusal"]` is now `"published"` and the probe's deadness is asserted), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 — op=affordances' no-target answer gains three `vocabularies` keys. ADDITIVE: no existing key changes shape, no refusal string changes, no op changes its contract.
interfaces consumed: none changed.
DISJOINT FROM UI-22 (surface residue batch), running concurrently on civicos-ui ground. UI-22's regions are `disposePreflight` (the inquiry page's sibling flow), `ballotPreflight`, the PROP_DISPOSE_VERB/PROP_DISPOSE_DONE literals in the proposals arm, relBar/attestBar/dispBar, and the `__QUEUE__` region's [data-open] routing. This claim's ONLY civicos-ui contact is (a) the UI-13 intent-layer comment block immediately above `const INTENT_PROBE` (comment text only) and (b) `civicos-ui/test/intent-write.test.mjs`, UI-13's own suite, which UI-22 does not name. No shared line, no shared function, no shared suite.
explicitly NOT touched: every other bio-plane/src file, checks/bio-checks.mjs, schema.mjs, `Store.#REQUIRED_FIRES` (a policy set, not a vocabulary — DEC-9's, deliberately left), every other civicos-ui suite, `SUBJ_REL_PHRASE` (UI-4's rendering phrases), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-35 (UI-13's delegation) — publish the intent layer's three vocabularies in `VOCABULARIES`, IMPORTED from the one place each set lives so a change in one place moves both the enforcement and the publication. `basis_roles` was checked FIRST and is already published by REC-37 with its own drift assertion (`bio-plane/test/citeinquiry.test.mjs`), so it is CONFIRMED, not re-added.
accepts-when: `cd bio-plane && npm run test:battery` green with entity_kinds/relation_kinds/stage_requiredness in op=affordances' no-target answer asserted equal to the store's enforced sets VIA THE IMPORT; `node civicos-ui/test/run.mjs` green with the options read from the publication (source === "published", the probe path dead); `npm run test:coverage --strict` exit 0; NEGATIVE CONTROL run and recorded in the suite's `NEGATIVE CONTROL:` line — publish one vocabulary as a literal copy instead of the import and the drift assertion fails. Restored after. No push — CONDUCT integrates.
landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 329d44d). Verified: battery 92/92 (4859), harness 26 suites green, --strict 126/126. The TDZ-forced direction accepted as the DISPOSITIONS precedent; the probe-as-check reasoning accepted; the instrument repoint (enforcing ops' own refusals) accepted. IC-13, I3 → 5.6.0. No DEC.
## CLAIM 2026-08-05 UI
session: ui22-agent (worktree agent-ac71cc066ab28f2d8)
opened: 2026-08-05T00:30:00Z
paths: civicos-ui/app.html (ONLY the FIVE regions below, each named precisely so a concurrent session can see the boundary):
  (1) THE DISPOSE FLOW — `disposePreflight` (~:6575) DELETED and replaced by `disposeProbe()`/`DISPOSE.pf`, plus `openDisposeDialog`, `disposeChoose`, `disposeValidate` (renamed `disposePaint`), `doDispose` and the `dz-*` markup they own. NOT `disposeReceipt`, NOT `disposeRefusalHtml`, NOT `DISPOSE_VERB_WORD`/`disposeVerb`/`dispositions()` (UI-12's published-vocabulary reader, REUSED verbatim);
  (2) THE PROPOSAL-DISPOSITION ARM — `PROP_DISPOSE_VERB` and `PROP_DISPOSE_DONE` (~:9241) DELETED, and their five call sites in `openProposalAct`, `proposalActChoose`, `proposalActValidate` (renamed `proposalActPaint`), `doProposalDispose` and `proposalDisposeReceipt` rewired onto UI-12's `disposeVerb`/`disposeStateWord`/`dispositions()`; `proposalDisposePreflight` DELETED. NOT `proposalAdoptPreflight` (UI-5's adopt flow — a different act, untouched), NOT `proposalContext`, NOT `proposalGradeHtml`/`proposalDerivedBadgeHtml`;
  (3) THE DOCUMENT PAGE'S THREE BARS — inside `openBundle` ONLY, the `relBar` / `dispBar` / `attestBar` blocks (~:4386-4434) and the ONE `actsFor(id)` call (~:4454) MOVED above them so all four read one answer. NOT the four strata, NOT `docKnows`, NOT `seals`, NOT `errPane`, NOT `actBarHtml` (UI-12/UI-20's, READ and reused);
  (4) THE BALLOT PRE-FLIGHT — `ballotPreflight` (~:6856) becomes `ballotNeeds` (the gates STAY: they read `op=projectownerarith`'s live row), its five `need:` sentences and its `refusal` composition DELETED, and `ballotValidate` (renamed `ballotPaint`) + `doBallot`'s early return rewired. NOT `ballotDenominator`/`ballotDenominatorHtml`/`ballotDivergenceHtml` (they render the plane's own `live`/`table`/`why` and are correct), NOT `ballotReceipt`, NOT `openBallotDialog`'s two D-173-corrected reads (UI-16's, verbatim);
  (5) THE QUEUE'S ROUTING — inside `/*__QUEUE_START__*/…/*__QUEUE_END__*/` ONLY: a NEW `queueOpen(id, type)` beside `queueWire`, the `data-open`/`data-type` attributes in `queueSubjectHtml` and `queueOptionsHtml`, the openable case heading in `queueGroupHtml`, and the ONE `queueWire` line that binds them. NOT `queueGrouped`, NOT `queueOrder`, NOT the mute block, NOT the feed loaders.
  civicos-ui/test/act-dispose.test.mjs (this item's harness for (1) — CORRECTED with dated comments, never exempted), civicos-ui/test/act-proposal.test.mjs (harness for (2), same discipline), civicos-ui/test/document-page.test.mjs + civicos-ui/test/document-structure.test.mjs + civicos-ui/test/cite-act.test.mjs (the THREE suites that pin the bars — CORRECTED with dated comments, never exempted), civicos-ui/test/act-ballot.test.mjs (harness for (4)), civicos-ui/test/queue.test.mjs (harness for (5)), civicos-ui/test/release-flow.test.mjs + civicos-ui/test/act-attest.test.mjs (only where a bar's availability rule moved), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=affordances` (both shapes), `op=dispose`, `op=select`, `op=proposedispose`, `op=projectownerarith`, `op=projectownerremove`, `op=projectparticipants`, `op=attest`, `op=release`, `op=queue`, `op=whoami`. NOTHING about any op shape is changed; `bio-plane/**` is READ (store.mjs's `dispose()`, `release()`, `proposeDispose()`, `projectOwnerRemove()` and `#queueAncestors`, affordances.mjs's ACTS/NON_ACTS/RUNGS, index.mjs's `decorateAct`) and never written.
interfaces owned: none. No plane file is opened.
DISJOINT FROM REC-35 (plane, the intent-layer vocabularies), running concurrently: REC-35 opens `bio-plane/src/affordances.mjs` and its own `bio-plane/test/` suite, and its ONE civicos-ui contact is `loadIntentVocab`'s options-probe path (~:8275) plus the harness pin over it. THIS CLAIM OPENS NEITHER — `loadIntentVocab`, `INTENT_VOCAB`, `intentAsk`, `intentPreflight` and `civicos-ui/test/intent-write.test.mjs` are named here as NOT TOUCHED so the boundary is stated from both sides. There is no shared line and no merge to resolve.
explicitly NOT touched: bio-plane/** (every file), `loadIntentVocab`/`INTENT_VOCAB`/the whole UI-13 intent-layer block, `civicos-ui/test/intent-write.test.mjs`, the `__ACT_SOURCE__` / `__CONCLUDE_ACT__` / `__CITE_ACT__` / `__INQUIRY_PAGE__` / `__PROJECT_WORKSPACE__` / `__ADD__` / `__CATALOG__` regions' bodies, `attestPreflight` (a THIRD-party pre-flight over a capture, not over a bundle act — see the DELEGATION below), `civicos-ui/check-semantics.mjs`, docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-22 — the five pre-DEC-8 refusal residues UI-12, UI-20 and UI-16 accumulated. Every flow ends rendering ONLY plane-worded refusals, asserted the arm-(d) way per site (correct reason code + invented sentence FAILS, naming the surface as author). DEC-8's acceptance clause; Q12 unchanged; DEC-10's grouping key made load-bearing by the routing chore.
accepts-when: `node civicos-ui/test/run.mjs` green with all five sites' pre-flights rendering no surface-worded refusal; commit controls ABSENT-not-greyed while inputs are unfilled; a project case routing to the workspace and an inquiry case to the inquiry page, asserted; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded per site in each harness's `NEGATIVE CONTROL:` block — restore disposePreflight's worded refusal and the harness fails naming the SURFACE as author; break the type routing and the harness fails naming the wrong destination; each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT.
released:
paths AMENDED 2026-08-05, mid-item, because the work reached two lines the opening claim did not name and a claim that quietly grows is worse than one that is corrected: (6) `boot()` (~:900) gains ONE awaited line — `await loadActSource(true)` — and its comment; the seam had no caller anywhere in the file, which made chores (1)-(3) unreachable against the real application (the finding is on the landed line). (7) the `.q-case-open` CSS rule beside `.q-case-kind` (~:263), two declarations, because chore (5) turns a heading into a control and a control needs to look like one. Nothing else moved outside the five regions; every diff hunk in `app.html` sits in one of the seven.
landed: left for CONDUCT. Measured this turn, so nobody re-derives it: harness 26 suites green (act-dispose 77, act-proposal 75, act-ballot 78, queue 133, cite-act 131, project-workspace 112, document-page +UI-22 bar block), battery 92/92 (4852) — no plane file opened. THE FIVE, and which shape each took: (1) `disposePreflight` DELETED and replaced by a WITHHELD-HANDLE PROBE — the item's scope predicted no analogue existed, and `store.mjs dispose()` says otherwise: it judges `to`, NO_REASON and BAD_REASON BEFORE `selectionResolve`, which is retire's ordering exactly (UI-20's rule, "retire yes"), so a call carrying the real token and the real reason with `handle:""` reaches the plane's own words and can never move anything; the arms below the selection are pre-flighted by PUBLICATION and worded by the plane at commit; `DISPOSE_REASON_MAX`/`DISPOSE_REASON_RULE` (the store's grammar, copied) and `disposeLegal`/`DISPOSE_LEGAL` (the state machine, read as an act rule) and `canDispose` and `disposeRefusalHtml` all DELETED with it. (2) the PROP_DISPOSE literals: NO-REFUSAL SHAPE, no delegation needed — `op=proposedispose` judges the progression and stage keys FIRST, so no field is both withholdable and last, and the VERBS were never the hard half (it imports the same published DISPOSITIONS array, so `dispositions()` + UI-12's `disposeVerb` serve both surfaces). (3) relBar/dispBar: AVAILABILITY FROM `op=affordances`, one read moved above the three sections, labels the record's own, and BOTH surface-worded unavailability sentences (crucial-release, elevated-question) DELETED — an absent control asserts nothing, those sentences asserted a great deal. attestBar: availability from the record's own facts, LABEL LEFT AS THE ONE RESIDUE and DELEGATED below, because `attest` is a NON_ACT by doctrine and the plane publishes no label for it on any object. (4) `ballotPreflight` -> `ballotNeeds`: gates KEPT (they read the live row), five `need:` sentences and the composed refusal DELETED; no probe is available (the target is judged before the reason and the vote is WRITTEN in the same method, so a probe carrying real values would cast it), so the commit is absent until ready and `projectOwnerRemove`'s own words carry everything else. (5) the routing chore: `queueOpen(id, type)` beside `queueWire`, the case heading made an ADDRESS carrying the record's own ancestor `type` — project to the workspace, inquiry to the question page, everything else and every UNTYPED subject to the document, nothing inferred from an id's shape. TWO DEFECTS FOUND AND FIXED IN PASSING, both live: (a) `loadActSource()` — the seam UI-12 built to read the published vocabularies — had NO CALLER IN app.html, so `actVocab()` answered the empty set forever against the real application and the dispose dialog offered no disposition at all; every harness that needed the set called the loader itself, which is D-173's shape one layer up. `boot()` now awaits it before the first screen paints, guarded structurally in act-dispose.test.mjs. (b) `recPost("select", …).handle` reads off the ENVELOPE; index.mjs returns `{ok:true, result:{…}, store, tokenClass}` for every passthrough, so the handle is `undefined` against the real plane. FIXED in `doDispose` (the one site in this claim); the FOUR others — cite, retire, sever/reinstate, release — carry the same line and are reported to CONDUCT for UI-23's D-173 sweep rather than reached into. NEGATIVE CONTROLS RUN PER SITE and recorded in each harness's own block: act-dispose three arms (probe neutered; ARM-(d) right code + invented sentence; option map) with arm (d) ALSO RUN ON DISK — app.html sha256 497bd9b79ce9e788a7aa1f4dc7a38d759b24ffdbc1ccfef639da8764de45ad9a before and after, 6 of 74 failed naming the surface as author; act-proposal two arms (derived marker; arm-(d)); act-ballot two arms (denominator wiring; arm-(d) `need:` sentence restored); queue arm (d) type routing ALSO RUN ON DISK, same sha before and after, 4 of 133 failed naming the WRONG DESTINATION including the wiring arm; document-page one arm (the crucial-release sentence restored). Every arm restored byte-identical.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 5ab4f9e). Verified: harness 26 suites green, battery 92/92 (4859). The mechanism-premise override accepted on its evidence (recorded on the landed line so nobody re-inherits the wrong claim); the attest delegation queued as REC-38; UI-23 widened with the four envelope sites + mocks-same-turn rule. No DEC.

## DELEGATION 2026-08-05 UI → RECORD
from: ui22-agent / UI-22 (the five pre-DEC-8 refusal residues)
need: publish the CAPTURE-DIRECTED acts the way `op=affordances` publishes object-directed ones — at minimum `attest`, with its `label`, its `needs` and its `rung`. It does not have to join `ACTS` and probably should not: a shape beside `catalog` in the no-target answer (a `capture_acts` block, or `non_acts` decorated with what `decorateAct` already computes) is enough, because what a surface needs is the producer's own NAME for the act and the capability it takes, not a per-object derivation.
why: UI-22 rewired the document page's three act sections off their surface-side rules and onto the publication. TWO of them landed clean — `release` and `dispose` are published acts, so their sections now exist exactly where the record publishes them, under the record's own labels, and the two sentences that used to explain their absence are deleted. THE THIRD COULD NOT, and the reason is the plane's doctrine rather than a shortcut: `attest` is a NON_ACT in `affordances.mjs` ("capture-directed: co-attestation of a capture's existence in time"), so the answer carries no entry, no label, no `needs` and no `rung` for it on any target. So `Co-attest this capture…` is still a name this surface invented for an act the record owns, and `canAttest()` is still a surface-side copy of `op=attest`'s NEEDS (`contribute`, read out of index.mjs). Both are the last of the class UI-12 opened, and neither can be closed from this side.
measured, so nobody re-derives it: `RUNGS.attest = "attested"` ALREADY EXISTS in affordances.mjs and is already sourced (Constructs:275) — it is simply unreachable, because `decorateAct` only runs over `ACTS`. The same is true of `ratify`. So the rung half is a publication change and not a new determination; only the LABEL is new wording, and it is the record's to choose because it is the record's act.
do NOT: fold `attest` into `ACTS`. The NON_ACTS entry states a real distinction — an act whose subject is a capture sha is not an act on a bundle — and collapsing it would make `op=affordances?target=<bundle>` publish an act that is not about that bundle, which is the pre-flight-disagreeing-with-the-refusal failure DEC-8 exists to forbid.
also worth knowing: this leaves `attestPreflight` (NO_STORAGE / BAD_SHA / NO_SUCH_CAPTURE, computed and worded on the surface) standing as a FIFTH residue of the same class — deliberately out of UI-22's scope, which was the BAR and not the dialog, and named here so it is queued rather than discovered again.
status: open

## CLAIM 2026-08-05 RECORD
session: rec24-agent (worktree agent-a9e361391af630618)
opened: 2026-08-05T00:00:00Z
paths: bio-plane/src/schema.mjs (ONLY: two NEW tables `action_basis` and `correspondence` with their indexes, placed BEFORE the `host_governor` block), bio-plane/src/store.mjs (ONLY: the six `bundles` projection columns `action_kind`/`action_risk_tier`/`action_counterparty_state`/`action_resolution`/`action_clock_next`/`action_clock_overdue` and their indexes; `projectionOf`/`PROJECTION_COLS`/`#writeProjection` gaining them; `promote`'s action arm — validation at the write and the two new WHOLE projections; `projection()` gaining the derive-on-read `action` block; NEW methods `actionMove`, `actionCorrespond`, `#actionDerived`, `#respondsToInto`; `purge`'s TABLES list and `stats`; the DO route table gaining `actionmove` and `actioncorrespond`), bio-plane/checks/bio-checks.mjs (ONLY: `ACTION_KINDS` gains `request_for_comment`; `REL_VOCAB` gains `responds_to`; NEW exported `ACTION_BASIS_KINDS`, `CORRESPONDENCE_DIRECTIONS`, `RFC_RESPONSE_WINDOW_PRECEDENT`, `actionBasisFindings`, `correspondenceFindings`, `respondsToEdgeFindings`, `consequenceState`; `checkActionExtension` calling them), bio-plane/src/query.mjs (ONLY: six new `FIELDS` entries over the six new columns), bio-plane/src/index.mjs (ONLY: the two OPS rows, the two NEEDS rows — `contribute`, no new token —, a new `ACTION_ACTIONS` array in both SESSION_OPS lists, and the viewer/author stamp conditions gaining it), bio-plane/src/affordances.mjs (ONLY: two new ACTS rows for the action ops and the header note that an action publishes none), bio-plane/test/action-loop.test.mjs (NEW — this item's harness), bio-plane/test/affordances.test.mjs (CORRECTED, never exempted, with dated reasons: the seven-value action_kind count and the "an action bundle publishes an empty act list" assertion are both superseded BY THIS ITEM), bio-plane/package.json (ONLY: one `test:action-loop` script), docs/development/CLAIMS.md (this entry)
interfaces owned: I5 (two new tables, six additive nullable columns) and I3 (two NEW ops, additive; `op=projection`'s single-bundle answer gains an `action` block on action bundles only). Nothing existing changes shape.
interfaces consumed: none changed.
DISJOINT FROM UI-22 (surface residues), running concurrently on civicos-ui ground: this claim opens NO file under `civicos-ui/` at all. No shared line, no shared suite.
explicitly NOT touched: civicos-ui/** (every file), newgroup/**, docprofile/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, every CAPTURE-owned store function
expected: QUEUE.md REC-24 / `BUILD-ORDER.md` §2 (REC-24) (a)-(g), with DEC-13 (`request_for_comment` naming the specific inquiries it disclosed; the authored response window, 7-30 days carried as GAO's SOURCED precedent and never enforced as a range; captured-not-summarised; a non-response recorded with its date) and DEC-14 (a consequence is an OUTCOME by default; IMPACT requires a basis leg on evidence that is not our own action; unproven is a STATED state on the R1 shape) folded in.
accepts-when: as QUEUE.md REC-24. NEGATIVE CONTROLS run and recorded in the suite's `NEGATIVE CONTROL:` line; restored after each. No push - CONDUCT integrates.
landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 52f2976). Verified: battery 93/93 (4942), hygiene 61/61, --strict 128/128. The three-places rule added to VERIFICATION.md; D-180 recorded; both judgements accepted; IC-14, I3 → 5.7.0. No DEC.
## CLAIM 2026-08-05 UI
session: ui23-agent (worktree agent-afc32d7305d506b53)
opened: 2026-08-05T01:10:00Z
paths: civicos-ui/app.html (ONLY: (1) the SEAM BLOCK at ~:750-800 — `recPostR` ADDED beside `recR` as the POST twin, and the D-173 header comment extended with the structural rule; (2) EVERY `rec(`/`recPost(` CALL SITE outside the four seams rerouted onto `recR`/`recPostR` — the four named sites `doCite`, `doRetire`, `doEdgeAct`, `doRelease` are the bug fixes; `doDispose`'s UI-22 per-site unwrap, `reviseWithCapture`'s lease/promote unwraps, `queueMuteCase`, `doProposalDispose`, the two `promote`+`allocid` pairs in the adopt and add flows, and the `.result` reads in `imageOf`/`projectionOf`/`boot`/`loadRecord`/`renderMembers`/`docBacklinks`/`loadActSource`/`heldMatch`/the three `search` sites are seam conversions, not behaviour changes; (3) `linksFor`, the two `acquire` sites and the two `attest` sites rerouted onto the same seams — those ops are FLAT on the wire and `recR` is shape-agnostic, so nothing about them changes but the route), civicos-ui/check-mock-envelope.mjs (NEW — this item's guard), civicos-ui/test/envelope-probe.mjs (NEW — the guard's runtime shim), civicos-ui/test/run.mjs (ONE new line: the guard joins the loop the reader actually runs), civicos-ui/test/*.test.mjs (CORRECTED mocks, dated, never exempted — every mock answer for a WRAPPED op wrapped in the same turn as the sweep), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — no op contract is changed, and no op is added. `bio-plane/src/index.mjs` is READ ONLY, to establish which ops the control plane WRAPS (`json({...body, store, tokenClass})`, the generic passthrough) and which it FLATTENS (`json({ok:true, ...r.result})` and the literal returns: links, acquire, attest, monitor, archivelookup, linkproject, governorstate, governorconfig, knock, login, publishedmanifest). That map is the guard's exemption table and is cited in the guard's own header.
interfaces owned: none. No plane file is opened.
DISJOINT FROM REC-24 (plane, the action loop), running concurrently: REC-24 opens `bio-plane/src/store.mjs`, `index.mjs`, `schema.mjs`, `checks/bio-checks.mjs` and its own `bio-plane/test/` suites, behind I5. THIS CLAIM WRITES NO FILE UNDER `bio-plane/` AT ALL — index.mjs is read and never written — and REC-24 names no file under `civicos-ui/`. There is no shared line and no merge to resolve.
explicitly NOT touched: bio-plane/** (every file, read-only), civicos-ui/check-semantics.mjs (the sibling guard — the new guard is a SEPARATE file so a failure names its own subject), `attestPreflight`'s WORDING (its NO_STORAGE/BAD_SHA/NO_SUCH_CAPTURE sentences are the fifth pre-DEC-8 residue and wait on REC-38, which is still `queued` — the plane publishes no attest label to read, so rewording here would invent one; its ENVELOPE reads are swept, and it has none of its own because it is a pure function), docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-23 (widened 2026-08-04) — the D-173 class closed as a CLASS. Every DO-op read opens the envelope through ONE seam rather than per-site unwrapping; every harness mock answers the WIRE shape in the same turn, so the sweep cannot look green on a mock that agrees with the bug; and a standalone guard `run.mjs` runs makes the class structurally unable to reopen.
accepts-when: `node civicos-ui/test/run.mjs` green with the guard in place and every swept site driven through a WRAPPED mock; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in the guard's own `NEGATIVE CONTROL:` block — (a) unwrap one mock answer and the guard NAMES the suite and the op; (b) re-introduce one envelope read (cite's `select` handle) and a harness fails against the wrapped mock. Each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT.
released:
paths AMENDED 2026-08-05, mid-item, because the sweep of the SIBLING helpers reached two lines the opening claim did not name and a claim that quietly grows is worse than one that is corrected: `api` — the UNTOKENED transport for `op=login` and `op=publishedmanifest` — gains a seam `apiR` beside it, and its two call sites (`signIn` ~:919, `pubList` ~:10456) are rerouted onto it. This was not tidying: `signIn` read `l.token` off `api("login")`'s envelope, which is D-173's SIXTH instance and a live one (see the landed line). The guard's arm A covers `api` for the same reason.
landed: left for CONDUCT. Measured this turn, so nobody re-derives it: `node civicos-ui/test/run.mjs` green — 26 suites plus BOTH guards (check-semantics and the new check-mock-envelope), exit 0; `cd bio-plane && npm run test:battery` 92/92 suites, 4859 assertions, 71.7s — no plane file was opened, and the number is unchanged from REC-35's landed line, which is the point. THE SWEEP: 34 `rec`/`recPost` call sites plus 2 `api` sites rerouted; after it, the three transports are reachable ONLY from the four seams (`recR`, `recPostR`, `actAsk`, `intentAsk`) and `apiR`, which is now enforced rather than asked for. SIX REAL DEFECTS FIXED, not just re-routed — cite/retire/sever-reinstate/release all read `select`'s `handle` off the envelope (so those four acts named NO SELECTION against the real plane), release ALSO read `rel.released` off the envelope (so its receipt reported the ids the browser ASKED about rather than the ids the plane says it MOVED), and `signIn` read `l.token` off `api("login")` (so no correct password ever signed anyone in, and a wrong one showed the member the ENVELOPE instead of BAD_PASSWORD — found by arm A, and unfindable by any harness because nothing reaches `signIn`). THE GUARD, in two arms: arm A is static over app.html and enforces that only the seams touch a transport — this is what closes the class, because it needs no test coverage at all; arm B re-runs every suite with `test/envelope-probe.mjs` preloaded, which patches `vm.createContext` (the one choke point every harness's mock passes through) and observes what shape each op was ANSWERED in, then judges it against the plane's own map — WRAPPED for everything through index.mjs's generic passthrough, FLAT for the ten ops with their own handler (each listed with its evidence), and the check runs in BOTH directions so the flat list cannot become a place to silence a failure. MOCKS CORRECTED IN THE SAME TURN, dated, never exempted: cite-act (`select`), release-flow (`select`, `release`), document-structure (`resolutions`, `connections`, `captureprogressions`, `entity`; `links` left FLAT because it genuinely is). LIMITS STATED IN THE GUARD'S OWN HEADER: arm B is only as wide as the harness and reports its coverage rather than implying more; refusal shapes are not judged; arm A is textual and would not see a call smuggled through `globalThis["rec"]`. ONE FINDING WORTH KEEPING: the probe's first version CONSUMED the response body, which broke `intent-write.test.mjs` — the one suite that drives the REAL plane through miniflare — while the probe itself reported success. That is D-173's own shape inside the instrument built to catch it; real Responses are cloned now. NEGATIVE CONTROLS RUN, three arms, each restored byte-identical (sha256 compared before and after; cite-act.test.mjs 3dee2547…, app.html 37b23352…) and recorded in check-mock-envelope.mjs's own `NEGATIVE CONTROL:` block: (a) unwrap cite-act's `select` answer -> the guard exits 1 naming the suite AND the op AND the keys it saw, and run.mjs fails with it — while `node test/cite-act.test.mjs` alone still exits 0 with 131 green, which is the measurement that justifies arm B existing at all (the seam is shape-agnostic, so the sweep alone can never detect a wrong mock); (b) restore cite's envelope read -> arm A fails naming `doCite` and the line, AND cite-act itself now exits 1 ("and the act ran through op=cite") against the corrected mock, where before the correction the same edit was green; (c) restore `signIn`'s `api("login")` read -> arm A fails naming `signIn`, the arm no harness could ever run.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 97f8bf0). Verified: harness 26 suites + both guards green, battery 93/93 (4943). The sign-in coverage gap queued as UI-24; D-173's row amended closed-structurally. No DEC.

## CLAIM 2026-08-05 UI
session: ui19-agent (worktree agent-a35e689ada0d86cdc)
opened: 2026-08-05T01:10:00Z
paths: civicos-ui/app.html (ONLY the regions below, each named so a concurrent session can see the boundary):
  (1) THE ACTION PAGE — a NEW region `/*__ACTION_PAGE_START__*/ … /*__ACTION_PAGE_END__*/` placed after `/*__PROJECT_WORKSPACE_END__*/`: `openAction`, `actionRouteFromHash`, `ACTION` (the flow holder), the move flow (`openActionMove`/`actionMoveTo`/`actionMoveProbe`/`actionMovePaint`/`doActionMove`), the correspondence flow (`openActionCorrespond`/`actionCorrArm`/`actionCorrPaint`/`doActionCorrespond`), the overdue single act (`openNonResponseInquiry`), and the read-only renderers (`actionCounterpartyHtml`, `actionBasisHtml`, `actionClockHtml`, `actionLedgerHtml`, `actionConsequenceHtml`, `actionResponsesHtml`). NOTHING outside this region's own functions;
  (2) `ACT_FLOW` (~:5586) — TWO new entries, `actionmove` and `actioncorrespond`. The object itself and `actGo`/`actBarHtml`/`actReachable`/`actsFor`/`actAsk`/`recR` are READ and REUSED, not modified;
  (3) THE RAIL AND THE ROUTES — one new `RAIL` row (`actions`, P-52), the matching `R` map entry in `go()`, its two title-map strings, `navLabel`'s `action` case, `boot()`'s ONE route line (`routeFromHash() && projectRouteFromHash() && actionRouteFromHash()`), `queueOpen`'s ONE new `action` arm inside `__QUEUE__`, and `wireRows`/`recordRows`/`monRows` gaining a `data-type` attribute + a shared `rowOpen(id, type)` whose ONLY non-default arm is `action`;
  (4) THE INTAKE, RESTORED — inside `__ADD__`: `ADD_TYPES` regains `action` (the dated UI-15 comment DELETED and replaced with what actually landed), `ADD_ACT` (the authored-action holder), `addActionPaneHtml`/`addActSync`/`addActPick`/`addActBasisAdd`/`addActBasisDrop`/`addActClockSet`, `addTypeSync`/`addValidate`/`addGo` gaining their action arms, and `addGo`'s `GATE_REFUSED` branch rendering the plane's finding MESSAGES verbatim instead of a list of check ids;
  (5) `mdFor` (~:2029) — the `action` arm ONLY: a ninth parameter carrying the member's authored counterparty / kind / basis / clock, replacing the two placeholder pushes. Every other arm byte-identical;
  (6) CSS — new rules only, appended beside the existing act-dialog block: `.act-led*`, `.act-clock*`, `.act-cp*`, `.act-unproven`.
  bio-plane/src/setup.mjs (ONLY: the `s-new` section's Action `<option>` restored with its dated comment replaced, a new `#n-act` fieldset with the REC-23 radio pair + the kind select, `syncNewForm` and the `#n-save` handler gaining their action arms, and `mdFor`'s action arm taking the authored values — the SAME ninth-parameter shape as app.html's, because the two writers must not diverge)
  bio-plane/test/conformance.test.mjs (CORRECTED, never exempted, with the dated reason: the action arm's "exactly one error" assertion is the FLIP-BACK its own comment promised — it becomes zero errors for an AUTHORED counterparty and KEEPS an assertion that an unauthored one still draws exactly C-2.10, so the machine is proven not to invent)
  civicos-ui/test/action-page.test.mjs (NEW — this item's harness)
  civicos-ui/test/add-surface.test.mjs (CORRECTED, never exempted: the action arm UI-15 removed comes back, gating a fresh authored action at zero findings and an unauthored one at exactly one)
  docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=affordances` (both shapes), `op=actionmove`, `op=actioncorrespond`, `op=projection` (the REC-24 derived `action` block), `op=image`, `op=list`, `op=search`, `op=backlinks`, `op=allocid`, `op=promote`, `op=whoami`. NO op shape changes; `bio-plane/src/store.mjs`, `src/index.mjs`, `src/affordances.mjs` and `checks/bio-checks.mjs` are READ (the ordering measurement below) and never written.
interfaces owned: none.
NEW READS GO THROUGH THE SEAMS FROM BIRTH: every DO-op read this item adds is `recR(...)` or `actAsk(...)` — there is no `rec(op).field` anywhere in the new region — and the harness's mock answers the WIRE shape (`{ok:true, result:…}`) for every op, so it agrees with the plane and not with the bug.
DISJOINT FROM UI-23 (the D-173 envelope sweep), running concurrently across the WHOLE of app.html: UI-23 rewrites EXISTING unwrapped reads and their mocks; this claim adds a NEW region that has none, plus the five named restorations above. The overlap risk is real and is stated rather than denied: UI-23's sweep reads every line of app.html, so ANY line it rewrites that this claim also touches is resolved by WHOEVER LANDS SECOND merging on top — this claim's diff is contiguous and named, so the merge is a read of six hunks and not a re-derivation. The one shared function either might reach is `queueOpen` (UI-22's, one new arm here) and `wireRows`; neither is a DO-op read.
explicitly NOT touched: every other bio-plane/src file, checks/bio-checks.mjs, schema.mjs, query.mjs, affordances.mjs, every other bio-plane test suite, `civicos-ui/check-semantics.mjs`, `SEMANTICS` (VERIFIED already carrying the five action states from UI-10's widening — read, not edited), the `__INQUIRY_PAGE__` / `__PROJECT_WORKSPACE__` / `__CONCLUDE_ACT__` / `__CITE_ACT__` / `__ACT_SOURCE__` region bodies, `attestPreflight`, docprofile/**, tools/**, newgroup/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md UI-19 / `BUILD-ORDER.md` §2 (UI-19) — O3 the action page, with REC-24's DEC-13/DEC-14 surface halves and DEC-8's clause. THE MEASURED PER-OP PRE-FLIGHT SHAPE, taken from store.mjs and reported rather than assumed (UI-20's ordering rule): `actionMove` judges the authored `reason` FIRST and `resolution` LAST, so it takes a HELD-FIELD PROBE (the UI-13 INTENT_HOLD shape) — the probe carries the member's real reason and target and a `resolution` sentinel the store cannot accept, so ILLEGAL_TRANSITION / BAD_TARGET_STATE / NO_REASON all arrive in the plane's own words and order and the probe can never commit. `actionCorrespond` judges `target` SECOND, before every authored field, and takes a LEASE (a write) before the last caller field is consulted — so no field is both withholdable and last, and it takes the NO-REFUSAL shape: the commit is ABSENT until the fields are filled, and the plane words everything that survives to commit.
accepts-when: `node civicos-ui/test/run.mjs` green with an action harness driving planned → active → awaiting_response → overdue → resolved; an illegal transition refused AT PRE-FLIGHT in the plane's words; an undetermined counterparty with no basis refused in the plane's words; the overdue note rendered ONLY when the injected clock says so; the intake restored on BOTH surfaces with a fresh authored action drawing zero findings (the conformance flip-back) and an unauthored one still drawing exactly C-2.10; `consequence.state: unproven` rendered STATED and never graded; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in the harness's own `NEGATIVE CONTROL:` block — render an overdue note when nothing is overdue and the harness fails; prefill the counterparty basis and it fails; render a surface-worded refusal (right code, invented sentence) and it fails. Each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 822aac3). Verified: harness 27 suites + both guards green, battery 93/93 (4948). REC-38 widened with the two vocabularies; D-181 (the action-as-leg tension, conservative state shipped) and D-182 (risk_tier residue) recorded. No DEC.

## CLAIM 2026-08-05 RECORD
session: rec38-agent (worktree agent-a17692e58ddf2c98d)
opened: 2026-08-05T02:00:00Z
paths: bio-plane/src/affordances.mjs (ONLY: the import line from `../checks/bio-checks.mjs` gaining `ACTION_BASIS_KINDS` and `CORRESPONDENCE_DIRECTIONS`; two NEW `VOCABULARIES` entries `action_basis_kinds` and `correspondence_directions` bound to those imports; a NEW exported `CAPTURE_ACTS` array declaring id+label for the capture-directed ops, placed beside `RUNGS`; the NON_ACTS entries for `attest`/`monitor` gaining a cross-reference to it in their reason text), bio-plane/src/index.mjs (ONLY: the `affordances.mjs` import clause gaining `CAPTURE_ACTS`, and the op=affordances handler's TWO return shapes gaining a `capture_acts:` field composed through the EXISTING `decorateAct` — no new op, no changed field, no other handler), bio-plane/test/affordances.test.mjs (this item's assertions and its NEGATIVE CONTROL line), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 — `op=affordances` gains two `vocabularies` keys and one new top-level `capture_acts` block on both of its shapes. PURELY ADDITIVE: no existing field changes name, shape or value, and no op is added or removed.
interfaces consumed: none changed. `bio-plane/checks/bio-checks.mjs` and `bio-plane/src/store.mjs` are READ ONLY (to establish that ACTION_BASIS_KINDS/CORRESPONDENCE_DIRECTIONS are already exported from the catalog and imported by the store's own refusals — nothing is moved).
DISJOINT FROM UI-21 (E1 the evidence finder), running concurrently on civicos-ui ground: THIS CLAIM OPENS NO FILE UNDER `civicos-ui/` AT ALL. UI-21 opens `civicos-ui/app.html` and its own harness and names no file under `bio-plane/`. The UI harness is RUN from here as an acceptance instrument and never written. There is no shared line and no merge to resolve.
explicitly NOT touched: civicos-ui/** (every file — read and run, never written), bio-plane/src/store.mjs, bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/checks/bio-checks.mjs, every other bio-plane test suite, newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-38 (widened 2026-08-04). The attest delegation from UI-22 — the last surface-authored wording class — plus UI-19's two absent vocabularies published by import (the REC-35 pattern exactly, one array, no copy).
accepts-when: as QUEUE.md REC-38. NEGATIVE CONTROL run and recorded in `affordances.test.mjs`'s own `NEGATIVE CONTROL:` line, restored byte-identical after. No push — CONDUCT integrates.
landed: left for CONDUCT. Measured this turn so nobody re-derives it: battery 93/93 suites, 4965 assertions, 70.7s; `npm run test:coverage --strict` exit 0 (128/128 ops, 53/53 checks, 93/93 controls); `node civicos-ui/test/run.mjs` exit 0, 27 suites + both guards, NO file under `civicos-ui/` opened; affordances 46 -> 63. THE SHAPE CHOSEN: a published `capture_acts` block, NOT promotion into ACTS — stated at CAPTURE_ACTS with its three reasons, the load-bearing one being that `applies()` is handed affordanceFacts (type, state, edges, legs) and no capture sha is in that shape, so the only writable predicate is `ty === "information"` while op=attest actually gates on the BYTES BEING IN THE STORE — an information bundle holding no capture would publish the act and meet NO_SUCH_CAPTURE, DEC-8's headline disagreement arrived at by way of fixing a label. RUNGS.attest is reachable either way and now is: `decorateAct` runs over the new block too, so needs/mode/rung still come from NEEDS/SESSION_OPS/RUNGS and the LABEL is the one new fact. ONE SHARED-FUNCTION CHANGE, additive: `decorateAct` now publishes `weight: a.weight ?? null` — every act in ACTS carries a real weight and nothing about the catalogue moves, but a capture act is not selection-backed, and this file's own rule for `rung` is that an absence is STATED rather than the key dropped. A SECOND TOTALITY ADDED: the NON_ACTS reason prefix `capture-directed:` is now load-bearing — the two lists are held equal in both directions, so `attest`'s history (correct, sourced and unreachable since REC-19) cannot repeat for a third op. UI-19's behavioural assertion verified TWICE: green in the harness as written, and green again with the harness's hand-written vocabulary literals REPOINTED at the plane's real `VOCABULARIES` export (90/90, run from a scratch copy — no repo file written), which is the only run that proves the picker and the `request_for_comment` intake light up off the publication that shipped rather than off a mock that agrees with it. NCs RUN x4, recorded in the suite's own NEGATIVE CONTROL: line, both sources restored byte-identical (sha256 compared).
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 47bd4a3). Verified: battery 93/93 (4965), --strict 128/128, harness 27 + both guards. The capture_acts structural reasoning accepted; DEC-39 raised for the fence wording; UI-24's batch widened with the rider and the mock-tie; IC-15, I3 → 5.8.0. No DEC beyond the raise.
session: ui21-agent (worktree agent-a8713b78fca51badf)
opened: 2026-08-05T02:00:00Z
paths: civicos-ui/app.html (ONLY: (1) the RAIL array and the router's `R` map — the `monitoring` rail entry removed and its route key kept, `inquiries` and `monitoring` re-pointed at the finder; (2) `reverseRefs` + `REVREF_CACHE` DELETED and the document page's one consumer moved onto `op=backlinks`; (3) `renderFiltered` DELETED; (4) `renderMonitoring`/`monTable`/`monRows`/`MON_COLS`/`monKey` DELETED (`monitorNext` KEPT — the document page reads it); (5) the whole `/* ---------- Search (op=search) ---------- */` region REPLACED by the FINDER region `__FINDER_START__`..`__FINDER_END__` — `SEARCH_SCOPES`, `SEARCH_SCOPE`, `widenSearch`, `backFromEmptySearch` and the `op=list` substring fallback DELETED; (6) `loadReviewRows`'s hand-composed query literal replaced by the published-field composer and the weaker engine STATED; (7) the CITE FLOW — `citeCandidates` admits an inquiry, the role control from `vocabularies.basis_roles`, grades FILLED from the plane's `legs[]`/`gradesFilled`/`gradesUndetermined` and no grade control anywhere, and the receipt routed by the plane's own `citingObjectType`), civicos-ui/test/finder.test.mjs (NEW — this item's harness), civicos-ui/test/cite-act.test.mjs (CORRECTED, never exempted, dated: UI-20's measured surface gaps (iii') are CLOSED by this item, so the two assertions that pinned them are corrected to their opposite with the reason), civicos-ui/test/record-list.test.mjs + test/project-workspace.test.mjs + test/release-flow.test.mjs (CORRECTED mocks/assertions where they pinned a deleted symbol, dated, never exempted), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — no op contract is changed and no op is added. NEW READS from this surface, all through the seams: `op=searchfields` (the scope vocabulary), `op=concerns` (the SUBJECTS route), `op=entitybyalias` (the SUBJECTS route's candidate bound), `op=selection` + `op=selectionrelease` (the live lease's published expiry and drift), `op=backlinks` (replacing the client-side `reverseRefs` walk — REC-25 built it for this).
interfaces owned: none. No plane file is opened; `bio-plane/**` is READ ONLY.
DISJOINT FROM UI-19 (the action page, running concurrently on the action region + the ADD_TYPES restorations): this claim opens no action-page function, no `ADD_TYPES` entry and no `renderAdd` line. Whoever lands second merges.
explicitly NOT touched: bio-plane/** (every file, read-only), newgroup/**, docprofile/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs (both guards left alone — the new reads are all WRAPPED, which is the guard's default, so nothing is added to its FLAT list)
expected: QUEUE.md UI-21 / `BUILD-ORDER.md` §2 (UI-21) with the two 2026-08-04 scope additions from REC-37's delegation. E1: one finder, two NAMED routes, each stating its OWN count, an overlap figure, and a cross-seam filtered query REFUSED with two runnable alternatives rather than approximated. The SUBJECTS route's candidate bound is EXACT SPELLING and is STATED (REC-36 is still queued; the name-only reverse read does not exist and is not built here). HOLE-4, the projected-intent-axis rework, is NOT in this item.
accepts-when: `node civicos-ui/test/run.mjs` green with the finder harness; `grep -c "SEARCH_SCOPES" civicos-ui/app.html` == 0; `reverseRefs` gone, asserted; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` block — present a combined total across the two routes and the harness fails; offer a grade control in the cite flow and it fails; restore the `op=list` fallback and the harness fails naming the gate bypass. Each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT. Measured this turn: `node civicos-ui/test/run.mjs` green — 27 suites plus BOTH guards, exit 0; finder.test.mjs 85 assertions, cite-act grown 131 → 145; `cd bio-plane && npm run test:battery` 93/93 suites, 4943 assertions, 72.8s — UNCHANGED from UI-23's landed line, which is the point, because no plane file was opened. `grep -c "SEARCH_SCOPES" civicos-ui/app.html` = 0 (the tombstone deliberately does not spell the name — D-160's precedent, since a grep is the acceptance and a comment quoting the word is an occurrence of it). DELETED, the full list: the surface-side scope map + its live scope variable + `widenSearch`; `backFromEmptySearch`; the `op=list` substring fallback (D-142); `renderFiltered`; `renderMonitoring` + `monTable` + `monRows` + `MON_COLS` + `monKey` and the Monitoring RAIL ENTRY (route key KEPT — UI-14's deep-link pattern); `reverseRefs` + `REVREF_CACHE`; Review's hand-composed query literal. `monitorNext` KEPT and said why (the document page's monitored seal reads it). THE TWO ROUTES: op=search and op=concerns fanned out with Promise.all, merged by bundle_id, each stating its OWN count with an OVERLAP as the only figure derived from both — never a total, asserted by a DIGIT SWEEP over the fixture's arithmetic. THE CROSS-SEAM REFUSAL fires BEFORE any call is made (asserted: neither op is reached) and offers two alternatives that are DRIVEN, each reaching only its own route. SUBJECTS ROUTE LIMIT STATED: exact-spelling via op=entitybyalias, rendered whether the route found anything or not, and absent when a subject is addressed by `concerns:ENT-…`; REC-36 is named as not built. NEW READS, all through UI-23's seams from birth: searchfields, concerns, entitybyalias, selection, selectionrelease, backlinks — the envelope guard's arm B coverage line moved to 58 ops. THE REC-37 SURFACE HALF: citeCandidates admits an inquiry (through normalizeType, so a legacy `focus` row lands on the question arm as the store's own widening does); the role control's options are `vocabularies.basis_roles` with the words from the guarded CATALOG block; the commit is ABSENT until a role is stated so the flow never walks into NO_ROLE; no role is sent on the case arm; grades are FILLED from the plane's `legs[]`/`gradesFilled`/`gradesUndetermined` and NO grade control exists on the path; the receipt routes Done by the plane's own `citingObjectType`. TWO LIVE FIXES in passing: the cite dialog now awaits `loadActSource` at the point of use (it rendered a published vocabulary while assuming some other surface had loaded it — UI-22's no-caller finding one layer on), and an unpublished query term is now SENT to the plane rather than dropped, so the plane's own `query.warnings` is what a member reads. NCs RUN x3, each restored byte-identical (app.html sha256 e67cda55… before and after every arm): (a) combined total → 1 of 85, and THE FINDING IS THE ARM — only the digit sweep fired, the honest paragraph about overlaps stayed green beside a fabricated number, so a suite asserting the WORDING would have called it correct (queue.test.mjs's lesson at a new altitude); (b) grade control → 1 of 85 AND 1 of 145 in cite-act, two independent instruments — and the first version of this arm failed ONLY in cite-act because the finder's sweep ran over the selection pane and could not reach the dialog, so the fixture gained a QUESTION and the handoff is now driven onto the role arm (a sweep that cannot reach its subject is not a sweep); (c) op=list fallback restored → 3 of 85, one of them NAMING THE OP as the gate bypass. SUITES CORRECTED, never exempted, all dated: cite-act (two measured-gap pins INVERTED with their history kept; the affordances mock now publishes `basis_roles`, which REC-37's landed line says the plane does; a second EARNED target so one act lands a graded and an ungraded leg), record-list (the scoped-search and monitoring-column blocks MOVED with a note saying where their subject went — the legacy-alias regression this file exists for is untouched), release-flow (searchfields mock added; the composed query asserted ON THE WIRE, plus a new arm driving the weaker engine and asserting the screen SAYS which one answered), document-page (the reverse-edge fixture moved from a projection walk to an op=backlinks answer in the plane's own field names).
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 704e4fb; one named seam applied at integration — the Actions route as a finder scope, harness green). Verified: harness 27 + both guards, battery 93/93 (4965). UI-25 queued for the uncapped query selection. No DEC.

## CLAIM 2026-08-05 UI
session: ui17a-agent (worktree agent-a354a5273f5d289d0)
opened: 2026-08-05T04:00:00Z
paths: civicos-ui/app.html (ONLY: (1) a NEW region `__PUBLICATION_ENTRY_START__`..`__PUBLICATION_ENTRY_END__` holding `publicationEntryHtml()` and nothing else, placed immediately before the inquiry-page region; (2) inside `openInquiry`, THREE lines — the `publicationEntryHtml(actsR)` call, its interpolation into the page template above the act bar, and the `elsewhere` option handed to the EXISTING `actBarHtml(...)` call so the strip routes the plane's `publish` act at the new section instead of naming it uncarried), civicos-ui/test/publication-entry.test.mjs (NEW — this item's harness and its NEGATIVE CONTROL: line), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=affordances?target=` (the acts array, READ for `publish`'s published `label` and `prompt`), `op=whoami` through `PLANE.me` (the one credential sentence). NO op shape changes; NO new op; NOTHING is written to the plane. `op=publish` IS NOT CALLED FROM ANY LINE THIS CLAIM ADDS, deliberately: DEC-33 defers the ceremony and this item is its entry point, not its submit.
interfaces owned: none. `bio-plane/**` is READ ONLY (`src/affordances.mjs`'s `publish` entry read to take the label; the battery run as an acceptance instrument, never written).
DISJOINT FROM REC-38 (plane), running concurrently: THIS CLAIM OPENS NO FILE UNDER `bio-plane/` AT ALL. REC-38 names only `bio-plane/src/affordances.mjs`, `bio-plane/src/index.mjs` and `bio-plane/test/affordances.test.mjs` and no file under `civicos-ui/`. There is no shared line and no merge to resolve. This claim also opens no `ACT_FLOW` entry, so it cannot collide with any item adding an act flow.
explicitly NOT touched: every file under bio-plane/**, newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs (both guards left alone — this item adds no plane read that is not already classified), the CATALOG and SEMANTICS blocks in app.html, every other civicos-ui test suite (`inquiry-page.test.mjs` is RUN and measured unaffected: its affordances mock publishes no `publish` act, so the new section does not render on any page it drives)
expected: QUEUE.md UI-17a, the placeholder DEC-33 ships in UI-17's place. Carrying DEC-19 AS AMENDED (publishing is THE irreversible act; correction always moves FORWARD — a new edition as a separate document with every published edition standing, a withdrawal as another attested act with both standing; nothing is ever unpublished), DEC-33 (the ceremony is deferred; publication runs through the group's operator for now), and Q12 (narration surface-scoped and plane-sourced — one sentence from `whoami`; controls never narrated and never greyed, ABSENT rather than disabled). RECONCILED WITH REC-14's landed line: `op=affordances` now publishes `publish` as an ACT on a concluded inquiry, so the act EXISTS on the strip and is NOT suppressed — the entry point renders under the plane's OWN label and prompt where offered, and the strip points at it through `actBarHtml`'s existing `elsewhere` mechanism rather than at a ceremony that does not exist.
accepts-when: `node civicos-ui/test/run.mjs` green with a harness where the entry point renders the statement (irreversibility, editions, correction-moves-forward, operator-run), offers NO ceremony control of any kind (no sign, no preflight, no submit wired to `op=publish` — asserted on the WIRE, not only in the markup), renders the plane's published act label where offered, and a read-only credential sees the SAME surface with exactly one whoami-sourced sentence; `cd bio-plane && npm run test:battery` stays green; `node civicos-ui/check-semantics.mjs` green. NEGATIVE CONTROL run and recorded in the harness's own `NEGATIVE CONTROL:` line — render a sign/preflight/submit control, or grey a control instead of omitting it, and the harness fails. Each arm restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT. Measured this turn: `node civicos-ui/test/run.mjs` exit 0 — 29 suites plus BOTH guards (publication-entry.test.mjs NEW, 112 assertions); envelope guard arm B unchanged at 60 distinct ops, and `publish` is deliberately NOT among them because the surface never reaches it; check-semantics green (the CATALOG and SEMANTICS blocks were not opened). `cd bio-plane && npm run test:battery` 93/93 suites, 4965 assertions, 70.8s — IDENTICAL to REC-38’s landed line, which is the evidence that no plane file was opened. THE SHAPE: `publicationEntryHtml(actsR)` renders IF AND ONLY IF `op=affordances` publishes `publish` on the question, so it is absent on an open one rather than shown-and-disabled; it renders THE SAME for every credential (asserted by slicing the section out of both views and comparing string-for-string) because it is a statement about the record and not a control; Q12’s one sentence stays `openInquiry`’s, unchanged. THE RECONCILIATION WITH REC-14, which is the substance of the item: the act is NOT suppressed — `elsewhere:["publish"]` is handed to the EXISTING `actBarHtml`, DERIVED from whether the section rendered rather than from a list written down (UI-20’s rule), so the strip names the act under the producer’s own label and points at the explanation, and no control for it exists on either side. NO `ACT_FLOW` ENTRY FOR `publish` AND NO CALL TO `op=publish` ANYWHERE — asserted on the wire and asserted again by CALLING `actGo("publish", …)` and draining the microtask queue. The plane’s `prompt` rides through verbatim where offered and renders NOTHING where it is not (it is null today); the label is never re-worded. NCs RUN x3, app.html restored byte-identical after each (sha256 3524c3ec… before and after every arm): (a) submit wired to the act → 6 of 112, including an unplanned failure of "the label is not re-worded" because an invented button must invent a verb; (b) greyed-instead-of-absent → 3 of 112, and THE FINDING IS THE COUNT — only the read-only view’s assertions fire, because a greyed control is invisible from the credential it is not greyed for, so the read-only arm is not redundant with the acting one; (c) an `ACT_FLOW` entry for the act → 7 of 112, and THIS ARM CORRECTED THE SUITE: on its first run the wire assertion stayed green at 6 failures because `actGo` returns synchronously while the flow it starts is async, so the microtask drain was added and the arm re-run against it. Every arm leaves the WORDING assertions green, which is why the control assertions are structural (`<button`, `disabled`) and not a word list.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 6ee25f0). Verified: harness 29 + both guards, battery 93/93 (4965) byte-identical. The FW-14 rider attached. No DEC.

## CLAIM 2026-08-05 RECORD
session: rec36-agent (worktree agent-a334b392e3f8ae2b5)
opened: 2026-08-05T03:00:00Z
paths: bio-plane/src/schema.mjs (ONLY: a NEW `reading_ref_terms` table + its `term` index, placed with the readings/reading_refs block and BEFORE the `host_governor` block, with its own comment block), bio-plane/src/store.mjs (ONLY: `#writeReadings` gaining the term projection write beside the existing reading_refs write; a NEW `#labelTerms` tokeniser beside `#normAlias`; a NEW `#backfillRefTerms` bounded pass and its ONE call beside `#backfillProjection(500)` at construction; a NEW `documentsNamingEntity` read placed beside `documentsByReference`; a NEW `readingNamePlan` DO helper on `projectionPlan`'s precedent; `purge`'s TABLES list gaining `reading_ref_terms`; the DO dispatch gaining `readingname` and `readingnameplan`), bio-plane/src/index.mjs (ONLY: a NEW `readingname` entry in OPS with its comment; `READING_READS` gaining it; `REC30_VIEWER_READS` gaining it), bio-plane/src/affordances.mjs (ONLY: a NEW `NON_ACTS` entry for `readingname`), bio-plane/test/readingname.test.mjs (NEW — this item's suite and its NEGATIVE CONTROL line), bio-plane/test/label-variance-probe.mjs (NEW — the measurement instrument named in MEASUREMENTS.md, re-runnable), bio-plane/test/earnedbasis.test.mjs (CORRECTED, never exempted: the REC-36 bound in its header is the statement this item lifts), docs/development/MEASUREMENTS.md (this item's measurement, dated, with its instrument), docs/development/CLAIMS.md (this entry)
interfaces owned: I5 (a new derived table and its index) and I3 (ONE new read-only op, `readingname`). PURELY ADDITIVE: no existing table, column, op, field or value changes.
interfaces consumed: none changed. `civicos-ui/app.html` is READ ONLY, to establish that `loadResolveCandidates` (~:9409) is this read's consumer and that its stated limit is the sentence this item narrows. THE UI EDIT IS NOT THIS ITEM'S and no file under `civicos-ui/` is written.
DISJOINT FROM UI-21 (E1, the evidence finder), running concurrently on civicos-ui ground: THIS CLAIM OPENS NO FILE UNDER `civicos-ui/` AT ALL, and UI-21 names no file under `bio-plane/`. There is no shared line and no merge to resolve.
explicitly NOT touched: civicos-ui/** (every file — read, never written), bio-plane/src/query.mjs (the viewer predicate is CONSUMED through `#bundleGate`, not modified), bio-plane/checks/bio-checks.mjs, bio-plane/src/setup.mjs, every other bio-plane test suite, docprofile/** (the doctypes are RUN by the probe and never edited), newgroup/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-36 (UI-13's delegation) — the §8.1 grade-C tier reachable from a member surface. MEASURE FIRST on the corpus the repository actually holds, then choose the shape; the measurement is recorded before the code is written.
accepts-when: as QUEUE.md REC-36. NEGATIVE CONTROL run and recorded in `readingname.test.mjs`'s own `NEGATIVE CONTROL:` line, restored byte-identical after. No push — CONDUCT integrates.
paths AMENDED 2026-08-04, mid-item, because the build reached two files the opening claim did not name and a claim that quietly grows is worse than one that is corrected: `bio-plane/test/gate-reads.test.mjs` (CORRECTED, never exempted — its EVERY-READ-IS-CLASSIFIED totality named `readingname` the moment the op existed, which is the guard working; the new GATED entry states this read's posture and why it differs from its two siblings), and `bio-plane/src/store.mjs` additionally gains two DO-ONLY test/repair hooks beside the backfill (`readingTermsClear`/`reindexNames`, on `projectionClear`/`reproject`'s precedent) so the backfill path can be exercised at all — neither is in OPS and neither is reachable from the control plane.

landed: left for CONDUCT. Measured this turn so nobody re-derives it: battery 94/94 suites, 5009 assertions, 72.8s; `npm run test:coverage --strict` exit 0 (129/129 ops through the control plane, 53/53 checks, 94/94 controls); `node civicos-ui/test/run.mjs` exit 0, 27 suites + both guards, NO file under `civicos-ui/` opened; readingname 42. THE MEASUREMENT DECIDED THE SHAPE and is recorded in MEASUREMENTS.md with its date and its re-runnable instrument: over the ONE real captured document this repository holds, a subject name was the WHOLE label in 0 of 41 labels against 33 names taken from the document itself — so the normalised-label index answers NOTHING here — while 7 label hits were ABBREVIATIONS (OPD/HUD/REAP/CSBG/MOU) whose full names appear in NO label, which no normalisation of spelling can reach. Hence BOTH and neither alone: an index on the label's normalised TERMS, read through an ALIAS JOIN. Corpus thinness STATED, not papered (one document, one doctype, one institution: no distribution is claimed; what n=41 settles is the shape). Diacritics DELIBERATELY not folded, with the reasoning at the function and an assertion holding it. One statement per alias with `term IN (...)` + HAVING rather than INTERSECT arms, because D-36's five-compound-term ceiling would have been met by a six-word name on somebody's live instance. GATED in the STRONGER of the D-15 postures — the ROW is withheld in SQL (#bundleGate), not the reference redacted, because a candidate a member cannot open is not a candidate. NCs RUN x2, restored byte-identically (store.mjs sha256 compared): (a) drop the normalisation -> 18 of 42 fail, every variance class to zero, the exact tier still green; (b) neuter the gate -> 5 of 42 fail naming the leak. ONE INSTRUMENT FINDING kept in the suite header: arm (a) first reported 5 failures and hid 13 because the suite THREW on an emptied list — D-93's lesson inside a negative control; every index is guarded now. TWO SELF-CAUGHT HAZARDS, both the repository's own documented classes: a backtick inside the schema template literal (caught by the parse, not by a reviewer), and a RAW NUL written into store.mjs as a Map-key separator — D-131 exactly, caught by hygiene, now the escape. FOR THE LEDGER: UI-13's `loadResolveCandidates` still states the old limit on the page and still loops op=readingref per alias; the UI half is NOT this item's and the surface sentence narrows honestly only when a UI item consumes op=readingname.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker c3313fd). Verified: battery 94/94 (5009), --strict 129/129. NC arm (b) RE-RUN BY CONDUCT (5 named failures; restored byte-identical). UI-26 queued for the UI half; IC-16, I3 → 5.9.0. No DEC.
## CLAIM 2026-08-05 UI
session: ui24-agent (worktree agent-ab101273f57c0ba1c)
opened: 2026-08-05T06:00:00Z
paths: civicos-ui/app.html (ONLY these five regions: (1) the `__ACT_SOURCE__` block — `ACT_SOURCE` gains a `captureActs` field, `loadActSource` reads `capture_acts` off the SAME `op=affordances` answer it already takes, and ONE new helper `captureAct(id)` beside `actVocab`; (2) `attestBar` inside `openBundle` — the button's hand-spelled label and the act's rung replaced by the published `capture_acts` entry, and the section ABSENT where the plane publishes none; (3) `attestPreflight` — the three surface-written `need:` sentences and the composed `refusal` DELETED (UI-22's `ballotNeeds` treatment exactly), the gates and their titles KEPT because they read facts; (4) `attestValidate` / `doAttest` — the commit control ABSENT-not-disabled while a gate is unmet, and the surface-composed refusal block deleted; (5) `openAttestDialog`'s `<h2>` heading — the published label. NOTHING ELSE in the file: the co-attestation FENCE sentences (`ATTEST_YIELDS_GRADE`, `attestStandingClaim`, `attestHonestyHtml`, the dialog's `what` paragraph, the ladder's attest text, `attestBar`'s `n` line) are NOT touched and carry a dated comment naming DEC-39), civicos-ui/test/auth-surface.test.mjs (NEW — this item's harness: sign-in and the published list), civicos-ui/test/act-attest.test.mjs (CORRECTED, never exempted, dated: the pins on the surface-composed pre-flight refusal and on the greyed commit control become their opposite, with the reason), civicos-ui/test/document-page.test.mjs (CORRECTED, never exempted, dated: the "the Attestation section carries a label this surface wrote — the residue, named" pin is INVERTED now that REC-38 published it), civicos-ui/test/action-page.test.mjs (CORRECTED: the hand-written vocabulary literals TIED to `bio-plane/src/affordances.mjs`'s own `VOCABULARIES` export by import, plus the tie's own negative control), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=affordances` (BOTH shapes; the NEW read is the `capture_acts` block REC-38 published, taken through the existing `recR` seam inside `loadActSource`, no new call), `op=login` and `op=publishedmanifest` (already reached by `apiR`; NEWLY DRIVEN by a harness for the first time, no shape change), `op=whoami`, `op=queue`, `op=tasks` (driven through `boot()`, which no harness had ever run). NO op contract changes and NO new op.
interfaces owned: none. `bio-plane/**` is READ ONLY — `src/affordances.mjs` is IMPORTED by two harnesses as the tie's source of truth (the `act-proposal.test.mjs` / `add-surface.test.mjs` precedent) and never written; the battery is run as an acceptance instrument.
DISJOINT FROM UI-18 (O2 the published case), running concurrently on the new PUBLIC region: UI-18 builds the published case-file reading surface — `pubOpen`, the container/PDF renderings and their own new region. THIS CLAIM OPENS NO LINE OF `pubOpen`, `enterPublished` OR `pubList`: the published half here is a HARNESS ONLY (`auth-surface.test.mjs` drives `pubList` as it stands and asserts nothing about a case-file reading surface), so the only overlap is the file itself. Whoever lands second merges; this claim's app.html diff is five named, contiguous hunks and no line of the published-space region is among them.
explicitly NOT touched: every file under bio-plane/** (read and run, never written), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, civicos-ui/check-semantics.mjs, the CATALOG and SEMANTICS blocks in app.html, `civicos-ui/app.html`'s published-space region (`enterPublished` / `pubList` / `pubOpen` — read and DRIVEN, never edited), every other civicos-ui test suite. `civicos-ui/check-mock-envelope.mjs` is NOT edited: `login` and `publishedmanifest` are WRAPPED, which is the guard's default, so the new coverage arrives without touching its FLAT list.
expected: QUEUE.md UI-24 (batch widened 2026-08-04, the REC-3 small-batch precedent). (1) the untested authentication surface gets a harness — UI-23's arm A found `signIn`'s token read broken since birth and measured that arm B reaches NEITHER `signIn` NOR `pubList`; (2) the attest rider REC-38's landed line hands over — the published `capture_acts` label consumed, `attestPreflight` taking UI-22's treatment, THE FENCE SENTENCE LEFT EXACTLY AS IT STANDS pending DEC-39 (Bob's, raised by REC-38: member-facing wording that is a claim about what the record asserts); (3) `action-page.test.mjs`'s vocabulary mock TIED to the plane, because REC-38's proof of the tie was a scratch run and a proof that is not in the loop the reader runs is not a mechanism (CLAUDE.md).
accepts-when: `node civicos-ui/test/run.mjs` green with the new auth suite driving sign-in and `pubList` through WRAPPED mocks and the envelope guard's arm-B coverage line MOVED (asserted in the suite itself, not read off the log); `attestBar` rendering the published label asserted against `bio-plane/src/affordances.mjs`'s own export rather than a copy; `action-page.test.mjs`'s vocabularies tied, with changing one in the plane's export moving the suite as the tie's own negative control; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in each suite's own `NEGATIVE CONTROL:` line — unwrap the login mock and the guard names it; break the token read and the auth suite fails where yesterday nothing could; restore `attestBar`'s hand-spelled label and the harness fails naming the copy. Each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it: `node civicos-ui/test/run.mjs` exit 0 — 30 suites plus BOTH guards (auth-surface.test.mjs NEW, 62 assertions); **THE ARM-B COVERAGE LINE MOVED 60 -> 63 distinct ops** (`login`, `publishedmanifest` and `whoami` join it; 695 -> 736 op answers, 121 -> 132 op/suite pairs judged, 21 -> 22 suites driving ops), and the suite asserts the move ITSELF by re-running under `test/envelope-probe.mjs` in a child process rather than reading it off a log. act-attest 55 -> 69, action-page 90 -> 96, document-page grown by the attestation arms. `cd bio-plane && npm run test:battery` 93/93 suites, 4965 assertions, 74.5s — IDENTICAL to REC-38 and UI-17a landed lines, which is the evidence that no plane file was written.
  THREE LIVE FINDINGS, none of them re-routing. (1) `whoami` WAS NOT ON THE COVERAGE LIST AT ALL before this turn, because NO harness had ever run `boot()` — every suite in `civicos-ui/test/` sets `PLANE.me` by hand and calls a renderer, which is the same class as UI-22 `loadActSource` no-caller defect and is why that one survived four items. `boot()` is driven end to end here. (2) THE PLANE PUBLISHES NO SENTENCE FOR A REFUSED LOGIN — `store.mjs login()` answers `{ok:false, reason:"BAD_PASSWORD"}` and `{ok:false, reason:"NO_SUCH_ROLE"}` with no `detail` — so the gate honestly renders a bare reason code at a member. NOT closed here: composing the missing sentence is precisely what DEC-8 forbids a surface to do, so it is raised for CONDUCT as a RECORD-side delegation. (3) `action-page.test.mjs` HAD BEEN DRIVING A VOCABULARY THAT IS NOT THE PLANE S: `ACTION_KINDS` has EIGHT members and the literal in the mock published FOUR, so the intake harness had never seen the picker the application actually draws. Found by the tie, in its first run. A fourth, smaller: the four action RESOLUTIONS are enforced in TWO places (`bio-checks.mjs` C-2.10 inline, `store.mjs` `actionMove()`) and published in NEITHER, so the surface can only learn them from the refusal — recorded in the suite, raised, not fixed.
  THE ATTEST RIDER, and what it deliberately did NOT touch: `attestBar` and the dialog read REC-38 published `capture_acts` label and rung, the section is ABSENT where the plane publishes no capture act (`actVocab` rule applied to a label), and the LAST surface-authored act label in `app.html` is gone. `attestPreflight` took UI-22 `ballotNeeds` treatment — the three `need:` sentences, the composed `refusal`, its reason codes AND the mirrored NO_STORAGE->BAD_SHA->NO_SUCH_CAPTURE order are DELETED, the gates kept because they read facts, and the commit control is ABSENT-not-greyed. WHY NOT A PROBE is measured and stated on the function: `op=attest` takes ONE caller field, every refusal is judged against it or against the instance, so nothing is withholdable that stops short of the act — and the act is irreversible and public, the one class where a probe that commits is unrecoverable. THE FENCE SENTENCE IS UNTOUCHED, byte for byte, with a dated comment naming **DEC-39** at `attestBar`, at `openAttestDialog` and in both suites.
  ONE FIXTURE-WIDENING CAUGHT A MOCK IN THE SAME TURN: giving `document-page.test.mjs` provenance register a real capture sha (which is what `op=acquire` writes, and without which that page could never render an Attestation section at all) made `linksFor` reachable for the first time — and the mock fallback answered `op=links` WRAPPED where `index.mjs` flattens it. The envelope guard named it immediately, in both of that file mocks. The surface reads through `recR`, which is shape-agnostic, so nothing else could have.
  NCs RUN, IN-VM AND ON DISK, everything restored byte-identical (sha256 compared: app.html c007d20035cc25febf7e75bb0a9711fa398e3899c855b1fbb2df4e18551dd2a8, auth-surface.test.mjs b10d0a811e42988ee77e4acf805a5c8023697b2f07415ba18e79599014c9f0e9, bio-checks.mjs bcba30cf99796393ac8cf708ecbcfe9844d1e76cc8a76c2401e92a15d945d206, affordances.mjs 2bef33646cab45bc429a7a7704fd3e2c0c0ef802e13372ee5db945b6f9b470d7 — before and after every arm). (a) the token read broken in `apiR` -> **26 of 62** auth-surface assertions fail, and the identical edit could not fail anything yesterday. (b) right code + invented sentence at the gate -> the residue scan names the surface as author while the refusal itself stays correct. (c) the login mock unwrapped -> `check-mock-envelope.mjs` exits 1 naming the suite, the op and the keys — **while auth-surface own surface assertions stay green**, which is the receipt for why arm B exists: a flat answer happens to put `token` where the correct read looks. (d) the hand-spelled attest label restored -> act-attest STAYS GREEN and only `document-page.test.mjs` SOURCE-LEVEL pin fires, REC-38 finding (an identical copy agrees at zero cost) reproduced one layer out; with a drift of the plane own label added on top, the one remaining copy site is named. (e) the tie own, two arms against the plane export -> `advances`->`advancez` moves 1, `request_for_comment`->`rfc` moves 2, and the first prediction was WRONG and is recorded as measured: a rendered-value scan does not notice a renamed token, which is why the tie carries explicit pins.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker ffa11da). Verified: harness 30 + both guards, battery 94/94 (5009). REC-39 queued for the login sentences + RESOLUTIONS publication; DEC-39's fence respected byte-for-byte. No DEC.
## CLAIM 2026-08-05 UI
session: ui18-agent (worktree agent-ad74fb4d5fd0f1b37)
opened: 2026-08-05T06:00:00Z
paths: civicos-ui/app.html (ONLY: (1) the whole `/* ---------- Published space ---------- */` block at the foot of the script — `enterPublished`, `pubList`, `pubOpen` — REPLACED by a new region `__PUBLISHED_CASE_START__`..`__PUBLISHED_CASE_END__`; (2) the `api` transport gaining an optional third `params` argument and a NEW untokened seam `apiQ(op, params)` beside `apiR` — backward compatible, `apiR`/`signIn` unchanged in behaviour; (3) `axisPanel` gaining an OPTIONAL fourth `opts` argument so a target can be rendered as a NAME rather than as a workspace link — additive, every existing call site byte-identical in behaviour; (4) the published surface's CSS added to the `<style>` block, including the file's first `@media print`; (5) one line in `boot()` and one at the script foot so `#case/<id>` is a real address reachable with and without a session), civicos-ui/check-mock-envelope.mjs (ONLY: `apiQ` added to `API_SEAMS`; `publishedcase` added to `FLAT_OPS`; `knock`'s evidence string corrected — it carried op=publishedcase's return literal), civicos-ui/test/publishedcase.test.mjs (NEW — this item's harness and its NEGATIVE CONTROL: block), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=publishedcase`, `op=publishedbytes`, `op=publishedmanifest`, `op=verify`. ALL FOUR ARE `classes: null` CREDENTIAL-FREE READS and are called with NO token from a seam that has none to send. No op shape changes; no op added; nothing is written to the plane from this surface.
interfaces owned: none. `bio-plane/**` is READ ONLY (the battery is run as an acceptance instrument and never written).
DISJOINT FROM REC-36 (plane), which may still be in flight: THIS CLAIM OPENS NO FILE UNDER `bio-plane/` AT ALL. REC-36 names the normalised-label reverse read on `reading_refs` in `bio-plane/src/store.mjs` (+ its schema index and its own suite) and names no file under `civicos-ui/`. There is no shared line and no merge to resolve.
explicitly NOT touched: every file under bio-plane/**, newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, civicos-ui/check-semantics.mjs, the CATALOG and SEMANTICS blocks in app.html, the member-space act flows, every other civicos-ui test suite
expected: QUEUE.md UI-18 / `RECONCILED.md` §3.1 (UI-18) / `BUILD-ORDER.md` §2 (UI-18). O2 THE PUBLISHED CASE: a public credential-free read at a real address; the index over EDITIONS with BOTH strengths and supersession; the case in full with the serves/names split rendered as two different things; Q6's named stance resolving to a PAIR of independent floors evaluated conjunctively, both stated in-band, `none` rendered explicitly, a suspended axis satisfying only `none`; DEC-17's declared bar beside the reached strength with absent rendered as absent; DEC-31's bound rule in every rendering; DEC-34's per-page header on every page-shaped artifact AND its negative control placed here; DEC-18/D-160's UNRATED; REC-17's supersession banner from published_bundles + frozen bytes and never the member-class op; print first-class with nothing print-hidden; the Verify button on `op=verify`; the four forbidden affordances considered and absent.
accepts-when: as QUEUE.md UI-18. `node civicos-ui/test/run.mjs` green with a published-case harness reading a case and its bytes with NO credential; `cd bio-plane && npm run test:battery` stays green. NEGATIVE CONTROLS run and recorded in the harness's own `NEGATIVE CONTROL:` block — drop a determining leg at a threshold; publish a child with no parent named; compose the two strengths into one letter anywhere including print; apply one floor to both axes; leave one axis unconstrained; omit a `none` floor; render a page-shaped artifact without the DEC-34 header; hide the in-band block in print. Each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT. Measured this turn: `node civicos-ui/test/run.mjs` exit 0 — 30 suites plus BOTH guards (publishedcase.test.mjs NEW, 114 assertions); the envelope guard's arm B coverage grew 60 → 63 distinct ops (`publishedcase`, `publishedmanifest`, `verify` — the credential-free three; `publishedbytes` is deliberately absent because it answers BYTES and the probe only judges answers it can read as an object). `cd bio-plane && npm run test:battery` 93/93 suites, 4965 assertions, 72.8s and `npm run test:coverage --strict` exit 0 (128/128 ops, 53/53 checks, 93/93 controls) — BOTH BYTE-IDENTICAL to UI-17a's landed line, which is the evidence that no plane file was opened. THE SHAPE: `#published` and `#case/<id>[/e<N>]` are real addresses asked FIRST at boot and again at the script foot before the gate, so a stranger holding nothing and a member holding a session land on the same page and the member keeps the session (`pubLeave` is the way back and renders only when a credential is actually held — the dead end BUILD-ORDER named). Q6 IS ENFORCED AT `stanceFloors`, NOT DESCRIBED: a stance resolves ONLY to a complete pair of two independently declared floors and otherwise resolves to null, and a null pair DRAWS NO CASE AT ALL — four stances, every label naming what the reader is DOING, at least one carrying a different floor per axis. DEC-34's header is stamped by `pubPage`, the only producer of a page-shaped artifact, and the harness counts pages against stamps in both directions. The in-band block is TEXT (asserted with the tags stripped, which is the only test of "survives the copied selection" that means anything) and is structurally first. THE PRINT STYLESHEET MAY ONLY ADD — the harness parses the `@media print` block and refuses ANY hiding rule rather than protecting a list of selectors; `pubExpandForPrint` on `beforeprint` is the one thing CSS cannot do and it only ever OPENS. THE SUPERSESSION BANNER SHOWS THE EDITION'S OWN FROZEN PAIR and says so in those words (REC-17's honest finding made a sentence), sourced from `published_bundles` + the frozen bytes; `op=reevaluations` is member-class and is asserted never asked. THREE THINGS MEASURED THAT COST TIME: (1) D-160 reaches this item's own vocabulary — QUEUE.md and RECONCILED both describe the boundary-case leg with the word `check-semantics.mjs` retires from app.html entirely, so the rule shipped and the word did not (UNRATED is the word; the leg is one that LEFT AN AXIS UNRATED); (2) arm A of the envelope guard reads app.html TEXTUALLY and a comment that spells a transport call IS a call to it — a prose example in the new `api` comment failed the guard; (3) the frozen pair carries NO `not_load_bearing` (the frontmatter block writes axis/state/grade/weakest/load_bearing/population/detail), so the qualifier set is taken from the basis instead — the conservative direction, one qualifier too many rather than one too few. TWO SHARED FUNCTIONS TOUCHED, both additive with the default byte-identical: `api` gains an optional third `params` argument (never a token — asserted on the wire), and `axisPanel` gains an optional `opts.link` so a target id renders as a NAME on the published surface instead of a workspace link a stranger cannot follow. GUARD FILE: `apiQ` added to `API_SEAMS`, `publishedcase` added to `FLAT_OPS`, `knock`'s evidence string CORRECTED (it carried op=publishedcase's return literal — the classification was right and the citation was not), and the new suite PINNED into `SUITES_WITH_OP_TRAFFIC` the day it landed because it is the only suite driving the credential-free ops. NCs RUN x8, app.html restored byte-identical after each (sha256 79a4a7c2… before and after every arm) — and ARM (a) CORRECTED THE SUITE: it fired only 3 at first because a dropped leg is still NAMED, so `includes(<leg id>)` was true whether the qualifier survived or not; `data-kept`/`data-dropped` now mark the two sets apart and the arm re-ran 3 → 5. ARM (c)'s finding is UI-21's at a new altitude: the count sweep stayed GREEN over a single composed letter, and only the paired both-axes assertions fired. THE FOUR FORBIDDEN AFFORDANCES considered and absent, each with its reasoning at the head of the region rather than in the suite.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 7be97b5; the UI-24 seam closed at integration with three dated pin corrections). Verified: harness 30 + both guards all green, battery 94/94 (5010). DEC-40 (the stance set) and DEC-41 (the container's rendering) raised with the shipped provisionals. THE CRITICAL PATH IS COMPLETE. No other DEC.

## CLAIM 2026-08-05 CONTENT-PDF
session: cpdf11-agent (worktree agent-ac8c6248f28103847)
opened: 2026-08-05T05:00:00Z
paths: docs/development/MEASUREMENTS.md (ONLY: ONE new dated section appended at the end — the CPDF-11 measurement), bio-plane/test/ocr-moondream-probe.mjs (NEW — the re-runnable probe driver), bio-plane/test/ocr-moondream-worker.mjs (NEW — the scratch Worker source the driver uploads, uses and deletes), docs/development/CLAIMS.md (this entry)
interfaces owned: none. THIS ITEM COMMITS NO PRODUCT CODE. Not one file under `bio-plane/src/`, `bio-plane/checks/`, `newgroup/`, `pdf-worker/` or `docprofile/` is opened for writing.
interfaces consumed: none changed. `bio-plane/test/ocr-measure-probe.mjs` (CPDF-9's instrument) is READ ONLY and is read AT RUN TIME by the new probe — the ground truth and the four scoring expressions are taken from it rather than copied, so the two measurements cannot drift apart; the new probe STOPS with a named error if those shapes are no longer found there. `bio-plane/src/cpu.mjs` is not touched (this probe times nothing against the CPU ceiling — the work happens on Cloudflare's side, not in an isolate we can walk).
BATTERY UNTOUCHED, and structurally rather than by exemption: `scripts/battery.mjs` discovers suites by `readdirSync(test/).filter(f => f.endsWith(".test.mjs"))`. Neither new file ends in `.test.mjs` (the same reason `ocr-measure-probe.mjs` and `facet-probe-worker.mjs` are not discovered), so nothing is added to the battery and NO skip marker is needed. Assertion counts and suite counts are unchanged by construction, not by claim.
DISJOINT FROM UI-18 AND UI-24, both running concurrently on `civicos-ui` ground: THIS CLAIM OPENS NO FILE UNDER `civicos-ui/` AT ALL, and neither of those items names a file under `bio-plane/` or `docs/development/MEASUREMENTS.md`. The one shared file in the repository is `docs/development/CLAIMS.md`, which is append-only and where a concurrent append is a merge of adjacent blocks, not a conflicting edit.
explicitly NOT touched: civicos-ui/** (every file), bio-plane/src/**, bio-plane/checks/**, every `*.test.mjs` in bio-plane/test/, bio-plane/package.json (the probe npm-installs its referee into an OS temp dir and adds nothing to the repo), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md CPDF-11 (added 2026-08-04 · BOB) under DEC-35 — measure Moondream 3.1 on Workers AI as the IN-ACCOUNT OCR path, against the SAME ground-truthed page and the SAME instrument CPDF-9 used. Four measurements: (a) accuracy vs the local-tesseract floor, digits separately, minted digits weighted; (b) coordinates VERIFIED not assumed; (c) blank and noise controls, weighted heavily; (d) the degradation ladder, scoring REFUSES vs INVENTS. Pseudo-confidence is FORBIDDEN by DEC-35 and is not asked for anywhere in the instrument.
CLOUDFLARE ACCOUNT: the probe asserts `CF_ACCT === 20b533579290b9b93168345edd3b7f72` and exits 3 before uploading anything if it does not. No wrangler command is used. The scratch Worker is uploaded under the slug `bio-ocrprobe`, used, and DELETED on the way out; it binds only `ai` and a per-run random plain-text gate, and it touches no R2 bucket, no Durable Object and no existing script. NOTHING WAS FUNDED and no account, credential or signup was created.
accepts-when: as QUEUE.md CPDF-11 — MEASUREMENTS.md carries character accuracy, digit accuracy, minted-digit count, the coordinate-alignment verdict, the blank/noise result and the ladder's refuse-vs-invent table, each with date and instrument, plus the stated transcription-fidelity cap and a GO/NO-GO. Negative controls RUN and reported, with any non-empty output on a page with no text on it reported as a FAILURE OF THE PATH. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session.
landed: left for CONDUCT. THE EXACT MODEL ID IS `@cf/moondream/moondream3.1-9B-A2B` and it IS reachable on this account through `env.AI` — but NOT through the REST route, because the project token carries Workers Scripts and not Workers AI (403 code 10000 on `/ai/models/search`, 200 on `/workers/scripts`); the binding needs no token permission and is the production shape anyway, so the probe uploads a scratch Worker, uses it, and deletes it (verified gone: the account lists biosmoke7, civicos, newgroup and nothing else). THE VERDICT IS **NO-GO** on Moondream 3.1 as the default in-account transcription path, on the ONE requirement DEC-35 called non-negotiable: coordinates VERIFIED — every returned box cropped out of the page and read by the local tesseract the floor was measured with — align in **2 of 24 box-checks**, `task=query` returns no boxes at all so no call returns text WITH its coordinates, and the model returns a confident box on every run for a dollar amount that IS NOT ON THE PAGE. ACCURACY is close but below the floor and is not the same claim: 99.44% median char (floor 99.96%), 89/90 GT digits with the one miss an OMISSION of the page-number `2` rather than a misread, ZERO minted digits at full resolution — and all three bid amounts to the cent correct in all three runs, which is exactly where the floor's best_int minted a `5`. But 3 runs of identical bytes gave 2 DISTINCT transcriptions, so the record cannot be re-derived from the engine+version it names. NEGATIVE CONTROLS RUN AND PASSED CLEANLY: blank 3/3 and uniform-noise 3/3 answered `ILLEGIBLE`, zero non-empty output, and the probe exits non-zero if that regresses. THE LADDER IS THE OTHER HALF OF THE VERDICT: R0/R1/R2 faithful with 0 minted; **R3 (75 dpi + blur 2.0) REFUSED 0/3 and minted 16-20 digits per run** at 84.96% char, turning `$50,000` into `$10,000`/`$100,000` and `lowest responsible bidder` into `least responsible bidder` in fluent, structurally perfect prose indistinguishable from an R0 run; R4 REFUSED 3/3. So refusal is a FLOOR alarm one rung PAST the invention band, not a boundary alarm, and measured refusal-reliability does NOT license structured self-refusal — DEC-35's own named alternative (no per-region trigger, the cap carries everything) is what the measurement returns. C1 (contrast collapsed to 16 greys) is recorded OFF the ladder because it turned out not to be a degradation at all (99.78%) and numbering it R5 would have asserted a monotone ordering the measurement contradicts. RECOMMENDED CAP: **C, never B**, plus a rule the cap does not reach — a digit from this engine may not carry a leg without member attestation. TWO INSTRUMENT FINDINGS kept in the probe because the wrong instrument gave the more flattering number: (i) the first block-alignment criterion (just "recover 40+ chars of ground truth") passed **7 of 10 DISPLACED boxes**, because a page that is dense text top to bottom hands you ground truth wherever you crop — it was measuring the page, not the box; the criterion now also requires the recovered text to be found within 10% of the page of where the box's own midpoint predicts, and the displaced control fails it 0/9; (ii) the composed detect-then-transcribe shape scored one region at 1.58% on a "region ground truth" derived from a referee read that was only 47% placeable — unscoreable, not a failure, and the probe now refuses to score a region below 80% placeable. COMPARABILITY IS ENFORCED, NOT CLAIMED: the probe READS `GT_PAGE2` and `norm`/`levenshteinPairs` out of `ocr-measure-probe.mjs` at run time and asserts all four of CPDF-9's scoring expressions are still literally present, exiting 4 if not. BATTERY UNTOUCHED, MEASURED: `npm run test:battery` 94/94 suites, 5009 assertions, 76.6s — identical to REC-36's landed line, and `readdirSync(test/).filter(endsWith(".test.mjs"))` returns 94 with neither new file among them. Cost of the whole run of record: 41 calls, 31,454 in / 9,488 out tokens, 1,720 neurons, ~$0.019 at vendor-stated pricing. ONE ITEM FOR CONDUCT, not written into DECISIONS.md by this session: the composed shape (detect the block, crop it, transcribe the crop) scored 99.62% / 10-of-10 digits / 0 minted on the one region with a trustworthy ground truth — n=1, UNPROVEN rather than refuted, and it is the only in-account shape that could carry an image-region anchor. It needs the SAME page-to-pixels renderer CPDF-12 was scoped to re-scope away on a NO-GO, so closing CPDF-12 and measuring the composed shape are alternatives rather than independent choices.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 9d6274a). Verified: battery 94/94 (5010), no product code. CPDF-12 re-scoped to the renderer + composed-shape probe (alternatives, not independent — the worker's decision item enacted); CPDF-10 waits on its verdict before external funding. The cap (C, never B; digits need attestation) recorded in MEASUREMENTS. No DEC — DEC-35's own structure covers the sequencing.
## CLAIM 2026-08-04 UI
session: ui26-agent (worktree agent-abd32f02bbb8a7cb0)
opened: 2026-08-04T14:36:00Z
paths: civicos-ui/app.html (ONLY: `loadResolveCandidates` and the comment block immediately above it — the per-alias `op=readingref` loop replaced by ONE `op=readingname` call with `entity=`, the candidate button gaining the correspondence the plane reports, and the `subj-note` limit sentence NARROWED; nothing else in the file), civicos-ui/test/intent-write.test.mjs (this item's assertions: the fixture gains ONE near-name document and a second enrolled member with a project the first is never invited to; the resolve arm rewritten; a coverage arm on `test/envelope-probe.mjs`'s precedent; this item's own `NEGATIVE CONTROL:` block), docs/development/CLAIMS.md (this entry)
interfaces consumed: I3 — `op=readingname` (REC-36, I3 5.9.0), READ ONLY and additive on this side; `op=concerns` unchanged; `op=readingref` NO LONGER CALLED from this surface (it is not removed from the plane and no other caller is touched).
interfaces owned: none. `bio-plane/**` is READ ONLY — the battery is run as an acceptance instrument and never written.
DISJOINT FROM REC-39 (plane, the login sentences), running concurrently: THIS CLAIM OPENS NO FILE UNDER `bio-plane/` AT ALL, and it does not open `civicos-ui/test/auth-surface.test.mjs`, whose `PLANE_WORDS` pins are the one civicos-ui line REC-39's landing is expected to move. There is no shared line and no merge to resolve.
explicitly NOT touched: every file under bio-plane/**, newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs, civicos-ui/test/auth-surface.test.mjs, every other civicos-ui test suite, the CATALOG and SEMANTICS blocks in app.html
expected: QUEUE.md UI-26 (REC-36's UI half). ONE `entity=` call to `op=readingname` replaces the N-call per-alias loop; the resolve control offers the grade-C tier's candidates for the first time; the limit sentence NARROWS to the MEASURED bound (term-normalised, alias-joined, diacritics deliberately unfolded — MEASUREMENTS.md 2026-08-04 REC-36) and never implies completeness; an uninvited member's list omits invisible documents because the op withholds the ROW, with nothing filtered surface-side. Reads go through UI-23's `recR` seam from birth.
accepts-when: as QUEUE.md UI-26. NEGATIVE CONTROLS run and recorded in `intent-write.test.mjs`'s own `NEGATIVE CONTROL:` block — restore the per-alias loop, and drop the narrowed sentence — each restored byte-identical. No push — CONDUCT integrates.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it: `node civicos-ui/test/run.mjs` exit 0 — 31 suites plus BOTH guards, intent-write 106 → **129 assertions**; **THE ARM-B COVERAGE LINE MOVED**, and it moved in BOTH directions at a constant 65 distinct ops — `readingname` JOINED it and `readingref` LEFT it (748 → 757 op answers), which is why the coverage assertion names the two ops and never a count. `cd bio-plane && npm run test:battery` 94/94 suites, **5010 assertions**, 75.0s — IDENTICAL to REC-36's landed line, which is the evidence that no plane file was opened.
  THE MEASUREMENT THAT SHOULD HAVE COME FIRST AND DID, before a line was written: `op=readingname` DOES NOT SUBSUME `op=readingref`, and the queue item's scope assumed it did. Probed against the real plane in miniflare on UI-13's own fixture: `readingref` answers on the REFERENCE STRING and returned BID_A + BID_C for the alias `contract:C-2024-88`; `readingname` answers on THE NAME A READING RECORDED (`reading_ref_terms` is projected from `reading_refs.label` ONLY — store.mjs `#writeReadings`, verified) and returned BID_C alone, at correspondence `name`. So the fixture had NO near-name candidate to offer at all, and the swap COSTS the identifier tier. Both facts shaped the item rather than being papered: the fixture gained a measured-class near-name document, and the loss is STATED on the page ("or by an identifier and no name at all, is not here") and asserted. RAISED FOR CONDUCT as the one genuine decision item — nothing in this claim decides it.
  AND THE SHAPE THAT MAKES THE LOSS SURVIVABLE, which is the part worth keeping: the bound is a bound on THE LOOKUP, not on the record. The suite DRIVES that rather than claiming it — registering "anticipated Recology contract", the name the reading actually recorded, brings BID_A back within reach through the SAME single call and it still resolves at Grade A, because the grade comes from the reference the plane reads out of the document and never from how the member found it. That is `loadResolveCandidates`' own improvability comment made reachable from the control for the first time, and it is why the grade-A arm did not have to move onto another document.
  THE ONE CORRECTION, never an exemption, dated at the site: "the document that names this subject by its own identifier is offered" INVERTED to "is no longer offered", beside a new assertion that the page says so. It was right while the control called op=readingref and is wrong now, and a suite that drove BID_A from the store while no surface proposes it would be testing the store (D-43).
  THE GATE IS THE PLANE'S AND NOTHING IS FILTERED HERE: four real members are enrolled for the first time in this suite (ruth/gus as the 4.2/4.3 administrators, CAROL owning a project, DAVE never invited), the same arrangement readingname.test.mjs uses, and the surface renders exactly what the op answered — a client-side filter would be a second, weaker gate that could only ever disagree with the SQL one.
  NCs RUN x3, every file restored byte-identical (app.html sha256 aa5bdc2f83e94f298cbff92838106773df1bb92e6ef877684194220d6688d0eb, intent-write.test.mjs 0f6deb191645cebc89de4a2f4b4d4e09f3afc55a3b630fe77547f106c0eb36f6, before and after every arm): (d) restore the per-alias loop → **14 of 129** fail and NAME THE OP, including all four coverage assertions, which is the arm-B line moving back; (e) drop the narrowed sentence → **6 of 129** fail; (f) the instrument's own, the gate arm driven as the INVITED member → **2 of 125** fail, so the arm reaches its subject.
  **ARM (e) IS THE FINDING.** The assertion that forbids completeness WORDS — "every document", "all the documents", "complete" — STAYED GREEN over a sentence with no bound in it at all. An unstated bound uses no completeness word and reads as completeness anyway, so a suite policing the vocabulary would have called the stripped sentence correct. Only the clause-by-clause assertions fired, which is why the measured bound is pinned as FOUR separate claims (normalisation, alias join, accents not folded, absence-says-nothing) and not as one string. UI-21's lesson at a new altitude, written into the file so nobody weakens it back into a wording check.
released: 2026-08-04 — landed on main by CONDUCT (merge of worker f98161e). Verified: harness 31 + both guards, battery 94/94. The identifier-tier trade accepted as shipped-narrowed-and-stated; REC-40 queued for the durable fix. No DEC.

## CLAIM 2026-08-05 RECORD
session: rec39-agent (worktree agent-a3e72264c172fbbd6)
opened: 2026-08-05T08:00:00Z
paths: bio-plane/checks/bio-checks.mjs (ONLY: a NEW `export const RESOLUTIONS` beside `ACTION_KINDS`/`ACTION_BASIS_KINDS`/`CORRESPONDENCE_DIRECTIONS` with its comment block, and `checkActionExtension`'s inline four-word literal + its finding sentence rewired to read it), bio-plane/src/affordances.mjs (ONLY: `RESOLUTIONS` added to the existing `bio-checks.mjs` import list, and a NEW `resolutions:` entry in `VOCABULARIES` with its comment), bio-plane/src/store.mjs (ONLY: `RESOLUTIONS` added to the existing `bio-checks.mjs` import list; `actionMove()`'s local `const RESOLUTIONS = [...]` literal DELETED and its two refusals reading the import; `login()`'s two refusals gaining `detail`; the DO dispatch's `login:` wrapper arm gaining the SAME `NO_SUCH_ROLE` detail from ONE static so the two arms cannot drift), bio-plane/test/members.test.mjs (GROWN: the login-refusal detail assertions, the two-arm identity pin and the D-57 instrument), bio-plane/test/affordances.test.mjs (GROWN: the `resolutions` drift pin, equal-by-import both directions), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts). PURELY ADDITIVE on the wire: `op=login`'s two refusals gain a `detail` field and neither reason CODE changes; `op=affordances`' `vocabularies` gains one key. No existing field, value or code changes, so no consumer breaks.
interfaces consumed: none changed.
DISJOINT FROM UI-26 (consume op=readingname), running concurrently on civicos-ui ground: THIS CLAIM OPENS NO FILE UNDER `civicos-ui/` AT ALL. UI-26 names `civicos-ui/app.html` (`loadResolveCandidates`) and its harness and names no file under `bio-plane/`. There is no shared line and no merge to resolve. `civicos-ui/**` is READ ONLY here, to establish who consumes the two publications: `app.html`'s `teach()` already renders `detail`, and `app.html`'s resolution chooser reads the `NO_RESOLUTION` refusal's `legal` list rather than the publication — a DELEGATION reported, not an edit.
explicitly NOT touched: civicos-ui/** (every file — read and run, never written), bio-plane/src/index.mjs, bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/setup.mjs, bio-plane/src/queuestate.mjs, every other bio-plane test suite, docprofile/**, newgroup/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md
expected: QUEUE.md REC-39 (UI-24's delegations, batched). (1) `login()`'s two refusals carry sentences in the store's own voice — D-57's rule taken as "true of the MECHANISM, never a claim about the caller"; (2) the four action RESOLUTIONS exported ONCE from where C-2.10 enforces them and published in `VOCABULARIES` by import, both enforcement sites rewired, so changing the array in one place moves all three.
accepts-when: as QUEUE.md REC-39. NEGATIVE CONTROLS run and recorded in each suite's own `NEGATIVE CONTROL:` line, restored byte-identical after each. No push — CONDUCT integrates.
paths AMENDED mid-item, because two suites grew beyond the opening claim and a claim that quietly grows is worse than one corrected: `bio-plane/test/members.test.mjs` additionally CORRECTS a pin it added in the same turn (the two-arm byte-equality had compared two callers that are BOTH answered by the wrapper, so the two arms were never compared — see the suite's `(b-instrument)` note), and `bio-plane/test/affordances.test.mjs` reads `checks/bio-checks.mjs` from disk for the structural no-literal-copy pin.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it: `cd bio-plane && npm run test:battery` 94/94 suites, 5026 assertions, 72.2s (5024 → 5026 across the two grown suites; affordances 63 → 69, members 71 → 81); `npm run test:coverage --strict` exit 0 (129/129 ops through the control plane, 53/53 checks, 94/94 suites declaring a control); `node civicos-ui/test/run.mjs` exit 0, 31 suites + both guards, NO file under `civicos-ui/` opened.
  (1) THE LOGIN REFUSALS' WORDS. `login()`'s two refusals carry `detail` from ONE static, `Store.LOGIN_REFUSAL_DETAIL`, and the reason CODES are untouched — additive on the wire, so nothing that pinned the codes moves. `civicos-ui`'s gate needs NO edit: `teach()` already renders `[reason, error, detail]`.
  THE DISTINGUISHABILITY QUESTION WAS MEASURED, NOT PREFERRED, and the measurement is the load-bearing part: `op=bootstrap` is `classes: null` — unauthenticated, no token — and its answer carries `roles`, EVERY role holding a credential with the date each password was last set. Probed against a live miniflare instance with one enrolled member: an anonymous GET returns `[{role:"admin",…},{role:"member:ruth",…}]`. So the role roster is already handed to any stranger in one call, more completely and more cheaply than login-probing could assemble it. Collapsing the two refusals would therefore defend nothing while making the store say less than it knows — and the reason codes have been distinct since birth with three suites pinning them, so only a code change (an I3 break) plus closing `op=bootstrap` would actually close enumeration. KEPT DISTINCT, with the reasoning written at the constant so it can be re-opened if that disclosure is ever closed.
  THE SECOND ARM FORCED THE WORDING, and this is the finding worth keeping: NO_SUCH_ROLE is returned from TWO places — `login()` (no credentials row) and the DO dispatch's `login:` wrapper (a credential EXISTS, the member is not active) — and the shared code is deliberate, the enrollment rule applied to the front door. The obvious sentence ("no role by that name is registered here") is FALSE for the revoked member, and writing a true sentence for each arm would have re-opened in prose exactly what the shared code closes. The sentence says "no ACTIVE credential", names BOTH possibilities and states that the record does not say which; both arms return the SAME string from one constant, pinned byte-equal.
  (2) RESOLUTIONS PUBLISHED. Exported from `checks/bio-checks.mjs` where C-2.10 enforces it — the `ACTION_KINDS` direction exactly, and forced for REC-35's reason (store.mjs already imports affordances.mjs, so exporting from the store closes an import cycle and crashes in the TDZ). THREE readers off one array: C-2.10's finding (whose SENTENCE is now derived too — it had transcribed the four words a second time inside the statement that tested them), `op=actionmove`'s NO_RESOLUTION refusal and its published `legal:` list, and `VOCABULARIES.resolutions`. Both hand-written copies are pinned dead BY NAME.
  NCs RUN x6, every file restored BYTE-IDENTICAL (sha256 compared before and after each arm: affordances.mjs 753aa75b…, store.mjs 84801ad6…, bio-checks.mjs eaa05176…). (e) publication as a literal copy -> 1 fail, the identity pin ALONE, every behavioural pin green — REC-35's zero-cost-copy finding, third restatement; (e2) the copy plus one extra word -> 5 fail naming both lists in four instruments, including the not-a-superset arm (a published word the act will not accept is the shape that costs a member a control that does not work); (e3) the store's local literal restored -> the structural pin alone; (e4) the catalogue's inline literal restored -> the same pin from the other site; (f-a) strip a login detail -> 2 fail naming the bare code; (f-b) give the wrapper arm its own honest-looking sentence -> 3 fail naming the two-arm equality.
  TWO INSTRUMENT FINDINGS KEPT IN THE SUITES, both of documented classes. Arm (f-a)'s FIRST run made members.test.mjs THROW instead of fail (`.detail.length` off an absent field) — D-93 inside a negative control, readingname's arm (a) again; every detail read is guarded now. Arm (f-b)'s first run fired only 2 and exposed a WRONG PIN, corrected not exempted: `member:nobody` and `member:meilan` are BOTH answered by the wrapper, so the two-arm comparison had never compared the two arms.
  AND THE MEASUREMENT THAT ROUTES A DELEGATION: under arm (f-a), `civicos-ui/test/auth-surface.test.mjs` STAYS GREEN at all 62 — its `PLANE_WORDS` mock answers `{reason:"BAD_PASSWORD"}` by hand, a copy that agrees at zero cost. The UI harness cannot see this class; the plane suite is its only instrument.
  UI DELEGATIONS REPORTED, NOT EDITED (UI-26 holds civicos-ui ground): (i) `app.html`'s resolution chooser (~:6209) reads the NO_RESOLUTION refusal's `legal` list, NOT `vocabularies.resolutions` — it holds no literal, so it invents nothing, but it can still only learn the set from a refusal; (ii) `action-page.test.mjs` holds `const RESOLUTIONS = ["complied",…]` with a comment saying the set has no published home — that comment is now stale and the array should join the UI-24 TIE; (iii) `auth-surface.test.mjs`'s `PLANE_WORDS` comment ("the plane publishes NO sentence") is now stale, and its login mock should carry the plane's detail so the arm-(d) residue instrument keeps its meaning.
  ONE FINDING FOR CONDUCT, out of scope and NOT closed here: `op=bootstrap`'s unauthenticated `roles` roster has NO consumer anywhere — not `setup.mjs`, not `civicos-ui`, not `newgroup`, and no assertion in the battery names it. It is an unconsumed disclosure of every credentialled role and the date each password was last set, and this item's distinguishability reasoning rests on it.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker e1fceb0). Verified: battery 94/94 (5026), --strict 129/129, harness green. The distinguishability measurement and the arm-ambiguous sentence accepted; D-184 + REC-41 routed; UI-25's batch gains the two suite riders; IC-17, I3 → 5.10.0. No DEC.

## CLAIM 2026-08-05 RECORD
session: rec33-agent (worktree agent-a7d62408d3b7f67e7)
opened: 2026-08-05T10:00:00Z
paths: bio-plane/src/index.mjs (ONLY: the token-class doc block's class list ~:203; `classify()` — one new `daemon` arm reading `env.DAEMON_TOKEN`, with DEC-37's reasoning at the site; `scopeFor()` — NO code change, one comment recording why the daemon class is deliberately NOT scratch-confined; `OPS.monitor.classes` and `OPS.acquire.classes` gain `"daemon"`; op=acquire's archive-arm class gate widened to admit `daemon` AND a NEW confinement refusal so a daemon credential reaching op=acquire without `via:"archive.org"` is refused; `op=selftest`'s `bindings` block reports `DAEMON_TOKEN` — NOT added to `bindingsAllPresent`, because it is optional by DEC-37's fallback), bio-plane/src/query.mjs (ONLY: `viewerPredicate`'s class alternation gains `daemon`, with the reasoning — op=monitor stamps `class:${cls}` on its own internal image read and the store fails closed, so without this the class would be inert rather than scoped), bio-plane/src/store.mjs (ONLY: `#monitorToken()` reads `DAEMON_TOKEN` first with the `ADMIN_TOKEN` fallback RETAINED, and its comment block), bio-plane/src/livefire.mjs (ONLY: the token-hygiene `names` list gains `DAEMON_TOKEN`; the list is filtered to CONFIGURED tokens, so an instance without one is unaffected), bio-plane/test/daemon-token.test.mjs (NEW — this item's suite), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts). PURELY ADDITIVE on the wire for every existing caller: no existing class list loses a member, no reason code changes, no field changes shape. The ONE new refusal is on a path no existing credential can reach (a `daemon`-class caller did not exist before this change).
interfaces consumed: none changed. I4 (the installer) is NOT touched — binding `DAEMON_TOKEN` on an installed instance is DIST's follow-on half, and DIST-1's constraint is binding in this direction: the plane learns the class BEFORE any installer binds it.
explicitly NOT touched: newgroup/** (every file), civicos-ui/**, docprofile/**, pdf-worker/**, tools/**, release/**, bio-plane/src/tokens.mjs (VERIFIED not assumed: the denylist is PER-VALUE — `PUBLISHED_TOKEN_HASHES` holds hashes and `liveToken()` takes a value, so a new binding NAME needs no entry; a `DAEMON_TOKEN` whose value was ever published is already refused by the same function), bio-plane/src/schema.mjs, bio-plane/src/setup.mjs, bio-plane/checks/bio-checks.mjs (VERIFIED: `ACTOR_CLASSES` already carries `daemon`, so op=acquire's existing `actor_class` stamp is already correct for the new class), bio-plane/package.json (its `cloudflare.bindings` block is deploy-button PROVISIONING metadata and belongs with the installer half), every other bio-plane test suite, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md
expected: QUEUE.md REC-33 under DEC-37 (Bob: MINT IT, `DAEMON_TOKEN` not `MONITOR_TOKEN` — the class is the UNATTENDED PATH, not the monitor). Scoped to EXACTLY the two verbs it needs today: op=monitor, and op=acquire's archive arm. Everything else refused, and the class's reach asserted STRUCTURALLY over the OPS table so a later op that admits `daemon` fails this suite until somebody answers for it (the REC-30 classification pattern). ADMIN_TOKEN stays the fallback so no installed instance breaks.
accepts-when: as QUEUE.md REC-33. `npm run test:battery` green; `npm run test:coverage --strict` exit 0; NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — grant the daemon class one op beyond its two and the suite names it — every file restored byte-identical after each arm. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session.
landed:
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker e63d6cb). THE MISSING VERIFICATION RUN BY CONDUCT after the disk cleared: battery 95/95 (5088), --strict 129/129 + 95/95 controls — the worker's 81/95 was all runtime-start failures under ENOSPC, zero failed assertions, now confirmed. The viewerPredicate addition and the DENY read posture accepted with their reasoning; DEC-43 raised; DIST-2 queued; IC-18, I3 → 5.11.0. No other DEC.

## CLAIM 2026-08-04 M0

session: m0-8-agent (worktree agent-a6a46e79597533833)
opened: 2026-08-04T09:35:00Z
paths: bio-plane/test/sandbox.mjs (NEW — the process-wide temp sandbox, imported for its side effect), bio-plane/scripts/battery.mjs (GROWN: the orphan sweep, this run's own `$TMPDIR`, and the before/after leak assertion; the discovery, tally, skip and reporting logic is untouched), bio-plane/test/hygiene.test.mjs (GROWN: one assertion block requiring the sandbox import of every suite that builds a Miniflare or mkdtemps, plus its `NEGATIVE CONTROL:` line), bio-plane/test/*.test.mjs × 84 (ONE LINE EACH and nothing else: `import "./sandbox.mjs";` inserted immediately before the suite's first existing import — no assertion, no fixture, no dispose call and no exit path is changed in any of them; verified `git diff --stat` = 1 insertion, 0 deletions per file), docs/development/DEBT.md (ONLY: D-186's Status cell), docs/development/CLAIMS.md (this entry)
interfaces owned: none — test estate, as QUEUE.md M0-8 states. No file under `bio-plane/src/`, `bio-plane/checks/` or `pdf-worker/` is opened, so no op, check, control or wire shape moves. A consumer of the plane cannot observe this change at all.
interfaces consumed: none changed.
NO OTHER WORKER HOLDS bio-plane/test GROUND, checked against the open claims in this file at the time of writing. The 84 one-line insertions are the whole reason to say so plainly: a claim on `bio-plane/test/**` reads as total, and it is not — every one of those files is touched on exactly one line, and any suite another item grows will merge with this cleanly because the line sits above the first import and nothing else in the file moves.
explicitly NOT touched: bio-plane/src/** (every file), bio-plane/checks/**, bio-plane/scripts/coverage.mjs, bio-plane/scripts/deploy.mjs, bio-plane/package.json, the PROBES (`test/*-probe.mjs`, `test/*-scale.mjs`, `test/scaling.mjs`, `test/*-worker.mjs`, `test/facet-probe.mjs`) — deliberately, per the item: they are probes and not battery, their npm-installs into temp are bounded separately, and `tier1-coverage-probe.mjs` keeps a PERSISTENT PDF cache in `$TMPDIR` (`cpdf5-oakland-pdfs`) that the sandbox would delete out from under it, civicos-ui/**, docprofile/**, newgroup/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md M0-8 under D-186. The three parts as scoped — (a) the harness cleans up whether the suite finishes or throws, (b) the suites that mint their own sandboxes are covered the same way, (c) `battery.mjs` sweeps ORPHANS at start, never a blanket wipe.
accepts-when: as QUEUE.md M0-8. `npm run test:battery` green twice in a row with the temp count not growing, and that comparison INSTRUMENTED inside the runner rather than left to somebody's eye; a suite made to throw mid-run leaves no sandbox behind, DRIVEN not reasoned; `npm run test:coverage --strict` exit 0; NEGATIVE CONTROL run and recorded. No push — CONDUCT integrates. DECISIONS.md is not written by this session.
SEQUENCING CORRECTION, stated rather than tidied away: this claim was appended AFTER the mechanism was measured and the code written, not before. The rule is before, and the reason it did not cost anything here is that the ground was uncontested — but a claim that appears after the fact protects nobody, and the next M0 worker should not read this entry as the pattern.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it:
  THE MECHANISM IS NOT THE ONE THE ITEM WAS WRITTEN ON, and this is the load-bearing correction. The item's part (a) says "the harness disposes of its sandbox in a `finally`, so a THROWING suite still cleans up — the leak is worst exactly when tests fail." MEASURED, three modes, three runs each, against `miniflare@4.20260722.0`: a suite that THROWS leaks 0 of 3, and a suite that succeeds leaks 3 of 3. It is exactly inverted. `Miniflare#dispose()` (`dist/src/index.js:104655`) calls `this.#removeExitHook?.()` — taking down its own synchronous `process.on("exit")` cleanup — and then `removeDir(this.#tmpPath, { fireAndForget: true })`, which it does not await. Every suite here ends `await mf.dispose(); process.exit(fail ? 1 : 0)`, as `hygiene.test.mjs` REQUIRES so a lingering handle cannot turn a green run into a hang, and `process.exit()` beats the unawaited removal with the safety net already gone. A throwing suite never reaches `dispose()`, so miniflare's exit hook is still armed and cleans up perfectly. **A `finally` around the suite body would have fixed nothing**, and a settle-delay (250 ms leaks 0 of 3) is a sleep, not a mechanism. So the leak is the SUCCESS path, it scales with GREEN integration throughput, and D-186's "23,263 directories in days" is the record of a battery working correctly.
  WHAT WAS BUILT INSTEAD: own the ground rather than chase the race. `os.tmpdir()` consults `$TMPDIR` on every call, so `test/sandbox.mjs` mkdtemps ONE directory, points the process at it, and removes it with `rmSync` — synchronously, in `process.on("exit")` plus the three signal handlers, because `process.exit()` runs `exit` listeners to completion before returning to the OS and therefore cannot outrun them. Miniflare still picks its own random name and still fails to finish removing it; it now does that inside a directory we delete outright. Part (b) comes free and structurally: `ratify-`, `sshsig-`, `signpage-`, `attest-`, `reuse-ratify-`, `publish-`, `publishedcase-` and `reeval-` all `mkdtempSync(join(tmpdir(), …))`, so they land inside the owned directory too — no per-suite disposal to write and none to forget.
  THE LIST MAINTAINS ITSELF. `hygiene.test.mjs` derives the requirement from the source — build a Miniflare or mkdtemp, and the sandbox import is required by name — so the D-93 failure (a hand-kept chain of 38 files while the directory held 41) cannot repeat here.
  THE SWEEP, AND WHY AN UNCONDITIONAL ONE IS WRONG. `bio-battery-<pid>-*` NAMES ITS OWNER, so liveness of that pid decides it with no clock at all. The legacy prefixes carry no owner, so age is the only evidence: the threshold is this run's start less a 2h GRACE, because "older than this run started" is NOT safe on its own — a concurrent battery that began sixty seconds before ours has directories older than our start and is still using them. OVER BOTH, a safety net that does not depend on the clock: any directory a live `workerd` or `node` still holds a descriptor in is spared outright, whatever its age (`lsof`, ~20 ms, best effort). That net is not decorative — WHILE WRITING THIS FIX a suite from the OTHER live worktree was found still running after 1h28m (`acquire.test.mjs`, pid 2236) holding `miniflare-bb85bff9…`, which a two-hour rule alone would have deleted out from under it. It survived all seven sweeps run this turn, verified by name.
  THE ASSERTION HAD TO BE MADE ATTRIBUTABLE, and the first version was wrong in a way worth recording: counting host-level `miniflare-*` before and after went RED on a green run, because the other worktree — which does not have this fix — was running its own battery into the same `$TMPDIR`. A false red on the battery is worse than the leak it reports. So the runner now takes a sandbox of its own and spawns every suite with `$TMPDIR` pointing at it: each child writes inside this run's directory whether the suite cooperates or not, because the ENVIRONMENT decides where `os.tmpdir()` points and the suite does not. What remains there when the suites finish is this run's leak exactly, with no attribution argument to make. The host count is still taken before and after — it is the number D-186 is written in — but it is REPORTED and attributed, and only the run's own residue fails the run.
  NUMBERS. Battery green TWICE IN A ROW: 95/95 suites, 5172 assertions, 84.7s then 88.1s, each leaving `0 directories holding 0 miniflare sandboxes` (0/0 before the suites ran). `npm run test:coverage --strict` exit 0 — 129/129 ops through the control plane, 53/53 checks named, 95/95 suites declaring a control, fleet 1/1. Baseline for comparison: before the fix, four suites measured individually leaked +1 each (`archive-monitoring`, `attest`, `capture-progressions`, `connection-derive-sweep`, and `audit`), and the machine was carrying 1,594 orphans at the start of this turn after the 37.2 GB hand cleanup.
  NEGATIVE CONTROLS RUN, both restored byte-identical (sha256 compared): (a) THE SUBJECT — comment out `process.on("exit", sweepSandbox)` in `test/sandbox.mjs` (`ff8b3b60…`) -> battery exits 1 reporting `LEAKING 84 miniflare sandbox(es) in 84 director(ies)`, which is one per suite that mints temp and is the pre-fix state exactly, WHILE ALL 95 SUITES STILL REPORT GREEN AND 5172 ASSERTIONS STILL PASS — which is precisely why this ran for weeks unnoticed and why the instrument had to live in the runner rather than in a suite; (b) THE GUARD — strip the `import "./sandbox.mjs"` line from `purge.test.mjs` (`f2ee2192…`) -> 1 hygiene assertion fails naming the file, 342 pass.
  THE THROW WAS DRIVEN, NOT REASONED. A real throw was injected into `audit.test.mjs` (`56c81869…`, restored byte-identical) after its workerd instance was live and its sandbox on disk: through the battery the suite FAILED and the run left `0 directories holding 0 miniflare sandboxes`; run directly outside the battery, host temp went 331 -> 331, new: none.
  ONE FINDING FOR CONDUCT, not closed here: the host's orphan count kept RISING during this turn's green runs (+71, +16) from the other worktree's un-fixed battery. That is expected and it is reported rather than failed on — but it means the disk keeps filling until this lands on main and every live worktree picks it up, and the sweep only drains what is older than the grace window. It is a reason to integrate this ahead of the other M0 work, not a defect in the fix.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 7b39d33), integrated AHEAD of other work as the worker advised: every live worktree keeps filling the disk until this is on main. Verified from the main checkout: battery 95/95 (5172), 0 directories left, --strict 129/129. The inverted premise, the three-rule sweep and the attributable assertion all accepted with their measurements. The claim-after-measuring sequencing self-reported rather than smoothed — noted and accepted (uncontested ground). No DEC.
## CLAIM 2026-08-05 RECORD
session: rec42-agent (worktree agent-ae1b65aba4fc10981)
opened: 2026-08-05T12:00:00Z
paths: bio-plane/src/schema.mjs (ONLY: the `inquiry_basis` CREATE TABLE gains one nullable `ground` column and its comment block; no other table touched, nothing moved relative to the `host_governor` block), bio-plane/src/store.mjs (ONLY: the additive-column list gains `["inquiry_basis","ground","TEXT"]`; the `inquiry_basis` projection insert in `promote` carries `ground`; `basisFor()` selects it; `#strengthWalk` carries it onto every member; `#axisResult` becomes the OR composer over a new `#groundResult`; `#redactAxis` sweeps the per-ground objects; `publish()` freezes `published_strength_grounds`), bio-plane/checks/bio-checks.mjs (ONLY: `checkInquiryBasis` gains the ground grammar; a new `checkGroundsBlock` beside it; `checkPublishedExtension` requires the frozen per-ground block when the basis is structured), bio-plane/test/grounds.test.mjs (NEW — this item's suite), bio-plane/test/strength.test.mjs + bio-plane/test/basis.test.mjs + bio-plane/test/publish.test.mjs + bio-plane/test/inquirystrength.test.mjs + bio-plane/test/publishedcase.test.mjs (CORRECTIONS ONLY where a landed assertion or header sentence pinned the FLAT implicit-AND shape, each dated at the site, never exempted), bio-plane/package.json (ONLY: one `test:grounds` script line), docs/development/CLAIMS.md (this entry)
interfaces owned: I5 (the store schema) and I3 (the op contracts). ADDITIVE on both: `inquiry_basis` gains one nullable column; `op=inquirystrength` / `/strength` axis objects gain a `grounds` array ONLY when the basis was structured, so an unstructured answer is BYTE-IDENTICAL to today; a published case gains `published_strength_grounds` only when structured. No existing field, value or reason code changes.
interfaces consumed: none changed.
DISJOINT FROM M0-8 (the bio-plane TEST HARNESS ground), running concurrently: M0-8 owns `bio-plane/scripts/**` (the battery/coverage runners) and the harness mechanics; THIS CLAIM OPENS NO FILE UNDER `bio-plane/scripts/` AND NO HARNESS FILE. What this claim opens under `bio-plane/test/` is SUITES (grounds.test.mjs new; five existing suites corrected at named assertions), which M0-8 does not write. If M0-8 lands a runner change and this lands after, there is no shared line; whoever lands second merges.
explicitly NOT touched: bio-plane/scripts/** (every file — run as instruments, never written), bio-plane/src/index.mjs, bio-plane/src/query.mjs, bio-plane/src/setup.mjs, bio-plane/src/affordances.mjs, newgroup/**, civicos-ui/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md
expected: QUEUE.md REC-42, enacting DEC-32 and CORRECTING shipped code. Legs group into GROUNDS; the derivation is MIN over AND legs and MAX over OR branches, per axis, both axes independently (DEC-21 unchanged). THE DEFAULT IS AND and it is a correctness requirement: an unstructured basis stays weakest-leg, and independent sufficiency is reachable only through an AFFIRMATIVE ATTRIBUTED act (a `grounds[]` block naming who asserted it and when, refused at BOTH gates by the one function REC-11 established). R1 composes one level up: a suspended leg suspends its BRANCH; the axis suspends only when EVERY branch is. Q14's contradiction case stays SEPARATE and UNDESIGNED and is not modelled here.
accepts-when: as QUEUE.md REC-42. `npm run test:battery` green; `npm run test:hygiene` green; `npm run test:coverage --strict` exit 0. NEGATIVE CONTROLS run and recorded in `grounds.test.mjs`'s own `NEGATIVE CONTROL:` line — make the default OR; compose across axes; suspend the finding on one suspended branch — each restored byte-identical. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it: `cd bio-plane && npm run test:battery` **96/96 suites, 5156 assertions, 83.3s** (95 → 96 suites: `grounds.test.mjs` NEW at 65 assertions; basis.test.mjs 28 → 29; every other suite's count unchanged, which is the evidence the correction preserved the landed answers rather than rewriting them); `npm run test:hygiene` green (483); `npm run test:coverage --strict` exit 0 — 129/129 ops through the control plane, 53/53 checks named, 96/96 suites declaring a control.
  THE SHAPE CHOSEN, and the reasoning is at the site in schema.mjs. THE PARTITION is ONE NULLABLE COLUMN, `inquiry_basis.ground`: a leg belongs to exactly one ground and the row is already keyed (bundle_id, ord), so D4's own argument for the table gives the column here. THE ATTRIBUTION is a top-level `grounds[]` block in bundle.md — one row per label carrying `asserted_by` and `at` — and is deliberately NOT PROJECTED: it is per (bundle_id, ground), so a column would state it once per LEG (D-21's second place to state one fact), and no query anywhere asks a question keyed on it. It is enforced at BOTH GATES by one catalog function (REC-11's precedent) and frozen into the ratified bytes, which is where a reader checks it. What would earn the table later is named at the site: a query like "which grounds did this member assert".
  TWO TOP-LEVEL KEYS, forced by the restricted grammar and not chosen — a block is a map of scalars or an array of objects, never a map holding an array of objects. `ground` (scalar, on the leg) + `grounds` (array, the act), exactly REC-14's `completeness`/`completeness_excluded` and REC-16's `division`/`division_apportionment`.
  THE DEFAULT IS AND IN TWO INDEPENDENT PLACES, and that is the part worth keeping. The GATE refuses a label with no attributed row, a half-labelled basis, a machine asserter, an undated assertion, a duplicate label and a ground no leg belongs to. THE ARITHMETIC defends the same rule alone: an UNLABELLED leg is NECESSARY, so the axis is the MINIMUM of (weakest unlabelled leg, strongest ground) and a row that reaches the projection around the gate can only make an answer WEAKER. Driven through the raw-insert door and asserted. An unstructured basis reads weakest-leg BYTE-IDENTICALLY — no `grounds` key on the axis, no `ground` field on a named member, the landed sentence unchanged — and that is asserted as a SHAPE, not just as values.
  WHAT REC-14'S FROZEN PAIR NOW FREEZES: the pair itself is the composed answer (MAX over grounds), and a structured case additionally freezes `published_strength_grounds` — one row per (axis, ground) with state, grade, weakest, load_bearing, population. Required by the catalog exactly when the basis names grounds, absent otherwise (an unstructured case's two axis objects are already the whole truth). The reason is DEC-32's third containment: OR takes the maximum, so "each of these was independently sufficient" must be a claim the READER can test, which needs the branches in the signed bytes.
  D-160 COST TIME AND IS WORTH NAMING: DEC-32 and REC-42 both state R1's branch composition using the RETIRED word, and strength.test.mjs holds the whole derivation region against it — including a comment that merely QUOTES it (UI-18 found the same thing). The code says `undetermined` for a branch the walk could not finish and UNRATED for one with nothing established, and the translation is written at the top of #groundResult so the next reader of either entry is not left to guess.
  CORRECTIONS, dated at the site and never exemptions: (1) store.mjs's REC-12 header paragraph "SINGLE-BASIS ARITHMETIC (DEC-32, still open)" REPLACED — it was right while DEC-32 was open and is wrong now, and its own prediction (the partition sits ABOVE the within-branch rule) held exactly; (2) strength.test.mjs's header no longer claims weakest-leg is the WHOLE rule, with the finding recorded that not one of its assertions moved because every basis in it is unstructured; (3) basis.test.mjs's header drops "DEC-32 (single-basis arithmetic)" as a folded ruling and gains ONE assertion, that an ungrouped leg projects a NULL ground. publish.test.mjs, publishedcase.test.mjs and inquirystrength.test.mjs were READ for flat-shape pins and carry NONE — their subjects are C-21.2, the rendering and the gated read — so nothing there was touched.
  NCs RUN x5, every file restored BYTE-IDENTICALLY (sha256 compared before and after each arm), recorded in grounds.test.mjs's own NEGATIVE CONTROL line with what each MEASURED: (a) the default made OR → 11 fail here and the unstructured basis reads B/A where it must read C/C, plus failures in strength.test.mjs and inquirystrength.test.mjs; (b) the axes composed → 15 fail; (c) one unfinished branch suspending the finding → 3 fail; (d) the gate's refusal neutered → 4 fail at BOTH gates; (e) the per-branch redaction dropped → 3 fail, dave handed a project id inside a branch while the axis-level field beside it is correctly null.
  ONE INSTRUMENT FINDING, of a documented class: arm (c)'s first run THREW instead of failing (D-93 inside a control), so every `weakest` read in the new suite is optional-chained and the arm re-ran as three named failures.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 503b5c7). Verified: battery 96/96 (5242), hygiene 483, --strict 129/129, zero leaked sandboxes. Both M0-8 guards fired at the merge on the suite that predated them — the import added at integration, dated. The column-not-table and not-projected shapes accepted with their reasoning; REC-45 queued ahead of UI-27; the D-160 collision amended in DEC-32's entry and REC-42's scope. No DEC.

## CLAIM 2026-08-04 RECORD

session: rec45-agent (worktree agent-ab2798b258844d767)
opened: 2026-08-04T11:00:00Z
paths: bio-plane/src/index.mjs (ONLY: one `OPS.inquiryground` entry with its class list; a new `STRUCTURE_ACTIONS = ["inquiryground"]` array beside `DECLARATION_ACTIONS` and its reasoning; that array added to the viewer-stamp condition, the author-stamp condition and BOTH `SESSION_OPS` lists; one `NEEDS.inquiryground = "contribute"` line with its reasoning — NO new capability token), bio-plane/src/store.mjs (ONLY: a new `groundInquiry()` method placed after `divide()`; one new private helper `#spliceBasisGround()` beside `#spliceEdgeStatus`; one DO-dispatch entry `inquiryground:`; NOTHING in `promote`, `#strengthWalk`, `#axisResult`, `#groundResult`, `#redactAxis` or `publish` is changed — REC-42's arithmetic and gates are consumed, not edited), bio-plane/src/affordances.mjs (ONLY: one `GROUND_PROMPT` export and one `ACTS` entry for `inquiryground` carrying it), bio-plane/test/inquiryground.test.mjs (NEW — this item's suite, carrying `import "./sandbox.mjs";` per M0-8/D-186), bio-plane/test/affordances.test.mjs + bio-plane/test/conclude.test.mjs + bio-plane/test/divide.test.mjs + bio-plane/test/reopen.test.mjs (CORRECTIONS ONLY, added in flight — see the amendment note below — at the named assertions that pin an inquiry's published act SET or the COUNT of acts carrying a prompt, each dated at the site, never exempted and never loosened), bio-plane/package.json (ONLY: one `test:inquiryground` script line), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts). PURELY ADDITIVE: one new op, one new act published by op=affordances, one new prompt field on that act (the field itself already exists — REC-16 added it for `inquirydivide`). No existing op, reason code, field or wire shape changes. I5 (the store schema) is NOT touched — `inquiry_basis.ground` already exists (REC-42) and this act writes it through the EXISTING promote projection.
interfaces consumed: none changed. `checkInquiryBasis`/`checkGrounds` in `bio-plane/checks/bio-checks.mjs` are CALLED and NOT EDITED — the item requires the catalog's one function at both gates and a second grammar is exactly what must not appear, so that file is on the not-touched list below.
explicitly NOT touched: bio-plane/checks/bio-checks.mjs (deliberately — see above; the act pre-flights through the catalog's own function and adds no grammar of its own), bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/setup.mjs, bio-plane/scripts/** (run as instruments, never written), bio-plane/test/grounds.test.mjs and every other existing suite, newgroup/**, civicos-ui/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md REC-45, routed out of REC-42 and sequenced BEFORE UI-27. `op=inquiryground` on op=publish's stamping shape: it AUTHORS/EDITS the partition REC-42 built and nothing hand-writes frontmatter to reach it. `asserted_by`/`at` are SERVER-STAMPED and a caller's values are DELETED before the stamp (the op=promote `ownerMemberId`/`author` precedent) — REC-42's gate already refuses a machine asserter and an undated assertion, and this closes the last door. An authored reason is REQUIRED on a RESTRUCTURE and NOT on first authorship (DEC-32 clause 6), with the distinction decided from the RECORD and never from the caller. The catalog's one `checkGrounds` runs at BOTH gates. A machine credential is refused BY NAME. Published through op=affordances from the one act source; `contribute`, no new capability token.
accepts-when: as QUEUE.md REC-45. `cd bio-plane && npm run test:battery` green; `npm run test:coverage --strict` exit 0 reaching the op; NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — accept a caller-supplied `asserted_by` and the suite fails (an attribution the caller can hand us is one a caller can invent). Every file restored byte-identical after the control. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session.
CLAIM AMENDED IN FLIGHT, stated rather than tidied away: four EXISTING suites — `affordances.test.mjs`, `conclude.test.mjs`, `divide.test.mjs`, `reopen.test.mjs` — were opened at named assertions and are added to `paths` above. They were not foreseen when this claim was written and are not scope creep: each pinned the published act set of an inquiry, or the count of acts carrying a prompt, and a thirteenth act makes those pins false. CLAUDE.md's rule is that a superseded test is CORRECTED with a dated reason and never exempted, so each carries one at the site. No assertion was loosened — the prompt pin was rewritten to name the WHOLE set rather than relaxed to "at least one", so an unattached prompt still fails it.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it. Worker commit 44a0fc2 (this worktree; NOT pushed).
  NUMBERS. `npm run test:battery` **97/97 suites, 5327 assertions, 88.4s** (was 96/96, 5242). The +85 is ATTRIBUTED rather than asserted: 81 the new `inquiryground.test.mjs`, +1 `affordances.test.mjs` (69 -> 70, one prompt assertion rewritten as two so each prompt is pinned to the constant of the module that owns it), +3 the two hygiene suites (569 -> 572), which derive per-FILE assertions and therefore grow by three whenever a suite is added. **`conclude` 49, `divide` 82, `reopen` 57 and `grounds` 65 are UNCHANGED** — measured against HEAD~1 rather than assumed — which is the evidence the four corrections moved the pins and nothing else. `npm run test:hygiene` green (572). `npm run test:coverage --strict` exit 0 — **130/130 ops through the control plane** (129 -> 130, the new op reached), 53/53 checks named, 97/97 suites declaring a control.
  THE SHAPE, and the reasoning is at each site. AUTHORED: which legs group together, the label, an optional statement, and the reason for a restructuring. STAMPED: `asserted_by`/`at`, per group. The caller's values are NEVER READ — the row object is never consulted for either field, which is what makes "DISCARDED" an assertable claim rather than "overridden" (a value that merely loses an assignment is one an extra code path can win back). The partition is addressed by ORDINAL and never by target id: REC-16's apportionment decision, for D4's reason exactly — one document legitimately carries two legs.
  FIRST AUTHORSHIP vs RESTRUCTURE, DECIDED FROM THE RECORD. The inquiry is in first authorship when its standing document carries NO partition at all (no leg names a group AND there is no `grounds` key); everything else is a restructure and takes a reason. It is not a parameter and must never become one — a caller who could declare "this is my first time" would walk past the reason gate on every restructuring they made. REMOVING a partition is a restructure too (it changes an authored structure; that it moves in the conservative direction is a fact about the ANSWER, not about the act).
  WHAT CARRIES FORWARD, which is the other half of making DEC-32 clause 6 legible: a group whose LEG SET and STATEMENT are unchanged keeps the `asserted_by`/`at` it already had, and a group whose membership moved becomes THIS member's assertion, NOW. Re-stamping everything would erase the one thing the ruling says the date is for; carrying everything forward would let a member re-cut a group under somebody else's name and an older date.
  ONE GRAMMAR AT BOTH GATES, MEASURED AND NOT INTENDED. The candidate document is judged by the catalog's own `checkInquiryBasis` over the same two registries `promote` injects, BEFORE a byte moves, and refuses under promote's own `BASIS_REFUSED` name. The suite drives FOUR REC-42 refusals (bad label, duplicate label, empty group, half-labelled basis) through the ACT and through op=promote on a hand-written document and asserts the check id AND THE MESSAGE STRING are equal — a second grammar written inside the act would pass an id comparison and fail this one. Only the two refusals the DOCUMENT cannot express are the act's own (an ordinal naming no leg; one leg claimed by two groups).
  A PROMPT WAS WARRANTED AND IS PUBLISHED (DEC-29(b), REC-16's mechanism), on division's argument one notch sharper: OR takes the MAXIMUM, so this is the one act in the record that raises a grade with no new evidence at all, and DEC-32 names that hazard in those words. EVERY CLAUSE IS MECHANISM this plane enforces, asserted clause by clause. NO DOCTRINE WORDING WAS INVENTED: the one sentence that would have been a claim about the reasoning a member must do — DEC-32's operational test, "would refuting this alone change your conclusion?" — is REPORTED as a DEC candidate and NOT written, because the ruling assigns the elicitation to UI-27. And DEC-32 clause 1 is binding on every member-facing string: the prompt and the LABEL say neither AND, OR, disjunction, branch nor the word `ground` (the wire name is `inquiryground` because a wire name is not a surface), asserted directly.
  NO NOTICER WAS BUILT, deliberately. DEC-32 says the system MAY notice a weak leg moved into its own group after a strength drop; building it inside the act would put a judgement about the member's motive in the one function that must never refuse them. What the act does instead is leave the evidence where an append-only history keeps it: the composed pair AS IT STOOD BEFORE the act, written into the Session Log entry beside the reason, and returned to the caller as `strength.before`/`strength.after`.
  NEGATIVE CONTROL RUN, src/store.mjs restored BYTE-IDENTICALLY (sha256 1f6323dc2ceedaf41cce04716a790d88a3df5b07e5fa11475cc5a3da29e0146c compared before and after): honour a caller-supplied attribution -> 74 pass, 7 FAIL, all seven in block 2 and every one naming an attribution nobody made, while blocks 1 and 3-8 stay green — the act still works perfectly and only the ATTRIBUTION has become the caller's, which is exactly why it is invisible in every other measurement.
  AND THE CONTROL MEASURED SOMETHING THE ITEM DID NOT PREDICT, ROUTED TO CONDUCT: the caller's `asserted_by: "token:member"` LANDED IN THE DOCUMENT AND THE GATE ACCEPTED IT. `checkGrounds` refuses an asserter in `NON_MEMBER_AUTHORS` ("agent", "claude", "daemon", ...) and knows nothing about the `token:<class>` prefix `index.mjs` actually stamps for a machine credential — so REC-42's "the gate already refuses a machine asserter" holds for the WORD LIST and not for the SPELLING the plane itself uses. This act closes it for every caller arriving through an op (it never reads the field); the hand-written op=promote door is still open. NOT fixed here: `checks/bio-checks.mjs` is REC-42's ground and the fix is a question about having ONE machine-identity predicate rather than two.
  ONE FURTHER FINDING, DRIVEN AND ASSERTED (block 8) rather than reasoned: once a question is GROUPED, `op=cite` is REFUSED at the write by REC-42's own total-or-absent rule, because a new leg arrives with no group. That is not obviously wrong and not obviously right, and the route through it is THIS act — ungroup with a reason, cite, group again, three attributed acts — which is only reachable because this item was built. Whether a new leg should instead default to NECESSARY is doctrine, so it is reported as a DEC candidate and not decided here.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker eece435). Verified: battery 97/97 (5327), hygiene 572, --strict 130/130, zero leaked sandboxes. The from-the-record authorship test and the per-group carry-forward accepted with their reasoning; the in-flight claim amendment self-reported and accepted (pins moved, nothing loosened). REC-46 queued, DEC-46 raised, the operational-test wording ruled onto UI-27; IC-19, I3 → 5.12.0. No other DEC.

## CLAIM 2026-08-04 UI

session: ui27-agent (worktree agent-aaff9a3d9ef4fdfc1)
opened: 2026-08-04T20:00:00Z
paths: civicos-ui/app.html (ONLY: (1) the `__PUBLISHED_CASE__` region — `THRESHOLDS`/`stanceOf`/`stanceFloors`/`pubStance`/`pubSelectorHtml` REMOVED and replaced by a reader-supplied pair of floors defaulting none/none, the filter line added to `dec34Header`, `pubInbandHtml` and `pubFooterHtml`, and one new self-naming function every one of them renders through; (2) a NEW `__ELICITATION__` region carrying DEC-32's consequence elicitation for `op=inquiryground`, placed after the conclude act; (3) one `ACT_FLOW` entry, `inquiryground`), civicos-ui/test/publishedcase.test.mjs (CORRECTIONS to UI-18's stance assertions, each dated at the site, never exempted — plus the new filter-line arms), civicos-ui/test/elicitation.test.mjs (NEW — this item's second suite), civicos-ui/check-mock-envelope.mjs (ONLY: `elicitation.test.mjs` added to `SUITES_WITH_OP_TRAFFIC`), docs/development/CLAIMS.md (this entry)
interfaces owned: none. UI CONSUMES I3 and changes no op contract, no wire shape and no plane file.
interfaces consumed: I3 — `op=publishedcase`, `op=publishedmanifest`, `op=verify`, `op=publishedbytes` (unchanged), `op=affordances` and `op=inquiryground` (REC-45, landed; consumed exactly as published — per-leg `ground` label plus a `grounds[]` row per label with `asserted_by`/`at` SERVER-STAMPED, an optional per-group `statement`, NO per-group falsifier).
explicitly NOT touched: bio-plane/** (read as the contract, never written — REC-44 runs concurrently on the plane's published/case ground and is fully disjoint from these surfaces; whoever lands second merges), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, civicos-ui/check-semantics.mjs, every other civicos-ui suite, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md UI-27, enacting DEC-40 (a CORRECTION to shipped code — UI-18's four named stances) and DEC-32's ELICITATION half. (a) the named stance set and its labels removed, replaced by a READER-SUPPLIED pair of floors defaulting none/none; (b) every rendering produced under a reader's floors carries the FILTER in DEC-34's per-page header and is never presented, printed or hash-described as "the case", and an UNFILTERED rendering states that it is unfiltered; (c) the determining/UNRATED-leg protection unchanged; (d) the new strip-the-filter-line negative control. Plus DEC-32: no analyst vocabulary on any surface, ONE CONSEQUENCE QUESTION per leg, the derived falsifier SHOWN BACK, structure authored BEFORE strength is shown, restructuring-after-seeing RECORDED and ATTRIBUTED rather than blocked, and DEC-32's operational test rendered VERBATIM from the entry.
accepts-when: as QUEUE.md UI-27. `node civicos-ui/test/run.mjs` green; `cd bio-plane && npm run test:battery` stays green; NEGATIVE CONTROLS run and recorded in each suite's own `NEGATIVE CONTROL:` line — strip a filter line; present a filtered rendering as the case; show strength before the structure is authored; leak analyst vocabulary onto the surface. Every file restored byte-identical after each arm. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it. Worker commits c46f365 (DEC-40), d58067e + c4bd50e (DEC-32), this worktree, NOT pushed.
  NUMBERS. `node civicos-ui/test/run.mjs` **32/32 suites green** (was 31): `publishedcase.test.mjs` 114 -> **131**, `elicitation.test.mjs` **NEW at 47**, every other suite unchanged. check-semantics green; check-mock-envelope green — arm A over 12,911 lines with the seam set unchanged, arm B 32 suites re-run under the probe, 24 driving ops, 767 op answers across 66 distinct ops (`inquiryground` joins the coverage). `cd bio-plane && npm run test:battery` **97/97 suites, 5329 assertions, 85.6s**, zero leaked sandboxes — BYTE-UNTOUCHED by this item, which changes no plane file. `node tools/plancheck.mjs` clean but for the expected UNPUSHED line.
  DEC-40, AND WHAT IT REMOVED. `THRESHOLDS`, `stanceOf`, `stanceFloors`, `pubStance` and `pubSelectorHtml` are GONE, and the suite asserts they are gone AND that not one of the four published stance labels survives anywhere in the file — on D-160's precedent that a retired string quoted in a comment is a string somebody restores. The reader supplies the pair through two independent per-axis controls, defaulting none/none, and there is no preset combination of them anywhere. Q6's FORM is untouched: a complete pair or no rendering at all, `none` rendered explicitly, the determining/UNRATED-leg protection unchanged.
  THE FILTER LINE IS ONE FUNCTION, and that is the design decision worth keeping. Every place the rendering names itself — DEC-34's per-page header, the in-band block, the statement above the title, the footer beside the rendering hash — renders through `pubRenderingName`/`pubFilterHtml`, so a filtered page cannot be a view in one place and the case in another. The harness counts `data-filter` marks at pages+2 in BOTH directions, because an unfiltered rendering that said nothing would make absence of the line the ambiguity.
  DEC-32's ELICITATION, and the mechanism is one sentence: **"If this turned out to be wrong, would your answer still hold?"** asked per reason, with ONE follow-up — which of the others stop carrying the answer the moment this one does. The sets are the connected components of that relation; the member is never asked for a structure, a count, a name or a relationship. The derived falsifier is shown back in the ruling's own two shapes and is correctable. NO strength appears in the flow until the record has answered the act, asserted over the markup AND over the wire.
  THE CONSERVATIVE DEFAULT IS REACHED THROUGH THE ANSWERS, not only through omission: ONE necessary reason and the flow authors NOTHING and offers no control that would, because the record holds a partition WHOLE or not at all (C-2.8) and the honest whole is the ungrouped one — the act's own refusal says so in its own words. A revision starts from EMPTY: the member's earlier answers are deliberately not prefilled, because a prefilled "yes" would carry a sufficiency claim forward by omission, which is the one way round the keystone.
  NCs RUN x8, `civicos-ui/app.html` restored BYTE-IDENTICALLY after every arm (sha256 214610070f1fc24fb6572605d6336e37196bb76487eea618ba2eaeb73c74b81c compared before and after each), all eight re-run against the FINAL file so every count in both suites' headers agrees with the file it names. DEC-40: strip the filter line -> 4 of 131; present a filtered rendering as the case -> 3 of 131; arms (d) and (e) RE-POINTED at the new seam (their old subjects no longer exist) and re-run -> 5 and 2. DEC-32: show a strength before the structure is authored -> 3 of 47; leak the analyst's vocabulary -> 1 of 47 naming all 12 hits; prefill the earlier answers -> 1 of 47; send an attribution -> 2 of 47.
  TWO INSTRUMENT FINDINGS, both of the documented class and both written into the suites. (1) Arm (j) — the `data-filter` MARKS stayed correct on every page while the WORDS lied, because the marks are computed and the sentence is written; only the word assertions fired. UI-18 arm (c)'s lesson at a new altitude. (2) Arm (k)'s FIRST run fired only ONE assertion, because the mock had no answer for `op=inquirystrength` and the illicit preview therefore rendered empty — "no grade appears here" was true whether the surface asked for one or not, an outcome that costs nothing. The mock now answers that op, the arm was re-run, and 1 failure became 3.
  ONE COLLISION NAMED AND NOT SMOOTHED AWAY, reported to CONDUCT as a decision item: DEC-32's entry states its operational test in a form containing a word its own clause 1 forbids on any surface. The surface renders the form CONDUCT's ruling on this item quotes — *"Would refuting this alone change your conclusion?"* — and the reasoning is at the site.
released:

released: 2026-08-04 — landed on main by CONDUCT (merge of worker caaf699). Verified: harness 32/32 + both guards, battery 97/97 (5329), zero leaked sandboxes. The connected-components elicitation and the one-function filter accepted; the DEC-32 vocabulary collision amended in the entry with the D-160 shape (the original kept above — a ruling that had to be re-spelled to be sayable is worth seeing). No DEC.
## CLAIM 2026-08-04 RECORD

session: rec44-agent (worktree agent-a00794ac26389e972)
opened: 2026-08-04T13:00:00Z
paths: bio-plane/src/schema.mjs (ONLY: two NEW tables `published_cases` and `published_case_members` placed before the `host_governor` block, plus the `completeness` column MOVING OFF `published_bundles` onto `published_cases` with the reasoning at the site), bio-plane/src/store.mjs (ONLY: `publishCase` takes a SET and mints/carries a case identity; the `publish()` ratify committer writes the case row and the membership from the RATIFIED BYTES; a new `recordCaseManifest`; `publishedCase` answers per CASE with `findings[]`; a new `publishedCaseRegistryFor`; `publishedRegistryFor` loses `completeness` and keeps the per-FINDING pair; `publishedEditions`/`publishedList` follow; three DO-dispatch entries), bio-plane/src/index.mjs (ONLY: op=ratify reads `case_id`/`case_findings`/`case_scope` out of the ratified bytes and builds the CASE manifest when the edition completes; op=publishedcase renders `findings[]` and NO case-level strength; the `DO_PATH` map unchanged), bio-plane/src/gate.mjs (ONLY: one new injected seam `publishedCaseRegistry` beside `publishedRegistry`), bio-plane/checks/bio-checks.mjs (ONLY: C-21.1 moves to the CASE altitude and gains the scope statement; `checkPublishedExtension` requires the case keys; C-21.2 and `checkInheritedLeg` are NOT touched — they stay per FINDING), bio-plane/test/multifinding.test.mjs (NEW — this item's suite and Bob's negative control, carrying `import "./sandbox.mjs";` per M0-8/D-186), bio-plane/test/publish.test.mjs + bio-plane/test/publishedcase.test.mjs + bio-plane/test/reevaluation.test.mjs + bio-plane/test/hygiene.test.mjs + any suite pinning one-inquiry-per-case (CORRECTIONS ONLY, dated at the site, never exempted), bio-plane/package.json (ONLY: one `test:multifinding` script line), docs/development/CLAIMS.md (this entry)
interfaces owned: I5 (the store schema) and I3 (the op contracts). **BREAKING BY DESIGN and that is the item**: DEC-44/D-187 rule that the one-inquiry-per-case shape was never chosen. `op=publish` gains `targets[]` (the singular `target` stays legal as DEC-44 determination 5's degenerate one-finding case); `op=publishedcase` answers with `findings[]` and NO top-level `strength` — the single case-level strength is R2's forbidden composition at case altitude and its ABSENCE is the negative control.
interfaces consumed: none changed.
DISJOINT FROM UI-27, running concurrently on `civicos-ui` ground: this claim opens NO file under `civicos-ui/` and UI-27 opens none under `bio-plane/`. UI-29 renders this surface NEXT and is not running.
explicitly NOT touched: civicos-ui/** (every file), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, bio-plane/scripts/** (run as instruments, never written), bio-plane/src/query.mjs, bio-plane/src/setup.mjs, bio-plane/src/affordances.mjs, bio-plane/src/container.mjs, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md REC-44, enacting DEC-44 as a CORRECTION to REC-14/REC-22/UI-18. A case is a CONTAINER over one or more FINDINGS. The finding stays the unit of truth (its own conclusion, falsifier, basis and its own frozen PAIR); the case becomes the unit of publication (its own identity, its own editions, its own authored scope statement and completeness assertion). NOTHING IS IN PRODUCTION — every instance is a development instance — so this is a SCHEMA CORRECTION and no migration machinery is built.
accepts-when: as QUEUE.md REC-44. `cd bio-plane && npm run test:battery` green with a case of two findings published as ONE edition, both frozen pairs readable, the completeness statement authored per case per edition and the inheritance checked per finding; `npm run test:hygiene` green; `npm run test:coverage --strict` exit 0. NEGATIVE CONTROL — publish a case of two findings whose strengths DIFFER and the harness FAILS if any surface, rendering or export presents a single case-level strength. Every file restored byte-identical after the control. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session.
CLAIM AMENDED IN FLIGHT, stated rather than tidied away: five further suites — `test/ratify.test.mjs`, `test/grounds.test.mjs`, `test/inquiryground.test.mjs`, `test/divide.test.mjs` and `test/capability.test.mjs`'s subject `publishedManifest()` — were opened at named assertions and belong in `paths` above. None was foreseen; none is scope creep. Each pinned a one-inquiry-per-case shape (a frozen pair at the top of an answer, a container built for material that is not a case, a manifest column on the wrong row), and CLAUDE.md's rule is that a superseded test is CORRECTED with a dated reason and never exempted. No assertion was loosened: every value demanded before is demanded still, one altitude down.
landed: left for CONDUCT. Worker commits in this worktree (NOT pushed): 7e952e7 and the follow-on below. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5391 assertions, 90.8s** (was 97/97, 5327). The +64 is ATTRIBUTED rather than asserted: 47 the new `multifinding.test.mjs`; +6 `publish.test.mjs` (71 -> 77 — the case identity, the scope refusal and the ABSENT case-level strength asserted where the old singular pins were corrected); +5 `publishedcase.test.mjs` (72 -> 77, the same three plus the manifest's own absence); +3 the two hygiene suites (578 -> 581, which derive per-FILE assertions and grow whenever a suite is added); +3 across ratify/reevaluation/grounds where a corrected pin became two. `npm run test:hygiene` green (581). `npm run test:coverage --strict` exit 0 — **130/130 ops** through the control plane (UNCHANGED, and deliberately: this item adds no op, because the case manifest is recorded through an internal DO path — no caller should be able to mint a container), 53/53 checks named, 98/98 suites declaring a control.
  THE CASE IDENTITY. `CASE-<year>-<seq>`, minted by `op=publish` through `allocId` like every other identifier here, and DISTINCT FROM A BUNDLE ID AT EVERY ARITY — including the one-finding case DEC-44 determination 5 keeps legal. Reusing the member's id when there happens to be one member was the tempting shape and is refused for two reasons: it makes `?id=` ambiguous at the public read path, and it makes the shape depend on the arity, so the degenerate case would stop being degenerate the moment a second finding joined. It is CARRIED IN THE SIGNED BYTES of every member (`case_id`, beside `edition`), so a case identity can never be claimed at the commit that was not inside the hash the member signed — DEC-12's rule for the edition, applied to the thing the edition is now OF. Resolution at the act: a caller may NAME an existing published case; otherwise it is DERIVED from what the members already belong to; otherwise MINTED. A caller may never mint one — an identity a caller can hand us is one a caller can invent.
  THE MEMBERSHIP. A TABLE, `published_case_members (case_id, edition, ord, bundle_id)`, and it earns itself on the D4/REC-42 test twice over. It needs an ORDINAL — the order the member published the findings in is the order the container's parts[] and every rendering take, and it is authored rather than alphabetical. And it answers a query keyed on it in BOTH directions: "which findings are in this case edition" (assembling the container) and "which case does this finding belong to" (the public read path resolving a finding id — hence the index on bundle_id). What settles it against a derived query over `published_bundles` is that this table holds the DECLARED set while published_bundles holds the RATIFIED SUBSET, and the difference between them is the only thing that can say a case edition is INCOMPLETE. Rows are written at RATIFICATION from each member's own signed `case_findings`, never from the request — `#publishEdges`' doctrine, for `#publishEdges`' reason — and two members who disagree are REFUSED (`CASE_MEMBERSHIP_DIVERGED`, `CASE_ASSERTION_DIVERGED`) rather than reconciled.
  THE THIRD SHAPE DECISION, which the item did not name and the build forced: WHAT DOES A MEMBER SIGN. Ratification stays `{bundleId, expectedSha, sig}` and stays PER FINDING, because the finding is the unit of truth and what a member reviews is one document's bytes; a case-level signature would be a signature over something nobody read. So a case edition is ratified N times and becomes SERVABLE when the last member lands. That window is a real state and is STATED (`complete: false`, `awaiting: [...]`) rather than hidden — the findings that ratified are published and answerable, and the container that would claim to carry all of them is refused until it can be assembled whole. The alternative (one signature over a digest of the members' shas) was rejected: it changes the signing statement, breaks every landed ratify pin and every signing surface, and buys nothing the per-finding signature does not already give.
  WHAT MOVED ALTITUDE, AND WHAT DELIBERATELY DID NOT. `completeness`, `manifest` and `manifest_sha` LEFT `published_bundles` for the new `published_cases`, because each is a fact about the CASE and would otherwise be stated once per member — D-21's second place to state one fact. `strength` and `required` STAYED on the finding row, and `parts` joined it (published_shas is append-only ACROSS editions on purpose, so it cannot say which parts belong to which edition, and the container needs exactly that). C-21.1 now reads a SECOND registry, `publishedCaseRegistryFor`, injected beside `publishedRegistry` at both gates; C-21.2 and `checkInheritedLeg` are untouched. Two registries rather than one, on purpose: one registry serving both altitudes is how the collapse this item corrects happened.
  ONE JUDGEMENT WORTH BOB'S EYE, made and recorded rather than returned: THE SCOPE STATEMENT IS REQUIRED PER EDITION BUT IS NOT UNDER C-21.1'S BYTE-CHECK. Completeness is edition-specific by nature — what this edition left out, as of its date — so reprinting it is evidence nobody looked. A case's SCOPE is the project's own question and legitimately does not move when a finding is revised; requiring it to change every edition would pressure a member into inventing a difference, and "a gate that pressures someone into inventing one is a bug in the gate" is CLAUDE.md's sentence about exactly this shape. It is REQUIRED, AUTHORED and never prefilled, which is the arm that fits the claim it makes. Asserted directly in the suite's block 3.
  A SCHEMA CORRECTION, NOT A MIGRATION, and the commit message says so in those words. Nothing is in production — every instance is a development instance — so `published_bundles` loses three columns and gains one and no migration machinery was built for a population that does not exist. An existing development store carries three dead nullable columns and is otherwise unaffected; REC-14's own `#migrate` block is untouched.
  ONE NARROWING TO REPORT, because it is a capability question rather than a refactor: A NON-CASE RATIFICATION NO LONGER PRODUCES A CONTAINER. `ratify.test.mjs`'s published-parts count goes 4 -> 3, which is REC-14's own 3 -> 4 returning. REC-14 was right about the container and wrong about whose it is: DEC-34's container is the PUBLISHED CASE's, and an information bundle is not a case. Its bytes stay individually answerable by hash through op=publishedbytes and op=verify (asserted, unchanged), and `op=publishedcase` still answers for it — as WHAT IT IS, with `caseId: null`, no scope and no completeness. Manufacturing a "case container" for material that is not a case was D-187's conflation one level down. If a group wants a portable zip of a captured document, that is a feature to name and build, not a side effect to keep.
  NEGATIVE CONTROLS RUN x4, every file restored BYTE-IDENTICALLY (sha256 compared before and after each arm; the three hashes are in the suite's own NEGATIVE CONTROL line with what each arm MEASURED). (a) is BOB'S OWN, carried verbatim off DEC-44: a case-level strength on the read path -> 46 pass, 1 FAIL, the sweep NAMING all four surfaces it appeared on. (b) the same bug in the EXPORT -> 45 pass, 2 FAIL, the composed claim reaching the signed-hash manifest inside the zip a stranger downloads. (c) the membership check neutered -> 45 pass, 2 FAIL. (d) C-21.1 read at the finding altitude again, in BOTH gates -> 43 pass, 4 FAIL.
  AND CONTROL (c) MEASURED SOMETHING REVIEW WOULD NOT HAVE, which is this turn's finding: on its first run it reported **47 pass, 0 fail**. Every member of a case published by `op=publish` carries the same roster and the same assertion BY CONSTRUCTION, so both divergence refusals could have been deleted outright with the whole suite green — the inbox-grammar failure mode exactly, which is CLAUDE.md's own worked example. Block 2b was written in response: two ADVERSARIES driven through op=promote's hand-written door, one signing a different roster and one signing a different scope, and the arm then bit. Control (a) measured the mirror image: blocks 1-4 stay ENTIRELY GREEN under a spurious case-level strength, because the surface goes on answering correctly and ADDITIONALLY answers wrongly — which is why the control had to be a structural sweep over whole responses rather than a value comparison, and why a value comparison would have passed.
  FOR UI-29, WHICH RENDERS THIS NEXT: the read path answers `{ caseId, edition, scope, completeness, complete, awaiting, findings[], manifest_sha, editions, edition_index, latest_edition }` and there is NO top-level `strength`, `required`, `bundle_sha`, `body`, `basis`, `serves`, `names` or `division` — every one of those is per finding, inside `findings[]`. A finding's bundle id resolves to its case and `asked` names which was reached for. The container's parts are namespaced `<case>/<finding>/<path>` (two findings both carry a `bundle.md`), and `manifest.format` is `bio-case-container/2`.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 81809fc). Verified: battery 98/98 (5391), hygiene 581, --strict 130/130, zero leaked sandboxes. The case-identity, membership-table and per-finding-signature shapes accepted with their reasoning; the scope-statement judgement accepted; the in-flight pin amendment self-reported and accepted. DEC-48 raised for the non-case container. No other DEC.

## CLAIM 2026-08-04 RECORD

session: rec43-agent (worktree agent-a91d1e84fe758d88d)
opened: 2026-08-04T22:00:00Z
paths: bio-plane/checks/bio-checks.mjs (ONLY: two new exports beside `BASIS_GRADES` — `EARNED_CAPTURE_CEILING` MOVED here from `Store.EARNED_CAPTURE_CEILING`, and `UNREACHABLE_CAPTURE_GRADE` derived from `BASIS_GRADES`; NO check function is edited), bio-plane/src/store.mjs (ONLY: `static EARNED_CAPTURE_CEILING = "B"` REMOVED, the symbol added to the existing `../checks/bio-checks.mjs` import, and its two use sites inside `earnedBasisRegistry()` rewired — nothing else in that method or anywhere else changes), bio-plane/src/affordances.mjs (ONLY: two symbols added to the existing bio-checks import, one `attestFence()` composer + one `ATTEST_FENCE` export, `prompt: ATTEST_FENCE` on the `attest` entry of `CAPTURE_ACTS`, and the header's "NO PROMPT, deliberately" paragraph CORRECTED with a dated reason), bio-plane/test/affordances.test.mjs (this item's assertions and its NEGATIVE CONTROL line; the REC-38 `prompt: null` pin on attest CORRECTED at the site with a dated reason, never exempted), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts). PURELY ADDITIVE on the wire: one already-published field (`prompt`, REC-16's, on the already-published `capture_acts` block, REC-38's) stops being null for one act. No op, reason code, field or shape is added or changed. I5 is NOT touched.
interfaces consumed: none changed. `checkEarnedLeg` is READ and NOT EDITED — it reads the ceiling out of the registry it is handed, so moving where the constant is DECLARED changes no refusal, no message and no finding.
explicitly NOT touched: civicos-ui/** (UI-28 is DEC-39's other half and is not this item — the surface is READ as the consumer and never written), bio-plane/src/index.mjs (the acquire `note:` at its Grade B/Grade A sentence is a THIRD statement of the same doctrine and is reported to CONDUCT rather than rewired here — it is a different act's sentence), bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/gate.mjs, every other suite including test/earnedbasis.test.mjs and test/grounds.test.mjs, newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
concurrency: REC-44 runs concurrently on the PUBLISHED/CASE ground — `published_bundles`, `publishCase`, C-21.1/C-21.2 and the publish/publishedcase/reeval suites. FULLY DISJOINT from this item, which touches the C-2.8 earned-capture ceiling region of `checks/bio-checks.mjs` (beside `BASIS_GRADES`, ~line 2100, far from C-21), one static field and two lines inside `store.mjs`'s `earnedBasisRegistry()`, and the act catalogue. Named here so whoever lands second knows the overlap is the FILE and not the region.
expected: QUEUE.md REC-43, enacting DEC-39. The co-attestation honesty fence — Bob's three-part sentence, taken VERBATIM from the DEC-39 entry and neither paraphrased nor improved — published on the `attest` act through REC-16's `prompt` mechanism, with its two grade letters COMPOSED FROM the enforcement's own values rather than typed, so the wording and the rule cannot drift.
accepts-when: as QUEUE.md REC-43. `cd bio-plane && npm run test:battery` green with the attest act publishing its prompt asserted equal to the enforcement's own wording; `npm run test:coverage --strict` exit 0; NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — publish the fence as a literal copy and the drift assertion fails. Every file restored byte-identical after the control. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it. Worker commit 33ba8e7 (this worktree; NOT pushed — this line is the one bookkeeping commit on top of it).
  NUMBERS. `npm run test:battery` **97/97 suites, 5337 assertions, 85.5s** (was 97/97, 5329). The +8 are ALL in `affordances.test.mjs` (70 -> 78) and no other suite moved by one: no new suite was added, so the two hygiene suites do not grow. `npm run test:coverage --strict` exit 0 — 130/130 ops through the control plane, 53/53 checks named, 97/97 suites declaring a control. Zero leaked sandboxes (0/0 before and after).
  THE IMPORT DIRECTION, WHICH IS THE ONLY DESIGN DECISION IN THIS ITEM, and the item anticipated that the rule's enforcement point and the wording's home are genuinely different places. They are, and neither could import the other where they stood. The RULE — Grade B is the ceiling, Grade A is unreachable — is enforced in two places: `Store.earnedBasisRegistry` published the ceiling into the registry, and `checkEarnedLeg` in `checks/bio-checks.mjs` is the arm that REFUSES a leg claiming more than it (earnedbasis.test.mjs arm (c) measured that this one arm is the only thing in the battery standing between the record and a capture grade the record cannot support). The WORDING must be published from `src/affordances.mjs`, because DEC-8 says a surface renders what it received and never composes a prompt of its own. And `affordances.mjs` CANNOT import `store.mjs`: the store already imports IT, so the edge would close a cycle and evaluate a top-level object literal against bindings in the temporal dead zone — the same wall REC-35 hit and wrote up on VOCABULARIES.
  SO THE CONSTANT MOVED DOWN TO THE LAYER BOTH SIDES ALREADY IMPORT: `EARNED_CAPTURE_CEILING` left `store.mjs` as a static field and is now declared in `checks/bio-checks.mjs` beside `BASIS_GRADES` and the refusal that reads it, with `store.mjs` importing it and keeping no copy. That is the DISPOSITIONS/REC-11 flip in the one direction that has no cycle, and it is a promotion rather than a demotion: the value now lives where the refusal is computed. ONE VALUE, THREE READERS — the registry, the refusal, the published fence. **AND THE SECOND LETTER IS DERIVED, NOT TYPED**: "it never reaches Grade A" is true because A is one rank stronger than the ceiling in the SAME `BASIS_GRADES` array `checkEarnedLeg` compares against, so `UNREACHABLE_CAPTURE_GRADE` is read out of that array. If a future ceiling were the strongest grade there would BE no unreachable letter, and the composer THROWS rather than publishing "never reaches Grade null" — a load failure, deliberately, because a doctrine string whose doctrine has moved out from under it should stop the plane and not degrade.
  BOB'S SENTENCE IS VERBATIM AND THE SUITE PROVES IT AGAINST THE RULING ITSELF, not against a copy: the assertion READS the DEC-39 entry's own blockquote out of `docs/development/DECISIONS.md`, strips the `**`/`*` markers and the line breaks (which are that file's RENDERING), and compares. It matches byte-for-byte, every word, its order, its punctuation and `TRUE`'s capitalisation. A session that paraphrased or "improved" it would fail against Bob's entry.
  ONE STRUCTURAL ACCOMMODATION, STATED PLAINLY BECAUSE IT MUST NOT BE MISTAKEN FOR A REWORDING: DEC-39 renders the fence as a markdown blockquote — three parts, bold labels, the opening question italicised. `prompt` is a plain string in the published act shape (DIVIDE_PROMPT and GROUND_PROMPT are both single prose strings), so the emphasis markers and the blockquote's line breaks are not carried. NO WORD CHANGED, nothing was added, and the deliberate three-part shape survives in the three labels the ruling itself wrote. If CONDUCT wants the three parts carried as structure rather than as labels, that is a shape change to the published field and belongs to a ruling, not to this turn.
  NEGATIVE CONTROL RUN, THREE ARMS, both files restored BYTE-IDENTICAL (sha256 compared before and after: `src/affordances.mjs` 9a894add…, `checks/bio-checks.mjs` 7d63a552…), recorded in `affordances.test.mjs`'s own NEGATIVE CONTROL line. (a) THE ITEM'S OWN — publish the fence as a hand-typed literal copy -> ONE assertion fails (77/78), the structural pin, and NOTHING else does: every wire and behavioural assertion passes because an identical copy agrees at zero cost. REC-35's finding restated a fourth time, and the first time on a SENTENCE rather than an array. (b) the literal copy KEPT and the enforced ceiling moved to C -> THREE fail (75/78): a member co-attesting is told the capture is strengthened at Grade B while the gate refuses anything above C, which is the record overclaiming on a doctrine sentence. (c) the COMPOSED fence under the same moved rule -> TWO fail (76/78) and they are the right two — the publication has moved off Bob's ruling and the suite says so — while NO MEMBER IS TOLD A GRADE THE GATE WILL NOT ACCEPT, because the sentence followed the rule. The difference between (b) and (c) is the whole item.
  ONE PIN CORRECTED, dated at the site and never exempted: "CAPTURE_ACTS declares ONLY id and label" was true while the block owned only those two facts and is now {id, label, prompt}. It is corrected as an EXACT set rather than loosened, so `needs`, `mode`, `rung` or an invented fourth key still fails there by name. The REC-38 `prompt: null` pin on attest is corrected the same way — a null prompt on attest now FAILS.
  ROUTED TO CONDUCT, NOT FIXED HERE: `src/index.mjs`'s op=acquire `note:` is a THIRD hand-written statement of this doctrine ("Grade B: bytes as fetched … Grade A needs a chain-of-custody web archive, which this surface cannot produce. Co-attestation raises B toward evidentiary weight."), with its own literal letters and its own pin in acquire.test.mjs. It is a different act's sentence to a different reader, so rewiring it was out of this item's scope — but it is now the only place the fence is spelled rather than composed, and whoever closes it inherits a ready ceiling to import.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 33ba8e7). Verified from the main checkout: battery 98/98 (5399, +8 exactly as attributed), hygiene 581, --strict exit 0 at 130/130 ops and 53/53 checks, zero leaked sandboxes. The import direction accepted with its reasoning — the constant belongs where the refusal is computed, and the derived second letter plus the throw-rather-than-degrade composer are the right shape. CONDUCT independently verified the publication against DEC-39's own blockquote (marker-stripped, whitespace-normalised: 576 chars, identical) and RE-RAN arm (a) from the main checkout: 77/78, the structural pin alone, every wire and behavioural assertion green — the zero-cost-equality trap measured a fourth time. ONE CORRECTION TO THE REPORT, not to the code: the fence is NOT "Bob's sentence verbatim". DEC-39 says in its own words that the wording is CONDUCT's draft under Bob's ruling. The error came from my own DEC-39 enacted line and travelled into this item's brief; corrected in DECISIONS.md, QUEUE.md and here. Nothing about the code changes — the publication still matches the recorded ruling byte for byte, which is what the suite actually asserts. The acquire `note:` finding routed as REC-48. The three-parts-as-structure question deliberately NOT pre-decided and handed to UI-28 as evidence to gather. No DEC raised.
## CLAIM 2026-08-04 UI

session: ui29-agent (worktree agent-ad3ebf68e2d5b84d9)
opened: 2026-08-04T16:00:00Z
paths: civicos-ui/app.html (ONLY the `__PUBLISHED_CASE__` region between `/*__PUBLISHED_CASE_START__*/` and `/*__PUBLISHED_CASE_END__*/`, plus the `@media print{…}` rules whose selectors that region uses), civicos-ui/test/publishedcase.test.mjs (the suite, CORRECTED at UI-18's single-inquiry pins with dated reasons and never exempted), civicos-ui/check-mock-envelope.mjs (ONLY the one FLAT_OPS evidence string for `publishedcase`, which quotes a return literal REC-44 changed), docs/development/CLAIMS.md (this entry)
interfaces owned: none. UI CONSUMES I3.
interfaces consumed: I3 — `op=publishedcase` and `op=publishedmanifest` AS REC-44 LANDED THEM. The read path answers `{caseId, edition, scope, completeness, complete, awaiting, findings[], manifest_sha, editions, edition_index, latest_edition}` with NO top-level `strength`, `required`, `bundle_sha`, `body`, `basis`, `serves`, `names` or `division`; `op=publishedmanifest` answers `{published[], cases[], caseMembers[], shas[]}` and the `published[]` rows no longer carry a `manifest` column at all. No interface change is requested by this item.
DISJOINT FROM REC-43, running concurrently on the plane's affordances/capture_acts ground: this claim opens NO file under `bio-plane/` (it RUNS the battery as an instrument and writes nothing there) and REC-43 opens none under `civicos-ui/`.
explicitly NOT touched: bio-plane/** (every file), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, every other civicos-ui/test suite, civicos-ui/check-semantics.mjs, civicos-ui/worker.template.mjs, civicos-ui/tokens.css, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md UI-29, DEC-44's SURFACE half, and it is a CORRECTION to UI-18 rather than new scope. The published case renders its FINDINGS, plural: every member finding gets its own frozen pair, its own declared bar, its own basis chain, its own falsifier and its own division disclosure, while the scope statement and the completeness assertion render PER CASE PER EDITION. **No surface, rendering, print or export may present a single case-level strength** — R2's forbidden composition arriving at case altitude. The `awaiting`/`complete:false` window renders as a REAL STATE, never as an error and never hidden.
accepts-when: as QUEUE.md UI-29. `node civicos-ui/test/run.mjs` green with a two-finding case rendering both pairs distinctly, the per-case completeness and per-finding inheritance in their right places, the awaiting window rendered as a state, and print/export carrying the same; `cd bio-plane && npm run test:battery` stays green (this item changes no plane file). NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — present one case-level strength anywhere (screen, print, export) and the harness FAILS naming it, as a STRUCTURAL SWEEP over whole responses and never a value comparison, because REC-44 measured that a value comparison PASSES: the surface goes on answering correctly and ADDITIONALLY answers wrongly. Every file restored byte-identical after each arm. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5463 assertions, 88.8s**, zero leaked sandboxes (0/0 before and after). The +15 over REC-48's 5448 is FULLY ATTRIBUTED, not estimated: the per-suite lines of the before and after runs were diffed and **exactly one suite moved, hygiene.test.mjs 369 -> 384**, which is the register detector's self-test. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED: **exit 0**, 130/130 ops, 53/53 checks, **98/98 suites declaring a control at 251 arms stated**.
  THE MECHANISM, RE-MEASURED A THIRD TIME, AND THE BRIEF WAS WRONG ON THE PART THAT DECIDES THE FIX. The item says `\s*` crosses newlines so a wrapped control still matches, and that the 60-line window is what broke REC-48. Measured directly: `\s*` reaches a close that sits alone on the next line, and matches NOTHING when the next line carries TEXT — which is what a multi-arm block is. Reconstructing the exact shape REC-48 wanted gives **NO MATCH for both suites**, and both markers sit on line 1 where no window can reach them. So REC-48's original report named the right mechanism and the integration re-measurement named the wrong one. All three defects are closed anyway, because the window is a real (separate) defect: a marker past line 60 is invisible, and a declaration STRADDLING line 60 was recorded as whatever fragment fell inside while still counting as controlled.
  WHAT THE OLD REGISTER HAD BEEN RECORDING, since the item asked. No suite's entry was a mangled fragment. But **14 of 98 suites had a fuller declaration the register could not see**, and three were registered with fewer arms than they state: reading-wire 2 of 3, reuse-ratify 4 of 5, setup-honesty 3 of 5. `readingname.test.mjs` is the window's own victim — its 4-line, 2,233-character declaration begins on line 59 and was entirely outside the window, so the register quoted its 870-character one-liner instead. After REC-48's blocks were restored, acquire went from 2 arms recorded to 7 and hygiene from 4 to 12. Over the whole tree the old detector read 234 arms and the new one reads 251.
  THE TWO JUDGEMENTS, made and recorded at the site (scripts/control-register.mjs's header). WHERE A DECLARATION ENDS: at its comment's close or at the first blank comment line, whichever comes first — a declaration is a PARAGRAPH, because several suites state the control as one paragraph of a longer header and running to the close would quote back prose the suite never declared. WHICH ONE IS RECORDED when a suite states its control twice, which M0-2's backfill left most of them doing: the FULLEST single block, **never the sum**, because summing two statements of one control credits it twice — the generous direction wearing a different hat. ARMS ARE REPORTED AND NEVER GATED: an arm is one ` -> ` transition, the register grammar's own separator, and `--strict` still fails on exactly what it always failed on. A floor on arm counts would be strictness this instrument cannot justify, and the brief is right that over-strict is as bad as generous.
  NCs RUN x4, every file restored BYTE-IDENTICALLY with sha256 compared before and after (scripts/control-register.mjs caeac36b…, scripts/coverage.mjs a5df1b87…, test/acquire.test.mjs e065c8e0…, test/hygiene.test.mjs f66974ac…, test/capture.test.mjs e71bf8b8…). **(a)** hide a suite's declaration entirely (every marker in acquire.test.mjs) -> strict run **EXITS 1**, 97 of 98, NAMING acquire.test.mjs. **(b)** truncate a multi-arm block (hygiene's own, 8 lines to 5) -> that declaration reports 6 arms instead of 9 and the total 243 instead of 246, while strict STAYS exit 0 — the design, since arms are reported not gated, and the visible drop IS the mechanism. **(c)** put the 60-line window back into the detector -> hygiene 381/3, exactly the three window arms. **(d)** make the detector first-line-only -> hygiene 377/7, and the REAL-CORPUS arm bites with the fixtures; the register's total falls to **234, which is exactly what the old detector reported over this same tree**, so (d) reproduces the closed defect rather than resembling it.
  ONE PROPERTY FOUND WHILE RECORDING THE CONTROLS, and stated rather than smoothed: the arm TOTAL is a function of the declarations' own prose, so writing an NC record INTO a declaration moves the total. The totals above are as measured when each arm ran; only the DELTAS are comparable across edits, and the suite says so at the site. For the same reason every fixture in the self-test builds its marker from the instrument's exported constant instead of writing it literally — a literal would have put the test's own fixtures into the register.
  REACH ASSERTED 14 WAYS at the foot of hygiene.test.mjs, every one a DELTA or a read of the real corpus rather than an absolute a deaf detector could also satisfy (REC-48's reach assertion was wrong when first written for exactly that reason): found past line 60 AND not found with the marker hidden; a straddling block read whole; five arms counted from a marker line that states none, and the truncation measured as a delta between two blocks rather than against 1; the extent asserted in BOTH directions (reaches its continuation lines, does NOT swallow the paragraph after); the `//` form and its end; a control stated twice recorded once; the register read out of capture.test.mjs's REAL source and then not read once its marker is hidden; more arms than suites; and **at least one MULTI-LINE declaration present in the tree**, which is the assertion that makes REC-48's workaround un-reinstatable. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `node civicos-ui/test/run.mjs` **32/32 suites green**: `publishedcase.test.mjs` 131 -> **191**, every other suite UNCHANGED. check-semantics green; check-mock-envelope green — arm A over 13,234 lines with the seam set unchanged (this item adds no transport call), arm B 32 suites re-run under the probe, 776 op answers across 66 distinct ops. `cd bio-plane && npm run test:battery` **98/98 suites, 5391 assertions, 88.8s**, zero leaked sandboxes — BYTE-UNTOUCHED, since this item changes no file under `bio-plane/`. `civicos-ui/app.html` sha256 8ebc32a4a2a3ba76dd956e97eded33551c1394e6c744ce1bd16213a8184788f3.
  THE ALTITUDE IS STRUCTURAL, NOT EDITORIAL, and that is the design decision worth keeping. Every page-shaped artifact declares which altitude it is at (`data-page="case"` / `data-page="finding"` with `data-findingsec`), every strength mark is produced by the one `pubStateBadge` and carries `data-finding` — THE FINDING WHOSE PAIR OR WHOSE LEG IT STATES — and the index's member blocks carry the SAME mark, so one mechanism serves both surfaces. The harness sweeps the COMPLEMENT of those regions over whole responses: screen, printed artifact, exported container, the index, and a POISONED ANSWER in which the plane hands a case-level `strength` over anyway. That last one measures THIS surface's refusal rather than inheriting the plane's.
  WHY A COMPLEMENT SWEEP AND NOT A COMPARISON, measured rather than assumed: control (m) — Bob's own, carried verbatim off DEC-44 — leaves EVERY PER-FINDING ASSERTION GREEN. Both findings still render both pairs, both bars, both bases, both falsifiers, both divisions; the surface goes on answering correctly and ADDITIONALLY answers wrongly. REC-44 measured the identical thing one altitude down. Control (m2) is the mirror and is why there are two instruments: put the composed CASE pair inside a FINDING's pages and the complement sweep is silent by construction — only the OWNERSHIP assertion (a mark must name a finding this case has, or one its legs cites, and never the case identity) bites, at 1 of 190.
  THE `awaiting` WINDOW RENDERS AS A STATE, in both directions. `complete:false` draws a block that says how many of how many declared findings have landed, that this is a state of the record and not a failure, WHY the window exists (a case is ratified one finding at a time; it becomes servable at the last), that the findings which landed are published and individually verifiable NOW, and that the container is refused rather than half-built. The declared roster renders in the authored order with ratified and awaited marked APART — the declared/ratified distinction REC-44 recorded is the only thing that can say an edition is incomplete, so both halves reach the page. A COMPLETE edition says it is complete, for DEC-40's reason: if only the incomplete case spoke, absence of the statement would become the ambiguity. The state also rides DEC-34's per-page header, because a reader may hold one sheet out of nine.
  TWO LIVE DEFECTS FOUND IN PASSING, both from REC-44's landing and neither foreseen. (1) `op=publishedmanifest`'s bundle rows LOST their `manifest` column — the container's manifest is the CASE's now and lives on `cases[]` — so the shipped index would have rendered "carries no frozen strength pair" for every case on every real instance: a lie that looks like modesty. The index is rebuilt over `cases[]` + `caseMembers[]` + `published[]`. (2) The DEC-34 header and the in-band bound block printed the case's `bundle_sha`, which no longer exists; a case has no bytes, it has a CONTAINER, so the case's checkable hash is the manifest's — and in the awaiting window there is none, which is STATED (`data-hash="none-yet"`) rather than filled in with a member's hash wearing the case's name.
  ONE WORDING RULE THE SWEEP FORCED, and it is a real improvement rather than a concession to the instrument: no surface may print an axis word followed by a bare grade letter unless it IS a strength mark. The footer and the bound block used to print `floors Documents B`, which is indistinguishable from a strength to the harness and — the part that matters — to a reader scanning a footer. `floorShort()` prints `Documents grade B`.
  NCs RUN x13, `civicos-ui/app.html` restored BYTE-IDENTICALLY after every arm (sha256 above compared before and after each) and ALL THIRTEEN re-run against the final file so every count in the suite's header agrees with the file it names. UI-29's four: (m) 8 of 191, (m2) 1, (n) the awaiting window hidden 5, (o) the completeness assertion rendered per finding 1 — and ONE is that arm's point, because the statement is still on the page, still verbatim, still from the signed bytes, and only an assertion that reads the two altitudes apart can see that it is at the wrong one. UI-18's and UI-27's ten re-pointed and re-run: (a) 4, (b) 1, (c) 6, (d) 5, (e) 2, (f) 2, (g) 18, (h) 1, (i) 4, (j) 3.
  ONE INSTRUMENT FINDING KEPT, of the documented class: arm (c)'s first run fired 4 rather than 6, because "finding 1 shows `Links C`" was read off finding 1's PAGES and a LEG of finding 1 is graded connection C and prints its own mark two inches lower — so the assertion was true whether the frozen PAIR carried it or not. UI-18 arm (a)'s outcome-that-costs-nothing in a new place. The pair is now read out of the `pub-axisrow` that names the finding, and the arm was re-run against the corrected instrument.
released: 2026-08-04 — landed on main by CONDUCT (merge of worker bc0bb46). Verified from the main checkout: harness 32/32 with publishedcase 191, both guards green (arm A's seam set unchanged), battery 98/98 (5402) with no bio-plane file touched — the +3 over REC-43's 5399 accounted for entirely by planning-hygiene deriving per-file assertions over the BOB session's new STORE-AS-CACHE.md, checked rather than assumed. The awaiting window's two-directional rendering accepted with its reasoning, including inventing no case hash and letting the state ride every per-page header. The two-instrument control accepted: (m) and (m2) are complements and either alone leaves the other defect invisible — the same lesson REC-44's control (c) taught one altitude down. The mid-run instrument correction self-reported rather than smoothed, noted and accepted. BOTH LIVE DEFECTS ROUTED AS REC-49, and the manifest-column regression is this session's most valuable finding: it was introduced by REC-44, which I integrated and verified GREEN, and it would have made every real instance's public index understate every case it holds. A green battery did not catch it because no suite asserted the index against a case that HAS a pair. The decision item was DETERMINED rather than raised — the index carries the per-finding ratified pair through the awaiting window, because that pair is the member's own signed one and composes nothing. No DEC.

## CLAIM 2026-08-05 RECORD

session: rec41-agent (worktree agent-ada803966e8dbb021)
opened: 2026-08-05T09:00:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) `bootstrapState()` — the `roles` SELECT and the `roles` key REMOVED from the answer, with the removal reasoned at the site; (2) `Store.LOGIN_REFUSAL_DETAIL` — the constant KEEPS ITS NAME so the next session finds the reasoning where REC-39 left it, and its two entries become ONE, carrying the revisited distinguishability decision and its evidence; (3) `login()` — both refusal arms return the one reason code and the one sentence; (4) the DO dispatch's `login:` wrapper arm — the same one refusal. NOTHING ELSE in this file), bio-plane/src/setup.mjs (ONLY: the `#do-login` handler's `reason === "NO_SUCH_ROLE"` branch — the served page stops PARAPHRASING the plane's refusal and renders the plane's own `detail`, DEC-8), bio-plane/test/members.test.mjs (GROWN: the STRUCTURAL SWEEP over the whole unauthenticated `op=bootstrap` response and this item's NEGATIVE CONTROL line; the REC-39 login-refusal pins CORRECTED at the site with dated reasons, never exempted), bio-plane/test/bootstrap.test.mjs (ONLY: the `BAD_PASSWORD` pin CORRECTED with a dated reason, and its NEGATIVE CONTROL line extended), docs/development/INTERFACE-CHANGES.md (IC-20), docs/development/INTERFACES.md (I3 version line only), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts). **BREAKING, TWICE, and deliberately versioned as MAJOR — I3 5.12.0 -> 6.0.0.** (a) `op=bootstrap` (classes null) stops publishing `roles`: a published field is REMOVED from an op's answer, which is a break by definition even at nil measured consumer impact, because recording it as additive would teach the registry to lie (IC-3's own reasoning). (b) `op=login`'s two refusal reason codes `NO_SUCH_ROLE` and `BAD_PASSWORD` are REPLACED by one, `SIGN_IN_REFUSED` — two wire strings a caller may match on are gone. I5 is NOT touched: no table, column or index changes; `credentials` is simply no longer READ by the unauthenticated path.
interfaces consumed: none changed.
explicitly NOT touched: civicos-ui/** (UI-29 runs concurrently there and this item is DISJOINT — measured, see below), newgroup/** (`newgroup/src/release.mjs` carries a BUNDLED copy of the pre-change `setup.mjs` inside the signed artifact; it is DIST's to regenerate at the next release and is NOT hand-edited here), bio-plane/src/index.mjs, bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/affordances.mjs, bio-plane/checks/bio-checks.mjs, every other suite, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, MEASUREMENTS.md, VERIFICATION.md
concurrency: UI-29 runs concurrently on `civicos-ui/**` ONLY. DISJOINT, and measured rather than assumed: `grep -rani bootstrap civicos-ui` returns ONE hit and it is a line of prose in `NEXT_SESSION_PROMPT.md`, so no UI file reads `op=bootstrap` at all; and `civicos-ui/app.html`'s `signIn` does NOT branch on the login reason code — it calls `teach($("#g-err"), l)`, which renders whatever the plane sent (DEC-8), so the reason-code collapse needs no UI edit. The one mention of `BAD_PASSWORD` in `app.html` is inside a UI-23 comment about the response ENVELOPE, not a match on the string.
expected: QUEUE.md REC-41, closing D-198 (that row was D-188 when this claim was written; CONDUCT renumbered it 2026-08-04 off a third collision). Close `op=bootstrap`'s unconsumed roster disclosure, file the IC with the measured nil impact and the DORMANT-UI proxy answer named in writing (protocol step 3), bump I3 honestly as MAJOR, and REVISIT the login-distinguishability reasoning at `Store.LOGIN_REFUSAL_DETAIL` IN THE SAME TURN, because REC-39's grounds for keeping the refusals distinct were the open roster and closing it destroys them.
accepts-when: as QUEUE.md REC-41. `cd bio-plane && npm run test:battery` green with an anonymous `op=bootstrap` answering NO role names and NO password dates; the IC filed; the distinguishability decision revisited with its outcome recorded at the constant; `npm run test:coverage --strict` exit 0. NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — restore the roster field and the suite fails NAMING the disclosure — and it is a STRUCTURAL SWEEP over the WHOLE unauthenticated response rather than a check for one field name, because REC-44 measured that a value comparison passes while the surface goes on answering correctly and ADDITIONALLY answers wrongly. Every file restored byte-identical after the control, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session.
landed: left for CONDUCT. MEASURED THIS TURN so nobody re-derives it. Worker commit 14e044b (this worktree; NOT pushed — this line is the one bookkeeping commit on top of it).
  NUMBERS. `npm run test:battery` **98/98 suites, 5411 assertions, 83.6s** (was 98/98, 5399). The +12 are ALL in `members.test.mjs` (81 -> 93) and NO OTHER SUITE MOVED BY ONE — no suite was added, so the two hygiene suites did not grow; `npm run test:hygiene` 581 unchanged. `npm run test:coverage --strict` exit 0 — 130/130 ops through the control plane, 53/53 checks named, 98/98 suites declaring a control. Zero leaked sandboxes (0/0 before and after). `node tools/plancheck.mjs` clean apart from UNPUSHED, which is this session's correct state.
  THE PREMISE WAS RE-MEASURED AND IT IS RIGHT ABOUT THE FIELD AND LOOSE ABOUT THE OP. Nothing consumes `roles` — `src/setup.mjs` calls op=bootstrap three times and reads `version`/`claimed`/`bootstrapConfigured`/`rearmed`/`consumedAt`; `civicos-ui` does not call the op at all (`grep -rani bootstrap civicos-ui` returns one line of PROSE in NEXT_SESSION_PROMPT.md); no battery assertion named the field. **BUT `newgroup` IS A CONSUMER OF THE OP**, which REC-41 and D-188 both deny: `newgroup/src/index.mjs:364` (`verifyUpdate`) and `:631` (the pre-update version probe) call it and read `version`. Not of the FIELD, so the item's conclusion stands and its wording does not; corrected in IC-20 rather than left, because a register that carries a small inaccuracy teaches the next reader to trust it less than it should.
  ONE NEAR-CONSUMER, and it is the shape that would have made this look consumed: `setup.mjs`'s panel row labelled **"Roles with passwords"** has ALWAYS been filled from `WHO`, the single signed-in role, never from this field. A label describing `roles` over a value that never came from it, and with the field gone it could never become true. Corrected to "Signed in as" rather than deleted.
  THE DISTINGUISHABILITY DECISION WAS RE-MADE AND **REVERSED**, and the reasoning is at `Store.LOGIN_REFUSAL_DETAIL` where the next session will look. REC-39's decision rested on ONE ground, which it recorded precisely so it could be reopened: the roster was already open, so collapsing the refusals "would defend nothing". That ground is gone. THREE MEASUREMENTS DECIDED IT. (1) `op=login` is `classes: null` and carries NO rate limit — the only unauthenticated op in this plane that meters a caller is `op=knock` — so distinct codes are an unmetered anonymous oracle answering one guess per request, forever; closing the wholesale route and leaving that open would have let the register record a closure the plane does not deliver. (2) THIS PLANE HAS ALREADY DECIDED THIS QUESTION THREE TIMES AND ALWAYS THE OTHER WAY: `#INVITE_MISS` (spent vs never-existed, "the security property and not tidiness"), `NOT_PUBLISHED` (three ways to miss, "one answer here"), and `NO_SUCH_ROLE` ITSELF, which already collapsed revoked against never-registered. Login was the LAST unauthenticated identity probe still separating its outcomes. (3) The distinction had exactly ONE consumer and the consumer was part of the defect — `setup.mjs`'s paraphrase stated the disclosure more plainly than the plane did, to an anonymous visitor, on the front door. `civicos-ui` does not branch on the code (`signIn` hands the refusal to `teach()`), so NO UI EDIT WAS OWED and UI-29's ground was never touched.
  AND THE COLLAPSE WAS DEFEATED BY A STOPWATCH UNTIL IT WAS CLOSED, which is the finding of this turn and was not in the item. Identical words are not an identical answer if one arrives in a millisecond and the other in a hundred: the arms that refuse WITHOUT checking a password (login()'s no-credentials row, and the DO wrapper's absent-or-inactive member — the arm that decides "is this an active member" from the members table alone) returned immediately, while a wrong password on a live member ran PBKDF2 at 100,000 iterations. **MEASURED: 6.7ms against 0.6ms, an eleven-fold gap**, so the live roster was enumerable with any password at all and a timer — recovering exactly what removing the roster had just taken away. `Store.#payLoginCost` makes every such arm pay an equivalent derivation; the three arms now land within 4% of each other (7.5 / 7.3 / 7.2 ms). STATED WITHOUT OVERCLAIM AT THE SITE AND IN IC-20: this equalises the DOMINANT cost and is NOT a proof of constant time — the SQL lookup and the string compare still differ.
  WHAT IS DELIBERATELY NOT CLAIMED, because a fix that overclaims is the failure this project exists to refuse: member identity is NOT secret after this, and it was never this mechanism's to keep. `op=publishedcase` is `classes: null` and publishes `attestor.member` on every ratified finding, deliberately — a signature that does not name its signer is not a signature. What closes is the GENERAL oracle over everyone who holds a credential, including the members who have never published anything and whom nothing else names.
  NEGATIVE CONTROLS RUN x3, `src/store.mjs` restored BYTE-IDENTICAL after every arm (sha256 40872e0e… before and after all three; `src/setup.mjs` 6aae2b79… and `test/members.test.mjs` c5a24137… untouched by the controls), recorded in members.test.mjs's own NEGATIVE CONTROL line with what each arm MEASURED. (a) THE ITEM'S OWN — restore the roster -> 4 FAIL (89/93) and they NAME the disclosure rather than reporting a shape mismatch: `["$.roles.0.role = \"member:ruth\"","$.roles.1.role = \"admin\"","$.roles.2.role = \"member:meilan\""]` and `["$.roles.0.updated = \"2026-08-04T19:08:37.880Z\"", …]`. (b) re-split the refusal codes -> 8 FAIL (85/93), which is what proves the SECOND half is enforced and not merely commented. (c) neuter the cost equaliser -> 2 FAIL (91/93) **and EVERY WIRE ASSERTION STAYS GREEN** — identical codes, byte-equal sentences, a clean sweep, and the oracle wide open. A collapse asserted only on the wire would have passed this suite completely, which is why the timing pin exists.
  THE SWEEP IS STRUCTURAL, AND THE ITEM WAS RIGHT TO INSIST. It walks EVERY key and EVERY leaf of the whole anonymous response and fails on the CLASS of fact — any role name (as key or as value, at any depth), any ISO instant except the one allowed BY PATH, any nested collection at all, and an exact key set so a NEW field fails by name. `t("no roles key", "roles" in b, false)` would have been satisfied by the identical leak under `credentials`, `members`, or nested inside `state` — REC-44's control (a) restated. It lives in `members.test.mjs` and NOT in `bootstrap.test.mjs`, deliberately and with the reason written into that file: bootstrap.test.mjs claims a single admin, so a closed roster and an empty one are indistinguishable there — an outcome that costs nothing to produce is not evidence. members.test.mjs has an `admin` credential plus `member:ruth` and `member:meilan` with three distinct password-set dates, and the sweep additionally PROVES the roster exists through the gated op before asserting the anonymous answer withholds it.
  SEVEN PINS CORRECTED WITH DATED REASONS, NEVER EXEMPTED: six in `members.test.mjs` (the two code pins, the two per-sentence content pins folded into one loop over BOTH callers, the inverted distinguishability assertion, the revoked/no-credential code pins) and one in `bootstrap.test.mjs`. The REC-39 two-arm block is WIDENED rather than rewritten — three arms became four, and a counted-set assertion was added beside the pairwise ones so a fifth arm added later cannot quietly answer its own way (REC-44's lesson applied to the refusal set).
  ONE THING ROUTED TO CONDUCT AND NOT FIXED HERE: `civicos-ui/test/auth-surface.test.mjs`'s `PLANE_WORDS` mock hand-answers `{reason:"BAD_PASSWORD"}`, a copy that agrees with the plane at zero cost. members.test.mjs's own REC-39 control already recorded that the UI harness cannot see this class; after this item the mock is not merely uninformative but WRONG — it asserts against a code the plane no longer emits, and it will stay green. It is `civicos-ui/**`, which is UI-29's claimed ground, so it is reported rather than edited.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 21400cd). Verified from the main checkout: battery 98/98 (5414, the +12 all in members.test.mjs exactly as attributed; the 3 above the worker's 5411 are planning-hygiene's per-file assertions over documents added since), --strict exit 0 at 130/130 ops and 53/53 checks, zero leaked sandboxes, plancheck green. The reversal accepted with its three measurements, and it is exactly why the two halves had to travel together: inheriting REC-39's distinctness would have kept an unmetered anonymous oracle whose only recorded justification had just been removed. The timing finding is the item's best work — not in the brief, not reachable by review, and an eleven-fold difference means the collapse was cosmetic until it was closed. The honest limit (dominant cost equalised, constant time NOT proved) and the explicit refusal to claim member identity is now secret are both the right kind of statement to leave behind. The premise correction accepted and carried into the record rather than smoothed: newgroup DOES consume op=bootstrap, just not the field, and the brief's instruction to re-measure rather than inherit the claim is what caught it. D-188 CLOSED. Two routed — UI-30 (the UI harness green against a retired code, plus the missing-sentence gap that suite raised for CONDUCT and the plane has now closed) and D-193 (newgroup's bundled setup.mjs copy, left to refresh at the next release cut rather than hand-patched). I3 6.0.0 and IC-20's named proxy answer accepted. No DEC.

## CLAIM 2026-08-04 UI

session: ui30-agent (worktree agent-aef8591a9119731c6)
opened: 2026-08-04T18:00:00Z
paths: civicos-ui/app.html (ONLY the SIGN-IN region: (1) the envelope comment that precedes `apiR` near line 903, whose worked example names a login refusal code REC-41 RETIRED and whose "no sentence" premise the plane has since closed — corrected in place with a dated reason; (2) `signIn`'s one refusal line, so the gate renders the plane's own `detail` SENTENCE when the plane sent one and its bare reason when it did not. Nothing else in this file, and specifically NOT the published/index region), civicos-ui/test/auth-surface.test.mjs (the suite: its `PLANE_WORDS` mock CORRECTED at the site with a dated reason and never exempted, the sentence READ FROM `bio-plane/src/store.mjs` rather than typed, a source-level sweep for the retired codes added, and its NEGATIVE CONTROL line rewritten with the arms as actually run), docs/development/CLAIMS.md (this entry)
interfaces owned: none. UI CONSUMES I3.
interfaces consumed: I3 6.0.0 — `op=login` AS REC-41 LANDED IT. One refusal code, `SIGN_IN_REFUSED`, carrying `detail` = `Store.LOGIN_REFUSAL_DETAIL.SIGN_IN_REFUSED`; `NO_SUCH_ROLE` and `BAD_PASSWORD` are gone from the wire. No interface change is requested by this item; this is the consumer half.
concurrency: **REC-49 runs concurrently on `civicos-ui/app.html`'s PUBLISHED INDEX region** (`publishedManifest()`'s consumer) and on the plane's `publishedManifest()`. DISJOINT BY REGION IN ONE FILE, so the seam is named rather than assumed: this claim opens `app.html` only between the `api`/`apiR` transport declarations and `signIn`, and touches no index, manifest, `pubList`, `pub-`, or published-case code, no `@media print` rule, and no `FLAT_OPS` entry. It also opens NO file under `bio-plane/`, and REC-49 opens none of the two files named above.
explicitly NOT touched: bio-plane/** (every file — `src/store.mjs` is READ as the authority for the refusal sentence and written to by nobody here), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, every other civicos-ui/test suite, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs, civicos-ui/worker.template.mjs, civicos-ui/tokens.css, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md UI-30, REC-41's consumer half, and it is a CORRECTION of a green suite testing a fiction rather than new scope. The harness stops asserting a retired code (D-173's class: a UI mock must answer the WIRE shape), and the gate takes up the sentence the plane now sends — still composing nothing of its own, which is DEC-8 exactly: a surface may render a refusal it RECEIVED and may never compute one. The gap that suite raised for CONDUCT in its own comment ("no `detail`, no sentence") is CLOSED BY THE PLANE, so the comment is corrected rather than left to mislead the next reader.
accepts-when: as QUEUE.md UI-30. `node civicos-ui/test/run.mjs` green with the mock answering `{ok:false, reason:"SIGN_IN_REFUSED", detail:<the plane's own sentence, read from store.mjs and never typed>}` and the gate rendering the SENTENCE; both guards green (`check-semantics`, `check-mock-envelope`, arm A included — no transport call is added, in code or in a comment); a source-level assertion that NO retired login code survives anywhere under `civicos-ui/`, with the names ASSEMBLED FROM PIECES so the sweep cannot find itself. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — the existing arm (b) KEPT and RETARGETED to the new shape (right code, invented sentence) so the surface still fails if it composes wording of its own, plus a new arm that restores a retired code and fails NAMING it. Every file restored byte-identical after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5463 assertions, 88.8s**, zero leaked sandboxes (0/0 before and after). The +15 over REC-48's 5448 is FULLY ATTRIBUTED, not estimated: the per-suite lines of the before and after runs were diffed and **exactly one suite moved, hygiene.test.mjs 369 -> 384**, which is the register detector's self-test. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED: **exit 0**, 130/130 ops, 53/53 checks, **98/98 suites declaring a control at 251 arms stated**.
  THE MECHANISM, RE-MEASURED A THIRD TIME, AND THE BRIEF WAS WRONG ON THE PART THAT DECIDES THE FIX. The item says `\s*` crosses newlines so a wrapped control still matches, and that the 60-line window is what broke REC-48. Measured directly: `\s*` reaches a close that sits alone on the next line, and matches NOTHING when the next line carries TEXT — which is what a multi-arm block is. Reconstructing the exact shape REC-48 wanted gives **NO MATCH for both suites**, and both markers sit on line 1 where no window can reach them. So REC-48's original report named the right mechanism and the integration re-measurement named the wrong one. All three defects are closed anyway, because the window is a real (separate) defect: a marker past line 60 is invisible, and a declaration STRADDLING line 60 was recorded as whatever fragment fell inside while still counting as controlled.
  WHAT THE OLD REGISTER HAD BEEN RECORDING, since the item asked. No suite's entry was a mangled fragment. But **14 of 98 suites had a fuller declaration the register could not see**, and three were registered with fewer arms than they state: reading-wire 2 of 3, reuse-ratify 4 of 5, setup-honesty 3 of 5. `readingname.test.mjs` is the window's own victim — its 4-line, 2,233-character declaration begins on line 59 and was entirely outside the window, so the register quoted its 870-character one-liner instead. After REC-48's blocks were restored, acquire went from 2 arms recorded to 7 and hygiene from 4 to 12. Over the whole tree the old detector read 234 arms and the new one reads 251.
  THE TWO JUDGEMENTS, made and recorded at the site (scripts/control-register.mjs's header). WHERE A DECLARATION ENDS: at its comment's close or at the first blank comment line, whichever comes first — a declaration is a PARAGRAPH, because several suites state the control as one paragraph of a longer header and running to the close would quote back prose the suite never declared. WHICH ONE IS RECORDED when a suite states its control twice, which M0-2's backfill left most of them doing: the FULLEST single block, **never the sum**, because summing two statements of one control credits it twice — the generous direction wearing a different hat. ARMS ARE REPORTED AND NEVER GATED: an arm is one ` -> ` transition, the register grammar's own separator, and `--strict` still fails on exactly what it always failed on. A floor on arm counts would be strictness this instrument cannot justify, and the brief is right that over-strict is as bad as generous.
  NCs RUN x4, every file restored BYTE-IDENTICALLY with sha256 compared before and after (scripts/control-register.mjs caeac36b…, scripts/coverage.mjs a5df1b87…, test/acquire.test.mjs e065c8e0…, test/hygiene.test.mjs f66974ac…, test/capture.test.mjs e71bf8b8…). **(a)** hide a suite's declaration entirely (every marker in acquire.test.mjs) -> strict run **EXITS 1**, 97 of 98, NAMING acquire.test.mjs. **(b)** truncate a multi-arm block (hygiene's own, 8 lines to 5) -> that declaration reports 6 arms instead of 9 and the total 243 instead of 246, while strict STAYS exit 0 — the design, since arms are reported not gated, and the visible drop IS the mechanism. **(c)** put the 60-line window back into the detector -> hygiene 381/3, exactly the three window arms. **(d)** make the detector first-line-only -> hygiene 377/7, and the REAL-CORPUS arm bites with the fixtures; the register's total falls to **234, which is exactly what the old detector reported over this same tree**, so (d) reproduces the closed defect rather than resembling it.
  ONE PROPERTY FOUND WHILE RECORDING THE CONTROLS, and stated rather than smoothed: the arm TOTAL is a function of the declarations' own prose, so writing an NC record INTO a declaration moves the total. The totals above are as measured when each arm ran; only the DELTAS are comparable across edits, and the suite says so at the site. For the same reason every fixture in the self-test builds its marker from the instrument's exported constant instead of writing it literally — a literal would have put the test's own fixtures into the register.
  REACH ASSERTED 14 WAYS at the foot of hygiene.test.mjs, every one a DELTA or a read of the real corpus rather than an absolute a deaf detector could also satisfy (REC-48's reach assertion was wrong when first written for exactly that reason): found past line 60 AND not found with the marker hidden; a straddling block read whole; five arms counted from a marker line that states none, and the truncation measured as a delta between two blocks rather than against 1; the extent asserted in BOTH directions (reaches its continuation lines, does NOT swallow the paragraph after); the `//` form and its end; a control stated twice recorded once; the register read out of capture.test.mjs's REAL source and then not read once its marker is hidden; more arms than suites; and **at least one MULTI-LINE declaration present in the tree**, which is the assertion that makes REC-48's workaround un-reinstatable. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `node civicos-ui/test/run.mjs` **32/32 suites green**: `auth-surface.test.mjs` **62 -> 71**, every other suite UNCHANGED. check-semantics green; check-mock-envelope green — **arm A over 13,267 lines of app.html's script with the SEAM SET UNCHANGED** (`actAsk, api, apiQ, apiR, intentAsk, rec, recPost, recPostR, recR`: this item adds no transport call, in code OR in a comment, and the +26 lines are comment), arm B 32 suites re-run under the probe, 776 op answers across 66 distinct ops, 140 op/suite pairs judged. `bio-plane/` is BYTE-UNTOUCHED — `git status` shows exactly three modified files, and `src/store.mjs` was READ as the authority and written by nobody. `civicos-ui/app.html` sha256 3788ef1bf9b3424eeafbf8d651687e01b0cd5025a1ae3065a5307a94f4043b28.
  WHAT THE SUITE WAS ACTUALLY TESTING, and it is the finding rather than the cleanup: from REC-41's landing until this item, `auth-surface.test.mjs` was **62/62 GREEN against a reason code `op=login` cannot emit**. Nothing was broken — the mock hand-answered the retired code and every assertion about "the record's own reason" was satisfied by the mock agreeing with itself. The envelope guard could not see it: it judges the SHAPE of an answer and this was the CONTENT. So the sentence is now READ OUT OF `bio-plane/src/store.mjs` (textually — the module opens with `import … from "cloudflare:workers"` and only workerd provides that, so the direct-import precedent of `affordances.mjs`/`bio-checks.mjs` is unavailable; `check-semantics.mjs` already reads this file the same way). Four assertions guard the READ itself, because an extraction that silently yielded "" would make every `includes()` in the file trivially true — the same costless equality one layer over: the constant is readable, it has EXACTLY ONE entry, the sentence is whole prose rather than a fragment, and **every arm in the plane that answers the refusal code answers the SENTENCE with it** (3 of 3, including the DO dispatch's wrapper arm that refuses before `login()` is reached). A future re-split of the codes fails here by name instead of drifting.
  HOW THE GATE RENDERS IT, and why this composes nothing. `signIn`'s refusal branch hands `teach()` the plane's `detail` when the plane sent one, and the whole refusal object when it did not. That is a SELECTION between two things the plane said — DEC-8 permits rendering a refusal received and forbids composing one, and neither branch writes a word. The code is deliberately NOT shown beside the sentence: the sentence says everything the code says, in words, and `SIGN_IN_REFUSED` at a sign-in gate is machine vocabulary a member is made to decode. The fallback is the old behaviour exactly — an older plane, or a refusal from another layer, still renders its bare reason rather than being silently blanked.
  THE SOURCE-LEVEL SWEEP, and why it is a sweep and not a grep of two files: every file under `civicos-ui/` is walked and read, because the way this defect comes back is a stale copy in a file NO HARNESS RUNS — which is where both instances found here were sitting (this suite's mock, and a worked example inside an `app.html` comment). The retired names are ASSEMBLED FROM PIECES so the file enforcing the absence is not an instance of it; the walk's own reach is asserted (file count, and both `app.html` and the suite present) so a walk that reached nothing could not pass silently.
  NEGATIVE CONTROLS RUN x4, ALL ON DISK AND ALL RE-RUN AGAINST THE FINAL FILE, `civicos-ui/app.html` restored BYTE-IDENTICAL after every arm (sha256 3788ef1b… before and after all four); arms (a)-(c) also run mechanically in-VM on every invocation. (a) break the envelope open in `apiR` and `apiQ` -> **27 of 71 FAIL**. (b) THE KEPT ARM, RETARGETED and sharper for it — the gate prints the plane's CODE and adds prose of its own -> **6 of 71**, and the arm now models a surface that looks MORE faithful than the one that shipped: every code-shaped assertion is green against it and only subtracting what the plane said and reading the remainder can see it. (c) the login mock answers flat -> `UI24_FLAT_LOGIN=1 node check-mock-envelope.mjs` exits 1 naming the suite and the op, while the suite itself moves only 4 of 71 and every one is a control or coverage assertion; reached through the file's own switch, so NOTHING is written to disk and the arm re-runs in one step. (d) UI-30's OWN — restore one retired code into the `app.html` comment that used to spell it -> **2 of 71**, the first NAMING the file and the string: `app.html still carries …`. There is deliberately no in-VM half for (d): the sweep's subject is what is ON DISK in this package, so an arm run against a mutated string in memory would measure a different thing.
  TWO STALE COMMENTS CORRECTED WITH DATED REASONS, NEVER EXEMPTED, and both were load-bearing rather than decorative. `app.html`'s envelope comment spelled a retired code in its worked example; it is now unspelled ON PURPOSE, because a wire code written into a comment is a copy this file has no business holding and it is the copy that went stale — the ENVELOPE point the comment exists to make never depended on which code rode inside. The suite's `PLANE_WORDS` comment said the plane sends *"no `detail`, no sentence"* and raised the gap for CONDUCT; the plane closed it, so the comment now records what was raised, that it is closed, and that DEC-8's reasoning is unchanged rather than reversed. The suite's header claim that its mocks answer the wire shape *"from birth"* was corrected too: it was true of the shape and false of the content, which is the whole lesson.
  ONE THING FOR CONDUCT, NOT DECIDED HERE: **D-174's tension is now realised on the first screen a member meets.** That row watches the plane's refusal `detail` strings carrying plane vocabulary against UI-4's member-facing guard, and it was written when those sentences appeared in UI-13's dedicated pre-flight containers. This sentence — "no active credential", "a salted derivation", "its stored hash", "this instance" — now stands at the SIGN-IN GATE, and no vocabulary guard covers that surface (each one is scoped to its own rendered surface: `subject-view`, `capture-honesty`, the act suites). Nothing was translated and nothing was blanked, which is the only state DEC-8 permits a surface to ship; the wording-ownership question (does the plane learn member-facing refusal wording, or do surfaces get a licensed translation layer?) is exactly D-174's and is not a UI worker's to settle.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 084cafa). Verified from the main checkout: harness 32/32 with auth-surface 71, both guards green with the seam set unchanged, battery 98/98 (5415) with no plane file touched (the +1 is planning-hygiene 228→229 over this session's own planning writes), plancheck green. CONDUCT independently swept `civicos-ui/` for both retired codes: zero occurrences. The selection-not-composition reading of DEC-8 accepted, and so is the decision not to show the code beside the sentence. The four assertions guarding the textual READ are the best part of this item and generalise: an extraction that silently yields "" makes every `includes()` trivially true, which is the zero-cost-equality shape arriving in the INSTRUMENT rather than the subject — the first time this project has caught it there. The retargeted arm (b) is sharper than the original for the reason the worker gives: the mutation looks MORE faithful than what shipped, and only subtracting what the plane said and reading the remainder can see it. D-174's trigger condition fired here, exactly as that row was written to catch: DEC-49 raised with Bob (who owns member-facing refusal wording; provisional = as shipped, recommendation = the plane owns it) and UI-31 queued to make the tension MEASURED meanwhile, deliberately not waiting on the ruling. No other DEC.
## CLAIM 2026-08-05 RECORD

session: rec49-agent (worktree agent-a48767fd718a5c60c)
opened: 2026-08-05T10:00:00Z
paths: bio-plane/src/store.mjs (ONLY `publishedManifest()` — the `published[]` row SELECT gains the member's OWN frozen `strength`/`required`, parsed the same way `#caseEditionState()` parses them, and the answer's `detail` says at which altitude a pair lives; NOTHING ELSE in this file), bio-plane/test/multifinding.test.mjs (GROWN: a new block 6 — the INDEX sweep, over the whole `op=publishedmanifest` response, in both the complete and the awaiting window — plus this item's NEGATIVE CONTROL arms folded into the file's own `NEGATIVE CONTROL:` line, and block 5's DEC-44 sweep CORRECTED to descend into the manifest JSON string it could not see through), civicos-ui/app.html (ONLY the `__PUBLISHED_CASE__` region's INDEX functions `pubManifestOf`/`pubList` — the per-finding pair is read from the member's own ratified row, and UI-29's honest-for-now "not assembled for this edition yet" wording is CORRECTED at the site with a dated reason so it is reachable ONLY when it is true), civicos-ui/test/publishedcase.test.mjs (the index mock re-shaped to what the plane actually answers, and the index assertions GROWN; UI-29 pins corrected at the site with dated reasons, never exempted), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I3 ADDITIVE ONLY: `op=publishedmanifest`'s `published[]` rows gain `strength` and `required`, two fields that were already in `published_bundles` and already published per finding by `op=publishedcase`.** No field is removed, no field changes shape, no wire string changes. I5 is NOT touched: no table, column or index changes — this reads two columns the schema has carried since REC-14.
interfaces consumed: none changed.
concurrency: UI-30 runs concurrently on `civicos-ui`'s LOGIN GATE region and `civicos-ui/test/auth-surface.test.mjs`. DISJOINT and stated so both ways: this claim opens NEITHER — it touches only the `__PUBLISHED_CASE__` region's index functions in `app.html` and `civicos-ui/test/publishedcase.test.mjs`. UI-30 must not touch the published region and this item does not touch the gate, `signIn`, `PLANE_WORDS`, or any login reason code.
explicitly NOT touched: civicos-ui/app.html's login/gate region and every other region of that file, civicos-ui/test/auth-surface.test.mjs, every other civicos-ui/test suite, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs, civicos-ui/worker.template.mjs, civicos-ui/tokens.css, bio-plane/src/index.mjs, bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/checks/bio-checks.mjs, every other battery suite, newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md REC-49. The published INDEX tells the truth about a case's strengths. (a) the index is guarded against the understatement REC-44's move made possible — no suite asserted the index against a case that HAS a pair, so the class was invisible to a green battery; (b) CONDUCT'S DETERMINATION, implemented and not re-litigated: the index SHALL carry the per-finding pair for every RATIFIED member, awaiting window or not, because `published_bundles.strength` is that member's own signed, ratified pair and composes nothing. What the index must NEVER carry is a case-level pair, ratified or not (DEC-44).
accepts-when: as QUEUE.md REC-49. `cd bio-plane && npm run test:battery` green with `op=publishedmanifest` answering a complete case's manifest AND its per-finding pairs, and an INCOMPLETE case's pairs for exactly its ratified members and none for the awaited ones; `node civicos-ui/test/run.mjs` green plus check-semantics and check-mock-envelope; `npm run test:coverage --strict` exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — (i) drop the pair-bearing column again and the suite FAILS naming the index that understates a case which HAS a pair, and (ii) carry a case-level pair on an index row and DEC-44's structural sweep FAILS. BOTH sweeps are structural over WHOLE responses and never value comparisons, because REC-44 and UI-29 both measured that a value comparison passes while the surface goes on answering correctly and ADDITIONALLY answers wrongly. Every file restored byte-identical after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT.  MEASURED THIS TURN so nobody re-derives it. Worker commit in this worktree (NOT pushed): see the report.
  NUMBERS. `npm run test:battery` **98/98 suites, 5426 assertions, 85.8s**, zero leaked sandboxes (0/0 before and after). The +24 over UI-29's 5402 are ALL in `multifinding.test.mjs` (47 -> 58, +11) and in planning-hygiene's per-file assertions over the documents added since; no other plane suite moved. `node scripts/coverage.mjs --strict` **exit 0** — 130/130 ops through the control plane, 53/53 checks named, 98/98 suites declaring a control. `node civicos-ui/test/run.mjs` **32/32 suites green** with `publishedcase.test.mjs` 191 -> **196** and every other suite UNCHANGED; check-semantics green; check-mock-envelope green (arm A 13,284 lines, seam set UNCHANGED — this item adds no transport call; arm B 776 op answers across 66 ops).
  AN INSTRUMENT FINDING WORTH MORE THAN THE ITEM, and it is reported rather than smoothed: **`npm run test:coverage --strict` DOES NOT PASS `--strict` TO THE SCRIPT.** npm treats it as an npm CLI flag (`npm warn Unknown cli config "--strict"`) and the script's own package script already carries it, so the run is strict by accident rather than by the words. Worse for a session that trusts it: `npm run test:coverage --strict | tail` reports the exit status of `tail`, so a FAILED strict run reads as exit 0. This session recorded a false exit 0 that way and caught it only by running `node scripts/coverage.mjs --strict` directly — which is what the real gate must be, and what every number above was measured with. The wording is in CLAUDE.md, VERIFICATION.md and most queue items' accepts-when; routed to CONDUCT, not edited here.
  WHAT THE ITEM FOUND ABOUT ITS OWN PREMISE, measured against the running plane and not recalled. **Defect (a)'s LIVE consequence does not exist and has not since UI-29 landed.** `publishedManifest()` DOES carry `manifest` — on `cases[]`, at the case altitude REC-44 moved it to — and `app.html`'s index has read it from there since UI-29 corrected the surface in the same turn that found the shape change. Probed through `op=publishedmanifest` against a real two-finding case: `cases[].manifest` is the full `bio-case-container/2` JSON with every member's `strength`. So the index was NOT rendering "carries no frozen strength pair" for every case; UI-29's report said it *would have*, and the routing read the conditional as present tense. WHAT WAS REAL AND IS NOW CLOSED is the other half of (a): **nothing guarded it.** No suite anywhere asserted that this op still answers a pair for a case that HAS one, which is why a green battery let the move through — block 5's DEC-44 sweep looks for a pair that must NOT be there and passes perfectly on an answer carrying no pairs at all. That is the empty-body-digest shape, and block 6 is its complement.
  DEFECT (b) WAS REAL, IS LIVE, AND IS FIXED AS CONDUCT DETERMINED. `publishedManifest()` selected `strength`/`required` nowhere, and `cases[].manifest` does not exist until the LAST member of an edition ratifies — so through the awaiting window, which is days on a live instance, the public index had no pair to show for members that had signed one. The fix is one altitude down from where the item looked: the pair is now read off the member's OWN ratified row (`published_bundles.strength`, the pair frozen into the bytes that member signed), on the plane and on the surface, so it answers in both windows and the index no longer depends on a container that may not exist. It composes nothing and states no new claim.
  THREE MORE UNDERSTATEMENTS ON THE SAME ROW, found while building and fixed with the same reading. (1) The loose-bundle row asserted four absences in one breath — no case identity, no scope statement, no completeness assertion and NO FROZEN STRENGTH PAIR — and the fourth is not structural: a pair belongs to the FINDING, so a ratified finding in no case has one, and the row now shows it. (2) The index read "is this edition finished" off the roster, but the container is recorded by the CONTROL PLANE after the last ratification returns, so "every member ratified" and "the container exists" are two facts; in the gap the row printed `container sha256:` with nothing in front of the ellipsis. It now reads the plane's own `manifest_sha`. (3) UI-29's honest-for-now sentence blamed an unassembled container for a missing pair; with the pair on the member's own row that sentence would have been REACHED while false, so it now says the only thing still true when it is reached — the record carries no pair for this ratified member.
  AND THE ONE THE INSTRUMENT FOUND ABOUT ITSELF. `op=publishedmanifest` hands the container manifest over as a JSON **STRING**, and DEC-44's structural sweep walks a response OBJECT — so it stopped dead at that string, and the copy of the manifest a reader of the PUBLIC INDEX meets first was the one surface Bob's own control could not see inside. Measured, not supposed: arm (ii-b') plants a case-level pair there with the sweep as it was and the sweep is SILENT. `expandIndex` corrects it, at the site, with the date and the reason.
  NCs RUN x26, every file restored BYTE-IDENTICALLY and sha256 compared before and after each arm (src/store.mjs 34698539…, src/index.mjs 76533355…, checks/bio-checks.mjs d8da7b9d…, civicos-ui/app.html 2ed5beb2…), and ALL TWENTY-SIX RE-RUN against the final files so every count in both suites' NEGATIVE CONTROL headers agrees with the file it names. REC-49's five on the plane: (i-a) drop the pair columns -> 53 pass, 5 FAIL, the sweep NAMING the understatement in both windows ("RATIFIED member has NO frozen pair on the index — the index UNDERSTATES a case that HAS one") rather than reporting a shape mismatch; (i-b) drop `manifest` from cases[] again -> 57/1; (ii-a) a case-level pair on an index ROW -> 57/1 in BLOCK 5, naming `publishedmanifest.cases[0].strength` and `[1]`, WHILE EVERY BLOCK 6 ASSERTION STAYS GREEN — REC-44's control (a) reproduced at the index; (ii-b) the same pair inside the embedded manifest -> 57/1 naming `…cases[0].manifest.strength`; (ii-b') the same defect with the sweep as it was -> BLOCK 5 SILENT. REC-49's four on the surface: (p) the pair read back out of the container manifest -> 194/196, 2 FAIL, both of them in the awaiting window while every complete case stays green; (q) UI-29's sentence restored -> 194/196 — AND THIS ARM CORRECTED THE FIXTURE: it first fired ZERO, because an assertion that a sentence is absent passes for free when nothing renders the branch, so the awaiting edition gained a third member (ratified, no pair) and only then does the arm bite; (r) the loose finding told it has no pair -> 195/196; (s) completeness read off the roster -> 195/196. REC-44's four and UI-29/UI-18/UI-27's thirteen ALL RE-RUN: every FAIL count reproduced EXACTLY except two that GAINED one each, and both gains are real reach rather than noise — REC-44 (c) 2 -> 3 (block 6's fixture assertion counts case editions, rostered members and ratified rows, so a member ratified into a case that never declared it now moves numbers a roster-shaped assertion cannot miss) and UI-29 (c) 6 -> 7 (a composition into one letter is now caught in the awaiting window too, where before REC-49 there was no pair on that row to damage).
  EIGHT PINS CORRECTED WITH DATED REASONS, NEVER EXEMPTED: two in `civicos-ui/test/publishedcase.test.mjs`'s index block (the row count 5 -> 7 as the fixture grew, and "1 of 2 ratified" -> "2 of 3"), the "the pairs are parsed out of the CASE row's manifest column" pin re-aimed at the member's own row, the PUB_ROWS mock re-shaped to the wire (D-173 — it hand-answered rows with no `strength`, which WAS the plane's shape and was the defect), block 5's `publishedmanifest` surface expanded so the sweep can see into the manifest string, and the three surface wordings above.
  ONE DELIBERATE NON-CHANGE, stated so nobody reads it as an oversight: `cases[].manifest` STAYS on the index. It is the case's own record of what its container carried and a reconstruction needs it; it is simply no longer what a strength is read from. Arm (i-b) is what keeps it there.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 48e156d). Verified from the main checkout: battery 98/98 (5426), harness 32/32 with publishedcase 196, both guards green, `node scripts/coverage.mjs --strict` run DIRECTLY with its exit status read unpiped — genuinely 0 at 130/130 ops, 53/53 checks, 98/98 controls — zero leaked sandboxes, plancheck green. THE PREMISE CORRECTION IS ACCEPTED AND THE ERROR IS MINE: I routed UI-29's past conditional ("would have rendered") as present tense and called defect (a) a live regression. It was not; UI-29 had corrected the surface in the same turn it found the shape change. The worker probed a real two-finding case instead of inheriting my sentence, which is what the brief asked for and is why it was caught. The finding that survives is better than the one I routed: nothing GUARDED it — block 5's sweep looks for a pair that must not be there and passes perfectly against an answer carrying no pairs at all, the empty-body-digest shape, and that is why a green battery let REC-44's move through. Block 6 is now its complement and the two are useless apart. The one-altitude-below fix accepted with its reasoning (the pair comes off the member's own ratified row, so it answers in both windows and depends on no container that may not exist). The `expandIndex` instrument finding is the item's most transferable work: a manifest handed over as a JSON string is invisible to a sweep that walks objects, so the copy a reader of the public index meets first was the one surface DEC-44's own control could not see inside. CONDUCT'S OWN INSTRUMENT WAS CORRECTED THE SAME TURN on this worker's report: `npm run test:coverage --strict` does not pass the flag, and piping the run to `tail` reports tail's exit status — so a failed strict run reads as exit 0, and I cited exit 0 that way repeatedly this session. The outcome was right every time (the package script already carries `--strict`) but the evidence was not evidence. Fixed in CLAUDE.md, VERIFICATION.md and kickoffs/CONDUCT.md — the three loops whose readers run it. No DEC.

## CLAIM 2026-08-04 UI

session: ui28-agent (worktree agent-ae79ba085df7ee61a)
opened: 2026-08-04T21:00:00Z
paths: civicos-ui/app.html (ONLY the ATTEST ACT region: (1) the `attestBar` block inside `renderDocument` near line 4870, whose `<span class="n">` spells the fence in this surface's own words and whose UI-24 deferral comment names DEC-39 as pending — the ruling has landed; (2) the attest act region from `canAttest()` to `attestRefusalHtml()`, i.e. `ATTEST_YIELDS_GRADE` (DELETED), `attestStandingClaim`, `attestHonestyHtml`, `openAttestDialog`'s lede, and both receipts. Nothing else in this file, and specifically NOT the PUBLISHED INDEX region, NOT the sign-in region UI-30 just corrected, NOT UI-27's reader-supplied floors or its filter machinery, and NOT the U8 ADD SURFACE's own capture-grade teach block near line 12609 — see the report, it is a separate statement of the CAPTURE grade doctrine in REC-48's family and is reported rather than edited), civicos-ui/test/act-attest.test.mjs (the suite: its fence pins corrected at the site with dated reasons and never exempted, the fence asserted against `bio-plane/src/affordances.mjs`'s `ATTEST_FENCE` export and the two grade letters against `checks/bio-checks.mjs`'s `EARNED_CAPTURE_CEILING`/`UNREACHABLE_CAPTURE_GRADE`, the existing negative control KEPT and RETARGETED, and its `NEGATIVE CONTROL:` line rewritten with the arms as actually run), civicos-ui/test/document-page.test.mjs (ONLY the attestation section: its `DECORATED` capture-acts mock stops hand-answering `prompt:null` — a mock that answers a shape the plane no longer sends is D-173's class at the CONTENT altitude, UI-30's finding — plus one arm asserting the section is ABSENT when the record publishes no fence), docs/development/CLAIMS.md (this entry)
interfaces owned: none. UI CONSUMES I3.
interfaces consumed: I3 — `op=affordances`' `capture_acts` block AS REC-43 LANDED IT. The `attest` entry carries `prompt` = `ATTEST_FENCE`, composed in `bio-plane/src/affordances.mjs` from `EARNED_CAPTURE_CEILING` and `UNREACHABLE_CAPTURE_GRADE`. No interface change is requested by this item; this is the consumer half of DEC-39.
concurrency: **REC-49 runs concurrently on `civicos-ui/app.html`'s PUBLISHED INDEX region** (`publishedManifest()`'s consumer) and on the plane's `publishedManifest()`. DISJOINT BY REGION IN ONE FILE, so the seam is named rather than assumed: this claim opens `app.html` only inside the `attestBar` block of `renderDocument` and inside the attest act region (`canAttest()` … `attestRefusalHtml()`), and touches no index, manifest, `pubList`, `pub-`, or published-case code, no `@media print` rule, no `FLAT_OPS` entry, and no login/gate code. It opens NO file under `bio-plane/` — `src/affordances.mjs` and `checks/bio-checks.mjs` are IMPORTED by the suite as the authority and written to by nobody here.
explicitly NOT touched: bio-plane/** (every file), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs, civicos-ui/worker.template.mjs, civicos-ui/tokens.css, every other civicos-ui/test suite (including auth-surface.test.mjs, which UI-30 has just landed, and add-surface.test.mjs), QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md UI-28, DEC-39's UI half. The surface STOPS AUTHORING the attestation fence: `ATTEST_YIELDS_GRADE` and the hand-written honesty block render the PUBLISHED wording instead, and UI-24's rider widens from "renders the published label" to "renders the published label AND the published fence, inventing neither". Three things the item records so they are not re-derived: the wording is CONDUCT's draft under Bob's ruling and NOT Bob's own sentence, so the assertion is against the PUBLICATION and never against DECISIONS.md or a copy; the published `prompt` is a PLAIN STRING carrying no markdown; whether the three parts should be published as STRUCTURE is an open I3 shape question deliberately not pre-decided.
accepts-when: as QUEUE.md UI-28. `node civicos-ui/test/run.mjs` green with the fence rendered FROM the publication (asserted against the plane's export, not a copy) and NO local constant remaining; both guards green (`check-semantics`, `check-mock-envelope` — arm A treats a transport call outside a seam as a failure, including one inside a comment); the retargeted control still fails on any claim of the unreachable grade. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — restore the hand-written constant and the harness fails NAMING the copy, plus the retargeted arm. Every file restored byte-identically after each arm, sha256 compared before and after. The textual READ of the source region is itself GUARDED (UI-30's measurement: an extraction that silently yields "" makes every `includes()` trivially true — the zero-cost-equality trap arriving in the instrument rather than the subject). No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit fa1ba32 in this worktree (NOT pushed). MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `node civicos-ui/test/run.mjs` **32/32 suites green, exit 0** with `act-attest.test.mjs` **69 -> 83** (69 is that file's own recorded count at UI-24) and `document-page.test.mjs` gaining 3 assertion sites; no other suite's file was opened. check-semantics green; check-mock-envelope green — **arm A over 13,340 lines of app.html's script with the SEAM SET UNCHANGED** (`actAsk, api, apiQ, apiR, intentAsk, rec, recPost, recPostR, recR`: this item adds no transport call in code or in a comment), arm B 32 suites re-run under the probe, 787 op answers across 66 distinct ops, 140 op/suite pairs judged. `bio-plane/` is BYTE-UNTOUCHED (`git status` shows exactly four modified files; `checks/bio-checks.mjs` d8da7b9d… and `src/affordances.mjs` are IMPORTED as the authority and written by nobody). `civicos-ui/app.html` sha256 8f28e66b5cbb0e8046eac7f964e064f2cb584fd6ef672ff34a110075ee0650bf. `cd bio-plane && npm run test:battery` **98/98 (5415), 94.0s, zero leaked sandboxes** — the SAME 5415 UI-30 landed, which is the number that proves no plane file moved rather than a claim that none did.
  HOW THE FENCE REACHES THE MEMBER: BY IMPORT, not by a textual read. `bio-plane/src/affordances.mjs` and `checks/bio-checks.mjs` touch no `cloudflare:workers` binding, so the suite imports `ATTEST_FENCE` AND the two letters the wording is composed from (`EARNED_CAPTURE_CEILING`, `UNREACHABLE_CAPTURE_GRADE`) — UI-30 had to read `store.mjs` textually and this item does not. At RUNTIME the surface reads `captureAct("attest").prompt` off the published block, and the harness mock answers `decorateAct`'s own expression over the PRODUCER'S array, so the fence a member meets in the test is the fence the plane publishes.
  THE INSTRUMENT IS A SUBTRACTION, and it had to be: the publication NAMES the unreachable grade in order to DENY it, so a sweep for that letter would fire on the honest rendering. Every honesty assertion strips the rendered HTML to text, subtracts the publication, and reads the REMAINDER — UI-30's "subtract what the plane said" one item on. The source-region read is GUARDED (both regions found, minimum length, named anchors present), because a region that came back "" would make every "does not contain" trivially true.
  NEGATIVE CONTROLS RUN x3 (5 executions), ON DISK, every file restored BYTE-IDENTICALLY and sha256 compared before and after each arm (app.html 8f28e66b…, bio-checks.mjs d8da7b9d…); arms (a) and (b) also run mechanically in-VM on every invocation. **(a) THE COPY** — a hand-written copy of the fence plus the local grade constant restored -> **3 of 83**, the sweep printing NINE complaints that NAME it (four grade letters spelled in the region, four verbatim runs of the publication, the retired constant by name) while every rendered-value assertion stays GREEN, because an identical copy agrees at zero cost. **The third failure was not predicted and is the better argument: with a copy in hand the surface renders a fence THE RECORD NEVER PUBLISHED** — the no-fence-no-act gate reads the publication while the block reads the copy, so a copy does not merely risk drift, it re-arms the surface to speak when the record is silent. **(b) THE RETARGET** (the old arm, kept and re-aimed: there is no constant to flip, so the surface CLAIMS THE UNREACHABLE GRADE with the letter taken from `UNREACHABLE_CAPTURE_GRADE` rather than typed) -> **5 of 83**. **(c) MOVE THE RULE** — `EARNED_CAPTURE_CEILING` B -> C in the plane ALONE, nothing else touched -> **83/83 STILL GREEN**, the member reading the new ceiling and the new unreachable grade with no edit in app.html and none in the suite; the same move WITH arm (a)'s copy -> **14 of 83**, a member told the old ceiling while the gate refuses above the new one. That pair is the item in one measurement.
  PINS CORRECTED WITH DATED REASONS, NEVER EXEMPTED: five fence assertions in act-attest (the local-constant pin, both standing-claim grade pins, the dialog's does/cannot headings, the receipt's and the fail receipt's `data-standing` letter pins), that suite's header honesty paragraph and its NEGATIVE CONTROL line, document-page's `prompt:null` mock and its "the fence must stay this surface's own" paragraph. The `data-standing` marker is no longer a grade letter at all: a grade written by this surface into a machine-readable attribute is still this surface stating doctrine, under a tidier name.
  QUESTION (iii) ANSWERED BY MEASUREMENT, so CONDUCT does not have to rule: **the string form is sufficient and no ruling is needed.** The surface styles the three parts by splitting on the SHAPE of the labels (`What …:`) and holds no label string of its own; the rendered parts REASSEMBLE into the publication character for character, which the suite asserts. Publishing the parts as STRUCTURE would be a convenience, not an honesty requirement.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 73db995). Verified from the main checkout: harness 32/32 exit 0 (act-attest 83), both guards green with the seam set unchanged, battery 98/98 (5426) UNCHANGED from REC-49's landing — which is the evidence no plane file was touched — coverage strict run directly at 130/130, plancheck green. CONDUCT RE-RAN ARM (c) ITSELF, because it is the item's whole claim and not merely a control: `EARNED_CAPTURE_CEILING` moved B→C in the plane alone, no UI edit, the entire UI harness stays green and exit 0, and `checks/bio-checks.mjs` restored to the identical sha256. The local `ATTEST_YIELDS_GRADE` constant is independently verified GONE. Three things accepted with their reasoning and worth carrying forward. The SUBTRACTION instrument — the publication names the unreachable grade in order to deny it, so an honesty sweep must subtract the publication and read the remainder, since a plain 'does the page say Grade A' test fires on the correct page. The unpredicted third failure in arm (a), which is a better argument than the one the item was written on: with a copy in hand the surface renders a fence THE RECORD NEVER PUBLISHED, because the gate reads the publication while the block reads the copy. And `data-standing` carrying the act's OUTCOME rather than a grade letter — a letter written into a machine-readable attribute is still this surface stating doctrine, which is sharper than the item asked for. QUESTION (iii) ANSWERED BY MEASUREMENT AND CLOSED: the string form is sufficient, the parts reassemble into the publication character for character, and publishing them as structure would be a convenience rather than an honesty requirement — so no I3 shape change is owed and no DEC is raised. That is why it was deferred to the build instead of pre-decided. UI-32 routed for the fourth statement and its two small riders. No DEC.
## CLAIM 2026-08-04 RECORD

session: rec48-agent (worktree agent-aa2131b9a46ad1386)
opened: 2026-08-04T18:00:00Z
paths: bio-plane/src/affordances.mjs (ADDITIVE ONLY, and only in the composed-doctrine region beside `attestFence`/`ATTEST_FENCE`: one new composer `acquireGradeNote(ceiling, unreachable)` and its evaluated `ACQUIRE_GRADE_NOTE`. No act, no vocabulary, no rung, no existing export changes), bio-plane/src/index.mjs (ONLY the `op=acquire` success answer's `note:` field near line 3339 and the `checks/bio-checks.mjs`/`affordances.mjs` import lines at the head — the literal doctrine sentence is replaced by the composed constant. NOTHING else in this file: no dispatch, no OPS entry, no other op's answer), bio-plane/src/store.mjs (ONLY the `ceiling:` string inside the earned-basis capture-axis block near line 8194 — the single spelled grade letter becomes the imported `UNREACHABLE_CAPTURE_GRADE`, exactly as the `why:` line beside it already interpolates `EARNED_CAPTURE_CEILING` — plus that one import line. NOT `promote`, NOT the gate, NOT the audit sweep, NOT purge, and NONE of CAPTURE's link/capture/task/reachability functions), bio-plane/test/acquire.test.mjs (the note's pin CORRECTED at the site with a dated reason and never exempted, plus the composer-equality pin; its `NEGATIVE CONTROL:` line rewritten with the arms as actually run), bio-plane/test/hygiene.test.mjs (GROWN: the tree-wide source sweep over `bio-plane/src/` — no grade letter spelled beside the word "Grade" outside a comment — with its OWN REACH asserted; its `NEGATIVE CONTROL:` line extended), bio-plane/test/earnedbasis.test.mjs (ONE added assertion that op=earnedbasis's ceiling sentence names the DERIVED unreachable letter rather than a typed one; no existing assertion removed), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I3: NO SHAPE CHANGE. `op=acquire` keeps its `note` field, `op=earnedbasis` keeps its `ceiling` field; both keep their type (string) and their meaning. The BYTES of two strings are unchanged today by construction — the composed sentence is character-identical to the literal it replaces while the ceiling is B — and become a function of the enforced rule rather than a copy of it.** I5 is NOT touched: no table, column or index.
interfaces consumed: none changed. `EARNED_CAPTURE_CEILING` and `UNREACHABLE_CAPTURE_GRADE` are consumed from `checks/bio-checks.mjs` exactly as REC-43 published them.
concurrency: **UI-28 runs concurrently on `civicos-ui/app.html`'s ATTEST region** (it stops authoring the attestation fence and renders REC-43's published `prompt`). FULLY DISJOINT FROM THE PLANE and stated both ways: this claim opens NO file under `civicos-ui/` at all, and specifically not `app.html`, not `ATTEST_YIELDS_GRADE`, not any `civicos-ui/test` suite; UI-28 opens no file under `bio-plane/`. The one thing both items touch is the SAME PUBLISHED VALUE read from opposite ends — UI-28 renders `ATTEST_FENCE`, and this item does not alter `attestFence`, `ATTEST_FENCE`, `CAPTURE_ACTS` or the `attest` entry's `prompt` by a byte.
explicitly NOT touched: civicos-ui/** (every file), bio-plane/checks/bio-checks.mjs (READ as the authority for both letters and written to by nobody here — it is where REC-43 landed them, and this item adds no constant), bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/gate.mjs, every other bio-plane/src module, every other battery suite, newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md REC-48, routed out of REC-43. Close the remaining hand-written statements of the capture-grade doctrine. `op=acquire`'s `note:` is a DIFFERENT act speaking to a DIFFERENT reader than the attest fence, so it is NOT the same sentence and is NOT replaced by `ATTEST_FENCE`; it is composed the same way, from the two symbols REC-43 exported. After this, no surface spells a capture grade letter, and a source-level sweep makes a further statement impossible to add silently.
accepts-when: as QUEUE.md REC-48. `cd bio-plane && npm run test:battery` green with `op=acquire`'s note asserted to INTERPOLATE the enforced ceiling rather than spell it, and `acquire.test.mjs`'s pin corrected at the site with a dated reason; a source-level assertion that NO grade letter is spelled beside the word "Grade" anywhere in `bio-plane/src/`, **with that sweep's OWN REACH asserted** (a walk that covers nothing passes everything — UI-30 caught this class in an instrument and REC-49 had an arm that first fired zero for the same reason); `node scripts/coverage.mjs --strict` run DIRECTLY with its exit status read UNPIPED, exit 0. NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — move the enforced ceiling to C and the suite fails NAMING the note that still says B. A copy that agrees today agrees at zero cost (REC-43 measured that a hand-typed identical copy leaves every behavioural assertion green and only a structural pin bites), so the pin that bites is STRUCTURAL. Every file restored byte-identical after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5463 assertions, 88.8s**, zero leaked sandboxes (0/0 before and after). The +15 over REC-48's 5448 is FULLY ATTRIBUTED, not estimated: the per-suite lines of the before and after runs were diffed and **exactly one suite moved, hygiene.test.mjs 369 -> 384**, which is the register detector's self-test. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED: **exit 0**, 130/130 ops, 53/53 checks, **98/98 suites declaring a control at 251 arms stated**.
  THE MECHANISM, RE-MEASURED A THIRD TIME, AND THE BRIEF WAS WRONG ON THE PART THAT DECIDES THE FIX. The item says `\s*` crosses newlines so a wrapped control still matches, and that the 60-line window is what broke REC-48. Measured directly: `\s*` reaches a close that sits alone on the next line, and matches NOTHING when the next line carries TEXT — which is what a multi-arm block is. Reconstructing the exact shape REC-48 wanted gives **NO MATCH for both suites**, and both markers sit on line 1 where no window can reach them. So REC-48's original report named the right mechanism and the integration re-measurement named the wrong one. All three defects are closed anyway, because the window is a real (separate) defect: a marker past line 60 is invisible, and a declaration STRADDLING line 60 was recorded as whatever fragment fell inside while still counting as controlled.
  WHAT THE OLD REGISTER HAD BEEN RECORDING, since the item asked. No suite's entry was a mangled fragment. But **14 of 98 suites had a fuller declaration the register could not see**, and three were registered with fewer arms than they state: reading-wire 2 of 3, reuse-ratify 4 of 5, setup-honesty 3 of 5. `readingname.test.mjs` is the window's own victim — its 4-line, 2,233-character declaration begins on line 59 and was entirely outside the window, so the register quoted its 870-character one-liner instead. After REC-48's blocks were restored, acquire went from 2 arms recorded to 7 and hygiene from 4 to 12. Over the whole tree the old detector read 234 arms and the new one reads 251.
  THE TWO JUDGEMENTS, made and recorded at the site (scripts/control-register.mjs's header). WHERE A DECLARATION ENDS: at its comment's close or at the first blank comment line, whichever comes first — a declaration is a PARAGRAPH, because several suites state the control as one paragraph of a longer header and running to the close would quote back prose the suite never declared. WHICH ONE IS RECORDED when a suite states its control twice, which M0-2's backfill left most of them doing: the FULLEST single block, **never the sum**, because summing two statements of one control credits it twice — the generous direction wearing a different hat. ARMS ARE REPORTED AND NEVER GATED: an arm is one ` -> ` transition, the register grammar's own separator, and `--strict` still fails on exactly what it always failed on. A floor on arm counts would be strictness this instrument cannot justify, and the brief is right that over-strict is as bad as generous.
  NCs RUN x4, every file restored BYTE-IDENTICALLY with sha256 compared before and after (scripts/control-register.mjs caeac36b…, scripts/coverage.mjs a5df1b87…, test/acquire.test.mjs e065c8e0…, test/hygiene.test.mjs f66974ac…, test/capture.test.mjs e71bf8b8…). **(a)** hide a suite's declaration entirely (every marker in acquire.test.mjs) -> strict run **EXITS 1**, 97 of 98, NAMING acquire.test.mjs. **(b)** truncate a multi-arm block (hygiene's own, 8 lines to 5) -> that declaration reports 6 arms instead of 9 and the total 243 instead of 246, while strict STAYS exit 0 — the design, since arms are reported not gated, and the visible drop IS the mechanism. **(c)** put the 60-line window back into the detector -> hygiene 381/3, exactly the three window arms. **(d)** make the detector first-line-only -> hygiene 377/7, and the REAL-CORPUS arm bites with the fixtures; the register's total falls to **234, which is exactly what the old detector reported over this same tree**, so (d) reproduces the closed defect rather than resembling it.
  ONE PROPERTY FOUND WHILE RECORDING THE CONTROLS, and stated rather than smoothed: the arm TOTAL is a function of the declarations' own prose, so writing an NC record INTO a declaration moves the total. The totals above are as measured when each arm ran; only the DELTAS are comparable across edits, and the suite says so at the site. For the same reason every fixture in the self-test builds its marker from the instrument's exported constant instead of writing it literally — a literal would have put the test's own fixtures into the register.
  REACH ASSERTED 14 WAYS at the foot of hygiene.test.mjs, every one a DELTA or a read of the real corpus rather than an absolute a deaf detector could also satisfy (REC-48's reach assertion was wrong when first written for exactly that reason): found past line 60 AND not found with the marker hidden; a straddling block read whole; five arms counted from a marker line that states none, and the truncation measured as a delta between two blocks rather than against 1; the extent asserted in BOTH directions (reaches its continuation lines, does NOT swallow the paragraph after); the `//` form and its end; a control stated twice recorded once; the register read out of capture.test.mjs's REAL source and then not read once its marker is hidden; more arms than suites; and **at least one MULTI-LINE declaration present in the tree**, which is the assertion that makes REC-48's workaround un-reinstatable. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5448 assertions, 90.1s**, zero leaked sandboxes (0/0 before and after). The +22 over REC-49's 5426 are FULLY ATTRIBUTED and measured, not estimated — the three changed suites were re-run from a stash of this worktree at REC-49's tree: acquire 72 -> 79, hygiene 356 -> 369, earnedbasis 52 -> 54, and NO other suite moved. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED: **exit 0**, 130/130 ops through the control plane, 53/53 checks named, 98/98 suites declaring a negative control.
  THE ITEM AS BUILT. `acquireGradeNote(ceiling, unreachable)` and its evaluated `ACQUIRE_GRADE_NOTE` sit in `src/affordances.mjs` BESIDE `attestFence`/`ATTEST_FENCE`, for REC-43's own reason one item on: both index.mjs and store.mjs already import that layer, a test can import it in node (neither module reaches `cloudflare:workers`), and a reader who moves the ceiling finds every composed doctrine sentence in one file. It THROWS rather than composing a sentence it cannot make true, exactly as the fence does. The composed string is CHARACTER-IDENTICAL to the literal it replaces while the ceiling stands at B, which is the point rather than a coincidence: nothing on the wire changes today, and everything changes on the day the doctrine moves.
  A FOURTH STATEMENT FOUND, and the item's premise corrected on the record rather than tidied: REC-48's scope counted THREE. `op=earnedbasis`'s `ceiling:` string in `store.mjs` spelled the unreachable letter in its own letters — a member-facing sentence, on the read a surface uses to fill a leg in BEFORE writing it — while the `why:` line directly above it already interpolated the ceiling. It was found by sweeping for the CLASS instead of taking the count on trust, which is the whole argument for the source-level sweep this item was asked to build. Composed the same way; the `why:`/`ceiling:` pair now agree by construction.
  THE SWEEP, AND ITS REACH. `hygiene.test.mjs` walks every `.mjs` in `bio-plane/src/`, strips comments in the three forms this tree writes (JS block, JS line, and the `--` lines inside schema.mjs's SQL literal), and asserts no grade letter is spelled beside the word. TWO DETECTORS, because one has a loophole: (A) `Grade <LETTER>` with a capital G, the doctrine's own capitalised term and the assertion the accepts-when names; (B) the same match case-insensitively but NARROWED TO THE TWO LETTERS THE RULE OWNS, taken from `EARNED_CAPTURE_CEILING` and `UNREACHABLE_CAPTURE_GRADE` so the sweep's subject is a function of the rule and not a second copy of it. The line-comment strips are ANCHORED to line start on purpose: an unanchored `//` strip eats everything after `https:` inside a string literal, which would make the sweep silently reach LESS than it claims. FIVE REACH ASSERTIONS, because a walk that covers nothing passes everything: the file list is non-trivial and names the four modules that state or enforce the doctrine; a planted control fires in EVERY file's own stripped text, measured as a DELTA against that file's own count so the reach assertion cannot be confused by a file that already has hits (it was written as `=== 1` first and arm (i) exposed it — reported, not smoothed); the same detector over the RAW sources DOES match (14 hits in 4 files), proving the walk reaches the very lines where the doctrine is discussed; both emitted doctrine SENTENCES survive the strip intact, so the stripper cannot have swallowed the region a spelled letter would appear in; and the rule's two letters are readable and distinct, so (B) is narrowed to something real.
  A STATED LIMIT RATHER THAN AN EXEMPTION LIST. Five sentences in `store.mjs` spell a lowercase CONNECTION-axis letter ("section 8.1's grade C", "grade D is recorded testimony"). They are a different doctrine on a different axis with no exported constant to compose from — `checkEarnedLeg` types testimony's 'D' at the enforcement point itself — and minting one is a doctrine act, not a sweep. They are OUT OF THIS SWEEP'S SUBJECT and are NOT exempted from it. The same block also shows why no letter-near-the-word rule can ever be complete for ALL axes: it says "no leg of it earns an A/B/C connection grade", with the letters on the wrong side of the word. The CAPTURE axis can be complete, because it owns exactly two letters and both are exported.
  NCs RUN x5 (three in acquire's header, three in hygiene's, two in earnedbasis's — they overlap by design, each suite recording the arms that move IT), every file restored BYTE-IDENTICALLY and sha256 compared before and after: src/index.mjs 16cf4e2f..., src/store.mjs 7c1ed3aa..., src/affordances.mjs 77243d90..., src/cdx.mjs a9e5912c..., checks/bio-checks.mjs d8da7b9d... — all five identical after all five arms. ALL RE-RUN against the FINAL files. (a) THE ITEM'S OWN — the note published as a hand-typed literal copy -> **acquire STAYS 79/79 GREEN** and only hygiene moves (367/2), which is REC-43's measurement reproduced on a second sentence and the reason the pin that bites is structural. (b) that literal kept and the ceiling moved to C -> acquire 74/5, naming the note that still says B in both directions; a caller told Grade B while the gate refuses above C. (c) the COMPOSED note under the same moved rule -> acquire 78/1, and the difference between (b) and (c) is the whole item. (d) the FOURTH statement hand-typed back into store.mjs -> **earnedbasis STAYS 54/54 GREEN**, hygiene 367/2. (e) a FIFTH statement written LOWERCASE in `src/cdx.mjs`, a module nothing else guards -> hygiene 368/1 and it is detector (B) ALONE, with (A) silent — the arm that earns (B) its existence instead of arguing for it.
  A REAL FINDING THE CONTROLS SURFACED, ROUTED AND NOT CLOSED HERE. Arm (c)'s one remaining failure is not this item's doing: `src/index.mjs` stamps `grade: via === "archive.org" ? "C" : "B"` — the capture's actual GRADE VALUE, where the direct-fetch letter is `EARNED_CAPTURE_CEILING` by definition and would drift the moment the ceiling moved. It is deliberately NOT closed: composing only that arm would leave the archive-sourced letter typed and possibly at or above the new ceiling, and giving it a constant means NAMING a second capture-axis doctrine value, which is a ruling rather than a sweep. The assertion that catches it is left IN (`a.document.capture.grade === EARNED_CAPTURE_CEILING`, green today), so the gap is MEASURED rather than remembered.
  AN INSTRUMENT DEFECT FOUND IN PASSING, and it is the same class CLAUDE.md was corrected for on 2026-08-04. `scripts/coverage.mjs` detects a suite's control with `/NEGATIVE CONTROL:\s*(.+?)\s*(?:\*\/|$)/` over the first 60 lines — `.` does not cross newlines and there is no `m` flag, so a NEGATIVE CONTROL block that opens on line 1 and CONTINUES onto line 2 matches NOTHING and the suite reads as declaring NO CONTROL. Writing the arms as a multi-line block took the strict run from exit 0 to **exit 1 at 96 of 98**, naming acquire.test.mjs and hygiene.test.mjs — a suite whose control got LONGER counted as a suite with no control at all. Fixed here by keeping the marker line self-contained and putting the arms in a second comment (earnedbasis.test.mjs's existing shape), which is convention holding up an instrument; ROUTED to CONDUCT, not edited here. It was caught only because the strict run is now made directly with `$?` read unpiped.
  ONE JUDGEMENT MADE AND RECORDED AT THE SITE, not returned. The note's WORDING stands; only the letters are composed. Its third clause ("co-attestation raises <ceiling> toward evidentiary weight") is the same claim as the ruled fence's own third part ("it strengthens a Grade <ceiling> capture toward evidentiary weight"), so the note states a SUBSET of ruled wording and cannot overclaim relative to it. What DEC-39 corrected was a SURFACE presenting that clause AS the co-attestation decision prompt with nothing beside it; op=acquire's note is the receipt for bytes already captured, handed to a caller who is deciding nothing, and the full fence is one op=affordances call away on the act that does decide. Widening it would put two accounts of co-attestation in front of one member. The reasoning is written on `acquireGradeNote` itself and pinned by an assertion that the note is NOT `ATTEST_FENCE`, so the next reader who notices they overlap cannot quietly collapse them.
  PINS CORRECTED WITH DATED REASONS, NEVER EXEMPTED: acquire.test.mjs's two grade pins at the site (one named two letters in its own label and compared against a third copy of one of them; the other checked that a reason-shaped phrase appeared and would have passed if every letter in the sentence had moved), its file header's claim about where the load sits, and its NEGATIVE CONTROL line rewritten with the arms as run. hygiene.test.mjs's reach assertion corrected mid-run from an absolute count to a delta. earnedbasis.test.mjs gains two assertions and its NC header two arms; no assertion was removed anywhere.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 3118c4b). Verified from the main checkout: battery 98/98 (5448), coverage strict run directly with `$?` unpiped at 130/130 ops, 53/53 checks, 98/98 controls, zero leaked sandboxes, plancheck green. The fourth statement is the item's justification: it was found by sweeping for the CLASS rather than trusting the count of three I put in the brief, and it sat on a member-facing read used to fill a leg in BEFORE writing it. The two-detector design accepted, and arm (e) — a lowercase fifth statement caught by (B) with (A) silent — earns detector (B) its existence rather than arguing for it. The anchored line-comment strip is the right instinct for the same reason: an unanchored strip eats everything after `https:` and makes the sweep reach less than it claims. The reach assertion being WRONG when first written, exposed by an arm and reported rather than smoothed, is exactly the discipline this project runs on. The connection-axis limit is STATED rather than exempted, and the observation that no letter-near-the-word rule can be complete for all axes (letters can fall on either side of the word) is worth more than the sweep itself. TWO ROUTED. REC-50 for op=acquire's stamped letters, with the determinate half determined by CONDUCT and the archive-sourced letter deliberately left open as a ruling rather than settled by writing a constant. M0-9 for the coverage instrument — and CONDUCT RE-MEASURED THE MECHANISM AT INTEGRATION AND GOT IT WRONG — **corrected 2026-08-04 when M0-9 landed and its worker measured my measurement.** I claimed `\s*` crosses newlines so a wrapped control still matches, and that the 60-line window was the cause. My test case put the comment's `*/` on the SAME line as the text; a real multi-arm block closes on its OWN line, and then nothing matches at all — REC-48's original report was right. The 60-line window is a genuine SECOND defect (`readingname.test.mjs` lost a 4-line declaration to it) but is not what bit REC-48. Also real, and the quieter half of the same expression — only the FIRST line is captured, so a five-arm control is recorded as one arm while the instrument reads fully green. That is the generous direction, which is the one failure this instrument exists to prevent. No DEC.

## CLAIM 2026-08-04 UI

session: ui31-agent (worktree agent-af3ef00035d5b8a28)
opened: 2026-08-04T22:30:00Z
paths: civicos-ui/test/preauth-vocabulary.test.mjs (NEW — the whole item lives here: the pre-authentication vocabulary guard, its REACH assertions, and its REPORT), docs/development/MEASUREMENTS.md (APPEND ONLY — one dated section carrying the measurement DEC-49 should be answered against), docs/development/CLAIMS.md (this entry)
interfaces owned: none. UI CONSUMES I3.
interfaces consumed: I3 — `op=login` (REC-41's one refusal code and its `detail` sentence, READ out of `bio-plane/src/store.mjs` and never typed), `op=publishedmanifest` and `op=publishedcase` (the two ops the plane declares `classes: null`, reached by the unauthenticated boot path). No interface change is requested and none is made.
concurrency: **REC-48 runs concurrently on `bio-plane/src/index.mjs` and `bio-plane/checks/bio-checks.mjs`. FULLY DISJOINT, and named rather than assumed: this claim opens NO file under `bio-plane/` at all** — `src/store.mjs` is READ as the authority for the refusal sentence and written to by nobody here. It also opens no file that UI-32 is queued against (`civicos-ui/app.html`, `civicos-ui/test/auth-surface.test.mjs`, `civicos-ui/test/add-surface.test.mjs`).
explicitly NOT touched: **`civicos-ui/app.html` — DELIBERATELY AND AS THE POINT OF THE ITEM.** UI-31 must not translate, blank or map any wording, because DEC-49 is open with Bob and a surface that invented a translation to satisfy a guard is the bug-in-the-gate shape DEC-8 forbids. Also not touched: bio-plane/** (every file), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs, civicos-ui/worker.template.mjs, civicos-ui/tokens.css, every EXISTING civicos-ui/test suite (auth-surface, publishedcase, add-surface and the rest are READ as the authority for the inherited term list and written by nobody), QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, VERIFICATION.md
expected: QUEUE.md UI-31, D-174's trigger. No guard covers ANY pre-authentication surface, so the plane vocabulary now standing on the first screen a member meets is UNMEASURED rather than accepted. This item turns an unmeasured tension into a measured one and settles nothing: the guard's REACH is extended to every surface a member can see before authenticating, discovered BY WALKING (the gate's own markup names its controls; the script's own top-level statements name the unauthenticated routes; the sibling suites' own sweeps name the terms) rather than by a hand-kept list that falls behind the thing it describes (D-93); and it REPORTS what it finds rather than failing on the shipped sentence.
accepts-when: as QUEUE.md UI-31. `node civicos-ui/test/run.mjs` green, exit status read unpiped; **the guard's own REACH ASSERTED** — a walk that covers nothing passes everything, and this project has hit that class three times (UI-30 in an instrument, REC-49 with an arm that first fired zero, UI-28's guarded-region read) — so the discovered entry points, the driven scenarios, the harvested surfaces and the inherited term list all carry their own count-and-name assertions; the pre-authentication plane vocabulary REPORTED with its exact terms and their sources; both guards green (`check-semantics`, `check-mock-envelope` — arm A treats a transport call outside a seam as a failure, including one inside a comment). NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — hide a member-facing surface from the walk and the harness FAILS naming what it stopped covering. Files restored byte-identically after each on-disk arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `node civicos-ui/test/run.mjs` **33/33 suites green, exit 0 read unpiped**, with `preauth-vocabulary.test.mjs` NEW at **31 assertions** and every other suite UNCHANGED. check-semantics green; check-mock-envelope green — **arm A over 13,383 lines of app.html's script with the SEAM SET UNCHANGED** (`actAsk, api, apiQ, apiR, intentAsk, rec, recPost, recPostR, recR`: this item adds no transport call in code or in a comment, and adds no line to app.html at all), arm B 33 suites re-run under the probe, **791 op answers across 66 ops, 143 op/suite pairs judged** (787/66/140 at UI-28's landing — the +3 pairs are this suite's `login`, `publishedmanifest` and `publishedcase`, all answered in the wire shape the guard's map declares). `cd bio-plane && npm run test:battery` **98/98 (5426), 92.3s, zero leaked sandboxes** — the SAME 5426 REC-49 and UI-28 landed, which is the evidence no plane file moved rather than a claim that none did. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read with nothing piped after it: **exit 0**, 130/130 ops, 53/53 checks, 98/98 controls. `civicos-ui/app.html` sha256 478ba5ca0fe141d34832d7b14ed33d43c907334bc1d51ac55abea9603d3478b3 — UNCHANGED, and deliberately so.
  THE MEASUREMENT IS IN `MEASUREMENTS.md` with its date and instrument, and the instrument is in the loop the reader runs. **13 plane-vocabulary terms reach 5 of the 12 pre-authentication surfaces walked; 67 occurrences in the rendered HTML, 56 of them in text a member READS.** Five are the login refusal's, at the gate, and are UNAVOIDABLE — the plane said them and DEC-8 forbids this surface translating or blanking them. The three findings that bear on DEC-49 are on that page; the sharpest is that **8 of 13 terms are the SURFACE's own words and NEITHER answer to DEC-49 touches them** — `MEMBER_TOKEN` is printed as a field LABEL at the gate, `CORS` and `R2` stand in prose, and a ruling about who owns the PLANE's wording leaves all of it exactly where it is.
  THE WALK IS DISCOVERED, NOT LISTED (D-93). The gate's own markup names its controls (12 ids, 6 bound to handlers, each of which some scenario must drive); `publishedRouteFromHash`'s own body names the two addresses that resolve AT LOAD for somebody holding nothing, each of which some scenario must open; and the TERM LIST is harvested from the sibling suites' own `for(const word of […])` sweeps — 74 terms from 10 suites — so this guard polices on the pre-auth surfaces exactly what its siblings already police on theirs and cannot fall behind them. Two STRUCTURAL rules sit beside the inherited list because a rule cannot fall behind at all: SCREAMING_SNAKE identifiers (which is how `MEMBER_TOKEN` was found — no sibling list has it, because no sibling surface is the gate) and bare 2-4 character acronyms that never appear in ordinary case on the same surface (`CORS`, `R2`). The acronym rule's four-character limit is MEASURED and stated at the site: at five it reported `SERVE` and `WHOLE` beside `BEGIN`.
  THE REACH IS ASSERTED BY NAME AND BY COUNT, because a walk that covers nothing passes everything and this project has hit that class three times in two days. Ten scenarios each assert a MARKER proving they rendered their own subject rather than an error pane; the harvested surface set is compared to an expected set with BOTH differences named ("STOPPED COVERING" and "NEWLY RENDERED AND UNCLASSIFIED"); the harvest's total size is asserted; and the attribution is asserted NON-DEGENERATE — if the subtraction silently returned nothing, every occurrence would read surface-authored and the report would blame this surface for the plane's own sentence, which is the one error that would have misdirected DEC-49 outright.
  NEGATIVE CONTROLS RUN x5 (7 executions). (a) THE ITEM'S OWN, run three times because the three hidings fail through three different assertions: `UI31_HIDE=public-record` -> **2 of 29**, naming the scenario and the gate control that lost its only driver (`g-pub`); `UI31_HIDE=design-preview` -> **3 of 30**, adding five member-facing surfaces by name; `UI31_HIDE=case-address-at-load` -> **3 of 30**, naming the scenario, the ADDRESS SHAPE nothing opens any more (printing the router's own regex) and the harvest collapsing 33,412 -> 8,727 characters. **That third hiding is the arm that proves the ADDRESS walk is load-bearing**: it renders into `#pub-body`, which another scenario also renders, so a surface-set check alone would have stayed green while the largest pre-authentication surface in the product stopped being read. (b) `UI31_EMPTY_TERMS=1` -> **4 of 31**. (c) `UI31_NO_PLANE_RANGES=1` -> **1 of 31**, printing "0 plane-sourced rows and 16 surface-authored". (d) `UI31_ENFORCE=1` -> **1 of 31**, which IS the failing arm DEC-49's answer makes permanent, measured now rather than promised. (e) ON DISK, and the only arm that writes anything: `app.html`'s gate hint gains "capture_sha" -> the report grows 13 terms to 14, the new row naming the right term, the right surface, and the SURFACE as its author; run again with `op=login` instead, the term count stays 13 and that row's SOURCE list gains the gate, so the report distinguishes a new word from a known word at a new site. `app.html` restored BYTE-IDENTICALLY after both runs, sha256 478ba5ca… before and after, `git status` clean on that file.
  HOW IT BECOMES A FAILING ARM, in one line and said at the site: set `REPORT_ONLY` to `false`. It is correct under EITHER answer to DEC-49 — (a) the plane learns member-facing wording and the plane-sourced rows go away at their source; (b) surfaces get a licensed translation layer and they go away at the surface — because either way the state the ruling produces is "no plane vocabulary reaches a pre-authentication surface".
  ONE PIN CORRECTED, and it is this session's own rather than someone else's: nothing in another suite needed correcting, because this item adds a guard where none existed rather than changing one. `bio-plane/package.json` was modified by `npm approve-scripts` while installing the battery's dependencies in this fresh worktree, and was REVERTED — it is not this item's file and the change was an artefact of the machine, not of the work.
  NOTHING WAS TRANSLATED, BLANKED OR MAPPED, and `app.html` is byte-identical. That is the item: DEC-49 is Bob's, and a guard that failed on the shipped sentence would have forced this surface to invent one.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 0e66041). Verified from the main checkout: harness 33/33 exit 0 read unpiped, both guards green with the seam set unchanged, battery 98/98 unchanged, coverage strict run directly at 130/130. `civicos-ui/app.html` confirmed byte-identical in the merge — which is the item, not an omission. CONDUCT ran the enforcement arm itself (`UI31_ENFORCE=1`) and it reproduces, naming all 13 terms with their surfaces and authorship. The measurement is accepted and CARRIED TO BOB as a pointer from DEC-49's entry rather than a copy into it (D-21). Its two structural findings reshape the question and are stated there: the login refusal sentence is the only plane-sourced group AT THE GATE, so the ruling's subject is smaller than it looked; and eight of thirteen terms are the surface's OWN words, which neither answer to DEC-49 moves — routed as UI-33, not waiting on the ruling. The best arm is (a)'s third hiding: `case-address-at-load` renders into `#pub-body` which another scenario also renders, so a surface-set check alone would have stayed GREEN while the largest pre-auth surface stopped being read — the reach discipline this project has now needed five times, and the first time it caught a walk that was covered on paper. The non-degenerate attribution assertion and the switch-driven arms (nothing written to disk, re-runnable in one step) are both right. Refusing to reword anything while DEC-49 is open is the correct call and is why the measurement is trustworthy. D-174's stale clause corrected to point at the measurement; the row stays open until DEC-49 is answered. The `allowScripts` machine artefact caught and reverted rather than committed — noted approvingly. No DEC raised.
## CLAIM 2026-08-04 RECORD

session: rec50-agent (worktree agent-a62b116ec3beab516)
opened: 2026-08-04T20:30:00Z
paths: bio-plane/src/index.mjs (ONLY the `op=acquire` success answer's `capture.grade` field near line 3283 and the comment block directly above it — the DIRECT-FETCH arm of `grade: via === "archive.org" ? "C" : "B"` becomes the imported `EARNED_CAPTURE_CEILING`. The archive-sourced arm's letter is NOT touched, NOT named and NOT given a constant: naming it is a ruling and CONDUCT left it open deliberately. No dispatch, no OPS entry, no other op's answer, and NOT the `note:` field REC-48 landed), bio-plane/test/acquire.test.mjs (the BODY only — the grade block near line 102 gains the STRUCTURAL pin that the direct-fetch arm interpolates the ceiling rather than spelling it, plus the explicit statement of the archive-sourced letter's OPEN status and the ruled ordering between the two arms; the `NEGATIVE CONTROL:` header block is extended with this item's arms as run), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I3: NO SHAPE CHANGE and NO VALUE CHANGE TODAY. `op=acquire` keeps `document.capture.grade` as a single letter; the direct-fetch answer is `EARNED_CAPTURE_CEILING`, which is `'B'` — character-identical to the literal it replaces while the ceiling stands where it stands, and a function of the enforced rule from now on. The archive-sourced answer is unchanged in every respect.** I5 is NOT touched: no table, column or index.
interfaces consumed: none changed. `EARNED_CAPTURE_CEILING` is consumed from `checks/bio-checks.mjs` exactly as REC-43 published it and REC-48 read it. No constant is added anywhere.
concurrency: **TWO WORKERS RUN CONCURRENTLY AND BOTH ARE DISJOINT FROM THIS ONE, stated both ways.** M0-9 holds `bio-plane/scripts/coverage.mjs` and the `NEGATIVE CONTROL:` HEADER BLOCKS of `test/acquire.test.mjs` and `test/hygiene.test.mjs`. This claim opens `scripts/coverage.mjs` not at all and `test/hygiene.test.mjs` not at all; it does open `test/acquire.test.mjs`, and the split is by REGION — **M0-9 holds that file's `NEGATIVE CONTROL:` header block, this claim holds its BODY**, with the one exception named and agreed in the brief: this item's own arms are appended to the header's arm list, which is the one line an NC discipline requires its author to write. UI-31 holds `civicos-ui/**` and `docs/development/MEASUREMENTS.md`; this claim opens no file under `civicos-ui/` at all and does not write MEASUREMENTS.md.
explicitly NOT touched: bio-plane/checks/bio-checks.mjs (READ as the authority for the ceiling and written to by nobody here — this item adds NO constant, which is the point of it), bio-plane/src/affordances.mjs, bio-plane/src/store.mjs, bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/cdx.mjs, every other bio-plane/src module, bio-plane/scripts/**, bio-plane/test/hygiene.test.mjs, bio-plane/test/earnedbasis.test.mjs, bio-plane/test/affordances.test.mjs, every other battery suite, civicos-ui/** (every file), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md REC-50, routed out of REC-48. `op=acquire` still STAMPS its capture grade letters as literals — the last place on the capture axis that is typed rather than composed. **CONDUCT'S DETERMINATION, implemented and not re-litigated: the DIRECT-FETCH arm is composed from `EARNED_CAPTURE_CEILING`**, because that letter IS the ceiling by definition, it names no new doctrine, and leaving it typed is the drift REC-43 and REC-48 closed everywhere else. **WHAT THIS SESSION MUST NOT DECIDE BY WRITING A CONSTANT: the archive-sourced letter.** Naming it asserts what an archive-sourced capture earns and whether that is a ceiling or a fixed grade — a second capture-axis doctrine value, which is Bob's ruling. It stays a literal, and its OPEN status is stated EXPLICITLY in the suite so the next reader knows it is open by decision and not by oversight. The five lowercase CONNECTION-axis spellings REC-48 recorded as a stated limit are the same question one axis over and are left exactly as they are.
accepts-when: as QUEUE.md REC-50. `cd bio-plane && npm run test:battery` green with `op=acquire`'s direct-fetch grade INTERPOLATING the enforced ceiling; **REC-48's arm (c) re-run before and after, and its one remaining failure GONE** — that is the evidence this item closed what it claimed; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, exit 0. NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — move `EARNED_CAPTURE_CEILING` and the suite fails NAMING any stamped letter that did not follow. A copy that agrees today agrees at zero cost (REC-43 and REC-48 each measured that a hand-typed identical literal leaves every behavioural assertion green and only a structural pin bites), so the pin that bites here is STRUCTURAL. Every file restored byte-identical after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5453 assertions, 91.8s**, zero leaked sandboxes (0/0 before and after, host orphans 0 -> 0). The +5 over REC-48's 5448 is ATTRIBUTED and not estimated: acquire.test.mjs 79 -> 84 is the whole of it, measured standalone before (79, as REC-48 recorded and as arm (c)-before reproduced) and after (84), and no other suite was opened — the only other file touched is this one, which planning-hygiene already counted. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED: **exit 0**, 130/130 ops through the control plane, 53/53 checks named, 98/98 suites declaring a negative control.
  THE ITEM AS BUILT. `src/index.mjs` stamps `grade: via === "archive.org" ? "C" : EARNED_CAPTURE_CEILING`, the symbol imported from `checks/bio-checks.mjs` on the import line that already brings the locator fence from the same module — the layer where `checkEarnedLeg` computes the refusal. NOTHING ON THE WIRE MOVES TODAY (the ceiling is B, which is the letter that was typed) and the field becomes a function of the enforced rule from now on. No constant was added anywhere, which is the point of the item rather than an omission.
  THE EVIDENCE THE ITEM CLOSED WHAT IT CLAIMED. REC-48's arm (c) — the composed note under `EARNED_CAPTURE_CEILING = 'C'` — was run BEFORE any edit of mine, from this worktree at the merge commit: **78 pass, 1 FAIL**, and the failure was exactly the one REC-48 named and left in ("and the letter it claims is the one the gate ENFORCES, not a copy that agrees today", want "C" got "B"). Re-run AFTER against the final files: **that failure is GONE**. The suite is 82/2 in that arm now, and both remaining failures are ones that SHOULD fire on a doctrine move — see arm (f).
  NC RUN x3, every file restored BYTE-IDENTICALLY with sha256 compared before and after (src/index.mjs db8b7b9b..., checks/bio-checks.mjs d8da7b9d... — the same digest REC-48 recorded), all three re-run against the final files, and recorded in the suite itself. (d) THE ITEM'S OWN — the direct-fetch letter hand-typed back as `"B"`, an identical copy that agrees today -> **83/1, the STRUCTURAL pin ALONE**, with both wire pins green AND **hygiene.test.mjs unmoved at 369/0**: REC-48's tree-wide sweep cannot see this copy, because a bare `"B"` in a ternary spells no word beside a letter. That is REC-35's finding measured a THIRD time, on a third subject, and it is why the pin had to be written at this altitude rather than left to the sweep. (e) that literal kept and the ceiling moved to C -> **81/3** (hygiene 368/1, earnedbasis 50/4 alongside): a caller is handed a capture STAMPED B while the gate refuses any leg above C — the record overclaiming on the capture's own grade, one worse than the sentence about the grade. (f) the COMPOSED stamp under the same moved rule (REC-48's arm (c) exactly) -> **82/2**, and the difference between (e) and (f) is the item.
  THE ARCHIVE-SOURCED LETTER'S OPEN STATUS IS STATED EXPLICITLY IN THE SUITE, in two assertions rather than a comment, so it is machine-checked and cannot rot into silence: one asserts the archive arm IS still a typed letter and says in its own label that this is OPEN BY DECISION and not by oversight, because naming it is a ruling; the other pins the ORDERING that is ALREADY RULED ("grade tracks directness, never technique" — an archive hop is one more party between us and the publisher), that the typed letter still ranks strictly BELOW the enforced ceiling in `BASIS_GRADES`, the same array `checkEarnedLeg` compares against. Pinning the ordering asserts NO VALUE and so is not a ruling; what it buys is that arm (f)'s second failure NAMES the archive letter that did not follow the ceiling, and a record that would have quietly claimed an archive-sourced capture is worth as much as a direct one says so instead. The determinate half is closed and the doctrine half is left open and made to SPEAK.
  ONE PLACEMENT DECISION, MADE AND REPORTED RATHER THAN WORKED AROUND. REC-50's accepts-when says the NC is recorded in the suite's own `NEGATIVE CONTROL:` line; the concurrent worker M0-9 holds exactly that header block of `acquire.test.mjs`. Both cannot be honoured without a collision, so the arms are recorded IN THIS SUITE in a full block at the assertion site — with the counts each arm produced, so it is re-runnable in one step — and NOT ONE BYTE of the header block was touched. The block deliberately does not contain the token `NEGATIVE CONTROL:` with its colon, so `scripts/coverage.mjs` (and M0-9's widened detector) cannot mistake it for a second declaration. CONDUCT folds it into the header at integration, or M0-9 does when it restores that block's shape.
  PINS CORRECTED WITH DATED REASONS, NEVER EXEMPTED: the literal wire pin `t("acquire claims the ceiling letter and never the one above it", …, "B")` is KEPT and RE-JUSTIFIED at the site — its job changed under it. While the plane stamped "B" it and the pin below it said the same thing twice; now that the stamp is composed it is the ONLY assertion in the battery still witnessing the VALUE this plane answers a caller today, while the other witnesses the RELATION to the rule. It is SUPPOSED to fail on a doctrine move (it does, in arms (e) and (f)) so that a session corrects it deliberately with a reason instead of a field following the move silently.
  A FINDING IN PASSING, ROUTED AND NOT TOUCHED (store.mjs is outside this claim). The grade VOCABULARY is copied by value inside `src/store.mjs` four times while `BASIS_GRADES` is exported from `checks/bio-checks.mjs` — a module store.mjs already imports two symbols from: `#requiredStrengthFor`'s `const rank = (g) => ["A","B","C","D"].indexOf(g)` and its `["A","B","C","D"].includes(rq[axis])` (~4973, ~4988), a third at ~5027, and `static #GRADE_RANK = { A: 4, B: 3, C: 2, D: 1 }` (~7576). None is a doctrine SENTENCE and none spells a letter beside the word "Grade", so REC-48's sweep is silent on all of them by design; they are the same class one level down — the vocabulary itself rather than a statement about it — and the day `BASIS_GRADES` gains or loses a letter they disagree with the catalog silently. Worth an item; NOT a DEC, because no doctrine question is open in it.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker 9b28179). Verified from the main checkout: battery 98/98 (5453, the +5 attributed to acquire alone), coverage strict run directly with `$?` unpiped at 130/130 ops and 53/53 checks, zero leaked sandboxes, plancheck green. CONDUCT re-ran REC-48's arm (c) itself, because it is this item's evidence and not merely a control: with the ceiling moved to C, acquire is 82/2 and REC-48's leftover assertion is CLOSED, while the two failures that remain are the literal wire pin and the archive-letter ordering — both of which should fire on a doctrine move. `bio-checks.mjs` restored to the identical sha256. THE BEST WORK IS HOW THE OPEN DECISION IS HELD OPEN: two assertions rather than a comment, so it is machine-checked — one saying in its own label that the archive letter is open BY DECISION and not by oversight, the other pinning the already-ruled ORDERING against `BASIS_GRADES`. Pinning an ordering asserts no value, so it is not a ruling, and it buys a control arm that names the archive letter when it fails to follow. That is a pattern worth reusing wherever a determinate half must ship while a doctrine half waits. Arm (d) is the third measurement of the zero-cost copy on a third subject, and it also shows exactly why the pin had to sit at the assertion altitude rather than in REC-48's sweep: a bare letter in a ternary spells no word, so the sweep is correctly silent. The placement conflict handled the right way — zero bytes of the header block M0-9 holds were touched, the arms recorded in full at the assertion site, and the marker token deliberately omitted so no detector reads them as a second declaration. TO BE FOLDED INTO THE HEADER AT M0-9's INTEGRATION, recorded on REC-50's queue item so it cannot be forgotten. REC-51 routed for the four grade-vocabulary copies in store.mjs — a class REC-48's sweep is blind to by design, since it looks for statements ABOUT the vocabulary and these are copies OF it. No DEC.
## CLAIM 2026-08-04 M0-9

session: m0-9-agent (worktree agent-a5f37959e67a60979)
opened: 2026-08-04T21:10:00Z (APPENDED LATE, and said so rather than back-dated: the mechanism was re-measured and the first edits to `scripts/` were made before this entry went in. Nothing outside the paths below was ever opened, and UI-31's paths were never opened at all.)
paths: bio-plane/scripts/control-register.mjs (NEW — the negative-control register's detector, extracted so the battery can test the instrument instead of trusting it), bio-plane/scripts/coverage.mjs (ONLY the control-register lines: the header paragraph describing how a control is declared, the `NEG` regex replaced by an import of `readControl`, the fleet member's control probe, and the NEGATIVE CONTROLS report block. NOT the OPS reader, NOT the check reader, NOT `called`/`surfaceCalled`, NOT the strict exit condition — the floor is unchanged), bio-plane/test/acquire.test.mjs (ONLY its `NEGATIVE CONTROL:` declaration at the head — REC-48's three arms moved back INSIDE the declaration where REC-48 wanted them, with the reason at the site. No assertion added, removed or changed), bio-plane/test/hygiene.test.mjs (its `NEGATIVE CONTROL:` declaration restored the same way, plus ONE new block at the foot: the register detector's own self-test and reach assertions, and the import line it needs. No existing assertion touched), docs/development/VERIFICATION.md ("The negative-control register" section only — the first-60-lines rule it documents is no longer true), docs/development/CLAIMS.md (this entry)
interfaces owned: none. This item changes no op, no schema, no published shape. It changes an INSTRUMENT and one comment in two suites.
interfaces consumed: none.
concurrency: **UI-31 runs concurrently on `civicos-ui/**` and `docs/development/MEASUREMENTS.md`.** FULLY DISJOINT, stated both ways: this claim opens NO file under `civicos-ui/` and does NOT open `MEASUREMENTS.md` at all; UI-31 opens no file under `bio-plane/` and no file in `docs/development/` but MEASUREMENTS.md. The only shared surface is CLAIMS.md, appended to.
explicitly NOT touched: civicos-ui/** (every file), docs/development/MEASUREMENTS.md, QUEUE.md, DECISIONS.md, DEBT.md, MILESTONES.md, INTERFACES.md, ORCHESTRATION.md, every kickoff, bio-plane/src/** (every module), bio-plane/checks/**, bio-plane/scripts/battery.mjs, bio-plane/scripts/deploy.mjs, the other 96 battery suites, newgroup/**, pdf-worker/**, docprofile/**, tools/**, release/**
expected: QUEUE.md M0-9, routed out of REC-48. `scripts/coverage.mjs` could report that a suite declares NO negative control when it declares an elaborate one, and could record one arm of a control that states five. Find the declaration wherever it is written, capture the whole block, count and REPORT its arms, and assert the instrument's own reach. Restore REC-48's block in acquire.test.mjs and hygiene.test.mjs to the multi-line shape it wanted, so the fix is proved rather than the workaround.
accepts-when: as QUEUE.md M0-9. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, exit 0, declared controls unchanged at 98/98 with both blocks multi-line; a self-test asserting the detector finds a control declared past line 60 and captures every arm of a multi-arm block; `npm run test:battery` green at 98/98 with any change to 5448 ATTRIBUTED, not estimated. NEGATIVE CONTROLS run and recorded: hide a suite's declaration entirely and the strict run fails NAMING that suite; separately truncate a multi-arm block and the reported arms drop. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. QUEUE.md is not edited; DECISIONS.md is not written by this session.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5463 assertions, 88.8s**, zero leaked sandboxes (0/0 before and after). The +15 over REC-48's 5448 is FULLY ATTRIBUTED, not estimated: the per-suite lines of the before and after runs were diffed and **exactly one suite moved, hygiene.test.mjs 369 -> 384**, which is the register detector's self-test. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED: **exit 0**, 130/130 ops, 53/53 checks, **98/98 suites declaring a control at 251 arms stated**.
  THE MECHANISM, RE-MEASURED A THIRD TIME, AND THE BRIEF WAS WRONG ON THE PART THAT DECIDES THE FIX. The item says `\s*` crosses newlines so a wrapped control still matches, and that the 60-line window is what broke REC-48. Measured directly: `\s*` reaches a close that sits alone on the next line, and matches NOTHING when the next line carries TEXT — which is what a multi-arm block is. Reconstructing the exact shape REC-48 wanted gives **NO MATCH for both suites**, and both markers sit on line 1 where no window can reach them. So REC-48's original report named the right mechanism and the integration re-measurement named the wrong one. All three defects are closed anyway, because the window is a real (separate) defect: a marker past line 60 is invisible, and a declaration STRADDLING line 60 was recorded as whatever fragment fell inside while still counting as controlled.
  WHAT THE OLD REGISTER HAD BEEN RECORDING, since the item asked. No suite's entry was a mangled fragment. But **14 of 98 suites had a fuller declaration the register could not see**, and three were registered with fewer arms than they state: reading-wire 2 of 3, reuse-ratify 4 of 5, setup-honesty 3 of 5. `readingname.test.mjs` is the window's own victim — its 4-line, 2,233-character declaration begins on line 59 and was entirely outside the window, so the register quoted its 870-character one-liner instead. After REC-48's blocks were restored, acquire went from 2 arms recorded to 7 and hygiene from 4 to 12. Over the whole tree the old detector read 234 arms and the new one reads 251.
  THE TWO JUDGEMENTS, made and recorded at the site (scripts/control-register.mjs's header). WHERE A DECLARATION ENDS: at its comment's close or at the first blank comment line, whichever comes first — a declaration is a PARAGRAPH, because several suites state the control as one paragraph of a longer header and running to the close would quote back prose the suite never declared. WHICH ONE IS RECORDED when a suite states its control twice, which M0-2's backfill left most of them doing: the FULLEST single block, **never the sum**, because summing two statements of one control credits it twice — the generous direction wearing a different hat. ARMS ARE REPORTED AND NEVER GATED: an arm is one ` -> ` transition, the register grammar's own separator, and `--strict` still fails on exactly what it always failed on. A floor on arm counts would be strictness this instrument cannot justify, and the brief is right that over-strict is as bad as generous.
  NCs RUN x4, every file restored BYTE-IDENTICALLY with sha256 compared before and after (scripts/control-register.mjs caeac36b…, scripts/coverage.mjs a5df1b87…, test/acquire.test.mjs e065c8e0…, test/hygiene.test.mjs f66974ac…, test/capture.test.mjs e71bf8b8…). **(a)** hide a suite's declaration entirely (every marker in acquire.test.mjs) -> strict run **EXITS 1**, 97 of 98, NAMING acquire.test.mjs. **(b)** truncate a multi-arm block (hygiene's own, 8 lines to 5) -> that declaration reports 6 arms instead of 9 and the total 243 instead of 246, while strict STAYS exit 0 — the design, since arms are reported not gated, and the visible drop IS the mechanism. **(c)** put the 60-line window back into the detector -> hygiene 381/3, exactly the three window arms. **(d)** make the detector first-line-only -> hygiene 377/7, and the REAL-CORPUS arm bites with the fixtures; the register's total falls to **234, which is exactly what the old detector reported over this same tree**, so (d) reproduces the closed defect rather than resembling it.
  ONE PROPERTY FOUND WHILE RECORDING THE CONTROLS, and stated rather than smoothed: the arm TOTAL is a function of the declarations' own prose, so writing an NC record INTO a declaration moves the total. The totals above are as measured when each arm ran; only the DELTAS are comparable across edits, and the suite says so at the site. For the same reason every fixture in the self-test builds its marker from the instrument's exported constant instead of writing it literally — a literal would have put the test's own fixtures into the register.
  REACH ASSERTED 14 WAYS at the foot of hygiene.test.mjs, every one a DELTA or a read of the real corpus rather than an absolute a deaf detector could also satisfy (REC-48's reach assertion was wrong when first written for exactly that reason): found past line 60 AND not found with the marker hidden; a straddling block read whole; five arms counted from a marker line that states none, and the truncation measured as a delta between two blocks rather than against 1; the extent asserted in BOTH directions (reaches its continuation lines, does NOT swallow the paragraph after); the `//` form and its end; a control stated twice recorded once; the register read out of capture.test.mjs's REAL source and then not read once its marker is hidden; more arms than suites; and **at least one MULTI-LINE declaration present in the tree**, which is the assertion that makes REC-48's workaround un-reinstatable.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker e2e17eb). Verified from the main checkout: battery 98/98 (5468, the +15 attributed to hygiene alone by a per-suite diff), coverage strict run directly with `$?` unpiped at 130/130 ops, 53/53 checks, 98/98 controls and 251 arms, zero leaked sandboxes, plancheck green. **THIS WORKER MEASURED CONDUCT'S OWN RE-MEASUREMENT AND FOUND IT WRONG, and it was right to.** I had told it the mechanism was the 60-line window and not the newline; my synthetic put the comment close on the SAME line as the text, and a real multi-arm block closes on its own line, at which point the old expression matches nothing. Re-verified both shapes side by side at integration. REC-48's original report was correct and mine was not; corrected in QUEUE.md's M0-9 scope, in REC-48's release above, and to the operator. The window is a genuine second defect and `readingname.test.mjs` is its victim, which is why all three are closed. The three judgements accepted with their reasoning, and the second is the one worth carrying: recording the FULLEST SINGLE BLOCK and never the sum, because summing two statements of one control credits it twice — the generous direction wearing a different hat, spotted in a place nobody was looking for it. Arms reported rather than gated is also right: a floor nobody has ruled on would be strictness invented by an instrument. The 14 under-read declarations and the three suites registered with fewer arms than they state are the finding; the tree stated more than the register could see, and now states 251 arms against the old 234. Arm (d) reproducing exactly 234 is the proof it re-creates the closed defect rather than resembling it. CONDUCT DETERMINED the one open call: the self-test stays in `hygiene.test.mjs` rather than becoming a 99th suite, because a suite whose subject is the instrument that counts suites would move that instrument's own denominator. The claim-after-first-edit sequencing self-reported rather than back-dated — noted and accepted. No DEC.

## CLAIM 2026-08-04 UI

session: ui32-agent (worktree agent-a7b8c4fd133d48d65)
opened: 2026-08-04T23:40:00Z
paths: civicos-ui/app.html (THE U8 ADD SURFACE ONLY — the teach block under "The document, if there is one", `addValidate`'s one-line preview, the U8 header comment that restates the same doctrine, and the ONE dead CSS line `.az-honesty .az-cannot b` at :416), civicos-ui/test/add-surface.test.mjs (the item's ground: the two grade pins corrected, the no-letter assertion, and the tree-wide sweep with its REACH), civicos-ui/test/auth-surface.test.mjs (rider (a) ONLY — the `capture_acts` mock answering the wire CONTENT), docs/development/CLAIMS.md (this entry)
interfaces owned: none. UI CONSUMES I3.
interfaces consumed: I3 — `op=affordances`' `capture_acts` (REC-38's publication, REC-43's `prompt` on the `attest` entry) and the two capture-axis symbols `EARNED_CAPTURE_CEILING` / `UNREACHABLE_CAPTURE_GRADE`, both IMPORTED from the plane and never typed. `op=acquire` is READ as the act this surface performs; nothing about its shape is changed or requested.
concurrency: **REC-50 runs concurrently on `bio-plane/src/index.mjs` and the plane's capture-grade stamps; M0-9 runs concurrently on `bio-plane/scripts/coverage.mjs` and two plane suites' NEGATIVE CONTROL blocks. FULLY DISJOINT, and stated rather than assumed: this claim opens NO file under `bio-plane/` at all.** `bio-plane/src/affordances.mjs` and `bio-plane/checks/bio-checks.mjs` are IMPORTED as the authority for the doctrine and its two letters and are written to by nobody here; if REC-50 moves a stamped letter, this item's sweep reads the new one because it composes rather than copies.
explicitly NOT touched: bio-plane/** (every file), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, civicos-ui/worker.template.mjs, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs, civicos-ui/tokens.css, **civicos-ui/test/preauth-vocabulary.test.mjs (UI-31's, and its REPORT is a downstream consumer — UI-33 is queued against its current contents, so any term this item's reword moves is REPORTED as a finding rather than absorbed)**, civicos-ui/test/act-attest.test.mjs (UI-28's attest region — READ as the source of the subtraction instrument, written by nobody here), app.html's login gate (UI-30's) and its attest region (UI-28's) and `renderFiltered`'s successor (UI-27's), every other civicos-ui/test suite, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md UI-32, routed out of UI-28. The FOURTH hand-written statement of the capture-grade doctrine, at a site whose act is NOT attest, plus two stale copies in its neighbourhood. `ATTEST_FENCE` is NOT pasted here — REC-48's rule, because a different act speaking to a different reader is not the same sentence.
accepts-when: as QUEUE.md UI-32. `node civicos-ui/test/run.mjs` green, exit status read UNPIPED, with the U8 teach block spelling no grade letter and its suite asserting so; the source-level sweep EXTENDED to all of `civicos-ui/` with its OWN REACH asserted (a walk that covers nothing passes everything — five times in this project now); both riders closed; both guards green. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — move `EARNED_CAPTURE_CEILING` in the plane alone and the harness stays green with the surface reading the new letters, and separately restore a spelled letter and the harness fails NAMING the file and the string. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `node civicos-ui/test/run.mjs` **33/33 suites green, exit 0 read unpiped**, with add-surface 121 -> 144 and auth-surface 71 -> 73 and every other suite UNCHANGED. Both guards green: check-semantics; check-mock-envelope with **arm A over 13,443 lines of app.html's script and the SEAM SET UNCHANGED** (`actAsk, api, apiQ, apiR, intentAsk, rec, recPost, recPostR, recR` — the +60 lines are comment and one template constant; this item adds no transport call in code or in a comment), arm B **791 op answers across 66 ops, 143 op/suite pairs**, identical to UI-31's landing. `cd bio-plane && npm run test:battery` **98/98 (5448), 92.0s, 0 leaked sandboxes** — the SAME 5448 REC-48 landed, which is the evidence no plane file moved rather than a claim that none did. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read with nothing piped after it: **exit 0**, 130/130 ops, 53/53 checks, 98/98 controls.
  THE TREATMENT CHOSEN, AND IT IS THE SECOND ONE. The teach block says what THIS site's reader needs and spells no grade letter; the reasoning is on `ADD_CAPTURE_TEACH` in app.html and is not summarised into a shorter thing here. `ATTEST_FENCE` is NOT pasted in — REC-48's rule, a different act speaking to a different reader is not the same sentence, and this site's act is not attest. **op=acquire's `note` IS NOT RENDERED EITHER, and that is the judgement rather than the default.** It is a RECEIPT over bytes already received and this block paints before the address is typed, so there is nothing published for this instant; nothing else on `capture_acts` carries the doctrine (`attest` carries the fence, `monitor` carries no prompt). And putting the receipt's note in front of a MEMBER rather than the caller it was written for is the question REC-48 recorded at `acquireGradeNote` as a RULING — so it is raised for CONDUCT, not taken here.
  A SHARPER READING THAN THE ITEM ASKED FOR, and it is why the same treatment reached a second site: a letter at this site was never only a copy of doctrine, it was a **PREDICTION**. The capture does not exist while that paragraph is on the screen. `addValidate`'s live line ("It will be captured at <state>, Grade B, and appear in the record for review") made the prediction explicit, to a member one control away from the button, and it lost the letter for that reason. The STATE stays: `FIRST_STATE` is the catalog's and this form decides it, so the surface knows that answer and does not know the other one.
  THE SWEEP, AND ITS REACH. It walks **all 43 files under `civicos-ui/` (7 outside `test/`), 1,973,967 characters**, strips comments in the three forms this tree writes (HTML, JS block, JS line ANCHORED to line start — REC-48's lesson: an unanchored strip eats everything after `https:` in a string literal and the sweep silently reaches less than it claims), subtracts the two publications and reads the REMAINDER, because the publication NAMES the unreachable grade in order to deny it (UI-28's measurement, reused not reinvented). TWO DETECTORS. **(A) the spelled letter**, `Grade <L>` for the two letters taken from the exports, over every file OUTSIDE `test/` — a total rule there, and it can afford to be, because every legitimate grade in this application is INTERPOLATED from a plane value at eleven sites, so a spelled letter in the surface is by construction the surface stating doctrine. **(B) the doctrine's own prose**, over the WHOLE tree including the suites — 4-word phrases derived from the publications' GRADE-BEARING CLAUSES ONLY (5 clauses -> 23 phrases). The narrowing is measured, not aesthetic: taking the whole fence fired 26 times on correct prose in the attest region and the glossary, because the fence's first two parts are about timestamps and secondhand reports, and a detector that noisy gets weakened until it finds nothing. SEVEN REACH ASSERTIONS plus five instrument guards, every one measured before an absence is believed: the file list is non-trivial and NAMES the surface, this suite, act-attest and both guards; bytes are counted, not just names; the stripper is proved to leave the surface's live prose intact at the exact site this item corrected (a stripper returning "" makes every absence trivially true — UI-30's instrument lesson); the same detector over the RAW sources DOES match in 11 files including app.html, so a walk finding nothing raw would be a walk that never opened them; and A PLANTED CONTROL FIRES BOTH DETECTORS IN EVERY FILE'S OWN TEXT, compared as a **DELTA** because REC-48 shipped the absolute-count version of exactly this assertion before catching it. **The planted control is the publication's OWN grade-bearing clause, lifted rather than typed** — a control typed out would BE a fifth statement, sitting in the file whose job is to forbid them, and detector (B) would find it; the first version of this sweep failed on itself for that reason and was corrected rather than weakened.
  A STATED LIMIT RATHER THAN AN EXEMPTION LIST, and it is REC-48's, re-measured one tree over. Detector (A) is deliberately NOT run over `test/`: **eight suites** (subject-view, document-structure, intent-write, inquiry-page, finder, cite-act, elicitation, project-workspace) pin `Grade <L>` on plane-supplied values from the §8.1 RESOLUTION axis and the CONNECTION axis — different doctrines that happen to share the capture axis's two letters. They are OUT OF THIS SWEEP'S SUBJECT and are not exempted from it. What guards a suite instead is (B), and the argument is that a zero-cost pin — the defect this line of items keeps correcting, and the one that stood in this very file — restates its subject's own words BY DEFINITION, which is what (B) reads.
  NCs RUN x6, each arm mutating ONE file, restored BYTE-IDENTICALLY with sha256 compared before and after, all re-run against the FINAL files: app.html 8e6db821…, add-surface.test.mjs 7562c19e…, auth-surface.test.mjs c23e89dd…, bio-checks.mjs d8da7b9d…, README.md e4e86f61… . **(g) THE ITEM'S OWN AND THE ONE THAT MATTERS — move `EARNED_CAPTURE_CEILING` B -> C in the plane ALONE, no UI edit -> the WHOLE HARNESS STAYS GREEN, 33/33 exit 0**, with act-attest 83/83 and add-surface 144/144, while the publication recomposes to "it strengthens a Grade C capture … It never reaches Grade B" and act-attest asserts the dialog carries that whole. **The sweep moves WITH the rule rather than against it**: its raw-hit count goes 11 files -> 13, because `Grade C` in the surface's §8.1 comments becomes a capture letter the moment the rule says so — the difference between a detector that is a FUNCTION of the doctrine and one that is a copy. (h) restore the spelled letter to the teach block -> FAIL, the rendered pin; this suite is fail-fast so the sweep's verdict is unreachable in that run rather than green, which is why (h2) exists. (h2) the same defect at a rendered site no form assertion reads (the glossary's RFC 3161 entry) -> FAIL "NO CAPTURE-GRADE LETTER IS SPELLED ANYWHERE OUTSIDE test/ — app.html spells Grade A (1x)", naming the file and the string. **(i) THE ARM THAT EARNS DETECTOR (B) ITS EXISTENCE, REC-48's arm (e) one tree over — put the doctrine back with NO `Grade` word at all ("and co-attestation raises B toward evidentiary weight") -> FAIL naming app.html and three phrases, with (A) SILENT and every rendered-letter assertion still green.** (j) neuter the walk -> FAIL "REACH: the walk reads the whole package — 0 files". (l) a fifth statement in a file no harness runs, appended to README.md -> FAIL naming README.md and the string — both of UI-30's stale copies were sitting in files no harness ran, which is the whole reason the walk covers the package rather than the surface.
  RIDER (a), AND THE MEASUREMENT IS THE 71 THAT STAYED GREEN. `auth-surface.test.mjs`'s `capture_acts` fixture hand-typed the label and answered `prompt:null` for `attest` — a shape the plane cannot send for that act, since REC-43 put the fence there as `prompt` under DEC-39. NC (e): restore it -> **2 of 73 FAIL and 71 do not**, because this suite renders no attest surface. That is D-173's family stated exactly and it is UI-30's own finding in this file one field further in: a mock must answer the wire CONTENT, not merely the wire SHAPE. Fixed by reading id/label/prompt off `CAPTURE_ACTS`; the four decorated fields stay literal, because those are index.mjs's tables and that is what a fixture is for.
  RIDER (b): `.az-honesty .az-cannot b` DELETED rather than re-pointed. It coloured the label of a "Cannot:" paragraph the surface used to WRITE and UI-28 stopped writing; re-aiming it at `.az-part b` would be a styling decision inside UI-28's region rather than the removal of dead source, so it is not made. Zero occurrences of `az-cannot` remain outside the comment recording the removal.
  UI-31'S REPORT DID NOT MOVE, checked rather than assumed: 12 surfaces, 10 scenarios, 33,412 characters, 74 terms, **13 terms on 5 surfaces, 67 occurrences, 56 visible** — identical in every figure and every term, because the Add surface is post-authentication and nothing this item reworded stands before the gate. **UI-33's queued basis is unchanged.**
  PINS CORRECTED WITH DATED REASONS, NEVER EXEMPTED: add-surface's two grade pins at the site (one asserted the form is "stated as B", the other matched a doctrine phrase this file had typed out — both green at zero cost, and neither could have noticed that the subject was PREDICTING a grade), its file-header load-bearing list (a fifth entry), and its NEGATIVE CONTROL line rewritten with the five new arms as run; auth-surface's mock and its NEGATIVE CONTROL line. Six assertions replaced four; no assertion was removed anywhere.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker b39ab4d). Verified from the main checkout: UI harness 33/33 exit 0 read unpiped, both guards green with the seam set unchanged, battery 98/98, coverage strict run directly, plancheck green. THE PREDICTION READING IS THE ITEM'S REAL WORK and it is better than the brief: a grade letter on the add surface was never only a copy of doctrine, it was a PREDICTION about what the capture would be worth, made one control from the button. That reading is what carried the treatment to a second site the item never named. Two instrument findings accepted. The narrowing of detector (B) to grade-bearing CLAUSES is measured rather than argued — the whole fence fired 26 times on correct prose. And the control being the publication's own clause LIFTED rather than typed, after the typed version made detector (B) fail on the sweep itself, is the instrument catching its own author; corrected rather than weakened. Arm (g) is the same proof UI-28 gave and now covers the whole harness: the ceiling moves in the plane alone and every surface follows with no UI edit. UI-31's report was CHECKED rather than assumed and did not move, so UI-33's queued basis stands — that check was not asked for and is exactly the coordination this ecosystem runs on. Both riders closed, and the dead selector deleted rather than re-pointed because re-aiming it is a styling decision inside another item's region. DEC-51 raised on REC-48's own written instruction that this would be a ruling and not a quiet edit — the note reaching a member at the moment of capture. That instruction was left in the source by one worker and honoured by another two items later, which is the mechanism working as designed. No other DEC.

## CLAIM 2026-08-04 RECORD

session: rec51-agent (worktree agent-a80a02bf4fc097c6a)
opened: 2026-08-04T22:10:00Z
paths: bio-plane/src/store.mjs (ONLY: (1) the `../checks/bio-checks.mjs` import list gains `BASIS_GRADES` beside `BASIS_ROLES`, with REC-51's reasoning at the site; (2) `#requiredStrengthFor`'s `const rank = (g) => ["A","B","C","D"].indexOf(g)` near line 4973 and its `["A","B","C","D"].includes(rq[axis])` near line 4988; (3) `strengthBarSet`'s `["A","B","C","D"].includes(v)` near line 5027 AND the `BAD_GRADE` detail sentence on the line below it, which spells the same vocabulary in prose and is the FIFTH copy this item's scope had counted as four; (4) `static #GRADE_RANK = { A: 4, B: 3, C: 2, D: 1 }` near line 7576, which becomes a DERIVATION over `BASIS_GRADES`' own order. **The `["A","B","C"]` subset near line 8154 is NOT touched, NOT collapsed and NOT given a constant** — it is the machine-mintable grades, a deliberate subset carrying its own doctrine, and it is held as a STATED LIMIT with its own assertions. NOTHING else in this file: not `promote`, not the gate, not the audit sweep, not `purge`, not `publish`, and none of CAPTURE's link/capture/task/reachability functions), bio-plane/test/hygiene.test.mjs (the BODY only — REC-48's tree-wide sweep block GROWS a THIRD detector (C) for copies OF the vocabulary, with its OWN REACH asserted, plus the two assertions holding the machine-mintable subset open and the structural pin that the rank map is derived. **NOT ONE BYTE of the `NEGATIVE CONTROL:` header block, which M0-9 holds** — this item's arms are recorded in full at the assertion site instead, deliberately without the marker token, per REC-50's precedent), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I3: NO SHAPE CHANGE and NO VALUE CHANGE TODAY.** Every rewritten expression evaluates to exactly what it evaluated to before while `BASIS_GRADES` is `['A','B','C','D']`: the membership tests admit the same four letters, `rank()` returns the same indices, `#GRADE_RANK` derives to the same `{A:4,B:3,C:2,D:1}`, and `op=strengthbar`'s `BAD_GRADE` detail composes to the same characters. Nothing on the wire moves today; all five become functions of the catalog from now on. I5 is NOT touched: no table, column or index.
interfaces consumed: none changed. `BASIS_GRADES` is consumed from `checks/bio-checks.mjs` exactly as it has been exported since REC-11 and as `checkEarnedLeg`, `checkInquiryBasis` and `UNREACHABLE_CAPTURE_GRADE` already read it. **No constant is added anywhere** — that is the point of the item rather than an omission, and minting one for the machine-mintable subset would be a ruling.
concurrency: **TWO WORKERS RUN CONCURRENTLY AND BOTH ARE DISJOINT FROM THIS ONE, stated both ways.** M0-9 holds `bio-plane/scripts/coverage.mjs` and the `NEGATIVE CONTROL:` HEADER BLOCKS of `test/acquire.test.mjs` and `test/hygiene.test.mjs`. This claim opens `scripts/coverage.mjs` not at all and `test/acquire.test.mjs` not at all; it does open `test/hygiene.test.mjs`, and the split is by REGION — **M0-9 holds that file's `NEGATIVE CONTROL:` header block (its first two block comments, lines 1-6), this claim holds its BODY and only the REC-48 sweep block inside it.** Unlike REC-50 I do not append to the header at all: my arms are recorded at the assertion site, without the marker token, for CONDUCT to fold in at M0-9's integration. UI-32 holds `civicos-ui/**`; this claim opens no file under `civicos-ui/` at all.
explicitly NOT touched: bio-plane/checks/bio-checks.mjs (READ as the authority for the vocabulary and written to by nobody here; it is MUTATED and RESTORED byte-identically for the negative control only), bio-plane/src/index.mjs, bio-plane/src/affordances.mjs, bio-plane/src/schema.mjs, bio-plane/src/query.mjs, bio-plane/src/cdx.mjs, bio-plane/src/gate.mjs, every other bio-plane/src module, bio-plane/scripts/** (including coverage.mjs and battery.mjs), bio-plane/test/acquire.test.mjs, bio-plane/test/earnedbasis.test.mjs, bio-plane/test/publish.test.mjs, every other battery suite, bio-plane/package.json, civicos-ui/** (every file), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md REC-51, routed out of REC-50. The grade VOCABULARY is copied by value inside `store.mjs` — one level BELOW the doctrine SENTENCES that REC-43, REC-48 and REC-50 composed. `BASIS_GRADES` is exported from a module `store.mjs` already imports, so every copy is avoidable: import the array, and DERIVE the rank from its order rather than restating it, so the two cannot disagree at all. **REC-48's sweep is blind to this class BY DESIGN and that is not a defect in it** — its two detectors look for a letter beside the word "Grade", i.e. statements ABOUT the vocabulary, and these are copies OF the vocabulary, which spell no word. This needs a THIRD detector, not a tweak to either existing one, and the reason is written at the site so the next reader does not "simplify" three detectors into one that catches less. **This is not a doctrine question and no DEC is open in it**: the values are already ruled and already enforced, and what is wrong is that they are stated in five places.
accepts-when: as QUEUE.md REC-51. `cd bio-plane && npm run test:battery` green with `store.mjs` holding NO literal grade vocabulary and the rank DERIVED from the catalog's own order — 98/98 and 5453 assertions on main today, and any change to that number ATTRIBUTED per suite rather than estimated; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, exit 0; a source-level assertion that no grade-vocabulary literal survives anywhere in `src/`, **with its own REACH asserted** — this project has hit the covered-on-paper failure five times (UI-30, REC-49, UI-28, REC-48's own first draft, UI-31). NEGATIVE CONTROL run and recorded: add a letter to `BASIS_GRADES` in the catalog alone and the suite fails NAMING every place that did not follow, which today is four and after this item is none. A copy that agrees today agrees at zero cost — measured three times already (REC-43, REC-48, REC-50) — so the pin that bites is STRUCTURAL and never a value comparison. Every file restored byte-identical after each arm, sha256 compared before and after. **If any copy turns out NOT to be the same vocabulary (a different axis, a different order, a deliberate subset), that is a real finding: it is reported and left alone with its difference STATED, the way REC-48 recorded a STATED LIMIT on the connection axis rather than an exemption.** No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5462 assertions, 95.4s**, zero leaked sandboxes (0/0 before and after, host orphans 0 -> 0). The **+9 over 5453 is ATTRIBUTED and not estimated: hygiene.test.mjs 369 -> 378 is the whole of it**, measured standalone at HEAD (369) and final (378) with the suite swapped in place, and NO OTHER SUITE MOVED — which is exactly what the item predicts, since every rewritten expression evaluates to what it evaluated to before. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED: **exit 0**, 130/130 ops, 53/53 checks, **98/98 suites declaring a control** — my arms block is deliberately written without the marker token, and that count confirms no detector read it as a second declaration.
  NOTHING ON THE WIRE MOVES TODAY, verified by evaluation rather than by eye: the composed `BAD_GRADE` detail is character-identical to the literal it replaces (`capture must be one of A, B, C, D, or null`), and the derived rank map is `{"A":4,"B":3,"C":2,"D":1}` — deep-equal to the object it replaces. Five statements of the vocabulary become one; all five are functions of the catalog from now on.
  A FIFTH COPY THIS ITEM'S SCOPE HAD COUNTED AS FOUR, found by sweeping for the CLASS rather than trusting the count — REC-48's own lesson, reproduced. `strengthBarSet`'s `BAD_GRADE` refusal SPELLED the vocabulary in prose (`must be one of A, B, C, D, or null`) on the line directly below the membership test that was already being fixed, and it is MEMBER-FACING. Its exact twin in `checkInquiryBasis` (C-2.8) has composed from `BASIS_GRADES.join(', ')` since REC-11, so composing this one asserts no new doctrine and closes the last place a member could be refused against a vocabulary the refusal itself misstates.
  **A SIXTH LITERAL THAT IS NOT THE SAME VOCABULARY, AND IT IS LEFT ALONE WITH ITS DIFFERENCE STATED.** `["A","B","C"]` in the earned-connection walk is a strict SUBSET carrying its own doctrine: the grades a MACHINE may mint, grade D being a member's testimony the recogniser never produces. `checkEarnedLeg` types that 'D' at the enforcement point itself, there is no exported constant to compose from, and minting one would assert what a machine earns and whether that set follows the catalog when the catalog moves — a ruling, and no DEC is open in it. Deriving it as "all but the weakest" would have ANSWERED that question silently, which is the one thing this item must not do. So it is held the way REC-50 held op=acquire's archive letter: OPEN BY DECISION, NOT BY OVERSIGHT, in two assertions rather than a comment. One pins (C)'s found set to EXACTLY that one site, named and reasoned, so a seventh literal fails the moment it is written AND the limit cannot rot away either (arm (d)). The other reads the subset OUT OF the source and pins that it stays a contiguous STRONGEST-FIRST PREFIX of `BASIS_GRADES`. Pinning a relation asserts no VALUE, so it is not a ruling; a catalog that REORDERS or RENAMES fails by name, a catalog that APPENDS leaves it green — the honest answer to an undetermined question.
  THE THIRD DETECTOR, AND WHY IT IS A THIRD. (A) and (B) match a LETTER BESIDE THE WORD — statements ABOUT the vocabulary, addressed to a reader. These are copies OF it and spell no word, so both are silent on all four BY DESIGN and that is not a defect in REC-48's sweep. Widening either cannot work: their whole precision comes from the word they are anchored to, and without the anchor "Grade" matches every capital in the tree. (C) has its own subject — an array of >= 3 quoted single capitals, or an object keyed by >= 3 of them. **(C) IS DELIBERATELY BLIND TO THE CATALOG'S CURRENT VALUE**, because the negative control MOVES the catalog and a value-comparing detector would go deaf at exactly that moment; arm (b) demonstrates it naming all four copies WITH the catalog already moved. The reason all three must coexist is written at the site so the next reader does not simplify three detectors into one that catches less.
  REACH ASSERTED FIVE WAYS, this being the project's sixth encounter with covered-on-paper (UI-30, REC-49, UI-28, REC-48, UI-31): C1 fires the detector in EVERY file's own stripped text as a DELTA against that file's own count — never against 1, which is REC-48's own corrected defect and would have recurred here verbatim, since store.mjs already carries a hit; C2 covers BOTH removed shapes, so a detector that found only arrays could not pass; C3 pins value-independence across a four-letter, a five-letter and a disjoint-letter copy; C4 pins NARROWNESS — a pair, a word vocabulary and an ordinary options object must all read zero, or (C)'s silence would mean nothing; **and C0 is a reach gap I found in the SHARED walk and closed for all three detectors**: `readdirSync` does not descend, `src/` is flat today (23 files, and `find` agrees), and nothing said so — a future `src/something/` would have left the sweep passing while guarding a smaller tree, the covered-on-paper failure arriving through the FILE LIST rather than through a detector.
  NC RUN x5, every file restored BYTE-IDENTICALLY with sha256 compared before and after (src/store.mjs 6bd3c5fc…, checks/bio-checks.mjs d8da7b9d… — the same catalog digest REC-48 and REC-50 each recorded), and ALL ARMS RE-RUN AGAINST THE FINAL FILE so the counts name the file that carries them. (a) THE ITEM'S OWN and the FOURTH measurement of the zero-cost copy on a fourth subject — the rank map hand-typed back as an identical literal -> hygiene 376/2 naming `store.mjs:4776`, WHILE resolution 39/0, connection 41/0, strength 42/0, publish 77/0, search 164/0 and earnedbasis 54/0 ALL STAY GREEN. (b) THE ACCEPTS-WHEN'S OWN, against the PRE-FIX tree with 'E' added to the catalog ALONE -> hygiene 376/2 and (C) NAMES ALL FOUR that did not follow, by file and line: store.mjs:3185, :3200, :3236 and :4775. **AND THE REST OF THAT ARM IS THE POINT OF THE WHOLE ITEM: with the catalog moved and all four copies stale, publish 77/0, resolution 39/0, strength 42/0, basis 29/0 and earnedbasis 54/0 ARE ALL STILL GREEN — the drift is completely silent behaviourally, and nothing in the battery but this sweep can see it.** (c) the same letter against the FIXED tree -> hygiene 378/0, the rank deriving itself to `{A:5,B:4,C:3,D:2,E:1}` and the refusal composing itself to "…A, B, C, D, E, or null" with nothing edited; the difference between (b) and (c) is the item. (d) the stated limit deleted -> hygiene 376/2, both holding assertions firing, so the limit cannot rot away unnoticed. (e) C0's own — `mkdir src/subdir-probe` -> hygiene 377/1 naming the directory while every other assertion stays green, which is precisely the danger; `rmdir` restored it and `git status src/` confirmed nothing stray.
  ONE INSTRUMENT COMMENT CORRECTED MID-BUILD, reported rather than smoothed: REACH C2's comment claimed it read the removed shapes out of the raw source when the code in fact uses hand-written specimens. A comment that overclaims what an instrument does is the same defect one altitude above the one this item closes, so it now says exactly what the code does and points at arm (b) for the empirical proof.
  PLACEMENT REPORTED RATHER THAN WORKED AROUND, as REC-50 did: the accepts-when wants the arms in the suite's `NEGATIVE CONTROL:` header and M0-9 holds exactly that block. **Zero bytes of it were touched — the first six lines hash identically to HEAD (45357b96…)** — and the arms are recorded in full at the assertion site with the marker token deliberately omitted, so neither `coverage.mjs`'s detector nor M0-9's widened replacement reads them as a second declaration. TO BE FOLDED INTO THE HEADER AT M0-9's INTEGRATION.
  NO DECISION ITEM FOR BOB. The values were already ruled and already enforced; what was wrong is that they were stated in five places. The one genuinely open question the item touched — what a machine may mint — was left open and made to SPEAK rather than answered in passing.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker c2758e8). Verified from the main checkout: battery 98/98 (5478), coverage strict run directly at 130/130 ops, 53/53 checks, 98/98 controls and 251 arms, zero leaked sandboxes, plancheck green. A REAL CODE CONFLICT resolved at the merge — M0-9 and this item both landed a load-bearing import in `hygiene.test.mjs` within the hour; combined with a dated note and verified at 393 = 384 + 9, so neither lost an assertion to the seam. CONDUCT re-ran arm (c) itself: a fifth letter added to the catalog ALONE leaves hygiene 393/0 and all five behavioural suites green, with the catalog observed as five letters at runtime — the tree follows the catalog wherever it goes now. `bio-checks.mjs` restored to the identical sha256. THE SIXTH LITERAL IS THE ITEM'S BEST JUDGEMENT: `["A","B","C"]` is not this vocabulary but a strict subset carrying its own doctrine — what a MACHINE may mint — and deriving it as 'all but the weakest' would have answered a live ruling silently. Held open in two relation assertions exactly as REC-50 held the archive letter, which is now a pattern this project uses deliberately rather than an improvisation. The fifth copy found in member-facing PROSE, and detector (C) being blind to the catalog's value so the control cannot deafen it, are both the right instincts. Arm (b) is the measurement that justifies the whole item: on the pre-fix tree a moved catalog leaves five behavioural suites entirely green while four sites disagree with it — completely silent drift. The C0 reach gap (readdirSync does not descend and nothing said so) was found in the SHARED walk and closed for all three detectors, which is worth more than this item's own subject. The instrument comment that overclaimed what its code does, caught and corrected mid-build, is the same defect one altitude up and was reported rather than smoothed. No DEC — the one genuinely open question was left open and made to speak.

## CLAIM 2026-08-04 UI

session: ui33-agent (worktree agent-a4d3d8ed17c99983c)
opened: 2026-08-04T23:05:00Z
paths: civicos-ui/app.html (ONLY the SURFACE-AUTHORED wording at the sites UI-31 measured as INCIDENTAL: (1) the gate's token panel label and placeholder near line 788, which printed the SCREAMING_SNAKE identifier `MEMBER_TOKEN` to a member as a FIELD LABEL; (2) `teach()`'s unreachable-plane fallback sentence near line 1094, which ended "(CORS)"; (3) `previewNote()` near line 1118, which said the record loads "from R2"; (4) `pubCaseHashLine()`'s gloss near line 13524 and the case-container block near lines 14116-14119, which used `manifest` in prose four times; (5) the one SURFACE-AUTHORED "this instance", in that same container paragraph. **NO PLANE-SOURCED STRING IS TOUCHED ANYWHERE** — not `Store.LOGIN_REFUSAL_DETAIL` as rendered at the gate, not `op=publishedcase`'s `detail` or `verification.detail`, not `parts[].path`), civicos-ui/test/preauth-vocabulary.test.mjs (UI-31's instrument: the structural-rule LIVENESS arm is corrected to fire on a PLANTED control instead of on the shipped defect, one REACH assertion is ADDED pinning the plane's case-page sentences verbatim, and the file header + `NEGATIVE CONTROL:` line are rewritten with the arms as run. **The measurement itself — the walks, the term harvest, the attribution, the report and the `REPORT_ONLY` arm — is NOT changed**), docs/development/MEASUREMENTS.md (APPEND-ONLY: one new dated entry for the re-reading, plus a five-line SUPERSEDED-IN-PART note at the foot of UI-31's entry. UI-31's table and reasoning are left standing — a measurement is not rewritten, it is superseded and the reader is told where), docs/development/CLAIMS.md (this entry)
interfaces owned: none. **behind-interface: none** — this item changes no op, no field, no shape and no wire string. It changes what THIS SURFACE prints.
interfaces consumed: I3 (the op contracts), read and rendered exactly as before.
concurrency: **REC-46 runs concurrently on `bio-plane/**` and is DISJOINT from this claim, stated both ways.** This claim opens NO file under `bio-plane/` at all — `bio-plane/src/store.mjs` is READ by the instrument (textually, for the refusal sentence) and is written to by nobody here. REC-46 opens no file under `civicos-ui/`.
explicitly NOT touched: every plane-sourced sentence rendered on any pre-authentication surface (DEC-49 is OPEN with Bob and touching one pre-empts it — the error UI-31 deliberately refused to make); UI-32's grade-letter sweep in `civicos-ui/test/add-surface.test.mjs` and the 43-file walk it installed; UI-31's MEASUREMENT code in `preauth-vocabulary.test.mjs` (walks 1-3, `planeRanges`, `countIn`, `HITS`, the report, `REPORT_ONLY`); UI-30's login gate logic in `signIn()`; UI-28's attest region; UI-27's one-function filter; every other suite under `civicos-ui/test/`; `bio-plane/**`; `newgroup/**`; `docprofile/**`; `tools/**`; QUEUE.md, DECISIONS.md, DEBT.md, MILESTONES.md, INTERFACES.md, VERIFICATION.md. **DEC-49's own entry in DECISIONS.md is NOT edited by this session, and it now quotes a headline this item superseded** — see the report; folding that in is CONDUCT's, not a worker's. UI-31's MEASUREMENTS.md table is likewise left standing rather than rewritten; it is superseded in place with a pointer.
expected: QUEUE.md UI-33, routed out of UI-31's measurement. The half of the pre-authentication vocabulary that NO answer to DEC-49 will fix. DEC-49 decides who owns the wording the PLANE sends — (a) rewords the plane, (b) licenses surfaces to translate what the plane sent — and **a surface's OWN vocabulary is outside both**, so this item does not wait on it and must not anticipate it.
accepts-when: as QUEUE.md UI-33. `node civicos-ui/test/run.mjs` green with the exit status read UNPIPED; **UI-31's report showing the surface-authored rows GONE and the plane-sourced rows UNCHANGED in number and in source** — the second half is the real assertion, because a reword that quietly touched a plane string would otherwise look like success; `UI31_ENFORCE=1 node civicos-ui/test/preauth-vocabulary.test.mjs` run before and after with both term lists reported, the difference being exactly the surface-authored set closed, and **any term deliberately KEPT named with its reason** so a later reader does not think it was missed; both guards green (`check-semantics`, `check-mock-envelope`). NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — reword a PLANE-SOURCED term and the harness fails NAMING it as the surface overstepping DEC-8. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `node civicos-ui/test/run.mjs` **33/33 suites green, exit 0 read UNPIPED**, with `preauth-vocabulary` 31 -> 32 assertions and EVERY OTHER SUITE UNCHANGED. Both guards green: check-semantics (the docprofile copy and the catalog block both OK); check-mock-envelope with **arm A over 13,557 lines of app.html's script and the SEAM SET UNCHANGED** (`actAsk, api, apiQ, apiR, intentAsk, rec, recPost, recPostR, recR` — this item adds no transport call in code or in a comment), arm B **791 op answers across 66 ops, 143 op/suite pairs**, identical to UI-32's landing. `bio-plane/**` NOT TOUCHED and no battery run needed. Reported rather than silently fixed: `bio-plane/node_modules` was EMPTY in this worktree and `intent-write.test.mjs` failed on a missing `miniflare` BEFORE a single edit was made — an environment state, closed with `npm ci` in `bio-plane/`.
  UI-31'S REPORT, BEFORE AND AFTER, AND THE SECOND HALF IS THE REAL ASSERTION. Before: **13 terms, 5 surfaces, 67 occurrences, 56 visible.** After: **9 terms, 4 surfaces, 55 occurrences, 45 visible.** FOUR ROWS GONE ENTIRELY — `manifest` (x7), `MEMBER_TOKEN` (x2), `CORS` (x1), `R2` (x1) — and `this instance` demoted from BOTH x4 to **UNAVOIDABLE x3**, its surface half closed and its two plane occurrences untouched. **ALL EIGHT PLANE-SOURCED ROWS ARE UNCHANGED IN NUMBER AND IN SOURCE**, checked row by row rather than by the total: `sha256` plane at case-address-at-load #pub-body, `op=` the same, `bundle.md`, `this instance` at refused-signin #g-err AND case-address-at-load #pub-body, `a salted derivation`, `its stored hash`, `no active credential`, `register`. DEC-49's subject is the same eight it was.
  THE INSTRUMENT WAS PROVED MEASUREMENT-NEUTRAL BEFORE app.html MOVED. The changed suite was run against the UNCHANGED `app.html` and the whole `PRE-AUTH VOCABULARY REPORT:` block came back CHARACTER-IDENTICAL to UI-31's, DIFFED rather than eyeballed. Only then was a word of the surface changed.
  THE STRUCTURAL-LIVENESS ARM WAS CORRECTED, NOT EXEMPTED, and it is the one place this item HAD to touch the instrument. As UI-31 wrote it, that arm required the SCREAMING_SNAKE rule and the acronym rule each to have matched something ON THE REAL SURFACES — which was true only because `MEMBER_TOKEN`, `CORS` and `R2` were still standing there. **It was fed by the very defect it was watching for**, so closing the defect would have turned it RED for the subject being FIXED, and its only remedy would have been to keep a defect alive to feed an instrument. It now fires on a PLANTED control through `structuralTerms()` — the same function the measurement calls, never a re-typed copy (REC-48's lesson) — and pins THREE properties where it used to pin two: SCREAMING_SNAKE fires, the acronym rule fires, and the discriminator still suppresses an emphasis word, which was never asserted at all before.
  THREE TERMS KEPT ON PURPOSE, ARGUED AT THE SITE (`pubVerifyPanel` in app.html) SO A LATER READER DOES NOT READ THEM AS MISSES. `sha256` names the ALGORITHM the reader is invited to run — "Check this without us" is only true if they know WHICH digest to compute, and it is the exact prefix the record, the container and `ssh-keygen` all write; a friendlier noun would cost the product claim to buy a softer word. `op=publishedbytes&sha256=…` is an ADDRESS, not a description, and a reworded address is a BROKEN one. `handle` is the closest call: it is the name of that identifier EVERYWHERE in the product, so rewording it at the gate alone hands a member one word before signing in and a different one after — renaming it everywhere spans three other suites' ground and is ROUTED rather than taken in passing.
  A SECOND SITE TREATED THAT THE MEASUREMENT COULD NOT SEE, and its reach gap reported rather than papered over. `pubVerifyPanel()` — the published rail's "Verify" pane — carries the same two words (`manifest` in prose, "without this instance continuing to exist") to the same uncredentialed stranger, and **no UI-31 scenario drives it**, so those occurrences are outside the measured 13 and rewording them moved no number in the report. They were reworded anyway. Adding a scenario would change UI-31's own measurement basis, which is not this item's to change, so the gap is STATED instead.
  NC RUN x7, app.html restored BYTE-IDENTICALLY after every on-disk arm with sha256 compared before and after (eaca866f… each time, `git status` clean on the file), and every arm re-run against the FINAL file so the suite's own `NEGATIVE CONTROL:` header names the file that carries it. **(f) IS THE ITEM'S OWN AND GUARDS THE HARD CONSTRAINT** — `signIn()` translates the plane's refusal ("a salted derivation" -> "a scrambled copy"), the move DEC-8 forbids and the one UI-33 was told above all not to make -> **2 of 32 FAIL**, and the PAIR is the point: the REACH arm names the ACT ("the gate rendered the plane's own sentence, whole"), and the ATTRIBUTION arm names the CONSEQUENCE — three plane terms flip UNAVOIDABLE -> **INCIDENTAL** and a fourth vanishes, so the measurement DEC-49 is to be answered against would have blamed this surface for the plane's own sentence and shrunk the ruling's subject by a term. **(g) FOUND A GAP AND CLOSED IT, so both numbers are recorded**: the same overstep on the CASE page (`verification.detail` through `.replace("this instance","this group")`) ran **31 of 31 GREEN, exit 0** — only the report moved — because UI-31 pinned the plane's sentence verbatim at the GATE and nowhere else, leaving the largest pre-authentication surface in the product unguarded against exactly this. One REACH assertion now pins it and the same mutation fails **1 of 32**, naming `verification.detail` and DEC-8. (a) three hidings, 2/30, 3/31, 3/30. (b) `UI31_EMPTY_TERMS=1` 4/32. (c) `UI31_NO_PLANE_RANGES=1` 1/32, "0 plane-sourced rows and 12 surface-authored". (d) `UI31_ENFORCE=1` 1/32, naming all NINE with their sources. (e) ON DISK, the gate hint gains "capture_sha" -> 32/32 green with the report growing 9 terms to 10 and the new row attributed to the gate, which is the arm that proves the report SEES something it was not built against.
  ONE ASSERTION CORRECTED MID-BUILD AND REPORTED RATHER THAN SMOOTHED. (g)'s new pin first required BOTH the case answer's top-level `detail` and its `verification.detail` to be rendered whole, and it FAILED on the clean file — because for a case that was FOUND, `op=publishedcase`'s top-level `detail` is rendered NOWHERE (`app.html` prints it only on the not-found and not-a-case branches). The arm was NARROWED to the sentence the page actually prints, with the reason and the measurement at the site; whether a found case should show the plane's own one-line description is a rendering question inside UI-29's ground and is left there.
  ONE ITEM ROUTED TO CONDUCT, AND NOTHING WRITTEN TO DECISIONS.md: **DEC-49's entry quotes UI-31's headline verbatim — "13 terms … EIGHT of the thirteen are the SURFACE'S OWN words" — and that is now the tree's history rather than its state.** Bob is meant to answer that decision AGAINST the measurement, and the eight it names are closed. The UNAVOIDABLE column did not move, so none of DEC-49's REASONING is invalidated; only its headline. MEASUREMENTS.md carries the new reading with a superseded-in-part pointer on UI-31's entry, and DEC-49 itself is left for CONDUCT.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker a33d84f). Verified from the main checkout: harness 33/33 exit 0 read unpiped, both guards green with the seam set unchanged, no plane file touched, plancheck green. CONDUCT re-ran the enforcement arm and the report reproduces at 9 terms / 4 surfaces / 45 visible, with all eight plane-sourced rows standing. **THE STRUCTURAL-LIVENESS FINDING IS THE BEST THING IN THIS LANDING and generalises well beyond it:** the arm required both structural rules to have matched something on the REAL surfaces, which was true only because the defect was standing there — so the guard was silently asserting that the defect PERSISTED, and fixing it would have turned the guard red. A guard fed by what it watches for is a class this project has not named before. Proving the corrected instrument measurement-neutral by running it against the unchanged file and diffing to a character-identical report is exactly how to change an instrument without changing what it measures. Arm (g) is the second: the same DEC-8 overstep on the CASE page ran entirely green, because UI-31 pinned the plane's sentence verbatim only at the gate — a gap in a guard that landed hours earlier, found by an arm rather than by review, and closed. The three KEPT terms are the right judgement and the reasons are better than the rewords would have been: an address that is reworded is broken, and an algorithm name is what makes 'check this without us' true. Keeping `handle` on the ground that a product-wide identifier cannot be renamed at one surface is also what identified the real item — routed as UI-34. UI-35 routed for the dead `detail` field, briefed to MEASURE consumers rather than assume, because REC-41 was wrong about its op while right about its field and only re-measuring caught it. DEC-49's headline was superseded in part by this landing and CONDUCT corrected it in the entry — the UNAVOIDABLE column did not move, so the reasoning stands, but Bob answers that ruling against the measurement and the measurement it quotes must be current. The environment finding (an empty node_modules failing a suite before any edit) reported rather than hidden. No DEC raised.
## CLAIM 2026-08-04 RECORD

session: rec46-agent (worktree agent-ac378edac67be1051)
opened: 2026-08-04T23:40:00Z
paths: bio-plane/checks/bio-checks.mjs (THE ONE HOME. `ACTOR_CLASSES` becomes exported; new exports `MACHINE_AUTHOR_PREFIX`, `MACHINE_CLASS_PREFIX`, `MACHINE_STAMP_PREFIXES`, `isMachineStamp()` and `isMachineIdentity()` beside `NON_MEMBER_AUTHORS`; the FIVE `NON_MEMBER_AUTHORS.includes(...)` asking sites at C-18.1 release authority (x2), C-2.8 `division.apportioned_by`, `checkGrounds`' `grounds[].asserted_by` and C-18.8's signed release author all rewired to `isMachineIdentity`. Nothing else in the catalog: no check is added, removed or renumbered), bio-plane/src/store.mjs (ONLY: (1) the `../checks/bio-checks.mjs` import list gains the predicate and the author prefix; (2) the NINE identical `!who || who === "member" || /^token:/.test(who)` guards on stateRelease/inquiryConclude/actionMove/correspond/inquiryReopen/publishCase/inquiryDivide/groundInquiry/strengthBarSet; (3) the TWO `/^token:/.test(String(actor))` guards in `taskForward`/`taskResolve`, which are DELIBERATELY NARROWER and stay narrower — they get the STAMP predicate, not the identity one; (4) `static QUEUE_MACHINE_AUTHOR_PREFIX = "token:"` becomes the imported constant. NOTHING else: not promote, not the gate, not the audit sweep, not purge, and none of CAPTURE's link/capture/task/reachability functions beyond the two task ACT guards named above, which are REC-28's and are RECORD's op contracts), bio-plane/src/index.mjs (ONLY the machine-identity STAMPS: five `` `token:${cls}` `` and fifteen `` `class:${cls}` `` interpolations compose from the two imported prefixes. No op, class list, capability or dispatch changes), bio-plane/src/query.mjs (ONLY `viewerPredicate`'s regex, whose `class:` literal becomes the imported prefix. The token-class vocabulary `(admin|member|probe|daemon)` and every semantic of that function are UNTOUCHED — it answers a SCOPE, not a refusal, and that difference is stated at the site), bio-plane/test/grounds.test.mjs (block 4 gains the minted-spelling refusals at the hand-written op=promote door and the pins that every existing machine refusal is unchanged), bio-plane/test/hygiene.test.mjs (the BODY: a NEW sweep block with detectors (D1)/(D2) for a locally restated machine identity, with its own REACH asserted; AND its `NEGATIVE CONTROL:` header, into which REC-51's arm block is FOLDED — M0-9 has landed, so the block it held is now foldable, and REC-51 left written instructions to fold it), bio-plane/test/queue-conditions.test.mjs (ONE pin corrected with a dated reason: it compares two hand-typed spellings that are now ONE constant, so it becomes a composition pin), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I5 is NOT touched: no table, column or index.** **I3: NO SHAPE CHANGE. One deliberate BEHAVIOUR change, and it is the item:** an `asserted_by` / `apportioned_by` / release author carrying a spelling the CONTROL PLANE ITSELF mints for a machine (`token:<class>`, `class:<class>`) is now REFUSED at the hand-written `op=promote` door, where today it passes. Every refusal that fires today goes on firing with the same reason string and the same detail — asserted, not assumed. The stamps on the wire are character-identical while the prefixes are `token:`/`class:`.
interfaces consumed: none changed.
concurrency: UI-33 runs concurrently and holds `civicos-ui/**`. **This claim opens no file under `civicos-ui/` at all** — disjoint, stated both ways. No other worker is named in an open claim over `bio-plane/`.
explicitly NOT touched: bio-plane/src/affordances.mjs, src/schema.mjs, src/cdx.mjs, src/gate.mjs, src/tokens.mjs, src/subresources.mjs, every other bio-plane/src module, bio-plane/scripts/** (coverage.mjs, battery.mjs, control-register.mjs), bio-plane/test/acquire.test.mjs (so REC-50's arm block stays where it is), every other battery suite, bio-plane/package.json, civicos-ui/** (every file), newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md
expected: QUEUE.md REC-46, routed out of REC-45's measurement. ONE machine-identity predicate. `checkGrounds` refuses an `asserted_by` by matching a WORD LIST and knows nothing about the `token:<class>` prefix `index.mjs` actually stamps, so `asserted_by: token:member` passes `op=promote` today. The fix is not another arm: one predicate, exported from where identity is decided, and EVERY asking site rewired to it, so that changing what counts as a machine in ONE place moves every site. **Sweep for the CLASS rather than trusting the item's count of three** — REC-48 found a fourth and REC-51 a fifth plus a sixth that was deliberately different, and the same is expected here. A site that turns out NOT to be the same question is a FINDING: state the difference, hold it open, and do not collapse it.
accepts-when: as QUEUE.md REC-46. `cd bio-plane && npm run test:battery` green — 98/98 and 5478 assertions on main today, any delta ATTRIBUTED per suite and not estimated — with one exported predicate answering at every asking site; `asserted_by: token:member` refused at the hand-written `op=promote` door exactly as it already is through REC-45's act; **every existing machine refusal unchanged, ASSERTED rather than assumed**; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — restore a local word list at one site and the drift assertion fails NAMING it; and separately widen the predicate in its one home and show every site following. **The drift assertion must be STRUCTURAL**: a copy that agrees today agrees at zero cost, measured four times now (REC-43, REC-48, REC-50, REC-51). **A detector must be BLIND to the current value where the control moves that value** (REC-51). **The sweep's own REACH is asserted as a DELTA, never against an absolute** — REC-48 shipped the absolute version and had to correct it, and this project has hit covered-on-paper six times. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98 suites, 5506 assertions, 88.3s**, zero leaked sandboxes (0/0 before and after, host orphans 0 -> 0). The **+28 over 5478 is ATTRIBUTED and not estimated**, measured by stashing to HEAD and running each suite standalone before and after: hygiene 393 -> 417 (+24), grounds 65 -> 68 (+3), queue-conditions 50 -> 51 (+1), daemon-token 56 -> 56 (0, a pin replaced not added). **NO OTHER SUITE MOVED**, which is what the item predicts — every composed expression evaluates to what it did before while the prefixes are `token:` and `class:`. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED: **exit 0**, 130/130 ops, 53/53 checks, 98/98 suites declaring a control, **264 arms** (251 before — the rise is REC-51's folded arms plus this item's six, and it is the register showing a declaration that GREW rather than shrank).
  THE DEFECT MEASURED FIRST, THROUGH THE OP, before a line of the fix was written: the three assertions were added to grounds.test.mjs block 4 against the tree as it stood and FAILED — `asserted_by: token:member`, `class:member` and `TOKEN:member` ALL PASSED op=promote's gate (65 pass, 3 FAIL) while the identical claim spelled `agent` was refused on the line above.
  **SIX ASKING SITES, NOT THREE — AND SEVENTEEN CALL SITES, NOT ONE.** The item named the word list, the `token:` prefix and `ACTOR_CLASSES`. Swept for the CLASS instead: (1) the catalog's word list at FIVE sites (C-18.1 release authority and its NEGATED fence, C-2.8 `division.apportioned_by`, `checkGrounds`, C-18.8's signed release author); (2) NINE identical `!who || who === "member" || /^token:/.test(who)` guards in store.mjs; (3) TWO `/^token:/.test(actor)` guards at taskForward/taskResolve, deliberately narrower; (4) `Store.QUEUE_MACHINE_AUTHOR_PREFIX`, a fourth statement of the spelling with a suite PROVING it equal to index.mjs's literal — a copy proven equal is still a copy; (5) the MINT itself, twenty interpolations in index.mjs; (6) `viewerPredicate` in query.mjs, a fifth statement of the spelling.
  **AND A SECOND MINTED SPELLING THE ITEM DID NOT KNOW ABOUT, which is the finding that mattered most.** `index.mjs` stamps `token:<class>` on AUTHORSHIP fields at 5 sites and **`class:<class>` on OWNERSHIP and viewer fields at 15**, and NO refusal anywhere in the plane knew about the second one. Closing only the routed spelling would have left the identical hole one prefix over: `asserted_by: class:member` reached the record exactly as `token:member` did, measured. Both are now `MACHINE_STAMP_PREFIXES` in one place.
  THE SHAPE: `checks/bio-checks.mjs` is the home, because it is the LEAF module every asking site already imports (index.mjs, store.mjs, query.mjs and the node suites all reach it; none of them can reach each other), and because two of the three old answers already lived in it. It exports `MACHINE_AUTHOR_PREFIX`, `MACHINE_CLASS_PREFIX`, `MACHINE_STAMP_PREFIXES`, `isMachineStamp` and `isMachineIdentity`, and `ACTOR_CLASSES` is exported rather than private. **The MINT composes from the same two constants**, so the stamp and the refusal are the same strings and cannot disagree at all — that is what makes the drift lever total rather than partial.
  **TWO PREDICATES AT TWO STRENGTHS, AND THE NARROW ONE IS A FINDING HELD OPEN, NOT AN OVERSIGHT.** `taskForward`/`taskResolve` (REC-28, D-151) deliberately omit the bare-class arm, and the reason is documented at the site: the bare string `admin` is ROOT_ADMIN's own session there, so a bare-class arm would refuse the root administrator's browser. They take `isMachineStamp` — which still derives from the one prefix set, so the SPELLING moves in one place and moves them too — while the nine act guards take `isMachineIdentity`. The difference is asserted (`isMachineStamp("daemon")` is false, `isMachineIdentity("daemon")` is true) so a later reader who notices the two overlap cannot quietly collapse them, and it is VISIBLE IN THE CONTROL: arm (c) moves nine sites and leaves those two alone, arm (d) moves all eleven.
  **A SECOND SITE THAT MEANS SOMETHING GENUINELY DIFFERENT, LEFT ALONE WITH ITS DIFFERENCE STATED (REC-51's pattern).** `viewerPredicate` asks the same words of the same spelling and answers a DIFFERENT question: not "is this a machine, and therefore refused" but "whose view does this credential compile for", and its answer for a machine is a PERMISSION (unfiltered scope), not a refusal. Rewiring it to `isMachineIdentity` would widen what compiles unfiltered from the four TOKEN CLASSES to every bare class word and every name in `NON_MEMBER_AUTHORS` — a ruling about who may see the group's thinking, and not a sweep. Its vocabulary is the token classes, a different set from `ACTOR_CLASSES`, and that is stated at the site. What IS shared is the spelling, imported, and it is pinned as a RELATION — the parser recognises what the mint writes, asserted by ANSWER not by source text, which asserts no value and so is not a ruling.
  **A THIRD, INSIDE THE CATALOG: `ACTOR_CLASSES` HAS A SECOND READER AND IT IS NOT AN IDENTITY QUESTION.** C-18.1 reads that array to validate a DECLARED FIELD (`provenance.documents[].capture.actor_class`) against the closed set of values it may take — a vocabulary-membership question about a value the record CARRIES, not an identity question about who is ACTING. The overlap is of VALUES, not of questions, and collapsing them would make the check refuse a legitimate `actor_class: daemon` on every sweep-origin capture in the record. Held exactly as REC-50 held the archive letter: the reader COUNT is pinned exactly so a THIRD reader fails by name and the second cannot rot away, plus an assertion that it is still asking the declared-field question.
  THE DRIFT ASSERTION IS STRUCTURAL AND ITS SUBJECT IS THE ASKING SITE, NOT THE VOCABULARY. **The obvious shape-based sweep was tried and MEASURED before it was rejected**: a value-blind hunt for a colon-prefixed stamp, a prefix regex and an inline word list returns **62 hits over src/ and checks/ and every one of them is innocent** — formats, part ids, frontmatter keys, status vocabularies. A detector that noisy gets weakened until it finds nothing (UI-32's lesson one tree over). So a site is found by the refusal it EMITS — the one thing a drifted site still has — and its GUARD is then required to call the predicate and to hold no local identity decision (no regex literal, no `.includes(`, no `.startsWith(`, no string equality). **That makes both detectors blind to the current value**, which is REC-51's REACH C3 and matters more here because the control MOVES the prefix: a detector looking for `token:` would go deaf exactly when a stale `/^token:/` copy became dangerous — proved by arm (d), where the prefix moves and hygiene stays 417/0.
  THE STRING-EQUALITY ARM WAS NARROWED, AND THE NARROWING IS MEASURED RATHER THAN AESTHETIC: it fired on two CORRECT guards (`typeof x !== 'string'` and `x.trim() === ''`) before it was written. Those are SHAPE tests, not identity tests, and they must stay — absent is not machine, so every asking site keeps its own blank arm and `isMachineIdentity('')` answers FALSE. The arm now excludes empty literals and `typeof` names, and still names none of the identity values.
  **AN INSTRUMENT DEFECT CAUGHT AND CORRECTED MID-BUILD, REPORTED NOT SMOOTHED — TWICE.** (1) The guard reader was one-line, and `checkGrounds`' corrected guard is written across two lines, so it reported the type test and never saw the predicate call: an instrument that lies in the ACCUSING direction. It now reads the guard whole by balancing parens. (2) The drift PROBE's first runs reported all eleven guards refusing `butler` AND refusing `carol` — because it passed the author in the body while the store reads it from the search params, so every refusal was `!who` firing. An outcome that costs nothing to produce is not evidence (CLAUDE.md); caught by running a NAMED MEMBER through the same instrument, which is the only reason arm (c) means anything.
  REACH ASSERTED SEVEN WAYS, this project's seventh encounter with covered-on-paper: R0 asserts the flat-directory assumption for BOTH roots (REC-51 closed this for `src/`; this is a SECOND walk and would have reopened it, so `checks/` is asserted too and arm (f) is its control); R1 that both trees and the three carrying modules are in the file list; **R2 that the anchors FOUND something in both trees AND that every anchor RESOLVED to a guard** — an unresolved anchor would drop silently out of the subject, which is the same failure one step in; R3 that (D1) recognises all three pre-fix guard shapes and not the composed one; R4 that it names a locally-deciding guard written against a DIFFERENT prefix, a different class word and a different list; R5 that both detectors fire in EVERY file's own stripped text **as a per-file DELTA, never against an absolute** — REC-48's own corrected defect, which would have recurred here verbatim since store.mjs and bio-checks.mjs both already carry sites.
  EVERY EXISTING MACHINE REFUSAL UNCHANGED, ASSERTED RATHER THAN ASSUMED: an ANSWER-SET pin runs the predicate over all 21 identities the three old ways refused (both minted prefixes across four classes, the ten word-list names, the three class words, and a mixed-case stamp) and over a complement of six that are NOT machines — a named person, an id, a member-prefixed viewer, **`admin` on purpose** (ROOT_ADMIN's bare session, which the narrow predicate must not refuse), the empty string and null. The complement exists because a predicate answering TRUE for everything would satisfy the first list perfectly.
  NC RUN x6, every file restored BYTE-IDENTICALLY with sha256 compared before and after and all arms re-run against the FINAL files (checks/bio-checks.mjs df71cf18…, src/store.mjs 5163d61b…, src/index.mjs 3d3aa9dd…, src/query.mjs 208e8aa4…), recorded IN FULL in hygiene.test.mjs's own `NEGATIVE CONTROL:` header. (a) THE ITEM'S OWN — the local word list restored at ONE site -> hygiene 414/3, all three detectors naming `checks/bio-checks.mjs:1721`, while inquiryground 81/0, publish 77/0, divide 82/0 and release 31/0 STAY GREEN. (b) THE ZERO-COST COPY MEASURED A FIFTH TIME ON A FIFTH SUBJECT — the pre-fix store guard restored at one site -> hygiene 415/2 naming `src/store.mjs:2908`, with grounds 68/0, inquiryground 81/0 and affordances 78/0 ALL GREEN. **(c) THE ITEM: `'butler'` added to `NON_MEMBER_AUTHORS` in the catalog ALONE moves a word that guard never knew from PAST all eleven store guards to REFUSED BY NAME at nine of them, and refuses `asserted_by: butler` at the gate, with nothing else edited.** (d) `MACHINE_AUTHOR_PREFIX` moved `token:` -> `bot:` in the catalog ALONE: the whole plane follows in one instant — `bot:member` refused by name at ALL ELEVEN including both task verbs, `token:member` walking past all eleven, the mint and the D-61 GLOB moving together — while hygiene STAYS 417/0 (the blindness property) and four suites that pin the wire VALUE fail as they should. **(e) THE DIFFERENCE BETWEEN (b) AND (d), WHICH IS THE WHOLE ITEM: the copy restored AND the prefix moved -> `bot:member`, a real minted machine credential, GROUNDS AN INQUIRY.** Ten sites refuse it; the one that decides for itself lets it through. (f) `mkdir checks/subdir-probe` -> 416/1 naming the directory; `rmdir`, 417, `git status` clean.
  THREE PINS CORRECTED WITH DATED REASONS, NEVER EXEMPTED, and two of them were WEAKER THAN THEY LOOKED. queue-conditions' prefix pin matched two hand-typed literals and checked they agreed — the best a proof-by-parsing can do, and still weaker than not having a copy; it now asserts that NEITHER file spells a prefix of its own and that both take the name from the catalog. daemon-token's `viewerPredicate` pin matched the LITERAL TEXT of the class alternation, so it would have gone red for a refactor that changed no answer and green for a regex that recognised all four classes and then compiled DENY; it now asserts the six ANSWERS. **REC-33's arm (b) was RE-RUN against the corrected pin and still bites at exactly its recorded strength (50/6, unchanged)** — the correction strengthened the suite rather than loosening it, which is the thing worth proving when a pin is rewritten.
  REC-51'S ARM BLOCK FOLDED INTO THE HEADER, on the written instruction it left for exactly this moment. M0-9 has landed, so the block it could not touch is writable again; the arms are in the `NEGATIVE CONTROL:` declaration verbatim as run, and the assertion site carries a pointer rather than a second copy. **The register shows it: hygiene's declaration went 12 arms -> 21, and the total 251 -> 264.** REC-50's arms are in `test/acquire.test.mjs`, which this item does not open, so they are left where they are.
  **A GENUINE DECISION ITEM FOR CONDUCT, and it is a consequence rather than an implementation question:** `class:<class>` is minted at fifteen sites and, before this item, was refused NOWHERE. It is now refused everywhere `token:` is, which is right for an AUTHORSHIP field. But it is also the identity the plane stamps on `owner`, `by`, `declaredBy`, `resolvedBy`, `threadedBy` and `memberId` — fields where a machine credential is a LEGITIMATE actor today and the store admits it. This item changed no such site and every one of them stayed green; the question worth routing is whether a machine credential should be able to DECLARE a relation, RESOLVE a reference or THREAD a progression at all, since each of those is described in its own comment as "a member's constitutive statement" while nothing enforces it. That is D-151's finding one subsystem over, and it is a ruling, not a sweep.

released: 2026-08-04 — landed on main by CONDUCT (merge of worker e62f2cb). Verified from the main checkout: battery 98/98 (5506, the +28 attributed per suite by stashing to HEAD), coverage strict run directly at 130/130 ops, 53/53 checks, 98/98 controls and 264 arms, zero leaked sandboxes, plancheck green. **THE SECOND MINTED SPELLING IS THE FINDING, and it is the strongest argument this session has produced for sweeping rather than fixing what was reported:** the mint writes `token:` on authorship fields and `class:` on ownership and viewer fields, and nothing in the plane refused the second — `class:member` reached the record exactly as `token:member` did, measured rather than inferred. Closing only the routed prefix would have left the identical hole one spelling over. Four items running have now found more than they were told to expect (REC-48 a fourth statement, REC-51 a fifth copy and a sixth that differed, UI-32 a second site, this one three whole groups). CONDUCT RE-RAN THE CONTROLS from the main checkout because this is identity-refusal code: the predicate answers true for both minted spellings, false for a real member id and a bare name, and false for `admin` — the held-open ROOT_ADMIN distinction intact. Moving the prefix in its ONE home makes `bot:member` recognised and `token:member` not, in one instant, with hygiene still 417/0. `bio-checks.mjs` restored byte-identically. Arm (e) is the item in one sentence: a real minted machine credential grounds an inquiry, ten sites refuse it, and the one deciding for itself lets it through. The three held-open sites are right and each for a different reason — a narrow predicate where bare `admin` is a real session, a PERMISSION rather than a refusal, and a DECLARED FIELD rather than an identity — with the last pinned by reader COUNT so a third fails by name. Rejecting the value-blind shape sweep after MEASURING it at 62 innocent hits, and reporting a drift probe whose refusals were all the empty-check firing, are both the discipline working. REC-51's arm block folded into the header on the instruction it left. DEC-52 raised: six fields carry a machine credential today while their own comments call them a member's constitutive statement, and nothing enforces it — D-151's finding one subsystem over. No other DEC.

## CLAIM 2026-08-04 UI

session: ui34-agent (worktree agent-ad1798ea15e5757f7)
opened: 2026-08-04T23:55:00Z
paths: civicos-ui/app.html (TWO regions only, and NEITHER changes a member-facing word: (1) the gate's own comment block near line 1138, where UI-34's DECISION on `handle` is RECORDED so the next reader does not re-open it; (2) `pubVerifyPanel`'s kept-terms note near line 14274, whose sentence "this function is NOT driven by any UI-31 scenario" this item makes FALSE and which is corrected with the new basis stated, and whose `handle` paragraph is replaced by the product-wide argument. **NO member-facing STRING in app.html changes at all** — not a label, not a placeholder, not one word of prose — which is the honest consequence of deciding to KEEP), civicos-ui/test/preauth-vocabulary.test.mjs (UI-31's instrument: WALK 1 is EXTENDED to discover the PUBLISHED MASTHEAD's own controls the way it already discovers the gate's, one SCENARIO is added driving `pubVerifyPanel`, one REACH assertion pins that scenario's subject, and the header + `NEGATIVE CONTROL:` line are rewritten with the arms as run. **The measurement itself — walk 3's term harvest, `planeRanges`, `countIn`, the HITS loop, the report and `REPORT_ONLY` — is NOT changed**; the BASIS is, deliberately and visibly, which is this item's part (b)), civicos-ui/test/identifier-vocabulary.test.mjs (NEW SUITE — the product-wide drift guard for the member-identifier word. It pins NO value: it asserts that every member-visible site that names that identifier uses the SAME word, so KEEPING is enforced and RENAMING everywhere stays green while renaming SOME sites fails naming the ones that did not move), docs/development/MEASUREMENTS.md (APPEND-ONLY: one new dated entry stating the NEW BASIS, plus a superseded-in-part pointer at the foot of UI-33's entry. UI-31's and UI-33's tables and reasoning are left standing), docs/development/CLAIMS.md (this entry)
interfaces owned: none. **behind-interface: none** — this item changes no op, no field, no shape and no wire string. The wire field `handle` is READ as the anchor the new guard is built on and is written to by nobody here.
interfaces consumed: I3 (the op contracts), read and rendered exactly as before.
concurrency: **REC-46 runs concurrently on `bio-plane/**` and is DISJOINT from this claim, stated both ways.** This claim opens NO file under `bio-plane/` — `bio-plane/src/store.mjs` is READ textually by UI-31's instrument for the refusal sentence and by the new guard for the wire field's authority, and is written to by nobody here. REC-46 opens no file under `civicos-ui/`.
explicitly NOT touched: every PLANE-SOURCED term on every pre-authentication surface (DEC-49 is OPEN with Bob, its subject is those eight rows, and ANY movement in them is a FAILURE of this item and not a result); the wire field name `handle` on `op=whoami`, `op=projectinvite`, `op=projectremove`, `op=projectowneradd`, `op=projectownerrescue`, `op=select`, `op=release` and `op=dispose` (renaming it is an I3 change through INTERFACE-CHANGES.md and is NOT what this item decided); UI-32's grade-letter sweep in `civicos-ui/test/add-surface.test.mjs`; UI-30's login gate logic in `signIn()`; UI-28's attest region; UI-27's one-function filter; every other suite under `civicos-ui/test/` (`members-roster`, `project-workspace` and `publishedcase` are READ for their surfaces and edited by nobody here); `bio-plane/**`; `newgroup/**`; `docprofile/**`; `tools/**`; QUEUE.md, DECISIONS.md, DEBT.md, MILESTONES.md, INTERFACES.md, VERIFICATION.md.
expected: QUEUE.md UI-34, routed out of UI-33's report. (a) `handle` decided PRODUCT-WIDE — renamed everywhere or kept everywhere, with the decision recorded at the gate rather than left as it is by default. (b) `pubVerifyPanel()` is a pre-authentication surface no UI-31 scenario drives; the scenario is added here and the measurement reported BEFORE and AFTER so the change to DEC-49's measurement basis is visible rather than silent.
accepts-when: as QUEUE.md UI-34. `node civicos-ui/test/run.mjs` green with the exit status read UNPIPED; `handle` either renamed at EVERY site or deliberately KEPT with the decision recorded at the gate; UI-31's report run BEFORE and AFTER with both term lists reported and **any movement in the PLANE-SOURCED rows treated as a FAILURE rather than a result**; `pubVerifyPanel` driven by a scenario with the measurement's NEW BASIS stated in MEASUREMENTS.md with its date and instrument; both guards green (`check-semantics`, `check-mock-envelope`). NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — a rename that reaches SOME sites and not others must fail NAMING the sites that did not move, and it must be STRUCTURAL, because a value comparison passes while a surface goes on answering correctly and ADDITIONALLY answers wrongly (measured three times in this project). Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report.

released: 2026-08-04 - landed on main by CONDUCT (merge of worker f5add98). Verified from the main checkout: harness 34/34 exit 0 read unpiped, both guards green with the seam set unchanged, no plane file touched, plancheck green. CONDUCT INDEPENDENTLY VERIFIED THE CENTRAL CLAIM with its own comment-stripper: 548,854 characters, identical digest before and after the merge - not one member-facing byte moved. That is the honest consequence of deciding to KEEP, and proving it rather than asserting it is what makes the decision reviewable. THE ARGUMENT IS ACCEPTED IN FULL and its first leg settles it: `handle` is the PLANE's field name - thirteen store methods declare it - so a member-facing rename without a wire rename gives the product two names for one field, which is UI-33's own argument moved one layer down and made harder to see. The second leg is better still: `username` collides with a DISTINCT existing field, and calling this a NAME erodes the identifier/identity separation on the very screen whose subject is who holds power. A kept word with a recorded reason is a result. THE DRIFT ASSERTION PINS NO VALUE, which is the right shape - it enforces everywhere-or-nowhere rather than enforcing `handle` - and its control found a NINTH site a careful read had missed, a bare `<th>Handle</th>` invisible to any inventory keyed on searchable phrasings. `DEC49_SUBJECT` is the other piece of durable work: it pins the eight plane-sourced rows by term and by source, fails on any movement, and generically catches the DEC-8 overstep UI-33 could only catch with a bespoke assertion. The real finding is why nobody had driven the verify panel: walk 1 discovers controls from the GATE's markup, and the published rail is entered with no credential by design while its own links were discovered by nothing at all. Walk 1b closes that. Two of the five corrected pins would have failed a guard on a file nobody had changed - a substring match inside a sentence about tokens, and an autocomplete ATTRIBUTE read as a word in use. UI-36 routed, and it matters to Bob rather than only to the queue: `pubVerify` is a public uncredentialed op whose plane-sourced answers no scenario harvests, so DEC-49's column is a lower bound in a second, previously unnamed way. Recorded in DEC-49's entry. Declining to absorb it here was right - two basis changes in one landing reintroduce the indistinguishability UI-33 avoided. No DEC.
## CLAIM 2026-08-05 RECORD

session: rec40-agent (worktree agent-ae2b29efd546339d2)
opened: 2026-08-05T00:20:00Z
paths: bio-plane/src/schema.mjs (ONLY the `reading_ref_terms` block — one new `src` column, the PRIMARY KEY re-keyed to (capture_sha, ref, src, term), and its header comment. No other table, column or index in the file is opened; the block stays exactly where it is, BEFORE `host_governor`), bio-plane/src/store.mjs (ONLY: (1) `#migrate`'s DERIVED-TABLE RE-KEY list gains `["reading_ref_terms","src"]` beside `links`/`captured_locators`; (2) `#writeReadings`' term-projection loop writes THREE sources instead of one; (3) `#backfillRefTerms` re-derives the same three; (4) `documentsNamingEntity` — the statement, the correspondence vocabulary, the new `grade_if_resolved` field and the answer's caveat sentence; (5) `readingNamePlan` reads the SAME statement text through a new private `#refTermsSql` so plan and read cannot drift; (6) one new private `#refTermSources`. NOTHING ELSE: not promote, not the gate, not the audit sweep, not `purge` (`reading_ref_terms` is ALREADY in the D-113 sweep and no new table is created), not `documentsByReference`, not `#recognise`, not `#upsertResolution`, and none of CAPTURE's link/capture/task/reachability functions), bio-plane/src/index.mjs (ONLY the two comment blocks above `readingref`/`readingname` in the OPS table and above the DO dispatch entries, saying what the widened read now answers. NO op added, removed, reclassified or redispatched), bio-plane/test/subresources.test.mjs (ONLY the "a derived table that changes shape is rebuilt, not patched" block — its reshape-list value pin CORRECTED with a dated reason, plus one new assertion that the newest entry has a re-derivation path. ADDED TO THIS CLAIM AFTER IT WAS OPENED, and said so rather than quietly: the battery found this pin, it is a superseded test that must be corrected and never exempted, and it is not another area's ground), bio-plane/test/readingname.test.mjs (the suite: new fixture documents whose REFERENCE is spelled like a registered name, the reference tier's assertions, the recogniser-agreement arm, the readingref RELATION pins, the widened plan arm, and its own `NEGATIVE CONTROL:` header rewritten with the arms as run), civicos-ui/app.html (ONLY the `loadResolveCandidates` region — its `how()` renderer, its `note` sentence and the comment block above `resolvePreflight` that documents UI-26's trade. The op it calls does NOT change: still ONE `op=readingname` call with `entity=`. No other function, style, route or seam in the file is opened), civicos-ui/test/intent-write.test.mjs (ONLY UI-26's block: the fixture answer gains reference-tier candidates, the narrowed-sentence pins are CORRECTED with a dated reason, and the `NEGATIVE CONTROL:` header gains arm (d)'s reversal as run), docs/development/CLAIMS.md (this entry)
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I5 IS TOUCHED, and it is a DERIVED-TABLE RE-KEY, not an additive column:** `reading_ref_terms` gains `src` AS PART OF ITS PRIMARY KEY, on the exact precedent `#migrate` already carries for `links.citation_norm` and `captured_locators.via` — old rows keyed the old way are not missing a field, they are WRONG, so the table is dropped and re-derived from `reading_refs`, which already persists every string it projects. No first-party material is anywhere near it. **I3: NO SHAPE CHANGE AND NO NEW OP.** `op=readingname` answers MORE — its `correspondence` vocabulary grows from two values to five and each candidate gains `grade_if_resolved` — and every value it answers today it still answers with the same spelling. `op=readingref` is UNCHANGED and is asserted unchanged.
interfaces consumed: none changed.
concurrency: **UI-34 runs concurrently and holds `civicos-ui/**` and `docs/development/MEASUREMENTS.md`.** This claim opens NO file under `docs/development/` except CLAIMS.md, and under `civicos-ui/` it opens EXACTLY TWO REGIONS, named here so a collision is a fact rather than a guess: `civicos-ui/app.html`'s `loadResolveCandidates` function together with the comment block immediately above `resolvePreflight`, and UI-26's block inside `civicos-ui/test/intent-write.test.mjs`. Nothing else in either file. This edit is REQUIRED by the item's accepts-when (UI-26's control must consume the widened call and its narrowed sentence must re-widen), so it cannot be delegated away; if UI-34's ground includes either named region I will say so in the report rather than widen.
explicitly NOT touched: bio-plane/src/query.mjs, src/affordances.mjs, src/cdx.mjs, src/gate.mjs, src/tokens.mjs, src/subresources.mjs, bio-plane/checks/bio-checks.mjs, bio-plane/scripts/**, every battery suite except readingname.test.mjs, bio-plane/test/label-variance-probe.mjs (REC-36's instrument, left as the measurement it recorded), bio-plane/package.json, every civicos-ui file except the two regions named above, civicos-ui/check-semantics.mjs, civicos-ui/check-mock-envelope.mjs, newgroup/**, docprofile/**, pdf-worker/**, tools/**, release/**, QUEUE.md, DEBT.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, MEASUREMENTS.md, VERIFICATION.md, kickoffs/**
expected: QUEUE.md REC-40, routed out of UI-26's MEASURED trade. `op=readingname` answers on the NAME a reading recorded and `op=readingref` on the REFERENCE STRING, so the Grade A path — a document whose reference is spelled like a subject's registered name — is proposable from no surface since UI-26 traded the N-call loop away. **THE DESIGN IS THIS ITEM'S TO MAKE and is made on the evidence, not returned:** one more TERM SOURCE in `reading_ref_terms` through the same normalised join, NOT an entity-shaped sibling to `op=readingref`. **Sweep for the CLASS rather than trusting the item's count of two ops** — four items running have found more than their brief predicted, and `#recognise` reads THREE strings (`ref` → A, `ref_key` → B, `label` → C), so a term index carrying two sources would leave the identical defect one tier down.
accepts-when: as QUEUE.md REC-40. `cd bio-plane && npm run test:battery` green — **98/98 and 5506 assertions measured on this worktree's HEAD before a line was written**, any delta ATTRIBUTED per suite by stashing and re-running, never estimated — with ONE call returning both a near-NAME candidate and a reference-spelled Grade A candidate, GATED IDENTICALLY (the same `#bundleGate` in the same SQL, asserted by driving the uninvited member at the new tier), and INDEXED rather than scanned — **asserted against the statement the read actually runs, not a hand-copied twin**, because a copy that agrees today agrees at zero cost. `node civicos-ui/test/run.mjs` green with UI-26's control consuming the widened call and its narrowed sentence RE-WIDENED, corrected at the site with a dated reason and never exempted. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, exit 0. NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line: **drop the ref-term source and the Grade A candidate vanishes WHILE THE NAME TIER STILL ANSWERS** — the arm that proves the two tiers are independent rather than one thing renamed. UI-26's arm (d) reversal driven. Every file restored BYTE-IDENTICALLY after each arm with sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `cd bio-plane && npm run test:battery` **98/98 suites, 5541 assertions, 88.4s**, zero leaked sandboxes (0/0 before and after, host orphans 0 -> 0). The **+35 over 5506 is ATTRIBUTED, not estimated, and not inferred from the suites I happened to open**: the whole battery was run TWICE — once on this tree and once stashed to HEAD — and the per-suite counts DIFFED. Exactly two rows move: readingname 42 -> 76 (+34) and subresources 343 -> 344 (+1). **NO OTHER SUITE MOVED**, which is the evidence that a re-keyed table and a widened answer preserved every landed answer rather than rewriting them. `node civicos-ui/test/run.mjs` **exit 0, 33/33 suites plus both guards**, intent-write 129 -> 141; the envelope guard's arm B is UNCHANGED at 66 distinct ops — readingname was already on the line and readingref is still off it, because the widening happened INSIDE the op and not by adding one. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED (redirected to a file; a redirect is not a pipe): **exit 0**, 130/130 ops, 53/53 checks, 98/98 suites declaring a control, **267 arms** (264 before — readingname's own declaration went 2 -> 5).
  THE DEFECT MEASURED FIRST, THROUGH THE OP, before a line of the fix was written: the new arms were added against the tree as it stood and **16 of 71 FAILED** — the A-tier document offered by nothing, the B tier answering only at the weaker name-inside-a-title, both grade predictions absent, the identifier gate arm without a subject, and the backfill unable to re-derive a reference.
  **THE DESIGN, DECIDED ON THE EVIDENCE AND NOT RETURNED: ONE MORE TERM SOURCE in `reading_ref_terms`, with `src` IN THE PRIMARY KEY — not an entity-shaped sibling to `op=readingref`.** The item offered both and left the choice here. THE ESTABLISHED TEST APPLIED EXPLICITLY (D4 as REC-42 stated it and REC-44 applied it twice): does the structure need an ORDINAL of its own? **No** — a term of an identifier is keyed by exactly what a label term is keyed by, has no ordering, no lifecycle and no identity apart from the `reading_refs` row it is derived from and dies with; REC-42's own words, "a ground needs neither ordinal nor grade", yield a column there and yield one column here. Is a QUERY genuinely keyed on it separately? **No** — there is exactly ONE question, and this item's whole requirement is that ONE call answer every tier, so a second table forces either a UNION of two compound arms (toward D-36's five-compound workerd ceiling) or two statements, **which is the N-call shape REC-40 exists to remove**. THE SIBLING WAS REJECTED ON THREE MEASURED GROUNDS, not on taste: (1) `op=readingref` is an exact equality on ONE column, so an entity-shaped twin would look each alias up in turn — UI-26's loop moved from the surface into the plane rather than removed — and would reach only WHOLE-string matches, leaving a name inside a composite key invisible; (2) two ops means the surface makes two calls, failing "ONE call answers both tiers" outright; (3) two ops means TWO GATES, and the two existing reads deliberately hold DIFFERENT postures (`documentsByReference` redacts the bundle id, `documentsNamingEntity` withholds the ROW), so "gated identically" would have become a second copy of the gate — the drift class this project has now measured five times.
  **AND `src` IS IN THE KEY BECAUSE IT IS A CORRECTNESS REQUIREMENT, not because the answer needed a label.** The lookup is a SUBSET test (`HAVING COUNT(DISTINCT term) = n`), so a shared group would let a registered name be satisfied by ONE WORD FROM A DOCUMENT'S TITLE AND ANOTHER FROM ITS REFERENCE STRING — a correspondence NEITHER string made, and a wrong subject on a document. Negative control (d) is that sentence measured: a subject whose two words live one in a title and one in a different string of the same reference matches 1 where it must match 0. Because the meaning is in the KEY, the migration is a DERIVED-TABLE RE-KEY, and `#migrate` already carries both the mechanism and the argument in its own words ("old rows keyed the old way are not merely missing a field, they are wrong") — `reading_ref_terms` joins `links` and `captured_locators` there, and is re-derived from `reading_refs`, which persists every string it projects. **No new table, so D-113 needed nothing: `reading_ref_terms` is already in `purge`'s whole-store arm.**
  **THE PREMISE HELD AND WAS UNDERSTATED — THE ITEM NAMES TWO TIERS AND THERE ARE THREE.** `#recognise` grades on `ref` (A), `ref_key` (B) and `label` (C), and `op=readingref` matches `reading_refs.ref` AND NOTHING ELSE. So the B tier — a document whose reference KEY is spelled like a registered name — **was never reachable from any surface at all**, before UI-26 or after; restoring the traded-away loop would not have restored it, and indexing only "the REF" as the scope says would have reproduced the identical defect one tier down. The term index carries all three. The sweep also confirmed there is no THIRD asking site: `loadResolveCandidates` is the only surface caller of either op (UI-32 deleted `reverseRefs`/`REVREF_CACHE`).
  **I FOUND AN OVERCLAIM IN MY OWN FIRST VERSION AND IT IS THE MOST IMPORTANT THING IN THIS LANDING.** `grade_if_resolved` was derived from how the alias corresponded — whole ref -> A, whole key -> B, whole label -> C. That is WRONG, because `#recognise` NEVER FALLS THROUGH: if any subject's identifier matches a reference at A, op=resolve records nothing at B or C for that reference, **including for a different subject whose registered name the label happens to be**. The first version therefore promised a Grade C the record would never mint — the record claiming more than it can support, which CLAUDE.md holds to be worse than a missing feature. **THE FIX IS ONE FUNCTION, NOT A SECOND RULE:** the tier cascade is extracted as `#recogniseTier`, and `#recognise` and `documentsNamingEntity` BOTH call it, so the prediction is made by the code that does the minting and the two cannot disagree. A candidate whose grade is null now SAYS why. Negative control (e) reproduces the bug (2 of 76) and **intent-write stays GREEN under it** — the surface renders whatever letter it is handed and cannot tell a true conditional from a false one, which is why the assertion had to live on the plane side.
  INDEXED RATHER THAN SCANNED, PROVED THREE WAYS AND NOT ASSUMED. (1) `readingNamePlan` no longer explains a hand-written twin of the query: both it and the read take their SQL from ONE builder, `Store.#refTermsSql`, so the plan is OF the statement that runs. The old plan explained a bare subquery carrying neither the join nor the group, and it would have gone on passing while the real query scanned. (2) The suite asserts `USING INDEX reading_ref_terms_term`, no `SCAN reading_ref_terms`, and — new — no `SCAN reading_refs`, so the join rides its primary key. (3) THE ANTI-DRIFT PIN IS STRUCTURAL: the suite reads `store.mjs` and requires `GROUP BY t.capture_sha, t.ref, t.src` and `JOIN reading_refs rr ON` to occur EXACTLY ONCE each, so a second builder fails by count rather than by review. Arm (d) proves it bites.
  NCs RUN x5, **ALL RE-RUN AGAINST THE FINAL FILES**, store.mjs restored byte-identically after every one and **the restore ASSERTED by the runner rather than eyeballed** (sha256 558d2e2d0575276153fb3b232fc71b02b55f7c9a027cc3e41285506f173b6879 before and after each; app.html 998f981eae59e8408767cf5a96216f2e74d418c75da303bd5e5608a221ea00e5). Recorded in readingname.test.mjs's own `NEGATIVE CONTROL:` line. **(c) IS THE ITEM'S REQUIRED ARM: drop the ref-term source -> 10 of 76 fail and EVERY ONE is an identifier arm, WHILE THE NAME TIER ANSWERS UNTOUCHED** — all six measured variance classes, the abbreviation alias arm, the diacritic refusal and the whole-label `name` correspondence stay green. The Grade A candidate vanishes and the name tier still answers, which is what proves the two tiers are INDEPENDENT rather than one thing renamed; had the name tier gone with it, the widening would have been a rename and the suite could not have told the difference. intent-write fails with it at 6 of 141 — two instruments on one subject. (d) drop `src` from the group -> 4 of 76, headed by the mixing control. (e) the overclaim above -> 2 of 76, intent-write GREEN. (a) REC-36's normalisation arm re-run -> 19 of 76, **and it gained a reading it did not have before: every identifier arm STAYS GREEN, because an A or B tier is a whole-string match that survives an unsplit term** — the arm now says which tier depends on the normalisation and which does not. (b) REC-36's gate arm re-run -> 7 of 76, and the TWO NEW failures are dave being offered the project-filed reference-spelled capture: **one gate, one piece of SQL, both tiers, measured rather than read off the source**. ONE READING KEPT because it looks like a hole and is not: the B-tier GRADE assertion stays green under (c), correctly — `#recogniseTier` reads `reading_refs` directly and never the term index, so dropping a term source changes what is OFFERED and not what would be GRADED.
  UI-26'S OWN ARMS RE-RUN because their recorded counts were stale, and (d) has become the clearest statement of the trade: **restore the per-alias loop -> 16 of 141, and "THE IDENTIFIER TIER IS BACK" STILL PASSES while "A NEAR-NAME CANDIDATE IS OFFERED" FAILS.** The loop reaches BID_A and loses BID_N — UI-26's measured trade seen from the other side — so what the arm proves is no longer "the loop lost a tier" but "the loop is not how the tier came back", and the assertion saying so is among the sixteen. (e) drop the narrowed sentence -> 7 of 141, and the composition is the useful part: the clause naming the THREE places the lookup reaches SURVIVES the cut, because the widening was placed structurally BEFORE the trailing caveat — a page may lose its caveat and must not lose the sentence that says what it did. **UI-26's own finding reproduced: the completeness-WORD screen stayed GREEN over the stripped sentence again**, an unstated bound reading as completeness for the second measured time.
  EIGHT PINS CORRECTED WITH DATED REASONS, NEVER EXEMPTED, and two of them were weaker than they looked. (1) subresources' reshape-list pin `["links","captured_locators"]` -> three; **kept deliberately as a VALUE pin, which is unusual here and is the point — dropping a table must be a REVIEWED act, so an addition is meant to fail a test and be argued for** — and strengthened with a new assertion that the newest entry has a re-derivation path, because a row dropped with nowhere to rebuild from is data destroyed. (2)(3) readingname's fixture counts, six -> ten documents and eight -> eleven subjects. (4) readingname's header paragraph, which described TWO correspondences and now describes five with the premise correction stated. (5) **UI-26's `!cands.includes(BID_A)` INVERTED BACK, with UI-13's and UI-26's readings kept above it in the past tense — a pin that has now moved twice, and the history is the most useful thing in that block.** (6) the page-says-so pin retargeted from the identifier clause, which would now be REACHED WHILE FALSE, to the three-places clause and the bound that is actually left. (7) **THE BID_C PIN SPLIT, and this is the weakness worth naming: `cands.includes(BID_C) && /which is this subject's name/` would have gone on passing on the A row's identical phrase** — agreeing for a reason it was not written for — because that document now renders at the stronger tier and the C tier is no longer visible in the HTML at all. The C tier is now asserted where it still exists, in the plane's own answer, and the surface's dedup asserted separately. (8) **THE IMPROVABILITY ARM RE-POINTED from BID_A to a new BID_I**, because BID_A became reachable from the start and the arm would have passed for free; an arm whose subject has quietly become reachable is an arm proving nothing.
  ONE MEASUREMENT BOUND STATED RATHER THAN PAPERED: the partial-reference tier (`name_in_reference`) is **UNMEASURED**. REC-36's n=41 was over LABELS, and this repository's one real document carries Legistar file numbers as references, so there is no evidence here about how noisy a name-inside-a-reference match is on a real corpus. It ships in the conservative shape — ranked LOWEST, carrying NO grade, stated as a candidate — and `test/label-variance-probe.mjs` should be re-run over REFERENCE strings before anyone leans on it. MEASUREMENTS.md was NOT opened (UI-34 holds it), so this is routed in words rather than written there.
  CONCURRENCY, STATED BOTH WAYS: UI-34 holds `civicos-ui/**` and `MEASUREMENTS.md`. This item's accepts-when REQUIRES a civicos-ui change, so it could not be delegated; it is confined to `loadResolveCandidates` plus the comment block above `resolvePreflight` in app.html, and UI-26's block in intent-write.test.mjs, both named in the claim above. `docs/development/MEASUREMENTS.md` was not opened at all.
  **A GENUINE DECISION ITEM FOR CONDUCT, and it is a consequence rather than an implementation question.** Before this item, every candidate this control could offer corresponded at grade C — `needs_confirmation`, never established. It can now offer candidates the recogniser would grade A or B, which `#isEstablished` treats as ESTABLISHED. So a member is one click from minting an ESTABLISHED resolution off a list the MACHINE composed, where previously the machine's own proposals could only ever produce something flagged for a human to confirm. Nothing changed about who may resolve or what a resolution means, and the implementation is the conservative one either way (the letter renders only as "resolving it would record Grade A", never as a grade held; no candidate wears a grade badge; a null grade says why). But **how far the machine may propose toward an established record is a ruling on the "less narrative" constraint, not a sweep**, and it is Bob's. No DECISIONS.md written by this session; QUEUE.md not opened.

released: 2026-08-04 - landed on main by CONDUCT (merge of worker 8dccad1). Verified from the main checkout: battery 98/98 (5541, attributed by running the battery twice and diffing per-suite), UI harness 33/33 exit 0, both guards green, coverage strict run directly at 130/130 ops and 267 arms, zero leaked sandboxes, plancheck green. THE SELF-FOUND OVERCLAIM IS THE REPORT'S BEST MOMENT and it is exactly the defect this project exists to refuse: the first version promised a Grade C the record would never mint, because the conditional was derived from how the ALIAS corresponded while the recogniser never falls through. Extracting `#recogniseTier` so the candidate read and the minting path answer from ONE function is the right fix, and NC (e) reproducing it at 2 of 76 WHILE the surface stays green is the measurement that matters - a surface cannot tell a true conditional from a false one. CONDUCT verified the fix and the decision's premise directly at the source. THE PREMISE HELD AND WAS UNDERSTATED - three tiers, not two, and the B tier was reachable from NO surface, so restoring the loop would not have restored it. That is the fifth item running to find more than its brief predicted. The column-not-table decision is argued on D4/REC-42's test explicitly rather than by feel, and `src` in the primary key is a CORRECTNESS requirement (a shared group would let one word of a title and one of a reference satisfy a name that neither string made) rather than tidiness. Taking the plan and the read from one builder, so the plan is OF the query, is the right answer to 'prove it is indexed'. Re-running UI-26's own arms is what a trade deserves: the identifier tier comes back AND the near-name candidate is lost, seen from the other side. The in-flight claim amendment for `subresources.test.mjs` self-reported, with a superseded pin corrected rather than exempted. DEC-53 raised - a member is now one click from an ESTABLISHED resolution off a machine-composed list, which is the 'less narrative' constraint on US arriving through convenience rather than error. M-4 routed for the unmeasured partial-reference tier, in the measurement lane so it holds no area slot. No other DEC.

## CLAIM 2026-08-04 UI

session: ui36-agent (worktree agent-ab96285c5a75f2d5c)
opened: 2026-08-04T00:10:00Z
paths: civicos-ui/test/preauth-vocabulary.test.mjs (UI-31's instrument, and this is the ONLY code file this claim opens: WALK 1c is ADDED — it discovers the controls the PUBLISHED CASE PAGE serves to an uncredentialed reader from the page as RENDERED, the way walk 1b discovers the published masthead's from the served markup; the `case-address-at-load` scenario is EXTENDED to drive every `pubVerify` call site the page renders, in its SUCCESS branch, its NOT-PUBLISHED branch, its REFUSED branch and its `catch` branch; the mock learns `op=verify` FLAT, with its refusal sentence read TEXTUALLY out of `bio-plane/src/index.mjs` and never typed; REACH assertions for each branch; `DEC49_SUBJECT` updated DELIBERATELY with every new row named; EXPECT_SURFACES extended; the header and the `NEGATIVE CONTROL:` line rewritten with the arms as run. **The measurement itself — walk 3's term harvest, `planeRanges`, `countIn`, the HITS loop, the report and `REPORT_ONLY` — is NOT changed**, and that is proved by running the changed file against the UNCHANGED app.html), docs/development/MEASUREMENTS.md (APPEND-ONLY: one new dated entry stating the NEW BASIS and the new plane-sourced rows, plus a superseded-in-part pointer at the foot of UI-34's entry. UI-31's, UI-33's and UI-34's tables and reasoning are left standing), docs/development/CLAIMS.md (this entry)
interfaces owned: none. **behind-interface: I3, CONSUMED not changed** — this item changes no op, no field, no shape and no wire string. `op=verify` is DRIVEN for the first time from a pre-authentication walk and is written to by nobody here.
interfaces consumed: I3 (the op contracts), read and rendered exactly as before.
concurrency: **REC-40 runs concurrently on `bio-plane/**` and needs a SMALL `civicos-ui` edit for UI-26's control.** This claim opens **exactly one file under `civicos-ui/`: `test/preauth-vocabulary.test.mjs`**, and **does NOT open `civicos-ui/app.html` at all** — not one byte, not a comment — so REC-40's surface edit and UI-26's control (`civicos-ui/test/subject-view.test.mjs` / `document-page.test.mjs`, whichever it lands in) are disjoint from this claim. Stated both ways. `bio-plane/src/store.mjs` and `bio-plane/src/index.mjs` are READ textually for the plane's own refusal sentences and are written to by nobody here.
explicitly NOT touched: **`civicos-ui/app.html` — NO edit of any kind, deliberately.** DEC-49 is open and the value of every measurement in this chain is that the surface was not touched while the reading was taken; a plane-sourced string is neither translated nor blanked anywhere, and no surface-authored string moves either. Also NOT touched: every other suite under `civicos-ui/test/`, `civicos-ui/check-semantics.mjs`, `civicos-ui/check-mock-envelope.mjs`, `bio-plane/**`, `newgroup/**`, `docprofile/**`, `pdf-worker/**`, `tools/**`, `release/**`, QUEUE.md, DECISIONS.md, DEBT.md, MILESTONES.md, INTERFACES.md, VERIFICATION.md.
expected: QUEUE.md UI-36, routed out of UI-34's report. `pubVerify(sha, into)` is a PUBLIC, UNCREDENTIALED op's control that renders five times on the published case page — a surface `case-address-at-load` already walks — and renders plane-sourced text (`m.path`, `m.kind`, `m.bundle_id`) that NO scenario harvests. Harvesting it can ADD rows to DEC-49's subject, and any that arrive must be named rather than absorbed.
accepts-when: as QUEUE.md UI-36. `node civicos-ui/test/run.mjs` green with the exit status read UNPIPED, with `pubVerify` driven on the published case page in BOTH its success and its error branch; the measurement's new BASIS stated in MEASUREMENTS.md with its date and instrument and UI-34's entry given a superseded-in-part pointer; **`DEC49_SUBJECT` UPDATED DELIBERATELY with every new row NAMED — never merely re-baselined**; both guards green (`check-semantics`, `check-mock-envelope`). NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — hide the new drive and the harness fails NAMING the uncredentialed control nobody drives; and make a branch TRANSLATE the plane's words and the subject arm must fail naming it as a DEC-8 overstep. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. DECISIONS.md is NOT written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `node civicos-ui/test/run.mjs` **34/34, exit 0 read UNPIPED**, with `preauth-vocabulary` 37 -> 48 assertions. Both guards green: `check-semantics` OK, `check-mock-envelope` arm A **13,638 lines and the seam set UNCHANGED** (app.html is not touched by this item at all), arm B 798 -> **803 op answers, 150 -> 151 op/suite pairs** — the +5 is `op=verify` being answered on a pre-authentication walk for the first time, FLAT, which is what its wire map entry says. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped: **exit 0**, 130/130 ops, 53/53 checks, 264 arms. No plane file touched, so the battery is not this item's subject.
  **DEC-49'S SUBJECT GREW: 8 PLANE-SOURCED ROWS -> 11, DELIBERATELY, AND THE NEW ROWS ARE `manifest`, `CASE` and `FIND`, PLUS THREE NEW SOURCES ON `bundle.md`.** All four movements come from `op=verify`'s own answer, harvested from the case page's five Verify buttons. `manifest` is the one that bears on the ruling: it is an ENGLISH WORD the plane chose (the `kind` value, printed in a sentence), and UI-33 had removed the surface-authored `manifest` from these surfaces entirely. `CASE` is the plane's real minted id prefix; **`FIND` is the FIXTURE's own id spelling and is labelled as such at the pin** rather than passed off as evidence about a live instance.
  THE READING: 11 scenarios (UNCHANGED), 12 surfaces -> 19, 34,375 -> 35,835 characters, 9 terms -> 13, 57 -> 67 occurrences, 47 -> 57 visible. **The instrument itself is unchanged and it is PROVED, not asserted** (UI-33's method, used a third time): this file with the new drive hidden reproduces UI-34's report CHARACTER-IDENTICALLY.
  **THE ITEM'S PREMISE IS CONTRADICTED BY THE MEASUREMENT, and the contradiction is a live defect.** `pubVerify`'s `catch` branch cannot print `e.error || e.reason` — `apiQ` goes through `api`, which rejects only on a transport failure or a non-JSON body, neither of which carries either field — so NO plane word can reach it. Arm (l) measures that: translating it, a DEC-8 overstep, leaves the suite 48/48 GREEN with the report character-identical. **The consequence: `apiQ` does not throw on `ok:false`, so a REFUSAL from `op=verify` lands in the NOT-PUBLISHED branch and an uncredentialed reader is told "No published part answers to that hash" when the plane in fact DECLINED to answer** — a substantive claim about the record standing in for a refusal, on the surface whose whole purpose is "check this without us", and reachable by a click (the container row composes its hash as `bundle_sha || ""`). Measured through the op with the plane's refusal read textually out of index.mjs. **The surface was NOT changed** — DEC-49 is open — so the state is PINNED by an assertion that says at the site that it pins a DEFECT and is the one to CORRECT when the fix lands, and it is ROUTED to CONDUCT.
  NC RUN x13, all re-run against the FINAL file and recorded in the suite's own `NEGATIVE CONTROL:` line, `app.html` restored BYTE-IDENTICALLY after every on-disk arm (sha256 333b4d7f… before and after each). The item's own two: **(j) `UI31_HIDE=case-verify` -> 3/42 FAIL naming `pubVerify` and `pubBytes` as controls on an uncredentialed page that no scenario drives**, the seven verify surfaces that stopped being covered, and the subject collapsing back to its pre-item state; **(k) `pubVerify`'s SUCCESS branch translating the plane's own `matches[0].path` -> 2/48 FAIL**, the REACH arm naming the act and the SUBJECT arm naming the consequence generically. Also (m) a sixth control planted on the case page -> 2/48 naming it in walk 1c and as undriven, and (a3) hiding the case address -> 8/40, walk 1c discovering ZERO call sites and FAILING rather than passing vacuously.

released: 2026-08-04 - landed on main by CONDUCT (merge of worker 3370514). Verified from the main checkout: harness 34/34 exit 0 read unpiped, both guards green with the seam set unchanged, arm B 803 answers over 66 ops, plancheck green. `app.html` untouched by the commit, so REC-40's concurrent edit was disjoint as predicted. THE SUBJECT MOVED AND WAS MADE TO SAY SO. Eight plane-sourced rows to eleven, annotated row by row, with the fixture-derived row LABELLED as fixture-derived rather than passed off as evidence about a live instance - that distinction is the difference between a measurement and a number. `manifest` is the one that changes the ruling's shape: DEC-49's subject now contains an English word the PLANE chose, not only hashes, paths and the login sentence. Carried into DEC-49's entry with the earlier readings kept for the record. UI-34's guard did exactly the job it was built for one item later. THE PREMISE I WROTE WAS WRONG AND THE CORRECTION IS BETTER THAN THE ITEM: the catch branch cannot print plane text, because the transport rejects only on transport failure - so the arm I asked to fail stayed green, character-identical. What is actually there is worse. `apiQ` does not throw on `ok:false`, so a REFUSAL renders as "No published part answers to that hash" to a stranger, on the surface whose whole purpose is verification without our cooperation. CONDUCT verified both ends independently: the plane's 400 refusal for a malformed hash, the transport's missing status check, and the container row composing an empty hash - reachable by a click. Routed as UI-37 and recorded as D-195. NOT FIXING IT HERE WAS RIGHT and is the discipline this chain has held throughout: DEC-49 is open and the surface must not move while its measurement is taken. Pinning the defect with an assertion that says at the site that it pins a defect is the correct way to leave it. Two instrument notes worth keeping: walk 1c FAILS when it discovers zero call sites rather than passing vacuously - the covered-on-paper lesson built in from the start rather than retrofitted after an arm exposed it - and the superseded-in-part pointer on UI-34's entry says explicitly that 'every plane-sourced row unchanged' is NOT true of the newer reading, which is the one sentence that stops a reader carrying forward a reassurance that has expired. No DEC.

## CLAIM 2026-08-04 UI

session: ui37-agent (worktree agent-a84825870368341a1)
opened: 2026-08-04T00:00:00Z
paths: **civicos-ui/app.html — THREE FUNCTIONS AND ONE NEW HELPER, named so a collision is a fact rather than a guess:** `pubVerify` (the item's subject — the refusal branch, split off the NOT-PUBLISHED branch, with the reasoning recorded at the site), `pubList`'s `apiQ("publishedmanifest")` result handling (the SAME collapse found by this item's required sweep: a refusal renders as "This group has not published any case files yet"), `pubOpen`'s `apiQ("publishedcase")` refusal branch (the same collapse in a milder form: an `<h1>Not published</h1>` heading asserted over ANY refusal, and only `detail` read while two of the three refusal arms carry `error`), and one new pure selector `planeSaid()` placed beside `errPane` that SELECTS the plane's own refusal words and composes none. Nothing else in app.html — no other function, no other string. civicos-ui/test/preauth-vocabulary.test.mjs (UI-36's defect pin CORRECTED at the site with a dated reason and never exempted; new arms for every refusal `op=verify` can emit and for the two sibling public reads; a NOT-PUBLISHED arm kept and strengthened so the two cannot collapse the other way either; `DEC49_SUBJECT` updated DELIBERATELY with every new row named; EXPECT_SURFACES extended; the header and the `NEGATIVE CONTROL:` line rewritten with the arms as run). civicos-ui/test/auth-surface.test.mjs — **AMENDED MID-ITEM, 2026-08-04, and the amendment is a finding rather than scope creep.** Its NEG-CONTROL (a) breaks the envelope seams and then asserted that the published list renders "This group has not published any case files yet" — so D-173's "honest-looking blank screen" WAS literally the sentence D-195 is about, and the two defects are one rendering. The assertion describes the BUG once the bug is gone and would have gone red for the fix, so it is CORRECTED at the site with a dated reason and never exempted (CLAUDE.md), and it is corrected to assert BOTH halves: the harm D-173 names is unchanged (neither case file reaches the reader) AND the broken surface no longer lies about it. One assertion becomes two; nothing else in that file is touched. It is inside UI's own area, so this is a claim amendment and not a delegation. docs/development/MEASUREMENTS.md (APPEND-ONLY: one new dated entry stating the new basis and the new plane-sourced rows, and a superseded-in-part pointer at the foot of UI-36's entry). docs/development/CLAIMS.md (this entry).
interfaces owned: none. **behind-interface: I3, CONSUMED not changed** — this item changes no op, no field, no shape and no wire string. `bio-plane/**` is READ textually for the plane's own refusal sentences and is written to by nobody here.
interfaces consumed: I3 (the op contracts). `op=verify`, `op=publishedmanifest` and `op=publishedcase` are read exactly as before; what changes is which of the plane's ANSWERS this surface is willing to render as a statement about the record.
concurrency: **REC-47 runs concurrently on `bio-plane/**` — DISJOINT, and named here.** This claim opens NO file under `bio-plane/`, `newgroup/`, `docprofile/`, `pdf-worker/`, `tools/` or `release/`. `bio-plane/src/index.mjs` and `bio-plane/src/store.mjs` are read TEXTUALLY by the suite (the plane's refusal sentence is extracted, never typed) and are not written. Under `civicos-ui/` this claim opens exactly two files, both named above.
explicitly NOT touched: `civicos-ui/check-semantics.mjs`, `civicos-ui/check-mock-envelope.mjs`, `civicos-ui/worker.template.mjs`, `civicos-ui/tokens.css`, every suite under `civicos-ui/test/` except the two named above, `bio-plane/**`, `newgroup/**`, `docprofile/**`, `pdf-worker/**`, `tools/**`, `release/**`, QUEUE.md, DECISIONS.md, DEBT.md, MILESTONES.md, INTERFACES.md, VERIFICATION.md, kickoffs/**. **DECISIONS.md is NOT written by this session** — DEC-49's entry is CONDUCT's to carry the new rows into; this session NAMES them in the commit message and in its report.
expected: QUEUE.md UI-37, D-195. The public verification surface renders a plane REFUSAL as a substantive negative. `pubVerify` calls `op=verify` through `apiQ`, which — unlike `rec` — does not throw on `ok:false`, so a refusal envelope carries no `published` field, falls to the else branch, and an uncredentialed reader is told "NOT PUBLISHED. No published part answers to that hash. A hash that was never ratified and a hash that never existed are the same answer here, deliberately." — a sentence whose second clause is TRUE of a real not-published answer and FALSE of a refusal, which is what makes it convincing. Reachable by a CLICK: the container row composes its hash as `bundle_sha || ""`.
accepts-when: as QUEUE.md UI-37. `node civicos-ui/test/run.mjs` green with the exit read UNPIPED, a refused `op=verify` rendering as a REFUSAL and never as "not published" — driven for the malformed-hash arm and every other refusal the sweep finds — **and a genuine not-published answer still reading as not-published**; UI-36's defect pin CORRECTED at the site with a dated reason; `DEC49_SUBJECT` updated DELIBERATELY with every new row named; both guards green; if `apiQ` changes, every caller swept with the sweep's reach asserted as a delta and `cd bio-plane && npm run test:battery` still green. NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — restore the collapse and the harness fails NAMING the refusal rendered as a negative. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report.

released: 2026-08-04 - landed on main by CONDUCT (merge of worker f51f8ae). Verified from the main checkout: harness 34/34 exit 0 read unpiped, both guards green with the seam set unchanged, battery 98/98 at 5,544, coverage strict run directly, plancheck green. CONDUCT read the corrected branch at the source: a three-way positive shape test, the honest clause moved to the branch where it is true, and a NOT ANSWERED branch that reports the state of the EXCHANGE rather than the state of the record. THE SWEEP IS WHY THIS ITEM WAS WORTH ITS SIZE: three refusals and three sites, not one - and the worst was not the one I routed. `pubList` rendered a refusal as a claim about the ENTIRE record. Sixth item running to find more than its brief predicted. THE FIX WAS DECIDED AGAINST THE ITEM'S OWN LEANING, on evidence: `apiQ` could not be made to throw, because §7a spreads a DO result into `{ok:true}` at HTTP 200 without checking it, so there is no `ok:false` to throw on - the motivating case would have sailed past. That reasoning also produced REC-52, which is the same defect one layer down and invisible to any surface. TWO OF THE THREE CORRECTED PINS ARE FINDINGS. UI-36's refusal arm had EVERY requirement backwards - it would have gone red for the fix and green for the bug - which is a warning about pinning a defect: the pin must be written to fail when the defect is FIXED, and this one was not. The not-published arm asserted a fixture's size rather than driving the surface. And the third unifies two rows: D-173's honest-looking blank screen was literally this defect's sentence, so the two are one rendering. The itemised plane wording the instrument CANNOT pin is exactly right and is carried into DEC-49's entry - a guard that silently understates the ruling's subject would be worse than no guard. Self-reporting that the bare wire code briefly reached the subject before the guard caught it, and that arm (r) fell silently from 4 to 1 when HEAD stopped being the pre-item state, are both the discipline this chain has held. D-197 renumbered at integration off a colliding D-195; the older row keeps the number. No DEC.
## RECORD — REC-47, the authored bias acknowledgement on the publish block

session: rec47-agent (worktree agent-ae65d9cd63a3699a5)
opened: 2026-08-05T00:20:00Z
paths: `bio-plane/src/schema.mjs` (the `published_cases` table gains ONE column, `bias_acknowledgement`, beside `scope` and `completeness` — a new COLUMN on an existing table, not a new table, so the `host_governor` ordering trap does not fire; plus the D-188 vocabulary correction in the `inquiry_basis.grade_source` comment block near line 1187), `bio-plane/checks/bio-checks.mjs` (`checkPublishedExtension` gains the entry requirement refused BY NAME; `checkCompletenessFreshness` — C-21.1 — gains the bias-acknowledgement arm AND the recorded reasoning for why this field IS byte-checked while REC-44's scope statement is NOT; the D-188 vocabulary correction in the `GRADE_SOURCES` comment), `bio-plane/src/store.mjs` (RECORD's own functions ONLY: `publishCase` — the authored parameter, the `NO_BIAS_ACKNOWLEDGEMENT` refusal, the C-21.1 pre-flight arm, the scalar written into the signed bytes; `publish` — the ratify-commit path, the `published_cases` write and the divergence refusal; the published-case READ paths that serve the case block; and the D-188 vocabulary correction near line 10759. NOT the link, capture, task or reachability functions, which are CAPTURE's), `bio-plane/src/index.mjs` (RECORD owns I3 and the dispatch: the ratified-bytes read of `bias_acknowledgement` and its pass-through to the store, and the container manifest's own field), `bio-plane/src/queuestate.mjs` (ONE line: the `bias-debt` obligation kind's description, which still states the PRE-DEC-20 blanket rule — see `expected` below), `bio-plane/test/publish.test.mjs` and `bio-plane/test/multifinding.test.mjs` (the arms, the corrected REC-14/REC-44 pins with dated reasons, and the `NEGATIVE CONTROL:` lines), `docs/architecture/BIO_Declared_Bias_v0_1.md` + the design docs carrying the D-188 wording (the DEC-46 (d) documentation pass), `docs/development/NOTIFICATIONS.md` (the same one-line drift as `queuestate.mjs`), `docs/development/DEBT.md` (D-188's row disposition ONLY), `docs/development/CLAIMS.md` (this entry)
interfaces owned: **I5 (the store schema) — one additive column on `published_cases`.** **I3 (the op contracts) — `op=publish` gains ONE REQUIRED authored parameter, `biasAcknowledgement`, and one new refusal name.** A newly required parameter is a BREAKING change to I3 by `INTERFACE-CHANGES.md`'s own test, and it is registered as such rather than as additive: a caller that publishes today without the field is refused tomorrow. That is DEC-46 (a)'s intent — the field is required on the publish block, not optional — so the breakage is the ruling, not a side effect.
interfaces consumed: none.
concurrency: **UI-37 runs concurrently on `civicos-ui/**` (D-195's fix, the public verification surface).** This claim opens **NOT ONE BYTE under `civicos-ui/`** — disjoint, stated by name. `civicos-ui` carries no occurrence of the D-188 wording (measured: `grep -ric "bias debt" civicos-ui/` returns nothing), so the vocabulary pass does not reach into UI-37's paths either. `newgroup/src/release.mjs` carries one occurrence inside its BUNDLED copy of `setup.mjs` and is deliberately NOT touched — hand-editing a bundled copy is how the copy and its source drift apart (D-21, and D-193 records exactly this for the same file); it refreshes at the next release cut. `bio-plane/dist/bio-plane.bundled.mjs` is a build artifact and is likewise not hand-edited.
explicitly NOT touched: `civicos-ui/**`, `newgroup/**`, `pdf-worker/**`, `docprofile/**`, `tools/**`, `release/**`, `bio-plane/dist/**`, QUEUE.md, DECISIONS.md, MILESTONES.md, INTERFACES.md, VERIFICATION.md, MEASUREMENTS.md. CAPTURE's functions in `store.mjs` and `schema.mjs`.
expected: QUEUE.md REC-47, enacting DEC-46 (a) and (d); a CORRECTION to REC-14 and a companion to REC-44. **Two things the brief did not predict and the sweep found, both routed rather than absorbed:** (1) **DEBT.md carries TWO rows numbered D-188** — the "bias debt" vocabulary row and a `op=bootstrap roles` row that was itself renumbered twice by CONDUCT (from D-184, then D-185) and landed on a third collision. Renumbering is CONDUCT's; this session reports it and touches only the vocabulary row's disposition. (2) **The `bias-debt` obligation kind states the PRE-DEC-20 blanket rule in TWO places that agree with each other** — `bio-plane/src/queuestate.mjs` and `docs/development/NOTIFICATIONS.md` both say it "blocks a transition", which is exactly what DEC-20 overruled and what D-188 predicts a reader will conclude. Corrected in both, and the agreement between them is why a copy that agrees today agrees at zero cost.
accepts-when: as QUEUE.md REC-47. `cd bio-plane && npm run test:battery` green with a published edition carrying an AUTHORED bias acknowledgement, a republish REFUSED when the acknowledgement is byte-identical to the previous edition's, and a missing acknowledgement refused BY NAME; `npm run test:hygiene` green; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0; any battery delta ATTRIBUTED per suite, never asserted. NEGATIVE CONTROL run and recorded in the suite's own `NEGATIVE CONTROL:` line — pass a carried-forward acknowledgement with the byte-check neutered and show the gate is a checkbox, exactly as the completeness arm's control shows. Every file restored byte-identically after each on-disk arm, sha256 compared before and after. REC-14's and REC-44's superseded pins CORRECTED with dated reasons, never exempted. No push — CONDUCT integrates. DECISIONS.md is not written by this session; QUEUE.md is not written at all.
landed: left for CONDUCT. Worker commits in this worktree (NOT pushed): `96a982b` (the item) and `af937b1` (four more vocabulary sites the final audit caught). MEASURED THIS TURN so nobody re-derives it.
  NUMBERS. `npm run test:battery` **98/98, 5541 -> 5567 assertions**, the +26 ATTRIBUTED per suite and not asserted: `publish.test.mjs` 74 -> 88 (+14), `multifinding.test.mjs` 58 -> 70 (+12). No other suite's count moved -- the nine suites edited for fixtures gained no assertions, which is what makes the attribution checkable. `npm run test:hygiene` 650. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped: **exit 0**, 130/130 ops, 53/53 checks, 267 -> 272 NC arms. `plancheck` clean but for UNPUSHED, which is correct -- CONDUCT integrates.
  **THE JUDGEMENT, MADE AND RECORDED AT THE SITE: the byte-check DOES apply, and REC-44's scope exemption stands beside it unchanged.** The discriminator is NOT "could this legitimately stay the same" -- that test cannot separate them, since a lens may sit unchanged for years and SHOULD. It is WHAT THE FIELD IS A CLAIM ABOUT: a FACT ABOUT THE CASE (scope; the unbuilt manifest) versus AN AUTHOR'S CLAIM ABOUT THIS EDITION'S MATERIAL (completeness, the subject justification, the acknowledgement). DEC-46 supplies it directly -- the manifest is *computed and stamped*, the acknowledgement is *authored*. The pressure-to-invent test does NOT fire, because nobody is asked to invent a change in their bias: "the lens is unchanged, and here is what it means for the findings added since edition 1" is a true sentence, and it is the same escape the completeness arm already offers in its own remedy line. And the failure modes are asymmetric -- a stale scope misdescribes the question, a stale acknowledgement asserts the publisher weighed their own lens against material they never looked at. Recorded at `checkCompletenessFreshness` beside the rule it discriminates from, because the two now sit side by side under different rules and the next reader will ask why.
  **NC RUN x3, all restored BYTE-IDENTICALLY** (sha256 equal before and after each arm: store.mjs 95332d64..., index.mjs 5202ffcb..., bio-checks.mjs a2be732c...), recorded in both suites' own `NEGATIVE CONTROL:` lines. **(e) the item's own** -- the bias byte-check neutered in BOTH gates with PRESENCE still required: publish **79/8**, the carried-forward acknowledgement PUBLISHES and the edition LANDS, which is the checkbox exactly; note the CASCADE, since the accepted republication consumes the edition and the legitimate one after it then fails too, so a reader taking only the first failure would misdiagnose it as an edition bug. **(f)** the catalog's entry requirement alone -> **87/1**, the gate-side arm the act cannot cover. **(g) CHANGED THE SUITE RATHER THAN CONFIRMING IT** -- the divergence clause dropped: the first run failed NAMING THE WRONG RULE (`CASE_MEMBERSHIP_DIVERGED`), because block 2b's adversary is not in the declared roster and *structurally cannot* isolate the acknowledgement. REC-16's recorded instrument defect arriving again. An ISOLATED adversary was built -- a LEGITIMATELY rostered member tampered in that one line and nowhere else, asserted at the site by comparing both images with that line removed from each -- and with the clause gone its ratification reports `reason: null`: **it succeeds**, so whichever member ratified first would silently decide what the public record says the case's lens was.
  **D-188'S SWEEP FOUND MORE THAN THE ROW PREDICTED, TWICE.** The row said "documentation pass, no code": FIVE code sites named the disqualifying rule with the disclosed term. And a second, worse class the row did not name -- **the pre-DEC-20 BLANKET RULE still asserted as live doctrine in SEVEN places**, two of them agreeing copies (`queuestate.mjs` + `NOTIFICATIONS.md`, both "blocks a transition", with the producer UNBUILT, so it would have been built to the sentence). FOUR of the seven were caught only by re-running the sweep AFTER the main commit rather than trusting the read that produced it. The one worth Bob's eye: `BIO_State_Rules_Consistency_v1_5.md` carries a cross-reference written in July EXPLICITLY so the state-rules corpus and the bias corpus could not drift apart -- and it went on asserting the blanket rule for three days after DEC-20 narrowed it. The row is the evidence for its own hazard.
  **FOR CONDUCT, NOT WRITTEN HERE:** (1) **DEBT.md carries TWO rows numbered D-188** -- the vocabulary row and an `op=bootstrap roles` row already renumbered twice by CONDUCT (D-184 -> D-185 -> D-188), landing on a third collision. Renumbering is CONDUCT's; only the vocabulary row's disposition was touched. (2) **I3 is BREAKING** (a newly REQUIRED `op=publish` parameter, plus two new refusal names) and I5 additive; the interface registration is CONDUCT's. (3) The container format moved `bio-case-container/2` -> `/3`, deliberately.

released: 2026-08-04 - landed on main by CONDUCT (merge of worker 7a25f67). Verified from the main checkout with every gate run UNPIPED: battery 98/98 at 5567 (exit 0 read directly), hygiene 650, coverage strict exit 0 at 130/130 ops, 53/53 checks and 272 arms, UI harness 34/34, plancheck green. THE BYTE-CHECK REASONING IS THE BEST WORK HERE and it improves on both items it sits between: REC-44's own test cannot separate scope from the acknowledgement, and the discriminator that can is WHAT THE FIELD IS A CLAIM ABOUT - a fact about the case versus an author's claim about this edition's material, which is DEC-46's computed-versus-authored distinction applied where it was needed. The asymmetric failure modes settle it: a stale scope misdescribes the question, a stale acknowledgement asserts the publisher weighed their lens against material they never looked at. Recording it beside the rule it discriminates FROM is exactly right. Arm (g) changed the suite rather than confirming it - the first run failed naming the wrong rule because the existing adversary structurally could not isolate the field, and the isolated adversary then showed the ratification SUCCEEDS with the clause gone, so whichever member ratified first would silently decide what the record says the case's lens was. That is the arm that earns the item. THE VOCABULARY SWEEP FOUND A SECOND CLASS THE ROW DID NOT PREDICT: five code sites, and the pre-DEC-20 blanket rule asserted as live doctrine in seven places - two of them agreeing copies whose producer is UNBUILT, so it would have been built to the overruled sentence. Four of the seven were caught only by RE-RUNNING the sweep after the main commit. Seventh independent measurement this session that a careful read undercounts. The self-reported process error is worth keeping: a battery piped into `tail` reported exit 0 while five suites had failed - CLAUDE.md's own warning, written into that file earlier the same day, hit by the next worker to run it. The fix was already in the file; what it needed was a reader, which is the argument for putting mechanisms in the loop rather than in the prose. CONDUCT registered what this item could not: I3 6.0.0 -> 7.0.0 via IC-21, with the UI proxy answer named in writing as CONDUCT answering FOR a dormant area. D-198 renumbered off the third D-188 collision - the CLOSED row moved rather than the open one, because renumbering a closed row costs two pointers and an open one costs every future reference. No DEC.

## CLAIM 2026-08-05 RECORD

session: rec52-agent (worktree agent-a27c76abd2896caf7)
opened: 2026-08-05T00:00:00Z
paths: **`bio-plane/src/index.mjs` — THE HANDLER-ENVELOPE LAYER ONLY, and the boundary is named rather than implied.** What this claim opens is the code that OPENS THE DURABLE OBJECT'S ENVELOPE and decides what a caller is told when the envelope says the store did not answer: one new chokepoint (`doAnswer` + `storeSilent`, placed beside `json()`), and at each caller-facing site the two or three lines that read `X.result` and hand it onward. It opens NO op's semantics, NO parameter, NO server-side stamp, NO field, NO wire string that an answering store produces, and adds/removes/renames NO op. Concretely converted: `op=verify`, `op=publishedmanifest`, `op=publishedbytes`' verify read, `op=publishedcase` (its own read AND its `publishedtargets` read), `op=knock`'s rate read, `op=bootstrap`, the session lookup, `op=affordances`' facts read, `op=queue`, `op=registeraudit`, `op=selftest`'s store probe, `op=runtime`, `op=cpuprobe`, `op=linkproject`, `op=governorstate`, `op=governorconfig`, `op=links` (both arms), `op=monitor`'s image read, `op=promote`'s echo. **EXPLICITLY NOT OPENED, and this is the concurrency boundary with REC-47: the publish/ratify block (`op=ratify`'s gatefacts/image/list reads, `op=publish`, `recordcasemanifest`, `reusedparts`, the capture-limit read beside them) and C-21.1.** Also not opened: CAPTURE's `op=acquire` body and the archive-fallback/governor helpers at the head of the file. Those seventeen sites are CLASSIFIED by the new sweep and held open in an exact list with a stated reason, not exempted and not edited. `bio-plane/test/plane-envelope.test.mjs` (NEW — this item's own suite, so nothing is added to another item's ground), `bio-plane/test/fixtures/do-fail-worker.mjs` (NEW — a worker module that subclasses the real `Store` to inject a Durable Object failure at a named DO path; it mutates no file on disk). `civicos-ui/test/preauth-vocabulary.test.mjs` — **ONE ASSERTION ONLY**, UI-37's `SEVENA_SPREAD` source pin of section 7a's spread, CORRECTED at the site with a dated reason and never deleted, exactly as UI-37 wrote it expecting. Nothing else in that file and nothing else under `civicos-ui/`. `docs/development/DEBT.md` (APPEND/annotate D-197's disposition only). `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts). **behind-interface: I3, and the question of whether this CHANGES it is answered rather than assumed: it does not.** No op's SUCCESS answer moves by one byte — every converted site returns exactly what it returned before when the store answers, which is the property the battery measures. What changes is the answer on a path that previously produced a FALSE SUCCESS or an INVENTED REASON, and a lie is not an interface commitment. One new refusal shape (`ok:false, reason:"STORE_DID_NOT_ANSWER"`) becomes reachable on ops that could previously only lie; it is additive and no consumer branches on it today (measured: civicos-ui's `pubVerify`/`pubList`/`pubOpen` use UI-37's positive shape tests, so it falls to their NOT-ANSWERED branch and renders the plane's own words, which is the branch UI-37 built for exactly this).
interfaces consumed: none.
expected: QUEUE.md REC-52, D-197 one layer down. The plane converts its own failures into substantive negatives. Two sites named by the item; the sweep is the item.
accepts-when: as QUEUE.md REC-52. `cd bio-plane && npm run test:battery` green at 98/98 with any delta ATTRIBUTED per suite and not estimated; a DO failure reaching a caller AS A FAILURE at both named sites and at every site the sweep found; `op=publishedcase` never synthesising a reason the DO did not send; a source-level assertion that no handler spreads a DO result without checking `ok`, with its own REACH asserted as a DELTA; UI-37's textual pin corrected at the site; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — the defect restored at each site and the suite failing NAMING the success envelope that carried nothing and the reason that was invented; **and separately a GENUINE not-published answer still reading as not-published**, because these must not collapse the other way either. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. QUEUE.md is not written at all.
concurrency: **REC-47 runs concurrently on `bio-plane/**` and the boundary is named above rather than hoped for.** REC-47 holds the publish/ratify block, C-21.1, and the `publish`/`publishedcase`/`reevaluation` suites. This claim writes NONE of those three suites — its assertions live in a new file of its own — and writes no line inside the publish/ratify block. Both claims have reason to touch `index.mjs`; this one is confined to the handler-envelope layer and says so site by site above, so a collision is a fact rather than a guess.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. **ELEVEN caller-facing sites where the item named two**, plus five writes/measurements and two that crashed rather than lied. Battery 99/99 at 5,593 (98/98 at 5,544 on main today; the +49 is ATTRIBUTED and not estimated — 46 from this item's own new suite and exactly 3 from hygiene.test.mjs, which applies three per-suite assertions to any new file, measured by diffing the per-suite counts: NO other suite moved by one assertion). Coverage strict run DIRECTLY with $? read unpiped, exit 0, 130/130 ops and 53/53 checks. NC RUN x9 with every file restored byte-identically and sha256 compared. Arm (g) had to be CORRECTED MID-RUN and is reported rather than smoothed.

released: 2026-08-04 - landed on main by CONDUCT (merge of worker 39be243). Verified from the main checkout with every gate unpiped: battery 99/99 at 5,616 exit 0, coverage strict exit 0 at 130/130 ops and 281 arms, UI harness 34/34, plancheck green. CONDUCT read the chokepoint at the source: `answered` is `ok === true` and nothing else, 502 rather than 500 because the control plane is intact and the store behind it failed, and the op named without the answer implying anything about what it was reading. ELEVEN SITES WHERE THE SCOPE NAMED TWO, and three of the nine are worse than either I routed: `op=publishedmanifest` fills the published INDEX, so its lie was about the whole record; `op=affordances` decides what acts a member is OFFERED; and `op=publishedbytes` was minting D-197's own sentence in the plane - the wording that item called unusually convincing, being manufactured one layer below the surface that was blamed for it. The second measurement is what makes the attribution trustworthy: with only the plane fix and no new suite the battery was UNCHANGED at 5,544, which is direct evidence no success path moved. Removing every guard from a copy of the source to drive detector A from 0 to 8 is reach asserted against the REAL defect at every REAL site rather than a planted specimen. ARM (g) IS THE INSTRUMENT LESSON OF THIS LANDING: the first sweep-neutering arm stayed fully green because with all guards removed there is no identifier left to scope on, so a whole-file region equalled a scoped one and the arm measured nothing. The paired correction - one guard removed, then the bound neutered - makes the bound's value the delta between them. Self-reported rather than smoothed, as was a statement-finder wrong in both directions on first write. The corrected pin is the trap UI-36 fell into whole: UI-37's spread pin REQUIRED the defect to persist, so it would have been red for the fix and green for the bug. Polarity inverted, never deleted. That is now twice in three items, and worth watching whenever anything is pinned as currently-wrong. REC-53 routed for the two live instances inside the publish/ratify block - `do/list`'s turns a store silence into a ratification refused for a reason ABOUT THE RECORD, which is the worst reachable form of this class. And a correction to our own standing instructions that I had been repeating in every brief: store.mjs is 16,287 lines, not ~4,900. Fixed in CLAUDE.md and the RECORD kickoff. No DEC.

## CLAIM 2026-08-05 RECORD

session: rec53-agent (worktree agent-af46d218e2cbdb08c)
opened: 2026-08-05T12:00:00Z
paths: **`bio-plane/src/index.mjs` — THE PUBLISH/RATIFY BLOCK ONLY (`op === "ratify"`, the block REC-52's claim explicitly left closed), and only its Durable Object envelope reads.** Every one of the eight DO reads inside that block is converted onto REC-52's existing chokepoint (`doAnswer`/`storeSilent`); no local guard is written, no op semantics move, no parameter, stamp, field or wire string an ANSWERING store produces changes, and no op is added, removed or renamed. Concretely: `do/gatefacts`, `do/image`, `do/list`, `do/publish`, `do/recordcasemanifest`, `do/reusedparts`, the `do/capturelimit` read beside it, and the `do/recordreuseverdicts` write. Nothing outside `if (op === "ratify")`. `bio-plane/test/plane-envelope.test.mjs` — REC-52's own suite: its `UNCONVERTED` set updated as the sites leave it, its two HELD-OPEN relation pins (i) and (ii) CORRECTED AT THE SITE with a dated reason and never deleted, and detector B widened to the two fallback shapes this block used that it could not see. `bio-plane/test/ratify-envelope.test.mjs` (NEW — this item's driven suite, so nothing is added to another item's ground; it drives a REAL signed ratification against a REAL Durable Object failure through REC-52's `fixtures/do-fail-worker.mjs`, which it does not modify). `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I3 IS NOT CHANGED, and the question is answered rather than assumed:** `op=ratify`'s SUCCESS answer does not move by one byte when the store answers — that is what the unchanged `ratify.test.mjs` and `reuse-ratify.test.mjs` counts measure. What changes is the answer on a path that previously produced a ratification refused for a reason ABOUT THE RECORD, an invented `PUBLISH_FAILED`/`MANIFEST_NOT_RECORDED`, or a silent omission that read as a fact about what the group did. A lie is not an interface commitment. The already-additive `STORE_DID_NOT_ANSWER` refusal REC-52 introduced becomes reachable on `op=ratify`; no new refusal name is minted. I5 is NOT touched: no table, column or index changes, and `src/store.mjs` is not opened at all.
interfaces consumed: none.
expected: QUEUE.md REC-53. Two live instances of REC-52's class inside the publish/ratify block, and the block swept for the class rather than the two fixed.
accepts-when: as QUEUE.md REC-53. `cd bio-plane && npm run test:battery` green — 99/99 at 5,616 on main today (MEASURED in this worktree before any edit), any delta ATTRIBUTED per suite and not estimated — with a store silence at `do/list` reaching the caller AS A FAILURE and never as a ratification refusal about the record, and the same at `do/reusedparts`; REC-52's two relation assertions corrected at the site with a dated reason, never deleted, and their POLARITY checked (they are pins on a defect and must fail when it is FIXED — UI-36 wrote one backwards whole and REC-52 found UI-37 had done the same, which is twice in three items); `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — each defect restored and the suite failing NAMING the refusal that was invented, IN THE DEFECT'S OWN WORDS; and separately a GENUINE unresolved reference still refusing, because these must not collapse the other way either (REC-52's arm (f) shape). The sweep's reach proved REC-52's way: the real guards mechanically removed from a copy of the real source, so reach is measured against the real defect at every real site rather than against a planted specimen. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. QUEUE.md is not written at all, and DECISIONS.md is not written by this session.
concurrency: no other worker is claiming `bio-plane/**` right now (checked: every RECORD and UI claim above this one is `released:`). REC-47's hold on the publish/ratify block — the reason REC-52 left these two sites alone — has landed, so the ground is free, which is what REC-53's `depends-on` line already states.
landed: left for CONDUCT. Worker commit in this worktree (NOT pushed): see the report. **EIGHT Durable Object reads in the publish/ratify block where the item named TWO**, and the sweep is the item. Beyond the two named: `do/image` — `runGate` does `Object.entries(image || {})`, so a silence gated an EMPTY BUNDLE and the publisher was told their document is missing its required files, which is `do/list`'s defect one field earlier; `do/publish` SYNTHESISED `PUBLISH_FAILED` at the COMMIT, asserting the ratification did not publish when the plane cannot know whether it did, and it is a TERNARY fallback which is why REC-52's detector B could not see it; `do/recordcasemanifest` synthesised `MANIFEST_NOT_RECORDED` — a claim that the published record does not hold this case's container — INSIDE an `ok:true` ratification answer, reaching the caller in a LOCAL VARIABLE spread twelve lines later, so detector A could not see it either; `do/capturelimit`'s silence left `observed` null, which is ALSO what "nothing calibrated yet" answers, so every not_attempted part's basis said the budget was "bounded by the calibrated subrequest ceiling none observed" about a ceiling nobody read — **and that sentence is WRITTEN INTO THE RECORD** by the verdict write below it; `do/recordreuseverdicts` was FIRE-AND-FORGET, so a silence left the verdicts out of the record while the answer handed the caller their outcomes under "every reused part carries an outcome", and being unread it was invisible even to detector C, which only sees a body that is CONSUMED; and `do/gatefacts`, the mildest, which threw a TypeError and answered a raw V8 stack trace to a publisher. **NONE MEANT SOMETHING DIFFERENT — all eight are this class**, which is why nothing is held open this time.
  **THE ONE JUDGEMENT, made and recorded at the site, because op=ratify is the only handler in the file with a COMMIT IN THE MIDDLE OF IT.** Sites reached BEFORE `do/publish` commits REFUSE with `storeSilent`: nothing has been written and 502's own sentence ("nothing here is a statement about the record") is exactly true. Sites reached AFTER may NOT refuse, because a ratification genuinely LANDED and that sentence would then be false in the other direction — denying knowledge we have is the same overclaim wearing modesty. So the post-commit sites keep the true `ok:true` answer and STATE THE UNDETERMINED PART IN ITS OWN FIELD, which is CLAUDE.md's "undetermined is first-class and must be STATED" applied to the half of an act that did not answer. Every post-commit conversion is BYTE-IDENTICAL ON THE WIRE when the store answers — the new fields appear only on the path that previously lied — and the per-suite delta is the evidence: ratify.test.mjs, reuse-ratify.test.mjs and publishedcase.test.mjs did not move by one assertion.
  NUMBERS. Battery **100/100 at 5,661** (99/99 at 5,616 measured in this worktree before any edit). The +45 is ATTRIBUTED PER SUITE by diffing the per-suite counts of a pristine run against the final one, not estimated: `ratify-envelope.test.mjs` +35 (new), `plane-envelope.test.mjs` 46 -> 53, `hygiene.test.mjs` 420 -> 423 (its three per-file assertions over the new file, REC-52's exact figure). **NO OTHER SUITE MOVED BY ONE ASSERTION.** `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped: **exit 0**, 130/130 ops, 53/53 checks, 100/100 suites declaring a control, 294 arms (281 before).
  **THE TWO RELATION PINS CORRECTED AT THE SITE, AND THE POLARITY IS THE POINT.** REC-52 wrote HELD OPEN (i) and (ii) to hold a DEFECT open, so as written they REQUIRED THE DEFECT TO BE PRESENT — `listSite` matched `.result || []` on the raw envelope and `reusedSite` matched the raw `do/reusedparts` read. Left alone they would have gone **RED FOR THE FIX AND GREEN FOR THE BUG**, which is the trap UI-36 wrote whole and REC-52 then found UI-37 had repeated. Inverted with the date and the reason, never deleted, and VERIFIED by running exactly that (arms (a) and (b)). A THIRD arm was added beside (i) for the other direction: the `|| []` must survive BEHIND the guard, because a viewer who genuinely sees no bundles is a real answer and treating it as a silence is REC-52's arm (f) collapse reversed. HELD OPEN (iii) (`governoradmit` fails open by an explicit written decision) is unchanged. Detector C's UNCONVERTED set lost six paths; **`capturelimit` deliberately STAYS on it** — op=ratify's read is converted but `op=acquire` reads the same path in CAPTURE's ground and the set is keyed by PATH, so removing it would have been the easy wrong edit.
  **AN INSTRUMENT ADDED BECAUSE ITS OWN NEGATIVE CONTROL EXPOSED THE GAP, reported rather than smoothed.** Arm (e) deletes `do/publish`'s guard line and `plane-envelope.test.mjs` stayed **51/51 FULLY GREEN** while the live drive reported `PUBLISH_FAILED` for a store that never answered. Detector D proves an envelope is OPENED through the chokepoint and says NOTHING about the handler ACTING on the answer; detector A is correctly silent because `pub` is spread into a REFUSAL, not a success envelope. **DETECTOR D2** — every identifier bound from `doAnswer(` in the block must be read with `.answered` in the block — closes it, with its reach asserted as a delta against the REAL guard at the REAL site. Its own limit is stated by arm (h): D2 passes when the branches are SWAPPED, because the binding is still read, only in the wrong order, and only an ORDERING assertion bites.
  NC RUN x11, every file restored BYTE-IDENTICALLY with sha256 compared before and after all of them (index.mjs 8b8515b4..., plane-envelope.test.mjs 163292ca..., ratify-envelope.test.mjs 7d918019..., do-fail-worker.mjs 77be0338...), and ALL ELEVEN re-run against the FINAL files so every count in both suites' NEGATIVE CONTROL headers agrees with the file it names. **(a)** `do/list` restored -> ratify-envelope 31/4 and plane-envelope 47/6, the drive naming the invention IN THE DEFECT'S OWN WORDS (`got 409 "GATE_REFUSED"`, and the arm asserting the catalog's "does not resolve in the store" appears NOWHERE fires) on TWO independent instruments. **(b)** `do/reusedparts` restored -> 33/2 and 48/5, `got undefined` for the key that should be present. **(c)** `do/image` -> 34/1 and 50/3. **(d)** `do/gatefacts` -> 34/1 and 50/3, the defect quoting itself: `got 500 "TypeError: Cannot read properties of undefined (reading 'ok'"`. **(e)** as above -> 34/1 and 51/2. **(f)** `capturelimit` -> 33/2 and 52/1. **(g)** `recordreuseverdicts` -> 33/2 and 50/3, with the store really holding 0 verdicts. **(h)** `recordcasemanifest`'s branches swapped -> 33/2 and plane-envelope SILENT. **(i) THE OTHER DIRECTION** — a genuinely EMPTY reused-part set treated as a silence, one clause from what a careless guard would write -> 34/1 and 52/1. **(j) THE INSTRUMENT'S OWN** — the injector disarmed -> **21/14 and 36/17**, REC-52's own seventeen reproduced exactly, which is what proves these drives answer an ACTUAL Durable Object failure. **(k) THE SWEEP'S OWN** — detector D's region bound neutered -> 35/35 untouched and 50/3, D reporting 23 violations ALL OUTSIDE the block.
  **A STATED LIMIT RATHER THAN AN EXEMPTION**, and it is reported as weaker than a drive: site 6 (`recordcasemanifest`) is the one site whose branch needs a COMPLETE PUBLISHED CASE to reach (`pub.case && pub.case.complete && !pub.case.manifest_sha`) — a roster of three enrolled members, two concluded inquiries and an authored case declaration. Seven of eight are DRIVEN against a real Durable Object failure; this one is pinned structurally (the silence branch is evaluated FIRST, the fallback survives BELOW it, and the R2 write is gated on the read having been answered) and arm (h) is what makes those pins real.
  A SECOND CORRECTION TO THIS SUITE'S OWN HARNESS, reported: `RAT` now parses the body DEFENSIVELY, because arm (d)'s defect answers a raw V8 stack trace rather than JSON and a helper doing `await r.json()` DIED on it — hiding every arm after it (D-93). Reading the text first is what lets the arm name the defect in its own words.

released: 2026-08-04 - landed on main by CONDUCT (merge of worker 27fa9ed). Verified from the main checkout with every gate unpiped: battery 100/100 at 5,661 exit 0, coverage strict exit 0 at 130/130 ops and 294 arms, plancheck green. THE COMMIT-IN-THE-MIDDLE JUDGEMENT IS THE ITEM and it is right: `op=ratify` is the only handler with a commit inside it, so pre-commit reads refuse through the chokepoint and post-commit reads may NOT - the ratification genuinely landed, and 502's own sentence would be false in the other direction. Keeping the true answer and stating the undetermined part in its own field is the honest shape, and every post-commit conversion being byte-identical on the wire when the store answers is the evidence it costs nothing. EIGHT SITES WHERE TWO WERE NAMED, and three of the six extras are worse than the two I routed: a raw V8 stack trace answered to a publisher; a publisher told their document is missing its required files; and a capture WRITTEN INTO THE RECORD as bounded by a ceiling nobody read. Eighth item running to find more than its brief predicted - that is now the expectation rather than the exception, and the briefs say so. ARM (e) IS THE INSTRUMENT FINDING: removing a guard left the sibling suite fully green, because the detector proved an envelope was OPENED through the chokepoint rather than that the handler ACTED on it. Closing that with a second detector rather than reporting it was the right call, and stating its own limit with a further arm is what keeps it honest. The two pins REC-52 left holding these defects open required the defect to be PRESENT - red for the fix, green for the bug. Third correction of that shape in four items. Inverted, never deleted, verified by their own arms, with a third arm added for the other direction. No DEC. CONDUCT separately enacted DEC-55 this turn, which VERIFIES REC-46's value from an unexpected direction: the machine fences generalise to a future `ai` credential class by construction, and only because that refactor landed the same day.

## CLAIM 2026-08-05 DIST

session: dist-session-1 (main checkout)
opened: 2026-08-05T13:00:00Z
paths: `bio-plane/scripts/deploy.mjs` (the slug guard only — D-201's one refusal), `release/**` (RELEASE.json and the signed asset), the version in `bio-plane/package.json` and `bio-plane/wrangler.jsonc`, `newgroup/**`, all tags, `docs/development/DEBT.md` (the D-201 row's status only), `docs/development/kickoffs/BATON.md` (the BATON-STATE block and the Log), `docs/development/CLAIMS.md` (this entry).
interfaces consumed: I3 (the op contracts) as a CALLER only — live-verify probes, nothing reshaped.
interfaces owned: none changed. A release cut publishes existing shapes; it does not move one.
expected: **Cut the 0.56.0 release that was deployed but never released.** `BIO_RELEASE_SEED` was recovered on this machine and VERIFIED against the pinned trust root (see below), so the blocker DIST-NEXT.md names is closed without a key rotation. Gate per `kickoffs/DIST.md`: full battery + installer suite on merged `main`, version sources already agreeing at 0.56.0 (no bump needed — hygiene's D-106 check is green), build, sign with the four negative controls, write `release/RELEASE.json`, re-cut and deploy the installer, read it back and confirm the embedded version and `bindings: []`, tag and push. Plus **D-201** — `deploy.mjs` refuses the `civicos` slug by name.
seed provenance, since it is the item's precondition: the private half was found at `~/Downloads/bio-signing-keys.txt`, and it is the RIGHT key rather than a plausible one — deriving the Ed25519 public half from the seed reproduces `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIGfzETopBeZe5mbD7ukYwaZczyBPjJ4S3sX+Ly3rN3Vl bio-release` **exactly**, which is the value pinned in `newgroup/src/index.mjs` `ARMED_SIGNERS` and named as `signer` in the 0.55.0 `RELEASE.json`. So no rotation, no `ARMED_SIGNERS` edit, and every installer already distributed keeps working. Loaded into gitignored `.env`; the value was never printed and never entered a prompt, a transcript or a tool call.
released:
## CLAIM 2026-08-05 UI

session: ui25-agent (worktree agent-ab6f43b6a6e6ceea6)
opened: 2026-08-05T13:00:00Z
paths: **`civicos-ui/app.html` — the FINDER region (`renderFinder` … `/*__FINDER_END__*/`), the CITE FLOW region (`citeDriftHtml` … `citeDone`), and the REVIEW region (`loadReviewRows`/`paintReview`).** No other region of the file is opened. `civicos-ui/test/finder.test.mjs` (this item's driven suite). `civicos-ui/test/cite-act.test.mjs` (the cite flow's own suite — the query-selection arm and any pin whose polarity the new shape moves, corrected at the site with a dated reason, never exempted). `civicos-ui/test/action-page.test.mjs` (batch rider: the `RESOLUTIONS` literal and its stale comment). `civicos-ui/test/auth-surface.test.mjs` (batch rider: the `PLANE_WORDS` structural tie). `civicos-ui/test/release-flow.test.mjs` (WIDENED mid-item: the sweep found the review list presenting a PAGE as the whole of what awaits review, and this is the suite that owns that screen). `docs/development/CLAIMS.md` (this entry).
interfaces owned: none. This area OWNS no interface.
interfaces consumed: I3 (the op contracts). **I3 IS NOT CHANGED AND NO PLANE FILE IS OPENED FOR WRITING.** `op=select` already accepts `kind:"query"` (`store.mjs` `selectionCreate`, the criterion stored rather than the rows, uncapped) and `op=selection` already publishes `drift.digestChanged` and its own `detail` for that kind (`selectionResolve`). This item CONSUMES both for the first time. `bio-plane/**` is read — textually, and by import where a module carries no `cloudflare:workers` binding — and never written.
expected: QUEUE.md UI-25. The finder offers a HOLD-EVERYTHING (query) selection beside the page lease; `digestChanged` renders as its OWN fact and never blurred into per-row drift; the cite flow consumes either. Plus the class sweep: wherever else a surface presents a BOUNDED result in a way a member could read as COMPLETE.
accepts-when: as QUEUE.md UI-25. `node civicos-ui/test/run.mjs` green with a finder harness holding a query selection whose criterion survives past 500 hits, its digest drift rendered as its own fact, and a cite consuming it; `cd bio-plane && npm run test:battery` stays green — **100/100 at 5,664 MEASURED IN THIS WORKTREE before any edit** — any delta ATTRIBUTED per suite and never estimated; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped. NEGATIVE CONTROL RUN, not described, and recorded in the suite's own `NEGATIVE CONTROL:` line — cap the query selection at the page and the harness FAILS NAMING THE COMPLETENESS BOUND. Every assertion's POLARITY checked (a pin on a known defect must FAIL when the defect is FIXED — corrected three times in four items). Every file restored byte-identically after each arm, sha256 compared before and after. No push, no merge — CONDUCT integrates. QUEUE.md and DECISIONS.md are not written by this session.
concurrency: no other worker is claiming `civicos-ui/**` right now (every UI claim above this one is `released:`). The plane is read-only from here, so the open RECORD ground is not contended.
released: 2026-08-05 · CONDUCT at integration — merged as 4e81fb2 (worker fb0dadd). Verified FROM THE MAIN CHECKOUT, not from the worker's report: battery 101/101 at 5,733 — the +1 over the worker's 5,664 baseline ATTRIBUTED to DIST's D-202 debt row via planning-hygiene 236 -> 237, checked rather than assumed, and UI-25 itself moved the battery by ZERO, which is the evidence no plane file was opened. UI harness exit 0 with the seam set unchanged at 66; coverage --strict run DIRECTLY, $? unpiped, exit 0. Delegation routed to REC-55 (the refuse gate, mechanism decided at integration); the four reported-not-edited bound sites routed to UI-39.
  **THE ITEM.** `op=select&kind=query` gets its first caller. The finder offers HOLD EVERYTHING THIS QUERY MATCHES beside the page lease; the criterion sent is byte-identical to the one `op=search` was asked (taken off the route's own answer, never recomposed); no ids ride with it. **NO PLANE FILE WAS OPENED** — battery 100/100 at 5,664, byte-identical to the pre-edit measurement in this worktree, which is the evidence rather than the claim.
  **THE DRIFT SEMANTICS, AND THE REASON THEY NEEDED A SEPARATE READ.** The plane computes `moved = revised.length + removed + added > 0` and `digestChanged` is NOT a term in it — so a query selection whose answer swaps one document for another AT A CONSTANT COUNT reports `moved:false` while the answer is not the answer the member holds. `selectionMoved` reads `digestChanged` on the query arm; `selectionDriftHtml` is ONE function shared by the lease card and the cite receipt (UI-27's precedent), naming every row on the enumerated arm and NO row on the query arm — with the absence STATED ("there is no list here because there is none to show — not because nothing changed") rather than left to read as "nothing moved". That relation is pinned in the suite against `store.mjs`'s own formula, and pinning a relation asserts no value.
  **FOUR SITES OF THE CLASS BEYOND THE ONE THE BRIEF NAMED, all in `app.html`, all "the plane published a bound and the surface dropped it".** (1) THE REVIEW SCREEN — `loadReviewRows` discarded `r.total` and `paintReview` printed `REVIEW_ROWS.length` as a bare figure in "Awaiting review (N)", directly above a select-all box and a bulk Release that writes a member's name onto a state transition. Fixed and driven. (2) `finderTextRoute` FELL BACK TO `hits.length` when the plane sent no numeric total — the one thing its own comment forbids, and on exactly the path where the record declined to say how many there were; `total` is now null-when-unsaid and the panel states the unknown. (3) the plane's published `limit` was captured into `t.limit` and READ NOWHERE; it now drives an at-the-limit bound the total-based test could never see. (4) THE OVERLAP was an intersection over a capped text side labelled "the honest third figure", ninety lines below this screen's own refusal to compute a capped intersection — the qualification now travels with the word.
  **FOUR MORE FOUND AND REPORTED RATHER THAN EDITED** (outside this claim, each a routable item): `loadResolveCandidates` is capped at 100 by the plane and its long bound sentence states only SEMANTIC bounds, never the numeric one; `heldMatch` searches at the plane's default of 50 and DECIDES WHETHER TO WRITE A DUPLICATE BUNDLE on that read; `op=queue`/`op=tasks` are capped at 200 with `counts` published and unread; and `lookupSubject` makes an affirmative FALSE statement ("a name that is not here has not been entered yet") that the finder states correctly forty screens away. The last is deliberately not drive-by-reworded while DEC-49 is open.
  **RIDER 1 CLOSED:** `RESOLUTIONS` joins the UI-24 tie — REC-39 exported it from `bio-checks.mjs`, `store.mjs` imports it and `affordances.mjs` publishes it, so all three clauses of the old "no published home" note were false and are corrected with a dated reason. Pinned by name, pinned as a WHOLE set (the `ACTION_KINDS` subset lesson), and the enforcement/publication identity pinned as a relation. action-page 95 -> 99.
  **RIDER 2 CLOSED BY MEASUREMENT, NOT BY EDIT:** UI-30 had already made `PLANE_WORDS` structural on 2026-08-04. The control REC-39 ran (strip every `detail:` off the plane's login refusal arms) is RE-RUN and recorded as arm (f) in the suite's own header: it moved NOTHING at 62 then and fails 2 of 74 now, `store.mjs` restored byte-identically. "Someone else already fixed it" is a claim; running the control is the evidence.
  NUMBERS. UI harness **exit 0**, 34 suites + both guards; finder 85 -> **132**, action-page 95 -> **99**, auth-surface **74**, release-flow green with the review bound driven; arm B 806 -> **833** op answers, 151 -> **152** op/suite pairs, SEAM SET AND OP SET UNCHANGED at 66. Battery **100/100 at 5,664, byte-identical to baseline**. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped: **exit 0**, 130/130 ops, 53/53 checks.
  NC RUN x7 (d)-(j), every file restored byte-identically with sha256 compared: app.html 1fdb2741…, finder.test.mjs 70069885…, store.mjs 95332d64… . (d) the item's own -> **25 of 132**, naming the completeness bound in its own words, while `cite-act.test.mjs` stays GREEN. (e) blur the two drifts -> 3 of 132. (f) per-row language over a criterion -> 4 of 132. (g) drop the page bound -> 2 of 132. (h) the review screen -> fails quoting `Awaiting review (2)` over a record answering 731. (i) **GREEN FIRST TIME — an instrument finding kept**: the ids guard was fed `[]` by its only caller and passed at zero cost; the suite now drives it as the caller that does not exist yet and it bites at 1 of 132. (j) the fixture's own -> exactly the 4 page-bound assertions, and the 128 that stay green are the honest reading.
## CLAIM 2026-08-05 RECORD

session: rec54-agent (worktree agent-a32b4dbcb4e161da8)
opened: 2026-08-05T18:00:00Z
paths: **`bio-plane/checks/bio-checks.mjs` — `checkAuthorityPublishable` (C-18.9) ONLY**, and within it only the chain arm: the single collapsed finding for a missing chain is SPLIT into two distinct findings (no chain RECORDED versus a chain recorded and EMPTY), and the repair advice is corrected where it names a transition the state machine does not have. **The check is NOT weakened — every input that errored before still errors**, which is asserted rather than claimed. No other check function is opened. **`bio-plane/src/store.mjs`** — RECORD ground, named by function rather than by file: a NEW pure derivation `#chainFromEvidence` + `provenanceChainRebuild` (new, no existing method's behaviour moves), the `release()` entry-requirement block (ONE added requirement, the chain, beside the three C-2.7 already there), and `auditPass()`'s tally shape ONLY where it must carry the split finding. **NOT opened: `promote` itself, the capture/link/task/reachability functions (CAPTURE's), the projections, membership, selections, the alarm.** **`bio-plane/src/index.mjs`** — the OPS table entry and the dispatch block for ONE new op (`provenancechain`), and nothing else in the file; no existing op's semantics, parameter, stamp, field or wire string moves. `bio-plane/test/provenance-chain.test.mjs` (NEW — this item's own suite, so nothing is added to another item's ground). `docs/development/MEASUREMENTS.md` (APPEND this item's measurements only). `docs/development/DEBT.md` (APPEND the rows this item opens and cannot close). `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I5 IS NOT CHANGED: no table, column or index is added, altered or dropped.** The reconstruction writes into `data/provenance.json`, which is a FILE in the bundle image, through the existing `promote` write path — so there is no new derived table and nothing to add to `op=purge`'s whole-store arm (D-113). **I3 GAINS ONE OP** (`provenancechain`, additive) and it carries a control-plane assertion in this same turn, per `VERIFICATION.md` step 2a. No existing op's SUCCESS answer moves by one byte; `op=release` gains ONE new named refusal (`ENTRY_REQUIREMENTS` grows a named missing item, the shape it already uses for the three it checks today), which is additive within an existing refusal.
interfaces consumed: none.
expected: QUEUE.md REC-54, D-200. Ten live bundles claim a provenance route they cannot show. The item is the SWEEP FOR THE CLASS, not the ten.
accepts-when: as QUEUE.md REC-54. `cd bio-plane && npm run test:battery` green — 100/100 at 5,664 on main today, to be MEASURED in this worktree before any edit and any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated; the ten live bundles dispositioned INDIVIDUALLY with the evidence for each; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line: (1) hand a bundle a chain the capture record does not support -> the suite FAILS naming the invention; (2) weaken C-18.9 to tolerate a missing chain -> the suite FAILS naming the check just neutered, **and the pin must go RED for the weakening and GREEN for a genuine fix, not the reverse**; (3) a document legitimately BELOW `verified` with no chain still reads CLEAN, because these must not collapse the other way; (4) the live-bundle walk neutered and its REACH assertion failing **as a DELTA**, never against an absolute. Every file restored byte-identically after each arm, sha256 compared before and after. No push — CONDUCT integrates. QUEUE.md is not written at all, and DECISIONS.md is not written by this session.
concurrency: no other worker is claiming `bio-plane/**` right now (checked: every RECORD and UI claim above this one is `released:`).
released: 2026-08-05 · CONDUCT at integration — merged as f53fc41 (worker db34376). Verified FROM THE MAIN CHECKOUT: battery 101/101 at 5,733, the +68 over main's 5,665 attributed per suite (provenance-chain +63 new, hygiene 423 -> 426, planning-hygiene 237 -> 239); coverage --strict run DIRECTLY, $? unpiped, exit 0 at 131/131 ops; UI harness exit 0. **CONDUCT RE-RAN TWO NEGATIVE CONTROLS ITSELF**, this being security-sensitive (inventing provenance, and the release entry gate): arm (a) the invention arm reproduces EXACTLY at 56 pass / 7 fail, refusing the evidence-free document by name; arm (e) the release write-path arm bit at 59/4 rather than the reported 60/3, because CONDUCT's arming was BROADER — it also removed the source text a structural pin reads — which is a difference in how the arm was armed rather than in whether the guard works, and is recorded rather than smoothed. store.mjs restored byte-identically after each, sha256 compared. D-200 STAYS OPEN: the dispositions are determined and evidenced but NOT applied to the live record, because op=provenancechain is not in the serving 0.56.0 build — routed as a DIST delegation. D-203 routed to REC-56; D-204 raised as DEC-56 (doctrine, Bob's).
  **(a) THE WRITE PATH IS REAL AND IS THE DEFECT.** `runGate` — the only thing that runs the check catalog — has exactly ONE call site, `op=ratify`. `op=release`, the OTHER way an Information document reaches `verified`, hand-checked three of C-2.7's entry requirements and never asked C-18.9's question at all. Closed at that layer, asserted at that layer (VERIFICATION.md 3a), and negative control (e) proves the catalog arm cannot see it.
  **(b) FIVE CHECKS ADVISE A REPAIR A SIXTH REFUSES**, and this is the sweep finding. `checks/bio-checks.mjs` tells an operator five times to "return the bundle to collected"; `STATES.information.edges.verified` is `["retired"]` and C-4.2 fires `transition verified -> collected is not a legal information edge`. **So D-200's own second disposition — move the document back off `verified` — HAS NO ROUTE THROUGH THE PLANE.** REC-54 corrected the one inside C-18.9, the only check function it claimed; the other four are D-202, and the missing retraction route is D-203, raised as DOCTRINE for Bob rather than decided here.
  **(c) THE AUDIT'S VOCABULARY.** *No chain recorded*, *a chain recorded and empty*, and *a chain field that is not a chain* were ONE message reading "with no provenance_chain". They are three now, each saying which fact it is, and the empty case's repair explicitly refuses to be fixed by assuming a route. Residual stated rather than hidden: the audit TALLY is still keyed by check id, so the distinction reaches a reader through the offender detail (bounded at 20) and not through the tally.
  **NOT APPLIED TO THE LIVE RECORD, AND THIS IS THE ONE THING THE ITEM ASKED FOR THAT IS NOT DONE.** `op=provenancechain` does not exist in the serving 0.56.0 build, DIST cuts releases and the plane deploy is gated to Bob, so the ten dispositions are DETERMINED and EVIDENCED but not written. Each was proved by running the REAL derivation over the REAL live register in a local scratch store — never against the record — and all ten report `reconstructed` with the hop they would receive.
  NUMBERS. Battery **101/101 at 5,732** (100/100 at 5,664 measured in this worktree before any edit). The +68 is ATTRIBUTED PER SUITE by diffing per-suite counts, not estimated: `provenance-chain.test.mjs` +63 (new), `hygiene.test.mjs` 423 -> 426 (its three per-file assertions over the new file), `planning-hygiene.test.mjs` 236 -> 238 (the two new DEBT rows). **NO OTHER SUITE MOVED BY ONE ASSERTION.** `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped: **exit 0**, 131/131 ops (the new op carries its control-plane assertion in the same turn), 53/53 checks, 101/101 suites declaring a control, 300 arms (294 before).
  NC RUN x6, every file restored BYTE-IDENTICALLY with sha256 compared before and after (src/store.mjs a9466d2b, checks/bio-checks.mjs 79c4588e, test/provenance-chain.test.mjs 5b8f72ae), and all six re-run against the FINAL files so every count in the header agrees with the file it names. **(a)** invention arm -> 7 FAIL. **(b)** C-18.9 neutered -> 9 FAIL, then the stripper's own guard THROWS rather than report a meaningless delta — reported rather than smoothed. **(c)** the other direction, fence at every state -> 6 FAIL. **(d)** the walk neutered -> 2 FAIL, and they are the two that can only fail AS A DELTA. **(e)** release's entry requirement removed -> 3 FAIL with the catalog arm silent, which is why both layers are asserted. **(f)** co_archive read as a second hop -> 2 FAIL.
  A SUITE-HARNESS CORRECTION REPORTED RATHER THAN SMOOTHED: the first draft indexed findings directly, so arms (a) and (b) DIED with a TypeError instead of failing — D-93's class inside one suite, the crash hiding every arm behind it. The reads are defensive now and both arms report their full tally.

## CLAIM 2026-08-05 RECORD

session: rec55-agent (worktree agent-a1eec0bf4a97bf253)
opened: 2026-08-05T21:00:00Z
paths: **`bio-plane/src/store.mjs` — named by function, not by file: `selectionResolve`'s drift/weight block ONLY (the `stopped` predicate and the ONE new private helper beside it), and `cite()`'s Session Log trigger clause ONLY (the two arms that read `sel.moved`).** NOT opened: `selectionCreate`, `selectionList`, `selectionRelease`, `#edgeTransition`/`sever`/`reinstate`, `dispose`, `retire`, `release` (read and asserted over, never written), the capture/link/task/reachability functions (CAPTURE's), the projections, membership, the alarm, the DO route table. `bio-plane/test/refuse-gate.test.mjs` (NEW — this item's own suite, so nothing is added to another item's ground). `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **NEITHER IS CHANGED.** No table, column or index (I5 untouched). **No op gains, loses or renames a field, a reason or a parameter (I3 untouched):** the answer-changed predicate is a PRIVATE static helper, deliberately not a published field, so `op=selection`'s answer stays byte-identical for every input that is not stopped and the published `moved` keeps the per-row meaning six surfaces read. What DOES move is which inputs `op=selection&weight=refuse` (and every refuse-weight act behind it) STOPS — a query selection whose membership swapped at a constant count is now refused `SET_MOVED` where it previously passed. That is the defect being closed, not a shape change, and no existing reason string or success shape moves.
interfaces consumed: none.
expected: QUEUE.md REC-55 (UI-25's delegation, mechanism decided by CONDUCT at integration and NOT re-opened here). The refuse gate cannot see a query selection that moved: `stopped = moved && weight === "refuse"` and `digestChanged` is not a term in `moved`. The gate tests the answer-changed predicate; `moved` is NOT folded. Plus the SWEEP FOR THE CLASS — every other place the plane decides whether a selection is safe to act on, or states whether the answer changed.
accepts-when: as QUEUE.md REC-55. `cd bio-plane && npm run test:battery` green — **101/101 at 5,735 MEASURED IN THIS WORKTREE before any edit** (the brief said 5,733; the measurement governs), any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated; a suite driving a `kind:"query"` selection whose membership SWAPS AT CONSTANT COUNT with a refuse-weight act STOPPED, an unchanged query selection still passing, and an enumerated selection unmoved at every existing caller; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line: (1) revert the gate to `moved` alone -> the suite FAILS naming the constant-count swap that got through; (2) fold `digestChanged` into `moved` instead -> the suite FAILS, because the wrong fix must be distinguishable from the right one; (3) neuter the sweep's walk -> its REACH assertion fails AS A DELTA, never against an absolute. Every file restored byte-identically after each arm, sha256 compared before and after. No push, no merge — CONDUCT integrates. QUEUE.md and DECISIONS.md are not written by this session.
concurrency: no other worker is claiming `bio-plane/**` right now (checked: every RECORD and UI claim above this one is `released:`).
released: 2026-08-05 · CONDUCT at integration — merged on main (worker 74decce). Verified FROM THE MAIN CHECKOUT: battery 102/102 at 5,799 with the +64 attributed per suite by diffing counts (refuse-gate +61 new, hygiene 426 -> 429, no other suite moving by one); coverage --strict run DIRECTLY, $? unpiped, exit 0 at 303 arms; UI harness exit 0. **CONDUCT RE-RAN NC (a) ITSELF**, the gate being security-sensitive (it protects `release` and `retire`): reproduces at 44 pass / 17 fail against the worker's 18 — a one-assertion difference in ARMING, not in the guard — and it names the sharp thing, that without the fix `op=release` moves documents to `verified` and `op=retire` retires them TERMINALLY over a set the operator never saw. store.mjs restored byte-identically, sha256 compared. **The worker's baseline of 101/101 at 5,735 was right and CONDUCT's brief figure of 5,733 was stale** — the two D-205/D-206 debt rows CONDUCT added after measuring moved planning-hygiene 239 -> 241; verified here rather than argued. CONDUCT's "six surfaces read `moved`" was NOT measured and is corrected to two on REC-55's scope. Routed: D-207, D-208.
---

## CLAIM 2026-08-05 UI

session: ui39-agent (worktree agent-ac6c0018a3d94d2c5)
opened: 2026-08-05T19:20:00Z
paths: **`civicos-ui/app.html`**, named by FUNCTION rather than by file, because this item is a class sweep and a whole-file claim would fence off ground it does not touch: `heldMatch` and its ONE caller's held-branch inside `addGo` (site a); `loadResolveCandidates`'s readingname call and its bound note (site b); `queueLoadFeed` / `queueResolutions` / `queueItems` and the queue's count-rendering block (site c); `lookupSubject`'s empty branch and the finder's `#f-subj-bound` sentence it will now SHARE (site d). **NOT opened:** the act surfaces, the published-case renderer, the finder's search routes (UI-25's ground, already bounded at `limit:"500"`), the docprofile copy, the catalog block. `civicos-ui/test/bound-sweep.test.mjs` (NEW — this item's own suite, so nothing is added to another item's ground). `civicos-ui/test/add-surface.test.mjs` (site a's write arm lands in the suite that already owns `heldMatch`). `civicos-ui/test/run.mjs` (registration of the new suite ONLY). `docs/development/CLAIMS.md` (this entry).
interfaces consumed: I3 (the op contracts). **NOTHING INSIDE `bio-plane/**` IS OPENED** — a zero-byte battery delta is this claim's evidence for that, not its claim.
interfaces owned: none.
expected: QUEUE.md UI-39. The four bound-dropping sites UI-25 reported rather than edited, plus the SWEEP for the class. (a) is the centre: `heldMatch` reads at the plane's `LIMIT_DEFAULT` of 50 and that read DECIDES WHETHER `addGo` WRITES A SECOND BUNDLE — a bound governing a WRITE, not a display.
accepts-when: as QUEUE.md UI-39. `node civicos-ui/test/run.mjs` green with `heldMatch`'s bound DRIVEN — a match past the plane's default must not produce a duplicate bundle and the suite must NAME THE WRITE IT PREVENTED (`op=promote`), asserting the write and not the display; `cd bio-plane && npm run test:battery` stays green at 101/101 / 5,733 with any delta ATTRIBUTED PER SUITE; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded: (1) restore each dropped bound and the harness FAILS naming the file and the figure; (2) for (a), plant a match beyond the cap and assert the duplicate WOULD have been written without the fix; (3) neuter the sweep's walk and its REACH assertion fails AS A DELTA; (4) polarity checked on every pin — RED for the defect, GREEN for the fix. Every file restored byte-identically after each arm, sha256 compared. **No push, no merge — CONDUCT integrates.** QUEUE.md and DECISIONS.md are not written by this session.
concurrency: the only other live claim is RECORD's REC-54, which is `released:` and is `bio-plane/**` ground this claim does not enter.
released: 2026-08-05 · CONDUCT at integration — merged as db82819 (worker 7e0a5c3 + 1e5d5b0). Verified FROM THE MAIN CHECKOUT: battery 102/102 at 5,801, the +2 over REC-55's 5,799 ATTRIBUTED to planning-hygiene 241 -> 243 (this item's two DELEGATION entries) with hygiene unchanged at 429, which is the evidence no plane file moved; UI harness exit 0 at 35 harnesses with arm B at 2,477 op answers across 67 ops; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT RE-RAN THE WRITE CONTROL ITSELF**, this item changing what the RECORD holds: stripping the walk to a single page fails 8, **the first being 'THE WRITE IS PREVENTED: a match past the plane's default produces NO op=promote'** — 8 against the worker's 9 because CONDUCT armed a single page where the worker armed a fully unpaged read, a difference in ARMING rather than in the guard. app.html restored byte-identically, sha256 compared. Routed: REC-57 (the two plane ops that publish no bound), M0-10 (the battery's concurrency sensitivity), DEC-57 and DEC-58 (both shipped provisionally, neither blocking).

---

## DELEGATION 2026-08-05 UI → RECORD

raised by: ui39-agent (worktree agent-ac6c0018a3d94d2c5), UI-39
what UI needs: **two read ops publish a bound they do not name, so a surface has to assert its own.** `op=search` gets this right — it answers `total`, `limit` and `offset` beside the page, which is exactly what let UI-39 fix `heldMatch` by walking the plane's own count. Two of its siblings do not:
  1. **`op=readingname` (`documentsNamingEntity`, store.mjs ~7583)** caps at `Math.min(Number(limit) || 100, 500)` and returns `count` — *the length of what it sent*, which is indistinguishable from the length of what exists. It publishes NO `limit` and NO truncation flag. So `loadResolveCandidates` cannot read the bound off the answer, and UI-39 shipped the honest half available to a surface: it SETS `limit` and states the number it ASKED FOR. That sentence is true whatever the plane clamps to (a clamp can only make the list shorter than the claim, UI-26's conservative direction) but it is a bound the surface authored, not one the record published. **Asked for: `limit` (the cap actually applied) and `truncated` on the answer.** Then the sentence states the record's own figure and the surface stops asserting one.
  2. **`op=tasks` (`taskList`, store.mjs ~15194)** caps at 200 and publishes a `counts` block whose figures count the whole visible set — but no truncation flag, where its sibling `op=queue` publishes `truncated` explicitly. UI-39 therefore INFERS the resolutions bound from the arithmetic (`counts.resolved` against the rows delivered) and words it as the inference it is, kept deliberately distinct from the queue's line, which quotes the record's own flag. **Asked for: `truncated` on `op=tasks`, matching `op=queue`.**
why it is not fixed here: `bio-plane/**` is RECORD's ground and this session opened none of it — the zero-byte battery delta is the evidence.
severity: neither is a correctness defect today. Both are the same shape as the class UI-39 exists to close, one layer down: **a producer that caps without publishing the cap forces every consumer to guess or to assert.** Two surfaces have now had to.

---

## DELEGATION 2026-08-05 UI → M0 (VERIFICATION)

raised by: ui39-agent (worktree agent-ac6c0018a3d94d2c5), UI-39
what was observed: **`npm run test:battery` FAILED `migrate.test.mjs` when run CONCURRENTLY with `node scripts/coverage.mjs --strict` and a UI harness run on the same machine; re-run alone it is green at 101/101 / 5,735.** No `bio-plane/**` file was open in this worktree in either run (`git status` confirms), so the failure cannot be attributable to a change. Reported rather than smoothed, because the failing run is the one a worker under time pressure would report as a regression and then spend an hour attributing — which is exactly what the UI-39 brief says two workers already did over a missing `miniflare`.
what UI needs: either the battery serialises against a lock, or `VERIFICATION.md` states plainly that the battery must not be run concurrently with another suite on the same machine and that a lone re-run is the authority.

## CLAIM 2026-08-05 RECORD

session: rec56-agent (worktree agent-a0bb936400f156736)
opened: 2026-08-05T23:30:00Z
paths: **`bio-plane/checks/bio-checks.mjs` — named by check function, not by file: the REPAIR ARRAYS ONLY of `checkReleaseAuthority` (C-18.1, two arms), `checkSignedRelease`'s pre-contract and unsigned-release arms (C-18.8, two arms), `checkConcludedExtension`, `checkDividedExtension` and `checkPublishedExtension` (C-2.8, one arm each); plus the `f()` helper gaining ONE optional trailing parameter (`code`), and the three C-18.9 chain arms passing it.** No finding MESSAGE moves, no predicate moves, no check fires or stops firing on any input. **NOT opened: every other check function's body, `STATES`, `checkStateLegality`, and the whole grammar/parser half of the file.** **`bio-plane/src/store.mjs` — `auditPass()`'s tally block ONLY** (one derived sibling key beside `tally`; the existing `tally`, `offenders`, `clean`, `withErrors`, `cursor` and `total` are byte-identical for every input). NOT opened: `promote`, `release`, `retire`, the capture/link/task/reachability functions (CAPTURE's), the projections, membership, selections, the alarm. `bio-plane/test/repair-reachability.test.mjs` (NEW — this item's own suite). `docs/development/DEBT.md` (APPEND only). `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I5 IS NOT CHANGED**: no table, column or index. **I3 GAINS ONE ADDITIVE KEY** on `op=audit`'s answer (`tallyDetail`), present only when a finding carried a code; no existing key, op, parameter, refusal reason or success shape moves by one byte. The catalogue's finding shape gains an OPTIONAL `code` property, additive, absent on all 142 findings that do not pass one.
interfaces consumed: none.
expected: QUEUE.md REC-56, D-203 — the four repair strings that advise `verified -> collected`, which `STATES.information.edges` does not carry and C-4.2 refuses BY NAME; the D-206 tally rider, decided inside this item with the reasoning at the site; and the SWEEP FOR THE CLASS across every repair string in the catalogue, against the STATE MACHINE **and** the OP SURFACE.
accepts-when: as QUEUE.md REC-56. `cd bio-plane && npm run test:battery` green — **102/102 at 5,801 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** (the brief said 5,799; the measurement governs, as it did for REC-55), any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated; a SOURCE-LEVEL assertion that every repair string in the catalogue names a reachable act, with its own REACH asserted as a DELTA; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line: (1) restore any of the `return the bundle to collected` strings -> the assertion FAILS naming the check and the illegal transition; (2) neuter the walk over the catalogue -> the reach assertion fails AS A DELTA, against a copy of the real source with the real guard MECHANICALLY STRIPPED; (3) polarity — RED for the defect and GREEN for the fix. Every file restored byte-identically after each arm, sha256 compared before and after. No push, no merge — CONDUCT integrates. QUEUE.md and DECISIONS.md are not written by this session.
concurrency: no other worker is claiming `bio-plane/**` right now (checked: every RECORD, UI and DIST claim above this one is `released:`).
released: 2026-08-05 · CONDUCT at integration — merged on main (worker 3f91aa9). Verified FROM THE MAIN CHECKOUT: battery 103/103 at 5,845 with the +44 attributed per suite (repair-reachability +39 new, hygiene 429 -> 432, planning-hygiene 243 -> 245, no other suite moving by one); coverage --strict run DIRECTLY, $? unpiped, exit 0 at 309 arms; UI harness exit 0. **CONDUCT RE-RAN NC (a) ITSELF**: restoring the unfollowable `return the bundle to collected` advice at one site fails 5, naming it in the instrument's own words ("directs a move to 'collected', which no transition in the information machine enters") with the three REACH arms moving as deltas — exactly as reported; bio-checks.mjs restored byte-identically, sha256 compared. The worker's worktree baseline of 102/102 at 5,801 was right where the brief said 5,799 — the second time this week a brief carried a stale figure and the worker was right to trust the measurement. D-206 decided in favour of carrying the distinction, with the reasoning in the code at both sites. D-209 and D-210 recorded.
---

## CLAIM 2026-08-05 UI

session: ui35-agent (worktree agent-ab13407cf67de9eaf)
opened: 2026-08-05T20:05:00Z
paths: **`civicos-ui/app.html`**, named by FUNCTION rather than by file: `pubLooseNote`'s not-a-case branch (the `c.detail` read at the "These bytes are not a member of any published case" line) and NOTHING else in the published-case renderer. `civicos-ui/test/publishedcase.test.mjs` (this op's own suite — the fixture correction and this item's new sweep block). `docs/development/CLAIMS.md`, `docs/development/MEASUREMENTS.md` (the consumer measurement this item exists to take). **NOT opened:** the gate, the act surfaces, `preauth-vocabulary.test.mjs`, `DEC49_SUBJECT`, the finder, the docprofile copy, the catalog block.
interfaces consumed: I3 (the op contracts). **NOTHING INSIDE `bio-plane/**` IS OPENED** — a zero-delta battery is this claim's evidence, not its claim.
interfaces owned: none.
expected: QUEUE.md UI-35. Measure the consumers of `op=publishedcase`'s top-level `detail` across surface, installer, battery and fleet, then take the branch the measurement earns.
accepts-when: as QUEUE.md UI-35.
outcome: **the premise is contradicted and NEITHER branch applied** — `op=publishedcase` publishes no top-level `detail` for a case that was FOUND, so there was nothing to render and nothing to withdraw, and NO INTERFACE CHANGE WAS FILED because no field is being removed. The defect was the mirror: a SURFACE read of `c.detail` that the wire can never satisfy, kept alive by a fixture that invented the field. Both corrected; the sweep pins the four published top-level fields nothing reads. `bio-plane/**` untouched — battery 102/102 at 5,801, byte-identical to the baseline measured in this worktree before any edit.
released: 2026-08-05 · CONDUCT at integration — merged as 1d3b164. Verified FROM THE MAIN CHECKOUT: battery 103/103 at 5,845 unchanged by this item (its own worktree measured 102/102 at 5,801 byte-identical before and after, with git status on bio-plane/ empty — structural evidence that no plane file moved); UI harness exit 0 at 35 harnesses with publishedcase 196 -> 205; coverage --strict run DIRECTLY, $? unpiped, exit 0. **THE ITEM'S PREMISE WAS CONTRADICTED AND THAT IS THE RESULT**: op=publishedcase publishes no top-level `detail` on the found path at all, so neither branch applied — nothing rendered, nothing withdrawn, NO IC FILED. What was there was the mirror: the SURFACE read a field the wire can never send and the FIXTURE invented it, so a dead branch rendered as alive past 196 assertions. Routed: UI-40 (render case_detail/graph_detail, remove `opened` through the IC protocol — decided at integration on REC-41's precedent, not raised), M0-11 (the loose branch no plane suite drives), DEC-59 (the group's own bias_acknowledgement reaching no reader — Bob's, because it puts wording on a pre-auth surface while DEC-49 is open).

---

## CLAIM 2026-08-05 UI

session: ui40-agent (worktree agent-a33932e0947e511c1)
opened: 2026-08-05T23:55:00Z
paths: **`civicos-ui/app.html`**, named by FUNCTION rather than by file, because this item touches two blocks of the published-case renderer and a whole-file claim would fence off ground it does not enter: `pubCaseHtml`'s page 1 (where `case_detail` lands beside the case's own two assertions), `pubFindingPages` (where `graph_detail` lands beside the per-finding graph), and TWO NEW functions this item adds (`pubPlaneAccount`, the verbatim renderer for a sentence the plane published, and `pubGraphHtml`, the per-finding `serves[]`/`names[]`/`unresolved[]` block). **NOT opened:** the gate, the act surfaces, the finder, the docprofile copy, the catalog block, `pubLegHtml`, `pubFooterHtml`, the floor picker, `pubStateHtml`. `civicos-ui/test/publishedcase.test.mjs` (this op's own suite — this item's new block and the fixture growth its arms need). `civicos-ui/test/preauth-vocabulary.test.mjs` (**the `opened:` key of its `op=publishedcase` fixture ONLY** — a second UI suite whose fixture carries the field, which UI-35's table did not name; when the field goes, leaving it would be a fixture inventing a key the plane does not publish, which is the exact D-173 class UI-35 itself found here). `bio-plane/src/store.mjs` — **`publishedCase()`'s SUCCESS RETURN ONLY, the single `opened: state.opened` key, under IC-22.** NOT opened: `#caseEditionState`, `#looseEditionState`, the `published_cases` SELECT, the schema, or any other method. `bio-plane/test/publishedcase.test.mjs` (the assertion that the field is no longer published). `docs/development/INTERFACE-CHANGES.md` (IC-22, APPEND only), `docs/development/INTERFACES.md` (I3's version line), `docs/development/MEASUREMENTS.md` (APPEND — the consumer re-measurement), `docs/development/CLAIMS.md` (this entry).
interfaces consumed: I3 (the op contracts).
interfaces owned: none. **I3 IS OWNED BY `RECORD`**, so the `opened` removal travels through `INTERFACE-CHANGES.md` as IC-22 and is landed here only because the item routes it that way; nothing else inside `bio-plane/**` is opened, and the battery delta is the evidence.
expected: QUEUE.md UI-40. (a) RENDER `case_detail` and `graph_detail` as the PLANE's own account, lifting what the record published rather than authoring wording (UI-32's rule, which is also why this does not collide with DEC-49). (b) REMOVE `opened` through the IC protocol on REC-41's precedent, with the consumer impact RE-MEASURED rather than inherited from UI-35's table.
accepts-when: as QUEUE.md UI-40. `node civicos-ui/test/run.mjs` green with `case_detail` and `graph_detail` RENDERED and asserted, each shown as the plane's account and never restated in the surface's own words; the consumer count for `opened` RE-MEASURED and REPORTED even where it contradicts UI-35; IC-22 filed; `cd bio-plane && npm run test:battery` green with the field gone and an assertion that it is NO LONGER PUBLISHED — **103/103 at 5,845 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** (the brief said 103/103 at 5,845 and the measurement agrees with it), any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in each suite's own `NEGATIVE CONTROL:` line: (1) blank each rendered field AT THE PLANE and the harness FAILS naming which account went missing, rather than rendering an empty box; (2) restore `opened` to the published shape and the assertion FAILS naming it; (3) neuter the consumer walk and its REACH fails AS A DELTA; (4) polarity checked on every pin — RED for the defect, GREEN for the fix. Every file restored byte-identically after each arm, sha256 compared before and after. **No push, no merge — CONDUCT integrates.** QUEUE.md and DECISIONS.md are not written by this session.
concurrency: no other worker is claiming these paths right now (checked: every RECORD, UI and DIST claim above this one is `released:`).
released: 2026-08-05 · CONDUCT at integration — merged on main. Verified FROM THE MAIN CHECKOUT: battery 103/103 at 5,846 with ONLY publishedcase.test.mjs moving (77 -> 78, the IC-22 assertion) and hygiene 432 / planning-hygiene 245 both unmoved; UI harness exit 0; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT RE-RAN NC (2) ITSELF**, this item removing a published field from a public I3 op: restoring `opened: state.opened` to publishedCase()'s success return fails the plane suite 77/1 naming it; store.mjs restored byte-identically, sha256 compared. **CONDUCT ALSO CHECKED A SUSPECTED GAP AND FOUND NONE** — `#looseEditionState` still carries an `opened` key, but publishedCase()'s success return picks its fields explicitly and never spreads the state, so it reaches no caller and IC-22 is accurate on BOTH branches. Established before recording it rather than after. CONDUCT's own scope error is corrected on the item: it asserted the surface already rendered serves[]/names[]/unresolved[], and UI-40 measured that it rendered NONE of them. Routed: REC-58 (op=publishcase's own case.opened), and DEC-49 gains measured input.

---

## CLAIM 2026-08-05 RECORD

session: rec57-agent (worktree agent-a31655e984177a8b6)
opened: 2026-08-05T23:55:00Z
paths: **`bio-plane/src/store.mjs` — named by METHOD, not by file: the RETURN BLOCK and the cap computation of `documentsNamingEntity`, `taskList`, `queueFeed`, `searchIndexCheck`, `taskDrain`, `reproject`, `reindexNames` and `exportLog`, plus the `exportlog` dispatch entry gaining a `limit` parameter.** No predicate, no gate, no SQL WHERE clause and no ordering moves; every existing key on every one of these answers is byte-identical for every input. **NOT opened:** `promote`, `release`, `retire`, the gate path, the audit sweep, `projection`'s body, membership, selections, the alarm, `query.mjs` (READ ONLY, as the authority for `LIMIT_DEFAULT`/`LIMIT_MAX` — `op=search` is the roster's already-honest model and is not touched). `bio-plane/test/bounds.test.mjs` (NEW — this item's own suite). **`bio-plane/test/gate-reads.test.mjs` — ONE assertion, added to this claim AFTER the battery named it**: its `op=list` paged answer pins the WHOLE key set as a leak guard, and `limit` joining that set is a superseded pin, CORRECTED with a dated reason and two new arms proving `limit` is viewer-independent while `total` is not — never exempted. No other assertion in that file moves. `docs/development/INTERFACE-CHANGES.md` (APPEND — IC-23 for the additive keys, IC-24 for the one non-additive proposal). **`docs/development/INTERFACES.md` — the I3 entry ONLY, 8.0.0 -> 8.1.0**, because protocol step 5 assigns the version bump to the interface's OWNER and RECORD owns I3; no other entry is opened. `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I5 IS NOT CHANGED**: no table, column or index. **I3 GAINS ADDITIVE KEYS ONLY** — `limit` on eight ops, `truncated` on three that publish no truncation signal at all, `remaining` on one (copying its own sibling `op=reproject`'s spelling rather than minting a second). No existing key, op, parameter, refusal reason or success shape moves by one byte. `op=exportlog` gains an OPTIONAL `limit` parameter; absent, it behaves exactly as before.
interfaces consumed: none.
expected: QUEUE.md REC-57, UI-39's delegation above. The two ops UI-39 named, and **the SWEEP FOR THE CLASS** — the roster of capped ops read off `store.mjs`/`query.mjs` rather than listed by hand, and for each one whether it publishes the bound it applied and whether it truncated.
accepts-when: as QUEUE.md REC-57. **Battery baseline 103/103 at 5,845 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** (the brief said the same; the measurement governs regardless), any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded: (1) drop the published bound from each op -> the suite FAILS naming the op and what a consumer can no longer tell; (2) make an op report a count equal to what it SENT while more exists -> the suite FAILS, the two must not read alike; (3) neuter the roster walk -> REACH fails AS A DELTA; (4) an over-strictness arm. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md and MEASUREMENTS.md are not written by this session.
concurrency: **CORRECTED AT REBASE, and the original line was wrong.** It said no other worker held `bio-plane/**`; UI-40 (ui40-agent) held `publishedCase()`'s success return under its own IC at the same time, on a local-main merge this worktree's base predated. **Disjoint by method** — UI-40 opened `publishedCase` and nothing else in the file; this claim opens none of it and names its eight methods individually. Rebased onto that main, `store.mjs` auto-merged with NO conflict (the doc appends conflicted and were resolved keeping both), and the whole battery was re-run on the merged tree rather than inherited. The IC numbers and the I3 version here are the POST-REBASE ones: UI-40 took IC-22 and moved I3 to 8.0.0 while this was in flight.
released: 2026-08-05 · CONDUCT at integration — merged on main (worker 1de9010). Verified FROM THE MAIN CHECKOUT: battery 104/104 at 5,920 with the +74 attributed per suite (bounds +69 new, gate-reads 110 -> 112, hygiene 432 -> 435, nothing else moving by one); coverage --strict run DIRECTLY, $? unpiped, exit 0 at 131/131 ops; UI harness exit 0. **CONDUCT RE-RAN NC (2) ITSELF**: reverting op=readingname to count what it SENT fails 4, headed by "DELTA — 'this is all of it' and 'this is the first N' do NOT read alike", exactly as reported; store.mjs restored byte-identically, sha256 compared. **CONDUCT ESTABLISHED THE CONSEQUENCE OF THE op=audit FINDING RATHER THAN FILING IT**: `ok` is the transport verdict and `clean`/`withErrors` are per PAGE, so past 200 documents a clean first page reads as a clean record. D-200's reading STANDS (31 documents, cursor null, a complete reading) but DIST's gate step 8 wording does not — recorded as D-211 and delegated to DIST, whose file it is. Both of the worker's decision items were resolved by CONDUCT rather than escalated to Bob: IC-24's sequencing (right not to land a non-additive I3 change in the turn it is proposed) is REC-59, and op=exportlog's cursor stays deferred as recommended. Routed: REC-59, UI-41, D-211.

---

## CLAIM 2026-08-05 UI

session: ui41-agent (worktree agent-a365be69848d3ae82)
opened: 2026-08-06T00:30:00Z
paths: **`civicos-ui/app.html`**, named by FUNCTION rather than by file, because this item touches two bound-sentence composers and a whole-file claim would fence off ground it does not enter: **`loadResolveCandidates`** (the `RESOLVE_CAND_LIMIT` ask, `candBound`, `atBound`/`atBoundNote` — the `op=readingname` bound sentence UI-39 authored) and **`queueBoundHtml`** (the RESOLUTIONS arm only — the `op=tasks` bound UI-39 INFERRED from `counts.resolved` against the rows delivered). **NOT opened:** the queue arm of `queueBoundHtml` (already reads `op=queue`'s own `truncated`/`item_count` off the wire), `heldMatch` and its `HELD_PAGE` walk (already reads `op=search`'s `limit`/`total`), `finderTextPanelHtml` (already reads `t.limit`/`t.total`), the review bound, the gate, the act surfaces, the finder, the published-case renderer. `civicos-ui/test/bound-sweep.test.mjs` (this class's own suite — this item's new arms and the walk that finds the class). `docs/development/CLAIMS.md` (this entry).
interfaces consumed: I3 (the op contracts), at 8.1.0 — `limit`/`truncated` as REC-57 published them. **NOTHING INSIDE `bio-plane/**` IS OPENED** — a zero-delta battery is this claim's evidence, not its claim. The plane's source is read TEXTUALLY by the sweep as the authority for the roster, never edited.
interfaces owned: none.
expected: QUEUE.md UI-41. Replace the SURFACE-AUTHORED bound with the RECORD's own on every surface that composes a bound sentence, so the number a member reads is the number the plane APPLIED after clamping rather than the number the surface ASKED FOR. Read `limit`/`truncated` off the wire; never re-derive them. Retires the exception DEC-58 was raised to license.
accepts-when: as QUEUE.md UI-41. **Battery baseline 104/104 at 5,921 MEASURED IN THIS WORKTREE BEFORE ANY EDIT — the brief said 5,920 and the measurement governs.** `node civicos-ui/test/run.mjs` green with each bound sentence sourced from the op's own published `limit`/`truncated` and ASSERTED TO COME FROM THE WIRE (a hand-typed number must FAIL); `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` unpiped, exit 0. NEGATIVE CONTROLS run and recorded: (1) the plane publishes a DIFFERENT bound and the surface's sentence MOVES WITH IT, failing if it does not; (2) blank the published bound and the surface says it does not know rather than falling back to a number it made up; (3) neuter the sweep's walk and REACH fails AS A DELTA; (4) polarity checked on every pin.
concurrency: no other worker is claiming these paths right now (checked: every UI and RECORD claim above this one is `released:`).
released: 2026-08-05 · CONDUCT at integration — merged on main (worker f7e49fe). Verified FROM THE MAIN CHECKOUT: battery 104/104 at 5,921 with ZERO delta and no suite moving by one (bio-plane/** never opened); UI harness exit 0 read UNPIPED at 35 suites with arm B 2,477 -> 2,494 op answers; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT RE-RAN NC (1) ITSELF**: hand-copying the bound instead of reading it fails 8 arms including "A HAND-TYPED NUMBER FAILS THIS: the figure this screen ASKED FOR appears nowhere in what a member reads" and both REACH-as-a-delta arms; app.html restored byte-identically, sha256 compared. **DEC-58's exception is RETIRED** and the entry carries a CONDUCT note recommending closure as MOOT. The worker's baseline of 5,921 was right where CONDUCT's brief said 5,920 — the third stale CONDUCT figure this session, each caught by the worker measuring its own. No delegation: REC-57 discharged the one this item depended on.
released:
## CLAIM 2026-08-05 RECORD

session: rec58-agent (worktree agent-abc1aa53ee2b00a8b)
opened: 2026-08-06T00:40:00Z
paths: **`bio-plane/src/store.mjs` — named by METHOD, not by file, and COMMENTS ONLY: no executable byte moves.** (a) `publishedCase()`'s IC-22 site comment (the block above `completeness: state.completeness`), which names the WRONG OP and is corrected with a dated reason rather than softened; (b) `#caseEditionState()`'s `opened:` line, which gains REC-58's decision at the site. **NOT opened:** `publishCase()`, `publish()`, `#looseEditionState`, the `published_cases` SELECT or INSERT, the schema, any return shape, any refusal, any predicate. **`bio-plane/src/index.mjs` — COMMENTS ONLY**, at the two places the control plane picks the case block's fields explicitly (the `op=ratify` success envelope's `case: {...}` and the container manifest's field list), naming the fence they already are. `bio-plane/test/case-opened.test.mjs` (NEW — this item's own suite: the consumer walk and the source-level fence pins). `bio-plane/test/multifinding.test.mjs` (this item's through-the-op key-absence pins ONLY, appended as a new block; no existing assertion moves). `docs/development/MEASUREMENTS.md` (APPEND — the re-measurement), `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **NEITHER CHANGES, AND THAT IS THE ITEM'S RESULT.** No key is added, removed, renamed or reshaped on any op; no table, column or index moves. **NO INTERFACE CHANGE IS FILED, because no published field is being removed** — the field the item was raised to remove is not published by the op the item names, nor by any other. The `INTERFACE-CHANGES.md` protocol is deliberately NOT walked, and the reason is recorded rather than the step skipped silently.
interfaces consumed: none.
expected: QUEUE.md REC-58, UI-40's delegation. RE-MEASURE the consumers of `case.opened` rather than inheriting UI-40's table, then DECIDE whether it comes out of `op=publishcase` or stays with its reasoning at the site.
accepts-when: as QUEUE.md REC-58. **Battery baseline 104/104 at 5921 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** (the brief said 104/104 at 5,920; the measurement governs and is one assertion higher), any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in each suite's own `NEGATIVE CONTROL:` line: (1) the ruling is KEEP, so restoring the other state — publishing `opened` on each of the three surfaces that could carry it — FAILS naming the field; (2) neuter the consumer walk and assert its REACH as a DELTA, with a paired positive arm so an empty corpus cannot read as a clean answer; (3) polarity checked on every pin; (4) an over-strictness arm — a genuinely correct alternative phrasing must pass. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md and INTERFACE-CHANGES.md are not written by this session.
concurrency: no other worker is claiming these paths right now (checked: every RECORD, UI and DIST claim above this one is `released:`). `bio-plane/test/multifinding.test.mjs` was last touched by rec49-agent/rec47-agent, both released.
released: 2026-08-05 · CONDUCT at integration — merged on main (worker b5238a5). Verified FROM THE MAIN CHECKOUT: battery 105/105 at 5,954 with the +33 attributed and ONLY three suites moving (case-opened +28 new, multifinding 70 -> 74, hygiene 435 -> 436); coverage --strict run DIRECTLY, $? unpiped, exit 0 at 131/131 ops. **CONDUCT RE-RAN NC (1a) ITSELF**: restoring `opened` to the public read is caught INDEPENDENTLY by three suites — case-opened 26/2, publishedcase 77/1, multifinding 73/1 — exactly as reported; store.mjs restored byte-identically, sha256 compared. **THE ITEM'S PREMISE WAS FALSE AND CONDUCT WROTE IT**: `op=publishcase` does not publish `case.opened` and never did. CONDUCT copied the claim out of IC-22's own SETTLED text without re-measuring — REC-41's lesson for the THIRD time, right about the FIELD and wrong about the OP. **IC-22's sentence is corrected in place at integration** (struck rather than deleted, with the propagation path recorded), which is CONDUCT's act because amending a SETTLED IC is a protocol act with an owner and the worker correctly routed it instead of editing it. The item's scope carries the same correction. Routed: M0-12, the mechanical defence.

## DELEGATION 2026-08-06 · BOB → FRAMEWORK · a stale claim in DOCUMENT-PROFILES.md

`docs/development/DOCUMENT-PROFILES.md:309-310` says the duplicate sweep and the
contemporaneity bracket arm both still compare raw hashes. **The first half is now wrong.**
Verified in source 2026-08-06: `checkRegisterIntegrity` (`bio-plane/checks/bio-checks.mjs:3553-3558`)
buckets on `docs[i].profile.digests.evidentiary` when `determined === true`, beside the raw
arm, firing only when two or more different raw shas share a digest. The bracket-arm half is
still accurate (`store.mjs:14859-14873` brackets over raw `capture_sha` rows).

Worth stating precisely when it is corrected, because the scope is narrower than either
phrase implies: the duplicate check is **intra-bundle** — `auditPass` iterates bundles and
calls `checkBundle` per bundle — and **no cross-bundle duplicate sweep exists at all**. D-60
and (until today) D-220 both referred to one; that was a category error inherited between
rows, now corrected on both.

FRAMEWORK's file, so BOB has not edited it. Same correction noted on D-60.


---

## CLAIM 2026-08-07 RECORD

session: rec59-agent (worktree agent-a7938ea842db2d665)
opened: 2026-08-07T00:00:00Z
paths: **`bio-plane/src/store.mjs` — named by METHOD, not by file: the CORPUS ARMS of `projection()` (the `jsonPath`/`jsonEquals` arm and the bare enumeration arm), the `limit`/`after` parameters on its signature, and the `projection:` dispatch entry that forwards them.** The `&id=` single-bundle arm is READ ONLY and does not move by a byte; `#actionDerived` is not opened. **NOT opened:** `listBundles` (READ ONLY, as the authority for the envelope shape this copies), `promote`, the gate, `viewerPredicate`, `projectionOf`, `projectionPlan`, `projectionClear`, `reproject`, the schema, any other method. **`bio-plane/src/index.mjs` — CLAIMED AND THEN NOT OPENED, corrected here rather than left standing:** the claim reserved a comment at the `op=projection` viewer-stamp site, and on doing the work no comment there was warranted — the control plane forwards every parameter it is given by a loop and needed no change at all for `limit`/`after` to reach the store, so the file is untouched and `git status` shows it. `bio-plane/test/bounds.test.mjs` (this item's own suite: the one-op pin transformed, the consumer walk, `projection` joining DRIVEN). **`bio-plane/test/projects.test.mjs`, `bio-plane/test/gate-reads.test.mjs`, `bio-plane/test/projection.test.mjs` — the MIGRATION of the corpus-arm consumers this item's re-measurement FOUND, each a superseded pin CORRECTED with a dated reason and never exempted.** `docs/development/INTERFACE-CHANGES.md` (IC-24 walked PROPOSED -> RESPONSES -> RESOLUTION -> CHANGING -> CHANGED -> SETTLED, APPEND-ONLY). **`docs/development/INTERFACES.md` — the I3 entry ONLY, 8.1.0 -> 9.0.0** (protocol step 5 assigns the bump to the interface's OWNER and RECORD owns I3). `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I5 IS NOT CHANGED**: no table, column, index or `purge` change. **I3 BREAKS, deliberately versioned MAJOR**: `op=projection`s two CORPUS arms stop answering with a bare JSON array and answer `op=list`s paged envelope `{ bundles, limit, cursor, total }`; the op gains OPTIONAL `limit` and `after` parameters it previously ignored. The `&id=` arm, every refusal, every class, the gate and the ordering are unchanged.
interfaces consumed: none.
expected: QUEUE.md REC-59; REC-57's IC-24, filed PROPOSED and deliberately not landed. Carry it through the remaining protocol steps, **RE-MEASURING the call sites rather than inheriting the count.**
accepts-when: as QUEUE.md REC-59. **Battery baseline 105/105 at 5,969 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** (the brief said the same — the first CONDUCT figure in six items that was not stale; the measurement governs regardless), any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line: (1) revert to the bare array -> the suite FAILS naming the op and what a consumer can no longer tell; (2) add a SECOND bare-array capped op -> the pin FAILS, proving it is a pin and not an exemption; (3) neuter the roster walk -> REACH fails as a DELTA with the corpus size printed; (4) an over-strictness arm. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md and MEASUREMENTS.md are not written by this session.
concurrency: no other worker is claiming these paths right now (checked: every RECORD, UI and DIST claim above this one is `released:`). `bounds.test.mjs` was last touched by rec57-agent (released); `gate-reads.test.mjs` by rec57-agent (released); `projects.test.mjs` and `projection.test.mjs` by no in-flight claim.
released: 2026-08-07 · CONDUCT at integration — merged on main (worker 9dddafa). Verified FROM THE MAIN CHECKOUT: battery 105/105 at 5,989 with the +20 attributed by diffing per-suite counts (bounds +15, gate-reads +3, projection +2, projects UNMOVED at 114); coverage --strict run DIRECTLY, $? unpiped, exit 0 at 131/131 ops. **CONDUCT RE-RAN THE CONTROL ITSELF**: restoring the bare array reproduces gate-reads 4, projection 3, projects 12 exactly as reported; store.mjs restored byte-identically, sha256 compared. (bounds prints no suite-name prefix so it was not captured by the harvest; it is 84/0 on the clean tree.) **CONDUCT CORRECTED THE I3 REGISTRY AT 8.1.0**, where the false nil-consumer-impact clause was still being read — the worker appended its correction at 9.0.0 and ROUTED the 8.1.0 half rather than editing a settled entry, which was the right call (REC-58's precedent). REC-59's `op=list` finding is routed onto REC-60's scope as a rider, with the instruction to DECIDE it rather than reflexively envelope a deliberate, documented arm. IC-24 SETTLED; I3 at 9.0.0.
## CLAIM 2026-08-07 UI

session: ui38-agent (worktree agent-a3d2796bef38c97c0)
opened: 2026-08-07T00:00:00Z
paths: **`civicos-ui/app.html`**, named by BLOCK rather than by file: a NEW marked block `/*__SURFACES_START__*/ … /*__SURFACES_END__*/` (the surface registry, the level vocabulary and the recipes — DATA only, no render path), a NEW marked block `/*__AI_SESSION_START__*/ … /*__AI_SESSION_END__*/` (the once-only running-session surface: the indicator, the transcript panel and the budget line), and the ONE line in `boot()`/the hash-router chain that asks the new `#session/<id>` route. **NOT opened:** the CATALOG block, the SEMANTICS block, the DOCPROFILE block, `go()`'s `R` table, any existing renderer, any act surface, the finder, the queue, the published-case renderer, any refusal string, any bound sentence. `civicos-ui/test/surface-registry.test.mjs` (NEW — this item's own suite: the three-arm discovery walk, the both-directions equality, the recipe resolution, the sourcing assertion and the ai-session render pins). **`civicos-ui/test/preauth-vocabulary.test.mjs` — ONE SUPERSEDED ASSERTION CORRECTED, added to this claim after the fact and named rather than absorbed:** its `WALK 2 REACH` arm pins the exact set of hash routers so that a new one must be CLASSIFIED as pre-auth or not before the harness passes, and the new `#session/<id>` router tripped it — which is the arm doing precisely its job. Corrected four -> five with a dated reason at the site, and the classification (POST-authentication: asked inside `boot()`, never at the top level) is now ASSERTED by two new pins rather than stated in a comment, so a later edit that hoists the router above the gate fails there. `docs/development/CLAIMS.md` (this entry).
interfaces consumed: I3 (the op contracts) — READ ONLY and read STRUCTURALLY: `bio-plane/src/index.mjs`'s `OPS` table is parsed out of source, and `bio-plane/src/affordances.mjs`'s `ACTS`/`ACT_IDS`/`CAPTURE_ACTS` are IMPORTED as live module exports. **NOTHING INSIDE `bio-plane/**` IS OPENED** — a zero-delta battery is this claim's evidence, not its claim.
interfaces owned: none. No op, table, column or published shape changes.
expected: QUEUE.md UI-38 as AMENDED 2026-08-07. (a) every member-facing surface self-describes and the described set is proved EQUAL to the real set in BOTH directions; (b) recipes are DATA whose every step resolves to a real surface id and a real EMITTED op, with the sourcing asserted so a hand-typed op list FAILS; and the absorbed E10 — ONE running-session surface for every AI feature, carrying F11's budget and its live consumption, reading what IS-6/IS-9 publish and saying so where they publish nothing.
accepts-when: as QUEUE.md UI-38. **Battery baseline 105/105 at 5,969 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** — the brief's figure was right and is confirmed rather than inherited. UI harness baseline 35 suites, exit 0 read UNPIPED, arm B 2,494 op answers across 67 ops. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line: (1) a recipe step naming an op the plane does not emit FAILS naming step and op; (2) a step naming a nonexistent surface id FAILS naming it; (3) a real surface added and described nowhere FAILS; (4) each discovery arm neutered and the REACH assertion fails AS A DELTA with the corpus size printed; (5) polarity checked on every pin; (6) an over-strictness arm — a correct alternative phrased unlike anything written here must PASS.
concurrency: no other worker is claiming these paths right now (checked: every UI and RECORD claim above this one is `released:`).
released: 2026-08-07 · CONDUCT at integration — merged on main. Verified FROM THE MAIN CHECKOUT: battery 105/105 at 5,989 unchanged by this item (its own worktree measured 5,969 identical before and after, zero delta, no bio-plane/** file opened); UI harness exit 0 read UNPIPED at 36 harnesses with surface-registry new; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT RE-RAN ARM X1 ITSELF** — it guards the very collision CONDUCT amended this scope to prevent: adding a SECOND `ai-session` surface makes the harness exit NON-ZERO with surface-registry failing, so "designed once for all AI features" is enforced rather than documented. app.html restored and verified by BOTH sha256 AND content, per this item's own finding that an NC harness once reported a byte-identical restore over a file that had not been restored. Two scoping questions resolved by CONDUCT rather than escalated — neither is Bob's: `purpose` strings stay AUTHORED and are asserted non-rendered, so DEC-49 is untouched and they are revisited when the training-pack generator lands; and the registry covering only ROUTED surfaces is ACCEPTED as principled (a recipe cannot navigate to an unaddressable surface), to be revisited if an unrouted surface ever hosts an act. Delegations routed: the run record's publication onto IS-6 (with IS-9(d)'s producer), and §14a's capability-unavailable sentence onto REC-64, where it is owed a code and a canned translation rather than being authored at a surface.
released:

---

## CLAIM 2026-08-07 RECORD

session: rec60-agent (worktree agent-a90a87bcb7f131e59)
opened: 2026-08-07T02:00:00Z
paths: **`bio-plane/src/store.mjs` — named by METHOD, not by file: `resolutionsForCapture()` (op=resolutions), `documentsConcerning()` (op=concerns), `connectionsFor()` (op=connections)**, their three `limit` parameters, the two new private statics `#MEANING_LIMIT_DEFAULT`/`#MEANING_LIMIT_MAX`, the three dispatch entries that forward `limit`, and **a COMMENT-ONLY addition at `listBundles()`'s unbounded arm recording this item's rider decision** (no executable byte moves in `listBundles`). **NOT opened:** `deriveConnections` (op=connect — READ ONLY, and reported as the unpredicted finding rather than folded in), `#connectionView`, `#resolutionView`, `#bundleRedactor`, `documentsNamingEntity`, `projection`, `listBundles`'s executable body, the schema, `op=purge`, any refusal, any gate, any ordering. **`bio-plane/src/index.mjs` — NOT OPENED**: `index.mjs:4544` copies every query parameter but `token`/`op` into the DO request, so `limit` reaches the store with no control-plane change (REC-59's finding, re-verified here). `bio-plane/test/meaning-bounds.test.mjs` (NEW — this item's own suite: the RETURN-SHAPE walk, the three live reads, the `op=list` rider pin). **`bio-plane/test/bounds.test.mjs` — the REC-57 roster suite: the three ops join its DRIVEN roster and its `OPS.size` pin is CORRECTED with a dated reason, never exempted.** `docs/development/CLAIMS.md` (this entry and one DELEGATION to UI).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I5 IS NOT CHANGED**: no table, column, index or `purge` change. **I3 BREAKS, deliberately versioned MAJOR**: three previously UNBOUNDED reads — `op=resolutions`, `op=concerns`, `op=connections` — now apply a bound (default 500, ceiling 5000) and publish it as `limit` beside `truncated`. Additive in keys; BREAKING in behaviour, because a caller that received everything today can receive the first 500 tomorrow.
interfaces consumed: none.
expected: QUEUE.md REC-60 (D-225), with REC-59's `op=list` rider routed in at integration. Bring the three uncapped meaning-layer reads onto REC-57's envelope discipline; SWEEP THE CLASS from RETURN SHAPES rather than from op registrations; and DECIDE the `op=list` rider with the reasoning at the site.
accepts-when: as QUEUE.md REC-60. **Battery baseline 105/105 at 5,989 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** (the queue item's `accepts-when` says 5,969 and is stale; CONDUCT's brief said 5,989 and was right — the measurement governs either way), any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line: (1) restore each unbounded read -> the suite FAILS naming the op and what a caller can no longer tell; (2) report a count equal to what was SENT while more exists -> FAILS, the two must never read alike; (3) neuter the walk -> REACH fails as a DELTA with the empty corpus VISIBLE; (4) an over-strictness arm — a correctly-enveloped read phrased unlike anything this file wrote must PASS. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and INTERFACE-CHANGES.md are not written by this session; the I3 registry bump and IC are ROUTED to CONDUCT with the measured impact.
concurrency: no other worker is claiming these paths right now (checked: every RECORD, UI and DIST claim above this one is `released:`). `bounds.test.mjs` was last touched by rec59-agent (released).
released: 2026-08-07 · CONDUCT at integration — merged on main (worker 1f5f95f). Verified FROM THE MAIN CHECKOUT: battery 106/106 at 6,058 with the +69 attributed by diffing per-suite counts (bounds 84 -> 99, hygiene 436 -> 439, meaning-bounds +51 new, nothing else moving by one); coverage --strict run DIRECTLY, $? unpiped, exit 0 at 324 arms. **CONDUCT RE-RAN NC (1) TWO WAYS AND THE SECOND FOUND A GAP THE SUITES DO NOT PIN.** Armed as the worker armed it (the published envelope removed) it bites — bounds 4 fail, matching the report exactly. Armed differently — **removing the SQL `LIMIT ?` while leaving the slice and the envelope intact — BOTH SUITES STAY GREEN at 99/0 and 51/0**: the answer stays honest and truncated while the SCAN goes unbounded again, which is D-225's memory concern rather than its honesty concern, and on the quadratic read it is the shape that bites. Recorded as **D-227**, riding REC-66. NOT a defect in this item's fix, which bounds the SQL correctly. store.mjs restored byte-identically after each, verified by hash AND content. **CONDUCT VERIFIED THE MEMBER-FACING FINDING ITSELF rather than taking it on report**: `civicos-ui/app.html:11857` and `:11858` both carry the now-false sentence, and the pin at `bound-sweep.test.mjs:557` states the stale reason in its own text — routed as **UI-42, first in the UI queue**, because a stated-and-now-false completeness claim is worse than an unstated bound. **IC-25 taken to SETTLED by CONDUCT as MAJOR** (the worker correctly left the version to CONDUCT): the shape change is additive but the BEHAVIOUR change is a break, and unlike IC-20/22/24 the impact is not nil — it is measured, live and in front of a member. **I3 9.0.0 -> 10.0.0.** Also routed: **REC-66** (`op=connect`'s unbounded DERIVATION, a strictly worse shape than the three fixed here). CONDUCT's own stale figure corrected on the item: REC-60's accepts-when said 5,969 when main was at 5,989 — the sixth such, and the worker measured its own and was right, as instructed.
released:

## DELEGATION 2026-08-07 · RECORD → UI · a member-facing completeness sentence that this item makes FALSIFIABLE

**`civicos-ui/app.html:11542` and its pin at `civicos-ui/test/bound-sweep.test.mjs:557`.** The subject screen tells a member, in words, *"Documents already resolved to this subject are added separately and are not capped."* That sentence rested on a MEASURED fact about the plane — `op=concerns` was uncapped — and REC-60 has just changed that fact. `op=concerns` now applies a bound (default 500, ceiling 5000) and publishes it as `limit` beside `truncated`, exactly as `op=readingname` beside it already does and which this screen already reads off the wire.

**What UI owes, and it is small because UI-41 already built the mechanism:** read `limit`/`truncated` off the `op=concerns` answer the same way `loadResolveCandidates` reads them off `op=readingname`, and replace the "are not capped" clause with the record's own figure — with the same no-fallback rule UI-41 established (where the record published no bound, say so; never substitute the number this screen asked for). The UI suite's arm at `bound-sweep.test.mjs:557` pins the OLD sentence and is a superseded pin: CORRECT it with a dated reason, never exempt it.

**Why this is a delegation and not an edit:** `civicos-ui/**` is UI's claim and RECORD does not edit it. **Nothing in the UI battery fails today** — `bound-sweep.test.mjs` drives a fixture, not the plane — so this will not announce itself; it is a live overclaim to a member and not a red build, which is the direction that goes unnoticed. RECORD's position is that capping is still right: before this change the truth was available to nobody, and after it the truth is on the wire and the only defect left is a consumer not yet reading it.

### CORRECTION 2026-08-07 to the CLAIM above (rec60-agent, at the close of its own turn)

The claim's `accepts-when:` line said *"INTERFACE-CHANGES.md ... are not written by this session; the I3 registry bump and IC are ROUTED to CONDUCT."* **The second half stands and the first half was wrong, so it is corrected here rather than quietly departed from.** This item BREAKS I3 — three reads that returned everything now return the first 500 — and a break routed only in a worker's report is a break on a channel the protocol's reader does not run. **`IC-25` is therefore FILED as PROPOSED in `INTERFACE-CHANGES.md`**, append-only, carrying the measured consumer impact and RECORD's position; the RESOLUTION, the consumer responses and the `INTERFACES.md` version bump are NOT taken by this session and remain CONDUCT's. `INTERFACES.md` is untouched, exactly as claimed.
## CLAIM 2026-08-07 RECORD

session: is6-agent (worktree agent-adefd8db6571e8b30)
opened: 2026-08-07T00:00:00Z
paths: **`bio-plane/src/airun.mjs` (NEW)** — the PURE half: D-129's observation vocabulary, §14b.6's bound vocabulary, the DEC-49 code→translation map and the pure refusal functions the store calls. Its own module for `queuestate.mjs`'s stated reason (store.mjs cannot be imported outside workerd, so a rule reachable only through a Durable Object is a rule that gets exercised less). **`bio-plane/src/schema.mjs`** — THREE new tables inserted BEFORE the `host_governor` block: `ai_runs`, `ai_run_bounds`, `ai_run_log`. No existing table, column or index moves. **`bio-plane/src/store.mjs` — named by METHOD:** the NEW `aiRunOpen` / `aiRunTick` / `aiRunClose` / `aiRunRead` / `aiRunLog` and their privates (`#aiRunTerminate`, `#aiRunAppend`, `#aiRunReapPending`, `#aiRunReapWake`, `#aiRunReap`); ONE appended entry in `#schedConsumers` (`ai-run-reap`) and the four lines in `onAlarm` that name it, per `SCHEDULER.md`'s own instruction — **no second alarm and no cron**; three `DELETE FROM` lines in `purge`'s whole-store arm and their `removed` keys (D-113); five entries in the DO dispatch map. **NOT opened:** any existing consumer's `due`/`wake`/`tick`, `#reconcileAlarm`, `#armScheduler`, `capture_sessions` and its three methods, `promote`, the gate, `viewerPredicate`, `bundle.md` generation, any existing refusal, any existing return shape. **`bio-plane/src/index.mjs`** — five `OPS` entries, one new `AI_RUN_ACTIONS` const joined into both `SESSION_OPS` sets, three `NEEDS` entries, and the server-side stamps for the two principals. **`bio-plane/checks/bio-checks.mjs`** — an APPENDED export `AI_RUN_CHECKS` allocating **C-22.1 … C-22.6**; no existing check function, finding or constant moves. **`bio-plane/src/affordances.mjs` — ADDED TO THIS CLAIM AFTER THE FACT AND NAMED RATHER THAN ABSORBED: three APPENDED `NON_ACTS` rows.** REC-19's totality guard (`test/affordances.test.mjs`: "every op in NEEDS is a published act or a named NON_ACT") went RED the moment the three `NEEDS` entries landed, which is that guard doing exactly what `attest`'s six-item history bought it. The three run verbs are named as NON_ACTS with their reason — a run changes NOTHING about the object it runs in (§14a), so an act offered beside an inquiry would imply the inquiry moves when it is taken. No `ACTS` row, no existing NON_ACT, no vocabulary and no derivation moves. **`bio-plane/test/airun.test.mjs` (NEW — this item's own suite).** **`bio-plane/test/gate-reads.test.mjs`** — the two new read ops CLASSIFIED in the GATED table, which is that sweep doing its job. **`bio-plane/test/hygiene.test.mjs` — NOT opened**; it gained four assertions on its own, because it is a per-file sweep and this item added one file to `src/` and one to `test/`. `docs/development/CLAIMS.md` (this entry).
interfaces owned: I3 (the op contracts) and I5 (the store schema). **I3 is ADDITIVE ONLY** — five new ops, no existing op's shape moves, so no interface change is filed. **I5 GAINS three tables**, all cleared by the whole-store purge; no existing table is reshaped.
interfaces consumed: none. **`civicos-ui/**` IS NOT OPENED** — UI-38's `aiSessionRead()` is a DELEGATION, not an edit; a zero-delta UI harness is not this item's claim to make.
expected: QUEUE.md IS-6 with its UI-38 rider. `INVESTIGATIVE-SESSION.md` §11 (the run is an object), §14b.3 (the scheduler join), §14b.6 (a run is bounded and the bound is RECORDED), §14b.7 (partial results survive). Discharges D-196; adopts D-129's four states plus `partial` and D-104's governed/unreachable split.
accepts-when: as QUEUE.md IS-6. **Battery baseline 105/105 at 5,989 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** — the brief's figure was right and is CONFIRMED rather than inherited; any delta ATTRIBUTED PER SUITE by diffing per-suite counts, never estimated. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0, every new op carrying a control-plane assertion in the same turn. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line — **(1) THE ITEM'S OWN, and it is first: a run KILLED MID-FLIGHT whose log must EXIST and must NAME THE BOUND that stopped it**; (2) make the log write only on success and the suite FAILS naming the killed run that left nothing; (3) put the log in `bundle.md` and it FAILS; (4) add a second alarm or a cron and it FAILS naming SCHEDULER.md's one-mechanism rule; (5) polarity checked on every pin; (6) an over-strictness arm — a correct alternative phrased unlike anything written here must PASS. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md and INTERFACE-CHANGES.md are not written by this session.
concurrency: no other worker is claiming these paths right now (checked: every RECORD, UI and DIST claim above this one is `released:`). `gate-reads.test.mjs` was last touched by rec59-agent (released). `bio-plane/src/airun.mjs` and `bio-plane/test/airun.test.mjs` do not exist on main.
released: 2026-08-07 · CONDUCT at integration — merged on main (worker 3fb28b8). Verified FROM THE MAIN CHECKOUT: battery 107/107 at 6,164 with the +105 attributed and only TWO suites moving (airun +101 new, hygiene 436 -> 440); coverage --strict run DIRECTLY, $? unpiped, exit 0 with OPS 131 -> 136 ALL REACHED and CHECKS 53 -> 59 ALL NAMED. **CONDUCT RE-RAN THE KILLED-RUN CONTROL ITSELF**, this being the item's whole acceptance: neutering the reaper reproduces at exactly 90 pass / 11 fail, each naming the killed run or what a surface can no longer render. store.mjs restored byte-identically, verified by hash AND content per UI-38's finding. The item's C-22.4 finding — a rule with TWO implementations, one absorbing the other's control, leaving the suite green at 98/98 — is IS-2's warned three-layer shape arriving A LAYER EARLY, and is carried onto IS-2's scope as EVIDENCE with an added instruction to pin the COUNT of implementations. Routed: UI-43 (the surface's read, whose consumer UI-38 built first and whose contract IS-6's ARM U already proved from the other side by lifting the real renderers rather than a fixture), and DEC-63 (which capability a run costs — Bob's, because it decides who in a group can spend that group's money).
delivered: 2026-08-07 · is6-agent, committed on `worktree-agent-adefd8db6571e8b30`, NOT pushed and NOT merged — CONDUCT integrates. **Battery 106/106 at 6,094, from a measured in-worktree baseline of 105/105 at 5,989 (the brief's figure, CONFIRMED rather than inherited). Delta +105, ATTRIBUTED BY DIFFING PER-SUITE COUNTS and only TWO suites move: `airun.test.mjs` +101 (new), `hygiene.test.mjs` 436 -> 440.** Those four are hygiene's own per-file arms over the two files this item adds (dispose, deterministic exit, sandbox import, control-byte scan) — a per-file sweep gaining two files, not a behaviour change. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped: **exit 0, OPS 131 -> 136 all reached through the control plane (100%), CHECKS 53 -> 59 all named, 106/106 suites declaring a control.** THE C-22 FAMILY IS REACHABLE BECAUSE IT WAS BUILT REACHABLE: two refusals were found unreachable by this item's own controls and both are recorded at the site — C-22.5 could not fire at all while `aiRunClose` derived a bound (silence became "completed"), and C-22.4's control left the suite GREEN AT 98/98 because a second copy of the vocabulary test absorbed it. **DELEGATION to UI, and it is the rider's other half:** `civicos-ui/app.html`'s `aiSessionRead()` still returns null and is the ONE function that changes — `op=airun&run=<id>` now publishes exactly the shape its field-name-blind renderers walk, proved by driving the plane's own answer through those renderers from this suite (ARM U). Nothing inside `civicos-ui/**` was opened.

## DELEGATION 2026-08-07 · RECORD (is6-agent) → UI · `aiSessionRead()` has its op

**The seam UI-38 declared is now supplied.** `civicos-ui/app.html`'s `aiSessionRead(_ctx)`
returns null with the comment *"No op publishes a run record yet… When IS-6 and IS-9 land,
this ONE function gains its read and nothing else moves."* IS-6 has landed and the op is
**`op=airun&run=<id>`**, gated (viewer stamped server-side), member/admin/probe classes, and
its `session` value is EXACTLY the shape the block's renderers already walk:

    { id, label, mode, status, ticks, created, updated, expires,
      context:   { type, id },
      principal: { plane, claude, ref, skill },
      budget:    [ { bound, allowed, consumed, unit }, … ],
      condition: { kind, detail, bound, at } | null }

**Nothing else in that block should move**, and three things are worth stating so they are
not rediscovered: (a) `found:false` / `session:null` is the SUPPORTED absence — the plane
answers it for an unknown run and for a run the viewer may not see, identically, so the
existing "no record means NO INDICATOR" branch is already correct; (b) `allowed` and
`consumed` travel separately and the plane publishes NO percentage, so F11's
derive-nothing pin holds without any change at the surface; (c) `condition.kind` is a slug
from the plane's own vocabulary (`queuestate.mjs`) and is rendered verbatim, exactly as the
block already does — it is null for a run that ENDED rather than being stopped by a bound,
which is a real distinction and not a gap.

**NOT delegated and still open, deliberately:** `runtime-ceiling-reached` as a QUEUE-FEED
notification is IS-9(d)'s producer, not this one. IS-6 records the condition ON THE RUN;
nothing here emits a queue item, and the boundary is asserted in `test/airun.test.mjs`
ARM V8.

**RECORD has not edited `civicos-ui/**`** — a one-function change in another area's file is
still that area's change, and the zero-delta UI harness is UI's evidence to produce.


---

## CLAIM 2026-08-07 UI

session: ui42-agent (worktree agent-aa053ee625b1c6f92)
opened: 2026-08-07T00:00:00Z
paths: **`civicos-ui/app.html`**, named by SITE rather than by file: the `(Documents already resolved to this subject are added separately and are not capped.)` clause on BOTH branches of `candBound` in `loadResolveCandidates`; a NEW shared function `concernsBoundHtml()` rendering `op=concerns`'s published bound, called from `loadResolveCandidates` and from `subjConcernsHtml`; a NEW request constant `MEANING_ASK`; and the `limit:` argument added to the EIGHT `recR("concerns"|"connections"|"resolutions")` call sites that inherit a cap they never state (app.html 8807, 8823, 11201, 11202, 11819, 12120, 12285, 12286). **NOT opened:** the readingname sentence's own wire sourcing (UI-41's, read-only and asserted unmoved), `queueBoundHtml`, `registryNameBoundHtml`, the finder, the CATALOG/SEMANTICS/SURFACES blocks, any act surface, any refusal string, `getProjection` (the `op=projection` `&id=` single-bundle arm — pinned as the plane's own fact, not edited). `civicos-ui/test/bound-sweep.test.mjs` (this item's own suite: WALK 1's roster extractor CORRECTED, ARM B's superseded pin CORRECTED with a dated reason, ARM B extended onto `op=concerns`, NEW ARM F the completeness-claim sweep). `civicos-ui/test/subject-view.test.mjs` (the subject view's concerns panel gains the record's bound; any superseded pin corrected with a dated reason). `docs/development/CLAIMS.md` (this entry).
interfaces consumed: I3 (the op contracts) — READ ONLY. `bio-plane/src/store.mjs` and `src/query.mjs` are read TEXTUALLY by the sweep; **NOTHING INSIDE `bio-plane/**` IS OPENED**, and a zero-delta battery is this claim's evidence rather than its claim.
interfaces owned: none. No op, table, column or published shape changes.
expected: QUEUE.md UI-42 — the LIVE member-facing overclaim REC-60 created (`op=concerns` capped 2026-08-07, the surface still telling a member the already-resolved half is "not capped"), sourced instead from the op's own published `limit`/`truncated`, with the bound reported over the RESOLUTION ROWS the join reads and never over `documents`; and the CLASS swept rather than the one sentence.
accepts-when: as QUEUE.md UI-42. **Battery baseline 106/106 at 6,059 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** (CONDUCT's brief said 6,059 and was right; the queue item's own `accepts-when` says 6,058 and is stale by one — the measurement governs either way). UI harness `node civicos-ui/test/run.mjs` exit read UNPIPED. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line: (1) restore the "are not capped" sentence -> the harness FAILS naming file, line and the op that caps; (2) make the plane publish a DIFFERENT bound -> the member's sentence must MOVE WITH IT; (3) report the bound over `documents` rather than over the resolution rows -> FAILS, because the replacement overclaim must be caught too; (4) neuter the sweep -> REACH fails as a DELTA with the corpus size printed; (5) polarity checked on every pin; (6) an over-strictness arm. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and INTERFACE-CHANGES.md are not written by this session.
concurrency: no other worker is claiming these paths right now (checked: every UI and RECORD claim above this one is `released:`; `bound-sweep.test.mjs` was last touched by ui41-agent, released; `app.html` by ui38-agent, released, and at blocks this item does not open).
released: 2026-08-07 · CONDUCT at integration — merged on main (worker 52bc9cf). **RENUMBERED UI-42 -> UI-46 at integration**: `IS-BUILD-PLAN.md`, Bob's confirmed authority, allocates UI-42 to "Version review: rotation and diff", and the collision protocol says the authority's number stands while the later allocation moves. Verified FROM THE MAIN CHECKOUT: battery 107/107 at 6,164 unchanged by this item (its own worktree measured 106/106 at 6,059 with ZERO delta, verified BOTH ways — before any edit, and again by reverting its three files to HEAD and diffing per suite); UI harness exit 0 read UNPIPED at 36 harnesses with bound-sweep 81 -> 134 and arm B 2,494 -> 2,502; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT RE-RAN NC (1) ITSELF**: restoring the false clause to RENDERED output makes the harness exit NON-ZERO with ARM F naming file, LINE, surface and op — and naming a DIFFERENT surface from the one armed, because the shared function reaches every screen, which is the design working. app.html restored and verified by hash AND content; the two remaining occurrences of the phrase are both COMMENTS recording the defect, checked rather than assumed. **THE FINDING THAT MATTERS MOST IS ABOUT THE INSTRUMENT: five capped ops were INVISIBLE to the walk that should have caught this**, because REC-59/REC-60 cap with `limit = null` plus an internal clamp while the walk read `limit = <digits>` — the roster saw 4 ops where the plane had 9, and eight call sites had inherited a cap they never stated. WALK 1 is rebuilt on a PROPERTY rather than a spelling, and REC-60's `op=list` rider is now honoured by MEASUREMENT rather than by an exemption. Routed: REC-67 (the loose matcher that turned another area's suite red), UI-48 (the five unstated bounds — the lesser class, routed because authoring five sentences is DEC-49's territory), DEC-64 (state a bound that did not bite?).
released:

### CORRECTION 2026-08-07 to the CLAIM above (ui42-agent, at the close of its own turn)

The claim reserved **`civicos-ui/test/subject-view.test.mjs`** and then **did not open it**, so the claim is corrected here rather than left over-broad (REC-59's precedent for exactly this). The subject view's concerns panel DID gain the record's bound — `subjConcernsHtml` now renders `concernsBoundHtml(c, "this list")` — but `subject-view.test.mjs` needed no change: it drives fixtures that carry no `limit`, so the panel takes the honest no-fallback branch and every one of its 33 assertions is unaffected and still green. The new screen's behaviour is pinned in `bound-sweep.test.mjs`'s ARM B2 instead, beside the op it belongs to, including the arm that the two screens render the SAME sentence through the SAME function. `git status` shows the file untouched.

**One path was NOT in the claim and was edited, which is the more important half of this correction:** the `limit:` argument was added at **eight** `recR("concerns"|"connections"|"resolutions")` call sites, and the claim named that. What it did not anticipate is **`getProjection`** — `op=projection` joined the bound roster today too, and rather than edit it the suite CLASSIFIES it: its single-bundle `&id=` arm returns the row before the plane computes any cap, and that plane-side fact is now ASSERTED against `store.mjs` so removing the arm turns the pin red. Classified, not exempted, and `app.html:1060` is unchanged.
## CLAIM 2026-08-07 RECORD
session: is-wave-w2a-pl8
opened: 2026-08-07T00:00:00Z
paths: the meaning-arm registry and its compilation in bio-plane/src/query.mjs (MEANING, meaningSql, selector's meaning branch, searchFields' meaning vocabulary), the inquiry_basis / resolutions grade-column indexes in bio-plane/src/schema.mjs, the meaning vocabulary block in Store#searchFields in bio-plane/src/store.mjs, bio-plane/test/meaningquery.test.mjs
interfaces consumed: none
interfaces owned: I3 (op contracts — op=search and op=searchfields gain vocabulary, additive), I5 (store schema — two indexes, additive)
expected: IS-BUILD-PLAN PL-8 — D-222 option A, the set-algebra ARM in the query compiler (`leg:`, `resolves:`, `concerns:`). Every arm keys on `fts_id`, inherits the one viewer-gate compilation point, and obeys MAX_COMPOUND. Discharges D-223: hunch debt enumerable at inquiry grain. Grade-column index decided by measurement. NOT option B (a second query path, closed by D-15) and NOT option C (the seventh statement shape — that is PL-9 and depends on this).
released: 2026-08-07 · CONDUCT at integration — merged on main (worker b42f87b). Verified FROM THE MAIN CHECKOUT: battery 108/108 at 6,270 with the +106 attributed by diffing per-suite counts (meaningquery +102 new, hygiene +3 for the two indexes, planning-hygiene +1) and every other suite byte-identical; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT RE-RAN NC (a) ITSELF** — D-223's own: removing the `leg` arm fails 35, the FIRST failure reading "D-223: the hunch question is ASKABLE AT ALL"; query.mjs restored, verified by hash AND content. **CONDUCT ALSO VERIFIED THE REVERTED INDEX rather than taking it on report**: `bundles_fts_id` is present in the migration at store.mjs:465, no duplicate exists in schema.mjs, and both newly shipped indexes are there — which matters because the probe had reported a 97% saving from an index the product has had for months, and the schema addition was already written before the worker caught itself. **THREE DECISIONS THE WORKER RAISED WERE RESOLVED BY CONDUCT rather than escalated, and one by a ruling Bob has ALREADY made:** (1) the `leg:capture` bare-word collision stays REFUSED with a warning naming both readings — on a debt surface a confidently wrong answer is the failure the item exists to remove; (2) a dropped arm WIDENS rather than narrows, which is **DEC-56/57/58's landed principle applied unchanged** — a silently narrowed hunch-debt answer is the sentence "you have none", an overclaim arriving through omission; (3) the gate's project half being unstageable through an arm is a STATED LIMIT in the suite header, not a decision. Routed: REC-68 (D-228), with the stale three-grade-sources schema comment as its rider.

---

## CLAIM 2026-08-07 UI

session: ui47-agent (worktree agent-a0eee2bad2223f9ad)
opened: 2026-08-07T00:00:00Z
paths: **`civicos-ui/app.html`**, named by SITE rather than by file: (a) `aiSessionRead()` inside the `__AI_SESSION__` block — the ONE function UI-38 left as the seam, wired to `op=airun`; (b) `aiSessionOpen()`, which must await it; (c) ONE line of `aiSessionPanelHtml()` adding the session's own published scalars through the EXISTING field-name-blind `aiSessionPairsHtml` (see the deviation note below); (d) the block's own header comment, corrected with dated reasons rather than left stating the pre-IS-6 world; (e) `SURFACES["ai-session"]`'s `reads` and `pending` in the registry block. **NOT opened:** `aiSessionIndicatorHtml`, `aiSessionInContext`, `aiSessionPairsHtml`, `aiSessionBudgetHtml`, `aiSessionPrincipalHtml`, `aiSessionConditionHtml`, `aiSessionTranscript`, `aiSessionRouteFromHash`, `RECIPES`, `CATALOG`/`SEMANTICS`, any other surface, any refusal string, any bound sentence UI-46 authored. `civicos-ui/test/ai-session-wire.test.mjs` (NEW — this item's own suite; drives the REAL plane in miniflare, the `intent-write.test.mjs` precedent). `civicos-ui/test/surface-registry.test.mjs` (UI-38's suite: ARMS X4, X5, Y1 and Y14 are SUPERSEDED BY IS-6 LANDING and are CORRECTED with dated reasons, never exempted). `docs/development/CLAIMS.md` (this entry).
interfaces consumed: I3 (the op contracts) — READ ONLY, and `op=airun` is consumed exactly as IS-6 published it. `bio-plane/src/index.mjs`, `src/store.mjs` and `src/airun.mjs` are READ (textually, and `airun.mjs` as a live module import) and the plane is DRIVEN in miniflare; **NOTHING INSIDE `bio-plane/**` IS EDITED**, and a zero-delta battery is this claim's evidence rather than its claim.
interfaces owned: none. No op, table, column or published shape changes.
expected: QUEUE.md UI-47 (queued as UI-43, renumbered) — the once-only running-session surface READS the run IS-6 now publishes. `allowed`/`consumed` stay separate with NO percentage and NO remainder computed by any route; `session: null` renders IDENTICALLY for an unknown run and for one the viewer may not see; no record still means NO INDICATOR rather than an invented "nothing is running"; no member-facing wording is authored — every value is the record's own, verbatim.
deviation, stated rather than discovered later: the item says "nothing else in that block moves", and its `accepts-when` REQUIRES a real run's **status** and **ticks** to render. UI-38's panel rendered only principal, budget and condition, so those two could not appear. The panel therefore gains ONE line — `aiSessionPairsHtml(session)`, the block's EXISTING field-name-blind renderer applied to the session's own scalars. No new function, no new field name, no authored word, and objects/arrays are skipped by that renderer so nothing is rendered twice. The acceptance governs over the prose, and the smallest change that satisfies it is recorded here rather than in a commit message.
accepts-when: as QUEUE.md UI-47. **Battery baseline 107/107 at 6,164 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** — the brief said 107/107 at 6,164 and is CONFIRMED rather than inherited. UI harness baseline: exit 0, 36 harnesses, `surface-registry` 359 assertions, envelope arm B 2,502 op answers across 67 ops. `node scripts/coverage.mjs --strict` baseline run DIRECTLY with `$?` unpiped: exit 0, OPS 136/136 reached, CHECKS 59/59 named. NEGATIVE CONTROLS run and recorded in the new suite's own `NEGATIVE CONTROL:` line: (1) make the plane publish a DIFFERENT bound and the surface must MOVE WITH IT; (2) introduce a percentage by any route -> the harness FAILS; (3) render something for `session: null` that distinguishes unknown from unviewable -> FAILS; (4) neuter any walk -> REACH fails as a DELTA with the corpus PRINTED; (5) polarity checked on every pin; (6) an over-strictness arm. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and INTERFACE-CHANGES.md are not written by this session.
concurrency: no other worker is claiming these paths right now — every UI and RECORD claim above this one is `released:`; `app.html` was last held by ui42-agent (released) at blocks this item does not open, and by ui38-agent (released) at the block this item does open; `surface-registry.test.mjs` by ui38-agent, released.
released: 2026-08-07 · CONDUCT at integration — merged on main. Verified FROM THE MAIN CHECKOUT: battery 108/108 at 6,270 unchanged by this item (its own worktree measured 107/107 at 6,164 identical before and after, 0 of 105 counted suites moving, no bio-plane/** file opened); UI harness exit 0 read UNPIPED at 36 -> 37 harnesses with envelope arm B 2,502 -> 2,511 across 67 -> 68 ops; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT RE-RAN NC (0) ITSELF**: un-wiring the read makes the harness exit non-zero with `surface-registry` failing INDEPENDENTLY — "a described read nothing performs is fiction" — and the run also REPRODUCED the fail-fast defect the item reported, only one arm reporting. app.html restored, verified by hash AND content. **CONDUCT VERIFIED THE SWEEP'S HEADLINE FINDING INDEPENDENTLY rather than taking it on report**: `aiSessionIndicatorHtml` appears exactly ONCE in app.html — its own definition, no call site — so §14a's promise that any window focused on an inquiry or project shows a running indicator is genuinely UNDELIVERED. Routed as UI-49, carrying the fail-fast rider. Three decisions resolved by CONDUCT rather than escalated: a plane refusal rendering as no indicator STAYS (the plane already collapses may-not-see into does-not-exist, so a refusal collapsing the same way invents nothing, and the alternative needs wording DEC-49 owns); `pending` REMOVED rather than re-pointed (a declared gap that has closed is a claim that is no longer true); and the one declared deviation from the item's own wording accepted, because the item's accepts-when required it. DEC-49 gains a new consumer: src/airun.mjs's vocabulary texts are now member-facing.
released:
## CLAIM 2026-08-07 RECORD
session: is-wave-w3a-pl9 (worktree agent-ab57a3b614d4b7ef7)
opened: 2026-08-07T00:00:00Z
paths: **`bio-plane/src/query.mjs`** — the meaning-grain ROW registry added to the existing `MEANING` entries (`row`, `identity`, `refs`, `rowGrain`), the new `MEANING_LIMIT_DEFAULT`/`MEANING_LIMIT_MAX` pair, the SEVENTH statement builder `statements.meaning` and its `rows`/`meaningRows` compile input. **NOT** `viewerPredicate`, **NOT** `meaningSql`/`selector`/`meaningAtom` (PL-8's, read-only — the three arms are consumed, not changed), **NOT** `FIELDS`, `SORTABLE`, `DEFAULT_FACETS`, `MAX_COMPOUND`. **`bio-plane/src/store.mjs`** — ONE new method `meaningRows()` beside `search()`, its dispatch entry, and the `MEANING`/`MEANING_LIMIT_*` import; nothing else in the file is opened. **`bio-plane/src/index.mjs`** — one OPS row `meaningrows` and its name in `RETRIEVAL_READS`. **`bio-plane/checks/bio-checks.mjs`** — one new `MEANING_READ_CHECKS` block (C-23.1/C-23.2, DEC-49 codes + canned translations), appended; no existing check touched. **`bio-plane/test/meaningread.test.mjs`** (new, this item's suite). **`bio-plane/test/bounds.test.mjs`** — the roster count and the DRIVEN descriptor, CORRECTED with a dated reason because the new op joins REC-59's roster. **`bio-plane/test/gate-reads.test.mjs`** — one GATED classification row. `docs/development/INTERFACE-CHANGES.md` (the IC entry only, appended). `docs/development/CLAIMS.md` (this entry).
interfaces consumed: none
interfaces owned: I3 (op contracts — ONE new op, additive; IC filed, `INTERFACES.md` NOT bumped, which is CONDUCT's)
expected: IS-BUILD-PLAN PL-9 — D-222 option C, a SEVENTH statement shape on the SAME compiler returning meaning-GRAIN rows for the bundles in scope. One new op, enveloped, carrying REC-36's stricter withhold-the-row rule. NOT option B (a second query path, closed by D-15 and by `query.mjs:701-705`): the gate stays minted in exactly one function and the site COUNT PL-8 pinned stays at three.
accepts-when: as IS-BUILD-PLAN PL-9. **Battery baseline 108/108 at 6,270 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** (the brief said 108/108 at 6,270 and was right — recorded because seven briefs running have carried a stale figure). `node scripts/battery.mjs meaningread` green; full `npm run test:battery` green with the delta ATTRIBUTED per suite; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md and INTERFACES.md are not written by this session.
concurrency: PL-8's claim above is `released:`; no other live claim names `query.mjs`, `store.mjs`, `index.mjs`, `bio-checks.mjs`, `bounds.test.mjs` or `gate-reads.test.mjs` (checked over every unreleased block in this file).
released: 2026-08-07 · CONDUCT at integration — merged on main (worker c26326b). Verified FROM THE MAIN CHECKOUT: battery 109/109 at 6,387 with the +117 fully attributed (meaningread +106 new, bounds +8, hygiene +3) and every other suite byte-identical; coverage --strict run DIRECTLY, $? unpiped, exit 0, OPS 136 -> 137 all reached and CHECKS 59 -> 61 all named. **IC-26 taken to SETTLED by CONDUCT as ADDITIVE; I3 10.0.0 -> 10.1.0.** **CONDUCT VERIFIED THE CORRECTION THIS ITEM MADE TO PL-8's HEADER rather than taking it on report**: `#writeReadings` carries no `object_type` gate in its body and its call site is unconditional, so a project bundle CAN carry resolution rows and PL-8's "a project bundle can carry neither" was half false. That correction is what let REC-36's withhold rule be staged LIVE against a row a real uninvited member cannot see. **AND CONDUCT'S OWN RE-RUN OF THE VIEWER CONTROL ARMED THE WRONG FUNCTION** — `searchIndexCheck`'s local gate rather than the compiler's, which is precisely the point of one compilation point — **so it measured NOTHING, and that is stated rather than presented as a result.** The guard was verified directly instead: GATE_MARK is minted only inside `viewerPredicate`, and store.mjs:1083 throws the D-15 refusal without it. Three decisions resolved by CONDUCT rather than escalated: a basis stays returned WHOLE (a basis returned in part reads as a basis, and every row carries the filter columns so a caller can narrow and still know what it did not take); no cursor is minted, because the ORDER BY is already total on the primary key so `offset` is correct rather than merely convenient; and the 200/1000 pair is recorded at the site with its reason (a meaning-grain answer is denser than the page it derives from), named so Bob may overrule the numbers rather than the shape. No delegation is owed: no existing shape moves and the op is available and unconsumed.
released:

### CORRECTION 2026-08-07 to the CLAIM above (is-wave-w3a-pl9, mid-turn, before the edit)

**One path was NOT in the claim and IS edited: `bio-plane/test/meaningquery.test.mjs`** — PL-8's suite, whose claim is `released:`. The edit is **comment-only plus one assertion LABEL**, and it is a correction of a FALSE SENTENCE rather than a change of subject: that header states, as its honest gap, *"Meaning rows attach to inquiries and to captures, and a project bundle can carry neither."* The second half does not hold. `#writeReadings` is called for EVERY promote with no `object_type` test, so a PROJECT bundle carrying `data/provenance.json` gets a reading, reference rows and — after `op=resolve` — `resolutions` rows keyed on the project; `gate-reads.test.mjs` has been promoting exactly such a project since REC-30. MEASURED by driving it before designing around it: a grade-A resolution lands with `bundle_id` = the project. **Nothing in that suite's ASSERTIONS was wrong** — its structural arm is about `inquiry_basis` and is unchanged and still true — so the prose is corrected with a dated reason and the assertion's LABEL narrowed to say `leg:`, never exempted. The correction matters because the sentence would have talked the next worker out of the live participant control, which is exactly what it nearly did here.

**Also added to the claim and named here rather than left implicit:** `bio-plane/test/gate-reads.test.mjs` (ONE classification row for the new op in the `GATED` map — no assertion added, none changed) and `bio-plane/test/bounds.test.mjs` (the roster walk's FIFTH cap shape, its `OPS.size` pin corrected 14 → 16 with a dated reason, and two DRIVEN descriptors). Both were in the claim; this records what was actually done to each.

---

## CLAIM 2026-08-07 UI

session: ui49-agent (worktree agent-ab5c84af95873d9de)
opened: 2026-08-07T00:00:00Z
paths: **`civicos-ui/app.html`**, named by SITE rather than by file: (a) inside the `__AI_SESSION__` block — a NEW `aiSessionContextHtml(ctx, store)`, the ONE door the rest of the application may use, plus the device-local ADDRESS roster (`aiSessionSeenStore`, `aiSessionSeen`, `aiSessionRemember`) and one line in `aiSessionOpen` that remembers an address the record answered for; `aiSessionIndicatorHtml` gains the record's own `status`, verbatim, so the ANIMATION is the record's claim rather than the surface's; (b) `openInquiry` — ONE call site, in the existing `Promise.all`; (c) `openProjectWorkspace` — ONE call site, after the visibility gate clears, in the header both branches render. **NOT opened:** `aiSessionRead`, `aiSessionInContext`, `aiSessionPairsHtml`, `aiSessionBudgetHtml`, `aiSessionPrincipalHtml`, `aiSessionConditionHtml`, `aiSessionPanelHtml`, `aiSessionTranscript`, `aiSessionRouteFromHash`, `SURFACES`/`RECIPES`, any other surface, any refusal string, any bound sentence. **the `.ai-run` STYLE RULES, and the claim is CORRECTED here rather than left wrong: they live in `civicos-ui/app.html`'s own `<style>`, not in `tokens.css`, which this session checked before editing and did not open.** `.ai-run .dot`'s animation becomes conditional on the record's own status word (the ONE copy of that word anywhere in this application, and it is PINNED against the plane's live `RUN_STATUS` so a rename fails the build rather than silently stopping the animation). `civicos-ui/test/ai-session-context.test.mjs` (NEW — this item's own suite; drives the REAL plane in miniflare and the REAL windows, the `ai-session-wire.test.mjs` precedent). `civicos-ui/test/ai-session-wire.test.mjs` (UI-47's suite: **ARM C2 is SUPERSEDED BY THIS ITEM and is CORRECTED with a dated reason, never exempted** — it forbade any call to the block's renderers from outside the block, which is exactly what UI-49 exists to add; ARM V's vocabulary is WIDENED to include `RUN_STATUS`). `civicos-ui/test/surface-registry.test.mjs` (the RIDER: `assert`-throwing `ok`/`eq` replaced by an ACCUMULATING collector so the first failing arm stops hiding every arm behind it — D-93's class, five recorded sightings). `docs/development/CLAIMS.md` (this entry, and its DELEGATIONS).
interfaces consumed: I3 (the op contracts) — READ ONLY. `bio-plane/src/index.mjs`, `src/store.mjs` and `src/airun.mjs` are READ (textually, and `airun.mjs` as a live module import) and the plane is DRIVEN in miniflare; **NOTHING INSIDE `bio-plane/**` IS EDITED**, and a zero-delta battery is this claim's evidence rather than its claim.
interfaces owned: none. No op, table, column or published shape changes.
expected: QUEUE.md UI-49 — §14a's actual promise delivered: an inquiry window and a project workspace show the running indicator, every rendered value the record's own, NO indicator where no record exists, and the unknown run and the unviewable one rendering identically.
**THE FINDING THAT RESIZES THIS ITEM, stated in the claim rather than discovered at integration: NO OP ANSWERS "WHICH RUNS ARE IN THIS CONTEXT".** `op=airun` and `op=airunlog` are BOTH keyed by run id (`store.mjs` `aiRunRead`/`aiRunLog`, `SELECT ... WHERE r.run = ?`), and `ai_runs` is queried by `run` at every one of its 14 sites. `op=airunopen` has no UI consumer either, so a browser never learns a run id by opening one. **A window therefore cannot ASK the plane whether a run is happening in it.** What this item can honestly build is the CALL SITE and one seam feeding it: the device's own roster of run ADDRESSES it has already opened — the member's own word, exactly as `#session/<id>` is — with **every other value, and the context match itself, coming from the record**. The half that reaches a teammate who never held the address needs a plane op that does not exist; it is DELEGATED below rather than faked here.
accepts-when: as QUEUE.md UI-49. **Battery baseline 108/108 at 6,270 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** — the brief said 108/108 at 6,270 and is CONFIRMED rather than inherited. UI harness baseline: exit 0 read UNPIPED, 37 harnesses, `surface-registry` 370 assertions, `ai-session-wire` 70, envelope arm B 2,511 op answers across 68 ops. `node scripts/coverage.mjs --strict` baseline run DIRECTLY with `$?` unpiped: exit 0, OPS 136/136 reached, CHECKS 59/59 named. NEGATIVE CONTROLS run and recorded in the new suite's own `NEGATIVE CONTROL:` line: (1) remove the call site again and the harness FAILS naming the WINDOW that lost it, not merely the function; (2) invent an indicator where no run exists -> FAILS; (3) distinguish the two absences -> FAILS; (4) neuter any walk -> REACH fails as a DELTA with the corpus PRINTED; (5) polarity checked on every pin; (6) an over-strictness arm; (7) **the rider's own: a control breaking SEVERAL arms of `surface-registry.test.mjs` must report ALL of them.** No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and INTERFACE-CHANGES.md are not written by this session.
concurrency: no other worker is claiming these paths right now — every UI and RECORD claim above this one is `released:`; `app.html` was last held by ui47-agent (released) at the block this item extends, `ai-session-wire.test.mjs` and `surface-registry.test.mjs` by ui47-agent (released), `tokens.css` by no open claim.
released: 2026-08-07 · CONDUCT at integration — merged on main (worker 42f9fe9). Verified FROM THE MAIN CHECKOUT: battery 109/109 at 6,387 unchanged by this item (its own worktree measured 108/108 at 6,270 with the delta attributed BOTH ways — reverting the four changed files to HEAD, re-measuring per suite, restoring, and verifying the restore by content and sha256: 106 counted suites, 0 moved, no bio-plane/** file opened); UI harness exit 0 read UNPIPED at 37 -> 38 harnesses with arm B 2,511 -> 2,557; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT VERIFIED THE ROUTED FINDING AND SHARPENED IT INTO THE ITEM:** `op=airunlog`'s read carries no LIMIT — confirmed at the source — **and REC-60's ratchet DID NOT CATCH IT.** `op=airunlog` appears in NONE of that walk's three buckets (BARE 27 unchanged, BOUNDED 8, UNJUDGED 20, the +1 there being PL-9's op=meaningrows), so the walk never reached the method at all. The ratchet exists precisely to catch a new unbounded read, so REC-70 is scoped to the BLIND SPOT with the op as its symptom — and it is the SECOND instrument blind spot in two days, after PL-9 found op=search outside the pin driving its own class. **The item's own best judgement was a change nobody asked for:** an unconditionally pulsing dot would assert a run is under way over a record that says it ENDED, so the dot carries the record's status verbatim, renders it as a WORD, and animates on it — the word in exactly one CSS rule, nowhere in the JS, pinned against the plane's live export so a rename FAILS THE BUILD, and failing SAFE either way. Three decisions resolved by CONDUCT rather than escalated: the device-local address roster stays (every rendered value is the record's, the device stores addresses and never context, and the seam is one function — REC-69 replaces one body); the roster is NOT folded into DEC-61's purge, because purging it would make the indicator vanish when the reasoning is deleted, paying §14a's price twice; and the status word stays in CSS, pinned. Routed: REC-69 (op=airuns) and REC-70 (the ratchet's blind spot).
evidence measured 2026-08-07 in this worktree: battery **108/108 at 6,270 BEFORE and 108/108 at 6,270 AFTER**, with the per-suite comparison run BOTH WAYS — the four changed files were reverted to HEAD, the battery re-measured per suite, and the files restored and verified by CONTENT and sha256 — **106 counted suites, 0 MOVED**. No `bio-plane/**` file opened. UI harness `node civicos-ui/test/run.mjs` exit **0 read UNPIPED**, **37 → 38 harnesses**: `ai-session-context` NEW at 77, `surface-registry` 370 → 376 (ARM S2a plus ARM Z's five), `ai-session-wire` 70 → 72 (ARM C3, C3b); envelope arm B **2,511 → 2,557 op answers across 68 distinct ops** (unchanged — `airun` was already exercised). `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` unpiped: **exit 0**, OPS 136/136 reached, CHECKS 59/59 named, unchanged. **NEGATIVE CONTROLS RUN x10 through the WHOLE harness**, every mutation passing an anchor-is-unique-and-the-bytes-changed guard first and every restore verified by CONTENT as well as sha256 — recorded in `ai-session-context.test.mjs`'s and `surface-registry.test.mjs`'s own `NEGATIVE CONTROL:` headers so re-running any of them is one step.
released:

### DELEGATION 2026-08-07 UI -> RECORD (the investigative-session area): NO OP LISTS RUNS BY CONTEXT

**What is needed:** an op that answers *which runs are attached to this inquiry or project* — `op=airuns&contextType=<inquiry|project>&contextId=<id>`, gated on `context_id` through the same `#bundleGate` `aiRunRead` already uses, publishing the same `session` shape per row, and **bounded with its bound PUBLISHED** (`limit`/`truncated`, REC-59/REC-60's pair) because a context may accumulate runs without limit.

**Why it is not optional.** `INVESTIGATIVE-SESSION.md` §14a promises that *any window focused on an inquiry or a project* shows the indicator, and names the price explicitly: *"a teammate watching the running-session indicator can see that work is happening and cannot read the reasoning."* **A teammate is the case this op is for.** Today `op=airun` and `op=airunlog` are both keyed by run id, so a window can only ask about a run whose address it already holds — and the member who already holds the address is precisely the member who did not need telling. UI-49 built the call site and every value on it comes from the record; what it cannot build is the question.

**What UI-49 shipped in the meantime, and why it is honest rather than a stand-in:** the window resolves the addresses THIS DEVICE has already opened, and asks the record about each one. The device supplies an ADDRESS and nothing else; the label, the status and **whether the run belongs to this window at all** are the record's answers. When the op lands, `aiSessionContextHtml` is the one function that changes — the call sites, the renderers and every pin over them stay.

### DELEGATION 2026-08-07 UI -> RECORD: `op=airunlog` PUBLISHES AN UNBOUNDED COLLECTION

`store.mjs` `aiRunLog` runs `SELECT ... FROM ai_run_log WHERE run = ? ORDER BY seq` with **no LIMIT, no `limit` and no `truncated`** — an append-only log read whole. That is REC-57/REC-59's boundedness class, and it is the reason UI-49 DECIDED the observation log is a SEPARATE surface rather than the indicator's click-through (the reasoning is at the site, in `app.html`'s `__AI_SESSION__` block). A UI built against it today would be a surface with a bound it cannot state, which is exactly what UI-46 spent an item removing.
## CLAIM 2026-08-07 RECORD
session: is-wave-w4b-pl10 (worktree agent-a9da89709e5e1c419)
opened: 2026-08-07T00:00:00Z
paths: **`bio-plane/src/store.mjs`** — ONE new read method `versionChain()` beside `capturedLocators()` in the "Links: what a document pointed at" block, its two named cap constants, and its ONE dispatch entry. **NOT** `recordCapturedLocator` (the single write site — asserted UNCHANGED rather than edited), **NOT** `capturedLocators`, **NOT** `#conditionBundlesForHost` (its join is the precedent, read-only), **NOT** `#bundleGate`/`viewerPredicate`. **`bio-plane/src/index.mjs`** — one `OPS` row `versionchain`, its name in `REC30_VIEWER_READS`, and one handler that normalises the address through the SAME `normalizeAddress` the write path used. **`bio-plane/checks/bio-checks.mjs`** — one new `VERSION_CHAIN_CHECKS` block (C-24.1/C-24.2/C-24.3, DEC-49 codes + canned translations), appended; no existing check touched. **`bio-plane/test/versionchain.test.mjs`** (new, this item's suite). **`bio-plane/test/bounds.test.mjs`** — the roster count and ONE DRIVEN descriptor, CORRECTED with a dated reason because the new op joins REC-59's roster. **`bio-plane/test/gate-reads.test.mjs`** — one GATED classification row. `docs/development/INTERFACE-CHANGES.md` (the IC entry only, appended). `docs/development/CLAIMS.md` (this entry).
interfaces consumed: none
interfaces owned: I3 (op contracts — ONE new op, additive; IC filed, `INTERFACES.md` NOT bumped, which is CONDUCT's)
expected: IS-BUILD-PLAN PL-10 — D-220's op. The document-version chain read off `captured_locators ⋈ register`: every version at an address, in date order, with its bundle. **NO EDGE BETWEEN VERSIONS IS ADDED** — no `supersedes` relation, no new table, no new column, no second index. The link Bob ruled for is the join, EXPOSED, and that is asserted STRUCTURALLY rather than promised. D-221's wrong-predecessor is fixed at the same join: the predecessor is chosen by ADDRESS EQUALITY and date order, never by FTS relevance.
accepts-when: as IS-BUILD-PLAN PL-10. **Battery baseline 109/109 at 6,387 MEASURED IN THIS WORKTREE BEFORE ANY EDIT, TWICE** (the brief said 109/109 at 6,387 and is CONFIRMED rather than inherited — recorded because eight briefs running have carried a stale figure). Coverage baseline run DIRECTLY with `$?` unpiped: exit 0, OPS 137/137 reached, CHECKS 61/61 named. `node scripts/battery.mjs versionchain` green; full `npm run test:battery` green with the delta ATTRIBUTED per suite; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and DEBT.md are not written by this session.
concurrency: PL-8's and PL-9's claims above are `released:`; no other live claim names `store.mjs`, `index.mjs`, `bio-checks.mjs`, `bounds.test.mjs` or `gate-reads.test.mjs` (checked over every unreleased block in this file). `civicos-ui/app.html` is NOT opened — D-221's defective site is UI's path and is a DELEGATION, recorded below.
released: 2026-08-07 · CONDUCT at integration — merged on main (worker 1e220c0). Verified FROM THE MAIN CHECKOUT: battery 110/110 at 6,486 with the +99 fully attributed (versionchain +91 new, bounds +5, hygiene +3) and every other suite byte-identical; coverage --strict run DIRECTLY, $? unpiped, exit 0, OPS 137 -> 138 all reached and CHECKS 61 -> 64 all named. **IC-27 taken to SETTLED by CONDUCT; I3 10.1.0 -> 10.2.0.** Routed as **UI-50**, and it is the urgent half: `heldMatch`'s FTS lookup returns the OLDEST version and that sentence is written PERMANENTLY into each new bundle, so every day on the old lookup adds another bundle carrying a wrong predecessor. **PL-10 also established that D-228's quoted-value defect does NOT bite the FTS columns — so this is not matching nothing, it is confidently matching the WRONG ROW, the worse of the two.** **AT INTEGRATION: this item and REC-70 EACH corrected `bounds.test.mjs`'s roster pin 16 -> 17 for DIFFERENT ops, and each filed an IC-27.** Both comment blocks are kept and the count is corrected to 18 — taking either figure would have left the pin asserting 17 over a roster of 18, a hand-carried count going stale inside the suite whose subject is that counts go stale. PL-10 merged first so IC-27 is its; REC-70's moved to IC-28.
released:

## DELEGATION 2026-08-07 RECORD → UI
from: is-wave-w4b-pl10 (IS-BUILD-PLAN PL-10 / D-220 consumer (1), D-221)
need: **`civicos-ui/app.html`'s `heldMatch` (~13614) should stop searching for prior versions and ASK for them.** Replace the `locator:"<url>"` query with `op=versionchain&address=<url>&at=<this capture's sha256>` and take the answer's `predecessor`. The new op is filed as IC-27 and is live on `main` once CONDUCT integrates; it answers by ADDRESS EQUALITY in date order, so the predecessor it returns is the temporally previous version rather than whatever bm25 ranked first.
why: **D-221, and it writes a permanent wrong fact.** `locator` is FTS-indexed, so a fielded query on it compiles to a text atom, which creates a rank arm, which orders by RELEVANCE; every capture at one address carries identical URL text, the scores tie, and `bundle_id ASC` decides. **MEASURED rather than reasoned, 2026-08-07, in `bio-plane/test/versionchain.test.mjs` against a sixty-version fixture: the FTS route returns the OLDEST version at the address FIRST** — the snapshot fifty-nine versions back, offered as "the one before this" — while the join returns the fifty-ninth. The sentence at ~`app.html:13485` is written into the bundle body and is **permanent in that bundle**, so every run on the old lookup adds another bundle carrying a wrong predecessor. This is a replacement, not a patch: the join answers the exact question the search was approximating, and D-220 records that as the reason it is consumer (1).
also worth knowing, measured in passing: **D-228's quoted-value defect does NOT bite the FTS columns.** `locator:"<url>"` and `locator:<url>` both matched (63 hits each in the fixture) and both returned the oldest first. So `heldMatch` is not silently matching nothing — it is confidently matching the wrong row, which is the worse of the two.
status: open — UI's to schedule. `bio-plane` owes nothing further; the op is available and unconsumed.

---

## CLAIM 2026-08-07 RECORD

session: rec70-agent (worktree agent-ac1c17ac625e005c5)
opened: 2026-08-07T00:00:00Z
paths: **`bio-plane/src/store.mjs`**, named by SITE rather than by file: (a) the new `AI_RUN_LOG_LIMIT_DEFAULT`/`AI_RUN_LOG_LIMIT_MAX` constant pair, placed beside `AI_RUN_LEASE_MS` with its reasoning; (b) `aiRunLog`'s body and doc comment — the `limit` parameter, the clamp, `LIMIT ?`/`cap + 1`, the `slice`, the `limit:`/`truncated:` keys on BOTH returns, and the CAUSE of the ratchet's blindness recorded IN THE BODY; (c) ONE line in the `op:` dispatch map — `airunlog:` gains `limit: url.searchParams.get("limit")`. **NOT opened:** `aiRunRead`, `aiRunTick`, `aiRunOpen`, `aiRunClose`, `#aiRunLogAppend`, `#bundleGate`, any other method, any schema table, any refusal string. **`bio-plane/test/meaning-bounds.test.mjs`** — the walk's success-marker gate INVERTED, the `dispatchedOps` denominator, the OPAQUE tripwire, the REACH block, the ratchet moved 27 → 40 with a FLOOR beside the ceiling, REC-70's source and live arms, two over-strictness arms and a refusal-exclusion arm, and the `NEGATIVE CONTROL:` header. **`bio-plane/test/bounds.test.mjs`** — the sibling instrument: one run fixture, one `DRIVEN` descriptor for `op=airunlog`, and its `OPS.size` pin **CORRECTED 16 → 17 with a dated reason, never exempted**. `docs/development/INTERFACE-CHANGES.md` (IC-27, PROPOSED — the version bump and RESOLUTION are CONDUCT's). `docs/development/CLAIMS.md` (this entry and its DELEGATION).
interfaces consumed: none new.
interfaces owned: **I3 — IC-27 filed, PROPOSED. The version bump is NOT taken by this session** (IC-25's precedent). I5 NOT touched: no table, column, index or `purge` change.
expected: QUEUE.md REC-70 — **the blind spot is the item and `op=airunlog` is the symptom.** THE CAUSE, NAMED AND MEASURED: `meaning-bounds.test.mjs` graded only return objects containing the literal **`ok: true`**, and `aiRunLog` answers **`found: true`** — one success spelling hard-coded as the only one, four lines after the same file wrote its bound and completeness keys as SETS *because the plane spells the second five ways on purpose*. **The instrument avoided the one-vocabulary mistake in its leaves and committed it at its root.** THE SCALE: `store.mjs` dispatches **156** ops and the walk graded **55**; **27 dispatched ops answer success without `ok: true`** — `found: true` (`airunlog`, `airun`, `airuntick`) or NO marker at all (`op=signerlist` → `{ signers }`, `op=publishedlist`, `op=inboxlist`, `op=memberlist`, `op=verify`, `op=index`, `op=thread`, `op=selection`, `op=reusedparts`, `op=reuseverdicts`, `op=readingnameplan`) — and **fourteen of them were BARE all along.** THE FIX INVERTS THE TEST rather than lengthening a list: the walk grades every return that does not DECLARE itself a refusal (`ok: false`), reaching **82 of 156**. **The fix is deliberately NOT `ok: true` on `aiRunLog`** — that would buy a green walk and leave the blindness for the next op that spells success a third way; the method keeps `found: true` so the corrected walk's verdict on it is evidence rather than coincidence. AND THE INVERSION IS NOT TRUSTED ON ITS OWN: a **REACH** block now asserts every DISPATCHED op lands in exactly one of four buckets (bare/bounded/unjudged/no-collection), and an **OPAQUE** tripwire ratchets the ops that SCAN ROWS and are DISPATCHED yet reach no verdict — **24 on the broken walk, `op=airunlog` among them; 8 on the corrected one, pinned BY NAME so a swap fails.** That arm is what would have caught this op on the day IS-6 added it. `op=airunlog` bounded at **default 200 / ceiling 5000**, both figures the plane's own and **the PAIR justified at the site rather than copied**: 200 is `op=exportlog`'s, the plane's only other append-only `seq`-ordered log, for the reader CHECKING a run (§11); 5000 is `op=list`'s shared ceiling for §14b.7's RESUMED run, for which a cut it cannot see is work silently redone. ASCENDING order KEPT so the cut falls at the END — reusing `op=exportlog`'s newest-first ordering would have handed a resumed run the end of its own history.
accepts-when: as QUEUE.md REC-70. **Battery baseline 109/109 at 6,387 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** — the brief said 109/109 at 6,387 and is CONFIRMED rather than inherited. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` unpiped. NEGATIVE CONTROLS run and recorded in `meaning-bounds.test.mjs`'s own `NEGATIVE CONTROL:` header so re-running any of them is one step.
concurrency: no other worker is claiming these paths — every RECORD claim above is `released:`; `store.mjs`'s `aiRunLog` was last written by IS-6, `meaning-bounds.test.mjs` by rec60-agent (released), `bounds.test.mjs` by pl9-agent (released). UI-49's claim is on `civicos-ui/**` only and no `bio-plane/**` file was opened by it.
released: 2026-08-07 · CONDUCT at integration — merged on main. Verified FROM THE MAIN CHECKOUT after BOTH merges: battery 110/110 at 6,521, coverage --strict run DIRECTLY, $? unpiped, exit 0, OPS 138/138 reached and CHECKS 64/64 named. **IC-28 taken to SETTLED by CONDUCT (renumbered from IC-27 at integration — see IC-28's head); I3 10.2.0 -> 10.3.0.** **THE BLIND SPOT WAS 27 OPS, NOT ONE.** The walk graded 55 of 156 dispatched ops and READ AS A COMPLETE SWEEP; 14 of the 27 hidden were BARE all along. The cause is one line — a classifier admitting only one spelling of success, four lines after that same file's other matchers were written as SETS precisely because the plane spells things several ways: **the instrument avoided the one-vocabulary mistake in its leaves and committed it at its root.** The fix INVERTS the test rather than lengthening a list, and deliberately does NOT change the op's spelling, because that would buy a green walk and leave the blindness. **And a CEILING IS NOT A RATCHET — neutering the walk left it green at 0 of 40, so REC-60's ratchet could only ever have failed from a reader that GAINED sight; the FLOOR is the more important half.** **Its own control found that `airun.test.mjs` — the op's OWN suite — could never have caught this**, driving the op at six sites none of which asks past 200 rows. The item's stale-reason rider is routed onto UI-50. D-227 is closed FOR THIS OP by a direct SQL pin and still holds for three others, riding REC-66.
released:

### DELEGATION 2026-08-07 RECORD -> UI: ONE REASON IN `app.html` IS NOW STALE

`civicos-ui/app.html`'s `__AI_SESSION__` block gives FOUR numbered reasons for keeping the observation log a separate surface. **Reason 4 reads *"AND IT PUBLISHES AN UNBOUNDED COLLECTION. `aiRunLog` selects the whole append-only log with no `LIMIT`, no `limit` and no `truncated`. A surface built on it today would be a surface with a bound it cannot state."* REC-70 makes that false**: `op=airunlog` now applies a bound (default 200, ceiling 5000) and publishes `limit` and `truncated`, on the not-found answer as well.

**Reasons 1–3 are untouched and the decision STANDS.** This does not reopen it — it removes one of its supports, and a support that has become false is worse than a missing one, because the next reader takes it as current. **Nothing fails**: it is a comment, no suite pins it, and this is exactly the kind of change that reaches nobody unless it is written down.

**What UI would change:** that one numbered reason, corrected to say the log IS bounded now and that the remaining three reasons carry the decision. RECORD does not edit `civicos-ui/**`.

---

## CLAIM 2026-08-07 VERIFY

session: vf2-agent (worktree agent-a12a5d578497244e9)
opened: 2026-08-07T00:00:00Z
paths: **`civicos-ui/check-refusal-codes.mjs` (NEW)** — the DEC-49 harness guard, on `check-mock-envelope.mjs`'s precedent (a `civicos-ui/check-*.mjs` that reads `bio-plane/` sources read-only and runs inside `test/run.mjs`). **`civicos-ui/test/refusal-codes.test.mjs` (NEW)** — the guard's own suite: its fixture trees, its over-strictness arms, and its `NEGATIVE CONTROL:` header. **`civicos-ui/test/refusal-codes.control.mjs` (NEW — AMENDED INTO THIS CLAIM 2026-08-07, same session, before any handoff)** — the seven negative-control arms RUN against the REAL tree, each armed, restored, and the restore verified BY HASH, BY CONTENT and by RE-RUNNING. Deliberately not a `.test.mjs`: it is destructive while it runs, so the battery must not run it. **`civicos-ui/test/run.mjs` — TWO LINES**: the guard's invocation, and the `check-semantics.mjs` call wrapped in the same try/catch its two siblings already have (D-93's class: an uncaught `execFileSync` there aborts the run and hides the two guards behind it). `docs/development/CLAIMS.md` (this entry and its DELEGATIONS), `docs/development/MEASUREMENTS.md` (one appended entry). **NOT OPENED: no file under `bio-plane/` and no line of `civicos-ui/app.html`** — the guard is READ-ONLY over both, and every gap it finds in either is a DELEGATION below, not an edit. The control script edits `bio-checks.mjs`, `airun.mjs` and `app.html` **transiently while it runs and restores every one**, which is `check-mock-envelope.mjs`'s own precedent for a control over another area's file.
interfaces consumed: I3 (read-only — the guard reads the plane's refusal vocabulary and asserts about it; it changes no shape).
interfaces owned: none.
expected: IS-BUILD-PLAN VF-2 — **DEC-49's harness guard (E7)**, the instrument REC-64's sweep must pass. Every code a surface can receive has a canned translation, and an untranslated code FAILS THE HARNESS rather than reaching a member. W0, slot-free.
accepts-when: `node civicos-ui/test/run.mjs` green with the guard ON; a codeless refusal in a fixture makes the harness FAIL naming it; removing the guard makes that fixture PASS. Battery baseline measured in this worktree before any edit — **110/110 at 6,521, CONFIRMED rather than inherited from the brief**. `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` unpiped.
concurrency: no live claim names any path above. Every live `UI` claim is on `civicos-ui/app.html` by SITE; every live `RECORD` claim is on `bio-plane/**`. This claim opens neither.
released: 2026-08-07 · CONDUCT at integration — merged on main (worker 7a9e53d). Verified FROM THE MAIN CHECKOUT: battery 110/110 at 6,521 with ZERO delta and no bio-plane/ file opened; UI harness exit 0 read UNPIPED at 38 -> 39; coverage --strict run DIRECTLY, $? unpiped, exit 0. **CONDUCT RE-RAN THE TEETH CONTROL ITSELF**: planting a codeless refusal inside a governed function makes the harness exit NON-ZERO naming file, line, function AND the offending object literal; airun.mjs restored, verified by hash and content. **It measures REC-64's remaining work rather than doing it, and the measurement is the value: 311 codes the plane can mint, 98 in reach, 74 of those untranslated AND NAMED BY CODE ON EVERY RUN, 287 out of reach reported but not gated** — because a gate above the current state gets switched off. Floor and ceiling both, since a ceiling alone passes trivially over nothing. The guard found two defects in ITSELF (a parameter list read as a body; a character floor that failed a real 19-character sentence), a control armed wrong caught itself, and `test/run.mjs` carried a D-93-class defect that hid BOTH guards behind an unguarded call — corrected, not exempted. Three decisions resolved by CONDUCT rather than escalated: the reach ceiling stays GATED (DEC-49 calls the guard not optional, and every IS fence inherits it); one-code-one-home stays gated at zero; and the 287-code census gap stays REPORTED rather than gated, with the widening left as Bob's to ask for. Delegations routed to REC-64, UI and RECORD.
released:

### DELEGATION 2026-08-07 VERIFY -> whoever runs REC-64: THE SWEEP'S SIZE IS NOW MEASURED, AND ITS CEILING IS ARMED

**REC-64 no longer has to guess how big it is.** `node civicos-ui/check-refusal-codes.mjs` prints, every run:

- **311 distinct refusal codes the plane can mint** (`bio-plane/src/**`, union of six matchers). A plain `reason: "CODE"` grep answers **294** — the other 17 arrive by a ternary (`reason: platform ? "PLATFORM_LIMIT" : "FETCH_FAILED"`), by a comparison site, or through a family lookup where the code is a VARIABLE and no source-text matcher can see it at all.
- **98 of those are IN REACH of a surface** — 11 are DEC-49 family rows, 50 are named by `app.html`, 65 are sent by a harness mock (overlapping).
- **74 of the 98 have no canned translation today, and the guard NAMES ALL 74 on every run.** That is REC-64's remaining work inside the ruling's own scope, and it is a **CEILING**: it may only ever FALL. A new receivable code with no translation fails the harness.
- **287 of the 311 have no translation and are not in reach today.** Reported, never gated — see the guard's header for why gating the whole plane would fail on internal refusals no member can meet.

**The two homes DEC-49 licenses are both wired and a code may use ONE:** a row in a `*_CHECKS` family in `bio-plane/checks/bio-checks.mjs` (11 rows over 3 families today), or an entry in a surface table the guard proves TOTAL against its producer (`PART_REASON` over `src/subresources.mjs`, 13/13). A code translated in both FAILS.

**Families are HARVESTED by export name matching `/_CHECKS$/`, never listed.** A fourth family minted by PL-1 or PL-12 is guarded the turn it lands. Lower `CEILING.reachGap` in the same turn you translate anything.

### DELEGATION 2026-08-07 VERIFY -> UI: `PART_REASON` IN `app.html` IS NOW A GUARDED SURFACE TABLE

`app.html`'s `PART_REASON` is the one surface-authored code-to-wording table in the tree, and DEC-49 licenses it. **It is now proved TOTAL over `bio-plane/src/subresources.mjs` on every harness run**, in both directions: a code the producer mints with no wording FAILS naming it, and wording for a code the producer no longer mints is reported (not a failure — an older instance can still send it).

**Two consequences UI should know before touching it.** (1) The render site's `PART_REASON[s.reason] || String(s.reason)` fallback is safe ONLY because the table is proved total; the guard is what makes that line honest, so do not treat the fallback as the safety net. (2) The pairing lives in `TABLE_PRODUCERS` in `civicos-ui/check-refusal-codes.mjs` (VF-2's file), on `check-mock-envelope.mjs`'s FLAT_OPS precedent. **A SECOND code-to-wording table added to `app.html` will FAIL the harness until it is paired with a producer there** — found by SHAPE, so it cannot arrive unnoticed. VERIFY owns that one line; append a delegation rather than editing it.

### DELEGATION 2026-08-07 VERIFY -> RECORD / whoever adds a member-facing refusal

**Every new refusable condition now owes a code and a canned translation, and the harness enforces it rather than a convention doing so.** Two shapes fail `node civicos-ui/test/run.mjs`:

- **A CODELESS refusal at a governed site** — an `ok:false` with no `code` and no `reason` inside a function named by a family row's `where` field. Fails naming the file, the line, the function and the offending object literal. **The `where` field is therefore LOAD-BEARING**: the guard opens it and reads that function. A `where` that stops resolving fails too.
- **A code minted at a governed site with no row in its family**, which would go out with nothing behind it.

The cheapest correct move is a row in the family that already covers the site: `{ check: 'C-nn.n', where: 'src/<file>.mjs <fn>', translation: '<the sentence a member reads>' }`. The guard also refuses a translation that restates the machine code, two codes sharing a C-number, and two codes sharing a translation.
## CLAIM 2026-08-07 RECORD

session: is-wave-w2b-pl1 (worktree agent-ac7eae62a24120bf9)
opened: 2026-08-07T00:00:00Z
paths: **`bio-plane/src/schema.mjs`** — TWO new tables `inquiry_basis_versions` and `inquiry_basis_version_legs` with their indexes, placed BEFORE the `host_governor` block (the hygiene trap); no existing table, column or index touched. **`bio-plane/src/store.mjs`**, named by SITE: (a) the version arm inside `promote`'s existing validation phase and the version PROJECTION inside `promote`'s existing transaction, immediately after the `inquiry_basis` projection it sits beside — **there is no second write site and that is the item's whole constraint**; (b) the two new table names in `purge`'s `TABLES` list (D-113); (c) ONE new read method `basisVersions()` and its ONE dispatch entry; (d) the composition-hash helper the freeze compares on. **NOT** `#strengthWalk`, **NOT** `#axisResult`, **NOT** the `inquiry_basis` projection's own INSERT, **NOT** `#bundleGate`/`viewerPredicate`, **NOT** any aiRun method. **`bio-plane/src/index.mjs`** — one `OPS` row `basisversions`, its name in `REC30_VIEWER_READS`, one handler. **`bio-plane/checks/bio-checks.mjs`** — one new `BASIS_VERSION_CHECKS` registry (C-25.x, DEC-49 codes + canned translations), one new exported `basisVersionFindings()`, and ONE call to it from `checkInquiryBasis` above its no-basis early return (checkGrounds' own precedent); no existing check's behaviour changed. **`bio-plane/test/versions.test.mjs`** (new, this item's suite). **`bio-plane/test/bounds.test.mjs`**, **`bio-plane/test/gate-reads.test.mjs`**, **`bio-plane/test/meaning-bounds.test.mjs`** — roster/classification/ratchet rows for the ONE new op, each CORRECTED with a dated reason, never exempted. `docs/development/INTERFACE-CHANGES.md` (the IC entry only, appended). `docs/development/CLAIMS.md` (this entry).
interfaces consumed: none
interfaces owned: **I3 — one new op, additive; IC filed PROPOSED, the version bump and RESOLUTION are CONDUCT's.** **I5 — TOUCHED: two new tables, two new index pairs, two new `purge` entries.**
expected: IS-BUILD-PLAN PL-1 — basis VERSIONS (IS-1), the spine of the IS build. Versions attach to the INQUIRY'S BASIS: frozen once written, unique name per inquiry, description required, ground partition AND the AND/OR relationship carried on every version, `derived_from` as the derivation edge (its first real producer), hide-only prune flag, reword user-selectable, and a regroup that rides REC-45's attributed act (DEC-50). **Built through `promote`'s ONE write site — the version block is authored in `bundle.md` frontmatter and PROJECTED, exactly as `inquiry_basis` is, so no table is a second place to state a fact (D-21).** §14b.7 is an acceptance: a version survives the death of the run that proposed it.
accepts-when: as IS-BUILD-PLAN PL-1. **Battery baseline 110/110 at 6,521 MEASURED IN THIS WORKTREE BEFORE ANY EDIT — the brief said 110/110 at 6,521 and is CONFIRMED rather than inherited.** `node scripts/battery.mjs versions` green; full `npm run test:battery` green with the delta ATTRIBUTED per suite; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and DEBT.md are not written by this session.
concurrency: every RECORD claim above this one is `released:`. Checked over every unreleased block in this file: no live claim names `schema.mjs`, `store.mjs`, `index.mjs`, `bio-checks.mjs`, `bounds.test.mjs`, `gate-reads.test.mjs` or `meaning-bounds.test.mjs`. `civicos-ui/**` is NOT opened — UI-42/UI-45's version surfaces are UI's and depend on this item, not the other way round.
released: 2026-08-08 - CONDUCT at integration - merged on main (branch pl1-basis-versions). Verified FROM THE MAIN CHECKOUT: battery 111/111 at 6,607 with the +86 fully attributed (versions +77 new, bounds +5, hygiene +3, meaning-bounds +1) and 105 suites unchanged; coverage --strict run DIRECTLY, $? unpiped, exit 0, OPS 138 -> 139 all reached and CHECKS 64 -> 82 all named. **CONDUCT RE-RAN THE FREEZE CONTROL ITSELF** - it is the item's core guarantee: removing the refusal makes the suite fail wanting [false, VERSION_FROZEN, opening account], with the stored version moving under a reader who already had it; store.mjs restored, verified by hash and content. **IC-29 taken to SETTLED; I3 10.3.0 -> 10.4.0, and I5 IS TOUCHED** - two projection tables, both in purge, with the control proving it (remove one and hygiene names the table). Two delegations to CONDUCT's own files are DONE in the same turn: CLAUDE.md's trap list gains the semicolon-inside-an-inline-comment trap (schema.mjs is split on ';' before execution, so a comment semicolon truncates the statement and every promote fails - the same class as the backtick trap already there), and D-226's stale row is annotated, since INVESTIGATIVE-SESSION.md section 0 enacts its resolution and PL-1 built against that rather than the row. PL-2 inherits the vocabularies as exports and OWNS wiring the transitive basis-cycle check at accept - recorded rather than half-built, because a second cycle walk would drift from the first.
released:

## CLAIM 2026-08-07 RECORD

session: is-wave-pl12 (worktree agent-a676fcc6b8f3aa6c8)
opened: 2026-08-07T00:00:00Z
paths: **`bio-plane/checks/bio-checks.mjs`** — `BUNDLE_ID_RE` (the `BIAS` prefix added to the alternation), `OBJECT_TYPES` (`BIAS: 'bias'`), `HEADINGS.bias`, `STATES.bias`, `knownSchemas` (`bias@1`), the new `BIAS_STATEMENT_KINDS` / `BIAS_CHECKS` exports, `checkBiasExtension` and its ONE call site inside `checkBundle`. **NOT** any existing check body, **NOT** `HEADINGS.inquiry/focus/project/action/information`, **NOT** `STATES.inquiry` and none of its siblings. **`bio-plane/src/schema.mjs`** — TWO new tables (`bias_statements`, `bias_adoptions`) placed BEFORE the `host_governor` block. **`bio-plane/src/store.mjs`** — `#writeBiasProjection` and its ONE call inside `promote`'s transaction; the new `biasManifest` / `biasAdopt` / `biasInhale` methods and their three dispatch entries; `purge`'s `TABLES` list and its per-bundle `scope_id` arm; `stats()`; and `aiRunRead`'s answer, which gains ONE `bias` block. **NOT** `aiRunOpen`, `aiRunTick`, `aiRunClose`, `#aiRunTerminate`, `#aiRunAppend`, `aiRunLog`, `#bundleGate`. **`bio-plane/src/index.mjs`** — three `OPS` rows, two `NEEDS` rows, one name in the viewer-read list. **`bio-plane/test/bias.test.mjs`** (new, this item's suite). **`bio-plane/test/bounds.test.mjs`** — the roster pin and ONE DRIVEN descriptor, CORRECTED with a dated reason. **`bio-plane/test/gate-reads.test.mjs`** — one GATED classification row. `docs/development/INTERFACE-CHANGES.md` (the IC entry only, appended). `docs/development/CLAIMS.md` (this entry and its DELEGATION).
interfaces consumed: none
interfaces owned: I3 (op contracts — THREE new ops, additive; IC filed, `INTERFACES.md` NOT bumped, which is CONDUCT's). I5 (two new tables, both in `purge`).
expected: IS-BUILD-PLAN PL-12 — **D-84, the bias object**, plus DEC-54's four scopes ENFORCED IN CODE: (a) a `required_strength` bar filed as a bias statement is REFUSED and the inhale returns bars and statements as two lists; (b) the unenforceable residue is a PUBLISHED OUTPUT of both the inhale and the manifest read, and an adopted bundle whose residue heading is empty is REFUSED; (c) the inhale holds NO write path at all — asserted at the SOURCE, not promised; (d) an adoption PINS source, retrieval date and content hash at the authored moment. DEC-46 and the HUNCH-DEBT vocabulary correction are NOT re-litigated: the acknowledgement stays authored-at-export under C-21.1 and ordinary declared bias stays disclosed and travelling (DEC-20).
REBASED 2026-08-08 onto a24f2b0 (PL-1), and TWO COLLISIONS were resolved at that rebase under the earlier-merge-keeps-its-numbers protocol: this item's check family moved C-25.x -> C-26.x (PL-1 took C-25.1-C-25.18) and its interface entry moved IC-29 -> IC-30 (PL-1 took IC-29, already SETTLED). Neither session could see the other. Post-rebase measurement: battery 111/111 at 6,607 -> 112/112 at 6,746, +139 attributed (bias +127 new, bounds +9, hygiene +3); coverage --strict exit 0 read UNPIPED, OPS 139 -> 143, CHECKS 82 -> 93; the bounds roster pin MEASURED at 21 rather than added up, and meaning-bounds' bare ratchet still holds at 40 against PL-1's corrected classifier.
accepts-when: as IS-BUILD-PLAN PL-12. **Battery baseline 110/110 at 6,521 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** — the brief said 110/110 at 6,521 and is CONFIRMED rather than inherited. `node scripts/battery.mjs bias` green; full `npm run test:battery` green with the delta ATTRIBUTED per suite; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and DEBT.md are not written by this session.
concurrency: every RECORD claim above this one is `released:`. Checked over every unreleased block in this file: no live claim names `bio-checks.mjs`, `schema.mjs`, `store.mjs`, `index.mjs`, `bounds.test.mjs` or `gate-reads.test.mjs`. `civicos-ui/**` is NOT opened — the type vocabulary's seven UI tables are a DELEGATION, recorded below.
released: 2026-08-08 - CONDUCT at integration - merged on main after a rebase CONDUCT sent it back for. Verified FROM THE MAIN CHECKOUT: battery 112/112 at 6,746, coverage --strict run DIRECTLY with $? unpiped exit 0, UI harness exit 0. Two collisions resolved: C-25.x renumbered to C-26.x (PL-1 took the family in parallel) and IC-29 to IC-30 (already SETTLED). The roster pin was MEASURED at 21 - and both arithmetic paths coincidentally give 21, which is exactly why arithmetic was not evidence. Its finding stands on the record: the run was carrying NOTHING, not the stated absence.
released:

### DELEGATION 2026-08-07 RECORD -> UI: THE TYPE VOCABULARY GAINED A FIFTH CANONICAL TYPE, AND `check-semantics.mjs` WILL FAIL UNTIL IT LANDS

**What happened.** PL-12 / D-84 adds `bias` to the catalogue's `OBJECT_TYPES`, `HEADINGS` and `STATES` — a fifth canonical type with prefix `BIAS-`, first state `draft`, schema stamp `bias@1`, and the heading set `## Statements · ## Adoption · ## What This Does Not Enforce · ## Session Log · ## Review Notes`.

**What this costs UI, named precisely rather than left to be discovered.** `civicos-ui/check-semantics.mjs` computes `CANONICAL = [...new Set(Object.values(OBJECT_TYPES))]` and requires an entry for every canonical type in **SEVEN** tables inside `app.html`: `PREFIX`, `FIRST_STATE`, `HEADINGS`, `SCHEMA_OF`, `STATE_EDGES`, `TYPE_LABEL` and `SEMANTICS.types`. **`node civicos-ui/test/run.mjs` will fail with seven `has no entry for the catalog type 'bias'` lines until those are added.** The bio-plane battery is unaffected and is green at 111/111 — `check-semantics.mjs` is run only from `civicos-ui/test/run.mjs`.

**What UI would add**, and every value is the catalogue's own so nothing is invented here: `PREFIX.bias = "BIAS"`, `FIRST_STATE.bias = "draft"`, `HEADINGS.bias` = the five headings above, `SCHEMA_OF.bias = "bias@1"`, `STATE_EDGES.bias` = the catalogue's `STATES.bias.edges`, plus a `TYPE_LABEL` and a `SEMANTICS.types` entry, which are the only two that are AUTHORED rather than copied.

**A judgement UI should make rather than inherit, and it is the reason this is a delegation and not a chore.** A bias set is **not** a document a member mints from the add surface the way an inquiry is — it is a governance object, written by admins at the instance level and by project managers at the project level (`BIO_Declared_Bias_v0_1.md`, "Bias bundles and adoption"). Adding it to `PREFIX` makes it mintable from wherever `PREFIX` is read. **D-189 is the item that owns this surface** — *"no surface can say that a project carries its own bias"* — and it is explicitly sequenced behind D-84, which is now closed. RECORD does not edit `civicos-ui/**`.

**status:** open — UI's to schedule, and it should ride D-189 rather than be done twice.

### DELEGATION 2026-08-07 RECORD -> UI (informational): `op=airun` PUBLISHES A NEW `bias` BLOCK

`op=airun`'s `session` gains `bias: { in_force, stated, manifest, now, moved }`. **Nothing breaks**: UI-49's renderers are field-name-blind, so the block is rendered as published rather than ignored. It is recorded here because it is the field that lets a surface say *"this run carried a lens, and the lens has changed since"* — ordinary bias debt, DISCLOSED and travelling, blocking nothing (DEC-20). Filed as IC-30 (renumbered from IC-29 at the rebase — PL-1 took that number on main).

### DELEGATION 2026-08-08 RECORD -> CONDUCT / VERIFY: THE DEC-49 GUARD IS RED ON `main`, AND IT IS NOT PL-12's

**MEASURED, not inferred.** `node civicos-ui/check-refusal-codes.mjs` run against `a24f2b0` **with this item's files removed from the tree** exits 1 with **32 failures**. Run against this item's rebased branch it exits 1 with **the same 32** — the offending-code sets are byte-identical (diffed, not eyeballed). **PL-12 contributes ZERO.**

**What they are, and why they arrived.** VF-2's arm C treats any function named by a DEC-49 row's `where` as a GOVERNED SITE: every `ok:false` in that function must carry a code the family holds. PL-1's `BASIS_VERSION_CHECKS` rows carry `where: 'src/store.mjs promote'`, so **the whole of `promote` became governed** — and `promote` has refused with `NO_BODY`, `UNDECLARED_OPERATION`, `REFS_IN_PAYLOAD`, `BASIS_IN_PAYLOAD`, `MALFORMED`, `EXISTS`, `ABSENT`, `NO_TITLE`, `NAME_TAKEN`, `NOT_THE_OWNER`, `CAS_STALE`, `FILES_DROPPED`, `NO_BUNDLE_MD` and others since long before either item. None of those is a defect PL-1 introduced; what PL-1 introduced is the **scope** that now measures them.

**Why this is worth a routed item rather than a shrug.** The guard is right: those codes reach a member with no canned translation, which is the state DEC-49 ended. But the guard's own header says the gate stands on **reach** and the census is reported rather than gated — and `where: '<file> <fn>'` at function granularity makes a governed site as wide as its widest function. `promote` is 939 lines and refuses for a dozen reasons that have nothing to do with basis versions. **Two shapes, and this is the choice:** give every `promote` refusal a row (REC-64's sweep arriving early, in the largest function in the plane), or let a `where` name a NARROWER site than a whole function so a family governs the refusals it actually owns.

**What PL-12 did about its own share, recorded so the pattern is reusable:** the guard found exactly one code of ours — `BIAS_REFUSED`, the ENVELOPE of the write-path refusal, whose `findings[]` all carried translations while the envelope itself carried none. That is a REAL defect and the guard is why it is fixed: **C-26.11** now gives it a row and a canned translation, its `where` names `src/store.mjs promote` (where it fires, unlike its ten siblings), the refusal ships `check` and `translation` on the envelope, and **negative-control arm 7 removes it and the suite fails.**

**status:** open — CONDUCT's to route. RECORD does not edit `civicos-ui/**`, and the `promote` rows are PL-1's family rather than this item's.

## CLAIM 2026-08-08 UI

session: ui-51-bias-vocabulary (worktree agent-a188bf2ac3c783ab6)
opened: 2026-08-08T00:00:00Z
paths: **`civicos-ui/app.html`** — the CATALOG block's `PREFIX`, `FIRST_STATE`, `HEADINGS`, `SCHEMA_OF`, `STATE_EDGES` and `TYPE_LABEL` (the `bias` KEY ADDED TO EACH, nothing else in any of them touched), and the SEMANTICS block's new `SEMANTICS.types.bias` row. **NOT** `ADD_TYPES`, **NOT** `PHASE`/`PHASE_LABEL`, **NOT** any basis/strength/workproduct vocabulary, **NOT** any renderer or act. **`civicos-ui/check-semantics.mjs`** — the `SCHEMA_OF` arm only, TIGHTENED with a dated reason, plus a floor on the knownSchemas extraction. **`civicos-ui/test/bias-vocabulary.test.mjs`** (new, this item's suite). `docs/development/CLAIMS.md` (this entry and its DELEGATION).
interfaces consumed: I3 (read-only — the catalogue's `OBJECT_TYPES`, `STATES`, `HEADINGS` and `knownSchemas` are IMPORTED, never restated). interfaces owned: none. No interface changed.
expected: QUEUE UI-51 as briefed by CONDUCT — the seven UI entries PL-12's DELEGATION named, closing the eight `has no entry for the catalog type 'bias'` lines that had `civicos-ui/test/run.mjs`'s drift guard red since PL-12 merged.
**`bio-plane/**` IS NOT OPENED BY THIS SESSION.** `git diff --stat` names two files, both under `civicos-ui/`. The battery delta is therefore expected to be ZERO and is measured rather than assumed.
accepts-when: `check-semantics.mjs` reporting no missing type, read UNPIPED; the five catalogue-sourced values asserted to come from the PLANE with a hand-typed value FAILING; battery green with the delta attributed; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped. NEGATIVE CONTROLS run and recorded in the suite's own header. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and DEBT.md are not written by this session.
concurrency: checked over every unreleased block in this file. The live RECORD claim (PL-12) states explicitly that `civicos-ui/**` is NOT opened by it and routes these seven entries here as a DELEGATION; no other unreleased claim names `app.html`, `check-semantics.mjs` or `civicos-ui/test/**`.
released: 2026-08-08 - CONDUCT at integration - merged on main. Verified FROM THE MAIN CHECKOUT: battery zero delta at 112/112 / 6,746; UI harness 39 -> 40 suites; coverage --strict run DIRECTLY, $? unpiped, exit 0. Five of seven values read FROM THE PLANE through one sourcing function used by the real arm and every control, so there is no parallel reader. It found the harness red with TWELVE failures rather than the eight it was briefed, and that the schema-stamp check had a hole - any type could carry any other type's stamp and pass, written permanently into bundle.md. Tightened.
released:

### DELEGATION 2026-08-08 UI -> CONDUCT / VERIFY: `node civicos-ui/test/run.mjs` CANNOT REACH EXIT 0, AND THE REMAINING RED IS NOT UI-51's

**MEASURED, not inferred, and by the same method PL-12 used.** With this item's two changed files STASHED — the tree then byte-identical to `main` at 25ae3ad — `node civicos-ui/check-refusal-codes.mjs` exits 1 with **32 failures**. With them restored it exits 1 with **the same 32**. Both files were restored and verified by sha256 AND by content afterwards. **UI-51 contributes ZERO.**

**What UI-51 DID close, so the two halves are not confused.** All 40 suites PASS (39 before, plus this item's new one); `check-semantics.mjs` is GREEN and prints `bias` in its catalog-types line; `check-mock-envelope.mjs` passes. The eight `has no entry for the catalog type 'bias'` lines are gone, together with the three `has drifted` lines and the `states the store writes with no semantics row: adopted` line that the brief did not count — **twelve failures, not eight.**

**What remains is exactly the delegation PL-12 already filed** — every one of the 32 is inside `promote`, governed by PL-1's `BASIS_VERSION_CHECKS` rows whose `where` is `src/store.mjs promote`, and `promote` is 945 lines refusing for a dozen reasons that predate both items. **The fix is in `bio-plane/**`, which this session does not open.** It is one of the two shapes PL-12 named: give every `promote` refusal a row, or let a `where` name a narrower site than a whole function.

**Why this needs saying to CONDUCT rather than being left in the item's report:** UI-51's acceptance line says the harness exits 0, and it cannot until the `promote` rows land. **If the push is being held for a green `civicos-ui/test/run.mjs`, it is being held on a `bio-plane` item and not on this one.**

**status:** open — CONDUCT's to route.
## CLAIM 2026-08-08 VERIFY

session: rec71-agent (worktree agent-ab9e84c9e27f4eff7)
opened: 2026-08-08T00:00:00Z
paths: **`civicos-ui/check-refusal-codes.mjs`** — VF-2's guard, `parseWhere` and arm C only: a `where` may now name a NARROWED REGION inside a function, and the guard resolves, bounds and judges it. **`civicos-ui/test/refusal-codes.test.mjs`** — the guard's own suite: the region arms and its `NEGATIVE CONTROL:` header. **`civicos-ui/test/refusal-codes.control.mjs`** — the controls RUN against the real tree, each armed, restored, and every restore verified BY HASH **and BY CONTENT**. **`bio-plane/checks/bio-checks.mjs`** — named by SITE: the `where` field of `BASIS_VERSION_CHECKS.VERSION_FROZEN` and `BASIS_VERSION_CHECKS.VERSION_LEG_UNRESOLVED`, and the block comment above the family that states what a `where` means. **No translation, no C-number, no code and no other row is touched.** **`bio-plane/src/store.mjs`** — named by SITE and COMMENTS ONLY: two `DEC-49 REGION` marker pairs around `promote`'s basis-version resolve arm and freeze arm. **No statement, no expression and no control flow is changed** — `git diff` on that file is comment lines and nothing else. `docs/development/CLAIMS.md` (this entry and its DELEGATIONS), `docs/development/MEASUREMENTS.md` (one appended entry).
interfaces consumed: I3 (read-only). I5 NOT touched: no table, column, index or `purge` change.
interfaces owned: none. No published shape moves, so no IC is filed.
expected: QUEUE.md **REC-71** — **THE SCOPE IS THE DEFECT, NOT ANY WORKER'S CODE.** PL-1's two store-side rows carry `where: 'src/store.mjs promote (…arm)'`; VF-2's arm C reads that as *the whole 870-line `promote` is a governed site*, so all 32 long-standing refusals anywhere inside the plane's largest function suddenly owe a canned translation. Found independently by PL-2, PL-12 and UI-51; each declined to silence another item's red guard, which was right. **THE FIX IS THE SCOPING, NOT 32 TRANSLATIONS.** A governed SITE and a governed FUNCTION are different claims and the guard was making the second on rows that meant the first. **THE GUARD IS NOT WEAKENED**: a codeless refusal inside the narrowed region still fails it, proved by planting one rather than asserted.
accepts-when: as QUEUE.md REC-71. **Battery baseline 111/111 at 6,607 MEASURED IN THIS WORKTREE BEFORE ANY EDIT** — the brief carried 112/112 at 6,746 from CONDUCT's local main, which additionally holds PL-12 and UI-51; this tree is `a24f2b0` (PL-1 only) and the measured figure governs. `node civicos-ui/test/run.mjs` exit read UNPIPED. `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` unpiped. NEGATIVE CONTROLS run and recorded in the guard's suite's own `NEGATIVE CONTROL:` header. No push, no merge — CONDUCT integrates.
concurrency: checked over every unreleased block in this file. VF-2's claim on the three `civicos-ui/check-refusal-codes.mjs`/`test/refusal-codes.*` paths is `released:` (2026-08-07, CONDUCT at integration). PL-1's claim on `bio-checks.mjs` and `store.mjs` is `released:` (2026-08-08, CONDUCT at integration). No live claim names any path above. `civicos-ui/app.html` is NOT opened.
released: 2026-08-08 - CONDUCT at integration - merged on main in TWO passes, the second after CONDUCT measured that the first left 36 failures in the family next door. Verified FROM THE MAIN CHECKOUT: battery 112/112 at 6,746 with ZERO delta (proved by stripping comments from both revisions and comparing byte for byte); **UI harness exit 0, 40 suites, 0 FAIL** - it was exit 1 with 32 before this item; coverage --strict run DIRECTLY, $? unpiped, exit 0. This item closed a regression CONDUCT PUSHED by verifying the battery and coverage at PL-1's integration and not the UI harness. Two findings outlive it: a floor with slack is not a ratchet (the census floor sat 19 codes low and had already turned a control from RED to GREEN), and a whole-function `where` conscripts refusals that arrive AFTER the row is written. 7 of 13 governed sites are UNFALSIFIABLE and delegated to REC-64 rather than fixed here.
released:

### DELEGATION 2026-08-08 VERIFY -> REC-64 / whoever allocates the next DEC-49 ROW: WHAT A `where` NOW MEANS

**A `where` names the SMALLEST SPAN in which the row's refusal is enforced, and the guard judges exactly that span and nothing else.** Two spellings, and the second is new:

- `<file> <fn>` — the whole function body is the governed site. **Correct ONLY when every refusal that function makes is your family's business.**
- `<file> <fn> > <region>` — a NAMED REGION inside it, delimited in the source by `DEC-49 REGION <name>` / `END DEC-49 REGION <name>` block comments.

**It is stated in three places, all of which a next allocator will meet:** a "WHAT A `where` MEANS" block at the head of the DEC-49 families in `bio-plane/checks/bio-checks.mjs`; the two rows' own comments (`BASIS_VERSION_CHECKS.VERSION_FROZEN`, `.VERSION_LEG_UNRESOLVED`); and `parseWhere`'s header in `civicos-ui/check-refusal-codes.mjs`. **The guard also says it in the failure itself** — a not-a-row failure at a whole-function `where` now adds *"if the row governs only an arm of it, the fix is a REGION `where` and not a translation for this code"*, because that is the wrong turn REC-71 exists to prevent and the failure message is where it will actually be read.

**WIDENING A `where` WIDENS WHAT MUST BE TRANSLATED TODAY.** If you find yourself widening one to cover a refusal, you are doing REC-64's sweep in the worst possible place. Add the row, or narrow the `where`.

**Every way a region can be wrong FAILS** — missing, unclosed, duplicated, inside out, outside the function it names, trivially short, judging no refusal at all, or declared in the source with no `where` claiming it. All eight are armed in `test/refusal-codes.test.mjs` ARM 9, and four more against the REAL `store.mjs` in `test/refusal-codes.control.mjs` (r1)-(r4).

### DELEGATION 2026-08-08 VERIFY -> REC-64 / VERIFY: ARM C'S TEETH REACH TWO OF EIGHT GOVERNED SITES, AND REC-71 DID NOT WIDEN THEM

**MEASURED, NOT INFERRED. Arm C reads 449 lines across four governed sites and compares ZERO refusal codes against a row in any of them.** The guard now PRINTS this every run (`N refusals judged and M code(s) actually COMPARED against a row`, per site) and carries a `codesChecked` floor, so it is visible rather than green. **REC-71 deliberately did not fix it** — it is arm C's matcher vocabulary, which is REC-70's class, and widening this item into it would repeat the mistake REC-71 exists to correct.

Two distinct causes, both worth fixing on their own item:

- **`checks/bio-checks.mjs basisVersionFindings` (213 lines, 14 rows) PUSHES FINDINGS and never returns an `ok:false` object.** Arm C's matcher sees no refusal at all in it. Fourteen C-25 rows point at a site the guard cannot judge — the `where` is neither passing nor failing on merit.
- **`store.mjs`'s `meaningRows`, `versionChain` and `basisVersions` refuse through a local `refuse(key, detail)` helper** — `return { ok: false, reason: key, … }`. The code is a VARIABLE at the mint site, so arm C records the refusal as coded and **checks nothing**: any key at all passes that site. Not unsafe today (the helper reads `MEANING_READ_CHECKS[key].check` and an unknown key throws), but **unproven, which is a different claim from safe** — and it is the same shape as the sixth matcher VF-2 had to add to its census walk for exactly this reason.

**The honest scoreboard: arm C's teeth bite at `airun.mjs`'s three check functions and inside REC-71's two regions. That is it.** Anyone reading a green guard as "every governed site is proved clean" is reading it wrong, and the printed per-site line is what makes that legible.

### DELEGATION 2026-08-08 VERIFY -> CONDUCT: `REC-71` HAS NO ROW IN `QUEUE.md`, AND THE QUEUE TOPS OUT AT `REC-70`

The brief for this item says *"the `REC-71` row in `QUEUE.md` (it governs)"*. **There is no such row in this worktree at `a24f2b0`** — `grep -n "REC-71"` over the whole tree returns nothing and the highest RECORD id present is `REC-70`. The work was done against the brief and against what PL-2, PL-12 and UI-51 measured, which agree; **but the row that is supposed to govern does not exist, so nothing in the repository records what this item's `accepts-when` was.** This claim block carries it in the meantime. CONDUCT owns `QUEUE.md` and is its only writer, so REC-71 does not write the row — it says so rather than letting it pass, because "the repository is the channel" and an item nobody can find in the queue reached nobody.

### CLAIM AMENDMENT 2026-08-08 VERIFY · rec71-agent — THE SAME TREATMENT, THE SECOND FAMILY

Amended into the REC-71 claim above, same session, before any handoff, at CONDUCT's direction at re-integration. **Two paths added, both by SITE:**

- **`bio-plane/checks/bio-checks.mjs`** — the `where` field of **`BIAS_CHECKS.BIAS_REFUSED`** (C-26.11) and its row comment. **No translation, no C-number, no code and no other BIAS row is touched.**
- **`bio-plane/src/store.mjs`** — one further `DEC-49 REGION bias-set-refusal` marker pair around `promote`'s bias-set write-path refusal. **COMMENTS ONLY**, proved by stripping every comment from both revisions and comparing byte for byte, with the stripper itself checked against a fixture that must trip it.

**WHY: `main` went red a SECOND time within hours, in the family next door.** PL-12's `BIAS_REFUSED` carried `where: 'src/store.mjs promote'` at whole-function granularity and conscripted **36** refusals — PL-1's defect exactly, arriving before the convention that fixes it existed. PL-12 was rebased onto PL-1, but neither could see the region mechanism. **36 and not 34**: a whole-function `where` also conscripts the refusals the OTHER family's regions correctly govern.

**THE JUDGEMENT, since CONDUCT asked for it rather than the pattern: `BIAS_REFUSED` GETS A REGION.** PL-12's reasoning for naming `store.mjs` rather than the catalogue was RIGHT and is preserved — the code fires there, and naming the site is what puts it inside the guard's governed set. **Only the GRAIN was wrong. Being an ENVELOPE is a fact about the refusal's SHAPE and says nothing about its SPAN.** It fires at one statement inside one `if`; it is not enforced across the other ~960 lines. **What WOULD justify the wider spelling is written at the marker rather than left as a closed door**: a `where` may name a whole function when every refusal that function makes is the family's business — true of `airun.mjs`'s three check functions, and unlikely ever to be true of a function that both validates and writes.

**Each newly narrowed region owes its OWN teeth arm** — control (r5) plants a codeless refusal inside `bias-set-refusal` and the harness fails naming file, line, function and region. Inheriting another region's arm is not proof.

released:

### DELEGATION 2026-08-08 VERIFY -> REC-64: THE UNFALSIFIABLE COUNT IS NOW **7 OF 13**, AND EVERY NEW FAMILY ADDS MORE

**Re-measured on the merged tree, and this is the number that should drive REC-64's shape.** Arm C reads **776 lines across seven governed sites and compares ZERO refusal codes against a row in any of them.** PL-12 added three: `checkBiasExtension` (158L, 7 rows), `biasInhale` (111L), `biasAdopt` (58L) — joining `basisVersionFindings` (213L, 14 rows), `basisVersions`, `meaningRows`, `versionChain`.

**Arm C's teeth reach 5 of 13 sites**: `airun.mjs`'s three check functions and REC-71's three regions. Everywhere else the `where` is neither passing nor failing on merit.

The two causes are unchanged and both are one item's work, not thirteen:

- **A check function that PUSHES FINDINGS** never returns an `ok:false` object, so arm C's matcher sees no refusal in it at all. That is `basisVersionFindings` and `checkBiasExtension` — **21 rows between them**, pointing at spans the guard cannot judge.
- **A local `refuse(key, detail)` helper** binds the code to a VARIABLE, so arm C records the refusal as coded and checks nothing. `meaningRows`, `versionChain`, `basisVersions`, and the bias read paths.

**This is REC-70's class — a matcher admitting one spelling of a thing the plane spells several ways — and REC-71 deliberately did not widen into it**, because widening one item's scope to reach a neighbouring defect is precisely the mistake REC-71 exists to correct. But **the trend is the point**: every family added since VF-2 has added unfalsifiable sites, and the ratio is getting worse, not better. A green guard does not mean every governed site is proved clean, and the per-site line is what makes that legible rather than assumed.

### DELEGATION 2026-08-08 VERIFY -> CONDUCT / whoever lands the NEXT DEC-49 FAMILY: MOVE THE FLOORS IN THE SAME TURN

**The corpus floors went stale twice in one day, and each time it cost something measurable.** PL-1 grew the census 311 -> 330 while the floor stayed at 311, and negative control (e) silently went RED -> GREEN: the guard passed a walk that had lost an entire spelling. PL-12 then grew it 330 -> 341 within hours, restoring 11 codes of slack.

**A family added without moving `FLOOR.families`, `FLOOR.rows`, `FLOOR.census` and `FLOOR.reach` is a family whose codes buy slack for everybody else's walk.** The guard prints all four figures and its floor on every run, so the remeasure is one line each. `FLOOR.bodyLines` is the deliberate exception and says so at the site: it FALLS whenever a `where` is correctly narrowed, which is the work REC-71 licensed, so it stays a collapse detector while `codesChecked` carries the ratchet.

---

## CLAIM 2026-08-07 FLEET

session: fl1-agent (worktree agent-a0f43da2792cac8d0)
opened: 2026-08-07T00:00:00Z
paths: **FOUR NEW PROBE FILES, and nothing else that runs in production.** `bio-plane/test/fl1-attribution-diag.mjs` (NEW — **AMENDED INTO THIS CLAIM 2026-08-08, same session, before any handoff**) — the diagnostic that established two properties of the platform the item could not assume: that a Worker cannot fetch another Worker on this account's own `*.workers.dev` name, and that a fresh script is not attributable by name OR tag on the analytics surface. `bio-plane/test/fl1-billing-surface-check.mjs` (NEW) — a read-only check of whether the platform's analytics/billing surface is reachable at all with this project's token, plus the shared `.env`/account-pin helpers; deploys nothing. `bio-plane/test/fl1-real-payload-sweep.mjs` (NEW) — the REAL-SOURCE sweep: it calls the live plane's NON-MUTATING ops read-only and THROWS if the real corpus is absent or trivial, so the CPU probe cannot be built on invented payloads. `bio-plane/test/fl1-cpu-probe.mjs` (NEW) — the deployed probe itself, on `free-tier-fleet-probe.mjs`'s precedent (a `bio-plane/test/*-probe.mjs` that is NOT a `.test.mjs` and therefore NOT in the battery: it deploys throwaway Workers and deletes them in a `finally`). `docs/development/MEASUREMENTS.md` (ONE appended entry — FL-1's row, carrying D-190's posture in the same entry). `docs/development/DEBT.md` (the **disposition text only** of the D-218 and D-190 rows; no row added, none closed). `docs/development/CLAIMS.md` (this entry and its DELEGATIONS).
**NOT OPENED — stated because FL-1 is a measurement item and its whole discipline is that it commits no product code:** no file under `bio-plane/src/`, `bio-plane/checks/`, `bio-plane/scripts/`, `civicos-ui/`, `newgroup/` or `release/`. `bio-plane/src/cpu.mjs` is READ ONLY — the probe copies its `burn()` verbatim into the deployed subject so the calibration arm is denominated in the reference iterations `MEASUREMENTS.md` already records, and asserts nothing about the file.
interfaces consumed: none. interfaces owned: none. No op added, no schema touched, no check minted.
expected: IS-BUILD-PLAN **FL-1** — **D-218 probed FIRST.** Whether a fleet-member Worker can hold a WHOLE investigative run inside the PAID CPU ceiling, measured on a DEPLOYED probe and read off **the platform's observed billing surface**, never the Worker's own clock. **D-190 RIDES THE SAME MEASUREMENT**: the DO storage-ceiling posture is recorded in the same entry, because three IS mechanisms and D-224's quadratic table all grow the same object. W0, slot-free, MEASUREMENT ONLY. The probe SIZES FL-3; it does not gate the start (§14a — the resumable shape is built either way).
accepts-when: a dated `MEASUREMENTS.md` row with the INSTRUMENT NAMED and `cpu_ms` echoed from the deployed probe rather than from vendor docs — **or an honest statement of exactly why no number could be taken and what would be needed**. D-190's posture in the same row. **Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 110/110 at 6,521 — the brief said 110/110 at 6,521 and is CONFIRMED rather than inherited** (recorded because nine briefs running have carried a stale figure). Expected battery delta: **ZERO** — the three new files are `fl1-*.mjs`, not `*.test.mjs`, so the discovering runner does not collect them. `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` read UNPIPED.
negative control (FL-1's row names it, and it is RUN): **the probe timing itself in-Worker is REFUSED as the fabrication `cpu.mjs` records.** Made STRUCTURAL rather than promised: every cpu figure enters the findings through one function, `recordCpuMs()`, which THROWS `FabricatedMeasurement` unless the reading's provenance is the platform's observed surface. A deployed `fl1-selftimed` arm exists solely to be refused by that gate, and its self-clock number is printed BESIDE the platform's reading of the same script so the frozen clock is demonstrated rather than asserted. The gate's own empty case is guarded — seven bad shapes including `undefined`, `null`, `{}` and a `vendor-docs` source must ALL be refused while a real platform reading is still accepted, because a gate that refuses everything asserts nothing.
deployment: **the plane and the installer are NOT deployed and are not touched.** What the probe deploys is THROWAWAY `fl1-*` Worker scripts in this project's own account, on the precedent of `free-tier-fleet-probe.mjs` (2026-07-31) and `scratchpad/plan-probe.mjs` (2026-08-04); `biosmoke7`, `civicos`, `newgroup` and `pdf-worker` are a hard name guard, the account is pinned, and teardown is confirmed by re-listing the account's scripts.
concurrency: no live claim names any `bio-plane/test/fl1-*` path — every one is NEW. The two live RECORD claims are on `bio-plane/src/store.mjs` by SITE and the live UI claims on `civicos-ui/**`; this claim opens neither. `MEASUREMENTS.md` and `DEBT.md` are APPEND/disposition-only edits, and the last claim naming `MEASUREMENTS.md` (VF-2) is `result: **THE NUMBER, AND IT IS NOT THE ONE THE ITEM WENT LOOKING FOR.** D-218's reasoning is CONFIRMED — **50.0 s of wall time waiting on subrequests cost 4.29 ms of billed CPU**, ~0.16 ms per awaited subrequest regardless of how long the wait lasts. A run-shaped loop over REAL plane payloads costs **24.29 ms at 25 turns, 189.28 ms at 100, 4,488.9 ms at 400** — ~n^1.9, the transcript re-serialised every turn. **BUT MEMORY BINDS FIRST BY AN ORDER OF MAGNITUDE: memoryUsageBytesP99 is 120.4 MB of the 128 MB isolate at 200 turns while CPU is still 2.5% of the 30 s ceiling.** Instrument NAMED: the Cloudflare GraphQL Analytics API, `workersInvocationsAdaptive`, read off a DEPLOYED probe — never the Worker's own clock. Also measured: 40,000,000 reference iterations = **1,071 ms billed**, which puts the 2026-07-29 `op=cpuprobe` row into the same units for the first time; **≥160 external subrequests with no refusal** (51 on Free); and **a Worker cannot fetch another Worker on this account's own `*.workers.dev` name — 404, `error code: 1042`, 7 ms** — the service binding is the only route, which is I6's pattern already. D-190's posture is in the SAME entry: **925,696 B empty baseline, 176,657 B per bundle**, with the platform proved to have NO per-object storage surface and the measured point carrying an EMPTY meaning layer, so the slope is a floor and D-224 is what would move it.
released: 2026-08-08 - CONDUCT at integration - merged on main. Verified FROM THE MAIN CHECKOUT: battery 112/112 at 6,746 with ZERO delta, UI harness exit 0, coverage --strict exit 0. D-218 CONFIRMED but the finding is elsewhere: MEMORY BINDS FIRST BY AN ORDER OF MAGNITUDE - 120.4 MB of 128 MB at 200 turns while CPU is 2.5% of its ceiling, so a segment sized on CPU headroom would be ~10x too long. FL-3 is sized before FL-3 is written, which is why the plan front-loads this lane. Its fabrication gate earned its existence: the self-timed arm WOULD HAVE LIED, reading 0 ms where the platform billed 498.57 ms. D-190's posture rides the same row WITH ITS LIMIT stated - the measured point has an empty meaning layer, so the slope is a floor on cost and an overstatement of capacity. No deploy gate was reached; the three real Workers were verified untouched before and after every pass.
released:`.
released:

### DELEGATION 2026-08-08 FLEET -> whoever builds FL-3: THE SEGMENT IS SIZED, AND CPU IS NOT WHAT SIZES IT

**FL-3 should be shaped against MEMORY, not against the CPU ceiling.** Measured 2026-08-08 on a deployed probe, platform-observed (`MEASUREMENTS.md`, the FL-1 entry):

- **Waiting is effectively free.** 25 subrequests each held open 2 s — 50.0 s of wall time — cost **4.29 ms** of billed CPU, about **0.16 ms per awaited subrequest regardless of how long the wait lasts**. So a segment that suspends on the daemon costs nothing to be suspended, and §14a's resumable shape carries no CPU penalty at all.
- **CPU per turn grows as ~n^1.9** — 24.29 ms at 25 turns, 189.28 ms at 100, 4,488.9 ms at 400 — because the whole transcript is re-serialised every turn. Extrapolated on the measured exponent, ~1,100 turns fit the 30 s default and ~3,700 the 5-minute maximum.
- **BUT MEMORY REACHES 120.4 MB OF THE 128 MB ISOLATE AT 200 TURNS**, while CPU is still 2.5% of the ceiling. **That is the wall, and it arrives roughly ten times sooner than the CPU wall.** 400-turn invocations still completed, so it is pressure rather than a kill — but a segment sized on CPU headroom would be sized about an order of magnitude too long.
- **A segment of 100–150 model turns sits comfortably inside both**, on this corpus — and the corpus is a FLOOR: one development instance's real op bodies, where a loaded instance's `projection` and `search` are larger.
- **The cheapest single change if a longer segment is ever wanted: stop re-serialising the whole transcript every turn.** Both the CPU exponent and the memory curve come from that one behaviour — not from the waiting, and not from the payload sizes.

**And one hard constraint FL-2/FL-3 must not rediscover:** a Worker CANNOT reach another Worker on this account's own `*.workers.dev` name — **404, body `error code: 1042`, in 7 ms, every time**. The **service binding** to the same script answered 200 and honoured a 1,500 ms delay. That is I6's pattern already, so nothing about the design changes — but a fleet member built to call the plane by URL fails in a way that looks like anything except routing. It cost this session two probe passes.

### DELEGATION 2026-08-08 FLEET -> whoever runs D-224: YOUR MEASUREMENT IS NOW THE ONE THAT MOVES D-190

D-190's posture is recorded (`MEASUREMENTS.md`, the FL-1 entry): **925,696 B for an empty store, 176,657 B per bundle**, so roughly 60,800 bundles against the vendor's 10 GB per-object claim. **Do not read that as headroom.** The instance it was measured on has `connections`, `entities` and `resolutions` all at ZERO, so the slope prices CAPTURE ONLY and none of the meaning layer. D-224's quadratic `connections` growth lands on the SAME Durable Object, and it is precisely the term that is missing from the number.

**Two things that will save you time.** (1) The per-object storage figure is already exposed — `op=stats` returns `dbBytes`, which is workerd's `ctx.storage.sql.databaseSize` (`src/store.mjs:11209`); read it before and after a derive and the delta is your answer, with no probe Worker needed at all. (2) **The platform CANNOT help you here**: `durableObjectsStorageGroups` carries no per-object dimension (only dates) and returned zero rows on this account, and `durableObjectsPeriodicGroups` is per-object but reports no stored bytes. It DOES carry `rowsWritten`, `cpuTime`, `exceededCpuErrors` and `exceededMemoryErrors` per object, which is the right surface for the alarm-driven write amplification D-224 describes.
## CLAIM 2026-08-08 RECORD

session: is-wave-w3a-pl2 (worktree agent-a44d6df9209381fee)
opened: 2026-08-08T00:00:00Z
paths: **`bio-plane/checks/bio-checks.mjs`** — ONE new exported `VERSION_MACHINE` (the SIXTH state machine: `VERSION_STATES` as its `legal`, its edge table, and the reason-bearing target set), ONE new `VERSION_ACT_CHECKS` registry (C-25.19+, DEC-49 codes and canned translations), ONE new exported predicate `versionNeedsReason`, and the reason/attribution arms appended INSIDE the existing `basisVersionFindings` loop. No existing check's behaviour changed; `VERSION_STATES` and `VERSION_RELATIONSHIPS` are READ from PL-1's exports and never re-typed. **`bio-plane/src/schema.mjs`** — THREE additive nullable columns on PL-1's `inquiry_basis_versions` (`state_by`, `state_at`, `state_reason`), plus the same three in `#migrate`'s additive-column list; NO new table (PL-1's pin is exactly two and it stands). **`bio-plane/src/store.mjs`**, named by SITE: (a) ONE new private `#moveVersion` implementing every transition guard ONCE, and the SIX thin public entry points that name their act; (b) the version-act arm inside `promote`'s existing projection (the three new columns only); (c) ONE dispatch block of six entries; (d) `affordanceFacts`' new `basis_version_states` fact, read from the DOCUMENT like every other fact there; (e) `basisVersions`' optional `project` argument and the `current` field it answers. **NOT** `#basisCyclePath`/`#basisReach` (CALLED, never edited — PL-1 recorded the wiring and a second walk would drift), **NOT** `#strengthWalk`, **NOT** the `inquiry_basis` projection, **NOT** `#bundleGate`/`viewerPredicate`, **NOT** `purge`'s table list (unchanged — no new table). **`bio-plane/src/index.mjs`** — one `VERSION_ACTIONS` array, six `OPS` rows, six `NEEDS` rows, the array spread into `SESSION_OPS`, and its name added to the existing author/viewer stamp conditions. **`bio-plane/src/affordances.mjs`** — six `ACTS` rows deriving from (d), the machine published in `VOCABULARIES`, and the six ops' NEEDS totality satisfied. **`bio-plane/test/versionstate.test.mjs`** (new, this item's suite) and **`bio-plane/test/versionstate.control.mjs`** (new, the destructive three-layer control, deliberately NOT a `.test.mjs`). **`bio-plane/test/affordances.test.mjs`**, **`bio-plane/test/bounds.test.mjs`**, **`bio-plane/test/capability.test.mjs`**, **`bio-plane/test/gate-reads.test.mjs`**, **`bio-plane/test/meaning-bounds.test.mjs`**, **`bio-plane/test/versions.test.mjs`** — roster/classification/ratchet/pin rows for the six new ops and the three new columns, each CORRECTED with a dated reason, never exempted. `docs/development/INTERFACE-CHANGES.md` (the IC entry only, appended). `docs/development/CLAIMS.md` (this entry and its DELEGATIONS).
interfaces consumed: I3 (PL-1's `op=basisversions` shape, extended additively), I5 (PL-1's two tables, three additive nullable columns).
interfaces owned: **I3 — six new ops, additive; IC filed PROPOSED, the version bump and RESOLUTION are CONDUCT's.** **I5 — TOUCHED: three additive nullable columns on an existing table, no new table, no index change.**
expected: IS-BUILD-PLAN PL-2 — **THE SIXTH STATE MACHINE over basis versions (IS-2)**, stated as the sixth in the code that defines it. `suggested/considering/accepted/rejected` plus hide and make-current; six member ops (accept · reject · consider · revert · current · hide), each a member act with the four beats; an authored reason REQUIRED on the construct's defer (`consider`) and dismiss (`reject`); proposals aggregate and age (§6.4); machine identity refused on every transition through REC-46's ONE predicate; the machine published through `op=affordances`; and **the transitive basis-cycle check fired at the ACCEPT path through PL-1's EXISTING `#basisCyclePath`**, which PL-1 recorded rather than half-built.
accepts-when: as IS-BUILD-PLAN PL-2. **Battery baseline 111/111 at 6,607 MEASURED IN THIS WORKTREE BEFORE ANY EDIT — the brief said 111/111 at 6,607 and is CONFIRMED rather than inherited.** `node scripts/battery.mjs versionstate` green; full `npm run test:battery` green with the delta ATTRIBUTED per suite; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped. **THE THREE-LAYER NEGATIVE CONTROL (VERIFICATION rule 3a) RUN: the credential-stamp layer, the endpoint-capability layer and the transition layer EACH broken with the other two HELD OPEN, each layer's OWN assertion required to fail** — plus the implementation-count pin, because IS-6's C-22.4 control left its suite green at 98/98 when a rule had two implementations and either one absorbed the control. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and DEBT.md are not written by this session.
concurrency: PL-1's RECORD claim above is `released:` and its paths are therefore free. Checked over every unreleased block in this file: no live claim names `schema.mjs`, `store.mjs`, `index.mjs`, `affordances.mjs`, `bio-checks.mjs` or any suite named above. **PL-13 (IS-3, CURRENT as a project property) runs in this same wave and is NOT opened here**: `versioncurrent` writes the DATED pointer §7 specifies onto the PROJECT's own `bundle.md` frontmatter and mints NO settings row, NO notification slug and NO projection table — the slugs, the shared-inquiry semantics and D-216's model check stay PL-13's, and a DELEGATION below says so. `civicos-ui/**` is NOT opened — UI-42/UI-43/UI-45 depend on this item, not the other way round.
released: 2026-08-08 - CONDUCT at integration - merged on main WITH UI-52, which is what let it land. Verified FROM THE MAIN CHECKOUT: battery 113/113 at 6,821, UI harness exit 0 with the six acts printed as OWED, coverage --strict run DIRECTLY with $? unpiped exit 0. Its three-layer control ran as designed and its count pin caught the shape it was written for. CONDUCT resolved the index.mjs/store.mjs conflicts by composing the UNION of both action lists rather than taking a side - taking either would have silently dropped a family's acts from the capability sets.
released:

### DELEGATION 2026-08-08 RECORD (PL-2) -> UI: THE SIX VERSION ACTS HAVE NO SURFACE, AND `surface-registry.test.mjs` SAYS SO CORRECTLY

`op=affordances` now publishes six object-directed acts on an inquiry — `versionaccept`, `versionreject`, `versionconsider`, `versionrevert`, `versioncurrent`, `versionhide`. **`civicos-ui/test/surface-registry.test.mjs` ARM A4 fails naming all six**, because its rule is that every act the plane publishes is hosted by a described surface and none of them is yet.

**That arm is RIGHT and PL-2 deliberately did not silence it.** Describing a surface that does not exist would be the registry lying about what a member can reach, which is the one thing the registry is for. The three items that build the surfaces — **UI-42** (version review: rotation and diff, the prune offer's wording), **UI-43** (the accept ceremony, the four beats, the per-branch affirmation) and **UI-45** (the aged/aggregated suggestion feed) — all list PL-2 as a dependency, and PL-2 is now landed, so all three are unblocked.

**What UI needs from the plane and already has, so nothing is owed back:**

- **The machine is PUBLISHED, not copied.** `op=affordances` answers `vocabularies.version_states`, `vocabularies.version_edges` and `vocabularies.version_reason_required`. **Read them; do not restate them.** The edge table is what says whether "put this back" is offerable at all, and `version_reason_required` is what says WHEN a surface must collect a reason BEFORE it sends the act — without it a member types a reason into a form that discards it, or worse, is refused after the fact.
- **BEAT 2 of the four beats is a real op parameter.** Every one of the six accepts `preview=1` (and `preview=true`), runs **every** guard, writes nothing, and returns either the refusal the act would return or `{ ok:true, preview:true, wrote:false, from, to }`. It is the same code path up to the write, so the pre-flight cannot disagree with the act. **Use it for the ceremony's "here is what will happen" step rather than composing a prediction.**
- **Every refusal carries a C-number, a DEC-49 wire code and a canned translation.** Render the translation; never compose one.
- **Hidden and settled readings are RETURNED AND FLAGGED, never filtered** (D-214, DEC-29(b)). The display shrinks; the query still answers. `op=basisversions` also carries `moved: { by, at, reason }` per version, which is the rejection record UI-45's feed and UI-42's history both want.
- **`op=versioncurrent` requires `project=` and there is no default.** A surface must name which project is moving its stance, and the plane refuses `VERSION_CURRENT_NO_PROJECT` otherwise — §7, so one team's decision never quietly moves another's. `op=basisversions&project=<PROJ>` answers what THAT project stands on.

**One wording bound worth carrying into the surfaces:** no member-facing string in this item says "ground", "partition", "AND" or "OR" as a member-facing word (DEC-32's elicitation clause 1, D-226), and the suite asserts it of every translation. The published act LABELS follow the same bound and are the plane's own words — render them.

### DELEGATION 2026-08-08 RECORD (PL-2) -> CONDUCT / whoever owns PL-1's follow-up: THE DEC-49 GUARD IS RED ON `promote`, AND IT PRE-DATES THIS ITEM

**MEASURED, not inferred.** `node civicos-ui/test/run.mjs` exits 1 with **32 failures on the PRISTINE tree** — verified by holding PL-2's seven modified files aside, `git checkout -- bio-plane/`, running the harness, and restoring all seven **byte-identically (`cmp` on every one)**. PL-2 adds ZERO of those 32; its own governed site (`#moveVersionState`, 239 lines) is read by the guard and **every refusal in it carries a code with a row**.

**The cause is PL-1's `where` fields.** `BASIS_VERSION_CHECKS.VERSION_FROZEN` and `.VERSION_LEG_UNRESOLVED` both declare `where: 'src/store.mjs promote …'`, which makes the WHOLE of `promote` — 874 lines — a DEC-49-governed site. The guard then requires every `ok:false` in `promote` to carry a code that is a row in `BASIS_VERSION_CHECKS`, and `promote` legitimately refuses with thirty-two codes from other families (`NO_BODY`, `CAS_STALE`, `BASIS_CYCLE`, `SELF_BASIS`, `NO_BUNDLE_MD` …). **The guard is not wrong about its rule; the `where` is wider than the two refusals it describes.**

**Two honest fixes, and the choice is a real one rather than a tidy-up.** (a) Narrow the `where` to the arm — but `promote` has no named inner function for the guard to resolve, so this means either extracting one or teaching `parseWhere` a sub-site form. (b) Translate the thirty-two, which is REC-64's work arriving early and is the outcome DEC-49 actually wants. **PL-2 did neither**, because both touch VF-2's file or PL-1's registry and neither is in this item's claim — and because a worker silencing another item's red guard is exactly how a measured floor stops measuring.

**One thing PL-2 asks CONDUCT to notice at integration:** the harness was ALREADY exit 1 when PL-1 merged, so the integration note recording PL-1 as verified did not include this run. It is not a regression PL-2 introduced and it is not a regression PL-1's own suites can see.

## CLAIM 2026-08-08 UI
session: ui52-arm-a4-narrowing
opened: 2026-08-08T00:00:00Z
paths: `civicos-ui/test/surface-registry.test.mjs` (ARM A only — the ARM A4 split; every other arm is READ ONLY and untouched), `civicos-ui/test/surface-registry-a4.control.mjs` (NEW — the committed negative-control driver, a `.control.mjs` so `civicos-ui/test/run.mjs`'s `*.test.mjs` discovery does not collect it; PL-2's `bio-plane/test/versionstate.control.mjs` is the precedent), `docs/development/CLAIMS.md` (this entry and its DELEGATION).
**NOT OPENED, and stated because this item is one assertion's scope and nothing else:** no file under `bio-plane/src/`, `bio-plane/checks/`, `civicos-ui/app.html`, `newgroup/` or `release/`. `civicos-ui/app.html` is READ ONLY — the negative controls mutate a COPY in `.ui52-harness/` and the file on disk is restored and verified BY CONTENT as well as by sha256 after every arm. `bio-plane/src/affordances.mjs` and `bio-plane/checks/bio-checks.mjs` are READ ONLY in the deliverable; the controls swap git's OWN three-way merge of them for the duration of a run and restore them the same way.
interfaces consumed: I3. interfaces owned: none. No op added, no check minted, no schema touched.
expected: **UI-52** — `surface-registry`'s ARM A4 conflates a claim that is always a lie with one that is a scheduled, transient gap, and the second one is FALSE BY DESIGN during a plane-first wave. Split them: a described surface that does not exist stays UNCONDITIONAL; a published act with no surface yet becomes a NAMED, RATCHETED list, printed in full every run, with a CEILING so it cannot grow silently and a FLOOR so the walk cannot go blind.
concurrency: no live claim names `civicos-ui/test/surface-registry.test.mjs`. PL-2's live claim names `bio-plane/**` paths only and this claim opens none of them.
released:

### DELEGATION 2026-08-08 UI -> CONDUCT (integration): THE LAST MILE OF UI-52'S ACCEPTANCE IS INSIDE `bio-plane/**`, WHICH THIS CLAIM DOES NOT OPEN

**UI-52's brief states that this worktree carries PL-2 on local `main`. IT DOES NOT.** `3ab1392` (PL-2 / IS-2) is an ancestor of NO branch this worktree can reach except `worktree-agent-a44d6df9209381fee`; my branch and local `main` are both at `36e9e60`, and `git merge-base --is-ancestor 3ab1392 HEAD` answers false. The consequence is that **`surface-registry` was GREEN here, 376 pass / 0 fail** — the red arm the item was queued against could not be observed until it was reconstructed.

**What was done instead of merging, and why.** `git merge --no-commit --no-ff 3ab1392` **CONFLICTS in `bio-plane/src/index.mjs` and `bio-plane/src/store.mjs`** — both plane paths, both delegations rather than edits for a UI session, and resolving them is the integration work this claim does not open. The merge was aborted and the tree left clean. But **`bio-plane/src/affordances.mjs` and `bio-plane/checks/bio-checks.mjs` AUTO-MERGED CLEANLY**, so git's OWN three-way merge of exactly those two files was taken from the staged merge before aborting and used as the act-catalogue fixture. That fixture is what the register was measured and driven against — **the six act ids were never typed from the diff into the walk; they are read from `ACT_IDS`/`CAPTURE_ACTS` as live module exports of git's own merged output.**

**WHAT CONDUCT MUST RE-RUN AFTER RESOLVING THOSE TWO CONFLICTS, because this session could not:**

- **`node civicos-ui/test/run.mjs`, exit read UNPIPED, on the genuinely integrated tree.** On the two-file fixture **all 40 discovered suites PASS and `surface-registry` is exit 0 with the six acts named in the register**, but `run.mjs` still exits 1 — from **`check-refusal-codes.mjs`, not from `surface-registry`**: PL-2's `bio-checks.mjs` rows name `#moveVersionState` in `src/store.mjs`, and the fixture does not carry PL-2's `store.mjs`. **That failure is an artefact of the partial fixture and has nothing to do with UI-52**; it closes the moment the real merge supplies `store.mjs`.
- **Do NOT close it by swapping PL-2's own `store.mjs`/`index.mjs` in wholesale — that was tried and is worse.** PL-2 branches from `a24f2b0`, so its `store.mjs` predates REC-71's `DEC-49 REGION` markers and PL-12's `biasInhale`/`biasAdopt`, and the same guard then fails **seven** ways in the opposite direction (`arm C resolved 0 region where(s), floor is 3`). **Only a genuine three-way merge produces a valid tree here**, which is precisely why it is CONDUCT's and not this session's.
- **The one figure that will move at integration and should be moved deliberately:** `ARM A4d`'s floor is measured over the catalogue **OUTSIDE** the register, so it reads **15 with PL-2 and 15 without** and does NOT need touching when PL-2 lands. That is deliberate — a floor that had to be raised by the very acts it already tracks would carry REC-71's slack built in. The floor that DOES move is the next act family's, and `ARM A4b`'s ceiling will name it.

**AND THE REGISTER'S ROWS ARE A CLAIM ABOUT SEQUENCING THAT CONDUCT OWNS.** `ACTS_AWAITING_SURFACE` names, per act, the item that owes its surface — derived from `IS-BUILD-PLAN.md` rather than from the brief: **UI-43 (W7)** for the four transition acts (`versionaccept`/`reject`/`consider`/`revert` — "the four beats on every transition"), **UI-42 (W7)** for `versionhide` (the hide-prune offer, DEC-29(b)/D-214), **UI-45 (W8)** for `versioncurrent` (line 113's "IS-3 CURRENT as a project property"). **If that mapping is wrong the register is wrong**, and it is wrong in the direction of sending a future session to the wrong item. Note also that PL-2 lands in **W3** and the surfaces in **W7/W8** — **four and five waves later, not the three the brief states.**

## CLAIM 2026-08-08 RECORD
session: is-wave-w4-pl3 (worktree agent-acad3e0b337d0848f)
opened: 2026-08-08T02:40:00Z
paths: **`bio-plane/checks/bio-checks.mjs`** — ONE new exported `SUGGEST_CHECKS` family (**C-27.x**, DEC-49 codes with canned translations; C-25 is PL-1/PL-2's and C-26 is PL-12's, both verified taken before allocating), ONE new exported `SUGGEST_KINDS` table (§9's five kinds), ONE new exported boilerplate predicate `isBoilerplate` with its named roster, and ONE additive `kind` arm appended inside the existing `basisVersionFindings` loop. No existing check's behaviour changed; `VERSION_STATES`, `VERSION_RELATIONSHIPS`, `BASIS_ROLES`, `GRADE_AXES` and `isMachineIdentity` are READ from the existing exports and never re-typed. **`bio-plane/src/schema.mjs`** — ONE additive nullable `kind` column on PL-1's `inquiry_basis_versions` (plus `#migrate`'s additive-column list), and ONE new SCRATCH-class table `suggest_refusals` before the `host_governor` block. **The new table's name deliberately contains no `version` substring**, because PL-1's pin counts `CREATE TABLE` names matching `/version/i` and requires exactly two — a third would fail that pin correctly, and this table carries refusals rather than versions. **`bio-plane/src/store.mjs`**, named by SITE: (a) ONE new public `suggestVersion` with its six pre-write checks inside a **`DEC-49 REGION is-suggest-checks`** span (REC-71 — a region, never the whole function); (b) ONE new private `#appendVersionRows` frontmatter splice; (c) `#strengthWalk` gains ONE optional `legsOverride` parameter so the pair over a candidate version runs through the EXISTING arithmetic and never a second one; (d) `Store.basisVersionsOf`'s `kind` read and its CONDITIONAL composition line; (e) `promote`'s existing version projection INSERT (the one new column only — the write site stays exactly one); (f) `purge`'s TABLES list and `op=stats`' count for `suggest_refusals` (D-113); (g) ONE dispatch entry. **NOT** `#basisCyclePath`, **NOT** `#moveVersionState` (PL-2's, untouched), **NOT** `#axisResult`/`#groundResult` (CALLED, never edited), **NOT** `viewerPredicate`, **NOT** PL-1's freeze arm. **`bio-plane/src/index.mjs`** — one `OPS` row, one `NEEDS` row, one `SESSION_OPS` membership. **`bio-plane/src/affordances.mjs` is NOT OPENED**: `op=suggest` is the AI's write path and is not a member-directed act, so publishing it as an `ACT` would put a non-member-facing op on UI-52's act register and owe a surface nobody owes. **`bio-plane/test/suggest.test.mjs`** (new, this item's suite) and **`bio-plane/test/suggest.control.mjs`** (new, the one-refusal-at-a-time control driver, deliberately not a `.test.mjs`). **`civicos-ui/check-refusal-codes.mjs`** — the `FLOOR` figures only, MEASURED and moved IN THE SAME TURN (REC-71's finding: a floor with slack had already flipped a control from RED to GREEN). Plus roster/ratchet/pin rows in whichever of `bio-plane/test/{bounds,meaning-bounds,capability,gate-reads,hygiene,versions,conformance}.test.mjs` the change makes owed, each CORRECTED with a dated reason and never exempted. `docs/development/CLAIMS.md` (this entry and its DELEGATIONS).
interfaces consumed: I3 (PL-1's `op=basisversions` shape and PL-2's six acts, both read-only here), I5 (PL-1's two projection tables).
interfaces owned: **I3 — one new op `suggest`, additive; the IC entry is filed PROPOSED and the version bump and RESOLUTION are CONDUCT's.** **I5 — TOUCHED: one additive nullable column on an existing table and one new scratch-class table.**
expected: IS-BUILD-PLAN PL-3 — **THE SUGGEST ENDPOINT (IS-4)**. ONE write path for both modes (§10); §9's five kinds including *this level is empty*; the SOLE possible output is a `suggested` version carrying its run. **The acceptance is the six PRE-WRITE checks and they are PLANE-SIDE, not model-side (§14b.5, SWEEP C11)**, each with its own C-number, DEC-49 code and canned translation: a leg exists AND is reachable at its address (D-168 — `op=cite` is TYPE-ONLY, so a naive check passes RETIRED information); the pair computes per axis over the declared partition; differs-in-substance; D-195 independence over OR-branches; no boilerplate; no unwritable state. **F10: a verbatim resubmit of a refused version is a STRUCTURAL NO-OP** — the same refusal returned from the stored refusal without a second evaluation, so a retry loop cannot be caught only by the budget.
accepts-when: as IS-BUILD-PLAN PL-3. **Battery baseline 113/113 at 6,821 MEASURED IN THIS WORKTREE BEFORE ANY EDIT — the brief said 113/113 at 6,821 and is CONFIRMED rather than inherited; UI harness exit 0 and `--strict` exit 0 both measured here first, unpiped.** `node scripts/battery.mjs suggest` green; full `npm run test:battery` green with the delta ATTRIBUTED per suite; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped; `node civicos-ui/test/run.mjs` exit read unpiped. **OWED CONTROL 6 (VF-1): REMOVE ANY ONE OF THE SIX PRE-WRITE REFUSALS AND ITS SUITE FAILS — one at a time, each named**, because a control that removes them all together proves only that the block exists. Plus: a leg citing RETIRED information refused; an untranslated code fails the harness; the idempotence key neutered makes the resubmit arm fail. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and DEBT.md are not written by this session.
concurrency: PL-1's and PL-2's claims above are both `released:` and their paths are therefore free. Checked over every unreleased block in this file: no live claim names `schema.mjs`, `store.mjs`, `index.mjs`, `bio-checks.mjs` or any suite named above. **`civicos-ui/check-refusal-codes.mjs` is opened for its FLOOR constants ONLY** — VF-2's claim is released and REC-71's is released; the guard's logic is READ ONLY and a floor that is not moved in the turn that grows the plane is the exact defect REC-71 measured twice. **PL-4 (`capture_requests`) and PL-11 (the `ai` credential class) both depend on this item and are NOT opened here**: no capture-request table, no credential class, no `token:ai` predicate is written by this session, and a DELEGATION below says what each inherits.
released: 2026-08-08 - CONDUCT at integration - merged on main. Verified FROM THE MAIN CHECKOUT: battery 114/114 at 6,887 with the +66 attributed and MEASURED by re-running the true baseline with the new suites moved aside rather than by subtraction; coverage --strict run DIRECTLY with $? unpiped exit 0, OPS 150 all reached and CHECKS 123 all named; UI harness exit 0 read unpiped. Its sharpest finding is a defect rather than a feature: a control in its OWN suite passed while asserting nothing, and correcting it IMMEDIATELY EXPOSED A REAL ONE - promote looked its translation up in one registry, so a new code went out with translation: undefined. An untranslated code reaching a member, invisible to the DEC-49 guard because the code is a variable at that site - the unfalsifiable-site class REC-71 flagged, now proven to hide a live defect rather than merely being unproven. It moved EVERY floor in the same turn, honouring REC-71's delegation as the next item to touch them - and found the families floor already stale, reading 5 over a tree carrying 6. DEC-65 raised: a background run can propose only level-empty, because two correct checks meet at a reach nobody chose.
released:

### DELEGATION 2026-08-08 RECORD (PL-3) -> BOB, via CONDUCT: **A BACKGROUND RUN CANNOT PROPOSE A READING THAT RESTS ON DOCUMENTS, AND THAT IS A PRODUCT CONSEQUENCE RATHER THAN A BUG**

**MEASURED, not inferred, and found by driving the endpoint rather than by reading the design.** PL-1's `C-25.5` makes a version's structure TOTAL — *"a version does not get the not-at-all arm, because §3 requires it to CARRY the partition"* — so **every** version carrying legs declares at least one part. PL-1's `C-25.15` then requires every declared part to be asserted by a **named member**, because *"these legs are enough on their own" is an authored judgment that makes the finding STRONGER*. Both rules are right and both are landed.

**Together they mean a machine credential can write §9's `level-empty` kind and cannot write a reading that rests on a single document.** PL-3 refuses that BY NAME (`C-27.13`) so the caller is told which act it is short of, rather than composing a document and meeting `C-25.15` at the write in another family's words. The **interactive mode (§10) runs under a named member and can propose all five kinds**, which is why the item's acceptance is met; the **background job (§10's first mode) is narrower than the design reads**.

- **Provisional:** refuse the machine by name at the endpoint; the background job proposes empty-level findings and the interactive session proposes everything.
- **Why it was ambiguous:** §5 says the session *composes* legs and §14b.5 lists checks that presuppose legged machine-composed versions; PL-1's own `C-25.15` message says *"a machine-composed version PROPOSES the structure rather than asserting it"* — anticipating exactly this and leaving no shape in which it can.
- **Alternative:** a version with EXACTLY ONE part asserts nothing beyond what its legs already say — the OR-maximum is what makes a part-assertion load-bearing, and with one part there is no maximum to take. Exempting the single-part case from `C-25.15` would let a background run propose an unstructured (implicit-AND, weakest-leg) reading, which is DEC-32's conservative default and cannot overstate anything.
- **Recommendation: TAKE THE ALTERNATIVE, but not in this item.** It edits a landed check of PL-1's on which PL-2 and PL-14 both build, and widening PL-3 to reach a neighbouring rule is the mistake REC-71 exists to correct. It belongs in the item that schedules the background mode — **FL-3**, which is the first thing that will actually run unattended.
- **Reversal cost:** low today and rising. Nothing has been written under the current rule, so relaxing it later costs one check arm and one suite arm. Once a corpus carries machine-proposed readings the relaxation is still safe (it only ever admits more), but the reverse — tightening it — would strand them.

### DELEGATION 2026-08-08 RECORD (PL-3) -> FL-3 (the run harness): **WHAT THE HARNESS INHERITS, AND THE ONE THING IT MUST NOT REBUILD**

- **`op=suggest` is the ONE write.** One endpoint for both modes; the composition goes in the POST body; `author` and `viewer` are server-stamped and a caller-supplied value is deleted at the control plane.
- **F10's `denied-means-adjust` has a plane-side half already built, and the harness owns the other half.** A verbatim resubmit is a STRUCTURAL NO-OP — the stored refusal comes back with `evaluated: false`, `repeated: true`, `wrote: false` and a `repeats` count, no second evaluation, no document touched. **So the harness's table row is about ADJUSTING, not about avoiding churn: the churn is already gone.** `repeats` climbing is the signal that a run is looping without adjusting, and it is visible to something other than the budget.
- **Every refusal carries a C-number, a DEC-49 wire code and a canned translation. RENDER the translation; never compose one.**
- **The kind `level-empty` is what an empty run emits**, and FL-3's owed control 7 (an inquiry the evidence does not support proposes NOTHING and emits §9's empty-level kind) has its object to count: the kind is a column on `inquiry_basis_versions`, readable through `op=basisversions`.
- **Do NOT rebuild the pre-write checks fleet-side.** SWEEP C11 and §14b.5 are explicit that they are the PLANE'S; a fleet-side copy is the drift class DEC-8 closed. The fleet member receives verdicts.
- **The bound REFUSES rather than truncates.** `SUGGEST_LEGS_MAX` is 120 and a submission over it is turned away naming the bound, so a harness splits a reading rather than discovering a silent cut.

### DELEGATION 2026-08-08 RECORD (PL-3) -> PL-4 and PL-11: **WHAT EACH INHERITS, AND WHAT THIS ITEM DELIBERATELY DID NOT BUILD**

- **PL-4 (`capture_requests`).** This item writes **no capture request and no capture**. §4 group 1 is untouched. `suggest_refusals` is the shape PL-4's table should copy — scratch-class, in `capture_sessions`' family, before the `host_governor` block, in `purge` in BOTH arms and counted by `op=stats` — and the trap to copy with it is that **a table keyed on something other than `bundle_id` cannot ride purge's `TABLES` list and needs its own DELETE in each arm** (D-113 arriving through a column name).
- **PL-11 (the `ai` credential class).** `op=suggest` rides `["admin", "member", "probe"]` today and PL-11 **narrows** it — the direction IS-6 recorded as the safe one. Note what the fence here does NOT depend on: the class list is not what stops a machine settling a reading. `MACHINE_CANNOT_MOVE_VERSION` (PL-2) and `SUGGEST_UNWRITABLE_STATE` (this item) both refuse **by shape** through REC-46's one predicate, so `token:ai` is caught the day it exists without either of them being edited. PL-11's `MACHINE_CANNOT_*` sweep should include **`SUGGEST_UNWRITABLE_STATE`**, which is a `MACHINE_CANNOT_` rule wearing a different name because it refuses a *structural assertion* rather than a *transition*.

### DELEGATION 2026-08-08 RECORD (PL-3) -> REC-64 / whoever next narrows a `where`: **THE UNFALSIFIABLE-GOVERNED-SITE TREND HAS A CHEAP, GENERAL FIX, AND IT IS MEASURED**

REC-71 measured that **7 of 13 governed sites read 776 lines and compared ZERO codes**, and that *"every family since VF-2 has added them and the ratio is getting worse."* The cause is mechanical: arm C compares a code against its family only when it can SEE the code, and a local `refuse(key, …)` helper passes the code as a **variable**.

**The fix is to name the local helper `refusal` and pass the code as a STRING LITERAL at every site.** Arm C already matches `refusal("CODE"` by name. PL-3 did this deliberately and the figure moved from **11 to 30 codes compared** across the whole plane — 19 of the 30 are PL-3's, and every one of its refusals is now falsifiable rather than merely read. The change is local to the site, costs nothing, and needs no guard edit.

**Two things the guard is still blind to and PL-3 could not fix from here:** `basisVersionFindings` (244 lines, 0 judged) and `checkBiasExtension` (158 lines, 0 judged) refuse by PUSHING findings rather than by returning `ok:false`, so no matcher sees them at all. That is a matcher gap rather than a naming one and belongs to whoever owns arm C.

### DELEGATION 2026-08-08 RECORD (PL-3) -> SK-3 (the PRACTICE-SURVEY prohibition set): **THE CODE HALF OF THE FIFTH PROHIBITION IS BUILT AND IT IS A BOUNDARY, NOT A PROSE JUDGE**

`isBoilerplate` and `BOILERPLATE_FORMS` are exported from `checks/bio-checks.mjs` — ONE predicate, one roster, no second membership test. `C-27.12` refuses a version whose description, claim, part-statement or leg note is filler, and the plan's own control for SK-3 ("submit a version whose description is placeholder text through PL-3 -> refused by C-number while the skill-only path would have passed it") is **RUN and green** in `test/suggest.test.mjs`.

**What it CANNOT do is stated at the predicate rather than implied**, on D-130's precedent: a machine that writes "the relevant department" gets past every rule in it. It matches the WHOLE field, case-folded and trimmed, **never as a substring** — a real finding that QUOTES a placeholder must land, and that over-strictness arm is driven. **The skill still owns the part the code cannot reach**, which is what SK-3's prohibition set is for.

### DELEGATION 2026-08-08 RECORD (PL-3) -> VF-1: **OWED CONTROL 6 IS RUN, RECORDED AND RE-RUNNABLE IN ONE STEP**

`node bio-plane/test/suggest.control.mjs` — TEN arms, all restored, every file verified by sha256 **and by content**. Six of them are VF-1's control 6 proper: **each of the six pre-write refusals neutered ALONE with the other five HELD OPEN**, each naming its own C-number, and each declaring what must **not** fail so a suite that failed "somewhere" could not pass for a suite that failed at the right place.

**Two arms did not behave as first declared, and both are findings rather than repairs:**

1. **Arm (6) predicted the DEC-49 floor would fail and it did not.** The floor measures a CODE's reachability, not a BRANCH's, and `C-27.13` has two producers — the arm neuters one and deliberately leaves the other live. The declaration was corrected; the control was right.
2. **Arm (8) left the suite GREEN at 59/59** — *a control that passes while asserting nothing*, in this item's own suite. The arm asserted that a registry ROW existed (its C-number and its `where`), which is true whether or not anything enforces it. **Corrected to DRIVE the refusal through `op=promote`**, and driving it immediately exposed a real defect: `promote`'s `BASIS_VERSION_REFUSED` looked its translation up in ONE registry, so the new document-gate code went out with `translation: undefined` — **an untranslated code reaching a member, which is exactly what DEC-49 ended, and the DEC-49 guard could not see it because the code is a variable at that site.**

## CLAIM 2026-08-08 RECORD
session: is-wave-w5-pl4 (worktree agent-ad191a5dd58a9327f)
opened: 2026-08-08T05:10:00Z
paths: **`bio-plane/checks/bio-checks.mjs`** — ONE new exported `CAPTURE_REQUEST_CHECKS` family (**C-28.x**, DEC-49 codes with canned translations; C-25 is PL-1/PL-2's, C-26 is PL-12's, C-27 is PL-3's, all three verified taken before allocating), ONE new exported `CAPTURE_PURPOSES` roster and ONE new exported `CAPTURE_UA_MODES` roster. No existing check's behaviour changed; `isPublicHttpsLocator`, `isMachineIdentity` and `MACHINE_AUTHOR_PREFIX` are READ from the existing exports and never re-typed. **`bio-plane/src/schema.mjs`** — ONE new SCRATCH-class table `capture_requests` before the `host_governor` block, plus its two indexes. **`bio-plane/src/store.mjs`**, named by SITE: (a) ONE new public `captureRequest` (the request door) inside a **`DEC-49 REGION is-capture-request`** span; (b) ONE new public `captureRequestDrain` with DEC-47's conduct inside a **`DEC-49 REGION is-capture-conduct`** span — the ONE enforcement point; (c) ONE new private `#captureRequestAttribution` (both principals, inside the conduct region); (d) ONE appended `#schedConsumers` entry `capture-request-drain` (the NINTH, no second alarm and no cron); (e) `purge`'s per-bundle DELETE and whole-store DELETE for `capture_requests` (D-113 — the table is keyed on `request`, so it cannot ride the `TABLES` list); (f) `op=stats`' count; (g) ONE new CONDITION producer `#conditionsCaptureRequested` for the EXISTING catalogued kind `capture-completed-unattended` (D-61), registered in `#queueConditions`; (h) two dispatch entries. **NOT** `taskEnqueue`/`taskDrain`, **NOT** `#monitorTick`/`#fireArchiveFallback` (READ as the precedent, never edited), **NOT** `suggestVersion` (PL-3's, untouched), **NOT** `aiRunOpen`/`aiRunTick` (CALLED through `#aiRunAppend` only). **`bio-plane/src/index.mjs`** — two `OPS` rows (`capturerequest`, `capturerequestdrain`), the `capture-request` arm on `op=acquire`'s daemon confinement, and the `userAgentIsLegible` predicate beside `userAgent`. **`bio-plane/test/capturerequests.test.mjs`** (new, this item's suite) and **`bio-plane/test/capturerequests.control.mjs`** (new, the negative-control driver, deliberately not a `.test.mjs`). **`bio-plane/test/daemon-token.test.mjs`** — the class's totality assertion CORRECTED with a dated reason (SWEEP §4b(1) is the DECISION DEC-37 required), never exempted. **`civicos-ui/check-refusal-codes.mjs`** — the `FLOOR` figures only, MEASURED and moved IN THE SAME TURN. **`bio-plane/src/affordances.mjs`** — ONE `NON_ACTS` row for `capturerequest`, on `op=suggest`'s precedent: the request door is not member-facing, so publishing it as an ACT would put an act on UI-52's register that no item owes a surface for. **FINAL, MEASURED SET of pin/ratchet rows corrected in the same turn, each with a dated reason and none exempted:** `bio-plane/test/airun.test.mjs` (ARM S1's scheduler-consumer DELTA, 8 -> 9, plus two arms of its own), `bio-plane/test/bounds.test.mjs` (the capped-op roster 22 -> 24 MEASURED by running the walk, plus three driven arms), `bio-plane/test/gate-reads.test.mjs` (both new read ops classified — one GATED, one UNGATED with its reason), `bio-plane/test/daemon-token.test.mjs`. `bio-plane/test/plane-envelope.test.mjs` was NOT edited: its DETECTOR C failed and the fix was to route the new read through `doAnswer` rather than to widen the pinned list. `docs/development/CLAIMS.md` (this entry and its DELEGATIONS).
interfaces consumed: I3 (PL-3's `op=suggest` shape, read-only here), I5 (PL-5's `ai_runs` two-principal columns, read-only here).
interfaces owned: **I3 — two new ops `capturerequest` and `capturerequestdrain`, additive; the IC entry is filed PROPOSED and the version bump and RESOLUTION are CONDUCT's.** **I5 — TOUCHED: one new scratch-class table.**
expected: IS-BUILD-PLAN PL-4 — **`capture_requests` (SWEEP §4b.1)**. Scratch-class table in `capture_sessions`' family, written by the request door, DRAINED BY THE DAEMON, with **DEC-47's conduct enforced ONCE at the drain** (a UA with a contact URL, a `purpose` token, rate; robots.txt disallows do NOT bar public documents and the member-browser UA is permitted, BOB-3). **THE STRUCTURAL GATE: THE AI DOES NOT CAPTURE. It REQUESTS, and the daemon captures with provenance preserved.** Attribution states BOTH principals (DEC-27(b)/DEC-55.4) — daemon-at-the-session's-request, the machine principal named as `token:<class>` and never a person's name.
accepts-when: as IS-BUILD-PLAN PL-4. **Battery baseline 114/114 at 6,887 MEASURED IN THIS WORKTREE BEFORE ANY EDIT — the brief said 114/114 at 6,887 and is CONFIRMED rather than inherited; `--strict` exit 0 (OPS 150/150, CHECKS 123/123) and UI harness exit 0 both measured here first, unpiped.** Full `npm run test:battery` green with the delta ATTRIBUTED per suite and MEASURED by re-running the true baseline with the new suite moved aside rather than by subtraction; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped; `node civicos-ui/test/run.mjs` exit read unpiped. Negative controls RUN: each conduct rule broken ALONE failing by name; **the AI capturing directly rather than requesting -> FAILS**; a record naming only one principal -> FAILS; the table absent from `purge` -> hygiene FAILS naming it; plus an over-strictness arm (a robots.txt-disallowed public document still captures, BOB-3). No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and DEBT.md are not written by this session.
concurrency: every claim block above this one is `released:`. Checked over the whole file: no live claim names `schema.mjs`, `store.mjs`, `index.mjs`, `bio-checks.mjs`, `civicos-ui/check-refusal-codes.mjs` or any suite named above. **PL-11 (the `ai` credential class) depends on this item and is NOT opened here**: no credential class, no `token:ai` predicate and no `classify` arm is written by this session, and a DELEGATION below says what it inherits.
released: 2026-08-08 - CONDUCT at integration - merged on main. Verified FROM THE MAIN CHECKOUT: battery 115/115 at 6,994 with the +107 attributed by re-running the true baseline; coverage --strict run DIRECTLY with $? unpiped exit 0, OPS 154 all reached and CHECKS 136 all named; UI harness exit 0 read unpiped. Two findings of its own work are worth the record: the DEC-49 guard caught a `where` pointing at a name that DOES NOT EXIST, so nothing would have been checking that site at all - REC-71's convention earning its keep three items after it landed; and it DELETED TWO OF ITS OWN CODES because they could not be driven, recording them UNALLOCATED with an assertion saying so, on the reasoning that an undrivable code is a refusal nobody can prove fires. Its arm (7b) demonstrates that hygiene's D-113 check cannot see half the defect it exists for - deleting only the whole-store purge arm leaves hygiene green while a purge reporting scope ALL leaves the queue standing. Measured and delegated. CONDUCT acted on its store.mjs finding: CLAUDE.md's line count was wrong for the FOURTH time (~16,300 against a real 21,248), so that line now carries THE COMMAND rather than a number, with the reasoning in MEASUREMENTS.md.
released:

### DELEGATION 2026-08-08 RECORD (PL-4) -> PL-11 (the `ai` credential class): **WHAT THE FENCE ALREADY DOES WITHOUT YOU, AND THE ONE ROW YOU OWE**

- **THE CAPTURE FENCE DOES NOT DEPEND ON THE CLASS LIST AND MUST NOT BE MADE TO.** `op=acquire`'s capture-request arm admits a row in `draining`, and `draining` is set by the drain inside the tick that then fetches. So a member's credential, an operator's, and **the daemon's own outside a tick** are all refused identically — driven, all three, in `test/capturerequests.test.mjs`. `token:ai` is caught the day it exists with no edit to that line. **Do not add `ai` to a list to make this work, and do not remove the shape check because a list now exists.**
- **What you DO owe: two OPS rows narrowed.** `capturerequest` rides `["admin", "member", "probe"]` today and PL-11 narrows it, which is the direction IS-6 recorded as safe. `capturerequestdrain` rides `["admin", "probe", "daemon"]` and **must never gain `ai`** — the AI requests, the daemon drains, and an `ai` credential on the drain would be the AI capturing with an extra step.
- **`CAPTURE_NOT_DRAINING` belongs in your `MACHINE_CANNOT_*` sweep** for the same reason PL-3 put `SUGGEST_UNWRITABLE_STATE` there: it is a `MACHINE_CANNOT_` rule wearing a different name, because it refuses an *act* rather than a *transition*.
- **The daemon class is now THREE verbs, not two**, widened BY DECISION (SWEEP §4b item 1) with `test/daemon-token.test.mjs`'s totality assertion corrected in the same turn. If your item widens it again, correct that assertion rather than exempting it.

### DELEGATION 2026-08-08 RECORD (PL-4) -> FL-3 (the run harness) and PL-15 (the out-of-inquiry lead): **THE REQUEST DOOR'S CONTRACT, AND THE ONE THING NEITHER SHOULD REBUILD**

- **`op=capturerequest` is the ONE ask.** It writes a row and holds no outbound call at all — asserted over its own source, because a fence you can only see by reading carefully grows a hole nobody notices. `op=capturerequests` reads the queue back (gated at the inquiry, `truncated` published); the completion arrives as a queue CONDITION.
- **THE DOOR IS IDEMPOTENT ON (run, address).** A run that asks twice has asked once and gets the standing row back. So the harness's dedup-before-write row is about *its own* work list, not about protecting somebody's server — that arithmetic is already at the door.
- **A HELD REQUEST IS NOT A FAILED ONE.** `held[]` carries a C-number and the row stays `requested`; `refused[]` is terminal. A harness that retries a `held` request immediately is fighting DEC-47's rate rule, and `attempts` climbing with no capture is the signal that it is.
- **DO NOT REBUILD THE CONDUCT RULES FLEET-SIDE.** They are the PLANE'S, enforced once at the drain (SWEEP C11's posture, one door over). The fleet member receives verdicts.
- **PL-15's lead lands exactly here:** a requested capture enters the store at `collected` and enters NO basis — asserted — which is the "captured, not evidence" half D-213 needs, and the completion CONDITION is a producer it can extend rather than a channel it must invent.

### DELEGATION 2026-08-08 RECORD (PL-4) -> WHOEVER OWNS `hygiene.test.mjs`'s D-113 CHECK: **THE CHECK CANNOT SEE HALF OF THE DEFECT IT EXISTS FOR, AND IT IS MEASURED**

**RUN, not argued** (`test/capturerequests.control.mjs` arms 7a and 7b, both green as declared). The D-113 check derives its covered set from `DELETE FROM <table>` **anywhere inside the purge METHOD**, so **ONE arm satisfies it**. Remove only the WHOLE-STORE delete and leave the per-bundle one and the check stays GREEN at 467/467 — while `op=purge` reports scope `ALL` and leaves every row of that table standing, which is exactly the silent leftover the check exists to prevent. PL-12's merge nearly dropped two tables in the same window; had it dropped only their whole-store arms, nothing would have caught it.

- **Provisional:** PL-4 covers the gap with its own arm (7b) against its own suite. That protects ONE table.
- **The fix is small and general:** derive the covered set PER ARM — split the purge source at the `if (bundleId)` / `else` boundary and require a table to appear in the arm that must clear it — then state the exemptions per arm too. It is a change to a landed instrument owned by another family, which is why it is delegated rather than made here (REC-71's rule).
- **Reversal cost:** none today; rising with every derived table added, because each one is admitted by half a check.

### DELEGATION 2026-08-08 RECORD (PL-4) -> PL-5 / FL-6 (the run object and the account cascade): **`aiRunOpen` ACCEPTS A PRINCIPAL OF WHITESPACE, AND A BLANK PRINCIPAL IS WORSE THAN AN ABSENT ONE**

**MEASURED while building this item's one-principal fixture, and it is why that fixture needs no hand-written SQL.** `aiRunOpen`'s guard is `if (!principalPlane || !principalClaude)` — a falsiness test — so `principalClaude: " "` opens a run that names nobody **while looking like it names somebody**. That is the worse direction: an absent principal is visibly absent, a blank one reads as present and travels into every act the run authorises.

PL-4's attribution composer TRIMS before judging, so the drain refuses to capture for such a row (`C-28.11`, driven). **It is caught downstream, not fixed upstream**, because the guard is PL-5's and reaching into a neighbouring family's landed check is the mistake REC-71 exists to correct.

- **Recommendation:** trim in `aiRunOpen` (and in `scopeFor`'s principal stamp when FL-6 lands the cascade), one line each.
- **Reversal cost:** low now. Once runs exist carrying blank principals, every act they authorised is unattributable and no later trim recovers the name.

### DELEGATION 2026-08-08 RECORD (PL-4) -> UI-52 / WHOEVER SURFACES THE QUEUE: **THE COMPLETION IS A CONDITION, AND THE PLAN'S "FINDING-CLASS SLUG" IS A CITATION ERROR RATHER THAN A DESIGN**

`IS-BUILD-PLAN.md`'s PL-4 row and `INVESTIGATIVE-SESSION.md` §4 both say the completion *"notifies via the existing FINDING-class slug (D-61's generator)"*. **D-61's kind is `capture-completed-unattended` and `queuestate.mjs` has classed it as a CONDITION since REC-32, with a live producer.** PL-4 built it as the CONDITION it is and did NOT reclassify it, because a kind's class decides whether leaving the list is a **personal mute** or an **authored record act** (D-125, DEC-16) — moving one to match a sentence in a plan would be changing doctrine to fix a citation.

**Consequence for a surface, and it is the only one:** a member may MUTE this kind, and muting is personal, so the completion disappears from one member's list while the capture stands and every other member still sees it. That is the right semantics for "a document you asked for has arrived" and the wrong semantics for anything that owes an authored disposition. **No new act, no new slug, no register row** — `capturerequest` is a `NON_ACTS` row for the same reason `suggest` is.

**Two producers now mint that kind** and they answer different questions: `#conditionsCaptureUnattended` (a PERSON's document finished by a daemon — requires an earlier human manifest author, so a requested capture never matches it) and `#conditionsCaptureRequested` (a capture the SESSION is waiting on). A surface that assumes one producer per kind will read the second as a duplicate.

## CLAIM 2026-08-08 RECORD
session: is-wave-w0-d216-model-check (worktree agent-ab6b0ba57cf7685e8)
opened: 2026-08-08T08:20:00Z
paths: **`bio-plane/test/d216-sharing.probe.mjs`** (NEW, and deliberately **NOT** a `.test.mjs`: `scripts/battery.mjs` discovers `.endsWith(".test.mjs")` and nothing else, so this instrument runs on demand and moves the battery by ZERO — which is what a slot-free measurement lane owes). **`docs/development/MEASUREMENTS.md`** — ONE new dated section, the answer with its instrument. **`docs/development/DEBT.md`** — the **D-216 row REWRITTEN IN PLACE**; no row added and none removed, because `planning-hygiene.test.mjs` emits ONE assertion PER DEBT ROW (`for (const r of rows) t(...)`) and adding a row would move the battery by +1 for a measurement item that must move it by zero. **`docs/development/CLAIMS.md`** (this entry and its DELEGATIONS). **NOT** `src/store.mjs`, **NOT** `src/index.mjs`, **NOT** `src/schema.mjs`, **NOT** `checks/bio-checks.mjs`, **NOT** any `.test.mjs`, **NOT** `INVESTIGATIVE-SESSION.md` and **NOT** `IS-BUILD-PLAN.md` — this item MEASURES and records; the design-document corrections it earns are DELEGATED below rather than made here, because §7 and the plan row are the surfaces whose claim is under test and a session must not mark its own paper.
interfaces consumed: I3 (`op=basisversions`, `op=versioncurrent`, `op=cite`, `op=promote`, `op=inquirystrength` — all read-only and all DRIVEN, none changed).
interfaces owned: none. Nothing is added to I3 or I5.
expected: IS-BUILD-PLAN's **W0 slot-free lane — the D-216 MODEL CHECK**, the precondition PL-13 is BLOCKED on. The question, and it is a fact about the code rather than a preference: **does an inquiry shared across projects have ONE stance every referencing project must share, or a PER-PROJECT one?** Establish (a) whether a real edge lets two projects reference one inquiry and what it is called; (b) whether it can be DRIVEN end to end through ops that exist; (c) what each project SEES — the same versions, evidence and strengths; (d) where a per-project CURRENT pointer hangs, and whether PL-2's shipped `current_versions[]` row survives the answer.
accepts-when: a dated `MEASUREMENTS.md` entry with the instrument NAMED; the answer stated as a fact about the code with what was DRIVEN; battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and final battery with **zero delta** (baseline measured here: **115/115 at 6,994**, which CONFIRMS the brief rather than inheriting it); `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped; every assertion the probe makes carries a negative control that RUNS, including a guard on the EMPTY case, because *"two projects both see the inquiry"* is trivially true if neither can see anything. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, INTERFACES.md and IS-BUILD-PLAN.md are not written by this session.
concurrency: checked over the whole file — no live claim names `MEASUREMENTS.md`, `DEBT.md`, or any file under `bio-plane/test/` beginning `d216`. This session opens no source path at all, so it cannot collide with a build slot.
released: 2026-08-08 - CONDUCT at integration - merged on main. Verified FROM THE MAIN CHECKOUT: battery 115/115 at 6,994 ZERO delta, coverage --strict run DIRECTLY with $? unpiped exit 0, UI harness exit 0. ANSWER: PER-PROJECT - section 7 is correct and cloning is not the honest answer, driven through twelve ops rather than read, with CROSSED BARS proving the plane walks the edge many-to-one in a way no single-project walk could produce. PL-2's pointer survives unchanged; PL-13 survives with its scope changed to CONFIRM rather than rebuild. ITS FINDING OUTRANKS ITS ANSWER and is routed as REC-72: op=cite REFUSES an inquiry on the project arm, and op=sever identically - so the edge the whole IS build hangs on has NO CURATED PRODUCER, and a project draws on a question only by hand-authoring bundle.md. CONDUCT confirmed the refusal class in source at three sites; the DRIVEN evidence is the worker's and is the stronger of the two. It also corrected the citation section 7 and D-216 both rested on (a name collision), and its own control arm 3 is a demonstration worth keeping: with an empty version list, 'both projects see the identical set' STILL PASSED - the non-empty guard is the evidence, not the equality.
released:

### DELEGATION 2026-08-08 RECORD (D-216 model check) -> PL-13 (IS-3): **THE ANSWER IS PER-PROJECT, HALF YOUR SCOPE HAS ALREADY LANDED, AND YOU INHERIT A GAP THE PLAN DID NOT KNOW ABOUT**

**THE ANSWER, as a fact about the code and not a preference: PER-PROJECT. §7 is correct, cloning is not the honest answer, and PL-13 is the right item.** Driven — `bio-plane/test/d216-sharing.probe.mjs`, 38/38, with `d216-sharing.control.mjs`'s three arms all as declared. The full record with its blind spots is `MEASUREMENTS.md`, 2026-08-08.

- **PL-2's POINTER SURVIVES UNCHANGED — do not re-specify it.** The dated, project-authored `current_versions[]` frontmatter row is built, is in the project's own bytes, is attributed and dated, has ONE writer and ONE reader, and no stance table exists anywhere. Two projects were driven onto two different readings of one inquiry simultaneously and neither moved the other. **PL-13's row as written re-specifies work that has landed**; rewrite it to CONFIRM rather than rebuild.
- **WHAT IS GENUINELY OUTSTANDING is the two FINDING-class slugs.** Asserted ABSENT by the probe (`queuestate.mjs` contains neither `stance-changed` nor `new-version-arrived`) precisely so nobody can read the measurement as evidence they exist.
- **AND ONE THING THE PLAN DID NOT KNOW: `op=cite` REFUSES AN INQUIRY ON THE PROJECT ARM (`NOT_INFORMATION`), so the edge your item stands on has NO CURATED PRODUCER.** A project draws on a question only by authoring its own `bundle.md` and calling `op=promote`. `op=sever` is refused identically, so a project cannot withdraw from a question through an act either. **Provisional:** nothing is broken today — `refs` is re-derived from the document inside promote's transaction (D-21), so the authored route writes the same table. **Alternative:** record deliberately that a project's interest in a question is authored and never acted. **Recommendation:** widen `cite()`'s project arm to admit `inquiry`, which is one predicate and exactly the shape REC-37 already used on the question arm — the act, its affordance, its four beats and its `ACT_FLOW` entry all exist and none of them would move. **Reversal cost:** one predicate per arm and the suites that pin them, today; rising once surfaces are built against "a project cites only Information".
- **THE ONE LIMIT THAT COULD MOVE THE ANSWER, and it is yours to watch: STRENGTH IS QUESTION-SCOPED TODAY.** `#strengthWalk` reads the inquiry's own `inquiry_basis` and never a version's legs (asserted over real source), and `op=inquirystrength` takes no project parameter. So §6 rule 5's *"current is what the effective strength pair is computed over"* is NOT built, and a per-project stance is INERT for strength. **Re-run the probe when PL-16 makes strength version-relative** — that is the change that could make a per-project stance produce a per-project NUMBER, which is a different and larger question than the one D-216 asked.

### DELEGATION 2026-08-08 RECORD (D-216 model check) -> WHOEVER OWNS `INVESTIGATIVE-SESSION.md` §7 AND `IS-BUILD-PLAN.md`'s PL-13 ROW: **THE CONCLUSION IS RIGHT AND THE EVIDENCE CITED FOR IT IS A NAME COLLISION**

Not corrected here on purpose: §7 and the plan row are the surfaces whose claim was under test, and a session must not mark its own paper.

- §7 says *"linking a bundle to a project creates an EDGE (a `refs` edge — many projects may cite one inquiry)"* and D-216's row named `linkproject` as the act that proves it. **`op=linkproject` is link + project-as-a-VERB.** It takes `capture=<sha256>`, calls `projectLinks`, and projects that capture's RESOLVED LINKS into `links_to` edges. It cannot take a project bundle and it never writes a `cites` edge.
- **The conclusion §7 drew is CONFIRMED by measurement** — the edge is real, it is `cites` in `refs`, and it is genuinely many-to-one. Only the citation is wrong. Replace it with what was driven: `op=backlinks` returns both citing projects, `op=stats.refs` grows by exactly one per citing project, and `op=strengthbarof` composes strictest-per-axis over BOTH.
- **§7's "Open verification" paragraph and its `→ D-216` pointer are now DISCHARGED** and should say so, with the date and the instrument.

### DELEGATION 2026-08-08 RECORD (D-216 model check) -> WHOEVER OWNS `versionstate.test.mjs`: **YOUR §7 SECTION PROVES NON-INTERFERENCE ONLY IN THE EMPTY CASE**

Measured, not argued. Block 10's arm reads *"AND IT MOVED NOBODY ELSE: the second project sharing this question still stands on nothing"* and asserts `[otherRead?.current, ...] === [null, undefined]`. **A project standing on NOTHING cannot be moved off anything**, so the arm holds whether or not the pointer is per-project. Arm 2 of `d216-sharing.control.mjs` makes the point concrete: with `#currentVersionOf` reading the FIRST citing project instead of the named one — D-216's literal alternative — the probe loses five arms and `versionstate.test.mjs`'s block 10 would not notice.

- **The fix is two lines in the existing block:** make the second project stand on the OTHER accepted reading, then assert the two differ and that the first did not move. The fixture already promotes `PROJ-2026-2000-budget` with the citation; only the act is missing.
- **This is not a defect PL-2 introduced** — nothing it asserts is false, and its own subject was the state machine rather than the sharing model. It is delegated rather than edited here because widening another family's landed suite is the mistake REC-71 exists to correct.
- **Reversal cost:** none. The arm gets strictly stronger.


## CLAIM 2026-08-08 RECORD
session: pl11-ai-credential
opened: 2026-08-08T15:10:35Z
paths: the `ai_credentials` table in bio-plane/src/schema.mjs, `classify`/`scopeFor`/`aiTaskScope`/the ai-credential ops in bio-plane/src/index.mjs, `aiCredentialMint`/`aiCredentialLook`/`aiCredentialRevoke`/`aiCredentials` and the `ai_credentials` exemption in bio-plane/src/store.mjs, `viewerPredicate`'s class alternation in bio-plane/src/query.mjs, the `AI_CREDENTIAL_CHECKS` family in bio-plane/checks/bio-checks.mjs, bio-plane/test/aicredential.test.mjs, bio-plane/test/aicredential.control.mjs, the D-113 exemption block in bio-plane/test/hygiene.test.mjs, the FLOOR block in civicos-ui/check-refusal-codes.mjs
interfaces consumed: I3
interfaces owned: none
expected: PL-11 / IS-5 — D-199's five determinations whole. ONE `ai` class carrying a DECLARED TASK SCOPE read FROM THE RECORD (never a settings row, never an env binding); minting is a MEMBER act; the record names token IDENTITY and PRINCIPAL; `token:ai` caught by every `MACHINE_CANNOT_*` through REC-46's one predicate. Carries DEC-55.5 WHOLE as owed control 1, including the second half — remove the predicate and every named refusal stops firing — which has never been run in this project.
released:

### DELEGATION 2026-08-08 RECORD (PL-11) -> PL-4 / FL-6 (the capture-request row and the account cascade): **A REQUESTED CAPTURE IS ATTRIBUTED TO THE MEMBER, NOT TO THE CREDENTIAL THAT ASKED — UNDER THE INVESTIGATIVE SCOPE, MEASURED**

**DRIVEN, not argued** (`test/aicredential.test.mjs` block 9, both arms). `capture_requests` copies `principal_plane` **off the run**, and under the investigative task scope the agent **cannot open a run** — `op=airunopen` mutates and is not among its declared writes — so the member opens it and the capture's attribution reads `member:ruth`. Correct as far as it goes: a named person did authorise the session. **Incomplete against D-199 (4):** the act the AGENT performed is attributed to its principal without saying WHICH OF THAT PRINCIPAL'S CREDENTIALS performed it, so two agents minted by the same member are indistinguishable in the provenance of everything they request.

The composite stamp itself works and is driven: a credential whose record declares `op=airunopen` opens its own run, and that run — and every capture requested under it — carries `member:ruth/session-opener`, the principal AND the identity, never the value. So the mechanism is present and the INVESTIGATIVE scope is the shape that does not reach it.

- **Provisional:** as shipped, and PL-11's suite asserts the state as it IS rather than as its brief expected, so the gap is visible rather than latent.
- **Two ways to close it, and they are not equivalent.** (a) Give the investigative scope `airunopen` — one word in the shipped scope, and it makes the agent the opener of its own session, which is a **doctrine change** (DEC-47 makes the SESSION LAUNCH the authorisation for reaching a public source, and a launch nobody human performed is a different authorisation). (b) Carry the requesting credential's identity on the `capture_requests` row beside the run's principals — a column, and no doctrine moves. **Recommend (b).**
- **Reversal cost:** low now, rising with every requested capture: a row written before the column exists cannot be re-attributed afterwards, which is DEC-52's own reason for ruling early.

### DELEGATION 2026-08-08 RECORD (PL-11) -> WHOEVER OWNS `check-refusal-codes.mjs`'s FLOOR BLOCK: **`vocabularyTerms` WAS ALREADY STALE BY TEN WHEN THIS ITEM ARRIVED, AND PL-11 ADDED NO VOCABULARY**

It read **40** over a tree carrying **50**. PL-11 moved it because the rule is to move what you measure, but **this item did not cause it** — the slack accumulated across PL-12/UI-51/PL-1/PL-3/PL-4 without any of them touching arm E. That is the **third consecutive item** to find a stale floor in this block by measuring (PL-3 found `families` reading 5 over 6, PL-4 found the census 19 low and had turned a control from RED to GREEN).

- **The pattern is now evidence rather than anecdote:** the floors an item does not touch are the ones that rot, because the rule as written is *move the floors YOU invalidated*, and nobody re-measures the rest.
- **The fix is small and general:** have the guard FAIL when a floor sits more than N below the measured figure, so slack is a red run rather than a thing the next worker happens to notice. It is a change to a landed instrument owned by another family, which is why it is delegated rather than made here (REC-71's rule).
- **Reversal cost:** none. The cost of not doing it is measured: one control that read GREEN over a reader that had gone partially blind.
## CLAIM 2026-08-08 SKILL
session: is-wave-w0-sk1 (worktree agent-a1f06561dfc61e51c)
opened: 2026-08-08T09:40:00Z
paths: **`bio-plane/src/skillpack.mjs`** (NEW — the doctrine pack, PURE, on `airun.mjs`'s and `queuestate.mjs`'s stated model: no storage, no clock, no viewer). **`bio-plane/test/skillpack.test.mjs`** (NEW) and **`bio-plane/test/skillpack.control.mjs`** (NEW — the committed negative-control driver, a `.control.mjs` so `scripts/battery.mjs`'s `.test.mjs` discovery does not collect it; PL-2's `versionstate.control.mjs` is the precedent). **`bio-plane/checks/bio-checks.mjs`** — ONE row added to the EXISTING `AI_RUN_CHECKS` family (`C-22.7`) and its family header's count corrected from six to seven; no other family, row, translation or `where` is touched, and NO new `*_CHECKS` family is minted (a family is a floor in `civicos-ui/check-refusal-codes.mjs` and a code that buys slack for everybody else's walk is the defect REC-71 measured). **`bio-plane/src/store.mjs`** — `aiRunOpen`'s guard block ONLY, one import line, and the header sentence that describes the conditions; no other method. **`civicos-ui/check-refusal-codes.mjs`** — the `FLOOR` block's figures, MEASURED by that file on a green run and moved in the SAME turn that grows the plane (REC-71's rule, PL-3's and PL-4's precedent); no matcher, arm or ceiling touched. **The six suites that open a run** — `bio-plane/test/{airun,suggest,capturerequests,meaning-bounds,versions,bounds}.test.mjs`, each at its `op=airunopen` fixture site ONLY, one field and one dated reason line: a floor this item invalidates is moved in the same turn rather than exempted. **`docs/development/CLAIMS.md`** (this entry and its delegations).
**NOT OPENED:** `bio-plane/src/index.mjs` (no op is added — the pack is rendered by the fleet member from what the plane ALREADY publishes, and an op nobody consumes is a coverage row bought for nothing), `bio-plane/src/schema.mjs` (`ai_runs.skill_version` exists — PL-5 built it), `civicos-ui/app.html`, `newgroup/`, `release/`, `QUEUE.md`, `DECISIONS.md`, `INTERFACES.md`, `IS-BUILD-PLAN.md`, `INVESTIGATIVE-SESSION.md`.
interfaces consumed: I3 (`op=affordances`, `op=airunopen`, `op=airun`, `op=airunclose` — all DRIVEN; the pack's vocabulary layer is rendered from `op=affordances`' published answer and from `airun.mjs`'s exported vocabularies, never typed). interfaces owned: none. No op added, no schema touched. **`op=airunopen` GAINS A REFUSAL** — a run that cannot name the skill version it ran under is refused where a run that cannot name its principals already is; that is a narrowing of an existing shape and the IC is CONDUCT's to file if it judges one owed.
expected: **SK-1** — the doctrine pack, VERSIONED. Two layers (always-resident: the §2 objective, the machine/member boundary, the four-level rule, the absence vocabulary; progressively disclosed: the vocabularies and recipes of §14b.1), a version string the pack CARRIES, and PL-5's run object recording it, such that a run under pack vN and a rerun under vN+1 are distinguishable in their run objects. Every vocabulary DRIVEN or IMPORTED and none typed.
accepts-when: battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT (**115/115 at 6,994**, which CONFIRMS the brief rather than inheriting it) and final battery with the delta ATTRIBUTED PER SUITE by re-running the true baseline rather than by subtraction; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED; `node civicos-ui/test/run.mjs` exit 0 read UNPIPED. **THE ROW'S OWN NEGATIVE CONTROL RUN: strip the version from a run's conditions and the run-object suite FAILS naming the missing condition** — run in both directions (the guard removed, and the recording removed), plus a sourcing control that replaces a driven vocabulary with a hand copy, plus an EMPTY-CASE guard (a pack rendered over an empty published vocabulary must FAIL, because a sourcing arm that passes over nothing asserts nothing), plus an OVER-STRICTNESS arm (a correct pack version phrased unlike anything this item wrote must PASS). No push, no merge — CONDUCT integrates.
concurrency: checked over the whole file. The only live claim is `ui52-arm-a4-narrowing`, whose paths are `civicos-ui/test/surface-registry.test.mjs`, `civicos-ui/test/surface-registry-a4.control.mjs` and this file — it opens no `bio-plane/**` path and no `civicos-ui/check-refusal-codes.mjs`. No live claim names any path above.
released:

### CLAIM AMENDMENT 2026-08-08 SKILL · is-wave-w0-sk1 — **TWO UI FIXTURE PATHS ADDED, AND THEY WERE FOUND BY THE HARNESS RATHER THAN PREDICTED**

Amended into the SK-1 claim above, same session, before any handoff. **Two paths added, both by SITE and both one line:**

- **`civicos-ui/test/ai-session-wire.test.mjs`** — the `SKILL` fixture constant only.
- **`civicos-ui/test/ai-session-context.test.mjs`** — the `openRun` helper's `skillVersion` argument only.

**WHY, and it is a real collision rather than housekeeping.** Both UI-49 suites draw their fixture values at runtime (the point of that suite: *"a literal in app.html cannot match a value that did not exist when it was written"*), and both drew the skill version as `skill-<tag>` — **a version that names no PACK.** SK-1's C-22.7 refuses that shape, so `op=airunopen` refused both fixtures and the UI harness went from exit 0 to **exit 1 with two suites down and two TypeErrors** (a null run read as `.label` and `.budget`). Caught because the harness was RUN, not because the change looked safe.

**THE JUDGEMENT, since it is a real one: the format rule STAYS and the fixtures are CORRECTED.** `<pack>@<edition>` is load-bearing rather than aesthetic — the investigative session and the assistant pilot are **siblings with two doctrine packs writing one `ai_runs.skill_version` column** (`ASSISTANT-PILOT.md` §4's DEC-60 note), so an opaque token identifies nothing the day the second pack exists. The fixtures are still drawn at runtime (`pack-<tag>@<tag>`), so nothing about what those suites actually test moves. Corrected with a dated reason at each site, never exempted.

### DELEGATION 2026-08-08 SKILL (SK-1) -> PL-5 / FL-6 / WHOEVER NEXT OPENS `aiRunRead`: **`op=airun` PUBLISHES TWO OF THE RUN'S THREE CONDITIONS, AND THE THIRD IS SILENT**

**MEASURED at the source while wiring SK-1's recording, not inferred.** §11 names three conditions a run is formed under — the bias manifest in force, **the launching project's declared standard PAIR**, and the skill version. `ai_runs.standard_pair` is written by `aiRunOpen` (store.mjs:18358) and published by `aiRunSpawnPayload` (store.mjs:18717) — **and `aiRunRead` does not publish it at all.** A reader of the run object sees the skill version and the bias block and cannot see the bar the run was working to.

- **This is PL-12's own finding arriving on the next field.** Its header records exactly this shape for bias: *"this method published NO bias field of any kind … the run held a column and the answer was silent. An unstated limit reads as completeness."* The fix there was three distinguishable answers rather than a bare echo; the standard pair needs at least the echo.
- **Not fixed here on purpose.** It is PL-5's method and DEC-17/DEC-21's semantics (the pair is per-project, and *a projectless inquiry has no CURRENT and no bar* — SWEEP C8), so the honest publication has to distinguish "no bar was in force" from "the bar was X", which is a decision about the pair and not about SK-1's condition.
- **Reversal cost:** none today. Rising with every run that lands: a version composed under a bar nobody can read back is a version whose sufficiency cannot be re-judged, which is the reason §11 records conditions at all.

### DELEGATION 2026-08-08 SKILL (SK-1) -> REC-64: **THE PACK RENDERS ONE MACHINE FENCE OF TWELVE, AND THE OTHER ELEVEN ARE NAMED**

The doctrine pack's machine/member boundary is rendered from the check catalogue's **canned translations**, verbatim, because `ASSISTANT-PILOT.md` §1's rule is that the assistant SURFACES a refusal's text and never paraphrases it. **Measured and printed by `test/skillpack.test.mjs` ARM H1 on every run: the plane mints 12 `MACHINE_CANNOT_*` codes in `src/` and exactly ONE carries a canned translation** (`MACHINE_CANNOT_MOVE_VERSION`, C-25.24). The pack therefore renders 1 of 12 and **states in its own body that it is rendering a subset** rather than letting the boundary read as complete.

- Four of the eleven are already inside REC-64's named reach gap (`MACHINE_CANNOT_CONCLUDE`, `_CORRESPOND`, `_MOVE_ACTION`, `_RESOLVE`); the rest are out of reach and in the reported census.
- **The consequence is specific to the AI build rather than general:** these are the sentences the investigative session is supposed to hand a member when the fence fires, so every untranslated one is a fence the assistant can only describe in words it made up — which is the one thing §1 forbids it to do. Translating this family is worth more per row than the rest of the sweep.
- **Reversal cost:** none. The pack's harvest is by prefix over the families, so a row translated tomorrow appears in the next render with no edit here — and moves the pack's version, which is the intended behaviour.

### DELEGATION 2026-08-08 SKILL (SK-1) -> SK-2 AND FL-3: **WHAT THE PACK GIVES YOU, AND THE ONE LAYER IT DELIBERATELY LEFT EMPTY**

- **`renderPack(published, catalogue)` is the whole contract**, and `published` is `op=affordances`' own no-target answer passed in UNRESHAPED. It THROWS on an empty vocabulary, an empty catalogue, or a catalogue with no machine fence — a pack rendered over nothing is the false-coverage shape and must not be silently produced. Do not add a fallback.
- **The version is DERIVED, not bumped.** `<pack>@<edition>+<digest of the rendered pack>`. Bump `DOCTRINE_EDITION` when the authored doctrine changes; the digest moves on its own when a published word moves. **Record the version you rendered — `op=airunopen` now refuses a run that cannot name one (C-22.7).**
- **THE RECIPE LAYER IS DECLARED AND EMPTY, and it is SK-2's.** `ASSISTANT-PILOT.md` §1 requires a recipe to be DATA whose every step names a surface id and an act, mechanically validated so a step naming a surface that does not exist FAILS THE BUILD. **The surface registry is `civicos-ui`'s and no plane op publishes it**, so a recipe authored plane-side could not be validated plane-side — the layer says so in its own body rather than being quietly absent. Filling it means either publishing the registry or rendering the pack where both halves are reachable.
- **The pack is rendered by whatever RUNS under it and by no op.** An op returning the pack would be a plane-side copy of a thing whose entire point is that it is rendered from what the plane already publishes.

## CLAIM 2026-08-08 RECORD
session: is-wave-w5-pl14 (worktree agent-a78ca0f9b029b7dfa)
opened: 2026-08-08T08:15:00Z
paths: **`bio-plane/checks/bio-checks.mjs`** — ONE new exported `VERSION_STRENGTH_CHECKS` family (**C-29.x as authored; RENUMBERED TO C-30.x AT INTEGRATION 2026-08-08 by CONDUCT** — PL-11 and PL-14 ran in parallel, each MEASURED C-29 as free over the real catalogue and each was right when it looked, and each allocated nine numbers in it. PL-11 merged first, so its numbers stand. This entry keeps the number PL-14 authored because the worker's report and suite header name it, which is what makes the renumbering traceable rather than silent; C-25 is PL-1/PL-2's, C-26 is PL-12's, C-27 is PL-3's, C-28 is PL-4's, C-29 is PL-11's) and ONE new exported `VERSION_STATE_SET` roster. No existing check's behaviour is changed; `VERSION_MACHINE`, `BASIS_GRADES` and `GRADE_SOURCES` are READ from the existing exports and never re-typed. **`bio-plane/src/store.mjs`**, named by SITE: (a) ONE new public `versionStrength` inside a **`DEC-49 REGION is-version-strength`** span; (b) ONE new private `#versionLegsAsMembers` (the earned-grade resolution, hunch exclusion and the inert-and-named naming); (c) ONE new private `#refusePairComposed` (DEC-44's guard, run over the return); (d) ONE dispatch entry. **NOT** `#strengthWalk`, `#axisResult`, `#groundResult`, `#weakestOf`, `#namedMember` or `strengthOf` — REC-12/REC-42's arithmetic is CALLED through the existing `legsOverride` seam PL-3 opened and is not re-implemented, re-typed or edited. **NOT** `earnedBasisRegistry` (READ only). **NOT** `basisVersions`, `#currentVersionOf`, `#moveVersionState` or `suggestVersion` (PL-1/PL-2/PL-3's, READ as precedent, untouched). No schema change: this item adds no table and no column. **`bio-plane/src/index.mjs`** — ONE `OPS` row (`versionstrength`) and its arm on the fail-closed viewer stamp. **`bio-plane/test/strengthpair.test.mjs`** (new, this item's suite — named so `node scripts/battery.mjs strengthpair` selects it) and **`bio-plane/test/strengthpair.control.mjs`** (new, the negative-control driver, deliberately not a `.test.mjs`). **`civicos-ui/check-refusal-codes.mjs`** — the `FLOOR` figures only, MEASURED and moved IN THE SAME TURN. Pin/ratchet rows corrected in the same turn with a dated reason and never exempted, FINAL SET TO BE MEASURED rather than predicted: `bio-plane/test/bounds.test.mjs` (the capped-op roster), `bio-plane/test/gate-reads.test.mjs` (the new read op classified). `docs/development/DEBT.md` — the **D-159 row only**, gaining a dated RE-READ note (PL-14's own instruction; the row's disposition is not changed by this session). `docs/development/CLAIMS.md` (this entry and its DELEGATIONS).
interfaces consumed: I3 (PL-1's `op=basisversions` version/leg projection and PL-2's version states, read-only here), I5 (untouched).
interfaces owned: **I3 — ONE new op `versionstrength`, additive; the IC entry is filed PROPOSED and the version bump and RESOLUTION are CONDUCT's.**
expected: IS-BUILD-PLAN PL-14 — **THE STRENGTH PAIR over the current version's accepted legs (IS-7, §12).** DEC-32's MIN over AND / MAX over OR / MIN within a branch, per axis; a state-set argument defaulting to accepted; **the return CARRIES the state set that produced it**; ungraded legs INERT **and** NAMED; hunches excluded; grades arriving from `earnedBasisRegistry` and never minted; what-if values in-band per DEC-40. **THE PAIR IS NEVER COMPOSED INTO ONE NUMBER — R2's forbidden composition, which DEC-44 refuses at case altitude for the same reason — and it is ASSERTED rather than merely avoided.** D-159 RE-READ against a machine shaping legs at volume, outcome STATED either way.
accepts-when: as IS-BUILD-PLAN PL-14. **Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief's figure** (the brief carries 115/115 at 6,994). Full `npm run test:battery` green with the delta ATTRIBUTED per suite and MEASURED by re-running the true baseline with the new suite moved aside rather than by subtraction; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped; `node civicos-ui/test/run.mjs` exit read unpiped. Negative controls RUN, **owed control 3 (DEC-40's) first**: produce a what-if rendering and STRIP its filter/state-set line -> the harness FAILS; compose the two axes into one value anywhere -> DEC-44's refusal FIRES; plus the arithmetic, hunch, earned-grade and inert-and-named arms each armed ALONE, and an over-strictness arm. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md and INTERFACES.md are not written by this session.
concurrency: every claim block above this one is `released:`. Checked over the whole file: no live claim names `store.mjs`, `index.mjs`, `bio-checks.mjs`, `civicos-ui/check-refusal-codes.mjs`, `DEBT.md` or any suite named above. `schema.mjs` is NOT claimed and is NOT edited — this item adds no table.
released:

### DELEGATION 2026-08-08 RECORD (PL-14) -> PL-1's FAMILY / WHOEVER OWNS `basisVersionFindings`: **THE EARNED CHECK DOES NOT REACH VERSION LEGS, AND IT IS MEASURED RATHER THAN INFERRED**

**DRIVEN, not read off the source** (`test/strengthpair.test.mjs` block 5, arm *"a version leg claiming more than the record earns LANDS at the write"*). `checkInquiryBasis` calls `checkEarnedLeg` over `basis[]`; `basisVersionFindings` has its own leg loop and calls it over NOTHING. So a `basis_version_legs[]` row may state `grade: A, grade_axis: connection` for a document the record earns C for, or `grade: A` on the capture axis where the ceiling is B, **and the promotion succeeds.** The same claim written as a `basis[]` leg is refused.

- **PL-14 covers it at the READ** — every effective grade comes from `earnedBasisRegistry`, the capture axis is capped at the ceiling, and the answer publishes `authored` beside `grade` so the divergence is visible. That is the right layer for it under §12 (*"a frozen version freezes the grade-REFERENCES — not the grades"*), and it would be needed even if the write refused, because the earned grade MOVES underneath a frozen version by design.
- **The open question is whether the WRITE should refuse as well**, and it is genuinely two-sided. FOR: a version is a document a member reads, and a row saying A that every derived answer reports as C is a document that lies to its own reader. AGAINST: refusing at the write would make a version's legality depend on a registry state that legitimately changes, so a version frozen today could become unpromotable tomorrow — which is exactly the coupling PL-1 refused when it kept `run` unresolved.
- **Recommendation:** do NOT refuse; instead have `op=basisversions` publish the effective grade beside the frozen one, as `op=versionstrength` already does, so the document and the derivation cannot silently disagree on any surface. One reader, two numbers, both visible.
- **Reversal cost:** low now. Rising once versions carrying overclaimed rows exist and a refusal would strand them.

### DELEGATION 2026-08-08 RECORD (PL-14) -> CONDUCT / WHOEVER OWNS `MILESTONES.md`: **M9's ACCEPTANCE SENTENCE CARRIES RETIRED VOCABULARY IN BOTH HALVES, AND ONE HALF NAMES THE WRONG STATE**

M9 reads: *"an ungraded leg SUSPENDS its axis, which then reads `undetermined` and names the leg that is why."* **Both words are pre-ruling.**

- `SUSPEND` is **D-160**'s retired word — its dated amendment declares UNRATED canonical and rules that *"every SUSPEND below it is to be read as UNRATED"*, because SUSPEND already meant the OPPOSITE thing in `SB-OUTPUT` §5.1, which D-160 calls *"the single most dangerous collision in the corpus"*.
- `undetermined` is the wrong state for the case the sentence describes. **DEC-18** settled it explicitly: *"if EVERY leg is ungraded there are no load-bearing legs, the conclusion rests on nothing established, and it is UNRATED naming all of them."* A single-ground reading whose only leg is ungraded is exactly that residual case. `undetermined` is what the plane says when a NECESSARY part could not be finished — a different claim (D-129's own distinction), and the record saying the wrong one.
- **IS-BUILD-PLAN's own reconciliation at §6 inherits the error**, writing that the M9-verbatim fixture *"reconciles DEC-18/DEC-32's branch-level suspension with M9's sentence (the single-ground case is the degenerate branch)"*. The degenerate branch is right; the state word it produces is UNRATED.
- **PL-14 asserts the BEHAVIOUR rather than the spelling**, in both states, each over the facts that produce it, and names the leg in both (`test/strengthpair.test.mjs` block 8, with the reasoning at the block head). **It did NOT edit `MILESTONES.md` or `IS-BUILD-PLAN.md`** — neither is this session's path, and correcting a milestone's acceptance sentence is a decision about what M9 means rather than a typo.
- **What is owed:** M9's sentence corrected to UNRATED (with a dated note saying why), and PL-14's row plus IS-BUILD-PLAN §6's conformance paragraph corrected with it. Until then the plan asks a future reader to assert a state that would make the record claim *we could not finish looking* where the truth is *nothing here was established*.

### DELEGATION 2026-08-08 RECORD (PL-14) -> CONDUCT: **`suggest.test.mjs` IS INTERMITTENTLY RED UNDER THE BATTERY, AND A FLAKY SUITE IS A FALSE-RED GENERATOR**

**MEASURED while taking this item's baseline, on a tree where none of PL-14's files were present.** One full battery run reported `suggest.test.mjs 60 pass, 1 FAIL` naming *"a resubmit of a version that LANDED converges too, and BOTH shapes terminate…"*. The identical tree re-run reported 61/0, and the suite run standalone reported 61/0 three times in a row. So the failure is real and intermittent, not a consequence of anything this item did.

- **Why it matters more than one flake:** every worker measures a baseline before editing, and this project's rule is to trust the measured baseline over the brief. A suite that is red one run in N teaches the next worker either to distrust its own instrument or to re-run until green — and both are worse than the flake.
- **Where to look:** the arm is F10's verbatim-resubmit convergence, whose key includes the inquiry's `bundle_sha`. A timing- or ordering-sensitive `bundle_sha` under battery-level concurrency is the shape that would produce exactly this.
- **Not fixed here** — `suggest.test.mjs` is PL-3's path and reaching into a neighbouring family's landed suite is the mistake REC-71 exists to correct.

### DELEGATION 2026-08-08 RECORD (PL-14) -> UI-43 (the accept ceremony) and PL-16 (the published case): **WHAT `op=versionstrength` HANDS YOU, AND THE ONE THING NEITHER MAY DO**

- **THE ANSWER IS A PAIR AND THERE IS NO THIRD NUMBER.** `pair` holds exactly `capture` and `connection`; the plane REFUSES (C-30.7, C-29.7 as authored — see the renumbering note on this claim's `paths` line) if anything beside them could read as a summary of both. **Do not compose them on a surface either** — DEC-44 refuses it at case altitude and PL-14 refuses it at version altitude, and a surface that averages what the plane refused to average is the same defect one layer out.
- **EVERY ANSWER CARRIES `filter` AND `state_set`, AND THE LINE MUST RENDER.** DEC-40: a filtered rendering states its filter IN the artifact. `what_if: true` marks a view the caller constructed; colour may decorate that line and may never BE it. Stripping it is owed control 3 and it fails the harness.
- **UI-43 specifically:** the per-branch independent-sufficiency affirmation is yours. This op reports `pair.<axis>.grounds[]` with each branch's own state, grade and weakest leg — that is the derived structure the member is affirming, in the plane's own words. **No AND/OR/ground/partition vocabulary reaches any surface** (DEC-32 clause 1, D-226); this op's canned translations are already held to that and the suite asserts it of every one.
- **PL-16 specifically:** `ungraded[]` and `hunches[]` are published per answer with a per-leg reason. Hunch debt is publication-DISQUALIFYING (DEC-15/D-223) and `hunches[]` is the enumeration at reading grain — do not re-derive it from `grade_source` on a second path.
- **`states` defaults to `accepted` and that default is load-bearing.** A surface that passes every state to "show more" turns the record's own answer into a what-if without saying so.

## CLAIM 2026-08-08 RECORD
session: rec73-machine-fences
opened: 2026-08-08
paths: bio-plane/test/aicredential.test.mjs (block 8 only — the COMPLETE-PAYLOAD arm per act), bio-plane/test/aicredential.control.mjs (new arms), docs/development/DEBT.md (D-229 disposition only), docs/development/MEASUREMENTS.md (one dated entry)
interfaces consumed: I3, I5 — read-only and DRIVEN, neither changed
interfaces owned: none
expected: REC-73 / D-229 — prove the twelve `MACHINE_CANNOT_*` fences. Eleven of them are LUCK: PL-11 measured that neutering `isMachineStamp` disarms all twelve and every one then falls through to an ordinary payload complaint (`NO_REASON`, `NO_TARGET`, `NO_CONCLUSION`, `NO_SUCH_TASK`) — while `MACHINE_CANNOT_DECLARE`, whose payload happened to be COMPLETE, FULLY SUCCEEDED. The fix is an arm per act driven under a payload that would otherwise SUCCEED, so what refuses is the FENCE and not the complaint behind it. REC-46's predicate is NOT weakened or rewritten — it is what made this measurable in one edit.
concurrency: checked over the whole file. The pl11-ai-credential claim above names the same two test files and is RELEASED work integrated on main; no live claim names them. This session opens NO src path — `src/store.mjs`, `src/index.mjs`, `src/query.mjs`, `checks/bio-checks.mjs` are read and driven, never edited (the control harness edits them transiently and restores by hash AND content).
released: 2026-08-08 - REC-73 worker, committed on branch worktree-agent-a75c0395e77e7eaed, NOT pushed and NOT merged - CONDUCT integrates. NEW: bio-plane/test/machine-fences.test.mjs (45 assertions) and bio-plane/test/machine-fences.control.mjs (5 arms, ALL RUN, all as declared). Battery baseline MEASURED IN THIS WORKTREE at 116/116, 7,090 (the brief carried 7,089); final 117/117, 7,139, delta +49 ATTRIBUTED by re-running and DIFFING the per-suite counts rather than by subtraction: machine-fences +45 (new), hygiene 472->475 (three per-suite loops, one new suite), planning-hygiene 264->265 (one new DEBT row). coverage --strict run DIRECTLY with $? unpiped, exit 0, 117 of 117 declare a control, 380 arms. civicos-ui/test/run.mjs exit 0 read unpiped, unchanged. NOTHING IN THE PLANE WAS CHANGED - REC-46's predicate untouched, no new fence - because the defect was in the CONTROLS. The one edit to a landed suite is a COMMENT in aicredential.test.mjs block 8 saying what it does and does not establish, and pointing at this one. D-229 closed; D-230 raised for the sweep's residual.
released:

### DELEGATION 2026-08-08 REC-73 -> WHOEVER OWNS `aiTaskScope` / THE AI CREDENTIAL MINT (PL-11's family): **A MEMBER CAN AUTHOR AN AI SCOPE NAMING `publish`, `conclude` AND `release`, AND ONLY THE IDENTITY FENCE STOPS IT**

Measured, not argued — REC-73's own credential does exactly that, and it has to, because holding the credential layer open is what lets the identity layer be measured at all (`bio-plane/test/machine-fences.test.mjs`). But the scope the record then holds says the agent may `publish`, and the agent may not publish and never could. **A scope that names an op the caller can never perform is a scope that lies about itself**, and D-199 (2) put the scope in the record precisely so a member could read what a credential may do.

- **Provisional:** as shipped. The mint admits any op a member reaches (`aiReachesAsMember`), and the act refuses.
- **Alternative:** refuse at the MINT any op carrying a `MACHINE_CANNOT_*` fence, so the twelve are unauthorable into an AI scope, and keep the act fence as well.
- **Recommendation:** the alternative, and Bob's own reasoning in DEC-55 is why — *"you do not make an AI safe by trusting it to behave; you make it safe by exposing only endpoints that are safe to call. THE ENDPOINT SURFACE IS THE FENCE."* REC-73 measured what the single shared predicate is worth when it goes: **ten of the twelve acts go all the way through on one edit.** Two do not, and the reason is instructive — `taskforward`/`taskresolve` carry REC-4's `NOT_YOURS` behind the machine fence. Defence in depth exists at exactly two of twelve sites and nobody had chosen that.
- **What NOT to do, and it is the same measurement read the other way: do not duplicate the predicate at the twelve sites.** REC-46 spent a whole item collapsing three unsynchronised answers to a smaller question, and it is the reason this was measurable in ONE edit rather than eleven. The answer to a thin fence is a second INDEPENDENT gate at a different layer, not a second copy of the same one.
- **Reversal cost:** low today — no AI credential exists outside test fixtures. It rises the moment a group authors a scope, because a scope narrowed afterwards is a capability withdrawn from a running agent.

### DELEGATION 2026-08-08 REC-73 -> WHOEVER OWNS `promote`'s OWNERSHIP AND CAS GUARDS: **`NOT_THE_OWNER` SHADOWS 18 REFUSALS AND `CAS_STALE` 17, AND BOTH ARE PINNED ONLY THAT THEY FIRE**

Not raised as debt because both ARE pinned and nothing is known to be wrong — this is the D-229 class asked one layer out, and the two deepest shadows in the plane are here. A refusal driven under a payload `promote` would have refused anyway has been shown to fire and has not been shown to be what fires; `promote` is the largest function in the plane and has 27 distinct refusals behind its first.

- **The instrument already exists**: `machine-fences.test.mjs` block 4 computes the shadow for every refusal and prints the ten deepest. The cheap version of the work is REC-73's shape — drive each under a payload that would otherwise SUCCEED.
- **Reversal cost:** none. The arms get strictly stronger. This is delegated rather than done here because widening another family's landed suite is the mistake REC-71 exists to correct.

## CLAIM 2026-08-08 FLEET
session: is-wave-w6-fl2-vf3 (worktree agent-ad2c65dacc2cd14ed)
opened: 2026-08-08T09:30:00Z
paths: **`agent-worker/**` (NEW, the whole directory — the second fleet member)**: `wrangler.jsonc` (pinned `account_id`), `package.json`, `fleet-member.json`, `.gitignore`, `src/index.mjs`, `test/agent-worker.test.mjs` (its suite), `test/agent-worker.control.mjs` (the negative-control driver, deliberately NOT a `.test.mjs` — it EDITS REAL SOURCES while it runs and the battery must not discover it; PL-3/PL-4/PL-11's precedent). **`bio-plane/wrangler.jsonc`** — ONE new `services` entry (`AGENT_WORKER`), config only, no `src` change. **`bio-plane/scripts/coverage.mjs`** — VF-3: the fleet walk gains a FLOOR, an accounted-for-Workers cross-check, a no-SURFACE-table gate and a fleet-rule-2 (`mutating: false`) gate. **`bio-plane/scripts/battery.mjs`** — VF-3's class sweep: fleet-member suites are DISCOVERED and RUN, or SKIPPED LOUDLY with a named reason (D-93's second half applied to the fleet). `docs/development/INTERFACES.md` (NEW entry I8), `docs/development/INTERFACE-CHANGES.md` (NEW IC-32), `docs/development/CLAIMS.md` (this entry and its DELEGATIONS). **NOT** `pdf-worker/**` (CONTENT-PDF's — DELEGATIONS are appended instead). **NOT** `bio-plane/src/**`, `schema.mjs`, `checks/bio-checks.mjs`, `civicos-ui/**`, `newgroup/**`: this item adds no op, no table, no check family and no member-facing surface.
interfaces consumed: I6 (the fleet-member pattern, as PRECEDENT — copied, not invented), I3 (the plane's op surface, read-only, through the service binding), I1 (untouched).
interfaces owned: **I8 — NEW, `plane <-> agent-worker`, the second fleet service binding. Registered PROVISIONAL BEFORE the first commit of code (PARALLELISM.md's rule, I6's precedent). The IC row (IC-32) is filed PROPOSED; the version bump and the RESOLUTION are CONDUCT's. `I3` is NOT bumped — this item adds no op.**
expected: IS-BUILD-PLAN FL-2 + VF-3, W6 slot A, landing in ONE turn. FL-2 builds the agent Worker on `pdf-worker`'s pattern; it is called only by the plane and calls only the plane's op surface under PL-11's `ai` credential class, holds NO store binding, writes nothing directly, and has NO member-facing surface. Sized on FL-1's MEASURED numbers (2026-08-08): the segment bound is set on the MEMORY curve, never the CPU curve. VF-3 keeps D-117 resolved: `coverage.mjs --strict` enumerates the agent worker's SURFACE table in the same turn, and the fleet figure MOVES rather than holding still while a component goes dark.
accepts-when: as IS-BUILD-PLAN FL-2 and VF-3. **Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief's figure** (brief carries ~118/~7,235; measured 118/118 at 7,235 — they agree, and the agreement is measured rather than assumed). Full `npm run test:battery` green with the delta ATTRIBUTED per suite and MEASURED by re-running the true baseline with the new files moved aside, never by subtraction; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, OPS/CHECKS/FLEET figures reported before and after; `node civicos-ui/test/run.mjs` exit read unpiped; `node civicos-ui/check-refusal-codes.mjs` exit read unpiped (no floor is expected to move — this item adds no plane-side refusal site; MEASURED either way rather than predicted); `node tools/plancheck.mjs` clean but for UNPUSHED. Negative controls RUN, each armed ALONE, every restore verified by sha256 AND by `cmp`, harness INSIDE THIS WORKTREE. No push, no merge, NO DEPLOY — `wrangler deploy` is not run; DIST deploys.
concurrency: checked over the whole file. No live (un-`released:`) claim names `agent-worker/**`, `bio-plane/wrangler.jsonc`, `bio-plane/scripts/coverage.mjs` or `bio-plane/scripts/battery.mjs`. W6 slot B is UI-38 (`civicos-ui/**`) and the DIST lane is `newgroup/**`/`release/**` — both disjoint from every path above. `bio-plane/src/**` and `checks/bio-checks.mjs` are NOT claimed and NOT edited.
released:

### DELEGATION 2026-08-08 FLEET (FL-2/VF-3) -> CONTENT-PDF: **`pdf-worker`'s SUITE HAS NEVER BEEN RUN BY ANYTHING, AND IT IS MEASURED RATHER THAN SUSPECTED**

`scripts/battery.mjs` discovers suites from `bio-plane/test/`, and the fleet lives BESIDE the plane. So `pdf-worker/test/pdf-worker.test.mjs` has sat in the repository since 2026-07-31 with a `npm test` script nothing invokes. **Meanwhile `scripts/coverage.mjs` credited `pdf-worker`'s `structure` surface as REACHED, because reach there is read out of the suite's SOURCE.** The first fleet member's coverage figure has been standing on a suite nobody executed — `bundle.test.mjs`'s defect (D-93 #2) one directory out, and D-117's own failure mode: a component dark while the figure holds still.

- **VF-3 closed the half it could reach without entering your paths.** The battery now DISCOVERS fleet members by `fleet-member.json` and RUNS their suites, and it prints a `fleet:` line every run naming any member that did not run. As of this item that line reads **`DARK: pdf-worker`** — visible on every battery instead of invisible.
- **What is owed, and it is two lines of your file.** `pdf-worker/test/pdf-worker.test.mjs` imports `miniflare` bare and `pdf-worker/` has no `node_modules` in a fresh checkout, so the battery reports it as a NAMED SKIP rather than failing (D-93's `ssh-keygen` treatment — failing would red the battery everywhere over a dependency the plane does not own). `agent-worker/test/agent-worker.test.mjs` shows the fix: try the local install, and fall back to resolving `miniflare` through `createRequire(bio-plane/package.json)`. Adopt that and your suite runs wherever the battery runs.
- **A SECOND, SMALLER TRAP IN THE SAME FILE.** Your source scan strips line comments as "two slashes to end of line". That is correct today and silently wrong the moment `pdf-worker` holds a URL literal: the same idiom here DELETED `"http://plane"` AND THE REST OF ITS LINE, and the only-one-absolute-URL arm came back green over a source truncated from 17,265 characters to 6,029. Requiring a non-colon before the two slashes fixes it.
- **AND A GAP IN I6 ITSELF: `pdf-worker` CANNOT SAY WHICH BUILD ANSWERED.** Fleet rule 4 — *"a verification must establish which build ANSWERED, for the member as well as the plane"* — is unverifiable for it, because nothing on its wire names its build. I8 gives `agent-worker` a `GET /version` for exactly this. **Recommendation:** add the same three-line endpoint and put `version` in its SURFACE table. **Reversal cost:** none; it is additive and nothing consumes its absence. **Cost of NOT doing it:** DS-4's rollout gate discovers it at deploy time, which is the worst moment, and D-108 already cost this project an afternoon once.

### DELEGATION 2026-08-08 FLEET (FL-2) -> RECORD / WHOEVER OWNS `op=whoami`: **AN `ai` CREDENTIAL CANNOT LEARN ITS OWN PRINCIPAL, AND FL-6 NEEDS IT TO**

**MEASURED against `bio-plane/src/index.mjs` at FL-2, not inferred.** D-199 (4) makes the PRINCIPAL the credential's operative identity — the viewer its reads compile under, `member:<id>` for a member-scoped key and `class:ai` for an organisation-scoped one — and `index.mjs` stamps it into the inner request for every viewer read. But **no read op an agent may call publishes it back.** `op=whoami` answers `tokenClass: "ai"`, `session: false`, `member: null`, and names no principal at all.

- **Why it matters now rather than later.** FL-6's row requires the run to record *"WHICH level paid beside the plane-credential principal (two principals, both named, PL-4's rule)"*. One of those two principals is currently unobtainable by the party that has to record it.
- **What FL-2 did instead, and it is deliberately not a workaround.** `agent-worker`'s `/run` answers `principal: null` with `principal_source: "UNPUBLISHED — ..."` on the wire. Undetermined is first-class and must be STATED: a run that quietly reported no principal could not be told from one acting for nobody. It does NOT infer the principal from the class, which would be the member inventing an attribution to get past a gate.
- **Recommendation:** `op=whoami` publishes `principal` when `cls === "ai"`, taken from `aiCred.principal` — the same value the viewer stamp already uses, so there is no second answer to age separately (REC-46's whole lesson). It is a read of a value the function already holds.
- **Reversal cost:** low now. Rising once FL-3/FL-6 have run objects in the record whose principal field was filled by something other than the plane's own statement.

### DELEGATION 2026-08-08 FLEET (FL-2) -> DIST (DS-1, DS-4): **WHAT YOU ARE NOW INSTALLING AND DEPLOYING, AND THE ONE VALUE THIS FILE CANNOT CARRY**

- **THE FLEET IS THREE WORKERS AS OF THIS ITEM**: the plane, `pdf-worker`, `agent-worker`. D-115's rule — an instance that gets the plane and not the members is an instance quietly doing less — now has a third term. `newgroup` is untouched by this item and is named in `coverage.mjs`'s `NOT_A_FLEET_MEMBER` set as the installer, with its reason.
- **NOTHING WAS DEPLOYED AND `wrangler deploy` WAS NOT RUN.** `agent-worker/wrangler.jsonc` pins `account_id` to `20b533579290b9b93168345edd3b7f72`, so the repository decides the account rather than the machine.
- **THE PLANE'S NEW BINDING IS INERT.** `bio-plane/wrangler.jsonc` gains `{ "binding": "AGENT_WORKER", "service": "agent-worker" }` and **nothing in `src/` reads it** — FL-3 is the first caller. An instance that has not installed the member is unaffected, exactly as `PDF_WORKER` shipped.
- **THE VALUE THIS FILE CANNOT CARRY.** `agent-worker`'s own `PLANE` binding names `bio-plane`, and for an INSTALLED instance the plane's worker name is per instance (the instance name IS the worker name, D-102). So the installer must TEMPLATE that `service` value from its `slug`, the same half that is already delegated for the plane's `SELF` binding. A sovereign instance whose agent worker points at `bio-plane` would be pointing at OUR plane.
- **DS-4's rollout gate has a third subject now.** Rollout is per-isolate and not atomic; `GET /version` on `agent-worker` is how you establish which build ANSWERED for this member. `pdf-worker` has no equivalent — see the CONTENT-PDF delegation above.

### DELEGATION 2026-08-08 FLEET (FL-2) -> FL-3 (the run harness): **WHAT THE MEMBER GIVES YOU, AND THE LAYER IT DELIBERATELY LEFT EMPTY**

- **`POST /run` IS THE ENVELOPE AND THE LOOP IS YOURS.** Today it validates, forwards the credential, does ONE non-mutating round trip, and answers `stage: "round-trip"`, `turns_run: 0`. **Do not delete those two fields — replace their values.** An answer that did not say what it had actually done would be indistinguishable from a run that executed and found nothing, and §9's empty-level kind is a different claim from an empty run.
- **THE SEGMENT BOUND IS 120 AND IT IS SIZED ON FL-1's MEMORY CURVE.** At 200 turns the isolate reports 120.4 MB P99 against 128 MB while CPU sits at 2.5% of its ceiling; the CPU curve extrapolates to ~1,100 turns and would be ~10x too long. The suite asserts the default is inside FL-1's measured 100-150 band, so moving it out of that band FAILS rather than being discovered in production. If you move it, move it on a MEASUREMENT and move the assertion with it.
- **THE OPS THIS MEMBER MAY NAME ARE PINNED AS AN EXACT SET (`{whoami}`), floor and ceiling both.** Every call you add fails that assertion until somebody updates it — which is the point: a call this member gains is a call somebody decided to give it. **Do not add an op allow-list to the member.** D-199 (2): the scope is a row a member authored, read at the plane's gate; a scope compiled into a Worker is the settings row the determination refused.
- **THE MEMBER HOLDS NO CREDENTIAL AND MUST NOT START.** One credential per call, the one it was handed; the suite asserts exactly one distinct token reaches the plane and that no string literal in the source opens with `aik-`.
- **A PLANE REFUSAL IS PASSED THROUGH VERBATIM** — code, C-number and canned translation. F10's denied-means-adjust belongs in YOUR table: adjust the submission, never re-word the refusal.
- **NO DEC-49 FAMILY IS OWED TODAY and the day that changes is nameable.** The guard walks `bio-plane/src` and `bio-plane/checks` only, and no member ever receives this Worker's codes. **The moment any surface renders one of them verbatim to a member** — which composing a run's failure could easily do — a family is owed AND `check-refusal-codes.mjs` must be taught to walk the member. The member already passes codes as STRING LITERALS through a helper named `refusal`, so arm C would have teeth the day it is pointed there.

### DECISION FOR BOB 2026-08-08 FLEET (VF-3): **A FLEET MEMBER WHOSE SUITE CANNOT RUN IS SKIPPED, NOT FAILED — AND THAT IS THE GENEROUS DIRECTION**

**What runs provisionally.** `scripts/battery.mjs` now runs the fleet's suites. A member whose suite cannot resolve a package in its own directory is reported as a NAMED SKIP and does not fail the battery; the run also prints a `fleet:` line naming every member that went DARK. Today that line reads `DARK: pdf-worker`.

**Why it was ambiguous.** This project's standing rule is that wrong-in-the-generous-direction is the worse failure, and a skip is generous: the battery can be green while a whole member is untested. The competing rule is D-93's, which is why `ratify.test.mjs` skips loudly rather than dying when `ssh-keygen` is absent — a gate that fails on every machine over an absent dependency gets switched off, and a switched-off gate protects nothing.

**The alternative.** Fail the battery when a fleet member's suite cannot run. That would red `main` on every fresh checkout until someone runs `npm ci` in each member directory, including CONDUCT's integration checkout, over a dependency the plane does not own.

**Recommendation:** keep the skip, because it is strictly better than the status quo it replaces (the suite was run by NOTHING and nothing said so) and the darkness is now printed on every run rather than being invisible. The right permanent fix is the CONTENT-PDF delegation above — a member that resolves its test dependencies through the plane's install always runs, and then the skip path is dead code that never fires.

**Reversal cost:** one line in `scripts/battery.mjs` (drop `fleetDepSkip`), and it becomes correct to reverse the moment `pdf-worker` adopts the resolution `agent-worker` uses. Until then reversing it reds the battery for everybody.

## CLAIM 2026-08-08 RECORD
session: m013-d231-suggest-flake
opened: 2026-08-08
paths: bio-plane/src/store.mjs (**ONE EXPRESSION ONLY** — the local `substanceOf` inside `suggestVersion`, ~line 17640, plus its comment), bio-plane/test/suggest.test.mjs (header + block 3's new boundary arm), bio-plane/test/suggest.control.mjs (new arms), docs/development/DEBT.md (D-231 disposition + one new row), docs/development/MEASUREMENTS.md (one dated entry)
interfaces consumed: I3, I4 — read-only and DRIVEN, neither changed
interfaces owned: none
expected: M0-13 / D-231 — `suggest.test.mjs` intermittently red under the battery. **THE CAUSE IS A WALL CLOCK INSIDE A COMPARISON THAT MUST BE TIME-FREE**, not F10's resubmit key (the brief's suspect, which the sweep CLEARED — `submission` is built 21 lines before `nowIso` exists and carries only caller-derived fields). `substanceOf` strips `name` and `derived_from` from the canonical composition but not the `at` field riding on every `ground\t` row, which `#suggestionFrontmatter` stamps with the server's clock at second resolution. So CHECK 3's duplicate gate fires ONLY when candidate and held version were stamped in the same one-second bucket. The suite runs 510ms standalone and usually stays inside one second; under load it does not. **THIS IS A LIVE PLANE DEFECT AND THE FLAKE IS ITS SYMPTOM:** §6 rule 8's write gate does not refuse a duplicate submitted more than a second after the original, so a retry loop pausing between attempts writes unbounded identical readings. Fixed in the comparison only — the composition itself, which the freeze compares, is NOT touched.
concurrency: checked over the whole file. No live claim names `bio-plane/test/suggest.test.mjs`, `suggest.control.mjs`, or `suggestVersion`. The PL-3 claim that built them is RELEASED and integrated on main. **`src/store.mjs` IS opened, and that is stated loudly rather than buried:** this is a test-estate lane item and the honest fix for its cause is in the plane. The edit is one expression inside one method, changes no interface, adds no op, and touches neither the canonical composition nor the freeze. The adjacent defect the sweep found in the same three lines (`#fmSafe` normalisation asymmetry) is DELEGATED as a debt row rather than fixed here, because it is not this item's cause and fixing it changes what the endpoint PUBLISHES.
released:

### DELEGATION 2026-08-08 M0-13 -> WHOEVER NEXT OWNS `suggestVersion` / THE PL-3 FAMILY (D-234 (raised as D-232; renumbered at integration — CONDUCT had taken D-232 for `pdf-worker`'s dark suite in the same hour and neither could see the other)): **THE SAME WRITE GATE IS ALSO DEFEATED BY A QUOTATION MARK, DETERMINISTICALLY, AND THAT IS WORSE THAN THE FLAKE I WAS SENT FOR**

Measured by driving the plane, not by reading it, and every scenario ran INSIDE one second so D-231's clock cannot be what failed them. `suggestVersion` builds its candidate composition from RAW args while the bytes it compares against went through `#fmSafe` — which rewrites `"` and `\` to `'`, folds newlines to spaces and trims. A duplicate whose description carries a quote **LANDED**; one whose ground statement carries a backslash **LANDED**; one whose description had trailing spaces **LANDED**. There is a narrower second half: `gRows` emits `statement:` only when non-empty while `#suggestionFrontmatter` passes it unconditionally, so an empty-string statement composes two ways too.

- **Provisional:** as shipped. D-231's clock is fixed; this one is not, so §6 rule 8 still does not reach any submission containing a quote, a backslash, a newline or stray whitespace — and it fails SILENTLY and ALWAYS rather than intermittently.
- **Why I did not fix it, and it is a scope judgement rather than a shrug:** M0-13 is a test-estate lane item and had already opened `src/store.mjs` once, for its own item's named cause. The honest fix here composes the candidate from the values that will actually be PERSISTED, which also changes the `composition` the endpoint publishes on its success path. That is a change to what a surface receives, and it does not belong on a flake diagnosis's battery.
- **The rider worth checking in the same turn:** today the published `composition` on the success path can DIFFER from what the record then holds, for exactly the same reason. An answer that publishes bytes the record does not hold is its own defect and nobody has looked at it.
- **Recommendation:** fix it with an arm per lossy transform (`"`, `\`, newline, trim, empty statement), and rule explicitly on whether the published composition must equal the stored one.
- **Reversal cost:** low. The gate only gets stricter, in the direction it was always meant to be strict.

### DELEGATION 2026-08-08 M0-13 -> WHOEVER OWNS `scripts/control-register.mjs` (REC-73's FAMILY) AND `VERIFICATION.md`'s REGISTER ROW (D-233): **THE ARMS TALLY SCORES FOUR SUITES AT ZERO WHILE THEY DECLARE DOZENS, AND `suggest.test.mjs` DECLARES TWELVE**

Measured with the instrument itself — `readControl` over all 119 suites. `countArms` is `/\s(?:->|→)\s/g`, the arrow grammar `VERIFICATION.md` documents. Several suites instead write their arms as `(1) … (2) …` prose, and those are invisible: **`suggest.test.mjs` declares twelve arms, `suggest.control.mjs` RUNS all twelve, and the register scores it 0.** Adding two more arms in this item moved the published figure by exactly nothing (388 before, 388 after), which is how I found it.

- **Provisional:** left alone. Nothing is red — the arms count is REPORTED and never gated, and the gate (does a suite declare a control at all) is unaffected.
- **Why I left it:** converting only my own suite would move the figure by 12 and make it neither an undercount nor a count; teaching the counter a second grammar moves `VERIFICATION.md`'s row, which belongs to whoever owns that row. This is M0-9's class read one notch further out — that section records a detector wrong in the GENEROUS direction, found only by measuring. This one is wrong in the STINGY direction, which is safer and still means the number proves less than it appears to.
- **Recommendation:** report the figure as a FLOOR with the uncountable suites NAMED, or teach the counter the numbered style — either way move `VERIFICATION.md`'s row in the same turn from what the instrument PRINTS, which is that row's own standing instruction.
- **Reversal cost:** none. It is a reported figure, not a gate.

### DECISION FOR BOB 2026-08-08 M0-13 -> CONDUCT: **A TEST-ESTATE LANE ITEM CHANGED PLANE BEHAVIOUR, ON PURPOSE, AND THAT IS THE ONE CALL HERE WORTH CHALLENGING**

- **What is running provisionally:** M0-13 was scoped as a test-estate lane item that *"changes no plane behaviour"*, and it changed some. One expression in `src/store.mjs`'s `suggestVersion` now excludes the assertion stamp from the duplicate-check comparison, so §6 rule 8's write gate refuses a duplicate submitted more than a second after the original — which it did not before.
- **Why it was genuinely ambiguous:** the row forbids two things that turned out to point in opposite directions once the cause was known. *Do not silence the arm* and *do not close by re-running* mean the arm has to become deterministic; but the arm was CORRECT and the plane was wrong, so the only way to make it deterministic without weakening it was to fix the plane. A test-lane item that quietly widens into `src/**` is exactly the escape REC-71 exists to correct, so it is stated loudly here rather than left in a diff.
- **The alternative, stated fairly:** diagnose only, leave `substanceOf` alone, and delegate the fix — leaving the suite intermittently red in the meantime, or leaving the arm relaxed to whatever passes. That keeps the lane boundary clean and is defensible on scope grounds alone.
- **Recommendation: keep it.** The defect is not cosmetic — §6 rule 8 is Bob's own write gate and it was unenforced against every retry loop that paused a second, which is every real one. Leaving a member's inquiry accumulating identical readings for one more integration cycle to protect a lane boundary is the wrong trade, and the change is one expression inside one method with no interface, no op and no schema movement.
- **What reversing it costs:** near nothing today, and it does NOT get more expensive as data accumulates — reverting restores the old comparison and the only consequence is that duplicates start landing again. Nothing is written differently, no stored bytes change shape, and the canonical composition the freeze compares was deliberately left untouched precisely so this stayed cheap to undo.
- **What I did NOT take, and it is the boundary I held:** the same gate is also defeated by a quotation mark (D-234 (raised as D-232; renumbered at integration — CONDUCT had taken D-232 for `pdf-worker`'s dark suite in the same hour and neither could see the other)), deterministically. That fix changes what the endpoint PUBLISHES, so it is delegated rather than folded in — one lane crossing for a named cause is a judgement, two is a habit.

session: is-wave-w5-pl15 (worktree agent-aa5e6711b4eb2c064)
opened: 2026-08-08T09:30:00Z
paths: **`bio-plane/src/queuestate.mjs`** — ONE new key in the existing `QUEUE_FINDING_KINDS` vocabulary (`out-of-inquiry-lead`). `classOfKind`, the three vocabularies' other entries, `MUTE_REFUSAL_DETAIL`, `serializeMutedKinds`/`parseMutedKinds` and `suppressedBy` are UNTOUCHED. **`bio-plane/src/store.mjs`**, named by SITE: (a) ONE new private `#findingsOutOfInquiryLead` (the producer) and ONE new private `#leadBasisAbsence` (the MEASURED absence of a basis entry); (b) the mint fence inside `queueFeed`, narrowed into a **`DEC-49 REGION is-queue-mint`** span and EXTENDED from CONDITION-only to every class — the class sweep, which is the defect class the existing fence left standing; (c) ONE line in `captureRequest`'s door writing the new column, inside the EXISTING `is-capture-request` region; (d) ONE extra predicate on `purge`'s PER-BUNDLE `capture_requests` DELETE. **NOT** `#queueConditions`, `#conditionsGovernorHolding`, `#conditionsPartialCapture`, `#conditionsCaptureUnattended`, `#conditionsCaptureRequested`, `#queueAncestors`, `#queueOptions`, `#queueMutes`, `queueMute` or `proposalsFeed` — all READ as precedent and called, never edited. **`bio-plane/src/schema.mjs`** — ONE nullable column `lead_inquiry` on PL-4's `capture_requests` (no new table, so no new `TABLES` row and no new purge arm; the per-bundle arm gains a predicate because the column names a SECOND bundle the existing `target=?` arm cannot see). **`bio-plane/checks/bio-checks.mjs`** — ONE new exported `QUEUE_MINT_CHECKS` family. **C-31 MEASURED as free over this file before allocating** (`grep -Eoh "(^|[^A-Z])C-3[0-9]"` over `bio-plane docs civicos-ui` yields C-30 and nothing above it; C-25 is PL-1/PL-2's, C-26 PL-12's, C-27 PL-3's, C-28 PL-4's, C-29 PL-11's, C-30 PL-14's). **If a parallel slot measured C-31 free at the same moment, CONDUCT renumbers the later merge and that is not this session's error.** **`bio-plane/test/leadslug.test.mjs`** (new, named so `node scripts/battery.mjs leadslug` selects it) and **`bio-plane/test/leadslug.control.mjs`** (new, the negative-control driver, deliberately not a `.test.mjs`). **`civicos-ui/check-refusal-codes.mjs`** — the `FLOOR`/`CEILING` figures only, MEASURED from what the guard PRINTED on a green run and moved IN THE SAME TURN. `docs/development/NOTIFICATIONS.md` — ONE catalogue line for the new FINDING kind (the vocabulary in `queuestate.mjs` is transcribed from that catalogue and from nowhere else, so the two land together or the code half has no authority). `docs/development/DEBT.md` — the **D-213 row only**, gaining its dated close. `docs/development/CLAIMS.md` (this entry and its DELEGATIONS).
interfaces consumed: I3 (op=queue's item contract — ADDITIVE ONLY: one new `kind` value inside the existing FINDING class, no field added, removed or reshaped, so no IC is owed), I5 (one nullable column on a table this session does not own the design of; PL-4's shape is otherwise untouched).
interfaces owned: none. **No new op.** The lead is surfaced through the EXISTING `op=queue` feed, which is the whole point of D-213's answer ("this is a new kind in an existing catalogue"); a second addressable surface for it would be the drift the catalogue exists to prevent.
expected: IS-BUILD-PLAN PL-15 — **THE OUT-OF-INQUIRY LEAD (D-213).** The FINDING-class slug WITH A PRODUCER: evidence for inquiry B, found while working inquiry A, is CAPTURED (a store entry, and deliberately **NO basis entry** — and the absence is MEASURED and STATED as *not made part of the case* rather than left for a reader to infer, per `CLAUDE.md`'s never-assume-the-lower-levels-are-complete rule) and surfaced as an actionable notification with a real `basis` and a real `options[]`. Its `case` set derives from **inquiry B's ancestors, not A's**. FINDING and not CONDITION, deliberately: a CONDITION is personally mutable and one member could silently mute a real lead.
accepts-when: as IS-BUILD-PLAN PL-15, plus: **battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief** (measured 118/118 at 7,235 — the brief's figure, confirmed rather than assumed; the worktree needed `npm install` first and a piped `tail` reported the failing pre-install run as exit 0, which is `CLAUDE.md`'s own named trap). Full `npm run test:battery` green with the delta ATTRIBUTED per suite and MEASURED by re-running the true baseline with the new files moved aside, never by subtraction; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped; `node civicos-ui/test/run.mjs` exit read unpiped. Negative controls RUN, **each armed ALONE with the others held open**, declared before arming, restores verified by sha256 AND by `cmp`, harness INSIDE this worktree and never in the shared scratchpad; the plan row's named control (mint under an `N-<n>` id or an uncatalogued slug -> refused at the mint) first; an OVER-STRICTNESS arm included. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and IS-BUILD-PLAN.md are not written by this session.
concurrency: every claim block above this one is `released:` except PL-14's, whose `released:` line is empty but whose work is MERGED at `fee6e9a` (`conduct: PL-14 integrated`) — the same-file handoff IS-BUILD-PLAN's wave rules define, slot B taking `store.mjs` after slot A. Checked over the whole file: no live claim names `queuestate.mjs`, `schema.mjs`, `NOTIFICATIONS.md`, or any suite named above. `index.mjs` is NOT claimed and is NOT edited — this item adds no op.
released:

### DELEGATION 2026-08-08 RECORD (PL-15) -> PL-4's FAMILY / WHOEVER OWNS `capturerequests.test.mjs`: **THE `collected` CLAUSE IS ASSERTED OVER AN EMPTY SET, AND THE PLAN ROW INHERITED IT**

**MEASURED, not read off the source** (`op=stats` after a successful `op=acquire` in a fresh store: `bundles 0, register 0`). `op=acquire` writes bytes to R2 and a `captured_locators` row and creates **no bundle and no register entry**. A bundle at `collected` is what a later `promote` makes, registering the digest under a document.

- **The consequence for PL-4's block 3b.** Its arm *"A CAPTURE IS NOT EVIDENCE: whatever the daemon filed is at `collected` and NEVER higher"* filters `op=list` for bundles whose id starts with `INFO` and asserts none is at another state. After a drain there are **no such bundles at all**, so the filter is empty and the arm passes over zero rows — the shape this project names as *a walk over an empty corpus reporting its verdict triumphantly*. It is not wrong about the doctrine; it is not testing it.
- **PL-15 inherited the same clause** from IS-BUILD-PLAN's PL-15 row (*"the capture landed at `collected`"*) and did NOT restate it. `test/leadslug.test.mjs` block 3 drives BOTH facts instead: `undetermined` straight off the drain (the bytes are held, no document carries them — reporting `absent` there would be an overclaim) and `collected` once a promote registers the digest. The producer's `basis_entry` is what carries the answer, so the claim is member-facing rather than test-only.
- **What is owed:** PL-4's arm made non-vacuous — either by promoting a bundle over the drained digest inside that suite, or by asserting the fixture's own precondition (`landed.length > 0`) before the verdict, which is the cheaper half and would have failed loudly today.
- **Reversal cost:** none. Nothing depends on the arm's current form.

### DELEGATION 2026-08-08 RECORD (PL-15) -> PL-14's FAMILY / WHOEVER OWNS `op=versionstrength`: **IT IS AN OPAQUE READ, AND A FALSE POSITIVE IN `meaning-bounds` WAS HIDING THAT**

`test/meaning-bounds.test.mjs`'s `REFUSAL_CALL` read `/^refuse\s*\(/`, and the plane's landed convention since PL-3 is `refusal(` — PL-4, PL-11, PL-14 and PL-15 all use it, because `check-refusal-codes`' arm C keys on that spelling. So a bare `return refusal("CODE", ...)` whose detail trims a value with `.slice(` was counted as a **BARE ARRAY**: PL-1's measured false positive, wearing the other name.

- **PL-15 widened it to both spellings** (`/^refus(?:e|al)\s*\(/`), driven in both directions with a new arm over the second name, because the first draft of the widening (`/^refusals?\s*\(/`) matched `refusal`/`refusals` and **not** `refuse` — it swapped which spelling was seen instead of covering both, and the both-directions arm caught it. Recorded because that arm is the reason the correction is a correction.
- **What the correction revealed:** `op=versionstrength` comes off the BARE roster and lands in the **OPAQUE** one — dispatched, scanning rows, outside every bucket. The opaque ceiling moves 8 -> 9 and the named residual gains it, both with a dated reason. **The 8 was never a smaller blind spot; it was a smaller measurement**, which is REC-70's own finding one layer down.
- **It is the ONE member of that residual that is not a write path.** The other eight scan rows for their own logic and answer a scalar or a status. `versionStrength` is a READ, and whether it should publish a bound belongs to PL-14's family rather than to this item.
- **PL-15's own new work adds nothing to either roster:** `op=queue` came OFF the bare roster by the same correction and is on neither residual.
- **Reversal cost:** low. Narrowing `REFUSAL_CALL` back would restore the false positive and the ceiling with it.

### DELEGATION 2026-08-08 RECORD (PL-15) -> UI-45 (notifications rendered) AND WHOEVER BUILDS THE QUEUE SURFACE: **WHAT THE LEAD HANDS YOU, AND THE ONE THING A SURFACE MUST NOT DO WITH IT**

- **TWO PRODUCERS OVER ONE TABLE, TWO DIFFERENT HOMES, AND NEITHER IS A DUPLICATE OF THE OTHER.** `capture_requests` now feeds `#conditionsCaptureRequested` (PL-4, a CONDITION filed under the run's own question — *your capture finished*) and `#findingsOutOfInquiryLead` (a FINDING filed under a DIFFERENT question — *evidence for that other question turned up*). They can both fire for the same row. **A surface that assumes one producer per kind, or that de-duplicates on the subject, reads the second as noise** — and `capture-completed-unattended` already had two producers before this item, so this is the third instance of a shape that keeps surprising readers.
- **`options_grain` IS PART OF THE CONTRACT, not decoration.** `options[]` holds document-grain acts on the lead's inquiry; the acts a member actually wants (*take this up under that question*, *set it aside*) are INQUIRY grain and do not exist (D-222). The item says so on its face. **Render that, do not paper over it** — an empty-looking action row with no explanation is how a member concludes the lead is broken.
- **`basis_entry` IS THREE-VALUED AND ALL THREE MEAN DIFFERENT THINGS.** `undetermined` (no document in this store holds these bytes yet), `absent` (looked for, part of no case), `present` (a reading carries it). **Collapsing `undetermined` into `absent` on a surface is the overclaim the plane refused to make** — it would tell a member the record has checked when it has not.
- **DO NOT OFFER A MUTE ON IT.** It is a FINDING by decision, and `op=queuemute` refuses the kind by name. A surface that shows a mute control it cannot honour is worse than one that shows none.

### DELEGATION 2026-08-08 RECORD (PL-15) -> FL-3 (the IS-9 harness): **WHAT `leadslug.test.mjs` DOES NOT PROVE, STATED SO YOU DO NOT INHERIT IT AS PROVEN**

The suite is fixture-driven and says so in its own header. It drives the real `op=capturerequest` door, the real drain, the real `op=queue`, and the real purge — every plane path a live session takes. **What it stands in for is the session's JUDGEMENT**: the suite names the lead, where a run would have to decide that a document it captured for one question bears on another.

- **That judgement is the whole value of D-213's answer and no plane-side arm can reach it.** A run that never names a lead produces a perfectly green plane and closes the hole for nobody; a run that names one on every capture turns the FINDING class into noise, which is worse than the silence it replaced.
- **What FL-3 owes:** a measurement of the lead RATE and of its ACCEPT ratio, in the same shape §15's other instruments take. The plane already persists everything needed — `capture_requests.lead_inquiry` beside `run`, and `proposal_dispositions` for what members did with the findings.
- **The door is deliberately narrow and will not save you:** it refuses a lead that is not a question and a lead equal to the target, and nothing else. It cannot judge relevance and does not try.

### DELEGATION 2026-08-08 RECORD (PL-15) -> WHOEVER OWNS `hygiene.test.mjs` / D-113: **THE PURGE CHECK IS BLIND TO A COLUMN, AND THIS IS THE SECOND ITEM TO SAY SO**

PL-4 already delegated that the D-113 check derives its covered set from `DELETE FROM <table>` anywhere in the purge method, so one arm satisfies it. **PL-15 adds the other half: the check compares TABLE lists, so a bundle id living in a COLUMN of an already-covered table is invisible to it entirely.** `capture_requests.lead_inquiry` is a second bundle id on a row the per-bundle arm found only by `target`, and deleting the clear-clause leaves hygiene GREEN at 482/482.

- **PL-15 proved its own coverage by CONSEQUENCE** (`leadslug.test.mjs` block 8) rather than structurally, and the control arm runs `hygiene.test.mjs` alongside to demonstrate the blindness rather than assert it.
- **A finding the control produced and the reader would not have:** the member-facing half of that consequence arm is ALSO covered by accident — `#bundleGate` compiles an `EXISTS` over `bundles` for an identified member, so a lead pointing at a purged question drops out of a member's feed whether or not purge cleared the column. **Only the OPERATOR view, whose gate is `1=1` by design, can see a stale pointer.** An arm written against a member's feed would have passed over a missing purge clause. That is why the suite now asserts the machine-credential feed and the row itself.
- **What is owed:** the D-113 check taught to ask about COLUMNS that hold a bundle id, not only about tables. `schema.mjs` names them — `suggest_refusals.target`, `capture_requests.target`, `capture_requests.lead_inquiry` — so the list is derivable rather than hand-kept, which is the whole reason D-113 exists.

### DELEGATION 2026-08-08 RECORD (PL-15) -> CONDUCT: **`op=capturerequests` GAINED A FIELD (I3, ADDITIVE), AND TWO ROWS WENT INTO PL-4's C-28 FAMILY**

- **`op=capturerequests` now publishes `lead_inquiry`.** Additive: every existing reader keeps the shape it reads, and no field moved. It is not decoration — that projection is explicit rather than a row spread, so an unpublished column is one no caller can see, and the operator view is the only place a stale lead is visible at all. **FOUND BY DRIVING IT:** the purge arm filtered on the field before it was published and counted zero for every input, which is an arm asserting nothing. No IC is filed — this session judged an added optional field on an existing answer to be inside I3's stable contract rather than a change to it, and flags it here so CONDUCT can disagree cheaply.
- **C-28.14 and C-28.15 were added to `CAPTURE_REQUEST_CHECKS`, which is PL-4's family**, because both refusals are enforced inside PL-4's `is-capture-request` REGION and a row elsewhere would leave two codes in a span whose rows do not name them. SK-1's rule decided it: a family is a DEC-49 FLOOR, and minting one for two rows buys slack for everybody else's walk. **`capturerequests.test.mjs` holds that family to a floor — every code DRIVEN by that suite — so PL-15 drove the two new codes THERE as well**, rather than narrowing the arm. A floor relaxed to fit new rows is not a floor.
- **`queue-conditions.test.mjs`'s `NO_CONDITION_KIND` pin was CORRECTED, never exempted**, with the reason at the assertion: the code no longer exists because the fence was swept to every class, and the replacement DRIVES the catalogue rather than matching a string in source.
- **C-31 was MEASURED free** before allocation (`C-30` is the highest family in the catalogue). If a parallel slot measured the same, the later merge is CONDUCT's to renumber — and if so, note that a regex literal writes `C-31\.` and not `C-31.`.

### DECISION FOR BOB 2026-08-08 (PL-15) — **THE LEAD IS A COLUMN ON `capture_requests`, NOT A TABLE OF ITS OWN**

- **What runs provisionally:** an observation *this captured document bears on question B* is stored as one nullable `lead_inquiry` column on the `capture_requests` row that asked for the capture. One capture request therefore carries at most ONE lead, and a lead can only be attached at the moment a capture is requested.
- **Why it was ambiguous:** D-213's answer says the mechanism *"needs no new machinery"* and that the document is CAPTURED, which points squarely at the capture request. But a lead is an OBSERVATION, and observations are not obviously one-per-fetch: a run could meet a document bearing on two other questions, or notice belatedly that something already in the store bears on one. Neither is expressible today.
- **The alternative:** an `inquiry_leads` table keyed (observation, document, inquiry), which would carry many leads per document, allow a lead over a document nobody just fetched, and carry its own authorship. It is a derived table, so it would owe both purge arms and a `TABLES` row.
- **Recommendation: keep the column.** It is the smallest thing that closes D-213, it inherits PL-4's attribution and conduct whole, and the shapes it cannot express are ones nothing can produce yet — the investigative session's only door to a document is the capture request. Building the table now would be structure bought before the producer that needs it exists, which is the mistake `PARALLELISM.md` warns about in its own last section.
- **What reversing it costs: LOW NOW, RISING.** Today it is one migration adding the table, one producer rewritten to read it instead, and the column left inert or dropped — nothing member-facing changes, because the notification's shape is derived and its `id` is keyed on the request either way. It gets expensive once leads have been dispositioned: `proposal_dispositions` keys on the item's identity, so moving the identity would strand every recorded adopt/defer/dismiss. **The decision point is therefore before the first surface ships (UI-45), not before the first lead is captured.**

## CLAIM 2026-08-08 FLEET
session: is-wave-w7-fl3 (worktree agent-ad6e5ed43aac4a2ab)
opened: 2026-08-08T10:20:00Z
paths: **`agent-worker/**`** — `src/harness.mjs` (NEW, the deterministic control-flow table), `src/index.mjs` (FL-3 fills `/run`, which I8 scheduled on this item), `test/harness.test.mjs` (NEW, the table driven PURE and through the op), `test/harness.control.mjs` (NEW, the negative-control driver — deliberately NOT a `.test.mjs`, it EDITS REAL SOURCES while it runs and neither the battery nor the fleet walk must discover it), `test/agent-worker.test.mjs` (FL-2's suite — four arms CORRECTED, never exempted, each with a comment saying why the old one was wrong; see the decision below), `fleet-member.json` (SURFACE unchanged). `docs/development/INTERFACES.md` (I8's scheduled RE-READ — the entry says "when FL-3 lands, whoever lands it re-reads this entry against the code"), `docs/development/INTERFACE-CHANGES.md` (an IC row for the shape change), `docs/development/CLAIMS.md` (this entry and its DELEGATIONS), `docs/development/DECISIONS.md` (DEC-65's disposition ONLY — appended, no other row touched). **NOT** `bio-plane/src/**`, **NOT** `bio-plane/checks/bio-checks.mjs`, **NOT** `civicos-ui/**`, **NOT** `pdf-worker/**`, **NOT** `newgroup/**`.
interfaces consumed: I8 (owned here), I3 (the plane's op surface, through the service binding, under PL-11's `ai` credential — PL-3's `op=suggest`, PL-5's `op=airuntick`/`airunclose`/`airun`/`airunlog`, PL-9's `op=meaningrows`, PL-12's `op=airunspawn`, PL-4's `op=capturerequest`).
interfaces owned: **I8.** FL-3 performs the RE-READ the entry scheduled and files the IC row; **CONDUCT takes the version bump.**
expected: IS-BUILD-PLAN FL-3, W7 slot A. The run harness (IS-9) as a DETERMINISTIC CONTROL FLOW TABLE — code, never skill: pass count and loop termination, four-level fan-out, dedup-before-write, log-always, denied-means-adjust as a table row (F10), query-never-load, versions written as formed, budget over fetches/sub-sessions/wall time recorded through PL-5's producer.
accepts-when: as IS-BUILD-PLAN FL-3. Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief: measured **121 suites (119 plane + 2 fleet) · 120 green + 1 named skip (`pdf-worker`, D-232 — not this item's) · 7,375 assertions**, which AGREES with the brief's figure, and the agreement is measured rather than assumed. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED (baseline: OPS 158/158 · CHECKS 155/155 · controls 119/119, 388 arms · FLEET 2 members, 3/3 surface ops, floor 2 members / 3 ops, exit 0); `node civicos-ui/test/run.mjs` exit read unpiped (baseline exit 0); `node tools/plancheck.mjs` clean but for UNPUSHED. Negative controls RUN, each arm ALONE with the other defences held OPEN, an OVER-STRICTNESS arm included, every restore verified by sha256 AND by `cmp`, the harness written INSIDE THIS WORKTREE. No push, no merge, no deploy, no version bump.
concurrency: checked over the whole file. The only live claim naming `agent-worker/**` is FL-2's (`agent-ad2c65dacc2cd14ed`), whose `released:` line is empty but whose work is MERGED on main at 5093bcd — its item is done and this is the successor item its own DELEGATION addresses ("DELEGATION 2026-08-08 FLEET (FL-2) -> FL-3"). W7 slot B is UI-42/UI-43 (`civicos-ui/**`) and the DIST lane is `newgroup/**`/`release/**` — both disjoint. `bio-plane/src/**` and `checks/bio-checks.mjs` are NOT claimed and NOT edited.
released:

### DELEGATION 2026-08-08 FLEET (FL-3) -> RECORD / WHOEVER OWNS `checks/bio-checks.mjs` AND PL-3's ENDPOINT: **DEC-65's SHAPE (b), WITH THE C-NUMBER CORRECTED AND THE SECOND SITE NAMED**

DEC-65's recommendation named FL-3 as the place to build shape (b). FL-3 read it in full, measured three things, and **held (a)** — the full disposition is appended to DEC-65 in `DECISIONS.md`. What is owed here is the work, re-scoped:

- **THE C-NUMBER IN DEC-65 IS WRONG AND IS CORRECTED IN THE ENTRY.** The rule requiring a named MEMBER to assert every declared part is **C-25.6 / `VERSION_GROUND_UNASSERTED`** (`checks/bio-checks.mjs:5802`), not C-25.15 — which is `VERSION_ORPHAN_ROW`, an unrelated rule about a row naming a version that is not there. Building (b) as DEC-65 worded it would have edited the wrong check.
- **IT IS A TWO-SITE CHANGE.** The refusal a background run actually meets is PL-3's endpoint guard in `store.mjs` (`SUGGEST_UNWRITABLE_STATE`), and it fires on `legsIn.length > 0` — **any leg at all**, regardless of how many parts are declared. Its own comment records the measurement that moved it there. C-25.6 alone would not unblock anything.
- **AND IT NEEDS A RULING FROM PL-1's OWNER FIRST.** Under the exemption, a single-part version still carries a `basis_version_grounds[]` row (C-25.5 makes the partition TOTAL). What does its `asserted_by` carry? The endpoint stamps it from the session, so a machine's row would read `class:ai` **in a field whose published meaning is "a member said this part is enough on its own"**. That field would then overclaim. The honest shape is a third state — *no independent-sufficiency claim was made* — and minting a state in that field is IS-1's.
- **PL-14 MUST RE-MEASURE.** The strength pair reads the grounds; the arithmetic argument for (b) is that with exactly one part there is no maximum to take, and that should be asserted rather than reasoned about.
- **THE CONSEQUENCE IS ALREADY EXERCISED AND IS HONEST.** `agent-worker/test/harness.test.mjs` B5 drives it: the legged submission is refused BY NAME, routes to the ADJUST row, cannot be answered, and the candidate is DROPPED with PL-3's `repeats` counter at zero. Nothing loops and nothing is silent. **Reversal cost is still LOW and rising** — FL-3 wrote nothing under either shape.

### DELEGATION 2026-08-08 FLEET (FL-3) -> RECORD: **THE PLANE HAS NO `AGENT_WORKER` CALLER, AND I8 CANNOT GO STABLE WITHOUT ONE**

`grep -a AGENT_WORKER bio-plane/src/` returns NOTHING. The binding is declared in `bio-plane/wrangler.jsonc` (FL-2) and read by no code. I8 names `RECORD` as the owner of the calling side.

- **Why it matters now.** I8's whole reason for being a separate entry from I6 is that this member is the first component that is both CALLED BY the plane and a CALLER of it. **The called-by half has never existed.** FL-3's re-read holds I8 at PROVISIONAL partly for that reason: an interface with one implemented end is a shape, not an agreement.
- **What the caller needs to hand over**, measured from the code FL-3 built: `{ run_id, store, credential, judgements?, max_steps? }` to `POST /run` over `env.AGENT_WORKER`. Everything else the member reads from the record itself — the MODE above all, which must NOT be a request parameter: SK-4's investigate-mode gate is only a gate while the mode is the record's.
- **Reversal cost:** none; it is additive and nothing consumes its absence today.

### DELEGATION 2026-08-08 FLEET (FL-3) -> RECORD / WHOEVER OWNS `op=whoami`: **THE FL-2 PRINCIPAL DELEGATION IS STILL OPEN AND FL-3 DID NOT WORK AROUND IT**

FL-2 filed it; nothing has changed. `op=whoami` still answers `tokenClass: "ai"`, `member: null` and names no principal, so `/run` still answers `principal: null` with `principal_source: "UNPUBLISHED — ..."` on the wire. **FL-3 deliberately did not infer it from the class**, which would be the member inventing an attribution to get past a gate. FL-6 needs it closed.

### DELEGATION 2026-08-08 FLEET (FL-3) -> SK-2 (the investigative skill): **THE REVIEW CRITERION YOU CITE IS NOW CODE, AND HERE IS ITS NAME**

SK-2's negative control reads *"a skill edit that moves loop termination into model judgement → FL-3's deterministic-table review criterion fails the change in review."* That criterion is no longer a review habit:

- `agent-worker/src/harness.mjs` exports **`NOT_JUDGEABLE`** — `pass`, `maxPasses`, `step`, `budget`, `mode`, `bound`, `run`, `store` — and `applyJudgement` REFUSES a judgement naming any of them, by name, citing the measurement (TREC 2011, +95/−87) rather than a preference. Through the op it is a 400 `JUDGEMENT_OVERREACH` naming the field and the step.
- **`CONTROL_FLOW`'s `judged` column is the §14b.4 table as DATA.** Every row states what the model decides inside it, or `null`. A skill that claims authority over a row whose `judged` is `null` is claiming authority the code refuses.
- Control arm **H4** is the measurement: remove `maxPasses` from `NOT_JUDGEABLE` and the overreach arms fail by name while the budget arms hold.

### DELEGATION 2026-08-08 FLEET (FL-3) -> FL-5 (sub-session fan-out) AND FL-6 (the cascade): **WHAT THE TABLE HANDS YOU**

- **FL-5.** `fanout` already takes ONE `op=airunspawn` payload per level, in `LEVELS` order, and the driver asserts on each that the search half's payload has **no `bias` key at all** — PL-12's fence, observed from the party it protects rather than only at its own site. What FL-5 owns is the RETURN contract: the harness's `collect` row currently takes reports from the judgement. **A sub-session that returns documents rather than a REPORT with a citation has defeated the architecture**, and the check for that belongs in your item.
- **FL-6.** `/run` answers `turns_run: 0` and `judgement_source: "supplied"` and says in words that the model account is yours. **Do not delete those fields — replace their values**, exactly as FL-2 told FL-3 about `stage`. The run object already carries both principals; what is missing is the one `op=whoami` will not publish (delegation above).
- **BOTH:** `harness.mjs` is PURE — no fetch, no clock, no globals, asserted — so your work can drive the whole table in a plain node process. Keep it that way; a decision that migrates into the driver is a decision nothing exhaustive covers.

### DELEGATION 2026-08-08 FLEET (FL-3) -> CONTENT-PDF: **THE `pdf-worker` SUITE IS STILL DARK, AND THE BATTERY STILL SAYS SO ON EVERY RUN**

Unchanged from FL-2's delegation and re-measured here rather than assumed: the battery prints `DARK: pdf-worker` on every run, and `pdf-worker/test/pdf-worker.test.mjs` is a NAMED SKIP because it imports `miniflare` bare. `agent-worker`'s two suites both resolve it through `createRequire(bio-plane/package.json)` and both run wherever the battery runs. **Two lines of your file.** This is D-232 and it is not FL-3's.

---

## CLAIM 2026-08-08 M0 (VERIFICATION)
session: m0-14-agent
opened: 2026-08-08T00:00:00Z
paths: bio-plane/scripts/control-register.mjs (the detector — the arm matcher, the marker, and the declaration's extent); bio-plane/scripts/coverage.mjs (the register report — the UNCLASSIFIED line, and the REGISTER_FLOOR block); bio-plane/test/hygiene.test.mjs (the detector's own arms, at the foot of the file, plus this item's six-arm declaration in its own control block); bio-plane/test/register.control.mjs (NEW — the harness that RUNS those six arms; deliberately not a `.test.mjs` because it edits real sources, `suggest.control.mjs`'s precedent); docs/development/VERIFICATION.md (the control-register row and section, moved from what the instrument PRINTED); docs/development/DEBT.md (D-233's disposition); docs/development/MEASUREMENTS.md (the register figures with their date and instrument). QUEUE.md NOT touched (CONDUCT's sole-writer file). NO `bio-plane/src/**`, no plane behaviour, no `newgroup/**`, no `pdf-worker/**`.
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-14 — D-233. The register's arms tally is blind to whole declaration styles and reports the blindness as ZERO. Fix inverts the matcher rather than lengthening a list of spellings, and NAMES what it cannot classify.
released: 2026-08-08 — handed to CONDUCT. NO suite's declaration was rewritten to suit the matcher: the 395 -> 462 move is the INSTRUMENT, measured by running the old detector and the new one over the same untouched sources. `case-opened.test.mjs` was NOT edited — it is reported as UNCLASSIFIED and NAMED, which is the outcome the item asks for when a declaration cannot be counted.

### DELEGATION 2026-08-08 RECORD (M0-14) -> CONDUCT / WHOEVER OWNS THE REGISTER NEXT: **TWO SUITES STATE THEIR ARMS IN A COMMENT THE REGISTER CANNOT SEE, AND IT IS M0-9's OWN RECEIPT REPEATING**

M0-9 recorded that REC-48 *"got past it by moving its arms into a SECOND comment the register never saw"*, and removed that workaround from two suites. **A sweep of all 120 suites for the same shape — a comment block that states arms, enumerates them and carries transition arrows, but has NO `NEGATIVE CONTROL` marker — finds it still present in exactly two:**

- **`earnedbasis.test.mjs`** — the register records its one-line entry at **1 arm**, while the block immediately below it says *"THE THREE ARMS (run 2026-08-04, rec18-agent…)"* and states (a), (b), (c) plus a fourth arm from REC-48. **The suite's own prose and the register disagree by at least three, and only the suite's prose is right.**
- **`citeinquiry.test.mjs`** — the same shape, *"THE TWO ARMS, RUN 2026-08-04 (rec37-agent)"*, in an unmarked block.

**NOT FIXED HERE, deliberately, and the reasoning matters more than the two edits:** this is a defect in two suites' DECLARATIONS, not in the instrument, and M0-14's subject is the instrument. Marking those blocks would raise the tally, which means moving `REGISTER_FLOOR` — **a gated figure should be moved by the item that measured the change, not by one that swept past it.** There is no urgency and no risk in leaving it: the floor fails on a FALL, and this can only make the number rise.

**The fix is one marker phrase per block** (the detector now accepts a colon OR a dash after it), then `REGISTER_FLOOR.arms` moved to what a green `node scripts/coverage.mjs --strict` PRINTS. The sweep that found them is reproducible: for every block comment in `bio-plane/test/*.test.mjs`, flag those that mention "arm", contain a parenthesised ordinal and a spaced arrow, and do NOT contain the marker phrase.

### DELEGATION 2026-08-08 RECORD (M0-14) -> EVERY SESSION THAT READS THE REGISTER: **THE TALLY IS A FLOOR ON ARMS STATED, AND THE INSTRUMENT NOW SAYS SO ON EVERY RUN**

- **It counts MARKED items**, and marks are transitions (` -> `, ` → `) or parenthesised ordinals opening a segment. **An arm carrying a LABEL rather than an ordinal is not counted** — `suggest.test.mjs` reads 8 against a real 10 because two of its arms are labelled `(D-231a)` and `(D-231b)`. Widening the ordinal to any bracketed token would count every `(D-113)` and `(DEC-46)` this prose is full of, which is the over-strictness failure in the other direction and is driven by a control arm.
- **A declaration the detector cannot count is `null`, never `0`, and is NAMED.** `case-opened.test.mjs` is the one on this tree. **A NEW one FAILS `--strict`** — that is D-233 arriving again — so add the marks or add the suite to `REGISTER_UNCLASSIFIED` with the reason.
- **The tally, the classified count and the CORPUS all carry a floor.** Move them only upward and only to a figure a green run PRINTED. The corpus figure is there because a matcher narrowed to nothing reports a beautiful 100% over an empty corpus.

## CLAIM 2026-08-08 CONTENT-PDF
session: cpdf-9 (worktree agent-a59da2fdb4abbfa53)
opened: 2026-08-08T11:10:00Z
paths: **`pdf-worker/**`** — `test/pdf-worker.test.mjs` (miniflare resolved through the plane's install, the D-186 sandbox import, the comment-stripper fix, the new `/version` arms), `test/pdf-worker.control.mjs` (NEW — the negative-control driver, deliberately NOT a `.test.mjs`: it EDITS REAL SOURCES while it runs and neither the battery nor the fleet walk must discover it; PL-3/PL-4/PL-11/FL-2's precedent), `src/index.mjs` (`GET /version` + the `version` SURFACE row), `dist/pdf-worker.bundled.mjs` (rebuilt from that source — the committed bundle IS what the suite and the deploy load). **`bio-plane/scripts/coverage.mjs`** — ONE line: `FLEET_FLOOR.surfaceOps` 3 -> 4, moved WITH the fleet in the same turn and taken from the figure the instrument PRINTED, never by adding to the previous number. **`bio-plane/scripts/battery.mjs`** — comment only: `fleetDepSkip` is now unreachable and the header SAYS so rather than the path being deleted silently. `docs/development/INTERFACE-CHANGES.md` (NEW IC-33), `docs/development/DEBT.md` (D-232's status cell only), `docs/development/CLAIMS.md` (this entry and its DELEGATIONS), `docs/development/kickoffs/CONTENT-PDF.md` (the provisional decision record). **NOT** `agent-worker/**` (FLEET's — read as the pattern to COPY, never edited; a DELEGATION is appended for the one stripper site still naive there). **NOT** `bio-plane/src/**`, **NOT** `bio-plane/test/**`, **NOT** `civicos-ui/**`, **NOT** `newgroup/**`, **NOT** `docs/development/INTERFACES.md` (I6's version bump is CONDUCT's), **NOT** `docs/development/QUEUE.md`.
interfaces consumed: I8 (`agent-worker`'s dependency-resolution and version-endpoint pattern, as PRECEDENT — copied, not generalised), I1 (untouched).
interfaces owned: **I6 — `plane <-> pdf-worker`. It GAINS `GET /version` (additive; fleet rule 4 was unverifiable for this member). IC-33 is filed PROPOSED; the version bump and the RESOLUTION are CONDUCT's, and this session does not touch `INTERFACES.md`.**
expected: QUEUE CPDF-9 — D-232's open half. The member's suite RUNS on a fresh checkout, so the battery's `fleet:` line reports 2 of 2 members RAN and prints no `DARK:` name.
accepts-when: as CPDF-9's row. **Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief** — measured 122 suites (120 plane + 2 fleet) · 121 green + 1 named skip · 7,456 assertions · `DARK: pdf-worker` · exit 0; the brief's figure and the measurement AGREE, and the agreement is measured rather than assumed. The worktree arrived with no `bio-plane/node_modules` and `npm ci` was run there first. Final battery green with the `fleet:` line naming no dark member and the delta ATTRIBUTED per suite by re-running the true baseline, never by subtraction; `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED and the FLEET figure and its floor both printed; `node civicos-ui/test/run.mjs` exit read unpiped; `node tools/plancheck.mjs` clean but for UNPUSHED. Negative controls RUN, each armed ALONE with the others held open, declared BEFORE arming, every restore verified by sha256 AND by `cmp` against a pristine pre-arm copy, the harness INSIDE THIS WORKTREE and never in the shared scratchpad. **The fresh-checkout condition is verified with `pdf-worker/node_modules` ABSENT** — an install there would prove the wrong thing.  No push, no merge, NO DEPLOY, no version bump.
concurrency: checked over the whole file. The only live-looking claim naming these paths is FLEET's `is-wave-w6-fl2-vf3` block, whose `released:` line is empty but whose work is MERGED at `5093bcd` (`conduct: FL-2 + VF-3 integrated`) and whose own `paths:` line says **NOT `pdf-worker/**`** and routes it here by DELEGATION — the same-file handoff `IS-BUILD-PLAN`'s wave rules define. No other claim names `pdf-worker/**`, `bio-plane/scripts/coverage.mjs` or `bio-plane/scripts/battery.mjs`. `bio-plane/src/**` and `agent-worker/**` are NOT claimed and NOT edited.
released:

### DELEGATION 2026-08-08 CONTENT-PDF (CPDF-9) -> FLEET / WHOEVER OWNS `agent-worker/test/agent-worker.test.mjs`: **THE STRIPPER FIX WENT INTO THE CODE SCAN AND NOT INTO THE CONFIG SCAN, IN THE SAME FILE**

FL-2 met `pdf-worker`'s naive `//`-to-end-of-line idiom, diagnosed it correctly, and fixed it at **line 85** (`const CODE = SRC.replace(…).replace(/(^|[^:])\/\/[^\n]*/g, "$1")`) with a header explaining exactly why. **Line 353 still reads the WRANGLER config through the naive form:** `readFileSync(WRANGLER,"utf8").replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/[^\n]*/g,"")`. Same file, same trap, one scan out — the class sweep stopped one line short of itself.

- **MEASURED, so it is a trap and not a defect today:** `agent-worker/wrangler.jsonc` is 3,058 chars and contains **zero** `://` sequences, so the naive and anchored forms read the same 512 characters of config. Nothing is currently lost. It goes wrong the moment that config holds a URL — a `service` templated from an installer slug, an observability endpoint, a comment carrying a workers.dev address — and it goes wrong SILENTLY and in the generous direction, because deleting text can only remove matches from a `.test()` that expects `false`.
- **The fix is the one already in the file**, four characters: `(^|[^:])` in front, `"$1"` behind. `pdf-worker`'s suite now uses one `strip()` helper for BOTH scans for exactly this reason.
- **CPDF-9 did not make this edit**: `agent-worker/**` is FLEET's, and reaching into it is the mistake REC-71 exists to correct.

### DELEGATION 2026-08-08 CONTENT-PDF (CPDF-9) -> UI / WHOEVER OWNS `civicos-ui/check-mock-envelope.mjs`: **THE SAME STRIPPER CLASS, AND ONE LINE OF `app.html` IS ALREADY BEING TRUNCATED**

`check-mock-envelope.mjs:206` strips a line comment as `L.replace(/\/\/.*$/, "")` before hunting transport call sites. Same idiom, same class as D-232's rider.

- **MEASURED against the real corpus rather than argued:** the runtime script block of `app.html` is **980,256 characters over 15,356 lines**; exactly **1** line carries `://`, and that line IS truncated — the walk reads **55 characters of a 91-character line** (`<input class="txt" id="a-loc" placeholder="https://&hellip;" …>`). **The verdict is unaffected TODAY**: that line holds no `rec(`/`recPost(`/`api(` call, so the number of call sites found is identical either way (measured both ways: 0 lost).
- **Why it is still worth fixing.** The arm's whole job is to prove no non-seam calls the transport, and it proves it by NOT FINDING one. A stripper that silently shortens the lines it searches is an instrument that can only ever fail generously — the same shape as the arm that came back green over a source truncated by two thirds. One line of `app.html` is already inside the blind spot, and `app.html` is where URLs live.
- **The fix:** require a non-`:` before the two slashes, as `pdf-worker`'s and `agent-worker`'s code scans now do. **CPDF-9 did not make this edit** — `civicos-ui/**` is not its path.

### DELEGATION 2026-08-08 CONTENT-PDF (CPDF-9) -> WHOEVER OWNS `bio-plane/test/daemon-token.test.mjs`: **THE THIRD SITE OF THE SAME IDIOM, IN THE PLANE'S OWN TEST ESTATE**

`daemon-token.test.mjs:179` asserts `/DAEMON_TOKEN|MONITOR_TOKEN/.test(TOKENS_SRC.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm, ""))` is **false** — a scan whose expected answer is "not found", read over a source stripped by the unanchored idiom.

- **MEASURED:** `bio-plane/src/tokens.mjs` is 1,728 chars and carries **zero** `://`, so naive and anchored both read 646 chars. Nothing is lost today.
- **Why it is named anyway:** an assertion that expects NOT-FOUND over a corpus an instrument may silently shorten is the exact combination that cannot fail loudly. The plane's other strippers are ANCHORED (`/^\s*\/\/.*$/gm` in `bias`, `strengthpair`, `versions`, `versionstate`, `meaningread`, `suggest`, `aicredential`, `capturerequests`, `machine-fences`; `/^[ \t]*\/\/.*$/gm` in `hygiene`, with a comment saying why; and `plane-envelope.test.mjs` blanks a comment to spaces with the reason written out), so this file, `check-mock-envelope.mjs` and `agent-worker`'s config scan are the three that did not get the memo.
- **THE FULL SWEEP, MEASURED. Corpus: 274 source files, 12,038,114 characters** (`*.mjs`/`*.js`/`*.html`/`*.cjs`, excluding `node_modules`, `.git`, `dist/` and the committed bundles). **27 candidate lines; 2 are URL normalisation (`/^https?:\/\//`) and not strippers; of the remaining 25 comment strippers, 21 are ANCHORED and correct, 1 is `pdf-worker`'s deliberate `stripNaive` fixture that exists to demonstrate the trap, and 3 are UNANCHORED** — the three named in these delegations, two of which (`agent-worker`'s config scan and `daemon-token`'s) lose nothing today and one of which (`check-mock-envelope.mjs`) is already truncating a line. `pdf-worker`'s own two sites were the fourth and fifth and are fixed by this item.
- **WHAT THIS MATCHER CANNOT SEE, stated rather than implied.** It reads SINGLE LINES that contain both `replace(` and a regex literal spelling two escaped slashes. It would miss a stripper whose regex is built by `new RegExp` from a variable, one whose `replace(` and regex sit on different lines, and one written as a `split`/`indexOf` walk rather than a `replace`. **Both blind spots were then checked directly and both are EMPTY:** no `new RegExp(` in the corpus builds a two-slash pattern, and of the 13 files where a `replace(` ends a line, none has a two-slash regex on the next.

### DELEGATION 2026-08-08 CONTENT-PDF (CPDF-9) -> FLEET / WHOEVER OWNS `scripts/coverage.mjs`'s FLEET REACH MATCHER: **A SURFACE OP MENTIONED IN A COMMENT IS CREDITED AS REACHED, AND THE HEADER SAYS OTHERWISE**

Found by CPDF-9's control arm A6, which **declared in advance that it might come back green** and did.

- **DRIVEN, in two stages.** Stage 1 deleted every DRIVEN `/version` assertion from `pdf-worker`'s suite and left the SURFACE row: `coverage.mjs --strict` **exit 0**, `4/4 surface ops reached`. Stage 2 additionally removed every textual `/version` from the file: **exit 1**, `3/4 surface ops reached`, the op named. **So the gate has teeth — it is the difference between a MENTION and a CALL that it cannot see.**
- **The matcher is `[/"'`]${op}(?=[?"'`&/\s]|$)` over the concatenated suite text**, and its comment says *"a bare word in a comment does not count"*. That is true of a bare word and false of a path-shaped mention: `/version` inside a comment matches. This suite's own header names `GET /version` half a dozen times, which is how stage 1 stayed green.
- **This is deliberately NOT fixed here, and the reason is a receipt.** Narrowing a reach matcher is how a walk goes blind (REC-67's class, and the 6,029-of-17,265 finding one file over). Any fix must assert its REACH as a DELTA with the corpus size printed, and must state what it cannot see. **It is the fleet instrument's owner's call, not `pdf-worker`'s** — CPDF-9 touched exactly one line of `coverage.mjs` (the floor, 3 -> 4, moved with the fleet in the same turn).

### DELEGATION 2026-08-08 CONTENT-PDF (CPDF-9) -> WHOEVER NEXT LANDS A PLANE SUITE / D-233's FAMILY: **`VERIFICATION.md`'s NEGATIVE-CONTROL REGISTER ROW IS THREE SUITES STALE, MEASURED**

Noticed in passing and NOT fixed, because that row's own rule is that it *"is moved BY THE ITEM THAT LANDS ONE"* and **CPDF-9 lands no plane suite** — the suite it makes run is a fleet member's, counted on the FLEET line and not in the register.

- **MEASURED on a green run, 2026-08-08:** `node scripts/coverage.mjs --strict` prints **`120 of 120 suites declare one (100.0%) · 395 arms stated · fullest 21 (hygiene.test.mjs)`**. `VERIFICATION.md`'s row reads **117 of 117 · 380 arms**, remeasured by REC-73 the same day and overtaken since by PL-14/PL-15/M0-13/REC-73's successors.
- **Why it is worth a line rather than a shrug:** this row is the one figure in that table anybody re-derives, it has now gone stale for the FIFTH consecutive time, and every one of the five was caught by an item measuring instead of reading. The instrument PRINTS the number; the fix is to copy what it printed.

## CLAIM 2026-08-08 RECORD (REC-75)
session: worker-rec-75
opened: 2026-08-08T17:25:28Z
paths: the `suggestVersion` / `#suggestionFrontmatter` span in bio-plane/src/store.mjs, bio-plane/test/suggest.test.mjs, bio-plane/test/suggest.control.mjs, docs/development/DEBT.md (D-234 disposition), docs/development/CLAIMS.md, docs/development/QUEUE.md (REC-75 landed line)
interfaces consumed: I3
interfaces owned: none
expected: REC-75 / D-234 — the substance gate defeated by a quotation mark. `suggestVersion` composes its CHECK 3 candidate from RAW args while the bytes it compares against went through `#fmSafe`, so a duplicate carrying a quote, a backslash, a newline or trailing space is never refused. Fix: ONE normaliser feeding both the candidate composition and the write, plus the settled question of what `composition` publishes. Class sweep over caller-derived-vs-stored comparisons.
released: 2026-08-08 — committed on `worktree-agent-a5f4a7acbcecf3057`, NOT pushed and NOT merged; CONDUCT integrates. Battery 121/122 green + 1 named skip, 7,473 assertions (true baseline re-run 7,456; delta +17 entirely in `suggest.test.mjs`). coverage --strict exit 0 unpiped; `civicos-ui/test/run.mjs` exit 0 unpiped. DEC-49 `regionLines` floor moved 953 -> 996 from the printed figure. 17 control arms run, 0 behaved other than declared.


### DELEGATION 2026-08-08 RECORD (REC-75) -> CONDUCT: **`op=suggest` GAINED ONE FIELD AND CHANGED WHAT AN EXISTING ONE CARRIES (I3) — THE SECOND HALF IS THE ONE TO LOOK AT**

- **ADDITIVE: `composition_of: "record"`.** Every existing reader keeps the shape it reads. This session judged an added optional field on an existing answer to be inside I3's stable contract rather than a change to it — PL-15's precedent, flagged here so CONDUCT can disagree cheaply. It exists so a consumer can tell WHICH BYTES it is holding without reading a comment.
- **NOT ADDITIVE, AND STATED PLAINLY: `composition` NOW CARRIES DIFFERENT BYTES for a submission whose description, ground statement or leg note contained `"`, `\`, a newline or padding.** It used to echo the candidate built from raw args; it now publishes what the record holds. **THIS IS NOT A NEW DIVERGENCE — IT IS AN EXISTING ONE CLOSED.** Before REC-75 that published value could already differ from the record's (M0-13's rider), and nothing said so. No consumer in `civicos-ui/` reads `composition` off this answer (the surface reads it from `op=basisversions`, which is the same string), so the blast radius measured is zero — but a consumer outside this repo comparing the published composition to its own submission will now see the record's spelling, which is the point.
- **NO IC IS FILED** for either. Say so if you disagree; the reversal is one line each.

### DELEGATION 2026-08-08 RECORD (REC-75) -> M0-14 (D-233, the register's arms tally): **A FIFTH CONFIRMATION, MEASURED ON A TREE THAT ADDED FIVE ARMS**

REC-75 added **5 negative-control arms** to `suggest.test.mjs`'s numbered declaration block and ran `node scripts/coverage.mjs --strict` before and after. **The register printed `395 arms stated` BOTH TIMES.** That is D-233 exactly — the tally is blind to the numbered form, `suggest.test.mjs` scores ZERO, and the direction is the generous one. **What this adds to the row: the blindness is not merely historical, it swallowed arms that landed today**, so the figure under-reports a growing estate in real time rather than only failing to notice a shrinking one.

**AND `VERIFICATION.md`'s control-register row is stale by hand again, MEASURED rather than inferred:** it reads *"117 of 117 · 380 arms"*; the instrument on this tree prints **`120 of 120 suites declare one (100.0%) · 395 arms stated · fullest 21 (hygiene.test.mjs)`**. REC-75 did NOT edit that row — it is M0-14's own subject and two items moving it in parallel is how it went wrong before — but the figure is recorded here so M0-14 does not have to re-derive it. That is the **fifth** consecutive item to find this row stale by measuring it.

### DELEGATION 2026-08-08 RECORD (REC-75) -> WHOEVER NEXT OWNS `suggestVersion` / THE PL-3 FAMILY (and REC-74, which is the same question): **THE ANSWER NOW HAS TWO SOURCES AND LABELS ONE**

Raised as **D-235** and repeated here because a debt row is not a channel a builder reads.

- `composition` is read back from `inquiry_basis_versions` and says so. **`legs`, `count`, `grounds`, `ground_count` and `pair` on the SAME answer are still candidate-derived**, computed before the write. They agree today only because the candidate is now composed from the persisted values, and **nothing pins that agreement.**
- **This is REC-74's shape arriving inside ONE answer** rather than across two ops: one row, two readers, and only one of them says which facts it is reporting. If REC-74 lands first it should take this with it.
- **The cheap fix is an ARM, not a rewrite:** assert that a candidate-derived collection equals what the record holds for the same version, so a future field composed from raw args on one side fails visibly.

### DELEGATION 2026-08-08 RECORD (REC-75) -> WHOEVER OWNS THE NEGATIVE-CONTROL HARNESSES: **A RELATIVE COUNT CAN DISARM ANOTHER ITEM'S CONTROL, AND IT DID**

Recorded because it cost this item a re-run and would have cost the next one silently.

- `suggest.test.mjs`'s F10 block asserted `(await read()).total === 6` — the fixture's ABSOLUTE version count. REC-75's fixture legitimately added two readings, so the literal had to move. **Replacing it with a delta captured just before the three resubmissions is better engineering and QUIETLY DISARMED `suggest.control.mjs`'s (D-231a) arm**: that arm re-arms the clock, a duplicate two blocks EARLIER lands, and the absolute count going 6 -> 7 is the second half of the exact 59/2 signature CONDUCT measured in the wild. A relative count taken after the landing is blind to it.
- **The harness caught it** — it printed `** WRONG: expected an assertion naming "STRUCTURALLY NOTHING MOVED" to FAIL and none did` — which is the harness working, and is why every arm must declare what must NOT fail as well as what must.
- **The rule this yields:** a count another item's control depends on is LOAD-BEARING and must be RE-MEASURED, never made relative. The literal is now 8 with that reason at the assertion.

### DECISION FOR BOB 2026-08-08 (REC-75) — **WHEN A RUN PROPOSES A READING, THE ANSWER SHOWS IT THE RECORD'S WORDS, NOT ITS OWN**

- **What runs provisionally:** the record refuses characters it cannot carry — a member who types a double quote gets a single one, a backslash becomes a quote, a line break becomes a space, padding is dropped. When a run proposes a reading, the plane's answer now hands back **the reading as the record actually holds it**, and labels it as the record's. Until today the answer handed back the run's own words, and the record quietly held something else.
- **Why it was genuinely ambiguous:** an answer that echoes what you sent is easier to reconcile against what you meant to send; an answer that shows what was kept is the only one that tells you what a later reader will see. Both are defensible, and the record has been doing the first while claiming nothing.
- **The alternative:** keep echoing the submission, and publish the record's version as a second, separate field. Nobody's submission ever appears to be silently altered, at the cost of two spellings of one reading travelling together and every consumer choosing between them.
- **Recommendation: keep the record's words.** The whole product is trustworthiness of the record, and an answer that shows a run its own sentence while the record holds a different one is the record claiming more than it can support — the defect `CLAUDE.md` ranks above a missing feature. It is also what closes the real hole underneath this item: two readings identical in the record but punctuated differently were both being accepted as different readings, which made "these two versions differ" false for anybody reading the case afterwards.
- **What reversing it costs: LOW, AND IT DOES NOT RISE.** One line in the plane and one field dropped from the answer. Nothing is stored differently — the record already held these bytes and always did; only what the answer SHOWS changed. No data accumulates under the choice, so the cost tomorrow is the cost today. The one thing that would make it expensive is a consumer outside this repository written to compare the answer against its own submission, and none exists here.

---

## CLAIM 2026-08-08 RECORD (REC-67)
session: rec67-agent
opened: 2026-08-08T17:44:23Z
paths: bio-plane/test/bounds.test.mjs, bio-plane/test/meaning-bounds.test.mjs, bio-plane/test/plane-envelope.test.mjs
interfaces consumed: none
interfaces owned: none — test estate only, no `src/**` and no `civicos-ui/**` (no plane source was touched, and the only `civicos-ui/` files that existed at any point were two transient negative-control plants, created and removed inside one turn and verified gone)
expected: REC-67. The REC-59 consumer walk in `bounds.test.mjs` misclassifies a NON-call-site — an ordinary `Map.get("projection")` counted as a corpus-arm CALL SITE in `civicos-ui/` — plus the SWEEP for the class of walks that anchor on a call-shaped prefix and therefore admit a METHOD NAME that is not a call to the plane. The matcher is INVERTED (REC-70's lesson: a callee is resolved against the file's own request-forming helpers rather than matched against a list of spellings that goes stale silently), and every walk touched keeps its REACH asserted as a DELTA with its corpus size PRINTED.
released: 2026-08-08 — work committed on the REC-67 branch; CONDUCT integrates.

### DELEGATION 2026-08-08 RECORD (REC-67) -> UI: **UI-46's WORKAROUND IS NO LONGER NEEDED — THE PLANE'S MATCHER IS FIXED, AND REMOVING IT IS YOURS**

`civicos-ui/test/bound-sweep.test.mjs` holds the op name in a constant (`const PROJECTION_METHOD = "projection"; const PROJECTION_BODY = CLASS.methods.get(PROJECTION_METHOD)`) with a long comment at the site explaining that writing it inline made `bio-plane/test/bounds.test.mjs` classify a Map read as a `corpus-bare` call site in `civicos-ui/`. **That was the right call and it is why REC-67 exists.** The cause is now gone.

- **What changed:** `bounds.test.mjs`'s helper arm no longer matches a list of callee spellings (`recR|rec|call|DO|get|j|GET`). It requires the callee to be a BARE identifier that is DEFINED IN THE SAME FILE as a function which forms an HTTP request, directly or through another local helper. `CLASS.methods.get("projection")` is refused twice over — it has a receiver, and `get` is not a request-forming helper.
- **PROVED, not asserted:** the exact regression was re-planted as a control (`civicos-ui/nc-rec67-arm2.mjs`, `CLASS.methods.get("projection")`, created and removed in one turn). `bounds.test.mjs` stayed **147/0 GREEN** with the file present, while the **PRE-FIX matcher read the same file and classified it `helper corpus-bare civicos-ui/nc-rec67-arm2.mjs:3`**, taking the site count 40 -> 41. The workaround is provably unnecessary.
- **What is owed, and it is small:** write the method name inline again if UI prefers it, and REPLACE the comment rather than deleting it — the paragraph recording what happened is worth keeping, with a line saying the plane's matcher was corrected on 2026-08-08 by REC-67. **Not done here: `civicos-ui/**` is not RECORD's to edit**, and the constant is harmless where it stands.

### DELEGATION 2026-08-08 RECORD (REC-67) -> UI: **THREE UI WALKS ARE IN THE SAME CLASS — MEASURED LATENT, NOT DEFECTIVE, AND THE MEASUREMENT IS THE POINT**

REC-67 swept the estate for walks that anchor on a CALL-SHAPED PREFIX and can therefore admit a METHOD NAME that is not a call to the plane. Three live in `civicos-ui/test/`, and all three anchor with `\b`, which matches between a DOT and a letter — so `x.rec("op")` or `x.api("op")` reads as the seam:

- `civicos-ui/test/surface-registry.test.mjs:888` — `new RegExp("\\b(?:" + seams.join("|") + "|apiR|apiQ|intentPreflight)\\(\\s*\"([a-z]+)\"")`, feeding ARM D1's floor of 50 and ARM D3;
- `civicos-ui/test/surface-registry.test.mjs:1151` and `:1161` — the same alternation over a sliced block;
- `civicos-ui/test/ai-session-wire.test.mjs:752` — the same alternation, feeding ARM C1's `eq(runOpsOutside, [])`, which is an EMPTY-SET assertion and therefore the one where a false positive would fail alarmingly.

**MEASURED over `civicos-ui/app.html` on 2026-08-08: 133 seam-shaped call matches, of which 0 are preceded by a dot; 108 seam-then-quoted-op matches, of which 0 are preceded by a dot.** So **the exposure is structural and not live** — nothing is wrong today, and this is a delegation rather than a defect report. `bound-sweep.test.mjs`'s own `callSites` anchors on `recR\("op"`, a name distinctive enough that a method of the same name is not a realistic collision, and it is listed only for completeness.

- **What is owed if UI agrees it is worth it:** replace `\b` with a negated class (`(?<![.\w$])`) in the four anchors above. That is the whole change; it is structural rather than a longer list, and `bio-plane/test/plane-envelope.test.mjs` carries the same one-token correction with a both-directions guard beside it that can be copied.
- **The reason to do it even though nothing is red:** `rec` and `api` are exactly the kind of short generic name that becomes a method on somebody's helper object, and REC-67 exists because `get` did. A walk that invents a consumer will one day invent one that hides a real answer.

## CLAIM 2026-08-08 UI
session: ui48-unstated-bound
opened: 2026-08-08T00:00:00Z
paths: `civicos-ui/app.html` (the five sites and the shared bound function only), `civicos-ui/test/bound-sweep.test.mjs` (ARM G, the unstated-bound walk, plus ARM H's five runtime pins), `docs/development/CLAIMS.md` (this entry and its DELEGATIONs).
**NOT OPENED:** nothing under `bio-plane/src/`, `bio-plane/checks/`, `bio-plane/test/`, `newgroup/` or `release/`. The plane is READ ONLY here — `store.mjs` and `query.mjs` are read TEXTUALLY by the walk, exactly as `bound-sweep` already reads them, and no plane file is written. `civicos-ui/test/*.test.mjs` other than `bound-sweep.test.mjs` are READ ONLY; the negative controls mutate `app.html` on disk one arm at a time and restore it against a pristine pre-arm copy kept in `.ui48-harness/` inside this worktree, verified by `cmp` AND by sha256 after every arm.
interfaces consumed: I3. interfaces owned: none. No op added, no check minted, no schema touched, no member-facing sentence authored beyond the ONE shared cut clause named below.
expected: **UI-48** — five surfaces read a capped op and state NO bound (`op=connections` on the subject view and on the document page; `op=resolutions` on the document page; `op=concerns` in the finder and in the progressions view). Each states the record's PUBLISHED `limit` and `truncated`, over the CORRECT SET at that site, through the shared function `concernsBoundHtml` was already half of — lifted rather than five new sentences. Plus a class SWEEP that finds out whether five is the number.
concurrency: no live claim names `civicos-ui/app.html` for WRITING (UI-52's claim names it READ ONLY and is `released:`-blank but merged), and no live claim names `civicos-ui/test/bound-sweep.test.mjs`.
released:

### DELEGATION 2026-08-08 UI (UI-48) -> CONDUCT / WHOEVER OWNS `VERIFICATION.md`: **THE NEGATIVE-CONTROL ROW IS STALE AGAIN, MEASURED RATHER THAN READ, AND IT IS NOT THIS ITEM THAT MOVED IT**

`VERIFICATION.md`'s floor table carries **117 of 117 suites · 380 arms**, moved there by REC-73 on 2026-08-08. `node bio-plane/scripts/coverage.mjs --strict` **printed `120 of 120 suites declare one (100.0%) · 395 arms stated · fullest 21 (hygiene.test.mjs)`** on a green run in this worktree today, exit 0 read unpiped.

- **It is not UI-48's to attribute to itself.** `coverage.mjs` scans `bio-plane/test/` only — `VERIFICATION.md` says in its own words that it does not measure the UI — so the three new suites and fifteen new arms came from plane items landing after REC-73, and this item's own new arms (`civicos-ui/test/bound-sweep.test.mjs`, 134 -> 202 assertions) are invisible to that instrument by design.
- **It is raised here rather than edited** because the row belongs to whoever landed the suites that moved it, and a UI worker rewriting a shared floor mid-wave is a merge conflict CONDUCT would have to resolve for no gain. **This is now the fifth consecutive item to find that row stale by measuring it**, which is an argument for the row printing the command instead of the answer — the same correction `CLAUDE.md` already made for `store.mjs`'s line count.
- **Reversal cost: none.** Nothing reads the number.

### DELEGATION 2026-08-08 UI (UI-48) -> RECORD / REC-70's FAMILY: **`op=airunlog` IS ON THE CAPPED ROSTER NOW, AND UI-49's REASON FOR NOT BUILDING ITS SURFACE HAS EXPIRED**

UI-48's WALK 1 run (the plane's own roster, read off `store.mjs` and printed every run) reads **UNCONDITIONAL (15)**: `readingname resolutions concerns connections queue versionchain basisversions biasinhale capturerequests aicredentials tasks airunlog projection searchindexcheck exportlog` — against the **9** UI-46 measured on 2026-08-07.

- **`airunlog` is in that list.** UI-49 decided AGAINST building a log surface partly because *"it publishes an UNBOUNDED collection, so a consumer today would be a surface with a bound it cannot state"*, and REC-70 was routed on the finding that `op=airunlog`'s read carried no `LIMIT`. **That is no longer true of this plane**, so the stated reason for the decision no longer holds and the decision should be re-taken on its remaining grounds (the transcript-versus-log distinction) rather than inherited.
- **Six of the fifteen have no UI consumer at all** — `versionchain`, `basisversions`, `biasinhale`, `capturerequests`, `aicredentials`, `searchindexcheck`, plus `exportlog` and `airunlog` — so UI-48's class does not extend to them today. **That is a fact about the surface being absent, not about the bound being stated**, and the first surface to read any of them inherits this item's obligation. ARM G will catch it: the roster is the plane's, so a new consumer joins the walk with nobody editing the suite.

### DELEGATION 2026-08-08 UI (UI-48) -> WHOEVER OWNS `civicos-ui/test/surface-registry.test.mjs`: **THE `progressions` SURFACE DOES NOT DECLARE THE OP IT READS**

`SURFACES.progressions.reads` is `["progression", "progressiondefine", "captureprogressions"]`. `progPaintInstance` reads **`op=concerns`** and threads its documents through every stage — it is the read that fills the pick list a member acts from. The registry's own header says `reads` is *"the surface's PRINCIPAL READS, NOT AN EXHAUSTIVE LIST"*, so this is **not a rule violation**; it is a declared read missing from a surface whose whole subject is that op's answer, and the omission is why a walk over the registry would not have found this item's fifth site.

- `SURFACES.subjects` and `SURFACES.finder` both name `concerns`; `progressions` is the odd one out.
- **UI-48 did not add it**, because `surface-registry.test.mjs` is not in this item's claim and its ARM A drives the declaration in both directions — adding a row without running that arm's sourcing control is how a registry becomes a hand-typed list again.
- **Reversal cost: none.** One array entry.

## CLAIM 2026-08-08 RECORD (REC-64 — DEC-49's enactment, the sweep)
session: worker REC-64, worktree agent-a0fc522fabe53b533
opened: 2026-08-08
paths:
  - `bio-plane/checks/bio-checks.mjs` (new rows and one new family; the DEC-49 catalogue is this item's subject)
  - `bio-plane/src/store.mjs` — **REGION MARKERS ONLY**, plus one refusal at `aiRunOpen` gaining a
    RECEIVED code (UI-38's §14a rider). No behaviour is changed at any marked site: a `DEC-49 REGION`
    pair is a block comment, and the marked spans are existing refusals that already carried their
    codes. **PL-14's claim block above is `released:`-empty but MERGED at `fee6e9a`**, and PL-15's
    concurrency note records the same handoff; this item takes `store.mjs` after them.
  - `civicos-ui/check-refusal-codes.mjs` — **THE `FLOOR`/`CEILING` CONSTANT BLOCK ONLY.** `civicos-ui/**`
    is otherwise NOT this item's, and no other line of that file, no line of `app.html` and no
    `civicos-ui/test/**` suite is opened. The ratchet block is claimed because the guard's own header
    delegates it: *"Move them WITH the corpus, in the same turn, or they stop meaning anything"* —
    five consecutive items have found a floor already stale by measuring it, and one of them had
    already flipped a control from RED to GREEN.
  - `bio-plane/test/machinefences-dec49.test.mjs` (new suite) and `bio-plane/test/nc-rec64.mjs` (this
    item's negative-control harness, INSIDE this worktree and never in a shared scratchpad)
interfaces consumed: I3 (the op contracts — a refusal gains `code`/`check`/`translation`, additive)
interfaces owned: none
expected: REC-64 — every refusable condition carries an error code with a canned translation, and an
  untranslated code fails the harness. First eleven are the `MACHINE_CANNOT_*` fences SK-1 and D-229
  converged on from opposite directions; then the single-homed tail of the `reachGap`.
accepts-when: as QUEUE.md REC-64, plus: **battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT**
  and trusted over the brief (the worktree arrived with no `bio-plane/node_modules`; `npm ci` run and
  its exit read UNPIPED). `node civicos-ui/test/run.mjs` exit read UNPIPED with **the `reachGap`
  CEILING LOWERED and every floor moved to the figures the guard PRINTED on a green run**, never by
  adding to the number in the file. `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` unpiped.
  Negative controls RUN, each arm ALONE with the others held open, declared before arming, restores
  verified by sha256 AND by `cmp` against a pristine pre-arm copy. No push, no merge.
concurrency: three other workers are running and at least one is in `store.mjs`. This item's
  `store.mjs` diff is deliberately marker-comments plus one refusal object, so a merge against a
  concurrent edit is a hunk conflict at worst and never a semantic one. `index.mjs` is NOT claimed
  and NOT edited — four `reachGap` codes live there (`BAD_LOCATOR`, `GATE_REFUSED`, `NO_ATTESTATION`,
  `NO_SUCH_CAPTURE`) and are left to the next item rather than reaching into an unclaimed file.
  QUEUE.md, DECISIONS.md, INTERFACES.md and IS-BUILD-PLAN.md are not written by this session.
released:

### DELEGATION 2026-08-08 RECORD (REC-64) -> WHOEVER NEXT OWNS THE DEC-49 GUARD: **ARM C IS BLIND TO EIGHT REFUSAL OBJECTS, AND REC-64'S OWN NEW SITE IS WHAT EXPOSED IT (raised as D-235)**

`check-refusal-codes.mjs` arm C collects refusals with `/\bok\s*:\s*false\b/`. **MEASURED over `bio-plane/src`: 704 `ok: false`, 5 `started: false`, 3 computed `ok: !<expr>`.** A codeless refusal in any of those eight passes the guard silently — which is precisely what arm C exists to fail on.

- **It was found by consequence, not by reading.** REC-64 gave `store.mjs aiRunOpen` a whole-function `where` and the guard reported it `92L (0 judged, 0 code(s) checked)`: read in full, asserting nothing, green. `aiRunOpen` refuses with `started: false`.
- **It has already cost a translation.** `selectionResolve`'s `SET_MOVED` refuses through `ok: !stopped`. A region `where` around it would judge zero refusals and FAIL as a drifted marker, so REC-64 could not govern it and the code is still in the reach gap. It is the one gap code REC-64 left for an instrument reason rather than a scoping one.
- **Not fixed here on purpose:** REC-64 claimed only that file's `FLOOR`/`CEILING` constant block. Widening the matcher is a change to VF-2's instrument.
- **What is owed:** the matcher widened to the measured set and driven in BOTH directions — a codeless `started: false` planted at a governed site must go RED, and the widening must not begin matching non-refusals. Then the newly-visible sites judged, since none has ever been.
- **Reversal cost:** none. The regex is one line and the control is the arm that proves it.

### DELEGATION 2026-08-08 RECORD (REC-64) -> SK-2 / FL-3 (the recipe layer): **REC-64 INVERTED `skillpack.test.mjs` ARM G1, AND THE ARM MUST BE PUT BACK WHEN YOU FILL THE RECIPES**

ARM G1 asserted `residentBytes < disclosedBytes` — §14b.1's context economy, the reason the pack has two layers. **REC-64 inverted it by doing what it was queued to do:** the eleven machine fences gained canned translations, and the boundary block is ALWAYS-RESIDENT by design. **Measured at REC-64: resident 9,294 bytes, disclosed 9,064.**

- **The inequality was a proxy and it expired for a reason ARM G3 already states: the RECIPE LAYER IS DECLARED EMPTY AND IS SK-2's.** The comparison today is a complete resident layer against a deliberately unfinished disclosed one.
- **The alternatives were both worse and are named so nobody re-litigates them:** deferring the fences would put the machine/member boundary behind a request, which §4 says it must not be; leaving eleven fences unexplained is the defect REC-64 exists to close.
- **What ARM G1 asserts now:** that the split BUYS something — a real deferred layer exists and the resident layer is not carrying its bodies. The reasoning and both byte figures are at the site.
- **What is owed: re-assert the strict inequality once recipes land**, and record the new ratio. If the recipe layer lands and the inequality still does not hold, that is a real finding about the pack's shape rather than about this arm.

### DELEGATION 2026-08-08 RECORD (REC-64) -> WHOEVER TAKES THE REST OF THE REACH GAP: **THE REMAINING 42 ARE NOT MORE OF THE SAME WORK, AND THE CONSTRAINT IS STRUCTURAL**

REC-64 took the `reachGap` ceiling from **73 to 42**. The 42 that remain do NOT yield to more translations, and this is the item's most useful output:

- **THE MULTI-SITE HALF.** A row holds ONE `where`; a `where` names THE SMALLEST SPAN in which the refusal is enforced; and one code may not hold two rows — arm A refuses a duplicated check number and a duplicated translation, and two rows for one code is two wordings for one condition, the drift the guard exists to stop. **So a code minted at many sites cannot honestly name one of them.** MEASURED: `NO_SUCH_BUNDLE` 15 sites, `NO_REASON` 12, `NO_ENTITY` 10, `NO_SUCH_PROJECT` 9, `NO_TARGET` 9, `NOT_A_PROJECT` 8, `ILLEGAL_TRANSITION` 8, `NOT_AN_INQUIRY` 7, `NO_SUCH_ENTITY` 7, `NO_SHA` 5, `NOT_THE_OWNER` 5, `EMPTY_SELECTION` 5.
- **The honest fixes are both bigger than a translation, and both are real options:** a `where` that can name a SET of spans, or those refusals consolidated behind one helper so there IS one site. The second is the better record — it is D-164's "solve it once" — and it is a change to the plane's shape.
- **THE `index.mjs` HALF, four codes:** `BAD_LOCATOR`, `GATE_REFUSED`, `NO_ATTESTATION`, `NO_SUCH_CAPTURE`, all inside the control plane's `fetch`. REC-64 did not claim `index.mjs` and did not reach into an unclaimed file for four codes. These are cheap for whoever holds it.
- **ONE code is blocked on the instrument, not on scope:** `SET_MOVED` — see the D-235 delegation above.
- **The reason is written at the ceiling itself**, not only here, because that is where the next reader meets the number.

### DECISION FOR BOB 2026-08-08 (REC-64) — **DEC-49's TRANSLATIONS ARE LOOKED UP AT RUNTIME, NOT BAKED AT BUILD**

- **What runs provisionally:** every refusal site keeps its code as a STRING LITERAL, and every consumer — the doctrine pack, the surfaces, `op=audit` — reads the canned translation out of `checks/bio-checks.mjs` at the moment it renders. Nothing is baked into a surface or a release artifact.
- **Why it was ambiguous:** you left it open explicitly — *"whether at development time or at runtime"* — and both work. A build-time bake renders faster and lets a surface hold the sentence with no plane round-trip.
- **The alternative:** emit the code-to-translation map into the surface bundle at build, the way the docprofile copy is embedded in `app.html` today.
- **Recommendation: runtime, as shipped, for two reasons that are not performance.** (i) A baked map is a COPY, and this project has measured five times that a copy agrees with its author at zero cost — including a complete hand copy of 131 op names that passed its own guard. (ii) A sovereign instance running an older surface against a newer plane would render the sentence frozen at ITS build, so two instances would tell a member different things about the same refusal. Runtime lookup makes the plane the single author of what it refuses, which is what DEC-8 protects and DEC-49 only narrowed.
- **What reversing it costs: LOW NOW, RISING WITH THE NUMBER OF SURFACES.** Today it is a build step plus one accessor, and the guard's arm D already proves a surface table TOTAL against its producer, so the machinery to keep a baked map honest exists. It gets expensive once more than one surface holds a copy, because then reversing means proving every copy total, and the drift REC-43 closed on the co-attestation fence is exactly what a second copy re-opens. **The decision point is before the second surface table, not before the next translation.** The reasoning is written at `MACHINE_FENCE_CHECKS`' header, where the next allocator reads it.

## CLAIM 2026-08-08 VERIFY
session: m0-10-migrate-concurrency (worktree agent-a0498732e9b265446)
opened: 2026-08-08T18:05:00Z
paths: **`bio-plane/test/migrate.test.mjs`** (the fixture ROOT only — `/tmp/civicos-fixture` becomes a per-process `mkdtemp` inside the sandbox `$TMPDIR`; no assertion, no fixture content and no exit path changed), **`bio-plane/test/bootstrap.test.mjs`** (the `defaultPersistRoot` line only — `/tmp/mfp` becomes a per-process directory inside the sandbox; persistence is KEPT because the suite's subject is a claim surviving an instance restart), **`bio-plane/test/hygiene.test.mjs`** (ONE assertion added inside the EXISTING D-186 sandbox block at ~line 114 — the containment half of a check that until now certified only the IMPORT; plus that block's `NEGATIVE CONTROL:` arms), **`docs/development/DEBT.md`** (D-235 NEW row, and D-186's disposition cell gaining the containment correction), **`docs/development/QUEUE.md`** (the M0-10 row's disposition ONLY), **`docs/development/CLAIMS.md`** (this entry). **NOT** `bio-plane/src/**`, **NOT** `checks/bio-checks.mjs`, **NOT** `scripts/battery.mjs`, **NOT** `scripts/control-register.mjs` (M0-14 holds it), **NOT** any other `.test.mjs`, **NOT** `civicos-ui/**`, **NOT** `pdf-worker/**`, **NOT** `newgroup/**`.
interfaces consumed: none — test estate, no plane behaviour.
interfaces owned: none.
expected: QUEUE.md M0-10. Name the CAUSE of `migrate.test.mjs`'s concurrency sensitivity rather than serialising the runner, and sweep the class.
accepts-when: as the M0-10 row. Baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief: **123 suites (120 plane + 3 fleet) · 122 green + 1 named skip (`pdf-worker`, D-232 — not this item's) · 7,659 assertions**, which AGREES with the brief. Battery run repeatedly AND concurrently with `node scripts/coverage.mjs --strict`, every run's count reported; `$?` unpiped on both; `node civicos-ui/test/run.mjs` from the repo root, exit unpiped; `node tools/plancheck.mjs` clean but for UNPUSHED. Negative controls RUN, each arm ALONE, restores verified by sha256 AND by `cmp` with uniquely-named copies, the harness written INSIDE this worktree (`m0-10-harness/`, untracked). No push, no merge, no deploy.
concurrency: checked over the whole file. No live claim names `migrate.test.mjs` or `bootstrap.test.mjs`. `hygiene.test.mjs` appears in two entries, both LANDED (M0-8's D-186 block at the top of this file, and PL-11's `aicredential` D-113 exemption block — merged on main); my edit is a NEW assertion inside M0-8's own sandbox block and touches neither the D-113 table walk nor any exemption row. `scripts/control-register.mjs` is M0-14's and is NOT touched; `scripts/battery.mjs` is deliberately NOT touched, because serialising or re-instrumenting the runner is the workaround this item exists to refuse.
released:

---

## CLAIM 2026-08-08 M0 (VERIFICATION / TEST ESTATE)
session: m0-12-agent
opened: 2026-08-08T00:00:00Z
paths: `bio-plane/scripts/op-claims.mjs` (NEW — the dispatch-table reader and the corpus walk); `bio-plane/test/op-claims.test.mjs` (NEW — the assertions, the reach delta and the corpus print); `bio-plane/test/op-claims.control.mjs` (NEW — the harness that RUNS the control arms; deliberately not a `.test.mjs` because it edits real files, `register.control.mjs`'s precedent); `bio-plane/scripts/coverage.mjs` **NOT touched**; comment-only corrections to `bio-plane/src/store.mjs`, `bio-plane/test/case-opened.test.mjs`, `bio-plane/test/meaning-bounds.test.mjs` where a comment names an op at the wrong level (hygiene, no behaviour — the files are byte-identical to HEAD after comment stripping); `docs/development/VERIFICATION.md` (what the new instrument can and cannot establish); `docs/development/MEASUREMENTS.md` (the corpus figures with date and instrument); `docs/development/DEBT.md` (the row for what is delegated). QUEUE.md NOT touched (CONDUCT's sole-writer file). NO plane behaviour, no `newgroup/**`, no `pdf-worker/**`, no `civicos-ui/**`, no `docs/development/research/**`.
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-12 — a comment naming `op=<name>` is a CLAIM about the dispatch table and nothing checked it. REC-58 burned a full item on one such sentence.

### DELEGATION 2026-08-08 M0 (M0-12) -> CONDUCT: **IC-22's CORRECTION IS ITSELF AT THE WRONG LEVEL, AND SO IS M0-12's OWN QUEUE ROW**

M0-12 built the mechanical check the row asked for and pointed it at the tree. **It fired on the correction REC-58 wrote, and on the row that queued M0-12.** (Every name below is written as a bare DO path on purpose: this file is inside the corpus the check sweeps, and prefixing them to quote the defect would BE the defect.)

- **`docs/development/INTERFACE-CHANGES.md`**, 3 sites. IC-22's struck sentence AND CONDUCT's appended CORRECTION both prefix `publishcase` as an op. **There is no such op.** `publishcase` is the STORE'S DO PATH; `DO_PATH` in `index.mjs` aliases `op=publish` onto it, so a caller sending the path name as `op=` gets `unknown op`. The true chain is **`op=publish` -> DO path `publishcase` -> `Store.publishCase()`**. The correction was right that the method computes no `opened`; it named the op one level down while doing so.
- **`docs/development/QUEUE.md`**, 7 sites — including **M0-12's own scope paragraph** and REC-58's landed row.
- **`INTERFACES.md`** 2, **`MEASUREMENTS.md`** 3, **`CLAIMS.md`** 10 (this file, append-only history).

**NOT CORRECTED BY M0-12, deliberately, and the reasoning is the same one REC-58 used when it routed this here rather than reaching for the IC:** amending a SETTLED IC is a protocol act with an owner, `QUEUE.md` is CONDUCT's sole-writer file, and `MEASUREMENTS.md` is a dated record of what was measured rather than a document that gets rewritten. **What M0-12 did instead is make it BOUNDED:** every site is registered in `LEDGER` in `bio-plane/scripts/op-claims.mjs` with an EXACT count, so it can only shrink, and a NEW one anywhere in the repository fails the battery naming the file, the line, the op and the op that actually routes there. Debt row **D-235**.

**The fix per site is one phrase** — write the DO path as a path, or name the op that reaches it (`op=publish`) — then drop the ledger entry; `node test/op-claims.test.mjs` prints the number to write.

### DELEGATION 2026-08-08 M0 (M0-12) -> RECORD / whoever owns `docs/development/research/`: **`DATA-MODEL.md` DOCUMENTS THE STORE'S ROUTE MAP WITH AN `op=` PREFIX THAT REACHES NONE OF IT — AND TWO OF ITS NAMES ARE IN NEITHER TABLE**

The largest single instance of the class in the tree: **27 registrations** in `research/DATA-MODEL.md`, plus 5 in `research/SB-EVIDENCE.md`. The "written by" / "read by" columns give the DO paths `recordlinks`, `siteassets`, `taskenqueue`, `governoradmit` and twenty more an `op=` prefix. Every one of those names is a DO PATH. `index.mjs`'s `OPS` is a strict whitelist and none of them is in it, so **none of those names is reachable as `op=` by any caller** — a reader taking the table at its word and probing the control plane gets `unknown op` for the whole column.

*(Written here as bare path names on purpose. This delegation is inside the corpus the check sweeps, and prefixing them to quote the defect would BE the defect — the same reason the instrument and its suite are both written around their own rule.)*

**And two are worse than mislevelled — they name neither an op nor a DO path**, which is the class that cost REC-58 an item rather than a rename:

- **`session` (`DATA-MODEL.md:128`)** — given as the reader of the `sessions` table, prefixed as an op. No such op and no such route. The session read is `op=whoami`.
- **`wake` (`DATA-MODEL.md:196`)** — given as a reader of `connection_dirty`, prefixed as an op. No such op and no such route.

**Neither was corrected here** — `docs/development/research/**` is not this item's path, and a research document's route inventory is its owner's to re-measure rather than mine to guess at. Both are ledgered as `NEVER` with an exact count, so they cannot spread, and **a `NEVER` entry FAILS the day its name appears in `OPS`** — the entry carries its own expiry.

### DELEGATION 2026-08-08 M0 (M0-12) -> UI: **`civicos-ui/app.html` STATES THE OPS/ROUTE-MAP GAP CORRECTLY AND WRITES IT IN THE WRONG GRAMMAR**

Two sites, one sentence (`app.html:5715-5717`), which prefixes `strength` and `basis` as ops and then says they exist in the store's route map but are absent from `index.mjs`'s OPS table, *"which is a strict whitelist"*. **The substance is right and it is a good sentence** — UI-35 measured a real gap and said so. The grammar is the M0-12 class: `strength` and `basis` are DO PATHS, so writing them as `op=` is the very conflation the sentence is warning about. Suggested: *"the store's route map carries `strength` and `basis`, and neither is in `index.mjs`'s OPS table, so no `op=` prefix reaches them."* Ledgered as `DO-PATH` x1 each; drop the two entries when the sentence changes.

### DELEGATION 2026-08-08 M0 (M0-12) -> CAPTURE: **`kickoffs/CAPTURE.md:132` NAMES A DO PATH AS AN OP**

`recordsourceoutcome`, prefixed as an op — it is a DO path (`recordSourceOutcome()`), reachable by no `op=`. One site, ledgered. Same one-phrase fix. The matching `QUEUE.md` row is delegated to CONDUCT above.

### DELEGATION 2026-08-08 M0 (M0-12) -> EVERY SESSION THAT WRITES ABOUT AN OP: **WHAT THE NEW CHECK DOES AND DOES NOT ESTABLISH**

`bio-plane/test/op-claims.test.mjs` runs in the battery on every `npm run test:battery`.

- **It asserts that every `op=<name>` token in the repository names a key of the `OPS` whitelist**, or is registered in `LEDGER` with a reason. `bio-plane/**` is gated at ZERO registrations of the mislevelled kind, so anything new there is a hard failure.
- **It separates the two levels**, which is the half that catches IC-22 — `DO_PATH` means an op's name and its DO path do not correspond, so a name-existence check alone would have passed the sentence that cost REC-58 an item.
- **IT DOES NOT CHECK WHAT AN OP RETURNS. Not one assertion reads a response shape.** IC-22's sentence was wrong twice and only the op half is mechanical: had it named `op=publish` — same falsehood about the field, correct op name — **every check here would pass it.** A field arriving through a SPREAD declares no key and no source-level instrument can see it.
- **It does not verify that a stated NON-existence is true**, except for `NEVER` ledger entries. The language-reading version was built, measured at 141 then 48 false positives on this corpus, and removed; `VERIFICATION.md` carries the four worked examples.
- **If you write a sentence about an op, write the op.** If you mean the Durable Object route, say *the DO path `x`*. The check now enforces the distinction and the estate has been made consistent with it.

released: 2026-08-08 — handed to CONDUCT. `bio-plane/**` corrected to zero mislevelled claims; everything outside it ledgered EXACTLY and delegated above. Six control arms run, each alone, **0 behaved other than declared**; every restore verified by sha256 AND by content compare, snapshots keyed by ARM as well as by path.

## CLAIM 2026-08-08 PLANE-TEST
session: m011-loose-branch
opened: 2026-08-08T00:00:00Z
paths: `bio-plane/test/publishedcase.test.mjs` (block 8, the loose-branch drive, and the two arms added to its `NEGATIVE CONTROL:` declaration), `bio-plane/scripts/coverage.mjs` (`REGISTER_FLOOR.arms` ONLY — moved from the figure a green run PRINTED), `docs/development/CLAIMS.md` (this entry and its DELEGATIONs).
**NOT OPENED:** nothing under `bio-plane/src/`, `bio-plane/checks/`, `bio-plane/schema.mjs`, `newgroup/`, `release/`, or `civicos-ui/**`. `bio-plane/src/store.mjs` and `src/index.mjs` are READ ONLY here and are read TEXTUALLY by block 7 exactly as they already were; the negative control mutates `store.mjs` on disk one arm at a time and restores it against a pristine pre-arm copy kept in `.m011/` inside this worktree, verified by `cmp` AND by sha256 after every arm, with the copies named UNIQUELY PER ARM.
interfaces consumed: I3 (asserted against, never changed). interfaces owned: none. No op added, no check minted, no schema touched, no published field added or removed.
expected: **M0-11** — `op=publishedcase`'s `#looseEditionState` branch (ratified bytes belonging to no case) is DRIVEN end to end by the plane's own battery and its answer asserted, including that its top-level key set is the SAME single success return the case path uses. Plus the class SWEEP: how much of the plane's contract is pinned only by `civicos-ui`.
concurrency: no live claim names `bio-plane/test/publishedcase.test.mjs` or `bio-plane/scripts/coverage.mjs`.
released:

### DELEGATION 2026-08-08 PLANE-TEST (M0-11) -> WHOEVER OWNS `bio-plane/src/store.mjs` BEHAVIOUR: **THE LOOSE BRANCH COMPUTES A SENTENCE SAYING "THIS IS NOT A CASE" AND PUBLISHES A PARAGRAPH SAYING WHAT A CASE IS**

Measured by driving `op=publishedcase` under Miniflare against ratified bytes in no case, not by reading source. The branch is **not broken** — it answers, and its answer is the same single success return the case path uses, which is what M0-11 asked for. What is wrong is which prose reaches the caller.

- **`#looseEditionState` composes `detail`** — *"this is a RATIFIED BUNDLE that is not a member of any published case: … no case identity, no scope statement, no completeness assertion and no bias acknowledgement — those are claims a CASE makes, and this is not one"* — and `publishedCase()`'s success return **picks its fields by name and never spreads the state, so that sentence reaches NO caller.** The same is true of two more fields the branch computes: `caseId` (the return uses the local `theCase`, which this path has just set to null) and `opened` (already established by UI-40/REC-58).
- **What the answer DOES carry on this branch is `case_detail`**, byte-identical to the case path's: *"a published case is a CONTAINER over one or more FINDINGS… the case carries the scope that brought them together, the completeness assertion, and the group's acknowledgement of the bias…"* — beside `caseId: null`, `scope: null`, `completeness: null`, `bias_acknowledgement: null`. **So the one branch where the plane deliberately refuses to manufacture case-ness is the branch whose only prose describes a case**, and the sentence that says it is not one is dropped on the floor.
- **This is not fixed here.** Publishing a new top-level key on a public I3 op is an interface change and plane behaviour; M0-11 is the test estate. Block 8 of `publishedcase.test.mjs` asserts the CURRENT answer in **both directions** — `"detail" in c` is false on both branches, and `case_detail`/`graph_detail` are identical on both — because the two-branches-one-return property is the op's contract. **If this is fixed, that assertion goes RED and somebody decides deliberately**, which is what a superseded assertion is for.
- **Recommended:** publish the loose state's own account under a THIRD name (`loose_detail`, or `state_detail` carried by both branches), additive, so no existing consumer moves. **Alternative:** leave it, and accept that the plane has a written sentence for this state that no reader can ever see. **Reversal cost of doing it: one key, additive, and an IC.**
- **The consequence that makes it worth doing:** the surface currently HAND-WRITES its own version of that sentence — `civicos-ui/app.html` renders *"These bytes are not a member of any published case"* in the surface's words (pinned at `civicos-ui/test/publishedcase.test.mjs:1433`). That is DEC-49's subject exactly: a pre-authentication surface authoring a claim about the record because the plane's own sentence is not on the wire.

### DELEGATION 2026-08-08 PLANE-TEST (M0-11) -> WHOEVER OWNS `civicos-ui/test/publishedcase.test.mjs` AND `civicos-ui/app.html`: **THE `LOOSE` FIXTURE INVENTS A FIELD THE WIRE DOES NOT SEND, FOR THE SECOND TIME, AND THE SURFACE RENDERS A CLAIM ON IT THAT CANNOT BE TRUE**

UI-35 removed an invented top-level `detail` from this same fixture on 2026-08-05. **There is another one, one level down, and it is load-bearing.**

- **`edition_index` rows differ between the two branches of `op=publishedcase`, MEASURED end to end.** The case path selects `edition, ratified_at, manifest_sha FROM published_cases`; the loose path selects `edition, ratified_at FROM published_bundles`. **A loose edition row therefore carries NO `manifest_sha` KEY AT ALL.** `civicos-ui/test/publishedcase.test.mjs:479` states `edition_index:[ { edition:1, ratified_at:"2026-06-01T00:00:00Z", manifest_sha:null } ]` — the mock answering a field rather than the wire's content, D-173's class.
- **The consequence is not cosmetic.** `civicos-ui/app.html:15637` renders, per edition row, `e.manifest_sha ? "container sha256:" + … : "no container assembled for this edition yet"`. On the loose branch the key is absent, so a reader of ratified bytes that belong to no case is told **"no container assembled for this edition yet"** — and no container will EVER be assembled for it, because a non-case ratification has none by design (DEC-44, and `op=ratify` answers `container: null` for it, asserted in block 8). **"Yet" is a claim about the future that the record cannot support.** The awaiting-CASE fixture at line 455 legitimately carries `manifest_sha: null`, and it is the one the sentence was written for; the invented field made one sentence look right for two different states.
- **Three smaller wire-vs-mock divergences in the same fixture, all measured:** the wire answers `body.question`/`conclusion`/`falsifies`/`excludes` as **`null`** for an information document (there are no such sections in the bytes) where the fixture states `""`; and `body.detail` is the full *"`authored` is what op=conclude wrote…"* sentence on the wire where the fixture states `""`. None of these changes a rendering today (both are falsy), which is exactly why they will not be found by anything but a comparison against the wire.
- **Not fixed here: `civicos-ui/**` is not this claim's path.** Reversal cost: one fixture field and one sentence.

### DELEGATION 2026-08-08 PLANE-TEST (M0-11) -> CONDUCT: **THE CLASS SWEEP — FIVE MORE PUBLISHED FIELDS PINNED ONLY BY `civicos-ui`, AND THE OP-LEVEL ANSWER IS ZERO**

The sweep ran over **120 plane suites / 3,533,380 chars and 45 `civicos-ui` test files / 1,664,855 chars, against the 158 ops declared in `index.mjs`**, using `coverage.mjs`'s OWN call-shaped matcher so the walk and the gate agree on what "drives an op" means.

- **At OP altitude the gap is ZERO.** 83 of 158 ops are named by a `civicos-ui` file; **every one of them is reached by the plane's own battery through the control plane**, and none is Durable-Object-only. So the answer to "which ops does the plane's contract depend on the surface for" is *none* — which is worth stating, because it means the whole exposure is **sub-op**, exactly where `scripts/coverage.mjs` cannot look.
- **At FIELD altitude there are five genuine ones besides `graph_detail` (which M0-11 closed).** A key the `civicos-ui` fixtures state, that the plane's SOURCE emits as a response key, that **no plane suite mentions anywhere**:
  - **`counterparty_state`** — `Store.#actionDerived()`, on the `action` object of every action read. Consumed live at `civicos-ui/app.html:6648` (`der.counterparty_state`), pinned only by `civicos-ui/test/action-page.test.mjs:363`. No plane suite names it (they name the COLUMN, `action_counterparty_state`, which is a different string).
  - **`target_bundle`, `target_retrieved`, `target_last_seen`, `target_captures`** — `Store.resolveLinks()`, reached through the DO path `resolvelinks` and NOT through any op — **CORRECTED AT INTEGRATION 2026-08-08 by CONDUCT, and M0-12's own new check is what caught it, hours after M0-12 landed.** M0-11 wrote it with an `op=` prefix; there is no such op, and the distinction is precisely M0-12's subject: a DO path is not an op name, and prose that conflates them sends the next reader looking for a door that does not exist. `bio-plane/test/subresources.test.mjs` DRIVES the op and asserts `resolution`, `verdict`, `basis`, `target_capture` and the `verdicts` tally — **and not these four.** They are pinned only by `civicos-ui/test/link-surface.test.mjs:117-125`. **And this correction had to be written TWICE: its first draft QUOTED the bad token while correcting it, and the check failed again naming the correction itself — which is exactly what M0-12 reported doing to its own ledger while writing the delegations that tell others not to. A check whose subject is a token in prose will catch the prose that discusses the token, and the honest fix is to describe the token rather than spell it.**
- **WHAT THE MATCHER CANNOT SEE, stated here rather than discovered later.** (1) It is a **key-literal** walk: a field that arrives through a SPREAD declares no key and is invisible — which is the same limit M0-12 names, and it is the limit that hid `#looseEditionState` in the first place. (2) It cannot tell a response key from an option-bag key or a comment: it reported **`sentinels`** as a hit, which is a ZIP64 comment in `ooxml.mjs` and a log label in a UI harness — one false positive in seven, named so the figure is read as a FLOOR with noise rather than a census. (3) It is **field-level, not branch-level**: a branch that answers with the same keys and different VALUES — which is precisely what `#looseEditionState` does — is invisible to it. **No instrument in this repository sees op BRANCHES**, and the honest way to find the rest of this class is what M0-11 did: drive the op and read the answer.
- **Recommended:** one queue item per op family (the action read, whose fields come through the DO path `resolvelinks` rather than through an op — see the correction above), each in the plane's own battery, each driven through the op. Not folded into M0-11: different ops, and this claim's diff is deliberately tight.

### DELEGATION 2026-08-08 PLANE-TEST (M0-11) -> WHOEVER OWNS `bio-plane/test/case-opened.test.mjs` (REC-58's INSTRUMENT): **ITS ANCHOR IS ORDER-SENSITIVE, AND THE FAILURE DIRECTION IS THE SAFE ONE**

M0-11's over-strictness arm (i) **reordered two published keys** in `publishedCase()`'s success return — `bias_acknowledgement` ahead of `caseId`, same key set, same values, **not one byte different on the wire**. Block 8 stayed at 100 pass (it compares the key set SORTED). `case-opened.test.mjs` went **25 pass / 3 FAIL**, its ANCHOR arm among them, and the UI's mock-envelope check went red with it.

- **This is reported, not filed as a defect.** The anchor reports the return as NOT FOUND rather than passing on an empty slice, which is the correct direction and is REC-30's own instrument lesson. But it does mean **the first two keys of that return are frozen by a suite whose subject is a different field**, and an author reordering for readability meets three red assertions with no hint that ordering is what did it.
- **What is owed if REC-58's owner agrees:** anchor on the method rather than on the return's opening literal, or say at the site that the order is load-bearing. **Reversal cost: none — nothing is red today.**

### DELEGATION 2026-08-08 PLANE-TEST (M0-11) -> CONDUCT / WHOEVER OWNS `VERIFICATION.md`: **THE NEGATIVE-CONTROL ROW IS STALE AGAIN — 471 IN THE FILE, 476 PRINTED**

`VERIFICATION.md`'s floor table carries `471 arms`, moved there at integration on 2026-08-08. `node bio-plane/scripts/coverage.mjs --strict` **printed `120 of 120 suites declare one (100.0%) · 476 arms stated across 119 classified declaration(s) · fullest 29 (hygiene.test.mjs) · 1 UNCLASSIFIED`** on a green run of this worktree, exit 0 read unpiped.

- **Five of the five new arms are M0-11's own** (block 8's (e)..(i) in `publishedcase.test.mjs`), so unlike the last five reports of this row, this one IS attributable — but the row belongs to whoever integrates, because several workers are running and two items moving one shared figure in parallel is a merge CONDUCT has to resolve for no gain.
- **`REGISTER_FLOOR.arms` in `scripts/coverage.mjs` HAS been moved 471 -> 476 in this claim**, from the printed figure, because that one is the GATE and leaving it low is slack in a ratchet. The document row is the copy, not the gate.
- **This is the sixth consecutive item to find that row stale by measuring it.** The row's own subject, answered in practice.

## CLAIM 2026-08-08 RECORD (REC-72)
session: worker-rec72 (worktree `agent-ac01538437c27982e`)
opened: 2026-08-08
paths:
  - `bio-plane/src/store.mjs` — TWO refusal predicates and nothing else: `cite`'s member
    citability test (the case arm) and `#edgeTransition`'s member test (the one helper behind
    `op=sever` AND `op=reinstate`). No new method, no schema, no new op, no region marker moved.
  - `bio-plane/src/affordances.mjs` — the `sever` / `reinstate` ACTS rows only (their `types`
    and `applies`), plus the reasoning comment on the `cite` row.
  - `bio-plane/test/citeproject-inquiry.test.mjs` (NEW suite — the act driven through the
    CONTROL PLANE and read back through `op=backlinks`, never through the op that wrote it)
  - `bio-plane/test/citeinquiry.test.mjs` — §8's two arms are SUPERSEDED by this item and are
    CORRECTED at the site with the reason, never exempted.
  - `bio-plane/test/d216-sharing.probe.mjs` — the instrument that MEASURED this gap; its
    "no curated producer" arm is corrected to record that the gap is closed, because a probe
    still asserting the old refusal is an instrument lying about its own subject.
  - `bio-plane/test/curated-producer.probe.mjs` (NEW — the CLASS SWEEP, a probe and not a suite so
    the battery's delta stays honest; it MEASURES the estate rather than pinning it)
  - `bio-plane/test/nc-rec72.mjs` (this item's negative-control harness, INSIDE this worktree)
  - `bio-plane/scripts/coverage.mjs` — **the `REGISTER_FLOOR` constant block ONLY**, moved to the
    figures `--strict` PRINTED on a green run of this branch (471 -> 482 arms, 119 -> 121
    classified, 120 -> 122 corpus), which is CPDF-9's precedent for moving a floor with the change
    that raised it. Nothing else in that file is touched.
  - ADDED MID-ITEM, and each because the change REQUIRED it rather than because it was convenient:
    `bio-plane/test/affordances.test.mjs` and `bio-plane/test/repair-reachability.test.mjs` both call
    the exported `deriveActs` with HAND-BUILT facts objects, and both had to learn the fact
    `sever`/`reinstate` are now derived over. `repair-reachability` also gained an assertion that its
    own `actsAt` oracle did not SWALLOW a throw — it had, and reported six offenders that do not
    exist.
interfaces consumed: I3 (the op contracts), I5 (read only — no schema change)
interfaces owned: none
expected: REC-72 — the project-to-inquiry `cites` edge gains a CURATED PRODUCER (`op=cite`) and a
  curated WITHDRAWAL (`op=sever`), so the edge D-216 proved the sharing model rests on stops being
  reachable only by hand-authoring `bundle.md`.
accepts-when: as QUEUE.md REC-72, plus: **battery baseline MEASURED IN THIS WORKTREE BEFORE ANY
  EDIT and trusted over the brief** (the worktree arrived with no `bio-plane/node_modules`; `npm ci`
  run, exit read UNPIPED; baseline **124/124 green · 7,811 assertions**, against a brief that said
  ~7,815). The edge driven by an ACT and read back through a DIFFERENT op; the withdrawal likewise
  and asserted RECORDED rather than absent. Negative controls RUN, each arm ALONE with the others
  held open, declared before arming, restores verified by sha256 AND by `cmp` against a pristine
  per-arm copy. No push, no merge.
concurrency: `store.mjs` is contended. This item's diff there is TWO predicate expressions and their
  adjacent `detail` strings — a hunk conflict at worst and never a semantic one. `index.mjs` is NOT
  claimed and NOT edited: no op is added, no dispatch entry moves. `civicos-ui/**` is NOT edited —
  `cite-act.test.mjs` mocks the store's refusal words and a mock that now understates the plane is
  DELEGATED below rather than reached into. QUEUE.md, DECISIONS.md and INTERFACES.md are not
  written by this session.
released:

### DELEGATION 2026-08-08 RECORD (REC-72) -> WHOEVER TAKES THE BASIS ALTITUDE: **A QUESTION CAN JOIN WHAT IT RESTS ON AND CANNOT LEAVE IT — REC-72'S EXACT SHAPE, ONE ALTITUDE UP, AND STILL OPEN**

REC-72's class sweep found it and **DROVE it** (`node bio-plane/test/curated-producer.probe.mjs`, finding 2). It is the sweep's headline and it is bigger than any of the four authored-only relations below.

- **MEASURED, not read.** `op=cite`'s inquiry arm composes a `basis[]` leg. `op=sever` on that question answers **`{ok:false, reason:"NOT_A_PROJECT", got:"inquiry"}`**, and `op=reinstate` answers identically — so the gap is the whole edge-transition family and not one verb. `#spliceBasis` has **exactly one caller** in the plane, which is `cite`. **A leg has a producer and no withdrawal.**
- **Why it matters exactly as much as REC-72 did:** *correction moves FORWARD* (DEC-19), and an unrecorded withdrawal is indistinguishable from never having rested on it. A question whose basis can only grow is a question whose record overclaims by construction.
- **THE MODEL IS ALREADY THERE — this is a missing ACT, not a redesign, and that is the difference between an item and a rewrite.** `#restsOnLive` already reads the citing document's `references[]` and treats a **severed** reference as a leg that is not live (`entry.status === "severed"`, asserted by the probe). So the record already knows how to represent a withdrawn leg. Nothing can put one there.
- **What the item has to decide, and it is a real judgement rather than a widening:** `#edgeTransition` promotes with `object_type: "project"` HARD-CODED in its `meta`, so admitting a question means the meta becomes arm-conditional the way `cite`'s write already is. And the doctrine question underneath it: does severing the REFERENCE withdraw the LEG, or does a leg need its own withdrawal with its own reason? `basis ⊆ references` says the first; `ord`-addressable legs (D4 — one document cited for two reasons) says a member may want to drop one leg and keep the other.
- **Do NOT solve it by widening `#edgeTransition`'s citing-object check alone.** That would make severing one reference silently drop EVERY leg on that target, which is the set-narrowing this act family refuses everywhere else.

### DELEGATION 2026-08-08 RECORD (REC-72) -> WHOEVER OWNS `affordances.mjs`: **`ACTS[].types` GATES NOTHING, AND A NEGATIVE CONTROL IS THE ONLY REASON ANYONE KNOWS**

- **MEASURED:** `deriveActs` is `ACTS.filter((a) => a.applies(facts, ty))`. **`types` is never consulted.** REC-72's control arm (e) dropped `inquiry` from `sever`/`reinstate`'s `types` and the suite came back **35 pass, 0 fail** against a declared MUST-FAIL.
- **It is NOT inert, which is what makes it dangerous.** `index.mjs:2750` publishes it as **`appliesTo`** in the no-target catalog — the shape a surface loads ONCE to know which objects an act can ever apply to. A surface told `appliesTo: [information, project]` never offers the withdrawal on a question, whatever the per-object answer says.
- **So `types` and `applies` are two statements of one rule that can disagree in total silence.** REC-72 asserted the catalog in its own suite, which closes it for these three acts and for nothing else.
- **What is owed:** a totality arm over the WHOLE catalogue — for every act, every type its `applies` can return true for is in its `types`, and nothing else is. It is the same class as the D-173 mock/plane seam and the same class as `NON_ACTS`' own totality check, which already exists one field over.

### DELEGATION 2026-08-08 RECORD (REC-72) -> UI: **`civicos-ui/test/cite-act.test.mjs`'s MOCK NOW UNDERSTATES THE PLANE, AND THE UI HARNESS IS GREEN ANYWAY**

- `cite-act.test.mjs` MOCKS the store (`return W(refuse(onto ? "NOT_CITABLE" : "NOT_INFORMATION", …))`) and its mock refuses **every** non-Information member on the case arm. **The plane no longer does:** since REC-72 a case may cite a QUESTION. `node civicos-ui/test/run.mjs` is **exit 0** and REC-72 did not edit `civicos-ui/**`.
- **The refusal WORDS did not move** — `NOT_INFORMATION` keeps its name and its site — so nothing renders wrongly today. What has moved is the mock's admitted set, and a mock narrower than the plane is the D-173 seam this area has already paid for twice.
- **What is owed:** the mock widened to `information | inquiry` on the case arm, and — the part that is actually worth the turn — the cite flow OFFERING a question as a citable member, because the act now exists and a surface that cannot reach it leaves REC-72 unreachable to a human. `op=affordances` (no target) publishes `appliesTo` for `cite`, `sever` and `reinstate` including `inquiry`; the plane-side refusal carries `citable: ["information","inquiry"]`, so the surface needs no copy of the vocabulary.

### DELEGATION 2026-08-08 RECORD (REC-72) -> CONDUCT: **FOUR RELATIONS IN THE CLOSED VOCABULARY HAVE NO CURATED PRODUCER, AND THE QUESTION IS WHETHER THEY SHOULD**

Printed by `node bio-plane/test/curated-producer.probe.mjs` over `REL_VOCAB` read out of `checks/bio-checks.mjs` (9 relations, 5 source files, 1,963,450 bytes walked, comments blanked before matching):

- **WITH a producer:** `cites` (`op=cite`/`op=sever`/`op=reinstate`, and `op=inquirydivide` for a child's apportioned legs) · `derived_from` (fork) · `supersedes` (`op=inquirydivide`) · `responds_to` (`op=actioncorrespond`) · `links_to` (`op=linkproject`).
- **WITHOUT one — authored-only today:** **`relates_to`, `elevated_into`, `initiates`, `corroborates`.** Each is in the closed vocabulary, each passes C-6.1, each is walked by `op=backlinks`, and **none can be created by any act.**
- **This is a QUESTION and not a finding to act on blindly.** `elevated_into` looks legacy (C-6.3's old arm was REPLACED by REC-11). `corroborates` is the one that reads load-bearing for the evidence story. Whether an authored-only relation is a gap or a deliberate choice is doctrine, and REC-72 did not rule on it.
- **`links_to` IS AN ANOMALY OF ITS OWN, reported rather than fixed:** it is the only relation written by an **`INSERT INTO refs`** and never through a document. Everything else in this family is a projection re-derived from `bundle.md` inside promote's transaction (D-21). A `refs` row with no bytes behind it is a claim the record cannot show you the authoring of.
- **WHAT THE SWEEP'S MATCHER CANNOT SEE, stated in the probe's own header and repeated here:** it knows two write shapes (an object literal carrying `rel: "<x>"`, and the frontmatter line the store composes inside a template literal) plus the `INSERT INTO refs` form; a relation built by string concatenation across lines, or from a computed value, is invisible to it. It does not know whether a producer is REACHABLE by a member — only the driven half answers that, and it is driven for two relations, not nine. It does not rule on whether an edge SHOULD have a producer. Its comment-blanking is naive about a `//` inside a string literal, and that error runs toward reporting a gap that is not one rather than hiding one.

### DECISION FOR BOB 2026-08-08 (REC-72) — **A CASE DRAWING ON A QUESTION IS THE SAME ACT AS CITING A DOCUMENT, SO `op=cite` WAS WIDENED RATHER THAN A NEW ACT ADDED**

- **What is running NOW, provisionally:** `op=cite` accepts a QUESTION as a member on the case arm, and `op=sever`/`op=reinstate` accept it too. A case can take up a question and put it down, with a reason, through the acts it already has. The refusal codes did NOT move: a non-citable member is still `NOT_INFORMATION` on the case arm, with its sentence rewritten to say what is actually refused and carrying `citable: ["information","inquiry"]` so a surface never needs a copy of the vocabulary.
- **Why it was genuinely ambiguous:** DEC-23 says CONTENT is a reference to a part of a document up to the whole document — **and a question is not a document.** So "a case cites a question" is either the same act at a different altitude, or a different relationship wearing the citation's name.
- **The alternative, stated fairly:** a NEW act — a hypothetical new act (named in prose only — deliberately NOT written with an `op=` prefix, because M0-12's check reads such a token as a CLAIM about the dispatch table, and REC-72's alternative was considered and REJECTED, so an op by that name must never appear to exist) or similar — with its own control, its own affordance and its own refusals, saying explicitly that a case's interest in a QUESTION is a different relationship from its reliance on MATERIAL. That reading is respectable: a case rests on documents as evidence, and stands beside a question as an inquiry it shares. Those are arguably not one thing.
- **RECOMMENDATION: keep the widening.** Three measured reasons, not one aesthetic one. (i) **The record already admitted the shape.** `op=promote` has always accepted a case whose `references[]` cites a question — C-6.1 judges the relation vocabulary and the id grammar, C-6.2 only that the target resolves, and neither reads the target's type; `op=backlinks` has always walked it; D-216 drove that route end to end. The fence was on the ACT alone, so widening created no new shape in the record, and a new act would have created a second name for a row that already exists. (ii) **REC-37 set this precedent at this exact site**, when it widened the CITING side to a question rather than minting a second op, and its argument holds unchanged: it is one act in the record's own terms — *this is why I think that* — and WHERE the record keeps it differs while WHAT the member did does not. (iii) **A second act would need a second withdrawal**, and the asymmetry this item exists to prevent gets easier to reintroduce with every duplicated verb.
- **What reversing it costs: LOW NOW, AND IT RISES ONCE MEMBERS USE IT.** Today: one predicate in `cite`, one in `#edgeTransition`, two affordance rows, and the suites that pin them. Once cases have drawn on questions, reversing means deciding what happens to the edges that exist — and because a severed edge is a recorded judgement rather than a deletion, they cannot simply be dropped. **The decision point is before members start using it, not before the next act is added.**
- **The smaller judgement inside it, stated so it is not smuggled:** the wire string `NOT_INFORMATION` now names a predicate that admits inquiries, which is a code slightly wider than its own name. It was kept because renaming a reason code is a BREAK by this repository's own definition (I3 2.0.0 recorded exactly that for `NOT_PROBLEMS`), it was measured to buy nothing (no consumer matches the literal anywhere in `civicos-ui` — the occurrences are prose and a mock's table), and what a member reads is `detail`. **If you would rather one code for what is now one predicate, it is one string per arm plus the suites that pin them, and it is cheapest today.**

## CLAIM 2026-08-08 RECORD (REC-66)
session: worker-rec-66
opened: 2026-08-08T00:00:00Z
paths: the `deriveConnections` / `#deriveConnectionsSweep` / `#MEANING_LIMIT_*` spans and the `connect:` dispatch entry in bio-plane/src/store.mjs; bio-plane/test/derivation-bounds.test.mjs (new suite); bio-plane/test/connections-growth.measure.mjs (new instrument, not a suite); bio-plane/test/nc-rec66.mjs (this item's negative-control harness, INSIDE this worktree); the RATCHET constants and the `op=connect` arms in bio-plane/test/meaning-bounds.test.mjs; the `OPS.size` pin and the `op=connect` descriptor in bio-plane/test/bounds.test.mjs; docs/development/MEASUREMENTS.md (D-224's measurement), docs/development/DEBT.md (D-224 / D-227 dispositions and one new row), docs/development/INTERFACE-CHANGES.md (IC-35), docs/development/CLAIMS.md
interfaces consumed: I3, I5
interfaces owned: none
expected: REC-66 — `op=connect` (`deriveConnections`) reads the entity's resolutions UNBOUNDED and emits every derived pair, k(k-1)/2, so the DERIVATION is unbounded and not merely the response. The bound goes where the WORK happens: the scan is bounded by DOCUMENTS and the document bound is the inverse of the quadratic taken from the pair bound, so a naive answer-only cap is distinguishable from the fix. Plus the class sweep — which other reads DERIVE over an unbounded scan rather than merely returning an unbounded result — as its own walk with its corpus printed and its reach asserted.
concurrency: several workers are in `store.mjs`. This item's plane diff is ONE method, two constants and one dispatch line. `civicos-ui/**` is NOT claimed and NOT edited — the receipt `connectGo()` renders is delegated to UI below. `newgroup/**` and `pdf-worker/**` are out of bounds. QUEUE.md is CONDUCT's and is not written here.
released: 2026-08-08 — committed on `worktree-agent-a6c072dccb45cdaae`, NOT pushed and NOT merged; CONDUCT integrates. **Battery 125/125 green, 7,863 assertions** against a true baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT of **124/124 · 7,811** (the brief said ~7,815 — the tenth consecutive item to find its brief's figure stale by measuring). Delta +52, attributed per suite by diffing the two runs: `derivation-bounds.test.mjs` +42 (new), `bounds` 147 -> 151, `meaning-bounds` 83 -> 85, `hygiene` 504 -> 507, `planning-hygiene` 271 -> 272. `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` unpiped, **exit 0**; `node civicos-ui/test/run.mjs` from the repo root, exit unpiped, **0**; `node civicos-ui/check-refusal-codes.mjs --strict` **exit 0 with every floor BYTE-IDENTICAL to the pre-edit run** (no refusal code added, no governed DEC-49 region touched). Instrument figures MOVED from what each PRINTED: `bounds` capped roster 25 -> 26, `meaning-bounds` bare ratchet 39 -> 38 (ceiling AND floor, one edit). `REGISTER_FLOOR` deliberately NOT moved — it now reads `arms 482/471 · classified 121/119 · corpus 122/120`, i.e. GREW; UI-48 and CPDF-9 made the same choice for the same reason, and the figures are delegated to CONDUCT below. Six control arms run, each ALONE; **one came back other than declared and it found the INSTRUMENT wrong rather than the subject** (see the delegation below).

### DELEGATION 2026-08-08 RECORD (REC-66) -> UI: **`op=connect`'s RECEIPT CAN NOW BE CUT, AND THE SURFACE STILL SAYS "THE RECORD DERIVED N CONNECTIONS"**

`connectGo()` in `civicos-ui/app.html` (~12,258) renders *"The record derived N connections among the documents that concern this subject."* **For a subject with more than 32 documents that sentence is now WRONG in the one way this project cares about: it reads as a statement about ALL the documents when it is a statement about the first 32.**

- **The plane already publishes everything the sentence needs**, in UI-48's own idiom: `limit` (pairs, applied), `document_limit` (documents the derivation was allowed to read), `documents` (documents it DID read), `resolution_rows`, `truncated`. IC-35 is filed.
- **Nothing the surface reads today has moved** — `count` and `connections` are unchanged — so this is additive wording, not a fix for a break. `node civicos-ui/test/run.mjs` is green as it stands (measured, exit 0, and the op probe exercises `connect`).
- **Suggested shape, and it is yours to reject:** when `truncated`, say *"among the first 32 of the 40 documents that concern this subject"* and offer the wider bound, since the derivation is RESUMABLE — asking again with a larger `limit` derives the rest and loses nothing.
- **Reversal cost: none.** It is one sentence in one receipt.

### DELEGATION 2026-08-08 RECORD (REC-66) -> WHOEVER NEXT OWNS `bounds.test.mjs` / `meaning-bounds.test.mjs`: **BOTH SUITES STAYED FULLY GREEN WITH THE DERIVATION UNBOUNDED — TWICE, MEASURED**

REC-66's negative controls armed the plane in two different ways and ran all three bounds suites under each. **Under (1) the unbounded derivation restored and under (2) the naive answer-only fix, `bounds.test.mjs` reported 151/0 and `meaning-bounds.test.mjs` 85/0 — green, both arms, both suites.** Only `derivation-bounds.test.mjs` went red (29/13 and 33/9).

- **This is D-227's claim re-measured on a THIRD op and it held exactly.** The published envelope is honest over an unbounded scan; a walk that grades what a method PUBLISHES cannot see the work it DID.
- **It is not a defect in either suite** — they answer different questions, and both now name `op=connect` in their own terms (the capped roster 25 -> 26, the bare ratchet 39 -> 38).
- **What is owed, and D-227's row now says so:** REC-60's three reads are pinned by TEXT only (`op=airunlog` gained a text pin at REC-70; the three have none). A text pin dies the day the SQL is reworded. The LIVE shape is in `derivation-bounds.test.mjs`: drive the store past the ceiling and assert the answer's own count of rows the scan RETURNED.

### DELEGATION 2026-08-08 RECORD (REC-66) -> M0-14 / WHOEVER OWNS `VERIFICATION.md`'s REGISTER ROW: **THE PRINTED FIGURE MOVED AGAIN, AND THIS ITEM DELIBERATELY DID NOT EDIT THE ROW**

`node scripts/coverage.mjs --strict` in this worktree PRINTS `REGISTER FLOOR arms 482/471 · classified 121/119 · corpus (suites read) 122/120 · GREW by 11 arm(s)`. The row in `VERIFICATION.md` says the current printed figure is 471 arms over 120 suites.

- **The +11 arms and +1 suite are this item's** (`derivation-bounds.test.mjs`, six declared arms, plus the arms added to the two sibling declarations).
- **The row was NOT edited and the floor was NOT raised, on purpose**, and it is the same judgement UI-48 and CPDF-9 recorded: several workers are running, each grows this number, and two items moving one shared floor in parallel is how it went stale four times in one day. **A floor that is exceeded gates correctly; only CONDUCT sees the merged tree.**
- **What is owed: CONDUCT re-reads the printed figures on the merged tree and moves both the row and `REGISTER_FLOOR` once**, from what the instrument prints rather than by adding up the workers' claims.

### DECISION FOR BOB 2026-08-08 (REC-66) — **A DERIVATION TOO LARGE FOR ITS BOUND IS CUT AND SAYS SO, RATHER THAN REFUSED**

- **What runs provisionally:** `op=connect` derives the connections among the first N documents concerning a subject (N = 32 by default, 100 at the ceiling, derived from the pair bound), persists them, and publishes `truncated: true` with the bound it applied and the documents it read. Every row written is TRUE; what is missing is pairs, not accuracy.
- **Why it was ambiguous, and it is a real fork rather than an implementation detail.** The plane's own landed convention for a WRITE over its bound is to REFUSE — `op=suggest` turns away a submission naming the cap, and `bounds.test.mjs` records the reasoning: *"a write over its cap is turned away naming the bound, so the caller knows exactly what did not happen."* By that rule `op=connect` should refuse a subject with 1,000 documents rather than connect 100 of them.
- **The alternative:** refuse with a DEC-49 code (`CONNECT_TOO_MANY_DOCUMENTS`) naming the bound and the count, deriving nothing.
- **Recommendation: cut and state it, as shipped, for three reasons that are about the RECORD rather than about convenience.** (i) **The refusal falls hardest on the most important subject** — D-224's own point is that k is largest exactly where a group's whole project is — so a refusing `op=connect` would answer "no connections" for the one subject a member most needs them for, while a bounded one answers with 4,950 true connections. (ii) **The alarm-driven sweep would refuse the same entity every tick, forever**, since `#deriveConnectionsSweep` clears the dirty flag and moves on; a permanent silent no-op inside a background loop is worse than a stated partial. (iii) **It is resumable and it is additive**: asking again with a wider bound derives the rest, and nothing has to be undone. `op=suggest`'s refusal is right for a MEMBER'S COMPOSITION, which must land whole or not at all; this is a SYSTEM-INFERRED derivation where a subset is meaningful on its own.
- **What reversing it costs: LOW, and it is a smaller change than the one that shipped.** The refusal is one guard at the top of the derivation plus a DEC-49 row and translation; the bound, the document arithmetic and the tests all stay. **What it does NOT reverse is the record**: connections already derived under the cut stay, correctly, because they were never wrong.
- **The gap this leaves is raised as D-237 in the same turn rather than argued away:** `op=connections` cannot tell a later reader that the derivation behind its rows was bounded. The recommended close is one additive nullable column stamping the derivation's extent per entity — deliberately not taken here because it touches `schema.mjs`, which this item did not claim.

---

## CLAIM 2026-08-08 RECORD (REC-65)
session: rec65-dec52-comments
opened: 2026-08-08T00:00:00Z
paths:
  - `bio-plane/src/index.mjs` — **COMMENTS ONLY.** The identity STAMP SITES (FW-6/7/8/9/10, the
    expertise pair, `proposedispose`) and the two OPS-table comments that restate the same claim
    (`REGISTRY_ACTIONS`, `PROGRESSION_ACTIONS`). **No executable line in this file is edited by this
    item** — the code is the half DEC-52 says is RIGHT. Five other workers are in
    `store.mjs`/`index.mjs`; a comment-only diff conflicts as a hunk at worst, never semantically.
  - `bio-plane/scripts/identity-claims.mjs` (new — the sweep instrument)
  - `bio-plane/test/identity-claims.test.mjs` (new suite)
  - `bio-plane/test/rec65.control.mjs` (this item's negative-control harness, INSIDE this worktree)
  - `bio-plane/scripts/coverage.mjs` — the `REGISTER_FLOOR` constant block ONLY, moved from the
    figures the register PRINTS on a green run.
  - `docs/development/CLAIMS.md`, `docs/development/VERIFICATION.md` (the register row's figures)
interfaces consumed: I5 (read only — no op contract moves; this item adds no op and changes no shape)
interfaces owned: none
expected: REC-65 — DEC-52's enactment. The six fields' comments claiming "a member's constitutive
  statement" are the WRONG half and are corrected to match the CODE and the ruling, **not the code
  fenced to match the comments**. Plus the class sweep: which comments in the plane state a constraint
  on WHO may write an identity field, and which of those are actually enforced.
accepts-when: as QUEUE.md REC-65, plus: battery baseline MEASURED IN THIS WORKTREE before any edit and
  trusted over the brief (this worktree arrived with no `bio-plane/node_modules`; `npm ci` run, exit
  read UNPIPED). An ordinary behavioural arm CANNOT fail on a comment-only diff — so the controls are
  built around that fact and say so. `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` unpiped.
  `node civicos-ui/test/run.mjs` from the REPO ROOT, exit unpiped. No push, no merge.
concurrency: `index.mjs` is claimed for COMMENTS ONLY and the claim names the exact blocks.
  `store.mjs` is NOT claimed and NOT edited. QUEUE.md, DECISIONS.md, INTERFACES.md and
  IS-BUILD-PLAN.md are not written by this session.
released:

### DELEGATION 2026-08-08 RECORD (REC-65) -> CONDUCT / BOB: **TWO IDENTITY CLAIMS DEC-52 RAISES AND DOES NOT ANSWER, MEASURED AND LEFT OPEN ON PURPOSE**

REC-65's sweep judged **21 identity stamp sites** in `bio-plane/src/index.mjs`. Six carry DEC-52's ruling; two carry a member-only claim that **nothing enforces and DEC-52 does not reach.** Both are MEASURED, not inferred, and both are pinned by name in `bio-plane/test/identity-claims.test.mjs` so they cannot quietly become normal.

- **`op=proposedispose` / `decidedBy`.** Driven under a machine credential through the control plane: **it SUCCEEDS, and the row reads `decided_by: "class:member"`.** An agent can defer or dismiss the record's own derived question, addressed to nobody but itself, and the record will say so honestly and permit it. The comment at the site describes it as "a member's decision to set aside the record's question."
- **`op=provenancechain` / `author`.** `provenanceChainRebuild` carries **no identity fence of any kind**, while the comment says reconstructing a chain is "a named member's judgement that the capture record supports the route being written" — the act D-200 exists to keep honest.

**WHY THEY ARE NOT DECIDED HERE.** Bob ruled on THREE VERBS — declare a relation, resolve a reference, thread a progression. Neither of these is one of them. Fencing them would be a worker deciding doctrine nobody asked for; blessing them would be worse, because extending a ruling by analogy is precisely how a ruling drifts. **Reversal cost is low now and rises the same way DEC-52's did: every stamped row written before a ruling is a row nobody can re-attribute afterwards.**

**What is owed:** a yes/no on each, in DEC-52's own shape. What is NOT at risk meanwhile: the stamp. A machine arrives honestly named, so whatever is ruled can be enforced on a real identity rather than a guessed one.

### DELEGATION 2026-08-08 RECORD (REC-65) -> CONDUCT: **DEC-53's REVISIT TRIGGER (BULK RESOLUTION) HAS FIRED, AND REC-65 SAYS SO RATHER THAN LETTING IT PASS**

DEC-52's 2026-08-06 mechanism established the running-session sidebar with **"capabilities for users to approve them individually AND IN BULK"** (`INVESTIGATIVE-SESSION.md` §14a). That is DEC-53's revisit trigger — bulk resolution — and it has fired through a surface DEC-53 was not written against. **Routed, not decided:** REC-65 is an enactment item and re-opening DEC-53 is CONDUCT's allocation, not a worker's. Recorded here so it is not lost between the two entries, exactly as REC-65's queue row instructs.

### DELEGATION 2026-08-08 RECORD (REC-65) -> WHOEVER NEXT WIDENS THE COMMENT-CLAIM SWEEP: **WHAT `scripts/identity-claims.mjs` CANNOT SEE, MEASURED**

The judged corpus is **21 stamp sites**; the WIDE ledger is **103 member-actor comment lines across 26 source files** (`store.mjs` 48, `checks/bio-checks.mjs` 15, `index.mjs` 16, `schema.mjs` 12, the rest in ones and threes). **The wide ledger is REPORTED and never judged, on purpose:** the sweep cannot tell which act a free-standing sentence governs, and guessing would be the over-strict direction — the error that looks like diligence. Four further limits are stated in the instrument's own header and repeated here because they are what a widening has to attack: enforcement is read from the store method the DISPATCH TABLE names plus ONE level of private helper, so a fence two helpers deep reads as absent; an op the dispatch table does not name (`op=promote`) is reported UNDETERMINED rather than collapsed into either answer; **the sweep measures the PRESENCE of a machine refusal, never that it FIRES for every payload** — that is REC-73's half and it is not re-done here; and a comment QUOTING an old claim in order to correct it would be flagged.

**Reversal cost: none.** The instrument is one file with no callers but its own suite.

## CLAIM 2026-08-08 RECORD
session: rec63-route-marker (worktree agent-ac23d92b0d07c1ab5)
opened: 2026-08-08T00:00:00Z
paths:
  - `bio-plane/src/schema.mjs` — ONE new table (`provenance_route_marks`), placed before the
    `host_governor` block per CLAUDE.md's trap list.
  - `bio-plane/src/store.mjs` — the `provenanceRouteAssess` method, one read helper, the
    `route` field on `listBundles`, the `route` block on `auditPass`, the `route` line on
    `provenanceChainRebuild`'s report, one `stats()` counter and the purge `TABLES` entry.
  - `bio-plane/src/index.mjs` — ONE new OPS row (`provenanceroute`) and its two stamp lists.
  - `bio-plane/checks/bio-checks.mjs` — ONE new DEC-49 family, `ROUTE_MARK_CHECKS` (C-34.1..4).
  - `bio-plane/test/provenance-marker.test.mjs` (new suite) and
    `bio-plane/test/nc-rec63.mjs` (this item's negative-control harness, INSIDE this worktree
    and never in a shared scratchpad).
  - the `FLOOR`/`CEILING` constant block of `civicos-ui/check-refusal-codes.mjs` ONLY — every
    figure re-measured from what the guard PRINTS on a green run, on REC-64's precedent. No
    other line of `civicos-ui/**` is opened; the surface is DELEGATED below.
  - `docs/development/INTERFACE-CHANGES.md` (IC-35), `docs/development/VERIFICATION.md`'s
    register row if the printed figure moves, `docs/development/MEASUREMENTS.md`.
interfaces consumed: I3 (the op contracts — `op=list` rows and `op=audit` gain an additive
  `route` block; one new op)
interfaces owned: none
expected: REC-63 — DEC-56's enactment. A standing MARKER at `verified` stating that a
  document's route cannot be shown, carried in the record beside the state rather than
  un-saying the verification (DEC-19: correction moves forward). The item's hard half is
  PUBLICATION, not storage: the marker is driven out through the reads a member uses, and a
  consumer can tell "the route cannot be shown" from "nobody looked".
accepts-when: as QUEUE.md REC-63, plus: battery baseline MEASURED IN THIS WORKTREE BEFORE ANY
  EDIT and trusted over the brief (it arrived with no `bio-plane/node_modules`; `npm ci` run,
  exit read UNPIPED, 0 — measured **124/124 green · 7,811 assertions**, so the brief's ~7,815
  was 4 high). `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` unpiped.
  `node civicos-ui/test/run.mjs` from the repo root, exit unpiped. Negative controls RUN, each
  arm ALONE with the others held open, declared before arming, restores verified by sha256 AND
  by `cmp` against a pristine per-arm copy. No push, no merge.
concurrency: several workers run in `store.mjs`/`index.mjs`. This item's `index.mjs` diff is
  one OPS row and two one-line additions to existing lists; its `store.mjs` diff is one new
  method plus additive fields on three existing returns. `QUEUE.md` and `DECISIONS.md` are NOT
  written by this session — the REC-63 row and DEC-56's `enacted:` line are CONDUCT's.
released:

### DELEGATION 2026-08-08 RECORD (REC-63) -> UI / whoever next owns `civicos-ui/app.html`: **THE ROUTE MARKER HAS NO SURFACE, AND A MARKER NOBODY CAN SEE IS NOT A MARKER**

REC-63 drove DEC-56's marker out through the PLANE reads a member uses — `op=list` (every
row, both arms), `op=audit`, `op=provenancechain` (both arms) and the act's own answer. **The
surface half is not written**, and `civicos-ui/**` is not this item's to open.

- **The shape is already on the wire and needs no plane change to render.** Every row of
  `op=list` carries `route: { applies, assessed, marked, finding, means, at, by, stateAt,
  register, undetermined, documents, note }`. `finding` is one of `NEVER_LOOKED` /
  `LOOKED_INDETERMINATE` / `PRESENT`, or `applies:false`.
- **The one thing the surface MUST get right, and it is the whole item:** *the route cannot
  be shown* and *nobody looked* are DIFFERENT FACTS and must not render alike. A surface that
  shows a badge for `LOOKED_INDETERMINATE` and nothing at all for `NEVER_LOOKED` has
  re-created the conflation one layer up, because "no badge" then means both "fine" and
  "unexamined".
- **`means` and `note` are AUTHORED BY THE PLANE and travel with the answer** (DEC-8 as
  amended by DEC-49: a surface may render a translation keyed on something the plane SENT,
  and may never compute one). `note` is the standing sentence that makes the state/finding
  disagreement legible — a `verified` document carrying a doubt must not read as a defect.
- **Reversal cost: none.** Nothing on the plane depends on a surface existing.

### DELEGATION 2026-08-08 RECORD (REC-63) -> WHOEVER TAKES THE ACT-AND-SAY CLASS NEXT: **23 SWALLOWED READS IN `store.mjs`, MEASURED, AND THE SWEEP IS A RATCHET RATHER THAN A VERDICT**

Bob's principle across DEC-56/57/58 is general — ACT, AND SAY WHAT YOU COULD NOT ESTABLISH —
so REC-63 swept for the class. **The mechanical shape of the failure in this plane is a
SWALLOWED READ: a `catch` that turns something the plane could not establish into a
normal-looking answer.** `provenance-marker.test.mjs` section I pins the roster at **23**
(comment-stripped, reach proved as a DELTA against a planted swallow) and the number may fall
freely but may not rise without somebody looking at the new one.

- **What REC-63 did NOT do, stated rather than left to be assumed: it did not judge which of
  the 23 are honest.** That is a reading, not a measurement, and 22 of them belong to other
  areas' methods. The ratchet is what a worker can honestly leave behind.
- **Two are worth a look first, from reading them in passing:** `#backfillProjection` SKIPS a
  row with no readable `bundle.md` (`if (!md || md.text === null) continue;`) and
  `op=reproject` then reports `reprojected`, `limit` and `remaining` with **no count of what
  it could not project** — so a row that can never be projected sits in `remaining` forever
  with no statement of why. And `queueFeed`'s and `readImage`'s callers each tolerate an
  absent image with `|| {}`. Neither was changed here: both are outside this item's claim and
  a shared read is the wrong place for a passing fix.
- **ONE PLACE THE SWEEP REACHED DEC-57/DEC-58's GROUND AND STOPPED, as instructed.** Any
  swallow that is *a bound the plane never published* is DEC-58, and *a walk that could not
  reach its end* is DEC-57. Both are ENACTED already (REC-57 + UI-41 for the first, UI-39 for
  the second), so nothing there is owed a new item — but a NEW instance of either belongs on
  those entries rather than on a widened REC-63.
- **What this item contributes to the class beyond the count:** the remedy demonstrated once.
  `provenanceRouteAssess` swallows its own register read and PUBLISHES what it met —
  `register_state` is `readable | absent | unparsable | no_documents | empty` — so the answer
  says which failure it hit rather than that it hit one.

## CLAIM 2026-08-08 VERIFY
session: m0-15-phantom-suite (worktree agent-a0e79024273135242)
opened: 2026-08-08T19:05:00Z
paths: **`bio-plane/scripts/battery.mjs`** (the provenance walk and its header — the runner is this item's subject; no change to discovery, to the temp sweep or to the exit code), **`bio-plane/test/battery-provenance.test.mjs`** (NEW suite, entirely this item's), **`bio-plane/scripts/coverage.mjs`** (the `REGISTER_FLOOR` numerals ONLY, moved from the figures the instrument PRINTED, because a new suite carrying a declaration invalidates all three), **`docs/development/DEBT.md`** (D-236 NEW row), **`docs/development/QUEUE.md`** (the M0-15 row's disposition ONLY), **`docs/development/VERIFICATION.md`** (the register row's figures and a new section naming the hazard), **`docs/development/ORCHESTRATION.md`** (the COMMUNICATING A CHANGE failure table gains the shared-stash row, and the worker recipe that produced it), **`docs/development/MEASUREMENTS.md`** (the stash-scope measurement), **`docs/development/CLAIMS.md`** (this entry). **NOT** `bio-plane/src/**`, **NOT** `checks/bio-checks.mjs`, **NOT** `scripts/control-register.mjs`, **NOT** any other `.test.mjs`, **NOT** `civicos-ui/**`, **NOT** `pdf-worker/**`, **NOT** `agent-worker/**`, **NOT** `newgroup/**`.
interfaces consumed: none — test estate and the runner, no plane behaviour.
interfaces owned: none.
expected: QUEUE.md M0-15. NAME the mechanism by which an untracked `.test.mjs` entered a worker's worktree and was run by the battery; make a phantom VISIBLE rather than absent.
accepts-when: as the M0-15 row. Baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief: **124/124 green · 7,815 assertions · 121 plane + 3 fleet · exit 0**, which AGREES with the brief — and the baseline's OWN honesty was checked, which is this item's subject: `git status --short` empty, and all 121 plane suites plus both fleet manifests present in the commit at HEAD, verified by `git ls-tree -r HEAD` rather than by `git status` (an ignored file is invisible to `git status` and `.claude/worktrees/` is ignored, so `git status` is the wrong instrument for this question).
concurrency: checked over the whole file. No live claim names `scripts/battery.mjs` — M0-10's entry explicitly does NOT touch it, and M0-14's holds `scripts/control-register.mjs`, which this does not touch. `scripts/coverage.mjs` is touched for the `REGISTER_FLOOR` NUMERALS only; the same three-numeral block has been moved by M0-14 and by CONDUCT at integration, and the resolution is mechanical (take the higher figure, which the instrument prints).
released:

## CLAIM 2026-08-08 MEASUREMENT (M-4)
session: m4-agent
opened: 2026-08-08T00:00:00Z
paths: bio-plane/test/ref-variance-probe.mjs and bio-plane/test/ref-variance.control.mjs (both new), the M-4 section appended to docs/development/MEASUREMENTS.md
interfaces consumed: none
interfaces owned: none
expected: M-4 — the partial-reference tier ships UNMEASURED. Re-run REC-36's variance measurement over REFERENCE STRINGS (`reading_refs.ref` and `ref_key`) rather than over labels, record n / instrument / population / date, and state what it does and does not license. MEASUREMENT ONLY: no op, field, shape, surface or behaviour changes, and the instrument is a `.probe.mjs` so `scripts/battery.mjs` (which discovers `*.test.mjs`) does not run it. Holds no area slot.
released: 2026-08-08 — done and committed on branch `worktree-agent-ad6cca371e6828415`, NOT pushed (CONDUCT integrates). No plane, surface or schema path was edited; the battery moved by ZERO (125/125 · 7,872 before and after). The measurement VINDICATES the tier's no-grade posture and CONTRADICTS the ORDER of the two partial correspondences in `Store.#CORRESPONDENCE_RANK` — routed to CONDUCT for a queue row rather than fixed here, because `QUEUE.md` has one writer and this item may change no behaviour.

## CLAIM 2026-08-08 RECORD (REC-76 / D-236)
session: rec76-refusal-shapes (worktree agent-a7c06631e829a208f)
opened: 2026-08-08T00:00:00Z
paths:
  - `civicos-ui/check-refusal-codes.mjs` — **arm C's refusal classifier INVERTED** (the
    return-position walk, the verdict rule, the UNCLASSIFIED bucket) plus the `FLOOR`/`CEILING`
    constant block, every figure re-measured from what the guard PRINTS on a green run.
  - `civicos-ui/test/refusal-codes.test.mjs` — the new fixture arms (counted-or-named,
    over-strictness) and the `NEGATIVE CONTROL:` header they are recorded in.
  - `civicos-ui/test/refusal-codes.control.mjs` — the real-tree arms for this item.
  - `bio-plane/checks/bio-checks.mjs` — THREE new rows in the existing `ACT_SHAPE_CHECKS`
    family (C-33.30/31/32). **No new family**, on SK-1's rule that a family is a floor and a
    new one buys slack for everybody else's walk.
  - `bio-plane/src/store.mjs` — ONE new `DEC-49 REGION is-selection-moved` marker pair around
    `selectionResolve`'s refuse-gate return, and the DEC-49 `code`/`check`/`translation` fields
    added to `aiRunOpen`'s TWO codeless refusals (additive on the wire; REC-64's own precedent
    one guard over).
  - `docs/development/DEBT.md` (D-236's disposition, and any row this item raises),
    `docs/development/VERIFICATION.md`, `docs/development/MEASUREMENTS.md`,
    `docs/development/CLAIMS.md` (this entry).
  - **NOT** `bio-plane/scripts/**`, **NOT** `bio-plane/test/**`, **NOT** `civicos-ui/app.html`,
    **NOT** `newgroup/**`, **NOT** `pdf-worker/**`, **NOT** `agent-worker/**`. `QUEUE.md` is
    CONDUCT's and is not written here.
interfaces consumed: I3 (two refusals at `op=airunopen` gain an additive `code`/`check`/
  `translation`; no field is removed, renamed or re-typed)
interfaces owned: none
expected: REC-76 — arm C stops grading a refusal by one literal and asks what makes something a
  refusal IN PRINCIPLE; the eight refusal objects it was blind to become visible; a shape it
  cannot classify is NAMED rather than silently scored zero; and `SET_MOVED` gets the region
  `where` and the canned translation the blindness had cost it.
accepts-when: as QUEUE.md REC-76, plus: battery baseline MEASURED IN THIS WORKTREE BEFORE ANY
  EDIT and trusted over the brief (arrived with no `bio-plane/node_modules`; `npm ci` exit read
  UNPIPED, 0 — measured **130/130 green · 8,142 assertions**, which is exactly what the brief
  said, the first item in twelve for which that is true). `node scripts/coverage.mjs --strict`
  run DIRECTLY, `$?` unpiped. `node civicos-ui/test/run.mjs` from the REPO ROOT, exit unpiped.
  Negative controls RUN, each arm ALONE, declared before arming, restores verified by sha256
  AND by `cmp` against uniquely-named per-arm pristine copies kept INSIDE this worktree.
  No push, no merge.
concurrency: several workers are in `store.mjs`. This item's `store.mjs` diff is one marker
  pair and three additive fields on two existing returns. **`regionLines` is a property of the
  MERGED source and has moved at integration four times — this item ADDS a region, so CONDUCT
  must re-read it from a green run of the merged tree.**
paths ADDED after opening, with the reason: `bio-plane/test/machinefences-dec49.test.mjs` — its
  `PINNED` list and its ARM D0 corpus floor are pinned BOTH WAYS against ACT_SHAPE_CHECKS, so the
  three new rows turned it red. **CORRECTED, never exempted** (CLAUDE.md's standing rule): the three
  C-numbers were added to the pin with the reason at the site and the corpus moved 40 -> 43. **The
  pin going red is that arm working** — a copy that could absorb three new rows in silence would be
  the drift the block exists to stop.
released: 2026-08-08 — committed on `worktree-agent-a7c06631e829a208f`, NOT pushed and NOT merged;
  CONDUCT integrates. Battery **130/130 green · 8,145 assertions** against a true baseline MEASURED
  IN THIS WORKTREE BEFORE ANY EDIT of **130/130 · 8,142** — the brief's own figure, and the FIRST
  item in twelve to find its brief exactly right rather than stale. **Delta +3, attributed per suite
  by diffing the two runs rather than by subtraction: `machinefences-dec49.test.mjs` 57 -> 60, ALL
  127 other suites unmoved.** `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` unpiped,
  **exit 0** — OPS 159/159, CHECKS 208/208 named (the three new C-numbers among them), REGISTER
  FLOOR unmoved at `526/526 · 126/126 · 127/127`. `node civicos-ui/test/run.mjs` from the REPO ROOT,
  exit unpiped, **0**. The guard PRINTS: census 410 -> 412, reach 204 -> 206, rows 149 -> 152,
  governed sites 60 -> 61, regions 47 -> 48, region lines 1289 -> 1310, codes compared 119 -> 122,
  outcome corpus 70, refusals judged 124, declared successes 7, unclassified 3; `reachGap`
  42 -> **41**, the fall being `SET_MOVED`. **13 fixture arms (ARM 10a-10g) and 6 real-tree arms
  (n1)-(n6), each armed ALONE with the others held open, declared before arming, every restore
  verified by sha256 AND by content AND by re-running; all 19 behaved as declared, and REC-64's and
  REC-71's twelve existing arms still behave.**

### DELEGATION 2026-08-08 RECORD (REC-76) -> whoever next owns `bio-plane/test/meaning-bounds.test.mjs` AND `bio-plane/test/plane-envelope.test.mjs`: **TWO MORE INSTRUMENTS GRADE A RETURN BY ONE LITERAL, AND ONE IS REC-70's OWN FILE ON THE HALF IT DID NOT TOUCH** (D-240)

REC-76 swept the class D-236 named. **Corpus: 232 instrument-candidate files across 8 directories
(`bio-plane/scripts`, `bio-plane/test`, `civicos-ui`, `civicos-ui/test`, `tools`, `agent-worker`,
`pdf-worker`, `newgroup`), 6,309,940 chars, 214 of them reading or matching source text. Reach: 36
regex literals naming a verdict-shaped field beside a boolean literal, hand-verified down to TWO
real classifiers** — the rest are single-site PINS (a regex anchoring one named return, which is a
different and legitimate thing) or `NEGATIVE CONTROL:` prose.

- **`meaning-bounds.test.mjs:361` — `const REFUSAL_RETURN = /\bok\s*:\s*false/`.** REC-70 inverted
  this walk's SUCCESS side and left the EXCLUDER that decides what a refusal is at one literal.
  MEASURED over `store.mjs`: **489 return-object literals excluded as refusals, 72 more
  refusal-shaped and NOT excluded, across 37 distinct verdict fields** (`found: false` x16,
  `started: false` x4, `terminated`/`recorded`/`configured: false` x4 each, `ok: !s`, and thirty
  more). The direction is the generous one — it inflates the BARE roster REC-70 ratcheted.
- **`plane-envelope.test.mjs:180` — DETECTOR A's `if (!/^\s*\{\s*ok:\s*true\b/.test(c.arg)) continue;`.**
  MEASURED over `index.mjs`: **152 `json()` calls, 26 graded, 126 skipped; 4 of the skipped spread a
  Durable Object `.result`.** Three are `ok:false` refusal envelopes and outside its subject; the
  fourth, `index.mjs:4587`, is `{ ok: !!promoted.result?.ok, … }` — **a COMPUTED verdict spreading a
  store result, D-236's exact shape arriving in a second instrument.**

**WHAT THE SWEEP'S OWN MATCHER CANNOT SEE, stated rather than implied:** a classifier built from a
string concatenated into a `RegExp` at runtime, one whose verdict field is held in a variable, and
one that tests a PARSED value rather than source text — it reads regex LITERALS in source. It also
cannot tell a classifier from a pin without a human reading it, which is why the 36 were
hand-verified rather than reported as the answer.

**NOT FIXED HERE, and the reason is not scope alone:** both files carry RATCHETED figures, and
widening the first MOVES REC-70's bare roster. That is a measurement whose owner must make it, not
an edit a passing item should land. The recipe is D-236's: invert, and NAME what cannot be
classified. Raised as **D-240**.

## CLAIM 2026-08-08 VERIFY
session: m0-16-uncontrolled-walks (worktree agent-a4e27ecb4d9654c5e)
opened: 2026-08-08T21:40:00Z
paths: **`bio-plane/scripts/provenance.mjs`** (NEW — M0-15's provenance check MOVED here unchanged in behaviour so the walks share ONE mechanism; the module states the four rules once and states what the check CANNOT see), **`bio-plane/scripts/battery.mjs`** (the inline provenance block replaced by a call to that module — no change to discovery, to the temp sweep, to the exit code, or to a single printed word that `battery-provenance.test.mjs` asserts; that suite is 23 pass before and after), **`bio-plane/scripts/coverage.mjs`** (the three walks GUARDED; the fleet walk gains the battery's `!startsWith(".")` filter it never had; the `REGISTER_FLOOR` comparison now reads the REPRODUCIBLE figure and the numerals move from what a green run PRINTED), **`bio-plane/test/hygiene.test.mjs`** (a NEW provenance block over its own corpus walks, a class census, and its `NEGATIVE CONTROL:` declaration gaining M0-16's marked arms), **`bio-plane/test/coverage-provenance.test.mjs`** (NEW suite, entirely this item's), **`bio-plane/test/coverage-provenance.control.sh`** (NEW harness, five arms), **`bio-plane/test/battery-provenance.test.mjs`** (it copies the REAL runner into a scratch repository and must now copy the module the runner imports — two lines; no assertion changed), **`docs/development/DEBT.md`** (D-238's disposition cell), **`docs/development/QUEUE.md`** (the M0-16 row's disposition ONLY), **`docs/development/VERIFICATION.md`** (the register row's figures and the provenance section), **`docs/development/CLAIMS.md`** (this entry and its DELEGATION). **NOT** `bio-plane/src/**`, **NOT** `checks/bio-checks.mjs`, **NOT** `scripts/control-register.mjs`, **NOT** any other `.test.mjs`, **NOT** `civicos-ui/**`, **NOT** `pdf-worker/**`, **NOT** `agent-worker/**`, **NOT** `newgroup/**`.
interfaces consumed: none — test estate and instruments, no plane behaviour.
interfaces owned: none.
expected: QUEUE.md M0-16 / D-238. Seven walks discover over an uncontrolled directory and only two were guarded; guard the rest with M0-15's shape rather than a second mechanism, and SWEEP FOR THE CLASS.
accepts-when: as the M0-16 row. **Baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief: 130/130 green · 8,142 assertions · exit 0**, which AGREES with the brief (~130/130 · ~8,142) — and it is the ELEVENTH-item streak of stale briefs BROKEN rather than continued, which is worth saying because a run of stale figures is not evidence the next one is stale. **The baseline's OWN honesty was established two ways, because this item's whole subject is that a baseline can lie:** (i) the runner's own M0-15 line reported `132 of 132 discovered item(s) are in the commit at HEAD (ec67a68)`, and (ii) independently, a full filesystem walk of the worktree diffed against `git ls-tree -r --name-only HEAD` returned exactly two entries not in the commit — `.env` (gitignored, carried by `.worktreeinclude`) and `.git` (the worktree pointer file) — and NO `.test.mjs`, no manifest, nothing under `src/`. `git status` was NOT used for this, because an ignored file is invisible to it and `.claude/worktrees/` is ignored, which is precisely how the original phantom stayed invisible. The worktree arrived with no `bio-plane/node_modules`; `npm ci` was run there first, exit 0.
concurrency: checked over the whole file. Every claim naming `scripts/coverage.mjs`, `scripts/battery.mjs` or `test/hygiene.test.mjs` is a LANDED item whose work is merged (M0-15 at `4e92bdb`/`b6b7a8f`, M0-10, M0-14, CPDF-9, UI-48, REC-73); none is live. `scripts/control-register.mjs` is M0-14's and is NOT touched — it is IMPORTED by the new suite for its `CONTROL_MARKER` constant and by hygiene as before. `REGISTER_FLOOR` is the one known collision point: it has ONE key set on purpose and a keep-both merge has twice left duplicate `arms:` keys where the LAST silently wins. If this conflicts, COLLAPSE TO ONE SET and re-read the printed figures.
released: 2026-08-08 — committed on `worktree-agent-a4e27ecb4d9654c5e` (4 commits, head `690768f`), **NOT pushed and NOT merged**; CONDUCT integrates. Battery **131/131 green · 8,181 assertions · exit 0**, provenance `133 of 133 discovered item(s) are in the commit at HEAD`; true baseline measured here before any edit **130/130 · 8,142**; **delta +39, attributed by DIFFING the two runs rather than by subtraction** — `coverage-provenance.test.mjs` +28 (new) and `hygiene` 522 -> 533 are the ONLY rows that moved. `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` UNPIPED, **exit 0**, naming NOTHING it counted that is not in a commit (`137 of 137`). `node civicos-ui/test/run.mjs` from the repo root, exit unpiped, **0**. `node tools/plancheck.mjs` **clean but for UNPUSHED**. `REGISTER_FLOOR` moved `526/126/127` -> **`542/127/128`** from the PRINTED figures — and, the change that matters more than the numerals, the comparison now reads the REPRODUCIBLE figure, which fired during the build by printing `arms 534` beside a reproducible `526` and refusing the higher one. **FIVE CONTROL ARMS RUN, EACH ALONE; TWO CAME BACK OTHER THAN DECLARED AND BOTH FOUND THE INSTRUMENT WRONG RATHER THAN THE SUBJECT:** arm (3) NEVER ARMED — a worktree's gitdir has no `info/`, the exclude write ENOENTed, `git status` was not empty and the arm silently re-measured arm (1), caught only because the printed pristine digest read `e3b0c442…`, the sha256 of the EMPTY STRING, which is the identical failure M0-15's own harness had; and arm (4) left `hygiene` at a SURPRISING GREEN because every arm read the check's RETURN and none read a printed WORD, so the naming branch could have gone dark there in silence. Both closed and both recorded in the declaration rather than smoothed. **A THIRD INSTRUMENT DEFECT WAS CAUGHT BY THE ESTATE ITSELF:** `op-claims.test.mjs` red'd the first full battery over a scratch fixture naming an op the dispatch table does not route — the fixture was corrected, not the check, and then corrected a SECOND time because the comment EXPLAINING the correction used the same spelling and that sweep reads comments. **AND MY OWN GUARD-DETECTOR WAS WRONG FIRST TIME:** the census graded a file GUARDED by any mention of `scripts/provenance.mjs`, which read `battery.mjs` as guarded because its HEADER says so in prose and `coverage.mjs` as unguarded because its import spells the path relatively — a classifier grading a file by a word in its comments, REC-70's and REC-64's shape exactly; corrected to match the import statement. Final tree verified by a full filesystem walk diffed against `git ls-tree -r HEAD`: nothing off-commit but `.env`, the `.git` pointer file and three gitignored `.wrangler/cache/cf.json` written by the battery runs; nothing in HEAD missing from disk. **`git stash` was NOT used at any point in this item**, which is D-238's own instruction and the reason the baseline above can be trusted.

### DELEGATION 2026-08-08 M0-16 -> UI / WHOEVER OWNS `civicos-ui/test/run.mjs`: **THE UI BATTERY'S RUNNER DISCOVERS OVER A DIRECTORY IT DOES NOT CONTROL, AND THE TOTAL IT PRINTS IS A BASELINE SESSIONS QUOTE**

M0-16 swept the class D-238 named — *what else walks a directory it does not control
and reports a number from it* — and `hygiene.test.mjs` now counts it: **21 files in
the estate walk with `readdirSync`, 3 are GUARDED, 18 are NAMED.** Seventeen of the
eighteen report findings about SOURCE (which module spells a grade letter, which
suite pins a refusal code, which op a comment names). A wrong number there is
noticed by the assertion beside it.

**One is different, and it is this one.** `civicos-ui/test/run.mjs` is
`fs.readdirSync(...).filter(f=>f.endsWith(".test.mjs")).sort()` — the UI battery's
whole discovery mechanism, and the total it prints is what a session quotes as its
UI baseline and what an item's `+N attributed` is computed against. **That is
`bio-plane/scripts/battery.mjs` exactly as it stood before M0-15, one estate over.**
A `git stash pop` deposits an untracked `.test.mjs` into `civicos-ui/test/` by the
same repository-wide mechanism, and nothing there would say so.

- **The fix is an import, not a design.** `bio-plane/scripts/provenance.mjs` exports
  `readGitProvenance`, `classifyDiscovered`, `stateOf`, `repoPath` and
  `reportProvenance`; `battery.mjs` is the worked example, about fifteen lines at the
  call site. **Do not write a second mechanism** — that is what this item exists to
  have avoided, and two mechanisms for one job is how the next component goes dark
  differently.
- **It must REPORT, not FAIL** (M0-15's provisional, and its reasoning holds: a
  worker writes a suite before committing it dozens of times an hour), must ask
  `ls-tree HEAD` and never `git status`, must tell UNTRACKED from staged-not-
  committed, must print the reproducible total beside the contaminated one, and must
  say UNVERIFIED rather than clean when git cannot answer.
- **Why M0-16 did not do it:** `civicos-ui/**` is not this item's to open. The
  census entry for it is a ratchet, not a fix — it will keep saying so until somebody
  changes it.
- **Reversal cost: none.** The module is additive and nothing depends on the UI
  runner staying silent.

### DELEGATION 2026-08-08 M0-16 -> WHOEVER NEXT TOUCHES `tools/plancheck.mjs`: **A DEAD `readdirSync` IMPORT**

`tools/plancheck.mjs:28` imports `readdirSync` and never calls it. Noticed while
sweeping the class, not fixed: `tools/` is outside this item's paths and a passing
edit to the handoff gate is not worth the risk. It is harmless — one unused binding —
and it is recorded so the next reader of that file does not have to wonder whether a
walk was removed and its report lost with it, which is the question the census exists
to make cheap.

## CLAIM 2026-08-08 RECORD
session: rec77-selectivity (worktree agent-a0ebdc75cc92389d3)
opened: 2026-08-08T20:20:00Z
paths: **`bio-plane/src/store.mjs` — named by SITE, not by file:** (1) `documentsNamingEntity()`'s alias loop, its per-candidate assembly and its final ordering; (2) THREE new private statics beside the existing correspondence block — `#PARTIAL_CORRESPONDENCES`, `#PARTIAL_BAND` and `#candOrder` — all DERIVED from `Store.#CORRESPONDENCE` / `#CORRESPONDENCE_RANK` rather than restating them; (3) ONE new SQL builder `#refReachSql`, which WRAPS `#refTermsSql` as a subquery rather than respelling it, plus the corpus-size statement beside it. **NOT opened:** `Store.#CORRESPONDENCE`'s and `#CORRESPONDENCE_RANK`'s VALUES (unchanged — M-4's probe reads them out of the source and other suites pin them), `#normAlias`, `#labelTerms`, `#refTermSources`, `#refTermsSql`'s own text, `#recognise`, `#recogniseTier`, `#bundleGate`, `readingNamePlan`, the schema, `op=purge`, any refusal string, any DEC-49 region (none is in this method). `bio-plane/test/readingname.test.mjs` (this read's own suite — new REC-77 block, the `NEGATIVE CONTROL:` header extended). `bio-plane/scripts/coverage.mjs` (the `REGISTER_FLOOR` numerals ONLY, from PRINTED figures — a widened declaration invalidates `arms`). `docs/development/INTERFACE-CHANGES.md` (APPEND — the IC row; the I3 version bump is CONDUCT's). `docs/development/MEASUREMENTS.md` (APPEND — the REC-77 re-run of M-4's probe and the discriminator's own figures). `docs/development/QUEUE.md` (the REC-77 row's disposition ONLY). `docs/development/CLAIMS.md` (this entry). **NOT** `civicos-ui/**`, **NOT** `checks/bio-checks.mjs`, **NOT** `bio-plane/src/index.mjs`, **NOT** `newgroup/**`, **NOT** any other `.test.mjs`.
interfaces consumed: none
interfaces owned: I3 (the `op=readingname` answer shape) — the IC row is filed here; **CONDUCT takes the version bump and the RESOLUTION.**
expected: QUEUE.md REC-77. The correspondence rank offers the least selective evidence first. Rank and gate the two PARTIAL tiers by selectivity COMPUTED OVER THE CORPUS IN FRONT OF THE READER, never pinned to M-4's percentages.
accepts-when: as the REC-77 row. **Baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 130/130 suites green · 8,142 assertions · 112.1s · exit 0**, `git status --short` empty, provenance 132/132 items in the commit at HEAD (73fca8b) — which AGREES with the brief's ~130/~8,142 exactly, and does NOT agree with M-4's own commit message (125/125 · 7,872), because M-4 branched earlier.
concurrency: checked over the whole file. No live claim names `documentsNamingEntity`'s ordering or the correspondence statics. REC-60's live claim names `documentsNamingEntity` explicitly as **NOT opened**; REC-59's claim, which did open its RETURN BLOCK, is `released: 2026-08-05`. `scripts/coverage.mjs` is touched for the `REGISTER_FLOOR` NUMERALS only — **if this conflicts at merge, COLLAPSE TO ONE SET and re-read the printed figures.**
released:

## DELEGATION 2026-08-08 RECORD → UI
from: rec77-selectivity (worktree agent-a0ebdc75cc92389d3), REC-77
what: **`op=readingname` now publishes `names_uninformative`, and `civicos-ui/app.html` does not read it — so a member who registers a vacuous alias sees an empty candidate list with no explanation.** That is the gap this item's own doctrine says must not be left: the surface already renders `names_unusable` at `app.html:12151` (*"…has no word in it to look anything up by, so nothing was looked up under it"*), and `names_uninformative` is the opposite failure and needs the opposite sentence — the name reached EVERYTHING, so nothing was offered under it. Each row carries `{ alias, source, reaches, corpus }`, so the sentence can state the arithmetic rather than assert a rule.
why it is UI's: `civicos-ui/app.html` is UI's path and RECORD does not edit it. IC-37 records the measured impact of NOT doing it (nothing breaks; a member is simply not told why), which is why this is a delegation and not a blocker.
also worth the surface's attention, NOT owed: every partial candidate now carries `selectivity` `{ source, reaches, corpus, value }`, which is the plane's own figure for how far the matching name reached. **It is VIEWER-relative** — dave's reach is 1 where carol's is 2 for the same name on the same document — so it may be rendered but must never be compared between members, and `corpus` must never be read as the size of the store.
the ordering contract UI relies on is UNCHANGED and was verified: `app.html:12073`'s first-wins-on-capture still gets the strongest correspondence first, because whole tiers still sort above every partial. UI harness exit 0, no assertion moved.

## CLAIM 2026-08-08 FRAMEWORK
session: fw13-citations-disposition (worktree agent-a04633fc02d8cd2aa)
opened: 2026-08-08T00:00:00Z
paths: **`bio-plane/checks/bio-checks.mjs` — named by SITE, not by file:** (1) `checkCitationRegister` (DELETED) and its one call line inside `checkBundle`; (2) ONE new exported table `CHECK_RETIREMENTS` in that function's place. **NOT opened:** any other check, `ACT_SHAPE_CHECKS`, `ROUTE_MARK_CHECKS`, any DEC-49 region, any refusal string, any regex or constant the deleted function used (`DATE_RE`, `CONTENT_HASH_RE`, `BUNDLE_ID_RE` are all still read by other checks and were left exactly as they were). `bio-plane/test/check-firing.test.mjs` (this item's own suite — the C-8.1 `proves()` block replaced by a RETIRED-CHECKS block at the foot, the header's M0-3 list annotated as SUPERSEDED, the `NEGATIVE CONTROL:` declaration extended with four arms). `bio-plane/test/retirement.control.mjs` (NEW harness, entirely this item's, deliberately not a `.test.mjs` because it edits real sources). **`bio-plane/test/hygiene.test.mjs` — ONE entry in `CLASS_NAMED_UNGUARDED` and its reason, and nothing else**: M0-16's class census NAMED this item's new estate walk on its first full battery, which is the mechanism working as designed (a new walk is a decision, not a silence), and recording the decision is the only action it admits. No assertion was added or changed there — the suite reads 533 before and after. `docs/development/QUEUE.md` (the FW-13 row's disposition ONLY). `docs/development/research/DATA-MODEL.md` and `docs/development/research/SB-CORE.md` (ONE annotation each, on the paragraph that poses this exact open question). `docs/development/CLAIMS.md` (this entry and its DELEGATION). **NOT** `bio-plane/src/**`, **NOT** `bio-plane/scripts/coverage.mjs` (no floor was invalidated — see below), **NOT** any other `.test.mjs`, **NOT** `civicos-ui/**`, **NOT** `pdf-worker/**`, **NOT** `agent-worker/**`, **NOT** `newgroup/**`.
interfaces consumed: none
interfaces owned: none — the check catalogue is not behind an interface, and no op shape moved.
expected: QUEUE.md FW-13 — decide `data/citations.json` / C-8.1: RETIRE, or BIND. **DECIDED: RETIRE.**
measured before deciding: **no producer, no consumer, and it fired ALONE.** Nothing in `bio-plane/src`, `civicos-ui`, `docprofile`, `tools`, `agent-worker`, `pdf-worker` or `newgroup/src` writes or reads `data/citations.json`; the only occurrence outside the catalogue and its suite is `newgroup/src/release.mjs`, which is one `RELEASE_SOURCE` string holding a copy of the catalogue — and that copy alone made this sweep's FIRST run report zero orphans, which is why it is excluded by name. Every sibling register the catalogue gates has a real producer (`data/provenance.json` 9 files, `_history/manifest.json` 4, `data/inbox.json` and `data/dataset.json` 2 each, `data/changes.json` and `data/gathering.json` 1 each); this one had **0**. Driven through `checkBundle` on seven inputs: the check FIRED on four distinct malformed registers, drew NOTHING on a conformant one, and fired **ALONE** — the same tamper under any other filename draws nothing at all, so no other check was doing its work. **Which of the three findings this is: NOT redundant and NOT unreachable — a check with NO PRODUCER, which has therefore never fired on anything real and never would have.**
reasoning: `inquiry_basis` / `inquiry_basis_versions` landed (REC-11 and after) and hold the claim layer as ROWS — a leg with a target, a role, a per-axis grade and a `grade_source` a caller cannot hand us. C-8.1's register put a claim INSIDE another object's file, where it cannot be cited, contradicted, graded or composed; that is the argument that produced the inquiry object in the first place. Keeping both would leave the record with two claim structures of overlapping shape and no rule saying which is authoritative — the diffusion D-69 measures, and worse than the missing check, because the second structure is the one a member can fill in by hand while the plane knows nothing about it. `DATA-MODEL.md` §2.2.2 and `BUILD-ORDER.md` §2 both recommend RETIRE and the measurement agrees with them.
accepts-when: as the FW-13 row. The retirement is RECORDED, not deleted: `CHECK_RETIREMENTS` states what the check was for, why the old rule was wrong, and what supersedes it, and `check-firing.test.mjs` READS that table rather than hand-listing it, so a retirement added there and not asserted here would be a retirement nothing checks. Three assertions stand behind it — the shape no longer fires (five arms including the over-strictness one), no retired id is pushed anywhere in the catalogue, and **the estate grew no producer**, which is the accepts-when's own "no two structures claim the same thing".
baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: **131/131 suites green · 8,209 assertions · 112.6s · exit 0**, provenance `133 of 133 discovered item(s) are in the commit at HEAD (47b4199)`, `git status --short` empty. **This AGREES with the brief's ~131/~8,209 exactly** — recorded because the practice is to trust the measurement rather than the streak, in either direction.
floors: **NONE moved, and none was invalidated.** `REGISTER_FLOOR` is untouched: no suite was added or removed (`retirement.control.mjs` is not a `.test.mjs` and the battery does not discover it), so `corpus` and `classified` are unmoved, and `arms` only ever RISES when a declaration grows. The DEC-49 guard is untouched: C-8.1 is a CHECK id, not a refusal code, its `where` names no region, and the guard's corpus floor of 43 rows is unmoved — **no floor fell, which is the answer to "a falling floor needs its reason at the site" for this item.** `coverage.mjs`'s CHECKS catalogue still counts C-8.1, deliberately: the id survives in `CHECK_RETIREMENTS`, so `--strict` keeps demanding an assertion that names it, and the assertion that names it is the one PROVING IT NO LONGER FIRES. That is the mechanism by which this retirement cannot be forgotten.
what the estate caught, recorded because it is three instruments doing exactly what they were built for and none of it was found by reading the diff: (1) `op-claims.test.mjs` RED on the first full battery — the retirement note said a claim belongs where `op=cite` and **an "op" spelled from the store's `basis` path** put it — and `basis` is a DO-internal route reached by no op at all. The COMMENT was corrected (to `op=cite` writes / `op=earnedbasis` reads), never the check. **And the correction had to be made TWICE**, because writing this sentence with the bad spelling in it moved the ledger's own count for that name from 1 to 2 and turned the line red again — which is precisely the second-order failure M0-16 recorded, met here from the other side and worth the two lines it costs to say so. (2) `hygiene.test.mjs` RED on the same run — M0-16's class census NAMED this item's new estate walk before any human read it. Recorded as a decision (NAMED, not guarded, with the reason at the site), which is the only action the census admits. (3) `civicos-ui/check-refusal-codes.mjs` FOUR failures on the UI harness — the table was first called `RETIRED_CHECKS`, and that guard HARVESTS every `/_CHECKS$/` export in `bio-checks.mjs` as a DEC-49 refusal family. It read the retirement registry as a family of one, failed correctly over a row with no wire code and no translation, **and grew its own families/rows floor 14/152 -> 15/153 — false growth in a ratchet built to have none.** Renamed to `CHECK_RETIREMENTS`; the floor is back at 14/152 exactly, and `_CHECKS` is now stated in the file as a RESERVED SUFFIX. **Two of those three were the instrument catching a real mistake of mine; the third would have installed slack.**
concurrency: checked over the whole file. No live claim names `checkCitationRegister`, the citation register, or `check-firing.test.mjs`. The live claims that open `checks/bio-checks.mjs` open it BY SITE and by different sites (`ROUTE_MARK_CHECKS` C-34.1..4; three rows in `ACT_SHAPE_CHECKS`; the machine-identity predicate). `coverage.mjs` was NOT touched, so this item cannot collide in `REGISTER_FLOOR` at all.
released:

### DELEGATION 2026-08-08 FRAMEWORK (FW-13) -> CONDUCT: **C-7.1 / `data/deletions.json` IS THE SAME CLASS AND IS STILL OPEN**

The class sweep FW-13 ran found **two** members, not one, and only one of them was this item's to decide.

`data/deletions.json` — the append-only deletion ledger specified in
`BIO_State_Rules_Consistency_v1_5.md:387` and gated by **C-7.1** — has the identical
signature: it is validated by the catalogue, proven to FIRE by `check-firing.test.mjs`,
and **written and read by NOTHING** in `bio-plane/src`, `civicos-ui`, `docprofile`,
`tools`, `agent-worker`, `pdf-worker` or `newgroup/src`. Its only occurrence outside the
catalogue and its suite is the same `RELEASE_SOURCE` copy. Reproduce it in one step:
`node bio-plane/test/check-firing.test.mjs` prints the estate corpus, and the walk it
uses is the same one that clears `data/citations.json`.

**Why it was NOT retired here:** the FW-13 row rules on the citation register, and the
argument that decided it does NOT transfer. C-8.1 was retired because a SECOND claim
structure now exists and collides with `inquiry_basis`; nothing collides with a deletion
ledger, so C-7.1 is a no-producer check with no rival, which is a different question with
a different answer — plausibly BIND (give `op=purge`/`op=retire` a ledger to write) rather
than retire. Deciding it inside this item would have been exactly the guess the row's own
doctrine forbids. **It needs a row.**

**One more thing worth a row's attention, and it is a limit of the sweep rather than a
finding:** `PENDING_PROMOTION.json` (C-16.1..5, C-17.1, C-17.2) also has no producer IN
CODE — but it has a real one OUTSIDE the repository, the bundle skill, documented at
`BIO_Bundle_Skill_Composite_Design_v1_7.md:547`. So it is NOT an orphan, and the reason
this sweep cannot tell the two cases apart is stated in the suite's own header: a producer
living in a document is invisible to a walk over code.

## CLAIM 2026-08-08 M0 (test-estate / tooling lane)
session: m0-17-mintid (worktree agent-a6a99245c369a9fd4)
opened: 2026-08-08T21:05:00Z
paths: **`tools/mintid.mjs` (NEW)** — the allocator. **`bio-plane/test/mintid.test.mjs` (NEW)** — its suite. `bio-plane/scripts/coverage.mjs` (**the `REGISTER_FLOOR` NUMERALS ONLY**, from PRINTED figures — a new suite moves `arms`/`classified`/`corpus`). `docs/development/kickoffs/CONDUCT.md` (**step 1 only** — the standing line every spawn brief must carry). `docs/development/ORCHESTRATION.md` (APPEND one section — the allocation rule and its receipts). `docs/development/DEBT.md` (APPEND — the residual rows). `docs/development/MEASUREMENTS.md` (APPEND — the sweep census and the race figures). `docs/development/QUEUE.md` (**the M0-17 row's `landed:` line ONLY**). `docs/development/CLAIMS.md` (this entry and its delegations). **NOT** `bio-plane/src/**`, **NOT** `bio-plane/checks/**`, **NOT** any other `.test.mjs`, **NOT** `civicos-ui/**`, **NOT** `newgroup/**`, **NOT** `tools/plancheck.mjs`.
interfaces consumed: none
interfaces owned: none — tooling and convention; no plane behaviour changes, no op, no field, no shape
expected: QUEUE.md M0-17. Mint ids instead of reading the file and adding one. Seven collisions in one day at a concurrency budget of eight; every worker measured correctly and every one was right when it looked.
accepts-when: as the M0-17 row. **Baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 131/131 suites green · 8,209 assertions · 111.5s · exit 0**, provenance 133/133 discovered items in the commit at HEAD (47b4199); `node scripts/coverage.mjs --strict` exit 0 (arms 546 · classified 127 · corpus 128); `node civicos-ui/test/run.mjs` from the repo root exit 0. **THE BRIEF'S FIGURES WERE EXACTLY RIGHT and that is recorded rather than assumed** — the practice is to trust the measurement, not the streak.
concurrency: `bio-plane/scripts/coverage.mjs` is touched for the `REGISTER_FLOOR` NUMERALS only — **if this conflicts at merge, COLLAPSE TO ONE SET and re-read the printed figures; do not keep both.** No live claim names `tools/mintid.mjs` (it does not exist yet) or `bio-plane/test/mintid.test.mjs`. `docs/development/kickoffs/CONDUCT.md` is edited in ONE section, which is what the M0-17 row requires.
released: 2026-08-08 — committed on `worktree-agent-a6a99245c369a9fd4`, NOT pushed, NOT merged. Battery **132/132 · 8,246 assertions · 111.7 s · exit 0** (baseline 131/131 · 8,209 measured in this worktree before any edit — **the brief AGREED exactly**); delta **+37 attributed by DIFFING the runs**: `mintid.test.mjs` +33 (new), `hygiene` 533 -> 535, `planning-hygiene` 276 -> 278, every other suite byte-identical. `node scripts/coverage.mjs --strict` exit 0, 159/159 ops · 208/208 checks · provenance 138 of 138 at `c1a230c`; `REGISTER_FLOOR` **546/127/128 -> 553/128/129** from the REPRODUCIBLE printed figures (the pre-commit run's contaminated figures were the SAME numbers and were still not used — the floor was moved only after the commit made them reproducible). `node civicos-ui/test/run.mjs` from the repo root exit 0, unchanged. `node tools/plancheck.mjs` 1 fail, UNPUSHED only. Seven control arms, each alone, all seven agreed; **two instrument findings, both recorded rather than quietly fixed**: (a) the over-strictness arm first reached its verdict by THROWING out of the module — `null pass, null fail` — caught only by the FOOT sentinel, and the suite is hardened; (b) **the tool caught its own documentation poisoning its own corpus** — the D-243 row explained the gap cost with a worked example naming the next free number, in `DEBT.md`, which is D own corpus, and `--list` read the floor off the prose. The generous floor stands; a per-namespace ALLOCATION pattern now reads beside it and NAMES a prose-driven floor, and the example is gone from every corpus file. That is arm (7). Residuals **D-242** and **D-243**, both delegated rather than smuggled in. **The paths named above were the only ones edited; `tools/plancheck.mjs`, `civicos-ui/**`, `newgroup/**`, `bio-plane/src/**` and `bio-plane/checks/**` were not touched.**

## DELEGATION 2026-08-08 M0 → CONDUCT: **the id ledger needs one step in YOUR loop, and it is deliberately not a suite**
from: m0-17-mintid (worktree agent-a6a99245c369a9fd4), M0-17
what: **D-243.** M0-17 closes the id race but does not make the allocator compulsory: a worker that reads `DEBT.md`, adds one and writes the next number gets exactly the behaviour of 2026-08-08 and nothing anywhere says a word. **The check cannot live in the battery**, and the reason is structural rather than a preference — the ledger is NOT committed (a committed ledger races exactly as the file does, which is why it is not), so the claim files sit in the shared `.git` that only the machine can see, and a suite running from a worker's commit cannot ask them anything. **What it can live in is your integration step:** when you merge a branch, the ids it introduces are visible in the diff, and `node tools/mintid.mjs --list` says which numbers the ledger holds. A new id the ledger does not hold is a QUESTION, not a failure — every id allocated before today is honestly `unknown`, and a gate that answers `unknown` for the whole corpus is the shape VERIFICATION.md already refuses.
why it is CONDUCT's: `kickoffs/CONDUCT.md` is the file whose owner performs the step, and step 2 (integration) is where it belongs. This item edited ONE section of that file — the spawn brief in step 1 — because that is what the M0-17 row required, and adding a step to step 2 is a change to your loop rather than to the brief you write.
not owed yet, and the reason is a measurement: the ledger holds 2 ids today. Until it has enough history for a MISS to mean something, the check would answer `unknown` about everything and train you to ignore it — which is exactly how a ratchet gets switched off.

## DELEGATION 2026-08-08 M0 → WHOEVER NEXT WRITES A NEW SHARED ID SPACE
from: m0-17-mintid (worktree agent-a6a99245c369a9fd4), M0-17
what: **`tools/mintid.mjs` holds a register of SIXTEEN namespaces and each one names the corpus its floor is read from. A new prefix that is not in that register is not protected** — the tool will refuse it by name (`REFUSED: unknown namespace`), which is the fail-closed direction, but nothing prompts you to add it. `bio-plane/test/mintid.test.mjs` asserts that every REGISTERED namespace still reads a live corpus and a non-zero floor, so a corpus file RENAMED out from under the register fails by name; it cannot assert the existence of a namespace nobody has told it about.
why it is not fixed here: enumerating "every prefix-number token in the repository" as a candidate list was measured and is not usable — the wide census returns FW, INFO, IS, INQ, SHA, UTF, RFC, FY2023 and thirty more, most of which are data vocabularies rather than allocated ids, and a detector that cannot tell those apart would produce a list nobody reads. **The judgement of what is an ALLOCATED namespace is a human one, and the register is where it is recorded.**

## CLAIM 2026-08-08 FRAMEWORK (FW-14)
session: fw14-rung-ladder (worktree agent-aed7f2e57ffe7cbab)
opened: 2026-08-08T00:00:00Z
paths: **`bio-plane/src/affordances.mjs`** — the `RUNGS` block and the four new exports beside it (`RUNG_LADDER`, `RUNG_ABSENT`, `RUNG_ABSENCE_GROUNDS`, `JUSTIFICATION_REFUSALS`), plus the header paragraph that carried the superseded "7 of 57" figure and the `vocabularies` block's new `rung_ladder` key. **NOT** `ACTS`, **NOT** `NON_ACTS`, **NOT** `CAPTURE_ACTS`, **NOT** `DISPOSITIONS`/`REOPENABLE_FROM`/`ENTITY_KINDS`, **NOT** any `applies()`. **`bio-plane/src/index.mjs` — TWO SITES ONLY:** the `decorateAct` return literal (one added key beside the existing `rung:`) and the `op=affordances` handler's published `vocabularies`/`capture_acts` assembly + its `note` string. **The `OPS` table is RECORD's and is NOT opened** — it is READ, never written; no op is added, removed or re-flagged. **`bio-plane/scripts/op-claims.mjs`** — `readDispatch()` gains ONE additional returned field (`mutating`), additively; the walk, the matcher, the ledger, `corpus()`, `sweep()` and every existing field are untouched, because M0-12's suite pins them. **`bio-plane/test/rung-ladder.test.mjs`** (NEW, entirely this item's) and **`bio-plane/test/rung-ladder.control.mjs`** (NEW — the committed negative-control driver, deliberately NOT a `.test.mjs`: `scripts/battery.mjs` discovers `.endsWith(".test.mjs")` and nothing else, so it runs on demand and moves the battery by zero). **`bio-plane/scripts/coverage.mjs`** — the `REGISTER_FLOOR` numerals ONLY, from PRINTED figures (a new suite invalidates `corpus` and `classified`). **`docs/architecture/BIO_Interaction_Constructs_v0_1.md`** — the ladder's two statements of its own rung list (the §"weight vs rung" line and the construct table row) and the A-construct's DEC-19 paragraph, corrected to the 2026-08-03 AMENDMENT; unowned by any thread and named here so it stops being unowned for the duration. **`docs/development/MEASUREMENTS.md`** (APPEND — one dated section). **`docs/development/CLAIMS.md`** (this entry and its DELEGATIONs). **NOT** `civicos-ui/**`, **NOT** `checks/bio-checks.mjs`, **NOT** `bio-plane/src/store.mjs`, **NOT** `newgroup/**`, **NOT** `pdf-worker/**`, **NOT** `docs/development/QUEUE.md` (CONDUCT is its sole writer), **NOT** any other `.test.mjs`.
interfaces consumed: I3 (op=affordances' answer shape — the `rung` field REC-19 published as null)
interfaces owned: none; I3 is RECORD's. The additive shape change (`rung_ladder`, `rung_absence`) is filed as an IC row and **CONDUCT takes the version bump.**
expected: QUEUE.md FW-14. Assign the weight-ladder rung to every mutating op **or STATE that it has none**, over an op set DERIVED from the dispatch table, with the classification asserted TOTAL IN BOTH DIRECTIONS — no mutating op unclassified, and no classification naming an op the dispatch table does not carry as mutating. DEC-19 as amended: the ladder's top rung reads IRREVERSIBLE with the correction path beside it.
accepts-when: as the FW-14 row. **Baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 131/131 suites green · 8,209 assertions · 111.4s · exit 0**, `git status --short` empty, provenance 133/133 discovered items in the commit at HEAD (47b4199) — which AGREES with the brief's ~131/~8,209 EXACTLY. Stated because the practice is to trust the measurement over the streak, not because the streak held.
concurrency: checked over the whole file. **No live claim names `affordances.mjs` at all.** RECORD's live claim at 2026-08-08 names `store.mjs` by SITE (`documentsNamingEntity` and the correspondence statics) and explicitly NOT `index.mjs`; the ai-credential claim names `classify`/`scopeFor`/`aiTaskScope` and the ai-credential ops in `index.mjs`, none of which is `decorateAct` or the affordances handler. `scripts/op-claims.mjs` was M0 (VERIFICATION)'s and that claim is `released: 2026-08-08`. `scripts/coverage.mjs` is touched for the `REGISTER_FLOOR` NUMERALS only — **if this conflicts at merge, COLLAPSE TO ONE SET and re-read the printed figures. Do not keep both.**
released: 2026-08-08 — committed on `worktree-agent-aed7f2e57ffe7cbab`, **NOT pushed and NOT merged**; CONDUCT integrates. **84 mutating ops derived from the dispatch table: 24 carry a rung, 60 carry a STATED absence, 0 unclassified — asserted TOTAL IN BOTH DIRECTIONS** (no mutating op missing from the classification, and no classification naming an op the table does not carry as mutating). Battery **132/132 green · 8,262 assertions · exit 0** against a true baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT of **131/131 · 8,209** — which AGREES with the brief’s ~131/~8,209 EXACTLY, stated because the practice is to trust the measurement rather than the streak. **Delta +53 attributed by DIFFING the two full runs, never by subtraction:** `rung-ladder.test.mjs` +47 (new), `affordances` 78 -> 81, `conclude` 49 -> 50, `reopen` 57 -> 58, `hygiene` 533 -> 534 (a new suite adds one determinism assertion). `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` UNPIPED, **exit 0**, provenance `138 of 138`. `node civicos-ui/test/run.mjs` from the repo root, exit unpiped, **0**. `node civicos-ui/check-refusal-codes.mjs` from the repo root, exit unpiped, **0**, every floor met and **no DEC-49 region touched** (`git diff` contains zero `DEC-49 REGION` marker lines; `regionLines` read 1310 and is a property of the MERGED source, so CONDUCT should re-read it at integration). `node tools/plancheck.mjs` **clean but for UNPUSHED**. `REGISTER_FLOOR` moved `546/127/128` -> **`552/128/129`** from the PRINTED figures, and moved AFTER the new suite was in a commit so the figures read are the REPRODUCIBLE ones, not the contaminated ones; **ONE key set, verified by grep**. **THREE CONTROL ARMS RUN, EACH ALONE, ALL THREE AS DECLARED, and re-run after every later edit:** (1) an unclassified mutating op planted in OPS -> 45/2, naming `frobnicate` in the FORWARD assertion while BACKWARD stayed green; (2) the mutating walk neutered -> 40/7, corpus PRINTING `0 declared mutating` — **and the finding this arm exists for REPRODUCED: the FORWARD totality assertion, this item’s own headline, STILL PASSED over the empty op set**, caught only by the printed-and-floored reach and by the BACKWARD direction; (3) over-strictness, a correctly classified op re-spelled compactly -> 47/0, passing only by NOT firing. **MY OWN INSTRUMENT WAS WRONG BEFORE THE SUBJECT WAS, and it is recorded in the suite rather than smoothed:** the first method-body reader took `indexOf("{")` from the method name and landed on the DESTRUCTURING brace of `release({ handle, … })`, reading the parameter object as the body and reporting NO justification refusal for six ops that plainly have one — caught only because I had read those refusals by eye first. **A SECOND CLASSIFIER DEFECT WAS CAUGHT BEFORE IT SHIPPED:** all six version acts share one `#moveVersionState` and one `VERSION_NO_REASON` refusal that fires for only TWO of them, so a textual classifier reports 6 ops demanding a reason where the truth is 2; the suite reads `versionNeedsReason`/`Store.VERSION_ACT_TO` instead. **Seven superseded pins CORRECTED, never exempted**, each carrying why the old value was right when written (affordances x5, conclude, reopen, divide, inquiryground, citeinquiry). **The row’s C-7 claim was CHECKED and HOLDS** — cite writes `status: "confirmed"`, sever’s from-set accepts it, so the derivation yields `reversible` and both halves are read out of `store.mjs` by the suite. IC-38 filed; the I3 bump and the RESOLUTION are CONDUCT’s. `git stash` was NOT used at any point.

## DELEGATION 2026-08-08 FRAMEWORK → UI
from: fw14-rung-ladder (worktree agent-aed7f2e57ffe7cbab), FW-14
what: **THE UI-17a RIDER IS NOW ACTIONABLE: `op=affordances` publishes `rung: "irreversible"` for `op=publish`, and the publication entry point still carries that word as SURFACE COPY.** The FW-14 row's rider states the swap exactly — *"when RUNGS.publish is assigned (DEC-19: IRREVERSIBLE, the top rung), UI-17a's entry-point section reads the rung off the act instead of carrying it as surface copy — one read swap, asserted."* The plane half is done: the act carries `rung`, the ladder is `vocabularies.rung_ladder` (ordered, `irreversible` last), and **the correction-path sentence rides with it as `vocabularies.rung_correction_path`** — which matters more than the rung, because "irreversible" without "correction moves forward" is the half that overclaims, and UI-17a's landing note records that its statement renders identically for every credential precisely because it is a statement about the record. Reading both off the plane is what stops the two from drifting.
why it is UI's: `civicos-ui/**` is UI's path and FRAMEWORK does not edit it. Nothing breaks today — the surface's own sentence and the plane's agree — so this is a delegation and not a blocker.
also worth the surface's attention, NOT owed: **`op=cite`'s rung moved from `null` to `reversible`** (C-7's answer, assigned). UI-20's landing recorded that its harness asserts the rung word appears NOWHERE for cite, on the grounds that cite published null; that assertion is in `civicos-ui`'s harness and is now pinning a stale premise rather than a stale value — it still passes, because the surface renders nothing, but what it is protecting has changed. `conclude`, `reopen`, `inquirydivide`, `inquiryground` and `actionmove` likewise moved from `null` to `reasoned`. **And every act now carries `rung_absence`**, so a surface can say WHY an act has no rung instead of rendering a blank: the grounds vocabulary is `vocabularies.rung_absence_grounds`.
what a surface must NOT do with it: `reversible` does not mean erasable — severing leaves the edge in the record with `status: "severed"` and the member's reason — and the five absence grounds are not a severity order (four mean the ladder does not reach that act; only `undetermined` means "no rung yet").

## CLAIM 2026-08-08 FRAMEWORK (FW-15)
session: fw15-deletions-disposition (worktree agent-a2c81ec63553ea011)
opened: 2026-08-08T00:00:00Z
paths: **`bio-plane/checks/bio-checks.mjs` — named by SITE, not by file:** (1) `checkDeletionRecords` (DELETED) and its one call line inside `checkBundle`, replaced by a pointer comment; (2) ONE new row `'C-7.1'` in the EXISTING `CHECK_RETIREMENTS` table with its reasoning block. **NOT opened:** any other check, `ACT_SHAPE_CHECKS`, `ROUTE_MARK_CHECKS`, any DEC-49 region or refusal string (the `FILES_DROPPED` row this item names as the supersessor was READ, never edited), `ISO_TS_RE` and every other constant the deleted function used (all still read by other checks, left exactly as they were), and C-8.1's existing `CHECK_RETIREMENTS` row. `bio-plane/test/check-firing.test.mjs` (this item's own suite — the C-7.1 `proves()` block replaced by a pointer, the retirement block widened to be keyed by retired id, the header's SUPERSEDED note and `NEGATIVE CONTROL:` line extended). `bio-plane/test/retirement.control.mjs` (FW-13's harness, WIDENED IN PLACE rather than copied — see below). `bio-plane/scripts/coverage.mjs` (`REGISTER_FLOOR.arms` ONLY, and nothing else in the file). `docs/development/QUEUE.md` (the FW-15 row's disposition ONLY). `docs/development/CLAIMS.md` (this entry and its DELEGATION). **NOT** `bio-plane/src/**`, **NOT** any other `.test.mjs`, **NOT** `civicos-ui/**`, **NOT** `docs/architecture/**`, **NOT** `pdf-worker/**`, **NOT** `agent-worker/**`, **NOT** `newgroup/**` (`newgroup/src/release.mjs` was READ to exclude it from the estate walk, as FW-13 established).
interfaces consumed: none
interfaces owned: none — the check catalogue is not behind an interface, and no op shape moved. The row said `I3 if it binds and publishes; none if it retires`. It retires, so **none**.
expected: QUEUE.md FW-15 — decide `data/deletions.json` / C-7.1: RETIRE, or BIND. **DECIDED: RETIRE.**
measured before deciding, in four passes, because the row's own premise turned out to be the thing that needed measuring:
  **(1) PRODUCERS AND CONSUMERS — none.** FW-13's estate walk, same seven roots, same `newgroup/src/release.mjs` exclusion: **0** files write or read `data/deletions.json` in a corpus of **118**, against `data/provenance.json` 9, `_history/manifest.json` 4, `data/inbox.json` and `data/dataset.json` 2 each. **A SURPRISE WORTH RECORDING RATHER THAN SMOOTHING:** the first run of that walk printed **116**, not 118, and it was the run taken while a full battery was executing against the same tree; two subsequent runs on a quiet tree printed 118, which is also what the suite's own walk prints and what FW-13 recorded. `git status --untracked-files=all --ignored` over the seven roots is empty, so nothing persists. **The corpus figure is not stable while a battery runs against the same checkout, and must be measured on a quiet tree.** The reproducible figure is 118 and that is the one written into the catalogue.
  **(2) DOES THE CHECK FIRE, AND IS IT THE CHECK DOING THE WORK.** Driven through `checkBundle` on sixteen planted inputs. C-7.1 **FIRED** on six distinct malformed ledgers (not `{records:[…]}`; an empty record; no reason; no `preserved_to`; a non-ISO timestamp; an empty `items[]`), drew **NOTHING** on a well-formed one, and **FIRED ALONE**: the identical tamper under `data/deletionz.json`, `data/removals.json`, `data/deleted.json` and bare `deletions.json` drew **no error at all**, so nothing else was doing its work. The path stays ordinary data — an unparsable one draws C-14.3, a badly named one C-14.2. **WHICH OF THE THREE FINDINGS THIS IS: NEVER FIRED. Not "cannot fire" (it fires on six shapes) and not "redundant" (nothing else draws) — a check with NO PRODUCER, which had therefore never fired on anything real.** That arm is now a STANDING assertion in the suite, not a one-time measurement, for both retired ids.
  **(3) THE ROW'S PREMISE IS FALSE, AND CORRECTING IT IS THE ITEM.** The row reads *"NOTHING COLLIDES WITH A DELETION LEDGER, so the retire argument does not carry, and C-7.1 PLAUSIBLY WANTS BIND"*. Something does collide, and it is not another ledger — it is the mechanism that already discharges all three of State Rules §2.5's requirements, at the only write in the plane that can remove anything. **Measured on the running code, not read off a comment** (a scratch harness driving `promote` through miniflare, since deleted): a promotion that drops a file by OMISSION is refused `FILES_DROPPED` naming the path (C-33.24, DEC-49 region `is-promote-files`, with a member-facing translation) — that is §2.5's *stated reason / never silent*; naming it in `drop[]` succeeds, and the removed bytes are then **byte-identical at `_history/data/extra_<snapKey>.json`**, listed in that promotion's `snapshotted[]` while absent from its `files[]` — that is §2.5's *preservation*, and C-12.2 fires if a recorded snapshot goes missing; every projection is rebuilt in the same transaction, so an edge into removed material reads as an unresolvable C-6.2 finding — that is §2.5's *cascade*, with C-5.1 refusing any append-only surface that shrank. **So `data/deletions.json` was a SECOND ACCOUNT OF ONE FACT — what the record no longer holds — sitting beside a machine-kept account of the same fact with no rule saying which is authoritative. That is D-69's diffusion, the same defect that retired C-8.1, arriving through a different door: not two claim structures, but two accounts of an absence.** §2.5 was written for the drive substrate, where a session with create-only access physically moved files and the only way to know a removal had happened was for somebody to write it down; the plane meets the same guarantee structurally, which is why the ledger went dark without anyone noticing.
  **(4) AND IT IS WORSE THAN C-8.1's, WHICH IS WHAT SETTLED IT.** Measured: C-7.1 validated the SHAPE of a deletion claim and **nothing about its truth**. A ledger naming `bundle.md` as deleted while `bundle.md` sat in the same bundle drew nothing; a `preserved_to` pointing at a path the bundle did not hold drew nothing; two records in descending time order in a ledger the spec calls append-only drew nothing. Every one of those is well-formed by C-7.1's rule and false about the record. **An absence is precisely the claim a reader cannot check from the bundle**, so a hand-authored *this was removed, and it is kept over there* is the record claiming more than it can support — which `CLAUDE.md` ranks above a missing feature. The hand-fillable structure the plane knows nothing about is the same hazard that decided C-8.1; here it is pointed at the one kind of claim that cannot be falsified from inside the document.
reasoning — **WHAT A DELETION LEDGER WOULD MEAN AGAINST A RECORD THAT DOES NOT ERASE, AND WHOSE ACT IT WOULD RECORD**, which is the question the row asked and the reason BIND was not automatic: **none that exists.** A **MEMBER** has no delete. Correction moves FORWARD (DEC-19 as amended): a new edition as a separate document with every published edition standing, a withdrawal as a further attested act with both standing, a claim removed from a finding rescinding it to an inquiry. Every one of those is an ADDITION, and each is already carried — by `state_history` and the Review Notes / Session Log surfaces C-5.1 holds verbatim-immutable, by the `_history/` snapshot chain, and by `inquiry_basis_versions`. An **OPERATOR's** `op=purge` is EVICTION, not gated deletion: it is admin-only (`purge is the only destructive op`), it takes the `history` and `manifest` tables with the bundle so it can never write a `preserved_to`, and it destroys the very bundle a per-bundle ledger would live in — a ledger that cannot survive the act it records is not a ledger. A **PURGE** is that same act. And published bytes are exempt from all of it by doctrine: `published_bundles` and `published_shas` are untouched by either purge arm, because a hash once published answers forever. **So the ledger has no author, no act, and no fact left to carry — while the two things §2.5 actually wanted, "say that you meant it" and "keep what you removed", are both already enforced by mechanisms with a producer, a consumer and a check that FIRES.** Binding it would have built a shape that is authorable and not actable, which is the defect REC-72 measured.
accepts-when: as the FW-15 row. The retirement is RECORDED, not deleted: `CHECK_RETIREMENTS` gains a `'C-7.1'` row stating what it gated, why the old rule was wrong, the measurement, and what supersedes it (`drop[]` / FILES_DROPPED C-33.24 + the `_history/` chain C-12.1/C-12.2 + C-5.1). `coverage.mjs` derives CHECKS by regexing `C-\d+\.\d+` over the whole catalogue source, so the id SURVIVES that derivation through the retirement block and `--strict` keeps demanding an assertion naming it — and the assertion naming it is the one PROVING IT NO LONGER FIRES. **`check-firing.test.mjs` was WIDENED, not duplicated**: the retirement block is now keyed by retired id and reads both the ids and the gated paths out of the catalogue, and it gained a **COMPLETENESS** arm — a row in `CHECK_RETIREMENTS` with no behavioural arms declared now FAILS, where FW-13's flat list would have let a future retirement be silently covered by another check's arms.
numbers: **baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 131/131 suites green · 8,219 assertions · 118.7s · exit 0**, provenance `133 of 133 discovered item(s) are in the commit at HEAD (61b95d7)`, `git status --short` empty. **This AGREES with the brief's ~131/~8,219 exactly** — recorded because the practice is to trust the measurement rather than the streak. **The worktree arrived one merge behind `main` and without `bio-plane/node_modules`; `npm ci` was run and the branch fast-forwarded to `61b95d7` so this item builds on FW-13's landed mechanism rather than re-inventing it.**
floors: **ONE MOVED, UPWARD, from a printed figure: `REGISTER_FLOOR.arms`.** `classified` (127) and `corpus` (128) are UNMOVED and untouched — no suite was added or removed, and `retirement.control.mjs` is not a `.test.mjs` so the battery does not discover it. `arms` is a floor on arms STATED across classified declarations, and this item extends `check-firing.test.mjs`'s `NEGATIVE CONTROL:` declaration with FW-15's arms, so the measured figure rises. **The baseline run of this worktree already printed `arms 550/546 · GREW by 4` — FW-13 added four arms and did not collect the floor, so it was already stale by 4 before this item touched it.** Moved to the figure this item's own green `--strict` run PRINTED; the reason it rises is a declaration growing, which is the only direction this key may move. **No floor FELL.** The DEC-49 guard is untouched: C-7.1 is a CHECK id, not a refusal code, and `CHECK_RETIREMENTS` is NOT a `*_CHECKS` export — FW-13 paid for that lesson and the reserved suffix was respected here without re-learning it.
what came back WRONG, recorded rather than smoothed, because it is the finding this item's controls actually produced: **CONTROL ARM (v) — the new UNCOVERED arm — did not fail as declared on its first run. It came back `THE SUITE NEVER REACHED ITS FOOT`.** The cause was a real defect in the widening: the different-filename loop indexed `RETIRED_SHAPES[id][1]` unguarded, so a retired id with no shapes threw a `TypeError` that ended the MODULE through no assertion at all — the exact failure this project recorded twice this week, and the arm that should have spoken (COMPLETENESS) never ran. **It was caught only because FW-13's harness READS THE FOOT LINE instead of trusting a tally**, which is that mechanism earning its keep on the first item after the one that built it. Corrected at the site with the receipt in the comment; the arm then behaved exactly as declared (84 pass, 1 FAIL naming C-7.1). **Every other arm-run behaved as declared on every run.**
concurrency: checked over the whole file. No live claim names `checkDeletionRecords`, the deletion ledger, `check-firing.test.mjs` or `retirement.control.mjs`. The live claims that open `checks/bio-checks.mjs` open it BY SITE and by different sites (the type catalog; three rows in `ACT_SHAPE_CHECKS`; the counterparty constants). `coverage.mjs` IS opened, for `REGISTER_FLOOR.arms` and nothing else — **if this conflicts at integration, COLLAPSE TO ONE KEY SET and re-read the printed figures; do not keep both, because the last duplicate key silently wins and on one occasion it was the lowest.**
released:

### DELEGATION 2026-08-08 FRAMEWORK (FW-15) -> CONDUCT: **§2.5's CASCADE VOCABULARY IS THE THIRD MEMBER OF THE SAME CLASS, AND IT IS NOT AN ORPHAN IN THE SAME WAY**

Closing C-7.1 leaves one piece of State Rules §2.5 still standing with no producer, and it is
deliberately NOT decided here because it is a different shape from the two the sweep closed.

`REEVAL_SOURCES` in `bio-checks.mjs` is `['deletion', 'source_status', 'wp_retraction',
'annotation']`, and C-10.1 refuses a `reeval_pending.source` outside it. **`'deletion'` is a
legal value that nothing in the estate ever writes** — the only source the plane itself
produces is `source_status`, from the monitor tick. It is NOT an orphan check: C-10.1 has a
real producer, fires on other arms, and is proven to fire by this suite. It is **one value of
one enum** with no writer, which a check-level sweep cannot see and which no assertion would
catch. `'wp_retraction'` and `'annotation'` are worth the same question.

**Why it was not folded into this item:** FW-15's row rules on `data/deletions.json` and on
C-7.1. Widening a retirement into an enum inside a live, producing check is a different act
with a different blast radius, and doing it here would be the guess the row's own doctrine
forbids — the same reason FW-13 handed C-7.1 back rather than deciding it. **It probably wants
a rider on FW-14** (which is already assigning a ladder rung to every mutating op and will
have to say what `op=purge` is), not a row of its own.

**One limit of the class sweep, restated because it still holds:** a producer living in a
document is invisible to a walk over code — `PENDING_PROMOTION.json` has a real producer in
the bundle skill, outside this repository. Nothing in this item's measurement depends on that
distinction (`data/deletions.json` has no producer in code OR in a document: §2.5 specifies
the FILE, and the substrate that would have written it is the one the plane replaced), but the
next member of the class may.

## CLAIM 2026-08-08 CONTENT-PDF (CPDF-12 — the page-to-pixels renderer)
session: cpdf12-pagepixels (worktree agent-a3bae0147bbd1869e)
opened: 2026-08-08 (RECORDED LATE, and said so rather than back-dated: the claim was
  written after the module and its suite were already green in this worktree. Nothing
  else touched these paths in the interval — `pdf-worker/**` has no other live claim and
  `pdfstructure.mjs`'s only live claim is CONTENT-PDF's own from 2026-07-31 — but the
  rule is claim-before-edit and this entry did not obey it.)
paths: **`pdf-worker/src/pagepixels.mjs` (NEW)** — the renderer. **`pdf-worker/src/pagepixels-worker.mjs` (NEW)** — a probe-only workerd entry point; NOT the member's surface and NOT wired into `pdf-worker/src/index.mjs`. **`pdf-worker/test/pagepixels.test.mjs` (NEW)** — the battery-resident suite. **`pdf-worker/test/pagepixels.control.mjs` (NEW)** — its seven negative-control arms, re-runnable in one step. **`pdf-worker/test/pagepixels-corpus.probe.mjs` (NEW)** — the measurement instrument (a `.probe.mjs`: NOT discovered by `battery.mjs` or `coverage.mjs`). **`pdf-worker/test/fixtures/scan-ccitt-g4-page.pdf` (NEW)** — the hermetic fixture. **`bio-plane/src/pdfstructure.mjs` — ONE WORD**: `class PdfDoc` becomes `export class PdfDoc`, with a comment saying why. `docs/development/MEASUREMENTS.md` (APPEND). `docs/development/DEBT.md` (APPEND — D-244/245/246, all minted with `tools/mintid.mjs`). `docs/development/kickoffs/CONTENT-PDF.md` (this area's own file — the landing section and the decision running provisionally). `docs/development/QUEUE.md` (**the CPDF-12 row's `landed:` line ONLY**). `docs/development/CLAIMS.md` (this entry and its delegations). **NOT** `bio-plane/src/index.mjs`, **NOT** `store.mjs`/`schema.mjs`/`query.mjs`, **NOT** `bio-plane/checks/**`, **NOT** any other test suite, **NOT** `bio-plane/scripts/**`, **NOT** `civicos-ui/**`, **NOT** `newgroup/**`, **NOT** `docprofile/**`, **NOT** `tools/**`.
interfaces consumed: none reshaped. The renderer reads PDF bytes and emits an image; it touches no op, no envelope and no stored shape.
interfaces owned: none yet — **I6 IS DELIBERATELY NOT EXTENDED.** The member's `SURFACE` table is unchanged and no route reaches this module in a deployed Worker. Adding a surface op is a change to I6 and belongs with the item that CONSUMES the pixels (CPDF-10), not with the measurement that establishes they can be produced.
expected: QUEUE.md CPDF-12 / DEC-42's re-scope. **The one instruction that shaped everything: "for the image-only class a page is typically ONE embedded image, so image EXTRACTION may serve where rasterising was assumed — VERIFY ACROSS THE CORPUS BEFORE BUILDING A RENDERER."** It was verified first and it held at 24 of 24.
accepts-when: as the CPDF-12 row. **Baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 132/132 suites green · 8,256 assertions · 112.5 s · exit 0**, provenance `134 of 134 discovered item(s) are in the commit at HEAD (054fb4f)`; `node bio-plane/scripts/coverage.mjs --strict` exit 0 (`arms 557/557 · classified 128/128 · corpus 129/129`); `node civicos-ui/test/run.mjs` from the repo root exit 0. **THE BRIEF'S FIGURES WERE EXACTLY RIGHT (~132/132, ~8,256) and that is recorded rather than assumed** — the practice is to trust the measurement, not the streak.
concurrency: `bio-plane/scripts/coverage.mjs` **is not edited** — see the delegation below about `REGISTER_FLOOR`. The one shared file touched is `bio-plane/src/pdfstructure.mjs`, for a single `export` keyword on an existing declaration; if that conflicts at merge, KEEP THE EXPORT (removing it breaks `pdf-worker/src/pagepixels.mjs`'s import and reds the battery).
released: 2026-08-08 — committed on `worktree-agent-a3bae0147bbd1869e`, NOT pushed and NOT merged; CONDUCT integrates.

## DELEGATION 2026-08-08 CONTENT-PDF → CONDUCT: the register floor moved and I did not move it
from: cpdf12-pagepixels (worktree agent-a3bae0147bbd1869e), CPDF-12
what: this item adds ONE suite (`pdf-worker/test/pagepixels.test.mjs`) with a
  multi-arm `NEGATIVE CONTROL:` declaration, so `REGISTER_FLOOR` in
  `bio-plane/scripts/coverage.mjs` GREW rather than fell. **The floor is
  deliberately NOT edited here.** Eleven items moved that one key set in parallel
  on 2026-08-08 and keep-both merges left duplicate `arms:` keys four separate
  times — valid JavaScript where the last silently wins, and the last was the
  lowest. The figures this worktree's `--strict` printed after the change are in
  the released line above; **re-read them from a green run of the MERGED tree
  rather than trusting these**, which is REC-63's own instruction and the reason
  five consecutive items raised this rather than editing it.
why it is CONDUCT's: one writer, one merge, one re-read.

## DELEGATION 2026-08-08 CONTENT-PDF → WHOEVER OWNS THE FLEET (and CPDF-10 first)
from: cpdf12-pagepixels (worktree agent-a3bae0147bbd1869e), CPDF-12
what: **the renderer exists, is verified, and is NOT REACHABLE BY ANYTHING.**
  `pdf-worker/src/index.mjs`'s `SURFACE` is untouched, so no deployed Worker can
  ask for a page of pixels. Wiring `render` into that table is an I6 change (a
  new surface op, a `mutating: false` row, a control-plane assertion in the same
  turn per VERIFICATION.md item 4) and it belongs to the item that consumes the
  pixels. **Do not read "it runs in workerd" as "it is wired": those are the two
  halves D-108 exists to keep apart.**
why it is not done here: CPDF-12 is the measurement plus the module. An op nobody
  calls is an op nobody tests, and this item would have shipped the surface
  without the caller.

## DELEGATION 2026-08-08 CONTENT-PDF → WHOEVER NEXT OWNS `bio-plane/scripts/control-register.mjs`
from: cpdf12-pagepixels (worktree agent-a3bae0147bbd1869e), CPDF-12
what: **D-247 — the negative-control register's corpus is PLANE SUITES ONLY, so a
  fleet suite's declaration is invisible and a fleet suite with NO declaration
  changes nothing.** Measured, not inferred: this item added a seven-arm
  declaration and `REGISTER_FLOOR` did not move (`arms 557/557 · classified
  128/128 · corpus 129/129`, identical before and after); stripping the whole
  block from the new suite left `--strict` at **exit 0** and the register still
  printing `129 of 129 suites declare one (100.0%)`. The restore was verified by
  sha256 (20,492 B, `9868c684…`) and by `git status`. The FLEET section's
  `2/2 declaring a negative control` is a per-MEMBER check: a member passes if
  ANY ONE of its suites declares one.
why it is not fixed here: `bio-plane/scripts/**` is not this item's paths, and
  widening the corpus MOVES all three `REGISTER_FLOOR` numerals — which eleven
  items moved in parallel on 2026-08-08 with keep-both merges leaving duplicate
  `arms:` keys four times. That number must be re-read from a PRINTED green run
  of the MERGED tree, which is the integrator's position, not a branch's.

## CLAIM 2026-08-08 M0 (test-estate / tooling lane) — D-243 + D-242
session: d243-detect-unminted (worktree agent-a14199b914b9086e2)
opened: 2026-08-08T21:33:00Z
paths: **`tools/mintid.mjs`** (the register, the allocation-site patterns, the new `--audit` mode, the exclusivity probe and the SCOPE line). **`bio-plane/test/mintid.test.mjs`** (its suite — new sections). **`tools/plancheck.mjs`** (ONE new block: the duplicate-allocation gate; it also removes the dead `readdirSync` import M0-16 delegated). `bio-plane/test/hygiene.test.mjs` (**ONE line added to `CLASS_NAMED_UNGUARDED`, nothing else** — the walk-class ratchet named this item's new sandbox walk on its first full battery, which is the ratchet working and is a decision that has to be recorded at the site). `docs/development/kickoffs/CONDUCT.md` (**step 2 only** — the integration-side audit command; M0-17 edited step 1 and this edits step 2). `docs/development/DEBT.md` (**D-242 and D-243 rows amended; D-248 appended** — append-only otherwise). `docs/development/MEASUREMENTS.md` (append). `docs/development/CLAIMS.md` (append).
interfaces consumed: none
interfaces owned: none — tooling only. No plane behaviour, no op, no field, no shape.
expected: **D-243** — nothing detects an id taken WITHOUT `tools/mintid.mjs`; **D-242** — the ledger is one clone on one machine and the real cost is the CONFIDENCE rather than the collision.
accepts-when: **Baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 133/133 suites green · 8,319 assertions · 116.2 s · exit 0**, provenance 135/135 discovered items in the commit at HEAD (`bb426ac`). Battery green at the end, `node scripts/coverage.mjs --strict` exit 0 read UNPIPED, `node civicos-ui/test/run.mjs` from the repo root exit 0, `node tools/plancheck.mjs` clean but for UNPUSHED.
concurrency: `tools/mintid.mjs` and `bio-plane/test/mintid.test.mjs` are named by no other live claim (M0-17's claim is `released:`). `tools/plancheck.mjs` is opened in ONE new block plus the dead-import removal M0-16 delegated; no live claim names it. `docs/development/kickoffs/CONDUCT.md` step 2 only — M0-17 took step 1 and is released. **`docs/development/DEBT.md` D-242 and D-243 are AMENDED IN PLACE**, which is the one non-append edit here: both rows are M0-17's own residuals and this item is the one that answers them. **`bio-plane/scripts/coverage.mjs` is touched for the `REGISTER_FLOOR.arms` NUMERAL ONLY (570 -> 576) — if this conflicts at merge, COLLAPSE TO ONE SET and re-read the printed figures; do NOT keep both, which has left duplicate `arms:` keys there six times.**
released: 2026-08-08 — committed on `worktree-agent-a14199b914b9086e2`, NOT pushed, NOT merged. **TRUE BASELINE RE-MEASURED AT `origin/main` (`86f0e73`) IN A SCRATCH WORKTREE rather than carried from the pre-rebase run: 134/134 suites green · 8,386 assertions · 128.9 s · exit 0.** Final: **134/134 · 8,431 assertions · 130.3 s · exit 0**, provenance 136/136 discovered items in the commit at HEAD. **Delta +45 attributed by DIFFING the two runs per suite:** `mintid.test.mjs` 33 -> 77 (+44), `planning-hygiene.test.mjs` 282 -> 283 (+1, the new D-248 row), **130 of 132 suites identical in count**. `node scripts/coverage.mjs --strict` exit 0 read UNPIPED — 159/159 ops · 208/208 checks · `REGISTER_FLOOR.arms` **570 -> 576** from the figure a green run PRINTED (`GREW by 6 arm(s)`), `classified` 129 and `corpus` 130 unmoved. `node civicos-ui/test/run.mjs` from the repo root exit 0, unchanged. **SIX control arms declared before arming, each run ALONE, all six agreed**, restores verified by sha256 AND `cmp` against uniquely-named per-arm pristine copies with byte counts printed and guarded against the empty-string sha256; **M0-17's legacy arms (1) and (4) re-run because `mint` was restructured — both still fire** (arm (1) gave all eight racers ONE AND THE SAME id — **the number is deliberately not written here: this file is `D`'s own corpus, and naming it would raise the floor off prose, which is the defect M0-17 caught in its own debt row and this item caught again in this very sentence**). **THREE INSTRUMENT FINDINGS, all recorded rather than smoothed** — a control arm reading `-1 pass, -1 fail` because `process.exit()` truncated a large PIPED stdout and lost the tally; M0-17's bulk-take hardening covering a THROW but not a REFUSAL, so arm (11) ended the module before its declared arms reached an assertion; and **`check-firing.test.mjs` reading a prose mention in this tool's own comment as a PRODUCER for a retired shape** — the documentation-poisons-a-corpus class for the third time in two days, in a third instrument, failing in the safe direction. `hygiene.test.mjs`'s walk-class ratchet named this item's new sandbox walk on the first full battery, which is why that file is in the paths above. Residual: **D-248** (renumber the six), delegated to CONDUCT rather than smuggled in.

## DELEGATION 2026-08-08 M0 (D-243) → CONDUCT: **SIX IDS ARE ALLOCATED TWICE IN `origin/main`, AND THREE OF THEM ARE IN YOUR FILE**
from: d243-detect-unminted (worktree agent-a14199b914b9086e2), D-243
what: **D-248.** The in-commit detector this item built found six live collisions on its
first run over the real corpus, known to nobody: **`D-121`**, **`D-124`**, **`IC-30`**,
**`CPDF-9`**, **`FW-15`**, **`M0-16`**. `D-124`'s first row already reads *(renumbered
from a colliding D-122 by CONDUCT 2026-07-31)* — **it was renumbered onto a second
collision**, which is the sharpest available argument that a renumber done by hand needs
an instrument watching it. `IC-30` is the worst of the six because an interface-change
number is a contract identity two areas build against.
why it is yours and not a worker's: **three of the six (`CPDF-9`, `FW-15`, `M0-16`) are
`QUEUE.md` headings and you are its sole writer**, and a renumber is a by-the-number sweep
across code, suites, claims and **reports written by sessions that have ENDED** — the exact
cost M0-17 priced when it chose minting over mechanising the renumber. Doing it inside a
tooling item would have been a worker editing another area's paths on a sweep it cannot
verify.
how to do it safely, and the mechanism is already in your loop: renumber the LATER member
of each pair **with a minted id** (`node tools/mintid.mjs <NS>`), sweep every reference —
**including regex literals, where `C-29\.` is not the text `C-29.`** — then **DELETE that
entry from `KNOWN_COLLISIONS` in `tools/mintid.mjs`**. The register has no slack in either
direction: a seventh collision FAILS `plancheck` and the battery, and **a registered
collision that has stopped being one ALSO fails**, so a renumber that leaves its entry
behind cannot pass. `node tools/mintid.mjs --audit` reports the whole state in one command.
### CLAIM 2026-08-08 PLANE-TEST · d240-agent — **D-240: THE TWO REMAINING CLASSIFIERS THAT GRADE A RETURN BY ONE LITERAL**

item: D-240 (DEBT.md), delegated by REC-76's class sweep because both files carry ratcheted
figures only their owner can move honestly.

paths: **`bio-plane/test/meaning-bounds.test.mjs`** — the `REFUSAL_RETURN` excluder, its census, its
arms and its `NEGATIVE CONTROL:` header. **`bio-plane/test/plane-envelope.test.mjs`** — DETECTOR A's
gate, its census, its arms and its `NEGATIVE CONTROL:` header. **`bio-plane/test/verdict-reader.mjs`
(NEW)** — REC-76's six reader functions, byte-identical, plus `readerDrift()`.
**`bio-plane/test/verdict-excluder.control.mjs` (NEW)** — the ten control arms, deliberately NOT a
`.test.mjs` because it mutates five files while it runs. `docs/development/DEBT.md` (D-240's
disposition and the new D-254 row), `docs/development/MEASUREMENTS.md` (one appended entry),
`docs/development/CLAIMS.md` (this entry and its DELEGATION).
**NOT OPENED: no file under `bio-plane/src/`, `bio-plane/checks/`, `bio-plane/scripts/` or
`civicos-ui/`.** The control MUTATES `src/index.mjs` and `src/store.mjs` transiently while it runs
and restores both, verified by sha256 AND by content — `check-mock-envelope.mjs`'s and
`refusal-codes.control.mjs`'s own precedent. `civicos-ui/check-refusal-codes.mjs` is READ ONLY, by
`readerDrift()`; the one line that would make the reader single-homed is a DELEGATION below.

accepts-when: both classifiers grade by whether the return DECLARES itself, what cannot be
classified is NAMED, every floor invalidated moved in the same turn from a PRINTED figure, and the
class sweep re-run rather than inherited.

numbers: **MEASURED TWICE, BEFORE AND AFTER THE REBASE, AND EVERY BASELINE RE-RUN RATHER THAN
SUBTRACTED.** On the spawn tree (bb426ac): baseline **133/133 · 8,319 · 124.4s · exit 0** ->
final **133/133 · 8,332 · 130.2s**. On the SHIPPED tree, rebased onto `origin/main` 86f0e73:
baseline **134/134 · 8,387 · 119.3s** -> final **134/134 · 8,400 · 126.4s · exit 0**. **The +13 is
identical on both and is meaning-bounds 85 -> 92 and plane-envelope 54 -> 60; no other suite
moved.** `node scripts/coverage.mjs --strict` exit 0 read UNPIPED; `node civicos-ui/test/run.mjs`
from the REPO ROOT exit 0; `node tools/plancheck.mjs` 0 fail, 0 warn, clean but for UNPUSHED.
**The worktree arrived without `bio-plane/node_modules` (`npm ci` run) and FIVE commits behind
`origin/main` — checked before trusting anything: `git diff HEAD origin/main` over `store.mjs`,
`index.mjs`, both suites, `check-refusal-codes.mjs` and `coverage.mjs` was EMPTY, so no figure
below was taken over a stale copy of its own subject.** The branch was then rebased onto
`origin/main`; the only conflicts were append-vs-append in `CLAIMS.md`, `DEBT.md` and
`MEASUREMENTS.md`, resolved KEEP-BOTH, and **the minted D-254 stepped over 244-253 so it does not
collide with CPDF-12's D-244..D-247, which landed in those five commits.** The whole battery,
`--strict`, the UI harness and all ten control arms were re-run AFTER the rebase; the ten arms
came back as declared a second time and the tree was byte-clean afterwards.

floors: **NONE MOVED, AND THAT IS A MEASUREMENT RATHER THAN AN OMISSION.** (i) The BARE ratchet
(38 ceiling / 38 floor) and the OPAQUE ceiling (10) are UNCHANGED because the corrected excluder
moves no op between buckets on this tree — every roster is identical, printed, and the value of the
correction is prospective, which control arm (5)/(5b) demonstrates by planting the case that would
otherwise break the ceiling over a non-defect. (ii) `REGISTER_FLOOR` is UNCHANGED at 570/570 ·
129 classified · 130 corpus, **verified by restoring both suites to HEAD and re-running
`coverage.mjs --strict`, which printed the same 570** — the register records the FULLEST SINGLE
declaration per suite and both files already declare fuller ones (REC-60's eleven, REC-53's eleven).
**`REGISTER_FLOOR` WAS THEREFORE NOT TOUCHED, which is the right answer under a raised concurrency
budget and is stated here so a conflict resolver does not have to guess:** if this file conflicts
there, it is not this item's edit.

**A FIGURE THAT LOOKS LIKE A REGRESSION AND IS NOT, said before anyone finds it:** the first draft
of the excluder used REC-76's own policy verbatim (a COMPUTED verdict is a refusal) and the suite
went **85/0 -> 82/3 with BOUNDED falling 20 -> 16 and OPAQUE rising 10 -> 16.** That is the reader
getting BLINDER, not the plane getting worse, and the cause is that a COMPARISON in this plane is
usually a CURSOR or a TRUNCATION FLAG rather than a verdict. The policy shipped is `false` only, the
divergence from REC-76 is written at both sites with the numbers that chose it, and the fixtures
that refused the first draft are standing arms rather than a sentence in a report.

concurrency: checked over every unreleased block in this file. No live claim names
`meaning-bounds.test.mjs`, `plane-envelope.test.mjs`, `verdict-reader.mjs` or
`verdict-excluder.control.mjs`. The live VERIFY amendment (rec71-agent) names
`bio-plane/checks/bio-checks.mjs` and `bio-plane/src/store.mjs` BY SITE — neither is opened here
except transiently by the control, which restores both. `civicos-ui/check-refusal-codes.mjs` is not
edited.

released:

### DELEGATION 2026-08-08 PLANE-TEST (D-240) -> WHOEVER OWNS `civicos-ui/check-refusal-codes.mjs` (VERIFY): **MAKE REC-76's VERDICT READER SINGLE-HOMED — IT IS ONE LINE, AND THE PIN THAT HOLDS IT UNTIL THEN IS ALREADY GREEN**

D-240 needed REC-76's verdict reader in two `bio-plane/test/` instruments. **It could not be
imported**: `check-refusal-codes.mjs` exports nothing, and its work runs at the top level ending in
`process.exit`, so importing it runs the whole DEC-49 guard as a side effect of loading a test. It is
also VERIFY's file. So the six functions were COPIED, byte-identically, into
`bio-plane/test/verdict-reader.mjs`, and the copy is held falsifiable by `readerDrift()`, which
extracts them from both files and compares — asserted by BOTH suites, floored and counted, and driven
in both directions by control arms (3) and (3b).

**WHAT WOULD CLOSE IT (D-254):** replace the six declarations in `check-refusal-codes.mjs` with
`import { skipString, matchBrace, outcomeReturns, topLevelProps, verdictKind, verdictOf } from
"../bio-plane/test/verdict-reader.mjs";`, then delete the drift-pin half of `verdict-reader.mjs`'s
header and turn its two `D-240 (a)` arms into an import assertion. **Do not do the reverse** (move
the reader into `civicos-ui/`): `bio-plane/test/` files may not import from `civicos-ui/` without
the same top-level-execution hazard reappearing in the other direction.

**ONE THING VERIFY SHOULD KNOW BEFORE TOUCHING THE READER, because it is a finding about REC-76's
rule rather than about the copy.** `verdictOf` takes the FIRST boolean-shaped top-level property, and
REC-76 measured over its SIXTY governed sites that no outcome leads with a datum. **That does not
hold over `store.mjs`'s 843 return-object literals.** Two SUCCESS returns lead with a boolean DATUM —
`#sessionRights` (`rootOfTrust: false`) and `#conditionHomes` (`ungrouped: false`) — and a
COMPARISON is how this plane spells a CURSOR (`bundles.length === cap ? …`) and a TRUNCATION FLAG
(`found.length > cap`) in at least seven reads, including `projection`, `listBundles`,
`aiCredentials`, `captureRequests`, `searchIndexCheck` and `verifySha`. **Neither fact makes REC-76's
guard wrong** — its corpus is the governed sites and its polarity is the safe one there — **but it
means the ordering rule is a property of that corpus and not of the plane**, and if arm C's reach
ever widens beyond the governed sites it will meet both. Both are named and pinned in
`meaning-bounds.test.mjs`; nothing is asked of VERIFY today.
## CLAIM 2026-08-08 M0 (D-237, the reporting half)
session: d237-residue-outside-the-fence (worktree agent-ad415bce6d85ec969)
opened: 2026-08-08T00:00:00Z
paths: **`bio-plane/scripts/residue.mjs` (NEW — entirely this item's)**: the shared-temp scan, the pid-chain HELD arm, the three-state classification and the report. **`bio-plane/scripts/battery.mjs` — named by SITE, not by file:** (1) one added import; (2) the block after `residueBefore` that derives the shared roots and takes the opening scan; (3) `takeHeldSample` (NEW) and the two timers inside `run()`; (4) six lines in the per-suite loop taking the shallow snapshot; (5) three added words (`INSIDE $TMPDIR`) in the existing fenced line; (6) the `else if (leakedAfter > leakedBefore)` note, CORRECTED; (7) the closing scan and the `reportResidue` call; (8) a comment block on `orphans()` naming it as a second instance of the class. **NOT opened:** discovery (both paths), the sweep's rules or its `LEGACY`/`OWNED` lists (both are now HANDED to the scanner rather than restated), `residue()`, the D-186 assertion, the exit code, the provenance block, the tally/skip parsers, the fleet line. **`bio-plane/test/battery-residue.test.mjs` (NEW)** and **`bio-plane/test/battery-residue.control.mjs` (NEW harness, deliberately not a `.test.mjs`: it edits real sources, and the battery discovers by that suffix)**. **`bio-plane/test/hygiene.test.mjs` — ONE entry in `CLASS_NAMED_UNGUARDED` and its reason, and nothing else**; no assertion added or changed. `bio-plane/scripts/coverage.mjs` (**the `REGISTER_FLOOR` NUMERALS ONLY**, from PRINTED figures — a new suite moves `corpus`/`classified`/`arms`). `docs/development/DEBT.md` (APPEND to D-237's status cell; two NEW rows, D-249 and D-250, both MINTED with `tools/mintid.mjs`). `docs/development/MEASUREMENTS.md` (APPEND — one dated section). `docs/development/VERIFICATION.md` (the instruments section only). `docs/development/CLAIMS.md` (this entry). **NOT** `bio-plane/src/**`, **NOT** `checks/bio-checks.mjs`, **NOT** `bio-plane/test/sandbox.mjs`, **NOT** `scripts/provenance.mjs`, **NOT** `scripts/control-register.mjs`, **NOT** any other `.test.mjs`, **NOT** `civicos-ui/**`, **NOT** `pdf-worker/**`, **NOT** `agent-worker/**`, **NOT** `newgroup/**`, **NOT** `docs/development/QUEUE.md` (CONDUCT is its sole writer, and this item was spawned without a queue id).
interfaces consumed: none
interfaces owned: none. No op shape moved and nothing outside `bio-plane/scripts/` and `bio-plane/test/` builds against any of this. `BIO_SHARED_TEMP_ROOTS` is a NEW environment variable read only by `scripts/residue.mjs`; it is not an interface another area consumes, and it is stated at its site with the reason it exists.
expected: D-237's reporting half — make residue OUTSIDE `$TMPDIR` visible rather than merely absent, without blaming this run for another process's files.
concurrency: checked over the whole file. **No live claim names `scripts/battery.mjs`.** Every claim that has ever named it is a LANDED item whose work is merged (M0-15 at `4e92bdb`/`b6b7a8f`, M0-16, M0-8, M0-10, VF-3, CPDF-9). `scripts/provenance.mjs` is IMPORTED and never edited. `test/hygiene.test.mjs` is opened for ONE list entry, which is the action M0-16's census requires of a new walk and the only action it admits. `scripts/coverage.mjs` is touched for the `REGISTER_FLOOR` NUMERALS only — **if this conflicts at integration, COLLAPSE TO ONE KEY SET and re-read the printed figures; do not keep both, because the last duplicate key silently wins and once it was the lowest.**
released:
## CLAIM 2026-08-08 PLANE-TEST (REC-78 / D-230)
session: rec78-shadowed-refusals (worktree agent-ae602f80abcaf9e01)
opened: 2026-08-08T00:00:00Z
paths: `bio-plane/test/shadowed-refusals.test.mjs` (NEW — this item's suite), `bio-plane/test/shadowed-refusals.control.mjs` (NEW — its negative-control harness, deliberately not a `.test.mjs`). `bio-plane/test/machine-fences.test.mjs` — named by SITE, not by file: (1) the ONE expected-set literal in block 4's unpinned-refusals assertion, moved from eight codes to the empty set with the reason and the date at the site; (2) the header's `NEGATIVE CONTROL:` arm-(3) figure, re-measured because REC-78 moved that arm's subject. **NOT** REC-73's block 1, 2 or 3, **NOT** its walk, **NOT** `machine-fences.control.mjs` (RE-RUN unedited, all five arms). `bio-plane/scripts/coverage.mjs` (`REGISTER_FLOOR` ONLY — one key set). `docs/development/DEBT.md` (the D-230 row's disposition ONLY), `docs/development/MEASUREMENTS.md`, `docs/development/CLAIMS.md` (this entry). **NOT** `bio-plane/src/**` — nothing in the plane was changed. **NOT** `bio-plane/checks/**`, **NOT** any other `.test.mjs`, **NOT** `civicos-ui/**`, **NOT** `newgroup/**`, **NOT** `agent-worker/**`, **NOT** `pdf-worker/**`.
interfaces consumed: none
interfaces owned: none — no op shape moved, no refusal was added, renamed or widened, and no DEC-49 region was touched. The defect D-230 names is in the CONTROLS and it is the controls that moved.
expected: DEBT.md D-230 — eight identity refusals shadow a payload complaint and no suite pins any of them. **DONE: all eight pinned, in REC-73's shape one layer out.**
what the shape is, stated precisely because it is a GENERALISATION of REC-73's rather than a copy: REC-73 varied the CALLER (machine, then member) because the twelve `MACHINE_CANNOT_*` fences guard WHO is acting. Only some of these eight do. So the rule one layer out is **flip ONLY the condition the refusal itself names, and show the act complete**. Where the condition IS the caller (`LEASE_HELD`, `NOT_AN_OWNER`, `NO_AUTHOR`) that is literally REC-73's arm; where it is a property of the target or of the bytes (`NOT_ACTIVE`, `NO_OWNERS`, `EDITION_NOT_INCREMENTED`, `BAD_HANDLE`, `NO_CASE`) the caller is held fixed and the named condition is the only thing that moves. Either way the success arm is what makes the payload PROVABLY complete.
measured — **FIVE OF THE EIGHT ACTS GO ALL THE WAY THROUGH ONCE THEIR OWN GUARD IS REMOVED UNDER A COMPLETE PAYLOAD**, which is REC-73's finding arriving one layer out and is larger than the row predicted. A member ENROLLED under the handle `Hilda Krause`; a second member WROTE INTO a correspondence ledger another member was holding (ruth's entry landed at `ord` 1, so gus's had taken 0 — two accounts of one exchange interleaved, the precise harm `LEASE_HELD`'s own detail names); a REVOKED member BECAME A PROJECT OWNER; an administrator's 7.13 rescue CARRIED on a project that never had an owner; and a published case was RE-RATIFIED at edition 2 while edition 3 already stood. **The other three fell to the complaint sitting directly BEHIND the fence — `NO_CASE`→`NO_SUCH_CASE`, `NOT_AN_OWNER`→`LAST_OWNER`, `NO_AUTHOR`→`NO_REGISTER` — which is the shadow D-230 named, demonstrated rather than argued: an instrument driving those three with an incomplete payload would have read the refusal behind them and reported the fence proved.**
three findings the row did not predict, and all three are about the SWEEP rather than the plane:
  **(a) `NO_AUTHOR` IS UNREACHABLE THROUGH ITS OP, and the suite pins the unreachability rather than pretending to a pin.** `op=provenancechain` is not in `SESSION_OPS`, so a signed-in member is refused it outright ("requires a machine credential"); every machine class that CAN reach it is stamped `token:<class>` by the control plane before the store sees it, and that is never blank. MEASURED at all four caller classes: session → refused the op; admin class → `NO_REGISTER`; member class → `NO_REGISTER`; probe class → `NO_SUCH_BUNDLE`. **Not one reaches `NO_AUTHOR`.** It is driven at the Durable Object route where it IS reachable, and the op-level fact is asserted beside it both structurally and behaviourally. **A consequence worth CONDUCT's eye and deliberately not acted on here: the act REC-54's own comment calls "a named member's judgement" is one NO MEMBER CAN PERFORM — only a machine class can reach the op at all.** That is a doctrine question (REC-65 left the identity claim on this verb OPEN and routed it to CONDUCT), not a worker's, and widening it would be the mistake REC-71 exists to correct.
  **(b) THE SWEEP'S IDENTITY CLASSIFIER IS GENEROUS BY DESIGN AND FOUR OF THE EIGHT ARE NOT IDENTITY GUARDS AT ALL.** It reads the 300 characters in front of a refusal for `author`/`owner`/`viewer`/`who`. In `enroll` the word `author` occurs inside the DETAIL STRING of `NO_HANDLE`, the refusal directly in front of `BAD_HANDLE`; in `#queueCaseFor` the word `viewer` is the method's own PARAMETER. `NOT_ACTIVE` and `EDITION_NOT_INCREMENTED` are likewise state/bytes checks that sat near ownership vocabulary. **This is exactly what D-230 predicted would turn up ("expect at least one to be fenced by something other than the refusal you are pinning") — and it errs in the direction REC-73 declared, reporting MORE work than exists rather than less. The pins are worth having anyway: three of those four went ALL THE WAY THROUGH when neutered.**
  **(c) `EDITION_NOT_INCREMENTED` CANNOT BE REACHED THROUGH THE PUBLICATION CEREMONY AT ALL.** `publishCase` mints `MAX(published_cases.edition)+1` and a non-case ratification auto-increments, so the number never regresses and never leaves a hole below itself. The ONLY route is an edition AUTHORED INTO THE RATIFIED BYTES that is lower than the highest published — which is precisely what the refusal guards, since DEC-12 takes the edition from the bytes the signature covers. The arm therefore publishes a real case through `op=publish`, ratifies it, and then revises it with real signatures and a rewritten edition scalar. **Two things that cost measurements and are recorded at the site: C-21.1 refuses a completeness claim carried forward, so a fresh edition's four sentences must be authored or the GATE is what answers and the edition refusal is never reached; and C-12.2 reads the snapshot key out of the history FILENAME and demands the `<YYYYMMDD>T<HHMMSS>Z_<8 hex>` shape, so a free-form `snapKey` promotes fine and then fails the ratification gate.**
accepts-when: all eight driven under a payload complete but for the condition each guards, each answering BY NAME, each followed by the same act driven to SUCCESS; the set pinned in BOTH files so it cannot drift either way; every control arm run.
numbers: **baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT, on a quiet tree: 133/133 suites green · 8,319 assertions · 114.6s · exit 0**, provenance `135 of 135 discovered item(s) are in the commit at HEAD (bb426ac)`, `git status --short` empty. The worktree arrived at `bb426ac`, level with `origin/main`, and WITHOUT `bio-plane/node_modules`; `npm ci` was run. **The brief quoted REC-73's sweep as 380 methods / 111 identity refusals / 66 shadowing; this worktree MEASURES 394 / 114 / 68 — the figures moved with unrelated growth in `store.mjs`, and the EIGHT UNPINNED CODES are unchanged, which is the part the row rests on.** REC-78's own suite: 44 assertions. Its walk prints 394 methods and 497 refusal sites.
floors: `REGISTER_FLOOR` in `bio-plane/scripts/coverage.mjs` moved UPWARD to the figures a green `--strict` run PRINTED as REPRODUCIBLE (never counted, never incremented by hand). **ONE KEY SET — if this conflicts at integration, COLLAPSE TO ONE and re-read the printed figures.** The suite's own two walk floors are BLINDNESS floors rather than ratchets and say so at the site: a ratchet over the plane's refusal count would fail every time unrelated work retires a refusal, which is not this item's subject, while a blind walk does not drop by ten but to nearly nothing. No DEC-49 floor is touched — no refusal was added or moved.
what came back WRONG, recorded rather than smoothed: **CONTROL ARM (10) — the blindness arm — did not fail as declared on its first run. It came back `THE SUITE NEVER REACHED ITS FOOT`.** With the walk blinded there were no refusal sites, the shadow table's print loop dereferenced a null, and a `TypeError` ended the MODULE through no assertion at all — taking all eight pins with it while the three arms that SHOULD have spoken were the only ones that had. Caught only because the harness READS THE FOOT LINE instead of trusting the tally and reports a missing one as `-1`. Corrected at the site with the receipt in the comment; the arm then behaved exactly as declared (41 pass, 3 FAIL, all eight pins green). **Every other arm behaved as declared on every run, including all five of REC-73's when re-run.**
concurrency: checked over the whole file. No live claim names `shadowed-refusals*`, `machine-fences.test.mjs`, or any of the eight codes. `coverage.mjs` IS opened by other live claims for `REGISTER_FLOOR` and nothing else — same key set, same instruction.
released:

### DELEGATION 2026-08-08 PLANE-TEST (REC-78) -> WHOEVER TAKES THE NEXT LAYER: **THE TWO DEEPEST SHADOWS ARE STILL UNMEASURED, AND THEY WERE EXCLUDED ON PURPOSE**

`NOT_THE_OWNER` in `promote` shadows **18** refusals and `CAS_STALE` shadows **17** — the two
deepest in the whole file and both outside the twelve. Both are PINNED by existing suites that
they FIRE; neither has been shown to be WHAT fires. REC-73 excluded them deliberately and
REC-78 did too, because widening an item to reach them is the mistake REC-71 exists to correct.

**What makes them harder than the eight, so the next item can size it.** The eight each had one
condition that could be flipped in isolation. `promote`'s `NOT_THE_OWNER` sits in front of
eighteen distinct payload refusals in the plane's single largest write path, so "a payload that
would otherwise succeed" means a promotion that passes every one of them — which is the whole
of `promote`'s contract, driven once per arm. `CAS_STALE` is worse in a different way: the
condition it guards is a RACE, and flipping only that condition means arranging a live
concurrent write rather than a fixture.

**And a smaller one, same class, cheaper:** REC-78 measured that four of its eight were swept in
by the classifier's 300-character window reading a refusal's own DETAIL STRING or a method
PARAMETER as identity vocabulary. The window is generous on purpose and errs safely, but a
sweep that distinguished "identity vocabulary in CODE" from "identity vocabulary in a STRING"
would report a materially smaller and more honest estate. That is an instrument change, not a
plane change.

### DELEGATION 2026-08-08 PLANE-TEST (REC-78) -> CONDUCT: **THE ACT REC-54 CALLS "A NAMED MEMBER'S JUDGEMENT" IS ONE NO MEMBER CAN PERFORM**

MEASURED, not read off a comment: `op=provenancechain` answers a signed-in session
`"this operation requires a machine credential, not a signed-in session"`. It is not in
`SESSION_OPS`. So reconstructing a provenance chain — the act D-200 exists to keep honest, and
whose store-level guard `NO_AUTHOR` says "the record must show who decided that the evidence
supported this route" — is reachable ONLY by a machine class, every one of which arrives stamped
`token:<class>`. The name against every such act is therefore a machine's, always.

REC-65 left the identity claim on this verb **OPEN** and routed it to CONDUCT
(`identity-claims.test.mjs` pins it as a known-open finding). This is the same question with a
measurement attached, and it sharpens it: the open question was *should a machine be refused
this act*, and the measured answer is that a machine is the only caller who can take it.
**REC-78 neither fenced it nor widened `SESSION_OPS` — both would be deciding doctrine nobody
asked for.** `NO_AUTHOR` is pinned at the Durable Object route where it IS reachable, and the
op-level unreachability is pinned beside it, so whichever way this is ruled the suite fails
rather than drifting.

## CLAIM 2026-08-08 RECORD
session: rec68-quoted-value
opened: 2026-08-08T00:00:00Z
paths: **`bio-plane/src/query.mjs`** — the TOKENIZER ONLY: the bare-value reader's terminator set and the `field:"value"` branch beside it (`tokenize`, ~lines 569-601). No other function is edited. `FIELDS`, `MEANING`, `viewerPredicate`, `selector`, `meaningAtom`, `textAtom`, `ftsExpr`, `setSql`, `chain` and `compile` are READ and DRIVEN, never modified — the fix is upstream of all of them by design, which is exactly why it is language-wide. **`bio-plane/test/query.test.mjs`** — the new quoted-value block and the tokenizer REACHABILITY block. **`bio-plane/test/search.test.mjs`** — the new block driving the fix over the PLANE'S ANSWER rather than over the compiler's SQL text. **`bio-plane/test/meaningquery.test.mjs`** — PL-8's TWO D-228 pins ONLY, CORRECTED (never exempted) with a dated reason; no other assertion in that suite is touched. **`bio-plane/test/hygiene.test.mjs`** — ONE new arm block, the schema-comment/vocabulary correspondence guard that stops the rider's comment going stale again, plus its declaration in the suite's own control block. **`bio-plane/test/query.control.mjs`** and **`bio-plane/test/query-reach.control.mjs`** + **`bio-plane/test/query-reach.report.mjs`** (NEW — the committed negative-control driver and the committed reachability instrument; deliberately NOT `.test.mjs` because the first EDITS REAL SOURCES while it runs and the second re-executes itself under `NODE_V8_COVERAGE`, so `scripts/battery.mjs` must not discover either. `suggest.control.mjs` / `register.control.mjs` are the precedent). **`bio-plane/scripts/coverage.mjs`** — the `REGISTER_FLOOR.arms` figure ONLY, 471 -> 478, MOVED in the turn that invalidated it and taken from what the instrument PRINTED; no matcher, gate or other floor touched. `docs/development/MEASUREMENTS.md` (one dated entry, the branch-reachability sweep). `docs/development/VERIFICATION.md` — the negative-control row's printed arms figure ONLY, moved with the same number. **`bio-plane/src/schema.mjs`** — ONE INLINE COMMENT on `inquiry_basis.grade_source` (line 1247). No column, no table, no statement, and NO SEMICOLON inside the comment (CLAUDE.md's `#migrate` trap). `docs/development/DEBT.md` — the **D-228 row's disposition only**. `docs/development/CLAIMS.md` (this entry and its DELEGATIONS).
interfaces consumed: I3. interfaces owned: none. **No op added, no check minted, no schema object changed, no member-facing sentence authored.** This is a behaviour change BEHIND an existing interface: `op=search` keeps its shape exactly and answers differently, which is the point of the item and is why the acceptance is a flipped pin rather than a new field.
expected: **REC-68 / D-228** — a quoted field value keeps its quotes, so `state:"open"` binds `"open"` WITH the quote characters and matches nothing, `title:"two words"` compiles to a mangled FTS5 expression, and the tokenizer branch written to prevent exactly this **cannot be reached**. Plus the CLASS SWEEP in both halves: which other branches of the selector language are unreachable, and which other values pass through the tokenizer un-normalised.
concurrency: checked over the whole file. **No un-`released:` claim names `bio-plane/src/query.mjs`, `bio-plane/test/query.test.mjs`, `bio-plane/test/search.test.mjs`, `bio-plane/test/meaningquery.test.mjs` or `bio-plane/test/hygiene.test.mjs`.** The only claim naming `bio-plane/src/schema.mjs` is PL-15's (`out-of-inquiry-lead`), whose `released:` line is blank but whose work is **MERGED into this worktree's base at `84ca736`** — verified by the presence of its `lead_inquiry` column at `schema.mjs:2074`, not by reading the claim. That is the same-file handoff pattern several blocks above this one already record. `store.mjs`, `index.mjs`, `checks/bio-checks.mjs`, `civicos-ui/**`, `newgroup/**` and `pdf-worker/**` are NOT claimed here and NOT edited.
released:

### DELEGATION 2026-08-08 RECORD (REC-68) -> UI / UI-50: **THE ONE LIVE CONSUMER THAT TYPES A QUOTED SELECTOR IS YOURS, AND WHAT REC-68 CHANGED UNDER IT IS *NOTHING* — MEASURED, NOT ASSUMED**

REC-68 fixed D-228: a quoted field value used to keep its quote characters, so `state:"open"` bound `"open"` and matched nothing. The fix is in `bio-plane/src/query.mjs`'s tokenizer and is **LANGUAGE-WIDE**, so every caller that types a quoted selector now compiles differently. **A sweep of the whole repository found exactly ONE such caller, and it is in `civicos-ui/`, which this item does not edit:**

- **`civicos-ui/app.html:14258`** — `const q = \`locator:"${String(locator).replace(/"/g,"")}"\`;`, the address lookup inside `heldMatch`. **This is UI-50's subject**, and its claim is live, so nothing was touched.

**WHAT CHANGED FOR IT: the compiled expression, not the answer.** `locator:"<url>"` used to compile to the FTS5 expression `{locator} : """<url>"""` and now compiles to `{locator} : "<url>"`. Both are valid and both match the same rows, because FTS5 tokenises a string literal and the quote characters were never token characters. **PL-10 measured this (63 hits both spellings, both returning the oldest) and REC-68 RE-MEASURED it rather than inheriting it** — `search.test.mjs` now asserts `locator:"opengov"` and `locator:opengov` return an identical row list, as a live arm over the plane's answer.

- **So UI-50's premise is unchanged and still correct:** `heldMatch` is not silently matching nothing, it is confidently matching the wrong row, and D-228 was never the cause. The fix remains `op=versionchain&address=&at=`.
- **The `.replace(/"/g,"")` on the locator stays necessary and stays correct.** Under the new tokenizer a quoted run still ends at the first quote, so a locator containing a `"` still cannot be represented in one selector. Do not remove it as "no longer needed" — it is load-bearing for a different reason than it may have been written for.
- **What UI *gains*, if it wants it:** a quoted value is now a real PHRASE on a free-text column. `title:"two words"` used to compile to a mangled expression that scoped only the first word to the column and matched the second over every column — it returned rows, and the wrong ones. It now compiles to `{title} : "two words"`. Any surface offering quoting as an affordance can now honestly describe it as a phrase search. `civicos-ui/app.html:8764` already tells a member *"the plane's own syntax line says a quoted phrase is one unit"* — **as of today that sentence is true of a FIELD-SCOPED phrase too, which it was not when it was written.**
- **Reversal cost: none for UI.** No op changed shape, no envelope field was added or removed; I3 is untouched.

### DELEGATION 2026-08-08 RECORD (REC-68) -> CONDUCT: **AN UNTRACKED SUITE FROM ANOTHER ITEM WAS TRANSIENTLY PRESENT IN THIS WORKTREE, AND IT INFLATED A BASELINE BY 57 ASSERTIONS BEFORE VANISHING**

Raised because it corrupted a measurement and I could not establish the mechanism, not because anything is broken in what REC-68 ships.

- **MEASURED:** this worktree's FIRST baseline battery discovered **124 suites (121 plane · 3 fleet) · 124/124 green · 7,811 assertions**, and one of them was **`bio-plane/test/machinefences-dec49.test.mjs` (57 pass)** — a file that **`git ls-files` does not know, that no commit contains, and that was gone from the very next battery run** with no deletion appearing in `git status`. The name is REC-64's (DEC-49's enactment over the machine fences), which is running concurrently.
- **The same run printed:** *"host orphans rose by 2 while this run leaked none — another checkout without this fix is running a battery into the same $TMPDIR"*, so this machine has concurrent batteries sharing temp state.
- **HOW IT WAS CAUGHT, and it is the only reason the numbers in this item are trustworthy:** the per-suite attributor reported a suite going to `undefined` and a baseline figure that moved between two reads of the same file. Rather than smooth it, the baseline was RE-ESTABLISHED by `git stash`-ing the whole change and re-running the battery at `bf69c27` — **123 suites (120 plane · 3 fleet) · 123/123 · 7,752**, which is exactly what the brief predicted and what every figure in this item is now measured against. The stash pop was verified by sha256 AND by `cmp` against the pre-stash diff.
- **What CONDUCT may want to check:** whether worktree creation can carry an UNTRACKED file out of another tree (`.worktreeinclude` carries `.env` by design, and this looked like the same mechanism reaching further). **An untracked `.test.mjs` appearing in a worker's tree is discovered by `scripts/battery.mjs`, counted in its totals, and invisible to `git status` as anything but `??` — so it can silently move a suite count and an assertion total in either direction.** REC-68's exposure was an inflated baseline, which is the harmless direction; the harmful one is a worker measuring a floor against a suite that is not in the repository.

### DELEGATION 2026-08-08 RECORD (REC-68) -> WHOEVER TAKES D-255 (renumbered from a colliding D-236, CONDUCT 2026-08-08): **`atom.phrase` IS A DEAD FIELD, AND THE ARM THAT FOUND IT COULD NOT FIRE**

`query.mjs`'s `textAtom` computes `phrase: quoted && /\s/.test(v)` on every text atom. **MEASURED: one write site, ZERO read sites** across `bio-plane/src/`, `bio-plane/checks/` and `civicos-ui/`. Found by a negative-control arm that set `quoted: false` expecting phrase detection to die; `query.test.mjs` stayed green because the compiled FTS5 literal is the same either way and FTS5 treats a multi-word string literal as a phrase on its own. **The arm was armed against a field with no consumer and could never have been honoured**, which is recorded in that suite's control block as the result rather than smoothed over.

Filed as **D-255** (renumbered from a colliding D-236 by CONDUCT 2026-08-08) with its disposition. Not fixed here because closing it touches `textAtom`, the one function every text arm in the language passes through, and REC-68's claim is the tokenizer. It is the same family as D-228 one layer along — a name that tells a reader something is handled, with nothing behind it — but **not the same severity**, and it should not be reported as if it were: an unread field changes no answer, whereas D-228's unreachable branch changed what every selector matched.
## CLAIM 2026-08-08 UI
session: ui50-wrong-predecessor
opened: 2026-08-08T00:00:00Z
paths: `civicos-ui/app.html` (`heldMatch`'s ADDRESS arm only — the hash arm, `addGo`, the bundle sentence's shape and every other function are untouched; plus the REC-70 rider, four words of a comment block at `:16141`), `civicos-ui/test/version-predecessor.test.mjs` (NEW — the item's suite and its class sweep), `civicos-ui/test/bound-sweep.test.mjs` (ARM A's header note and ARM A2's router only — ARM A2 now needs `op=versionchain` mocked because the changed-from sentence is composed off it, which IS the item), `civicos-ui/test/version-predecessor.control.mjs` (NEW — the negative-control driver, deliberately NOT a `.test.mjs`: it EDITS `app.html` while it runs and `test/run.mjs` must not discover it), `docs/development/CLAIMS.md` (this entry and its DELEGATIONS), `docs/development/DEBT.md` (the **D-221** disposition cell, which this closes, and ONE new row **D-236**, RENUMBERED to **D-256** by CONDUCT 2026-08-08, for the sentences already written), `docs/development/kickoffs/UI.md` (APPENDED to, never rewritten — several UI workers are live — one "Decisions for Bob" section).
**NOT OPENED:** nothing under `bio-plane/src/`, `bio-plane/checks/`, `bio-plane/test/`, `newgroup/`, `pdf-worker/`, `agent-worker/` or `release/`. **`op=versionchain` is CALLED, never changed** — PL-10 built the join and the op and proved both; this item makes the writer use it. `bio-plane/src/store.mjs` and `src/query.mjs` are READ TEXTUALLY by the class sweep, exactly as `bound-sweep.test.mjs` already reads them, and no plane file is written. Every other `civicos-ui/test/*.test.mjs` is READ ONLY.
interfaces consumed: I3 (`op=versionchain`, I3 10.2.0, landed by PL-10). interfaces owned: none. No op added, no check minted, no schema touched, no refusal code named in `app.html` (so the DEC-49 guard's reach does not move).
expected: **UI-50** — `heldMatch` composed the *"changed from"* sentence, PERMANENTLY WRITTEN INTO THE BUNDLE, off the FIRST row of a `locator:"<url>"` FULL-TEXT query, which is bm25-ordered over rows whose URL text is identical; PL-10 measured it returning the OLDEST version at the address over sixty. The address arm now asks `op=versionchain` and takes the LAST version in `first_retrieved` order as the predecessor. Plus a class SWEEP for the sites that pick ONE row out of a relevance-ordered answer and compose a sentence, a field or a link from it.
concurrency: checked over the whole file. No live claim names `civicos-ui/test/version-predecessor.test.mjs`. UI-48's claim names `civicos-ui/app.html` and `civicos-ui/test/bound-sweep.test.mjs` and its `released:` line is BLANK — but its work is MERGED at `bf69c27` (`conduct: UI-48 integrated`), which is this worktree's HEAD, so it is the same stale-but-landed shape UI-48's own claim recorded about UI-52. Named here rather than assumed away.
released: 2026-08-08 — committed on branch `worktree-agent-a773e28c7c7d0fb8b`, NOT pushed and NOT merged; CONDUCT integrates. **THE JOIN ALREADY EXISTED: PL-10 built and proved it, and this item's work was making the writer use it.** `heldMatch`'s address arm asks `op=versionchain` for the chain's `total` and seeks the LAST version at `offset = total - 1`; that version is the predecessor of a capture not yet written. `at=<sha>` is NOT used and the reason is MEASURED rather than preferred — the chain joins `register`, whose row `op=promote` writes, so at this moment the fresh capture is not in the chain and cannot be an anchor. The paged `op=search` walk survives as an IDENTITY-ONLY backstop (hash equality, order-free) for a bundle registered without a `captured_locators` row. NEW `civicos-ui/test/version-predecessor.test.mjs` (41 assertions) driving the REAL `addGo` over SIXTY chain lengths with ground truth computed in JavaScript and never read back from the op, the trap proved armed on every subset. **BATTERY BASELINE MEASURED IN THIS WORKTREE and it AGREED with the brief: 123 suites (120 plane + 3 fleet) · 123/123 green · 7,752 assertions · NO skip.** The worktree arrived without `bio-plane/node_modules`; `npm ci` was run there first. FINAL 123/123 · **7,753**, delta **+1 ATTRIBUTED**: `planning-hygiene.test.mjs` 270 -> 271, one new DEBT row (D-236), measured by re-running that suite alone; every other suite byte-identical, and the TRUE baseline re-run with this item's files removed reported 7,752. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` UNPIPED, **exit 0**, unchanged (OPS 158/158 · CHECKS 160/160 · controls 120/120, 471 arms · FLEET 2 members / 4 ops). `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED, **exit 0**; `bound-sweep.test.mjs` stays at 202 assertions. `check-refusal-codes` floors did NOT move and the figures were READ OFF THE GUARD rather than predicted: **REACH stayed 191**, while its components moved R2 50 -> 51 and R3 66 -> 68 — the two `VERSION_CHAIN_*` literals this item added (one in an `app.html` comment, two in the new suite) are already R1 family rows, so they joined two matchers without joining the union. *(A first draft of this line said the reach grew to 194. It was wrong, and it was caught by reading the guard's own output — which is the failure this project names most often, arriving inside the sentence claiming to have measured.)* No refusal code is keyed on in `app.html`'s executable code. **8 NEGATIVE-CONTROL ARMS RUN** through `test/version-predecessor.control.mjs`, each armed ALONE with the others held open, declared before arming, restores verified by sha256 AND `cmp` against a per-arm pristine copy named with the ARM ID as well as the path, plus a pristine-of-record taken before any arm — final 8/8 as declared, and **TWO were NOT as declared on their first run and are recorded in that driver's header rather than smoothed away** (an arm that could never have been honoured, and an arm whose declared pin was overtaken by the licence FLOOR). A BASELINE arm is included. D-221 CLOSED; **D-236 raised for the sentences already written**, with the decision written up for Bob in `kickoffs/UI.md` and a DELEGATION to RECORD below. **AND A DELEGATION TO CONDUCT: another session is working in THIS worktree** (D-228's `query.mjs` work), which cost two false battery readings and a `git stash` collision — nothing was lost, the other session's stash was restored unchanged and sits at `stash@{0}` of this worktree; do not discard it.

### DELEGATION 2026-08-08 UI (UI-50) -> RECORD: **THE WRONG "CHANGED FROM" SENTENCES ARE STILL IN THE RECORD, AND EVERY ONE OF THEM IS DECIDABLE TODAY**

UI-50 closed D-221 — `heldMatch` now asks `op=versionchain` and names the version that
actually preceded the capture. **The fix does not reach backwards.** Every bundle written
by the pre-2026-08-08 `addGo` whose body carries *"The record already holds an earlier
capture of this same address (`<ID>`)"* had that `<ID>` chosen by bm25 tiebreak among rows
with identical url text, and PL-10 measured that answer coming back as the OLDEST version
at the address. The sentence is in the bundle body and is permanent there.

- **The set is enumerable and each member is decidable, which is what makes this a task
  and not a regret.** The sentence has ONE fixed literal shape, so the bundles carrying it
  are findable; each names its own address and its own capture; and — unlike at write time —
  **the capture is now IN the register, so `op=versionchain&address=&at=<that capture>`
  answers with the TRUE `predecessor` for it.** `at` works here precisely where it did not
  work in the writer, and that is the whole reason this is cheap.
- **Three outcomes, and they must be reported separately:** (a) PROVABLY WRONG — the chain
  names a different predecessor; (b) PROVABLY RIGHT — the address held exactly one prior
  version, where the two routes cannot disagree; (c) UNDETERMINED — the chain holds no
  version at that address (a bundle registered without a `captured_locators` row), where
  the record can only say it cannot check. **Collapsing (a) and (c) into one number would
  be this defect's own mistake repeated.**
- **Why UI-50 did not do it, and it is not scope-avoidance.** Every act it needs is outside
  `civicos-ui/**`: a read over bundle BODIES that no surface has, a WRITE to bundles already
  in the record, and a member-facing correction sentence DEC-49 owns. A UI worker doing any
  of the three would be authoring the record's own correction from the surface.
- **Correction moves FORWARD (DEC-19).** Rewriting the bodies is not available and is not
  being asked for. The decision — appended correction per bundle, or one published report —
  is Bob's and is written up in `kickoffs/UI.md` under "Decisions for Bob", with the
  recommendation that the ENUMERATION runs first because the ruling should be made with the
  size of the *provably wrong* set in front of it.
- **Raised as `D-236`, RENUMBERED to `D-256` by CONDUCT 2026-08-08** — a third row had taken the number the same day, which is the exact failure `tools/mintid.mjs` exists to prevent and this row's own next sentence predicted. The number was measured free at the moment of writing (D-235 is
  REC-75's, landed hours earlier); a parallel slot taking it at the same moment is CONDUCT's
  renumber, not this session's error.

### DELEGATION 2026-08-08 UI (UI-50) -> CONDUCT: **ANOTHER SESSION IS WORKING IN THIS WORKTREE, AND IT COST A MEASUREMENT BEFORE IT WAS NOTICED**

**MEASURED, not inferred.** Two battery runs from this worktree reported **124 suites ·
7,811 assertions** and **123 · 7,806** while a foreground run minutes later, and the true
baseline with this item's files removed, both reported **123 · 7,752**. The difference was
not this item: `bio-plane/src/query.mjs`, `src/schema.mjs`, `scripts/coverage.mjs`,
`test/query.test.mjs`, `test/search.test.mjs`, `test/meaningquery.test.mjs`,
`test/hygiene.test.mjs`, `DEBT.md`, `MEASUREMENTS.md`, `VERIFICATION.md` and `CLAIMS.md`
were modified in this tree by **another session working on D-228** (the quoted-value defect
in `query.mjs`), and an UNTRACKED plane suite `bio-plane/test/machinefences-dec49.test.mjs`
(57 assertions) appeared in `bio-plane/test/` and later vanished.

- **`CLAUDE.md` says ONE SESSION PER WORKING TREE, no exceptions**, and `PARALLELISM.md`
  records what two sessions in one tree cost the day it last happened. A claim reserves
  PATHS between checkouts and does nothing about two sessions writing one tree.
- **It also produced a `git stash` collision, and it is recorded because it nearly ate
  somebody's work.** UI-50 stashed its own three files to measure the true baseline; the
  other session pushed a stash in between; `git stash pop` therefore popped THEIRS. The
  other session's changes were captured to a patch, RE-STASHED unchanged with a message
  naming what happened, and UI-50's own stash was then popped by explicit index and
  verified byte-identical against copies taken before any git operation. **Nothing was
  lost, and that was diligence rather than luck — but the next collision is a merge or a
  force.**
- **What is owed:** whoever is running the D-228 item needs its own worktree, and its
  `CLAIMS.md` entry does not yet appear on disk in a form this session could see (its claim
  is inside the stash, which is exactly why the collision was invisible until it happened).
- **For the integrator specifically:** this item's `CLAIMS.md` and `DEBT.md` edits and the
  D-228 session's are in the same files. The D-228 session's were held in `stash@{0}` of this
  worktree; **by the end of this session that stash was gone from `git stash list` and its
  content was in neither the working tree nor any commit here.** It is not lost and here is
  exactly where it is, because a recovery instruction that lives only in a report is not a
  channel: the stash commit is UNREACHABLE-BUT-PRESENT in this worktree's object store at
  **`8706832`** (`git show 8706832`, `git stash apply 8706832`), and a plain diff of the same
  eleven files was written to this session's scratchpad as `OTHER-SESSION-changes.patch`.
  **Recover it before this worktree is pruned** — an unreachable object survives only until
  `git gc`. The eleven files are `bio-plane/src/query.mjs`, `src/schema.mjs`,
  `scripts/coverage.mjs`, `test/query.test.mjs`, `test/search.test.mjs`,
  `test/meaningquery.test.mjs`, `test/hygiene.test.mjs`, `docs/development/DEBT.md`,
  `MEASUREMENTS.md`, `VERIFICATION.md` and `CLAIMS.md`.
## CLAIM 2026-08-08 UI (UI-38 — the running-session surface, third pass)
session: ui38-running-session (worktree agent-ae139b3de556ab2ea)
opened: 2026-08-08T00:00:00Z
paths: **`civicos-ui/app.html` — named by SITE, not by file:** (1) one CSS rule ADDED under
  `.ai-panel .ai-bound`, for a block nested inside a block; (2) inside the
  `__AI_SESSION_START__`/`__AI_SESSION_END__` block ONLY — `aiSessionPairsHtml` (widened to print an
  array's members without an index), `aiSessionBudgetHtml` / `aiSessionPrincipalHtml` /
  `aiSessionConditionHtml` (DELETED, collapsed into ONE new `aiSessionBlockHtml`),
  `aiSessionPanelHtml` (one call instead of four), and three comment regions (the DEC-61
  paragraph, the missing-op paragraph, the observation-log decision). **NOT** `aiSessionRead`,
  `aiSessionTranscript`, `aiSessionInContext`, `aiSessionIndicatorHtml`, the seen-roster, or
  `aiSessionContextHtml` — the READ, the INDICATOR, the DEVICE ROSTER and the ONE DOOR are
  untouched, and no call site outside the block moved.
  `civicos-ui/test/ai-session-wire.test.mjs` (the `vm` export list; `wireFailures` INVERTED; ARM
  W1's wording; ARM W1r/W1s/W1t ADDED; ARM W6's path spellings; ARM C2's renderer list; ARM O3/O3b
  ADDED). `civicos-ui/test/surface-registry.test.mjs` (the `vm` export list; ARM Y7/Y8/Y8b/Y10/Y11/
  Y15/Y16 re-made through the replacement; ARM Y8c/Y11b/Y11c ADDED).
  **CROSS-AREA, FORCED, AND FILED:** `bio-plane/test/airun.test.mjs` — ARM U ONLY (the `vm` export
  list, U3..U7 re-made through the composition, U9/U9b/U9c/U9d ADDED). See IC-41 and the DELEGATION
  below. **NOT** any other arm of that file.
  `docs/development/INTERFACE-CHANGES.md` (IC-41), `docs/development/CLAIMS.md` (this entry and its
  delegations), `docs/development/MEASUREMENTS.md` (appended).
  **NOT** `bio-plane/src/**` (READ ONLY), **NOT** `bio-plane/checks/**`, **NOT**
  `bio-plane/scripts/**`, **NOT** `civicos-ui/tokens.css`, **NOT** `docs/development/QUEUE.md`,
  **NOT** `docs/development/IS-BUILD-PLAN.md`, **NOT** `newgroup/**`, **NOT** `agent-worker/**`.
interfaces consumed: I3 (`op=airun`, read and rendered; nothing else asked of the plane).
interfaces owned: none registered — **and IC-41 is filed because that is the defect**: the plane's
  own battery lifts this block's internal function names and no interface says so.

**THE BRIEF'S TWO LANDED INPUTS ARE NOT ON `main`, MEASURED RATHER THAN INFERRED, AND THIS IS THE
FIRST THING CONDUCT SHOULD READ.** The brief said to take REC-69's context-listing op and REC-74's
third published condition as inputs. Both are marked `· done` in `QUEUE.md`, and REC-69 is named in
the line *"FIVE ITEMS INTEGRATED TOGETHER 2026-08-08 — REC-72, REC-66, REC-65, REC-63, REC-69 —
verified at 129/129 GREEN."* **Neither is in the tree.** A grep for `airuns` over
`bio-plane/src`, `bio-plane/test`, `bio-plane/checks` and `bio-plane/scripts`, excluding
`airunspawn`, returns NOTHING; `index.mjs`'s op table has no such key; `aiRunRead` publishes `bias`
and `principal.skill` and **no `standard`**. The work exists on unmerged worker branches —
`2d9c57b` (REC-69) and `a3af6bb` (REC-74) — and neither is an ancestor of `origin/main`
(`57b5067`). **UI-38 therefore built against what is actually there**, said so at the site, and did
not pre-shape itself around a wire this harness cannot drive.

**WHAT THE ITEM IS, AFTER THAT.** REC-74's delegation to UI was measured exactly right and it is the
whole of this pass: **`aiSessionPairsHtml` skipped objects, so the panel rendered NEITHER the bias
block NOR the standard block.** On `main` today the plane publishes TWO of §11's three conditions
and a member sees ONE. The panel made up for the object-skipping with one dedicated renderer per
nested field somebody had named — `principal`, `budget`, `condition` — **which is a LIST OF FIELD
NAMES wearing a field-name-blind renderer's clothes.** PL-12/D-84 published a fourth (`bias`) and
nothing carried its name, so not one field of the LENS the run was formed under reached a member;
`context` was invisible for the same reason, so the panel never said which inquiry the run was in.
**AND THE SAME LIST IS WHY NOBODY NOTICED:** `ai-session-wire.test.mjs`'s `wireFailures` enumerated
the identical three nestings by name, so the surface and the instrument that grades it went blind
together and ARM W1 answered `[]` over a panel missing a whole published condition.

**THE FIX IS ONE RECURSIVE RENDERER AND THE PANEL NOW KNOWS NO FIELD NAMES AT ALL.** Three
renderers become `aiSessionBlockHtml`, applied at every depth: the level's scalars, then a block per
nested value, keys printed for objects and never for array indices (an index is the surface's
arithmetic, not the record's word). `bias` renders today for that reason and no other, and
**`standard` will render the day REC-74 lands WITHOUT A LINE CHANGING** — which is what "designed
ONCE for all AI features" has to mean if it is to survive the next AI feature. It still derives
nothing: every visible token is a value the record sent or a key it named, and ARM S5 and ARM D
both hold over the new function.

**THE INSTRUMENT WAS INVERTED IN THE SAME TURN**, because a defect and the reason it was invisible
are one finding rather than two. `wireFailures` recurses over everything the record published at
every depth; ARM W1r PRINTS the corpus (**24 scalars**) and floors it at 20; ARM W1s names any
nested value the walk entered and read nothing from; ARM W1t asks the RECORD which top-level values
are nested and requires the panel to NAME every one — so a fifth condition is graded without this
file moving.

**THE OTHER THREE THINGS THE BRIEF ASKED FOR, EACH DECIDED AND RECORDED AT THE SITE.**
  - **The observation-log surface: RE-TAKEN, not inherited.** Of UI-49's four grounds, **ground 4
    has EXPIRED** (`aiRunLog` now clamps to `AI_RUN_LOG_LIMIT_DEFAULT`/`_MAX` and publishes `limit`
    and `truncated`, on the ABSENT answer too — REC-70, verified at the source) and **ground 3 has
    WEAKENED** (four vocabularies travelling WITH the answer is the DEC-8 case a surface is
    ALLOWED, not a doctrine of its own). Grounds 1 and 2 still decide it, and **one ground is
    ADDED and it is structural**: `SURFACES["ai-session"].kind` exists so there is EXACTLY ONE
    surface of this kind, and an audit reader of a finished run is a DIFFERENT KIND, not a second
    of this one. **SEPARATE, and the click-through stays on the transcript.** It needs its own
    registry entry, route and bound-rendering pins: an item, not a rider.
  - **§14a's "SAYS SO WHEN UNAVAILABLE" — located, not authored.** REC-64 gave it
    `AI_RUN_CAPABILITY_UNAVAILABLE` / `C-33.29` with a canned translation, refused by `aiRunOpen`.
    **It is refused where a run is OPENED, which this application never does** — the fleet member
    opens runs, not the browser — so there is no run object to render and nothing here to say.
    Recorded at the site rather than papered over: a line rendered by a window that asked nothing
    would be a claim about the record made by a surface that never asked, which is UI-39's and
    UI-40's class.
  - **NO "YOU MAY NOT SEE THIS" BRANCH, and none added.** The block already reads `session` and
    never `found`; when the context-listing op lands, `runs: []` is byte-identical for an uninvited
    member and a context that does not exist, so a branch telling them apart would leak what the
    plane deliberately withholds. Stated at the site so the next reader does not add one.

accepts-when: as the IS-BUILD-PLAN UI-38 row. `node civicos-ui/test/run.mjs` from the REPO ROOT,
  exit read UNPIPED, **0**: indicator bound to run state (untouched, still green), transcript never
  persisted server-side (ARM Y14/Y14d, untouched), budget and spend rendered from the run object
  (ARM W5, ARM U3/U3b) — **and now every OTHER condition the run object carries as well.**
numbers: **MEASURED IN THIS WORKTREE, and the brief's harness figure was WRONG IN BOTH DIRECTIONS
  until `npm ci` was run.** First run: `node civicos-ui/test/run.mjs` **exit 1, 3 FAIL** — not 12 as
  UI-51 recorded and not 8 as briefed, but `Cannot find module 'miniflare'` in the three suites that
  drive the real plane (`ai-session-context`, `ai-session-wire`, `intent-write`). After `npm ci` in
  `bio-plane/` and a fast-forward from `bb426ac` to `origin/main` `57b5067`: **exit 0, 40 suites
  PASS, 0 FAIL** — a GREEN baseline, recorded because the practice is to trust the measurement
  rather than the streak.
  **PER-SUITE, by re-running the TRUE baseline (all changed files restored from `git show HEAD:`),
  never by subtraction:** `ai-session-wire` **72 → 81** (+9; its own header claims "Clean tree: 70",
  stale by 2), `surface-registry` **382 → 393** (+11), `ai-session-context` **77 → 77** (0, and it
  was not edited), `bio-plane/test/airun.test.mjs` **103 → 107** (+4; its own header claims "Clean
  tree: 101", stale by 2). Every restore verified by sha256 AND by `cmp` against uniquely-named
  per-arm pristine copies, with byte counts printed and a 20,000-byte floor guarded.
floors: **NONE MOVED, and none needed to.** No DEC-49 region, no `REGISTER_FLOOR` key, no
  `regionLines`, no coverage floor: this item edits no `bio-plane/src/**`, no `checks/**` and no
  `scripts/**`. **Two floors were ADDED rather than moved** — `ai-session-wire` ARM W1r's corpus
  floor of 20 scalars (measured 24) and ARM U9's `biasShown.length >= 2` (measured 2). ARM S3's
  existing `renderers.length >= 4` was checked deliberately: collapsing three renderers into one
  takes the block from 6 driven renderers to **exactly 4**, so the floor still holds and was NOT
  lowered. Had it fallen, the reason would have been an instrument counting fewer functions rather
  than slack — it did not fall, so nothing had to be argued.
concurrency: checked over the whole file. No live claim names `civicos-ui/app.html`'s AI-session
  block, `ai-session-wire.test.mjs`, `surface-registry.test.mjs` or `airun.test.mjs`. **THREE LIVE
  DELEGATIONS POINT AT `civicos-ui/app.html` at OTHER sites** (REC-63's route marker, REC-66's
  connect receipt, REC-72's `cite-act.test.mjs` mock) — none of them inside the AI-session block,
  and none touched here. **IF `airun.test.mjs` CONFLICTS AT INTEGRATION, TAKE THIS ITEM'S ARM U
  WHOLE:** it is the only version that resolves against a block with no `aiSessionBudgetHtml` in it,
  and a keep-both merge there is a `ReferenceError` that ends the module with the tally reading
  `assertions unknown`.
released:

### DELEGATION 2026-08-08 UI (UI-38) -> CONDUCT, AND IT OUTRANKS EVERYTHING ELSE HERE: **TWO QUEUE ROWS SAY `done` FOR WORK THAT IS NOT ON `main`, AND ONE IS INSIDE A "VERIFIED AT 129/129 GREEN" INTEGRATION LINE**

`QUEUE.md` carries `### REC-69 · done` and `### REC-74 · done`, and line ~601 reads *"FIVE ITEMS
INTEGRATED TOGETHER 2026-08-08 — REC-72, REC-66, REC-65, REC-63, REC-69 — verified at 129/129 GREEN
/ 8,116 assertions, `--strict` exit 0, UI harness exit 0, every floor sitting EXACTLY at measured."*

**MEASURED ON `origin/main` AT `57b5067`, from a clean fast-forwarded worktree:**

- A grep for `airuns` over `bio-plane/src`, `bio-plane/test`, `bio-plane/checks` and
  `bio-plane/scripts`, excluding `airunspawn`, returns **no output.** The control plane's op table
  carries `airun`, `airunlog`, `airunspawn`, `airunopen`, `airuntick` and `airunclose` — and no
  context-listing op of any spelling.
- `aiRunRead` (`store.mjs`) returns `{ id, label, mode, status, ticks, created, updated, expires,
  context, principal, budget, condition, bias }`. **No `standard`.** `ai_runs.standard_pair` is
  written by `aiRunOpen` and published only by `aiRunSpawnPayload` — REC-74's row describes the
  state the tree is still in, exactly.
- `git log --all -S` finds the work on branches: **`2d9c57b`** *"REC-69 / op=airuns — the question
  no op could answer"*, and **`a3af6bb`** *"REC-74: the run's third condition is published"*.
  `git branch -a --contains` lists **ONE** branch for each, and neither is `main`.

**Why this is the delegation and not a footnote.** This project's whole product is trustworthiness
of the record, and its own development record now claims a battery figure over code that is not in
the commit it names. **A worker briefed to "take these as INPUTS rather than rediscovering them"
would have built a consumer for an op the harness cannot call** — a surface with a declared read
nothing performs, which is `surface-registry.test.mjs` ARM D4's own subject. UI-38 measured instead
and spent roughly a fifth of its budget establishing it. CONDUCT is asked to establish **which of
the five items in that line are actually on `main`** and to correct the rows, because the same brief
text may have gone to other workers. Whether the merges were lost or never made is not something
this item can see from here, and it is not guessed at.

### DELEGATION 2026-08-08 UI (UI-38) -> PLANE-TEST (`bio-plane/test/**`): **ARM U OF `airun.test.mjs` WAS EDITED CROSS-AREA, FORCED, AND IT NEEDS YOUR REVIEW RATHER THAN YOUR REPAIR**

IS-6's ARM U lifts `civicos-ui/app.html`'s renderers out by NAME. UI-38 deleted three of those names
(they are why the bias block rendered nowhere — see IC-41 and the claim above), which turned
`airun.test.mjs` into `ReferenceError: aiSessionBudgetHtml is not defined` and the battery into
**131/133 with `assertions unknown`**. Landing that and filing a note would have pushed a red
`main`, so the consumer was corrected in the same turn.

**What changed, so you can check it rather than re-derive it:** the `vm` export list; U3/U3b/U4 now
judge the PANEL rather than the budget block (strictly stronger — a value reaching its own block but
not the panel used to pass, and that is exactly the shape the bias block failed in); U5/U5b/U5c
judge the principal block **deliberately on its own**, because for a run over a PROJECT the word
"project" also appears as `context.type` and asking the panel would pass for free; U6 folded into
the panel. **ADDED: U9 (REACH), U9b, U9c, U9d** — the plane's own proof that §11's LENS now reaches
a member, asking the RECORD which keys to look for rather than naming them. **103 → 107 assertions,
green, measured against the true baseline.**

**One arm came back wrong and the correction is recorded rather than smoothed:** U9's first draft
asked for every KEY of the bias block and failed naming `now` and `moved`. Both are `null` on a run
opened with no manifest, and the surface renders nothing for a null — **correctly**, because
`#biasForRun`'s own header says `moved: null` means *unknown* and a key printed with a blank beside
it would read as *no*. **The arm was wrong, not the surface**, and it is narrowed to the fields that
have a value.

### DELEGATION 2026-08-08 UI (UI-38) -> M0 / whoever owns `bio-plane/test/op-claims.test.mjs`: **THE WALK SWEEPS UNTRACKED SCRATCH FILES, AND `WORKER.md` REQUIRES EVERY WORKER TO PUT THEM THERE**

Measured, not inferred. UI-38's negative-control harness lived in `<worktree>/.ui38-nc/` — which is
what `WORKER.md` instructs, in those words: *"The shared scratchpad is NOT isolated between
sessions … Keep every harness and scratch file INSIDE your own worktree."* It held per-arm pristine
copies of `app.html`. `op-claims.test.mjs` then failed with **12 WRONG-LEVEL violations, every one
of them a copy of a sentence that is REGISTERED in the ledger under `civicos-ui/app.html`** — the
two DO-path mentions at app.html:5720/5721 — because **the ledger is keyed by `(file, name)` and a
copy has a different file.**

So the suite is correct about the sentences and wrong about the corpus: it grades bytes that are not
in the commit and cannot reach anybody. **Two workers following two standing instructions produce a
red battery**, and the failure mode is confusing rather than obvious — the violations name real
registered exceptions, so it reads like the ledger drifted. Suggested shape, not prescribed:
exclude paths `git check-ignore` claims, or paths not tracked at HEAD, and **assert the exclusion's
REACH** so the walk cannot quietly stop reading the real corpus. UI-38 did not edit the suite: it is
not UI's, and the arm is otherwise working exactly as designed.

### DELEGATION 2026-08-08 UI (UI-38) -> WHOEVER LANDS REC-74 AND REC-69: **THE SURFACE IS ALREADY READY FOR BOTH, AND HERE IS WHAT THAT DOES AND DOES NOT MEAN**

- **REC-74.** `aiSessionPanelHtml` renders whatever the run read publishes, at every depth, naming
  no field. `standard` will render on arrival with **no UI change**, and
  `surface-registry.test.mjs` ARM Y11c already drives that exact shape (`{in_force, stated, basis}`)
  under a name the surface has never seen. **ONE THING TO KNOW BEFORE YOU CHOOSE THE SHAPE: a field
  whose value is `null` renders NOTHING, key included, and that is deliberate** — `bias.moved: null`
  means *unknown*, and printing `moved` with a blank beside it would read as *no*. So **"no bar was
  in force" must arrive as a STATED STRING, exactly as `bias.stated` does, and never as
  `standard: null`** — otherwise a member sees nothing at all and cannot tell it from a run whose
  bar simply was not published, which is the distinction REC-74's own row exists to make.
- **REC-69.** `aiSessionContextHtml` is still the ONE function that changes; the call sites, the
  renderers and every pin over them stay. **Do not add a "you may not see this" branch** — the empty
  list is byte-identical for an uninvited member and a context that does not exist, with no count of
  the withheld, and a surface that told them apart would leak exactly what the plane withheld.

### DECISIONS FOR BOB 2026-08-08 (UI-38): **NONE**

Read against `kickoffs/README.md`'s three tests. The observation-log question was a tactical
determination with the evidence in the repository, so it was decided and recorded at the site rather
than returned. The two missing merges are an INTEGRATION fact for CONDUCT, not a ruling. The
cross-area edit to `airun.test.mjs` is a coordination question and is filed as IC-41. **An empty
list is a real answer.**
## CLAIM 2026-08-08 RECORD
session: rec74-run-conditions (worktree agent-a2130ded47307129c)
opened: 2026-08-08T11:10:00Z
paths: **`bio-plane/src/airun.mjs`** — ONE new exported `STANDARD_BASIS` vocabulary (the code->sentence map for HOW the run's bar is known), appended beside `RUN_ENDINGS`. No existing export, refusal or predicate is touched, and **no new `*_CHECKS` family and no new C-number is minted** — this item PUBLISHES a condition and refuses nothing, which is its own over-strictness arm. **`bio-plane/src/store.mjs`**, named by SITE: (a) ONE new private `#standardForRun` beside PL-12's `#biasForRun`; (b) `aiRunRead`'s return object gains ONE key, `standard`; (c) `aiRunSpawnPayload`'s `payload` literal gains ONE key, `standard`, **with the raw `standard_pair` KEPT beside it** because `agent-worker` builds against it and removing it is an interface change this item has no mandate for. **NOT** `aiRunOpen` (SK-1's, untouched — the write is already correct), **NOT** `#biasForRun` (READ as the precedent, never edited), **NOT** `aiRunTick`/`aiRunLog`/`#aiRunTerminate`, **NOT** `#bundleGate`/`viewerPredicate`, **NOT** `purge` (no new table, no new column). **`bio-plane/src/schema.mjs` IS NOT OPENED**: the column exists and is written correctly; this item is entirely about what the READERS publish. **`bio-plane/test/run-conditions.test.mjs`** (NEW — the stored-vs-published sweep and this item's suite) and **`bio-plane/test/run-conditions.control.mjs`** (NEW — the negative-control driver, deliberately NOT a `.test.mjs` so `scripts/battery.mjs`'s discovery does not collect a script that edits real sources while it runs; PL-2's `versionstate.control.mjs` is the precedent). **`civicos-ui/check-refusal-codes.mjs`** — the `FLOOR` figures ONLY, MEASURED from the guard's own printed output on a green run and moved in the SAME turn; no matcher, arm, ceiling or row touched. **`docs/development/CLAIMS.md`** (this entry and its DELEGATIONS), **`docs/development/MEASUREMENTS.md`** (one appended entry). **NOT** `civicos-ui/app.html` (UI's, and the finding about its renderer is a DELEGATION below rather than an edit), **NOT** `agent-worker/**` (FLEET's, live claim), **NOT** `newgroup/**`, **NOT** `pdf-worker/**`.
interfaces consumed: I3 (the plane's op surface — `op=airun`, `op=airunspawn`, `op=airunlog`, `op=airunopen`).
interfaces owned: none. **I3's `op=airun` answer gains one key and loses none; `op=airunspawn`'s payload gains one key and loses none.** Additive in both directions, so no consumer that reads what it read yesterday reads anything different.
expected: REC-74. The run's THIRD condition (§11 — the launching project's declared standard pair) published by `aiRunRead`, which published it nowhere; and the CLASS swept — every condition and principal `ai_runs` stores, checked against what each reader publishes, with the readers that disagree NAMED and the corpus size printed.
accepts-when: as the QUEUE row. Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief: **123 suites (120 plane + 3 fleet) · 122 green + 1 named skip (`pdf-worker`, D-232 — not this item's) · 7,659 assertions**, which AGREES with the brief. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` UNPIPED (baseline: OPS 158/158 · CHECKS 160/160 · 120 of 120 suites declare a control, 395 arms · FLEET 2 members, 3/3 surface ops, floor 2/3, exit 0); `node civicos-ui/test/run.mjs` run FROM THE REPO ROOT with the exit read unpiped (baseline exit 0, 40 suites); `node civicos-ui/check-refusal-codes.mjs` exit 0 (baseline floors: 405 census · 11 families / 105 rows · REACH 191 · ratchet 73 · 28 sites / 60 lines / 16 regions / 953 region lines / 76 codes · 8 vocabularies / 51 terms). Negative controls RUN, each arm ALONE with the others held open, every restore verified by sha256 AND by `cmp`, every snapshot named UNIQUELY per ARM, the harness written INSIDE THIS WORKTREE. No push, no merge, no deploy, no version bump.
concurrency: checked over the whole file before opening anything. No live claim names `bio-plane/src/airun.mjs`. SK-1's claim (`is-wave-w0-sk1`) names `bio-plane/src/store.mjs` at **`aiRunOpen`'s guard block ONLY** and this item does not open that method. PL-11's claim names `store.mjs` at the `aiCredential*` methods. No claim names `aiRunRead`, `aiRunSpawnPayload` or `#biasForRun`. `civicos-ui/check-refusal-codes.mjs`'s FLOOR block is claimed by four landed items (PL-3, PL-4, PL-14, SK-1) **for its figures only, which is the same discipline followed here** — REC-71's rule that a floor is moved by the item that invalidates it. `civicos-ui/app.html`, `agent-worker/**`, `newgroup/**` and `pdf-worker/**` are NOT opened.
released:

### DELEGATION 2026-08-08 RECORD (REC-74) -> UI: **THE PLANE NOW PUBLISHES ALL THREE OF §11's CONDITIONS AND THE RUNNING-SESSION PANEL RENDERS TWO OF THEM AS NOTHING**

MEASURED, not inferred, by reading `civicos-ui/app.html` rather than by reasoning about it. **`aiSessionPairsHtml` SKIPS every value whose `typeof` is `"object"`** (`if(typeof v === "object") continue;`), and `aiSessionPanelHtml` renders the session through it plus three named helpers — `aiSessionPrincipalHtml`, `aiSessionBudgetHtml`, `aiSessionConditionHtml`. There is no helper for `bias` and none for `standard`.

- **CONSEQUENCE.** PL-12's bias block reaches `op=airun` and reaches no member. REC-74's standard block now does the same. The plane publishes the lens and the bar; the surface shows a member neither. **A condition published where nobody renders it is one step better than a condition published nowhere, and it is not the finish line.**
- **THE FILE ALREADY SAYS SO** and that is why this is a delegation rather than a complaint: the renderer's own comment reads *"Values that are themselves objects are not flattened — an unrendered nested shape is a gap a later item closes, and it is stated rather than papered over with a guess."* This item is naming the two shapes that are now waiting on it.
- **WHAT THE PLANE HANDS YOU, so no wording has to be authored on the surface.** `session.standard` is `{ in_force, basis, stated, pair }`. `stated` is ALWAYS a member-readable sentence from the plane's own `STANDARD_BASIS` vocabulary and travels WITH the answer, exactly as the condition sentence does — so the panel can render it verbatim and hold no copy, which is DEC-8 and is what `aiSessionConditionHtml` already does. `pair` is `{capture, connection}` or `null`, and **the two axes must never be composed into one value or one letter** (DEC-21/DEC-44 refuse it four ways).
- **AND THE ABSENT CASE MUST RENDER AS A SENTENCE, NOT AS A BLANK.** `in_force: false` is a first-class answer with four distinguishable reasons; a panel that showed nothing for it would put the surface back where the plane was before this item. DEC-17: *"an absent bar is not a bar of zero, and it must not render as one."*
- **Reversal cost: none** — this item added a key and removed none, so the surface can take it whenever UI-38's remaining work lands.

### DELEGATION 2026-08-08 RECORD (REC-74) -> RECORD / IS-9 / FL-5: **`ai_runs.state` IS WRITTEN BY TWO OPS AND PUBLISHED BY NO READER AT ALL**

Found by this item's sweep and asserted in `bio-plane/test/run-conditions.test.mjs` ARM P6, so it cannot go quiet: of the twenty columns `ai_runs` stores, **`state` is the one no publishing reader answers with.** `aiRunOpen` writes it, `aiRunTick` updates it, and `aiRunRead`, `aiRunSpawnPayload` and `aiRunLog` publish nothing of it.

- **THE PRECEDENT POINTS THE OTHER WAY.** §11 says the run object extends `capture_sessions`' proven model, and `loadCaptureSession` **does** publish its `state`. The schema calls this column *"the run's resumable SCRATCH — its work list"*, and `airun.test.mjs`'s ARM P (resumption, §14b.7) resumes from the **LOG** and never from `state`.
- **SO ONE OF TWO THINGS IS TRUE and neither is decided here:** either the column is redundant and the log is the whole resumption story, or a reader is owed and resumption today is reconstructing from observations what the run already wrote down.
- **REC-74 DELIBERATELY DID NOT DECIDE IT.** `state` is SCRATCH and not a CONDITION, so REC-74's own doctrine sentence — *a condition recorded and never published is not recorded for anybody who was not there* — does not bind it, and picking a reader for it would be this item choosing where a work list is rendered. **The member-facing read is the wrong home for it in any case**: `op=airun`'s answer feeds the running-session panel, and a model's work list on a member's screen is model working state where the record's own words belong.
- **Reversal cost: LOW NOW.** Nothing consumes the column, so adding a reader later breaks nothing; the cost rises the moment a sub-session is written to reconstruct state from the log, because that reconstruction then has to be unwound.

### DELEGATION 2026-08-08 RECORD (REC-74) -> RECORD: **NO OP PUBLISHES A PROJECT'S DECLARED STANDARD PAIR, SO THE BAR CANNOT GET THE COMPARISON THE LENS HAS**

PL-12's bias block earns its keep by publishing the recorded manifest BESIDE the record's current effective set and answering whether the lens has `moved` — *"that comparison is the whole payoff: it is what makes bias debt computable."* **The bar gets no such comparison, and the reason is that there is nothing to compare against.**

- **MEASURED:** `grep -a standard_pair bio-plane/src/` returns the `ai_runs` column, `aiRunOpen`'s write and the two reads this item touched, and nothing else. There is no `op=` that answers *what bar does project P declare today* — DEC-17's declared `required_strength{capture, connection}` is a property of a project that no read surface publishes.
- **SO `#standardForRun` DELIBERATELY DOES NOT LOOK ONE UP**, and says so at the site. A read that substituted the project's current declaration for the one the run was formed under would answer a different question and look identical, which is the whole hazard §11 exists to close.
- **WHAT IS OWED:** an op publishing a project's declared pair, after which the run's block can gain `now` and `moved` on `#biasForRun`'s exact shape and the honest sentence *"the bar has moved since this run was formed"* becomes computable. **Until then the run's bar is an AS-RECORDED fact and is published as one**, which is honest and is stated at the site rather than implied.
- **Reversal cost: none.** The block is additive in the same direction `bias` already went; adding `now`/`moved` later adds keys and removes none.

### DELEGATION 2026-08-08 RECORD (REC-74) -> FLEET / WHOEVER OWNS `op=whoami`: **FL-2's PRINCIPAL DELEGATION IS A DIFFERENT ITEM AND REC-74 LEFT IT STANDING**

REC-74's brief raised it as possibly inside this sweep. **It is not, and the reason is measurable rather than a matter of taste.** This sweep's corpus is *what the run ROW stores against what each reader of that row publishes*. `ai_runs.principal_plane` and `principal_claude` are both stored and both published by `aiRunRead` — ARM P2 drives them — so the run object's principals are not silent. **FL-2's finding is about `op=whoami`, which reads no `ai_runs` row and is therefore not a reader in this corpus at all.** Widening the sweep to reach it would have meant widening it to every op, which is a different item with a different corpus and a different instrument.

The delegation stands unchanged and unworked. The agent worker's `principal_source: "UNPUBLISHED — …"` remains the honest shape and REC-74 followed the same discipline in its own field: `basis` names WHY there is no bar rather than inferring one.

### DECISION FOR BOB 2026-08-08 (REC-74) — **THE ABSENT BAR PUBLISHES *WHICH* ABSENCE IT IS, AS A FIVE-TERM VOCABULARY, RATHER THAN AS A NULL OR AN ABSENT KEY**

- **What runs provisionally.** `op=airun` and `op=airunspawn` publish a `standard` block on every found run: `{ in_force, basis, stated, pair }`. **The key is ALWAYS present**; the absence lives INSIDE it, as one of five `basis` terms each carrying a canned member-readable sentence — `recorded`, `none-recorded`, `context-has-no-project`, `names-no-axis`, `unreadable`. `pair` is `{capture, connection}` only when a bar is in force, and never composed.
- **Why it was ambiguous.** D-216's model check established the shape the plane already uses one construct over: a read naming no project gets **no `current` field at all**, not a default — an absent key as a legitimate answer. Following that literally here would have made `op=airun` publish nothing when there was no bar, which is byte-identical to what it did **before this item**, when the field was silent. **An absent key cannot be told from *we did not look***, and telling those two apart is the whole of REC-74.
- **The alternative.** Publish `standard_pair` verbatim — the column, `null` when there is none — which is what `op=airunspawn` already did. It is one line, it needs no vocabulary, and it moves no floors. **It also cannot say which of four absences is true**, so the honest sentence DEC-17 requires (*"an absent bar is not a bar of zero, and it must not render as one"*) would have to be authored on every surface that renders it, in a system whose renderers are field-name-blind by design and author nothing.
- **Recommendation: keep the vocabulary.** It costs one exported map in `airun.mjs`, it is the same shape PL-12's bias block already established one field over, and the sentence travels WITH the answer so no surface holds a copy (DEC-8). The cases are not decorative: **`context-has-no-project` is DEC-17's structural absence and `none-recorded` is a fact about the formation, and a reader that could not tell them apart would read a projectless run as a project that failed to set a standard.**
- **The one place this reads beyond what was said.** DEC-17 rules that a project with no declared strength *"gates nothing, and the case says so rather than showing a blank"*. This item applies that to the RUN as well as to the case, on the reasoning that §11's conditions exist so a version is interpretable against them and an uninterpretable condition is not one. If the intent was that the run object stay silent about a bar it never had, **that is a one-line change to `#standardForRun`'s return and nothing else moves** — the consumer would then read UNKNOWN, which the suite already drives as its own arm.
- **What reversing it costs: LOW, AND IT DOES NOT RISE THE WAY A SCHEMA DECISION WOULD.** No column, no table, no migration and no refusal was added — the change is entirely in what two readers publish, and both are additive. Reversing means deleting one key from two returns and one exported map; every consumer that reads what it read yesterday is unaffected either way. **It gets slightly more expensive once a surface renders the sentence**, because the surface would then have to grow the wording the plane stopped supplying — which is the direction DEC-49 exists to prevent, and is the real argument for deciding it now rather than later.
## CLAIM 2026-08-08 RECORD (D-235a)
session: worker-d235 (worktree agent-ae7c0334a4057a38f)
opened: 2026-08-08T00:00:00Z
paths: **`bio-plane/src/store.mjs` — named by SITE, not by file:** (1) `suggestVersion`'s SUCCESS RETURN only — the `storedComposition` read and the object literal AFTER `END DEC-49 REGION is-suggest-write`; **no refusal, no check, no write line and no DEC-49 region is opened**; (2) ONE new private `#versionCollections` beside `basisVersions`; (3) `basisVersions`' per-row `legs`/`grounds`/`leg_count`/`legs_complete` assembly, which now calls that helper instead of spelling the two reads itself. **NOT opened:** `#suggestionPersisted`, `#suggestionFrontmatter`, `substanceOf`, `basisVersionsOf`, `#canon`, `#fmSafe`, `promote`'s projection write, `schema.mjs`, any refusal string, any `SUGGEST_CHECKS` row. `bio-plane/test/suggest.test.mjs` (this endpoint's own suite — a new block 8, the `NEGATIVE CONTROL:` header extended). `bio-plane/test/suggest.control.mjs` (new arms, plus REC-75's (D-234c) arm RE-ANCHORED — see below). **`bio-plane/test/versions.test.mjs` — ONE superseded pin CORRECTED, and it was not predicted:** that suite's D-227 bound arm demanded exactly THREE `#rows(` inside a 4,600-character slice of `basisVersions`, which pinned the reader's TOPOLOGY rather than its rule, and this item's helper extraction moved it. Corrected in the same turn with the reason at the site, never exempted. `docs/development/DEBT.md` (the **D-235 row's disposition ONLY**). `docs/development/INTERFACE-CHANGES.md` (APPEND — **IC-40**, minted with `node tools/mintid.mjs IC`; the I3 version bump and the RESOLUTION are CONDUCT's). `docs/development/MEASUREMENTS.md` (APPEND — one dated section). `docs/development/CLAIMS.md` (this entry and its DELEGATIONs). **`bio-plane/scripts/coverage.mjs` WAS NOT TOUCHED — no floor moved and the measurement says so**, see `numbers` below. **NOT** `docs/development/QUEUE.md` (CONDUCT is its sole writer), **NOT** `civicos-ui/**`, **NOT** `checks/bio-checks.mjs`, **NOT** `bio-plane/src/index.mjs`, **NOT** `newgroup/**`, **NOT** `pdf-worker/**`, **NOT** any other `.test.mjs`.
interfaces consumed: I3 (`op=suggest`'s success answer and `op=basisversions`' version projection — **both CHANGE here; IC-40 filed with measured consumer impact**)
interfaces owned: none
expected: D-235(a) — `op=suggest`'s success answer has TWO SOURCES and says so about only one. REC-75 made `composition` publish the record's bytes and label them `composition_of: "record"`; `version`, `kind`, `run`, `state`, `author`, `at`, `legs`, `count`, `grounds` and `ground_count` on the SAME answer stayed caller/candidate-derived and unlabelled. DECIDE what each field publishes, source it there, and label EVERY field so a consumer can tell from the answer alone which bytes are the record's.
concurrency: checked over the whole file. **No live claim names `suggestVersion`, `basisVersions`, `suggest.test.mjs`, `suggest.control.mjs` or `versions.test.mjs`** — REC-75's claim (the last to hold them) is `released: 2026-08-08` and integrated at `bb426ac`. The other live claims that open `store.mjs` open it by DIFFERENT sites (`documentsNamingEntity` + the correspondence statics; a `DEC-49 REGION is-selection-moved` marker pair; `provenanceRouteAssess`). `scripts/coverage.mjs` is NOT touched, so it cannot conflict. `git stash` was NOT used at any point.
numbers: **TRUE BASELINE MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 133/133 suites green · 8,319 assertions · exit 0**, HEAD `bb426ac`, `git status --short` empty. The worktree arrived **without `bio-plane/node_modules`** (`npm ci`, exit 0 read unpiped) and **level with `main`**, so no fast-forward was needed. **AFTER: 133/133 · 8,333 · exit 0.** Delta **+14, attributed by DIFFING the two full runs and never by subtraction:** `suggest.test.mjs` 80 -> 93, `versions.test.mjs` 77 -> 78, **nothing else moved.** `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` UNPIPED, **exit 0**. `node civicos-ui/test/run.mjs` from the REPO ROOT, exit unpiped, **0**. `node civicos-ui/check-refusal-codes.mjs` from the repo root, exit unpiped, **0**. `node tools/plancheck.mjs` clean but for UNPUSHED.
floors: **NONE MOVED, AND THAT IS A MEASUREMENT RATHER THAN AN OMISSION.** `REGISTER_FLOOR` printed **`arms 570/570 · classified 129/129 · corpus 130/130`** — met exactly, no slack, nothing to collect. **This item added SIX control arms and the register counted ZERO of them**, because they are LABELLED (`(D-235a)`) rather than numbered and the instrument says in its own output that a labelled arm is not counted: **a sixth confirmation of D-233 on the same file REC-75 measured it on, produced by an item that did not set out to measure it.** The DEC-49 guard printed every floor met with **`regionLines 1310`** unchanged: **no DEC-49 REGION MARKER is added, removed or moved by this diff** (`git diff | grep "DEC-49 REGION"` returns five hits, all of them test-file string literals and prose). **ONE THING FOR CONDUCT TO RE-READ ANYWAY:** `basisVersions` is a governed WHOLE-FUNCTION site in arm C (105L) and this item SHORTENED it by removing a query; whole-function sites are counted in the `sites`/`lines` totals and not in the `regionLines` floor, so nothing moved here — but `regionLines` is a property of the MERGED source and has moved at integration five times.
what came back WRONG, recorded rather than smoothed, because it is what the controls actually produced: **(i) REC-75's OWN (D-234c) ARM REFUSED TO ARM BLIND.** Its second edit anchored on `composition: storedComposition,`, which this item replaced, so `edit()` threw `ARM REFUSED TO ARM BLIND … occurs 0 times` — **the guard working**, and it is why the harness stops rather than reporting a green arm that armed nothing. Re-anchored, with the finding kept at the site. **(ii) (D-235b) BEHAVED OTHER THAN DECLARED AND THE FINDING IS WORTH MORE THAN THE ARM.** It re-sources `version` from the submission, and I declared that the CROSS-OP arm and the STRUCTURAL PIN would fail with the name arm. **Neither did.** The cross-op fixture's name carried no fold, so the two sources agreed and the re-sourcing was invisible — **REC-75's own receipt one field over, reproduced**; and the pin was a BANNED LIST that could not name the local `name` without also matching `recorded.name`. **Both were fixed at the site rather than by relaxing the declaration:** the fixture now folds, and the pin was INVERTED into a totality over the ROOT IDENTIFIERS the group draws on (printed: `["b","promoted","rc","recorded"]`), so a value taken from somewhere nobody anticipated fails too. **Final run: 23 arms, 0 behaved other than declared.**
released: 2026-08-08 — committed on `worktree-agent-ae7c0334a4057a38f`, **NOT pushed and NOT merged**; CONDUCT integrates. D-235(a) CLOSED; (b) left open and re-pointed in the row. IC-40 filed with measured consumer impact (zero inside the repository). Two superseded pins corrected with their reasons at the site, none exempted.

### DELEGATION 2026-08-08 RECORD (D-235a) -> CONDUCT: **IC-40 IS FILED WHERE REC-75 DELIBERATELY FILED NONE, AND THE DIFFERENCE IS WORTH ONE LINE**

REC-75 judged `composition_of` additive and `composition`'s changed bytes an existing divergence
CLOSED, and filed no IC while flagging it *"so CONDUCT can disagree cheaply"*. **This item files
one, and the reason is not a disagreement with that call:** three fields a consumer can already
read now carry different bytes rather than one, and **one of them is an ADDRESS** — `version` is
what `derived_from` names — so a consumer that fed the old answer back was naming a reading the
record does not hold. That is a different kind of change from a spelling inside a byte blob.

**The version bump and the RESOLUTION are yours.** Measured impact inside the repository is ZERO
(`agent-worker/src/index.mjs` is the only consumer of either op and reads none of the moved
fields); the reversal is named field by field in the IC row and is held open by two control arms.

### DELEGATION 2026-08-08 RECORD (D-235a) -> REC-74 / WHOEVER NEXT OWNS `op=basisversions`: **THE READ OP DOES NOT PUBLISH `kind` AT ALL, AND THE PROJECTION HAS STORED IT SINCE PL-3**

Found by measurement rather than by reading: this item's cross-op arm compared ten fields on its
first run and printed **`["kind!=kind", "legs!=legs"]`**. Only the second was D-235's defect.

- `inquiry_basis_versions.kind` is written at PL-1's one write site and **is inside the canonical
  composition, therefore inside the FREEZE** — PL-3 put it there deliberately, because *what a run
  PROPOSED a reading as is part of what the version IS*. `op=basisversions`' answer never carries
  it, so the one op a member reads a basis through cannot say which of §9's five kinds each
  reading is, while the freeze refuses any change to it.
- **This is REC-74's own shape** — a fact stored and never published — one op over from the one
  REC-74 holds, and the two should probably be answered together.
- **Deliberately NOT folded in here.** Adding a field to a read op is a different act with a
  different blast radius from labelling the source of a write op's answer, and this item's brief
  is the second. The cross-op arm names `kind` in a comment and excludes it from its corpus rather
  than scoring it silently, so the next reader inherits the finding instead of re-deriving it.

### DELEGATION 2026-08-08 RECORD (D-235a) -> M0-14 (D-233, the control register's arms tally): **A SIXTH CONFIRMATION, FROM AN ITEM THAT WAS NOT LOOKING FOR ONE**

Six negative-control arms were added to `suggest.test.mjs`'s declaration block. `node
scripts/coverage.mjs --strict` printed **`570 arms stated`** BEFORE and AFTER, floor met exactly.

The arms are LABELLED (`(D-235a)` … `(D-235f)`) because this harness has carried labelled arms
since D-231, and the register's own output says a labelled arm is not counted. **REC-75 measured
this on the same file on the same day and reported it as a fifth confirmation; this is a sixth,
and what it adds is that the blindness is now costing arms on every consecutive item touching this
one suite** — the figure does not merely lag, it is structurally deaf to the form this suite's
declarations take, and the direction is the generous one.

## CLAIM 2026-08-08 CONTENT-PDF (CPDF-10)
session: cpdf10-tier3-ocr-path (worktree agent-abf7fa164b4016146)
opened: 2026-08-08T00:00:00Z
paths: **`bio-plane/src/textchain.mjs` (NEW, the whole file)** — the transcription provenance chain, engine-generic. **`bio-plane/checks/bio-checks.mjs`** — ONE new export `TEXT_CHAIN_CHECKS` (C-35.1..11) APPENDED at the end of the file; **no existing family, row or constant is touched**. **`bio-plane/src/index.mjs`** — named BY SITE: the `textchain.mjs` import line, `needsTier2` (one guard added), the NEW `needsTier3`, the NEW `ocrTextFromMember`, the two `LAYER_FIDELITY_*` constants, the acquire reading assembly's wire branch and its three reading literals, ONE `READING_READS` line, three `OPS` rows, two `SESSION_OPS` lines, one `NEEDS` capability row, one `REC30_VIEWER_READS` line. **`bio-plane/src/store.mjs`** — by site: the `textchain.mjs` import, the `safeJson` module helper, `#writeTextSource` + `attestText` + `attestationsFor` + `transcribedDocuments` (all NEW), two lines inside `#writeReadings`, the `readingFor` return, the `TABLES` purge array (two names added), `TEXT_SOURCE_LIMIT_*`, three arms of the DO dispatch map. **`bio-plane/src/schema.mjs`** — TWO new tables (`text_attestations`, `reading_text_source`) placed BEFORE the `host_governor` block; no backticks and no semicolon in any comment, both traps checked BY GREP rather than by memory. **`bio-plane/src/affordances.mjs`** — one `CAPTURE_ACTS` entry, one `NON_ACTS` row, one `RUNG_ABSENT` row. **`bio-plane/src/pdfstructure.mjs`** (CONTENT-PDF's own) — the image-only marker and `pageDrawsImage`. **`bio-plane/test/textchain.test.mjs`** (NEW, this item's suite). **`bio-plane/test/reading-wire.test.mjs`** — ONE superseded assertion CORRECTED with the reason at the site. **`bio-plane/test/gate-reads.test.mjs`** — two rows in `GATED`. **`bio-plane/test/bounds.test.mjs`** — the roster pin, two `DRIVEN_ELSEWHERE` names, two `answersByOp` entries. **`bio-plane/test/provenance-marker.test.mjs`** — the `CEILING` constant only. **`bio-plane/test/nc-cpdf10.mjs`** (NEW — the negative-control driver, deliberately NOT a `.test.mjs`: it EDITS REAL SOURCES while it runs and neither the battery nor the fleet walk must discover it; PL-3/PL-4/PL-11/FL-2/CPDF-9's precedent). **`bio-plane/scripts/coverage.mjs`** — `REGISTER_FLOOR`'s THREE keys ONLY, and nothing else in the file. **`civicos-ui/check-refusal-codes.mjs`** — the floor block's nine keys ONLY. **`civicos-ui/test/surface-registry.test.mjs`** — ONE new row in `ACTS_AWAITING_SURFACE`. `docs/development/INTERFACE-CHANGES.md` (NEW IC-39), `docs/development/DEBT.md` (three NEW rows), `docs/development/CLAIMS.md` (this entry and its delegations). **NOT** `docs/development/INTERFACES.md` (I2's version bump is CONDUCT's — IC-39 is PROPOSED), **NOT** `docs/development/QUEUE.md`, **NOT** `civicos-ui/app.html` or any other UI source, **NOT** `newgroup/**`, **NOT** `agent-worker/**`, **NOT** `pdf-worker/**`.
interfaces consumed: I1 (bytes), I2 (the text shape, structurally unchanged), I6 (the fleet-member call shape, for the seam only), I7 (the FORMAT registry).
interfaces owned: I2's producer side at the reading boundary. **IC-39 filed, PROPOSED; the version bump is CONDUCT's.**
expected: QUEUE.md CPDF-10 — the Tier-3 OCR path, placement re-based by DEC-35/DEC-42 to in-account tesseract-as-a-fleet-member pending CPDF-12.

**WHAT WAS BUILT, AND WHAT WAS DELIBERATELY NOT.** CPDF-12 is running in parallel on the page-to-pixels renderer and the tesseract member. So this item built EVERYTHING THAT DOES NOT NEED PIXELS and declared the seam exactly. **NO SECOND RENDERER WAS BUILT AND NO ENGINE WAS MEASURED.** The Tier-3 arms drive a STUB member answering the declared contract; the suite's header says so and every arm's label says so, so no reader can mistake a green here for a measured engine.

**THE ITEM'S OWN THESIS, BUILT: `text_source` IS A CHAIN.** It was the string `"layer"`; it is now an ordered array of steps (`layer · pixels · ocr · ai · attested`), each naming what performed it, with FOUR rules ENFORCED rather than documented — (1) a single label is REFUSED by name, and so is an `ocr`/`ai` step that does not name its engine, which is rule 1 arriving one level down; (2) EVERY DERIVATION STEP MAY ONLY WEAKEN — `appendStep` computes the cap as the minimum over derivation steps and refuses a stronger claim, saying why (*cleaned text is more READABLE, not more RELIABLE*); (3) confidence is `{value, basis:"engine"}` or the STATED string `"none"`, and the fence is on the BASIS not the value, because a self-reported 0.99 and a computed 0.99 are the same bytes; (4) a region below the floor becomes `undetermined` **with its text DISCARDED** — while its ANCHOR is KEPT, so a reader can still be pointed at the pixels nobody could read.

**THE SUBTLE PART, AND WHERE THE ITEM COULD HAVE GONE WRONG.** Rule 2 says every step weakens; attestation plainly does not. The resolution is NOT an exception — `attested` is a VERIFICATION step and not a derivation, so `derivationCap()` is never raised by anything, and `gradeCeiling()` is where an attestation covering the extent supersedes it. That is QUEUE.md's own sentence — *"verification supersedes it as grade determinant, never as record"* — as two functions, both driven. Collapsing them is exactly how an attestation would come to look like a derivation that improved the text.

**ATTESTATION IS A MEMBER ACT AND IT IS SCOPED.** `op=attesttext` is refused to a machine credential at BOTH doors (`mutating: true` keeps a machine off the session route; `checkAttestation` refuses the stamp at the store) — two fences on purpose, because REC-45 measured the gate accepting `asserted_by: token:member`. The predicate is `isMachineIdentity`, IMPORTED, because REC-46 measured what eleven hand-typed copies of that question cost. **`extentCovers` DEFAULTS TO NOT COVERING**: an extent this module cannot parse covers nothing, since the failure that matters is an unreadable extent quietly reading as "all of it" — which is how a member's careful check of one paragraph would come to underwrite a whole scanned budget book.

**DISTINGUISHABLE IN THE PROJECTION, THE INDEX AND AN EXPORT — all three driven through ops.** `op=reading` spells the provenance out; `reading_text_source` is a derived table so the question is a QUERY (`op=textprovenance&step=ocr`) rather than a blob a reader must parse; the exported bundle's `data/provenance.json` carries the chain step for step with the engine named.
measured before deciding, and TWO MEASUREMENTS CHANGED THE DESIGN:
  **(1) THE IMAGE-ONLY CLASS — THE ENTIRE REASON OCR EXISTS — REACHED NO ESCALATION AT ALL, AND IT WAS FOUND BY DRIVING THE CODE RATHER THAN BY READING IT.** `needsTier2` fires when undetermined REGIONS outnumber decoded CHARACTERS. A scanned page produces **zero of each** — there is no font, so nothing ever reaches the decode path to fail — so the test was `0 > 0`, FALSE. MEASURED on a real Tier-1 run over an image-only fixture: `{"document":"","pages":[{"page":0,"text":"","undetermined":[]}],"counts":{"chars":0,"undetermined":0}}`. **A Tier-3 trigger reading Tier 2's `no_text_layer` marker would therefore have fired on NOTHING, for ever, while looking entirely correct** — a mechanism believed on the strength of its existence, which WORKER.md names as the defect this project meets most. FIXED in `pdfstructure.mjs`: Tier 1 now emits `no_text_layer` (I2's EXISTING vocabulary, not a new spelling) when a page has **no font resource AND draws an image** — CPDF-9's own structural signal (its exhibit verified image-only at *4 pages, 0 fonts*), a fact about the file rather than a threshold on an output. **AND A BLANK PAGE IS NOT A SCAN**: zero fonts alone would route every separator sheet to an engine, which is the invitation to invention CPDF-9's blank-page control exists to catch, so the image half is REQUIRED and the over-strictness arm drives it. A rider landed with it: a pure scan now does NOT escalate to Tier 2 either, because pdf.js would walk the same file and reach the same answer one cross-worker hop later — and the test is *is EVERY marker a scan marker*, never *is ANY*, so a MIXED document still escalates.
  **(2) A TEXT LAYER'S FIDELITY IS UNMEASURED, so it is recorded as `null` — UNDETERMINED, STATED.** The tempting answer was a strong letter (Tier 1 decodes the real agenda at 99.9%). That is the DECODE, not the fidelity: `pdfstructure.mjs` decodes through the FILE'S OWN `/ToUnicode` map, so a perfect decode of a wrong layer is a perfect decode of a wrong layer — and CPDF-9 measured that 3 of 14 recent Legistar attachments name ABBYY FineReader, meaning the Clerk's **certified enacted resolutions** carry garbled machine OCR the record has been reading as authored text. A letter invented here would have been the record overclaiming **one field wide on every document in the store**. Recorded as D-251 with the ~30-line read that would narrow it.
accepts-when: as the CPDF-10 row, every clause driven THROUGH AN OP. **THE THREE NEGATIVE CONTROLS THE ROW NAMES ARE STANDING ARMS RATHER THAN ONE-TIME BREAKAGES**: strip the marker → `checkChain` refuses and an OCR'd document would be indistinguishable (RULE 1 arms); drop the floor → a garbled region emits a best guess (RULE 4 arms, driven through the seam with a 0.12-confidence `$10,000`); collapse the chain to one label → `TEXT_CHAIN_COLLAPSED`.
numbers: **BASELINE MEASURED IN THIS WORKTREE BEFORE ANY EDIT, on a quiet tree: 133/133 suites green · 8,319 assertions · 113.6s · exit 0** at HEAD `bb426ac`; `git status --short` empty; `coverage.mjs --strict` exit 0 read UNPIPED; `civicos-ui/test/run.mjs` exit 0. The worktree was **0 commits behind `main`** (checked, not assumed) and arrived **without `bio-plane/node_modules`** — `npm ci` run first, per WORKER.md. FINAL figures are in the report.
floors: **THREE MOVED, EACH FROM A PRINTED FIGURE, AND ONE THAT WAS NOT MOVED IS THE MORE INTERESTING ENTRY.**
  - **`meaning-bounds.test.mjs`'s BARE-COLLECTION RATCHET: NOT MOVED, deliberately.** It went **38 → 40** the moment the two new reads landed unbounded. The fix was to BOUND THEM (`limit`/`truncated`, the plane's own spelling, `cap + 1` so `truncated` is MEASURED rather than inferred from a full page) — back to **38, the baseline exactly**, with both ops now printed on the BOUNDED roster. **A ceiling is not a ratchet**; moving it would have bought the item a fortieth unbounded read.
  - **`bounds.test.mjs`'s capped-op roster: 26 → 28**, MEASURED by running the walk and taking what it PRINTED (28 against a pin of 26), never by adding two — which matters here more than usual, because that pin's own recorded history is two items colliding on it by arithmetic. Both arrivals are the ORDINARY `named-cap` kind, so nothing about the detector had to move.
  - **`provenance-marker.test.mjs`'s SWALLOWED-READ ratchet: 23 → 24.** That ratchet's rule is that the number may not RISE without somebody LOOKING at the new one. Looked at: it is ONE site, `safeJson`, and it is the class's REMEDY rather than a new instance of it — every caller SURFACES its null as a stated absence (`readingFor` publishes `{recorded:false, why:…}`, distinguished deliberately from "not transcribed"). It also REPLACED two ad-hoc `try { JSON.parse } catch {}` blocks written earlier in this same item, so the net contribution is one site instead of three. The reasoning is at the constant.
  - **`coverage.mjs`'s `FLEET_FLOOR` and `REGISTER_FLOOR`: untouched.** No fleet member was added (the OCR member is CPDF-12's), and `TEXT_CHAIN_CHECKS` is a `*_CHECKS` family so the DEC-49 guard harvests it automatically — the RESERVED-SUFFIX trap was respected without re-learning it.
  - **`coverage.mjs`'s `REGISTER_FLOOR`: all three keys moved, `arms` 570 → 576, `classified` 129 → 130, `corpus` 130 → 131**, from what the green run PRINTED (`GREW by 6`). One new suite declaring six arms — the six negative-control arms. THE ONE KEY SET WAS KEPT AS ONE: no duplicate `arms:` key was introduced, which this file's own header records having been done SIX times by keep-both merges, once with the LOWEST value winning silently.
  - **`civicos-ui/check-refusal-codes.mjs`: NINE keys moved** — `families` 14 → 15, `rows` 152 → 163, `census` 412 → 424, `reach` 206 → 217, `governedSites` 61 → 66, `regions` 48 → 53, `regionLines` 1310 → 1407, `codesChecked` 122 → 141, `refusalsJudged` 124 → 143. Every one from the figure the harness PRINTED on a green run in this worktree. `outcomeReturns` (70) and `unclassifiedOutcomes` (3) are UNCHANGED and none of the new sites is among the unclassified. `bodyLines` is deliberately NOT moved — it is that block's one non-ratcheted figure by its own stated reason. **THIS CORRECTS THIS ITEM'S OWN FIRST DECISION**, which was to leave them for UI: see the delegation below, which is kept and amended rather than rewritten.
what came back WRONG, recorded rather than smoothed:
  - **A REFUSAL PATH REPORTED THE WRONG FINDING, AND THE SUITE CAUGHT IT.** The seam's HTTP-failure arm was declared to leave the document unread naming the STATUS; it came back naming *"the OCR member could not be reached"* — because the code parsed `r.json()` before checking `r.ok`, and parsing a 500's body THROWS into the catch. **Two different facts were collapsing into the wrong one**: "the member is down" and "the member failed on this document" are not the same finding, and only the second is about the document. Corrected at the site with the receipt in the comment.
  - **I CLASSIFIED THE NEW ACT'S RUNG WRONG, AND `rung-ladder.test.mjs` REFUSED IT.** `attesttext` was first declared `attested`, reasoning from DEC-4's doctrine that member attestation is the route to the top of the transcription axis. The suite's pin — *"`attested` is carried by exactly the two acts Constructs:275 sources"* — was RIGHT: that rung's stated property is that the act requires AN AUTHORITY THE GROUP DOES NOT HOLD ALONE (a signer's key, a timestamp authority's token), and `attesttext` requires neither. **A rung tighter than its rule is not a safer rung; it is an undeclared change to what the rung MEANS.** Corrected to `RUNG_ABSENT` on the ground `undetermined` — the bucket whose own comment describes exactly this shape (performed once, kept attributed and dated, corrected by a further act moving FORWARD, and NOT signed), beside `resolvetestify`, which is the same shape one axis over. **The ladder's gap is named rather than papered over, which is what that bucket is for.**
  - **THREE MORE OF THE PLANE'S TOTALITY GUARDS FIRED ON THE NEW OPS, which is them working**: `affordances.test.mjs` (an op in `NEEDS` that is neither a published act nor a named NON_ACT — answered by classifying `attesttext` CAPTURE-DIRECTED for `op=attest`'s three stated reasons, rather than promoting it into `ACTS` and splitting the class); `gate-reads.test.mjs` (two unclassified reads); `hygiene.test.mjs` (a suite building two Miniflares and disposing neither, plus a non-deterministic exit).
  - **A test-arithmetic error of my own**: two arms asserted a 3-step chain where `layerChain()` + one append is 2. Corrected in the TEST, not the code — the code was right.
concurrency: checked over the whole file. No live claim names `textchain.mjs`, `text_attestations`, `reading_text_source`, `TEXT_CHAIN_CHECKS` or any C-35 id. `bio-checks.mjs` is opened by live claims BY SITE and at different sites (the type catalog, `ACT_SHAPE_CHECKS` rows, the counterparty constants); this item only APPENDS a new export at the end and touches no existing row. `index.mjs`/`store.mjs`/`schema.mjs`/`affordances.mjs` are RECORD's and CAPTURE's — see the DELEGATION below. **If `TABLES` in `store.mjs`'s purge conflicts at integration, KEEP BOTH SIDES' NAMES IN ONE ARRAY**: that array has already been merged by hand once (PL-1/PL-12, recorded in its own comment), and taking either side alone silently drops derived tables from the purge, which is D-113's exact failure arriving through a merge. Three test files carry PINNED FIGURES this item moved (`bounds`, `provenance-marker`) or left deliberately unmoved (`meaning-bounds`) — **if any of them conflicts, RE-RUN THE INSTRUMENT AND TAKE WHAT IT PRINTS**; none of these numbers may be arrived at by arithmetic.
released:

### DELEGATION 2026-08-08 CONTENT-PDF (CPDF-10) -> RECORD / CAPTURE: **THIS ITEM LANDED IN `index.mjs`, `store.mjs`, `schema.mjs` AND `affordances.mjs` IN ONE TURN, AND EVERY SITE IS NAMED**

The CPDF-6 precedent exactly: a content area's capability that cannot exist without an op, a table
and a projection lands WITH the item and is FLAGGED, rather than being split across a delegation
round-trip that would leave the module unreachable in the meantime. Every site is enumerated in
`paths:` above.

Nothing existing was reshaped. The ops are additive, the two tables are new, the purge array gained
two names, and **the only edit to an existing behaviour is `needsTier2` gaining a guard in the same
turn that created the behaviour it guards** — a scan could not reach that predicate at all until
Tier 1 learned to name it, so this is a bound on new behaviour rather than a narrowing of old.

**One judgement RECORD may want to revisit, named rather than buried:** `attesttext` carries
`contribute` and mints NO fifth capability token, on REC-13's stated reasoning. What makes it
different from its siblings is not a permission — it is that a MACHINE cannot perform it — and that
is enforced where machine-ness is decided rather than by a capability a group would have to be
told about.

### DELEGATION 2026-08-08 CONTENT-PDF (CPDF-10) -> UI / WHOEVER OWNS `civicos-ui/check-refusal-codes.mjs`: **FIVE FLOORS MOVE, AND THEY ARE YOURS TO MOVE FROM THE FIGURES THE INSTRUMENT PRINTS**

`TEXT_CHAIN_CHECKS` is a new DEC-49 family with **11 rows** and **5 new governed REGIONS**
(`is-text-chain-shape`, `is-text-chain-monotone`, `is-text-region-confidence`, `is-text-anchor`,
`is-text-attestation`), all reachable by a surface through `op=attesttext`. So `families`, `rows`,
`reach`, `governedSites` and `regions` all rise.

**AMENDED, AND THE FIRST VERSION OF THIS DELEGATION WAS WRONG.** It said the floors would be left
for UI, on the reasoning that `civicos-ui/**` is not this item's path and that a floor must be moved
from a figure the instrument PRINTED rather than by arithmetic. The second half is right and is why
the correction happened; the first half does not follow from it. **The rule is to move a floor you
invalidate IN THE SAME TURN**, and the moment this item's own run went green the harness printed
every figure needed — so leaving them would have shipped a ratchet sitting eleven rows and five
regions below the truth, which is exactly the slack this file's own header argues a ratchet cannot
carry. **NINE keys are moved, each from a printed figure, and they are listed in `floors:` above.**
`bodyLines` is untouched (that block's one deliberately non-ratcheted figure) and
`outcomeReturns`/`unclassifiedOutcomes` did not move.

**WHAT IS STILL YOURS, and it is the part this item genuinely could not do.** `attesttext` is
registered in `ACTS_AWAITING_SURFACE` as a DEBT — the arm asked for that by name rather than for an
exemption — and **the surface itself is owed**. It is not a button: it has to put the page image and
the transcribed text side by side and let a member mark WHAT THEY ACTUALLY CHECKED, because an
unscoped attestation is refused (C-35.11) and an over-scoped one is the exact failure the extent
exists to prevent. Until that lands the act is reachable only through the API, which is the honest
state and is what the row records.

**One thing worth checking while you are there, and it is a real question rather than a formality:**
the eleven translations are written for a member looking at a scanned document. C-35.7
(pseudo-confidence) and C-35.11 (extent) are the two a member is most likely to actually receive, and
they are the two most likely to want a surface's own wording review.

### DELEGATION 2026-08-08 CONTENT-PDF (CPDF-10) -> CPDF-12 / WHOEVER LANDS THE OCR MEMBER: **THE SEAM IS DECLARED, DRIVEN AND REFUSING — THE ONLY NEW CODE IS THE MEMBER**

The contract is `ocrTextFromMember` in `bio-plane/src/index.mjs`, stated in full in the comment block
directly above it. The member binds as `OCR_WORKER` and answers `POST /transcribe` with
`{ok, engine, version, cap, measured_by, confidence_floor, pages:[{page, regions:[{text, source:{kind:"pdf-page",page,rect}, confidence}]}]}`.

**FIVE THINGS IT MUST NOT DO, each already REFUSED and each already driven by an arm in
`test/textchain.test.mjs`** — these are not requests, they are fences you will meet: report a `cap`
with no `measured_by`; omit an engine or a version; omit a region's `{page, rect}` anchor;
self-report confidence (`basis` must be `engine`, or the region says `none`); send a best guess for a
region below its own floor. A member doing any of them leaves the document HONESTLY UNREAD with the
reason on the reading's basis — never half-transcribed, never crashing an acquire.

**WHAT YOU DO NOT HAVE TO BUILD:** the chain, the monotone rule, the confidence contract, the anchor
check, the floor, the attestation act, the projection, the index, the export, the purge coverage, or
any of the refusals. All of it is landed and driven.

**AND THE PREDICATE THAT ROUTES TO YOU IS ALREADY REAL** — this is the finding above, restated here
because it is the half of the seam that would otherwise have silently never fired. Tier 1 emits
`no_text_layer` for a page with no font resource that draws an image, so `needsTier3` selects the
image-only class structurally. Before this item it selected nothing, for ever.

**WHAT THIS ITEM COULD NOT CLOSE AND HANDS YOU (D-252):** Tier-3 routing is PER DOCUMENT and the
thing it routes is PER PAGE. A mixed document — a text-layer report with scanned exhibits stapled to
the back, an ordinary Council packet shape — routes WHOLE to OCR, and the wire assigns
`i2text = built.text` wholesale, so a good text layer would be REPLACED by an OCR pass at cap `C`.
Harmless today (the branch is untaken); a live defect the moment your member exists. The fix is a
page-wise merge, and I2's text shape already has the right grain (`text.pages[]`), so it is a merge
rule rather than a contract change. **It wants writing in the turn that lands the member, not
guessing at now** — which is why it is a debt row pointed at your item rather than code written blind
against a producer that does not exist yet.

## CLAIM 2026-08-09 VERIFY

session: is-wave-vf1 (worktree agent-a66f1cf86b51a86bd)
opened: 2026-08-09T00:00:00Z
paths: **`bio-plane/scripts/coverage.mjs`**, named by SITE: (a) `discoverFleet`'s per-member control flag REPLACED by a per-SUITE walk (`suiteControls`) — the old `some()` flag is DELETED, not kept beside it; (b) `FLEET_FLOOR` gains `suites` and `arms`, both from a printed green run; (c) the derived `fleetSuiteRows`/`fleetSuitesUndeclared`/`fleetSuitesUnclassified`/`fleetArms` and their two error blocks; (d) a new `OWED_CONTROLS` ledger with `OWED_TOTAL`/`OWED_OUTSTANDING`/`OWED_ANCHOR` and its report section; (e) `REGISTER_FLOOR`'s three numerals moved to what this item's own green run PRINTED. **NOT** the OPS walk, **NOT** the CHECKS walk, **NOT** `control-register.mjs`, **NOT** `provenance.mjs`. **`bio-plane/test/owed-controls.test.mjs`** (new, this item's suite) and **`bio-plane/test/owed-controls.control.mjs`** (new, its harness — NOT a `.test.mjs`, it edits real sources). `docs/development/VERIFICATION.md` — the negative-control register row RE-MEASURED, and a new VF-1 section. `docs/development/IS-BUILD-PLAN.md` — **the VF-1 row's `accepts-when` only**, corrected: it named a command that cannot report. `docs/development/CLAIMS.md` (this entry).
interfaces consumed: none
interfaces owned: none — no op, no schema, no wire shape. This item is an instrument.
expected: IS-BUILD-PLAN VF-1 — the seven owed negative controls, PLACED on their owners and RUN where the owner has landed. A LEDGER item and a slot-free VERIFY lane.
accepts-when: as IS-BUILD-PLAN VF-1, with its `accepts-when` corrected in the same turn. Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT at 138/138 / 8,827 (the brief carried no figure; VERIFICATION.md's register row said 128 of 128 · 542 arms against a real 134/621 — stale for the SIXTH time). `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read unpiped, exit 0. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line. No push, no merge — CONDUCT integrates. QUEUE.md, DECISIONS.md, MEASUREMENTS.md, INTERFACES.md and DEBT.md are not written by this session.
concurrency: checked over every unreleased block in this file — no live claim names `scripts/coverage.mjs`, and the two new test files did not exist. `agent-worker/**` and `pdf-worker/**` are READ and armed-then-restored by the control harness, never edited: every restore in that harness is verified by sha256 AND by `cmp` against a per-arm pristine copy with its byte count printed.
released:

### DELEGATION 2026-08-09 VERIFY (VF-1) -> UI, and a note for DIST: **22 UI SUITES DECLARE NO NEGATIVE CONTROL, AND UNTIL NOW NO INSTRUMENT SAID SO**

VF-1's class sweep asked where else this repository holds a set of suites to a WEAKER rule than
its siblings — the kind of defect the fleet hole was, not the instance. Measured 2026-08-09 and
now PRINTED by `node bio-plane/scripts/coverage.mjs` on every run:

    civicos-ui/test  19/41 declaring · 110 arms · 3 unclassified
      NO CONTROL (22): act-attest · action-page · artifact-fetch · auth-surface · bias-vocabulary
      · bound-sweep · capture-honesty · change-layers · cite-act · conclude-act · document-page
      · finder · glossary-nav · header-facts · inquiry-page · intent-write · link-surface
      · publication-entry · record-list · seals-backrestore · snapshot-render · surface-registry
    newgroup/test    1/2 declaring · 3 arms   (NO CONTROL: embed.test.mjs — DIST's)

**IT IS REPORTED AND DELIBERATELY NOT GATED, and the reason is measured rather than assumed:**
arm (8) of `bio-plane/test/owed-controls.control.mjs` wires the sweep into `--strict` and the run
EXITS 1 over an honest tree. A gate that fails honest runs gets switched off, and a fence tighter
than its rule is an undeclared interface change wearing the costume of caution. **What VF-1 will
not do is author 22 controls for another area's suites** — a control nobody ran is exactly the
defect this item exists to defend against, so writing 22 declarations from outside would produce
the appearance of coverage and none of it.

**WHAT IS ASKED:** each UI suite states what to break in the surface it tests and what must then
fail, and RUNS it. `civicos-ui/test/run.mjs` is not the battery, so nothing about this is
mechanised for you today; the figure above is, and it moves when you move it.

**NOT A DEBT ROW ON PURPOSE.** `DEBT.md` is not written by this session, and CONDUCT should decide
whether this is one row or twenty-two. The measurement is the part that was missing.
## CLAIM 2026-08-09 UI (UI-42 — version review: rotation and diff)
session: ui42-version-review (worktree agent-a8c8ed9c32eb56980)
opened: 2026-08-09T00:00:00Z
paths: **`civicos-ui/app.html` — named by SITE, not by file** (it is shared ground and other UI
  workers may be live): (1) the NEW region between `/*__VERSION_REVIEW_START__*/` and
  `/*__VERSION_REVIEW_END__*/`, appended after `__AI_SESSION_END__` — every renderer in it is new
  and nothing outside it moved into it; (2) ONE key ADDED to `SURFACES` inside
  `__SURFACES_START__`/`__SURFACES_END__` — `"basis-versions"`, no existing key touched; (3) ONE
  line inside `boot()` — the route chain `if(!publishedRouteFromHash() && …)`, which gains
  `&& !versionReviewRouteFromHash()` and nothing else. **NOT** the CSS block (this surface reuses
  existing classes and adds none), **NOT** `__CATALOG__`, **NOT** `__SEMANTICS__`, **NOT**
  `__DOCPROFILE__`, **NOT** `__INQUIRY_PAGE__`, **NOT** `__ELICITATION__`, **NOT** any other
  marked region.
  `civicos-ui/test/version-review.test.mjs` (NEW), `civicos-ui/test/version-review.control.mjs` (NEW).
  `civicos-ui/test/surface-registry.test.mjs` — **the `versionhide` row STRUCK from
  `ACTS_AWAITING_SURFACE` (ARM A4c's DRAIN requires it in the same commit), and FIVE floors moved
  from the figures the arms PRINTED: ARM A3 18→19, A4d 15→16, A4e 15→16, D1 50→64, D5 30→39.**
  No other arm touched.
  `civicos-ui/test/preauth-vocabulary.test.mjs` — WALK 2's router census five → SIX and the new
  router CLASSIFIED as post-authentication, with both halves of the classification pinned. That arm
  exists to stop a new router arriving unclassified and it fired on the first run of this item; it
  is CORRECTED, never exempted. No other arm touched.
  `.gitignore` — one pattern (`.ui*-harness/`) for negative-control pens, in the block that already
  documents transient harness directories under `civicos-ui/`.
  `docs/development/CLAIMS.md` (this entry), `docs/development/kickoffs/UI.md` (APPENDED, not
  rewritten — other UI workers are live), `docs/development/MEASUREMENTS.md` (appended).
  **NOT** `bio-plane/src/**` (READ ONLY), **NOT** `bio-plane/checks/**` (read only — the suite
  IMPORTS `VERSION_MACHINE` rather than copying it), **NOT** `bio-plane/test/**`, **NOT**
  `bio-plane/scripts/**`, **NOT** `civicos-ui/tokens.css`, **NOT** `civicos-ui/check-*.mjs`,
  **NOT** `docs/development/QUEUE.md`, **NOT** `docs/development/IS-BUILD-PLAN.md`, **NOT**
  `newgroup/**`, **NOT** `agent-worker/**`.
interfaces consumed: I3 — `op=basisversions` (read) and `op=versionhide` (the one act this surface
  hosts). Nothing else is asked of the plane and no shape is changed, so no IC row is owed.
interfaces owned: none.

### DELEGATION 2026-08-09 UI (UI-42) -> UI-43 (the accept ceremony) AND UI-45 (CURRENT): **THE MEMBER-FACING WORDS FOR THIS VOCABULARY NOW EXIST IN ONE PLACE, AND A THIRD SPELLING WOULD BE THE DEFECT**

UI-43's row asks for *"the derived falsifier back in plain words (\"your answer fails only if ALL of
these fail\")"*. That sentence already exists TWICE and must not come to exist three times:

- `elicFalsifier` in `__ELICITATION_START__` (UI-27) composes it for the member AUTHORING a
  structure — *"Your answer fails only if ALL of these fail: …"* / *"Your answer fails if ANY of
  these fails: …"*.
- `VREV_FAILS_ALL` / `VREV_FAILS_ANY` in `__VERSION_REVIEW_START__` (this item) compose it for a
  member READING one back — *"This reading fails …"*, the same two tails, different subject.

`version-review.test.mjs` §0b pins that the elicitation block still carries both tails, so if UI-43
re-words either, that pin fails and the next session re-words BOTH rather than shipping two answers
to one question. **Reuse one of the two constants; do not author a third.** The same applies to
`VREV_STATE_WORD`, the four member-facing sentences for `suggested/considering/accepted/rejected`:
its keys are pinned against `VERSION_MACHINE.legal`, IMPORTED from `bio-plane/checks/bio-checks.mjs`
rather than copied, so a fifth state fails the harness instead of rendering a blank line.

**AND WHAT THIS SURFACE DELIBERATELY DID NOT TAKE.** It hosts `versionhide` and nothing else. The
other five version acts are still on `ACTS_AWAITING_SURFACE` in
`civicos-ui/test/surface-registry.test.mjs` with your item names on them; ARM A4c will require the
row struck in the same commit that surfaces each one, and ARM A4d/A4e's floors move UP by one each
time. For UI-45 specifically: `op=basisversions` publishes `current` ONLY when a `project` is named,
and **this surface names none and therefore renders none** — §7's rule that a stance belongs to the
project and not to the question, honoured by not asking rather than by filtering an answer.

### DELEGATION 2026-08-09 UI (UI-42) -> CONDUCT: **FIVE FLOORS MOVED IN `surface-registry.test.mjs`, AND TWO OF THEM WERE ALREADY SLACK BEFORE THIS ITEM TOUCHED THEM**

Moved from the figures the arms PRINTED, never by adding to the number in the file, and each with
its reason at the site:

| arm | was | now | why it moved |
| --- | --- | --- | --- |
| A3 (act placements) | 18 | 19 | this item hosts `versionhide` |
| A4d (catalogue outside the register) | 15 | 16 | STRIKING the register row moves that act into this floor's corpus |
| A4e (distinct hosted acts) | 15 | 16 | a surface now hosts it |
| D1 (ops called statically from app.html) | 50 | **64** | this item added ONE call; the floor was already **13 low** |
| D5 (declared reads) | 30 | **39** | this item added TWO; the floor was already **seven low** |

**The last two are the reportable half.** D1 and D5 are not floors this item invalidated — they were
carrying 13 and 7 of slack before it arrived, which is REC-71's finding (*a floor with slack is not
a ratchet*) sitting in two arms nobody had re-measured since they were written. D1 at 50 would have
sat green through the deletion of a fifth of app.html's static op calls. They are moved rather than
noted, because noting a stale floor is what left them stale, and moving three of five while leaving
two known-slack ones behind is the inconsistency the next reader would trip on.

### DECISIONS FOR BOB 2026-08-09 (UI-42): **NONE**

Measured against `kickoffs/README.md`'s three tests. The two wording judgements this item made are
ones the record already assigned to the surface: DEC-29(b) states in as many words that *"(b)'s
wording clause is a UI string"*, and DEC-32 clause 1 forbids the analyst's vocabulary while leaving
the member-facing rendering to the surface — which is what UI-27's elicitation already established
one construct over. Nothing here is a consequence Bob has not already ruled on.
## CLAIM 2026-08-09 RECORD (PL-17 — the THIRD `asserted_by` state)
session: pl17-third-asserted-by-state (worktree agent-a6ce65818b1c1b8d2)
opened: 2026-08-09T00:00:00Z
paths: **`bio-plane/checks/bio-checks.mjs`** (the MINT ONLY — a new block beside the REC-46 machine-identity predicates at the head of the file. **`C-25.6` / `VERSION_GROUND_UNASSERTED` and `C-2.8` / `checkGrounds` ARE NOT TOUCHED**, which is DEC-65's own sequencing and the reason this item exists separately from the one that owns them), **`bio-plane/src/affordances.mjs`** (`VOCABULARIES` gains one key, IMPORTED and never restated), **`bio-plane/test/sufficiency-state.test.mjs`** (NEW), **`bio-plane/test/sufficiency-state.control.mjs`** (NEW — the negative-control driver, deliberately NOT a `.test.mjs` because it EDITS REAL SOURCES while it runs), **`bio-plane/scripts/coverage.mjs`** (`REGISTER_FLOOR` only, moved from PRINTED figures), `docs/development/CLAIMS.md` (this entry and its DELEGATIONS), `docs/development/DECISIONS.md` (DEC-65's enactment note ONLY — appended, no other row touched), `docs/development/INTERFACE-CHANGES.md` (the IC row for the additive `op=affordances` key). **NOT** `bio-plane/src/store.mjs`, **NOT** `bio-plane/src/schema.mjs`, **NOT** `civicos-ui/**`, **NOT** `pdf-worker/**`, **NOT** `newgroup/**`.
interfaces consumed: I3 (the plane's op surface — `op=affordances`).
interfaces owned: none. The `op=affordances` addition is ADDITIVE and its IC row is filed; **CONDUCT takes the version bump and the RESOLUTION.**
expected: DEC-65, answered 2026-08-09 by session BOB under Bob's standing delegation: *"take (b) as amended by FL-3 … with the third `asserted_by` state minted FIRST."* Mint an explicit **"no independent-sufficiency claim was made"** value for the field whose published meaning is *a member said this part is enough on its own* — undetermined-is-first-class applied to one field — so that a machine's single-part ground row never wears a member's meaning. **The check (`C-25.6`) and PL-3's endpoint guard are the NEXT item's** and are not touched here; PL-14 re-measures the strength pair after that.
accepts-when: battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief (the brief carried no figure): **138 suites (134 plane + 4 fleet) · 138/138 green · 8,827 assertions · 126.0 s · exit 0**. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED (baseline: OPS 162/162 · CHECKS 219/219 · controls 134/134, 621 arms · REGISTER_FLOOR arms 621 / classified 133 / corpus 134 · exit 0); `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED (baseline exit 0); `node tools/plancheck.mjs` clean but for UNPUSHED. Negative controls RUN, each arm ALONE with the other defences held OPEN, an OVER-STRICTNESS arm included, restores verified by sha256 AND by `cmp` against uniquely-named per-arm pristine copies, the driver written INSIDE THIS WORKTREE. No push, no merge, no deploy, no version bump.
concurrency: checked over the whole file at `ad87db7` (this worktree is AT `origin/main`, not behind). **No claim in `CLAIMS.md`, open or released, names `bio-plane/checks/bio-checks.mjs` as a live path today**: FL-3's claim (the item that raised this work) names it under **NOT**, and the DELEGATION that owns `C-25.6` and PL-3's endpoint guard is FILED but UNCLAIMED — DEC-65 sequences it AFTER this one. `bio-plane/src/store.mjs` and `bio-plane/src/schema.mjs` are deliberately NOT in the paths above so that item finds them untouched.
released: 2026-08-09 — committed on `worktree-agent-a6ce65818b1c1b8d2`, **NOT pushed and NOT merged**; CONDUCT integrates. **THE STATE IS MINTED AND IT IS DELIBERATELY INERT.** `SUFFICIENCY_UNCLAIMED = 'none:independent-sufficiency'`, `sufficiencyClaimState()`, `isSufficiencyClaimed()` / `isSufficiencyUnclaimed()` and `SUFFICIENCY_CLAIM_STATES` land in `checks/bio-checks.mjs` beside the REC-46 identity predicates — **the edit to that file is PURELY ADDITIVE, proven by `git diff` containing not one `-` line in it**, so `C-25.6` and `checkGrounds` are byte-identical and the next item finds them as FL-3 left them. The words are published through `op=affordances` (IC-45), IMPORTED and never restated. **Battery 139/139 green · 8,865 assertions · 141.9 s · exit 0** against a true baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT of **138/138 · 8,827 · 126.0 s** (the brief carried no figure, so there was nothing to agree or disagree with; the worktree arrived without `bio-plane/node_modules` and `npm ci` was run there first). **Delta +38 attributed by DIFFING the two full runs PER SUITE, never by subtraction:** `sufficiency-state.test.mjs` +35 (new) and `hygiene.test.mjs` 553 -> 556 (its per-suite walk gains rows for a new file); **135 of 137 suites byte-identical in count.** `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, **exit 0** — OPS 162/162, CHECKS 219/219, provenance `145 of 145 discovered item(s) are in the commit at HEAD`. `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED, **0**. `node civicos-ui/check-refusal-codes.mjs` exit **0**, **no DEC-49 region touched** (`git diff` contains ZERO `DEC-49 REGION` marker lines) and arm E unmoved at 9 vocabularies / 56 terms — **which is itself the measurement that arm E does NOT reach this vocabulary**, stated at the site and delegated to UI rather than papered over. `node tools/plancheck.mjs` **clean but for UNPUSHED**. **`REGISTER_FLOOR` moved 621 -> 629 arms / 133 -> 134 classified / 134 -> 135 corpus, ALL THREE IN THE SAME TURN**, from the figures a green run PRINTED **as REPRODUCIBLE** — read only AFTER the new files were in a commit, because the pre-commit run reported the identical numerals as CONTAMINATED and a floor moved on those is permanently too high; **ONE key set, verified by `grep -c`.** **EIGHT CONTROL ARMS, each armed ALONE with every other defence held OPEN, a BASELINE arm included, every restore verified by sha256 AND by `cmp` against uniquely-named per-arm pristine copies with byte counts printed and floored against the empty-string digest; re-run at HEAD with zero restore problems. THREE CAME BACK OTHER THAN DECLARED and all three are written into the driver's header rather than smoothed:** (6) the OVER-STRICTNESS arm caught **this suite hand-typing two case variants of a minted literal**, which agreed for free until the literal moved; (7) caught **the sweep matcher grading asking sites by the spelling they use TODAY**, so a site converted to the new predicate VANISHED from the sweep while the assertion over it went on passing — a classifier grading one literal hiding exactly what it was built to find; and (3) fell on two pins its declaration did not anticipate, and **that surprise is a DESIGN finding rather than an instrument one: adding `none:` to `MACHINE_STAMP_PREFIXES` makes `C-25.6` refuse the new value with no line of `C-25.6` changing — a fail-closed shape for one array entry, recorded precisely so the next item does not reach for it, because it makes the record say a machine claimed this about a value whose whole meaning is that nobody did.** **NO MIGRATION IS OWED and the reason is structural rather than a survey:** there is no SQL column for this field — `schema.mjs` states at the `inquiry_basis` site why the attribution is deliberately not projected — nothing writes the new value, and every stored row predating it carries a named member because both gates refused anything else at the moment it was written. **WHAT THIS ITEM COULD NOT DO:** it does not make the state DO anything. Nothing writes it and no gate distinguishes it, which is DEC-65's own sequencing and not a shortfall — but it means the state is a mechanism believed on its EXISTENCE until the next item drives it, and the only defence against that is `§7` of the suite, which MEASURES the gates' present verdict and is written to FAIL when they change. **Two DELEGATIONS filed** (the C-25.6/endpoint item, and UI). **IC-46 was minted by an accidental second invocation of `mintid.mjs` and is UNUSED — named in the IC-45 row rather than left as a silent gap.** `git stash` was NOT used at any point.

### DELEGATION 2026-08-09 RECORD (PL-17) -> WHOEVER LANDS DEC-65's SHAPE (b) — the item that owns `checks/bio-checks.mjs`'s C-25.6 AND PL-3's ENDPOINT GUARD: **THE STATE IS MINTED AND HERE IS EXACTLY WHAT IS LEFT**

DEC-65's first clause is done. FL-3's DELEGATION above is still the scope of yours, minus the
vocabulary question, which is answered. What you inherit:

- **THE VALUE AND THE PREDICATE, ALREADY THERE.** `SUFFICIENCY_UNCLAIMED` and
  `isSufficiencyClaimed` / `isSufficiencyUnclaimed` / `sufficiencyClaimState` are exported from
  `checks/bio-checks.mjs`, beside the REC-46 identity predicates. **ASK THE PREDICATE, NEVER MATCH
  THE LITERAL.** `isSufficiencyClaimed` answers TRUE for a named member and for nothing else, which
  is where DEC-32's *only ever reached by an affirmative, attributed act* is enforced in one place.
  A site that matches `'none:independent-sufficiency'` itself has rebuilt the predicate and will be
  wrong the day a fourth state exists.
- **TWO ASKING SITES, NOT ONE, AND A SWEEP THAT FINDS THEM.** `C-25.6` (`basisVersionFindings`) and
  `C-2.8` (`checkGrounds`) ask the IDENTICAL question one level apart. `test/sufficiency-state.test.mjs`
  finds them structurally rather than by name, so a third site written later is found too — **and
  its first two matchers were both WRONG in ways worth knowing before you write yours:** a line-wise
  matcher saw only one of the two (`checkGrounds` wraps its condition across two lines), and a
  matcher keyed on `isMachineIdentity` made a CONVERTED site VANISH from the sweep while the
  assertion over it went on passing.
- **BLOCK 7 OF THAT SUITE WILL FAIL WHEN YOU LAND, AND THAT IS ITS PURPOSE.** It pins that the value
  passes `C-25.6` today and that no site consumes it. **CORRECT those assertions with a comment
  saying why the old expectation was right when it was written. Do not exempt them.** Control arm
  (7) in `test/sufficiency-state.control.mjs` IS your change, run for one arm, so you can see in
  advance exactly which two assertions move: `node test/sufficiency-state.control.mjs`.
- **THE SECOND SITE STILL FIRES FIRST.** FL-3's measurement stands and PL-17 did not touch it:
  PL-3's endpoint guard in `src/store.mjs` refuses on `legsIn.length > 0` — **any leg at all** —
  so amending `C-25.6` alone still unblocks nothing.
- **AND `schema.mjs` CARRIES A SENTENCE THAT BECOMES INCOMPLETE THE DAY YOU LAND.** The
  `inquiry_basis` comment reads *"asserted per ground, by a named member"*. **PL-17 deliberately did
  NOT correct it**, because until a gate accepts the third state that sentence is still true, and
  documenting a mechanism that does not exist is the defect this project meets most. Correct it in
  the same turn as the check. **Mind the trap while you are in there: no semicolon inside an inline
  `--` comment in `schema.mjs`.**
- **AND DEC-65's NARROW LICENCE, so it is not quietly widened.** The arithmetic argument is that
  with **exactly one part** there is no maximum to take. Several parts, none of them claimed, is a
  different thing and this state does not license it. If your check permits the second, say so out
  loud, because it is a doctrine change and not an implementation detail.

### DELEGATION 2026-08-09 RECORD (PL-17) -> UI: **A SURFACE RENDERS THIS FIELD VERBATIM, AND THE PLANE NOW PUBLISHES THE WORDS FOR IT**

`civicos-ui/app.html`'s grounding receipt renders `Asserted by ${esc(String(g.asserted_by||""))}`
directly (the `elicReceiptHtml` block). **PL-17 did not edit it — `civicos-ui/**` is yours.**

- **WHAT IS AVAILABLE:** `op=affordances` now answers `vocabularies.sufficiency_claim_states`, a
  map of the field's states to the sentence a member reads instead of the machine word. IC-45 is
  filed with the measured consumer impact (zero: the key is additive, the UI harness reads exit 0
  before and after).
- **WHAT TO DO WITH IT:** read the field through the plane's `sufficiencyClaimState()` reading
  rather than printing the stored value. Today the field can only hold a member's name, so nothing
  is broken; **the moment DEC-65's shape (b) lands, that same line prints
  `Asserted by none:independent-sufficiency` at a member.**
- **AND THE WORDING IS WORTH YOUR OWN REVIEW rather than mine.** The four sentences honour DEC-32's
  ban — *never show AND / OR / disjunction / grounds, not even as tooltips* — and speak in the
  register `groundInquiry`'s own receipt already uses ("group of reasons", "enough on its own").
  A matcher in the suite holds them to the ban, and **it can only see the analyst's WORDS; it
  cannot see a sentence that teaches the structure without using them.** That reading is a person's.
- **ONE MEASURED THING NOT TO DO, so you do not spend the hour I did.** `check-refusal-codes.mjs`
  arm E does not reach this vocabulary, and **adding either module to its `VOCABULARY_MODULES` walk
  would make the guard FAIL ON CORRECT CODE**: harvesting `checks/bio-checks.mjs` by arm E's shape
  rule picks up four maps that are not member-facing text at all (`OBJECT_TYPES`,
  `LEGACY_TYPE_ALIASES`, `FORBIDDEN_ALIASES`, `EARNED_SOURCE_AXIS`), and `src/affordances.mjs`
  picks up `RUNGS`, whose values are single ladder words — every one of them would fail arm E's
  phrase rule. **A fence tighter than its rule is not a safer fence.** Arm E's three rules are
  therefore carried in `bio-plane/test/sufficiency-state.test.mjs` instead, in the battery, and the
  limitation is stated at the site. If you want them under arm E properly, the shape rule needs a
  way to tell a member-facing text map from a machine-to-machine one, and that is a real item.
## CLAIM 2026-08-09 FLEET (FL-4 — the scheduler consumer)
session: is-wave-w7-fl4 (worktree agent-a970ab6bb20ad1444)
opened: 2026-08-09T00:00:00Z
paths: **`bio-plane/src/store.mjs`, BY SPAN AND NOT BY FILE** — (a) the `#schedConsumers` registry ONE APPENDED ENTRY (`ai-run-wake`, the TENTH — the row's "eighth" is stale and is corrected below); (b) `onAlarm`'s named-consumer chain, ONE `else if` and ONE spread key, so the consumer does not disappear into `probes`; (c) THREE new private methods `#aiRunWakePending` / `#aiRunWakeWake` / `#aiRunWake`, appended BESIDE the reaper's three (`#aiRunReap*`) and modelled on them; (d) ONE row appended to `#migrate`'s additive-column list. **NOT** `#aiRunTerminate`, **NOT** `aiRunOpen`/`aiRunTick`/`aiRunClose`, **NOT** `#aiRunReap*` (READ as the precedent, never edited), **NOT** `captureRequest`/`captureRequestDrain`/`#captureRequestConduct` (READ, and `#captureRequestConfigured` CALLED, never edited), **NOT** `purge` (no new table — see below), **NOT** `#reconcileAlarm`/`#armScheduler`. **`bio-plane/src/schema.mjs`** — ONE nullable column (`run_woken_at`) appended to the EXISTING `capture_requests` table, which already sits before the `host_governor` block; no new table, no reshape. **`bio-plane/test/scheduler.test.mjs`** (the accepts-when's own suite) and **`bio-plane/test/scheduler.control.mjs`** (NEW — the negative-control driver, deliberately NOT a `.test.mjs` because it EDITS REAL SOURCES while it runs; PL-4's `capturerequests.control.mjs` precedent). **THREE OTHER SUITES, EACH CORRECTED AND NEVER EXEMPTED, each with a comment at the site saying why the old one was right when written:** `bio-plane/test/airun.test.mjs` (ARM S1's registry delta 9→10, plus ONE new arm S3c pinning that the wake was APPENDED after the drain); `bio-plane/test/leadslug.test.mjs` (its NULLABLE pin read `TEXT` at END OF LINE, which pinned nullability by way of the column being LAST in the table — a trailing comma broke it); `bio-plane/test/run-conditions.test.mjs` (REC-74's `ROLE` table gains the two new `ai_runs` readers as `HOUSEKEEPS` — its ARM W3 caught them BY NAME, which is that sweep doing exactly what its own comment promises). **`docs/development/CLAIMS.md`** (this entry and its delegations), **`docs/development/IS-BUILD-PLAN.md`** (the FL-4 row's two stale figures ONLY), **`docs/development/DEBT.md`** (ONE appended row, D-260, minted with `tools/mintid.mjs`). **NOT** `bio-plane/src/airun.mjs`, **NOT** `bio-plane/src/index.mjs`, **NOT** `bio-plane/checks/bio-checks.mjs` (no new refusal is minted — see below), **NOT** `agent-worker/**` (FLEET's own live claim), **NOT** `civicos-ui/**`, **NOT** `pdf-worker/**`, **NOT** `newgroup/**`.
interfaces consumed: I5 (the store's schema — one additive nullable column). I3 is NOT touched: no op is added, removed or reshaped, so no IC row is owed.
interfaces owned: none.
expected: IS-BUILD-PLAN FL-4, W7 slot A. ONE appended entry to `#schedConsumers` waking suspended runs on daemon completion and on resumption ticks. No second alarm and no cron — SCHEDULER.md's recorded decision, read before anything was designed.
accepts-when: as IS-BUILD-PLAN FL-4. Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT: **138/138 suites green · 8,827 assertions** (2 suites report no count: `bundle`, `livefire`; fleet 2 members, both RAN — `pdf-worker` is no longer dark). `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, **exit 0** (OPS 162/162 · CHECKS 219/219 · controls 134/134, 621 arms · REGISTER FLOOR arms 621/621, classified 133/133, corpus 134/134 · FLEET 2 members, 4 ops). `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED, **exit 0**. Negative controls RUN, each arm ALONE with the others held open, an OVER-STRICTNESS arm and a BASELINE arm included, every restore verified by sha256 AND by `cmp` against uniquely-named per-arm pristine copies. No push, no merge, no deploy, no version bump.
concurrency: checked over the whole file. The live FLEET claim (FL-3, `agent-ad6e5ed43aac4a2ab`) reserves `agent-worker/**` and says **NOT `bio-plane/src/**`** — disjoint from this claim, which is the mirror image. `store.mjs` is claimed BY SPAN per the standing budget; the spans named above are the scheduler registry and the AI-run block, and no other live claim names either.
released:

### DELEGATION 2026-08-09 FLEET (FL-4) -> RECORD / CONDUCT: **THE WAKE NOW EXISTS AND NOTHING CONSUMES IT — FL-3's DELEGATION RE-MEASURED, AND THE MISSING PIECE IS A DECISION**

FL-3 filed *"the plane has no `AGENT_WORKER` caller, and I8 cannot go STABLE without one"*. FL-4 re-measured it rather than citing it: `grep -a AGENT_WORKER bio-plane/src/` still returns NOTHING. **What FL-4 changes is that the record now has something to hand a caller**, so the gap is sharper and its shape is known.

- **WHAT FL-4 BUILT, AND IT IS ENTIRELY RECORD-SIDE.** On daemon completion the tenth consumer HOLDS the run's lease (so a run waiting on our own daemon is no longer reaped at one hour with `lease` written as the bound that stopped it — our pacing recorded as the run's death), appends ONE observation entry saying the daemon answered, and stamps `capture_requests.run_woken_at` so the completion is delivered exactly once. Every part is driven in `test/scheduler.test.mjs` and every part has a control arm.
- **WHAT IT DID NOT BUILD, AND WHY NOT A CALL SITE.** `/run` wants `{ run_id, store, credential, … }` and the credential is PL-11's `ai` class. `ai_credentials` stores `token_id` and `secret_sha` and **never the value** (D-199 (2)); minting is a member act (D-199 (3)). **The plane holds nothing it could hand over**, so writing the caller is not a wiring task — it needs a ruling on whether an instance may hold a minted `ai` credential as an env binding the way it holds `DAEMON_TOKEN`. **Filed as D-260 with that framing**, and the suite's own header states the limit in words rather than leaving a reader to infer it from arms that are not there.
- **NOTHING REGRESSES MEANWHILE.** The hold is a strict improvement whether or not anything ever resumes a run, and it is bounded by the request's own expiry — a request nothing can satisfy stops holding its run at its TTL and the reaper takes it then, with an honest bound. That bound has its own control arm, because an unbounded hold would be a worse defect than the one it closes.

### DELEGATION 2026-08-09 FLEET (FL-4) -> CONDUCT: **`op=capturerequests` GAINED ONE ADDITIVE FIELD, AND NO IC IS FILED — DISAGREE CHEAPLY IF YOU WANT ONE**

`op=capturerequests` now publishes `run_woken_at`. Additive: every existing reader keeps the shape it reads and no field moved. **This is PL-15's precedent taken deliberately** — that item added `lead_inquiry` to the same projection, judged an added optional field on an existing answer to be inside I3's stable contract rather than a change to it, and flagged it here instead of filing an IC. FL-4 does the same and for the same reason PL-15 gave: the projection is EXPLICIT rather than a row spread, so a column not named there is a column no caller can see — and this one is the single field that distinguishes *the daemon has not answered* from *the daemon answered and nothing collected it*, which is exactly what an operator looking at a suspended run needs. Measured consumer impact inside the repository: **ZERO** — `agent-worker/src/index.mjs` does not call this op at all, and `civicos-ui/**` does not read this answer.

### DELEGATION 2026-08-09 FLEET (FL-4) -> WHOEVER OWNS `INVESTIGATIVE-SESSION.md`: **§14b.3 CARRIES TWO STALE FIGURES OF THE KIND THIS PROJECT MEASURES MOST OFTEN**

§14b.3 reads *"**SEVEN live consumers today** (`store.mjs:1452`)"*. **MEASURED 2026-08-09: the registry holds TEN and sits at `store.mjs:1778`**, and `store.mjs` is **23,971** lines rather than the ~16,300 the surrounding plan was written against. PL-5 added the eighth, PL-4 the ninth, FL-4 the tenth, and none of them moved the design's sentence. **NOT EDITED HERE** because the design document is not this item's path and because a build item rewriting the design it was built from is the wrong direction — but it is the hand-carried-number failure this repository names as its most repeated, sitting in the paragraph that tells the next consumer how to join. The IS-BUILD-PLAN FL-4 row's own two stale figures WERE corrected, from measurement, in the same turn.
## CLAIM 2026-08-09 UI (D-257)
session: agent-ab5e6576a583e2cc4
opened: 2026-08-09T00:00:00Z
paths: civicos-ui/test/version-predecessor.test.mjs, civicos-ui/test/add-surface.test.mjs, civicos-ui/test/auth-surface.test.mjs, civicos-ui/test/bound-sweep.test.mjs, civicos-ui/test/preauth-vocabulary.test.mjs, civicos-ui/test/publishedcase.test.mjs, civicos-ui/test/run.mjs, civicos-ui/check-refusal-codes.mjs, the CLASS_NAMED_UNGUARDED list in bio-plane/test/hygiene.test.mjs, the D-257 row in docs/development/DEBT.md
interfaces consumed: none
interfaces owned: none
expected: D-257 — wire `bio-plane/scripts/provenance.mjs` into the civicos-ui walks that FLOOR on what their working-tree walk found, so every floored figure is the `git ls-tree HEAD`-backed one, and remove the D-257 entry from the hygiene allowlist. The sweep for the class is what decides which files are touched; the list above is what the measurement found.
released:

## DELEGATION 2026-08-09 UI (D-257) → M0 (VERIFICATION)
D-257 guarded every `civicos-ui` walk that FLOORS on what its working-tree walk found, and removed
seven entries from `hygiene.test.mjs`'s `CLASS_NAMED_UNGUARDED` allowlist. The sweep asked the same
question of the bio-plane entries and **five of them floor on their own walk**, measured by reading
each site rather than by the matcher alone:

- `bio-plane/test/bounds.test.mjs` — a whole-repository walk floored at `corpus.length > 200 && walkChars > 5_000_000`.
- `bio-plane/test/case-opened.test.mjs` — the same shape, the same two floors.
- `bio-plane/test/identity-claims.test.mjs` — `files.length >= 24` over its `src/` + `checks/` walk.
- `bio-plane/test/machinefences-dec49.test.mjs` — `minted.size >= 12`, the codes harvested by walking `src/`.
- `bio-plane/test/planning-hygiene.test.mjs` — `found.length >= 2` over headings discovered by walking `docs/`.

A sixth is a DIFFERENT exposure and is named rather than counted: `bio-plane/test/machine-fences.test.mjs`
walks `test/` only to decide whether a code is `pinned()` by some suite. A phantom cannot raise a floor
there, but it CAN satisfy a requirement — a fence code reads as pinned by a suite no other checkout has,
which fails in the GENEROUS direction.

**The fix is the two lines this item wrote seven times**, and the pattern is in any of
`civicos-ui/test/{version-predecessor,add-surface,auth-surface,bound-sweep,publishedcase}.test.mjs`:
import `scripts/provenance.mjs`, keep the SWEEP over the whole working tree (so a finding in
uncommitted work is not hidden), and compute the FLOOR over `git ls-tree HEAD` alone. Say UNVERIFIED
rather than clean when git cannot answer — including in the caller's OWN printed labels, which is the
defect D-257's control ARM 3 caught in its own first draft.

**And a second finding, paid for rather than reasoned about:** `bio-plane/scripts/op-claims.mjs` walks
the whole repository with NO dotfile filter, so D-257's control harness — pristine copies of `app.html`
under a dot-directory — turned the battery RED with ten findings attributed to the copies. That is the
same class M0-16 found in `coverage.mjs`'s fleet walk, in the opposite direction: one walk admits what
another would never run. Whether op-claims SHOULD descend into dot-directories is M0's call; it is
recorded here because it is a real difference between two walks of the same estate.
## CLAIM 2026-08-09 FLEET (FL-5 — sub-session fan-out with REPORT contracts, IS-9(a))
session: is-fl5 (worktree agent-a0b07bfdf348ecea8)
opened: 2026-08-09T00:00:00Z
paths: **`agent-worker/src/subsession.mjs` (NEW)**, `agent-worker/src/index.mjs` (the driver's `fanout` and a NEW `collect` step, plus the one `meaningRead` helper), `agent-worker/src/harness.mjs` (`collect` becomes a JUDGED row and its `why` names both numbers — the return contract must be enforced at the row that COLLECTS, not at `compose`, which has already interpreted), **`agent-worker/test/fanout.test.mjs` (NEW, the suite)**, **`agent-worker/test/fanout.control.mjs` (NEW, the negative-control driver — deliberately NOT a `.test.mjs`)**, `agent-worker/test/harness.test.mjs` (FL-3's suite: one VACUOUS arm corrected with its measurement at the site, and five judgement fixtures given `collect`'s slot), `agent-worker/test/harness.control.mjs` (H8's patch SITE moved with FL-5's second reader; figures re-measured), `docs/development/INTERFACE-CHANGES.md` (NEW IC-43), `docs/development/CLAIMS.md` (this entry and its DELEGATIONS), `bio-plane/scripts/coverage.mjs` (**REGISTER_FLOOR only**, moved from the figures a green run PRINTED after this item's suite entered a commit). **NOT** `bio-plane/src/**`, `schema.mjs`, `checks/bio-checks.mjs`, `civicos-ui/**`, `pdf-worker/**`, `newgroup/**`: this item adds no op, no table, no check family and no member-facing surface.
interfaces consumed: I3 (the plane's op surface, read-only through the service binding — `op=airunspawn`'s search half and `op=meaningrows`' EXISTING `ids` restriction, both consumed and neither changed), I8 (this member's own).
interfaces owned: **I8 — CHANGED ADDITIVELY, filed as IC-43 PROPOSED with MEASURED consumer impact of ZERO (`grep -a AGENT_WORKER bio-plane/src/` returns nothing). FL-5's verdict is that I8 STAYS PROVISIONAL at 0.1.0 and the bump is CONDUCT's. `I3` is NOT bumped.**
expected: IS-BUILD-PLAN **FL-5**, W7. The spawn contract (four levels, no write scope, **no bias-manifest field BY CONSTRUCTION** — an exact key set written out one by one, so there is no field under any spelling) and the return contract (a REPORT with a citation, **never documents**; the parent re-reads by address). Sub-sessions share no state — the briefs are deep-frozen and share no object with each other or the parent. The parent holds the only write and the only manifest.
accepts-when: as IS-BUILD-PLAN FL-5. `node scripts/battery.mjs fanout` green; a sub-session's return validated against the REPORT shape; the spawn payload has no manifest field to read. **Battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT** and reported either way. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED; `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED; `node tools/plancheck.mjs` clean but for UNPUSHED. **The declared negative control is IS-9(a)'s and it is the point of the item:** neuter the return-contract check and a document-returning sub-session must FAIL an assertion. Nine arms, each armed ALONE, restores verified by sha256 AND `cmp` against uniquely-named per-arm pristine copies with a byte-count floor. FL-3's ten arms RE-RUN because this item changed their subject. No push, no merge, NO DEPLOY.
concurrency: checked over the whole file. The only claims naming `agent-worker/**` are FL-2/VF-3's and FL-3's, both MERGED TO `main` (their code is at HEAD `ad87db7`, verified by reading it). No live claim names `bio-plane/scripts/coverage.mjs`. `bio-plane/src/**` and `checks/bio-checks.mjs` are NOT claimed and NOT edited by this item.
released:

### DELEGATION 2026-08-09 FLEET (FL-5) -> WHOEVER TOUCHES A FENCE THIS MEMBER ASSERTS ABOUT ITSELF: **FL-3's §14 ARM WAS VACUOUS, AND IT WAS MEASURED RATHER THAN REVIEWED**

Not raised as debt because it is FIXED here, but the CLASS is worth carrying, and it is this
project's most-repeated finding arriving inside a fence rather than inside a document.

- **What it was.** FL-3's driver computed `manifest_field_present` for each spawn payload into a
  local (`spawned`) that **never reached the wire**, and `harness.test.mjs` asserted the fence by
  filtering trace notes for the literal `manifest_field_present` — a phrase the notes never carry.
  The arm could only ever fail if somebody wrote those nineteen characters into a note.
- **The measurement, taken before anything was changed.** The plane mock's SEARCH-half payload was
  patched to carry a full bias block; `harness.test.mjs` came back **194 pass / 0 fail** with that
  arm PASSING. Restored, verified by sha256 and `cmp`.
- **The general rule this pays for again:** a member asserting a fence ABOUT ITSELF must publish
  the thing the fence is about, or the assertion is over a value nobody can see. FL-5 publishes the
  spawn contracts on `/run` and asserts on the manifest's own `statements_sha` bytes, and F4b of
  `fanout.control.mjs` exists solely to prove that arm CAN fail.

### DELEGATION 2026-08-09 FLEET (FL-5) -> FL-6 (the cascade) AND SK-2 (the skill): **WHAT THE CONTRACTS HAND YOU, AND THE ONE HALF THAT IS STILL SUPPLIED**

- **FL-6.** The returns are still CALLER-SUPPLIED — `collect` is a judged row and the answer says
  `turns_run: 0` / `judgement_source: "supplied"`, unchanged from FL-3. What you replace is where a
  return comes FROM, never the contract it is held to: `checkReport` runs on whatever your cascade
  hands back, and a sub-session your cascade spawns has to satisfy the same exact key set. The brief
  it needs is already composed — `spawnContract` — and it carries the return contract with it.
- **SK-2.** Do NOT restate the return contract in the skill. It is code (`subsession.mjs`), it
  travels to the sub-session in `contract.returns`, and a second statement of it in prose is the
  §14b.4 defect this item is on the other side of. If the skill needs to explain it, cite it.
- **BOTH: a refused return is UNDETERMINED and never an absence.** If you need a level's state after
  a broken return, the honest answer is "we do not know" — `reports_refused` names it. Nothing
  downstream may read a refused level as `LOOKED_ABSENT`, and `emptyLevelCandidates` cannot see one
  because the parent never accepted it.

### DELEGATION 2026-08-09 FLEET (FL-5) -> PL-1 / WHOEVER LANDS D-164: **THE CITATION HAS ONE FIELD ON PURPOSE, AND IT IS WHERE THE EXTENT GOES**

`CITATION_KEYS` is exactly `{ address }`, and a citation carrying an `extent` is REFUSED today
(driven, `fanout.test.mjs` A3). That is §14b.2's declared precondition kept honest: legs are
DOCUMENT-GRAIN, the passage lives in the description, and a field for a grain the record cannot
express would read as coverage while carrying none. **When D-164 lands, the extent belongs on the
LEG first and arrives here as a second citation field with an arm behind it** — not as a hole
somebody fills in because the shape looked incomplete.

### DELEGATION 2026-08-09 FLEET (FL-5) -> WHOEVER OWNS `scripts/coverage.mjs` (VF-3's instrument): **THE CONTROL REGISTER CANNOT SEE A FLEET SUITE, AND THE FLEET LINE COUNTS MEMBERS RATHER THAN SUITES**

Measured while landing FL-5's own suite, not suspected, and NOT fixed here because widening a
shared instrument from inside a feature item is the mistake REC-71 exists to correct.

- **The register's corpus is the PLANE's `test/` only.** `const battery = suites.map(... join(ROOT,
  "test", f) ...)` — so `fanout.test.mjs`'s declaration and its **ten stated arms are invisible to
  `arms` / `classified` / `corpus`**. Measured: those figures read **621/621 · 133/133 · 134/134**
  identically before and after this item's suite entered a commit. **No floor was owed and that is
  the reason** — not slack, and worth saying, because "the ratchet did not move" would otherwise
  read as an item that added no control.
- **The FLEET line is PER MEMBER:** `fleet.filter((m) => !m.control)`. `agent-worker` now has THREE
  suites and the line still reads `2/2 declaring a negative control`. **A fleet member can gain a
  suite with no declared control and no instrument anywhere says so** — D-233's own shape (a thing
  the instrument cannot see, scored as if it were fine) one directory out, and the same class as
  the dark `pdf-worker` suite VF-3 closed.
- **Recommendation:** count fleet suites into the register's corpus rather than adding a second
  tally — one instrument, one number — and report `MISSING` per SUITE while keeping the member-level
  gate. **Reversal cost:** none; the figures only get more honest. **Cost of not doing it:** every
  fleet suite added from here is an undeclared control nobody is measuring, and the fleet is the
  half of this system that is growing.
## CLAIM 2026-08-09 RECORD (D-255 — the dead `atom.phrase` field, and the class it belongs to)
session: d255-dead-field (worktree agent-a7bc405e6b9c76b58)
opened: 2026-08-09T00:00:00Z
paths: **`bio-plane/src/query.mjs`** — `textAtom` ONLY: the `phrase` property removed from the atom literal, plus the comment that says why the name is gone and what actually performs phrase matching, and the two `tokenize` comments that name `phrase` detection as the reason `quoted` exists (they would otherwise point at a field that no longer exists). **NOTHING ELSE in the file is edited** — `FIELDS`, `MEANING`, `viewerPredicate`, `tokenize`'s code, `selector`, `meaningAtom`, `ftsAtom`, `ftsExpr`, `rankExpr`, `setSql`, `chain` and `compile` are READ and DRIVEN, never modified. **`bio-plane/test/query.test.mjs`** — ONE new block (the compiled-expression pin for a quoted multi-word value, and the shape pin that says which fields a text atom carries) plus this item's arms declared in the suite's existing `NEGATIVE CONTROL:` block. **`bio-plane/test/fieldread.control.mjs` (NEW)** — the read detector and its arms; deliberately NOT a `.test.mjs` because it EDITS `src/query.mjs` while it runs (`query.control.mjs` and `suggest.control.mjs` are the precedent) and `scripts/battery.mjs` must not discover it. **`bio-plane/scripts/coverage.mjs`** — the `REGISTER_FLOOR` figures ONLY, moved from what a green run PRINTED, ONE key set. `docs/development/DEBT.md` — the **D-255 disposition cell** and any row this item's sweep raises. `docs/development/MEASUREMENTS.md` (APPEND — the field-read sweep). `docs/development/CLAIMS.md` (this entry).
**NOT OPENED:** `bio-plane/src/store.mjs`, `src/index.mjs`, `src/schema.mjs`, `checks/**`, every other test suite, `civicos-ui/**`, `newgroup/**`, `pdf-worker/**`, `release/**`, `bio-plane/dist/**`. **`dist/bio-plane.bundled.mjs` and `release/bio-plane.bundled.mjs` carry the same line and are NOT edited** — they are build outputs and DIST's; the field disappears from them at the next build, and that is named in the report rather than fixed here.
interfaces consumed: none. interfaces owned: none. **NO INTERFACE CHANGE, and it is measured rather than asserted:** `compile()`'s returned plan is unchanged field-for-field; `phrase` lives on an AST node that no op, no envelope and no surface reads, and the only reader of `plan.ast` anywhere is `query.test.mjs` (`p.ast.op`, `p.ast.kids.length`). No op added, no check minted, no refusal code named, no schema touched.
expected: D-255 — remove a field that is COMPUTED AND READ BY NOBODY, and sweep for the class rather than the instance. The instance is REC-68's; the class is "a field a module computes onto an object it constructs and no execution ever reads", and this item's answer to it is an instrument rather than a list of spellings: every object the query language constructs is wrapped in a recording Proxy, the whole node-side corpus is driven through it, and the never-read set is what the sweep REPORTS.
concurrency: checked over the whole file. The only claim naming `bio-plane/src/query.mjs` as an EDIT is REC-68's (`rec68-quoted-value`, line ~4400), whose `released:` line is blank but whose work is **MERGED into this worktree's base** — verified by reading the fix at `query.mjs:651` (`if (src[i] === '"') {`) and the D-228 disposition in DEBT.md, not by trusting the claim. It named `textAtom` as READ-ONLY, so even were it live this item's span is disjoint from its span. No other un-`released:` claim names `bio-plane/test/query.test.mjs` or `bio-plane/scripts/coverage.mjs`.
released: 2026-08-09 — committed on branch `worktree-agent-a7bc405e6b9c76b58` at base `ad87db7`, NOT pushed and NOT merged; CONDUCT integrates. **D-255 CLOSED: `atom.phrase` is DELETED, and the choice REC-68 left open was decided as DELETE rather than give-it-a-reader** — FTS5 already treats a multi-word string literal as a phrase, so `ftsAtom` performs the distinction the field only named; a flag could not have changed that without changing what `ftsAtom` emits, and nothing asked it to. The reasoning is at the site, not only in the report. **REC-68'S FIGURE WAS RE-MEASURED BEFORE IT WAS ACTED ON AND HELD EXACTLY — one write site, zero read sites** (the only other occurrences in the repository are `bio-plane/dist/` and `release/`, which are BUILD OUTPUTS, are DIST's, and were not touched: the field leaves them at the next build, and that is named rather than fixed here). **BATTERY BASELINE MEASURED IN THIS WORKTREE BEFORE ANY EDIT: 138 suites (134 plane · 4 fleet) · 138/138 green · 8,827 assertions · exit 0**, `npm ci` run first. FINAL **138/138 · 8,836 · exit 0**, delta **+9 ATTRIBUTED PER SUITE against a re-measured true baseline for each**: `query.test.mjs` 117 -> 125 and `planning-hygiene.test.mjs` 292 -> 293 (the D-258 row); no other suite's file was touched. `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` UNPIPED, **exit 0** — OPS 162/162 · CHECKS 219/219 · **REGISTER_FLOOR.arms MOVED 621 -> 627 in the same turn from the figure the run PRINTED**, ONE key set, classified 133 and corpus 134 unchanged (no suite added). `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED, **exit 0**; `check-refusal-codes` floors did not move and no refusal code is named by this item. **THE ITEM'S REAL PRODUCT IS AN INSTRUMENT RATHER THAN A DELETION: `bio-plane/test/fieldread.control.mjs`** wraps every object the query language constructs in a recording Proxy and reports the fields nothing ever asks for — the class inverted instead of listed, because a spelling-based matcher cannot see `a["ph" + "rase"]`, a destructure, a spread into an envelope or a `JSON.stringify`, and a classifier that misses one REPORTS A LIVE FIELD DEAD. **SEVEN ARMS, each armed ALONE, declared before running, restores verified by sha256 AND `cmp` against unique per-arm snapshots, with a BASELINE arm.** `atom.phrase`: written 6,517, read 0 over 41,580 compilations plus three real suites. **ONE CAME BACK NOT AS DECLARED AND IS RECORDED RATHER THAN SMOOTHED** — ARM D was declared GREEN and went RED, because this item's own pins are STRUCTURAL and fire on the field's PRESENCE; the declaration was corrected into something stronger (the exact SET of suites permitted to fail, since RED alone would also be satisfied by a field read in forty places) and RE-RUN, AS DECLARED. **THE SWEEP FOUND TWO MORE INSTANCES AND ONE NEAR-MISS THAT MATTERS MORE:** `plan.meaning.columns`/`.refs` are the same class and are raised as **D-258**, deliberately NOT deleted because "give them a reader" is a live alternative belonging to `op=meaningrows`; and FIVE of the seven `plan.meaning.*` fields the node sweep called never-read are LIVE, read by `store.mjs` inside workerd — ARM O2 arms one of them and takes the battery RED, which is the arm that stops the instrument's blind spot from becoming a deletion. **What this sweep does NOT reach, stated plainly:** anything outside `query.mjs` (`store.mjs`, `index.mjs`, the exported static registries), and a name-based scan cannot substitute there — `key`, `row`, `type` and `col` collide with unrelated reads across 297 files, so that scan cannot find a dead field in principle. **CONCURRENCY OBSERVED DIRECTLY, not inferred:** four to six `npm run test:battery` processes from other worktrees of this clone ran against this machine throughout, one writing into the SHARED scratchpad root.
## CLAIM 2026-08-08 RECORD (REC-69)
session: worker-rec69 (worktree agent-a5723f4c87dfd5bd0)
opened: 2026-08-08T00:00:00Z
paths: bio-plane/src/store.mjs (the ONE new method `aiRunsInContext` and its dispatch
  entry — nothing else in the file), bio-plane/src/index.mjs (the `OPS` row for
  `op=airuns` and its addition to the fail-closed list that already names
  `airun`/`airunlog`/`airunspawn`), bio-plane/checks/bio-checks.mjs (the new
  `AI_RUNS_CONTEXT_CHECKS` family, C-34 — measured free before allocating),
  bio-plane/test/airuns.test.mjs (new), bio-plane/test/bounds.test.mjs (the roster pin
  moves 25 -> 26 and the new op joins DRIVEN — it MUST, or the roster pin fails),
  civicos-ui/check-refusal-codes.mjs (the FLOOR constants only, moved from PRINTED
  figures because this item invalidates them by adding a family and a region)
interfaces consumed: I3 (the op contracts)
interfaces owned: none; files IC-27 for the new op
expected: REC-69 — `op=airuns&contextType=&contextId=`, the read that answers WHICH RUNS
  ARE IN THIS CONTEXT. Gated on `context_id` through the SAME `#bundleGate` every other
  `ai_runs` read compiles (D-15's one compilation point — no second predicate), bounded
  with its bound published after clamping, per-row `session` shape identical to
  `op=airun`'s.
NOT CLAIMED, delegated instead: `civicos-ui/**` (app.html's surface — UI-49 built the
  seam and exactly one function changes there; see the DELEGATION below).
released:

### DELEGATION 2026-08-08 RECORD (REC-69) -> UI (UI-49's own follow-on): **THE OP EXISTS. `op=airuns&contextType=&contextId=` ANSWERS "WHICH RUNS ARE IN THIS CONTEXT", AND EXACTLY ONE FUNCTION ON THE SURFACE CHANGES**

UI-49 built the seam and said what it needed. This is it, landed and driven.

- **The call:** `op=airuns&contextType=<inquiry|project>&contextId=<bundle id>[&limit=N]`. The envelope is `{ ok, context:{type,id}, runs:[…], count, limit, truncated }`.
- **Every row is `op=airun`'s OWN `session` block** — not a similar shape, the SAME one: the list calls `aiRunRead` per run, and `airuns.test.mjs` asserts the two answers BYTE-IDENTICAL for the same run. The field-name-blind renderers do not move.
- **The bound is 200 by default and 1000 at the ceiling**, published AFTER clamping, with `truncated` beside it — and both are published on the empty answer too, so the seam never has to guess which bound it was answered at. **`truncated` is a fact about what THIS VIEWER may see**: the gate is applied before the bound, deliberately.
- **Whether a member may see a run is the record's answer, not the page's.** A run over a project they were not invited to is absent byte-identically to one that does not exist, and no count of the withheld travels. So the surface needs NO branch for "you may not see this".
- **What UI-49's device-local roster still has that this does not:** nothing. This is strictly wider — it reaches runs the device never opened, which is the half §14a's promise was about. Whether the two sources are merged or the device one retired is UI's call and is NOT decided here.
- **One thing to code against rather than discover:** an unrecognised `contextType` is a REFUSAL (`AI_RUNS_UNKNOWN_CONTEXT_TYPE`, C-34.2) and not an empty list, because an empty list would tell a member nothing is running on the strength of a typo. It carries a canned translation; render it, do not compose one.
- **Reversal cost: none for UI.** The op is additive and nothing existing moved.

### DELEGATION 2026-08-08 RECORD (REC-69) -> PL-5 / WHOEVER OWNS THE RUN OBJECT: **`aiRunOpen` DOES NOT FENCE `contextType`, SO A RUN CAN BE OPENED IN A CONTEXT KIND NO READ WILL ASK ABOUT**

MEASURED, not inferred: `aiRunOpen` stores `String(contextType)` verbatim and `ai_runs.context_type` is a bare `TEXT` column. **§14a names exactly two kinds — an inquiry or a project — and until REC-69 the plane held those two words NOWHERE.** They now exist as `RUN_CONTEXTS` in `src/airun.mjs`, a text vocabulary beside `RUN_BOUNDS` and `RUN_ENDINGS`, and `op=airuns` refuses a kind outside it (C-34.2).

- **The asymmetry that remains:** the READ is fenced and the WRITE is not. A run opened as `contextType: "meeting"` is in the record and no member can ever ask for it.
- **NOT closed here, and the reason is scope rather than difficulty:** the open is PL-5's site, its refusals are C-22's family (facts about the run object, refused where it is written), and widening a WRITE's refusal set from inside a READ's item is how one item's blast radius becomes another item's red suite.
- **What is owed:** `aiRunOpen` refuses a `contextType` outside `RUN_CONTEXTS`, as a C-22 row with its own canned translation, driven in `airun.test.mjs`. **Read `op=airuns`' case-blind match before deciding**: it matches `lower(r.context_type)` precisely because the write is unfenced today, and a fenced write would let that come back out — but only after the fence lands, never before, or a capitalised run already in a record becomes unreachable.
- **Reversal cost: low now, rising.** Today no instance has a run with a rogue kind. Once one does, fencing the write is a migration question rather than a guard.

### DELEGATION 2026-08-08 RECORD (REC-69) -> WHOEVER TAKES THE CLASS: **ELEVEN ACCESS PATHS THE SCHEMA BUILT FOR QUESTIONS NO OP ASKS**

The item was told to sweep for the class — *which other questions a surface plainly needs cannot be asked of the plane at all?* — and the sweep is MECHANICAL rather than a reading, because REC-69's own gap had a fingerprint: `CREATE INDEX ai_runs_context ON ai_runs(context_id)` existed from the day IS-6 landed and **nothing in the plane filtered `ai_runs` by `context_id`.** Somebody built the access path for §14a's question and no op ever asked it.

**MEASURED (`airuns.test.mjs`, printed every run): 79 indexes declared in `schema.mjs`, 68 with a statement filtering their leading column, 11 without.** Ratcheted as a CEILING *and* a FLOOR, so a new one fails the build naming the index and a shrinking list forces somebody to say why.

Three were read by hand rather than trusted to the matcher, and all three are real:
- **`links(source_bundle)`** — `source_bundle` appears only in an INSERT's column list and one projected field. Nothing asks *which links start at this bundle*.
- **`tasks(assignee)`** — appears only as a field set to `null`. Nothing asks *what is assigned to me*, which is a question an inbox surface plainly needs.
- **`inquiry_basis(grade_source)`** — appears only where a row is PROJECTED. Nothing asks *which legs got their grade from a resolution rather than from a member*.

The other eight: `bundles(last_updated)`, `site_asset_refs(primary_sha)`, `links(citation_norm)`, `resolutions(grade)`, `connection_dirty(stamped_at)`, `proposal_dispositions(at)`, `correspondence(artifact_sha)`, `ai_credentials(principal_kind)`.

**WHAT THE SWEEP CANNOT SEE, stated so nobody reads it as a census:** an index reached only through a JOIN's ON clause; one filtered through a dynamically composed fragment; and — the important one — **a missing question nobody built an index for.** It is a FLOOR on the class. **Eleven ops on one battery is how a diff stops being reviewable**, so none of them is built here.

### FINDING 2026-08-08 RECORD (REC-69) -> REC-70's RATCHET, and it is a HAZARD rather than a defect

While building this op the `session` block was first EXTRACTED into a shared private composer, which is the obvious way to make two reads publish one shape. **`meaning-bounds.test.mjs`'s FLOOR caught it within one run**, and the reason is worth carrying: `op=airun` sits on that ratchet's BARE roster because `aiRunRead` publishes `budget` off an unbounded `ai_run_bounds` scan, and moving that scan into a PRIVATE method took the op off the roster — **not because the read got better, but because the walk cannot follow a private helper.** The bare roster would have shrunk 39 → 38 and nothing in the plane would have improved.

- **The extraction was REVERTED** and the list calls the public `aiRunRead` per row instead. `op=airun` is classified exactly as it was; the floor did not move.
- **The general hazard, for whoever next refactors a dispatched read:** extracting a scan into a private helper is invisible to that walk, so it silently converts a KNOWN residual into an UNKNOWN one. The floor is the only thing that catches it, which is exactly what REC-67 moved it for.
- **The residual itself is unchanged and is NOT this item's:** `aiRunRead`'s `budget` scan is still unbounded, and `aiRunSpawnPayload` already shows what bounding it looks like (`Object.keys(RUN_BOUNDS).length`, `cap + 1`, `limit`/`truncated` published). **Bounding it changes `op=airun`'s published shape**, which UI-49's suite pins, so it is a two-area change rather than a one-line one.

## CLAIM 2026-08-09 RECORD (REC-69 · THE REPLAY ONTO `main`)
session: worker-rec69-replay (worktree agent-a7e307e5502e319c0)
opened: 2026-08-09T00:00:00Z
paths: **the REPLAY of the reverted merge** (`git revert` of `80473ea`, which restores `e241672`'s hand-resolution work), plus FOUR files opened to answer the two cross-item ratchets and a third nobody listed: `bio-plane/test/run-conditions.test.mjs` (the `ROLE` table gains ONE entry and a fifth role; ARM W4 and ARM W7's WORDING corrected with dated reasons; ARMs W8 / W8 GUARD / W8b added; a second `NEGATIVE CONTROL:` paragraph — **no runtime block, no fixture and no other arm touched**), `bio-plane/test/airuns.test.mjs` (the SWEEP's ceiling/floor re-measured 11 → 13, both arrivals named, three arms added, a second `NEGATIVE CONTROL:` paragraph — **REC-69's own runtime sections A–R untouched**), `bio-plane/test/bounds.test.mjs` (the capped-op roster pin re-measured to 29 and ONE stale WORD corrected), `bio-plane/scripts/op-claims.mjs` (**ONE `PLANNED_OPS` row removed — `airuns`, whose own expiry rule fired**), `bio-plane/scripts/coverage.mjs` (`REGISTER_FLOOR`, all three keys, ONE key set), `bio-plane/test/nc-rec69-selects.mjs` (new — the replay's control driver, inside this worktree). **NOT** `bio-plane/src/**` beyond what the revert-of-the-revert restores byte-for-byte, **NOT** `docs/development/QUEUE.md`, **NOT** `civicos-ui/**`, **NOT** `newgroup/**`.
interfaces consumed: I3 (IC-42, already filed by REC-69 and already renumbered by CONDUCT; **IC-42 measured FREE on `main`, whose maximum is IC-41**)
interfaces owned: none
expected: REC-69 merged and backed out on 2026-08-08 having failed two ratchets that only fire on the pair with REC-74. The judgement CONDUCT left open — what ROLE `aiRuns` carries — is answered below.

### DECISION 2026-08-09 RECORD (REC-69) — a FIFTH ROLE, `SELECTS`, minted rather than a bad fit forced

**What is running:** `aiRunsInContext` is classified `SELECTS` in `run-conditions.test.mjs`'s ROLE table, and is therefore NOT owed a row in ARM P1's twenty-column disposition matrix. The reasoning is at the site, at length, because a wrong answer here installs a false assertion about what the record publishes.

- **Why not PUBLISHES.** Run facts plainly DO reach a member through `op=airuns` — but every one of them is composed by `aiRunRead`, called per row, and asserted BYTE-IDENTICAL to `op=airun`'s own `session` block. Classifying it PUBLISHES would oblige twenty disposition cells that are each a COPY of `aiRunRead`'s. A copy agrees with its original for free, and worse, a SECOND declaration can DRIFT from the reader it describes while the code cannot. The matrix would then read as two independent judgements agreeing where there is only one.
- **Why not AUTHORISES**, the closest of the four: its second clause fits and its FIRST is its definition — a reader deciding whether a DIFFERENT ACT may proceed. No act is authorised here. Filing it there would weaken AUTHORISES to mean "reads and does not itself publish", which is a much weaker claim than it currently makes about `suggestVersion` and `captureRequest`.
- **The role is EARNED, not granted.** ARM W8 fails if a SELECTS reader projects any stored column beyond the key, or calls no PUBLISHES reader; ARM W8 GUARD fails over an empty SELECTS corpus; ARM W8b proves the reader can see both violations. All three are driven by `test/nc-rec69-selects.mjs`.
- **The alternative, and what reversing costs: LOW.** Reversing means deleting one ROLE entry, three arms and one paragraph, and writing twenty matrix cells that duplicate `aiRunRead`'s. No source, no schema, no refusal and no published shape moves either way — the whole decision lives in one test's classification.
- **Stated and NOT decided:** whether a SELECTS reader that publishes a fact COMPUTED FROM the rows it selected (a count of running jobs, a newest timestamp) is still SELECTS. ARM W8 reads the SQL projection, not arithmetic over the page, and would not catch it. Named at the site.

### DELEGATION 2026-08-09 RECORD (REC-69) -> whoever takes the unread-index roster

`provenance_route_marks(finding)` is a REAL instance of REC-69's class and is the arrival that took the roster 11 → 12. `finding` appears in NO `WHERE` anywhere in the plane: every reader takes the LATEST mark per bundle by `seq` and classifies in JS, and `op=list`'s route tally is computed over the gated PAGE. **The question no op asks: "which documents in this instance carry a standing `LOOKED_INDETERMINATE` marker."** A group asking where its own record's provenance is doubted must page the whole store and count for itself; the index for it was declared the day the table landed. Not fixed here — one op per unread index on one battery is how a diff stops being reviewable — and it joins `links(source_bundle)`, `tasks(assignee)` and `inquiry_basis(grade_source)` on the list REC-69 already delegated.

### DELEGATION 2026-08-09 RECORD (REC-69) -> M0-14 / D-233's area (`scripts/control-register.mjs`)

**The register records the declaration STATING THE MOST ARMS and never the sum, and this item is the shape that rule did not anticipate: TWO DIFFERENT controls, for two different items, in ONE suite.** Measured, not inferred: `airuns.test.mjs` now carries REC-69's original 7-arm block and this replay's 4-arm block, and reports 7 — the new block contributes nothing; `run-conditions.test.mjs` carries REC-74's 5-arm block and this replay's, and moved 5 → 6 because the NEW one became the larger, so REC-74's five stopped being counted. `arms` is therefore a floor on ARMS STATED IN THE LARGEST SINGLE DECLARATION PER SUITE. It is SAFE — the number is reported and never gated, and the floor still cannot fall without a declaration really shrinking — but a reader doing the arithmetic between two `--strict` runs will conclude a declaration shrank when one was ADDED. The "never the sum" rule is right for the case it was written for (M0-2's backfill left most suites stating ONE control twice) and needs a way to tell one control stated twice from two controls stated once.

### FINDING 2026-08-09 RECORD (REC-69) — A THIRD CROSS-ITEM RATCHET, AND NOBODY LISTED IT

The 2026-08-08 backout named two ratchets that fire only on the pair. There are **three**. `test/op-claims.test.mjs` failed with `["airuns"]` on *"no PLANNED op has been BUILT — a registration that outlived its deferral is a document that became true while nobody re-read it"*. M0-12's `PLANNED_OPS` ledger registered `airuns` by name **with QUEUE.md's own sentence as its reason** — *"NO OP CAN ANSWER THAT QUESTION TODAY"* — and `plannedStale()` is self-cleaning by design, so building the op made the row's reason false and the build failed in one run. **M0-12 landed BETWEEN the two merge attempts**, which is why the 2026-08-08 backout could not have seen it. The row is removed with the reason at the site. **The general point is not the row: a backout that lists the ratchets it failed is listing the ones that existed THAT DAY**, and the queue moves underneath it.

### FINDING 2026-08-09 RECORD (REC-69) — **THE 2026-08-08 MERGE SILENTLY DROPPED A WHOLE FILE, AND EVERY INSTRUMENT REPORTED SUCCESS**

MEASURED, not inferred, with two `git diff --stat`s:

- `git diff 722c37b 2d9c57b --stat` — REC-69's branch changed **twelve** files, including **`civicos-ui/check-refusal-codes.mjs`, by 70 lines**, moving every floor that its new C-family invalidated **in the same turn**, exactly as C-22's header requires.
- `git diff 7e5f9b0 e241672 --stat` — the merge commit carried **eleven**. `civicos-ui/check-refusal-codes.mjs` is not among them.

**So the floor moves never landed, and `git revert -m 1` could not remove what was never there.** The replay therefore restored the code with ten stale floors, and **nothing went red** — a dropped floor move does not fail, it goes SLACK. Battery green, `--strict` exit 0, UI harness exit 0, and ten ratchets quietly carrying between 1 and 18 of headroom: families 15/16, rows 163/166, census 424/427, reach 217/220, governedSites 66/67, regions 53/54, regionLines 1407/1425, codesChecked 141/144, refusalsJudged 143/146, vocabularies 9/10, vocabularyTerms 56/58. All moved in one turn from the printed run; each now sits EXACTLY at measured.

**The general point, and it is bigger than this file.** The 2026-08-08 integration was verified by running the battery, `--strict` and the UI harness on the merged tree, and all three passed **over a merge that had lost a file**. Nothing in this repository compares the FILE SET a merge carried against the file set its branches changed. `git merge-base --is-ancestor` proves a merge happened; the queue already records that it does not prove the content survived; **this is the next step down — the content of ELEVEN files survived and a TWELFTH did not, and the difference is invisible to every green light we have.** The cheap check is `git diff <base> <merge> --stat` against `git diff <base> <branch> --stat` per branch, and it is a CONDUCT-loop step rather than a worker one, so it is stated here for `kickoffs/CONDUCT.md` rather than added to a suite.

**And it explains the backout's own shape.** REC-69's `bio-checks.mjs` header says *"The floor in `civicos-ui/check-refusal-codes.mjs` is moved in the same turn, from the figure the guard PRINTED"* — a true sentence about the branch, describing work the merged tree did not contain. A comment that describes a mechanism the tree does not carry is this project's most-repeated defect arriving through the integrator rather than through an author.

## CLAIM 2026-08-09 UI (UI-45 — notifications rendered)
session: ui45-notifications (worktree agent-a4f9c3083de5f28e3)
opened: 2026-08-09T00:00:00Z
paths: **`civicos-ui/app.html` — named by SITE and by RENDERER, never by file** (it is shared ground
  and UI-43 is live on it): (1) the NEW region between `/*__NOTIFICATIONS_START__*/` and
  `/*__NOTIFICATIONS_END__*/`, appended after `__VERSION_REVIEW_END__` — every function in it is new
  (`notifOptionsGrainHtml`, `notifBasisEntryHtml`, `notifDispositionKeyed`, `notifNoDispositionHtml`,
  `notifRememberDisposition`, `notifDisposedHtml`, `notifClassLine`, `notifAbsenceHtml`, and the
  `stance*` family) and nothing outside it moved into it; (2) FOUR named renderers INSIDE
  `__QUEUE_START__`, and ONLY these — `queueEntryControlsHtml` (its FINDING branch gains the
  dispositionable test), `queueItemHtml` (three call lines added, one existing call gated),
  `queuePaint` (two call lines added, `PROPOSALS_LAST`'s filter narrowed); (3) ONE line inside
  `doProposalDispose` recording the plane's disposition answer; (4) ONE key ADDED to `SURFACES`
  inside `__SURFACES_START__`/`__SURFACES_END__` — `"inquiry-stance"`, no existing key touched;
  (5) ONE line inside `boot()` — the route chain gains `&& !stanceRouteFromHash()` and nothing else.
  **NOT** the CSS block, **NOT** `__CATALOG__`, **NOT** `__SEMANTICS__`, **NOT** `__VERSION_REVIEW__`
  (UI-42's constants are CALLED, never edited), **NOT** `__ELICITATION__`, **NOT** any other marked
  region, **NOT** any other function in `__QUEUE_START__`.
  `civicos-ui/test/notifications.test.mjs` (NEW), `civicos-ui/test/notifications.control.mjs` (NEW).
  `civicos-ui/test/surface-registry.test.mjs` — **the `versioncurrent` row STRUCK from
  `ACTS_AWAITING_SURFACE` (ARM A4c's DRAIN requires it in the same commit), and FIVE floors moved
  from the figures the arms PRINTED: ARM A3 19→20, A4d 16→17, A4e 16→17, D1 64→65, D5 39→41.**
  No other arm touched.
  `civicos-ui/test/preauth-vocabulary.test.mjs` — WALK 2's router census six → SEVEN and the new
  router CLASSIFIED as post-authentication, with both halves of the classification pinned. That arm
  exists to stop a new router arriving unclassified and it fired on the first run of this item; it
  is CORRECTED, never exempted. No other arm touched.
  `docs/development/CLAIMS.md` (this entry), `docs/development/kickoffs/UI.md` (APPENDED, not
  rewritten — UI-43 is live), `docs/development/MEASUREMENTS.md` (appended).
  **NOT** `bio-plane/src/**` (READ ONLY — the suite IMPORTS `queuestate.mjs` rather than copying it),
  **NOT** `bio-plane/checks/**`, **NOT** `bio-plane/test/**`, **NOT** `bio-plane/scripts/**`,
  **NOT** `civicos-ui/tokens.css`, **NOT** `civicos-ui/check-*.mjs`, **NOT** `.gitignore` (UI-42's
  `.ui*-harness/` pattern already covers this item's pen), **NOT** `docs/development/QUEUE.md`,
  **NOT** `docs/development/IS-BUILD-PLAN.md`, **NOT** `docs/development/DECISIONS.md`,
  **NOT** `docs/development/INTERFACES.md`, **NOT** `docs/development/INTERFACE-CHANGES.md`,
  **NOT** `newgroup/**`, **NOT** `agent-worker/**`.
interfaces consumed: I3 — `op=queue` (read), `op=proposedispose` (act, already reached by this
  surface), `op=basisversions` (read, now WITH `project=`) and `op=versioncurrent` (the one act this
  item newly hosts). Nothing is asked of the plane that it does not already publish and no shape is
  changed, so **no IC row is owed**.
interfaces owned: none.
expected: IS-BUILD-PLAN UI-45, W8 slot B. The FINDING-class slugs surfaced from the plane's own
  published wording; the out-of-inquiry lead with its `options[]`; suggestion kinds LOOK derived,
  aggregate, and age rather than vanish (§6.4). Plus the `current`/project half UI-42 delegated, and
  `versioncurrent`'s owed surface from `ACTS_AWAITING_SURFACE`.
accepts-when: as IS-BUILD-PLAN UI-45. **UI harness baseline MEASURED IN THIS WORKTREE BEFORE ANY
  EDIT, on a quiet tree, after `npm ci` in `bio-plane/`: 42 harnesses, exit 0 read UNPIPED.** Final:
  **43 harnesses, exit 0 read UNPIPED**, the new suite at **67 assertions**. **`main` MOVED UNDER
  THIS ITEM WHILE IT RAN — `d579ae8` -> `ae34ec8`, eight merges — so it was merged in and THE WHOLE
  GATE WAS RE-RUN on the merged tree at `ecf8743`: UI harness 43, exit 0 unpiped; battery
  **142/142 at 9,179** (this item's own delta is ZERO and structurally so, it modifies no file under
  `bio-plane/`); `coverage.mjs --strict` run DIRECTLY, `$?` unpiped, **exit 0** (OPS 163/163, CHECKS
  222/222, controls 137/137 at 654 arms); the negative control re-run **12/12 as declared**. Both
  sets of figures are in `MEASUREMENTS.md`.** NEGATIVE CONTROLS RUN — `node
  civicos-ui/test/notifications.control.mjs`, **12 arms, 12 as declared**, each armed ALONE with
  every other defence held OPEN, two OVER-STRICTNESS arms GREEN, a BASELINE row GREEN, every restore
  verified by sha256 AND by `cmp` against a per-arm pristine copy AND a pristine-of-record, the pen
  kept INSIDE this worktree. No push, no merge.
concurrency: checked over the whole file. The only other live claim naming `civicos-ui/**` is
  **UI-42's** (`agent-a8c8ed9c32eb56980`), whose work is MERGED on main at `d579ae8` and whose own
  DELEGATION addresses this item; its `__VERSION_REVIEW__` region is READ and CALLED here, never
  edited. **UI-43 (the accept ceremony) runs concurrently and is NOT opened**: the four transition
  acts (`versionaccept`/`reject`/`consider`/`revert`) stay on `ACTS_AWAITING_SURFACE` with UI-43's
  name on them, untouched, and this item's `SURFACES` key and marked region are disjoint from
  anything a ceremony would add. `bio-plane/**` is NOT claimed and NOT edited.
released:

### DELEGATION 2026-08-09 UI (UI-45) -> WHOEVER LANDS PL-13 (IS-3, CURRENT + the shared-inquiry slugs): **YOUR TWO SLUGS ARE ASSERTED ABSENT, SO THE DAY YOU MINT THEM THIS SUITE GOES RED ON PURPOSE**

`IS-BUILD-PLAN.md`'s UI-45 row names `stance-changed-here-not-elsewhere` and
`new-version-arrived-from-another-team` among the slugs to surface. **Neither exists.** They are
PL-13's to mint, PL-13 has not landed, and `queuestate.mjs` names neither — which `store.mjs` says
itself at `suggestVersion`'s header (*"the slug vocabulary, its subscriber and the shared-inquiry
semantics are PL-13's and PL-4's, so this item writes the object and NOT the notification"*).

**So this item did not render them, and that is the item's own negative control rather than a gap.**
UI-45's declared NC is *render a slug with surface-authored wording → the published-words assertion
fails*; authoring member-facing words for a slug the plane does not publish is exactly that defect,
performed deliberately. `notifications.test.mjs` §0 therefore asserts both slugs are ABSENT from the
IMPORTED catalogue, with the reason in the assertion text: **when PL-13 lands, that arm FAILS**, and
the next session surfaces them instead of discovering the gap a wave later. An absence asserted is a
gap with an alarm on it; an absence noted in a comment is a gap.

**What you will find already built when you get here.** Nothing about the two new slugs needs a new
renderer: the queue renders a FINDING from the producer's own `summary`, `detail` and `basis.detail`,
so a new kind under an existing class arrives rendered. What you must check is the DISPOSITION half —
`notifDispositionKeyed` asks whether the item's basis carries `progression_key` and `stage_key`,
because that is what `op=proposedispose` is keyed on. **If your slugs are to be adoptable, deferrable
or dismissable, the plane needs an identity the act can be keyed on and today it has exactly one.**
That is a PLANE question, not a surface one, and it is the third instance of D-222's grain problem.

### DELEGATION 2026-08-09 UI (UI-45) -> UI-43 (the accept ceremony) AND WHOEVER NEXT DRAWS A CONTROL ON A QUEUE ITEM: **THE DEFECT THIS ITEM FOUND IS A CLASS, AND IT WAS LIVE**

PL-15's delegation states the rule for one control — *"DO NOT OFFER A MUTE ON IT … a surface that
shows a mute control it cannot honour is worse than one that shows none"* — and the queue honoured
that half correctly: `queueMutableKinds` filters to CONDITION, which is what `op=queuemute` accepts.

**The same defect was live one control over and nobody had looked.** `queueEntryControlsHtml` drew
Adopt / Defer / Dismiss on EVERY FINDING. `op=proposedispose` is keyed on
(`progression_key`, `stage_key`) and refuses a pair that is not a real stage of a defined
progression (`NO_SUCH_PROGRESSION` / `BAD_STAGE`), so on PL-15's out-of-inquiry lead — whose basis
carries neither — **all three controls could only ever have been refused**, and the act dialog they
fed was built from a `PROPOSALS_LAST` row carrying two nulls.

**THE FIX IS A PROPERTY AND THAT IS THE POINT.** `notifDispositionKeyed(it)` asks the ITEM whether it
carries the identity the act is keyed on. A list of kinds would have gone stale the day PL-13 mints
its two — *invert, do not lengthen a list*. **The general rule for anyone adding a control to a queue
item: ask what identity the act is keyed on and whether THIS item carries it. Do not ask what kind it
is.** The queue's own item shape makes that answerable, and `notifications.control.mjs` arm 5 is the
measurement that it bites: making the predicate answer true for everything restores the old surface
exactly and takes five assertions down.

### DELEGATION 2026-08-09 UI (UI-45) -> CONDUCT: **FIVE FLOORS MOVED, AND THIS TIME EVERY ONE OF THEM WAS EXACT — NO SLACK ANYWHERE**

Moved from the figures the arms PRINTED (the arms were driven to fail on an absurd floor so they
would print, then the file was restored and verified by `cmp` before the real figures were written),
never by adding to the number in the file, and each with its reason at the site:

| arm | was | now | why it moved |
| --- | --- | --- | --- |
| A3 (act placements) | 19 | 20 | this item hosts `versioncurrent` |
| A4d (catalogue outside the register) | 16 | 17 | STRIKING the register row moves that act into this floor's corpus |
| A4e (distinct hosted acts) | 16 | 17 | a surface now hosts it |
| D1 (ops called statically from app.html) | 64 | 65 | this item added exactly ONE call, `op=versioncurrent` |
| D5 (declared reads) | 39 | 41 | the `inquiry-stance` surface declares exactly TWO |

**The reportable half is the opposite of UI-42's.** UI-42 found D1 and D5 carrying 13 and 7 of slack.
**This item found NO slack in any of the five** — every figure sat exactly where UI-42 left it a day
earlier, and every move is exactly the delta this item created. That is what a ratchet looks like
when it has just been re-measured, and it is worth recording precisely because the alternative
finding (more slack) would have been the interesting one and it is not what the instrument said.

### DELEGATION 2026-08-09 UI (UI-45) -> CONDUCT: **THE DRAIN HAS NOW FIRED TWICE IN TWO DAYS, AND THE REMAINING VERSION ACTS ARE ALL UI-43's**

`versionhide` was struck by UI-42 and `versioncurrent` by this item, each in the commit that hosted
it. Both strikes were triggered by ARM A4c firing on the item's FIRST run, before anything else had
been touched, naming the row and the item that owed it. **The mapping CONDUCT owns is carrying its
own weight.** What remains on `ACTS_AWAITING_SURFACE` for the version machine is the four transition
acts (`versionaccept`, `versionreject`, `versionconsider`, `versionrevert`), all four owed by
**UI-43**, which is live now — so the register should empty of version acts entirely within this
wave, and if it does not, ARM A4b's ceiling is the thing that will say so.

### DECISIONS FOR BOB 2026-08-09 (UI-45): **NONE**

Measured against `kickoffs/README.md`'s three tests. Two judgements were made and both are ones the
record had already assigned to the surface. **(a) Not rendering PL-13's two slugs** is not a scope
choice, it is what the item's own negative control requires — authoring words for an unpublished
slug is the drift class DEC-8 closes, and the absence is asserted so it cannot go unnoticed.
**(b) Withdrawing three controls from findings the disposition act cannot key** applies PL-15's own
stated rule about the mute to the control beside it; the alternative (leave them, let the plane
refuse) is the "gate that pressures somebody into finding a way around it" the record already rules
against. Nothing here is a consequence Bob has not already ruled on.

---

## CLAIM 2026-08-09 M0-18
session: worktree-agent-a62aec7acd493144e
opened: 2026-08-09T00:00:00Z
paths: bio-plane/test/bounds.test.mjs, bio-plane/test/case-opened.test.mjs, bio-plane/test/identity-claims.test.mjs, bio-plane/test/machinefences-dec49.test.mjs, bio-plane/test/planning-hygiene.test.mjs, bio-plane/test/check-firing.test.mjs, bio-plane/test/machine-fences.test.mjs, bio-plane/scripts/op-claims.mjs, bio-plane/test/provenance-floor.control.mjs (new), the CLASS_NAMED_UNGUARDED allowlist and its census matcher in bio-plane/test/hygiene.test.mjs
interfaces consumed: none
interfaces owned: none
expected: D-257's delegation, one estate over. The class is A SUITE THAT READS ITS CORPUS OFF THE WORKING TREE AND THEN FLOORS ON WHAT IT FOUND — an untracked arrival can only push such a floor the wrong way, and `refs/stash` being repository-wide across all sixty worktrees is the measured delivery mechanism (M0-15, D-238). The fix is D-257's exactly: the SWEEP keeps reading the whole working tree, because a finding in an uncommitted file is still a finding, while the FLOOR is computed over `git ls-tree HEAD` alone via `bio-plane/scripts/provenance.mjs`. Plus two questions of my own: `machine-fences.test.mjs`'s walk feeds a SATISFACTION rather than a floor and needed a different answer, and `scripts/op-claims.mjs` descends into dot-directories where its two sibling whole-repo walks do not.
released:

**NOTE FOR CONDUCT — A PREDICTED CONFLICT, NAMED BEFORE IT LANDS.** This item edits
`CLASS_NAMED_UNGUARDED` in `bio-plane/test/hygiene.test.mjs`, removing the entries for the six
bio-plane walks it guards. **D-257's own branch edits the same array**, removing seven civicos-ui
entries. The two removals are disjoint by path and the resolution is KEEP BOTH REMOVALS — but a
keep-both merge of a JS array literal is the `REGISTER_FLOOR` duplicate-key shape that has bitten
this repository six times, so re-read the printed census line after merging rather than trusting the
diff. The census assertion (`newlyUnguarded`) does NOT catch a stale entry left behind; only
`goneFromList` does, and it is silent for a file that became guarded.

### M0-18 RESULT 2026-08-09 — seven floors guarded, one different exposure closed, NO FLOOR MOVED

**The sweep found more than the brief.** Seven walk-derived floors in `bio-plane/`, not five:
`test/check-firing.test.mjs` (`estate.length >= 50`) and the `scripts/op-claims.mjs` /
`test/op-claims.test.mjs` pair (four floors) were not in the brief. The op-claims pair is the
one worth reading twice — **the WALK and the FLOOR live in different files**, so
`hygiene.test.mjs`'s class census, which grades a file by whether IT contains a
`readdirSync(`, had graded the walk as harmless and never enumerated the suite that floors on
it. Filed as **D-265**, because the two instances are now guarded and the detector's blindness
is not.

**The brief was right about the sixth**, and it needed a different answer:
`test/machine-fences.test.mjs` floors on nothing its walk produced (`methods` and `rows` come
from a `readFileSync` of one named path) and instead lets a walked file SATISFY the `unpinned`
ratchet. `pinned()` now asks `git ls-tree HEAD` — the OPPOSITE narrowing from the six, because
there it would hide a finding and here it is what makes one visible. The cost (a red for a
worker whose pinning suite is uncommitted), the reason it departs from `provenance.mjs`'s
report-only provisional, and the one-line reversal are all at the site.

**NO FLOOR MOVED, and it is a measurement.** Six of seven reproducible figures equalled their
contaminated one on a clean tree. **The seventh did not:** `check-firing`'s estate walk counted
129 files of which 127 are in any commit — `agent-worker/.wrangler/cache/cf.json` and
`pdf-worker/.wrangler/cache/cf.json`, wrangler's local cache. The floor of 50 had slack so it
did not move; the printed figure did.

**`scripts/op-claims.mjs` no longer descends into dot-directories**, decided under the standing
delegation and recorded at the site with the measurement (**cost: zero tracked files**), the
rejected alternative (`git check-ignore`), and the one-line reversal.

Gates: battery 139/139 green, **8,869 → 8,880 assertions**, attributed per suite by re-running
the baseline (bounds +1, case-opened +1, check-firing +1, identity-claims +1, machine-fences +2,
machinefences-dec49 +1, op-claims +2, planning-hygiene +2 — and the last of those is +1 for the
floor and +1 because `planning-hygiene` asserts one arm per open DEBT row and this item wrote
D-265). `coverage.mjs --strict` exit 0
unpiped. `civicos-ui/test/run.mjs` green — **on the second run: the first was RED at 224/226
because this item wrote a field name as prose inside a STRING LITERAL and UI-40's consumer walk,
which keeps strings by design, counted the sentence as a consumer.** The walk was right.
`test/provenance-floor.control.mjs`: **58 of 58 as declared**, two arms wrong first, both
finding defects in the harness rather than in the subject.

### DECISIONS FOR BOB 2026-08-09 (M0-18): **NONE**

Both judgements this item made are ones the record already assigns to a worker. The
dot-directory rule is a walk's own corpus definition, decided with a measurement and reversible
in one line. The `machine-fences` narrowing is an instrument's own strictness, and its
alternative is written beside it. Neither is a consequence Bob has not already ruled on.


## CLAIM 2026-08-09 RECORD (PL-19 — DEC-65's shape (b): the check AND the endpoint guard, together)
session: pl19-dec65-shape-b (worktree agent-a875e2afd837947d7)
opened: 2026-08-09T00:00:00Z
paths: **`bio-plane/checks/bio-checks.mjs` — named by SITE, not by file:** (1) `basisVersionFindings`'
  `basis_version_grounds[]` loop — the `asserted_by` arm at C-25.6 and ONE new pre-pass that counts the
  distinct declared parts per version. **`C-2.8` / `checkGrounds` KEEPS EVERY EXECUTABLE LINE IT HAD
  and gains a COMMENT ONLY, stating why it is a MEASURED closure rather than a site the sweep missed**
  — amended into these paths mid-item, deliberately, because a reader meeting two sites that ask the
  identical question with only one of them wired would read the second as a miss. The `git diff` on
  that span contains no executable change. PL-17's mint block
  (`SUFFICIENCY_UNCLAIMED`, `sufficiencyClaimState`, `isSufficiencyClaimed`, `SUFFICIENCY_CLAIM_STATES`)
  is IMPORTED and READ, never edited. CPDF-10's open claim on this file appends `TEXT_CHAIN_CHECKS`
  at the END of the file; my span is ~line 6600 and does not meet it.
  **`bio-plane/src/store.mjs` — named by SITE, not by file:** (2) `suggestVersion`'s machine-asserter
  pre-write guard (the `needsPartition` arm inside `DEC-49 REGION is-suggest-checks`, the site FL-3
  measured as firing FIRST); (3) `#suggestionPersisted`'s ONE `asserted_by` stamp line. **NOT**
  `#standardForRun`/`aiRunRead`/`aiRunSpawnPayload` (the open RECORD claim's spans), **NOT**
  `suggestVersion`'s success return (D-235a's, released), **NOT** `groundInquiry`, **NOT** `promote`.
  **`bio-plane/test/dec65-single-part.test.mjs`** (NEW), **`bio-plane/test/dec65-single-part.control.mjs`**
  (NEW — the negative-control driver, deliberately NOT a `.test.mjs` because it EDITS REAL SOURCES).
  **`bio-plane/test/sufficiency-state.test.mjs`** — §7's superseded pins CORRECTED with the dated reason
  at the site, never exempted. **`bio-plane/test/suggest.test.mjs`** — the SAME, and it was not
  predicted by the brief: PL-17's arm (7) shows which assertions move in ITS suite and cannot see this
  one, where TWO more were superseded (CHECK 6's machine arm and F10's absolute version count).
  **`bio-plane/scripts/coverage.mjs`** — `REGISTER_FLOOR` ONLY, all three keys moved in the same turn
  from the figures a green run PRINTED as REPRODUCIBLE; ONE key set, verified by `grep`.
  **`civicos-ui/check-refusal-codes.mjs`** — the `regionLines` FLOOR figure ONLY, moved from what the
  guard PRINTED on this worktree's green run; no matcher, arm, ceiling or row touched. **AMENDED INTO
  THESE PATHS MID-ITEM AND IT IS A KNOWN CONCURRENCY:** the open RECORD claim (`airun.mjs` /
  `STANDARD_BASIS`) names this same FLOOR block. If it conflicts, KEEP BOTH COMMENTS AND ONE KEY.
  `docs/development/CLAIMS.md` (this entry), `docs/development/DECISIONS.md`
  (DEC-65's enactment note ONLY — appended, no other row touched), `docs/development/MEASUREMENTS.md`.
  **NOT** `bio-plane/src/schema.mjs` (see the release line — the sentence PL-17 flagged stays TRUE),
  **NOT** `bio-plane/src/index.mjs`, **NOT** `civicos-ui/**`, **NOT** `agent-worker/**`, **NOT**
  `pdf-worker/**`, **NOT** `newgroup/**`.
interfaces consumed: I3 (the plane's op surface — `op=suggest`).
interfaces owned: none. No shape a consumer builds against changes: `op=suggest`'s parameters and
  its success object are untouched. What changes is WHICH submissions are accepted (strictly more)
  and what the plane STAMPS into one field of the document it composes.
expected: DEC-65, answered 2026-08-09 by session BOB and enacted in three steps by CONDUCT. Step ONE
  was PL-17 (the third `asserted_by` state, minted and deliberately inert). **This is step TWO: land
  shape (b) — the single-part exemption — in `C-25.6` AND in PL-3's endpoint guard TOGETHER**, per
  DEC-65's own sequencing and FL-3's measurement that the guard refuses on `legsIn.length > 0` and
  therefore fires first, so amending the check alone would land, pass its own suite, and change no
  behaviour. Step THREE is PL-14's re-measurement of the strength pair over grounds.
accepts-when: battery baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief
  (the brief carried no figure): **140 suites (136 plane + 4 fleet) · 140/140 green · 8,907 assertions
  · 135.9 s · exit 0**. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED;
  `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED; `node tools/plancheck.mjs`
  clean but for UNPUSHED. Negative controls RUN, each arm ALONE with the other defences held OPEN, an
  OVER-STRICTNESS arm included (a genuine member claim in a spelling I did not anticipate must still
  be ACCEPTED), a BASELINE arm included, restores verified by sha256 AND `cmp` against uniquely-named
  per-arm pristine copies. Tested THROUGH `op=suggest`, not only at the store. No push, no merge.
concurrency: checked over the whole file at `8096452` (this worktree was ONE MERGE BEHIND `main` on
  arrival — PL-17's merge — and was fast-forwarded before anything was measured). Of the claims open
  today, **two name my files and NEITHER meets my spans**: CPDF-10 appends `TEXT_CHAIN_CHECKS` at the
  END of `checks/bio-checks.mjs` (mine is `basisVersionFindings`, ~6600), and the open RECORD/airun
  claim names `store.mjs`'s `#standardForRun` / `aiRunRead` / `aiRunSpawnPayload` (mine is
  `suggestVersion`'s pre-write guard and `#suggestionPersisted`). D-235a's `store.mjs` claim is
  RELEASED and its span was `suggestVersion`'s SUCCESS RETURN, after the region I edit.
released: 2026-08-09 — committed on `worktree-agent-a875e2afd837947d7`, **NOT pushed and NOT merged**;
  CONDUCT integrates. **DEC-65's SHAPE (b) IS LANDED AND IT IS LANDED AT BOTH SITES.** `C-25.6` admits
  PL-17's explicit no-claim value on a version declaring EXACTLY ONE part and REFUSES it on two or more
  — asked through `isSufficiencyUnclaimed`, never by matching the literal — and PL-3's endpoint guard
  moved in the same turn from `legsIn.length > 0` to *a machine may compose one part and no more*.
  `#suggestionPersisted` STAMPS `SUFFICIENCY_UNCLAIMED` for a machine composer, so `class:ai` never
  stands in a field whose published meaning is *a member said this part is enough on its own*; the
  version's own `author` still names the machine, because WHO COMPOSED and WHO ASSERTED are different
  facts. **Battery 141/141 green · 8,953 assertions · 141.2 s · exit 0** against a true baseline
  MEASURED IN THIS WORKTREE BEFORE ANY EDIT of **140/140 · 8,907 · 135.9 s** (the brief carried no
  figure; the worktree arrived ONE MERGE BEHIND `main` and without `bio-plane/node_modules`, both
  fixed before measuring). **Delta +46 attributed by DIFFING the two full runs PER SUITE, never by
  subtraction: 135 of 139 shared suites byte-identical in count** — `dec65-single-part.test.mjs` +37
  (new), `sufficiency-state.test.mjs` 35 -> 39, `suggest.test.mjs` 93 -> 95, `hygiene.test.mjs`
  558 -> 561. `node scripts/coverage.mjs --strict` run DIRECTLY with `$?` read UNPIPED, **exit 0** —
  OPS 162/162, CHECKS 219/219, provenance `147 of 147 discovered item(s) are in the commit at HEAD`.
  `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED, **0**.
  `node civicos-ui/check-refusal-codes.mjs` exit **0**. **BOTH FLOORS MOVED IN THE SAME TURN, FROM
  PRINTED FIGURES:** `REGISTER_FLOOR` 632 -> 647 arms / 134 -> 136 classified / 135 -> 137 corpus
  (ONE key set, verified by `grep`), read only AFTER the new files were in a commit; and `regionLines`
  1407 -> 1436, where the attribution is EXACT — `is-suggest-checks` grew 314L -> 343L and 1436 minus
  this item's 29 lines is 1407. **AND THE REGISTER FLOOR WAS ALREADY STALE ON ARRIVAL BY 8 ARMS / 1
  CLASSIFIED / 1 CORPUS, MEASURED RATHER THAN INFERRED:** `main` at `8096452` was checked out into a
  scratch `git worktree` (never `git stash`) and `--strict` run on that quiet tree with nothing
  uncommitted printed `arms 640/632 · classified 135/134 · corpus 136/135` with no contamination note
  — so only 7 of the 15 arms are this item's, and the sixth consecutive item has found a floor stale
  by measuring it. **I TOUCHED A GOVERNED DEC-49 REGION** (`is-suggest-checks`) and say so here
  because `regionLines` is a property of the MERGED source and has moved at integration four times out
  of five. **SEVEN CONTROL ARMS, each armed ALONE with every other defence held OPEN, a BASELINE arm
  and an OVER-STRICTNESS arm included, every restore verified by sha256 AND by `cmp` against
  uniquely-named per-arm pristine copies with byte counts printed and floored against the
  empty-string digest; zero restore problems. THREE CAME BACK OTHER THAN DECLARED and all three are
  written into the driver's header rather than smoothed:** (2) widening the guard showed **the two
  sites are NOT redundant** — a clean named refusal becomes one in another family's words about a
  document the endpoint had already composed; (4) **my own declaration was wrong about what the patch
  does**, and correcting it showed the check half's contribution is the BOUND rather than the
  admission; and (5) **PL-17's recorded WRONG FIX no longer refuses the value at all**, because this
  item wired the check through the predicate whose arm ordering PL-17 built for exactly this — the
  claim measured against the patch it was written to survive, and the one-line change is now purely a
  defect that buys nothing. **ARM (1) IS THE RECEIPT THAT THIS HAD TO BE ONE ITEM:** with `C-25.6`
  fully wired and only the guard reverted, the feature is completely unreachable through the op.
  **C-2.8 IS A DECIDED CLOSURE, NOT A MISS**, with its reason at the site and DRIVEN at
  `op=inquiryground`, which refuses a machine credential outright (`MACHINE_CANNOT_GROUND`, REC-64 /
  C-32.8) — so there is no machine writer there for the third state to keep honest. **`schema.mjs` IS
  THEREFORE UNTOUCHED and PL-17's prediction that its `inquiry_basis` sentence would become incomplete
  does not come true** — that sentence is about the block this item did not change. **NO MIGRATION IS
  OWED:** no SQL table projects `basis_version_grounds` at all (`inquiry_basis_versions` and
  `inquiry_basis_version_legs` are the only two), so the field lives in `bundle.md` only. **AND THE
  WRONG C-NUMBER WAS STILL LIVE IN THE PLANE:** PL-3's guard cited `C-25.15` in its comment and TWICE
  in the refusal a caller reads; FL-3 corrected the entry, PL-19 corrected the site. **WHAT THIS ITEM
  DID NOT DO:** it did not re-measure the strength pair ACROSS several grounds — that is PL-14's step
  three. It measured only the half DEC-65's argument rests on: a machine's single-part reading and a
  member's derive the IDENTICAL pair. `git stash` was NOT used at any point. **One DELEGATION filed**
  (PL-14).


### DELEGATION 2026-08-09 RECORD (PL-19) -> PL-14 (the strength pair): **DEC-65's STEP THREE, AND WHAT IS ALREADY MEASURED SO YOU DO NOT RE-MEASURE IT**

DEC-65's sequencing is mint (PL-17), then check + guard together (PL-19), then *"PL-14 re-measures
the strength pair over grounds after."* The first two are done. What you inherit:

- **A NEW SHAPE CAN NOW REACH THE ARITHMETIC.** A version's `basis_version_grounds[].asserted_by`
  may now hold `SUFFICIENCY_UNCLAIMED` — but **only on a version declaring exactly ONE part**, and
  only there. `C-25.6` refuses it on two or more, and PL-3's endpoint refuses a machine composing
  more than one. So the maximum §12 takes is never taken over an unclaimed part.
- **ONE HALF IS ALREADY MEASURED AND IT IS THE HALF DEC-65's ARGUMENT RESTS ON.** Driven through
  `op=suggest` over the same single leg, a machine's single-part reading and a member's derive the
  IDENTICAL pair (`capture graded/B · connection unrated/null`). That is in
  `test/dec65-single-part.test.mjs` §5, and it is deliberately the narrow claim: *with one part
  there is no maximum to take.*
- **WHAT IS NOT MEASURED, AND IT IS YOURS.** Whether anything in the strength walk READS
  `asserted_by` at all across SEVERAL grounds. PL-19 did not touch `#strengthWalk` and did not
  survey it. If the walk takes its maximum over declared grounds without consulting the
  attribution, the third state is arithmetically inert by construction and that is worth saying
  out loud; if it does consult it, the reading of the new value is a decision nobody has made.
- **ASK THE PREDICATE, NEVER MATCH THE LITERAL.** `isSufficiencyClaimed` is TRUE for a named member
  and for nothing else — not for this value, not for a blank, not for a machine stamp. A site that
  tests `'none:independent-sufficiency'` itself has rebuilt the predicate and will be wrong the day
  a fourth state exists.
- **AND ONE THING NOT TO REACH FOR, now measured twice.** Adding `none:` to `MACHINE_STAMP_PREFIXES`
  no longer makes `C-25.6` refuse the value (PL-19 control arm 5) and never made the record honest:
  it makes the record say *a machine claimed this* about a value meaning *nobody did*.
---

## CLAIM 2026-08-09 UI · UI-44 — **THE CONNECTIONS SIDEBAR (DEC-52 FINAL)**

session: ui44-agent (worktree agent-a43daf108f7ec006c)
opened: 2026-08-09T00:00:00Z
paths: **`civicos-ui/app.html` — claimed by RENDERER, never by file, because UI-43 and UI-45 are live in the same file.** The renderers this item declares and owns, all of them INSIDE the existing `/*__AI_SESSION_START__*/`…`/*__AI_SESSION_END__*/` region and delimited by their own `/*__AI_CONNECTIONS_START__*/`…`/*__AI_CONNECTIONS_END__*/` markers: `AI_CONNECTIONS_KEY`, `AI_CONNECTION_MINT_PREFIXES`, `aiConnectionMachinePrincipals`, `aiConnectionAttribution`, `aiConnectionDressHtml`, `aiConnectionCardHtml`, `aiConnectionsSelected`, `aiConnectionsSelectionBind`, `aiConnectionsSelectionToggle`, `aiConnectionsReviewMotion`, `aiConnectionsSidebarHtml`, `aiConnectionsReviewGo`, and the `.ai-conn*` CSS rules. **ONE line outside those markers is opened and it is named:** `aiSessionPanelHtml`'s single return expression, which gains the sidebar call. **AND ONE SUITE THAT IS NOT THIS ITEM'S IS OPENED, DECLARED HERE RATHER THAN DISCOVERED AT INTEGRATION: `civicos-ui/test/ai-session-wire.test.mjs`, ARM S only** — corrected in place with a dated reason and two new arms (S7, S8), never exempted. Same area, and the reason is at the site: ARM S5 asserts *the surface renders nothing the record did not publish*, which is right about the RUN and becomes the WRONG RULE the moment the block hosts D-82's dress, since D-82 is satisfied only by saying something the record cannot say about itself. Left standing it would have made delivering D-82 FAIL THE BUILD — the same inversion UI-49 corrected on ARM C2 one arm over. The replacement partitions by REGION MARKER rather than by a list of names, falls back to sweeping the WHOLE block if the marker is ever missing, and requires the held-out region to be graded by this item's suite. **Nothing else in that file is touched.** **NOT opened:** `aiSessionRead`, `aiSessionTranscript`, `aiSessionInContext`, `aiSessionIndicatorHtml`, `aiSessionContextHtml`, `aiSessionPairsHtml`, `aiSessionBlockHtml`, `aiSessionOpen`, `aiSessionRouteFromHash`, the roster, any router anywhere in the file, and every screen renderer. `SURFACES["ai-session"].purpose` gains one clause; `kind`, `routes`, `levels`, `acts` and `reads` are UNCHANGED. `civicos-ui/test/connections-sidebar.test.mjs` (NEW — this item's own suite). `docs/development/CLAIMS.md` (this entry and its delegations); `docs/development/kickoffs/UI.md` (APPENDED, never rewritten).
interfaces owned: none. No op, table, column or published shape changes. **This item adds NO plane read and NO plane write** — the sidebar renders the run object `op=airun` already answers, through the read `aiSessionRead` already performs.
interfaces consumed: I3, READ ONLY and read STRUCTURALLY: `bio-plane/checks/bio-checks.mjs`'s `MACHINE_STAMP_PREFIXES` / `MACHINE_AUTHOR_PREFIX` / `MACHINE_CLASS_PREFIX` are IMPORTED as live module exports by the suite and pinned against the surface's own copy, so the control plane renaming a mint fails the build. **NOTHING INSIDE `bio-plane/**` IS OPENED.**
expected: `IS-BUILD-PLAN.md` UI-44, governed by DEC-52 (final, 2026-08-07). The machine MAY rule: connections it identifies land in the record MACHINE-ATTRIBUTED (D-82 look-derived, the `token:`/`class:` principal named verbatim), and the sidebar is a VISIBILITY and BULK-REVIEW surface on the running-session surface — **not a required approval gate**. Bulk review is the same act over a set.
**SHIPPED FIXTURE-VERIFIED, AND THAT IS STATED IN THE SUITE'S OWN FIRST PARAGRAPH RATHER THAN INFERRABLE FROM IT.** The post-processing task scope that would produce LIVE machine connections HAS NO ITEM and does not exist (`IS-BUILD-PLAN.md` §VERDICT, `QUEUE.md`). `op=airun` publishes no connections collection today, and this item did not invent one plane-side. Every arm below drives the renderers against FIXTURES whose values are drawn at RUNTIME and PRINTED. **What is verified is the SURFACE's treatment of a machine-attributed connection; what is NOT verified is that any machine connection exists to treat.** A surface verified against fixtures is a real result; one that reads as verified against live data is the overclaim class this project ranks worst.
accepts-when: as `IS-BUILD-PLAN.md` UI-44 — `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED, 0: machine-declared connections visibly machine-attributed; bulk review operates over a set; no approval gate blocks the write. Baselines MEASURED IN THIS WORKTREE BEFORE ANY EDIT and reported per suite. NEGATIVE CONTROLS run and recorded in the suite's own `NEGATIVE CONTROL:` line, each arm ALONE with the others held open, every restore verified by sha256 AND by content: (1) strip the derived dress -> the D-82 arm FAILS; (2) re-introduce approval as a write gate -> the DEC-52-final arm FAILS (the SUPERSEDED provisional); (3) make bulk review N acts instead of one over the set -> the set arm FAILS; (4) neuter the attribution predicate -> the REACH arm fails AS A DELTA with the corpus printed; (5) polarity on every pin; (6) an over-strictness arm — a machine mint this file never anticipated must still be attributed.
measured: **REBASED ONTO `origin/main` AT `ae34ec8` AFTER THE WORK WAS DONE — main moved 26 commits during this session (UI-42, REC-69, FL-4, FL-5, PL-17, D-255, D-257 among them), and every figure below is re-measured on the rebased tree rather than carried over.** UI harness `node civicos-ui/test/run.mjs` from the REPO ROOT, exit read UNPIPED: **0, 43 harnesses**. Per-suite assertions **3,988 -> 4,037**, and the delta is ATTRIBUTED BY RE-RUNNING THE TRUE BASELINE rather than by subtraction — a scratch `git worktree` at `ae34ec8` ran the same per-suite tally: **connections-sidebar +47 (new), ai-session-wire 81 -> 83 (this item's ARM S7 and S8), every other suite identical row for row.** Battery **142/142 at 9,179**, unchanged by this item (no `bio-plane/**` file is opened). `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` unpiped, **exit 0**. `check-refusal-codes` floors all sit EXACTLY at measured with no `GREW`, and this item adds no refusal code, no `where` and no governed region — **no floor was invalidated and none was moved.** Seven suites print a foot this tally's matcher cannot read; they are reported **NO TALLY (-1)**, never 0, and they PASS.
concurrency: two UI workers are live in `civicos-ui/app.html` — **UI-43 (the accept ceremony)** and **UI-45 (notifications rendered)**. This claim is RENDERER-GRAINED for that reason and names the one line outside its own markers that it opens. Neither of those items' plan rows touches the running-session block or `SURFACES["ai-session"]`. Checked over every unreleased block in this file: no live claim names `civicos-ui/app.html`'s AI-session region, `civicos-ui/test/ai-session-*.test.mjs` (NOT opened by this item) or `connections-sidebar.test.mjs`.
released:

### DELEGATION 2026-08-09 UI (UI-44) -> CONDUCT, AND IT IS THE ONE THAT MATTERS MOST: **UI-44 SHIPPED FIXTURE-VERIFIED, THE PRODUCER STILL HAS NO ITEM, AND THE SUITE NOW FAILS WHEN THAT STOPS BEING TRUE**

The plan's own verdict and `QUEUE.md`'s one flagged follow-up both say it: the post-processing task scope that would produce LIVE machine connections for UI-44 (DEC-52-final's WRITE half) **has no item and does not exist**. UI-44 took the plan's second option and shipped FIXTURE-VERIFIED.

- **IT IS SAID IN THE PRODUCT OF THE ITEM, not only in this report.** The suite's first paragraph states it; the suite's FOOT prints it on every run (`connections-sidebar: FIXTURE-VERIFIED — no plane was driven; no op publishes a machine connection today`); and `app.html`'s own block header states it beside the code.
- **AND IT IS MEASURED RATHER THAN ASSERTED, WHICH IS THE PART WORTH KEEPING.** SECTION 0 brace-extracts `aiRunRead`'s own body out of `bio-plane/src/store.mjs` (3,585 chars, reach-asserted against a floor and against two blocks it must contain) and asserts that `op=airun` publishes **no** connections collection. A second arm asserts the PLAN still records the follow-up as having no item. **When either stops being true the suite goes RED**, and the next session must re-take the fixture claim instead of inheriting it. A caveat that cannot expire is a caveat nobody re-reads.
- **WHAT CONDUCT SHOULD DO WITH IT.** Nothing is blocked. When the producer is scoped, the seam is ONE line — `AI_CONNECTIONS_KEY` in `app.html` — and every renderer and every pin above it stays as it is. Expect exactly two reds at that moment and both are wanted: SECTION 0's ARM P1, and `ai-session-wire.test.mjs`'s ARM S5 on `aiSessionPanelHtml` (the panel composes the sidebar, and the sweep's sentinel run would then carry connections). Both are named at their sites.
- **WHAT IS NOT VERIFIED, stated plainly:** that any machine connection exists to treat. What IS verified is how this surface treats one.

### DELEGATION 2026-08-09 UI (UI-44) -> WHOEVER OWNS THE DESIGN DOCS / F9's LANE: **`INVESTIGATIVE-SESSION.md` STILL CARRIES THE PROVISIONAL DEC-52 REVERSED, AND A SURFACE NOW CONTRADICTS IT**

`docs/development/INVESTIGATIVE-SESSION.md` states at TWO sites — §14a's post-processing paragraph (`[BOB-4 — RULED 2026-08-07, PROVISIONAL]`) and the open-questions list — that *"the sidebar approval (identify → present → member approves) remains the act of record for the constitutive fields."* **DEC-52's decided entry reverses exactly that**: *"The earlier provisional (sidebar approval as the act of record) is SUPERSEDED as a gate; the sidebar remains a visibility and bulk-review surface, not a required approval."* The doc's own conformance table already knows (`F9 — §14a's [BOB-4] provisional is superseded by DEC-52's decided entry; rewrite the tier text`), and `IS-BUILD-PLAN.md` places F9 on W0's lanes.

- **NOT EDITED BY THIS ITEM**, because it is another lane's row and this item is a surface item. **PINNED INSTEAD:** `connections-sidebar.test.mjs` ARM P3 asserts the stale text is still there, so a corrected surface standing beside a stale design doc stays visible rather than quietly diverging. When F9 lands, ARM P3 goes red and is deleted with the doc's correction — one line, and the pin names it.
- **WHY IT IS WORTH DOING NOW RATHER THAN WHENEVER:** the shipped surface and the design document now say opposite things about whether a machine connection stands before a member touches it. That is the single fact a member could be misled about, and a reader who trusts the design doc over the code will build the gate back.

### DELEGATION 2026-08-09 UI (UI-44) -> RECORD: **THERE IS NO PLACE TO KEEP A MEMBER'S REVIEW OF A MACHINE CONNECTION, AND THE SURFACE SAYS SO RATHER THAN PRETENDING**

DEC-52-final leaves the sidebar as visibility and BULK REVIEW. The review half is built — one motion over a set, carrying the record's own published objects — and it **writes nothing**, because no op records it. `op=proposedispose` disposes a PROPOSAL and a machine connection is not one; nothing else fits.

- **WHAT THE SURFACE DOES TODAY:** composes the motion in full, and tells the member, in the surface's own words, that nothing was written **and that the connections are already in the record** — which is the part that keeps it honest, because under DEC-52-final a review that recorded nothing has cost the record nothing. That is the same shape the proposal DEFER/DISMISS path already takes one screen over, for the same reason.
- **WHAT AN OP WOULD NEED TO DECIDE, and it is a real question rather than a shape:** a review is not a disposition and not an approval. It asserts that a NAMED MEMBER LOOKED. That is a member's constitutive statement about their own attention, so it takes a member credential and must be refused to `token:*` by REC-46's predicate like every other one — and it must never be readable as changing the connection's standing, or the superseded provisional comes back through the store instead of through the surface.
- **Reversal cost: none today.** Nothing has been written, so nothing has to be unwound; the surface's motion shape is the input an op would take.

### DELEGATION 2026-08-09 UI (UI-44) -> UI-43 AND UI-45 (both live in `civicos-ui/app.html` right now): **WHAT THIS ITEM TOUCHED, TO THE LINE, SO YOUR REBASE IS A READ RATHER THAN A GUESS**

- **A NEW CSS BLOCK** immediately after `.ai-panel .ai-bound .ai-bound`, headed `---- the connections sidebar (UI-44, DEC-52 final) ----`. All rules are `.ai-conn*`. Nothing existing was edited.
- **ONE CLAUSE APPENDED** to `SURFACES["ai-session"].purpose`. `kind`, `routes`, `levels`, `acts` and `reads` are byte-unchanged, and this item's ARM K4/K5 assert that.
- **ONE EXPRESSION CHANGED**: `aiSessionPanelHtml`'s single `return`, which now interpolates `aiConnectionsSidebarHtml(session)` between the run block and the transcript.
- **A NEW REGION**, `/*__AI_CONNECTIONS_START__*/`…`/*__AI_CONNECTIONS_END__*/`, inside the existing AI-session region and before `let AI_SESSION_HASH_LOCK`. Everything else this item wrote is inside it.
- **NO ROUTER WAS ADDED**, deliberately: the sidebar is reached at the `#session/<id>` address that already exists, so `preauth-vocabulary.test.mjs`'s address walk is untouched and no new router needed classifying. **NO PLANE READ AND NO PLANE WRITE WAS ADDED**, so `bound-sweep.test.mjs`'s two walks see nothing new — there is no explicit ask to state because there is no ask.
## CLAIM 2026-08-09 PLANE (PL-18 — DEC-63's enactment: the run verbs' gate is PROJECT MEMBERSHIP)
session: pl18-project-membership-gate (worktree agent-a4e2eff5ca09197e2)
opened: 2026-08-09T00:00:00Z
paths: named by SITE, never by whole file, because up to four other workers may be in these three files.
  **`bio-plane/checks/bio-checks.mjs`** — the `AI_RUN_CHECKS` family ONLY: its header's C-22 COUNT
  sentence (corrected in place, SEVEN -> EIGHT, per that header's own rule) and ONE new row,
  `AI_RUN_NOT_PROJECT_MEMBER` (`C-22.8`), appended after `AI_RUN_SKILL_VERSION_UNNAMED`. **No new
  `*_CHECKS` family** — SK-1's precedent and the `FLOOR` block's own rule, because a family is a floor
  in `civicos-ui/check-refusal-codes.mjs` that buys slack for everybody else's walk. No other family,
  no `checkBundle` arm, no existing row edited.
  **`bio-plane/src/airun.mjs`** — ONE new exported function, `projectGate`, appended to the refusals
  section after `checkBound`, plus its `PROJECT_GATE_GROUNDS` vocabulary and the section header's
  function list. The existing `refusal` helper is CALLED, never copied. `checkObservation`,
  `checkCondition`, `checkBound`, `finishedBound`, `translationOf` and every vocabulary already there
  are READ and DRIVEN, never modified.
  **`bio-plane/src/store.mjs`** — FOUR sites and nothing else: (1) two new private helpers
  `#runContextProjects` and `#aiRunProjectGate`, inserted immediately before `aiRunOpen`; (2) one guard
  block inside `aiRunOpen` between the `AI_RUN_NO_CONTEXT` refusal and the principals refusal, plus the
  `actor` parameter in its signature and the gate statement on its success return; (3) the same guard
  in `aiRunTick` and (4) in `aiRunClose`, each placed AFTER the run row is resolved; plus the three
  `airunopen`/`airuntick`/`airunclose` lines in the DO dispatch table, which gain `actor`. `#participation`
  and `#citesInto` are CALLED and not modified — the gate deliberately reuses the record's existing
  predicates rather than writing a second membership test or a second citation test.
  **`bio-plane/src/index.mjs`** — THREE sites: (1) one new const `RUN_VERB_ACTIONS` beside
  `AI_RUN_ACTIONS` (which is NOT edited — it also carries `suggest` and `capturerequest`, which this
  item does not gate); (2) the `actor` server-side stamp, added beside the existing `principal` stamp,
  deleted-then-set on QUEUE_ACTIONS' precedent; (3) the `NEEDS` COMMENT above `airunopen`/`airuntick`/
  `airunclose`, rewritten to say the capability is now the FLOOR and the gate is participation. **The
  three `NEEDS` VALUES stay `contribute`** — that is the ruling, not an omission.
  **`bio-plane/test/airun-projectgate.test.mjs` (NEW — entirely this item's)** and
  **`bio-plane/test/nc-pl18.mjs` (NEW harness, deliberately NOT a `.test.mjs`: it EDITS REAL SOURCES
  while it runs and `scripts/battery.mjs` discovers by that suffix — `nc-rec72.mjs` is the precedent)**.
  **`bio-plane/scripts/coverage.mjs`** — the `REGISTER_FLOOR` NUMERALS ONLY, from the figures the
  instrument PRINTS, because a new suite carrying arm declarations invalidates all three.
  **`civicos-ui/check-refusal-codes.mjs`** — the `FLOOR` NUMERALS ONLY, from the figures that harness
  PRINTS. No matcher, no gate, no walk.
  `docs/development/MEASUREMENTS.md` — ONE dated section, APPENDED: the two class sweeps.
  `docs/development/CLAIMS.md` (this entry and its DELEGATIONS).
  **TWO MORE `.test.mjs` FILES, EACH CORRECTED AND NEITHER EXEMPTED, both found by the WHOLE
  battery and by nothing smaller.** **`bio-plane/test/leadslug.test.mjs`** — the FIXTURE only: its
  run was opened by `ruth` over a question `PROJ_A` draws on while `carol` was that project's only
  participant, which DEC-63 now refuses. The fixture is made LEGAL through the real acts (carol
  invites, ruth joins) with the reason at the site; **no assertion in that suite is changed and
  none is exempted** — the refusal it hit was correct. **`bio-plane/test/run-conditions.test.mjs`**
  — ONE row in its `ROLE` table: DEC-63's gate makes `aiRunClose` the THIRTEENTH reader of
  `ai_runs`, and ARM W3 FAILED naming it, which is that arm working exactly as its own sentence
  promises. Classified `WRITES` with the reasoning at the site; ARM W4's "exactly three publishers"
  re-checked rather than assumed. (Neither path was in the claim as first written; both are added
  here in the turn they were edited.)
  **`bio-plane/test/airun.test.mjs` — ARM D1 ONLY, CORRECTED AND NEVER EXEMPTED.** That arm pins the
  C-22 family's SIZE and it went RED on this item's first run, which is the arm working. The count and
  its comment are moved SEVEN -> EIGHT with the reason at the site, on the identical terms SK-1 used
  one row earlier. **No other assertion in that suite is touched and none is exempted.** (This path was
  NOT in the claim as first written; it is added here in the turn it was edited rather than left to be
  discovered in the diff.)
  **NOT** `docs/development/QUEUE.md` (CONDUCT is its sole writer), **NOT** `src/schema.mjs`,
  **NOT** `src/skillpack.mjs`, **NOT** any other `.test.mjs`, **NOT** `civicos-ui/app.html`,
  **NOT** `newgroup/**`, **NOT** `pdf-worker/**`, **NOT** `agent-worker/**`.
interfaces consumed: I3. interfaces owned: none reshaped. **No op added and no op contract moved:** the
  three verbs keep their names, arguments and answer shape. What changes is WHICH CALLERS ARE REFUSED
  and a new STATED field on the success answer, which is additive. An IC row is filed if the refusal is
  a shape a consumer builds against — see the delegation below.
expected: **PL-18 / DEC-63.** The three run verbs check participation in the project the inquiry belongs
  to, `contribute` stays as the FLOOR beneath it, and **the refusal NAMES WHICH OF THE TWO FAILED** —
  because "you are not in this project" and "you lack contribute" are different facts about a member.
  Plus the case DEC-17 makes real rather than edge: **an inquiry outside any project**, decided and
  STATED rather than answered by a silent allow or a silent deny.
concurrency: checked over the whole file. The only claims still open are DIST's (`newgroup/**`,
  `release/**`, `scripts/deploy.mjs`, tags — disjoint), VERIFY/M0-15's (`scripts/battery.mjs`,
  `test/battery-provenance.test.mjs`, and `scripts/coverage.mjs`'s `REGISTER_FLOOR` numerals) and
  M0/D-237's (`scripts/residue.mjs`, `scripts/battery.mjs`, `test/battery-residue.*`, and the same
  `REGISTER_FLOOR` numerals). **NO OPEN CLAIM NAMES `src/store.mjs`, `src/index.mjs`,
  `src/airun.mjs`, `checks/bio-checks.mjs` or `civicos-ui/check-refusal-codes.mjs`.** The one overlap
  is `scripts/coverage.mjs`'s `REGISTER_FLOOR`, which three items now touch for its numerals alone:
  **if this conflicts at integration, COLLAPSE TO ONE KEY SET and re-read the printed figures — do not
  keep both, because the last duplicate key silently wins and once it was the lowest.**
  `git stash` was NOT used at any point.
baseline: **MEASURED IN THIS WORKTREE BEFORE ANY EDIT and trusted over the brief, which carried no
  figure: `npm run test:battery` -> 138/138 suites green (134 plane · 4 fleet) · 8,827 assertions ·
  exit 0 read UNPIPED.**
released:

### DELEGATION 2026-08-09 PLANE (PL-18) -> CONDUCT, AND IT CORRECTS THE RECORD: **DEC-63's ENACTMENT SAYS THE PROVISIONAL IS "NARROWER THAN THE RULING". IT IS NOT — IT IS WIDER ON THE AXIS THE RULING IS ABOUT, AND SOMETHING *WAS* PERMITTED MEANWHILE THAT THE RULING FORBIDS**

`DECISIONS.md` DEC-63's `enacted:` line, and `QUEUE.md`'s PL-18 row after it, both say:

> *The provisional keeps running until PL-18 lands. It is NARROWER than the ruling, so nothing is
> permitted meanwhile that the ruling would forbid — the safe direction, and stated rather than
> assumed.*

**Measured against the shipped code, that sentence is false in one direction.** The provisional
was `contribute` ALONE. The ruling is participation AND `contribute`. So:

- A **project member without `contribute`** was refused under the provisional and is still refused
  under the ruling. On this axis the provisional is indeed no wider.
- A **`contribute`-holder who is in NO project** was PERMITTED under the provisional and is REFUSED
  under the ruling. **That is the exact population DEC-63 is about, and the provisional let them
  start runs that spend the group's Claude budget.**

The correct sentence is that the provisional was **narrower on the capability axis and WIDER on the
participation axis**, and the participation axis is the one the ruling moved. Nothing needs undoing —
no run's record is invalid, and the window was one day — but the register should not carry
"nothing is permitted meanwhile that the ruling would forbid" as a settled claim, because a future
reader will use it as a precedent for how to phrase the next provisional. **The accepts-when in the
same row is what settles the reading and it is unambiguous: it asks for BOTH members to be refused,
each in their own words, which is what shipped.**

I have not edited `DECISIONS.md` or `QUEUE.md` — both are outside this claim and `QUEUE.md` has a
sole writer. This is CONDUCT's one-line correction to make.

### DELEGATION 2026-08-09 PLANE (PL-18) -> WHOEVER OWNS THE CAPABILITY REFUSAL (REC-64's remaining sweep): **`NOT_CAPABLE` IS ONE OF THE TWO REFUSALS THIS ITEM IS ABOUT, AND IT IS UNTRANSLATED UNDER DEC-49**

PL-18's requirement is that the two refusals NAME WHICH OF THE TWO FAILED. I made the participation
half fully DEC-49-compliant: `AI_RUN_NOT_PROJECT_MEMBER` / C-22.8, a canned translation in
`AI_RUN_CHECKS`, the code a string literal at its site through `refusal()`.

**The capability half is not, and it is not mine to fix.** `civicos-ui/check-refusal-codes.mjs`
prints `NOT_CAPABLE` in arm B's ratchet list — *41 of 218 codes in reach still have no canned
translation* — and it is there because the control plane's capability gate answers with a hand-written
`detail` and no code. It is a **plane-wide** refusal shared by roughly sixty ops, so giving it a code
is a change to every capability-gated op's answer and a fence far wider than DEC-63's rule. I
deliberately did not touch it.

**Why it is worth naming rather than leaving in a list of 41:** every other untranslated code in that
list is a lone condition. This one is now HALF OF A PAIR whose whole point is that a member can tell
the two apart. Today they CAN — the sentences differ, the shapes differ, and
`bio-plane/test/airun-projectgate.test.mjs` ARM C3 pins that neither carries the other's subject —
but one of the two is a sentence a surface must render verbatim or blank while the other is one it
can author. **`civicos-ui/app.html:3039` already hand-authors a NOT_CAPABLE sentence, and it is
about CAPTURING** (*"Capturing needs a member holding contribute"*), so a surface rendering it for a
refused run would tell a member the wrong thing about the wrong act. That is the concrete cost.

### DELEGATION 2026-08-09 PLANE (PL-18) -> UI: **C-22.8 IS A NEW CODE A MEMBER CAN RECEIVE AND NO SURFACE BRANCHES ON IT**

`AI_RUN_NOT_PROJECT_MEMBER` is in the guard's REACH (family row, R1) and is NOT in R2 — `app.html`
names 52 codes and this is not one of them. That is correct today: `app.html:16239` states in its own
words that `op=airunopen` has no UI consumer. **The plane sends the code AND its canned translation
on the wire**, so a surface that renders `translation` verbatim is already correct without changing
anything; a surface that branches per code will want this one when the running-session panel gains a
start control. Nothing is broken and nothing is owed today — this exists so the next UI item does not
have to rediscover the code.

### DECISION FOR BOB 2026-08-09 (PL-18) — **PARTICIPATION NOW FENCES *WORK* FOR THE FIRST TIME, AND THE SAME REASONING REACHES SEVERAL NEIGHBOURS. WHETHER IT SHOULD IS DOCTRINE, NOT MECHANISM**

**What is running:** exactly DEC-63 and nothing wider. The three run verbs check project
participation; every other act is unchanged.

**Why it is raised:** the class sweep measured something the ruling did not mention.
**Of 74 capability-gated ops, ELEVEN consult project participation — and before PL-18, ALL EIGHT of
them were the project ROSTER's own acts** (invite, join, leave, remove, the three owner acts, fork).
So until today participation was a fence over PARTICIPATION ITSELF and fenced no work anywhere.
PL-18's three verbs are the first acts in the plane where being in the project decides whether you
may do something to the record.

**The ambiguity:** DEC-63's reason — *"participation in the project the inquiry belongs to is what
licenses asking the system to look"* — is not obviously specific to runs. It reads at least as
strongly for `op=suggest` (proposing a reading of that project's evidence), the six version acts
(moving what a project stands on), `op=conclude`, and `op=inquiryground`. Each is gated on
`contribute` alone today, so **a contributor in no project can propose a version of a question that
three teams are working on.**

**The alternative:** leave it. Participation scopes a group's THINKING (§7's own words) and the
evidence corpus stays shared; a broad participation fence over ordinary record work would be a
significant narrowing of who can contribute, and it is the kind of change that is cheap to make and
expensive to walk back once members have organised around it.

**My recommendation: LEAVE IT, and do not extend by analogy.** DEC-63's argument names a specific
thing — SPENDING THE GROUP'S BUDGET — which is true of a run and of nothing else on that list. The
sweep is worth having on the record so the next person to ask this question starts from the
measurement rather than from an impression, but extending a fence because its reasoning *sounds*
general is the shape `CLAUDE.md` calls a fence tighter than its rule.

**Cost of reversal: LOW today and rising.** Each op would gain the same three-line gate that already
exists and is tested; the expensive part is not the code, it is that members will have arranged
themselves around whichever answer runs. Reversing after a group has organised its projects around a
tight fence means telling people their access changed twice.

### DELEGATION 2026-08-09 PLANE (PL-18) -> WHOEVER OWNS `VERIFICATION.md`'s REGISTER ROW (M0-15 and D-237 both hold it open): **IT IS STALE AGAIN — AND IT EXISTS TWICE, WHICH NOBODY HAS RECORDED**

**RAISED, NOT EDITED,** on UI-48's and CPDF-9's precedent: two open claims name
`docs/development/VERIFICATION.md` and two items moving one shared figure in parallel is the
failure this row is itself about.

**(1) STALE.** The row reads `128 of 128 suites · 542 arms across 127 classified declaration(s)`.
`node scripts/coverage.mjs --strict` on this worktree at commit `d8c6653` PRINTS
**`arms 631 · classified 134 · corpus 135`**. PL-18 contributed 10 of that gap and moved
`REGISTER_FLOOR` to `631/134/135` in the same turn from the printed figures. The rest is other
items' — **so this is the SIXTH consecutive item to find this row stale by measuring it**, and the
first to find it stale by roughly ninety arms.

**(2) THE DEFECT NOBODY HAS RECORDED, AND IT IS WORSE THAN THE STALENESS: THE ROW EXISTS THREE
TIMES.** `grep -n "suites declaring a negative control" docs/development/VERIFICATION.md` returns
**lines 54, 56 AND 58** — three rows with the same label in the same table, carrying **three
different figures** (`127 of 127 · 526 arms`, `120 of 120 · 470 arms`, `120 of 120 · 486 arms`),
each with its own accreted history, and **the NEWEST measurement is the FIRST of the three**, so a
reader scrolling to the label finds the freshest figure only by luck. That is a
keep-both merge artefact of exactly the class `REGISTER_FLOOR`'s own header block warns about six
times over — *"valid JavaScript where the last key silently wins"* — reproduced in Markdown, where
there is no last-wins rule at all and **a reader simply gets whichever one they scroll to first.**
Every one of the three is stale against 631/134/135, so there is no question of picking a survivor
by freshness — all three are wrong and the live figure is the instrument's.

**Why it outranks its size, and it is this row's own argument turned on itself:** the row exists so
a session can see at a glance whether the estate's controls are growing. A row that appears twice
with two different numbers cannot be falsified by reading it — you cannot tell a stale figure from
the other copy. **Fixing only the staleness would leave three rows and make it worse**, because the
corrected one would then disagree with its twins by ~100 arms rather than by 16. **Collapse to ONE
row and take the figure from a green `--strict` run on the merged tree** — do not reconcile the two
histories by hand, and do not move the numeral in only one of them.
released: 2026-08-09 — committed on `worktree-agent-a4e2eff5ca09197e2`, **NOT pushed and NOT merged**; CONDUCT integrates. **Battery 139/139 suites green · 8,877 assertions · exit 0 read UNPIPED**, against a true baseline MEASURED IN THIS WORKTREE BEFORE ANY EDIT of **138/138 · 8,827** (the brief carried no figure). **Delta +50, attributed by DIFFING the two full runs and never by subtraction: `airun-projectgate.test.mjs` ABSENT -> 47 (new) and `hygiene.test.mjs` 553 -> 556 (its per-suite arms over one new file). 137 of 139 suites IDENTICAL.** `node scripts/coverage.mjs --strict` run DIRECTLY, `$?` UNPIPED, **exit 0** (162 ops, 220 checks, all named). `node civicos-ui/test/run.mjs` from the repo root, exit unpiped, **0**. `node civicos-ui/check-refusal-codes.mjs` exit unpiped, **0**. `node tools/plancheck.mjs` clean but for UNPUSHED. **TEN FLOORS MOVED, every one from a figure an instrument PRINTED on a green run: `check-refusal-codes.mjs` rows 163->164, census 424->425, reach 217->218, governedSites 66->67, codesChecked 141->142, outcomeReturns 70->74, refusalsJudged 143->145, vocabularies 9->10, vocabularyTerms 56->59; `coverage.mjs` REGISTER_FLOOR 621/133/134 -> 631/134/135, read AFTER the suite was in a commit because the register compares against the REPRODUCIBLE figure. `regions` (53) and `regionLines` (1407) DELIBERATELY UNCHANGED and RE-READ rather than assumed — this item adds, moves and removes no `DEC-49 REGION` marker; `git diff` carries zero marker lines. `REGISTER_FLOOR` verified ONE key set by grep.** **TEN CONTROL ROWS in `test/nc-pl18.mjs`, each armed ALONE, each declared before arming, each refusing to arm on an anchor not occurring exactly once, every restore verified by sha256 AND `cmp` against a per-arm pristine copy under a byte floor — and the refuse-to-arm guard FIRED TWICE on my own restructures, which is it working.** Four arms came back other than declared and all four are recorded rather than smoothed: (b) declared B2 and **B2 stayed GREEN** — both sides of that arm read one catalogue and move together, so it can see a mis-keyed translation and never a wrong one (named at B2's site; B3 is what holds the content); (d), (e) and (f) each went WIDER than declared, (e) most usefully — the P arms caught PROJECTLESS becoming unreachable. **THREE THINGS THE INSTRUMENTS CAUGHT IN MY OWN WORK AND THE SOURCE MOVED FOR, NEVER A FLOOR:** the DEC-49 guard read the gate's two PERMITTING returns as codeless refusals (the verdict field was named `applied`, which is not a verdict) and named three REGION markers no row claimed; and an UNCONDITIONAL `searchParams.delete("actor")` **BROKE `op=lease`**, caught by one assertion in `members.test.mjs`. **THREE SUPERSEDED PINS CORRECTED AND NONE EXEMPTED**, each with why the old one was right when written: `airun.test.mjs` ARM D1 (C-22 family SEVEN -> EIGHT), `leadslug.test.mjs`'s fixture (made legal through the real acts), `run-conditions.test.mjs` ARM W3 (`aiRunClose` classified as the thirteenth reader of `ai_runs`). IC-47 filed; the version bump and the RESOLUTION are CONDUCT's. `git stash` was NOT used at any point.
