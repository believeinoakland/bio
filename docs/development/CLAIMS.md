# Area claims

The register of who is working where, so no two sessions edit the same paths at
once. Established 2026-07-31 as step 2 of the parallel-development move
(`PARALLELISM.md`, "What to build first"). A text file and a rule; no tooling.

## The rules, in short

- **Claim your area before you edit its paths.** Append a `## CLAIM` block below.
  A claim keeps other sessions out of the paths it names; it is not a courtesy.
- **A session that needs work inside another area's claim DELEGATES.** Append a
  `## DELEGATION` block naming what you need and why, and continue with your own
  work. The owning area picks it up. Do not edit a claimed path quietly.
- **Release a claim explicitly**, by setting `released:` to the date. An
  unreleased claim older than its expected scope is stale, and `ARCH` may
  reassign it; silence does not hold ground forever.
- **Unclaimed paths are nobody's** — a collision risk, not a licence. Claim
  before editing even briefly.
- **Append only.** States are added, never edited in place, so the history of who
  held what is readable the same way the record's own history is. The one field
  that changes on an existing block is `released:`.

The format is the one `PARALLELISM.md` fixes:

```
## CLAIM <date> <AREA>
session: <name>
opened: <ISO8601>
paths: <comma-separated paths this claim reserves>
interfaces consumed: <ID(s), or none>
interfaces owned: <ID(s), or none>
expected: <the work this claim covers>
released: <date, or blank while live>
```

---

## CLAIM 2026-07-31 CAPTURE
session: capture-bootstrap-1
opened: 2026-07-31T17:57:07Z
paths: bio-plane/src/subresources.mjs, bio-plane/src/cpu.mjs, bio-plane/src/cdx.mjs, the capture/link/task/reachability tables in bio-plane/src/schema.mjs, the capture ops in bio-plane/src/index.mjs, the link/capture/task/reachability functions in bio-plane/src/store.mjs, the capture and authority checks in bio-plane/checks/bio-checks.mjs, bio-plane/test/subresources.test.mjs, bio-plane/test/governor.test.mjs, bio-plane/test/inbox.test.mjs, bio-plane/test/reachability.test.mjs, bio-plane/test/cdx.test.mjs
interfaces consumed: none
interfaces owned: I1 (bytes → content)
expected: CAPTURE's standing responsibility (fetch, governor, subresources, links, reachability, archive fallback). This turn stood up the parallel-development scaffolding under the ARCH role from the main checkout — CLAIMS.md, INTERFACES.md with I1, the UI/FRAMEWORK kickoff refresh, and the biosmoke-ui instance — none of which is another area's code.
released: 2026-07-31 — RELEASED as STALE by CONDUCT per PARALLELISM.md's stale-claim rule (session capture-bootstrap-1 long gone, never released, over-reserved most of CAPTURE's paths and collided with RECORD's store-core work and a future CAP-3). Paths now belong to their areas: the store core/scheduler to RECORD, the capture/link/task/reachability functions to CAPTURE's live per-item claims.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-1
opened: 2026-07-31T20:30:00Z
paths: bio-plane/test/hygiene.test.mjs, the purge table list in bio-plane/src/store.mjs
interfaces consumed: none
interfaces owned: none
expected: D-113 as a CLASS. A hygiene check asserting every CREATE TABLE in schema.mjs is either named in op=purge's whole-store table list or in a small explicit exemption allowlist (each with a one-line reason), so a forgotten derived table fails loudly. Also corrects derived capture tables currently missing from the whole-store purge. No deploy this turn; ARCH integrates.
released: 2026-07-31 — landed on main by ARCH (integration turn), commit 6eb2b42. Full battery green; the negative control was re-run by ARCH before landing (throwaway table -> the check fails naming it).

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-1
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/pdfstructure.mjs, bio-plane/test/pdfstructure.test.mjs, the pdfstructure entries in bio-plane/package.json (the test:pdfstructure script and the additive link in the test chain)
interfaces consumed: I1 (bytes → content)
interfaces owned: none yet; expects to propose I2 (structure → framework) producer-side from what the extractor emits
expected: D-91 phase 1 — the PDF outbound-link structure graph (anchor / intra / deferred / refused / undetermined), byte-identical wrappers to subresources.mjs. No text extraction (unpdf) this turn.
released: 2026-07-31 — phase 1 landed on main by ARCH (integration turn), commit 0b1c6ce. Phase 2 (text via unpdf, measured against the 3MB bundle and CPU ceilings) remains; the op wiring needed to live-verify is the DELEGATION below.

## DELEGATION 2026-07-31 CONTENT-PDF → CAPTURE
from: content-pdf-agent-1 (recorded by ARCH at integration)
need: wire op=pdfstructure into the `if (op === …)` dispatch in bio-plane/src/index.mjs — a few additive lines that read a capture_sha through the existing op=capture byte path and call extractPdfStructure(bytes) from bio-plane/src/pdfstructure.mjs. index.mjs is CAPTURE's; this is the delegation the CONTENT-PDF kickoff anticipated, not a quiet edit.
why: the extractor is complete and unit-tested against fixtures but cannot be live-verified against real captured PDFs until an op exposes it. If op-wiring delegation recurs across content areas, that is the trigger to promote op-contract ownership out of CAPTURE (the I3 move PARALLELISM.md anticipates).
status: fulfilled 2026-07-31 — op=pdfstructure landed 2ab62f4 (capture-agent-2 / QUEUE.md CAP-1). Live-verify (QUEUE.md CPDF-3) still awaits a DIST deploy.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-2
opened: 2026-07-31T00:00:00Z
paths: the pdfstructure op in bio-plane/src/index.mjs (the OPS spec entry, the op=pdfstructure handler, the shared captureKey helper, and the extractPdfStructure import), bio-plane/test/pdfstructure-op.test.mjs
interfaces consumed: I1 (bytes → content), I2 (structure, provisional — consumed as the extractor's output shape)
interfaces owned: none
expected: QUEUE.md CAP-1 — fulfil the CONTENT-PDF→CAPTURE delegation above. Add op=pdfstructure (GET, read-only, same auth/caps posture as op=capture GET) that reads a capture sha through the exact R2 path op=capture uses and returns extractPdfStructure(bytes) as JSON. Test through the op with a fixture PDF (link annotation) plus a non-PDF negative control. Does NOT touch CONTENT-PDF's src/pdfstructure.mjs or test/pdfstructure.test.mjs. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — CAP-1 landed on main by CONDUCT, commit 2ab62f4. Full battery green; the captureKey extraction verified a no-op (capture behaviour unchanged), the op is read-only and mirrors op=capture GET auth (no new permission).

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-2
opened: 2026-07-31T00:00:00Z
paths: the CPDF-1 measurement section in docs/development/MEASUREMENTS.md (append-only), bio-plane/test/unpdf-measure-probe.mjs
interfaces consumed: none (a measurement)
interfaces owned: none
expected: D-91 phase-2 go/no-go MEASUREMENT (CPDF-1). Bundle `unpdf` through the plane's esbuild target in a scratch temp dir (NOT added to shipped deps/bundle) and measure bundled size vs the 3MB Free-worker limit; time extraction on a real Oakland agenda as a labelled node proxy (not Worker CPU); record both with date and instrument and recommend go/no-go. Commits no text extractor and changes no shipped dependency or bundle. Verdict: GO — bundle fits with 2.29 MB gzip headroom; node-proxy cost modest. CPDF-2 unblocked.
released: 2026-07-31 — measurement landed on branch content-pdf/phase2-measure; CONDUCT integrates. Worker-CPU-vs-ceiling confirmation via a deployed probe is the remaining gated follow-on (rides on the op=pdfstructure delegation above).

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-3
opened: 2026-07-31T00:00:00Z
paths: the task-queue drain alarm in bio-plane/src/store.mjs (the alarm()/onAlarm handler, #rearmSchedule, #armDrain, #drainDelayMs, the TASK_DRAIN_* constants, and the arming added to taskEnqueue), bio-plane/test/task-drain-alarm.test.mjs, the TASK_DRAIN_DELAY_MS binding added to bio-plane/test/inbox.test.mjs, and the two test entries in bio-plane/package.json
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md CAP-2 / D-109 — drain the task queue automatically on a Durable Object alarm, the mechanism #armSweep proved for selections: armed on enqueue, re-armed by the alarm while task_queue is non-empty, self-terminating (deleteAlarm) when it drains. The capture path only ENQUEUES; taskEnqueue arms the alarm (a schedule, never a task write) so the producer/consumer split is preserved. onAlarm shares the single DO alarm with the selection sweep and reconciles to the earliest wake. op=taskdrain still works — the alarm and the manual drain coexist. NEGATIVE CONTROL run: neutering onAlarm's taskDrain call fails the suite naming the undrained queue ("the queue is empty after the drain" want 0 got 1; "the queue count is zero" want 0 got 1); restored and green. Full battery green (exit 0). No inbox-grammar or write-gate change. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — CAP-2 / D-109 landed on main by CONDUCT, commit 39a0e1b. Full battery green (exit 0); the selection, inbox, and task-drain suites pass together, confirming the shared-alarm reconciliation preserves the selection sweep.

## CLAIM 2026-07-31 BOB
session: bob-planning-1
opened: 2026-07-31T21:00:00Z
paths: docs/development/MILESTONES.md (new), docs/development/VERIFICATION.md (new), docs/development/PLAN.md (header only), docs/development/ORCHESTRATION.md, docs/development/PARALLELISM.md (area table), docs/development/INTERFACES.md (I3/I4/I5 appended), docs/development/DEBT.md (Status column dispositions + appended section), docs/development/MEASUREMENTS.md (appended), docs/development/CAPTURE-SCALING.md (status markers), docs/development/kickoffs/README.md, CLAUDE.md (verification section), bio-plane/scripts/battery.mjs (new), bio-plane/scripts/coverage.mjs (new), bio-plane/test/bundle.test.mjs, bio-plane/package.json (two test scripts)
interfaces consumed: none
interfaces owned: registers I3, I4, I5 from the code as it stands (I3 owner moved CAPTURE -> RECORD)
expected: the planning-infrastructure cleanup Bob directed — place every open construct architecturally, refactor the plan into milestones and pipelines CONDUCT can orchestrate, and stand up a verification process with a measured floor. NO area code touched: the two new scripts are instruments, and bundle.test.mjs was REPAIRED (it read an absolute container path, was absent from the npm test chain, and failed on an 11-character token fixture against livefire's 16-character floor). QUEUE.md deliberately NOT edited — it is CONDUCT's sole-writer file and CONDUCT was landing CAP-2 concurrently; the M0 items are handed over as a decomposition instead.
released: 2026-07-31 — docs only; nothing deployed, nothing pushed.

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-4
opened: 2026-07-31T00:00:00Z
paths: the CPDF-7 measurement section in docs/development/MEASUREMENTS.md (append-only), the D-118 row in docs/development/DEBT.md (Status column only), bio-plane/test/free-tier-fleet-probe.mjs (new probe, not in the battery)
interfaces consumed: none (a measurement)
interfaces owned: none
expected: QUEUE.md CPDF-7 / D-118 — MEASURE whether Workers Free permits a second Worker script and service bindings, and what a cross-Worker call costs, through the project's own Cloudflare account and egress. Deploys THROWAWAY Workers (cpdf7-probe-*) and TEARS THEM DOWN, never touching biosmoke7/civicos/newgroup, the record or the installer; attaches no R2 binding. Commits no shipped code and changes no dependency. Findings: the account is on Workers FREE (the API refused limits.cpu_ms with "not supported for the Free plan"); on Free a second script deploys, a service binding deploys and RESOLVES, and a cross-Worker call runs end-to-end for ~1 ms wall-clock / one subrequest (25 binding calls in one Free invocation succeeded; teardown re-confirmed, back to three scripts). So D-118's conditional does NOT fire: pdf-worker (CPDF-6) is viable on Free and central; Tier 1 (CPDF-4) is not forced to be the floor by any binding limitation. D-118 CLOSED; the residual 10 ms Worker-CPU-ceiling question is narrowed onto CPDF-1's existing gated follow-on. No deploy of the plane; CONDUCT integrates.
released: 2026-07-31 — measurement landed on branch content-pdf/cpdf7-fleet-measure; CONDUCT integrates. Not pushed.

## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-5
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/pdfstructure.mjs, bio-plane/test/pdfstructure.test.mjs
interfaces consumed: I1 (bytes → content)
interfaces owned: none; extends the provisional I2 (structure → framework) producer-side with a `text` field (for FW-1 to confirm/counter)
expected: QUEUE.md CPDF-4 — Tier 1 text extraction IN THE PLANE, pure JS, no dependency. Parse the text-showing operators (Tj, TJ, ', ") in each page's content stream(s), reusing the existing object/stream parser (FlateDecode, /ObjStm), and decode the shown bytes to Unicode via each font's /ToUnicode CMap (beginbfchar/bfrange). EXTEND the extractor's existing I2 output object with `text` (document-level + per-page, plus per-region `undetermined` markers) — do NOT fork the output shape. `undetermined` is first-class and per-region: a glyph/run with no ToUnicode, a CID font, or an unmapped code is recorded as undetermined NAMING the cause (the font), never dropped, never guessed into mojibake. No op edit (op=pdfstructure returns the extractor's object, so text flows through automatically). No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (314f4b7). I2 extended with a `text` field (document/pages/undetermined markers, per-region markers naming the font/reason); registered provisional in INTERFACES.md for FW-1 to confirm/counter. battery 43/43 (2313); negative control run. index.mjs untouched.
opened: 2026-07-31T22:00:00Z
paths: the docprofile package — docprofile/index.mjs, docprofile/registry.mjs, docprofile/monitoring.mjs, docprofile/pipeline.mjs, docprofile/doctypes/index.mjs, docprofile/doctypes/registry.mjs, docprofile/doctypes/meeting-calendar.mjs, docprofile/doctypes/generic.mjs, docprofile/handlers/aspnet-webforms.mjs, docprofile/handlers/wordpress.mjs (member extraction removal only); the new docprofile/recogniser.mjs and docprofile/events.mjs; tools/bundle-docprofile.mjs (ORDER list); the flattened docprofile block in civicos-ui/app.html (rebuilt, not hand-edited); civicos-ui/test/docprofile.test.mjs and civicos-ui/test/change-layers.test.mjs (rewritten onto the consolidated shape)
interfaces consumed: I2 (structure, provisional — noted for FW-1/CPDF-4 that it will extend with text)
interfaces owned: none changed by this step (Step 0 is internal consolidation; I2 confirm/counter is FW-1)
expected: QUEUE.md FW-2 / D-68 / CONSTRUCTS Step 0 — the full reconciliation. One recogniser interface + one shared registry helper carrying both axes; one confidence ladder (CONFIDENCE, TYPE_CONFIDENCE merged); assess() the sole public entry point (monitor()/diffMembers/handler-derived contract() deleted, compare() demoted to the internal L3 primitive assess() calls); CONTRACT declared by the content type; a shared event catalogue; meaningful derived from SIGNIFICANCE. Must SHRINK the codebase and add no capability. Both npm run test:battery (bio-plane) and civicos-ui/test/run.mjs green; negative control on the single ladder / single entry point recorded in the suites.
released: 2026-07-31 — CONSTRUCTS Step 0 landed on main by CONDUCT; docprofile net -27 lines; civicos-ui/test/run.mjs + full battery green; negative controls (single ladder / sole entry point) confirmed.

## CLAIM 2026-07-31 RECORD
session: record-agent-1
opened: 2026-07-31T22:30:00Z
paths: the SCHEDULER block in bio-plane/src/store.mjs — specifically the reserved `alarm()` forwarder and the `onAlarm()` handler, the reconcile helper (renamed from `#rearmSchedule` to `#reconcileAlarm`), the arm helpers `#armScheduler`/`#armSweep`/`#armDrain`, the new consumer registry `#schedConsumers`, the `#lastDrainProgress` field, the `SCHED_GRACE_MS` constant, and the env-gated test-probe seam (`#probeSpecs`, `#probeState`, `schedProbeArm`, `schedProbeLog`); bio-plane/test/scheduler.test.mjs (new); the test:scheduler entry and the battery-chain entry in bio-plane/package.json; docs/development/SCHEDULER.md (new, the shape decision)
interfaces consumed: none
interfaces owned: I5 (the store schema — unchanged this turn; no table added or reshaped)
expected: QUEUE.md REC-1 / M1 — decide the plane's scheduler shape ONCE and build the mechanism. Generalises the `#armSweep`/`#rearmSchedule` single-reconciling-alarm pattern into a consumer registry where each consumer declares its own wake and the one DO alarm reconciles to the earliest, self-terminating when all are idle. Moves the two existing consumers (RECORD's selection sweep and CAP-2's task drain) onto the registry as proof. Does NOT touch CAPTURE's `taskDrain`/`taskEnqueue` bodies, the link/capture/task/reachability functions, or any schema table — only the alarm handler and reconcile/arm helpers, which PARALLELISM.md places with RECORD (the scheduler). No new op, so no control-plane surface change. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — landed on main by CONDUCT (4d77938). One reconciling DO alarm + consumer registry; both existing consumers (selection sweep, task drain) moved onto it with bodies unchanged; battery 43/43 (2272 assertions), scheduler suite + starvation negative control green; I5 unchanged.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-1
opened: 2026-07-31T00:00:00Z
paths: bio-plane/test/reachability.test.mjs, bio-plane/test/acquire.test.mjs, bio-plane/test/subresources.test.mjs (+ a one-line NEGATIVE CONTROL header comment), bio-plane/test/members.test.mjs — test files only; no plane behaviour touched
interfaces consumed: I3 (the control-plane op= dispatch)
interfaces owned: none
expected: QUEUE.md M0-1 — control-plane assertions for the three ops no suite reached (archivelookup, linkproject, signerlist) and for sourcereach, reached only at the Durable Object (the D-43 class). Reachability, not success: each op is driven through op= dispatch with a minimal valid-enough request and asserted to answer STRUCTURALLY rather than crash — a governed BIO refusal counts. Placements, each in the op's own area suite: sourcereach -> reachability.test.mjs (op=sourcereach read of the same "bio" store the suite drives at the DO); archivelookup -> acquire.test.mjs (a fresh store refuses NOT_ELIGIBLE by the D-104 fence — structured); linkproject -> subresources.test.mjs beside op=links (NEED_CAPTURE refusal proves the handler ran, then a valid capture projects with ok:true / edges[] / projected:number); signerlist -> members.test.mjs beside signeradd (ok:true, result.signers[]). Coverage BEFORE: 81/85 control-plane (95.3%), 1 durable-object-only (sourcereach), 3 unreached (archivelookup, linkproject, signerlist). AFTER: 85/85 (100.0%), 0 durable-object-only, 0 unreached. NEGATIVE CONTROL RUN 2026-07-31: deleted the linkproject dispatch block from subresources.test.mjs (its op= appears only there — the header comment was worded to NOT contain op=linkproject, so the instrument is not fed by the comment) and `npm run test:coverage` reported unreached 0 -> 1 naming linkproject (mutating); restored, back to 0. Recorded in subresources.test.mjs's first-60-lines NEGATIVE CONTROL comment. Full battery green: 42/42, 2264 assertions (was 2252; +12). No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT. CONDUCT confirmed via `npm run test:coverage` on the integrated state: 85/85, 0 DO-only, 0 unreached (was 81/85 + 1 DO-only + 3 unreached — the D-43 class closed for the current surface). The worker RAN its negative control (delete the linkproject dispatch → unreached 0→1) and recorded it in subresources.test.mjs; battery green.

## CLAIM 2026-07-31 RECORD
session: record-agent-2
opened: 2026-07-31T23:30:00Z
paths: in bio-plane/src/store.mjs — `acquireLease` (the lease-taking path, ~line 2761) ONLY: it gains a fail-closed guard that refuses a null/blank actor by name instead of tripping SQLITE_CONSTRAINT_NOTNULL (D-39 class). In bio-plane/src/index.mjs — the control-plane authorship stamps ONLY: the lease `actor` stamp (~line 2222) is extended to stamp a NAMED machine identity `token:<class>` for a machine credential and to delete a caller-supplied `actor` first (previously stamped only for a session, leaving a machine's actor caller-supplied/absent), and the op=promote body stamp (~line 2270) is extended so a machine credential's `author` is server-stamped `token:<class>` (deleted-then-set), matching the `by`/`viewer` machine convention already there, so the manifest NAMES the machine writer. New test bio-plane/test/unattended-lease.test.mjs; its entry in bio-plane/package.json (test:unattended-lease + the npm test chain). NO schema.mjs change (leases.actor stays NOT NULL TEXT; a machine identity string is a valid actor), so I5 is untouched and the D-113 purge list (leases already present) and hygiene check are unaffected.
interfaces consumed: I3 (the control-plane op= dispatch — additive authorship stamping, no op renamed, no classes/reason/shape changed)
interfaces owned: I5 (the store schema — UNCHANGED this turn; no table added or reshaped)
expected: QUEUE.md REC-2 / D-61 — an unattended (machine) writer cannot take a lease because leases.actor is NOT NULL and was stamped only from a session, so a daemon cannot complete a capture a member walked away from. DECISION: option (a), a named machine actor identity on the lease, NOT (b) dropping the lease from the refill path. Rationale in the suite header and the report: the lease is a courtesy lock worth keeping in the loop for the daemon (a returning member sees the job is being finished; two daemons do not both re-fetch), naming costs nothing and needs no schema change, and promote's CAS on `base` — the real integrity mechanism — is untouched, so a daemon that holds a lease still cannot cause a lost update. The writer is NAMED (server-stamped token:<class>, caller value deleted), never anonymous. accepts-when: a machine credential completes a capture a session started and the manifest author names the machine writer; NEGATIVE CONTROL: an anonymous (null/blank actor) lease is refused by name. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (e425b24). CONDUCT re-ran the anonymous-lease negative control (neuter the acquireLease guard → 20/2, restored → 22/22); battery 44/44 (2337). Battery 44/44 (2308 assertions); the new unattended-lease.test.mjs 22/22 and its negative control RUN (neuter the acquireLease guard -> 20/2, restored -> 22/22); coverage 85/85 ops reached, 0 unreached, no new op, suite declares its control. schema.mjs untouched (I5 unchanged); D-113 purge list + hygiene still green. node_modules was symlinked from the main checkout for miniflare and removed before commit.
## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-6
opened: 2026-07-31T00:00:00Z
paths: the CPDF-5 section of docs/development/MEASUREMENTS.md (append-only); bio-plane/test/tier1-coverage-probe.mjs (new probe, NOT in the battery); bio-plane/test/tier1-coverage-tier2-oracle.mjs (new probe, NOT in the battery)
interfaces consumed: I2 (structure -> text; consumes the CPDF-4 `text` field of extractPdfStructure, read-only)
interfaces owned: none — a measurement; commits no extractor and changes no shipped code
expected: QUEUE.md CPDF-5 — MEASURE Tier-1 text-extraction coverage on REAL Oakland PDFs to SIZE Tier 2 (the unpdf pdf-worker, CPDF-6). 14 real documents fetched 2026-07-31 across classes (agenda packets + staff reports from oakland.legistar.com via the per-file GUID; ACFRs, budget books, budget exhibits from oaklandca.gov via the honest contact-URL agent), each with source URL. Ran extractPdfStructure (Tier 1, pure JS) on each and read the per-region text.undetermined markers; ran unpdf/pdf.js (Tier 2) on the SAME bytes as an ORACLE so "the residue is recoverable by the pdf-worker" is measured, not assumed. FINDINGS: per-document buckets — Tier 1 FULLY 4/14 (agenda packets, modern ACFR, slide deck), Tier 1 PARTIAL 3/14 (budget books ~88%, transmittal 41%), Tier 2 REQUIRED 5/14 (2 encrypted incl. the 2025 ACFR, 3 fonts-without-ToUnicode; unpdf recovers all measured), OCR-only 2/14 (scanned/image — neither tier). Two failure causes zero out whole documents: permission-only ENCRYPTION (Tier 1 has no decryption; pdf.js decrypts empty-password Standard/AES transparently — measured on the Legistar staff report, 13,012 clean chars) and fonts with no /ToUnicode (Tier 1 refuses to guess; pdf.js maps glyphs — cafr-2003 739 chars Tier1 vs 278,758 Tier2). CONCLUSION: the pdf-worker (CPDF-6) is CENTRAL not marginal — the staff-report class (the substance an agenda links to) and encrypted ACFRs need it; Tier 1 remains valuable, fully serving agendas + modern books/decks (~50% of the sample in-plane, free). Extractor gap recorded: Tier 1 does not NAME encryption (degrades to objstm_undecodable/content_stream_undecodable notes though /Encrypt is in the trailer) — a reason:"encrypted" marker recommended for CPDF-4/6. Runtime caveat for CPDF-6: unpdf/pdf.js threw Math.sumPrecise on one doc on node v26.5.0 — the pdf-worker must pin a Workers-runtime-verified version. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (62a650b). Verdict: pdf-worker (CPDF-6) is CENTRAL (~36% of the class-spread sample needs Tier 2). OCR gap lifted to DEC-4. Probes confirmed not in the battery.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-4
opened: 2026-07-31T00:00:00Z
paths: in bio-plane/src/store.mjs — the archive-fallback MONITORING consumer (CAPTURE's tick logic): the new `#monitorTickMs`/`#monitorToken`/`#monitorConfigured`/`#monitorFloor`/`#monitorPending`/`#monitorTick`/`#fireArchiveFallback` methods and the `MONITOR_TICK_MS`/`MONITOR_TICK_BATCH` constants placed beside the reachability code, and the arm added to `recordSourceOutcome` (made async; arms the scheduler on a COUNTED real failure only, never on `governed` or `success`); AND the one authorised cross-area touch of RECORD's scheduler — appending the `archive-monitor` entry to `#schedConsumers`, and in `onAlarm` awaiting `c.tick(now)` (so an async consumer's work completes within the alarm) and wiring the `archive-monitor` result through the return. bio-plane/test/archive-monitoring.test.mjs (new); its test:archive-monitoring entry and the battery-chain entry in bio-plane/package.json
interfaces consumed: none (I3 op=acquire/op=recordsourceoutcome/op=sourcereach reached, not changed)
interfaces owned: none; no schema table added or reshaped (I5 unchanged)
expected: QUEUE.md CAP-3 — make a periodic MONITORING tick actually INVOKE the archive fallback (built, live-verified, IDLE). RECORD owns the scheduler but is DORMANT and CONDUCT authorised this cross-area touch; kept to appending ONE consumer + arming it (SCHEDULER.md's documented extension) plus the minimal `await` onAlarm needs for an async consumer — no second alarm, no cron, no reshape. The tick consults `sourcereach` for `fallback_eligible` documents (three consecutive REAL source failures, or the aged arm; a governed refusal is our own politeness and moves no counter — D-104, read before touching it) and fires the fallback for each by invoking op=acquire with `via:"archive.org"` and the DOCUMENT address over `env.SELF` under a daemon credential, producing the two-hop grade-C chain from the one acquire path (fence + provenance hop unchanged). INERT unless `env.SELF` (a service binding to this instance's own Worker) and a daemon token are present — the SCHED_PROBE-seam pattern — so every existing suite is byte-for-byte unaffected. accepts-when bio-plane/test/archive-monitoring.test.mjs: three source_refused outcomes drive the tick to fire the fallback (grade C, 2-hop chain, failing run reset by the archive success = the RULED re-fetch-for-monitoring). NEGATIVE CONTROL RUN: make recordSourceOutcome's `governed` branch increment consecutive_failures (drop the D-104 exclusion) -> three governed refusals fire a SPURIOUS fallback; restored, recorded in the suite's first-60-lines NEGATIVE CONTROL comment. Live wiring of env.SELF is per-instance (the instance name IS the worker name) and belongs to the installer/CONDUCT — raised as a decision item. No deploy; CONDUCT integrates and live-verifies.
released: 2026-07-31 — landed on main by CONDUCT (dea4c13). battery 45/45 (2361); the trust-critical governed-refusal exclusion is the EXISTING D-104 mechanism CAP-3 reads (reachability.test 40 + archive-monitoring.test 22 verify it end-to-end, plus the worker's negative control governed→spurious fallback→4 fail). Mechanism proven; LIVE arming is the DELEGATION below + DEC-5.

## DELEGATION 2026-07-31 CAPTURE → DIST
from: capture-agent-4 / CAP-3 (recorded by CONDUCT at integration)
need: the monitoring tick is built and tested but INERT on live instances until `env.SELF` (a service binding to the instance's OWN Worker) and a daemon token are provisioned. The instance name IS the worker name, so the self-binding target must be TEMPLATED per-instance by the installer (newgroup) and the plane deploy — out of bounds for CAPTURE. Until then the archive fallback stays "built and idle" (the consumer no-ops without the binding). Composes with CPDF-6's pdf-worker service binding — the same "how does a Worker call another Worker on this instance" provisioning.
why: the DIST/installer half of making resilience actually run; gated (deploy + installer) and carrying a token-exposure surface — see DEC-5.
status: open
## CLAIM 2026-07-31 CONTENT-PDF
session: content-pdf-agent-7
opened: 2026-07-31T00:00:00Z
paths: pdf-worker/** (new fleet member: src/index.mjs, wrangler.jsonc, package.json, scripts/build.mjs, fleet-member.json, .gitignore, the committed dist/pdf-worker.bundled.mjs, test/pdf-worker.test.mjs); the service-binding wiring in bio-plane/src/index.mjs (op=pdfstructure escalation + the needsTier2 helper — RECORD's I3 control plane, see the DELEGATION note) and bio-plane/wrangler.jsonc (the PDF_WORKER service binding); bio-plane/src/pdfstructure.mjs (CONTENT-PDF's own — Tier-1 encryption detection: isEncrypted + the encrypted text marker); bio-plane/scripts/coverage.mjs (M0-5 fleet enumeration); tests bio-plane/test/pdf-worker-binding.test.mjs and additions to bio-plane/test/pdfstructure.test.mjs; docs INTERFACES.md (I2 text reasons + I6 as-built), MEASUREMENTS.md (append), this CLAIMS entry
interfaces consumed: I1 §2 (the R2 key shape — promoted to a load-bearing dependency with a second consumer); I3 (the op= dispatch — additive: op=pdfstructure gains a Tier-2 escalation, no op renamed, no classes/reason/shape changed)
interfaces owned: I6 (plane → pdf-worker — the worker now EXISTS; as-built shape recorded in INTERFACES.md, still PROVISIONAL pending a DIST deploy and FW-1); extends the provisional I2 text shape with `encrypted`/`over_envelope`/`no_text_layer`/`tier2_extraction_error` reasons and a top-level `tier`
expected: QUEUE.md CPDF-6 + M0-5 — build pdf-worker, the FIRST fleet member (I6): holds unpdf 1.8.0 (PINNED, verified extracting on workerd — its real runtime, where pdf.js Math.sumPrecise resolves; a guarded polyfill covers node), reads bytes from R2 itself (CAPTURES read binding, never PUBLISHED, no STORE), returns the record's I2 structure+text shape, WRITES NOTHING. The plane's op=pdfstructure runs Tier 1 in-plane and escalates over a service binding (PDF_WORKER) only when Tier 1 got essentially nothing (undetermined regions > decoded chars — CPDF-5's encryption + no-/ToUnicode buckets), never for a budget book at ~88%; absent the binding it degrades to Tier 1, named, no crash (an un-fleeted instance, D-115). A document over the envelope (MAX_PDF_BYTES, default 16 MB) returns text-undetermined(over_envelope), never truncated. Tier-1 now NAMES encryption (reads the Standard Security Handler from the trailer without decrypting — the CPDF-5 gap). M0-5: coverage.mjs enumerates fleet members (discovered from fleet-member.json, never hand-listed) and holds each to surface-ops-reached + a declared control. accepts-when ALL GREEN: (1) the plane returns "Hello Oakland 2026" for a no-/ToUnicode CID-class PDF Tier 1 could not decode, through the binding (pdf-worker-binding.test.mjs 12/12); (2) the worker writes nothing — R2 byte-for-byte unchanged + no put/delete in source, no PUBLISHED/STORE binding (pdf-worker.test.mjs 28/28); (3) an over-envelope document returns text-undetermined not truncated; (4) test:coverage lists pdf-worker and its `structure` surface. NEGATIVE CONTROLS RUN 2026-07-31: (a) needsTier2->false -> the two Tier-2 binding assertions fail (10/2), restored 12/0; (b) inject env.CAPTURES.put into the worker, rebuild -> the R2-unchanged + source-scan assertions fail (25/3), restored 28/0; (c) add a stub fleet member with an untested op -> coverage reports it UNREACHED and --strict exits 1, removed -> clean. Battery 45/45 (2360). DELEGATIONS: the op=pdfstructure escalation call site is in index.mjs (RECORD's I3) not CONTENT-PDF's paths — landed here in one turn with the member per the CPDF-6 item, flagged to CONDUCT; and the DEPLOY of the member + binding is DIST's (a new Worker + service binding are a gated release; installer must install the FLEET, D-115/D-116). No push, no deploy.
released: 2026-07-31 — landed on main by CONDUCT (7e5b67f). First fleet member (pdf-worker) + M0-5 fleet coverage; battery 46/46 (2384); write-nothing verified structurally; unpdf pinned for workerd. RECORD I3 (escalation call site) + DIST (deploy/install fleet) delegations recorded; live-verify (CPDF-3) gated.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-2
opened: 2026-07-31T00:00:00Z
paths: the I2 section of docs/development/INTERFACES.md (Status/Version/reasoning); docs/development/INTERFACE-CHANGES.md ONLY IF a counter were needed (it was not — no counter, so this file was not created)
interfaces consumed: I2 (structure -> framework) — the interface being confirmed; read pdfstructure.mjs (producer) and docprofile/** (consumer) to judge it
interfaces owned: I2 (FRAMEWORK is the consumer/owner; this turn is the confirmation that turns it STABLE)
expected: QUEUE.md FW-1 — confirm or counter the provisional I2, the EXTENDED shape (link partitions + text/undetermined markers + tier). Judged as the consumer's owner whether the shape CONTENT-PDF produces serves FRAMEWORK's need to identify content and intent. VERDICT: CONFIRMED, no change. WHY: (1) container-agnostic BY CONSTRUCTION — the four partitions/wrappers are subresources.mjs's own, imported not re-derived, so docprofile's content-type recognition and connections() consume PDF and HTML links through one path (Bob's "content in PDFs as in HTML"); (2) undetermined first-class on BOTH axes (link why + text Marker naming the font) is the never-invent/state-the-gap doctrine docprofile's every default rests on, letting a doctype honestly refuse instead of parsing mojibake; (3) it gives the two doctype inputs — text.document/pages[] for L1/L4/L5 recognition+meaning, links[] partitioned for L6 referential/temporal (intra content-addressed by sha256 = a stronger referential key than an href), keyed on a shared 0-based page index so they compose; (4) tier is stamped at the op boundary the consumer actually reads (op=pdfstructure: 1 in-plane, 2 pdf-worker); (5) the flat no-layout text is the honest Tier-1 maximum — richer positional/table structure is a future doctype-driven ASK via the change protocol, not a reason to hold the contract provisional. Touched NO plane code: the shape confirmed is already the shape the battery pins (pdfstructure.test.mjs partitions/wrapper-byte-identity/counts/text.*, pdf-worker tests text+tier), so a redundant assertion was unwarranted. I2 set to Version 1.0.0 / Status STABLE (matching I1/I3/I6's stable convention). One producer-side residual noted: CONTENT-HTML emitting the same text shape (link parity already by-construction) — CONTENT-HTML's to raise via INTERFACE-CHANGES.md when it activates, not a consumer reservation. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (44a867a). I2 STABLE 1.0.0; docs-only, battery 46/46 unaffected.

## CLAIM 2026-07-31 CAPTURE
session: capture-agent-5
opened: 2026-07-31T00:00:00Z
paths: in bio-plane/src/store.mjs — the reuse-verification helpers ONLY: new `reusedParts` (enumerate a bundle's reused subresource parts by joining `site_asset_refs.reused=1` to `register` and `site_assets`), `recordReuseVerdicts` (append rows to the new `reuse_verdicts` table), `reuseVerdicts` (read them), the POST-HOC verdict write added inside the existing `recordSiteAssets` change-case (phase `posthoc`), the three new DO routes (`reusedparts`/`recordreuseverdicts`/`reuseverdicts`) in the dispatch map, and the `DELETE FROM reuse_verdicts` line in `purge`'s whole-store arm. In bio-plane/src/index.mjs — the `op=ratify` handler ONLY: after publish, re-fetch every reused part with a PLAIN GET bounded by the calibrated `capture_limits` ceiling, record `confirmed`/`changed`/`unavailable`/`not_attempted`, and surface the summary on the response. In bio-plane/src/schema.mjs — ONE new derived table `reuse_verdicts` placed BEFORE the `host_governor` block (I5, additive only). New test bio-plane/test/reuse-ratify.test.mjs; its test:reuse-ratify entry and the npm-test-chain entry in bio-plane/package.json.
interfaces consumed: I3 (the control-plane op= dispatch — op=ratify gains a re-fetch-and-record step, no op renamed, no classes/shape changed) 
interfaces owned: I5 (the store schema — ADD ONLY: one derived table `reuse_verdicts`, named in purge and in the D-113 hygiene coverage, no existing table reshaped)
expected: QUEUE.md CAP-4 / CAPTURE-SCALING.md item 6, the four decided specifics. (a) POST-HOC reuse verification from `site_assets`, unconditional, zero requests, verdicts appended and dated (persisted in the change-case of recordSiteAssets, phase `posthoc`). (b) At ratification re-fetch every reused part — MANDATORY as an attempt and a record, never as agreement: `confirmed`/`changed`/`unavailable` all ratify; ratifying with a reused part while saying nothing is forbidden. (c) A PLAIN GET, not `If-None-Match`: our own SHA-256 over what we received is the evidence where a 304 is only the origin's assertion (reverses the conditional-GET suggestion elsewhere in that document, right for working capture, wrong here). (d) A fourth outcome `not_attempted`, recorded WITH its reason, for parts the invocation's budget (the calibrated `capture_limits` ceiling) could not reach — never silently omitted. accepts-when bio-plane/test/reuse-ratify.test.mjs: a bundle ratified with reused parts records an outcome for EVERY reused part; a dark source still ratifies as `unavailable`; a bundle whose reuse count exceeds the calibrated ceiling ratifies with the residue `not_attempted`. NEGATIVE CONTROL: drop the outcome record for a part and the suite FAILS naming the unrecorded part. D-113 purge/hygiene re-checked green for the new table. No deploy; CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (c7c57c9). battery 47/47; reuse_verdicts in purge, hygiene 138 + purge 14 green; coverage 85/85.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-2
opened: 2026-08-01T00:17:53Z
paths: bio-plane/test/check-firing.test.mjs (NEW — the only file touched). No plane behaviour, no catalog, no other suite.
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-3 — name the 33 checks no assertion named. One assertion per check that TAMPERS a conformant bundle and requires THAT check id in the findings, closing the S-7 direction (a check run only where it passes is unproven). New suite check-firing.test.mjs builds a conformant bundle per object type (the exact field set setup.mjs's mdFor writes, proven clean by conformance.test.mjs), tampers exactly one thing, and asserts BOTH ways: the check FIRES on the tamper AND is ABSENT from the untouched base (the built-in negative control). All 33 named this turn: C-1.3 C-2.1 C-2.2 C-2.3 C-2.4 C-2.6 C-2.9 C-2.10 C-3.1 C-6.3 C-7.1 C-8.1 C-9.1 C-10.1 C-11.1 C-12.1 C-12.2 C-13.1 C-14.1 C-14.2 C-14.3 C-14.4 C-15.1 C-16.1 C-16.2 C-16.3 C-16.4 C-16.5 C-17.1 C-18.3 C-18.4 C-18.7 C-18.8. accepts-when: `npm run test:coverage` reports 51/51 checks named (was 18/51); check-firing.test.mjs 70 pass, 0 fail; battery 48/48 (2493). NEGATIVE CONTROL declared in the suite's first 60 lines and SPOT-CHECKED on C-2.1, C-13.1, C-16.4: removing the tamper (bundle == base) drops the finding and flips `fires` true->false. No push, no deploy — CONDUCT integrates. M0-3 is COMPLETE (no split needed; all 33 landed in one turn).
released: 2026-08-01 — handed to CONDUCT.

## CLAIM 2026-07-31 M0 (VERIFICATION)
session: verify-agent-3
opened: 2026-08-01T00:33:43Z
paths: bio-plane/package.json (the `test` script ONLY — the hand-maintained `&&` chain replaced by `node scripts/battery.mjs`; `test:battery` and every `test:*` alias unchanged); bio-plane/scripts/battery.mjs (skip-awareness — tally reads an optional named skip count, a wholesale `SKIPPED — reason` marker gets its own `skip` status and is surfaced by name in the summary, neither counting as a failure); bio-plane/test/ratify.test.mjs, bio-plane/test/reuse-ratify.test.mjs, bio-plane/test/signpage.test.mjs (each gains a top-of-file `spawnSync("ssh-keygen")` guard that SKIPS LOUDLY with a named reason and exits 0 instead of dying with an unhandled spawn error); bio-plane/test/sshsig.test.mjs (the `else` branch now NAMES and COUNTS the 2 skipped fresh-signature cases on the tail line). Doc corrections of superseded text (same-turn, ORCHESTRATION rule 6): docs/development/VERIFICATION.md (the "battery runs every suite" section + floor item 4) and CLAUDE.md (verification-discipline step 1) — both said `npm test` chains with `&&`, now false. QUEUE.md NOT touched (CONDUCT's sole-writer file — M0-4 `landed:` is CONDUCT's to fill). DEBT.md D-93 NOT closed here (CONDUCT closes at integration).
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-4 — close BOTH halves of D-93. First half: `npm test` becomes the discovering runner, so a crashing suite can no longer hide the suites behind it; both `npm test` and `npm run test:battery` now run every suite and report all of them. Second half: a suite that wholesale-depends on stock ssh-keygen for signing (ratify, reuse-ratify, signpage) detects its absence and SKIPS LOUDLY WITH A NAMED REASON (`name: SKIPPED — ssh-keygen not on PATH; …`) or would fail loudly — never quietly does less; and sshsig, which has a pinned-fixture fallback, runs its 16 pinned assertions and NAMES the 2 fresh-signature cases it skips (`16 pass, 0 fail, 2 skip (fresh signature verifies; fresh signature bound to its bytes; ssh-keygen absent)`) instead of silently reporting 16 (the 16-vs-18 case). Only ratify + sshsig were named in the item; reuse-ratify + signpage share the same wholesale-ssh-keygen dependency and were failing loudly once the runner stopped hiding them, so the accepts-when (green when ssh-keygen hidden) required the same guard — added and claimed here. accepts-when GREEN: normally battery 48/48 (2493 assertions); with ssh-keygen hidden from PATH `npm test` COMPLETES exit 0, 45/48 green + 3 SKIPPED-by-name + sshsig ran-short-by-2-named, every other suite still runs (none hidden). NEGATIVE CONTROLS RUN 2026-08-01, restored, recorded in each suite's first-60-lines comment: (ratify) remove the guard + hide ssh-keygen -> `FAIL ratify.test.mjs ... assertions unknown`, a bare failure with no named reason (other suites still ran — the runner does not hide them); (sshsig) silence the skip note + hide ssh-keygen -> a bare `16 pass` with no hint two cases were skipped, the exact D-93 shortfall. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-08-01 — handed to CONDUCT.
## CLAIM 2026-07-31 RECORD
session: record-agent-3
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/setup.mjs — the installer wizard surface (RECORD/REC-3 scope): `acquireWhy` (delete the stale NO_AUTHORITY branch, D-110) and `mdFor` + its one caller in the `#n-save` handler (emit `content_hash: sha256:<primary>` when a document is attached, D-62). bio-plane/src/index.mjs — the op=promote control-plane body stamp ONLY (~line 2419, beside the existing author/ownerMemberId stamps): server-stamp `surfaced_by` in a focus/problem CREATION's bundle.md from the caller's ACTOR CLASS (viaSession→human, machine→agent), caller value overwritten, bundle.md sha recomputed (D-78). NO store.mjs change and NO schema change — the store keeps byte-trusting bundle.md (40+ suites depend on that; see the surfaced_by writers note below). New tests bio-plane/test/empty-body.test.mjs (D-39), bio-plane/test/setup-honesty.test.mjs (D-110 + D-62), bio-plane/test/surfaced-by.test.mjs (D-78). DEBT.md — D-39/D-110/D-62/D-78 dispositions.
interfaces consumed: I3 (the control-plane op= dispatch — additive: op=promote gains a server-side surfaced_by stamp for focus/problem creations; no op renamed, no classes/reason/shape changed)
interfaces owned: I5 (the store schema — UNCHANGED this turn)
expected: QUEUE.md REC-3 — four honesty defects in the plane's own surfaces. D-39: an empty POST body was found already guarded at the Durable Object (store.mjs fetch, commit 6ac72d0a) so it returns a named JSON reason, NOT a Cloudflare 1101 — the code fix had landed but no suite locked it in and the debt stayed open; empty-body.test.mjs drives an empty POST to the five debt-named ops (stats/index/dangling/searchfields/selectionlist) and asserts a structured reason. D-110: acquireWhy still explained NO_AUTHORITY, the refusal D-97 removed; deleted (grep confirms NO_AUTHORITY existed only there). D-62: the wizard's mdFor omitted content_hash for a document bundle, so C-2.7's verified branch refused it and the hash: facet could not find it; now emits `content_hash: sha256:<primary capture sha>`, mirroring civicos-ui's already-landed port, absent for typed intake where inventing one would be a lie. D-78: both writers hardcoded `surfaced_by: human`; rather than trust a caller-asserted literal (and rather than break the 40+ suites that rely on the store byte-trusting bundle.md — e.g. projection.test asserts an arbitrary surfaced_by projects), the value is now SERVER-STAMPED at the trust boundary in index.mjs, exactly where author/owner/by/viewer already are: a focus opened by a machine credential honestly records `agent`, one opened by a member `human`. This fixes BOTH writers (and civicos-ui) at once with no cross-area delegation, since the store, not the surface, is now authoritative. accepts-when: an empty POST to five ops returns a named reason (not a 1101); a wizard bundle with a document carries content_hash and drops the C-2.7 content_hash finding; a focus written by an agent records surfaced_by: agent. NEGATIVE CONTROLS RUN — recorded in each suite's first-60-lines NEGATIVE CONTROL line. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (a0c6d98). battery 51/51, coverage 85/85. CLAIMS conflict (vs M0-4) resolved union. D-39/62/78/110 closed by worker; I3 additive stamp registered; D-121 opened for the revision-carry residual.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw3
opened: 2026-07-31T00:00:00Z
paths: the op=acquire body in bio-plane/src/index.mjs (the docprofile import at the top, and the profile-writing block just before the acquire response literal — adds a `document.profile` sibling field, changes no existing field); bio-plane/test/profile.test.mjs (new); the test:profile entry in bio-plane/package.json; docs/development/INTERFACES.md (I1 version bump note for the additive `document.profile` field)
interfaces consumed: I1 (bytes → content) — WRITER side (op=acquire), additive
interfaces owned: none (FRAMEWORK owns docprofile, which is READ here, never copied; I1 is CAPTURE's and this is the FW-3 DELEGATION with FRAMEWORK's guidance)
expected: QUEUE.md FW-3 / CONSTRUCTS Step 1 — the plane RECORDS THE PROFILE. op=acquire calls docprofile's identify() (host stack) and doctypeFor() (content type) — the existing functions, not a second copy — and writes onto the acquire document a `profile` field carrying: the handler (key/label/version) and its confidence + signals; the content type (key/label/version) and its confidence + signals; the handler's declared normalisation and boundary; the document kind; the source content-type; and the profiling instant. The recogniser key+version on BOTH axes IS the "author + version" the ladder above needs so a wrong judgment can later be found and revised. This is the FIRST plane consumer of docprofile (until now consumed by zero plane files). I1 VERDICT: ADDITIVE — `document.profile` is a new sibling field on the acquire document; no existing field's shape or meaning changes, and C-18.1 tolerates extra keys (checks required keys only). accepts-when bio-plane/test/profile.test.mjs: an op=acquire of an ASP.NET-WebForms meeting-calendar page carries document.profile naming handler aspnet_webforms + content_type meeting_calendar, both confidences, both signal lists, the normalisation, and the recogniser versions. NEGATIVE CONTROL: stop stamping the handler (delete profile.handler) → the handler assertions fail. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (02491bd), clean cherry-pick. battery 52/52, coverage 85/85. I1 additive bump verified (C-18.1 tolerates extra keys); no INTERFACE-CHANGES protocol needed. No DEC.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw4
opened: 2026-07-31T00:00:00Z
paths: the op=acquire body in bio-plane/src/index.mjs (the docprofile import at the top gains `digests` + `CONFIDENCE`; a new module-level `sha256Hex` helper beside `fingerprint`; and, in the profile block just after `document.profile` is built, capture the primary bytes and set `profile.digests` — the COMPUTED normalisation digests. Adds a sub-field, changes no existing field). bio-plane/checks/bio-checks.mjs — `checkRegisterIntegrity` (C-18.3) ONLY: a SECOND bucket keyed by the determined evidentiary digest, so two register documents whose SUBSTANCE is identical though their raw bytes differ (viewstate/boilerplate) are folded as corroboration, not two review items; the raw-sha arm is unchanged and the normalised arm never fires on two undetermined (null) digests or on a pair already caught by identical raw bytes. New test bio-plane/test/framework-digest-audit.test.mjs; its test:framework-digest-audit entry in bio-plane/package.json. Possibly one line in bio-plane/test/check-firing.test.mjs (a normalised-arm firing case for C-18.3).
interfaces consumed: I1 (bytes → content) — WRITER side (op=acquire), additive: `document.profile.digests` is a new sub-field
interfaces owned: none (FRAMEWORK owns docprofile, which is READ here — `digests()` — never copied; I1 is CAPTURE's and this is the FW-4 DELEGATION with FRAMEWORK's guidance)
expected: QUEUE.md FW-4 / CONSTRUCTS Step 2 — the plane COMPUTES and STORES the normalisation digests on the capture, per the handler's declared normalisation policy FW-3 recorded. The framework defines THREE digests (docprofile/index.mjs, DOCUMENT-PROFILES.md "Three digests, not one"): identity = sha256 of raw bytes = `capture_sha` (I1 §1, REUSED, never recomputed under a new name), rendition = mechanical-normalised, evidentiary = presentational+mechanical-normalised. op=acquire stores `profile.digests = { determined, rendition, evidentiary, boundary_missed, basis }`. `determined` is true ONLY when the bytes were read as text AND the stack was identified with CERTAINTY (a signal only that stack emits): the failure asymmetry runs the OTHER way for deduplication than for change — folding two distinct review items into one corroboration HIDES a document — so unlike compare() the trust is NOT extended to the conservative handler, and an undetermined capture (PDF/multipart/uncertain) records rendition+evidentiary ABSENT (null), never a fabricated value. Then op=audit's duplicate sweep (C-18.3, run by auditPass→checkBundle) gains the normalised comparison so it catches a duplicate whose viewstate/boilerplate differs, which today it cannot see (raw bytes only). The Add-surface already-held check (civicos-ui) is a DOWNSTREAM consumer of the stored digest — recorded, NOT built here. I1 VERDICT: to be confirmed additive (a new sub-field under the existing `document.profile`; no existing field reshaped; C-18.1 tolerates extra keys) → register I1 1.2.0; else INTERFACE-CHANGES. accepts-when bio-plane/test/framework-digest-audit.test.mjs: two op=acquire captures of the same document differing ONLY in viewstate/boilerplate report as duplicates through op=audit (their evidentiary digests are equal though raw shas differ); two genuinely different documents do NOT; two undetermined (PDF) documents do NOT. NEGATIVE CONTROL: drop the normalised-digest write and the sweep misses the viewstate pair again. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (2145416), clean cherry-pick. battery 53/53, coverage 85/85, checks 51/51. I1 1.2.0 additive verified (conformance.test green with real acquire docs carrying digests). FW→UI delegation kept open for when UI activates. No DEC.

## DELEGATION 2026-07-31 FRAMEWORK → UI
from: framework-agent-fw4 (FW-4, CONSTRUCTS Step 2)
need: the Add-surface already-held check in civicos-ui (today it fetches BOTH captures and compares them in the browser) should consume the plane's newly STORED normalisation digest instead. As of FW-4, op=acquire records `document.profile.digests.evidentiary` (a 64-hex sha when `determined:true`, else null), persisted by op=promote into data/provenance.json and registered as I1 1.2.0 §4c. The already-held check can compare a new capture's evidentiary digest against held captures' stored evidentiary digests to recognise "the same document, different viewstate/boilerplate" WITHOUT re-fetching and re-normalising both in the browser — which is the same duplicate op=audit's C-18.3 now folds server-side. Two rules the UI must honour, both structural, not optional: (1) compare evidentiary digests ONLY when both are `determined:true`; a null/undetermined digest is never equal to anything, including another null (an equality that costs nothing to produce is not evidence). (2) `determined:false` (a PDF, a multipart giant, or a merely-likely stack) means fall back to the raw-bytes/current behaviour for that capture, never treat absence as a match or a mismatch.
why: FW-4's scope explicitly RECORDS this consumer and does not build it (QUEUE.md FW-4; the constraint that UI paths are not edited by FRAMEWORK). The stored digest now exists and is stable at I1 1.2.0, so the UI work is unblocked whenever UI activates (M8 / UI-PLAN). No I1 change is needed to do it — the field is already additive and registered.
status: recorded, not started — UI is DORMANT. No plane change owed by this delegation; it consumes an already-landed additive field.
## CLAIM 2026-07-31 M0 (VERIFICATION)
session: m0-agent-m02
opened: 2026-07-31T00:00:00Z
paths: header comments only (first 60 lines) of the bio-plane/test/**/*.test.mjs suites that do not yet declare a negative control — a single `NEGATIVE CONTROL:` line each, comment-only, no assertion or subject changed. Temporary src mutations are made ONLY to RUN each control and are git-restored immediately (no src committed). pdf-worker/test already declares its control (unchanged).
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-2 — backfill the negative-control register across every suite that lacks one (41 of 51 bio-plane suites; the other 10 + the pdf-worker fleet member already declare theirs). RUN each control (break the named subject, run the suite, record the REAL observed failure, restore), never infer it. accepts-when: `npm run test:coverage` reports every suite declaring a control; three spot-checks with their real observed failures in the report. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (0dbcb86), CLAIMS union-resolved. battery 53/53, coverage 53/53 declaring after CONDUCT fixed the two new-suite NC formats. No dishonest suite found. M0-6 (hygiene gate) now UNBLOCKED (M0-1/2/3 all done).

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw5
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (two NEW tables `readings` and `reading_refs` inserted BEFORE the host_governor block, after reuse_verdicts — additive, no existing table changed); bio-plane/src/store.mjs (a `#writeReadings` call inside promote()'s transaction beside the register/refs writes, the `#writeReadings` method, the read methods `readingFor` + `documentsByReference`, two router-map entries `reading`/`readingref`, and the two new tables added to the purge TABLES list for D-113); bio-plane/src/index.mjs (op=acquire: the reading computed from the resolved doctype's parse() over the primary text FW-3/FW-4 already read, carried as a new `document.reading` sibling; two new read ops `reading`/`readingref` in the OPS table + a READING_READS set spread into SESSION_OPS member/admin); bio-plane/test/reading.test.mjs (new) + its test:reading entry in bio-plane/package.json; docs/development/INTERFACES.md (I5 additive version bump to 1.1.0 + the two tables' shape/ownership note).
interfaces consumed: I1 (docprofile is READ at op=acquire — parse() via the resolved doctype — never copied)
interfaces owned/extended: I5 (the store schema) — ADDITIVE: two new derived tables `readings` + `reading_refs`; no existing table's columns change, so no INTERFACE-CHANGES protocol is needed.
expected: QUEUE.md FW-5 / CONSTRUCTS Step 3 — READINGS ARE PERSISTED. op=acquire calls the resolved doctype's parse() on the captured text to produce a reading { entities[], facts } (framework:480), carried on the acquire document; op=promote DERIVES it from data/provenance.json (mirroring refs-from-frontmatter, in the same transaction) and persists it into `readings`, indexing every entity by the RAW source-assigned reference it carries (`ref` = kind:key, e.g. meeting:2101 — NOT resolved to a canonical entity; that is Step 4 / D-83 and is NOT built here). A reading that finds nothing (a doctype with no reader, bytes not read as text, or a reader that found/threw nothing) is persisted HONESTLY as failed/empty (found:false, no entities), never fabricated (framework:489). Reads: op=reading (by capture_sha) and op=readingref (the reverse index, by ref). I5 VERDICT: ADDITIVE (new derived tables only) → I5 1.1.0. accepts-when bio-plane/test/reading.test.mjs: acquire+promote a meeting-calendar document, retrieve its reading (entities[]+facts) by capture_sha, and look it up by an entity reference (meeting:2101) to get the document(s) back — including the cross-document case where two captures both carry the reference; the failed-reading case (generic page / PDF) persists honestly. NEGATIVE CONTROL RUN: dropping the persist makes op=reading return found:false and the reference lookup return count 0 for a document known to carry it. D-113: both tables added to op=purge's whole-store arm; hygiene + purge tests green. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (aee6d52), CLAIMS union-resolved. battery 54/54, ops 87/87 (0 unreached), checks 51/51. I5 1.1.0 additive (two derived tables, purge-covered). No DEC. Cross-document reference lookup confirmed unresolved-but-joinable — sets up Step 4 (D-83).
## CLAIM 2026-07-31 M0 (VERIFICATION)
session: m0-agent-m06
opened: 2026-07-31T00:00:00Z
paths: bio-plane/test/planning-hygiene.test.mjs (NEW — the planning-drift gate); bio-plane/package.json (two scripts ONLY — `test:hygiene` now runs `node scripts/battery.mjs hygiene` so it reports BOTH hygiene suites without &&-stopping, and `test:coverage` now runs `scripts/coverage.mjs --strict` so the floor cannot fall back per VERIFICATION.md step 5); docs/architecture/CONSTRUCTS.md (the "## The plan: bottom up, and each step has a consumer" order-of-work list ONLY — added a status-per-item convention note under the heading and a status marker to each of the 13 steps: Step 0 `BUILT (FW-2)`, Step 1 `BUILT (FW-3)`, Step 2 `BUILT (FW-4)`, Step 3 `QUEUED FW-5`, Steps 4/5/5a/6/7/8/8a/8b/9 `UNSCHEDULED`). NO other file changed — DEBT.md and QUEUE.md carried no drift (all 121 DEBT rows already held a disposition token; the only concrete QUEUED cross-reference, CAPTURE-SCALING's `QUEUED as CAP-4`, already resolves). Temporary DEBT.md/CONSTRUCTS.md mutations were made ONLY to RUN the two negative controls and were reverted (nothing of them committed). node_modules was symlinked from the main checkout for the miniflare battery and REMOVED before commit.
interfaces consumed: none
interfaces owned: none
expected: QUEUE.md M0-6 — the hygiene check that closes the planning-drift CLASS, on D-113's precedent, wired into the standing battery gate so a session that never runs plancheck still cannot drift. planning-hygiene.test.mjs enforces three invariants (MILESTONES.md "How this file stays true" 1/2 + the QUEUED cross-reference the status vocabulary carries): (1) every OPEN DEBT row carries a disposition token — plancheck.mjs's exact predicate, ported so the battery enforces it too; (2) every `QUEUED <ID>` cross-reference names an item that EXISTS in QUEUE.md (ids read from QUEUE.md's own `### <ID> ·` headings, never hand-listed); (3) every design-doc ORDER-OF-WORK item carries a status marker (`BUILT`/`QUEUED <ID>`/`UNSCHEDULED`/`BLOCKED`). Order-of-work lists are a small GOVERNED registry (CAPTURE-SCALING.md "Order of work"; CONSTRUCTS.md "The plan") plus a DISCOVERY GUARD that fails on any new "Order of work"-titled heading not governed or explicitly exempt — so the registry cannot silently fall behind the way D-113's list did. One list is EXEMPT with a reason: CONFORMANCE-AND-INTAKE-ARC.md "5. Order of work" is a CLOSED migration plan (all eight steps executed and superseded); retro-stamping build versions on it would fabricate a ledger it never kept — left tolerant, a judgment for CONDUCT rather than guessed. The load-bearing constraint honoured: the CURRENT estate PASSES every check (the one real drift, CONSTRUCTS's unmarked steps, was fixed mechanically from QUEUE+MILESTONES before the gate went on). accepts-when GREEN: `npm run test:hygiene` runs both hygiene suites (149 + 154 pass); `npm run test:coverage` (now --strict) exits 0 — 54 suites, 54/54 declaring a control (the new suite counted), 85/85 ops, 51/51 checks; battery 54/54 (2730). NEGATIVE CONTROLS RUN 2026-07-31, recorded in the suite's first-60-lines NEGATIVE CONTROL line, each restored: (disposition) strip the M7 token from open DEBT row D-50 -> 2 fail naming D-50; (status marker) strip the BUILT(FW-3) marker from CONSTRUCTS "The plan" Step 1 -> 2 fail naming Step 1. No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (6a72951), CLAIMS union-resolved. CONDUCT updated CONSTRUCTS Step 3→BUILT(FW-5)/Step 4→QUEUED FW-6 markers post-M0-6. battery 55/55, coverage --strict exit 0, hygiene 154 pass. The M0 verification floor is now fully self-enforcing.

## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw6
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (three NEW tables `entities`, `entity_aliases`, `entity_relations` inserted BEFORE the host_governor block, after reading_refs — additive, no existing table changed); bio-plane/src/store.mjs (the registry section: `createEntity`/`addEntityAlias`/`declareRelation` writes, `readEntity`/`entitiesByAlias`/`readRelation` reads, the shared `#entityView` + `#normAlias`/`#cleanLabel`/`#ENTITY_KINDS`/`#RELATION_KINDS` helpers; six router-map entries; the three tables added to the whole-store purge arm for D-113; entity/alias/relation counts added to stats() and to purge's removed{}); bio-plane/src/index.mjs (six new ops in the OPS table — three mutating writes + three reads; a REGISTRY_ACTIONS set spread into SESSION_OPS member/admin; declared_by stamping for the three writes; the three writes named "contribute" in NEEDS); bio-plane/test/entityregistry.test.mjs (new) + its test:entityregistry entry in bio-plane/package.json; docs/development/INTERFACES.md (I5 → 1.2.0 + the three tables' shape/ownership note); docs/development/DECISIONS.md (new DEC-6); docs/development/DEBT.md (D-83 disposition moved to SLICE A LANDED).
interfaces consumed: none (builds on FW-5's reading_refs but resolves nothing yet)
interfaces owned/extended: I5 (the store schema) — ADDITIVE: three new tables `entities` + `entity_aliases` + `entity_relations`; no existing table's columns change, so no INTERFACE-CHANGES protocol is needed.
expected: QUEUE.md FW-6 / CONSTRUCTS Step 4 SLICE A — the SUBJECT REGISTRY, which IS the framework's entity axis, built ONCE (D-83). ENTITIES (kind ∈ the closed UNION of safeguard 4's four subject kinds {source,institution,office,movement} and the framework's entity kinds {person,body,ordinance,parcel,contract,fund}; the union is the reconciliation D-83 demands, and DEC-6 raises the residual ambiguity — whether a bias STATEMENT may take the non-safeguard-4 kinds as a subject — with a provisional that blocks nothing) each with first-class ALIASES (canonical label seeded as one; reverse lookup on a case-folded alias_norm), plus DECLARED RELATIONS (`proxy_for`/`member_of`/`overlaps`) each carrying a NOT-NULL justification + citation "like a pattern statement" and — structurally — NO §8.1 connection grade: a declared relation is CONSTITUTIVE, not evidentiary, so grading it Grade D is the category error D-83 names, prevented by the shape (no grade column) rather than by convention. Ops: entitycreate/entityalias/relationdeclare (writes, declared_by stamped from the session, "contribute"); entity/entitybyalias/relation (reads). RESOLVING a reading_refs reference to a registry entity, declaring the §8.1 method-as-grade, is the NEXT slice and is NOT built here. I5 VERDICT: ADDITIVE (new tables only) → I5 1.2.0. accepts-when bio-plane/test/entityregistry.test.mjs: an entity with aliases + a justified proxy_for/member_of/overlaps relation round-trips and is retrievable BY KEY (op=entity) and BY ALIAS (op=entitybyalias, case/space-insensitive); a read relation carries justification+citation and NO grade key (asserted on the relation read AND the entity view, and a supplied grade is dropped). NEGATIVE CONTROL RUN: dropping the alias persist makes op=entitybyalias return count 0 for a name known to exist (6 fail); dropping the relation persist makes op=relation return found:false. D-113: all three tables in op=purge's whole-store arm, asserted cleared; hygiene (152) + purge tests green. Coverage: ops 93/93 reached (0 unreached), checks 51/51, NC 55/55. No push, no deploy — CONDUCT integrates. node_modules symlinked from the main checkout for miniflare, removed before commit.
released: 2026-07-31 — landed on main by CONDUCT (4037add), CLAIMS union-resolved. battery 56/56, ops 93/93 (--strict exit 0), hygiene 154 (D-83 disposition token intact). DEC-6 (bias-statement entity-kind reach) recorded for bob-session; worker proceeded under the union provisional. I5 1.2.0 additive.
## CLAIM 2026-07-31 UI
session: ui-agent-1
opened: 2026-07-31T00:00:00Z
paths: civicos-ui/app.html (the TASK INBOX surface ONLY — a new `tasks` rail entry in the RAIL array + buildRail, a `renderTasks()` screen and its `go()` router entry, the task kind→label map, and the forward/resolve wiring through recPost; no existing screen's behaviour changed), civicos-ui/test/task-inbox.test.mjs (NEW)
interfaces consumed: I3 (plane → UI op contracts — consumes op=tasks, op=taskforward, op=taskresolve, op=memberlist, op=whoami; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-1 / MILESTONES M8 — the TASK INBOX, the member surface over the already-shipped plane attention layer (D-98). Lists a member's tasks (each pointing at the act it resolves in), forwards (op=taskforward) and resolves (op=taskresolve) through the existing ops. Honours the TASK construct's accountability rules (v0.2): a task ages with a recorded reason and is never silently dropped (age surfaced from `created` + any history ageing event; the ageing JOB itself is D-79/M4 and NOT built here); a task is addressed to somebody or honestly `unassigned`, never a phantom; the refusal shape "this is not yours to resolve, and here is who it is with" is rendered for a task assigned to another member. Points at the acts (openBundle on refers_to); does NOT build the acts. accepts-when civicos-ui/test/task-inbox.test.mjs: lists a member's tasks, forwards one and resolves one through the plane ops, and shows an `unassigned` task honestly; NEGATIVE CONTROL RUN — break the taskresolve wiring and the resolve path fails. Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates.
released: 2026-07-31 — landed on main by CONDUCT (105198b), CLAIMS union-resolved. civicos-ui all harnesses green, plane battery 56/56 (no plane code touched). No DEC. UI→RECORD delegation (server-side task-actor fence) lifted by CONDUCT into REC-4.

## DELEGATION 2026-07-31 UI → RECORD
from: ui-agent-1 (UI-1, the task inbox)
need: consider enforcing the TASK construct's refusal shape at the plane, not only in the surface. `taskResolve` (bio-plane/src/store.mjs ~5299) and `taskForward` refuse NO_ACTOR / NO_SUCH_TASK / ALREADY_RESOLVED, but do NOT refuse a member who is neither the task's `assignee` nor a group admin — any member-class credential can resolve any task by id (the actor is stamped honestly into history, so it is traceable, but it is not prevented). BIO_Interaction_Constructs v0.2 makes "this is not yours to resolve, and here is who it is with" an accountability rule of the construct; the UI renders it (a task held by another member shows the refusal and no resolve/forward control), but the refusal is cosmetic until the plane returns a named reason (e.g. NOT_YOURS with the current assignee) for a non-assignee, non-admin resolve/forward.
why: not blocking — UI-1 shipped the surface against the ops as they are. But an accountability rule enforced only in the browser is one a non-browser caller ignores, which is the class CLAUDE.md's "test through the op" lesson names. RECORD owns store.mjs and I3; this is RECORD's call on whether to add the server-side fence (and what its reason string is, since the UI would then render that verbatim rather than composing its own).
status: open — raised by UI-1, not worked around in the plane (BOUNDARIES: UI owns civicos-ui/** only).

---

## CLAIM 2026-07-31 RECORD
session: record-agent-4
opened: 2026-08-01T02:39:26Z
paths: bio-plane/src/store.mjs [taskForward/taskResolve + the new #refuseNotYours fence helper], bio-plane/src/index.mjs [TASK_ACTIONS in SESSION_OPS member/admin + taskforward/taskresolve in NEEDS as null; the actor-stamp block was already present], bio-plane/test/task-fence.test.mjs [new], docs/development/INTERFACES.md [I3], docs/development/DECISIONS.md [new DEC-7]
interfaces consumed: none
interfaces owned: I3 [additive: new NOT_YOURS reason + session-reach for taskforward/taskresolve → 1.2.0]
expected: REC-4 — the server-side TASK-ACTOR FENCE (lifts UI→RECORD delegation above). taskResolve/taskForward now refuse a caller who is neither the task's assignee nor an admin with NOT_YOURS, naming who it is with (assignee, assignee_role, human detail); an honestly `unassigned` task stays claimable per D-98 routing; the admin override is #isAdminMember (ROOT admin session or any in-app role='admin' member); a machine credential is fenced off assigned tasks (a daemon cannot close a member's work) and confined to unassigned ones. Both ops were opened to member/admin SESSIONS (they were machine-credential-only, which left the Tasks screen's session-token recPost unreachable — DEC-7), NEEDS[op]=null because the authorization is identity not capability (parallels release). accepts-when proven in task-fence.test.mjs THROUGH the control plane with real member/admin sessions (non-assignee nate refused both verbs with NOT_YOURS; assignee mona succeeds at both; admin iris succeeds at both; unassigned stays claimable by nate; a spoofed body actor does not bypass). NEGATIVE CONTROL run (neuter #refuseNotYours → 9 fail). battery 57/57 (2832). coverage --strict exit 0: ops 93/93, checks 51/51, NC 57/57, fleet 1/1. node_modules symlinked from the main checkout for miniflare, removed before commit. No push, no deploy, QUEUE.md landed: left for CONDUCT.
released: 2026-07-31 — landed on main by CONDUCT (edfbea5). Security-sensitive auth change VERIFIED: battery 57/57, coverage --strict 93/93, and CONDUCT independently re-ran the fence negative control (neuter #refuseNotYours → 9 fail, restored → 19). Completes UI-1's loop (the ops now admit sessions). I3 1.2.0. DEC-7 for bob-session.
## CLAIM 2026-07-31 FRAMEWORK
session: framework-agent-fw7
opened: 2026-07-31T00:00:00Z
paths: bio-plane/src/schema.mjs (new derived table `resolutions`, BEFORE host_governor), bio-plane/src/store.mjs (the recognisers: resolveReferences/testifyResolution/#recognise/#upsertResolution/resolutionsForCapture/documentsConcerning + purge TABLES entry + stats/removed; store DO dispatch for resolve/resolvetestify/resolutions/concerns), bio-plane/src/index.mjs (4 new ops + RECOGNISER_ACTIONS session set + scopeFor contribute + resolved_by stamping), bio-plane/test/resolution.test.mjs (NEW), docs/development/INTERFACES.md (I5 1.2.0 → 1.3.0)
interfaces consumed: I5 (own), FW-5 reading_refs + FW-6 entities/entity_aliases (built upon)
interfaces owned: I5 registered here in the same turn (ADDITIVE bump)
expected: QUEUE.md FW-7 / CONSTRUCTS Step 4 SLICE B — the RECOGNISERS. Resolve a raw reading_refs reference (kind:key) to a registry entity and DECLARE THE METHOD, which IS the §8.1 connection grade: A the reference's composite key matched a registered identifier exactly (source's own identifier, both ends captured); B the bare key matched a registered identifier in content; C a name/title matched an entity ALIAS (correspondence — NEVER established, flagged for a member to confirm). A reference matching nothing stays honestly UNRESOLVED (never force-matched). The recogniser (op=resolve) mints only A/B/C, matches an entity's OWN aliases only, and NEVER traverses a declared relation (do not resolve THROUGH a constitutive proxy_for/member_of/overlaps edge, D-83). Grade D is member TESTIMONY (op=resolvetestify — an author + a date, no captured basis), never minted by the machine; the model holds it. Grade is IMPROVABLE: keyed (capture_sha, ref, entity_id), a re-resolution that finds a stronger basis RAISES the grade+method IN PLACE (raised_from records the prior grade), never a second row and never a downgrade. `established` is DERIVED from grade at write (1 for A/B, 0 for C/D) so a C can never read back as established. Delivers the REVERSE INDEX op=concerns — every document that concerns an entity, joined on entity_id — the single largest manual task the framework removes; op=resolutions reads a document's resolutions. I5 VERDICT: ADDITIVE (one new derived table `resolutions`) → I5 1.3.0. accepts-when bio-plane/test/resolution.test.mjs: a reference matching an entity alias resolves at C; one matching a source identifier at both ends resolves at A (composite) / B (bare); op=concerns returns every capture whose reading resolved to X; a reference matching nothing is honestly unresolved; a C raised to A in place when the identifier is later registered (one row, raised_from C). NEGATIVE CONTROLS RUN: (a) #isEstablished("C")=true → a Grade C reads back established (3 fail); (b) INSERT dropped in #upsertResolution → op=resolve reports resolved but op=resolutions/op=concerns empty for a document known to concern the entity (resolutions 3→0, concerns 2→0). D-113: `resolutions` in op=purge's TABLES (clears in BOTH per-bundle and whole-store arms), asserted removed + after=0; hygiene + purge green. Coverage --strict: OPS 97/97 reached (0 unreached), CHECKS 51/51, NC 57/57. Battery 57/57, 2852 assertions. No push, no deploy — CONDUCT integrates; landed: left for CONDUCT. node_modules symlinked from the main checkout for miniflare, REMOVED before commit.
released: 2026-07-31 — landed on main by CONDUCT (2d11c03). index.mjs SESSION_OPS conflict vs REC-4 resolved by union (both TASK_ACTIONS + RECOGNISER_ACTIONS spread into member/admin). battery 58/58, coverage --strict 97/97 (0 unreached), resolution 39 + task-fence 19 green post-merge. I5 1.3.0. No DEC. Step 4 done — the M4 reverse-index capability is live.

## CLAIM 2026-07-31 UI
session: ui-agent-2
opened: 2026-08-01T00:00:00Z
paths: civicos-ui/app.html (the FIRST ACT SURFACE — focus disposition as a JUSTIFIED TRANSITION, ONLY: a `canDispose()` capability gate mirroring `canRelease()`; a self-contained DISPOSE ACT module — `DISPOSE_LEGAL` (the plane's legal-transition table, mirrored with a source cite), `weightLadderHtml()`, `disposePreflight()` (the pure pre-flight), `openDisposeDialog()`/`disposeChoose()`/`disposeValidate()`/`doDispose()`/`disposeReceipt()`/`disposeDone()`/`disposeRefusalHtml()`, plus the dialog CSS; and a Disposition act bar added to openBundle() for focus/problem document pages beside the existing release bar — no existing screen's behaviour changed), civicos-ui/test/act-dispose.test.mjs (NEW), docs/development/DECISIONS.md (new DEC-8 — the ACT construct's "see what it will refuse before it runs" pre-flight-source ambiguity; CONDUCT to apply the three tests at integration and relocate/drop if the repo already answers it)
interfaces consumed: I3 (plane → UI op contracts — consumes op=select to build a single-focus enumerated selection and op=dispose to move its state; consumed, never reshaped)
interfaces owned: none
expected: QUEUE.md UI-2 / MILESTONES M8 — v0.2's FALSIFIABLE TEST: build the queue (UI-1) and ONE act, then re-read the two-construct collapse. The ONE act is FOCUS DISPOSITION (op=dispose to deferred/dismissed, which C-2.8 already requires a reason for — the natural "reasoned" rung) built through the ACT construct + WEIGHT LADDER as a JUSTIFIED TRANSITION (§J). The ONE MOTION rendered exactly and only: (1) CHOOSE the act (defer / dismiss this focus); (2) PRE-FLIGHT the refusal — see WHAT IT WILL REFUSE and WHY *before* it runs (the ACT construct's defining property): the C-2.8 reason requirement (NO_REASON), the reason grammar (BAD_REASON, ≤160 chars no quote/backslash/newline — Store.EDGE_REASON_MAX), and the legal-transition gate (ILLEGAL_TRANSITION / NOT_A_DISPOSITION) computed from the focus's known current_state against the plane's own LEGAL table; (3) AUTHOR the reason — REQUIRED and NEVER prefilled (placeholder hint only, never a value; the surface itself refuses to send an empty-reason act, so the C-2.8 refusal is felt before the plane is even reached); (4) RECEIPT — what the plane returned (the disposed id, the target state, the reason as recorded, weight `refuse`). The act's WEIGHT-LADDER position (reversible · reasoned · terminal · attested) is shown: dispose is **reasoned**. The op=dispose contract has no dry-run, so the pre-flight is computed from the op's declared refusal shape + the focus's known state (honest against what exists); a plane-side dry-run is NOT needed for this act and is noted, not delegated. accepts-when civicos-ui/test/act-dispose.test.mjs: disposing a focus with an authored reason succeeds and yields a receipt (op=select then op=dispose called, receipt rendered from the plane's return); the act is REFUSED with the reason shown when the required reason is absent AND op=dispose is never called (the surface refuses before the plane). NEGATIVE CONTROL RUN — remove the reason-required pre-flight gate in doDispose and the empty-reason act is no longer refused in the surface (op=dispose is now called with an empty reason). Battery untouched (no plane code edited). No push, no deploy — CONDUCT integrates. The two-construct-collapse VERDICT is a reported deliverable.
released:
