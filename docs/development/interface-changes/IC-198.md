## IC-198 · I3 + I5: an image `{part}` on a capture that is not an office container is REFUSED `CONTENT_EXTENT_NOT_A_CONTAINER` (C-45.11); an admitted `{part}` whose bound was not held says so (`undetermined`); `readings` gains the nullable `capture_format` · PROPOSED 2026-09-23 (D-440, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interfaces:** I3 (the op contracts) and I5 (the store schema). **Base read off THIS TREE (`origin/main` @ `0e7cc03e`):
  I3 59.2.0 (IC-191), I5 1.27.0 (IC-190). Read the base AT RESOLUTION.**
- **Proposed classification:** I3 MINOR — 59.2.0 → 59.3.0; I5 ADDITIVE — 1.27.0 → 1.28.0. **Why I3 MINOR and not MAJOR, stated so
  CONDUCT can overrule it:** the one refusal NARROWS what `op=promote`, `op=cite` and `op=contentmint` accept, which is the shape
  IC-188 resolved MAJOR. It is proposed MINOR on the ground that what it refuses was never a legal address in the published
  grammar (IC-125's `{part}` is "the content hash of the embedded media part, for a container", EXTRACTION-BREADTH §3.2) and that
  NO consumer sends it (measured below). If CONDUCT reads any narrowing of an accepted input as MAJOR, it is MAJOR.
  1. **The refusal.** `checkContentExtent`'s image arm refuses a `{part}` when the store resolves the capture as a registered format
     whose registry entry walks no parts (today `html`, `pdf`) — code `CONTENT_EXTENT_NOT_A_CONTAINER`, check `C-45.11`, a new
     sub-number of the C-45 family with a canned translation; the detail names the format and says an image served beside a page
     is its own document to acquire and cite whole (for `pdf`, that an image on a page is addressed by page and rect). Asked BEFORE
     the image-list bound (C-45.1), which is unchanged. At `op=promote` it arrives as a C-45.11 finding; at `op=cite` inside the
     act's refusal, as C-45.1 does. Skipped, never guessed, where the kind is undetermined and in the catalogue's document-only pass.
  2. **The statement (ADDITIVE).** `mintContent`'s answer, and so `op=promote`'s `content[]` entry and `op=contentmint`'s answer,
     carry `undetermined: { level, why }` when an image `{part}` was ADMITTED without its bound: `level: "image_list"` (an office
     container whose image list this record does not hold) or `level: "container_kind"` (a capture whose format this record does not
     hold). Absent otherwise — a checked part carries no key.
  3. **The column (I5, ADDITIVE).** `readings.capture_format TEXT`, NULLABLE AND NEVER BACK-FILLED, added by `CREATE TABLE` on a
     fresh store and by the ADDITIVE_COLUMNS migration on an old one. Written at `op=promote` from the provenance document's
     `profile.format.format` (COFF-1's FORMAT axis: magic bytes first, declared Content-Type second), the SAME document the reading
     is projected from; a re-extraction (which hands no profile) KEEPS the prior value. **`readings` is FRAMEWORK's table**
     (INTERFACES.md I5, "Table ownership"): FRAMEWORK is named here as the owner to answer, and no FRAMEWORK code moves.
- **Proposer:** RECORD, D-440 worker (CONDUCT #16), 2026-09-23.
- **Owner to land it:** `RECORD`.
- **Design:** `docs/development/EXTRACTION-BREADTH-DESIGN.md` §3.2 ("`{part}` is a member of a CONTAINER's own bytes, and nothing
  else") and §3.4; `docs/development/CLIENT-RENDERED.md` "DESIGNED 2026-09-21" (an image served beside a page is its own document).
- **Consumers to answer, MEASURED** (`git grep -n "extent_part\|CONTENT_EXTENT_NOT_A_CONTAINER\|C-45\.11"` over `civicos-ui`,
  `agent-worker/src`, `tools`, `skills`, 2026-09-23): **none sends an image `{part}`** — the only hit outside the plane is the
  DEC-49 guard's own floor notes. In the plane's suites only `fw19-extent-arms.test.mjs` cites a `{part}`, on a DOCX and an XLSX,
  and is GREEN unchanged (both are office containers). No reader selects `readings.*`.
- **What this cannot see, stated:** the format is the provenance document's, which is the caller's, exactly as the reading and its
  image list are; the store does not re-sniff bytes. A capture promoted before this landing whose reading names no container reads
  `container_kind` undetermined (admitted, stated) until its bundle is next promoted.
- **Suite:** `bio-plane/test/d440-image-part.test.mjs` (19 assertions, through the op; NEGATIVE CONTROL on its first line, two arms
  as declared).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MINOR 63.0.0 → 63.1.0; I5 ADDITIVE 1.27.0 → 1.28.0.** Base RE-READ at resolution off `land/conduct/c17-batch2` over main 91bcea6b (I3 63.0.0, I1 1.5.0, I5 1.27.0), in landing order D-440, D-420, REC-179, REC-177, then D-60 and REC-181.
