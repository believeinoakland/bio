## IC-362 · I3: a derivation draft (no `caseId`, no `newCase`) binds to the case publication DERIVES, at any edition; a named case that differs is refused `PUBLISH_DRAFT_CASE_NOT_DERIVED` (C-44.6); the signed block states `draft_case` · ACCEPTED 2026-09-25, MAJOR — I3 134.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-680)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a wire token that moves — a derivation draft named on a further edition was refused `PUBLISH_DRAFT_NOT_THIS_CASE` (C-44.4) whatever case was named; with a named case that is not the derived one it now answers C-44.6 (with `case_id` and `derived_case`), and with none (or the derived one) it publishes. The lifted refusal and `draft_case` (`derived_at_publication` · `named_and_confirmed` · `new_case_asked_at_publication` · `named_by_draft` · `new_case_asked_by_draft`, on op=publish, the re-author splice and op=caseratify) are additive and ride this bump. Proposed as additive; classified by IC-25's moved-token test.
- **The change:** C-44.4 remains for a derivation draft whose findings serve several cases; its translation names that arm (changed). A publisher asking `newCase` passes as before (BOB #35's ruling (a)).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-680's branch stated the interface and left the IC to the integrator.
