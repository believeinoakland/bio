## IC-264 · I5: `case_drafts.statement_by` records who wrote the exclusion statement; the draft doors' author exclusion reads it · ACCEPTED 2026-09-24, MINOR (REC-193, `land/worker/REC-193` @ e6716189; minted by CONDUCT #20 at integration with `node tools/mintid.mjs IC`)

- **Interface:** I5 (the store schema) ADDITIVE, with additive I3 keys. The row named I5 and the worker proposed I5; the case document's bytes do not move.
- **Design:** `docs/architecture/BIO_Publication_v0_1.md` §3 rule 13 (BOB #32, 2026-09-23) — an acknowledgement binds to one case identity, and the statement's writer may not acknowledge it.
- **Proposer:** the REC-193 worker, 2026-09-24. **Owner:** RECORD. **Resolved by:** CONDUCT #20.

### What changes

`case_drafts` gains `statement_by TEXT` (ADDITIVE_COLUMNS, nullable, NEVER back-filled — the only value a backfill could reach for is `updated_by`, the liar being removed). `#caseDraft` stamps it server-side from the session when the statement TEXT changes. `acknowledgeStatement` excludes `statement_by` at both draft doors instead of `updated_by`. `op=reviewcopy` gains `statement_by` and `statement_by_stated`; `op=casedrafts` gains `statement_by` per row; a draft whose author is NULL reads UNDETERMINED and the act is refused by name, `STATEMENT_ACK_AUTHOR_UNDETERMINED`.

### Classification

**MINOR, ADDITIVE** — I5 3.5.0 → 3.6.0 on this integration's base (main's line). The BEHAVIOUR change (who is excluded) corrects a wrong answer. No first-party surface reads the new keys yet (measured by the worker). NOTE FOR c20-batch14: that branch carries I5 3.6.0 (IC-252) and 3.7.0 (IC-255) from batch10's line; the later integration renumbers so I5 reads one linear sequence.
