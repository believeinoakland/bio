## IC-380 · I3: an out-of-inquiry lead's item publishes its inquiry-grain doors (`inquiry_acts`: take up through op=cite, set aside through op=proposedispose) and `options_grain.missing` reads `null` · ACCEPTED 2026-09-25, MAJOR — I3 152.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; REC-202)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning of an existing field, IC-327's shape — `options_grain.missing` read `"inquiry"` on every such item and now reads `null` (nothing at inquiry grain is missing; its `detail` says so). `inquiry_acts` is additive. The one consumer (civicos-ui's lead-inquiry-acts) moved in the same row. Proposed as possibly an act; classified by IC-25.
- **The change:** `#leadInquiryActs` composes the doors from the one `#dispositionOf`, reading nothing.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** REC-202's branch stated the interface and left the IC to the integrator.
