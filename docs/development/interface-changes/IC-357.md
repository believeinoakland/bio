## IC-357 · I3: a `pdf-page` / `image` rect carries its coordinate `space`; any space but user space is refused `CONTENT_EXTENT_NOT_USER_SPACE` (C-45.13), never converted · ACCEPTED 2026-09-25, MAJOR — I3 129.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-670 @ 9385caf7; the row assigned the IC to the integrator; D-670)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — `op=extractpropose` minted an OCR anchor's pixel rect (`space: image-px`) as a user-space rect wherever it fit the MediaBox; it is now refused by name. An unstated space reads as user space and `space` is not in the canonical bytes, so every content id minted before is unchanged.
- **The change:** `readingSource` keeps `space` beside a rect, `legExtent` reads `extent_space`, and `extentCovers` / `readingPositionInExtent` / `extentRelation` (op=narrow) answer no across spaces. Stacked on D-374.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-670's branch stated the interface and left the IC to the integrator.
