## IC-312 · I3: op=monitor on a rendered capture watches the FRAME and states the content undetermined · ACCEPTED 2026-09-25, MINOR — I3 111.6.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-567)

- **Interface:** I3.
- **Classification:** MINOR. additive keys; no refusal added.
- **The change:** A tick on a register row whose pair is rendered compares the served shell with `pair.shell.sha256`; the answer gains `frame` and `content: "undetermined"` with its sentence; a frame match moves no `source_status`.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-567's branch stated the interface and left the IC to the integrator.
