## IC-400 · I2: Tier 1 marks every painted image above a measured floor `image_unread`, with its `rect` and `area_share` and `count: 0`; it is not routed · ACCEPTED 2026-09-25, MINOR — I2 2.16.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-665)

- **Interface:** I2.
- **Classification:** MINOR. additive, IC-351's reading: a new marker reason with two new fields. `text.counts.undetermined` counts it as it counts every marker; the plane's two readers that judge the DECODE by that count (`needsTier2`, `readText`) are handed `decodeView`, so a page of photos neither escalates nor reads as undecodable. Stacked on D-627.
- **The change:** BOB #35 06:25Z: per image, no classifier. Floor 0.001 of the visible box (M-182: 781 bullets at most 0.0000624, the smallest other image 0.0079). Routing stays off: no measured signal separates a chart under a title from a photo page.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-665's branch stated the interface and left the IC to the integrator.
