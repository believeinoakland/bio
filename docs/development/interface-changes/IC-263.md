## IC-263 · I3: NO_BASIS and NO_CITATION gain DEC-49's code, check and canned translation (C-33.40, C-33.41), every existing key unchanged · ACCEPTED 2026-09-24, MINOR (D-484, `land/worker/D-484` @ a834c025; minted by CONDUCT #20 at integration with `node tools/mintid.mjs IC`)

- **Interface:** I3 only. No table changes.
- **Design:** DEC-49, as `docs/architecture/BIO_Assistant_and_AI_Roles_v0_1.md` rule 10 restates it, following REC-79's single-helper shape.
- **Proposer:** the D-484 worker, 2026-09-24. **Owner:** RECORD. **Resolved by:** CONDUCT #20.

### What a caller sees

The seven store sites that refuse NO_BASIS (conclude, the grouping partition, testify's grade-D basis, progressiondefine's revision basis) and NO_CITATION (relationdeclare, progressiondefine, discharge) now route through two governed helpers (`actNoBasis`, `actNoCitation`, each in its own DEC-49 region), so each code has ONE site and one catalogue row: C-33.40 NO_BASIS, C-33.41 NO_CITATION, allocated as dotted members of ACT_SHAPE_CHECKS by the family owner (no new family number). `reason`, each site's own `detail` and every per-site key stay first and unchanged; `code`, `check` and `translation` join them.

### Classification

**MINOR, ADDITIVE** — I3 81.3.0 → 81.4.0 on this integration's base. Nothing that answered is refused; no key is removed or renamed (asserted through the op by the worker's suite). A member now reads the canned sentence where they read the raw `detail`.
