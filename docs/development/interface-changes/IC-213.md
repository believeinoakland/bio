## IC-213 · I8: the run reads DOCUMENT VERSIONS AS VERSIONS — `POST /run` answers a new `holdings` block (documents counted ONCE with their versions), and the member's pinned op set gains two READS, `search` and `versionchain` · PROPOSED 2026-09-23 (D-220, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I8 (plane ↔ agent-worker). **I3 does not move**: both ops exist and neither changes; `op=versionchain` is
  PL-10's (I3 10.2.0) and `op=search` S-10's. I5 does not move.
- **Base read off THIS TREE (`origin/main` @ `91bcea6b`): I8 0.1.0, PROVISIONAL. Proposed ADDITIVE (0.1.0 → 0.2.0).** Nothing
  refused before is refused now and no field is removed or re-meant: `/run`'s answer gains `holdings`, and its `collect`
  observation entry's `note` gains a sentence. Read the base AT RESOLUTION.
- **Proposer:** FLEET's ground, D-220 worker (CONDUCT #17, cloud), 2026-09-23.
- **Owner to land it:** `FLEET`
- **Design:** `docs/development/INVESTIGATIVE-SESSION.md` §3 *"What the session sees"*, *"AND IT MUST READ DOCUMENT VERSIONS
  AS VERSIONS (D-220, Bob 2026-08-06)"*, and its BUILT note.

### The change, on the wire

- **`POST /run` → `holdings`** (the LAST pass's, like `fanout`; `null` when no `collect` ran):
  `{ documents, citations, versions_cited, versions_held, unchained, undetermined, by_document: [{ address_norm,
  versions_held, truncated, cited, versions_cited, cited_not_listed }], unchained_items: [{ citation, address, reason }],
  undetermined_items: [{ citation, at, code, check }], identity }`. `documents` counts each document ONCE; its identity is
  the `address_norm` `op=versionchain` answers with, never a title or a text. `citations_reread` is unchanged and remains a
  count of READS.
- **The `collect` row's plane calls**: per cited address (≤ `CITATIONS_MAX` = 20), one `op=search` (`q=id:"<citation>"`,
  `limit=1`, `facets=none`) and, where the citation resolves to an address, one `op=versionchain` (`address=`, `limit=1000`).
  Both are answered through `planeAnswer`; a refusal is pushed onto the run's `refusals` (in the plane's words) and makes
  that citation's document UNDETERMINED. Each call spends one `runtime` unit, as every plane call does.
- **`PLANE_OPS`** (`agent-worker/src/harness.mjs`) gains `search` and `versionchain`, both `mutating: false`, agreeing with
  the plane's OPS table (asserted in `harness.test.mjs`). An `ai` credential reaches both by `aiTaskScope`'s shape (every
  non-mutating op a member reaches); no task scope must declare them.

### Consumer impact, measured

- **The plane (RECORD, the calling side):** no caller of `/run` exists in `bio-plane/src` (`AGENT_WORKER` occurs 0 times —
  D-260), so nothing reads the new field today.
- **The fleet suites:** `fanout.test.mjs`'s mock answered the two new ops with a 400, which put their refusals at the head
  of `refusals` (3 assertions red); it now answers an EMPTY record's shapes through `test/plane-versions.mjs`.
  `harness.test.mjs`'s exact op-set pin moved nine-named-ten → twelve, corrected in place with its reason.
  `agent-worker.test.mjs`, `cascade.test.mjs` and `wire-vocabulary.test.mjs` unchanged and green.
- **DIST:** the agent-worker bundle and manifest are rebuilt (`dist/agent-worker.bundled.mjs` sha256 `123ca20d…`); the
  deployed member is unchanged until DIST's next release.

### Evidence

`agent-worker/test/versions.test.mjs` (22 assertions): PART A drives `documentHoldings` pure over every outcome; PART B
drives `POST /run` with `search` and `versionchain` answered by the REAL plane over a fixture of three captures of one
address, one capture of a DIFFERENT document sharing its title and summary, and one bundle at an uncaptured address — six
citations read as TWO documents and one item. Its NEGATIVE CONTROL line records three arms and the baseline.

**RENUMBERED at integration by CONDUCT #17, 2026-09-23: IC-205 → IC-213** (minted with `node tools/mintid.mjs IC`, checked absent on every remote land/* ref); IC-205 is D-60's, landed at 02603e88. A second clone-local collision on the same id (COFF-13's became IC-207).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I8 MINOR, ADDITIVE 0.1.0 → 0.2.0** (still PROVISIONAL, as I8 states). I3 does not move.
