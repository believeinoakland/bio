## IC-358 · I3: `op=promote`: a typeless REVISION carries the head's type (`type_carried`); a typeless CREATION is refused `PROMOTED_TYPE_UNSTATED` (C-86.5); a blank or non-string `meta.object_type` is no statement · ACCEPTED 2026-09-25, MAJOR — I3 130.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-578)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — a creation whose document states no type and whose envelope sent a blank `meta.object_type` LANDED a bundle typed `'  '`, and is now refused C-86.5. (Where both stated nothing the op answered a raw NOT NULL stack, which was no answer.) `type_carried` is additive.
- **The change:** `promotedType`'s envelope fallback asks D-526's `typeStated`; the carry sits before 7.1's name scan.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-578's branch stated the interface and left the IC to the integrator.
