## IC-331 · I3 + I5: the render throttle: `op=acquire` refuses a render over the concurrency cap (`RENDER_AT_CAPACITY`, C-83.8) and the drain holds the row `waiting`; table `render_slots` · ACCEPTED 2026-09-25, MAJOR — I3 122.0.0; I5 7.3.0 (MINOR) (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-520)

- **Interface:** I3 + I5.
- **Classification:** MAJOR. I3 MAJOR, IC-25: a refusal where an answer stood — a render asked over `renderConcurrencyCap(env)` (default 10, Cloudflare's stated included concurrency, labelled as theirs) was admitted before and is now refused by name. I5 MINOR: one new table.
- **The change:** `render_slots (slot, admitted_at, expires_ms)` before the `host_governor` block; slots release on report and expire at admission + reservation (45,000 -> 25,000 ms). The navigation bound is 10,000 ms, measured (M-151), and `browserrender.mjs` now holds it.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-520's branch stated the interface and left the IC to the integrator.
