## IC-347 · I3 + I5: a held render is released UNDETERMINED at its `expires` (state `expired`), `op=capturerequests` publishes `render_deferral`, and `op=queue` shows a `render-deferred` condition · ACCEPTED 2026-09-25, MINOR — I3 126.14.0; I5 7.4.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-523)

- **Interface:** I3 + I5.
- **Classification:** MINOR. additive: a new key, a new condition kind, and a new value of `capture_requests.state` for a row the drain previously never released (D-491's bound was unenforced — the defect this fixes); no existing value changes meaning.
- **The change:** BOB #33, 2026-09-24 19:54Z (CLIENT-RENDERED.md). The expiry sweep keeps the code the row was held under.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-523's branch stated the interface and left the IC to the integrator.
