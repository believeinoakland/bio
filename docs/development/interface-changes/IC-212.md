## IC-212 · I4: the plane's deploy metadata carries an explicit `limits.subrequests` (10000), derived from `bio-plane/wrangler.jsonc`, and `deploy.mjs` reads the script's settings back after the upload · ACCEPTED 2026-09-23 (D-54; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration and checked absent on every remote land/* ref; the worker's report proposed it for the integrator to mint and classify)

- **Classification:** MINOR, ADDITIVE — the upload metadata gains `limits`; `derive-bindings.mjs` gains `deriveLimits` (refusing `NO_SUBREQUEST_LIMIT` / `UNKNOWN_LIMIT_KEY`) and `limitsReadBack` (MATCH / MISMATCH / UNDETERMINED); a MISMATCH refuses success (`LIMITS_MISMATCH`). No op, wire or plane refusal code changes.
- **Live half:** DIST's, held by Bob's no-release ruling of 2026-09-23. UNDETERMINED: whether Cloudflare's `/settings` states `limits` once set. The installer half is DIST-7.
- **Evidence:** `deploybindings` (36 arms) and its NEGATIVE CONTROL line (3 arms plus over-strictness, AS DECLARED).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I4 MINOR 2.0.0 → 2.1.0**, on c17-batch3.
