## IC-311 · I3+I5: a bias-debt obligation is settled by a re-run under the current lens or a member's reasoned resolve, each recorded (`op=biasdebtresolve`, `op=biasdebt`, `op=airunopen rerun_of=`) · ACCEPTED 2026-09-25, MINOR — I3 111.5.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; REC-207)

- **Interface:** I3+I5.
- **Classification:** MINOR. additive: new ops, a new optional argument whose refusals reach only a caller who sends it, new nullable columns and a new table.
- **The change:** New ops `op=biasdebtresolve` (member, a REQUIRED reason; C-26.13..C-26.19) and `op=biasdebt` (read); `op=airunopen` takes an optional authored `rerun_of` (refusals C-33.45..C-33.47 only when sent). I5 (6.1.0): `ai_runs.rerun_of`, `bias_debts.settled_kind` (nullable, never back-filled) and the append-only table `bias_debt_settlements`. REC-207's ids were renumbered at integration off D-468's C-26.12 and REC-205's C-33.44.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** REC-207's branch stated the interface and left the IC to the integrator.
