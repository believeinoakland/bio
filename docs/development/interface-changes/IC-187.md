## IC-187 · I3: `op=frontier`'s `truncated` READS `true` ON A FULL RAW FETCH at the document, content and meaning levels — the one over-fetch the three arms share (`Store#frontierPage`) says truncated whenever its raw fetch came back full, for every viewer; an exhausted supply answers as before · PROPOSED 2026-09-23 (D-389, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column). **Base read off THIS TREE (`origin/main` @
  `91913d6b`): I3 57.0.0 (IC-184). Proposed I3 MINOR — 57.0.0 → 57.1.0. Read the base AT RESOLUTION.** **Why MINOR, on IC-114's
  precedent:** every change WEAKENS a published claim. No op, key, class list or refusal moves; one boolean that read `false` now
  reads `true` in exactly one condition — the raw over-fetch (`(cap + 1) * 2` at document and content, `* 3` at meaning) came back
  FULL, so rows beyond it were never fetched. Before, a viewer the §6 fence had narrowed to `cap` or fewer rows read `false` there,
  a coverage claim the reader did not establish (DRIVEN: an uninvited member at `limit=1` received ZERO rows and `truncated: false`
  at all three levels while entitled to a row beyond the fetch). No answer that read `true` now reads `false`; the page itself
  (`looked`) is byte-unchanged. **The one over-report accepted:** a supply of EXACTLY the fetch size also reads `true`. **It leaks
  nothing:** an entitled viewer on a full fetch already read `true`, so on a full fetch every viewer reads the same bit, and no
  count of what was withheld is published.
  **If CONDUCT reads IC-118's rule as applying** (a consumer that treated `false` as "you have every row" now sees `true` and would
  page again, finding nothing more only in the exact-size case), the break is MAJOR; the proposer's reading is that this is the
  safe direction and not a changed meaning.
- **Proposer:** RECORD, D-389 worker (CONDUCT #15, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/OBSERVATION-LOG-DESIGN.md` §5 (the frontier is a view over the log) and §6 (the readers, which
  compute `truncated` from what the viewer may read).
- **Consumers to answer, MEASURED** (`grep` for `frontier` over the tracked sources of `civicos-ui/`, `agent-worker/src`,
  `skills/`, `tools/`): **none calls `op=frontier`.** `civicos-ui/app.html` names "the frontier read" once, in prose telling a
  member where a distinction lives; it issues no call. Only the plane's own suites read the field: `observation-log`,
  `observation-content`, `observation-meaning` (unchanged answers at their fixtures' bounds — their raw fetches never come back
  full), and the new `d389-fullfetch.test.mjs`.

**THE SHAPE.** `store.mjs`: a new private `#frontierPage(level, cap, { limit, subjectKind }, gate)` beside `#frontierLatest` —
fetch `limit` raw rows, gate, cut at `cap`, and claim `truncated: gated.length > cap || raw.length === limit`. The document arm
(`frontier`), `#frontierContent` and `#frontierMeaning` take their `looked` page and its claim from it; each still ORs its own
never-looked / unexplained lists (`never.length > cap || … || latest.truncated`). `#frontierInternet` is not a caller and is not
in the class: it gates inside its statement, so its `cap + 1` fetch is exact.

**NEGATIVE CONTROL** (`node test/d389-fullfetch.control.mjs all` from `bio-plane/`): ALL FOUR AS DECLARED, baseline both ends
10/0 · **nodisjunct (THE ROW'S CONTROL) 5/5, B1 B2 B3 B4 S1 by name** · onearm (THE LIAR: one arm fixed alone) 6/4, B1 B3 B4 S1,
B2 green · overstrict 6/4, C1 C2 C3 S1; `store.mjs` restored byte-identical after each.

**FOR CONDUCT at integration:** classify and resolve this IC against the base read then (I3 57.0.0 or later), move I3 in
`INTERFACES.md`, and re-run the control.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #16 — I3 MINOR, 58.1.0 → 58.2.0.** Base RE-READ at resolution off the
integration tree (`land/conduct/c16-batch1`, cut at origin/main `b41d1edb`, with IC-185 resolved first in the same batch):
**I3 58.1.0**, NOT the proposal's 57.0.0 — IC-186 (at `b41d1edb`) and IC-185 (this batch) landed in between. MINOR on IC-114's
precedent, as proposed: no answer that read `true` now reads `false`, no key, op or refusal moves, and no consumer calls
`op=frontier` (grepped by the worker). The MAJOR reading the proposal states is weighed and not taken. I5 untouched.
