## IC-185 · I3: `op=publishedcase`'s `FINDING_IN_SEVERAL_CASES` CARRIES ITS CANNED TRANSLATION — the refusal a finding id several cases pin receives gains `code`, `check: "C-44.2"` and `translation` (DEC-49), from a new row in `CASE_DERIVATION_CHECKS`; every field it carried is unchanged · PROPOSED 2026-09-23 (UI-81, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (plane → UI, the op contracts). **Base read off THIS TREE (branch `worktree-agent-a306ff29faf082ef3`,
  base `origin/main` @ `4355bfda`): 55.2.0 (IC-182). Proposed MINOR, ADDITIVE — 55.2.0 → 55.3.0. Read the base AT
  RESOLUTION**, by this file's own rule.
  **Why MINOR, by IC-25's test:** nothing that answered before is refused, and no field changes meaning. The refusal still
  says `ok: false`, `reason: "FINDING_IN_SEVERAL_CASES"`, `target`, `cases` (sorted), `memberships` and `detail`, byte for
  byte; it GAINS `code` (the same string as `reason`), `check` and `translation`. The same three keys arrive on the refusal
  at every caller of `Store#resolveOneCase` (the finding-id route and the hash route of `op=publishedcase`, and the store's
  own answer under `result`).
- **Proposer:** UI, UI-81 worker (CONDUCT #15, cloud), 2026-09-23.
- **Owner to land it:** `UI` (the plane half is one catalogue row and one DEC-49 region; RECORD's paths, claimed by site).
- **Design:** `docs/architecture/BIO_Publication_v0_1.md` §3 rule 12 (a finding several cases pin is the normal shape), with
  DEC-49 as `BIO_Assistant_and_AI_Roles_v0_1.md` rule 10 restates it, and IC-74 (the refusal's `cases[]`).
- **Consumers to answer, MEASURED** (`git grep` over the tracked sources of `civicos-ui/`, `agent-worker/src`, `pdf-worker/`,
  `ocr-worker/`, `tools/` and `docprofile/`): the code appears in NO consumer source. Two UI readers take the refusal and both
  key on `cases[]`: `inquiryCasePairs` (UI-80) reads nothing else and is unchanged; `pubOpen` (this item) now renders
  `translation` through `refusalWords`, falling back to `detail` for a plane older than this IC. Plane suites pinning the
  refusal: `caseflip.test.mjs` (reads `[ok, reason, cases]`, 58/58 on this tree) and `multicase.test.mjs` (20/20). No
  suite held the refusal's key set exactly.

**THE SHAPE.** `bio-plane/checks/bio-checks.mjs`: `CASE_DERIVATION_CHECKS.FINDING_IN_SEVERAL_CASES` (C-44.2), `where`
`src/store.mjs #resolveOneCase > is-finding-in-several-cases`. `bio-plane/src/store.mjs`: `#resolveOneCase`'s return is built
through the file's `refusal` helper inside that DEC-49 region. **MEASURED, and it decides what carries the wire:** D-262's
`dec49Attach` (`src/index.mjs`) already decorates every `ok:false` answer whose `reason` has a family row, so the ROW is what
puts the translation on the control plane's wire; with the store's helper removed and the row kept, the UI-81 suite stays
15/15 (control arm F). The helper is what makes the code a literal in the region the DEC-49 guard reads.

**NEGATIVE CONTROL** (`node civicos-ui/test/several-cases-choice.control.mjs` from the repo root): the row's translation
dropped → the guard RED naming `CASE_DERIVATION_CHECKS.FINDING_IN_SEVERAL_CASES has NO CANNED TRANSLATION` (arm A2), and the
suite RED at "DEC-49: the refusal carries" (arm F2).

**FOR CONDUCT at integration:** resolve this IC against the base read then (I3 55.2.0 or later) and move I3 in `INTERFACES.md`;
re-read the DEC-49 guard's `regionLines` on the merged tree (this item adds an 8-line region).
