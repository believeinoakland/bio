## IC-302 · I3: `op=publish` names the draft it publishes (`draft=`), readings taken through it bind to the case it produced, and op=statementack keys a no-case reading by its draft · ACCEPTED 2026-09-25, MAJOR — I3 106.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; REC-217)

- **Interface:** I3.
- **Classification:** MAJOR. a changed answer for the same input (op=statementack's second draft no longer answers `existed`).
- **The change:** `op=publish` takes an optional `draft=` (refusals C-44.3 PUBLISH_DRAFT_NOT_FOUND, C-44.4 PUBLISH_DRAFT_NOT_THIS_CASE, C-44.5 PUBLISH_DRAFT_ALREADY_BOUND, only when it is sent); the link is recorded on `case_documents.draft_id` with the publisher and time, and readings through that draft bind to that case. `op=statementack` at no case identity now keys on the draft, so acknowledging one sentence on two drafts is TWO readings where the second answered `existed`.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** REC-217's branch stated the interface and left the IC to the integrator.
