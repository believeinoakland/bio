## IC-360 · I3: `op=promote` takes `created` and `last_updated` from the DOCUMENT; a non-replay envelope contradicting either is refused `ENVELOPE_DATES_DISAGREE` (C-86.7) · ACCEPTED 2026-09-25, MAJOR — I3 132.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-615)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood (an envelope date contradicting the bytes landed), and a changed meaning — `bundles.created`, `bundles.last_updated`, the manifest row's `created` and a minted content row's `at` were the envelope's and are now the document's (the envelope only where the bytes state none). Replay is exempt from the refusal, not the derivation.
- **The change:** `provenanceChainRebuild` sends the row's dates only where the carried document states none; `op=projectfork` writes the fork's own `created` into its bytes. M-181: 0 of 31 live head rows differ.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-615's branch stated the interface and left the IC to the integrator.
