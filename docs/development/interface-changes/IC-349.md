## IC-349 · I1: an `.odt` export's evidentiary digest is DETERMINED, over `content.xml` with Google's per-export `text:list` ids relabelled (`odt-list-ids v1`); `.odp` stays UNDETERMINED · ACCEPTED 2026-09-25, MINOR — I1 1.12.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-473)

- **Interface:** I1.
- **Classification:** MINOR. additive, IC-245's reading: nothing refused, and captures that read `determined: false` may now read `true`; `basis` names the normalisation.
- **The change:** Measured over a fresh public population two rounds apart (M-167). A continuation (`text:continue-list`) is rewritten to its label, so a list continuing a different list still moves the digest.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-473's branch stated the interface and left the IC to the integrator.
