## IC-184 · I3: A RUN'S BUDGET MOVES ONLY UP, BY WHOLE NUMBERS, AND ONLY ON THE BOUNDS THE CALLER COUNTS — `op=airuntick`'s `consume` takes, per RUN_BOUNDS key, a non-negative safe-integer JSON number, else `AI_RUN_CONSUME_INVALID` (C-22.13); a non-zero figure for a bound the PLANE counts (`mints`, `surfaces`) is refused `AI_RUN_BOUND_PLANE_COUNTED` (C-22.14); one bad figure refuses the whole tick and nothing is written; `op=airunopen`'s `bounds[].consumed` seed is held to the same rule, absent meaning none spent · PROPOSED 2026-09-23 (REC-169, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column). **Base read off THIS TREE (`origin/main` @
  `b5ce975a`, merged with `4355bfda`): I3 55.2.0 (IC-182). Proposed I3 MAJOR, BREAKING — 55.2.0 → 56.0.0. Read the base AT RESOLUTION.** **Why MAJOR, by
  IC-25's test — the ops REFUSE what they accepted:** `op=airuntick` answered `ticked: true` to `consume: { surfaces: -1 }`,
  `{ fetches: -3 }`, `{ fetches: 1.5 }`, `{ fetches: "3" }`, `{ fetches: 1e309 }` and `{ surfaces: 1 }`, and `op=airunopen` opened
  a run seeded `consumed: -10`; each is now refused and nothing is written (no log entry appended, no bound spent, no tick counted,
  the lease not extended). **AN AUTHORITY closing:** the run's own principal — the `ai` credential a bound exists to hold — could
  REFUND a plane-counted bound it had spent and run past it; DRIVEN on the unedited tree, a second question landed under a run
  allowed one (`rec169-consume.test.mjs` ARM D, 13 pass / 25 fail against `b5ce975a`'s sources).
- **Proposer:** RECORD, REC-169 worker (CONDUCT #15, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/INVESTIGATIVE-SESSION.md` §14b item 6 (A RUN IS BOUNDED, AND THE BOUND IS RECORDED), §11 item 5
  rule 2 (the `surfaces` bound, IC-181).
- **Consumers to answer, MEASURED** (`grep` over the tracked sources of `civicos-ui/`, `agent-worker/src`, `tools/`, `skills/`):
  **agent-worker** is the one production caller of `op=airuntick`; it sends `fetches` (a length), `subsessions` (a length) and
  `runtime` (a subrequest count), all non-negative integers, and no `mints` or `surfaces` — unaffected. **civicos-ui/app.html**
  calls neither op (its own comments say so). The UI suites (`ai-session-wire`, `ai-session-context`) tick and seed integers on
  caller-counted bounds only — unaffected, and the UI harness is green on this tree. `bio-plane/test/vf4-live-scratch.mjs` (a live
  instrument, not a suite) sends `consume` as an ARRAY, whose keys (`"0"`) name no bound and were — and still are — skipped: a
  finding, reported with its fix, not changed here.

**THE SHAPE.** `airun.mjs`: `PLANE_COUNTED_BOUNDS = ["mints", "surfaces"]` and `checkConsume(entries, { seed })` — null, or the
refusal for the first `[bound, value]` that may not be written. `store.mjs`: `aiRunTick` asks it AFTER sight, position, the project
gate and status (a caller who may not drive the run learns nothing; an ended run's tick stays its stated no-op) and BEFORE the
transaction; `aiRunOpen` asks it (seed mode) after the skill-version check and before anything is computed or written. The tick's
upsert and the open's insert write the judged value, never `Number(v) || 0`.

**THE WIRE.** `op=airuntick` refused: `{ run, ticked: false, found: true, status, code, check, translation, detail, bound, note }`
(`bound` names the offending key). `op=airunopen` refused: `{ run, started: false, code, check, translation, note }`, the
`started: false` shape the open's other refusals use. Every accepted call is byte-unchanged.

**NEGATIVE CONTROL** (`node test/rec169-consume.control.mjs` from `bio-plane/`, every arm on a COPY of `src/`, real sources
hashed before and after, untouched): ALL NINE AS DECLARED — baseline 38/0 · **drop-check (THE ROW'S CONTROL) 18/20, the refund
arm D1 by name** · clamp (THE LIAR) 18/20 · open-seed-dropped 35/3 · no-negative 30/8 · no-integer 33/5 · no-plane-counted 34/4 ·
census 34/4 · overstrict 38/0.

**FOR CONDUCT at integration:** resolve this IC against the base read then (I3 55.2.0 or later), move I3 in `INTERFACES.md`, and
re-run the control.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #15 — I3 MAJOR, BREAKING, 56.0.0 → 57.0.0.** Base RE-READ at resolution off
the integration tree (`int/c15-batch5` @ `353b3035`): **I3 56.0.0** (IC-183's, resolved first in this same train by id order),
NOT the proposal's 55.2.0. **WHY MAJOR:** IC-25's test — `op=airuntick` and `op=airunopen` now REFUSE a `consume` figure (and an
open's `consumed` seed) they accepted: a negative, non-integer or non-finite figure (`AI_RUN_CONSUME_INVALID`, C-22.13) and
any caller-written figure for a plane-counted bound (`AI_RUN_BOUND_PLANE_COUNTED`, C-22.14). **AN AUTHORITY closing**: a run's
principal could refund what it spent and run past a plane-counted bound (driven on b5ce975a). Said in the merge and sent to DIST.
