## IC-368 · I3: `op=actioncorrespond`: a lifecycle token field (`stage`, `follows`, `outcome`, `due_by`) that is not one lower-case token answers `LIFECYCLE_TOKEN_MALFORMED` (C-94.12), split from `LIFECYCLE_TEXT_UNWRITABLE` (C-94.11) · ACCEPTED 2026-09-25, MAJOR — I3 140.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-688 @ ca653547; the row assigned the IC to the integrator; D-688)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a wire token that moves — `stage=Appeal` answered `LIFECYCLE_TEXT_UNWRITABLE` and now answers `LIFECYCLE_TOKEN_MALFORMED`. Nothing that passed is refused.
- **The change:** Its own DEC-49 region `is-lifecycle-token`; C-94.11 keeps the prose case (changed).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-688's branch stated the interface and left the IC to the integrator.
