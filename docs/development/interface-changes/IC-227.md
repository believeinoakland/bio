## IC-227 · I3 + I5: an exclusion statement's ACKNOWLEDGEMENTS — `op=statementack` (ungated, the review copy's two doors), the signed completeness block's `acknowledgements` on `op=publish`, `op=publishedcase` and `op=reviewcopy`; table `statement_acknowledgements` · PROPOSED 2026-09-23 (D-150; minted by CONDUCT #18 with `node tools/mintid.mjs IC` at c17-batch7, on BOB #32's relay of the worker's report) — the classification and the RESOLUTION are CONDUCT's

- **Interfaces:** I3 (the op contracts) and I5 (the store schema).
- **The change.** The plane half of `BIO_Publication_v0_1.md` §3 rule 11. A NEW op, `op=statementack`, through the review copy's two doors (a recipient by the grant's secret; a joined participant by session). A NEW table, `statement_acknowledgements` (purged). The completeness fields on `op=publish`, `op=publishedcase` and `op=reviewcopy` GROW: the signed completeness block lists the acknowledgements of this statement by anybody but its author (count and list). `[]` means "nobody but the author"; `null` means "the document says nothing", never "nobody". Publication is NEVER refused for want of an acknowledgement. `op=caseratify` and a C-41.10 arm read the same block. The case document stays `/2` for now. Five new refusal codes `STATEMENT_ACK_*` (census 635 → 640 at the merge).
- **Classification: I3 MINOR, ADDITIVE; I5 MINOR, ADDITIVE** (IC-25's test): a new op, new fields on existing answers, a new table; nothing that answered before answers differently, and nothing is refused that was admitted.
- **Consumers:** none yet. The surface is delegated to UI (CLAIMS.md, CONDUCT #18, 2026-09-23).
- **Design:** `docs/architecture/BIO_Publication_v0_1.md` §3 rule 11 and its "As built".
- **Evidence:** `bio-plane/test/d150-statement-acknowledgement.test.mjs` (34/0 on c17-batch7), and its negative control's three arms, recorded on the suite's NEGATIVE CONTROL: line.
- **Proposer:** D-150 worker (CONDUCT #17), 2026-09-23.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #18 — I3 MINOR, ADDITIVE 75.0.0 → 75.1.0; I5 MINOR, ADDITIVE 2.0.0 → 2.1.0** (base read on c17-batch7).
