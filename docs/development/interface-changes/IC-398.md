## IC-398 · I2: Tier 1 marks a page an image fills while its text is a folio `image_content_unread` (routed to OCR as a no-text page) and a page in the measured gaps `image_content_undetermined` (not routed), each with `image_share` and `glyphs` · ACCEPTED 2026-09-25, MINOR — I2 2.13.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-627)

- **Interface:** I2.
- **Classification:** MINOR. additive, IC-351's (D-608's) reading: two new marker reasons with two new fields on them, and more of a page read (its content now OCR'd); no glyph a page held is dropped and OCR output stays machine-read (DEC-4). I9's contract does not move: the plane asks the member for more pages.
- **The change:** BOB #35 05:50Z: two facts, two markers. Thresholds measured (M-178): at most 4 glyphs and a share of at least 0.18; 5-21 glyphs or a lower share read undetermined. `needsTier2` counts the markers as scan markers.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-627's branch stated the interface and left the IC to the integrator.
