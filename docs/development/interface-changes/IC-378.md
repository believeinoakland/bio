## IC-378 · I3: the capture grade below the ceiling is MEASURED for a direct capture: `earned.capture[...].fetch` (from `captured_locators.via`), and `#capturedAt` reads a leg at no less than that letter · ACCEPTED 2026-09-25, MAJOR — I3 147.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-177)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning — a leg on a document this instance fetched `direct` was read at the member's authored letter under the ceiling, and is now read at the measured letter where that is stronger, so the strength walk and the three other leg readers answer a different letter for the same record. The `fetch` entry is additive. Proposed as additive; classified by IC-25 (IC-332's reading: a derived value's rule changed, not its reach).
- **The change:** A capture with no locator row reads exactly as before; D-177's own archive arm (UNDETERMINED) is superseded in the same batch by D-693.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-177's branch stated the interface and left the IC to the integrator.
