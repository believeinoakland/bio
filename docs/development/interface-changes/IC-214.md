## IC-214 · I3: a published case document NAMES THE BIAS LENS it was held to — `bias_manifest` and `bias_manifest_bundles` in its front matter and a `## Bias Manifest` section, stamped before `doc_sha`; `op=publish` echoes `bias_manifest` · ACCEPTED 2026-09-23 (D-84; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration and checked absent on every remote land/* ref, as the worker's report asked)

- **Classification:** MINOR, ADDITIVE — new keys and a new body section on `bio-case-document/2` (format token unchanged; IC-166's bump condition not met: no key removed or changed in meaning); one new answer key on `op=publish`. When nothing is adopted the block states `in_force: false` and "no manifest was in force".
- **Findings placed for SCHEDULER:** the adoption's pin names the PROPOSED bytes, not the adopted head (PL-12, pre-existing; needs a ruling); the C-41 gate does not yet REQUIRE the block (a format bump or a measured zero first).
- **Evidence:** `bio-plane/test/d84-case-manifest.test.mjs` (17) and its NEGATIVE CONTROL line (3 arms AS DECLARED).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MINOR 70.1.1 → 70.2.0**, on c17-batch4.
