## IC-383 · I3 + I5: `op=actionriskpropose` — a machine's or a member's PROPOSAL of an action's risk tier with its basis, labelled machine work and never the tier; `op=projection`'s action block serves `risk_tier_proposals`; table `action_risk_proposals` · ACCEPTED 2026-09-25, MINOR — I3 153.2.0; I5 8.3.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; REC-215)

- **Interface:** I3 + I5.
- **Classification:** MINOR. additive: a new op (NON_ACTS), a new projection field, a new refusal on the new op only (`RISK_PROPOSAL_BASIS_REFUSED`, C-90.6) and a new table before `host_governor`. `op=actionrisktier`'s `BAD_RISK_TIER` keeps its code, C-number and `target`; only its `detail` sentence is reworded (the grammar moved into `#riskTierAsked`).
- **The change:** BOB #33's risk-tier ruling, item 3; `riskProposalLabel` composes the label.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** REC-215's branch stated the interface and left the IC to the integrator.
