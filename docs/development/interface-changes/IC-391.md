## IC-391 · I3: C-4.2 reads an undeclared edge in a document's own `state_history` as STATED (info) where the record's own history holds the same move before the fence; `op=audit` answers `stated_moves` · ACCEPTED 2026-09-25, MINOR — I3 153.10.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-673 @ b8405ed4; the row assigned the IC to the integrator; D-673)

- **Interface:** I3.
- **Classification:** MINOR. a refusal narrowed and none added: an edge the record corroborates moves from error to info; any other keeps the ERROR (the entry's own timestamp is never read for it). `stated_moves { listed, total, note }` is a new key on op=audit.
- **The change:** BOB #35 08:00Z option (b); `#recordedStatePairs` is the census's own pairing, injected into gateFacts and the audit sweep. C-4.2 CHANGED on the catalogue's rule. Stacked on D-546.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-673's branch stated the interface and left the IC to the integrator.
