## IC-379 · I3: an archive-only capture earns `C`, MEASURED (`ARCHIVE_CAPTURE_GRADE`, one rank below the direct ceiling): its leg is read at that letter from below and, where every capture sits on a measured route (`whole`), from above · ACCEPTED 2026-09-25, MAJOR — I3 148.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-693)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning — a leg on a document held only through an archive replay was read at its author's letter under the direct ceiling and is now read at C from above as well as below (BOB #35 07:55Z). `fetch.archive`, `unrecorded`, `earned_via` and `whole` are additive. Stacked on D-177; D-698 moves the constant into the catalogue (same letter, no IC).
- **The change:** `captureBound(chain, ARCHIVE_CAPTURE_GRADE)`, so an unmeasured transcription still bounds it to nothing; a via no ruling names stays `CAPTURE_GRADE_VIA_UNRULED`.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-693's branch stated the interface and left the IC to the integrator.
