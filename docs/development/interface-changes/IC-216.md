## IC-216 · I3: ONE CAPTURE, ONE HOME — `op=promote` refuses a register entry whose `capture_sha` is registered under ANOTHER bundle that still exists: `CAPTURE_HELD_BY_ANOTHER_BUNDLE` (C-53.13), naming the holder only to a caller who may see it · ACCEPTED 2026-09-23 (D-179; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration and checked absent on every remote land/* ref, as the worker's report asked)

- **Classification:** MAJOR, BREAKING (IC-25: the op refuses what it accepted). No replay exemption, as ruled (Intake Doctrine §8, BOB #26). A same-bundle revision is unchanged; a purged home's bytes register afresh.
- **Consumer impact, measured:** one fixture in the whole battery (observation-log, CORRECTED). Not measured: CAPTURE-lane callers and migrations that register one capture under two bundles. A live store may already hold such rows; which bundle held them first is undeterminable (for DIST/RECORD).
- **Evidence:** `bio-plane/test/d179onehome.test.mjs` (16) and `d179onehome.control.mjs` (drop / nojoin / disclose / sameowner, AS DECLARED).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MAJOR 70.3.0 → 71.0.0**, on c17-batch4.
