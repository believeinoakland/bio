## IC-230 · I3 + I5: an action's governing laws — `op=actionlaws` (a member's act), `vocabularies.law_levels`, `action.governing_laws` on `op=projection`; frontmatter `governing_laws[]`/`_by`/`_at`, which `promote` now refuses to set outside the act · PROPOSED 2026-09-23 (D-149; the worker left the IC to the integrator, and CONDUCT #18 minted it with `node tools/mintid.mjs IC` at c17-batch7) — the classification and the RESOLUTION are CONDUCT's

- **Interfaces:** I3 (the op contracts) and I5 (the action document's frontmatter, judged by C-2.10).
- **The change.** A NEW act `op=actionlaws`: a member sets an action's `governing_laws[]` of `{level, citation}` (level one of federal/state/local). It replaces the whole list and stamps `governing_laws_by`/`_at` from the session; the Session Log keeps what it replaced. A machine credential is refused `MACHINE_CANNOT_SET_LAWS` (C-32.18). Shape refusals `NO_LAWS`, `BAD_LAW_LEVEL`, `BAD_CITATION`, `TOO_MANY_LAWS` (C-73.2–.5). `law_levels` is published in the vocabularies, and `actionlaws` joins the action's acts (an action now publishes three). `op=projection`'s `action.governing_laws` is read by the one catalog reader `governingLawsOf`: `stated`, or `undetermined` with a sentence that is never federal. `cpra_request` is unchanged. **`promote` REFUSES a creation or revision that sets or changes the list without the act: `GOVERNING_LAWS_REWRITTEN` (C-73.1).**
- **Classification: I3 MINOR, ADDITIVE; I5 MAJOR, BREAKING** — the I5 half is a refusal where none stood before (a document carrying `governing_laws` promoted before this change and is refused after), which IC-25 settles as breaking whatever the measured impact. Measured by the worker: zero consumers — no writer in the tree (`app.html`, `setup.mjs`, `newgroup`) emits `governing_laws`.
- **Design:** `docs/architecture/BIO_Case_Making_v0_1.md` §2, *A RECORDS REQUEST NAMES EVERY LAW THAT GOVERNS IT*.
- **Evidence:** `bio-plane/test/d149-governing-laws.test.mjs` (48/0 on c17-batch7); negative control arms A–D recorded on the suite's line (A NOT AS DECLARED in two §3 arms: the declaration, not the arm, was wrong, as the worker stated).
- **Not built:** a machine PROPOSAL of the list; the member surface (registered in ACTS_AWAITING_SURFACE); a law-neutral `records_request` kind is PROPOSED, not decided.
- **Proposer:** D-149 worker (CONDUCT #17), 2026-09-23.

**RE-RESOLVED 2026-09-24 by CONDUCT #19 at c19-batch9: +2 I3 majors, the security train (IC-237, IC-238) having landed first; the line below carries the new figures.**

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #18 — I3 MINOR, ADDITIVE 77.3.0 → 77.4.0; I5 MAJOR, BREAKING 2.2.0 → 3.0.0** (base read on c17-batch7).
