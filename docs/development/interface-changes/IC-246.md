## IC-246 · I3: `op=groupidentity` publishes the bound on its check log; `op=statementack` REFUSES over the bound on the unsigned documents it re-authors (C-82.1) · PROPOSED 2026-09-24 (c19-unionfix at integration, CONDUCT #19's spec; minted with `node tools/mintid.mjs IC`, a CAS push to coord — the local "IC-240" of c19-capfix's container collides with REC-187's IC-240 and is not used)

- **Interface:** I3 only. No table changes (I5 untouched).
- **Design:** `docs/architecture/BIO_Publication_v0_1.md` §3 rule 11 (the acknowledgement) and §7 points 2 and 3 (the group identity). **DESIGN GAP:** neither section states a bound; the figures below are the ones the first cuts carried as SQL literals (20, 8), now named and published.
- **Proposer:** c19-unionfix, 2026-09-24. **Owner:** RECORD. **CONDUCT resolves the version.**

### I3 — what a caller sees

- **`op=groupidentity`** (a credentialed reader's projection): `domain_checks` is the newest `GROUP_DOMAIN_CHECKS_MAX` (20) checks, newest first, read at max + 1; the answer ADDS `domain_checks_limit` and `domain_checks_truncated`. It was a bare `LIMIT 20` with no way to tell twenty checks from twenty of many. The PUBLIC projection is unchanged. Additive.
- **`op=statementack`**: the unsigned case documents one acknowledgement re-authors are read under `STATEMENT_ACK_DOCUMENTS_MAX` (8) at max + 1, BEFORE anything is written. **Over the bound the act is REFUSED** `STATEMENT_ACK_DOCUMENTS_OVER_BOUND` (C-82.1, new family `STATEMENT_ACK_CHECKS`, canned translation), carrying `{ limit, statement_sha, edition, case_id }`, and NOTHING is written — no acknowledgement row, no document re-authored. It was a silent `LIMIT 8` cut: the ninth document kept listing fewer second readers than the record held, and its owner would sign that absence. A success ADDS `case_documents_limit` and `case_documents_truncated: false`.
- **A second defect corrected in the same read:** D-150 filtered the producing project AFTER the limit, so another project's documents of the same sentence could fill the page and crowd this project's out. The project line (`\ncase_project: <id>\n`, as `#caseDocumentText` writes it) is now matched in the statement; the frontmatter parse still decides, applied after the bound.

### Classification (the proposer's; CONDUCT decides)

- **I3: MAJOR, BREAKING.** A request that answered `ok: true` (an acknowledgement over more than eight unsigned documents of one statement) now REFUSES — breaking by IC-25's test however rare. Measured consumer impact: no UI surface calls `op=statementack` at all (`git grep statementack` over `*.mjs`, `*.md`, `*.html`, 2026-09-24, outside the plane, its suites, the refusal-code guard and the design and interface documents), and no suite drives more than eight such documents except block 8, written for this. The groupidentity half is additive. Proposed on I3 81.0.0.

### Evidence

`bio-plane/test/bounds.test.mjs` (193 pass): op=groupidentity's real bite (an enrolled administrator's 21 claims, every verdict `undetermined`, nothing fetched), whole, DELTA, public projection unchanged; statementack's named-constant arm and envelope; both in DRIVEN_ELSEWHERE. `bio-plane/test/d150-statement-acknowledgement.test.mjs` block 8 (40 pass): nine unsigned documents refused with limit 8 and nothing written; one signed, the act lands and re-authors exactly the eight remaining; the other project's document untouched; DELTA. Controls, each recorded on its suite: (gi) the literal `LIMIT 20` back -> 191/2 (the bite, the DELTA); (sa) read AT the bound, a silent cut -> 36/4 (refusal, nothing-written, at-bound, DELTA); (proj) the project matched only after the read -> 38/2 (at-bound, DELTA).
