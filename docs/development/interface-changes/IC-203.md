## IC-203 · I1: A PDF READING'S `container_extent` GAINS ITS IMAGES — `op=acquire` persists the structure op's painted-image placements as `container_extent = {container: "pdf", levels: ["images"], images: [{page, rect}] | null, images_why?}` where it wrote `null` · PROPOSED 2026-09-23 (D-420, minted with `node tools/mintid.mjs IC` BEFORE the entry was written; IC-193..IC-199 were stepped over because other clones' landing branches already carry them) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I1 (the acquire document and the reading it carries). I5 does not move (no table, no column: the level rides `readings.reading`'s JSON, CAP-12's reasoning unchanged).
  **Base read off THIS TREE (`origin/main` @ `0e5f7054`): I1 1.5.0 (IC-124).** **Proposed I1 MINOR, ADDITIVE** — 1.5.0 → 1.6.0 on this base; read the base AT RESOLUTION.
  **Why ADDITIVE:** a key that was present-and-NULL on a PDF reading is now present and an OBJECT in the shape CAP-12/FW-19 already defined for containers (`container`, `levels`, one level per itemised notion). No other reading changes: an office container's extent is byte-identical (the PDF branch runs only when no office level was itemised), an HTML reading still carries no key, and a PDF whose structure op did not answer still stores NULL.
- **The shape:** `images` is the structure op's `image {page, rect}` placements (CPDF-18), `page` 0-based and `rect` `[x0,y0,x1,y1]` in default user space at 1/1000 pt, in painting order, ONLY those two fields. **IC-124's absence rule:** an EMPTY list is a MEASURED ZERO (every page walked, none painted an image) and bounds; NULL with `images_why` (the producer's reason, e.g. `encrypted`) is a walk that did not finish and bounds nothing. EXHAUSTIVE OR NULL: one placement unreadable as `{page, rect}` makes the whole list NULL.
- **Consumers, MEASURED (grep for `container_extent` across the tree, excluding `node_modules`, `dist/` and control pens):** ONE in code — `Store#containerExtentForCapture` (`bio-plane/src/store.mjs`), changed in the same landing to read it. Fixtures that hand in `container_extent: null` (`capture-text-index`, `passage-arm`, `passage-axis-probe`, `civicos-ui/test/passage-surface`) now model a PDF acquired before D-420 and keep passing unchanged. `release/` and `newgroup/src/release.mjs` hold the released bundle's bytes, not a consumer. `capture-container-extent.test.mjs`'s PDF pin `[true, null]` was CORRECTED (not exempted) with the reason at the site.
- **Captures acquired before this change** carry `container_extent: null` and are NOT backfilled; the store states the absence (`page_images_why`) and admits their page-form image rows (IC-204). Re-acquiring a document records the list.
- **Proposer:** CAPTURE with RECORD, D-420 worker (CONDUCT #16, cloud session WORKER D-420), 2026-09-23.
- **Owner to land it:** `CAPTURE`
- **Design:** `docs/development/EXTRACTION-BREADTH-DESIGN.md` §3.2 (the `image` reference's `{page, rect}` form; `covers` refuses from the container's own extent) and §3.3 item 2 (a PDF's image objects and their painted rectangles). Front matter updated in this landing.
- **Evidence:** `bio-plane/test/d420-image-page.test.mjs` §1 (through `op=acquire` in miniflare: the fixture's hand-derived rectangles, the pages that paint nothing, an encrypted PDF persisted NULL with `encrypted`); negative control `nc-d420.mjs dropwire`.
