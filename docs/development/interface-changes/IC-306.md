## IC-306 · I3: a statement acknowledgement's `at` is stamped to the MILLISECOND; the record's stamps go through one helper naming its precision · ACCEPTED 2026-09-25, MAJOR — I3 110.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-543)

- **Interface:** I3.
- **Classification:** MAJOR. an existing field's spelling changes; a consumer comparing it as a string reads a different order.
- **The change:** `stampInstant("second"|"millisecond")` and `instantOrder` in store.mjs; every site keeps its precision but op=statementack's `at`, which moves from `...:00Z` to `...:00.123Z` so it orders with the review copy's other acts.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-543's branch stated the interface and left the IC to the integrator.
