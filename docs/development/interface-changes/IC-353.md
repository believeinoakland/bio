## IC-353 · I9: the OCR member decodes a baseline DCT page in-isolate (`image.route: decoded-dct`, upright); a non-baseline JPEG is refused by name (`PAGE_NOT_RENDERABLE`, `render.reason: UNSUPPORTED_JPEG_PROCESS`) · ACCEPTED 2026-09-25, MAJOR (breaking under PROVISIONAL) — I9 0.3.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-320)

- **Interface:** I9.
- **Classification:** MAJOR (breaking under PROVISIONAL). IC-25: a refusal token moves — a DCT page the member could not decode answered `PIXELS_UNREADABLE` and now either transcribes (baseline) or answers `PAGE_NOT_RENDERABLE` with the renderer's reason; under PROVISIONAL the minor moves (IC-282's precedent). I6 does not move: `renderPageToPixels(…, { decodeDct })` is a module option whose default (pass-through) is unchanged, not part of I6's registered contract.
- **The change:** `pdf-worker/src/dctdecode.mjs` is a baseline decoder to libjpeg's integer arithmetic, so `pixels_sha256` equals Pillow/libjpeg-turbo's (M-163).
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-320's branch stated the interface and left the IC to the integrator.
