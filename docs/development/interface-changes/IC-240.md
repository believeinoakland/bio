## IC-240 · I3: `op=resolve` takes a SET as `items` under the `per-item` weight, and `op=affordances`' `set_acts` publishes it · PROPOSED 2026-09-24 (D-291; id minted with `node tools/mintid.mjs IC` by the D-291 worker against this clone's ledger — the ledger is NOT exclusive across cloud clones, so CONDUCT renumbers on a collision and resolves the version)

- **Interface:** I3. I5 does not move (no table, no column).
- **Design:** `docs/architecture/BIO_Interaction_Constructs_v0_1.md` §S "SELECTION-SCOPED ACTION — how any act goes bulk,
  safely", named by BOB #32's ruling of 2026-09-23 23:30Z (cite until folded); DEC-69 as amended; the weight is D-126's
  (`NOTIFICATIONS.md` §Applying a handler to a selection, IC-235).
- **Rests on:** IC-235 (D-126's `store.mjs #perItem`, `PER_ITEM_CHECKS` C-75, `set_acts`). This branch MERGES
  `land/worker/D-126` @ `168895a4` because D-126 is integrated on `land/conduct/c17-batch7` and not on `main`.
- **What changes on the wire (ADDITIVE; the proposer's classification is MINOR — CONDUCT decides):**
  1. **The set form.** A body carrying `items` (an array of objects, each `{ captureSha }` or `{ captureSha, ref }`) is
     the per-item act. Every item is resolved by `#resolveOne`, the SAME code the single form runs; a top-level `ref` is
     shared and an item may override it; the control plane's `resolvedBy` stamp is forced onto every item. At most
     `PER_ITEM_MAX` = 100 items.
  2. **The answer** is IC-235's shape exactly: `{ op: "resolve", weight: "per-item", count, applied, retained, items:
     [{ index, outcome, asked, ...that document's own op=resolve answer }] }`; `ok: true` only when every item applied,
     otherwise `SET_ITEMS_RETAINED` (C-75.5) with `items[]` beside it. The applied documents stand.
  3. **The connection sweep** (REC-5 / D-122) is armed ONCE per set, and only when an applied item inserted or raised a
     resolution — the single form's own rule, asked of the set.
  4. **`op=affordances` (no target) `set_acts`** gains a fourth row: `{ id: "resolve", label: "Resolve the selected
     documents' references", weight: "per-item", set_key: "items", item_keys: [["captureSha"], ["captureSha","ref"]],
     shared_keys: ["ref"], max_items: 100 }`.
- **No new refusal code.** An item is refused by op=resolve's own (`NO_SHA`, `NO_REF`, `NO_SUCH_REFERENCE`); the set by
  C-75's.
- **Unchanged:** a body with no `items` gets the single act, byte-for-byte (driven: `resolveset.test.mjs` block 3).
- **Consumers, measured:** `civicos-ui/app.html` — the subject page's resolve list (`resolveCandPaint`,
  `resolveApplySet`) reads `set_acts` off the act source it already loads and sends ONE call per selection. The queue's
  `queueSetActsFrom` keys its bulk controls by op id (`taskresolve`, `proposedispose`), so a fourth row draws nothing
  there (measured: `queue-peritem.test.mjs` green). Suites whose reading of the source had to move:
  `bio-plane/test/peritem.test.mjs` block 8 (the exact `set_acts` list, CORRECTED with a dated reason) and
  `civicos-ui/test/member-respect.test.mjs` (ARM 4d's `resolve` carry STRUCK and the set form measured; the SETS row
  re-keyed to `resolveCandPaint` and marked bulk, CORRECTED with a dated reason).
- **Proposer:** D-291 worker (CONDUCT #19, cloud), 2026-09-24. **Owner:** RECORD (I3).
- **Evidence:** `bio-plane/test/resolveset.test.mjs` (22; control: set branch deleted -> 15 named fails);
  `civicos-ui/test/resolve-set.test.mjs` (17, real plane) and `resolve-set.control.mjs` (baseline + 3 arms, the row's
  client-side-loop arm among them, every arm as declared).
