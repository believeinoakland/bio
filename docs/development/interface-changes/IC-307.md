## IC-307 · I3+I5: a rendered capture records a SHA-256 for every subresource it loaded, or `undetermined` with its reason · ACCEPTED 2026-09-25, MAJOR — I3 111.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-529)

- **Interface:** I3+I5.
- **Classification:** MAJOR. a value's meaning changes (`null` -> `undetermined`/a digest).
- **The change:** `render.data[].sha256` is a digest over bytes the plane kept, or the word `undetermined` with a reason, where it was `null`; additive keys beside it (I3 on op=acquire's document; I5 in the stored render record). BUILT against the injected renderer and fake binding, not yet seen against a live browser.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-529's branch stated the interface and left the IC to the integrator.
