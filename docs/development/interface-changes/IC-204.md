## IC-204 · I3: AN `image {page, rect}` CITATION WHERE THE PAGE PAINTS NO IMAGE IS REFUSED — `CONTENT_EXTENT_NO_IMAGE_PAINTED` (C-45.12) at the content mint — DRIVEN through `op=promote`'s basis legs and `op=contentmint`; `op=cite` and the transcribe act run the same checker and are NOT driven here; a page-form image admitted WITHOUT that bound carries `image_bound` on the mint's answer · PROPOSED 2026-09-23 (D-420, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). The refusal is `checkContentExtent`'s, so every writer that runs the one extent checker carries it.
  **Base read off THIS TREE (`origin/main` @ `0e5f7054`): I3 60.0.0 (IC-192).** **Proposed I3 MAJOR, BREAKING** — 60.0.0 → 61.0.0 on this base. I3 WILL MOVE before this resolves (D-440 proposes a refusal in the same family from the same base); **read the base AT RESOLUTION.**
  **Why BREAKING:** a request that answered `ok: true` is now refused: an `image` extent in its page form whose `rect` equals no placement the record holds for that page (equality is the crop's — within 1/1000 pt, corners normalised), or whose `{page}` alone names a page painting none — on a capture whose reading holds a PDF placement list (IC-203). IC-176/IC-177's precedent: a refusal where there was an acceptance is a break however few callers meet it.
  **The ADDITIVE half:** when a page-form image is ADMITTED because no list is held (a PDF acquired before IC-203, a walk that did not finish, a capture that is not a PDF), the mint's answer carries `image_bound: {determined: false, empty_level: "the images this capture's pages paint", why}` — on `op=promote`'s `content[]` entry and on `op=contentmint`'s answer. Absent when the bound was held and passed.
- **The row:** `CONTENT_EXTENT_CHECKS.CONTENT_EXTENT_NO_IMAGE_PAINTED`, `C-45.12`, `where: checks/bio-checks.mjs checkContentExtent > is-content-extent`, with its canned translation (DEC-49). C-45.12 because D-440 holds C-45.11 in the same family; if D-440 does not land first the number stays (a gap costs nothing). The DEC-49 guard's floors were moved from its own print (rows, census, reach +1; regionLines +6; codesChecked, refusalsJudged +1), and `regionLines` is a property of the MERGED source — re-read at integration.
- **Consumers, MEASURED:** `civicos-ui/app.html` names no `CONTENT_EXTENT_*` code and reads no `image_bound`; the refusal reaches a member through the generic translated-refusal path (R1 reach, translated). Suites minting page-form image rows (`cpdf18-pdf-images`, `rec121-chain-bytes`, `rec127-cap-bytes`, `fw19-extent-arms`, `transcribe`) re-run GREEN unchanged: every rect they mint is either a real placement or on a capture holding no list.
- **Proposer:** CAPTURE with RECORD, D-420 worker (CONDUCT #16, cloud session WORKER D-420), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/EXTRACTION-BREADTH-DESIGN.md` §3.2 (*"`covers` … each refused from the container's own extent, by name"*) and §3.3 item 2.
- **Evidence:** `bio-plane/test/d420-image-page.test.mjs` §2–§4 (through the op: equal mints, far and one-point-off refused C-45.12 by name, page-alone on a blank page refused, pre-D-420 PDF admitted with `image_bound`, crop and mint agree); negative control `nc-d420.mjs` — `droppage` (the row's), `widetol` (how a liar passes), `absentrefuses` (over-strictness), all AS DECLARED.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MAJOR, BREAKING 63.1.0 → 64.0.0** (the mint refuses what it admitted). `image_bound` is to be withdrawn for `undetermined` by CPDF-22 (DONE: IC-222, 2026-09-23, I3 73.0.0) (BOB #31, 20:02Z); no client may read it meanwhile. Base RE-READ at resolution off `land/conduct/c17-batch2` over main 91bcea6b (I3 63.0.0, I1 1.5.0, I5 1.27.0), in landing order D-440, D-420, REC-179, REC-177, then D-60 and REC-181.
