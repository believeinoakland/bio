## IC-259 · I3: the gate stamp's catalogue version moves 1.20.0 → 1.21.0 (`gateVersion` / `gate_version` reads `plane-gate/1.0 (bio-checks 1.21.0)`) · ACCEPTED 2026-09-24, MINOR (D-470, `land/worker/D-470` @ 8344367c; minted by CONDUCT #20 at integration with `node tools/mintid.mjs IC`)

- **Interface:** I3 only. No table changes (I5 untouched: `gate_version` is unconstrained TEXT).
- **Design:** `BIO_Publication_v0_1.md` §3 rule 17 — *the gate stamp's catalogue version moves whenever the catalogue does* (BOB #32, 2026-09-24, folded verbatim in this integration) — following REC-14's version-bump precedent.
- **Proposer:** the D-470 worker, 2026-09-24. **Owner:** RECORD. **Resolved by:** CONDUCT #20.

### What a caller sees

`op=ratify`, `op=caseratify` and every projection that carries the stamp answered `gate_version: "plane-gate/1.0 (bio-checks 1.20.0)"` and now answer `… 1.21.0)`. 1.20.0 had stamped two different catalogues (before and after C-41.10, C-44.2 and C-73.1 landed); 1.21.0 names exactly one, pinned by a sha256 census per version (`bio-plane/test/d470-catalog-census.test.mjs`).

### Classification

**MINOR, ADDITIVE, non-breaking** — I3 81.0.0 → 81.1.0 on the base read at this integration (`origin/main` @ 16fe1e7f). Nothing that answered is refused and nothing refused is answered; the value of one opaque string moves. MEASURED consumer impact (the worker, 2026-09-24): zero first-party consumers PARSE the string — two display sites in `civicos-ui/app.html`, both `esc(String(…))`. Stamps already written with 1.20.0 are records that crossed and are never rewritten (§3 rule 1).
