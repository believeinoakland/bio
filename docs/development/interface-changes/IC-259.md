## IC-259 · I3: the gate stamp's catalogue version moves 1.20.0 → 1.22.0 (`gateVersion` / `gate_version` reads `plane-gate/1.0 (bio-checks 1.22.0)`; proposed as → 1.21.0 and corrected at resolution, see the foot) · ACCEPTED 2026-09-24, MINOR (D-470, `land/worker/D-470` @ 8344367c; minted by CONDUCT #20 at integration with `node tools/mintid.mjs IC`)

- **Interface:** I3 only. No table changes (I5 untouched: `gate_version` is unconstrained TEXT).
- **Design:** `BIO_Publication_v0_1.md` §3 rule 17 — *the gate stamp's catalogue version moves whenever the catalogue does* (BOB #32, 2026-09-24, folded verbatim in this integration) — following REC-14's version-bump precedent.
- **Proposer:** the D-470 worker, 2026-09-24. **Owner:** RECORD. **Resolved by:** CONDUCT #20.

### What a caller sees

`op=ratify`, `op=caseratify` and every projection that carries the stamp answered `gate_version: "plane-gate/1.0 (bio-checks 1.20.0)"` and now answer `… 1.22.0)` (proposed as `… 1.21.0)`; corrected at resolution, see the foot). 1.20.0 had stamped two different catalogues (before and after C-41.10, C-44.2 and C-73.1 landed); the stamped version now names exactly one, pinned by a sha256 census per version (`bio-plane/test/d470-catalog-census.test.mjs`).

### Classification

**MINOR, ADDITIVE, non-breaking** — I3 85.0.0 → 85.1.0 (renumbered at integration from 81.0.0 → 81.1.0; see the note at the foot) on the base read at this integration (`origin/main` @ 16fe1e7f). Nothing that answered is refused and nothing refused is answered; the value of one opaque string moves. MEASURED consumer impact (the worker, 2026-09-24): zero first-party consumers PARSE the string — two display sites in `civicos-ui/app.html`, both `esc(String(…))`. Stamps already written with 1.20.0 are records that crossed and are never rewritten (§3 rule 1).


**RENUMBERED AT INTEGRATION (CONDUCT #20, c20-batch13, 2026-09-24): I3 85.0.0 -> 85.1.0, not 81.0.0 -> 81.1.0.** This entry was ACCEPTED and resolved on `c20-batch12`, whose base carried I3 at 81.x; `c20-batch11fix` had independently taken 82.0.0 through 85.0.0 on the other side of the same integration. The CLASSIFICATION is untouched — still MINOR, still the same change — and only the number moved, on top of 85.0.0 and in the order the three entries were resolved (IC-259, IC-260, IC-257). The figure written here is the one `docs/development/INTERFACES.md` carries on the merged tree.

**THE FIGURE CORRECTED AT RESOLUTION (CONDUCT #20, c20-batch13, 2026-09-24): the stamp reads `plane-gate/1.0 (bio-checks 1.22.0)`, not 1.21.0.** This entry proposed 1.20.0 -> 1.21.0 over the base IT was read on. The other side of this integration, `c20-batch11fix`, had grown the catalogue by FIVE checks — C-32.19, C-41.13, C-71.8, C-71.9 and C-78.2 — while standing at 1.20.0 with no census suite on it to notice, so on the MERGED base 1.21.0's recorded census (433 checks, sha256 `7e1c85cd…`) no longer described the catalogue that would be stamped (438 checks, sha256 `1cce052a…`). D-470's own `test/d470-catalog-census.test.mjs` went RED at its census pin and named the remedy; this is that remedy taken at the integration. **The CLASSIFICATION does not move** — five arrivals, no departures, still MINOR and ADDITIVE on the same precedent the entry cites — and the CONSUMER IMPACT is unchanged, since the measurement was that no first-party consumer PARSES the string. **No new IC was minted:** this is the same interface change resolved against the base as read at its resolution, which is what resolving an IC at integration means, and a second id would claim a second change that did not happen. The suite's five negative-control arms were re-run in full after the move (5 of 5 as declared) because arm (d)'s needle is the constant itself.

**CORRECTED AGAIN AT INTEGRATION — c20-batch14, 2026-09-24: bio-checks 1.22.0 -> 1.23.0.** The census arm that forced 1.21.0 -> 1.22.0 at c20-batch13 forced it once more one integration on. c20-batch13 and c20-integ1b had each re-done the same c19-batch10 + `origin/main` union; c20-integ1b carried D-64, whose `RENDER_CAPTURE_CHECKS` family (C-83.1 to C-83.7) the 1.22.0 census could not have seen, and at the union `d470-catalog-census.test.mjs` went RED naming 438 against 445. The figure written is the one THAT SUITE PRINTED on the merged tree (445, digest b8b84189…), never 438 plus seven. SEVEN ARRIVALS, NO DEPARTURES: ADDITIVE, so the classification stands at MINOR, this is the same interface change resolved on the base as read here, and no new IC was minted. I3 85.1.0's entry carries the same correction.
