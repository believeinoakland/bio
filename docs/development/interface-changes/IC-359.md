## IC-359 · I3: `op=promote` asks EVERY type's state-edge table: an undeclared MOVE is refused `STATE_MOVE_UNDECLARED` (C-86.6); `op=statemovecensus` counts and says the recorded history · ACCEPTED 2026-09-25, MAJOR — I3 131.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-546)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — an undeclared move of any non-bias type (information collected -> retired, action planned -> resolved, …) landed, and is now refused before any write for every caller (BOB #34: the fence governs moves made from now on). `op=statemovecensus` (admin, probe; read-only) is additive and rides this bump.
- **The change:** A move is `to !== from` on an item with a head, read in the NORMALISED type's machine (legacy `elevated` / `published` unreachable by promote); head and promoted type both asked where they differ. The history is counted, dated and never rewritten (`STATE_MOVE_FENCED_SINCE`, `stateMoveOutsideRules`).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-546's branch stated the interface and left the IC to the integrator.
