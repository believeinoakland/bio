## IC-399 · I2: a page Tier 2 wins keeps Tier 1's `image_content_*` markers, RE-GRADED against Tier 2's glyph count (4 or fewer keeps `unread`, 5-21 reads `undetermined`, 22 or more drops it), and still routes to OCR · ACCEPTED 2026-09-25, MINOR — I2 2.14.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-633)

- **Interface:** I2.
- **Classification:** MINOR. additive over IC-398: `mergeTier2Text` dropped the markers with Tier 1's decode markers, so the page routed nowhere; they now survive, and no new reason or field is added. A marker counts 0 undetermined characters, so no award moves. Stacked on D-627.
- **The change:** BOB #35 08:05Z (re-grade at the carry); `regradeImageMark` imports D-627's thresholds rather than restating them.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-633's branch stated the interface and left the IC to the integrator.
