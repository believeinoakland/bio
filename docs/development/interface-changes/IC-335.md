## IC-335 · I3: a PROJECT run's `context` publishes `questions` — the questions it confirmed-cites that this viewer can see · ACCEPTED 2026-09-25, MINOR — I3 126.2.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-451)

- **Interface:** I3.
- **Classification:** MINOR. additive: one key on `op=airun`'s answer for a project-context run; a question-context run is unchanged.
- **The change:** `#runContextQuestions` returns exactly the set `op=suggest`'s context check (d) admits for this caller, sorted; a question the viewer cannot see is omitted and not counted.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-451's branch stated the interface and left the IC to the integrator.
