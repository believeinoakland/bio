## IC-217 · I3 + I5 data: an action nobody assessed reads `risk_tier: undetermined`, not 1 (*file freely*) — `vocabularies.risk_tiers`, `op=projection`'s `action.risk_tier` may be the string `undetermined` and gains `risk_tier_words`; C-2.10 accepts `undetermined` and an absent tier · ACCEPTED 2026-09-23 (D-182; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration and checked absent on every remote land/* ref, as the worker's report asked)

- **Classification:** I3 MAJOR, BREAKING — `action.risk_tier` was a number or null and may now be the string `undetermined` (a client reading it as a number breaks); the rest is additive. The writers (app.html and setup.mjs `mdFor`) no longer default 1.
- **Not built, placed by SCHEDULER:** a UI tier chooser; a plane fence refusing a MACHINE credential's write of a determined tier (MACHINE_CANNOT_MOVE_ACTION's pattern). Rows written at the old default still read 1 (no back-fill, as the row rules).
- **Evidence:** `bio-plane/test/risk-tier.test.mjs` (31) and its NEGATIVE CONTROL line (3 arms AS DECLARED); add-surface's /risk_tier: 1/ pin CORRECTED.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MAJOR 71.0.0 → 72.0.0**, on c17-batch5.
