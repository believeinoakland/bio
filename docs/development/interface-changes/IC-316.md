## IC-316 · I5: `reading_refs` is keyed (capture_sha, ref, occurrence) with a sequence, and `connection_pair_choices` gains `occurrence` · ACCEPTED 2026-09-25, MAJOR — I5 4.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-454)

- **Interface:** I5.
- **Classification:** MAJOR. a key a reader could rely on as unique per (capture_sha, ref) no longer is.
- **The change:** One string read on several pages is several mentions: the table's key widens and the migration keeps every row (M-155).
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-454's branch stated the interface and left the IC to the integrator.
