## IC-186 · I3: RULE 2 REACHES EVERY CREATION THE RECORD CALLS AN ASSISTANT'S — `op=promote` creating an inquiry under an admin, member or probe DEPLOY TOKEN names a RUNNING run whose principal the token is (`run` in the body; the principal is the run verbs' own stamp, `class:<cls>`), with room under the run's declared `surfaces` bound, else `SURFACE_NO_RUN` / `AI_RUN_NOT_PRINCIPAL` / `SURFACE_RUN_NOT_RUNNING` / `SURFACE_NO_BOUND` / `SURFACE_BOUND_REACHED` (C-66.1..4, C-22.12) — exactly IC-181's refusals and landing, widened from the `ai` class to every non-session caller · PROPOSED 2026-09-23 (REC-171, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 is untouched: no table, column or index moves (the `inquiry_run_surfacings` row
  and the `surfaces` bound are IC-181's). **Base read off THIS TREE (branch `worktree-agent-ab177e2083187aeae`, base
  `origin/main` @ `4355bfda`): I3 55.2.0 (IC-182). Proposed I3 MAJOR, BREAKING — 55.2.0 → 56.0.0. Read the base AT
  RESOLUTION**, by this file's own rule.
  **Why MAJOR, by IC-25's test — the op REFUSES what it accepted:** an admin, member or probe token holding `promote`
  created an inquiry naming no run and was answered `ok: true` (stamped `surfaced_by: agent`, no run, no lens, no bound);
  it is now refused `SURFACE_NO_RUN` and nothing is written. Measured on the unedited stamp: the new suite reads 9 pass /
  19 fail (the control's `ai-only` arm, which restores `4355bfda`'s condition and nothing else).
- **Proposer:** RECORD, REC-171 worker (CONDUCT #15, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/INVESTIGATIVE-SESSION.md` §11 item 5, "Rule 2's reach" (BOB #30, 2026-09-23), resting on
  rule 2 (BOB #25) and D-78's restamp (`index.mjs`).
- **Consumers to answer, MEASURED** (`git grep` for `promote` over the tracked sources of `civicos-ui/`, `agent-worker/`,
  `bio-plane/src`, `bio-plane/scripts`, `bio-plane/migrate`, `tools/`, `docprofile/`, `pdf-worker/src`, `ocr-worker/src`,
  `newgroup/src`; the design names livefire, the setup scripts and `agent-worker`):
  - `bio-plane/src/livefire.mjs` — UNAFFECTED: it promotes through the Durable Object's own door (no control-plane stamp)
    and creates an `information` bundle, never an inquiry.
  - `bio-plane/src/setup.mjs` (the setup page) — UNAFFECTED: it promotes under `op=login`'s SESSION, which D-78 stamps
    `human` and rule 2 does not ask.
  - `civicos-ui/app.html` — UNAFFECTED: every creation is a member SESSION's.
  - `agent-worker/src` — ZERO `promote` call sites (re-measured on this tree).
  - `tools/fw21-onpoint-probe.mjs` (a one-shot measurement, in no battery) — creates inquiries under the MEMBER token;
    MOVED INSIDE A RUN by the shared fixture below. It exits 2 (`GROUND BROKEN`) on `4355bfda` too — measured on an
    archive of that commit — so that exit is pre-existing and not this change's (see the item's report).
  - **`bio-plane/migrate/migrate.mjs` — AFFECTED, and it CANNOT honestly be moved inside a run.** It replays a Drive
    mirror through `op=promote` under a deploy token (`--token <admin-or-member token>`), so every `inquiries/`,
    `focuses/` or `problems/` bundle it migrates is now refused `SURFACE_NO_RUN`. A replayed historical question was
    surfaced by whoever surfaced it in the Drive era, not by a run opened today, and naming a run would record a lens
    and an objective it was never formed under. **Brought to BOB as a finding, never an exemption** (the item's report).
    Its suite (`migrate.test.mjs`) stays green because its fixture migrates no inquiry.
  - **The battery** — 62 plane suites, 8 UI suites and `agent-worker/test/harness.test.mjs` built fixtures by creating
    questions under the ADMIN or MEMBER token outside any run. CORRECTED, never exempted: the shared fixture
    `bio-plane/test/surfacing-run.mjs` (`withSurfacingRun(mf)`) sends such a creation with `run` set to a run the SAME
    token opened, through `op=airunopen`, over a fixture project it created, with a `surfaces` bound; `mint-ledger` creates
    its question through the concluding member's SESSION instead (a fixture project there would draw from the PROJ
    sequence that suite forces); `agent-worker`'s harness opens its surfacing run inline; `gate-reads` and
    `meaning-bounds` count the fixture's project, with dated reasons at each assertion.

**THE SHAPE.** `index.mjs`, in `op=promote`'s stamp block: `delete b.assistantPrincipal` for every caller (unchanged), then
for EVERY non-session caller `b.assistantPrincipal = cls === "ai" ? <principal>/<tokenId> : class:<cls>` — the same two
forms `RUN_VERB_ACTIONS` / `RUN_PRODUCTION_ACTIONS` stamp as `principal`, so `runPrincipalGate` compares them unchanged.
The condition is `!viaSession`, not a list of classes. `store.mjs` is unchanged: `#surfacingGate` already asks any creation
carrying the stamp. D-78's restamp is unchanged: a deploy token's creation is `agent` whatever it wrote, so relabelling it
`human` does not escape the check (driven, ARM <C>2), and restamping it `human` — the alternative BOB #30 refused, since it
would invent a person — is not done (the control's `restamp-human` arm).

**THE WIRE.** `op=promote` (a deploy token creating an inquiry): the five refusals and the `surfaced_in` landing of IC-181,
unchanged in shape; `op=projection`'s `surfaced_in.by` reads `class:<cls>` for such a question. A member SESSION's creation
is byte-unchanged (ARM H1–H3). `op=airunopen` already admitted the deploy classes (its answer's `projectGate` reads
`NO_MEMBER_BEHIND_CALLER`), so every deploy token CAN hold the run it now needs; the probe class holds it in `scratch`.

**NEGATIVE CONTROL** (`node test/rec171-surface-token.control.mjs` from `bio-plane/`, every arm on a COPY of `src/`, real
sources hashed before and after, untouched): ALL NINE AS DECLARED at the first run — baseline 28/0 · **ai-only (THE ROW'S
CONTROL, `cls === "ai"` restored) 9/19, every ADMIN*, MEMBER* and PROBE* arm by name** · forget-admin 21/7 · forget-member 22/6
· forget-probe 22/6 · **gate-by-claimed-label (THE LIAR) 15/13, ARM ADMIN2/MEMBER2/PROBE2 by name** · restamp-human 6/22 ·
wrong-prefix 18/10 · listed-classes (over-strictness) 28/0. D-85's control's `no-stamp` anchor was re-pinned to the new
stamp line and re-run: AS DECLARED, 17/30.

**FOR CONDUCT at integration:** resolve this IC against the base read then (I3 55.2.0 or later), move I3 in `INTERFACES.md`,
and re-run the control. Route `migrate.mjs`'s finding to BOB with the item's report if it has not reached him.
