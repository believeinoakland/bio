## IC-189 · I3: `op=reevaluations` MARKS A SEVERED LEG — every `obligations[].legs[]` entry gains `status` (`severed` | `confirmed`), from the ONE severance predicate `#refEdgeSevered(dependent, target)`; a severed leg's `edition` cause `detail` says the withdrawn leg NAMED edition N and supports nothing (DEC-70), never that it RESTS ON it; the obligation still FIRES on a severed leg · PROPOSED 2026-09-23 (REC-160, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column — the status is read off the dependent's
  document, as `op=backlinks` and `restingOn` already do). **Base read off THIS TREE (`origin/main` @ `d89e04d1`): I3 57.0.0
  (IC-184). Proposed I3 MINOR, ADDITIVE — 57.0.0 → 57.1.0. Read the base AT RESOLUTION.** **Why ADDITIVE:** no key is removed
  or renamed, no row is dropped, no refusal moves, and every `detail` on a leg whose reference is `confirmed` (or carries no
  status, or an unrecognised one) is byte-identical. The only prose that changes is the `edition` cause `detail` on a leg whose
  reference records `status: severed` — which, before this, said *"this leg rests on edition N"* about a leg its author WITHDREW:
  the record claiming support it cannot make (CLAUDE.md §2), the defect this entry closes.
- **Proposer:** RECORD, REC-160 worker (CONDUCT #15, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/architecture/BIO_State_Rules_Consistency_v1_5.md` §5.4 (cascade semantics; DEC-70's home since BOB #23's
  fold): *a read of the obligation marks which legs are severed and never describes one as resting on its target (REC-160)*;
  *the connection INFORMS, never binds*.
- **Consumers to answer, MEASURED** (`git grep reevaluations` over `civicos-ui/`, `agent-worker/`, `tools/`, `skills/`):
  **none reads the op.** `civicos-ui/app.html` names it once, in a comment saying the public banner never asks it (member-class),
  and `civicos-ui/test/publishedcase.test.mjs` asserts the UI NEVER calls it. No agent-worker, tool or skill caller. Within the
  battery, `reevaluation.test.mjs` block 2's exact leg-shape pin was CORRECTED (gains `status: "confirmed"`, dated reason).

**THE SHAPE.** `store.mjs` `reevaluations()`: per dependent, `legStatus = this.#refEdgeSevered(bundleId, t) ? "severed" :
"confirmed"` — asked once per (dependent, target), which is the predicate's own grain; carried on the raw leg and published by
`#reevalLegsEarned` as `status: l.status === "severed" ? "severed" : "confirmed"`. Only a POSITIVE recorded `severed` narrows
(D-267's rule): unreadable, no `status:` key, or a spelling the catalog never writes (`Severed`) reads `confirmed`. **Nothing is
filtered**: DEC-70 rules that severance discharges SUPPORT and never CONNECTION, so a severed dependent still receives its
obligation, and nothing is derived from strength.

**THE WIRE.** `legs[]`: `{ ord, role, grade, grade_axis, grade_source, target_edition, status, grade_authored, grade_why }`.
A severed leg's `edition` cause `detail`: *"this leg was WITHDRAWN (severed) and named edition N of T, which now stands at
edition M. A withdrawn leg supports nothing here: it adds nothing to strength, gates nothing and counts toward no bar. It is listed
because the connection still informs a second look (DEC-70)."* (and *"named no edition of T"* where the leg named none). The
cause's `cited_edition` / `latest_edition` / `latest_ratified_edition` facts are unchanged.

**NEGATIVE CONTROL** (recorded on `reevaluation.test.mjs`'s `NEGATIVE CONTROL:` line; each arm alone, restored by sha256 + cmp):
drop the status → 71/2, `REC-160 SEVERED ARM` by name · THE LIAR, filter the severed leg → 68/5, `REC-160 DEC-70: the SEVERED
dependent STILL RECEIVES the obligation` by name, and `d280-strengthbar` SITE (c) 34/1 · wording reverted → 72/1, `REC-160
SEVERED WORDING` by name. Failing-before on `d89e04d1`'s store: 67/6.

**FOR CONDUCT at integration:** resolve this IC against the base read then (I3 57.0.0 or later) and move I3 in `INTERFACES.md`.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #16 — I3 MINOR, ADDITIVE, 58.2.0 → 58.3.0.** Base RE-READ at resolution off the
integration tree (`land/conduct/c16-batch2`, built on `land/conduct/c16-batch1`, which resolved IC-185 and IC-187): **I3
58.2.0**, NOT the proposal's 57.0.0. MINOR as proposed, on IC-114's precedent: the one `detail` that moves stops the record
claiming support a withdrawn leg cannot give (`CLAUDE.md` §2), and no key, row or refusal moves. Zero non-test consumers
(the worker's grep). I5 untouched.
