## IC-389 · I2 + I3: positional tier-1 PDF text (`text.pages[].lines[]` with `{page, text, rect}`), link anchor text (`links[].anchor`), and a DERIVED item-to-file `membership` beside `links[]` on `op=pdfstructure` · ACCEPTED 2026-09-25, MINOR — I2 2.12.0; I3 153.8.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; REC-206)

- **Interface:** I2 + I3.
- **Classification:** MINOR. additive: every pre-existing output byte is unchanged on all seven PDF fixtures (the superseded whole-output pins strip the new keys by name and hash to the same literals); `membership` is labelled machine work, grade C, inferred, and withheld with `membershipWhy` when its label check fails.
- **The change:** BOB #32's 2026-09-23 23:30Z ruling; `membership.mjs` (Legistar gateway shapes, containment by top edge).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** REC-206's branch stated the interface and left the IC to the integrator.
