## IC-317 · I5: a unit with no GLYPH is neither emitted nor indexed: an all-whitespace capture reads `indexed_none` · ACCEPTED 2026-09-25, MAJOR — I5 5.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-531)

- **Interface:** I5.
- **Classification:** MAJOR. a stored value changes for a class of inputs that were indexed before.
- **The change:** Both emission sites (`index.mjs`'s `arm`, `store.mjs`'s `capture_text` ordering) read `glyphCount`, not `text.length`.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-531's branch stated the interface and left the IC to the integrator.
