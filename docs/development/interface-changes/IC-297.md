## IC-297 · I3: `op=promote` refuses a bias revision whose state is not a declared edge from its head (`BIAS_ILLEGAL_TRANSITION`, C-26.12) · ACCEPTED 2026-09-25, MAJOR — I3 100.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-468)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — a revision that moved a set backwards landed before this change.
- **The change:** A bias set's revision is held to the STATES table read from its HEAD: `adopted` -> `proposed` (and every other undeclared move) is refused by name with nothing written; a revision that keeps its state is not a transition and lands.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-468's branch stated the interface and left the IC to the integrator.
