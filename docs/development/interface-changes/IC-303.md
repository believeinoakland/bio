## IC-303 · I3: `op=connectionchoose` names WHICH occurrence of a mention is on point (`occurrence=`), and refuses a choice by ref alone where the ref was read at several places (`CONNECTION_CHOICE_OCCURRENCE_UNNAMED`, C-74.4) · ACCEPTED 2026-09-25, MAJOR — I3 107.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-454)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — a choice by ref alone landed before.
- **The change:** The choice gains `occurrence=`; its answer gains `occurrences`, `limit`, `truncated` and `chosen.occurrence`, and `lapsed`/`ambiguous` states. A ref read at more than one place and named without an occurrence is refused by name.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-454's branch stated the interface and left the IC to the integrator.
