## IC-183 · I3: `op=publishedmanifest` SERVES A FINDING'S FROZEN PAIR PER CASE WHERE THE RATIFIED CASE DOCUMENTS PINNING IT DISAGREE — the `published[]` row's `strength` is `null`, a new `strengthUndetermined: "CASES_DISAGREE"` says why, and a new `strengthByCase: [{ case_id, edition, strength }]` carries every ratified case edition's own pair; a row whose pinning documents agree (or which one case pins) is byte-identical · PROPOSED 2026-09-23 (REC-170, minted with `node tools/mintid.mjs IC` before the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (plane → UI, the op contracts). **Base read off THIS TREE (branch `land/worker/REC-170`, base
  `origin/main` @ `b5ce975a`, merged with `origin/main` @ `4355bfda`): I3 55.2.0 (IC-182).** Read the base AT RESOLUTION. I5 does not move (no table, column or index).
- **Proposed classification: MAJOR, 55.2.0 → 56.0.0, on IC-117's precedent** — an EXISTING field's value moves: where the
  documents disagree, `published[].strength` was a pair (the FIRST ratification's case's, when a later case froze another)
  and is now `null`. A consumer rendering the scalar keeps working and prints a different thing. The two new keys are
  additive and appear ONLY on a disagreeing row. If CONDUCT reads the scalar's move as correcting a wrong value rather than
  changing a contract, MINOR is the alternative; the facts for either are below.
- **Proposer:** RECORD, REC-170 worker, 2026-09-23, spawned by CONDUCT #15.
- **Owner to land it:** `RECORD`
- **Design:** `docs/architecture/BIO_Publication_v0_1.md` §3 rule 12 (b)–(d) — the frozen pair is stated once, in the case
  document, as one case's reading of the finding at that sha — with IC-74 (a finding in several cases answers every case,
  never one).

**WHAT WAS TRUE ON `b5ce975a`, MEASURED THROUGH THE OP** (`bio-plane/test/rec170-manifest-pair.test.mjs` against the
unchanged store: 7 pass, 6 fail). `published[].strength` is `published_bundles.strength`, which the ratify committer writes
ONCE, at a member's FIRST ratification (`ON CONFLICT(bundle_id,edition) DO NOTHING`). Two defects followed, not one:
(1) the row's own — a finding first ratified while two pinning documents already disagreed was stored and served as a BARE
`null` (the committer's `strengthUndetermined: true` reached `op=ratify`'s answer only); (2) found by this item — a finding
ratified under case X and later pinned by case Y, whose document froze a DIFFERENT pair, kept serving X's pair: ONE case's
reading as THE pair, which IC-74 forbids. `op=publishedcase` already served each case's pair from that case's document
(D-442), so the per-case fact existed one read away; the published INDEX reads this op alone.

**THE SHAPE.** On each `published[]` row, computed at read from the RATIFIED case editions pinning that row's `bundle_sha`
(every edition, not a case's latest: each is a separately signed document), through `#caseDocMemberFrozen` — the one parser
`op=publishedcase` and the committer use:
- two or more `/2` documents state DIFFERENT pairs → `strength: null`, `strengthUndetermined: "CASES_DISAGREE"` (a reason
  code, where `op=ratify`'s field of the same name is a boolean), `strengthByCase` ordered by `case_id`, `edition`, each
  `strength` exactly the case document's `case_strength` rows for that member (the member-bytes block's row shape);
- otherwise nothing is added and nothing moves. A LEGACY `/1` document states no pair of its own and does not join the
  comparison — stated in the reader's comment, and not driven (a `/1` document can no longer be minted through the ops).
- the answer's `altitudes` sentence gains one sentence saying what those three fields mean (top-level prose; no row moves).

**CONSUMERS, MEASURED** (every tracked file under `civicos-ui/`, `agent-worker/`, `newgroup/src/` and `tools/`,
`newgroup/src/release.mjs` excluded as the embedded last CUT): `strengthUndetermined` 0 · `strengthByCase` 0 ·
`publishedmanifest` 7 files, of which ONE READS `published[].strength`: `civicos-ui/app.html` `pubList` → `pubPair(row)`, the
published INDEX, which draws a member block PER CASE ROW (`cs`) from the member's one ratified row. **`UI` — CHANGED, NOT
BROKEN IN ITS HARNESS, AND WRONG IN BOTH STATES:** before this IC, case Y's block showed case X's pair for Q (the liar); after
it, both blocks show the no-pair sentence ("carries no frozen strength pair … nothing was established"), which is false for a
finding with two frozen pairs. **UI-82 is the owed act:** in `pubList`, for case row `cs`, read the pair from
`row.strengthByCase` where `case_id === cs.case_id && edition === cs.edition`, and fall back to `row.strength` only when
`strengthByCase` is absent. `agent-worker` — names the op in a routing list only (`harness.test.mjs`), reads no field.
`tools/` 0. `DIST` — the embedded last CUT carries the old answer until the next cut.

**NEGATIVE CONTROL** (`node test/rec170-manifest-pair.control.mjs` from `bio-plane/`, each arm alone, every restore sha256
MATCH, content IDENTICAL, cmp SAME): ALL AS DECLARED — baseline 13/0 · **(a) serve the null again (THE ROW'S CONTROL) 7/6, by
name at both PER CASE, both REASON, Q's SCALAR and the publishedcase-equality arms** · (b) THE LIAR, one case's pair as THE pair
beside the per-case list 11/2, the two SCALAR arms · (c) flag every multi-case finding 12/1, the over-strictness arm · (d) the
per-case pairs invented from the stored column 10/3 · (e) over-strictness, the list in descending order 13/0.

**FOR CONDUCT at integration:** resolve against the base read then; move I3 in `INTERFACES.md`; re-run the control. UI-82 is
the reader's half (above).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #15 — I3 MAJOR, BREAKING, 55.2.0 → 56.0.0.** Base RE-READ at resolution off
the integration tree (`int/c15-batch5` @ `353b3035`, cut at origin/main `19101d04`, with REC-169, REC-170 and DIST-6 merged):
**I3 55.2.0** (IC-182's), as proposed. Resolved before IC-184 of the same train, in id order. **WHY MAJOR, as proposed on IC-117's
precedent:** `op=publishedmanifest`'s existing `published[].strength` changes value for one class of rows. A finding whose
ratified case documents state different pairs now reads `null` with `strengthUndetermined: "CASES_DISAGREE"` and
`strengthByCase[]`, where it read the FIRST ratification's pair (one case's pair served as THE pair, which IC-74 forbids). A
consumer reading `strength` alone finds null there. Agreeing and single-case rows are byte-identical. The UI half is UI-82.
