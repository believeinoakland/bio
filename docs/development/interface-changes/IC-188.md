## IC-188 · I3: A RUN'S BUDGET IS SPENT AND DECLARED ONLY ON BOUNDS IT HAS — `op=airuntick` refuses a `consume` that is not a map (an array above all) and a key naming no bound (`AI_RUN_BOUND_UNKNOWN`, C-22.15), and any figure for `lease` (C-22.14); `op=airunopen` refuses a `bounds` that is not a list, an entry that is not an object or names no bound (C-22.15), a `lease` entry (C-22.14), and an `allowed` that is not a non-negative safe-integer JSON number (C-22.13) · PROPOSED 2026-09-23 (REC-172, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column). **Base read off THIS TREE (`origin/main` @
  `91913d6b`): I3 57.0.0 (IC-184). Proposed I3 MAJOR, BREAKING — 57.0.0 → 58.0.0. Read the base AT RESOLUTION.** **Why MAJOR, by
  IC-25's test — the ops REFUSE what they accepted:** on `91913d6b`, `op=airuntick` answered `ticked: true` (spending nothing) to
  `consume: [{ bound: "fetches", amount: 1 }]`, `[]`, `"fetches"`, `3`, `true`, `{ fetchs: 1 }`, `{ fetches: 2, fetchs: 1 }`
  (spending the 2) and `{ lease: 1 }` (writing a `lease` row into the run's budget); `op=airunopen` opened runs declared
  `allowed: -1`, `1.5`, `"3"`, `1e309`, `true`, `{ bound: "fetchs", allowed: 3 }` (no fetch ceiling), `bounds: { fetches: 3 }` (no
  bounds at all) and `{ bound: "lease", allowed: 3 }`. Each is now refused, and nothing is written (no log entry, no spend, no tick
  counted, no lease extended; no run row). **AN AUTHORITY closing:** a member who declared a ceiling in a spelling the plane did not
  know got a run WITHOUT it, and a caller was told a spend landed that the record never held.
- **Proposer:** RECORD, REC-172 worker (CONDUCT #15, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/INVESTIGATIVE-SESSION.md` §14b item 6 (A RUN IS BOUNDED, AND THE BOUND IS RECORDED).
- **Consumers to answer, MEASURED** (`git grep` over the tracked sources of `agent-worker/src`, `civicos-ui/`, `tools/`, `skills/`,
  `bio-plane/test/`, `bio-plane/scripts/`): **agent-worker** is the one production caller of `op=airuntick`; it sends a MAP of
  `fetches`, `subsessions` and `runtime` (all RUN_BOUNDS rows, non-negative integers) and never `lease` — unaffected; it never
  calls `op=airunopen` (grep: two comments only). **civicos-ui/app.html** calls neither op. The UI suites
  (`ai-session-wire`, `ai-session-context`) tick maps of caller-counted bounds and declare lists of known bounds with integer
  allowances — unaffected. Every battery suite that opens or ticks a run sends a list of known bounds and a map of known keys
  (grepped: 40+ call sites) — unaffected. **`bio-plane/test/vf4-live-scratch.mjs`** (a live instrument, not a suite) sent the array
  and is CORRECTED here to `{ fetches: 1 }`, with a dated note; its `ticked` counter now reads the result's `ticked` rather than the
  envelope's `ok` (a refused tick answers inside an `ok: true` envelope, which would have counted it as landed).

**THE SHAPE.** `airun.mjs`: `PLANE_DECIDED_BOUNDS = ["lease"]` beside REC-169's `PLANE_COUNTED_BOUNDS` (kept apart: that list is held
equal to the store's literal-named bound writers, and `lease` has none); `checkConsume(entries, { seed, allowance, map, list })` —
still the ONE rule and the ONE governed span its three rows name. `map` (the tick) takes the caller's `consume` whole; `list` (the
open) takes the member's `bounds` whole and judges each entry's bound and `allowed` (`allowance` mode: the same whole-number shape,
no plane-counted refusal, since the member sets the `mints`/`surfaces` ceilings) and then its `consumed` seed (REC-169's check,
unchanged). An unknown key is refused where it was `continue`d; `lease` is refused for ANY figure, zero included, because it is not
a count. `store.mjs`: `aiRunTick` asks `checkConsume(consume, { map: true })` where it asked it of `Object.entries(...)`;
`aiRunOpen` asks `checkConsume(bounds, { list: true })` where it asked the seed alone, and writes the judged `allowed` (absent is 0,
as ever) where it wrote `Number(b.allowed) || 0`. Same order as IC-184 on both ops.

**THE WIRE.** `op=airuntick` refused: `{ run, ticked: false, found: true, status, code, check, translation, detail, bound, note }` —
IC-184's shape; `bound` names the offending key (`null` when the `consume` itself is not a map). `op=airunopen` refused:
`{ run, started: false, code, check, translation, note }`. Every accepted call is byte-unchanged. **Translations:** C-22.15 new;
C-22.13's widened from SPENDING to GIVING an amount (it now covers an allowance); C-22.14's names the lease beside the counts.

**NEGATIVE CONTROL** (`node test/rec172-bounds.control.mjs` from `bio-plane/`, every arm on a COPY, real sources hashed before and
after, untouched): ALL EIGHT AS DECLARED — baseline 36/0 · **restore-continue (THE ROW'S CONTROL) 29/7, ARM K1 by name** ·
liar-ignore-array (THE LIAR) 29/7 · lease-spendable 31/5 · allowance-coerced 29/7 · bounds-map-ignored 35/1 · vf4-array 35/1 ·
overstrict 36/0. REC-169's control re-run on the same sources: all nine as declared.

**FOR CONDUCT at integration:** resolve this IC against the base read then (I3 57.0.0 or later), move I3 in `INTERFACES.md`, and
re-run both controls.
