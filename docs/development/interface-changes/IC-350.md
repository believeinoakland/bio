## IC-350 · I1 + I7: a CSV reading's dialect is persisted as `reading.dialect = { delimiter, encoding }`; the I7 entry shape gains an OPTIONAL `dialect(bytes)` slot · ACCEPTED 2026-09-25, MINOR — I1 1.13.0; I7 1.1.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; REC-218)

- **Interface:** I1 + I7.
- **Classification:** MINOR. additive on both: a new key on the acquire document (beside `container_extent`), read back through `op=reading`, and an optional registry slot.
- **The change:** BOB #33, 2026-09-24 21:55Z, option (b). A latin-1 body reads its encoding UNDETERMINED with its reason.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** REC-218's branch stated the interface and left the IC to the integrator.
