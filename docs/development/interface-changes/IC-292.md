## IC-292 · I3: `op=statementack`'s unreachable bound is RETIRED — the refusal `STATEMENT_ACK_DOCUMENTS_OVER_BOUND` (C-82.1) and the answer keys `case_documents_limit` / `case_documents_truncated` leave; `CATALOG_VERSION` moves 1.28.0 → 2.0.0 · PROPOSED 2026-09-24 by the D-521 worker (minted with `node tools/mintid.mjs IC`, a CAS push to `coord`)

- **Interface:** I3 only. No table changes (I5 untouched). No op is added, removed or renamed; no request argument changes.
- **Design:** DEC-49, as `docs/architecture/BIO_Assistant_and_AI_Roles_v0_1.md` §3 rule 10 restates it (a catalogued refusal is a claim the record makes about what a member can meet); `docs/architecture/BIO_Publication_v0_1.md` §3 rule 11's "As built" and rule 13 (REC-194's one-case-identity narrowing, which made the bound unreachable) carry the landing; §3 rule 17 moves the catalogue version.
- **Proposer:** the D-521 worker, 2026-09-24, branch `land/worker/D-521`. **Owner:** RECORD.

### Why

REC-194 made `acknowledgeStatement`'s re-author read name `(case_id, edition)`, the PRIMARY KEY of `case_documents`, so
it returns at most one row. IC-246's bound on that read (`STATEMENT_ACK_DOCUMENTS_MAX` = 8, read at max + 1) and its
refusal C-82.1 could therefore never fire — measured by `d150-statement-acknowledgement.test.mjs` block 8 over nine
same-sentence documents, and reported by REC-194 as F1. The read is now `#one`, and the bound, the refusal, its
catalogue row and its DEC-49 region `is-statement-ack-documents-bound` are removed together.

### I3 — what a caller sees

- **`STATEMENT_ACK_DOCUMENTS_OVER_BOUND` / C-82.1 is no longer in `STATEMENT_ACK_CHECKS`** (six rows remain, C-82.2..C-82.7).
  No caller could ever have received it after REC-194, so no answer anybody has seen changes. C-82.1's number is not reused.
- **A successful `op=statementack` no longer carries `case_documents_limit` (was `8`) or `case_documents_truncated`
  (was always `false`).** `case_documents` is unchanged — an array, now of length 0 or 1. These are REMOVED keys, so on
  the wire this is a removal, not an addition.
- **`CATALOG_VERSION` 1.28.0 → 2.0.0**, and `GATE_VERSION` — `plane-gate/1.0 (bio-checks 2.0.0)` — with it. ONE
  DEPARTURE, NO ARRIVALS (461 → 460 checks, the d470 suite's own print, digest `e6651478…`). Every earlier move was
  additive and so MINOR; a removed check is not, so the step is proposed MAJOR. **D-463 (c20-batch27, not on main) also
  moves this constant: CONDUCT takes the next number at the union and re-reads the census from the d470 suite's print.**

### Measured consumer impact

- `civicos-ui/`: ZERO readers of `case_documents_limit`, `case_documents_truncated` or the code (grep over `*.html`,
  `*.js`, `*.mjs`; the only hits are dated history comments in `check-refusal-codes.mjs`).
- Plane suites that read them — `bounds.test.mjs` (the named-constant arm, the capped-op roster 45 → 44, the envelope
  map), `d150-statement-acknowledgement.test.mjs` block 8, `d507-statement-ack-translation.test.mjs`, `ratify.test.mjs`
  and `d470-catalog-census.test.mjs` (the version literal) — are CORRECTED in the same landing, each with a dated reason.
- `check-refusal-codes.mjs`: eight floors fall with their reason at the site (rows 395 → 394, census 700 → 699, reach
  447 → 446, governedSites 186 → 185, regions 169 → 168, regionLines 3967 → 3958, codesChecked 446 → 445,
  refusalsJudged 440 → 439), each read from `--strict`'s print.

### Proposed classification

I3 MAJOR on the letter (a removed code and two removed answer keys), with the measured impact above: no consumer reads
any of the three, and the code was unreachable. CONDUCT classifies.
