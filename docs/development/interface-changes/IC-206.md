## IC-206 · I3: `op=promote` moving Information INTO `retired` refuses `CITED` while a live edge cites it — the same predicate (`#retirementCitedBy`), code, offenders shape and detail as `op=retire` · ACCEPTED 2026-09-23 (REC-181; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration, as the row asked)

- **Interface:** I3. I5 does not move.
- **Classification:** MAJOR, BREAKING (IC-25's test: the op refuses what it accepted). A package naming `current_state: retired` for a
  cited item used to land; it is now refused `{ ok: false, reason: "CITED", to: "retired", offenders: [{id, citedBy}], detail }`
  before any write. An edit of an item already retired is not asked; a severed edge does not block.
- **Also:** `op=retire`'s CITED `detail` is now read from the static `Store.RETIRE_CITED_DETAIL` (text byte-identical).
- **Proposer:** REC-181 worker (CONDUCT #17, cloud), 2026-09-23. **Owner:** RECORD. **Design:** State Rules v1.5 §4.1.
- **Evidence:** `bio-plane/test/rec-181-promote-retire.test.mjs` (12) and its control (skip 6/6, overstrict 10/2, AS DECLARED);
  `d168-retired-cite.test.mjs` §3 CORRECTED (it walked a cited item to retired by promote — the bypass this closes).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MAJOR 66.1.0 → 67.0.0**, on c17-batch2.
