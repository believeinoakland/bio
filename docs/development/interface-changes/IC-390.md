## IC-390 · I2 + I3: a routed page whose folio decoded keeps it and gains the transcription APPENDED; it is listed in the layer part AND the engine's part (chain parts may overlap, `extent.part` names a part when they do); the reading provenance names both `producers` · ACCEPTED 2026-09-25, MINOR — I2 2.15.0; I3 150.9.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-635)

- **Interface:** I2 + I3.
- **Classification:** MINOR. additive: a page `mergeTier3Text` refused (listed in `refused`, content unread) is now read, and a refusal lifted adds none; `extent.part` and a provenance page's `producers` are optional and absent wherever the parts partition, which reads byte for byte as before. The partition was never a registered I2 invariant. A derivation cap on a shared page answers by the partition rule (UNDETERMINED for a folio page).
- **The change:** BOB #35 06:25Z (APPEND). The readers that assumed a partition are listed in M-180 and moved. Stacked on D-627.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-635's branch stated the interface and left the IC to the integrator.
