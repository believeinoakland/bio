## IC-245 · I3: every COUNT a member session is served is taken through that member's own sight · PROPOSED 2026-09-24 (D-464; minted with `node tools/mintid.mjs IC`)

- **Interface:** I3 (`op=stats`, `op=selftest`'s relayed `store`, `op=livefire`'s `storeState`, `op=searchindexcheck`'s
  `counts.indexed`, `op=selectionlist`'s `bytes`). I5 does not move: no table, no column.
- **Classification:** MINOR, not breaking for any credential the gate does not filter. A NUMBER changes meaning for one
  audience only — an identified member session that is not an administrator: each counter whose rows NAME a bundle (by
  `bundle_id`, `target_id`, `project_id`, `context_id`, `scope_id`, `case_id`, `refers_to`, …) no longer counts rows naming a
  bundle that member's `viewerPredicate` does not pass (today: a project they are not invited to). The keys, their spelling
  and their types are unchanged. For the ADMIN, MEMBER, PROBE, DAEMON and organisation `ai` tokens, and for an enrolled
  administrator's session, every figure is exactly what it was (driven: `project-sight.test.mjs` §8, WHOLE and EXACT).
  - `op=stats` and its two relays now take the control plane's VIEWER stamp and FAIL CLOSED on an absent one (a DENY viewer
    subtracts every bundle). `op=selectionlist` takes the stamp too.
  - `op=searchindexcheck`'s `counts.indexed` now counts the index rows no hidden bundle claims — so it is parity against
    `keyed` plus the orphans, over what the caller can see. Orphans still count and are still listed.
  - `selections` drops a whole handle that holds a hidden bundle; `selectionItems` drops only its hidden rows.
  - `aiRunBounds` drops the bounds of a run whose context is a hidden project; `aiRuns` drops such runs.
  - NOT changed, stated: `observationsNonLead`, `aiRunLog`, and the run-keyed side of `proposedReadings`,
    `inquiryRunSurfacings` and `captureRequests` — what a hidden project's run produced at the evidence levels is routed
    as a design question (D-464's report), not decided here.
- **Why (the disclosure):** Membership v2 §7.9, *"Not its existence"*; BOB #15's *A COUNT IS A DISCLOSURE OF EXISTENCE*
  (`MEMBER-KNOWLEDGE-DESIGN.md` §5). Measured at the op before the change (MEASUREMENTS M-124, first seen in M-122): a
  hidden project's creation and revision moved six of a member's `op=stats` keys and her `op=searchindexcheck`.
- **Consumers, measured 2026-09-24 by grep over the tree at `15b2a4c0` plus this change:**
  - `civicos-ui/app.html` — 0 reads of `op=stats`, `op=searchindexcheck` or `op=selectionlist`. No change.
  - `agent-worker/src` — 0 reads. No change.
  - `tools/m031-index-measure.mjs` — names `op=stats`' `dbBytes` in PROSE only; `dbBytes` is the admin class's and unchanged.
  - DIST's live `op=stats` readings (e.g. M-68) use the ADMIN token — unchanged by construction.
  - `newgroup/` — carries the plane only as the embedded release bundle (DIST's artifact).
  - **Tests:** `bio-plane/test/nc-rec129.mjs` (a control driver) anchored on the DO route's `stats:` line, which now passes
    the viewer; its anchor and the two arms that replace that line were CORRECTED with the reason at the site, so each arm
    still moves only its own subject. No `.test.mjs` assertion changed.
- **Proposer:** WORKER D-464 (CONDUCT #19, cloud), 2026-09-24. **Owner:** RECORD. **Design:** Membership v2 §7 (item 7.9).
- **Evidence:** `bio-plane/test/project-sight.test.mjs` §8 and `project-sight.control.mjs` (arms `stats-whole-store`,
  `indexcheck-whole-index`, `selectionbytes-whole`, `stats-stamp-dropped`, `subtract-for-everyone`), recorded on the
  suite's NEGATIVE CONTROL line.
- **Version:** CONDUCT resolves it at integration.
