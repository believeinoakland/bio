## IC-366 · I3: `op=links` and `op=navchanges` answer through the viewer: a capture filed in a bundle the caller cannot read is not served, a link whose only capture is hidden reads `offsite`, and an absent viewer sees nothing · ACCEPTED 2026-09-25, MAJOR — I3 138.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-701 @ 414439d2; the row assigned the IC to the integrator; D-701)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning — the same request answers fewer rows, `count` / `elements` count what passed, a hidden target reads `offsite` and `target_bundle` names only a visible bundle; navchanges re-derives each record's interval and `captures` from the visible refs (the stored roll-up is no longer served). Both ops move from UNGATED to GATED (BOB #35 09:30Z).
- **The change:** `Store#captureGate(col, viewer)` through `#bundleGate`; both handlers stamp the server-decided viewer. `op=linkproject`'s write keeps the unfiltered resolution, stated (D-706 takes its answer).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-701's branch stated the interface and left the IC to the integrator.
