## IC-286 · I3: `op=knock`'s three pre-store refusals take catalogued codes and canned translations (C-85.3, C-85.4, C-85.5), and their WIRE TOKENS MOVE; `CATALOG_VERSION` 1.25.0 → 1.26.0 · **ACCEPTED 2026-09-24, MAJOR — I3 98.0.0 (resolved by CONDUCT #20 at integration on c20-batch27, on base 97.2.0; D-513; the `reason` token moves on a PUBLIC op, reported to BOB #34)

- **Interface:** I3 only. No table changes (I5 untouched). No op is added, removed or renamed; no request argument changes; no status code changes.
- **Design:** DEC-49 (Bob, 2026-08-06), as `docs/architecture/BIO_Assistant_and_AI_Roles_v0_1.md` §3 rule 10 restates it, following D-484's single-site consolidation (`actNoBasis`, `actNoCitation`) and D-508's at this same door.
- **Proposer:** the D-513 worker, 2026-09-24, branch `land/worker/D-513`. **Owner:** RECORD.
- **THIS ONE IS NOT PURELY ADDITIVE, and the row that raised it says "I3 additive".** The row is right about `code`, `check` and `translation` and wrong about `reason`. The correction is argued below rather than smoothed, and **CONDUCT classifies.**

### I3 — what a caller sees

`op=knock` is the one door in this plane open with no credential, so its caller is guaranteed not to be a member: a stranger with no account, no session and no screen of ours. It makes three refusals BEFORE `Store.knock` is reached. All three were bare.

| before (`reason`) | after (`reason` = `code`) | check | region (`where`) | status |
| --- | --- | --- | --- | --- |
| `TOO_LARGE` (request body past the ceiling, refused unparsed) | `KNOCK_ENVELOPE_TOO_LARGE` | C-85.3 | `src/index.mjs knockEnvelopeTooLarge > is-knock-envelope-too-large` | 413 |
| `TOO_LARGE` (decoded payload past what this instance stores) | `KNOCK_PAYLOAD_TOO_LARGE` | C-85.4 | `src/index.mjs knockPayloadTooLarge > is-knock-payload-too-large` | 413 |
| `EMPTY` (content field decoded to zero bytes) | `KNOCK_EMPTY` | C-85.5 | `src/index.mjs knockEmpty > is-knock-empty` | 400 |

- **THREE CODES WHERE THERE WERE TWO TOKENS.** The oversize token was minted at two sites for two CONDITIONS, not one. The first refuses to READ a request at all — nothing is decoded, so nothing about the material has been judged; the remedy is to send less or send it in parts. The second refuses material this INSTANCE cannot hold, which without evidence storage is 64 KB rather than 8 MB; the remedy is the group's, not the sender's. A DEC-49 row holds ONE `where` and one canned sentence, and a translation that misdescribes the condition that fired is the record telling a member something untrue about their own input — the class `dec49-onecode-twoconditions.sweep.mjs` walks for.
- **WHY THE TOKEN HAD TO MOVE, which is the whole of the non-additive part.** `TOO_LARGE` is minted at **seven** sites across `bio-plane/src` (`container.mjs`, `subresources.mjs` ×2, `store.mjs` selection, and three in `index.mjs`) and `EMPTY` at **three** (`index.mjs` ×2, `store.mjs`). A row under either key would have claimed every one of those sites for the doorbell's sentence. `TOO_LARGE` is the sharper case: `app.html`'s `PART_REASON` table already words it for a capture PART, so the DEC-49 guard **already counted it as TRANSLATED** — *"That document is larger than this surface will capture, even in parts"*, standing in the census as this door's answer to a stranger who will never load that page — and a second row under the same key fails the guard's translated-twice gate outright (gated at zero, `check-refusal-codes.mjs` arm B). There is no spelling of this landing in which `reason` stays put.
- **Everything else is unchanged and additive.** Each answer keeps `ok: false`, its HTTP status, its `maxBytes`, and `knockPayloadTooLarge`'s `detail` (byte-identical, still carried only where no evidence storage is configured). `code`, `check` and `translation` JOIN them.
- **`CATALOG_VERSION` 1.25.0 → 1.26.0**, MINOR, and `GATE_VERSION` moves with it; the census moves 457 → 460 checks. Nothing that passed is refused by the move: the three conditions already refused, at the same three sites, under the same statuses.

### Classification (the proposer's; CONDUCT decides)

- **I3: MINOR, but BREAKING for a consumer that keys on `reason` at `op=knock`.** By IC-25's test the key is not removed and no request that answered `ok: true` now answers `ok: false` — but a value a consumer may switch on CHANGES, at three sites, so this entry does not claim additive and leaves the grade to the integrator.
- **Measured consumer impact, 2026-09-24 on this tree.** `git grep` for `TOO_LARGE` and `EMPTY` over `*.mjs`, `*.md`, `*.html` outside `node_modules`: **no consumer in this repository reads either token from `op=knock`.** `civicos-ui/app.html` reads both, at lines 2973 and 3423–3424, for a capture PART and a capture verdict — `subresources.mjs`' producers, not this door. `newgroup/` never calls `op=knock`. The only readers were `bio-plane/test/doorbell.test.mjs` (two assertions, CORRECTED here with the reason at the site) and `d278-codeless-refusals.test.mjs`'s header clause (corrected here). **What CANNOT be measured from this repository is a third-party knocker's client**, since the door is public by design; that is stated rather than scored zero, and it is the substance of what CONDUCT is classifying.
- **Proposed on the I3 version current at `origin/main` 1a7f0bcc0.** CONDUCT takes the version bump and the RESOLUTION, and takes `CATALOG_VERSION` once at the union — c20-batch25 moves the same constant in parallel.

### Evidence

- `bio-plane/test/doorbell.test.mjs` (72 pass, 0 fail): all three refusals DRIVEN through `op=knock` on Miniflare, each asserted to arrive with its `code`, its `check` as a literal, a translation floored at 120 characters AND equal to `KNOCK_CHECKS`' own row (which is what catches an inline copy), plus the status, `maxBytes` and `detail` each site carried before.
- `civicos-ui/check-refusal-codes.mjs --strict` exit 0, every ratchet key at slack 0, nine floors moved from its own print.
- `bio-plane/test/d470-catalog-census.test.mjs` (9 pass, 0 fail): the 1.26.0 row is `{ count: 460, digest: 36e153c03ce9f749… }`, from that suite's own print.
- **Negative control:** declared and run in `bio-plane/test/doorbell.test.mjs`'s `NEGATIVE CONTROL:` line.

### What this entry does NOT claim

It does not claim that `TOO_LARGE` or `EMPTY` are now translated anywhere else. They are not: both remain in `check-refusal-codes.mjs` arm F's MULTI-SITE partition for the seven other sites that still mint them (five oversize, two empty), `untranslated` does not move (292 before and after) and F4 does not move (100 before and after). This entry closes ONE door, and says so.
