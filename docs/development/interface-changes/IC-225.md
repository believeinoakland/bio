## IC-225 · I3: `op=monitor` asks the document profile's assess — additive `assessment`, `assessment_basis`, `cadence`, and one observation-log row per look — and `reeval_raised` now reads FALSE on a routine change where D-60 read true · PROPOSED 2026-09-23 (D-65; minted by CONDUCT #18 with `node tools/mintid.mjs IC` at c17-batch7, on BOB #31's relay of the worker's report) — the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts).
- **The change.** `op=monitor` asks docprofile's assess (Framework §6) with the baseline's own bytes, read back from R2 under the capture key and verified by hash. The answer carries `assessment` (verdict, stopped_at, trail, graded events, confirmation), or null with `assessment_basis`; `cadence` names the authored frequency, or the content type's contract when none is stated (membership daily, substance weekly — the DEFAULTS, an address's own setting overriding them, BOB #31's ruling of 2026-09-23); each look is one `observation_log` row (§4.1). A meeting gone inside the calendar's window reads `delisted`. **BEHAVIOUR CHANGE:** a moved window reads `routine` and `reeval_raised` is FALSE where D-60's answer read true; every other verdict keeps D-60's flag.
- **Classification: I3 MAJOR, BREAKING** — the additive fields alone would be MINOR, but a key that answered true now answers false for the same input, which IC-25 settles as breaking.
- **Design:** Framework §6 (assess), `OBSERVATION-LOG` §4.1.
- **Evidence:** `bio-plane/test/monitor-assess.test.mjs` with its control.
- **Not built:** REC-26's cadence scheduler still reads only the authored frequency (finding carried to SCHEDULER).
- **Proposer:** D-65 worker (CONDUCT #17), 2026-09-23.

**RE-RESOLVED 2026-09-24 by CONDUCT #19 at c19-batch9: +2 I3 majors, the security train (IC-237, IC-238) having landed first; the line below carries the new figures.**

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #18 — I3 MAJOR, BREAKING 76.0.0 → 77.0.0** (base read on c17-batch7).
