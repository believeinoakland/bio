## IC-197 · I3: `op=promote` REFUSES A REVISION THAT REWRITES AN INQUIRY'S `surfaced_by` — a revision of a bundle whose current version is an inquiry, whose `surfaced_by` (as the catalog's parser reads it out of each version's `bundle.md`, an absent or unreadable value counting as a value) differs from the current version's, is refused `SURFACED_BY_REWRITTEN` (C-66.5, SURFACE_CHECKS) with its canned translation, before any write, in either direction and whoever the caller · PROPOSED 2026-09-23 (REC-179, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column). **Base read off THIS TREE (`origin/main`
  @ `0e5f7054`, merged): I3 60.0.0 (IC-192, REC-175). Proposed I3 MAJOR — 60.0.0 → 61.0.0. Read the base AT RESOLUTION**, by this file's
  own rule. **Why MAJOR, on IC-112's and IC-181's precedent — a refusal where none was before:** a revision that changed
  `surfaced_by` (or dropped the line, or added one to a document that had none) answered `ok: true` and now answers
  `ok: false, reason: "SURFACED_BY_REWRITTEN"`. Every revision that carries the value forward — including a respelling
  the parser reads as the same value (`surfaced_by: "human"`) — is byte-for-byte what it was.
- **Proposer:** RECORD, REC-179 worker (CONDUCT #16, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/INVESTIGATIVE-SESSION.md` §11 item 5, "Rule 2's reach" (the SURFACING ACT is what
  `surfaced_by` records, decided at the trust boundary), with the D-78 restamp's own stated intent in `index.mjs` that
  "a revision carries the document's value forward"; the BUILT note under the migration paragraph.
- **The wire, added:** the refusal carries `ok: false`, `reason` = `code` = `"SURFACED_BY_REWRITTEN"`, `check: "C-66.5"`,
  `translation` (the catalog row's), `detail`, `bundleId`, and `current` / `revision` — each either the JSON of the parsed
  value (`"\"agent\""`), `"absent"` or `"unreadable"`. No existing key changes.
- **Consumers to answer, MEASURED** (grep for `surfaced_by` over `civicos-ui/`, `agent-worker/`, `tools/`, `docprofile/`,
  `bio-plane/scripts`, `bio-plane/migrate`, `newgroup/src`): the ONE writer outside the plane is `civicos-ui/app.html`'s
  CREATION template (a literal `surfaced_by: human`, restamped by D-78 as before); its revision paths carry the held
  document's frontmatter forward — the UI harness is the evidence (see the landing's gate). `tools/fw21-onpoint-probe.mjs`
  writes `surfaced_by: agent` on a creation only. `agent-worker` names the field only in a test comment. The plane's own
  internal re-promoters (`cite`, `sever`, `reinstate`, the ratify committer) rebuild bytes from the held document and keep
  the line. **One caller that COULD meet the refusal:** `bio-plane/migrate/migrate.mjs` replays Drive-era revisions with
  `replay: true`; a Drive-era history in which the value changes between revisions would be refused at that revision.
  Whether the Drive corpus holds any such history is UNDETERMINED (the corpus is not in the tree); it is brought to BOB,
  not exempted — `replay` on a revision is a caller's assertion (REC-173 verifies only a creation).

**THE SHAPE.** `store.mjs` `promote`, inside the transaction, directly after the compare-and-swap (`is-promote-cas`) and
before the testimony fence and every write: for `cur && base !== null && normalizeType(cur.object_type) === "inquiry"`,
read the held `bundle.md`'s `content` and the incoming `bundle.md`'s `text` through `parseFrontmatter`, and compare the
two readings (`JSON.stringify` of the value, `"absent"`, or `"unreadable"`); unequal → the refusal (region
`is-promote-surfaced-by`). REC-175's digest check (`is-promote-digest`, before the transaction) is not touched and
composes: a revision it refuses never reaches this comparison.

**Evidence:** `bio-plane/test/rec179-surfaced-by.test.mjs` (22/0), through the op under miniflare — agent→human (a
session), human→agent (a deploy token and a session), a second-line flip the parser reads, a `replay: true` flip, a
dropped line, and a flip after a landed revision are each refused by name with the whole `op=image` digest, `bundle.md`
and head unchanged; keeping the value lands from a session and a deploy token, and a quoted respelling lands. Its control
(`rec179-surfaced-by.control.mjs`, seven arms, all as declared): dropping the comparison fails every flip arm by name
(7/15).
