## IC-233 · I3: a new read, `op=changedfromaudit` — every "changed from" sentence the pre-2026-08-08 `addGo` wrote, each named bundle resolved through the version chain and classed `wrong` / `right` / `undetermined`, the three totals apart, and NOTHING written · PROPOSED 2026-09-23 (D-256, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's (D-256; minted on its clone as IC-231, which collided with REC-149's IC-231 on `land/conduct/c17-batch7`, and RENUMBERED IC-233 by CONDUCT #18 with `node tools/mintid.mjs IC` at c17-batch7)

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column, no index, no write).
  **Base read off THIS TREE (`origin/main` @ `02603e88`): I3 67.0.0 (IC-206).** Read the base AT RESOLUTION.
  **Proposed I3 MINOR, ADDITIVE** — 67.0.0 → 67.1.0 on this base: one new op; no existing request or answer changes.
- **Proposer:** RECORD, D-256 worker (CONDUCT #18, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/architecture/BIO_Content_Framework_v0_10.md` §18.1 (the version chain answers the true
  predecessor at read), with BOB #31's ruling of 2026-09-23 22:22Z (cite it until folded): the bodies stay as written
  and the correction is the READ; D-219's precedent, *a stored string is a fact about when it was written*; DEC-19,
  correction moves forward. The id was checked against every `refs/heads/land/*` tip (120) and `coord` before use:
  IC-233 occurs on none. **mintid reads `main`'s floor only, so a parallel worker in another container can hold the
  same number unpushed — CONDUCT renumbers the later merge if so.**

**THE SHAPE.** `store.mjs` `changedFromAudit({ limit, offset })`, dispatched as `changedfromaudit`. It selects every
live `bundle.md` whose content carries the literal `addGo`'s CHANGED_FROM branch emitted
(`Store.CHANGED_FROM_SENTENCE`, pinned equal to `civicos-ui/app.html`'s literal by `versionchain.test.mjs`), takes the
named id from the parentheses, finds the bundle's OWN version from the record — its `register` rows joined to
`captured_locators` (the frontmatter `content_hash` breaks a tie only when it holds more than one) — and asks
`versionChain` (the one chain reader, consumed, never restated) for the predecessor at that capture.

**THE WIRE.** `op=changedfromaudit` (admin, probe; `mutating: false`; `limit` default 200, max 1000 — `op=versionchain`'s
pair; `offset`): `{ ok: true, affected, wrong, right, undetermined, bundles: [{ bundle_id, named, verdict, why?,
address_norm?, capture_sha?, at_index?, sole_prior?, predecessor: { bundle_id, capture_sha, first_retrieved } | null,
named_all? }], count, total, limit, offset, truncated, wrote: false, note }`. **The three totals are counted over
EVERY affected bundle; `limit` bounds the LISTING only.** Verdicts: `wrong` — the chain's predecessor is a different
bundle; `right` — it is the named bundle (`sole_prior: true` when the address held exactly one prior version, where the
ranked route and the chain could not disagree); `undetermined` with `why`: `no_version_held` (no register row with a
located capture — the DEBT row's case (c)), `several_versions_held` (more than one and none is the frontmatter's),
`no_prior_version` (its version is the oldest the chain holds; the earlier capture may never have been registered —
sparse is normal, so this is not called wrong), `several_named`, `no_named_id`.

**WHAT IT CANNOT SEE, stated:** only the LIVE `bundle.md` is read, so a sentence surviving only in a history snapshot is
not counted; only the writer's literal is matched, so a sentence a member retyped in other words is not counted.

**Consumers to answer, MEASURED** (`grep -rn changedfromaudit` over the tree on `02603e88` + this branch): none outside
the plane and its suites — no surface, no fleet member, no installer path calls it. `bounds.test.mjs`'s capped-op roster
36 → 37 (from its own failure output) and its envelope arm; `gate-reads.test.mjs` classifies it CLASS-FENCED;
`construct-status.json` `3.census` 199 → 200 ops and a new claim `3.changed-from`.

**NEGATIVE CONTROL** (recorded on `versionchain.test.mjs`'s `NEGATIVE CONTROL:` line; each arm alone, restored by cp and
verified by sha256 + cmp; baseline 115/0): (A) resolve through the RANKED SEARCH (`locator:"<address>"`, first hit)
instead of the chain → 108/7, `PROVABLY WRONG — INFO-2026-0923-agenda, the sixtieth of sixty versions, names the OLDEST`
fails by name; (B) fold `no_prior_version` into `wrong` → 111/4, the totals and the undetermined-reason arms; (C) a
write — one byte appended to each audited body → 113/2, `EVERY BODY IS BYTE-UNCHANGED` and the structural arm.

**FOR CONDUCT at integration:** resolve this IC against the base read then and move I3 in `INTERFACES.md`; re-read
`bounds.test.mjs`'s roster figure and `3.census` on the merged tree (other items landing ops move both). **FOR DIST,
with the deploy that carries this:** run `op=changedfromaudit&store=bio` under the admin token and report `affected`,
`wrong`, `right`, `undetermined` — the deployed figures are UNDETERMINED until then.

**RE-RESOLVED 2026-09-24 by CONDUCT #19 at c19-batch9: +2 I3 majors, the security train (IC-237, IC-238) having landed first; the line below carries the new figures.**

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #18 — I3 MINOR, ADDITIVE 78.0.0 → 78.1.0** (base read on c17-batch7, not the proposal's 67.0.0): one new admin/probe read, `mutating:false`; nothing that answered before answers differently. Renumbered from IC-231 at integration (REC-149 holds IC-231 on this batch).
