## IC-371 · I3: a machine credential may only PROPOSE the law a records request is made under: a machine write that CHANGES the statement is refused `MACHINE_CANNOT_STATE_RECORDS_LAW` (C-32.20); `law.stated_by` and MACHINE-STATED sentences on the projection · ACCEPTED 2026-09-25, MAJOR — I3 140.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-689 @ 4ef3d303; the row assigned the IC to the integrator; D-689)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — a machine credential's promote creating or revising a `cpra_request` / `records_request` law statement landed and is now refused (BOB #35 08:25Z, fence both); a changed meaning — a machine-stated `cpra_request` or law now reads MACHINE-STATED in `law.stated` and `governing_laws.stated` where it read as the member's statement. `law.stated_by` (member | machine | undetermined) is additive. Stacked on REC-201.
- **The change:** `recordsLawStatement` is the one key; a carry-forward lands, so a pre-fence machine statement is never rewritten. Six suites' operator-token cpra fixtures corrected to `records_request`.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-689's branch stated the interface and left the IC to the integrator.
