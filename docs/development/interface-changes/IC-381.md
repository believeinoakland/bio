## IC-381 · I3: a connection's pair states the member's choice: `determining_pair.selection.says` names a current or LAPSED choice per end; `op=connections`' id/sha arm serves `on_point[side]` with `lapsed`; the content= arm counts the other end's choice only while current · ACCEPTED 2026-09-25, MAJOR — I3 153.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-575)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning — on the id/sha arm a choice whose occurrence a re-read dropped was served in `on_point[side]` as standing and is now served as the lapse (`lapsed: true` with its why; a current one carries `lapsed: false`), and on the content= arm the other end's grade no longer counts a lapsed choice. The `says` sentence with no choice is REC-120's byte for byte. Proposed as additive; classified by IC-25.
- **The change:** `#resolvePairChoice` (D-454's resolution, moved unchanged) and `#pairChoiceStates`; `chosen` stays about the machine's pair.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-575's branch stated the interface and left the IC to the integrator.
