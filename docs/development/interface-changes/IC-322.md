## IC-322 · I3: `SESSION_ROLE_CANNOT_REACH_OP` names the SESSION that reaches the op (`session`, `reachedBy: founder|member`) in place of `role`; `op=aicredentialmint`'s refusal says which property refused · ACCEPTED 2026-09-25, MAJOR — I3 113.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; REC-162)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a wire token disappears — the refusal's `role` key is gone, replaced by `session` and `reachedBy` — and the refusal now also fires for an op held by `SESSION_OPS.member` alone, met from the founder's session.
- **The change:** The refusal's `detail` and `translation` say the op is reserved to the founder's session (an enrolled administrator signs in as a member) or to a member's own session; the scope-authoring refusal's sentence names machine-credential reach where the OPS row carries `machineClasses`.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** REC-162's branch stated the interface and left the IC to the integrator.
