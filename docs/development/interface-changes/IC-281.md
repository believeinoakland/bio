## IC-281 · I4: the release's deploy derivation carries the `browser` binding class; the installer binds BROWSER on install and restates it on update · ACCEPTED 2026-09-24, MINOR — I4 2.3.0 (resolved by CONDUCT #20 at integration on c20-batch25, on base 2.2.0; DIST-11, DIST-13)

- **Interface:** I4 (plane → installer, the release artifact). DIST-11's commit named "I8 additive (a binding class)"; I8 is plane ↔ agent-worker, and what moved is the binding set the installer derives from the release, which is I4's. Classified here by CONDUCT #20, minted with `node tools/mintid.mjs IC` at integration because the landing carried none.
- **What changed:** `derive-bindings.mjs` carries the `browser` binding class (it refused it by name before); `bio-plane/wrangler.jsonc` declares `BROWSER`; `newgroup` binds it at install and restates it on every update (it is not in `keep_bindings`, so an update that did not name it would drop it). DIST-13 adds `bio-plane/test/newgroup-bundle-fresh.test.mjs`, a battery guard that rebuilds the installer bundle from source in memory and byte-compares the committed one.
- **Classification:** MINOR, ADDITIVE. No binding is removed and none changes meaning; an instance installed before gains `BROWSER` at its next update.
- **Not changed, stated:** the in-plane render DRIVER is NOT BUILT — `render: true` still answers `RENDER_NO_RENDERER` (renderer `browser-binding-without-driver`). The binding is live from the next release DIST cuts. IC-252's owed DIST act is discharged.
