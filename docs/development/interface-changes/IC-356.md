## IC-356 · I3 + I2 + I5: a `pdf-page` extent's rect is bounded by its page's MediaBox: a rect reaching off the page is refused `CONTENT_EXTENT_OUT_OF_RANGE` (C-45.1); the structure object carries `pageBoxes` and the reading `page_boxes` · ACCEPTED 2026-09-25, MAJOR (I3) / MINOR (I2, I5) — I3 128.0.0; I2 2.11.0; I5 8.1.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-374)

- **Interface:** I3 + I2 + I5.
- **Classification:** MAJOR (I3) / MINOR (I2, I5). I3 MAJOR, IC-25: a refusal where an answer stood — a `pdf-page` rect outside its page's MediaBox was minted, and is now refused C-45.1 wherever the record holds the page's box (an absent box is admitted and stated, `undetermined {level: page_box}`). I2 MINOR: `pageBoxes {boxes, of_page}` is a new member of the structure object. I5 MINOR: the stored reading gains `page_boxes` (absent / null / object, page_count's three-state rule).
- **The change:** `pdfstructure.mjs` reads /MediaBox and /Rotate up the page tree; `checkContentExtent` bounds the rect by the MediaBox (never the CropBox; /Rotate carried and named, never applied). C-45.1's translation widened to a region of a page. `op=versionnotice`'s bound-held switch asks the box too.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-374's branch stated the interface and left the IC to the integrator.
