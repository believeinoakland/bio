## IC-401 · I2: a workbook's named units emit `sheet-range` units: `text().rangeUnits` from .xlsx defined names and table parts and .ods named and database ranges, each carrying its author's name, scope and hidden flag; `rangeUnitsSkipped` with a reason · ACCEPTED 2026-09-25, MINOR — I2 2.17.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-415)

- **Interface:** I2.
- **Classification:** MINOR. additive: two new members of the workbook text object; every other output is unchanged. Only a name naming ONE rectangle on ONE sheet is emitted, never widened.
- **The change:** Through the one `sheetRangeRef` builder. Not indexed (D-672 indexes whole sheets only).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-415's branch stated the interface and left the IC to the integrator.
