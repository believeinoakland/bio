## IC-377 · I3: a re-read (`op=pdfstructure&ocr=1`) asks the OCR member only for the pages past the budget: pages a stored tier-3 part transcribed are kept, and with nothing left to ask the member is not called and the re-read is not performed · ACCEPTED 2026-09-25, MAJOR — I3 146.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-616)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed answer class for the same request — a re-read of a capture whose OCR'd pages the record holds re-asked the member for the same first pages and answered `performed: true`; it now answers `performed: false` with "already transcribed" (ocr-member-e2e §10 corrected), and `candidate` reads `stillWanting`. The tail past D-606's budget is now read, which is additive. Proposed under I6; I6 does not move (the member's contract is unchanged).
- **The change:** `tier3SeedFrom` reads the stored chain and the capture's untruncated `pdf-page` units; a kept page stays under the chain of the build that read it (a changed build yields two scoped tier-3 parts).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-616's branch stated the interface and left the IC to the integrator.
