## IC-367 · I3: `op=linkproject` answers through the viewer (the disclosure half): a hidden source answers as a capture the record does not hold and writes nothing; an edge to a hidden target is not listed and moves no count · ACCEPTED 2026-09-25, MAJOR — I3 139.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-706 @ 6dd3e530; the row assigned the IC to the integrator; D-706)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning — for a caller who cannot see, `edges[].target_capture`, `edges[].to` and `source_bundle` are withheld and `projected`, `skipped_self`, `skipped_unregistered` count visible targets only; a hidden source now writes nothing where it projected. Stacked on D-701.
- **The change:** Two resolutions: `seen` decides what is SAID, the unfiltered one what is WRITTEN (the write half is BOB #36's, stated in LINK-FIDELITY). The subresources callers name the machine viewer.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-706's branch stated the interface and left the IC to the integrator.
