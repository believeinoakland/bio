## IC-386 · I3 + I5: a workbook's sheets are indexed units: one `sheet-range` unit per sheet in `capture_text` (xlsx, ods, csv), found by passage search · ACCEPTED 2026-09-25, MINOR — I3 150.5.0; I5 8.4.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-672)

- **Interface:** I3 + I5.
- **Classification:** MINOR. additive, IC-349's reading: a workbook that read "no unit arm" (none with a reason) now carries units; `capture_text.extent_kind` gains the value `sheet-range` (schema comment only, no DDL). A workbook promoted before still reads as it did until re-promoted.
- **The change:** `textUnitsFor` recognises `sheets[]`; D-415's named ranges are NOT indexed (stated in CONTENT-SEARCH-DESIGN §4.1).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-672's branch stated the interface and left the IC to the integrator.
