## IC-341 · I3: project create and fork take `visibility` (`discoverable` | `hidden`, absent is HIDDEN); a machine's `discoverable` is refused (`PROJECT_VISIBILITY_NO_OWNER`, C-97.1), and `visibility` on anything but a project's creation is refused (`PROJECT_VISIBILITY_NOT_A_CREATION`, C-97.2); the creation answer carries `visibility` · ACCEPTED 2026-09-25, MINOR — I3 126.8.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; REC-197)

- **Interface:** I3.
- **Classification:** MINOR. additive: both refusals judge a field no earlier contract defined, and an absent field writes nothing (the sight index already read no act as hidden), so every request formed under the published contract answers as before, plus one key on a project creation's answer.
- **The change:** BOB #32's ruling (b), 2026-09-23. The owner's choice is recorded in `project_visibility` inside the creation's transaction; the answer reads `#visibilityOf`.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** REC-197's branch stated the interface and left the IC to the integrator.
