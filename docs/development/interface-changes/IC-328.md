## IC-328 · I3: a whole-hash register row held in PARTS ratifies and publishes when every part the record names verifies; otherwise the gate names `PLANE_PART_MISSING` / `PLANE_PART_UNVERIFIED`; `op=ratify`'s `published` gains `parts`; `registerholds` takes `bundle=` · ACCEPTED 2026-09-25, MAJOR — I3 119.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-556)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a gate finding token moves for an unchanged input — a parted row whose bundle's `data/provenance.json` names its parts was refused `PLANE_HELD_IN_PARTS` and now passes, or is refused `PLANE_PART_MISSING` / `PLANE_PART_UNVERIFIED`. `PLANE_HELD_IN_PARTS` remains for a row whose record names no parts.
- **The change:** The gate heads and digests each named part (D-533's `partsHeld`, now keyed by the caller's bucket); publication copies the parts part by part (`kind: capture_part` manifest rows), re-verifies them in the published bucket, and reports `published.parts: { parts, verified, missing_parts?, disagreeing_parts?, unverified_parts? }`, with `r2` reading `INCOMPLETE: …` when a part does not verify. The internal `registerholds` route answers `parts` when `bundle=` is named.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-556's branch stated the interface and left the IC to the integrator.
