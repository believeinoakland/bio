## IC-210 · I3: codeless refusals get codes — storage-absent 503s (`EVIDENCE_STORAGE_NOT_CONFIGURED`, C-68.1), the unknown op (`UNKNOWN_OP`, C-69.1), required arguments on verify/publishedbytes/publishedcase/knock (C-61.1) and the bootstrap claim (C-68.2–.4) · ACCEPTED 2026-09-23 (D-278; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration and checked absent on every remote land/* ref; the worker's report proposed it for the integrator to mint and classify)

- **Classification:** MINOR, ADDITIVE — new `reason`/`code`/`check`/`translation` keys (and `op`/`argument`/`shape`/`detail` on the required-argument sites); every `error` string and HTTP status is byte-identical, and `error: "unknown op"` stays the first key so an older UI still recognises it.
- **Families:** INSTALLATION_CHECKS (C-68) and DISPATCH_CHECKS (C-69), minted by the worker.
- **Owed, placed by SCHEDULER:** UI-84 (the mocks carry the new translations) and REC-185 (op=purge's bare refusal).
- **Evidence:** `bio-plane/test/d278-codeless-refusals.test.mjs` (19) and its NEGATIVE CONTROL line (3 arms AS DECLARED).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MINOR 70.0.0 → 70.1.0**, on c17-batch3.
