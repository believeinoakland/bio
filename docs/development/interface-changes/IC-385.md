## IC-385 · I3: `op=content` (and every generic DO-forwarded op) answers `store=`: the edge's own parameter no longer reaches the store's accept set · ACCEPTED 2026-09-25, MINOR — I3 150.4.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-675)

- **Interface:** I3.
- **Classification:** MINOR. a refusal lifted and none added: `op=content&store=…` answered `FIXED_KEY_ONLY rejected ["store"]` and now answers; no Durable Object route reads `store` (grep: zero reads), so no other op's answer moves.
- **The change:** Stripped with `token` and `op` at the generic forward; D-419's `contentCrop` strips it the same way.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-675's branch stated the interface and left the IC to the integrator.
