## IC-327 · I3: a derived-case draft's `edition` is `null` on the wire (op=casedraft, the review-grant, review-copy and statement-acknowledgement answers) · ACCEPTED 2026-09-25, MAJOR — I3 118.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-568)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning of an existing field — `edition` read `1` for a draft that names no case and does not set `newCase`, and now reads `null` (UNDETERMINED until publication derives the case).
- **The change:** `Store.#statedEdition` states the edition only where the draft names a case or asks for a new one; the internal key `(case_id NULL, edition 1)` still binds grants and acknowledgements. A dead grant bound to no case reads `edition: null`.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-568's branch stated the interface and left the IC to the integrator.
