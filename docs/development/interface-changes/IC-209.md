## IC-209 · I3 + I5: a progression definition is VERSIONED — a revision writes version N+1 with its author, date and basis; `op=progression` reads any version; every finding names the version it was read against · ACCEPTED 2026-09-23 (D-128; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration and checked absent on every remote land/* ref; the worker's report proposed it for the integrator to mint and classify)

- **I5, ADDITIVE:** two tables, `progression_def_versions` and `progression_stage_versions` (every version, its author, date and basis); `progression_defs`/`progression_stages` stay the CURRENT version; both purged.
- **I3:** `op=progressiondefine` takes `basis` and `citation` and answers `version`, `prior_version`, `unchanged`, `basis`; `op=progression` takes `version=N` and answers `version`, `current`, `current_version`, `basis`, `version_count`, `versions[]`; `definition_version` is added to the instance, every finding, every discharge, `op=proposals`, `op=captureprogressions` and the queue FINDING subject.
- **Classification:** I3 MAJOR, BREAKING — a CHANGING re-definition without a basis and a citation is now refused NO_BASIS / NO_CITATION where it overwrote before (IC-25: the op refuses what it accepted); an identical re-definition writes nothing. I5 ADDITIVE.
- **Owed, placed by SCHEDULER:** UI-83 (the progression form gains basis and citation fields) and REC-184 (proposal_dispositions records the definition version).
- **Evidence:** `bio-plane/test/progression-versions.test.mjs` (36) and its control (4 arms AS DECLARED); `connection.test.mjs`' superseded REPLACES block CORRECTED.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MAJOR 69.0.0 → 70.0.0; I5 ADDITIVE 1.28.0 → 1.29.0**, on c17-batch3.
