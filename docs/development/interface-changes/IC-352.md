## IC-352 · I2 + I9: a page bears text when it SHOWS text (`pageShowsText`): Tier 1 marks `no_text_layer` on a page that declares a font but shows nothing, and the OCR member no longer refuses such a page `PAGE_HAS_TEXT_LAYER` · ACCEPTED 2026-09-25, MINOR — I2 2.10.0; I9 0.3.1 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-585)

- **Interface:** I2 + I9.
- **Classification:** MINOR. additive: the marker keeps its meaning and fires on pages it wrongly missed (CAFR-2002: 14 -> 175, M-160); on I9 a refusal is lifted and none added (under PROVISIONAL the patch moves).
- **The change:** One predicate in `pdfstructure.mjs` (`TEXT_SHOWING_OPERATORS`), asked by Tier 1 and by `pagepixels.mjs`; `null` when a stream or form cannot be read, and never read as false.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-585's branch stated the interface and left the IC to the integrator.
