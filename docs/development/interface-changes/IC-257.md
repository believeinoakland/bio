## IC-257 · I3: `op=purge`'s missing-`confirm` refusal gains C-61.1's code and canned translation, `error` byte-identical · ACCEPTED 2026-09-24, MINOR — resolved by CONDUCT #20 at integration, I3 81.2.0 → 81.3.0 (REC-185, `land/worker/REC-185`; minted with `node tools/mintid.mjs IC`, a CAS push to coord)

- **Interface:** I3 only. No table changes (I5 untouched). No new DEC-49 family and no new row: the condition is C-61.1's condition and it is minted through the one governed helper `requiredArgument`, so `REQUIRED_ARGUMENT_MISSING`'s `where` still names a single span.
- **Design:** DEC-49, as `docs/architecture/BIO_Assistant_and_AI_Roles_v0_1.md` rule 10 restates it — every refusable condition carries a named code with a canned translation — with D-278's landed `requiredArgument` (C-61.1, D-270) as the precedent this follows rather than extends.
- **Proposer:** REC-185, 2026-09-24. **Owner:** RECORD. **CONDUCT resolves the version and classifies.**

### I3 — what a caller sees

`op=purge` called without `confirm`, or with a `confirm` that is not the store the request resolved to, answered:

```
{ ok: false, error: "purge requires confirm=<store>", expected, got, tokenClass, store }   HTTP 400
```

**No `reason`, no `code`, no `check`, no `translation`** — a refusal DEC-49's guard cannot see at all, one layer further out than a code with no sentence, and on the ONE op in the plane that destroys a record. It now answers:

```
{ ok: false, reason: "REQUIRED_ARGUMENT_MISSING", check: "C-61.1", translation: <the canned row>,
  error: "purge requires confirm=<store>", op: "purge", argument: "confirm", shape: "<store name>",
  detail: "op=purge needs 'confirm' in the shape <store name>, and this request carried none the
           operation could use. Nothing was changed.",
  expected, got, tokenClass, store }                                                        HTTP 400
```

- **`error` is passed into the helper BYTE-IDENTICAL** (D-270's pattern), so every consumer reading `error` moves not at all.
- **`expected`, `got`, `tokenClass`, `store` are kept**, so the old answer is ADDED TO and not replaced. The status stays 400.
- **`detail` states NOTHING WAS CHANGED.** On the plane's only destructive op that is the fact most worth stating: a caller who cannot tell a refused purge from a half-applied one has to go and look.

### Classification (the proposer's; ACCEPTED as MINOR by CONDUCT #20, 2026-09-24: the answer is added to, never replaced)

**I3: MINOR, ADDITIVE, non-breaking.** Nothing that answered is refused and nothing refused is answered; the status, the `error` sentence and all four existing keys are unchanged, and five keys are added. **Measured consumer impact, 2026-09-24 on this tree:** `grep -rl "purge requires confirm"` finds **0** files under `civicos-ui/`, `agent-worker/`, `pdf-worker/`, `ocr-worker/` and `tools/`; the only reader of the sentence is `bio-plane/test/purge.test.mjs` lines 47–48, which assert it by equality and pass unchanged. `newgroup/src/release.mjs` and `release/bio-plane.bundled.mjs` carry embedded copies of the plane's own source and are regenerated by DIST, so they are artifacts of this change rather than consumers of it.

### Evidence

`bio-plane/test/refusal-wire.test.mjs` section 6b (33 pass, 23 before this item), driven THROUGH the op under the admin credential — `purge` is `["admin","probe"]` and so outside the member-class sweep that suite drives, which is why this op was invisible to every wire instrument until now. Six assertions: the code, the C-number and translation compared BY EQUALITY against the catalogue row, the argument and shape, the byte-identical `error`, the NOTHING-WAS-CHANGED sentence, and the four retained keys. `bio-plane/test/purge.test.mjs` unchanged and green. NEGATIVE CONTROL, arm (h) of `test/refusal-wire.control.mjs`: the bare sentence restored at the site → **RED 29/4**, exactly the four declared lines and no others (6b's `error` line held, which is what says the arm tests the CODE and not the wording). OVER-STRICTNESS, arm (i): the same refusal rebuilt AT its site with the code in `code`, no `reason`, the row imported from the catalogue, an unanticipated `detail` and an extra key → **GREEN 33/0**.
