## IC-364 · I3: the credential-free `op=claim`, `op=login`, `op=invitelook` and `op=enroll` answer a store failure as 502 `STORE_DID_NOT_ANSWER`, never HTTP 200 · ACCEPTED 2026-09-25, MAJOR — I3 136.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-679)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed status for the same request — a store that threw reached an anonymous caller at HTTP 200 (`STORE_INTERNAL_ERROR` in the body, IC-354's envelope), and a non-JSON store answer at 500 `PLANE_INTERNAL_ERROR`; both now answer `storeSilent` (502, naming the op). An answer, a returned refusal included, is re-wrapped byte for byte as `{ ok: true, result }`, so every success and every named refusal is unchanged.
- **The change:** The four doors open the envelope through `doAnswer` like the thirteen other public ops (REC-52). Stacked on D-629; IC-354's "relayed at HTTP 200 (unchanged; D-679)" is superseded here.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-679's branch stated the interface and left the IC to the integrator.
