## IC-219 · I5 + I1: a reused part NAMES THE CAPTURE ITS BYTES CAME FROM — `site_assets.last_fetched_by`, `site_asset_refs.reused_from`; the capture manifest's reused part carries `reused_from`; `reusedParts` answers `reused_from` and `reused_from_state` · ACCEPTED 2026-09-23 (CAP-14; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration and checked absent on every remote land/* ref, as the worker's report asked)

- **Classification:** I5 ADDITIVE (two nullable columns via ADDITIVE_COLUMNS, never back-filled; a pre-build reuse reads `undetermined`); I1 MINOR, ADDITIVE (a manifest field and reworded detail/note sentences; no suite pinned the old wording).
- **Evidence:** `bio-plane/test/cap14-reused-from.test.mjs` (23, incl. a live-store migration) and its NEGATIVE CONTROL line (3 arms + over-strictness, AS DECLARED).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I5 ADDITIVE 1.30.0 → 1.31.0; I1 MINOR 1.7.0 → 1.8.0**, on c17-batch5.
