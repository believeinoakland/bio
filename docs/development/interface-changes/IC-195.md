## IC-195 · I3: A DECLARED BOUND STATES ITS ALLOWANCE — `op=airunopen` refuses a `bounds` entry whose `allowed` is absent, `null` or zero (`AI_RUN_BOUND_NO_ALLOWANCE`, C-22.16), and nothing is written · PROPOSED 2026-09-23 (REC-177, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column). **Base read off THIS TREE (`origin/main` @
  `0e7cc03e`): I3 59.2.0 (IC-191). Proposed I3 MAJOR, BREAKING — 59.2.0 → 60.0.0. Read the base AT RESOLUTION.** **Why MAJOR, by
  IC-25's test — the op REFUSES what it accepted:** on `0e7cc03e`, `op=airunopen` opened runs declared
  `[{ bound: "fetches" }]`, `[{ bound: "fetches", allowed: null }]`, `[{ bound: "fetches", allowed: 0 }]` (and `-0`), each at
  `allowed: 0` — which `finishedBound` reads as NO CEILING, so the run's record named a bound it did not have. Each is now refused
  and no run row is written. **An AUTHORITY-ADJACENT narrowing:** a record claiming a ceiling that bounds nothing.
- **Proposer:** RECORD, REC-177 worker (CONDUCT #16, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/INVESTIGATIVE-SESSION.md` §14b item 6, *"A declared bound STATES its allowance"* (BOB #30).
- **Consumers to answer, MEASURED** (grep over `agent-worker/src`, `civicos-ui/`, `tools/`, `bio-plane/src`, `bio-plane/test/`,
  `bio-plane/scripts/` for `op=airunopen` callers and every `bounds` entry with an absent or zero `allowed`): **agent-worker** never
  calls `op=airunopen` (comments only) — unaffected. **civicos-ui/app.html** never calls it (its own comments say so) — unaffected.
  **The skill pack** (`skillpack.mjs`) publishes `RUN_BOUNDS` and declares no run — unaffected. **The battery**: ONE suite declared
  such a bound as an over-strictness PASS — `rec172-bounds.test.mjs` ARM G2 opened `{ bound: "fetches", allowed: 0 }` and
  `{ bound: "subsessions" }` and asserted `[0, 0]` for both; it is CORRECTED with a dated reason (it was REC-172's own stated
  caveat (v) written as a pass), never exempted. The full battery is otherwise unmoved (read off the gate's completion line in
  REC-177's report). `airun.test.mjs`'s `finishedBound([{ allowed: 0 }])` is the pure function's own undeclared-row reading and
  stays. **Live instruments** (`vf4-live-scratch.mjs`, `surfacing-run.mjs`, `rec93-*`, `rec113-identity.mjs`): none declares a
  bound with an absent or zero allowance (grepped).

**THE SHAPE.** `airun.mjs checkConsume`, the `allowance` pass of the open's `list` branch (the one governed span C-22.13–.16 name):
after a bound's NAME is judged (C-22.15, then `lease` C-22.14), an `allowed` that is `undefined`, `null` or `0` is refused
C-22.16 BEFORE its form is judged — so a present figure of the wrong FORM (a string, a fraction, a negative, a non-finite) stays
C-22.13, unchanged. The first refusal in the list wins; nothing is written. `store.mjs aiRunOpen` writes the judged `allowed`
where it wrote `b.allowed == null ? 0 : b.allowed` — the default was the no-ceiling path and is unreachable. `finishedBound` is
untouched: `0` stays the internal "no ceiling" reading ONLY for a bound nobody declared (the tick's upsert of an undeclared
bound, and the plane's own `mints`/`surfaces` counter rows).

**THE WIRE.** `op=airunopen` refused: `{ run, started: false, code: "AI_RUN_BOUND_NO_ALLOWANCE", check: "C-22.16", translation,
note }` — IC-188's open shape; `note` names the bound and whether `allowed` was absent or 0. Every accepted call is byte-unchanged;
a run declaring no such bound opens exactly as before. **Translation** (canned, DEC-49): *"The investigation was given a limit on
part of its budget without saying how much it may use. A limit of nothing would mean no limit at all, so the investigation was not
started. Give it an amount, or leave that part out."*

**WHY A NEW CODE AND NOT C-22.13.** C-22.13 refuses a figure of the wrong FORM and its translation says the amount "is not a whole
number of zero or more" — false of `0`, and of an absent figure there is no amount at all. The remedy differs (state an allowance,
or leave the bound out) and a surface must be able to say which.

**NEGATIVE CONTROL** (`node test/rec177-allowance.control.mjs` from `bio-plane/`, every arm on a COPY, real sources hashed before
and after, untouched): ALL FIVE AS DECLARED — baseline 22/0 · **drop-check (THE ROW'S CONTROL: the check dropped and the store's
default restored) 9/13, ARM A1–A6 and Z1–Z5 by name** · liar-absent-only (admit 0) 16/6 · too-wide 21/1 · overstrict 22/0.
REC-172's (eight arms) and REC-169's (nine arms) controls re-run on the same sources: all as declared.

**FOR CONDUCT at integration:** resolve this IC against the base read then (I3 59.2.0 or later), move I3 in `INTERFACES.md`, and
re-run all three controls (`rec177-allowance`, `rec172-bounds`, `rec169-consume`). The C-22.16 number was allocated in the C-22
family by reading `bio-checks.mjs` (families are minted, sub-numbers are the family's own) — if a concurrent branch also took
C-22.16, renumber at the merge.
