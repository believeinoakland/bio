## IC-258 · I3: a hidden project's RUN rows leave the five observation-log tallies · PROPOSED 2026-09-24 (D-486; minted with `node tools/mintid.mjs IC`)

- **Interface:** I3 (`op=stats` and its `op=selftest` / `op=livefire` relays — the keys `aiRunLog` and
  `observationsNonLead`; `op=frontier&level=document|content|meaning` — the key `tally`; and, at the meaning level
  only, `looked`, `by_subject_kind` and `truncated`). I5 does not move: no table, no column, no index.
- **Classification:** MINOR, not breaking for any credential the gate does not filter. Every key, spelling and
  type is unchanged. A NUMBER changes meaning for one audience only — an identified member session that is not an
  administrator — and only for one row class: a row of `observation_log` whose `authority_kind` is `run` and whose
  run's context is a PROJECT that member's `viewerPredicate` does not pass.
  - `aiRunLog` and `observationsNonLead` drop those rows. `op=purge`'s `observations` is UNCHANGED and still counts
    the whole log (§5: *the purge proof's own count stays whole*); it takes a never-sent viewer.
  - The three bundle-level `tally` fields drop those rows. They still count EVERY other row at their level, still
    do not follow the page, and still name no bundle, subject or address — REC-110's ruling (D-386) is narrowed by
    one row class on Bob's own word, not reopened. The internet level's tally was already viewer-scoped (IC-143)
    and counts no run row.
  - **`op=frontier&level=meaning` additionally withholds a run row ROW-WHOLE**, which is a DEFECT FIXED rather than
    a widening: that arm gates a `capture` and a `reference` and returned `true` for everything else, reasoned for
    an ENTITY, while `#aiRunAppend` stamps every run row `subject_kind = 'unstated'` — a fourth kind. So the level
    published a hidden project's run rows whole, `authority` (the run id) included, where the document arm withheld
    the same rows. `looked`, `by_subject_kind` and `truncated` therefore narrow for a filtered viewer at that level.
  - An absent control-plane stamp: `op=stats` keeps D-464's reading (never sent = a direct internal call = WHOLE);
    `op=frontier` FAILS CLOSED (`viewer` defaults to `null`, which compiles DENY), which is `#frontierDocumentVisible`'s
    posture one method away and the reason `index.mjs` stamps the op.
- **Why (the disclosure):** Membership v2 §7.9, *"Not its existence"*, and **BOB #32's ruling of 2026-09-24 02:30Z**:
  *a hidden project's run output is the PROJECT'S THINKING until something outside uses it; the bytes stay shared,
  only the run's attribution is withheld.* This is the question D-464 (IC-254) explicitly ROUTED rather than decided.
  Measured at the op before the change (MEASUREMENTS M-131): one run over a hidden project moved all five of an
  uninvited member's figures, and the meaning level published the run's id to her.
- **Consumers, measured 2026-09-24 by grep over the tree at `cff0ede6` plus this change:**
  - `civicos-ui/` — 0 reads of `op=frontier`, `aiRunLog` or `observationsNonLead`. No change.
  - `agent-worker/src` — 0 reads. No change.
  - `bio-plane/src/airun.mjs` — `searchedSection` (the case document's `searched` block, D-196/C-41.10) takes its
    `levels` FROM THE CALLER and does not read these tallies, so **no signed artifact's content moves**. This was
    re-driven rather than carried from `OBSERVATION-LOG-DESIGN.md` §6, which states the same fact at its own weight.
  - `newgroup/` — carries the plane only as the embedded release bundle (DIST's artifact).
  - **Tests, corrected with the reason at the site and never exempted:** section J of `observation-log.test.mjs`,
    `observation-content.test.mjs` and `observation-meaning.test.mjs`. J3's second element was an ADJACENCY pin
    (`level = 'content' GROUP BY state`) and D-486 interpolates the predicate between those tokens; it now pins the
    two tokens separately, so a future narrowing to the page still fails there. J5/J5b are new site pins for the
    narrowing, and observation-log's J5 measures the precondition J1's byte-identity now rests on.
- **Proposer:** WORKER D-486 (CONDUCT #20, cloud), 2026-09-24. **Owner:** RECORD. **Design:** Membership v2 §7
  (item 7.9) with BOB #32's 02:30Z ruling; `OBSERVATION-LOG-DESIGN.md` §6.
- **Evidence:** `bio-plane/test/project-sight.test.mjs` §9 and `project-sight.control.mjs` (arms
  `d486-content-tally-unsubtracted`, `d486-stats-airunlog-unsubtracted`, `d486-meaning-run-ungated`,
  `d486-predicate-de-morgan`), recorded on the suite's NEGATIVE CONTROL line. MEASUREMENTS M-131.
- **Version:** CONDUCT resolves it at integration.
