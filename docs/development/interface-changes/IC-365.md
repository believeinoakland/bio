## IC-365 · I3 + I5: `content.chain_kind` is how THAT unit was read (the last derivation step covering its page; a unit with no page reads its pages' one kind, or `mixed`); a PLAIN column, and its MIGRATION REBUILDS the `content` table; `content:mixed` is a search word · ACCEPTED 2026-09-25, MAJOR (I3, I5) — I3 137.0.0; I5 8.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-686 @ 526cc17f; the row assigned the IC to the integrator; D-686)

- **Interface:** I3 + I5.
- **Classification:** MAJOR (I3, I5). IC-25: a changed meaning — I5: `content.chain_kind` was REC-104's GENERATED column over the whole chain's last step and is now a plain column holding the unit's own kind, with a new value `mixed`; `capture_text.chain_kind` keeps the document-level meaning and says so. I3: `content:ocr` (and every `content:<kind>` arm) now matches by how the passage's page was read, not the chain's last step, and `mixed` joins the `chain` vocabulary.
- **The change:** `textchain.mjs chainKindFor` is the one computation (mint, `#migrate`'s recompute, the capture_text writer via `CHAIN_LAST`). **THE MIGRATION REBUILDS `content`:** a REC-104 store's generated column cannot be dropped in place (workerd's DROP COLUMN leaves the closing paren inside the last line's `--` comment and bricks the Durable Object, measured), so the table is re-created as the schema declares it, every row copied, the old dropped and the new renamed into place, then every row's kind recomputed ONCE from its own chain; a pre-REC-104 store gains the column and is recomputed. Covers the branch tip 526cc17f: `mixed` (591ecfa6, BOB #35 09:35Z) arrives with the second composition; c22-batch30 @ d557a837 holds only 8c55a9c2. D-710 (running) rides this IC.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-686's branch stated the interface and left the IC to the integrator.
