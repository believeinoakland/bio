## IC-332 · I3: `op=monitor` grades NO change for an unmonitorable shell: `status` is withdrawn, the look's outcome is `unmonitorable` and it is logged LOOKED_INDETERMINATE · ACCEPTED 2026-09-25, MAJOR — I3 123.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-338)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning — the same tick over a document whose contract is `unmonitorable` answered `status: modified` (and raised re-evaluation) or `unchanged`; it now answers `status: null` with a `note` saying why, and its observation-log look reads LOOKED_INDETERMINATE where it read PRESENT.
- **The change:** A 404 is not touched (a gone address is a finding whatever it served). D-60's flag is no longer raised for a shell whose address still answers.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-338's branch stated the interface and left the IC to the integrator.
