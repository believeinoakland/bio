## IC-300 · I3: `op=promote` honours `replay` only over a drive-provenance capture it verifies, and refuses an unverifiable one (`REPLAY_UNVERIFIED`, C-66.6) · ACCEPTED 2026-09-25, MAJOR — I3 103.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-512)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — an admin's asserted replay landed before (with the fences skipped).
- **The change:** Every replayed promotion, of any type and revision, must name `provenanceCapture`: registered by this promotion at the Drive provenance path, bytes held and hashing to the sha named, and one preserved promotion record naming this bundle and this revision's bundle.md SHA-256. An admin asserting a replay it cannot show is refused with nothing written; a non-admin's flag is still removed silently (D-511).
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-512's branch stated the interface and left the IC to the integrator.
