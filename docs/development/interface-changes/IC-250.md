## IC-250 · I3: `store=scratch` on a public op that always answers from `bio` is REFUSED by name (`NAMESPACE_PINNED`, C-78.2), where it answered from the real record · PROPOSED 2026-09-24 (D-461; `node tools/mintid.mjs IC` answered IC-245, and the `land/*` refs on the remote held IC-240..IC-244 and nothing above) — the version bump, the classification and the RESOLUTION are CONDUCT's

*Proposed as IC-245 by D-461's clone; renumbered IC-250 at integration by CONDUCT #19's c19-batch10 (IC-245 is D-351's, merged first on the batch).*

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column).
- **Base read off THIS TREE (`origin/main` @ `15b2a4c0`): I3 74.0.0. Proposed I3 MAJOR, BREAKING — 74.0.0 → 75.0.0 on this
  base. Read the base AT RESOLUTION.** Why MAJOR, by IC-25's test: a refusal where none stood. A request that answered
  from `bio` before now answers 400.
- **Proposer:** RECORD, WORKER D-461 (CONDUCT #19, cloud), 2026-09-24.
- **Owner to land it:** `RECORD`
- **Design:** `CLAUDE.md` §5 (*NAME `store=scratch` ON EVERY CALL*, D-325); D-456's `namespaceGate` (IC-237). The row's
  named design, `docs/development/VERIFICATION.md`, holds no namespace rule (the row's own note says so).

### The change, on the wire

- **CORRECTED 2026-09-24 at c20-batch11fix, and the old figures were TRUE for the tree this entry was written on
  (c19-batch10): TWELVE -> THIRTEEN pinned ops and THREE -> FOUR exempt.** Two arrivals at the c19-batch11 union, both
  MEASURED off the OPS table on this tree with the suite's own matcher rather than counted by hand: `groupidentity`
  joined the EXEMPT list (REC-164's op reads `store=` itself, `op=instancegroup`'s way — the red where c19-unionfix met
  c19-batch10), and `statementack` is a thirteenth PINNED op the c19-batch10 enumeration does not name. Measured here:
  17 `classes: null` ops, 4 exempt, 13 pinned.
- The thirteen `classes: null` ops other than `invitelook`, `enroll`, `instancegroup` and `groupidentity` — enumerated
  from the OPS table: `publishedmanifest`, `publishedcase`, `publishedbytes`, `caseflags`, `casedocument`, `reviewcopy`,
  `reviewcomment`, `statementack`, `bootstrap`, `claim`, `login`, `verify`, `knock` — answer a request carrying exactly
  `store=scratch`, with or without a
  credential, before anything is read:
  `400 { ok: false, reason: "NAMESPACE_PINNED", code, check: "C-78.2", translation, error, op, asked: "scratch", pinned: "bio" }`.
  Nothing is read or written. ONE governed span: `pinnedNamespaceGate > is-pinned-namespace-gate` (family `NAMESPACE_CHECKS`).
- The exemption is the list of public ops that DO address scratch (`SCRATCH_ADDRESSING_PUBLIC_OPS`); a `classes: null`
  op added later is refused `store=scratch` until it answers from scratch and is listed.
- **Unchanged:** `store=bio` and an absent `store=` on those ops; `store=scratch` on the four exempt ops; every gated op
  (`scopeFor`); D-456's `NAMESPACE_UNKNOWN`, which still runs first.
- Before this change, measured by control arm A: `op=knock&store=scratch` answered `ok` and the knock landed in `bio`'s
  inbox (the suite's witness assertion fails in that arm). `claim` and `reviewcomment` wrote `bio` the same way.

### Consumer impact, measured

- `civicos-ui`: sends no `store=` (`grep -c "store=" civicos-ui/app.html` = 0). No surface moves.
- `agent-worker`: forwards a run's `store` on every call, but calls only gated ops (`whoami`, `airun*`, `search`,
  `suggest`, `basisversions`, `versionchain`, `capturerequest`). Unaffected.
- `newgroup/`: names `store=scratch` only for `op=instancegroupseed`, a gated op. Unaffected.
- `tools/`, `bio-plane/scripts/`, `scripts/`: no caller of any of the twelve ops. `pdf-worker`, `ocr-worker`: never address
  the plane's namespaces.
- The battery: `d456-namespace-scope.test.mjs`'s `store`-reader sweep named the new reader and was CORRECTED (dated
  comment). `check-refusal-codes.mjs --strict`: rows 323 → 324, census 632 → 633, reach 376 → 377, governedSites 141 → 142,
  regions 124 → 125, regionLines 3299 → 3304, codesChecked 365 → 366, outcomeReturns 141 → 142, refusalsJudged 365 → 366 —
  each from the guard's own print; a pristine `15b2a4c0` worktree gated all 18 keys at slack 0.

### Evidence

`bio-plane/test/d461-pinned-namespace.test.mjs` (55 assertions, through the op): the pinned set read from the OPS table at
run time; `op=knock&store=scratch` refused and the record's counters (stats, inbox, roster, `bio/inbox/` R2 keys) unmoved;
every pinned op refused by code, C-number and the row's sentence, with and without a credential; both namespaces' counters
unmoved; `store=bio` and absent answer without the refusal; the four exempt ops shown to address scratch (a scratch-only
invitation `bio` cannot see); a gated op naming scratch answers from scratch; the witness shown to move when a knock does
land in `bio`. NEGATIVE CONTROL: `node test/nc-d461.mjs`, four arms, 4/4 as declared.

**RESOLUTION · 2026-09-24 · ACCEPTED by CONDUCT #19 (c19-batch10) — I3 MAJOR, BREAKING 81.0.0 → 82.0.0** (base read on c19-batch10 after D-351's merge; the proposal's 74.0.0 → 75.0.0 was read on `15b2a4c0`). As proposed: a request that answered from `bio` now answers 400 `NAMESPACE_PINNED`, a refusal where none stood. I5 does not move. The DEC-49 region `pinnedNamespaceGate > is-pinned-namespace-gate` is governed: `regionLines` 3584 → 3589 and every other refusal-code floor +1, re-read from `check-refusal-codes.mjs --strict` on the merged tree.
