## IC-208 · I3: `op=affordances` publishes the seven ROSTER acts (each derived from its own refusal, asked of the roster `by` stamp in THIS project), and withholds fifteen acts from a MACHINE caller that the store refuses it · ACCEPTED 2026-09-23 (D-311; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration, as the worker's report asked)

- **Interface:** I3 (the op contracts). I5 does not move.
- **(a) ADDITION:** the act catalogue grows 20 -> 27; the seven roster acts leave NON_ACTS and are offered by `affordanceFacts`' new `roster` fact (#isProjectOwner, #participation, ownerMath, #rescueRefusal, byte-identical refusals), `needs: null`, weight `single`. `op=queue`'s options stay equal to `op=affordances`.
- **(b) NARROWING:** fifteen acts (release, conclude, withdrawconclusion, reopen, publish, inquirydivide, inquiryground, actionmove, actioncorrespond and six version acts) are no longer offered to a `class:*` or `ai` caller, whom the store already refuses; `actor_is_machine` null does not narrow.
- **Classification:** MAJOR, BREAKING, for half (b): a consumer asking as a machine credential reads fewer acts than it did (the answer was false before, offering acts the store refused, and the correction is still a change a client can observe). Half (a) alone would be MINOR.
- **Consumers measured by the worker:** nine plane suites that asked as the machine token and asserted the false offers (each CORRECTED with a dated reason, never exempted), and the UI registry: `civicos-ui/app.html` SURFACES.project.acts now lists the seven roster acts (a CROSS-AREA edit, RATIFIED by CONDUCT #17: the surface's ROSTER_ACTS table has hosted all seven since UI-3; surface-registry floors A3/A4d/A4e moved to the printed 29/25/25; 434/0 on the union).
- **Evidence:** `bio-plane/test/d311-roster-affordances.test.mjs` (21) and `test/d311.control.mjs` (4 arms, 0 other than declared, re-run on the union).

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MAJOR 68.0.0 → 69.0.0**, on c17-batch3.
