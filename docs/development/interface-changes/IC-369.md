## IC-369 · I3: seven refusal codes move to rows whose sentences are true of them: `OBS_PRESENT_REFERENT_UNBACKED` (C-22.17), `TEXT_CHAIN_TYPED_UNNAMED` (C-35.15), `TEXT_ATTEST_UNATTRIBUTED` (C-35.16), `TEXT_ATTEST_UNDATED` (C-35.17), `CAL_UNATTRIBUTED` (C-42.8), `CAL_SUBJECT_UNNAMED` (C-42.9), `CAL_SUBJECT_NO_PROBE` (C-42.10) · ACCEPTED 2026-09-25, MAJOR — I3 141.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-668 @ 02848426; the row assigned the IC to the integrator; D-668)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: wire tokens that move — each condition answered another row's code (`OBS_PRESENT_NO_REFERENT` at its referent-fault site, C-35.5's machine sentence for a typed step, `TEXT_ATTEST_MACHINE`, `TEXT_ATTEST_EXTENT`, `CAL_NO_PROBE` for a missing `measured_by`, `CAL_UNNAMED`, `CAL_NO_PROBE` at the subject register) and now answers its own. None is a refusal where an answer stood; each is a code moved for the same input.
- **The change:** C-35.5 reworded to its two remaining sites; `referent_fault` unchanged. The C-numbers are the row's; the union re-reads them.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-668's branch stated the interface and left the IC to the integrator.
