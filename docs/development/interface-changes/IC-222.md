## IC-222 · I3 + I5: DISCOVERABLE or HIDDEN (Membership v2 §7.14, step 1) — the owner's setting, the directory, and C-70.1 at every project-targeted act at EXISTENCE · PROPOSED 2026-09-23 (REC-149; minted with `node tools/mintid.mjs IC`)

- **Interfaces:** I3 (three new ops, a new answer at twenty existing acts, a new parameter on five routes) and I5 (one new
  table, `project_visibility`, before the `host_governor` block; cleared by both purge arms).
- **Classification (the integrator's):** proposed MINOR for I3 — nothing any caller receives today changes, because no project
  is discoverable until an owner makes it so (no record = HIDDEN, and no migration writes one). Once one is, an act a member
  outside it aims at it answers `PROJECT_SEEN_NOT_A_PARTICIPANT` (C-70.1) where it answered `NO_SUCH_PROJECT` / `ABSENT` /
  `NOT_INVITED` / `VERSION_CURRENT_UNRELATED` / `NOT_A_PROJECT` / `REVIEW_NOT_PROJECT_OWNER` / `AI_RUN_NO_SUCH_CONTEXT`
  before. I5: additive.
- **New ops:** `op=projectvisibilityset&projectId=&setting=discoverable|hidden[&reason=]` (mutating, in `PROJECT_ACTIONS`:
  `by` and `viewer` stamped; owner only — C-70.2 for everyone else, machines and administrators included; C-70.3 for another
  word); `op=projectvisibility&projectId=` (read: `{ setting, recorded, history[{setting,set_by,reason,at}] }` to a caller with
  full sight, else `NO_SUCH_PROJECT`); `op=projectdirectory` (read, member session: `{ projects[{id,name,request:null}],
  requests: "NOT_BUILT: …" }`; C-70.4 for a credential with no member).
- **The new answer:** `{ ok:false, reason:code:"PROJECT_SEEN_NOT_A_PARTICIPANT", check:"C-70.1", translation, detail,
  project, name }` — the id and name and nothing else (the run open adds its own `run`, `started:false`). Asked at: promote
  (a revision), cite, sever, reinstate, proposedispose (project arm), publish, versioncurrent, conclude (project arm),
  biasadopt (project scope), casedraft (a new draft), airunopen (a word in `RUN_CONTEXTS`), ratify (`gatefacts`, answered 404
  by the control plane's existing `!facts.ok` branch — NOT driven through `op=ratify` by the suite; stated, not measured), the five viewer-stamped roster acts, projectjoin, projectleave,
  projectremove, projectvisibilityset.
- **Stamps added:** `viewer` now reaches `biasadopt` and `casedraft` (added to the control plane's viewer-stamp list) and the
  DO routes of `projectjoin`, `projectleave`, `projectremove`; each asks it ONLY for EXISTENCE.
- **Unchanged, measured:** `viewerPredicate` (query.mjs) is untouched; REC-138's `project-sight.test.mjs` green UNEDITED
  (94/0) and its control's arms as before (the one arm that throws, `promote-stamp-dropped`, throws identically on pristine
  `02603e88` — a pre-existing defect, reported).
- **Consumer impact, measured:** `civicos-ui/app.html` calls none of the three new ops (construct-status probe `uinone`); the
  UI harness is green. No suite outside this item's changed (one classification line in `gate-reads.test.mjs`).
- **Proposer:** REC-149 worker (CONDUCT #17, cloud), 2026-09-23. **Owner:** RECORD. **Design:** Membership v2 §7 item 7.14.
- **Evidence:** `bio-plane/test/project-discoverable.test.mjs` (154) and `project-discoverable.control.mjs` (six arms, AS
  DECLARED).
