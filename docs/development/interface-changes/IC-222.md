## IC-222 · I3: `op=reviewcopy` carries DEC-31's in-band quartet (`inband`), and the case container's assembly answer carries the same quartet, both computed by ONE function (`bio-plane/src/inband.mjs` `inbandQuartet`) · PROPOSED 2026-09-23 (REC-148)

- **Interface:** I3. I5 does not move (no table, no column).
- **Classification:** MINOR, ADDITIVE. Two new keys, nothing removed or renamed:
  - `op=reviewcopy`'s answer gains `inband: { format: "bio-inband/1", hash: { algorithm: "sha256", sha256, over, bytes,
    canonical }, date, author, floors: { capture, connection, declared, detail } }`, as its LAST key. `hash` is over the
    answer exactly as served minus `inband` (`JSON.stringify(rest, null, 1)`, UTF-8); `date`/`author` are the draft's
    `updated_at`/`updated_by`; `floors` are the producing project's `required_strength` as `#projectBar` reads it, the
    quantity `op=publish` freezes into the case document.
  - `op=ratify` / `op=caseratify`'s `container` object (present when an edition completes) gains `inband`: `hash` is the
    manifest's (`manifest_sha`, unchanged), `date` the edition's `ratified_at`, `author` the case document's signer, `floors`
    the frozen `bar`. MANIFEST.json's bytes and hash are UNCHANGED (the same serialisation, now in one place).
  - The DO method `reviewCopy` also returns `required_strength` (the control plane reads it into `floors` and does not serve it).
- **Consumers measured:** `grep -rn "reviewcopy\|\.container\.inband\|inband" civicos-ui agent-worker newgroup/src` — no UI calls
  `op=reviewcopy` (13.review-copy's surface is ABSENT); no consumer reads `container` keys by exhaustion.
- **Proposer:** REC-148 worker (CONDUCT #17, cloud), 2026-09-23. **Owner:** RECORD. **Design:** `BIO_Publication_v0_1.md`
  §6A.3 point 1 (DEC-31's bound rule; BOB #16's measurement of 2026-09-19).
- **Evidence:** `bio-plane/test/reviewcopy-inband.test.mjs` (22) and `reviewcopy-inband.control.mjs` (baseline 22/0; (a)
  canonicalise differently at the review copy's site 18/4; (b) over-strictness 22/0 green; (c) floors swapped 19/3).
- **What this unblocks:** §6A.3 point 1's precondition for a surface offering to export a review copy. The surface and point
  2's statement at the act stay UI's and are NOT built.
