## IC-340 · I3: the records-request lifecycle: correspondence entries take `stage` / `follows` / `outcome` / `exemptions` / `due_by` / `due_cite` (C-94, LIFECYCLE_CHECKS), `op=actioncorrespond` refuses a `due_cite` off the governing laws (`DUE_CITE_NOT_GOVERNING`), and `op=projection`'s action block carries `lifecycle` · ACCEPTED 2026-09-25, MINOR — I3 126.7.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-147)

- **Interface:** I3.
- **Classification:** MINOR. additive: the refusals judge only keys no earlier grammar defined, so no request formed under the published contract answers differently; `op=affordances` gains `correspondence_stages` / `correspondence_outcomes`.
- **The change:** No due date is computed from a kind, a law or a stage (D-149); with none stated it is UNDETERMINED. BIO_Case_Making_v0_1.md §2 (BOB #27).
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-147's branch stated the interface and left the IC to the integrator.
