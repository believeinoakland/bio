## IC-387 · I3 + I5: a `text/csv` capture read as text at intake reaches its format entry: its `sheet-range` units are indexed and its reading gains `text_container` · ACCEPTED 2026-09-25, MINOR — I3 150.6.0; I5 8.5.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-684 @ 9f6112d3; the row assigned the IC to the integrator; D-684)

- **Interface:** I3 + I5.
- **Classification:** MINOR. additive: units and a container where none were claimed; the profile reading (reader, entities, classified text, provenance digest) is unchanged. Stacked on D-672.
- **The change:** After the three reading branches, a textual capture whose FORMAT entry declares `text()` is handed the bytes the profile already read whole. D-694 minted (no `container_extent` on that path).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-684's branch stated the interface and left the IC to the integrator.
