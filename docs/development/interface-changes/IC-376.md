## IC-376 · I3 + I5: OBSERVATION-LOG-DESIGN §4.2's fourth outcome: a scan read to nothing by tier 3 is `LOOKED_ABSENT`; empty text with pages left unread, or with no tier that reads images, is `LOOKED_INDETERMINATE`; the reading carries `text_chars`, `text_glyphs`, `text_undetermined` · ACCEPTED 2026-09-25, MAJOR (I3) / MINOR (I5) — I3 145.0.0; I5 8.2.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-375)

- **Interface:** I3 + I5.
- **Classification:** MAJOR (I3) / MINOR (I5). I3 MAJOR, IC-332's (D-338's) precedent: the observation a promotion files for a reading whose text holds no glyph read `PRESENT` and now reads `LOOKED_ABSENT` or `LOOKED_INDETERMINATE` — a state moved for the same input. I5 MINOR: three counts on the stored reading. A reading persisted before this landing carries no count and reads exactly as before. Proposed as additive; classified on IC-332's precedent.
- **The change:** `contentObservationsFor` reads the counts before row 3 (the reader declines empty text, so a scan OCR'd to nothing arrives `read_from_text: false`); a region below the OCR floor is residue (`ocr_below_floor`), reported as a design gap against §4.2.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-375's branch stated the interface and left the IC to the integrator.
