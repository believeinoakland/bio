## IC-326 · I3: `op=promote` takes a bundle's title and state from the DOCUMENT and refuses an envelope that contradicts it (`ENVELOPE_TITLE_DISAGREES` C-86.3, `ENVELOPE_STATE_DISAGREES` C-86.4); a minted project slug is taken from the document's title · ACCEPTED 2026-09-25, MAJOR — I3 117.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-563)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — a promote whose `meta.title` / `meta.current_state` disagreed with the bytes landed (the projection showed the envelope's value); it is now refused by name with nothing written.
- **The change:** `title`, `current_state`, `prior_state` and `closed_reason` are derived once from the sent `bundle.md` (the envelope is the fallback where the document states nothing); every fence (7.1's name scan, 7.11's owner test, REC-181's retirement arm) and the projection read the derived value. Comparison collapses whitespace; case is kept.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-563's branch stated the interface and left the IC to the integrator.
