## IC-236 · I3: a `store=` naming no namespace is REFUSED by name (`NAMESPACE_UNKNOWN`, C-78.1) for every caller, where it fell through to the real record · PROPOSED 2026-09-23 (D-456; `node tools/mintid.mjs IC` answered IC-234 from this clone, which `git ls-remote` showed already held on land/* along with IC-222..IC-235, so the id was taken past the remote max) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column).
- **Base read off THIS TREE (`origin/main` @ `a8f6094a`): I3 72.3.0. Proposed I3 MAJOR, BREAKING — 72.3.0 → 73.0.0 on this
  base. Read the base AT RESOLUTION.** Why MAJOR, by IC-25's test: a refusal where none stood. A request that answered
  `ok` before (from the `bio` namespace) now answers 400.
- **Proposer:** RECORD, WORKER D-456 (CONDUCT #18, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `CLAUDE.md` §5 (*NAME `store=scratch` ON EVERY CALL*, D-325); the row's named design, `docs/development/VERIFICATION.md`,
  holds no namespace rule at all (DESIGN GAP, reported).

### The change, on the wire

- Every op, every class (admin, member, probe, daemon, `ai`, a signed-in session) and the no-credential path: a request
  carrying `store=` whose value is not EXACTLY `bio` or `scratch` answers, before any credential is read,
  `400 { ok: false, reason: "NAMESPACE_UNKNOWN", code, check: "C-78.1", translation, error, asked, namespaces: ["bio","scratch"] }`.
  That includes a namespace that does not exist (`biosmoke-pdf`), a case variant (`Scratch`, `BIO`) and an EMPTY `store=`.
  Nothing is read or written. The ONE governed span is `namespaceGate > is-namespace-gate` (family `NAMESPACE_CHECKS`).
- `scopeFor` no longer defaults an unrecognised value to `bio`; one that reached it would be refused (`SCOPE_REFUSED`).
  No op reaches that arm today because the gate runs first (control arm C, recorded GREEN).
- **Unchanged:** an ABSENT `store=` (probe → `scratch`, every other class → `bio`); `store=bio` (probe still `SCOPE_REFUSED`);
  `store=scratch` for every class; the public ops pinned to `bio`, which still ignore a valid `store=`.
- Before this change, measured on `a8f6094a` by control arm A: `op=stats&store=biosmoke-pdf` with the admin token answered
  `store:"bio"`, and an admin, member, `ai` or session `op=promote` naming `biosmoke-pdf` WROTE THE REAL RECORD (the witness
  assertion fails in that arm).

### Consumer impact, measured

- `civicos-ui`: sends no `store=` at all (`grep -c "store=" civicos-ui/app.html` = 0). No surface moves.
- `agent-worker`: forwards the run body's `store` to the plane (`askPlane`), accepting any `/^[a-z0-9_-]+$/i` token. A run
  naming anything but `bio`/`scratch` now meets the plane's `NAMESPACE_UNKNOWN` at its first call (`whoami`), which the worker
  relays UNCHANGED as `PLANE_REFUSED` (its own rule: a plane refusal is passed through as the plane worded it) — where before
  the run worked the REAL record. Its suites use a mock plane and do not move. Not edited here (FLEET's path); reported.
- `pdf-worker`, `ocr-worker`: take a `store` too, but only as an R2 key prefix for a READ (`<store>/captures/<sha>`); an
  unknown prefix answers `NOT_FOUND` and writes nothing. They never address the plane's namespaces. Unaffected.
- `tools/`: no caller passes `store=`. `newgroup/`: names only `store=scratch`, in the setup page's text.
- The battery: the 33 suites that pass `store=` or assert `SCOPE_REFUSED` all pass unchanged (none passed an unknown
  namespace). `check-refusal-codes.mjs --strict`: families 41 → 42, rows 322 → 323, census 631 → 632, reach 375 → 376,
  governedSites 140 → 141, regions 123 → 124, regionLines 3294 → 3299, codesChecked 364 → 365, outcomeReturns 140 → 141,
  refusalsJudged 364 → 365 — each from the guard's own print, every key re-gated at slack 0 (pristine `a8f6094a` printed
  exactly the old floors).
- Live callers that take a store from argv (`rec88-instance-census.mjs`, `surfacing-run.mjs`, `vf4-call.mjs`,
  `fl1-cpu-probe.mjs`) now get a named refusal instead of the real record when handed a wrong name.

### Evidence

`bio-plane/test/d456-namespace-scope.test.mjs` (76 assertions, through the op): 7 callers × a read op and a mutating op ×
three spellings (`biosmoke-pdf`, `Scratch`, empty) each refused by code, C-number and the row's own sentence; op=instancegroup
refused on both of its arms; the record's counters in BOTH namespaces read before and after every refused call and unmoved;
`bio`, `scratch` and absent answer as before per class, and a `store=scratch` write lands in scratch only; every reader of the
`store` parameter in `src/index.mjs` listed from the source and required to be one the suite drives. Its NEGATIVE CONTROL line
records four arms, each AS DECLARED (`node test/nc-d456.mjs`).
