## IC-236 · I3: `op=connections`' ENTITY arm publishes `derivation {state, cut, at, documents, derived, says}` — whether the DERIVATION behind its rows was CUT, read from REC-95's latest meaning-level observation; no row is stated in words (never derived · pre-log · undetermined), never an absent key · PROPOSED 2026-09-23 (D-241, minted with `node tools/mintid.mjs IC` BEFORE the entry was written, past the remote max IC-234 on `land/conduct/c17-batch7`) — the version bump, the classification and the RESOLUTION are CONDUCT's (D-241; minted on its clone as IC-235, which collided with D-126's IC-235 on this batch, and RENUMBERED IC-236 by CONDUCT #18 with `node tools/mintid.mjs IC` at c18-batch8)

- **Interface:** I3 (the op contracts). I5 does not move: no table, no column; the read is one hit on `observation_log_frontier`.
  **Base read off THIS TREE (`origin/main` @ `02603e88`): I3 67.0.0 (IC-206).** **Proposed I3 MINOR, ADDITIVE** — 67.0.0 → 67.1.0 on this base; read the base AT RESOLUTION.
- **The change:** on `op=connections&id=<entity>` only, a new key `derivation`:
  `state` (the latest derivation row's own state — `partial` · `PRESENT` · `LOOKED_ABSENT` — or `null` when there is none), `cut` (`state === "partial"`, `null` with no row),
  `at`, `documents` (the ends that derivation read, parsed from the row's own detail beside its writer; `null` if unrecorded), `derived`
  (`derived` · `never_derived` · `pre_log` · `undetermined`, the last three from §5.1's `#missingMeaningCause`), and `says` (the sentence).
  `truncated` is UNCHANGED and still means only that THIS PAGE was cut at `limit`. The capture arm (`&sha256=`) and the portion arm (`&content=`) do not carry the key.
- **Why:** a subject derived over 32 of its 40 documents (the default pair bound of 500) read back `truncated: false` over 496 connections — the record presenting part of the set as the set (CLAUDE.md §2, "less narrative" binds us first).
- **Consumers, MEASURED:** `civicos-ui/app.html` reads `op=connections` at the subject view (`recR("connections", …)`, ~line 14614) and renders `count` / `truncated` through `connectionsBoundHtml`; it reads no `derivation`, so nothing it does changes. **It does not yet SAY a cut derivation** — owed by UI (named in the D-241 report). Every suite reading `op=connections` re-ran GREEN unchanged (18 suites plus hygiene; `bounds.test.mjs` 173/0 — the capped roster's answer shape moved only by an added key, so it needed no correction).
- **Proposer:** RECORD, D-241 worker (CONDUCT #18, cloud session WORKER D-241 (CONDUCT #18)), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/CONTENT-SEARCH-DESIGN.md` §4.3 (the cap, and truncation stated); `OBSERVATION-LOG-DESIGN.md` §5.1 (the three causes).
- **Evidence:** `bio-plane/test/d241-derivation-stated.test.mjs` (13) and its control `test/d241-derivation-stated.control.mjs` — `noread` (the row's: A2, the cut arm, fails by name), `readbit` and `complete` (the two liars), `overstrict` (green) — SIX OF SIX AS DECLARED, `store.mjs` restored by `cp` and verified by sha256 and byte compare.

**RE-RESOLVED 2026-09-24 by CONDUCT #19 at c19-batch9: +2 I3 majors, the security train (IC-237, IC-238) having landed first; the line below carries the new figures.**

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #18 — I3 MINOR, ADDITIVE 78.3.0 → 78.4.0** (base read on c18-batch8, not the proposal's 67.0.0): one key added to `op=connections`' entity arm; `truncated` unchanged; the capture and portion arms unchanged. Renumbered from IC-235 at integration (D-126 holds IC-235 on this batch; the clone's second mint, IC-236, was unused, so it is taken here).
