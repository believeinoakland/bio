## IC-301 · I3: `op=proposedispose` refuses a CONDITION or OBLIGATION item by its CLASS (`CLASS_NOT_DISPOSED`, C-33.44), and a queue selection can carry a project-scoped finding · ACCEPTED 2026-09-25, MAJOR — I3 104.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; REC-205)

- **Interface:** I3.
- **Classification:** MAJOR. the refusal's wire token moves (IC-286's precedent).
- **The change:** Where it answered `NO_SUCH_PROGRESSION` for an item of the wrong class, `op=proposedispose` now answers `CLASS_NOT_DISPOSED` naming the act that does reach it; a D-126 selection carries a project per item so a project-scoped finding can join it (additive).
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** REC-205's branch stated the interface and left the IC to the integrator.
