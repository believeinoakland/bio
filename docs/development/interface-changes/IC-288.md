## IC-288 · I3: the observation log's published cause vocabulary gains a fourth WORD — `watermark_band`, the band the stored watermark's whole-second precision leaves open, stated instead of picked · ACCEPTED 2026-09-24, MINOR — I3 99.1.0 (resolved by CONDUCT #20 at integration on c20-batch27, on base 99.0.0; D-516)

- **Interfaces:** I3 (plane → UI, the op contracts). Base read off THIS TREE (`origin/main` @ `1a7f0bcc`):
  I3 **91.1.0**. **Read the base AT RESOLUTION** — this branch is one of several in the train.
- **Proposed classification:** I3 **MINOR, ADDITIVE**. One new member in a vocabulary that is already
  PUBLISHED on the answer for exactly this reason (`missing_causes`, PL-17's rule: *a surface renders the
  sentence the plane holds rather than matching a literal it learned somewhere else*). No key is added or
  removed from any answer, no refusal is added, no type changes, no op moves, no column moves and there is no
  migration. `observation_log.at` stays at whole-second precision — BOB #33 ruled the column does not move.
- **THE ONE THING THAT IS NOT PURELY ADDITIVE, STATED SO THE INTEGRATOR CLASSIFIES IT AND NOT ME:** the
  `missing_cause` FIELD, which already existed and already carried three words, can now carry a fourth. A
  consumer that switched on all three and defaulted safely is unmoved; one that enumerated the three
  EXHAUSTIVELY would meet a word it does not know. **Measured, not assumed:** no such consumer exists outside
  the battery (below).
- **Proposer:** RECORD, WORKER D-516 (CONDUCT #20, cloud), 2026-09-24.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/OBSERVATION-LOG-DESIGN.md` §6, the watermark rule — **BOB #33's ruling of
  2026-09-24 17:58Z, FOLDED INTO §6 IN THIS SAME LANDING** beside D-500's named ceiling, so this entry cites a
  governed section and not an inbox line.

### The change, on the wire

On the observation log's readers, and nowhere else:

- `missing_causes` (the vocabulary map published on `op=frontier&level=content`, `op=frontier&level=meaning`
  and `op=contentaxis`) gains the key `watermark_band`, with its own sentence at each level — three keys to
  four. The sentence names the stored watermark's whole-second precision, which is the fact the reader could
  not narrow.
- `missing_cause` (on a `missing_unexplained` row, and on `op=contentaxis`) can now carry `watermark_band`.
  Such a subject is in `missing_unexplained` and NOT in `never_looked`, exactly as `purged` is.
- `not_ruled_out` is UNCHANGED in shape and in membership: it is still drawn from `ALL_MISSING_ROW_CAUSES`,
  still §5.1's three causes, and a `watermark_band` row publishes precisely the set a `purged` row publishes
  at the same sidedness. **The band widens no claim and narrows none.**
- `op=contentaxis`'s `indexed` reads `undetermined` (already a member of the published vocabulary, on a new
  path) with `determined: false`, where D-500's rule reached `not_extracted` on the placements of the clock
  second that fell the other way. **That is the state this row exists to correct**, and it is the only
  answer that MOVES.

### What moves, as a partition rather than a sample

D-500's two-way rule is exactly `after ∪ within_band` and its negation is exactly `before`. So:

- every pair it classified `purged` is still `purged`;
- every pair this rule classifies `never_looked` it classified `never_looked` too;
- the ONLY pairs that move are the band's, and they move from a positive statement to a stated undetermined.

Driven as `observation-log.test.mjs` arm M3b over the whole corpus, against a verbatim copy of D-500's rule
(`d500`) and not against REC-94's older one — a control armed at the wrong subject refutes nothing.

### Consumer impact — MEASURED on this tree

| consumer | reads | impact |
| --- | --- | --- |
| `civicos-ui/src` | `missing_cause` occurs **0** times | none |
| `tools/` | `missing_cause` occurs **0** times | none |
| `newgroup/` | `missing_cause` occurs **0** times outside the embedded release bytes | none |
| the battery | 6 suites name `missing_cause` | 4 pins asserted the vocabulary's SIZE (3) and are CORRECTED to 4 with the reason at the site, counted from the constant rather than typed so a fifth word moves them too |

**WHAT THIS MEASUREMENT CANNOT SEE, STATED:** a consumer outside this repository. The vocabulary is published
on every answer precisely so that such a consumer renders the plane's sentence instead of a literal of its own,
which is the mitigation this interface was given at IC-112 and is not a claim that none exists.

### The negative control

`observation-log.test.mjs` section M: collapse the three-way answer back into a two-way comparison and the band
arms fail BY NAME (M3, M3a, M9c, M9e, M9f). Declared and driven; the result is on the suite's
`NEGATIVE CONTROL:` line.
