## IC-397 · I1: an `.odt` evidentiary digest does not count a `font-face-uri` reference: real Google Doc exports (which embed `Fonts/fontN.ttf`) read DETERMINED; `Pictures/` and `Object N/` references still refuse · ACCEPTED 2026-09-25, MINOR — I1 1.14.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-612)

- **Interface:** I1.
- **Classification:** MINOR. additive, IC-349's (D-473's) reading: nothing refused, and captures that read `determined: false` may now read `true`; `basis` names the font faces as presentational.
- **The change:** `PRESENTATIONAL_REF` in `referencedMembers` (by local name, any prefix). M-167 addendum: the moved count is not re-measured. Stacked on D-473.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-612's branch stated the interface and left the IC to the integrator.
