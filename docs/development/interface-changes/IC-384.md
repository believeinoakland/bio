## IC-384 · I3 + I6: the crop of a cited PDF image can be asked for: the pdf member's `POST /crop` (I6) and `op=contentcrop` (I3, read-only, viewer-gated), with the C-99 family · ACCEPTED 2026-09-25, MINOR (I3) / additive under PROVISIONAL (I6) — I3 153.3.0; I6 0.2.1 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-419)

- **Interface:** I3 + I6.
- **Classification:** MINOR (I3) / additive under PROVISIONAL (I6). additive: a new op and a new member route; no existing answer moves. I6 is PROVISIONAL, so an additive change moves the patch (IC-352's reading on I9). `POST /crop` reads the capture exactly as `/structure` does (`readCapture`, factored out unchanged) and writes nothing; its named refusal answers 422, OVER_ENVELOPE 413.
- **The change:** C-99.1 CROP_NO_PDF_MEMBER .. C-99.5 CROP_CAPTURE_MISMATCH; the member's `capture_sha256` is checked against the row's capture. UI-120 renders it (consumer).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-419's branch stated the interface and left the IC to the integrator.
