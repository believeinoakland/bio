## IC-196 · I3: A RETIRED ITEM IS NOT CITABLE — `op=cite` refuses a selection carrying a RETIRED Information member, `RETIRED_NOT_CITABLE` (C-33.39), for every caller and on both arms (onto a case, onto a question); `op=affordances` no longer publishes `cite` on a retired Information bundle · PROPOSED 2026-09-23 (D-168, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column).
- **Base read off THIS TREE (`origin/main` @ `0e7cc03e`): I3 59.2.0 (IC-191). Proposed I3 MAJOR, BREAKING — 59.2.0 → 60.0.0.
  Read the base AT RESOLUTION.** **Why MAJOR, by IC-25's test: the op REFUSES what it accepted.** Before this change a
  selection carrying a `retired` Information bundle cited `ok: true` onto a case (a `cites` edge) or a question (a basis leg);
  it is now refused whole, and nothing is written. `op=affordances` answers one act fewer for a retired Information target.
- **Proposer:** RECORD, D-168 worker (CONDUCT #16, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/architecture/BIO_State_Rules_Consistency_v1_5.md` §4.1, *"A RETIRED ITEM IS NOT CITABLE"* (BOB #30,
  2026-09-23, landed at `4355bfda`).

### The change, on the wire

- `op=cite` — a new refusal, checked after the type offenders (`NOT_INFORMATION` / `NOT_CITABLE`) and before the document is
  read: `{ ok: false, reason: "RETIRED_NOT_CITABLE", code: "RETIRED_NOT_CITABLE", check: "C-33.39", translation, project,
  handle, offenders: [<retired ids>], drift, detail }`. The translation names the door (cite what superseded it, or collect the
  source again as a new item). Asked in the STORE (`cite > is-cite-retired`), so a member's session and a machine credential
  meet the same rule.
- **Not refused, deliberately:** a `source_status: removed` (or `modified`) bundle — the publisher's axis, not the group's
  retirement; §4.1 says it stays citable. Legs and edges written before the retirement are not rewritten.
- `op=affordances` — `cite` is not published on an Information target whose `current_state` is `retired` (DEC-8: the pre-flight
  agrees with the refusal it fronts). Unchanged for every other target.

### Consumer impact, measured

- `civicos-ui`: renders a plane refusal through its `translation`; the new code is a family row, so `check-refusal-codes.mjs`
  counts it in reach by R1 (reach 355 → 356, every ratchet key gated at slack 0 on this tree). No surface matches the new
  literal; no surface edit is owed.
- The battery: no existing suite cited retired material through `op=cite` (the full gate on this tree is the measurement; see
  the landing report). `citeinquiry.test.mjs`'s header, which said the act asked nothing about state, is corrected in comment only.
- The machine's suggest path is unchanged — it already refused (`SUGGEST_LEG_UNREACHABLE`, C-27.8).

### Evidence

`bio-plane/test/d168-retired-cite.test.mjs` (19 assertions, through the op): refused by name for a member session and the
MEMBER_TOKEN machine credential onto a question and onto a case; a mixed selection refused whole; a removed-source bundle lands
for both; the older leg and edge byte-identical across the retirement and the refused re-cite; the affordance. Its NEGATIVE
CONTROL line records three arms, each AS DECLARED.
