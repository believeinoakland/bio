## IC-361 · I3: `op=promote`: a revision stating no `current_state`, `created` or `last_updated` carries the head's (`fields_carried`); a creation stating one nowhere is refused `PROMOTED_FIELD_UNSTATED` (C-86.8) · ACCEPTED 2026-09-25, MAJOR — I3 133.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-628 @ db3b94a0; the row assigned the IC to the integrator; D-628)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — a blank envelope `current_state` over a document stating none LANDED as a state of `''`, and a creation of that shape is now refused C-86.8 (the all-absent case was a raw NOT NULL stack, no answer). `fields_carried` is additive. Stacked on D-615.
- **The change:** Each envelope fallback asks `textStated`; a string `meta` states nothing (D-563's `envelopeMeta`). D-707 (the same raw stack on snap_key / files) is minted, not fixed here.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-628's branch stated the interface and left the IC to the integrator.
