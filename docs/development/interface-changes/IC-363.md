## IC-363 · I3: `op=connectionchoose`: a PRESENT-but-empty `occurrence=` names the unplaced ('') read; an ABSENT one is none named; an empty one where no unplaced read exists is refused C-74.3 · ACCEPTED 2026-09-25, MAJOR — I3 135.0.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-625)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — `occurrence=` sent empty collapsed to none named, so a mention read at ONE place chose that place; it is now refused `CONNECTION_CHOICE_NOT_A_MENTION` (C-74.3) where the document holds no unplaced read. The unplaced read of a string read at several places becomes choosable (a C-74.4 refusal lifted).
- **The change:** Both the op's argument line and the act's `named` read PRESENT, not truthy.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-625's branch stated the interface and left the IC to the integrator.
