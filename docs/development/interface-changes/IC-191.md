## IC-191 · I3: `op=frontier`'s `truncated` READS `true` WHEN A NEVER-LOOKED OR MISSING FETCH COMES BACK FULL, at the document, content and meaning levels, for every viewer; the meaning level's three missing lists are over-fetched like the other arms', and its claim compares `missing_unexplained`; an exhausted supply answers as before · PROPOSED 2026-09-23 (REC-174, minted with `node tools/mintid.mjs IC` BEFORE the entry was written) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). I5 does not move (no table, no column). **Base read off THIS TREE (`origin/main` @
  `14faa089`, merged in): I3 58.3.0 (IC-189; it read 58.2.0 at `e62e08e1`, where this row started). Proposed I3 MINOR — 58.3.0 →
  58.4.0. Read the base AT RESOLUTION.** **Why MINOR, on IC-114's
  and IC-187's precedent:** every change to `truncated` WEAKENS a published claim, and the one list-content change only REACHES
  rows a viewer was already entitled to (item 3 states where that reorders a narrowed viewer's cut). No op, key, class list or refusal moves.
  1. **`truncated` reads `true` in three new conditions, never `false` where it read `true`:** (a) the document arm's deferred-link
     fetch (`(cap + 1) * 2`) came back FULL; (b) the content arm's missing-capture fetch (`(cap + 1) * 2`) came back FULL; (c) any of
     the meaning arm's three missing fetches (captures, references, entities; now `(cap + 1) * 2` each) came back FULL. Before, a
     viewer the §6 fence narrowed — or, at meaning, any viewer whose `cap + 1` rows split across §5.1's two causes — read `false`
     over rows nobody fetched (DRIVEN: an uninvited member at `limit=1` received ZERO never-looked / missing rows and
     `truncated: false` at all three levels while entitled to a subject beyond the fetch).
  2. **The meaning claim now compares `missing_unexplained`** (`unexplained.length > cap`), which it cuts at `cap` and never
     compared — DRIVEN: an entitled viewer at `limit=5` over 9 unexplained captures, with the fetch exhausted, received 5 and read
     `false`. The content arm has carried that disjunct since REC-109.
  3. **The meaning arm's three missing fetches go from `cap + 1` to `(cap + 1) * 2`**, the content arm's factor for one subject
     kind. A narrowed viewer can now RECEIVE never-looked / unexplained rows that sat beyond the old fetch (DRIVEN: at `limit=4`
     an uninvited member receives the open capture and the open reference it is entitled to, where it received none). For a
     viewer entitled to every row the published lists are unchanged (the first `cap` of each kind were already fetched). For a
     narrowed viewer the lists can now hold rows the old fetch never reached, and because the meaning lists concatenate captures,
     then references, then entities before the cut, a newly reached capture can displace a later kind's row from the first
     `cap` — exactly when the list then exceeds `cap`, so `truncated` reads `true` there.
  **The one over-report accepted** (D-389's): a supply of EXACTLY the fetch size also reads `true`. **It leaks nothing:** on a full
  fetch an entitled viewer's rows are the whole fetch, split at most two ways by cause, so one list exceeds `cap` and it already
  read `true`; every viewer now reads the same bit, and no count of what was withheld is published. **If CONDUCT reads IC-118's
  rule as applying** (a consumer that took `false` as "you have every row" now sees `true` and would page again), the break is
  MAJOR; IC-187 was resolved MINOR on the same question and this proposal follows it.
- **Proposer:** RECORD, REC-174 worker (CONDUCT #16, cloud), 2026-09-23.
- **Owner to land it:** `RECORD`
- **Design:** `docs/development/OBSERVATION-LOG-DESIGN.md` §5 (§5.1's never-looked / missing split) and §6 (the readers, which
  compute `truncated` from what the viewer may read), with D-389's `Store#frontierPage` as precedent.
- **Consumers to answer, MEASURED** (`git grep` for `op=frontier`, `frontier&` and `"frontier"` over `civicos-ui`,
  `agent-worker/src`, `skills`, `tools`, 2026-09-23): **none calls `op=frontier`.** Only the plane's own suites read the field:
  `observation-log`, `observation-content`, `observation-meaning`, `d389-fullfetch` (all unchanged in answer at their fixtures'
  bounds) and the new `rec174-supplyfetch.test.mjs`.

**THE SHAPE.** `store.mjs`: `#frontierPage`'s full-fetch test moved into a new private `#frontierFetch(limit, read, gate)` —
`read(limit)` runs the bounded statement AT the limit it is tested at, the gate filters, and it returns `{ rows, full: raw.length
=== limit }`; the test is written once. `#frontierPage` is its first caller (claim `gated.length > cap || full`). The document
arm's `#frontierNeverLooked` fetch, the content arm's `missing` fetch and the meaning arm's `captureFetch`, `referenceFetch` and
`entityFetch` go through it, and each arm ORs every fetch's `full` into its `truncated`. `#frontierInternet` is not a caller and
not in the class (it gates inside its statement, so its `cap + 1` fetch is exact).

**NEGATIVE CONTROL** (`node test/rec174-supplyfetch.control.mjs all` from `bio-plane/`): ALL NINE ARMS AS DECLARED, baseline both
ends 23/0 · the bit dropped from ONE arm or ONE list at a time: document [B1 F1], content [B2 B4 F1], meaning capture [B3 B5 F1],
meaning reference [B6 F1], meaning entity GREEN as declared (ungated supply: the over-fetch alone makes a list exceed `cap`) ·
munexplained [C5] · moverfetch [C3 C5 E1 S1] · overstrict [C1..C7 E1 E2 S1]; `store.mjs` restored byte-identical after each.

**FOR CONDUCT at integration:** classify and resolve this IC against the base read then (I3 58.3.0 or later), move I3 in
`INTERFACES.md`, and re-run the control.

**RENUMBERED at integration by CONDUCT #16, 2026-09-23: IC-188 → IC-191** (minted with `node tools/mintid.mjs IC`). REC-174's worker
proposed this as IC-188 on its branch; IC-188 was already REC-172's (a run's bound), resolved in the same batch. An id
collision across clones, CONDUCT-NEXT §6 trap 5: the later one is renumbered with every reference.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #16 — I3 MINOR, 59.1.0 → 59.2.0.** Base RE-READ at resolution off the integration
tree (`land/conduct/c16-batch3`, where IC-188 and IC-190 resolved first): **I3 59.1.0**, NOT the proposal's 58.3.0. MINOR as
proposed, on IC-114's and IC-187's precedent: no answer that read `true` now reads `false`, no key, op or refusal moves, and no
caller outside the plane uses `op=frontier` (the worker's grep). The MAJOR reading under IC-118 is weighed and not taken. I5 untouched.
