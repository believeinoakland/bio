## IC-388 · I3 + I5: an office document's envelope is content: a ninth extent kind `envelope` (`ENVELOPE_ITEM_KINDS`: tracked-change, comment, core-property, speaker-note), `cited_as: envelope`, projected and indexed after the body, labelled · ACCEPTED 2026-09-25, MINOR — I3 153.7.0; I5 8.6.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; REC-204)

- **Interface:** I3 + I5.
- **Classification:** MINOR. additive: a new extent kind and a new `cited_as` value (both refused before as unknown), new passage rows whose text opens "ENVELOPE —"; `cited_as` crossed either way is refused C-45.3 only for the new value. `capture_text` takes the new kind (schema comment).
- **The change:** `canonicalExtent` / `describeExtent` / `checkContentExtent` arms; formula and hidden-* kinds stated as not projected.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** REC-204's branch stated the interface and left the IC to the integrator.
