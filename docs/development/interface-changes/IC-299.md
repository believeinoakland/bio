## IC-299 · I3: `op=promote` refuses a non-replay revision that changes the bundle's type (`REVISION_RETYPES_BUNDLE`, C-86.2) · ACCEPTED 2026-09-25, MAJOR — I3 102.0.0 (minted and resolved by CONDUCT #21 at integration on c21-batch28; the row assigned the IC to the integrator; D-547)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — a retyping revision landed before.
- **The change:** A revision whose derived type differs from the head's (both through `normalizeType`) is refused after the compare-and-swap and before the first write; `bundles.object_type` is never rewritten in place. Replay stays exempt; M-156 counted no bundle already retyped.
- **Base read at resolution:** I3 99.4.0, I5 3.13.0 (`origin/main` @ 964da679); this batch's ICs take their numbers in the order INTERFACES.md lists them.
- **Proposer:** D-547's branch stated the interface and left the IC to the integrator.
