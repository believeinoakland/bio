## IC-228 · I3 + I5: a fee quote is evidence — `op=actionquotes` (read), five optional `quote_*` keys on a `received` correspondence entry through `op=actioncorrespond` (which answers `quote`), `op=stats`' `actionQuotes`; table `action_quotes` · PROPOSED 2026-09-23 (D-148; the worker left the IC to the integrator, and CONDUCT #18 minted it with `node tools/mintid.mjs IC` at c17-batch7) — the classification and the RESOLUTION are CONDUCT's

- **Interfaces:** I3 (the op contracts) and I5 (the store schema and the action document's correspondence entries).
- **The change.** A `received` entry may carry flat keys `quote_amount` (as quoted, a number with separators allowed), `quote_currency` (as quoted, not normalised), `quote_basis` (verbatim, optional), `quote_answers` (the ord of an earlier SENT entry) and `quote_revises` (the ord of an earlier quote); a waiver is a revision to zero. One pure rule, `quoteFindings`, runs at C-2.10 over the document (findings carry C-72 codes), at `op=actioncorrespond` (refused by name before any write) and at promote (`CORRESPONDENCE_REFUSED`). A NEW read `op=actionquotes` (admin/member/probe, read-only) answers by `counterparty=` or `request=` (optionally `answers=`), lists `revised_by`, and names which level an empty answer was empty at (`no_request` / `no_reply` / `no_quote` / `no_quote_by_name`); it judges no quote. `op=stats` gains `actionQuotes`. A NEW table `action_quotes`, cleared in both purge arms.
- **Classification: I3 MINOR, ADDITIVE; I5 MINOR, ADDITIVE** (IC-25's test): a new op and new optional inputs whose refusals apply only to the new keys; an action with no quote reads byte-identical (asserted by the suite); a new table and new optional entry keys.
- **Design:** `docs/architecture/BIO_Case_Making_v0_1.md` §2. DESIGN GAP reported: which counterparty a quote is read "by" (the worker used the action's own `counterparty.name`, matched exactly).
- **Evidence:** `bio-plane/test/actionquote.test.mjs` (53/0 on c17-batch7) and its three-arm negative control, all AS DECLARED.
- **Proposer:** D-148 worker (CONDUCT #17), 2026-09-23.

**RE-RESOLVED 2026-09-24 by CONDUCT #19 at c19-batch9: +2 I3 majors, the security train (IC-237, IC-238) having landed first; the line below carries the new figures.**

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #18 — I3 MINOR, ADDITIVE 77.1.0 → 77.2.0; I5 MINOR, ADDITIVE 2.1.0 → 2.2.0** (base read on c17-batch7).
