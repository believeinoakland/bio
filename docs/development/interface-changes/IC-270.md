## IC-270 · I3 additive: six `STATEMENT_ACK_*` refusals carry a catalogued code and a canned translation (C-82.2..C-82.7), and `CATALOG_VERSION` moves 1.23.0 → 1.24.0 · ACCEPTED 2026-09-24, MINOR — I3 89.2.0 (resolved by CONDUCT #20 at integration on c20-batch22, on base 89.1.0; D-507). CATALOG_VERSION: 1.24.0 taken ONCE at the union with IC-271 (447 + 6 + 2 = 455 checks) · was PROPOSED 2026-09-24 by the D-507 worker (minted with `node tools/mintid.mjs IC`, a CAS push to `coord`)

- **Interface:** I3 only. No table changes (I5 untouched). No op is added, removed or renamed; no request argument changes.
- **Design:** DEC-49 (Bob, 2026-08-06), as `docs/architecture/BIO_Assistant_and_AI_Roles_v0_1.md` §3 rule 10 restates it, following REC-79's single-helper shape; `docs/architecture/BIO_Publication_v0_1.md` §3 rule 11 (the acknowledgement) is the op's own authority and carries the landing under "The translations". The six member-facing sentences are BOB #33's, approved 2026-09-24 and used verbatim.
- **Proposer:** the D-507 worker, 2026-09-24, branch `land/worker/D-507`. **Owner:** RECORD.

### I3 — what a caller sees

- **`op=statementack`** refuses on seven conditions. Only `STATEMENT_ACK_DOCUMENTS_OVER_BOUND` (C-82.1, IC-246) carried a catalogued code and a canned translation. The other six now do:

  | code | check | region (`where`) |
  | --- | --- | --- |
  | `STATEMENT_ACK_NO_SUBJECT` | C-82.2 | `acknowledgeStatement > is-statement-ack-subject` |
  | `STATEMENT_ACK_ALREADY_SIGNED` | C-82.3 | `acknowledgeStatement > is-statement-ack-signed` |
  | `STATEMENT_ACK_NOT_A_PARTICIPANT` | C-82.4 | `acknowledgeStatement > is-statement-ack-participant` |
  | `STATEMENT_ACK_NO_STATEMENT` | C-82.5 | `acknowledgeStatement > is-statement-ack-statement` |
  | `STATEMENT_ACK_BY_ITS_AUTHOR` | C-82.6 | `acknowledgeStatement > is-statement-ack-by-its-author` |
  | `STATEMENT_ACK_AUTHOR_UNDETERMINED` | C-82.7 | `acknowledgeStatement > is-statement-ack-author-undetermined` |

- **ADDITIVE, and asserted so rather than said so.** Each answer keeps `ok: false`, its `reason`, the site's own authored `detail` and its per-site keys (`caseId` and `edition` on C-82.3, `author` on C-82.6, `draft` and `author` on C-82.7). `code`, `check` and `translation` JOIN them. A caller reading `reason` reads exactly what it read before.
- **`CATALOG_VERSION` moves 1.23.0 → 1.24.0**, MINOR, and `GATE_VERSION` — `plane-gate/1.0 (bio-checks 1.24.0)` — moves with it. Every ratification stamps that string, so a stranger reading it can tell which catalogue judged the act (D-470). The catalogue's census moves 447 → 453 checks. **Nothing that passed is refused by the move:** the six conditions already refused, at the same six sites, under the same six `reason`s; what they gained is a row and a sentence.

### Classification (the proposer's; CONDUCT decides)

- **I3: MINOR, ADDITIVE.** No request that answered `ok: true` answers `ok: false`, and no key is removed or changed in meaning — IC-25's test for BREAKING is not met in either direction. The only strings that MOVE are `CATALOG_VERSION` and `GATE_VERSION`, whose movement is the point of D-470's pin rather than a break.
- **Measured consumer impact.** `git grep -l statementack` over `*.mjs`, `*.md`, `*.html` outside the plane, its suites, the DEC-49 guard and the design and interface documents, 2026-09-24: the only surface caller is `civicos-ui/app.html` (`rvsAcknowledge`, `pubStatementAcksHtml`), which renders the plane's own refusal and computes none of its own — UI-89 built it that way, and a catalogued `translation` is what it was waiting for. No consumer reads `CATALOG_VERSION` as a literal outside `src/gate.mjs` and `test/d470-catalog-census.test.mjs`, both moved here.
- **Proposed on the I3 version current at `origin/main` 68fecb8d.** CONDUCT takes the version bump and the RESOLUTION.

### Evidence

- `bio-plane/test/d507-statement-ack-translation.test.mjs` (63 pass, 0 fail): the catalogue holds all six rows with six different sentences; each code is minted at EXACTLY ONE site, inside the DEC-49 region its `where` names, through the helper; the helper stands ABOVE the first refusal; five of the six are DRIVEN through `op=statementack` and asserted to arrive with `reason`, `detail`, per-site keys AND the catalogue's own sentence; and the act that should succeed still succeeds carrying none of the three new keys.
- `civicos-ui/check-refusal-codes.mjs --strict` exit 0, every ratchet key at slack 0. Floors moved from its own print: `rows` 381→387, `reach` 433→439, `governedSites` 174→180, `regions` 157→163, `regionLines` 3747→3797, `codesChecked` 429→435, `refusalsJudged` 426→432; `untranslated` FALLS 300→294, which is the landing's payload and is argued at the site.
- `bio-plane/test/d470-catalog-census.test.mjs` (9/0): the census row for 1.24.0 is `{ count: 453, digest: b92168843fe6534c… }`, taken from that suite's own print.
- **Negative control** (`bio-plane/test/d507-statement-ack-translation.control.mjs`, 4 arms, driver exit 0, all AS DECLARED): the row's own arm returns one code OUTSIDE the helper and the DEC-49 guard names the region — *"arm C judged NO refusal inside the region `is-statement-ack-by-its-author`"* — while the WIRE arm for that same code stays GREEN, which is the measured reason a wire assertion is not evidence about routing (`dec49Decorate` fills the sentence from the row alone).

### What this entry does NOT claim

`STATEMENT_ACK_AUTHOR_UNDETERMINED` (C-82.7) is not driven through the op anywhere, because no sequence of ops can produce its condition — a draft with a statement and a NULL `statement_by` — since REC-193 stamps that column at every statement write. Its row, its single site and its region are pinned structurally. That is a narrowed unknown, stated rather than scored.
