## IC-374 · I3: the bundle image's `_history/manifest.json` entries carry `seq` (write-order rank, 1..n) and the gate's C-20.1 audit walks write order, not snap-key order · ACCEPTED 2026-09-25, MAJOR — I3 143.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-700 @ 50116ded; the row assigned the IC to the integrator; D-700)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood, both ways — C-20.1 (error severity, run by the gate and op=audit) picked a mechanical tick's "prior" by snap key, so with keys chosen against write order an overreaching tick whose key sorted last was skipped (it now draws an error) and a conformant tick was blamed for an earlier edit's prose (it no longer does). `seq` on each entry and the info finding for an image without it are additive. Stacked on D-674.
- **The change:** `readImage` reads the manifest by rowid; the file stays in snap-key order (C-12.1). `historyWriteOrder` walks `seq` when every entry carries a distinct integer, else key order, and says so. D-718 and D-719 minted.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-700's branch stated the interface and left the IC to the integrator.
