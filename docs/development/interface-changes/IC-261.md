## IC-261 · I4: install and update CARRY an operator-supplied `INSTANCE_AI_TOKEN` as the Worker secret, and never generate one · ACCEPTED 2026-09-24, MINOR (DIST-9, `land/dist/DIST-9` @ 41c195d5; minted by CONDUCT #20 at integration with `node tools/mintid.mjs IC`)

- **Interface:** I4 (plane → installer). The row and DIST's report named I8; I8 is the plane ↔ agent-worker binding, which this change does not touch. What moves is what the installer carries into the plane's deployment — a secret binding the plane already reads (D-260) — which is I4's contract. Recorded here so the misnaming does not travel.
- **Design:** `docs/architecture/BIO_Distribution_v0_1.md` §6 with `BIO_Assistant_and_AI_Roles_v0_1.md` §6 (D-260, BOB #22: *carrying that secret through install and update, as DAEMON_TOKEN is*). Minting a credential is a MEMBER act (DS-3), so the installer never invents one.
- **Proposer / builder:** DIST #6, 2026-09-24. **Owner:** DIST. **Resolved by:** CONDUCT #20.

### What changes

newgroup's `/update` page gains an OPTIONAL password box and `/begin` refuses a malformed value by name; both upload paths bind the value only when one is supplied. None supplied sends none; an update keeps a value the copy already holds (`keep_bindings` secret_text). Each page says which way it went and never shows the value. `bio-plane/scripts/deploy.mjs` / `derive-bindings.mjs` send it only when the environment holds it. Construct `15.instance-ai-secret` ABSENT → BUILT.

### Classification

**MINOR, ADDITIVE** — I4 2.1.0 → 2.2.0. One optional operator input; nothing that installed before is refused, and an installation with no value supplied is byte-for-byte the old path. NO RELEASE is cut by this landing (Bob's ruling: no releases until he asks); DIST cuts when asked.
