## IC-375 · I3: the record's order is WRITE order, never the writer's date: op=export's promotions, the gate's facts, op=selection's drift class and op=queue's latest / started order by rowid; `created` is published as the writer's date · ACCEPTED 2026-09-25, MAJOR — I3 147.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-674 @ 96a7802f; the row assigned the IC to the integrator; D-674)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning — those readers ordered by `manifest.created` (the writer's `meta.last_updated`, REC-182 / D-171), so a caller could date a promotion before the one it follows; they now order by the store's write order, so for such a history the order, the "latest" row and `#revisionKind`'s class (a backdated authored amendment read `mechanical`) change for the same request. Proposed as additive; classified by IC-25.
- **The change:** `created` stays on every row (C-12.1 compares against it) and orders nothing. State Rules I-20 amended.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-674's branch stated the interface and left the IC to the integrator.
