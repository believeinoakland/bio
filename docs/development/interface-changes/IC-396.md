## IC-396 · I3 + I5: site chrome derived per HOST: `op=navchanges` names the links a host's navigation lost between captures; tables `site_chrome` and `site_chrome_refs`; `links.chrome_basis` · ACCEPTED 2026-09-25, MINOR — I3 153.15.0; I5 8.7.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-340 @ fdf6c8c9; the row assigned the IC to the integrator; D-340)

- **Interface:** I3 + I5.
- **Classification:** MINOR. additive: a new op, two derived tables before `host_governor` (both in purge's whole-store arm) and a nullable column never back-filled; every link is filed as before (a classification, never a filter), and the `chrome` column nothing ever set now carries a value on links filed after this landing. D-701 (IC-366) gates the op through the viewer.
- **The change:** Every chrome scan is bounded (`CHROME_PAGES_PER_CAPTURE` 64, `CHROME_LINKS_PER_CAPTURE` 2000); recurrence across pages is NOT built (containment only), stated in the read's note.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-340's branch stated the interface and left the IC to the integrator.
