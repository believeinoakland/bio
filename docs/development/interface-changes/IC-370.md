## IC-370 · I3: a law-neutral action kind `records_request` with a `law` field; `op=projection`'s action block carries `law` (`recordsLawOf`); C-2.10 refuses a `law` on any other kind or one longer than a citation · ACCEPTED 2026-09-25, MAJOR — I3 142.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/REC-201 @ 45ce0bc5; the row assigned the IC to the integrator; REC-201)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — C-2.10 runs in `checkBundle` (so in the gate `runGate` asks, as well as the audit sweep): an action carrying `law` on another kind, or an over-long one, drew no finding and now draws an error. No surface wrote `law` before UI-119, which measures the reach, not the class (IC-112's reading). `records_request` in `vocabularies.action_kind` and `law` on the projection are additive. Proposed as additive; classified by IC-25.
- **The change:** `cpra_request` answers `law.state: kind`, read as written; no citation is synthesised. D-695 runs the same arm at the act.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** REC-201's branch stated the interface and left the IC to the integrator.
