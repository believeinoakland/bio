## IC-333 · I3 + I5: monitoring schedules an ADDRESS: the cadence plan (`due`, `unscheduled`, `next`) has one entry per monitored address, checked against its current version; a document authoring no frequency takes its type's contract; table `monitor_address_type` · ACCEPTED 2026-09-25, MAJOR — I3 124.0.0; I5 7.1.0 (MINOR) (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; REC-191)

- **Interface:** I3 + I5.
- **Classification:** MAJOR. I3 MAJOR, IC-25: a changed meaning — a `due` entry stood for a monitored BUNDLE and now stands for an ADDRESS (sixty captures of one calendar were sixty entries and are now one), and a document stating no frequency moves from `unscheduled` to `due` by its contract. Entries gain the address, the versions it stands for, `frequency_source`, `newer_unmonitored` and `disagreement`; the answer gains `addresses`. I5 MINOR: one new table.
- **The change:** `monitor_address_type (address_norm PK, address, content_type, confidence, contract, basis, read_at)` before `host_governor`, written by op=monitor's look only when a document was READ, cleared by a whole-store purge. `Store.CONTRACT_FREQUENCY` is the one copy. Landed carried by D-455's branch (cfcb33e3).
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** REC-191's branch stated the interface and left the IC to the integrator.
