## IC-321 · I3: `op=projectleave` refuses a project's ONLY owner (`LAST_OWNER_CANNOT_LEAVE`, C-33.48); `op=affordances` offers `projectjoin` only to a participant not joined, and `projectleave` not to the only owner · ACCEPTED 2026-09-25, MAJOR — I3 112.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; REC-186)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a refusal where an answer stood — the only owner's request to leave was recorded (`state: leaving`) before this change.
- **The change:** `projectLeave` asks `Store.ownerMath` over `#owners` (the arithmetic `projectOwnerRemove`'s LAST_OWNER runs) and refuses the last owner with `owners` and a `detail`, nothing written; a co-owner's request is recorded as before. The affordance predicates read `f.roster.state !== "joined"` and `f.roster.owner_floor_clear`. The row's C-33.47 is C-33.48 on this tree (c22-rec186-renumber: REC-207 took C-33.47 on main).
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** REC-186's branch stated the interface and left the IC to the integrator.
