## IC-295 · I3: `op=biasadopt` answers `pins_proposed`, and `op=biasmanifest` carries `pins_proposed[]` and `pins_proposed_stated` when an adoption pins a revision that is not `adopted` · ACCEPTED 2026-09-24, MINOR — I3 99.4.0 (minted and resolved by CONDUCT #20 at integration on c20-batch27; REC-210 — the row assigned the IC to the integrator)
- **Interface:** I3.
- **Classification:** MINOR, ADDITIVE: `op=biasadopt` gains `pins_proposed: boolean`; `op=biasmanifest` gains OPTIONAL `pins_proposed: [{bundle_id, revision, scope, pinned_state, adopted_by, adopted_at}]` and `pins_proposed_stated`, both PRESENT only when a pinned revision is not `adopted`. No key removed, no meaning changed, no schema change.
- **Consumer impact (measured by the worker):** publishCase and aiRunRead pick named fields and are unaffected; no UI caller.
