## IC-329 · I3: `op=publish` authors `bio-case-document/4`: the bias manifest states adoptions pinning a proposed revision (C-41.14), and the case's citation edges are pinned inside the signed document (`case_citations[]`, C-41.15); `op=cite` pins each new case edge; `op=casedocument` answers `citations` · ACCEPTED 2026-09-25, MAJOR — I3 120.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; REC-219)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: the signed document's FORMAT moves (`/3` -> `/4`) and a `/4` lacking the new keys is refused by C-41.14 / C-41.15 — a reader or verifier keyed on `/3` meets a format it does not know. `/3` and older documents stay valid and are never re-signed or re-gated.
- **The change:** `/4` adds `bias_manifest.pins_proposed`, `pins_proposed_stated`, `bias_manifest_pins_proposed[]` and `case_citations[]` (version `pinned | only_capture | undetermined | no_capture | no_bytes`), with prose under `## Bias Manifest` and `## Citations`. `op=cite`'s case arm writes `extent_capture` and answers `pinned_captures`. Carries D-579(a) (BOB #34, 2026-09-25 02:30Z).
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** REC-219's branch stated the interface and left the IC to the integrator.
