## IC-373 · I3: a draft that names a case AND asks for a new one states `edition: null` (op=casedraft, op=casedrafts, op=reviewcopy, op=reviewgrant, op=statementack); a live grant's roster row reads the stated edition · ACCEPTED 2026-09-25, MAJOR — I3 142.0.0 (minted and resolved by CONDUCT #23 for c22-batch30's second composition, read at land/worker/D-618 @ dc4c41f9; the row assigned the IC to the integrator; D-618)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed meaning of an existing field, IC-327's (D-568's) exact shape — `edition` read the named case's next edition for that pair and now reads `null` (UNDETERMINED: publication refuses the pair together, CASE_IDENTITY_AMBIGUOUS). Proposed as additive; classified on IC-327's precedent.
- **The change:** `#statedEdition` returns null for the pair; the internal key is unchanged, so grants and acknowledgements still bind at it. D-708 rewrites statementack's `listed` sentence for the pair (sentence text, no IC).
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-618's branch stated the interface and left the IC to the integrator.
