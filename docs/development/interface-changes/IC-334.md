## IC-334 · I3 + I5: a `changed` monitor tick CAPTURES the bytes it fetched, and its observation-log look names the NEW capture; `op=monitor` answers `capture` · ACCEPTED 2026-09-25, MINOR (I3) / MAJOR (I5) — I3 126.1.0; I5 7.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-455)

- **Interface:** I3 + I5.
- **Classification:** MINOR (I3) / MAJOR (I5). I5 MAJOR, IC-25: a changed meaning of a stored field — a `changed` look's `result_ref` named the BASELINE capture and now names the new capture where the monitor filed it (D-65's baseline form is kept, and says why, when it could not). I3 MINOR: `op=monitor`'s answer gains `capture` (with `registered`, read from the look's outcome).
- **The change:** op=monitor holds the served bytes in R2 under the capture key (no second request), the tick's mechanical promotion files them in `snapshots/` with a register row, and the version is filed at the address. The baseline a person accepted never advances (BOB #34, 2026-09-25 03:05Z; OBSERVATION-LOG-DESIGN §4.1).
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-455's branch stated the interface and left the IC to the integrator.
