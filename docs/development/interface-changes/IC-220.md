## IC-220 · I3: D-420's `image_bound` is WITHDRAWN from the mint answers, and a page-form image admitted without its painted-image bound carries `undetermined: {level: "page_images", why}` — the ONE shape for "admitted, bound not held" (BOB #31) · PROPOSED 2026-09-23 (CPDF-22, minted with `node tools/mintid.mjs IC` BEFORE the entry was written; IC-217..IC-219 were minted too and are TAKEN on `land/conduct/c17-batch5`, so this clone's first three were discarded) — the version bump, the classification and the RESOLUTION are CONDUCT's

- **Interface:** I3 (the op contracts). The row names I5; the answer shape is I3's, as IC-204's additive half was, and I5's schema does not move. **Base read off THIS TREE (`origin/main` @ `02603e88`): I3 67.0.0 (IC-206).** Read the base AT RESOLUTION.
- **The change.** `op=promote`'s `content[]` entry and `op=contentmint`'s answer no longer carry `image_bound: {determined: false, empty_level, why}` (IC-204's additive half). When an image `{page}` or `{page, rect}` is admitted because the record holds no list of the images the capture's pages paint (a PDF acquired before D-420, a walk that did not finish, a capture that is not a PDF), the answer carries `undetermined: {level: "page_images", why}`. `why` is the same sentence `image_bound.why` carried. Absent when the list was held and checked, as before.
  **One producer.** `mintContent` asks `mintUndetermined(extent, ctx)` (`checks/bio-checks.mjs`) and nothing else. It returns D-440's `imagePartUndetermined` (levels `image_list`, `container_kind`, unchanged) or the new `imagePageUndetermined` (level `page_images`). A `{part}` and a `{page}` are exclusive address forms, so at most one answers.
- **Classification, proposed: I3 MAJOR, BREAKING in form, with no reader.** A key the answer carried is removed. IC-204's resolution recorded that no client may read it before this change. Measured on this tree: `git grep -n image_bound -- . ':!docs'` returns nothing (exit 1), so no plane, UI, fleet, skill or installer path reads it. `civicos-ui/app.html` never calls `op=contentmint` and reads no `undetermined` from a mint answer. If CONDUCT judges a withdrawal nobody could have read to be MINOR, that is its call; the precedent this proposes is that a removed key is a break regardless of readers.
- **Consumers, MEASURED:** as above, none outside `docs/`. Suites: `d420-image-page` was corrected (it pinned the withdrawn key; corrected with the reason at the site, never exempted) and is GREEN 29/0; `d440-image-part` GREEN 19/0, unchanged.
- **Proposer:** CONTENT-PDF (D-420's paths), CPDF-22 worker (CONDUCT #17, cloud session WORKER CPDF-22), 2026-09-23.
- **Owner to land it:** `CONTENT-PDF`
- **Design:** `docs/architecture/BIO_System_Design.md` §3, construct 12 (UNDETERMINED as a display primitive), and BOB #31's ruling of 2026-09-23 ~19:58Z, *"ONE SHAPE FOR 'ADMITTED, BOUND NOT HELD'"* (drained to `docs/archive/ledgers/BOB-INBOX-drained.md` on `coord`).
- **Evidence:** `bio-plane/test/d420-image-page.test.mjs` §2–§3, through the ops. A pre-D-420 PDF's far rect is admitted with `undetermined.level` `page_images` and its `why`, on `op=promote` and on `op=contentmint`. The unfinished walk states the walk's reason. The held-and-passed mint carries no statement. The withdrawn key is asserted absent. Negative control `nc-d420.mjs`: `restorekey` (the row's; fails BY NAME at "in ONE shape: the withdrawn image-bound key is absent"), `silent`, `overstate`, and D-420's four, all AS DECLARED.
