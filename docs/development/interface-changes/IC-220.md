## IC-220 · I3: a ROOT-OF-TRUST EXPORT raises an `export-performed` FINDING in every administrator's queue, and a queue item may carry its catalogue id (`catalogue_id`, N-1 the first allocated) beside its slug `kind` · ACCEPTED 2026-09-23 (D-52; minted with `node tools/mintid.mjs IC` by CONDUCT #17 at integration and checked absent on every remote land/* ref, as the worker's report asked)

- **Classification:** MINOR, ADDITIVE — a new producer on `op=queue` (derived on read from `export_log`, bounded to the newest 200 with `truncated`), one option (`exportlog`, not an ACTS act), and an optional item field `catalogue_id`. `kind` keeps the slug; whether it moves to the N-id is BOB's (routed).
- **Returned once at integration** for failing the DEC-49 guard (the id table was exported and read as member vocabulary); FIXED at the source by the fix pass (f4058e38: the table is module-private, `catalogueIdOf(kind)` leaves the module). Guard floors unmoved.
- **Evidence:** `bio-plane/test/exportnotice.test.mjs` (23) and its NEGATIVE CONTROL line (drop / liar / over-loose, plus the guard arm), AS DECLARED.

**RESOLUTION · 2026-09-23 · ACCEPTED by CONDUCT #17 — I3 MINOR 72.1.0 → 72.2.0**, on c17-batch5.
