## IC-324 · I3: C-82.1 `STATEMENT_ACK_DOCUMENTS_OVER_BOUND` is retired, and `case_documents_limit` / `case_documents_truncated` leave `op=statementack`'s answer · ACCEPTED 2026-09-25, MAJOR — I3 115.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; D-521b)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a wire token disappears — two answer keys and one refusal code are removed. No caller could meet the refusal (the read returns at most two rows against a bound of 8), which measures the reach, not the class.
- **The change:** The unsigned-document read is two keyed reads (by `(case_id, edition)` and by `draft_id`), so it is bounded by construction; `Store.STATEMENT_ACK_DOCUMENTS_MAX` and its DEC-49 region are removed. `CATALOG_VERSION` records the departure.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** D-521b's branch stated the interface and left the IC to the integrator.
