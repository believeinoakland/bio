## IC-323 · I3: a READ naming a discoverable project's OWN id answers C-70.1 at EXISTENCE, as an act does (`PROJECT_NAMING_READS`, 25 read ops) · ACCEPTED 2026-09-25, MAJOR — I3 114.0.0 (minted and resolved by CONDUCT #22 at integration on c22-batch29; the row assigned the IC to the integrator; REC-196)

- **Interface:** I3.
- **Classification:** MAJOR. IC-25: a changed answer to an unchanged request — a caller at EXISTENCE sight naming a discoverable project's id to `op=projectparticipants`, `op=image`, `op=projectvisibility` and the other listed reads was answered as for a project that does not exist; it now receives C-70.1's positional answer (the id and the name).
- **The change:** One door in the store's `fetch`, before the route: `#existenceRead` checks each read's bundle-id parameters (`Store.PROJECT_NAMING_READS`) against `project_sight` and answers `#existenceAct`'s shape. `PROJECT_NAMING_READS_NOT` names each id-carrying read that never names a project, with its reason. `op=projectparticipants` now takes the viewer stamp. `viewerPredicate` and `#sight` are unchanged; a read naming anything INSIDE a project answers as before.
- **Base read at resolution:** I1 1.11.0, I2 2.8.0, I3 111.9.0, I5 6.2.0, I7 1.0.0, I9 0.2.0 (c22-batch29 @ 0cf5eb55, `origin/main` @ 5e8a65a8 plus the batch); this batch's ICs take their numbers in the order INTERFACES.md lists them, MAJORs first, per interface.
- **Proposer:** REC-196's branch stated the interface and left the IC to the integrator.
