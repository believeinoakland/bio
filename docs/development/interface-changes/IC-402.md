## IC-402 · I2: the OpenDocument entries read `meta.xml` into the same `core-properties` item the OOXML entries emit, and walk `META-INF/manifest.xml` for sha256 `intra` links; the two `outside_content_xml_not_read` markers are removed · ACCEPTED 2026-09-25, MINOR — I2 2.18.0 (minted and resolved by CONDUCT #23 at integration on c22-batch30; the row assigned the IC to the integrator; D-346)

- **Interface:** I2.
- **Classification:** MINOR. additive, IC-349's reading: two item kinds the ODF part-map did not emit now appear, and the markers that said the parts were unread are removed because the parts are read; a package without `meta.xml` says `part_absent`. Stacked on D-612.
- **The change:** Mapped by MEANING (ODF `meta:initial-creator` = author, `dc:creator` = last editor); the package's own parts, `Pictures/` images (IC-124) and font faces are not intra links; absent, encrypted or unreadable members are undetermined links.
- **Base read at resolution:** I1 1.13.0, I2 2.10.0, I3 126.16.0, I5 7.8.0, I6 0.2.0, I7 1.1.0, I9 0.3.1 (c22-batch30 @ d557a837, `origin/main` @ 95fe7bc7 plus the batch); this batch's ICs take their numbers I3 first (MAJORs, then MINORs), then I1, then I2, and each interface's versions move in that order, MAJORs first.
- **Proposer:** D-346's branch stated the interface and left the IC to the integrator.
