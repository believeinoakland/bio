# BOB — resume here. Kept CURRENT by BOB #36 (session_01TDAu2wMVfAbxnzwhBUzEt6), last written 2026-09-25 ~11:26Z at 26% context; successor: **BOB #37** (not yet requested)

Read `CLAUDE.md`, `kickoffs/BOB.md` and `BIO_System_Design.md`, then this. Every line is a POINTER, measured at the time it names.

## 0. BOB'S STANDING DIRECTIONS (still in force)
- Productivity first until the build plan in its CURRENT SCOPE is complete (Bob, 2026-09-24 22:30Z). RELEASES ARE HELD: no cut unless Bob types the ask in DIST's session.
- 16 workers WORKING. Batch everything; trains ~2 h, never early. Report done and decided in plain words. Every ruling goes into the BOB INBOX in the same act as its message.
- **SUPERSEDED 2026-09-25 ~12:50Z by Bob:** "Stop spawning new worker sessions and focus on batching up the changes for merging." No new worker spawns; running workers finish; CONDUCT batches and trains every ready branch. This replaces "16 workers WORKING" until Bob says otherwise.
- **PARKED 2026-09-25 ~13:25Z by Bob** ("It was a mistake running this process in the cloud overnight"; the weekly token budget cannot complete batch30). Every lane was told to push its work, write its NEXT file, delete its timers and stop; each worker pushes WIP and stops, with no gate. BOB #36 deleted its own probe and dead-man. The estate resumes ONLY when Bob restarts it. On restart: read CONDUCT-NEXT (batch30 tips, what remains: IC trim, figures, docs, gate, train), then land batch30 BEFORE anything else; then batch31; no new workers until every current branch is merged (Bob, 13:00Z). The weekly budget resets 2026-09-29 18:00Z on this account (Bob's second account resets early Saturday).
- **BOB'S HYPOTHESIS, to test FIRST on restart, before any batch (Bob, ~14:00Z 2026-09-25): the debt rows are "playing tennis": a fix moves something one way, the process then finds it must move back, over and over. Three days ago the plan had about 100 rows; after spending three weeks' worth of tokens it has 225, mostly debt created in those three days.** Measured by BOB #36 from coord @ 90e0a835 (13:10Z): 225 open rows; 222 carry `added:` dates of 23-25 Sep (167 on the 25th; a date may mark a re-placement); 175 are D rows; 83 are owned by M0 (process and test tooling) against 26 REC/UI/MK; 171 rows' text says another row's worker or an integration found them. Observed today: BOB #36 ruled D-706 one way, then the reverse, then back again (10:50, 10:58, 11:15); batch30's 24 drift arms were ALLOWED with 15 rows (D-743..D-757) to un-allow them later; the `map` budget was raised with a row to lower it again; defect chains UI-121 to D-712 to D-731, and D-708 to D-720/D-721 to D-728. Suspected mechanism: the rule "a defect you find is diagnosed and minted", with no brake weighing a row against product value, multiplied by 16 workers. The analysis should pair rows that reverse each other, and should measure how many rows exist only to serve the plan's own tooling.
- **ANALYSIS of the tennis hypothesis (BOB #36, 2026-09-25 ~14:15Z, on Bob's ask). Instruments: git numstat and diffs on origin/main since 2026-09-22 (non-merge commits); coord QUEUE.md, BACKLOG.md and QUEUE-closed.md @ ba6cfb83, parsed by script.** (1) Volume: 533 rows were added since 22 Sep (311 closed: 306 done, 5 superseded; 222 open); 409 of them are D or M0. (2) What landed: 1,045 commits. Of the added lines, PRODUCT code is 12.0%; tests and controls 42.8%; generated bundles and json 22.3%; process docs and ledgers 10.5%; plan tooling 5.5%; design docs 4.5%. (3) Back and forth in product code is SMALL: 28,211 product lines added, 3,676 deleted, and only 1,701 (6.0%) of the deleted lines had themselves been added in the same window. (4) Fix-caused rows: 33 of 409 D/M0 rows state in their own text that a recent row's change caused them (8%, a lower bound). (5) By the files each D/M0 row names: 146 machinery-only, 92 product-only, 37 both, 134 unclear. Open and not done: 70 machinery, 58 product, 15 both, 54 unclear. VERDICT: the waste is real but mostly SELF-SERVING VOLUME, not reversal. About 88% of what landed is not product code, and the plan mints rows about its own tests and tooling. True tennis exists (BOB #36's own D-706 flips; allow-then-remove rows such as D-743..757 and the map budget), but it is a minority. The product fixes include real ones that must NOT be thrown away (disclosure: REC-196, D-706/D-722, D-480; false statements: D-731, D-538, D-552). Candidate discard: the machinery-only open rows (70), after a per-row look.
- **PROCESS-DESIGN.md WRITTEN (Bob's permission, ~14:30Z): `docs/development/PROCESS-DESIGN.md` on land/bob/process-design @ 9591cdeb (pushed, not landed; the estate is parked). PROPOSED and awaiting Bob's approval. Once approved it OUTRANKS every process rule. On restart, read it BEFORE anything else: its §10 replaces "land batch30, then batch31" with: land batch30 once; sort the 225 rows into module job lists, dropping what is not product-affecting; run at most 3 jobs; merge each as it passes.**

## 1. THE SESSION TREE (11:26Z)
Root FLEET #4 session_01YB9VgJtjiXwQ5vtx4fLvRB creates every lane. Lanes: SCHEDULER #24 session_01AFfq8GhbuuRpxUsjPtrigi (created 10:45Z; #23 archived) · CONDUCT #22 session_01NdCepFRW8KzaQMyVjyFTqM, REFRESHING: CONDUCT #23 requested from FLEET at 11:24Z (CONDUCT-NEXT on coord 9304ba3d) · DIST #7 session_01FQcUMZ2f34zhHzBkMEEdQ6. BOB #35 ARCHIVED 10:41Z (D-398 met; land/bob/claude-namespace @ ee8b2db7 handed to CONDUCT for the next train).
- Two stale branches NOT BOB #35's: land/bob/folds-0924c and folds-0924d (BOB #32, 2026-09-24), not on main by cherry. Unjudged; check whether main supersedes them before deleting or re-riding them.

## 2. TIMERS (read your OWN list_triggers; delete mine, arm yours)
- 20-min "BOB #36 slot probe" chain (send_later). Its counter is scratchpad slots.py (it dies with this container; rewrite it: WORKING, or REVIEW_READY whose summary names gate/battery/running).
- Hourly "BOB #36 hourly dead-man", trig_014R2DgZPBsGyHgYLGfNFN9A (cron 47 * * * *).

## 3. PLAN PAGE https://claude.ai/artifact/M5hUaNBgeM292h4D6odXbX
Last built after batch27. The rebuild is BLOCKED on Bob's permission for the builder (the auto-mode classifier refused the builder files fetched from the artifact). Do NOT work around it.

## 4. PUT TO BOB, UNANSWERED (restated once by BOB #36 at 10:17Z; each runs as built until he answers)
- THE RELEASE HOLD: main carries D-512, REC-196, D-134/D-610 and D-561 (disclosure and authority fixes), none of them live on biosmoke7 (0.79.0). DIST #7 says its note was for awareness, NOT an ask. UPDATED 11:25Z: keep the hold at least until D-725 lands (false attribution in signed bytes, per CONDUCT #22). After that, ship only if someone besides Bob is a member.
- MK-7's two provisionals (§4.4 narrow veto; §4.6 `name` = HANDLE). Recommendation: keep both. When answered, fold them into MEMBER-KNOWLEDGE-DESIGN together with the founder ruling.
- DEC-33's re-entry (UI-121 → UI-122, placed BLOCKED). Recommendation: keep DEC-33.
- The plan-page builder permission (§3).

## 5. RULINGS BY BOB #36 (all in the BOB INBOX; SCHEDULER drains)
- 10:26Z UI-118: a review-copy tie that names a recipient makes NO FILE and says why (option 1). No row.
- D-706's write half, FINAL 11:15Z (supersedes 10:50Z and 10:58Z): (A)+(C). linkproject resolves ONCE through the viewer's gate and writes only the edges its answer names; bundle= naming an invisible bundle is refused NO_SUCH_BUNDLE; a hidden source is not-held; a project's source bundle needs a JOINED caller. Row D-722.
- 11:00Z D-701's residue (an unfiled capture is visible to all): NOT a defect; acquire records no project.
- 11:05Z D-710's gap: a D-635 shared page reads `mixed`. Row D-723. `content:ocr` stays an equality; capture_text.chain_kind stays the last step.
- 11:20Z D-724: a partial index keeps every unit that fits, and RECORDS and SERVES the skipped unit keys ("not indexed: over the bound").
- 11:30Z D-720: a pair draft's statementack reading is keyed at no case, draft-bound, and binds only by REC-217's publish draft=.
- Ordering, told to SCHEDULER #24: UI-121 must not reach main before D-712, or in the same train without it (a public page otherwise says a signed case is unsigned).
- D-673's fence day: already ruled by BOB #35 ("before"); built as ruled.
- 11:50Z D-731: the verify button on the signed case document: (a) in D-712 (it checks op=casedocument and says so), plus (b) as a RECORD row (the case document's sha goes into published_shas at caseratify; publishedbytes serves it). UI-121 and D-712 train only with (a) in.
- 12:30Z D-717's sweep: no row; REC-23 stands (promote is not the gate) except where a harm is measured.
- 12:35Z D-720's residue: a LOW read-only measurement row (the live count of pair-draft readings keyed at C1). Any re-keying comes to BOB before a write.
- 12:40Z D-707's finding (GOVERNING_LAWS_REWRITTEN on unreadable front matter): readability is judged before any content fence; lawsOf answers UNDETERMINED and the fence abstains. Placed behind D-726.
- 12:45Z per-link recurrence on op=links: no row; recurrence stays at the navchanges read (D-340).
- Lanes at 12:37Z: CONDUCT #23 session_01NYMcSDBEBi7p1Ny3kfQJVW (CONDUCT #22 archived); SCHEDULER #24. BOB #36 at 37% context. Batch30 not yet landed (main 95fe7bc7).
- 13:15Z PLAN PAGE REBUILT (version 22) by BOB #36 on Bob's ask, with no summaries (Bob: "Don't bother with the plain language summaries"). Built by BOB #36's own script, which reads QUEUE.md and BACKLOG.md from coord, uses each row's headline as its title, and adds a "Right now" banner; no fetched builder code was run, so question 4 (the builder permission) is MOOT. The script is in the scratchpad (build.py, parse.py) and dies with this container: rewrite it from this description if needed.

## 6. OWED BY ME
- When D-346 (ODF) is on main: fold its ODF column into OFFICE-FORMATS.md §"What each part-map offers". Batch30 has not landed; main is 95fe7bc7.
- Slot count 11:25Z: 12 active. SCHEDULER #24 asked to fill, or to name the cap holding it.
- Weekly usage: allowed_warning, resets 2026-09-29 18:00Z (recorded, not brought to Bob, per his ruling).
