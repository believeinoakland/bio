# Thread FRAMEWORK: paste-ready kickoff

STUB, written 2026-07-30 by the CAPTURE thread when kickoffs were split per
thread. The FRAMEWORK thread's own session should replace this wholesale at its
next close; nothing here is that thread's account of itself.

This thread owns `docprofile/**`, `docs/architecture/BIO_Content_Framework_*`,
`docs/architecture/CONSTRUCTS.md`, and
`docs/development/DOCUMENT-PROFILES.md`. See `kickoffs/README.md` for the
register and the append-only rules for shared files.

Its work to 2026-07-30, read from the commit log rather than from its own
handoff: Content Framework v0.1 through v0.10, six diagrams validated against
the Mermaid parser, declared bias integrated with evidence accruing to bias
statements, contradiction treated as a discovery that leaves a record, and decay
made loud and never blocking.

---

Kickoff: thread FRAMEWORK. Fetch these from
raw.githubusercontent.com/believeinoakland/bio/main and read them before anything
else: docs/development/kickoffs/README.md (the thread register and the
append-only rules for shared files), docs/architecture/BIO_Content_Framework_v0_10.md,
docs/architecture/CONSTRUCTS.md, docs/development/DOCUMENT-PROFILES.md,
docs/development/DEBT.md (take the next free D-number at the moment you write).

Then tell me what this thread's next work is before starting it, because the
last session's own handoff was overwritten before it could be written and this
file is a reconstruction by another thread rather than that session's account of
itself.

Grants for this session (sessions carry no secrets; paste all three even if
unchanged): Cloudflare deploy token: [PASTE]. GitHub fine-grained token for
believeinoakland/bio, Contents read/write, 7-day expiry: [PASTE]. Member token,
read-only: [PASTE]. Deploy target account id 20b533579290b9b93168345edd3b7f72
(biocloudflare).

---

## What this thread should know without being told

**Rewrite only this file at the close.** Kickoffs are per thread now, precisely
because two sessions previously rewrote one shared file.

**Fetch and rebase before pushing; never force-push main.** A rejected push means
another thread landed work. Reset onto the remote, re-apply your additions, and
check the other thread's work survived.

**Bob's standing rulings** are collected in `kickoffs/CAPTURE.md` under "Bob's
rulings, already made". Several are cross-cutting and bind this thread too,
particularly that `undetermined` is first-class and must be stated, and that
third-party content is attributed to the third party.
