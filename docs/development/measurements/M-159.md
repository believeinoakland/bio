## M-159 · 2026-09-25 · DIST-8 — the first sweep of gone sessions' scratch residue on biosmoke7

Instrument: a ~15-line caller in DIST #7's scratchpad (`call.mjs`: `GET|POST https://<BIO_INSTANCE>.believeinoakland.workers.dev/?op=<op>&…`,
the ADMIN machine credential as the `token` query parameter, never printed), Node 26.10.0, the cloud container, through the
agent proxy. Plane serving 0.79.0 (DIST-NEXT's rollback table). Every call named its store; the WITNESS is `op=stats store=bio`
read before and after every arm and compared byte-for-byte on its `result`.

**Whose residue it was (read before sweeping, `op=list store=scratch`, 01:40Z):** 17 bundles, every one a GONE session's —
14 `INQ-2026-9172-dist5-{a,b}-*` and 1 `-c-*` (DIST #5), `INFO-2026-9436-dist4-muc6a1x0` (DIST #4), one livefire canary
`INFO-2026-9001-livefire-30844294`. None was a live session's; the count equals CPDF-3's (17 bundles, 11 aiRuns), so nothing
was written to scratch since.

**NEGATIVE CONTROL (the row's), run first:** `POST op=purge confirm=scratch` with NO `store` → **400 REQUIRED_ARGUMENT_MISSING,
C-61.1, `expected: "bio"`, `got: "scratch"`, "Nothing was changed"**; `bio`'s stats byte-identical across it. An admin call that
does not name scratch resolves to `bio`, and purge's confirm guard is what refuses it.

**THE SWEEP:** `POST op=purge store=scratch confirm=scratch` → 200, `scope: "ALL"`, `store: "scratch"`.

| `op=stats store=scratch` | before | after |
| --- | --- | --- |
| bundles / files / indexed | 17 / 17 / 17 | 0 / 0 / 0 |
| history | 2 | 0 |
| taskQueue / sourceReachability | 48 / 10 | 0 / 0 |
| monitorFired / monitorTickEpoch | 2 / 1 | 0 / 0 |
| aiRuns / aiRunBounds / aiRunLog | 11 / 12 / 11 | 0 / 0 / 0 |
| inquiryRunSurfacings / basisVersions | 1 / 1 | 0 / 0 |
| observationsNonLead | 59 | 0 |
| every other counter | 0 | 0 |
| dbBytes | 1,740,800 | 1,601,536 |

`op=list store=scratch` after: `[]`. **`bio` WITNESS: `op=stats store=bio` byte-identical before the control, after the control
and after the sweep** (bundles 31, files 142, history 244, register 88, dbBytes 7,077,888).

**NOT SWEPT — NARROWED, stated:** `op=memberlist store=scratch` still lists **14 members** (the three `d41-sf72-a*`,
`probe-1785025010`, `probe-ms11t4s2`, `scr-ms11znmp` of 2026-07-26; `vf4ruth`, `vf4t1`, five `vf4m2026091*` of 2026-09-14/19;
`dist3-rec156-muboxe9j` of 2026-09-21). Membership is outside `purge`'s reach in both arms (the whole-store arm deletes no
member row), and no op deletes a member. `op=stats` counts no members, so "scratch reads empty" holds of every counter and
not of the roster. R2 captures under `scratch/captures/` are untouched too (purge's own doctrine: orphaned, content-addressed).
