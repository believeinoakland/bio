## M-172 · 2026-09-25 · D-563 — THE RECORD'S HEAD ROWS AGREE WITH THEIR DOCUMENTS ON TITLE AND STATE, AND NOT ON `prior_state`: **0 title and 0 `current_state` drifts of 31 in `bio`, 11 `prior_state` drifts** (row `null`, document `collected`/`surfaced`); `scratch` holds no bundles. And the battery's fixtures label a title the document does not carry in **2,006 promotions across 163 suites**. (WORKER D-563, SCHEDULER #22)

**What was asked.** D-563 derives `bundles.title`, `current_state` and `prior_state` from the promoted DOCUMENT and refuses
an envelope that contradicts it (C-86.3, C-86.4). Two questions decide what that refusal meets: which callers send a
label contradicting the document, and which live rows an envelope already wrote apart from their bytes (an internal
writer that relabels from the ROW would meet the refusal on them).

**Instrument 1 — the live record.** `tools/m172-title-state-census.mjs`, node, cloud container, worktree `/home/user/bio`
on `land/worker/D-563`, 2026-09-25, against the instance named by `BIO_INSTANCE`. READ ops only, each naming its store:
`op=list` (its `total` equals the count listed, so the viewer gate hid nothing), `op=image` per bundle for the HEAD
`bundle.md`, parsed by the catalogue's own `parseFrontmatter`; an inquiry's title is also compared with its question's
rendering (C-16, `deriveInquiryTitle`). **WITNESS:** `op=stats` before and after each register, identical (bio 31
bundles, 244 history rows; scratch 0 bundles).

| register | bundles | heads read | blob-held | title differs | `current_state` differs | `prior_state` differs |
| --- | --- | --- | --- | --- | --- | --- |
| bio | 31 | 31 | 0 | **0** | **0** | **11** |
| scratch | 0 | 0 | 0 | 0 | 0 | 0 |

The eleven: INFO-2026-0099, -0100 (×2), -0103 … -0108, INFO-2026-5460 (row `null`, document `collected`) and
PROB-2026-0001 (row `null`, document `surfaced`) — the Drive-era migration wrote their rows with `prior_state` null.
**Consequence taken in the same landing:** `provenanceChainRebuild` relabelled its own write from the ROW; it now sends
a row value only where the carried `bundle.md` states no such key (`#rowUnlessStated`), so it cannot meet
ENVELOPE_STATE_DISAGREES on these eleven, and a rebuild writes the document's `prior_state` into the row. The
action-correspondence family sends `current_state` from the row and `prior_state` from the document: with 0
`current_state` drifts it meets nothing today; a future drift would be refused by name, not written.

**Instrument 2 — the battery's callers.** A temporary `console.log` probe at the top of `promote` (removed; the tree was
restored to HEAD by `git diff --quiet` before the fix was applied), every `test/*.test.mjs` run once on origin/main
5e8a65a8: 2,430 promotions whose envelope and document differed in any of title / state / prior_state. Classified with
the fix's own rule (both sides stated, compared whitespace-collapsed, an inquiry's question title accepted, replays
excluded): **2,006 title contradictions in 163 suites; 28 `current_state` contradictions in 11 suites** (projects labelled
`collected` over a document saying `forming` — or the reverse — in `d389-fullfetch`, `project-mint`, `rec174-supplyfetch`
and `grounds`; concluded inquiries re-promoted under a legacy `published` label in the case suites); 0 `prior_state`. Every one is a
FIXTURE label (`Bundle ${id}`, `t ${id}` over a document titled otherwise); the plane's internal writers send the
document's `fm.title`, and the UI (`app.html`) and `setup.mjs` send the title they wrote into the document.

**What the matchers cannot see.** Instrument 2 walked `bio-plane/test/*.test.mjs` ONLY: the UI suites under `civicos-ui/test/` also drive a real plane and were NOT probed — the full gate found 14 of them labelling the same way (accept-ceremony, agent-surfaced-inquiry, case-frozen-pair, conclude-nofalsifier, conclude-reading, content-extent, passage-surface, published-index-pair, question-npc, queue-recipients, queue-unmute, several-cases-choice, statement-writer, version-notice-surface) by failing them, and they were corrected in the same landing; their promotion counts are not measured. Instrument 1 reads HEADS only (a drift in history is not a live row) and cannot see a
bundle `op=list` does not return to an admin viewer. Instrument 2 sees only the battery's promotions: a client outside
this repository that labels arbitrarily would meet C-86.3 and is not counted.
