## M-185 · 2026-09-25 · D-726 — THE RECORD'S HEAD ROWS AGREE WITH THEIR DOCUMENTS ON THE PRODUCING GROUP: **0 `group` drifts of 31 in `bio`** (and 0 `id` drifts of 31, the sweep's second column); `scratch` holds no bundles. (WORKER D-726, SCHEDULER #24)

**What was asked.** D-726 refuses a non-replay revision whose DOCUMENT restates a `group` different from the head's
`bundles.group_id` (REVISION_REGROUPS_BUNDLE, C-86.14). The row asks for the live-corpus count: a head whose bytes
ALREADY disagree with its row is one a revision wrote before the fence, and the fence does not rewrite it.

**Instrument.** `tools/m185-group-census.mjs`, node, cloud container, `/home/user/bio` on `land/worker/D-726` (stacked on
`land/worker/D-707` @ 95839afa), 2026-09-25, against the instance named by `BIO_INSTANCE` (`biosmoke7`). M-181's
instrument one column over. READ ops only, each naming its store: `op=list` (its `total` equals the count listed),
`op=projection id=<bundle>` for the row's `group_id` (op=list carries none), `op=image` for the HEAD `bundle.md`, parsed
by the catalogue's own `parseFrontmatter`. A document's group is compared as the fence compares it: any stated value,
String()-ed and trimmed. **WITNESS:** `op=stats` before and after each register, identical (bio 31 bundles, 142 files,
244 history rows; scratch 0 bundles).

| register | bundles | heads read | blob-held | row states no group | document states no group | `group` differs | documents stating `id` | `id` differs from bundle id |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| bio | 31 | 31 | 0 | 0 | 0 | **0** | 31 | **0** |
| scratch | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

Every row's `group_id` is `believe-in-oakland` (31 of 31); sample INFO-2026-0001-sewer-transfer-series, row and document
both `believe-in-oakland`. The comparison is not empty by construction: 31 of 31 heads were read and state both a group
and an id.

**Consequence taken in the same landing.** Nothing live stands for the refusal to meet, so it is a refusal, and no head
is rewritten.

**The sweep (D-726's class: a column the promote upsert's ON CONFLICT arm never writes, restated by a revision).** The arm
writes object_type, title, current_state, prior_state, last_updated, criticality and bundle_sha; it never writes
`group_id` (this item), `created` (D-692, C-86.9) or the key `bundle_id`. Driven through op=promote in a local Miniflare on
this tree: a REVISION whose bytes say `id: INFO-2026-0726-someoneelse` over `INFO-2026-0726-sweepid` LANDS, and the head
bytes then name another bundle; a CREATION whose bytes' `id:` differs from the `bundleId` it is filed under LANDS too.
C-1.1's rule (folder name equals frontmatter id) is not asked at the promote door. Minted **D-738**, not fixed here; the
live count above (0 of 31) is its census too.

**What the matchers cannot see.** The census reads HEADS only; an earlier version's bytes are not compared. It cannot see
a bundle `op=list` does not return to an admin viewer. `meta.group` on a revision is read nowhere (D-436) and is not
counted.
