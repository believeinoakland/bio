## M-173 · 2026-09-25 · D-557 — THE DECODE CENSUS NOW JUDGES THE TEXT THE READING CLASSIFIED: **all 34 of the documents M-152 found judged on EMPTY text are now judged on their tier-3 text, and no document is labelled tier 3 unless tier-3 text was judged.** That tier-3 text is **PAGE ONE** of each document: the plane OCRs one page per acquire (D-606, queued, unbuilt). (WORKER D-557, SCHEDULER #22)

**What changed in the instrument.** `bio-plane/scripts/fw20-decode-census.mjs` with `FW20_TEXT_DIR` set writes
`<sha>.txt` from the acquire document's own `text_units`. Those are REC-91's per-page units, taken from the same i2
text the content-type reader was handed, after tier 2's merge and tier 3's transcription. Before, it wrote the plain
`op=pdfstructure` answer, which stops at tier 2. The plain answer is still written, as `<sha>.plain.txt`, for audit.
The row gains `judged`: the units, their characters, and the tiers and members of the producers of exactly those pages
(from `reading.provenance.producers`, never `text_tier`). It also carries the reader label and whether the SHA-256 of
the joined units equals the reading's own `text_sha256`. `tools/m032-class-census.py`'s `plane_text` takes that label
as it stands. `reread` now compares `reading_provenance`, the provenance of the text judged. `classhash` reads
**`161d2ff9…`, 38 definitions, 26,117 bytes**, the same as M-143 and M-152, so no threshold moved.

The `ocr=1` alternative the row named was not taken. `op=pdfstructure&ocr=1` re-reads only a PROMOTED capture and
refuses otherwise with `REEXTRACT_NOT_READ`. A census acquires and never promotes. The script's header claimed it
called `ocr=1`. It never did, and the header is corrected in place.

**Instrument and run.** The walk ran from a snapshot worktree of commit `0111212c`. Its census script is identical to
the landed one outside comments (checked by `diff` with comments stripped). The only change to the Python file after the
walk adds `judged` to the kept `plane_prov`, which is not on the classification path. Cloud container, Python 3 stdlib
and node, 2026-09-25 04:30–05:33Z.

    M032_PEN=<pen> M032_HALVES=bucket python3 tools/m032-class-census.py list        # 43,283 bucket keys, exit 0
    M032_PEN=<pen> M032_HALVES=bucket M032_KEEP=1 python3 tools/m032-class-census.py bodies 1000 20260924   # 3,198 s, exit 0
    M032_PEN=<pen> M032_HALVES=bucket python3 tools/m032-class-census.py names && … derive   # exit 0

**Corpus.** `s3://cao-94612` listed **43,283 bucket keys**, the same as M-126, M-143 and M-152. The draw used seed
`20260924` over the text-bearing stratum of 28,915 with `M032_HALVES=bucket`, which is **D-66's sample**.

### THE MOVED FIGURES

| | M-143 (final) / M-152 | this walk (D-557) |
| --- | --- | --- |
| documents the acquire read at `text_tier` 3 | 38 | 38 |
| of them judged on EMPTY text | **34** | **0** |
| of them judged on their tier-3 text | 4 (see below) | **38** |
| labelled tier 3 with no tier-3 text judged | 34 | **0** |
| agenda / minutes / staff_report (any) | 132 / 92 / 101 | **141 / 94 / 106** |
| ordinance_res / directory / budget_dataset / financial_report | 48 / 24 / 16 / 2 | 48 / 24 / 16 / 2 |
| other (read, no class) | not stated in M-143 | 511 |
| unclassified | 202 | **173** |
| escalated to the plane · REFLOWED | 332 · 8 | 332 · 1 |

**Readers, with labels now taken from the producers of the pages judged.** M-143 read: 668 instrument tier 1 ·
164 plane t1 · 82 t2 · 38 t3 · 47 no tier · 1 refused. This walk reads: **668 instrument tier 1 · 132 plane tier 1 ·
79 tier 2 · 3 tier 1+2 · 34 tier 3 · 4 tier 1+3 · 79 no text judged · 1 acquire refused**, summing to 1,000. It
reconciles line by line with M-152's re-read:
- 164 plane t1 = 132 with text + 32 that served no text.
- 82 t2 = 79 + 3 mixed documents whose pages came from tiers 1 and 2.
- 47 no tier + those 32 = the 79 with no text judged.
- 38 t3 = 34 wholly tier 3 + 4 mixed tier 1+3.

A label now names a tier only if text from that tier was judged, so a mixed document names both tiers.

**THE CLASS MOVES ARE EXACTLY THE DOCUMENTS D-557 IS ABOUT.** Take the 34 out of today's tally and count them as
unclassified, as they were when judged on empty text. That reproduces M-143 for minutes, ordinance_res, directory,
budget_dataset and financial_report exactly. It leaves agenda +1, staff_report +1 and unclassified −1. That residue is
ONE mixed document, `Minutes-Signed-01.27.20.pdf`. Its tier-1 text page held **9 characters**, so M-152's "the other 4
were judged on their text layer" was true of it only in letter. It is now judged on 2,472 characters, its tier-3 page
included, and reads agenda + staff_report. The 34, today:
- agenda 8, minutes 2, staff_report 4 (a document may hold two)
- 15 read with no class threshold met
- 6 still unclassified, "text too short" (fewer than 60 words on the one page read)
- so 28 of 34 moved out of UNCLASSIFIED "no text layer"
- their judged text runs 62 to 5,216 characters, median 1,854

**The text judged IS the text the reading classified.** For the 34 single-producer documents, the judged characters
equal the reading's own `text_chars` exactly. The 4 mixed documents differ only by the page separator: `\n\f\n` in the
file against `\n` in the reading's flatten, +2 characters per page break (+16, +2, +6, +6 for 9, 2, 4, 4 pages
judged). This walk's rows do not carry `judged.matches_reading`, because that field was added to `plane_prov` after the
snapshot was taken. Digest equality is therefore asserted by the suite, on a real scanned Oakland page and on a mixed
document, not by this walk.

### WHAT "TIER-3 TEXT" IS TODAY: PAGE ONE

Every one of the 34 has **exactly ONE page transcribed, and it is page 1**, by `tesseract-wasm 0.11.0` on
`ocr-worker`. Those 34 documents hold **849 pages**, and only 2 of them are single-page documents. So the census judges
**34 of 849 pages** of this class, 4.0%. That is what the record holds: the plane OCRs one page per acquire (D-606,
queued, unbuilt; measured by D-460, M-165). A class this census assigns to one of these documents rests on its first
page. A document it leaves unclassified, "text too short", was read on one page and not on the rest. **That is a fact
about the plane's reach, not about the documents.** When D-606 lands, this walk re-taken moves these figures again, and
the instrument needs no change to see it: the units carry every page the reading transcribed.

### THE SUITE AND ITS NEGATIVE CONTROL

`bio-plane/test/d557-census-judged-text.test.mjs` runs the committed census script as its caller does, over three
documents in miniflare: the real plane, the committed pdf-worker bundle and the REAL OCR member. The documents are:
- the real scanned Oakland page (`pdf-worker/test/fixtures/scan-ccitt-g4-page.pdf`)
- a born-digital page (the over-strictness arm)
- a mixed document: a text page, then the scanned page lifted byte for byte

**16 passed, 0 failed.**

`node test/nc-d557.mjs` from `bio-plane/` ran 3 arms, 0 not as declared. Each arm was declared before arming and armed
alone:
- **baseline**: 16/0.
- **plain**, the row's control: the census judges the plain answer again. Result 12/4. The TIER-3 arm fails BY NAME,
  *"TIER-3 TEXT JUDGED: the scanned page's judged text is its OCR text, not empty"*. So do the digest arm, the mixed
  document's both-pages arm and *"NO ROW IS LABELLED TIER 3 UNLESS TIER-3 TEXT WAS JUDGED"*. Both born-digital arms stay
  green.
- **texttier**: the label is read off `text_tier` again. Result 15/1, exactly *"THE MIXED LABEL"*. The scanned page
  alone cannot tell the two labels apart, since both read 3, which is why the mixed fixture exists.

The census script was restored by `copyFileSync` from per-arm pristine copies outside the worktree. Each restore was
verified by sha256 AND byte comparison. The final restore: 11,402 B, sha256 `4387382e…`.

**Blind spots.**
- The 668 documents the instrument's own tier 1 read never reach the plane.
- The units obey the acquire wire's budget (`ACQUIRE_TEXT_UNITS_BUDGET`, 524,288 B). `judged.over_bound` counts what it
  dropped. This walk's kept rows do not carry it, so on this walk it is UNDETERMINED. The judged characters equal the
  reading's own for all 38, which no truncation would leave true.
- A page of pure whitespace is not a unit (D-531), so it is never judged.
- Whether tier-3 text is stable across two reads is M-152's question, and it is not re-asked here.
