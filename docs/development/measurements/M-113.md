## M-113 · 2026-09-23 · M0-126's per-unit result record: the trace census of the whole estate, and a gate run twice

Instrument: `node` 26.10.0 in the cloud container (shared with other lanes' gates), the M0-126 worker's worktree
(`.claude/worktrees/agent-af8815030f1fe92e5`). Results remote for every run below: a LOCAL bare repository
(`BIO_GATE_RESULTS_REMOTE`), never `origin` — nothing here wrote to the shared `gate-results`.

**1 · The trace census (condition 2's measurement).** Every unit of the estate — the whole battery, every UI suite, every
UI check, `coverage --strict` — run once under `tools/gatetrace.mjs` (33 min wall), and each unit's traced repository
reads compared with its derived input set (`gates.mjs --inputs all`). 346 of 347 units left a trace (the one without is
`gateresults.test.mjs`, written after the census began).

| derivation | units under-included (never-cached units excluded) |
| --- | --- |
| MENTION read forward, plus the plane/fleet runtime set and docs/ for doc-facing units | 20 |
| + root walks in a unit's own file or a helper in ≤ 3 units' closures; plancheck runners never cached | 4 |
| + the import closure followed through runtime code; mentions in a directly imported runtime module | 2 |
| + a UI check takes every UI suite's inputs; `status.test.mjs` declares `GATE: reads *` | **0** |

Input sets: min 3, median 136, max 1,032 files. Never cached: 12 units (11 that run plancheck, and plancheck-stub
fixtures among them). A root walk read in a SHARED helper as every importer's walk took 96 units to the whole tree while
their traces read a median of 54 files — rejected. Key computation for all 350 units: 2.9–3.6 s per gate (it was ~12.7 s
before the forward index; measured by `--cpu-prof`).

**1b · History readers (BOB #30's ruling).** A second full traced gate (`--full --no-reuse`, tree after commit
`M0-126: BOB #30's history ruling`, battery `287/288 suites green · 17309 assertions passing`, 2,592 s, the one red
`mergecarry.test.mjs` from main's own 4355bfd drop) with the tracer recording `git` history and live-ref commands run
over this checkout: 11 cacheable units read one — `decided` (58 commands), `op-claims` (35), `migrate-released` (32),
`mintid` (26, including `git fetch origin coord`), `readbudget` (7), `owed` (5), `register-grammar` (2), and one each
`owed-controls`, `status`, `coverage` (`git log -1`, a commit date) and `retirable` (`git ls-remote`). Each is now marked
`GATE: never-cache (history)`; 0 under-inclusions of files in the same run.

**2 · The same gate run twice** (tree `878a38b3`, the M0-126 branch merged with `origin/main` @ `b5ce975a`):

| run | class | units run | REUSED | wall | verdict |
| --- | --- | --- | --- | --- | --- |
| 1 · `gates.mjs` | FULL (a BACKSTOP: nothing reused) | all 350 + plancheck | 0 (the merge moved the plane, so every key was new) | 2,056 s | GREEN · `285/285 suites green · 17203 assertions passing`, 0 skipped |
| 2 · `gates.mjs --full` (skips §2d's tree shortcut) | FULLREUSE | 12 never-cached + plancheck | **338** | **488 s** | RED at `mergecarry.test.mjs` (below) |

Run 2's one red is not M0-126's: `mergecarry.test.mjs` ("no UNREGISTERED drop sits in main's history") reads the LIVE
`origin/main`, which another session's fetch into the shared git directory had moved to `4355bfda` between the runs; it
then names `4355bfd:docs/development/TREE-SHARING.md` and `4355bfd:docs/development/kickoffs/BOB.md`. It is never cached
(it runs plancheck), so it ran, as designed. Without `--full`, the second run of an unchanged tree is §2d's shortcut and
runs nothing (unchanged by M0-126). An earlier run 0 (tree `a382cf72`, before the merge) wrote 334 PASS records and found
two defects in M0-126 itself, fixed before run 1 (the fixture's inherited results remote; a one-suite battery step
attributing the runner's read to its suite).
