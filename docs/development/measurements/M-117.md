## M-117 · 2026-09-23 · D-178's corpus count — what giving `op=audit` the published registry moves

Instruments, each named with what it read: (1) the DEPLOYED record — `op=audit`, `op=stats`, `op=list`, `op=image` and
`op=publishedmanifest` against `https://biosmoke7.believeinoakland.workers.dev` (serving `0.78.0`, the build WITHOUT this
change), member credential, `store=bio` and `store=scratch` each named on every call, 21:23–21:27Z, all read-only; (2) the
FIXTURE — `bio-plane/test/audit-inheritance.test.mjs` in miniflare, and its control harness `bio-plane/test/nc-d178.mjs`,
whose `drop` arm IS the pre-change sweep, on `origin/main` @ `02603e88` plus the D-178 change, `node` in the cloud container.
Every figure below was PRINTED by a run.

**What moves, in principle.** `Store#auditPass` ran `checkBundle` with no `publishedRegistry`. Two outcomes move, both in
`checkInheritedLeg` (C-21.2's rule, `BIO_Publication_v0_1.md` §3 rule 5): an `inherited` leg on a published target stops
reading **C-2.8** ("cannot be checked against the published record here"), and a leg carrying its OWN grade on a published
target starts reading **C-21.2**. **C-21.1 cannot move:** the catalogue has NO reader of `ctx.publishedCaseRegistry` (grep of
`bio-checks.mjs`: one injection site, zero reads — C-21.1 runs over the CASE DOCUMENT in `checkCaseDocument`, which the
bundle sweep does not reach), so this item injects only the finding-altitude registry, exactly as the row's scope says.

| corpus | bundles | inquiries | basis legs | legs on an inquiry | inherited legs | published bundles | C-2.8 that stop | C-21.2 that appear |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| deployed `bio` (audit: 31 checked, 21 clean, tally `C-18.9`×10 only) | 31 | 1 | **0** | 0 | 0 | 0 | **0** | **0** |
| deployed `scratch` (audit: 17 checked, 0 clean, no C-2.8 in the tally) | 17 | 15 | **0** | 0 | 0 | not readable (rule 10) | **0** | **0** |
| the fixture, pre-change sweep (`drop` arm) | 7 | 4 | 4 | 3 | 1 | 1 | — | — |
| the fixture, with the injection | 7 | 4 | 4 | 3 | 1 | 1 | **1** (`INQ-2026-1780-inherits`) | **1** (`INQ-2026-1780-own-grade`) |

Fixture tallies as printed: pre-change `{"C-13.2":3,"C-2.7":2,"C-2.8":1}`, after `{"C-13.2":3,"C-2.7":2,"C-21.2":1}` — the
same 7 checked, the same 4 with errors; the C-13.2/C-2.7 findings are the fixture's own documents and do not move.

**So on the deployed record the landing moves nothing**: no bundle there has a basis leg, so the sweep's blindness had nothing
to misread yet. The defect was latent, not absent — the first inherited leg written to that instance would have made `op=audit`
unclean at C-2.8 on correct data, which is DIST's rung 6 (`BIO_Distribution_v0_1.md` §6).

**The witness.** `store=bio` counters read before and after the whole read set: bundles 31, files 142, history 244, refs 10,
register 88 — identical. Nothing was written.

**What this cannot see.** Only the one deployed instance this cloud reaches; any other member's record is UNDETERMINED. The
`scratch` published-record column is undetermined, not zero: `op=publishedmanifest` is pinned to the record namespace — but
with 0 basis legs in `scratch` nothing there can move whatever it holds.
