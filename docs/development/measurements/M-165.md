## M-165 · 2026-09-25 · D-460 — WHY SOME TIER-3 AGENDAS AND MINUTES READ GENERIC: **TWO CAUSES, SEPARATED BY MEASUREMENT. The suspected one is REAL at the code and live: the OCR member transcribes ONE page per invocation and the plane calls it ONCE, so on FW-20's walk 174 of the 190 scanned pages tier 3 was asked for were never transcribed. But on the real walk it is NOT what makes these documents read generic: transcribed whole, 20 of 21 still read generic, because the agenda and minutes readers are written to Legistar's shape and these are not Legistar documents.** Fix named for the first: D-606. A second, smaller defect found on the way: D-607.

**Instrument.** `bio-plane/scripts/d460-perpage-ocr.mjs` (D-460). It boots FW-20's census exactly — the REAL plane
(`bio-plane/src/index.mjs`), the COMMITTED pdf-worker bundle as `PDF_WORKER` and the COMMITTED OCR member (bundle + wasm
core + model, `ocr-worker/test/memberworker.mjs`'s recipe) as `OCR_WORKER`, in miniflare — and drives `op=acquire` once
per document in TWO ARMS that differ in one thing: arm `shipped` binds the member as deployed; arm `perpage` binds an
instrument-only WRAPPER that calls that same member once per requested page and hands the plane one answer carrying
every page the member answered for (it transcribes nothing itself and alters no region). The plane's own merge, chain
and content-type registry judge both. Figures are the plane's own `reading`: `content_type`, and the pages its basis
sentence says were and were not transcribed. Node in the cloud container, branch `land/worker/D-460` on `origin/main`
@ `964da679`. Every figure below was PRINTED by a run.

**At the code (read 2026-09-25 on `964da679`).** `ocr-worker/src/contract.mjs` `chooseChunk` takes the LOWEST requested
page and returns the rest as `deferred`; `transcribe.mjs` `transcribeRequest` transcribes that one page and says so in a
note. `bio-plane/src/index.mjs` `tier3Extend` is the ONE call site of `OCR_WORKER.fetch` in `bio-plane/src/`, sends
every wanted page, calls once, and never reads `deferred`; `mergeTier3Text` keeps each unanswered page's
`no_text_layer` marker. So the row's phrasing is refined: the plane does not DROP pages it was given — it is given one,
and never asks for the rest. Both acquire and the D-319 read path (`op=pdfstructure&ocr=1`) go through `tier3Extend`.

**The corpus.** FW-20's fixed-seed walk, REBUILT (FW-20 committed neither its sampler nor its manifest): the bucket
`cao-94612.s3.amazonaws.com` listed 2026-09-25 (44 pages, **43,283 keys** — M-121's count), `.pdf` keys with
20,000 ≤ size ≤ 4,000,000 bytes (**22,832** — M-121's "22,832 in FW-18's bounds", which binary KiB/MiB bounds do not
give: 22,916), sorted, mulberry32 seed `20260923`, Fisher–Yates from the end, first 300. Tier 1
(`extractPdfStructure`) selected **21** of the 300 for tier 3 (a `no_text_layer` marker, not encrypted), and arm
`shipped` read **16 at tier 3 with 5 unread** — M-121's "t3 16" and "OCR refusals ×5" exactly. That agreement is the
evidence this is FW-20's sample; the sampler's code is not. Manifest committed: `bio-plane/test/fixtures/d460/walk-tier3-manifest.json`
(sha256 `943b2f39…456b`). M-121 NAMES NONE of its tier-3 documents and does not say any read generic; that observation
reached D-460 through the row, not through M-121's text — so the scope is "the agendas and minutes among these 21".

**Result, the 21 (both arms, same bytes, same plane).**

| arm | pages transcribed | selected pages left unread | content types |
| --- | --- | --- | --- |
| `shipped` | **16** | **174** | generic 21 |
| `perpage` | **183** | 10 (member refusals) | generic 20 · regulation 1 (SSOC special-meeting packet) |

Per document (transcribed/selected, `shipped` → `perpage`): FINAL-2-6-PC-Agenda 1/7 → 7/7; FEB-MTG-PACKET 1/8 → 8/8;
Minutes-Signed-04.19.18 1/3 → 3/3; BPAC-Minutes_Jan-16-2020 1/6 → 6/6; Police-Commission-Selection-Panel-Agenda 1/10 →
6/10; Package-09.24.18 1/10 → 9/10; SSOC-Special-Meeting…Agenda-and-Materials 1/4 → 4/4; Complete-Agenda-February-15-2018
1/1 → 1/1; August-21-2019-Planning-Commission-Directors-Report 1/58 → 58/58; Item-02-PLN16100 1/22 → 22/22; oak070783
1/46 → 43/46; others in the run's JSON. Wall time in miniflare: 80 s `shipped`, 858 s `perpage` — a cost figure for
THIS runtime only; per-invocation memory on the deployed isolate is not measured here (D-312).

**Why they still read generic, by the readers' own rules over the fully transcribed text** (tier-1/2 text plus every
page the member transcribed, `meeting-agenda.mjs` and `meeting-minutes.mjs` `detect` called directly): **NONE of the
eight agenda/minutes/packet documents carries a single line-anchored Legistar file number (`NN-NNNN`)**, and none
carries both `Subject:` and `Recommendation:`. FINAL-2-6 names itself `AGENDA` on 5 lines (furniture rate) with a roll
call and still makes 2 of the 3 families LIKELY needs; FEB-MTG-PACKET's FIRST page — the one arm `shipped` DID transcribe
— reads `Meeting Agenda` and it is generic in both arms. **By eye they are not generic documents**: a Planning Commission
agenda, a Privacy Advisory Commission agenda, I-Bond oversight committee minutes, BPAC minutes. `MEASUREMENTS.md`
(the agenda type's own 2026-08-03 measurement) records this as a DELIBERATE closure: "the item shape of NON-Legistar
agendas — … an unrecognised agenda falls to `generic`, honestly." So on this corpus the generic reading is doctype
BREADTH (plan-owned, not a defect), not the page limit.

**NEGATIVE CONTROL — the row's, and it came back as declared.** Fixture `bio-plane/test/fixtures/d460/`, made by
`bio-plane/scripts/d460-fixture.mjs`: two 1700×2200 raw grey8 FlateDecode page images, no font, no text operator (tier 1
marks both pages `no_text_layer`). `agenda-p2.pdf` (sha256 `db4edd5d…22b6`): page 0 a cover notice with no agenda signal,
page 1 an agenda (masthead, roll call, `26-0910` alone on its line, `Subject:`/`Recommendation:`). `agenda-p1.pdf`
(`fb68e728…60f2`): the same two images swapped. Declared before arming, and actual:

| arm | fixture | MUST | actual |
| --- | --- | --- | --- |
| `shipped` | agenda-p2 | read `generic`, 1 page transcribed, 1 unread | **generic**, 1/1 — as declared |
| `shipped` | agenda-p1 (engine control) | read `meeting_agenda` (the engine CAN read the page) | **meeting_agenda** — as declared |
| `perpage` | agenda-p2 (the fix) | read `meeting_agenda`, 2 transcribed | **meeting_agenda**, 2/0 — as declared |
| `perpage` | agenda-p1 (over-strictness) | still `meeting_agenda` | **meeting_agenda** — as declared |

So the mechanism CAN turn a document generic, and the loop is sufficient to undo it; the walk shows it is not what did
so on FW-20's documents.

**Named fix — D-606.** `tier3Extend` asks the member again for each page in the answer's `deferred` (one invocation per
page, sequential, bounded by the selected-page count, each answer checked by `mergeTier3Text` as today), and merges
every ok answer; a refused page keeps its marker. The member's one-page contract (CPDF-15's memory bound) is unchanged —
the CALLER loops, which is what its note already asks ("call again per page"). Open for the fix's own row: the plane's
CPU/subrequest budget for a 58-page scan on one acquire (Workers' subrequest limit is the vendor's CLAIM, not measured
here), and whether the tail belongs in a deferred task rather than the acquire. The under-read it closes is the class
CLAUDE.md §2 ranks worst: 174 of 190 selected pages on this walk stayed unread, stated honestly by marker and basis, but
unread.

**Second defect — D-607.** `tier3Note` says "the page that already had text kept it" whenever the merge is not wholesale
and filled a page — including on a WHOLLY scanned document where no page had text (FINAL-2-6: 7 of 7 pages
`no_text_layer`; its `shipped` basis reads "1 scanned page(s) were transcribed … the page that already had text kept
it"). The record claims a text layer the document does not have. Fix: say that clause only when a page kept text of its
own (`layerPages` non-empty, already computed beside it).

**Blind spots.** The bucket only (Legistar not reached). The walk is a reconstruction whose agreement with M-121 is by
its counts, not by FW-20's code. Minutes' non-recognition is read from `detect` returning NONE over the joined text,
not from a minutes-shaped fixture. "By eye" is this worker's reading of the transcriptions, not an instrument.

**Commands** (from `bio-plane/`): `node scripts/d460-fixture.mjs`; `node scripts/d460-perpage-ocr.mjs <manifest.json>
<pdf-dir> [out.json]` (`D460_ARMS=shipped|perpage`, `D460_TEXT_DIR=<dir>` keeps per-page transcriptions); the walk's
bytes are fetched by key from the bucket and stored as `<sha>.pdf`.
