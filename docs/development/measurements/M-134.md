## M-134 · 2026-09-24 · M0-143 — THE DOC-FACING SUITE SET, BEFORE AND AFTER `gates.mjs` READS IT AS CODE

**Instrument.** `node tools/gates.mjs --explain`'s own line `gates: doc-facing suites derived fresh — plane […] · ui
[…]`, which prints only under a DOCS classification. **Sets no ceiling and no floor.** The one ceiling this landing
invalidates (`statepaths.test.mjs` `UNITS_CEILING`) is moved at its own site, from that suite's printed figure.

**Headline.** The doc-facing set went **72 → 41** (plane 60 → 37, ui 12 → 4). **31 of 72 — 43% — were doc-facing by a
COMMENT mention alone**, and **nothing was added**: the after-set is a strict subset of the before-set, checked as a
set difference in both directions.

### Method, and what it can and cannot see

Both figures come from the SAME instrument line under the SAME classification, which is why they are comparable: a
DOCS classification requires a diff that is entirely prose, so a tree carrying the change to `gates.mjs` itself
classifies TARGETED and never prints the line. Both arms were therefore taken in a **local clone**
(`git clone --local /home/user/bio`) whose `refs/remotes/origin/main` was moved, in the clone only, to the commit
carrying that arm's `tools/gates.mjs`; a one-line append to `docs/development/MILESTONES.md` was the prose diff, and
`BIO_GATE_RESULTS=off` kept the shared per-unit record out of it. No ref in the estate was moved.

- **BEFORE** — estate at `16fe1e7f` unchanged: **plane 60 · ui 12 = 72**. This reproduces the figure the D-470
  worker reported at `548eb2c5` (60 and 12), so the baseline had not moved between the two trees.
- **AFTER** — the same clone with M0-143's `tools/gates.mjs`, `bio-plane/test/gates.test.mjs` and
  `bio-plane/test/gates.control.mjs` committed: **plane 37 · ui 4 = 41**.
- **REACH, stated.** This counts SUITES, not wall clock: it says 31 suites stop being selected for a prose landing,
  not how many seconds that is worth, and nothing here measures the battery. The set is derived from the files'
  text, so a suite reading prose through a path assembled at run time from pieces none of which spells `docs/` was
  invisible to the selector before this change and still is — that is a blind spot the change neither opens nor
  closes. A `docs/` path written inside a REGEX literal is blanked with the comments (the lexer's rule, D-301).

### The 31 that were doc-facing by a comment alone

Plane, 23:

- `adminvote.test.mjs` · `contradiction-overstrict.test.mjs` · `contradictionpairs.test.mjs` · `d125-findingmute.test.mjs`
- `d149-governing-laws.test.mjs` · `d270-refusal-truth.test.mjs` · `d278-codeless-refusals.test.mjs` · `fleetbundles.test.mjs`
- `gateverdict.test.mjs` · `hygiene.test.mjs` · `mk6-bundle-names-no-author.test.mjs` · `observation-content.test.mjs`
- `observation-log.test.mjs` · `observation-meaning.test.mjs` · `occupancy.test.mjs` · `rec165-production-principal.test.mjs`
- `rec168-capturerequest-principal.test.mjs` · `reextract.test.mjs` · `scheduler.test.mjs` · `skillprohibitions.test.mjs`
- `skillsequencing.test.mjs` · `testify.test.mjs` · `testimonyaxis.test.mjs`

UI, 8:

- `agent-surfaced-inquiry.test.mjs` · `group-surface.test.mjs` · `preauth-vocabulary.test.mjs` · `project-id-surface.test.mjs`
- `question-npc.test.mjs` · `recipe-drive.test.mjs` · `refusal-translation-surface.test.mjs` · `review-copy.test.mjs`

### Two of them checked at the code, because a count is not a judgement

- `adminvote.test.mjs`'s ONLY spelling of `docs/` is its header line 7, ` * THE DESIGN IS docs/architecture/…` — a
  citation of its design document. It opens no file under `docs/`.
- `occupancy.test.mjs` names `tools/occupancy.mjs` in CODE (it imports it at line 57 and spawns it at line 60), so it
  left the set through the SECOND site, `toolReachesDocs`: `tools/occupancy.mjs`'s own two `docs/` spellings are both
  in its header comment, and the tool contains **no `readFileSync` and no file path at all** — it is a pure judge over
  values its caller hands it. Read whole it looked like a prose reader; read as code it is not one, and it never was.

### What moved with it

- `bio-plane/test/statepaths.test.mjs` `UNITS_CEILING` **74 → 47**, from that suite's own printed line on a clone of
  this tree: `class TARGETED · 47 unit(s) of 416 selected · 33 MEASUREMENTS reader(s) · 4 through tools/coord.mjs`.
  A narrowing, not slack, and left at the printed figure. `THROUGH_COORD_CEILING` (5, printed 4) and `UNITS_FLOOR`
  (300, printed 416) were not invalidated and were not touched.
