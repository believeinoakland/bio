## M-156 · 2026-09-25 · D-547 — NO BUNDLE IN EITHER REGISTER HAS EVER BEEN RETYPED BY A REVISION: **0 of 31 in `bio` (110 `bundle.md` versions read), 0 of 17 in `scratch` (19 versions read)**, every version's `object_type` parsed and none blob-held. So D-547's fence has nothing already retyped to REPORT, and nothing was rewritten. (WORKER D-547, SCHEDULER #21)

**What was asked.** D-547's scope: *FIRST read the corpus (bio and scratch registers) for revisions that already retyped a
bundle and state the count*, before refusing a revision whose derived type differs from the head's. A retyped bundle is
REPORTED, never rewritten.

**Instrument.** `tools/m156-retype-census.mjs`, node, cloud container, worktree `/home/user/bio` on `land/worker/D-547`,
against the live instance named by `BIO_INSTANCE`, plane `/version` answering `0.79.0`, 2026-09-25 ~00:50Z. READ ops only,
each call NAMING its store (`store=scratch`, `store=bio`): `op=list` (cursor-paged; its `total` equals the listed count,
so the viewer gate hid nothing), then `op=image` per bundle, which returns the head `bundle.md` AND every
`_history/bundle_<snap>.md` the record holds. Each version's front-matter `object_type` is parsed and normalised
(`problem`/`focus` → `inquiry`, the catalogue's `LEGACY_TYPE_ALIASES`); a bundle whose versions carry more than one type
is a retype. The head row's `object_type` is also compared with the head document's.

**WITNESS.** `op=stats` read before and after each register's walk: identical in both (bio 31 bundles, 244 history rows;
scratch 17 bundles, 2 history rows). Nothing was written.

| register | bundles | `bundle.md` versions read | blob-held (unreadable here) | stating no type | retyped | row type ≠ head document |
| --- | --- | --- | --- | --- | --- | --- |
| bio | 31 | 110 | 0 | 0 | **0** | 0 |
| scratch | 17 | 19 | 0 | 0 | **0** | 0 |

**What the matcher can and cannot see.** It sees every `bundle.md` version the plane's `image` read returns as inline
text; a blob-held version would be counted apart and NOT classified (there were none). It cannot see a retype that
happened and was then followed by a purge of history, nor any bundle `op=list` does not return to an admin viewer (the
`total` agreeing with the count listed is the evidence that there was none). Its first run read the heads alone — its
history pattern did not match the `_history/` prefix, so it reported 17 versions for 17 scratch bundles — and was
corrected before the figures above were taken; a zero from that run would have been a zero over the wrong corpus.
