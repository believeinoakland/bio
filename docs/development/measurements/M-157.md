## M-157 · 2026-09-25 · D-504: the six identifier-space measurements M-132 left open (Framework §8.3, CONSTRUCTS Step 5a)

**What this measures.** The six items D-453's measurement (M-132) named and did not close, each recorded
separately: **(1)** APN ↔ permit in Accela, the other half of M-132 scope (c); **(2)** whether Oakland's
`c3xp-qcgn` copies the county's roll, and whether any HISTORICAL roll is published (33 of 102 Legistar APNs were
absent); **(3)** the `100xxxx` project-number join, sought in the CIP line-item tables; **(4)** what
`data.acgov.org` is; **(5)** M-119's recorded tool sha256 `322fcb95…` against main's `b204fc1e…`; **(6)** the
scanned `0201-cafr-2002`, unread. **Nothing was built.** Construct `6.identifier-spaces` stays ABSENT. Every host
call was a GET, or a search form's own POST (Accela); nothing was written to any source or to the plane.

**The one-paragraph answer.** All six are measured, and two of them correct M-132. **(2) corrects M-132's
central APN premise: `c3xp-qcgn` is not the current roll.** It is a partial snapshot of the county
assessor's parcel layer as of the **2012–13 roll**, so M-132's 33 "retired" Legistar APNs were missing from a
2012 snapshot, not from today's roll. The county itself publishes the history (4): `data.acgov.org` is the
**Alameda County Open Data Hub** (ArcGIS Hub), with parent→child **parcel lineage for every roll year from 2005–06
to 2026–27** and **secured tax rolls for 2019–20 to 2025–26**. Against the county's record, **27 of the 33 have a
published retirement** (roll year and child parcels), **1 is in the county's CURRENT layer**, so it was never
retired, and **5 have no trace in any published vintage**. **(1) Accela holds the APN, retired ones
included:** 63 of Legistar's 102 APNs return permit records, including **35 of the 50 the county has
retired**. The 5 with no trace anywhere return none. **(3) The CIP tables join where M-132's matcher found nothing:** 24 new-form project
numbers are shared between the CIP publications and Legistar titles, every referent agreeing when read by hand,
and no crosswalk line exists. **(5) is resolved from git:** `322fcb95…` is `tools/m117-idspace.py` before the
renumbering. **(6) was read by an OCR outside the record: it cites no C.M.S. number.** Reading it also found
**D-585**: the record's own scan detection misses 161 of the file's 175 scanned pages.

### Egress, at the hour

`curl -sS -o /dev/null -A "BIO-CivicOS-measurement/1.0 (civic records research)" --max-time 25 -w '%{http_code}'`,
2026-09-25 01:07:12Z. The proxy status reported `enabled: true`, `selective: false`, no recent relay failures.

| host | status | reached? |
| --- | --- | --- |
| `webapi.legistar.com` | 200 | yes — 32,975 matters |
| `data.oaklandca.gov` | 200 | yes — `c3xp-qcgn` and its metadata |
| `www.oaklandca.gov` | **403** | **REFUSED, 01:07:12Z, GET `/`** — unchanged from M-132, which established it is the origin's Akamai edge, not the proxy |
| `aca-prod.accela.com` | 200 | yes — driven, see (1) |
| `data.acgov.org` | 200 | yes — identified, see (4) |
| `services5.arcgis.com` (org `ROBnTHSNjoZ2Wm1P`) | 200 | yes — 24 county tables, see (2) |
| `cao-94612.s3.us-west-2.amazonaws.com` | 200 | yes — 43,283 keys listed (the count M-132 recorded), 7 PDFs |

### (5) M-119's recorded hash — RESOLVED, from git

M-119 records its pristine copy of the tool as **12,868 bytes, `322fcb95…`**. Main's `tools/m119-idspace.py` at
`6ff3cf563` is **12,868 bytes, `b204fc1e…`**. **Both are real files, and git holds both.**

- D-74's branch commit **`bbe44d8e0`** carries the file as **`tools/m117-idspace.py`: 12,868 bytes, sha256
  `322fcb953ae5e1414460c7bf4a1fd449dd4b05623c9eef97bc9ac48919adba6c`.** That is M-119's figure exactly.
- The integration merge **`6ff3cf563`** ("M-117 renumbered M-119") renamed it and rewrote the id in **six
  places** (the docstring title, the measurement-file pointer, three usage lines, one correction comment).
  `diff` shows **nothing else changed.** "M-117" → "M-119" is a same-length substitution, which is why the size
  agrees and the hash does not.

**So M-119 measured `322fcb95…`, the file under its pre-renumbering name, and its controls restored to that
file.** Main's `b204fc1e…` is the same instrument with its self-references renumbered, and no pattern or
arm differs. M-132 said it "did not establish" this. It is now established, and it happens to confirm M-132's
guess. The file on main today is `d685de15…` (18,483 bytes): M-132's four corrections, recorded there.

### (4) `data.acgov.org` — IDENTIFIED

Its root page (01:07:34Z, 200, 38,482 bytes) is titled **"Alameda County Open Data Hub"**, an **ArcGIS Hub site
(Esri's product; that it is Hub is read from the page, not claimed by us)**. Its search API
(`/api/search/v1/collections/all/items`) answers and points at one ArcGIS Online organisation,
**`services5.arcgis.com/ROBnTHSNjoZ2Wm1P`**, whose services directory lists **246 services**. M-132's reading
stands: the host is not a Socrata domain, which is why Socrata's catalogue said "Domain not found". **The
Assessor's own publications there**, enumerated from the directory rather than from search:

| series | roll years | rows (server count) |
| --- | --- | --- |
| `Parcels` (current parcel layer; the service names its layer "Parcel Boundaries 2000"; last data edit 2026-09-15) | current | 489,636 |
| `Parcels_Inactivated_in_Roll_Year_*` (parent APN → child APN, with dates) | 2005–06 … 2017–18, 2025–26, 2026–27 (15 tables) | 3,270 – 50,065 each |
| `Assessor_Office_Deleted_Parcel_List_*` (the same lineage, a later series) | 2018–19 … 2025–26 (8 tables) | 1,395 – 11,030 each |
| `Assessor_Office_Secured_Tax_Roll_*` (the full secured roll, one table per year) | 2019–20 … 2025–26 (7 tables) | 463,890 → 472,690 |
| also: `Assessor_Office_Ownership_Transfer_List`, `Assessor_Office_Use_Codes` | — | not read |

The two lineage series together cover **every roll year from 2005–06 to 2026–27**. The 2025–26 year appears in
both series with an identical row count (3,270), recorded as a likely duplicate and not established as one. **No
full roll before 2019–20 is published there, and no lineage before 2005–06.**

### (2) What `c3xp-qcgn` is, and the historical roll

**Instrument.** `tools/m157-fetch-county-parcels.py` fetched the current layer's `APN` column and all 23 lineage
tables whole (01:10:08Z–01:20:25Z, 1.0s between pages). **Every table's fetched count equals its server count**,
and the tool prints SHORT where one does not: 0 SHORT. `tools/m157-apn-vintage.py` keys every source on its
printed APN, parsed into book, page, parcel and sub, with each numeric part read as an integer. The sources spell
the sort key three ways (`085A380000118`, `37A 278901101`, and 11- or 12-character county rows), so no sort
column is trusted. Unparsed: c3xp 4, county current 25, lineage 0. **The county names its lineage fields four
ways** (`PARENT_APN`, `Parent_APN`, `PARENT_PARCEL`, `Parent_Parcel_Number`). The first run read only one of them,
dropped 8,821 rows, and roll years 2009–10, 2020–21 and 2024–25 **vanished from every tally with no error.**
Corrected with aliases, and an arm was added (see Negative control).

**`c3xp-qcgn`'s own metadata** (Socrata `/api/views/c3xp-qcgn.json`, 01:08Z): created and `rowsUpdatedAt`
**2013-02-01**; the newest `date_updat` in its rows is **2012-04-25 12:22:23**; attribution and description
empty (as M-132 found); **178 distinct books, 1 to 99B**. That is the whole county, not Oakland alone: Oakland's
books 1–48 hold 77,036 of its 394,597 rows. Its columns `apn, apn_sort, book, page, parcel, sub_parcel,
clca_categ` are the county layer's `APN, APN_SORT, BOOK, PAGE, PARCEL, SUB_PARCEL, CLCA_CATEGORY`. `clca_categ` is
cut to ten characters, a shapefile's limit. The rows are byte-identical to M-132's: sha256 `944cbbcb…`,
394,597 rows.

**The copy test.** c3xp ∩ county current = **386,687 of c3xp's 392,040 keys (98.6%)**; c3xp only 5,353; county
current only 83,691.

**The VINTAGE, dated by the county's own retirements.** For every parcel the county records as retired, whether
c3xp holds it:

| roll year retired | 2005 | 2006 | 2007 | 2008 | 2009 | 2010 | 2011 | **2012** | 2013 | 2014 | 2015 | 2016–2026 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| retired parents | 1,466 | 1,469 | 1,432 | 1,634 | 1,170 | 1,146 | 1,386 | 1,162 | 1,188 | 725 | 787 | 257 – 1,041 |
| in c3xp | 0.0% | 0.1% | 1.4% | 0.4% | 0.6% | 0.3% | 0.1% | **9.1%** | 45.4% | 55.6% | 59.7% | 22.0% – 54.4% |

**A step between roll years 2011–12 and 2013–14.** A parcel gone by 2011–12 is almost never in c3xp; one still
alive at 2013–14 is about half the time. **So c3xp is the county's parcel layer as of the 2012–13 roll**, which
agrees with its last row edit (2012-04-25) and its publication (2013-02-01).

**It is not a COMPLETE copy of that roll.** About half of the parcels alive then are in it. The county says
12,741 current parcels were created before 2012 and they are absent, and only 23 of their parents are present.
**Why it is partial is UNDETERMINED.** It is a polygon layer (`the_geom`), and a subset such as parcels without
their own polygon would produce this, but that was not measured. **Whether Oakland copied it from the county or
derived it through its own GIS is still UNDETERMINED** from the metadata. What is measured is that its schema,
its keys (98.6%) and its vintage are the county's.

**The historical roll: YES, published by the county from 2019–20; parcel LINEAGE from 2005–06.**

**Legistar's APNs against every vintage.** The same 32,975 matters and M-132's padded pattern, unchanged: **102
distinct keys, 69 in c3xp, 33 absent. That reproduces M-132 exactly.** Only 46 of the 102 are in the county's
CURRENT layer, and 24 of those in c3xp have been retired since 2012. Of the 33 absent from c3xp:

- **27 carry a published retirement**, with roll year and children. M-119's two Walker Avenue easement APNs,
  `011-0836-017-00` and `-018-00` (matter 03-0339), were **retired in roll year 2006–07 and merged into
  `11-836-49`**, which is itself not in the current layer. **M-132's downtown cluster is one event:** all 17 of
  its book-008 page-0643/0644 APNs were retired in roll year 2006–07 into **page 716** (`8-716-1` … `8-716-41`),
  a re-mapping of those blocks after the 2002–2004 redevelopment matters that cite them. The tool follows one
  generation only; `8-716-*` is not current either, and its later lineage was not traced.
- **1 was never retired: `007-0620-001-04`** (matter 07-0045, 2007-03-06) **is in the county's CURRENT layer**
  and absent from c3xp only because c3xp is partial. **Under M-132's reading ("absent from the current roll reads
  RETIRED") this parcel would have been recorded as retired, and it is not.**
- **5 have no trace in any published vintage**: `033-2130-035` (000288b, 2000), `041-3902-013` (03-0118a, 2003),
  `044-4989-006` (03-0223, 2003), `045-5276-022-02` (03-0223-2, 2003), `005-0421-000` (05-0997, 2005). Each is
  UNDETERMINED between "retired before 2005–06, where lineage begins" and "the matter wrote a number that was
  never a parcel". `005-0421-000`'s parcel part `000` is itself suspect. **None is read as either.**

### (1) APN ↔ permit in Accela

**Instrument.** `tools/m157-accela-apn.py` drives Accela Citizen Access (`aca-prod.accela.com/OAKLAND`, the city's
permit system by its own SB 272 catalogue, M-132 (b)) the way a browser does. It GETs the Building module's
record search, carries the ASP.NET form state, fills **Parcel No.**, and posts the page's own Search button.
Anonymous, 2.0s between searches. **Three findings about the instrument, each measured before the join ran:**

- **Accela refused the first POSTs** ("Potential cross-site request forgery attacks. The Referer and Origin
  headers are missing", 01:21Z) and answered with `Error.aspx`. That page reads like "no result" to a careless
  reader. The driver sends the two headers a browser sends, naming the page's own origin.
- **The page defaults its date window to the last ten years** (`09/23/2016`–`09/24/2026`). A search left at the
  default is a search of a decade, which would score every 2003 parcel "no permits". The window is set to
  `01/01/1900` and printed with each result.
- **Accela reads the assessor's SORT spelling.** Measured on 300 Frank H Ogawa Plz: `008 061900401` → **200+
  records**, the same grid its address search returns; the printed `8-619-4-1` → 2; **Legistar's padded
  `008-0619-004-01` → 0**; the unspaced `008061900401` → 1, and that one opens a record detail page. The reader first
  scored that page NONE (a loose "no records" pattern matched it). Corrected: SINGLE is tested first, and NONE
  is only the page's own notice, "Your search returned no results". Control: `999 999999999` → NONE.

**Result** (01:23:30Z–01:30:26Z; all 102 searched from `01/01/1900`; `accela-join.json` in the item's scratch):
**63 of 102 return at least one record** (56 as a grid, 7 as a single record opened directly); 39 return the
page's "no results" notice; **0 UNREAD.** By vintage:

| Legistar APN is … | APNs | with Accela records |
| --- | --- | --- |
| in the county's CURRENT layer | 46 | 27 |
| in the 2012 snapshot, retired since | 24 | 16 |
| in neither (27 retired by 2012, 5 no trace) | 32 | 20 (all among the 27 retired) |
| **retired by the county, any year** | **50** | **35** |

**Accela keeps a parcel number after the assessor retires it.** Permits filed against a parcel stay on the parcel
as it was, so Accela is a second system that holds retired APNs, alongside Legistar. M-119's two Walker Avenue
APNs, retired in roll year 2006–07, return **8 and 10** records.

**The 5 no-trace APNs return nothing in Accela either.** That is consistent with no system having issued them,
but it is still UNDETERMINED. One of them, `033-2130-035` (matter 000288b), may be the sub-parcel-less form of
`033-2130-035-02`, which matter 06-0179 writes in full. That was not tested.

**The APN ↔ permit space is SHARED, and the referent agrees where it can be read.** A crude check found that 47 of
the 56 grid results carry an Accela address whose street name appears in the Legistar title. That is a token
overlap, not a referent reading, so a seeded sample of 6 plus M-119's pair were read by hand:
- 319 Chester St ↔ "The Property At 319 Chester Street … APN 004-0103-035-00";
- 2016 Telegraph Ave ↔ "2000-2016 Telegraph Avenue … 008-0649-010-00";
- 9409 International Blvd ↔ "9409, 9415 … International Boulevard … 044-4967-009";
- 2100 Telegraph Ave ↔ "2100 Telegraph Avenue- APN 008-0648-016-03".
All agree. **Walker Avenue looked like a disagreement and is not.** Legistar puts `-017-00` at 717 and `-018-00`
at 711 Walker, yet both APNs return the same 2003–2005 records at 711 Walker. An address search for **717
Walker returns those same 8 records.** Accela files that project at 711 across both lots, the lots the county
merged into `11-836-49` the next roll year. **One possible mismatch is UNDETERMINED:** `009-0684-005-01`.
Legistar 16-1007 gives "404 26th Street", while Accela's records are on 27th Street. A through-lot would do this,
and that was not measured. Of the 9 grids without a street match, 8 are titles that list APNs with no address,
so there is nothing to compare.

### (3) The `100xxxx` project number, in the CIP tables

**Instrument.** `tools/m157-cip-join.py`. M-119's project pattern catches a `100xxxx` value only after a
"Project No." cue. A CIP table writes the number bare in a column, **so that pattern could not see a table.
That is why M-132's FIN found 9 new-form values in 3,148 pages, and it is a reach limit of the matcher, not a
property of the books.** The FY25-27 proposed CIP was in M-132's corpus too; this matcher finds **47** there. The
new matcher reads the bare seven-digit `100dddd` and refuses a comma, a `$`, an eighth digit, or a letter on
either side. It therefore also sees any bare seven-digit amount beginning 100, so every shared value's
referent is read.

**Corpus** (fetched from `cao-94612` 2026-09-25 01:13Z–01:15Z; pypdf 6.19.0 in a clean venv, because this
container's system `cryptography` panics on import; **0 page failures across 641 pages**):

| tag | key under `documents/` | HTTP | bytes | sha256 | pages | chars | new-form values |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `0302-cip-fy1921-program` | FY-2019-21-Capital-Improvement-Program.pdf | 200 | 20950938 | 9540ecf48f9473d1… | 167 | 254875 | 0 |
| `0302-cip-fy2123-adopted` | FY-21-23-Adopted-CIP-Book-7.28.22.pdf | 200 | 37553676 | e59b43d6c4c684bd… | 215 | 264444 | 0 |
| `0302-cip-fy2325-adopted` | FY23-25-Adopted-CIP_07.09.2024.pdf | 200 | 94575027 | 9f8df6955ee8bd74… | 120 | 204654 | 0 |
| `0302-cip-fy2527-proposed` | FY25-27-Proposed-CIP-Budget_05.05.2025_Reduced.pdf | 200 | 41690842 | a8540bafc751aab6… (= M-132's) | 119 | 204840 | 47 |
| `0302-cip-bydistrict-2021` | CIP-Projects-By-Council-District_21-0513-1.pdf | 200 | 14302196 | 6ec9c61fd8decac5… | 10 | 27085 | 0 |
| `0302-kk-projects-2023` | Attachment-D-List-of-all-Measure-KK-Funded-Facilities-Projects-by-Tranche-and-Phase-as-of-June-30-2023.pdf | 200 | 650455 | 74f47078ed9bdc54… | 10 | 13454 | 73 |

**Four of the six carry NO project number, and that is a property of the books.** The FY19-21, FY21-23 and
FY23-25 CIP books list their tables by a row number internal to the book (e.g. `294 Estuary Park (Ex) Parks &
Open Spaces …`), and those tables are in the text layer, so the zero is not an extraction gap. The FY25-27 book's
maps list `Project Title | Project Number | Location` (`16 Parks & Open Spaces Estuary Park 1000858 5
Embarcadero`). The Measure KK list's first column is `Project #`.

**Result: 108 distinct CIP values, 107 in Legistar titles (bare), SHARED 24.** A "Project No."-cued count on
Legistar's side gives 117 values and 14 shared. That cue is close to M-119's pattern but not identical and is not
offered as a reproduction. **Every one of the 24 was read by hand, CIP row against Legistar title, and every
referent AGREES.** Examples:
`1000858` Estuary Park ↔ 16-1286 "Estuary Park Renovation And Expansion Project (#1000858)"; `1003434` "Dimond
Park – Lion Pool Repl." ↔ 18-0690 "Lions Pool Renovation (Project No. 1003434)"; `1003443` "Head Start – Arroyo
Viejo Remodel" ↔ 18-1625 "Head Start Facility Renovation At Arroyo Viejo Recreation Center Project (No.
1003443)"; `1004851` "East Oakland Arts Center (formerly Daca Phase 2 Reno)" ↔ 22-0909 "Digital Arts And Culinary
Academy Improvements Project (Number 1004851)" and 26-0699 "East Oakland Arts Center Project (Project No.
1004851)". **That last one is the same identifier across a RENAME**, the case a name-matcher would split in two.
**One further pair is NOT counted:** Legistar 18-2447 writes `1003439A` (Fire Station #12 Remodel) against the KK
list's `1003439` (Fire Stations Citywide Renovations). The referent is consistent, a sub-project of the
programme, but **a suffixed value is a different string.** The matcher's first draft joined them and was
corrected (see Negative control).

**The KK list's extraction was misread once, and that is recorded.** pypdf emits each project number on its own
line BEFORE its row. The first referent pass read the row above each number, which paired `1003443` with "Lion
Pool". The layout was then read, and the pass re-run on the row that follows each number.

**No crosswalk:** **0** CIP lines carry both a C/P-form and a new-form value. **(d)'s "one space, no shared
value" becomes one space, 24 shared values, referents agreeing across unrelated subjects** (parks, Head Start,
fire, libraries, animal services), **in two forms with no published bridge.**

### (6) `0201-cafr-2002` — READ, by an OCR outside the record, and it cites no C.M.S. number

**The file** re-fetched 01:13:59Z: 200, 12,477,689 bytes, sha256 `3c358ed7fd931349…`, identical to M-132's.

**First, what the RECORD would do with it, measured at the code on this tree with the plane's and the fleet's own
modules:**

- **Tier 1** (`bio-plane/src/pdfstructure.mjs`, `extractPdfStructure`): **0 characters on all 175 pages**, and a
  `no_text_layer` marker on **14 of them** (0-based 25–38).
- `needsTier2` returns false, because every marker is a scan marker. **Tier 2 never runs.**
- `needsTier3` → the 14 marked pages go to the OCR member. The member's renderer
  (`pdf-worker/src/pagepixels.mjs`, `renderPageToPixels`) renders **exactly those 14** (12 `decoded-ccitt-g4`,
  2 `passthrough-dct`) and refuses the other **161 with `PAGE_HAS_TEXT_LAYER`**.
- **Every page is a scan.** Each draws one 2480×~3300 1-bit CCITT image (or a JPEG). The 161 pages each carry
  an EMPTY `BT … ET` pair next to the image and inherit a 12-font resource dictionary. **None shows a glyph.**
  Frames are all under the member's measured bound (0 over `MAX_FRAME_BYTES`).

**D-585, minted.** A scanned page with an inherited font dictionary and an empty `BT … ET` is read as having a
text layer, **twice and independently:**
- Tier 1's image-only marker requires "no font declared", so it does not mark the page, and the record reads it
  as 0 characters of TEXT, not as UNREAD.
- The renderer's `hasTextOps` counts a bare `BT` (`SHOW_TEXT_BLOCK`) as text, so even a marked page of this shape
  would be refused before OCR.

**The named fix:** both predicates test whether a text-SHOWING operator (`Tj`/`TJ`/`'`/`"`, the renderer's own
`TEXT_OPS`) occurs, not whether a font is declared or a text block is opened. Ideally that is one shared
predicate, so the two cannot disagree again. **This is the "record claims more than it supports" class
(`CLAUDE.md` §2):** 161 pages would read as read-and-empty. Sent to SCHEDULER for placement.

**Then the read.** `tools/m157-ocr-cafr2002.mjs`: pixels from the **project's own renderer**
(`allowTextPage: true`, an option the renderer offers) and text from the **local `tesseract` 5.3.4** (eng,
default page segmentation; installed from the distribution for this measurement). **That is not the OCR member's
tesseract-wasm, so none of the member's calibration or fidelity letter applies to this text.** It is a machine
transcription nobody has checked. **175 of 175 pages transcribed** (173 CCITT, 2 DCT), **341,289 characters**,
0 refused, 01:16:21Z–01:22Z; `ocr.txt` sha256 `5748e8b9…`. The text is legible: the table of contents, the
transmittal letter and the notes read as prose.

**What it cites:** **0** occurrences of `C.M.S`/`CMS`. It names ordinances and resolutions **by title only**
("Zone and Linked Banking Ordinances, and the Tobacco Divestiture Resolution", "the Nuclear Free Ordinance"),
**3 and 3 mentions, none numbered.** Over this text beside Legistar, `tools/m119-idspace.py` (unchanged,
`d685de15…`) counts FIN **0** resolution/ordinance, **0** project, **0** contract, **0** APN, **3** fund values,
**0 shared**. **So M-132's "absence of a 2002 C.M.S. citation is *nothing was extracted*" becomes, in this OCR
reading, *nothing was cited*.** The residual is an OCR miss of a marker. `Ordinance` and `Resolution` were
transcribed where they occur and no numbered form appears near them, but the text was not proof-read, so this is
stated as the OCR reading's result and not as the document's certified content.

### Negative control

**Declared before running.** MUST count exactly one: the row's planted join. MUST fail when broken: each new
instrument's named arm. MUST NOT count: a value on one side only, a suffixed value, a comma amount, a planted
non-existent parcel.

- **The row's control, `tools/m132-negative-control.py`** (sha256 `d837d988…`) into a fresh corpus, then
  `tools/m119-idspace.py` (`d685de15…`): **`resord SHARED 1`** (90001; LEG 2 distinct, FIN 2 distinct), and
  contract, project, APN and fund all SHARED 0. **Declared one, counted one. GREEN.** `--selftest` GREEN 17/17.
  M-132's recorded vacuous arm (`090001`) is still vacuous, for the reason M-132 gives.
- **`tools/m157-cip-join.py --selftest` GREEN 8/8.** **Arm:** the lookahead restored to the first draft's
  `(?![\d,])` → **RED 6/8**, failing at `suffixed 1009907A NOT joined to 1009907` and at `planted 1009901 SHARED
  (bare)`, because the suffixed plant leaks into the shared set. Restored from a per-arm pristine copy, verified
  by `cmp` and sha256 `281beee2…` (8,866 bytes); GREEN 8/8. The file was then relabelled (`3a5cbd48…`, one
  output string) and re-run GREEN 8/8.
- **`tools/m157-apn-vintage.py --selftest` GREEN 10/10. AN ARM THAT DID NOT ARM, AND IT IS A FINDING ABOUT MY
  ARM.** Narrowing `PARENT` to `("parent_apn",)` first came back **GREEN**, because the alias arm iterated
  `PARENT` itself and narrowed with it. The arm was rewritten against the three spellings measured in the county
  tables. **Re-armed: RED 9/10, failing at `each measured county spelling is read`.** Restored and verified by
  `cmp` and sha256 `1b8c09c4…` (10,198 bytes); GREEN 10/10. The full analysis re-run on the final file is
  byte-identical to the run before the arm's rewrite (`cmp`).
- **Accela:** the planted non-existent parcel `999 999999999` → NONE. The over-strictness arm, a known parcel in
  the sort spelling → GRID 200+, equal to the address search. The reader's SINGLE defect was caught by the
  unspaced probe and corrected before the join.
- **The corpora are non-empty and floored:** every instrument asserts a floor (`len(matters) > 1000`, `snap >
  100000`, `lineage > 1000`, `total > 100`) and printed its counts above.

### What is owed, and to whom

- **D-585** (above) → SCHEDULER, for placement in build order.
- **To BOB: Framework §8.3 rule 3's APN clause rests on M-132's reading of `c3xp-qcgn` as the CURRENT roll.** It
  says "a parcel absent from the CURRENT roll reads RETIRED … 33 of 102 Legistar APNs are retired", and "an APN
  pair is cross-institution only once the county's own roll is captured". Measured here: the roll read was a
  partial 2012–13 snapshot. Of the 33, 27 are retired by the county's record, 1 is current, and 5 are
  undetermined. The county's own roll and lineage are now read. **The rule's direction survives: absence is
  never "no such parcel".** Its inference, absent → retired, is false for at least one of the 33, and the rule
  needs the county's lineage as the instrument that says which. That is a ruling, so it is BOB's to amend.
- **M-132 carries a pointer to this entry** at its two superseded statements (the "current roll" reading and
  the unresolved hash).
- **Not measured, stated:** Accela's other modules (Planning, Enforcement, Fire) beyond what the Building search
  returns; the lineage past one generation; why `c3xp-qcgn` is partial; the county's Ownership Transfer List and
  Use Codes; the one Legistar matter fewer than M-132's harvest (32,975 vs 32,976, sha256 `6bdee75d…` vs
  `5d248f78…`; which matter is not established).
