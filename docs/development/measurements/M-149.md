## M-149 · 2026-09-24 · D-525 — THE REAL RECORD HOLDS **NO** DRIVE-LINKED BUNDLE, SO **0** SHELL BASELINES: 31 bundles in `bio` on biosmoke7, 29 with a `source.locator`, across 10 hosts, **none of them a Google Drive host**. The sweep's remedy is therefore proven only in the suite's scratch namespace. (WORKER D-525, SCHEDULER #20)

**Instrument.** A READ-ONLY client of the deployed plane, run from the cloud container on `land/worker/D-525` over
`origin/main` @ `9f8b69e6`, member credential, through the proxy's instance reach. It applies
`bio-plane/src/drive.mjs`'s own `readDriveAddress` and `classifyDriveBaseline` (the functions `op=driveshells`
answers with) to what `op=projection` (every bundle's `source.locator`, paged) and `op=image` (each Drive bundle's
`data/provenance.json`) return. **It could not run `op=driveshells` itself**: the deployed build is **0.79.0** and
answered **400** for that op, because D-525 is not deployed (DIST's act, not this item's). It could not read the plane's
retrieval record (`captured_locators`) either, since no deployed op exposes it, so a Drive bundle, had one been found,
would have been judged on its register alone and said so.

**Witness.** `op=index` was read before and after the sweep, in both namespaces: `bio` 31 bundles, the id:sha
concatenation 3,250 characters, identical before and after; `scratch` 17 bundles, 1,632 characters, identical. The sweep
wrote nothing and re-acquired nothing.

| namespace | bundles | with `source.locator` | on a Drive host | shell | export | undetermined | no baseline |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `bio` | 31 | 29 | **0** | **0** | 0 | 0 | 0 |
| `scratch` | 17 | 1 | 0 | 0 | 0 | 0 | 0 |

**The hosts in `bio`, printed, because a search for absence has to show what it looked at:** `oakland.legistar1.com` 6,
`www.oaklandca.gov` 6, `webapi.legistar.com` 5, `oaklandca.opengov.com` 3, `cao-94612.s3.us-west-2.amazonaws.com` 2,
an unparsable locator 2, and `oakland.legistar.com`, `www.oaklandauditor.com`, `data.oaklandca.gov`,
`scocal.stanford.edu`, `timeapi.io` 1 each. **Matcher liveness**: the same run's `readDriveAddress` read a Drive Doc
address as `document`, so "0 on a Drive host" is the matcher seeing locators, not a matcher that sees nothing.

**What this does and does not say.** It says that on 2026-09-24, by this member credential's view (an instance-level
credential, unfiltered by D-15's machine carve-out), no bundle in either namespace carries a Drive link, so D-472's F3
defect has **no instance on this record today**. It says nothing about another group's instance, and nothing about a
bundle promoted after the read. On an instance serving D-525, `op=driveshells` answers the same question with the retrieval
record as its first fact.
