## M-149 · 2026-09-24 · D-525 — THE REAL RECORD HOLDS **NO** DRIVE-LINKED BUNDLE, SO **0** SHELL BASELINES: 31 bundles in `bio` on biosmoke7, 29 with a `source.locator`, across 10 hosts, **none of them a Google Drive host**. The sweep's remedy is therefore proven only in the suite's scratch namespace. (WORKER D-525, SCHEDULER #20)

**Instrument.** A READ-ONLY client of the deployed plane, run from the cloud container on `land/worker/D-525` over
`origin/main` @ `9f8b69e6`, member credential, through the proxy's instance reach. It applies
`bio-plane/src/drive.mjs`'s own `readDriveAddress` and `classifyDriveBaseline` (the functions `op=driveshells`
answers with) to what `op=projection` (every bundle's `source.locator`, paged) and `op=image` (each Drive bundle's
`data/provenance.json`) return. **It could not run `op=driveshells` itself**: the deployed build is **0.79.0** and
answered **400** for that op, because D-525 is not deployed (DIST's act, not this item's). It could not read the plane's
retrieval record (`captured_locators`) either, since no deployed op exposes it, so a Drive bundle, had one been found,
would have been judged on its register alone and said so.

**Witness.** `op=index` was read before and after the sweep, in both namespaces: `bio` 31 bundles, the id:sha
concatenation 3,250 characters, identical before and after; `scratch` 17 bundles, 1,632 characters, identical. The sweep
wrote nothing and re-acquired nothing.

| namespace | bundles | with `source.locator` | on a Drive host | shell | export | undetermined | no baseline |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `bio` | 31 | 29 | **0** | **0** | 0 | 0 | 0 |
| `scratch` | 17 | 1 | 0 | 0 | 0 | 0 | 0 |

**The hosts in `bio`, printed, because a search for absence has to show what it looked at:** `oakland.legistar1.com` 6,
`www.oaklandca.gov` 6, `webapi.legistar.com` 5, `oaklandca.opengov.com` 3, `cao-94612.s3.us-west-2.amazonaws.com` 2,
an unparsable locator 2, and `oakland.legistar.com`, `www.oaklandauditor.com`, `data.oaklandca.gov`,
`scocal.stanford.edu`, `timeapi.io` 1 each. **Matcher liveness**: the same run's `readDriveAddress` read a Drive Doc
address as `document`, so "0 on a Drive host" is the matcher seeing locators, not a matcher that sees nothing.

**What this does and does not say.** It says that on 2026-09-24, by this member credential's view (an instance-level
credential, unfiltered by D-15's machine carve-out), no bundle in either namespace carries a Drive link, so D-472's F3
defect has **no instance on this record today**. It says nothing about another group's instance, and nothing about a
bundle promoted after the read. On an instance serving D-525, `op=driveshells` answers the same question with the retrieval
record as its first fact.

---

## THE REPORT — WORKER D-525 (for CONDUCT and SCHEDULER; the branch is the report, SCHEDULER #21 00:44Z)

**Branch** `land/worker/D-525` over `origin/main` @ `9f8b69e6`. **Tip and gate line**: the commit that carries this
section; the code tree it reports on is `321164cc` (tree `775ff27b`), gated GREEN: **`gates: GREEN · class FULLREUSE` — 79/79 suites green · 6350 assertions passing** re-run, with 347 units REUSED (each a PASS keyed on identical inputs) and plancheck 0 fail. The two gates before it were RED, and each red is adjudicated under F3. This section is a docs-only change, gated again on the tree that carries it; that line is in the commit message.

**What landed.** `op=driveshells` (READ; admin/member/probe; op=index's D-15 stamp; PAGED — `limit`, `after`,
`truncated` meaning more exist, `cursor`). `drive.mjs` `driveBaselineRow` + `classifyDriveBaseline` (shell | export |
undetermined | no_baseline); `Store#driveShells`. Each shell names its remedy: `op=acquire` on the document address
(CAP-8 fetches the export and files a NEW capture beside the shell's), then append the answer's `document` to
`data/provenance.json`. Claim `10.drive-shells` BUILT; census 219 → 220 ops reviewed by meaning; framework §6 note.

**Accepts-when, met in the suite (`test/d525-driveshells.test.mjs`, 42/0):** the sweep names exactly the two shell
baselines among seven shapes; a re-acquired one reads `unchanged`,`unchanged` on the evidentiary comparison with the
old row, bytes and retrieval kept; op=monitor's baseline equals the sweep's for every judged bundle.

**NEGATIVE CONTROL** (`test/d525-driveshells.control.mjs`, re-run after the subject changed): 7 of 7 as declared.
The row's own arm (re-acquire skipped) reads **["modified","modified"]** on the raw comparison. Figures are on the
suite's NEGATIVE CONTROL line. Also: independence C1's corrected matcher still catches a planted second origin walk
(13/1); provenance-marker's ceiling at 39 fails 112/1.

**The real record** (above): 0 Drive-linked bundles in `bio` on biosmoke7, so 0 shell baselines; read-only,
witnessed unchanged. **The re-acquire was NOT run live**: the deployed build (0.79.0) has no op=driveshells, the real
record has no shell to mirror, and this session holds no public Drive document address. It is proven in the suite's
scratch namespace only.

### Findings, each with its named fix

- **F1 — docprofile's handler key cannot tell a Drive shell from a Drive document.** An OpenDocument export
  profiles as `conservative`, exactly as Google's shell does, because every registered stack is an HTML stack and
  `conservative` is the fallthrough. So a register row says "an HTML stack handled this" of an `.ods`. FIX NAMED:
  the plane records the FORMAT axis beside the stack (it already does, `profile.format`); D-525 discriminates on
  the plane's retrieval record and the format, and REPORTS the handler. No mint: the record states the format
  correctly and nothing reads the handler as a format. If a reader ever should, the fix is a non-HTML stack in
  docprofile for a container (CONTENT-OFFICE's), which is design → BOB.
- **F2 — overlap with D-524 in the register lookup (the SEAM the brief named).** op=monitor's baseline-row rule
  (export row first, else the bundle-locator row) is RESTATED in `drive.mjs` `driveBaselineRow`, deliberately not
  shared, because D-524 changes that lookup now ("prefer the row whose hop names the bundle's locator"). FIX NAMED,
  for the integrator: when D-524 lands, either (a) make op=monitor call `driveBaselineRow` and move D-524's hop
  preference into it, or (b) mirror D-524's rule in `driveBaselineRow`. The suite's PIN arms ("op=monitor's baseline
  IS the sweep's", before and after the remedy) fail by name if the two drift; control arm (e) proves it.
- **F3 — the first gate found the sweep unbounded, correctly.** Fixed at the cause (LIMIT cap+1, a keyset cursor,
  bounded per-sha reads); derivation-bounds and meaning-bounds went green with their ceilings UNMOVED. bounds' roster
  45 → 46 and provenance-marker's swallowed-read ceiling 39 → 40 each moved from the printed figure with the site
  examined. independence C1's matcher was corrected, not exempted: it counted every sha-keyed read of
  `captured_locators` as an origin walk.
- **F4 — an instrument defect in this item's own suite**, found by control arm (d): a pin read `export[0]` by
  position. Corrected to read by bundle id.
- **F5 — CREDENTIAL EXPOSURE, this session's own act, reported so it is not silent.** A live-probe script built its
  URL from `BIO_INSTANCE` as though it were a URL (it is a bare name), and Node's error trace printed the request URL
  **with `BIO_MEMBER_TOKEN`'s value** into this session's tool output. It went nowhere else: not the repository, not
  a commit, not a message. FIX NAMED: rotation of the member token is the operator's call (→ BOB); the script was
  corrected so that no error echoes a URL.

### DESIGN GAP

`BIO_Content_Framework_v0_10.md` §6: the remedy is a MEMBER act (acquire, then a promote that appends the register
row), because the register is caller-built (C-18.1). Nothing in the design says whether the plane may append a
register row on a re-acquire it performs. Until it does, the sweep lists and a member remedies.

### For CONDUCT

- Merge `land/worker/D-525`. I3/I5 is additive: `op=driveshells` is a new read. The integrator classifies it.
- Resolve F2's seam with D-524 at the union and re-run `test/d525-driveshells.test.mjs`.
- DIST: nothing is owed until a release; the live proof of the sweep needs a deployed build.

### What was NOT done

A live `op=driveshells` call or live re-acquire, for the reasons above. No trigger reports: SCHEDULER #21 said
this depth cannot create them, so the branch is the report. No design question was sent to BOB. F5's rotation is
routed here for CONDUCT to carry.
