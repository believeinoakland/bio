## M-180 · 2026-09-25 · D-635: A ROUTED PAGE WHOSE FOLIO DECODED KEEPS IT, GAINS THE TRANSCRIPTION, AND IS LISTED IN BOTH DERIVATION PARTS. THE READERS THAT ASSUMED A PARTITION, AND THE CONTROLS

**Headline.** BOB #35 ruled APPEND at 06:25Z. `mergeTier3Text` now appends the transcription to a routed page that
holds a glyph, where it used to refuse the page. The page is listed in the layer part and the engine's part. Every
reader that assumed the parts partition pages was found, named and driven. Five control arms ran as declared.

**Instrument.** `bio-plane/test/d635-append.test.mjs` (35 assertions), through the real `op=acquire`, `op=promote`
and `op=textattest`. The OCR and pdf.js members are stubs. The driver is `bio-plane/test/nc-d635.mjs`. The sources
are D-635's branch, stacked on `land/worker/D-627` @ `056d3092`. Taken on 2026-09-25 on a quiet worktree.

**The corpus is synthetic, and that is stated.** Three built PDFs: MIXED (a text page, an image page whose folio
`19` decodes, and a fontless scan), FOLIO (one image page with a decoded folio, so both parts name one page list),
and PARTITION (the D-252 shape, a text page and a scan). They are 1,398 B, 908 B and 1,123 B. **The 8 real pages
D-627 measured (ACFR pp 19, 21, 22; adopted budget pp 11, 38, 170, 881, 882) were NOT re-driven.** Their folios
decode, so they take the append path this suite drives. That is an inference from M-178's figures, not a run.

### The readers that assumed the parts partition pages

Found by reading every site that reads a step's `extent`, `filled`, `reextracted.pages` or a per-page chain answer
in `bio-plane/src`, `docprofile/`, `civicos-ui/`, `pdf-worker/src`, `ocr-worker/src`, `tools/` and
`bio-plane/checks`.

| reader | assumed a partition? | what D-635 did | driven by |
| --- | --- | --- | --- |
| `tier3Extend`'s layer part (`index.mjs`) | yes: it left out every filled page | an appended page is in it | BOTH PARTS |
| `mergeTier3Text` (`index.mjs`) | yes: it refused a page holding a glyph | appends; drops the routing marker only | KEEP + APPEND, FILL |
| `tier3Note` (`index.mjs`) | yes: "the pages that already had text kept it" | adds a sentence for an appended page | the basis assertion |
| `mergedChain` (`textchain.mjs`) | yes: a part was named by its page list | stamps `extent.part` only when parts overlap | BOTH PARTS, SAME PAGES, PARTITION |
| `derivationCap(chain, {page})` | yes: every covering step was treated as one sequence | two parts on a page: an unmeasured part makes it UNDETERMINED | PAGE CAP |
| `derivationCap(chain)` | yes: parts keyed by page list | keyed by `part` when present | SAME PAGES |
| `extentOf` | no | an unreadable `part` makes the extent unreadable | the rules |
| `readingProvenance` (`readingprov.mjs`) | yes: one tier per page, pixels wins | a page names both producers (`producers`, only when two), and is credited to both | PROVENANCE |
| `compareProvenance` | yes: one producer per page | attributes a change to both | PROVENANCE |
| `tiersEvidenced` (`textchain.mjs`) | no: unions pages per tier | unchanged | TIERS |
| `contentObservationsFor` (`airun.mjs`) | no: unions coverage | unchanged | TIERS |
| `describeChain` | no: each step names its own pages | unchanged | BOTH PARTS (sentence) |
| `#pageSetForCapture` (`store.mjs`) | no: reads the highest page named | unchanged, proven by reading | none; no behaviour depends on the overlap |
| `mergeTier2Text`'s parts (`index.mjs` tier-2 wire) | yes, and correctly: kept and replaced partition by construction | unchanged; D-635 does not reach it | the existing tier-2 suites |
| `capture_text.chain_kind`, `content.chain_kind` | not a partition reader: a DOCUMENT value on a per-unit question | **not fixed: D-686** | none |
| the UI (`civicos-ui/app.html`) | reads no chain extent and no per-page provenance | nothing to change | none |

**Consumer impact on the shape.** Two optional fields. `extent.part` on a derivation step, written only when parts
overlap. `producers` on a provenance page, written only when a page has two producers. No UI, docprofile or
worker code reads either shape. A chain whose parts partition is byte-identical to before (PARTITION pins it). The
row names interface I6 and leaves the IC's classification to the integrator.

### The suite

`node test/d635-append.test.mjs` → `d635-append: 35 pass, 0 fail`, exit 0. Its sections:

1. KEEP, APPEND, FILL: page 0's text is untouched; page 1's text is `19`, then the transcription (checked by SHA-256);
   page 2 holds the transcription alone; the document is no longer a tier-3 candidate.
2. BOTH PARTS: the layer part covers pages 0 and 1, the engine's part pages 1 and 2, and `part` is 0, 1, 1.
3. PAGE CAP, through `op=textattest`: page 0 null, page 1 **null** (never C), page 2 C.
4. PROVENANCE and TIERS: page 1 names tier 1 on the plane and tier 3 on the OCR member; tier 1 covers pages 0 and 1,
   tier 3 covers pages 1 and 2; the content rows read partial, then PRESENT.
5. SAME PAGES: FOLIO's two parts both name page 0 and are told apart by `part`; the document and its page are null.
6. PARTITION: the D-252 shape records its chain with no `part`; page 0 null, page 1 C.
7. The rules over composed chains.

### The negative control, `node test/nc-d635.mjs` → exit 0, `every arm AS DECLARED`

| arm | edit (one source) | declared MUST FAIL | result |
| --- | --- | --- | --- |
| baseline | none | nothing | 35/0, green |
| **onepart** (the row's control) | the layer part filters out every filled page, as before D-635 | BOTH PARTS, the page-1 cap, PROVENANCE, TIERS, SAME PAGES | 21/14; 7/7 declared present, 0/7 held-open broken |
| refuse | a selected page holding a glyph is refused again | KEEP + APPEND, the basis, BOTH PARTS, SAME PAGES | 21/14; 5/5 present, 0/5 held-open broken |
| nopart | `mergedChain` stops stamping `part` | SAME PAGES' caps, the stamp assertions | 29/6; 5/5 present, 0/9 held-open broken (the page-1 cap on MIXED held: its parts have different page lists) |
| spelling | OVER-STRICTNESS: the layer part read off the base text's pages | nothing | 35/0, green |

Every restore was byte-identical by sha256 and by content: `src/index.mjs` 891,193 B `83b391263ad6…`,
`src/textchain.mjs` 91,205 B `383b2dc3cfb2…`.

### The controls whose anchors moved

- `nc-cpdf10.mjs` (baseline 210/0, foot reached). Arms (h) and (j) anchor on lines D-635 changed. Their anchors now
  follow those lines, and the arms are unchanged. a 4, b 2, c 5, d 9, e 1, g 11, **h 2** (it was 4), i 3, j 8, k 1.
  (h) fails 2 because, with eligibility gone, the over-reaching answer for a page holding text is now APPENDED, so
  the good text survives. The two assertions about the member's text reaching the record fail. **(f) is a SURPRISE,
  fail 0, and it is the same on D-635's base** (`056d3092`, run in a detached worktree): since D-627 a page that
  loses `no_text_layer` is still an image with no glyph, so `image_content_unread` routes it. **Minted D-687.**
- `nc-d536.mjs`: arms `nodigest` and `nochain` anchor on `readingprov.mjs` lines D-635 changed, and follow them.
  7 arms, 0 not as declared: baseline 60/0, nodigest 51/9, nochain 57/3, overwrite 56/4, inferred 58/2,
  nodedupe 59/1, nopurge 59/1.
