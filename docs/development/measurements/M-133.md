## M-133 · 2026-09-24 · D-481 — TIER 1 READ A PER-GLYPH-POSITIONED PDF AS ONE CHARACTER PER LINE: CHARACTERS AND WORDS, BEFORE AND AFTER

**Headline.** On Oakland's `Budget-Basics-FY23-25.pdf` — 11 pages, the document class D-481 names — Tier 1's
reading went from **47 words to 750** and from **4,528 lines to 194**, with the **non-whitespace character count
UNCHANGED at 4,228**. Nothing was decoded that was not being decoded before. Every glyph, the document's own
SPACES INCLUDED, was already coming out of the extractor; they were being separated by a newline apiece, so the
record held a page of characters where the document holds sentences. Words per page rose **4.3 → 68.2** (mean).

**`Budget-Basics-FY21-23` — THE ROW'S NAMED DOCUMENT — WAS NOT REACHED, and this entry is about FY23-25 instead.**
Four addresses were tried on 2026-09-24 03:47Z under the honest CivicOS contact agent and each returned **404**:
`https://cao-94612.s3.us-west-2.amazonaws.com/documents/Budget-Basics-FY21-23.pdf`, the same key on
`cao-94612.s3.amazonaws.com`, `…/Budget-Basics-FY-21-23.pdf` and `…/budget-basics-fy21-23.pdf`.
`…/Budget-Basics-FY23-25.pdf` returned **200, 579,573 B, sha256 `505ede3e542d160953cd62a68792ce75fd4263ab3f1ef6984213a84fefff66d3`**
— the same series, the same publisher and the same producer. **A 404 at four addresses is not a statement that the
FY21-23 document does not exist**; nobody enumerated the bucket, and the row's id for it may be a name rather than a
key. What is measured here is FY23-25's bytes.

### Instrument

- `extractPdfStructure(bytes).text` from `bio-plane/src/pdfstructure.mjs`, run in node v26.10.0 on the D-481
  worktree, 2026-09-24 between 03:47 and 03:55Z. Harness: `.d481-cache/measure.mjs` (not committed — it is
  nine lines over the public export, and the figures below are its whole output).
- A **WORD** is a whitespace-delimited token of ≥2 characters containing a letter or a digit. Single characters are
  excluded ON PURPOSE: the defect's output is a column of them, and a counter that scored them would have read the
  broken document as having 3,980 "words" and the fixed one as having 770. **The count that flatters the defect is
  the one to refuse.**
- **BEFORE** figures were taken by restoring the pristine module (sha256
  `64fcc691c77409be1d6c6f1414e4304304da2c81984410ee7557079a54cc7bf7`) over the patched one and re-running, then
  restoring the patched module and verifying it by sha256 AND `cmp`. Not by subtraction.

### The figures

| document | pages | non-ws chars | words BEFORE | words AFTER | lines BEFORE | lines AFTER | 1-char lines BEFORE | AFTER |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `Budget-Basics-FY23-25.pdf` (200, 579,573 B) | 11 | 4,228 → 4,228 | **47** | **750** | 4,528 | 194 | 4,496 | 2 |
| `legistar-agenda-1425401` (200, 176,484 B) | 9 | 14,406 → 14,406 | 2,414 | **2,406** | 421 | 397 | 9 | 9 |
| `2025-2027-transmittal-letter-final.pdf` (200, 230,514 B) | 7 | 5,061 → 5,061 | 933 | **922** | 137 | 107 | 33 | 17 |

**Words per page, FY23-25, after:** `[14, 33, 92, 91, 102, 31, 70, 80, 109, 108, 20]` — mean **68.2**, and **four
pages under 60**. Those four are the cover (14 words: *Fiscal Year 2023-25 / Overview of the City Budget Process /
City of Oakland / BUDGET BASICS*) and three section dividers. **The document does not hold 60 words on those pages,
so no reading of it can honestly produce 60**; the row's "at least 60 words per page" is read here as the
document's per-page mean, and both figures are given rather than the one that passes.

**THE DIRECTION IS THE POINT, AND IT RUNS BOTH WAYS.** On the two documents Tier 1 already read well the word count
went DOWN by 8 and 11 — spurious splits rejoined. A reading that simply stopped emitting newlines would have raised
the count on all three while destroying every real line. **A rise everywhere is the liar's signature; a large rise
on the broken class and a small fall on the sound one is the fix's.**

### The residue, measured rather than asserted

Two runs on ONE baseline separated only by a horizontal jump — table columns, a footer's left and right halves —
are now CONCATENATED where they used to be split by a newline. Counted as tokens containing a lower→upper
transition: `legistar-agenda-1425401` **5 → 13** (the 8 new ones are all `OaklandPrinted`, a page footer whose
*City of Oakland* and *Printed on …* sit on one baseline), **0.32% of its 2,534 tokens**; `transmittal` **1 → 1**;
FY23-25 **0 → 0**. **What the matcher can see:** a glue whose second half starts with a capital or the token
contains a digit boundary. **What it cannot see:** two lowercase runs glued (`ofthe`), and any glue inside a
document it was not run over — three documents is the whole corpus here.

Closing it needs the PEN's position after a shown string, which needs glyph advance widths (`/Widths`, `/W`), which
`loadFont` in this module does not read. A gap threshold chosen without them would be an invented figure, so none
was invented. **Named for the build plan, not parked: parse the width arrays, track the pen, and set the
same-baseline word-gap threshold from a measured distribution.**

---

### 2026-09-24 · THE RESIDUE ABOVE IS CLOSED BY D-502 — see `M-141.md`

The last paragraph of this entry named what closing it needed: *parse the width arrays, track the pen, and set the
same-baseline word-gap threshold from a measured distribution.* All three landed the same day. **Re-measured over
these same bytes** (`legistar-agenda-1425401`, 176,484 B, sha256 `5381c37f…`, re-fetched and byte-count-matched):
the glue tokens go **13 → 5**, which is this entry's own BEFORE figure, and the five that remain are the document's
own words (`eComment` ×4, `McGuire`). The eight this entry counted were all `OaklandPrinted`, and all eight are
gone — eight lines of 397 differ and they are exactly those. **This entry's figures are NOT revised**: they were
right when taken, and D-481's reading is what they measured. M-141 carries the after-state, the gap distribution
the threshold came from, and the sweep that says the threshold's value is not what does the work.
