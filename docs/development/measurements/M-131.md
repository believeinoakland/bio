## M-131 · 2026-09-24 · D-486 — what does an uninvited member learn from a RUN over a project she cannot see? Before and after, at the op

Instrument: `bio-plane/test/project-sight.test.mjs` §9, driven through the control plane in miniflare (`node` in the
cloud container), worktree `/home/user/bio` on base `land/conduct/c19-batch10` @ `cff0ede6`. The reader is `vera`, an
enrolled member never invited. The act is `iris` — who OWNS the hidden project — opening a run over it
(`op=airunopen`, `contextType=project`) and ticking it with one `LOOKED_ABSENT` observation at each of the three
bundle levels (`op=airuntick`). The witness is the ADMIN token's own five figures, which move. Every figure below was
PRINTED by a run.

**BEFORE (the unedited `store.mjs` at `cff0ede6`, §9 written first and run against it through `PROJECT_SIGHT_SRC`):
`project-sight: 131 passed, 5 failed`.** Vera's answers moved, by key, as the suite printed them:

| reader | what moved for vera |
| --- | --- |
| `op=stats` | `aiRunLog`, `observationsNonLead` (each +3, the run's three rows) |
| `op=frontier&level=document` | `tally` |
| `op=frontier&level=content` | `tally`, and four watermark-derived keys |
| `op=frontier&level=meaning` | `tally`, **`looked`**, `by_subject_kind`, and four watermark-derived keys |

The meaning level's `looked` carried the run's OWN ID (`RUN-2026-9486-hidden`) and `ran_and_found_nothing: true` —
a hidden project's coverage claim published verbatim to a member who may not know the project exists. Before the
change the ADMIN token's `aiRunLog` and vera's were both 6; after it they are 6 and 3.

**AFTER (D-486's one predicate at all five readers, plus the meaning arm's run fence): `project-sight: 136 passed,
0 failed`.** Vera's `op=stats` and her DOCUMENT frontier are BYTE-IDENTICAL across the act; at the content and
meaning levels every published field is unmoved except the four watermark keys (see below); the ADMIN token's
`aiRunLog` less vera's is exactly the hidden run's own row count, read back out of `op=airunlog` rather than out of
this suite's arithmetic; a run over a context vera CAN see still moves all five of her figures by exactly 3 rows
each; and the MEMBER token, the ADMIN token and an enrolled administrator (`ruth`) read one another's figures.

**THE RESIDUE, MEASURED AND ROUTED RATHER THAN ROUNDED OFF (D-486 FINDING 2, for BOB).** Four keys still move at
the content and meaning levels: `never_looked`, `never_looked_count`, `missing_unexplained`,
`missing_unexplained_count`. `#missingContentCause` and `#missingMeaningCause` classify an absent look by comparing
a subject's entry date against `MIN(at)` over the WHOLE log at that level, so a hidden project's run writing the
earliest row at a level re-dates that watermark and flips an outsider's `missing_cause` from `purged` to
`never_looked` for unrelated subjects. It is a RECLASSIFICATION and not a disclosure — asserted: neither answer
contains the run's id, the project's id or the run's subjects anywhere in its bytes.

**AND IT IS INTERMITTENT, WHICH IS WORSE THAN STEADY.** Measured twice in this worktree ~20 minutes apart: once with
all four keys moving, once with none. Both causes normalise to the SECOND before comparing (REC-94's inherited
defect note) while `register.registered` carries milliseconds, so whether the run's rows and the fixture's captures
fall inside one second decides the outcome. The §9 arm is therefore spelled as a CEILING (every key that moves must
be one of the four) rather than an equality, and it still fails on the two regressions it exists for.

**NOT MEASURED, STATED:** `#contentAxisTally` reads the same `MIN(at)` watermark for `op=contentaxis` and the search
envelope. It is outside D-486's five readers and was not driven; it is named in the report as the same class.
