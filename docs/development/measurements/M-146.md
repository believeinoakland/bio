## M-146 · 2026-09-24 · M0-176 — which doc-facing units does a prose change select? Before and after the path-granular door

Instrument: `node tools/gates.mjs --explain` under `BIO_GATE_RESULTS=off` (no key, no reuse — selection only),
run in an ISOLATED CLONE (`git clone --shared`) kept in the session scratchpad, never in a worktree. The clone's
`refs/remotes/origin/main` is moved to the tree being measured, so the diff the gate classifies is exactly ONE
dirtied prose file and nothing else; each probe appends one comment line, reads the plan, restores the file by
`cp` from a pristine copy and confirms `git status --short` is empty. The probe script is
`m0176-probe.sh`, outside the worktree. Node 26.10.0, the cloud container.

Tree: `land/worker/M0-176` at its base — `origin/main` `1a7f0bcc` fast-forwarded onto
`land/conduct/c20-batch25` @ `21d69e60`. Corpus: **280 files under `docs/`** (`git ls-files docs | wc -l`).
The DERIVED doc-facing set is **42 units — plane 38 · ui 4**, and this row does not move it: what moved is the
DOOR, which until now handed every one of those 42 any `docs/` change at all.

**BEFORE — the door does not look at the path.** Two diffs that share no reader whatsoever selected the SAME
42 units, printed as the same list in the same order:

| the diff | units selected |
| --- | --- |
| `docs/development/MEASUREMENTS.md` alone | **42** (plane 38 · ui 4) |
| `docs/development/kickoffs/SCHEDULER.md` alone | **42 — the identical list** |

**AFTER — §2f resolves which prose each unit takes.** The same three probes, same clone, same instrument:

| the diff | before | after | who left |
| --- | --- | --- | --- |
| `docs/development/MEASUREMENTS.md` | 42 | **39** (plane 37 · ui 2) | `plane:owed-controls`, `ui:analyst-vocabulary`, `ui:queue-allclear-limit` |
| `docs/development/kickoffs/SCHEDULER.md` | 42 | **35** | — |
| `docs/architecture/BIO_System_Design.md` | 42 (read at the control arm below) | **30** | — |

**NEGATIVE CONTROL, on the estate rather than the fixture (2026-09-24, this worker).** `docsTaken`'s one
return put back to the whole tree — the pre-M0-176 door, at the single point the key, the DOCS step and both of
`readersOf`'s bounds read through — and all three probes go back to **42 of 42**, the same list each time,
`calibration.test.mjs` among them in all three. Restored by `cp` and re-measured. The fixture-side arms are
G23–G25 in `bio-plane/test/gates.control.mjs`.

**WHY THE WIN IS 3 AND NOT 30, which is the honest shape of it.** Most doc-facing units are the planning and
ledger suites, and they really do read the whole corpus. Each unit's docs set, from `--inputs all` on the same
tree:

| docs paths taken | units |
| --- | --- |
| 280 (the whole tree) | 29 — `coord`, `decided`, `ledger`, `mintid`, `status`, `train`, `pushguard`, `statepaths` and the rest of the corpus walkers |
| 257 · 228 | `caseflip` · `undesignedclaims` |
| 29 | `caselifecycle`, `casepin`, `caseproduction` |
| 14 · 11 · 10 · 6 · 4 · 4 · 1 · 1 | `ui:connections-sidebar` · `affordances` · `ui:member-respect` · `calibration` · `owed-controls` · `rec-182-created-tie` · `ui:analyst-vocabulary` · `ui:queue-allclear-limit` |

So the door was never the whole cost, and this measurement says which half it was: the 31 corpus-walking units
are selected by what they genuinely read, and the 11 bounded ones are now selected by their own prose.

**D-535, ISOLATED HERE.** `calibration.test.mjs` is STILL selected by a MEASUREMENTS-only diff after this row,
which is M0-176's accepts-when unmet and the reason the row NARROWS rather than closes. The cause is not the
door: with the door path-granular, `calibration`'s docs set is the six paths above, and `MEASUREMENTS.md` is
among them because `bio-plane/checks/bio-checks.mjs` and `bio-plane/src/index.mjs` CITE the ledger by name
inside member-facing strings (a DEC-49 refusal translation and three OCR cost sentences), and §2e reads a
DIRECTLY-IMPORTED runtime module's text for path mentions. MEASURED: rewriting those four citations as prose
("the MEASUREMENTS ledger") in the clone takes the MEASUREMENTS-only selection **39 → 37**, and
`calibration.test.mjs` leaves it. That is the same correction M0-165 made to `calibration.test.mjs`'s own
`measured_by` labels, one level down, in the plane.
