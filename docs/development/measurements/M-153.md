## M-153 · 2026-09-25 · D-521 — C-82.1's retirement: the figures, the negative control, and the WORKER REPORT that no channel would carry

*Minted with `node tools/mintid.mjs M` by WORKER D-521 (session_017oGHVZRbcp6PS7sVFafim3, spawned by SCHEDULER #20).*

**WHY THE REPORT IS HERE.** The brief says to report to CONDUCT #20 (session_011PzZW1FSobMne4cYeAYWfU) and SCHEDULER #20
(session_01RxoRvCfY35n2aXnn2unRJp), each by one one-shot `create_trigger`. Both calls were REFUSED at 2026-09-25T00:06Z:
*"caller session is at lineage depth 8 (limit 8); cannot spawn or re-arm further child sessions"*. `ListAgents` found
no reachable session either. The row's own status line says to read `land/worker/D-521` and the session when no worker
is live, so the report is committed to the branch it describes. **FINDING for SCHEDULER: a worker spawned at lineage
depth 8 cannot report by the channel its brief names. The fix is to spawn workers shallower, or to name a fallback
channel in the brief.**

### What landed (land/worker/D-521, base origin/main 9f8b69e6)

- `store.mjs` `acknowledgeStatement`: the re-author read is `#one` on `(case_id, edition)`, `case_documents`' primary
  key. Removed: the constant STATEMENT_ACK_DOCUMENTS_MAX, the refusal C-82.1, its DEC-49 region
  `is-statement-ack-documents-bound`, and the answer keys `case_documents_limit` / `case_documents_truncated`.
- `bio-checks.mjs`: C-82.1's row is removed and its number is not reused. `STATEMENT_ACK_CHECKS` holds six rows.
- `CATALOG_VERSION` 1.28.0 → 2.0.0, proposed MAJOR: one departure, no arrivals. The census is 461 → 460 with digest
  `e665147887ee8a4379f79448105fbf0e595cd2f33ff86b99d02225675494d2f5`, both from the d470 suite's own print.
- IC-292 is PROPOSED (I3: one removed code and two removed answer keys). Consumer impact in `civicos-ui/` is zero.
- `check-refusal-codes.mjs`: **eight** floors fall, not the six expected, each read from `--strict`'s print with its
  reason at the site. rows 395→394, census 700→699, reach 447→446, governedSites 186→185, regions 169→168,
  regionLines 3967→3958, codesChecked 446→445, refusalsJudged 440→439. families is unchanged at 54.
  **A GOVERNED REGION WAS REMOVED: re-read regionLines at the union.**
- Superseded arms were corrected with dated reasons, never exempted:
  - `bounds.test.mjs`: the capped-op roster 45 → 44 from its own output, because statementack left it.
  - `d150` block 8.
  - `d507`.
  - The `d470` census row, its A5 literal and its control needle.
  - `ratify.test.mjs`'s version literal.
- `construct-status.json` `13.statement-ack`: the text is updated, the version probe is moved, and a `none` probe is added
  for both retired tokens. `BIO_Publication_v0_1.md` rule 11 is updated. `bundles.mjs` rebuilt the plane's bundle.

### Verification

`node tools/gates.mjs` GREEN on tree 9d51b463, read unpiped: **360/360 suites green · 20668 assertions passing ·
EXCLUDES 2 untallied suite(s)**. Also:

- `check-refusal-codes --strict`: exit 0.
- `status --check`: 0 drift.
- `corpuscheck`: 0 fail.
- `plancheck`: 0 fail apart from UNPUSHED, before the push.

The FIRST gate run, on tree 23442868, was RED at `ui statement-ack.test.mjs`. That is the finding below.

### Negative control (the same record is on the head of `d150-statement-acknowledgement.test.mjs`)

All five arms came back AS DECLARED. Every restore was verified by sha256 MATCH, content IDENTICAL and `cmp` SAME.

- **(0) Baseline.** The guard exits 0, d150 is 64/0, d507 is 63/0, and status shows 0 drift.
- **(a) The row's named control.** The row and the region were restored WITHOUT their site. The guard exits 1 and names
  the region `is-statement-ack-documents-bound` (0 characters). d150 goes 63/1 and d507 62/1, each at its retirement
  arm, and status drifts.
- **(b) The row alone.** The guard exits 1: "found 0 opening marker(s)". The same suite arms fail and status drifts.
- **(c) Over-strictness.** A comment naming the retired tokens leaves everything green.
- **(d) REC-194's arm (b), re-run over the new read.** 58/6. REC-194's surprise green arm ("NOTHING WAS WRITTEN") now
  FAILS, because no bound stands between the first act and the write.

### Finding, with its named fix (UI's ground, needs a D id)

`civicos-ui/test/statement-ack.test.mjs` line 360 builds its `STATEMENT_ACK_*` corpus from `store.mjs` with
`/["']?(STATEMENT_ACK_[A-Z_]+)["']?\s*[,:}]/` and **does not blank comments first**. A comment that named the retired
code followed by a comma was read as a live, untranslated code, and the gate went RED. I worked around it here by
rewording the comment. **FIX:** blank comments before the harvest, as `status.mjs` has done since M0-155.

### Seams

- **D-463** (c20-batch27) also moves `CATALOG_VERSION`. At the union, take the next number and re-read d470's print.
- **D-558**: if its non-literal form of `ratify.test.mjs:240` lands first, its form wins.
- **REC-217**: this diff touches only `acknowledgeStatement`'s document read and its answer keys, never `publishCase`.

### Left

Nothing of the row. The IC-292 classification and the version number at the union are CONDUCT's. There are no design
questions for BOB.
