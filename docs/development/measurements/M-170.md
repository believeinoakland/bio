## M-170 · 2026-09-25 · D-321 — THE HELD BYTES CONTAIN NO REAL SCANNED AGENDA PAGE: **1 image-only page in 52 censused, and it is the known City Council RESOLUTION**; the `reading_refs` join over real OCR stays proved on synthetic ink only

**What was asked.** D-321 (QUEUE, owner CONTENT-PDF; design `BIO_Content_Framework_v0_10.md` Part II §16): commit
ONE real scanned-agenda page image to the OCR fixtures, taken from bytes already held — the cloud proxy refuses
Legistar, and a refusal is not routed around — and drive the `reading_refs` join over it. If the corpus holds no
such page, record with date and instrument that the search came back empty, stating the population searched.
**It came back empty. Nothing was built, no fixture was committed, and no page was synthesised.**

**Instrument.** `pdf-worker/test/agenda-scan-census.probe.mjs` (committed; a `.probe.mjs`, outside the battery),
run 2026-09-25T04:15:35Z on `5e8a65a83` (origin/main), WORKER D-321. It reads only the local git object store.
Every blob reachable from every ref is classified by MAGIC BYTES (`%PDF`, PNG, JPEG, TIFF), not by extension;
every PDF is censused page by page with `pagepixels.mjs`'s own `analyzePage`, the predicate the OCR path uses — a
page is image-only when it paints at least one image and has no text operator.

**The population, stated.** Every pushed branch fetched first (`git fetch origin '+refs/heads/*:refs/remotes/origin/*'`):
**496 refs, 117,950 reachable blobs.** An earlier pass over ALL objects in the store, reachable or not
(`git cat-file --batch-all-objects`, blobs over 2,000 B: 20,459 of them), found the same ten image/PDF blobs and no
other.

| found | n |
| --- | --- |
| PDFs (all readable) | **8** — the 4 `cpdf20` Legistar attachments, the 2 `fw20` NCPC/NSS documents, `legistar-agenda-1425405.pdf`, `scan-ccitt-g4-page.pdf` |
| pages censused | **52** |
| image-only pages | **1** — `scan-ccitt-g4-page.pdf` p0 (sha256 `591b1615b674…`) |
| raster blobs | **2** — `changes/UX-1/after-ux.png`, `before-main.png`: read by eye, both are CivicOS sign-in screenshots |

**The one image-only page is not agenda-shaped, and that was measured before this item, not assumed here.** It is
page 2 of Legistar attachment 15721260, a scanned City Council RESOLUTION; `ocr-member-e2e.test.mjs` section 4
drives it through the real `ocr-worker` engine and asserts `content_type` `generic` with **0** entities. The
agenda that IS held (`legistar-agenda-1425405.pdf`, 33 pages) carries a text layer on every page, so it never
reaches tier 3; rasterising it would be exactly the synthesised page the row forbids calling real.

**Negative control on the instrument** (W34: an empty answer is also what a broken census prints). The probe
FAILS unless it finds the known resolution as image-only. Arm: the image-only predicate forced false. Declared:
exit 1 at that gate, population unchanged. Actual: exit 1, `FAIL — the census did not find the known image-only
page`, PDFs 8 / pages 52 / rasters 2 unchanged. Restored by `cp`, `cmp` identical, sha256 `4ecc62d61f754877…`.

**What this measurement cannot see — the undetermined half, stated.** Bytes held OUTSIDE git: the project
instance's R2 captures, and any other machine's harvest cache (CPDF-12's 59-PDF and CPDF-14's 60-PDF Legistar
corpora were fetched to caches, not committed; CPDF-14 found 13 image-only pages in two documents, whose kind is
not recorded as agenda). **A read of the live instance's record to list its tier-3 captures was attempted and
REFUSED by this session's permission policy (2026-09-25 ~04:08Z, WORKER D-321, one `op=stats` GET), and was not
retried by any other route.** So *no scanned agenda page is held* is established for the git population only;
for the instance's own captures it is UNDETERMINED.

**What would close D-321.** A session permitted to read the project instance lists its tier-3 captures
(`op=textprovenance&step=ocr`, a non-mutating read) and inspects each for agenda shape; or a scanned agenda
arrives through a capture route that is not refused. Either yields the page; the fixture and its arm follow.
Until then `ocr-member-e2e.test.mjs`'s synthetic-ink arm, which says at its own site that its ink is synthetic,
is the only proof of the join over OCR — as that suite already states.

### Part 2 · 2026-09-25 · the INSTANCE's captures — **24 image-only candidates in 1,836 censused pages; NONE is agenda-shaped, and NONE can reach tier 3**. D-321 stays open as a population gap (D-313)

**What was read, and how.** Instance `biosmoke7` (`https://biosmoke7.believeinoakland.workers.dev`), store `bio`,
the member token, WORKER D-321 part 2, 05:09Z–05:18Z. Every call was a GET on an op checked at the code on
`3d7ad5a4` as a read: `stats`, `textprovenance`, `list` and `image` are `mutating: false` in `index.mjs`'s OPS table;
`op=capture` is `mutating: true` for its PUT/POST branch, and its GET branch only does `CAPTURES.get` and returns the
bytes. `transcribedDocuments` (`store.mjs`) is a single `SELECT`. **Witness:** `op=stats&store=bio` read at 05:09:51Z
and again at 05:18:34Z, 53 counters, **byte-identical**. Nothing was refused. `store=scratch` was read once
(`textprovenance`, 0 rows); it holds no capture of record (M-166).

**Tier 3 has never run on this store.** `op=textprovenance&step=ocr` → **0** documents, and so does the same op with
no `step` filter: `reading_text_source` is empty and `textUnits` is 0. The empty OCR list means *nothing was ever
read*, not *nothing scanned is held* (CLAUDE.md §2). So the census went one level down, to the bytes.

**The population.** `op=list` → 31 bundles; `op=image` on each → 69 distinct blob hashes, each classified by a
16-byte range read of `op=capture`: **11 PDFs**, **0 rasters** (PNG/JPEG/TIFF/GIF). The other 58 are HTML, DER
timestamp tokens, JSON/CSV/Markdown, base64 copies of the PDFs, and the second part of the split budget book. This is
M-166's population, found again independently. Each PDF was fetched whole and checked against its sha256 on arrival
(11 of 11). The two parts of `INFO-2026-0301` were joined, and the whole hashes to `3f21377d…`, as M-166 found.

**Instrument.** `pdf-worker/test/agenda-scan-census.probe.mjs --dir <dir>`, which gained `--dir` in this landing.
It uses the same `analyzePage` predicate, and the committed `scan-ccitt-g4-page.pdf` is added to the population as the
positive control. It gained one fix too: **an encrypted file is no longer censused.** Its content streams are
ciphertext to `analyzePage`, which then sees no text operator. The first run therefore called the EBMUD agenda
report's p0 and p3 image-only, although an independent reader (PyMuPDF 1.28.2) finds 2,226 characters of text layer on
p0. The OCR path refuses such a file before analysis (`ENCRYPTED`), so it never reaches tier 3 either. Control
arm recorded on the probe's `NEGATIVE CONTROL:` line.

| capture (bundle) | pages | image-only | why it cannot reach tier 3 (`renderPageToPixels`) | what it is, read by eye |
| --- | --- | --- | --- | --- |
| `2f931fa7c69c` INFO-2026-0116 ordinance 13035 C.M.S. | 7 | **7** | `UNSUPPORTED_FILTER` JBIG2Decode ×7 | Fujitsu scan: Ordinance 13035 (sewer service charge), rate tables, notice and digest, signature page |
| `657479bf50a5` INFO-2026-0118 fund amendments 2010 doc 2 | 3 | **3** | `UNSUPPORTED_FILTER` JBIG2Decode ×3 | Fujitsu scan: a City Council resolution amending the FY 2010-11 budget, with its Exhibit B table |
| `b4ef41494580` INFO-2026-0104 adopted budget book | 911 | **5** | JPXDecode ×3 (p38, 170, 882), several JPX images ×2 (p11, 881) | a screenshot, an org chart, financial tables |
| `3f21377d8b8e` INFO-2026-0301 FY23-25 budget book (joined) | 653 | **9** | JPXDecode ×1 (p634), several JPX images ×8 (p633, 645–651) | financial tables, a glossary |
| `978ac0c81ffb` INFO-2026-0114 EBMUD staff report | 4 | — | encrypted: not censused (`ENCRYPTED`) | "AGENDA REPORT" cover: a TEXT-LAYER page, not a scan |
| `5f2a39200b01` INFO-2026-0106 ACFR FY21-22 | 216 | — | encrypted: not censused | (M-166: tier 2 reads 375,302 glyphs) |
| the other five | 45 | 0 | — | — |

**Censused: 1,836 pages in 9 unencrypted PDFs. Image-only candidates: 24. Agenda-shaped: 0.** Each candidate was
rendered outside the product (PyMuPDF, 60 dpi grey) and read by eye, which is this worker's judgement and not an
instrument. The positive control was found (exit 0). **No candidate is a DCT page**, so D-320's route does not apply
to any of them. The route they would need is a JBIG2 or JPX decoder, which no tier has. **D-622** was minted for that
and sent to SCHEDULER: 10 held pages of enacted City legislation that no tier reads.

**Blind spot of the predicate, stated.** `analyzePage` reads only a page's own content stream, not the Form XObjects it
paints. The budget-book pages carry a 1–3 character page number that pdf-lib stamped inside a Form XObject
(`Fm0`). PyMuPDF reads that number; the census does not. So the census OVER-counts candidates, never under-counts.
That is the safe direction for a search, and every candidate was judged by eye.

**The git population, re-run the same hour** (`git fetch` of every branch, **506 refs, 123,170 reachable blobs**):
11 PDFs, 57 pages, **6 image-only**. Five are new since part 1 and none is a real scanned agenda. They are
`scan-dct-page.pdf` (D-320, `land/worker/D-320`: page 4 of the SAME resolution 15721260) and
`bio-plane/test/fixtures/d460/agenda-p1.pdf`/`agenda-p2.pdf` ×2 pages each (D-460, `land/worker/D-460`). The D-460
fixtures are agenda-shaped but **SYNTHETIC**: `d460-fixture.mjs` draws them, and M-165 says so.

**Where real scanned agendas DO exist, and why they do not close this row.** M-165 (D-460) rebuilt FW-20's walk of the
public `cao-94612` bucket. It found real scanned agendas and minutes among its 21 tier-3 documents: a Planning
Commission agenda, a Privacy Advisory Commission agenda, BPAC and I-Bond committee minutes. Their bytes are NOT held,
because only the manifest `walk-tier3-manifest.json` is committed. Transcribed whole, 20 of the 21 still read
`generic`: none carries a line-anchored Legistar file number, and the agenda and minutes readers are written to
Legistar's shape (a deliberate closure, `MEASUREMENTS.md` 2026-08-03). One, the SSOC special-meeting packet, read
`regulation`. The regulation reader mints entities, but whether they reach `reading_refs` on that document is
**UNDETERMINED**: nobody has measured it, and the bytes are not held. So on every population searched, **a real page
whose OCR yields a `reading_refs` hit needs one of two things**: a Legistar-shaped scanned agenda (none is held
anywhere searched), or non-Legistar agenda breadth in the readers (plan-owned, not a defect). A third, unmeasured
possibility is the SSOC packet's `regulation` reading, which needs D-606 (every page transcribed) and bytes fetched
from the bucket.

**D-321 stays OPEN as a population gap (D-313).** `ocr-member-e2e.test.mjs`'s synthetic-ink arm is still the only
proof of the join over OCR, and its header now says so and cites this part. No fixture was committed and no page was
synthesised.
