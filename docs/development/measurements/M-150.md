## M-150 · 2026-09-24 · D-533 — `op=registeraudit` CALLED A PARTED CAPTURE'S HELD BYTES MISSING: **`unbacked: 1, sound: false` before, `held_in_parts: 1, sound: true` after**, on the same 21 MB, 3-part document filed as C-18.1 describes. Also a sweep finding, **D-556**: the ratify gate heads the whole key the same way. (WORKER D-533, SCHEDULER #20)

**Why this file carries the report.** This worker's session is at lineage depth 8 of 8, so `create_trigger` refused
both reports ("cannot spawn or re-arm further child sessions"). `ListAgents` reached no peer either. The branch is the
only channel left, so the report travels with the landing.

**Instrument.** Miniflare in the cloud container, worktree `/home/user/bio` on `land/worker/D-533` from `origin/main`
@ `9f8b69e6`. The flow runs through the ops: `op=acquire` of a scripted 21 MiB body (3 parts: 8 + 8 + 5 MiB), then
`op=promote` of a bundle holding the parts as blob files, `data/provenance.json` carrying acquire's own document with
its `parts`, and one register row for the whole. Then `op=registeraudit`. The probe driver was kept in the session
scratchpad.

| tree | held_in_parts | unbacked | undetermined | sound |
| --- | --- | --- | --- | --- |
| `origin/main` 9f8b69e6 (before) | (no such field) | **1** | (no such field) | **false** |
| `land/worker/D-533` (after) | **1** | 0 | 0 | **true** |

**Gate.** `node tools/gates.mjs` GREEN at `74f514dd` (tree e6dc33ef, class FULLREUSE): **78/78 suites green · 6353
assertions passing**. The first run was RED on `provenance-marker.test.mjs`, the swallowed-read ratchet at 40 of 39.
The new site is this item's own: `#partsNamedFor`'s parse catch returns a stated `unreadable` state, which the audit
counts undetermined. The ratchet moved 39 → 40 from the printed figure, with the reason at the site, and the ceiling
arm was re-run: 112/1 by name, then 113/0 once restored.

**Suite** `bio-plane/test/d533partsaudit.test.mjs`, 43/0. **Control** `bio-plane/test/d533partsaudit.control.mjs`, six
arms, each run alone against a patched copy, with the real sources hashed untouched:

| arm | result |
| --- | --- |
| baseline | 43/0 |
| **wholeonly** (THE ROW'S: head the whole key only) | 25/18; first failure "THE ROW: … HELD IN PARTS" |
| nodigest | 37/6 |
| nopresence | 39/4 |
| insidesound | 41/2 |
| strictspelling (over-strictness) | 39/4 |

Every arm came back as declared on the second run. On the first run, strictspelling's declaration missed one pin. That
is recorded on the suite's line.

**FOR CONDUCT.**
- Interface: I3, additive (`held_in_parts`, `undetermined`, and `missing_parts` / `disagreeing_parts` /
  `unverified_parts` on sampled rows). No IC was minted. Classifying it is the integrator's.
- No DEC-49 region was touched.
- **Seam with D-530:** `partsHeld` in `bio-plane/src/index.mjs` (beside `captureKey`) is this item's minimal "held in
  parts" check. D-530 had not pushed when this worker looked. If it lands a helper, unify the two into ONE (REC-35).

**FOR SCHEDULER: D-556, minted by this worker.**
- **The defect.** The ratify gate's missing-bytes check heads `captures/<whole sha>` only (`index.mjs`, `hasCapture`
  passed to `runGate`, and `gate.mjs`, "bytes the register claims must exist", `PLANE_MISSING_BYTES`). A parted
  capture has no whole key, so filing one correctly should get it refused at ratify.
- **Evidence.** Diagnosed at the code. It was NOT measured through `op=ratify`.
- **Named fix.** For a row whose whole key misses, the gate's register loop takes D-533's rule: read the image's
  `data/provenance.json`, then head and verify each part it names, and name any missing part. It should reuse the one
  helper rather than add a third copy.
- **Negative control.** Head the whole key only, and a parted bundle's ratify is refused, failing by name.
- Owner RECORD.

**Not done.**
- The reassembled whole's digest is not re-read. That is C-18.6's check.
- No live verification was made.
