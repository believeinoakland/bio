## M-179 · 2026-09-25 · D-546 — NO RECORDED STATE MOVE IN EITHER REGISTER TAKES AN EDGE ITS TABLE DOES NOT DECLARE: **`bio` 11 moves over 79 version pairs, 0 undeclared; `scratch` 0 bundles**. Per type: information 10 moves (all `collected -> verified`), 0 undeclared; inquiry 1 move (the legacy `problem` `surfaced -> elevated`), 0 undeclared under the vocabulary it was written in; project 0 moves. (WORKER D-546, SCHEDULER #23)

**What was asked.** D-546's scope (1): *MEASURE the corpus first: per type, the count of recorded moves whose edge is
undeclared today, with dates* — BOB #34's ruling (2026-09-24 23:55Z) that the history is COUNTED and SAID, never
rewritten, before `op=promote` is fenced for every type.

**Instrument.** `tools/m179-statemove-census.mjs`, node, cloud container, worktree `/home/user/bio-d546` on
`land/worker/D-546` (stacked on `land/worker/D-578` @ 700a432d), against the live instance named by `BIO_INSTANCE`, plane
`/version` answering `0.79.0`, 2026-09-25 07:07Z. READ ops only, each call NAMING its store (`store=scratch`,
`store=bio`): `op=list` (its `total` equals the listed count), then `op=image` per bundle, which returns the head
`bundle.md`, every `_history/bundle_<key>.md` snapshot and `_history/manifest.json`. A promotion's snapshot is the
version BEFORE it, so the version after promotion *j* is the snapshot promotion *j+1* took (the head, for the last). A
MOVE is two consecutive versions whose front-matter `current_state` differs; it is classified against the catalogue's
own `STATES` through `vocabFor` over the earlier version's DECLARED `object_type` (the MAP RULE, `checkStateLegality`'s),
and dated by the later promotion's manifest `created`. The `state_history` each head document carries in its own bytes
is read as a second register and kept apart.

**Order.** `op=image` exposes no write order (`rowid`) and no digest a promotion wrote, and `created` is the writer's
`last_updated` where it sent one — a caller's statement (the plane's census suite measured a pairing by `created` alone
reading a move nobody made). So a bundle is paired only when its `created` order and its snap-key order AGREE; otherwise
every pair of it is counted UNDETERMINED. They agreed for all 31 bundles. (The instrument's second spelling also
compared each later promotion's `base` with the digest of the snapshot that same promotion took — equal by
construction, joining nothing; it was removed before these figures were taken, and the plane's `op=statemovecensus`
joins on the digest the EARLIER promotion's manifest row says it wrote, which the image does not carry.)

**WITNESS.** `op=stats` read before and after each register's walk: identical in both (`bio` 31 bundles, 244 history
rows, 142 files; `scratch` 0, 0, 0). Nothing was written.

| register | bundles | versions read | pairs | revisions in place | moves | undeclared | undetermined | blob-held |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| bio | 31 | 110 | 79 | 68 | **11** | **0** | 0 | 0 |
| scratch | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

Per type in `bio`: information 76 pairs (10 moves, 66 in place), inquiry 1 pair (1 move), project 2 pairs (0 moves, 2 in
place). No action and no bias set in `bio` has more than one version.

**The eleven moves, with dates** (the writer's `created`):

| bundle | move | dated |
| --- | --- | --- |
| INFO-2026-0099-auditor-report-feb-2022 | collected -> verified | 2026-07-19T04:40:00Z |
| INFO-2026-0100-acfr-fy2023-24-fund-statements | collected -> verified | 2026-07-19T04:40:00Z |
| INFO-2026-0100-adopted-budget-fy2026-27 | collected -> verified | 2026-07-19T04:40:00Z |
| INFO-2026-0103-acfr-fy2023-24-pdf | collected -> verified | 2026-07-19T21:12:19Z |
| INFO-2026-0104-adopted-budget-book-pdf | collected -> verified | 2026-07-19T21:12:19Z |
| INFO-2026-0105-adopted-budget-fy13-15-csv | collected -> verified | 2026-07-19T21:12:19Z |
| INFO-2026-0106-acfr-fy2021-22-pdf | collected -> verified | 2026-07-19T21:12:19Z |
| INFO-2026-0107-revenue-expenditure-reports-page | collected -> verified | 2026-07-19T21:12:19Z |
| INFO-2026-0108-zolly-opinion | collected -> verified | 2026-07-19T22:04:21Z |
| INFO-2026-5460-member-release-key-registry | collected -> verified | 2026-07-22T16:45:00Z |
| PROB-2026-0001-sewer-franchise-diversion | surfaced -> elevated | 2026-07-19T21:43:53Z |

**The one reading that depends on the vocabulary, stated rather than rounded.** PROB-2026-0001's move is a declared edge
of the legacy Focus machine its document was written under (`problem`, §4.2: `surfaced -> elevated`), and is counted
declared. Read in the machine the plane runs NOW (the normalised `inquiry`, which has no `elevated`), it would be the one
undeclared move. The census reads the declared spelling for the reason `checkStateLegality` does: a legacy document is
judged by the contract it was written under. `op=promote` asks the normalised machine, so that move cannot be made again.

**`state_history` register.** `bio`: 11 in-bytes entries (information 10, inquiry 1), 0 undeclared. `scratch`: none.

**What the matcher can and cannot see.** It sees every `bundle.md` version `op=image` returns as inline text (none was
blob-held). It cannot see a move whose version was lost to a pre-REC-176 snap-key overwrite (`op=snapkeycensus` counts
those), nor any bundle `op=list` does not return to an admin viewer (`total` agreeing with the count listed is the
evidence there was none), nor a move its writer misdated (a `created` is the writer's claim). A deployed SOVEREIGN
instance's history is not read here: `op=statemovecensus`, built by this row, is the method it runs, and running it
there is an administrator's act.
