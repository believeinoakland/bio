## M-122 · 2026-09-23 · D-447 — does revising a project the reader cannot see move that reader's search answer? Before and after, at the op

Instrument: `bio-plane/test/project-sight.test.mjs` §7, driven through the control plane in miniflare (`node` in the cloud
container), worktree `/home/user/bio` on `origin/main` @ `02603e88`. The reader is `vera`, an enrolled member never invited
to project P; P is revised (a long body heavy in both query words) by the ADMIN token; the witness is the ADMIN token's own
answer, which DOES move (it can see P), so the index took the revision. Every figure below was PRINTED by a run.

**BEFORE (the unedited `query.mjs`, the final §7 run against a clean `main` checkout): `project-sight: 101 passed, 9 failed`.**
Every one of the 7 searches moved by digest — `q=culvert`, `q=levy`, `q=culvert OR levy`, `q=culvert levy`,
`q=culvert OR levy&mode=ids`, `…&sort=relevance&dir=desc`, `…&limit=1&offset=1` — and so did the order of a query
selection's members: `[short, other, long]` became `[short, long, other]`. `mode=ids` carries no number at all, so **the
ORDER leaks by itself**, not only the score. All 24 hits served carried a `score`.

A second, independent fixture (a scratch probe through the same op, three documents and one hidden project, not kept) printed the raw
figures: `q=sewer` LONG `-1.3394e-6` → `-1.8370e-6`, SHORT `-1.1334e-6` → `-1.5544e-6`; `q=sewer OR transfer` order
`[short, other, long]` → `[short, long, other]`; `q=sewer&mode=ids` did not move there (one term, two rows, no flip), which
is why §7's fixture was built with two terms of different spread.

**AFTER (`visibleBm25`): `project-sight: 110 passed, 0 failed`.** All 7 digests and the selection order identical; no
score key on any of 24 hits; `culvert` ×3 still outranks ×1; descending is the exact reverse of ascending; a VISIBLE
revision still moves the order.

**Cost (a scratch bench through `op=search` as vera, `facets=none`, 7 runs each, median; not kept):** 2,000 visible documents, every one matching `culvert`:

| query | `main` @ 02603e88 | D-447 first form (snippet cut for every match) | D-447 as landed (snippet for the page only) |
| --- | --- | --- | --- |
| `q=culvert` | 73.9 ms | 306.0 ms | 210.1 ms |
| `q=culvert OR levy OR audit` | 146.2 ms | 681.3 ms | 483.5 ms |
| `q=culvert&mode=ids` | 73.3 ms | 303.4 ms | 203.6 ms |

Attribution, by removing one piece at a time: `highlight()` for term frequency ~120 ms of the 306; `snippet()` over every
match ~65 ms (hence the page-only cut); the gated `vis` scan and the per-term counts the remainder. The worst case is a term
in every document; a term in few documents costs little. **Not measured:** a deployed instance, and any corpus above 2,000.

**The sibling class, measured the same day (NOT closed by D-447, routed with its fix):** `op=stats` served to a MEMBER
moves when a hidden project is CREATED (`bundles` 0→1, `indexed` 0→1) and when one is REVISED (`history` 0→1);
`op=searchindexcheck`'s `counts.indexed` moves on a hidden creation (0→1), its `keyed` and `bundles` do not (gated).
A scratch probe through the ops (not kept): one enrolled member, one hidden project, same container.
