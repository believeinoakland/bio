## M-148 · 2026-09-24 · D-545 — `check-semantics.mjs` §3's STATE HARVEST, before and after it stopped reading comments and `typeof` guards as states: **12 states -> 11 on main `9f8b69e6`**, the one dropped (`published`) harvested ONLY from two comments; the pre-D-545 instrument, re-armed, recreates the measured RED by name. (WORKER D-545, SCHEDULER #20)

**Instrument.** `node civicos-ui/check-semantics.mjs [store]` (its `store literals seen:` line), a one-off scratch reader
over `bio-plane/src/store.mjs` through `stripComments` (`bio-plane/scripts/walkfloor.mjs`), the suite
`civicos-ui/test/semantics-harvest.test.mjs` and its driver `civicos-ui/test/semantics-harvest.control.mjs`. Cloud
container, worktree `/home/user/bio`, branch `land/worker/D-545` from `origin/main` @ `9f8b69e6`, 2026-09-24.

**Before (main `9f8b69e6`, the raw harvest).** `store literals seen:` 12 — adopted, collected, concluded, deferred,
dismissed, divided, forming, open, published, retired, surfaced, verified. Per harvest over the RAW store:
`current_state [!=]==? "…"` 17 matches, of which **2 are inside comments** (store.mjs ~4760 and ~30487, both quoting
the retired guard `current_state === "published"`), and they are the ONLY source of `published`; `#setScalar(…,
"current_state", …)` 7, none in a comment; `to_state:` 5, `from_state:` 0, `criticality` 1, `_STATES = [...]` 1 —
none in a comment. No match on main was preceded by `typeof`, and every code match's right-hand side is a catalogue
state. So the defect was LIVE on main and masked: `published` is a legacy case state with a semantics row, so its two
comment sources read GREEN.

**After.** `store literals seen:` 11 — the same set without `published`. `store harvest reach: current_state compared 15 ·
#setScalar current_state 7 · to_state 5 · from_state 0`; `unrecognised matches: 2`, both the comment quotes above, each
printed by line as `blanked by the lexer`. The check is GREEN. `published` still has its semantics row (§2 keeps it,
under `legal ∪ legacy`); what moved is only that §3 no longer claims the STORE writes it.

**The negative control** (driver, 7 arms declared, 7 run, each armed alone, check restored by sha256 `a7fb5531…` at the
run and `cmp`, 27,478 bytes):

| arm | what it breaks | suite | declared |
| --- | --- | --- | --- |
| baseline | nothing | 16 pass, 0 fail | AS DECLARED |
| (a) raw | `storeCode = store`: harvest the raw file again | 13 pass, 3 fail (F1 GREEN; both "blanked" labels) | AS DECLARED on the 2nd run |
| (b) typeof | `typeof` guard disarmed | 14 pass, 2 fail (F1 GREEN; typeof label) | AS DECLARED |
| (c) predefect | raw + no typeof + no catalogue guard | 9 pass, 7 fail | AS DECLARED |
| (d) catalogue | catalogue guard alone disarmed | 15 pass, 1 fail (`zombie` counted as a state) | AS DECLARED |
| (e) silent | an uncatalogued code literal reported, not failing | 13 pass, 3 fail (F2 goes GREEN) | AS DECLARED |
| (f) eaten | the lexer blanks the whole store | 6 pass, 10 fail (the reach floor) | AS DECLARED on the 2nd run |

Arm (c), run by hand over the real store with the planted `typeof x.current_state === "string"` guard and a commented
`x.current_state === "nobody_wrote"`: **`FAIL: states the store writes with no semantics row: string, nobody_wrote`**,
and over the unplanted store the 12-state set above — the pre-D-545 instrument, reproduced.

**Two arms were DECLARED TOO NARROWLY on the first run and are recorded, not smoothed:** (a) also lost both "blanked"
labels (with nothing blanked there is nothing to label), and (f), over an empty harvest, left F1's and F2's set
comparisons trivially equal while failing every GREEN and every naming. Both broke MORE than declared, never less; the
declarations were corrected in the driver with a comment saying so and the second run was 7 of 7 AS DECLARED.

**The reach arm (F2) is what stops "fewer states" passing for the fix:** a planted `#setScalar(id, "current_state",
"zombie")` and `row.current_state !== "revenant"` — states the store genuinely writes and gates on, uncatalogued, with no
semantics row — FAIL the check naming each, as UNRECOGNISED MATCHES, and arm (e) proves that failure is load-bearing.

**What the matcher cannot see, stated.** It reads `store.mjs` only; it sees a state only in the four literal shapes above
(a state held in a variable, built by concatenation, or written through any helper other than `#setScalar` is
invisible, as it was before). `stripComments` blanks regex literals by a heuristic: a `/` it misreads as a regex opener
would blank code — every such match is still PRINTED as an unrecognised match, never silently dropped. The `typeof`
test reads up to 120 characters back through an identifier/member chain (`typeof a.b?.[c].current_state`); a
parenthesised `typeof (x.current_state) === …` does not match the harvest pattern at all.
