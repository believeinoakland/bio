## M-120 · 2026-09-23 · FW-20 — THE DIRECTORY DECODE CENSUS, RE-TAKEN THROUGH THE PLANE: **D-376's premise does not hold. The directories were never scans; they are fonts with no Unicode map — TIER 2's case — and with the fleet bound 56 of 57 name-matched documents read from text (tier 1 alone: 5 of 57)**

Instrument: `bio-plane/scripts/fw20-decode-census.mjs` (FW-20). It boots the REAL plane (`bio-plane/src/index.mjs`), the
COMMITTED pdf-worker bundle (`pdf-worker/dist/pdf-worker.bundled.mjs`) as `PDF_WORKER`, and the committed OCR member
(bundle + wasm core + model, `ocr-worker/test/memberworker.mjs`'s recipe) as `OCR_WORKER`, in miniflare; serves each
document's cached bytes from an outbound stub; and drives `op=acquire` once per document, recording what the plane's own
`reading` says (text tier, `read_from_text`, content type, basis). `FW20_TIER1_ONLY=1` binds NO member — the plane's tier 1
alone, which is the instrument D-376 was measured with; same bytes, same plane, only the fleet differs. Node in the cloud
container, worktree `/home/user/fw20` on `origin/main` @ `02603e88`. Bytes fetched 2026-09-23 22:50–22:57Z from
`https://cao-94612.s3.amazonaws.com/` (public ListObjectsV2: 44 pages, **43,283 keys**); every fetch answered 200 with the
listed size. `oakland.legistar1.com` and `www.oaklandca.gov` REFUSED this container's CONNECT (proxy 403, 22:49Z), so this
census reaches the bucket only. Every figure below was PRINTED by a run.

**The two corpora.** (a) NAME-MATCHED: every `.pdf` key under 3 MB matching FW-18's own pattern
`roster|contact-list|directory|phone-list|staff-list|membership` — **57** keys (FW-18 took 30; which 30 is not recorded, so
this is a superset of the same population, not the same sample). (b) A FIXED-SEED WALK: 300 PDFs of the 22,832 in FW-18's
bounds (20 KB–4 MB), keys sorted then Fisher–Yates with mulberry32 seed `20260923` (not FW-18's seed, which is not recorded).

| corpus | N | read from text, TIER 1 ONLY | read from text, FLEET BOUND | by tier (fleet) | unread (fleet) |
| --- | --- | --- | --- | --- | --- |
| (a) name-matched | 57 | **5** | **56** | t1 5 · t2 51 · t3 0 | 1 (a scan the OCR member refused, `PAGE_NOT_RENDERABLE`) |
| (b) fixed-seed walk | 300 | **65** | **293** | t1 62 · t2 215 · t3 16 | 7 (OCR refusals ×5, one tier-2 extraction error, one with no text at all) |

**Why tier 1 could not read them.** The undetermined reasons tier 1 gave on the 52 unread name-matched documents: `no_tounicode`
18, `code_width_misaligned`+`unmapped_code` 31, both 2, **`no_text_layer` 1**. Only a `no_text_layer` marker asks tier 3
(`needsTier3`), so **tier 3 was never the remedy for this class**; a font with no Unicode map is exactly what the tier-2
decoder recovers. D-376 read "0–100 characters against 1,204–33,189 undetermined markers" as "single-page scans and scanned
booklets"; on these bytes the markers are font-encoding markers, and the one real scan in the set (the SSOC ordinance) is
not a directory. **FW-18's tier-1 figure reproduces in kind** (0 of the 11 directories read at tier 1 here, as 0 of 30 did
there); its CAUSE and its REMEDY do not.

**What the staff directories are, read by eye from the decoded text.** In corpus (a): **11 keys, 9 distinct documents** (by
sha256) are staff directories — 3 revisions of the Neighborhood Services Staff Beat & Program Directory and 6 distinct
revisions (8 keys) of the OPD CRO/NCPC Assignment Directory — and all 11 keys decoded at TIER 2 and at none other. Found
OUTSIDE the name pattern while sourcing negatives: the Neighborhood Services Division staff sheet
(`…Division-Staff-0823.pdf`) and the City's Benefits Contacts sheet — M0-32's "a filename is not evidence of a document's
kind", again. The other 46 name-matched keys are NOT staff directories: 24 membership cards, applications, guidelines and
letters; committee rosters and a board roster with no contact points; a candidate contact list; business, housing,
contractor and health-plan provider directories; an ordinance about a commission's membership; a commenter roster; a
blank participant sheet.

**The walk found NO staff directory** among its 293 read documents — by the `staff_directory` type and by eye over the 11
documents M0-32's contact-density family alone flags (`contacts >= 10 and >= 0.4/100 words`, ported verbatim: two City
Administrator weekly reports, three agendas, a public-comment compilation, two homebuyer-provider lists, a parks weekly
report, a permit notice). At M0-32's measured rate (~1.3% of the text-bearing stratum) ~3.8 were expected, and 0 of 293
is below it (Poisson P(0) ≈ 0.02). **Not resolved here, and stated rather than smoothed:** M0-32's class is BROADER than
this type ("people OR offices with contact points", phone-only included) and was counted with its own threshold over a
different sample; the difference may be definitional, sampling, or both.

**The type, driven over what was read.** `staff_directory` (`docprofile/doctypes/staff-directory.mjs`) recognises all 11
distinct staff directories above (CERTAIN on the 5 that name themselves in a title line, LIKELY on the 6 CRO revisions whose
first line is a column header) and NONE of the 46 other name-matched keys, the 293 read walk documents, or the seven further
documents sourced by hand as negatives (two NCPC meeting schedules read from text, two copies of a staffing memo, an
emergency-contact form, two org charts). Its first draft recognised the 2018 candidate contact list (19 of 31 addresses at gmail.com, 61%) as
LIKELY — the liar's pass — and the one-organisation share was raised to 0.8, the measured separation (directories
0.88–1.00, the candidate list 0.61). Its `also` pass answered `[]` for every real directory: no document in this reach is
both a directory and another class.

**Blind spots.** The bucket only (Legistar refused). The OCR member transcribes one page per invocation, so a multi-page
tier-3 document is read as far as the plane read it and no further. The walk's content types were re-judged OFFLINE with the
current registry over the `op=pdfstructure` text, because its plane booted before the type was registered; the 16 tier-3
walk documents were RE-ACQUIRED through the plane with the type registered (none is a directory). The "by eye" judgement is
this worker's reading of each decoded text, not an instrument.

Commands (from `bio-plane/`): `node scripts/fw20-decode-census.mjs <manifest.json> <pdf-dir> [out.json]`, and the same with
`FW20_TIER1_ONLY=1`; the manifests are `[{key, sha, bytes}]` for the bucket keys above.
