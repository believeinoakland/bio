## M-114 · 2026-09-23 · `ai-session-context.test.mjs`'s fixture draw — how often it collided, and that the corrected draw cannot

Instrument: `node` 26 in the cloud container (shared, several gates live), measured by the D-286 worker in worktree
`agent-a4159bf9b2ec2ebd8` on `origin/main` @ `4355bfda` + the D-286 commit `bf1a6845`. Every figure below was PRINTED by a
run; none is computed from another. The harness scripts were scratch (outside the tree); the permanent instrument is the
suite's own ARM D0c, which re-runs (i)–(iii) on every pass.

| arm | draws | colliding | what it measured |
| --- | --- | --- | --- |
| OLD ranges (PROJ_ALLOWED 5001..5999, PROJ_CONSUMED 2003..2499), in-process, through `collisions()` | 2,000,000 | **3,459 (1 in 578)** | the rate at which the suite went RED BY THE DRAW |
| NEW ranges (`ai-session-fixture.mjs`: 9001..9999 / 4003..4499), in-process, through `collisions()` | 2,000,000 | **0** | — |
| NEW ranges, **500 consecutive runs of the DRAW ALONE**, each a FRESH `node` process (its own unseeded `Math.random`), each judged by ARM D0b's OWN code copied verbatim (ARM D's derivations + the `publishedScalars` walk) over a synthetic wire shaped like a measured `op=airun` session (`ticks`, timestamps, labels, principal, bias and standard blocks) | 500 | **0 — D0b PASS 500/500**, 500 distinct fixtures, 0 out of range | the accepts-when figure |
| OLD ranges, same 500-process harness | 500 | 0 | NOT a refutation: at 1 in 578 the expected count in 500 is 0.87 (P(0) ≈ 42%); the in-process row is the rate |
| FORCED 3354 / 1278 / 2076, same harness | 1 | **1 — D0b FAIL**, `inquiry fetches remainder` | the harness can see a collision |
| the suite itself, clean | — | — | 80 pass, 0 fail; `ARM D0c: proof over 4 derived intervals x 5 published ranges, 0 fault(s); 500 draws, 0 colliding, 500 distinct` |

**Interval proof (`rangeProof()`):** inquiry remainder 1502..2992 and project remainder 4502..5996 each sit in a gap between
the published intervals 1007..1499, 3001..3999, 4003..4499, 8001..8999, 9001..9999; percents are 25..50, below all of them;
1dp percents always carry a ".". **What it does not reach:** the plane's own scalars in the wire (`ticks`, timestamps,
prefixed labels and tags) — none is drawn, and ARM D0b still reads the real wire on every run.

**The same class, measured and NOT fixed here (scope):** `civicos-ui/test/ai-session-wire.test.mjs` L166–169 draws
FETCH_ALLOWED 3001..6999 / FETCH_CONSUMED 1007..1499 / SUBS_ALLOWED 7001..9973 / SUBS_CONSUMED 2003..2999, whose ARM D0b
collides **928 in 2,000,000 (1 in 2155)** — fetches remainder = SUBS_CONSUMED (520) and subsessions remainder =
FETCH_ALLOWED (408). Measured with the same predicate over its four figures plus small integers 1..3.
