## M-112 · 2026-09-23 · M0-127 — a RED gate names every cause: the verdict annotation on a planted residue and a planted coverage failure, driven through the REAL gate

Instrument: `node` 26.10.0 in the cloud container (4 cores, shared); the REAL `node tools/gates.mjs` (FULL, derived) in a
scratch `git worktree` of `land/worker/M0-127` @ `d3bc6d47` plus ONE planted commit (never pushed): a suite
`bio-plane/test/zzm0127-plant.test.mjs` that leaves `bio-battery-<its pid>-plant/miniflare-e2e0` in the battery's
`$TMPDIR` and declares no negative control. Its log was then handed to `tools/gateverdict.mjs` exactly as the workflow's
gate step hands it (`--log --tree --exit --t0 --t1`, with a step-summary file). **The GitHub Actions runner was NOT
exercised**: this is the workflow's verdict logic run locally on a real gate's log.

| what | read |
| --- | --- |
| battery headline | `282/286 suites green · 1 skipped · 17129 assertions passing · EXCLUDES 2 untallied suite(s) · 1772.1s` |
| the residue, by the battery | `RESIDUE (D-186): /tmp/bio-battery-32036-99w5Uo/bio-battery-12414-plant — 1 miniflare sandbox(es) — left by zzm0127-plant.test.mjs (pid 12414, by the pid in its name)`; the plant's result line ends `· pid 12414` |
| the gate's line | `gates: CAUSES plane:hygiene.test.mjs plane:owed-controls.test.mjs plane:statepaths.test.mjs residue:/tmp/bio-battery-32036-99w5Uo/bio-battery-12414-plant:by=zzm0127-plant.test.mjs:pid=12414 step:coverage--strict:exit=1`, then `RECORDED RED` (tree `a8c0df21`, class FULL), exit 1, wall 2014 s |
| the annotation | `VERDICT=RED TREE=a8c0df21… CLASS=FULL EXIT=1 WALL=2014s FAILED=` the same five tokens; the writer exited 1 |
| what the pre-M0-127 writer would have read | `FAILED=hygiene.test.mjs owed-controls.test.mjs statepaths.test.mjs` — the residue and coverage --strict unnamed; with no suite failing (6ef503c4's shape) it read `FAILED=none` |

The three suite failures were the plant's own consequences, each by name: `hygiene` (the plant imports no `stdio.mjs`),
`owed-controls` A13b (coverage --strict exits non-zero over a suite with no control), and `statepaths` (its unit ceiling
of 60, which this branch then moved to the printed figure — 61 on the branch, 62 once merged with M0-100). The local
RED record for the plant tree `a8c0df21` sits in the shared git common dir; the tree was never pushed.
Suite-level proof, including the liar (the old writer restored reads `FAILED=none` over a RED again): the
`NEGATIVE CONTROL:` line of `bio-plane/test/gateverdict.test.mjs`.
