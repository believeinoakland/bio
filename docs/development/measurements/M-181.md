## M-181 · 2026-09-25 · D-615 — THE RECORD'S HEAD ROWS AGREE WITH THEIR DOCUMENTS ON BOTH DATES: **0 `created` and 0 `last_updated` drifts of 31 in `bio`**, no respelled instants, no head document stating neither date; `scratch` holds no bundles. (WORKER D-615, SCHEDULER #23)

**What was asked.** D-615 derives `bundles.created`, `bundles.last_updated`, the manifest row's `created` and a minted
content row's `at` from the promoted DOCUMENT and refuses an envelope that contradicts it (C-86.7). The row's scope says
to take the census of live drifts FIRST, as M-172 did, before the refusal: an internal writer that relabels from the ROW
(`provenanceChainRebuild`) would meet the refusal on any row an envelope wrote apart from its bytes.

**Instrument.** `tools/m181-dates-census.mjs`, node, cloud container, worktree `/home/user/wt-D615` on
`land/worker/D-615` (base `land/worker/D-546` @ b690552a), 2026-09-25, against the instance named by `BIO_INSTANCE`. READ
ops only, each naming its store: `op=list` (its `total` equals the count listed, so the viewer gate hid nothing),
`op=projection id=<bundle>` for the row's two dates, `op=image` for the HEAD `bundle.md`, parsed by the catalogue's own
`parseFrontmatter`. Two dates agree when both parse to the same instant, else when their trimmed text is equal — the rule
`promote`'s refusal uses. **WITNESS:** `op=stats` before and after each register, identical (bio 31 bundles, 142 files,
244 history rows; scratch 0 bundles).

**RECORDED, NOT SMOOTHED.** The first draft compared `op=list` rows, and `op=list` carries no `created`: 31 of 31 rows
had no date to compare, so its "0 drifts" was equal by construction. The instrument now reads each row from
`op=projection`, counts rows lacking either date (0), and prints a sample pair (INFO-2026-0001-sewer-transfer-series: row
and document both `2026-07-18T22:00:00Z` / `2026-07-20T18:58:01Z`).

| register | bundles | heads read | blob-held | row states no date | document states no date | `created` differs | `last_updated` differs | same instant, respelled |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| bio | 31 | 31 | 0 | 0 | 0 | **0** | **0** | 0 |
| scratch | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

**Consequence taken in the same landing.** Nothing live stands for the refusal to meet, so it is a refusal, not a
record-per-census. `provenanceChainRebuild` still sends the two dates only where the carried `bundle.md` states none
(`#rowUnlessStated`, D-563's helper), so a future drift would be corrected by a rebuild, not refused.

**The plane's own writers.** Read at the code, every internal `promote` caller in `src/store.mjs` and `src/index.mjs`
(24 sites: 23 in `store.mjs` beside the op dispatcher, and the monitor tick in `index.mjs`): each stamps the `last_updated` it sends into the bytes it sends (`#setScalar(text, "last_updated", …)` or a
freshly composed document) and sends the document's own `created`, EXCEPT `forkProject`, which sent `created: when` over
bytes keeping the ORIGIN's `created` — the row took the fork's time and the bytes said the origin's. The fork now writes
its own `created` into its bytes (arm `fork-unstamped` of the control shows the refusal catching the unstamped fork).

**The battery's callers.** The first full battery on the fix (389 suites) went red in 25: 22 plane suites and 1 UI suite (published-index-pair) whose promote helpers labelled a fixed `created: NOW, last_updated: LATER` over bytes stating other dates — mostly a plane-written revision (conclude, divide, ratify) re-promoted with its own freshly stamped `last_updated`. Attributed by a temporary `console.error` at the refusal (the store copied aside and restored, sha256 550b5376… and `cmp` equal after): every one non-replay, no mechanical writer, a suite's author. Each helper now labels the document's own dates (`datesOf`), a CORRECTED comment at each; `d546-state-edge` dated its manifest rows through the envelope's `when`, so its helper writes `when` into the bytes instead (its control re-run: 9/9 AS DECLARED, the figures D-546 recorded). The other two reds were instruments moved by name: pen-sweep's UNCLASSIFIED ceiling 21 -> 22 (this item's control driver) and the refusal-code floors (nine keys, from `check-refusal-codes.mjs --strict`'s print).

**A defect found, minted, not fixed here: D-692.** A REVISION whose bytes restate `created` (driven in a scratch probe: created 2026-07-24, revised bytes saying 2020-01-01) LANDS, and `bundles.created` keeps the creation's value — the `ON CONFLICT` arm never writes `created` — so the row and the head bytes disagree about when the bundle was made. The fix is a choice between refusing a revision that restates its creation (C-86.2's shape, one field over) and letting it move the row (which lets any writer backdate a creation); routed to SCHEDULER. Live, the table above shows no such row.

**What the matchers cannot see.** The census reads HEADS only: a drift in an earlier version is not a live row, and the
manifest rows' `created` are not compared (they are D-674's). It cannot see a bundle `op=list` does not return to an
admin viewer. The writer read is by reading the code, not by instrumenting it; a client outside this repository that
labels arbitrarily would meet C-86.7 and is not counted.
