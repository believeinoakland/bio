## M-177 · 2026-09-25 · D-578 — NO BUNDLE IN THE LIVE RECORD CARRIES A BLANK OR NON-STRING TYPE: **31 of 31 in `bio` are `information` (29), `inquiry` (1), `project` (1); `scratch` holds none.** And through op=promote on the unfixed tree, a promotion stating no type answered a RAW STACK, while one whose envelope type was blank LANDED typed `'  '`. (WORKER D-578, SCHEDULER #22)

**What was asked.** D-578 carries a head's type into a revision that states none, refuses a creation that states none
(PROMOTED_TYPE_UNSTATED, C-86.5), and stops taking a blank or non-string `meta.object_type` as a type. The unfixed
derivation (`normalizeType(meta.object_type)` as the fallback) could write `''`, `'  '` or a number into
`bundles.object_type`; the question is whether the record already holds one, which D-578 would not rewrite.

**Instrument 1 — the live record.** `tools/m177-type-census.mjs`, node, cloud container, worktree `/home/user/bio` on
`land/worker/D-578`, 2026-09-25T05:55Z, against the instance named by `BIO_INSTANCE`. READ ops only, each naming its
store: `op=list` paged at 500 (its `total` equals the rows listed, so the viewer gate hid nothing), each row's
`object_type` bucketed as a string, a blank string, or a non-string. **WITNESS:** `op=stats` before and after each
register, identical.

| register | bundles | `information` | `inquiry` | `project` | blank | non-string |
| --- | --- | --- | --- | --- | --- | --- |
| bio | 31 | 29 | 1 | 1 | **0** | **0** |
| scratch | 0 | 0 | 0 | 0 | 0 | 0 |

**Instrument 2 — op=promote on the unfixed tree.** `bio-plane/test/d578-typeless-revision.test.mjs` pointed
(`D578_SRC`) at `land/worker/D-563` 30cac9a6's own `src/` and `checks/`: **8 pass / 10 fail**. A typeless revision and
a typeless creation each answered `{ ok: false, error: "Error: NOT NULL constraint failed: bundles.object_type ...
at act (file:///.../src/store.mjs:18748:16) ..." }`; a typeless inquiry revision under `meta.object_type: ''` was
refused REVISION_RETYPES_BUNDLE naming a revision type of `''`; a typeless creation under `meta.object_type: '  '`
**landed**, typed `'  '`. On the fixed tree the same suite reads 18/0.

**Instrument 3 — the sweep for the class (the rest of `bundles`' NOT NULL columns).** A scratch driver through
op=promote on the unfixed tree: `current_state` stated nowhere (creation and revision), `meta.created` absent, and
`meta.last_updated` absent each answered the same raw NOT NULL error with a stack; `meta` absent or `null` is refused
MALFORMED; `meta` a string reaches the raw error on `created`; `group_id` is D-436's and `bundle_sha` is behind
NO_BUNDLE_MD. D-578 does not change them — minted **D-628**. Separately, the Durable Object's `fetch` catch serialises
ANY throw as `String(e.stack)` onto the answer, which is why a constraint error reaches a caller as a stack at all —
minted **D-629**.

**What this cannot see.** Instrument 1 reads the projected ROW, not the head document; a row typed correctly over a
document stating another type is M-156's question (none). A bundle the list does not return is not counted
(`total` equals rows, so none).
