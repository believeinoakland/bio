## M-115 · 2026-09-23 · `ai-session-wire.test.mjs`'s fixture draw — how often it went red by the draw, that the corrected draw cannot, and whether a repeated snap key moves any suite

Instrument: `node` 26 in the cloud container (shared; load average ~9 on 4 cores, several gates live), measured by the
M0-132 worker in worktree `agent-a79c71128123e82b8` on `origin/main` @ `d89e04d1` + the M0-132 commit `cf0aebf0`. Every
figure below was PRINTED by a run; none is computed from another. The harness scripts were scratch (outside the tree);
the permanent instrument is the suite's own ARM D0c, which re-runs the proof and 500 draws on every pass.

| arm | draws / runs | red by the draw | what it measured |
| --- | --- | --- | --- |
| OLD draw (FETCH_ALLOWED 3001..6999, FETCH_CONSUMED 1007..1499, SUBS_ALLOWED 7001..9973, SUBS_CONSUMED 2003..2999, OTHER_* = FETCH_* + 1000..2000), in-process, through `wireCollisions()` (every arm the draw moves) | 2,000,000 | **2,194 (1 in 912)** — D0b-shaped 1,805, W7c 277, W7b 112 | the rate at which the suite could go RED BY THE DRAW |
| OLD draw, ARM D0b's OWN published set only (the live wire: the four budget figures and `ticks` 3) | 2,000,000 | **866 (1 in 2,309)** | agrees with M-114's 1 in 2,155 (928) for D0b alone; the W7b/W7c reds were unmeasured before this row |
| NEW draw (`WIRE_RANGES`), in-process, through `wireCollisions()` | 2,000,000 | **0** | — |
| NEW draw, **500 consecutive runs of the WHOLE SUITE**, each a FRESH `node` process (its own unseeded `Math.random`) against the real plane in miniflare, suite `fbddb359…`, fixture `1cc84c6c…` | 500 | **0 — 500 of 500 exit 0 with `ai-session-wire: 86 pass, 0 fail`**, 500 distinct fixtures, 0 without a tally; wall 2,701 s | the accepts-when figure: the suite itself, not a model of it |
| FORCED collision (FETCH_ALLOWED 5500, FETCH_CONSUMED 1200, SUBS_CONSUMED 4300), the suite | 1 | **2 FAIL: ARM D0b by name** + D1 (`fetches remainder (4300)`) | the suite names a collision |

**Interval proof (`wireRangeProof()`):** every band is disjoint from every other — ticks 0..9, RUNTIME 2..5, FETCH_CONSUMED
1007..1499, fetches remainder 1502..2992, FETCH_ALLOWED 3001..3999, SUBS_CONSUMED 4003..4499, subsessions remainder
4502..5970, OTHER_CONSUMED 6001..6999, OTHER_ALLOWED 7001..8999, SUBS_ALLOWED 9001..9973; percents 25..50 and 40..50;
1dp percents carry a ".". FETCH_ALLOWED and OTHER_ALLOWED are exactly four digits and every figure beside them has at
most four, so W7b/W7c's substring test reduces to equality, and both avoid the clock's 2000..2999 year band. Tags are
drawn from letters only. **What it does not reach:** the plane's own scalars (`ticks`, timestamps, labels); ARM D0b
still reads the real wire on every run.

**The snap keys, and a SURPRISING GREEN recorded rather than smoothed.** Every corrected suite was run with its counter
FROZEN (`++snapKeySeq` -> `snapKeySeq`, so every key a suite builds for one bundle repeats), each ALONE, restored by
copy and verified by sha256 and cmp: **18 of 18 stayed GREEN** (e.g. `versions.test.mjs` 78/78, which re-promotes one
inquiry several times). So no suite today reads the rows a repeated key replaces, and the old `Math.random` collision
was SILENT in them rather than a red. The reason is the plane's, and it is a finding: `promote` writes `manifest` and
`history` with `INSERT OR REPLACE`, so a second promotion reusing a bundle's snap key is ACCEPTED and REPLACES the
first's rows (versions' over-strictness arm `same.ok === true` with the key repeated).
