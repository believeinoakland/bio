## M-124 · 2026-09-24 · D-464 — do a member's COUNTS move when a project that member cannot see is created and revised? Before and after, at the op

Instrument: `bio-plane/test/project-sight.test.mjs` §8, driven through the control plane in miniflare (`node` in the cloud
container), worktree `/home/user/bio` on `origin/main` @ `15b2a4c0`. The reader is `vera`, an enrolled member never invited;
the acts are a project CREATED by the ADMIN token, an owner claimed, a member invited, a REVISION citing a second document,
and (for `op=selectionlist`) the owner's selection of it. The witness is the ADMIN token's own answer, which moves. Every
figure below was PRINTED by a run.

**BEFORE (the unedited `store.mjs`, §8 added and run first): `project-sight: 115 passed, 7 failed`.** vera's `op=stats`
moved on `bundles, files, history, refs, indexed, projectParticipants` (printed by name), and both of her
`op=searchindexcheck` reads moved by digest (`counts.indexed`; her `indexed` no longer equalled her `keyed`).
`op=selectionlist`'s `bytes` was not in §8 on that run; its move is measured instead by the `selectionbytes-whole` control
arm (the fix reverted for that one figure: the digest arm fails, 124/1).

**AFTER (`Store#counts` through the caller's `viewerPredicate`; `searchIndexCheck`'s `indexed` and `selectionList`'s `bytes`
likewise): `project-sight: 125 passed, 0 failed`.** Every one of vera's four count reads is byte-identical by digest across
the acts; her `indexed` equals her `keyed` and `ok` holds; a document she CAN see still moves `bundles`, `files`, `register`
and `indexed` by exactly 1 each; the MEMBER token and an enrolled administrator (ruth) count exactly what the ADMIN token
counts; and the ADMIN token's `bundles` less vera's is exactly the number of bundles her `op=list` does not show — 4 on this
fixture (the two hidden projects, iris's fork, REC-171's fixture project).

**Not measured:** a deployed instance; cost (every gated counter adds one `EXCEPT` over `bundles` per statement, about 30
statements per `op=stats` for a filtered member session and none for an unfiltered credential — not timed).
