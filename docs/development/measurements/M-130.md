## M-130 · 2026-09-24 · UI-84 — WHAT MOVED IN DEC-49's PRE-AUTHENTICATION SUBJECT WHEN D-278's CANNED TRANSLATIONS DISPLACED TWO PLANE SENTENCES

*Minted `node tools/mintid.mjs M` by UI-84 (cloud worker, CONDUCT #20), base `origin/main` 548eb2c5.*

**Instrument:** `node civicos-ui/test/preauth-vocabulary.test.mjs`, run alone from the repo root, exit read
unpiped, on a quiet tree. The figures below are the ones the instrument PRINTED on each tree — not one number
is derived by subtracting another. Both runs are of the SAME file on the SAME app.html; only the two mock
refusals and the arms reading them differ.

**This entry sets no cap and moves no floor.** It records a movement, and the reason the movement is a result
rather than a regression.

### Headline

**DEC-49's subject SHRANK, and nothing arrived.** Plane vocabulary reaching an unauthenticated reader fell from
**11 of 21 surfaces to 10**, and from **77 occurrences (67 visible) to 76 (66)**. The whole delta is **one
source on one term**. Every other line of the report is character-identical, diffed line by line.

### The two runs

| figure | OLD (548eb2c5, this item's base) | NEW (UI-84) |
| --- | --- | --- |
| assertions | 78, all green, exit 0 | 84, all green, exit 0 |
| scenarios | 16 | 16 |
| surfaces walked | 21 | 21 |
| characters of pre-auth surface | 50,669 | 51,340 |
| inherited terms in the basis | 74 | 74 |
| plane-vocabulary terms | 15 | 15 |
| surfaces they reach | **11 of 21** | **10 of 21** |
| occurrences (visible) | **77 (67)** | **76 (66)** |
| `"sha256"` | x35 (31 visible), plane sources `{#pub-body, #v-refused}` | x34 (30 visible), plane source `{#pub-body}` |
| plane-sourced rows in `DEC49_SUBJECT` | 12 | 12 |

### Why it moved, and why that is the ruling working

`op=verify`'s malformed-hash refusal said **"verify requires sha256=<64 lowercase hex>"** — a sentence addressed
to a CALLER OF THE OP. The word `sha256` in it is the entire reason `DEC49_SUBJECT` has carried
`case-address-at-load #v-refused` since UI-37 (2026-08-04). D-278 (2026-09-23) mints that refusal through
`requiredArgument`, so C-61.1's **canned translation** now travels with it; `refusalWords` (UI-73) renders a
translation FIRST; and the canned sentence is addressed to a PERSON and names no wire argument. So the plane
vocabulary at that pane went to zero.

- **The +671 characters are the canned sentences being LONGER than the terse ones they displaced.**
- **The −1 occurrence is plane vocabulary being DISPLACED by them.**

This is DEC-49's own branch (a) arriving one refusal at a time: *the plane learns member-facing wording, so the
plane-sourced rows disappear AT THEIR SOURCE.* It is recorded as a shrink rather than smoothed, because a table
that only ever grows cannot show a ruling working.

### What is NOT claimed

**DEC-49 is still OPEN and `REPORT_ONLY` is unchanged.** Two codes of the plane's 592 gaining a sentence is not
the ruling being answered, and **eleven plane-sourced rows still stand on these surfaces**. The three-sentence
rule (BOB #17, 2026-09-19) is untouched here.

### What this measurement cannot see

- It is taken through the suite's **mock** plane, not through a live wire. The mock's fidelity is the item's
  subject: the code is read out of `bio-plane/src/index.mjs` at the site that mints it and the sentence out of
  `bio-plane/checks/bio-checks.mjs` under that code, so no value in it is a hand copy — but a mock derived
  correctly from a source is still not the source. **The wire half is driven through the op by
  `bio-plane/test/d278-codeless-refusals.test.mjs` section 3**, which is where the claim that the plane really
  sends these rows is tested; this entry measures only what a surface RENDERS from them.
- The term basis is **harvested**, not hand-extended. A word in the canned sentences that no sibling sweep
  polices is invisible here, exactly as UI-40 recorded for `plane`. Neither canned sentence contains any of the
  74 inherited terms or a structural acronym — **measured**, by the term count not moving (15 before, 15 after).
