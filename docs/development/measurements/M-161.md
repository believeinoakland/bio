## M-161 · 2026-09-25 · D-535 — which units does a MEASUREMENTS-only diff select once the plane cites the ledger as prose?

Instrument: `node tools/gates.mjs --explain` under `BIO_GATE_RESULTS=off` (selection only), M-146's method: an
ISOLATED CLONE (`git clone --shared`) in the session scratchpad, the tree under test committed there, its
`refs/remotes/origin/main` moved to that commit, then ONE line appended to `docs/development/MEASUREMENTS.md`. For the
TARGETED view an empty root file nobody names is planted too (M-106's method, `statepaths.test.mjs`'s unit-count arm).
Node 26, the cloud container. Base: `origin/main` @ `964da679`.

**THE CLASS, SWEPT — not the row's four.** The code of every `.mjs` under `bio-plane/src` and `bio-plane/checks`
(37 files), comments blanked by `walkfloor.mjs`'s `stripComments` (the gate's own lexer), held **14** spellings of
`MEASUREMENTS.md`: the row's four (`bio-checks.mjs` C-2.8's repair text, three in `index.mjs`), plus `store.mjs` ×3,
`schema.mjs` ×5 (SQL `--` comments inside the schema template string, which the JS lexer reads as string) and
`odf.mjs` ×2. None sits inside a `DEC-49 REGION`; the `bio-checks.mjs` one is a check finding's repair line, not a
`refusal()` translation. All 14 now read "the MEASUREMENTS ledger".

**DOCS class (the ledger alone), `N of 43 doc-facing unit(s) take a changed path`:**

| tree | taken | who left |
| --- | --- | --- |
| `964da679` (before) | **40** | — (M-146's 39 plus one doc-facing unit landed since) |
| + the row's four (`index.mjs`, `bio-checks.mjs`) | **38** | `calibration`, `rec-182-created-tie` — the row's predicted −2 |
| + `store.mjs` alone, `schema.mjs` alone, `odf.mjs` alone | 40 each | none: each unit also reaches `index.mjs` |
| + all fourteen | **32** | the two above and `affordances`, `caselifecycle`, `casepin`, `caseproduction`, `ui:connections-sidebar`, `ui:member-respect` |

**TARGETED view (M-106's method): 41 → 33 units of 446**, the same eight by name; MEASUREMENTS readers stay 29.

So the row's accepts-when ("37 units") was written against a 39 that is now 40; the row's own change moves it by the
predicted two (40 → 38), and the class sweep takes it to 32. `calibration.test.mjs` leaves in both views.

**NEGATIVE CONTROL** (`bio-plane/test/statepaths.control.mjs`, arm e): the `index.mjs` OCR cost sentence given its file
name back → the static pin fails naming `bio-plane/src/index.mjs:7322`, `calibration` returns, the DOCS-class ceiling
(32) and the TARGETED ceiling (33) fail by count (TARGETED reads 40). Arm f (another prose spelling) stays green.

**WHO ELSE READ THE CHANGED TEXT (§5), measured at the first gate (363/366).** Two suites pin sha256 digests of a
text chain whose layer step carries `index.mjs`' `LAYER_FIDELITY_SOURCE`: `drive-convert.test.mjs` (four pins) and
`capture-container-extent.test.mjs` (the PDF reading). With ONLY that one string reverted in `index.mjs`, both passed
their old pins (43/0, 56/0) — the attribution is exact — so the pins were corrected to the digests the D-535 tree
prints, with the reason at each site. `drive-convert.control.mjs` re-run after: baseline 43/0, letter 17/26, omit
27/16, sequence 41/2, overstrict 39/4, its recorded figures exactly. `nc-cap12.mjs` re-run: every arm but
`dropslides` armed; `dropslides` NEVER ARMED (its `index.mjs` anchor is absent on `origin/main` too) — D-600.
The third failure was `hygiene.test.mjs`'s walk census naming the new scan: it is now GUARDED through
`scripts/provenance.mjs`, and the census floor moved 44 -> 45 from its print.
