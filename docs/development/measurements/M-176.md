## M-176 · 2026-09-25 · FW-24 — THE DOCUMENT-TYPE CENSUS OVER THE WHOLE CORPUS, BOTH HALVES: a 2,000-document body sample of all **29,607** text-bearing items (bucket 28,915 + Legistar 692). The counts move a little and **the order in EXTRACTION-BREADTH §2 does NOT move.** Every separation M-143 found holds; the two ties M-143 found are still ties. FINANCIAL REPORT counts **6 of 2,000, ~89 ± 71**, still the smallest class and separated from budget_dataset (z = 4.95). (WORKER FW-24, SCHEDULER #22)

**What "whole corpus" means here, and why the brief's `M032_HALVES=bucket` was not used.** The row says the census
"can run over the whole corpus (`M032_HALVES=bucket`) instead of the sampled halves". In the instrument,
`M032_HALVES=bucket` does the opposite: `load_pop` KEEPS only the named halves. FW-22 set it to drop the Legistar half
so that its recount would redraw D-66's sample. The row's own premise is that Legistar now answers, so the census can
include it. **This run names both halves, `M032_HALVES=bucket,legistar`**, which is the same population as leaving the
variable unset, and says so explicitly. The bucket-only figure is M-143's and is not re-taken here.

**Instrument.** `tools/m032-class-census.py` (M-18, M-126, M-143, M-152). Python 3 stdlib and node, cloud container,
worktree `/home/user/bio` on `land/worker/FW-24` from `origin/main` @ `5e8a65a8`. **`classhash` reads `161d2ff9…`, 38
definitions, 26,117 bytes**, the same digest as M-143 and M-152, both before the walk and on the committed file, so no
threshold differs from FW-22's. FW-24 changed one print in `derive` and no classification code: the paired-comparison
header printed a literal "600 documents" (M-18's n) through three later walks, and now prints the walk's n. The pen is
in the session scratchpad and is not committed. Its figures are reproduced below, and the six financial reports are
named.

    M032_PEN=<pen> python3 tools/m032-class-census.py list                                  # 03:59–04:02Z, exit 0
    M032_PEN=<pen> M032_HALVES=bucket,legistar python3 tools/m032-class-census.py names     # exit 0
    M032_PEN=<pen> M032_HALVES=bucket,legistar python3 tools/m032-class-census.py bodies 2000 20260924
                                                                  # 04:02:37Z–05:37:46Z, 5,708 s, exit 0
    M032_PEN=<pen> M032_HALVES=bucket,legistar python3 tools/m032-class-census.py derive    # every figure below, exit 0
    M032_PEN=<pen> M032_HALVES=bucket,legistar python3 tools/m032-class-census.py control   # 110 arms, 0 failed

**Politeness, stated.** The walk is serial: one request in flight at a time, a 0.25 s pause after each document, and
0.25 s between Legistar matter calls in `list`. So each host sees fewer than 4 requests a second, and in practice about
one every 2.9 s on average (2,000 documents in 5,708 s). The 697 PDFs this instrument could not read went to the
plane's reader locally in miniflare. That makes no further request to the source.

**Reach, by host and hour.** `cao-94612.s3.amazonaws.com` answered the listing and every body: 43,283 keys, 0 of 2,000
fetches failed. `webapi.legistar.com` answered at 2026-09-25 03:59Z (HTTP 200, 0.46 s) and throughout `list`: **781
attachments over 250 matters.** Of those, 692 are text-bearing and all 692 carry a url. The other 89 have names with
no extension, so the census counts them as `unknown media kind` and does not open them. **Nothing was refused, and no
host was denied by the network policy.** The 3 `EMPTY` answers below are the plane's own acquire refusing bytes it
fetched, not the source refusing.

**Corpus.** 44,064 items: 43,283 bucket keys + 781 Legistar attachments. The text-bearing stratum is 29,607; the rest is
media · image 14,276, unknown media kind 104, container · archive 42, media · audio/video 35. The sample is **2,000,
seed `20260924`**, drawn over the whole text-bearing stratum. Adding the Legistar half changes the population, so this
is a NEW DRAW. It is not D-66's sample, and its documents are not M-143's. The shuffled walk order makes any completed
prefix a uniform sample; the walk completed.

### THE COUNT — whole corpus, both halves

Body sample, n = 2,000, scaled to 29,607; interval ± 1.96 binomial SE. M-143's bucket-only figures (n = 1,000, scaled
to 28,915) are beside them for comparison, not as a baseline this run was drawn against:

| class | in sample | rate | scaled | ± 95% | M-143 (bucket only) |
| --- | --- | --- | --- | --- | --- |
| agenda *(registered — control)* | 269 | 13.5% | **3,982** | 443 | 3,817 ± 607 |
| staff_report | 216 | 10.8% | **3,198** | 403 | 2,920 ± 540 |
| minutes | 187 | 9.3% | **2,768** | 378 | 2,660 ± 518 |
| ordinance_res | 125 | 6.2% | **1,850** | 314 | 1,388 ± 383 |
| directory | 41 | 2.1% | **607** | 184 | 694 ± 274 |
| budget_dataset | 39 | 1.9% | **577** | 179 | 463 ± 225 |
| **financial_report** | **6** | **0.3%** | **89** | **71** | 58 ± 80 |

Conservation: named-class 447 + other 977 + multi 190 + unclassified 386 = 2,000, OK. Every interval narrowed, since
n doubled. The largest move is ordinance_res (+462), which the Legistar half explains below.

**Paired comparisons (the order test):**

| pair | discordant | z | verdict | M-143 |
| --- | --- | --- | --- | --- |
| agenda > staff_report | 207 vs 154 | 2.79 | SEPARATED | 2.39 |
| staff_report > minutes | 188 vs 159 | **1.56** | **NOT separated** | 0.70 |
| minutes > ordinance_res | 171 vs 109 | 3.72 | SEPARATED | 3.95 |
| ordinance_res > directory | 123 vs 39 | 6.67 | SEPARATED | 2.88 |
| directory > budget_dataset | 41 vs 39 | **0.22** | **NOT separated** | 1.27 |
| budget_dataset > financial_report | 39 vs 6 | 4.95 | SEPARATED | 3.32 |

### WHAT THIS DOES TO §2'S ORDER: NOTHING, and why

§2's rule is *"minutes first unless the count says otherwise"*. The count still does not say otherwise: staff reports
lead minutes on the point estimate, as they did in M-18 and M-143, and the paired test **still does not separate
them**. The z rose from 0.70 to 1.56 with n doubled. That is a trend, and it is not a separation. **So the structural
argument stands and minutes stay first.** If a larger draw ever separates staff_report above minutes, that is the
reading that would move row 1. Directory and budget_dataset stay tied (z = 0.22). They keep §2's rows 4 and 5 in the
order the design already gives, which the count neither supports nor overturns. Financial report stays last and
separated. **§2's table is not reordered.** The design's §2 gains one sentence citing this entry.

### THE LEGISTAR HALF, which the order had never seen

48 of the 2,000 are Legistar attachments, 2.4% against a 2.3% population share. By body they read: **ordinance_res
21, staff_report 16, agenda 3, budget_dataset 2**, and 11 unclassified (10 read with no class met, 1 no text layer).
The bucket half's 1,952 read agenda 266, staff_report 200, minutes 187, ordinance_res 104, directory 41,
budget_dataset 37, financial_report 6. **The Legistar half holds no minutes and no directories.** Its attachments
belong to legislative MATTERS, so it is rich in the instruments and the staff reports that carry them. That composition
is why ordinance_res rose the most. It is a fact about how COFF-6 draws the Legistar half (the 200 most recently
modified matters plus two 25-matter windows from 2015–16). It says nothing about Oakland's legislative record as a
whole.

**Ground truth.** Each sampled Legistar attachment carries the publisher's declared `MatterTypeName`. Of 26 declared
`City Resolution`, the body recogniser reads 12 as ordinance_res, 6 as staff_report, 1 each as agenda and agenda +
staff_report, and 6 as no class. Of 16 declared `Ordinance`, it reads 6 as ordinance_res, 5 as staff_report, 1 each as
staff_report + ordinance_res, agenda + ordinance_res, budget_dataset, and staff_report + ordinance_res +
budget_dataset, and 1 as no class. The other 6 are 5 `Informational Report` (2 staff_report, 3 no class) and 1
`Report and Recommendation` (no class). **The declaration is the MATTER's type, not the attachment's**: a resolution matter carries a staff
report as an attachment, and the recogniser is right to call that a staff report. So a disagreement here is not by
itself an error. How many are is UNDETERMINED without reading them, and this row did not.

### FINANCIAL REPORT — the six, by name

| document | classes | reader | families |
| --- | --- | --- | --- |
| `documents/OAK071404.pdf` | financial_report | instrument tier 1 | money · self-naming · spine · ended |
| `documents/OAK062141.pdf` | staff_report + financial_report | instrument tier 1 | money · self-naming · opinion · spine · ended |
| `documents/OAK025620.pdf` | financial_report | instrument tier 1 | money · self-naming · opinion · spine · ended |
| `documents/SSOC-Agenda-and-Materials-3-27-23-FINAL.pdf` | staff_report + financial_report | instrument tier 1 | money · opinion · ended |
| `documents/OAK024840.pdf` | financial_report | instrument tier 1 | money · self-naming · opinion · spine · ended |
| `documents/Oakland-Redevelopment-Successor-Agency-FY22-Audited-Financial-Statements.pdf` | financial_report | plane tier 2 | money · opinion · spine · ended |

**OAK071404 and OAK025620 are M-143's rows 14 and 15 again**, drawn anew. The ORSA FY22 statements are one of FW-22's
thirteen calibration documents, which were read before FW-22's walk and fixed its thresholds; here it came up in a
random draw. All six are bucket keys. The SSOC agenda packet is a meeting's materials that carry an auditor's opinion,
so the census reports it as multi-class, which is D-66's rule and not a defect.
**Recount, by the instrument:** D-66's budget arm took 20 of the 2,000, BOB #32 excludes 6, the recounted arm keeps 14.
All 6 financial reports came from D-66's budget count, and 0 are ones that arm never had.

### READERS

1,303 instrument tier 1 · 356 plane tier 1 · 176 plane tier 2 · 65 labelled plane tier 3 · 97 plane with no tier ·
3 plane acquire refused (`EMPTY`: two copies of the Special-Assessment-Exemption application and one Administrative
Hearing questionnaire). 13 plane texts were REFLOWED (D-481). **D-557 still stands**: a document labelled "tier 3" is
judged on the plain `op=pdfstructure` text, which stops at tier 2, so OCR text never reaches `judge`. Those documents
land in UNCLASSIFIED, `no text layer` (257 here), so the classes are UNDER-counted, not over-counted, by an amount this
run does not measure. Tier 1 alone, which is M-18's reader and is comparable with M-18: agenda 222, staff_report 178,
minutes 157, ordinance_res 96, budget_dataset 34, directory 21, financial_report 5, unclassified 709.

### NEGATIVE CONTROL (the row's arm, ALONE, declared before it ran; restored by `cp` from the uniquely-named pristine copy `fw24-pristine-N1.py`, verified by sha256 AND `cmp`, 145,557 bytes, floored at 100,000)

Baseline, no arm, on this sample: **110 arms driven, 0 failed**, exit 0. The instrument before the arm had sha256
`d7434895…`.

- **N1 — FOLD FINANCIAL REPORT BACK INTO BUDGET** (`_fw22_budget` drops BOB #32's conjunct). DECLARED to fail by name
  at *"ON THE REAL SAMPLE the RECOUNT MOVES: at least one document D-66 counted as budget_dataset is an audited
  financial statement and is now counted apart"*, and nowhere else. The three sibling real-sample arms, including
  *"…exactly D-66's minus the moved documents"*, MUST pass (20 vs 20 − 0). **Result: 110 arms, 1 failed, exactly that
  one**, exit 1. **`derive` under the arm prints the class count moving by name:** the recounted budget arm reads
  **20, not 14**, "BOB #32 excludes **0**", and the six documents named above leave the moved list, which reads
  "(none …)". `classhash` stayed at `161d2ff9…`, as FW-22 recorded for the same arm, because the edit is in the
  re-derivation, not the classification path.
- Restored: sha256 `d7434895…` by hash and by `cmp`, 145,557 bytes; **110 arms, 0 failed**, exit 0.

**A limit of the arm, stated rather than smoothed.** N1 moves the RECOUNT the instrument derives from recorded
families. It does not move the scaled table's budget_dataset (39), which reads the walk's recorded verdicts. Folding
those would need a re-walk under a recogniser edit, which is FW-22's arm N2, and on this sample N2 would be seen only by
the fixture arms. The arithmetic (39 + 6 = 45, ~666) is stated here, not printed by the instrument.

### What this count cannot see

M-143's list stands unchanged: financial reports with no text layer (2 of FW-22's 13 calibration documents), the
image stratum (14,276), a dataset printed into a PDF, legacy `.xls`, and D-481's reflowed text. Added here:
- **The 89 Legistar attachments whose names carry no extension** are counted as `unknown media kind` and not opened.
- **The Legistar half is COFF-6's draw of matters, not Oakland's Legistar.** Its share of the corpus is set by that
  draw.
- **The raw pen is not committed**: 2,000 per-document records (3.4 MB) and a population that the next `list` will
  not reproduce exactly, because "the 200 most recently modified matters" moves. The figures above are what `derive`
  printed. A re-derivation needs a re-walk.
