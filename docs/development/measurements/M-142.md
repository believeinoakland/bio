## M-142 · 2026-09-24 · D-480 — what does an uninvited member lose when two people she cannot see cite the same questions? Before and after, at the op

Instrument: `bio-plane/test/project-sight.test.mjs` §10, driven through the control plane in miniflare (`node` in
the cloud container), worktree `/home/user/bio` on `origin/main` @ `58293bf3` merged with `land/worker/D-497` @
`29d8409d`. The reader is `vera`, an enrolled member. She owns two projects that share one question, one of them
standing on a reading and the other on nothing, so her `op=queue` carries exactly one
`stance-changed-here-not-elsewhere` FINDING. The act is `iris` creating TWO projects vera is never invited to,
each citing the same 70 filler questions — ids that sort BEFORE her own question, and that name no bundle at all,
which is the ordinary shape of a `references[]` entry in this record. The witness is the ADMIN token's own queue,
which moves. Every figure below was PRINTED by a run; `now` is pinned on every read, so nothing here is the wall
clock.

**BEFORE (`#queueSharedInquiryCandidates` grouping over the UNGATED `refs`, measured by reverting the whole
subtraction on a COPY of `src/` and running §10 against it through `PROJECT_SIGHT_SRC`): `project-sight: 140
passed, 7 failed`.** Vera's `op=queue` did not merely change a flag. It changed by these paths, as the suite's own
diff printed them:

| path in vera's answer | before the hidden act | after |
| --- | --- | --- |
| `items[0]` | her divergence FINDING | **removed** |
| `item_count` | 1 | 0 |
| `counts.finding` | 1 | 0 |
| `unattributed_readings.count` | 1 | 0 |
| `unattributed_readings.inquiries[0]` | her question | **removed** |

The mechanism is the page's EDGE and not its contents. The candidate read is bounded at
`Store.QUEUE_SHARED_INQUIRIES_MAX` (64) and ordered by `target_id`; the 70 filler questions each had two distinct
citers on the ungated table, so they filled the page, and her own question — which sorts last — was never reached.
The later gate in `#projectsDrawingOn` cannot undo that: it narrows candidates the read REACHED. `inquiries_bound`
and `inquiries_examined` read `-1` after the act for the plainest possible reason — there was no item left to
publish them on.

**AFTER (the subtraction at both ends of the edge): `project-sight: 147 passed, 0 failed`.** Vera's `op=queue` is
BYTE-IDENTICAL across the act (status, content type and body, by digest), her item is still there, and its
`inquiries_truncated` still reads `false`. The ADMIN token's answer moves across the same act, which is what says
the 70 questions really are shared and the identity above is not two reads of a store nothing changed.

**THE FIGURES AT THE BOUND, printed by the same run.** With the fix, vera's visible candidates read **2 of a
published bound of 64** before her page is filled; the suite then adds exactly the 62 filler questions needed to
reach the bound and asserts `inquiries_examined === 64` with `inquiries_truncated === false`. Citing the hidden
project ITSELF from two projects she can see leaves both figures where they are; citing a project she CAN see, by
the same act, tips `inquiries_truncated` to `true`. That pair is what distinguishes the fix from a page that
simply never counts a target.

**WHAT THIS DID NOT MEASURE.** No timing figure was taken: the change adds one non-correlated `NOT IN` subquery
per end of the edge over `#hiddenSets`' existing `hid` set, which SQLite materialises once per statement, and the
candidate read keeps its `LIMIT`. `derivation-bounds.test.mjs`'s census is the instrument that would see an
amplification appear here, and it is unmoved (see the gate line on the landing commit). The cost at the scale a
member's feed actually reads was not separately benched and is not claimed.
