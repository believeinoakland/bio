# DEBT

Known defects, deferred obligations, and unverified claims. Stable IDs so a
session can reference an item without restating it. Nothing is removed from this
file; resolved items keep their row and gain a resolution.

Severity: `defect` (shipped and wrong), `gap` (missing and known), `drift`
(two sources disagree), `unverified` (claimed but not independently established),
`design` (a decision deliberately deferred).

| ID | Sev | Found | Item | Status |
|---|---|---|---|---|
| D-1 | design | 2026-07-24 | **Root of trust is unmodelled.** Three parts of the membership design lean on it; the only implementation is ADMIN_TOKEN, a bootstrap credential that became the root of trust by accident. It is a proxy for hosting access, has no custody model (no m-of-n, no split custody), leaves no audit trail, and cannot be rotated without returning the instance to unclaimed. Deciding what a root of trust should BE for a BIO group is doctrine work of the same weight as the membership model. See `architecture/BIO_Membership_Architecture_v1.md` §9. | DOCTRINE · DEFERRED 2026-07-31 (DEC-2) until a BIO instance runs with MULTIPLE MEMBERS, from which what a real group can actually hold in custody can be OBSERVED rather than guessed — a custody model assumed in the abstract fails silently in the group it was built for. No session re-raises it before the trigger. |
| D-2 | defect | 2026-07-24 | **History paths use a non-canonical layout.** `readImage` emits `_history/<snap_key>/<path>`; canonical is `_history/bundle_<snap_key>.md`. All 30 bundles fail C-12.2, 168 errors, no content missing. `schema.mjs` line 3 settles which side moves. | fixed 0.5.0 |
| D-3 | defect | 2026-07-24 | **Promotion records are not projected.** The catalog requires `_history/promotion_<key>.json`; the plane stores the data and emits none of it. Makes `classifyDivergence` and C-20.1 unreachable rather than passing. | fixed 0.5.0 |
| D-4 | drift | 2026-07-24 | **Manifest `kind` vocabulary differs.** Plane writes `creation`/`direct_write`; catalog switches on `promotion`. This is why D-3 did not surface as an error: the check never fired. A divergence that hides another divergence. | fixed 0.5.0 |
| D-5 | gap | 2026-07-24 | **`plane-gate/0.1` implements 4 checks of 49.** No frontmatter contract, state legality, transition edges, canonical headings, append-only, citation register, provenance register, release authority, release signature, or mechanical conformance. This is how D-6 and D-7 shipped invisibly. | S-2 | fixed 0.6.0 |
| D-6 | defect | 2026-07-24 | **Intake UI stamps illegal first states.** A Problem lands at `forming` (legal: surfaced, elevated, deferred, dismissed); an Action lands at `forming` (legal: planned, active, awaiting_response, resolved, abandoned). C-4.1 refuses both. Shipped in 0.4.0 and 0.4.1. | S-3 | fixed 0.5.1 |
| D-7 | defect | 2026-07-24 | **Intake UI writes 4 of 15 core fields**, and `## Summary` for all four types where only Information takes it. C-2.2 fires eleven times per created bundle; C-3.1 on three types. Same root cause as D-6: written against the plane's tolerant store because the catalog was unavailable. | S-3 | fixed 0.5.1 |
| D-8 | drift | 2026-07-24 | **Relationship vocabulary disagrees.** State Rules 5.1 declares a closed six-value vocabulary; the catalog carries a seventh, `corroborates`. Implementation is ahead of a spec that claims to be the closed authority, so the document needs the amendment. | open | amended 2026-07-24 |
| D-9 | unverified | 2026-07-24 | **Unreferenced register rows: MEASURED and then RE-MEASURED, 2026-07-26. All 87 rows are sound.** The entry guessed "superseded bytes plus dropped transport twins", untestable because no op could read the register. `op=registeraudit` (0.22.0) reported 57 live, 30 "dropped", 0 orphan, all 30 being `migration/drive-provenance.json`, one per bundle. **That "dropped" classification was WRONG and so was the conclusion drawn from it.** `registerAudit` looks only in `files` and `history` and never in R2. `migrate/migrate.mjs` line 14 states its intent plainly: the Drive provenance is carried "verbatim as a registered drive-provenance capture, so the Drive era remains inspectable WITHOUT POLLUTING THE LIVE FILE IMAGE". It registers the capture, uploads the bytes, and deliberately keeps the file out of the bundle, which is exactly what the two-bucket architecture is for. Verified against the live plane: `op=capture&sha256=45a07df1…` returns HTTP 200 and the actual provenance JSON. **The migration IS auditable and no register row is broken.** The intermediate claim that it was not, recorded here on 2026-07-26 and now retracted, came from reading `op=file` returning null and never checking whether the bytes existed elsewhere. CONSEQUENCE: `registerAudit`'s `dropped` class is misleading and its output must not be trusted until it distinguishes `captured` (not in the image, bytes retrievable from `bio-captures`, sound) from genuinely `unbacked` (bytes nowhere). No row is known to be unbacked. | M6 · measured, sound; registerAudit needs an R2 check |
| D-45 | gap | 2026-07-26 | **An unbacked register entry is refused at RATIFY, not at promote, so one can sit in the working corpus until publication.** REWRITTEN 2026-07-26; the first version of this entry said "nothing anywhere refuses it", which was wrong and was written before reading `src/gate.mjs`. `runGate` carries the invariant explicitly, under the comment "The plane's own remaining duty: bytes the register claims must exist": it probes R2 per register row and refuses with `PLANE_MISSING_BYTES`, or `PLANE_SIZE` when the register and the stored object disagree. That runs on ratify, which is the publication boundary and the two-bucket fence, and is arguably exactly the right place for it. What remains true is narrower: `promote` verifies nothing, so a provenance claim about bytes that exist nowhere is accepted into the WORKING corpus and reported as success, and stays undetected until someone ratifies or runs `op=registeraudit`. Whether to close that at promote is a design question, not an obvious fix. The obvious fix is wrong and was tried and reverted: requiring every entry to name a file in the same payload refuses `migrate.mjs`'s deliberate and correct pattern, and `migrate.test.mjs` failed on it immediately. A real promote-time guard would need R2 and therefore could not sit inside promote's `transactionSync`. DEFAULT POSITION: leave it, because the fence is where the fence should be, and `op=registeraudit` now reports the condition on demand. | ACCEPTED · open, default is to leave it |
| D-46 | defect | 2026-07-26 | **The deployed plane enforces the SUPERSEDED v1.4 rule for project participant removal.** `projectRemove` in `store.mjs` requires an in-app administrator and answers `ADMIN_ONLY`, which is Membership Architecture v1.4 section 7.7. v2 section 7.7 REVERSES it: only a project OWNER removes a participant, and administrators hold no authority over project participation at all (v2 section 4.9), retaining only sight (7.3, 7.8). Confirmed live on biosmoke7 at 0.24.0, which answers `ADMIN_ONLY`. `projects.test.mjs` asserts the old rule and must be CORRECTED rather than exempted, per standing lesson 3. RESOLVED in 0.25.0: `projectRemove` now requires an owner and answers `NOT_THE_OWNER`, `projectInvite` lost its administrator bypass, both run through one `#isProjectOwner` helper, and the four assertions encoding the old rule were corrected. Verified live on biosmoke7. | resolved 2026-07-26, 0.25.0 |
| D-47 | gap | 2026-07-26 | **7.13 is not built: an administrator cannot add an owner to a project whose owners are all inactive.** Membership Architecture v2 7.13 gives administrators exactly one participation power, and only when EVERY owner of that project is inactive. Until it exists, a sole owner who gets deactivated leaves a project that accepts no new participants, cannot be reactivated, and cannot change hands. Not urgent on a development instance with one project and no deactivated members. RESOLVED in 0.30.0 as `op=projectownerrescue`: an administrator adds ONE owner when EVERY owner is inactive, adding rather than replacing, recorded with the acting administrator and a reason. | resolved 2026-07-26, 0.30.0 |
| D-48 | gap | 2026-07-26 | **Project name uniqueness (7.1) is enforced at FORK and nowhere else.** `Store.projectNameKey` is the single comparison and `forkProject` consults it, but an ordinary `promote` creating a project does not, so two projects created by the normal path may still collide. The rule belongs in the check catalog and at the write path, per v2 section 11 item 8. Checked against biosmoke7 on 2026-07-26: 30 bundles, one project, no collisions, so closing this costs no migration there. | SUPERSEDED — closed by 0.28.0 (PLAN S-12 item 6); the residue is D-50 · open, S-12 step 6 |
| D-49 | defect | 2026-07-26 | **Reactivating an ejected administrator restored their administrator status.** A section 4.7 removal sets `status='revoked'` and leaves `role='admin'`, so `memberSet(active)` returned them to office with one call by any single administrator, undoing a group vote and defeating 4.7's consensus-on-addition. Found while building section 1.3, not by a report. RESOLVED in 0.27.0: reactivation demotes them to `member` and says so in the response; restoring the office goes through `adminendorse` like any other appointment. Asserted in `membership.test.mjs`, written failing first. | resolved 2026-07-26, 0.27.0 |
| D-50 | gap | 2026-07-26 | **Project name uniqueness is enforced at the write path but not in the check catalog.** v2 section 11 item 8 asks for both. `promote` refuses a colliding name, so nothing can be written wrong, but `bio-checks` cannot report the condition on a corpus it is handed from elsewhere, which is what the conformance path is for. Lower priority than the write path, which is the one that prevents the damage. | M7 · open |
| D-51 | drift | 2026-07-26 | **The `expertise` column on `members` is dead.** Section 1.3 moved to the append-only `member_expertise` table in 0.27.0, because a list column cannot carry a confirmation state, a confirmer and a withdrawal per entry. `memberAdd` still accepts and stores an `expertise` argument and `memberList` still returns the column, so two places now answer the same question and one of them is never updated. Left rather than dropped, since nothing here deletes, but the reader and the writer should be retired and `memberlist` should serve the table instead. RESOLVED in 0.30.0, and it was not merely drift: v1.4 let an administrator ASSIGN expertise at invitation time, which v2 1.3 forbids. `memberAdd` now refuses the argument rather than ignoring it and `memberList` serves from `member_expertise`. The column stays, unused. | resolved 2026-07-26, 0.30.0 |
| D-52 | gap | 2026-07-26 | **An export is recorded but no administrator is NOTIFIED.** Section 8.1 says an export is recorded in the append-only history AND every administrator is notified. `export_log` delivers the first half and is readable by in-app administrators through `op=exportlog`, so an export is discoverable. There is no notification channel in this system at all, so the second half is not implemented and an administrator who never looks will not learn of an export. Closing this needs a channel decision (in-app banner on next sign-in, or the communications surface the Roadmap describes) rather than more code here. | M7 · open |
| D-53 | gap | 2026-07-26 | **BIO has no reputation or credence mechanism, and S-11 step 5 is blocked on deciding whether it needs one.** Grepped, not assumed: no credence, reputation, trust or confidence field, table or check exists in `checks/` or `src/`. Bob's framing is that information from a credible source can be ratified with trust inherited from that source, that member ratification adds that member's trust, and that trust never decreases. Two existing doctrine statements bear on it and one partly contradicts it: `BIO_Technical_Architecture_Decisions_v10` **No transitive trust** says trust is never inherited but re-established locally at every hop and credibility is never demonstrated by the producer's reputation, scoped to PEER BIO GROUPS and the R13 infiltration threat rather than to external primary sources; and `BIO_Design_Requirements_v2` section 4 says reputation accrues through consistent quality with no authority managing the process, so any mechanism must be a group's own ledger and not a shared score. Blocking finding for any design: **references target a bundle id and nothing finer** (`BUNDLE_ID_RE`), with no anchor, span or claim identifier anywhere in the catalog, so credence attached to an edge is still document-granular and cannot distinguish an authoritative table of figures from a self-interested assertion in the same document. See SESSION-KICKOFF.md for the full parked analysis. | DOCTRINE (Bob) · open, blocks S-11 step 5 |
| D-10 | defect | 2026-07-24 | **A no-op update reads as success.** The installer reports "Updated to X" whenever the upload succeeds, including when X equals the version already installed. Observed live: a 0.3.10-over-0.3.10 update read as a successful update and the load-bearing line was easy to skim past. | open | fixed, wizard 0.5.1 |
| D-11 | gap | 2026-07-24 | **Apps Script credentials remain live.** Four of eight survive decommissioning because they are credentials in other systems: the R2 key pair (Cloudflare object access) and the SPN2 pair (Internet Archive, usable to archive under the account holder's identity). The four `TOKEN_*` bearer tokens retire only when the deployment is deleted, not when it goes idle, so the frozen Drive snapshot is not actually read-only yet. | resolved 2026-07-27 by S-9 — every Apps Script-era credential revoked in its own system and the deployment deleted; see PLAN.md S-9, which records the account topology and why each revocation was safe. The row's status had never been updated from the step id, which is how it read as open work for four days. |
| D-12 | gap | 2026-07-24 | **Scale benchmark unrun.** Conversion Plan step 6: synthesized stores at 5,000 and 20,000 bundles against the prediction table. Real measurements exist and beat the refutation thresholds widely, but the formal benchmark has never run, and it should run with the full gate in the path. | S-8 | measured 2026-07-24 |
| D-13 | unverified | 2026-07-24 | **Source fidelity to Drive is not independently established.** The record is spec-conformant and every referenced capture hashes correctly, both verified this session. Fidelity to the Drive original rests on the migration tool's comparison against its own mirror. Judged sufficient because the corpus is development reference that production will refetch. | ACCEPTED · accepted |
| D-14 | defect | 2026-07-24 | **The enrolment screen is unreachable.** `s-enroll` exists in the markup and nothing ever calls `show("#s-enroll")`, so an invited member has a code and no way to use it. Shipped in 0.4.0 and 0.4.1. The burner URL is what should reach it. | S-3 | fixed 0.5.1 |
| D-15 | gap | 2026-07-24 | **The index must filter derived reverse edges by the viewer's position.** CLOSED 0.23.0. The seam reserved in S-10 held: satisfying it was one function, `viewerPredicate` in `query.mjs`, and not an audit of every query path. An identified session now compiles a predicate over `project_participants`; an uninvited member sees no project at all, not its existence, name, references or participants (7.9). The EVIDENCE CORPUS STAYS SHARED by design: the filter applies to project bundles and nothing else, because compartmenting Information and Problems would fracture the record and stop a member on one project seeing what another had gathered. Machine credentials are deliberately unfiltered: `class:member` is a shared instance token with no participation to check, and anyone holding one already has instance-level access, so filtering it would buy nothing while breaking the operator path. MEASURED, because the bench drives `class:member` and therefore did not exercise the new predicate at all: a member-viewer shape was added and the filter costs 16ms against 17ms unfiltered at 20,000 bundles, the `EXISTS` arms short-circuiting on `object_type <> 'project'` for almost every row. `test/projects.test.mjs`, 25 assertions. | closed 0.23.0 |
| D-17 | defect | 2026-07-24 | **Manifest `created` was the server's wall clock**, where C-12.1 compares live `last_updated` against earlier entries' `created`. A signed ratification backdates `last_updated` to the transition instant, so an honest document read as history running backwards. The catalog's own comments record this exact deadlock freezing a bundle on 2026-07-22: satisfying C-12.1 meant editing bundle.md and destroying the ratification. Now stamped from the revision's own time. Found by the conformance suite on its first run. | fixed 0.5.0 |
| D-21 | drift | 2026-07-24 | **References live in two places and only one is checked.** The plane's `refs` table is populated from the promote payload; the catalog reads `references` out of `bundle.md` frontmatter. A caller can set them inconsistently and nothing notices, because C-6 only ever sees the frontmatter. Either promote should parse references from the document, or the payload field should go. | open | fixed 0.6.2 |
| D-22 | gap | 2026-07-24 | **The revise path writes no Session Log entry.** C-13.2 requires one on any bundle whose `last_updated` moves past `created`, and C-5.1 requires prior entries be retained. A member editing through the browser can therefore produce a bundle the gate refuses, with a message about the Session Log that will mean nothing to them. The editor should append an entry on save. | open | fixed 0.6.1 |
| D-23 | defect | 2026-07-24 | **The revise path had never worked.** The save handler read `lease.result.baseSha`; the lease returns `base`. So every revision sent `base: undefined`, the store correctly refused it as a stale write, and the page reported the raw code `CAS_STALE` because its friendly message matched on `STALE`. It also overwrote `created` with the save time, destroying when the bundle was actually created, and never moved `last_updated` in the document the catalog reads. Found while fixing D-22, which was one of four things wrong in the same twenty lines. | fixed 0.6.1 |
| D-24 | defect | 2026-07-24 | **A comment broke the served page, for the second time.** setup.mjs is one template literal, so an unescaped backtick terminates it and a dollar-brace starts an interpolation. A comment quoting two field names in backticks made the module unparseable. The 0.3.8 hang was the same class. Both were found by accident; `hygiene.test.mjs` now scans the template for unescaped backticks, counts interpolations, loads the module, and parses the script it serves. | guarded 0.6.1 |
| D-25 | design | 2026-07-24 | **`promote` writes a whole image, so a partial writer deletes what it does not mention.** This is correct for a content-addressed store with a single writer, and it is a trap: the monitor's first tick supplied only `bundle.md` and removed the provenance register from every bundle it touched, taking the monitoring baseline with it. Fixed at the call site, and the image now carries a blob's size so callers CAN pass the rest back, but the sharp edge is still there for the next writer. Options: a patch-style promote that merges, or a refusal when a promotion drops a file that the previous revision had and the writer did not name. The second is probably right, since silent deletion has no legitimate use. | open | fixed 0.13.1 |
| D-26 | design | 2026-07-24 | **A whole-store conformance pass pays one round trip per bundle.** Measured: 63s locally for 20,000 bundles, about 2,060s on the deployed plane sequentially, roughly 97% of it fixed per-call latency. The catalog is a pure function over an injected filesystem and the images already live inside the Durable Object, so the pass belongs inside the object: one call in, a findings summary out. Store and checks are not the constraint; the shape of the API is. | open | fixed 0.12.0 |
| D-27 | gap | 2026-07-24 | **`list` returns every bundle and is linear.** 81ms at 5,000, 434ms at 20,000, roughly 2s at 100,000. Fine now, and it returns everything because nothing has needed less. Pagination or a narrower projection is the answer, and the benchmark above says when. | open | fixed 0.14.0 |
| D-28 | gap | 2026-07-24 | **The Conversion Plan's prediction table is not in the corpus.** Four documents reference "Conversion Plan step 6" and its prediction table; the plan itself is not in the repository, so the benchmark records actuals with nothing to compare them against. Either the plan joins docs/architecture or the references should stop implying a document a reader can find. | ACCEPTED · open; probe 1 (RETRIEVAL-PROBE.md) recorded actuals with no prediction table to check against, as this gap predicts |
| D-29 | defect | 2026-07-24 | **The browser's revise path deleted every file but bundle.md.** Identical to the monitor's first tick, in a different place: editing a bundle with a captured document removed its provenance register and every capture. Found by closing D-25, which refused the write instead of obeying it. Both call sites now carry files forward. | fixed 0.13.1 |
| D-31 | design | 2026-07-25 | **The `public` token class was an attractive nuisance and is retired.** A credential handed to the public is not a credential: to be public it must be widely distributed, and once distributed it bounds nothing. It granted `selftest` and `publishedlist`, was never issued by the installer (which creates exactly boot, member, and probe), and was bound on no instance, yet its existence is what invited `op=index` onto its list and produced D-30. The published surface is protected structurally by the `classes: null` ops that read only the published projection. When a public directory of published material is wanted, it is an unauthenticated op on that projection, like `verify`, never a token class. | fixed 0.14.2, held by test/fence.test.mjs |
| D-30 | defect | 2026-07-25 | **`op=index` served working-corpus metadata to the public token class.** The control plane's own header says the public class is published-scope reads only, and its note on `verify` justifies that op being unauthenticated precisely because it answers from the published projection. `op=index` reads the `bundles` table, so it met neither condition, and a public-class credential received every bundle's id, title, current state, last-updated time, and image hash. A title names what a group is investigating and the state says how far along it is, both before there is anything to answer, which is the disclosure the two-bucket fence exists to prevent. No caller depended on the grant. Found while settling the retrieval design, since the same fence governs search. | fixed 0.14.1, held by test/fence.test.mjs |
| D-19 | gap | 2026-07-24 | **Migrated manifest entries carry the migration's wall clock** as their `created`, not the original promotion time, so C-12.1 reports 18 bundles as having a live bundle.md older than a superseded version. D-17 fixed the stamping code; these rows predate it. Cured by re-migration on 0.5.1 rather than by in-place repair. | M6 · open |
| D-20 | gap | 2026-07-24 | **The gate needs byte-complete images.** C-12.2 checks history-snapshot presence with `files.has` rather than the elided-aware helper, so a tier-scoped read fails it even when the bytes exist in R2. Correct for a byte-complete gate, but it means gating a capture-heavy bundle pulls its history blobs: 148MB for the whole record. Measure in S-8 and consider whether R2 `head` can satisfy presence without a fetch. | S-8 | fixed 0.6.0, catalog 1.16.5 |
| D-18 | gap | 2026-07-24 | **The conformance suite asserts only the C-12 family.** Other families are counted and printed but not failed, because the intake path owes D-7. Tighten to "zero findings" when S-3 lands. | M0 · S-3 |
| D-16 | gap | 2026-07-24 | **The cover field must be labelled as a label**, not a name, wherever it appears. A field that reads as "identity" invites an administrator to type a legal name, which is the whole exposure the cover-and-handle split exists to avoid. Naming it is the cheapest part of the obligation and the only part the software can enforce. | open | fixed 0.14.0 |

| D-32 | gap | 2026-07-25 | **The shipped retrieval path is 3.5x slower than the probe it inherited its numbers from.** MEASURED, 2026-07-25, by `npm run bench:retrieval` (test/retrieval-scale.mjs), which loads a corpus through the real `promote` and drives the real `op=search`. At 20,000 bundles the shipped path runs 5ms to 163ms; probe 2 recorded nothing above ~46ms. The gap is real work the probe did not do: a 26-column provenance projection per hit, the viewer gate, bm25 with snippets, and a facet pass. Two structural causes were found and fixed in 0.17.0, halving the worst shape from 305ms: the gate was a CTE intersected into every statement, which cost a full table scan per statement, and is now a WHERE predicate on rows already selected; and the facet pass ran one statement per field, rebuilding the same scope six times, and is now batched. What remains is inherent to the contract rather than accidental, so closing this further means a covering index for the provenance columns, or counting facets from one scan in JS instead of a GROUP BY per field. Bob's decision, 2026-07-25: ship in the measured condition and stop quoting the probe's numbers as if they described this code. | M5 · measured 0.17.0; further optimisation open |
| D-33 | gap | 2026-07-25 | **The id tiebreak is required by argument, not by a failing test.** Every sort compiles to `ORDER BY <field> <dir>, bundle_id ASC` because without a total order a row can appear on two pages or on none. `test/search.test.mjs` builds 600 bundles that all carry one value on the sort field and pages the whole set. Removing the tiebreak from the compiler and re-running it STILL PASSES: at that size SQLite returns tied rows in a deterministic order. So the order is deterministic but undeclared, which is the real hazard, since the delivery order is a property of the plan and the plan changes with corpus size, with an added index, and with an engine upgrade. The guard that actually holds is at compile time, in `test/query.test.mjs`, which asserts every sortable field in both directions ends in a unique column. Demonstrating the runtime failure needs a corpus large enough to spill the sorter, which belongs with D-32 and not in a suite. | M0 · open |
| D-34 | design | 2026-07-25 | **A server-side selection has no lifecycle and no owner.** Step 5 of the retrieval build order is not in 0.16.0. `mode=ids` returns the whole set in the page's order, which satisfies "select-all is a distinct operation", but the snapshot-by-handle construct is absent and with it every resource question it raises. A Worker holds no connection, so the plane cannot observe a closed tab and can only require proof of life: ownership by session so a logout or a revocation collects, a TTL refreshed on read, a Durable Object alarm to sweep, and caps by BYTES before count, since a 20,000-id selection is 400 to 800KB in the same SQLite that holds the record. Above the byte cap, store the query plus the SHA-256 of the ordered id list rather than the ids: drift detection stays exact and storage becomes O(1), at the cost of being able to say a large selection drifted without saying which rows moved. Note also that a selection is the first thing in an append-only store that is legitimately collectable, and that exception needs writing into doctrine or a later session will read the sweep as a violation. | fixed 0.17.0 |
| D-35 | design | 2026-07-25 | **Drift in a selection must be detected and must not be absorbed.** When the corpus moves under a selection, five things can have happened: an item was revised, an item was purged, an item stopped matching the query, a new bundle started matching, or the viewer's visibility changed. The first two are one join if `bundle_sha` is stored per item; the next two are one re-run of the stored query; the last is the gate applied at action time, which is obligatory anyway. Auto-updating the set is rejected: a selection records an operator's INTENT, and an action landing on rows the operator never saw is an accountability failure in a record whose whole purpose is attribution. The monitor complicates this honestly, since mechanical revisions will drift a long-lived selection with no human involved, so drift should be CLASSIFIED using the `writer` and `operation` already on the manifest rather than absorbed. | fixed 0.17.0, held by test/selection.test.mjs |

| D-36 | defect | 2026-07-25 | **workerd's SQL limits are orders of magnitude below SQLite's documented defaults, and a bench found them rather than the suite.** Two ceilings, both binary-searched through the real code path on 2026-07-25: a statement may bind about **100 variables** (SQLite's default is 32,766) and a compound SELECT may have **five terms** (SQLite's default is 500). The first broke a chunked id list at 400 per chunk; the second broke a six-arm facet statement AND, more seriously, would have broken the query compiler itself, since six metadata filters is one ordinary pass over a filter sidebar and compiles to six INTERSECT arms. Both are now guarded, with the id chunk at 64 and compound chains nesting through subqueries at 4 per compound, and `test/query.test.mjs` asserts the nesting across four query shapes. The open item is the CLASS: these limits are undocumented, they are not the platform's published behaviour, and any new statement shape can meet another one. The lesson is the one that keeps recurring in this repository, which is that a bench that runs the real path at real size finds what a suite at small scale cannot. | M5 · guarded 0.17.0; the class is open |
| D-37 | gap | 2026-07-25 | **The 300-second selection keep-alive.** CLOSED by Bob's decision, 2026-07-25, on the grounds that a selection timeout is not a failure mode but a term of the contract, the same way an idle logout is: intrinsic to the relationship rather than an error, and its consequences the client's to decide. The client may re-select against a possibly different record set, tell the user the window closed, or warn as expiry approaches and offer to hold. The contract is already expressed on the wire and needs no new machinery: `op=select` returns `expires` and `ttlSeconds`, `op=selection` returns a refreshed `expires`, and `NO_SUCH_SELECTION` IS the timeout signal. `op=selection` is non-mutating and defaults to weight `report`, so a client polls it both to hold the lease and to learn what drifted, which is what a UI wants to show anyway. Reclamation is alarm-driven: `#armSweep` on creation, `alarm()` sweeping and re-arming while any selection remains, self-terminating when the table empties. 300 seconds stands. Three claims made against this item during the 0.21.1 session were WRONG and are recorded so they are not made again: that the sweep was opportunistic-only and needed an alarm (it has one), that expired and unknown needed distinguishing server-side (the client knows its own creation time and TTL and can distinguish locally), and that no way existed for a client to say it was still present (`op=selection` is exactly that). | closed by decision |
| D-38 | design | 2026-07-25 | **The number of Information records a Project can cite is bounded by INLINE_MAX, not by SELECTION_MAX_ITEMS, and the two were set independently.** Citing writes one frontmatter entry per cited record into a single `bundle.md`, measured at 83 bytes per edge at a 25-character bundle id. The enumeration cap is 10,000 and the inline file limit is 1MB, so a MAXIMUM LEGAL SELECTION originally produced a 1,070,846-byte document and was refused by `promote` with `OVERSIZE_INLINE`: an error about inline byte storage, in answer to an operator who selected a legal number of records and pressed cite. Found by `test/cite-scale.mjs`, not by the suite. Two changes in 0.18.0 make it survivable rather than resolved: the Session Log entry is bounded at 20 named ids plus a count (it had listed every id, costing a further 24 bytes per edge), which took an edge from 107 to 84 bytes and lets a maximum 10,000 selection fit at 841KB; and `CITATION_TOO_LARGE` now refuses before anything is written, naming the real limit and the room remaining. The ceiling is now reachable only CUMULATIVELY, and is: at 12,000 edges a further 2,000 refuses and leaves the document byte-identical. `bundle.md` cannot spill to R2 to escape this, because the gate compares it byte-wise against history (C-5, C-12) and `schema.mjs` keeps every file the gate reads inline by rule. Bob's decision, 2026-07-25: leave the ceiling where it is. The tested ceiling is far above any plausible group, lowering the selection cap would penalise selections that are not for citing, and there may be Cloudflare configuration raising these limits that has not been explored. Revisit when a real corpus approaches it. | ACCEPTED · open, by decision |
| D-39 | defect | 2026-07-25 | **Every op returns a worker exception on a POST with an empty body.** `store.mjs`'s `fetch` calls `await req.json()` unconditionally when the method is POST, so an empty body throws before any op is dispatched and the caller sees Cloudflare error 1101 rather than a BIO refusal. Verified on the deployed 0.18.0 against `stats`, `index`, `dangling`, `searchfields` and `selectionlist`, so it is general and pre-dates the citing work; it was found while exercising `op=cite` live. Harmless to correctness, since every op that matters is reachable by GET or by POST with `{}`, but it turns a client bug into an opaque platform error instead of a named reason. One-line fix, deliberately not folded into the 0.18.0 release. RESOLVED 2026-07-31 (REC-3): the guard already existed at the Durable Object (store.mjs `fetch`, commit 6ac72d0a — an empty POST body is `null`, a present-but-invalid one is refused `BAD_JSON` by name), so an empty POST already returns a named JSON reason rather than a 1101; the debt stayed open only because NO suite locked it in. `test/empty-body.test.mjs` now drives an empty POST to the five debt-named ops (stats/index/dangling/searchfields/selectionlist) and to mutating ops (promote→NO_BODY etc.), with the negative control RUN (break the guard → the five ops return a non-JSON 500, 8 fail; restored → 10 pass). | resolved 2026-07-31, REC-3 |
| D-40 | defect | 2026-07-25 | **Test and bench fixtures write `criticality: notable`, which is not a legal value.** The catalog's enum is `crucial` or `supporting` (C-2.7), so every Information bundle these fixtures build is non-conformant, and a conformance assertion run over one would fail for a reason unrelated to what it was testing. Found on the deployed 0.19.0 by auditing the scratch namespace after a live check, not by any suite, because the suites conformance-check the PROJECT they cite into and never the Information they cite. Fixed in the fixtures written this session (`cite`, `edges`, `cite-scale`, `limits-probe`). NOT fixed in `retrieval-scale.mjs`, `search.test.mjs` and `selection.test.mjs`, where `notable` is deliberately used as facet and filter test DATA (`q=criticality:notable` is a bench shape measuring the dominant-value case) and changing it would rewrite assertions to fix a fixture rather than a defect. The real risk is a future session copying one of those fixtures as a template. | M0 · open, narrowed |
| D-41 | gap | 2026-07-25 | **The Section 4 governance write path had never run against a deployed instance.** CLOSED 2026-07-25 on the deployed 0.21.1, once Bob issued a throwaway ADMIN_TOKEN. 27 of 27 assertions passed live: consensus refusing an addition past the second and naming who must still endorse, the last endorsement issuing the invitation, one removal vote falling short with the tally reported, the target refused their own vote, a second vote carrying with deciders and reason recorded, `IMPOSSIBLE_AT_TWO` at two administrators, `ROOT_OF_TRUST` refusing to remove the founder, `ADMIN_REQUIRES_VOTE` refusing a direct revocation, and the capability rules. So 0.20.0 carries no D-43-shaped hole. The record was untouched throughout: 30 bundles, 137 files, 239 history rows, audit 30 clean. NOTE: the verification ran against the `bio` store rather than `scratch`, because D-44 made the member lifecycle uncompletable in `scratch`. Two probe member rows and some admin_votes rows remain in the biosmoke7 roster as a result; the RECORD is unaffected and the instance is disposable. | closed |
| D-44 | defect | 2026-07-25 | **Unauthenticated ops ignored the `store` parameter, making an invitation created in `scratch` unredeemable.** `enroll` and `invitelook` are `classes: null` and the control plane pinned every such op to `idFromName("bio")`. So `op=memberadd&store=scratch` answered `ok` and handed over a token that could never be redeemed, because enrolment always reached `bio`. A silent dead end, and it made the scratch namespace useless for rehearsing the member surface, which is exactly what an operator would want to do before touching a live roster. Pinning is CORRECT for `claim` and `login`, since an instance has one identity and there is nothing to claim in a scratch namespace, so the fix scopes only the invitation ops: the token is the authority and exists in exactly one store, so naming a store grants an unauthenticated caller nothing. Fixed 0.21.2, asserted in `fence.test.mjs`. Found against the deployed plane while closing D-41, not by 1280 green assertions. | fixed 0.21.2 |
| D-42 | gap | 2026-07-25 | **Burner-URL invitations are specified but only half built.** Section 6 wants a URL carrying a one-time code that is SPENT ON USE and afterwards resolves to nothing, carrying no record of what it formerly addressed, so a leaked or archived link is inert and reveals neither the group nor the invitee. What exists is an invite code the administrator hands over, reached through a URL fragment (the D-14 fix), and the code is correctly cleared at enrolment so it cannot be replayed. What is missing is the URL itself being the credential and resolving to nothing afterwards rather than to an enrolment form that says the invite is spent. Unblocked. | M7 · open |
| D-43 | defect | 2026-07-25 | **A `classes: null` op tested only at the Durable Object is untested where it is used.** `op=invitelook` shipped in 0.21.0 with a ReferenceError in its control-plane branch, so every live call answered a Cloudflare 1101 while 1276 assertions passed: the burner suite drove the store directly, as every store-level suite does, and for an unauthenticated op the control plane is the ONLY route a real caller has. Fixed in 0.21.1. THE CLASS IS NOW CLOSED TOO: `fence.test.mjs` reads the `classes: null` set out of the module and calls every one of them through the control plane with input that should produce a structured refusal, asserting only that a BIO answer comes back rather than a worker exception or `unknown op`. Reachability, not success. The probe catches a thrown parse, because a worker exception returns a non-JSON body and a verifier that dies on the failure it exists to detect reports nothing. Reading the set from the module means an op added later cannot pass by not being listed. | closed 0.22.1 |

## How to use this file

Reference items by ID in commit messages and in session notes. When a step in
`PLAN.md` resolves an item, change its status to the commit that did it rather
than deleting the row: the history of what was wrong is worth as much as the
record of what was fixed, and a ledger that only shows current problems teaches
nobody anything.

Items marked `accepted` are known, judged tolerable, and carry the reasoning in
the row. They are not resolved and should be re-read when circumstances change.
| D-54 | gap | 2026-07-29 | **The installer does not check or enforce the Workers plan.** BIO now depends on platform limits that differ by plan, and a group installing on Workers Free gets a working instance that behaves differently from every description of it: 50 external subrequests per invocation instead of 10,000, 10ms of CPU per request instead of 30s, 10 minutes of browser time per DAY instead of 10 hours per month. All of that is survivable (the plane calibrates its own ceiling and captures across ticks) and none of it is visible to the person installing. The installer must detect the plan, state plainly what capture will and will not do on it, and record the answer where the instance can report it. Enforcement is the open question: REFUSING to install on Free would contradict the ruling that Workers Paid is an optimisation and never a requirement, so the likely shape is inform-and-record rather than refuse. Wrangler's `limits.subrequests` is also unset in this repo and should be set explicitly once a plan is known, because the default moved from 1,000 to 10,000 in February 2026 and a default that moves is not a decision. | M2 · open **CHANGED IN CHARACTER 2026-08-04 by DEC-42: the installer no longer merely DETECTS the plan — it REQUIRES Workers Paid, VERIFIES it, and refuses to complete honestly rather than installing something quietly degraded.** That refusal IS this row's fix now: the D-106 failure this guards against (a group installing today getting something quietly different from every description of it) is closed by refusing, not by warning. Free is no longer a supported configuration. |
| D-55 | design | 2026-07-29 | **A rendered capture may contain content whose authority is not the hosting site, and `capture.authority` holds exactly one value.** Bob's ruling: if content produced by a third-party script is ever evidence, it must be recorded as produced by that third party and not by the hosting site. The provenance document has one `authority` per capture, so a rendered document carrying an advertiser's, an analytics vendor's, or a CDN-hosted widget's output cannot currently attribute it. Attribution has to be per-origin and sub-document, which is a shape the register has never held: `references[]` targets a bundle and nothing finer, the same granularity problem D-53 is blocked on. Deciding this is a precondition for treating any rendered capture as evidence, not a follow-on. | DOCTRINE (Bob) · BLOCKS D-64 · open, blocks CLIENT-RENDERED.md |
| D-56 | unverified | 2026-07-29 | **CPU headroom: MEASURED 2026-07-29, and the worry was unfounded on the numbers available. HELD OPEN DELIBERATELY as a watch item with no task attached.** The entry asked whether capture was near the free plan's documented 10ms CPU ceiling. Measured on biosmoke7 (Workers Free) at 0.44.0: `op=cpuprobe` completed 20 steps of 2,000,000 reference iterations, 40,000,000 in total, and the isolate was killed during step 21 with HTTP 503 `error code: 1102`. Forty million modular multiplications is not 10ms of anything, so whatever is enforced on this account is not the documented figure. Real captures are nowhere near it: the heaviest measured, a news front page, does 49 compute calls over 16.97MB of hashing and parsing. **A SEPARATE AND MORE IMPORTANT FINDING came out of trying: a Worker CANNOT TIME ITSELF.** Cloudflare freezes `Date.now()` during synchronous execution as a timing-attack defence, so the first meter reported zero for every segment of every real capture. Any millisecond figure reported from inside a Worker is a fabrication. Consumption is therefore counted in WORK (calls, bytes) and the ceiling in REFERENCE ITERATIONS; see `src/cpu.mjs`. **WHY THIS STAYS OPEN.** A CPU overrun terminates the isolate: there is no catchable error, no chance to record anything, and the caller sees an opaque 1102. Unlike the subrequest ceiling, this failure mode can never announce itself, so a future scope of work can begin silently overrunning it and the only symptom will be requests that stop working. Nothing is asked of any session by this row. It exists so that an unexplained 1102, or a capture that fails only on large inputs, is read as a CPU overrun on the first guess instead of the fifth. If work volume per capture ever grows by an order of magnitude, re-run `op=cpuprobe` and compare. | WATCH, no task · open as a watch item, no task |
| D-57 | defect | 2026-07-30 | **`resolveLinks` describes a self-reference as a target that CHANGED, and the UI prints that sentence to a member verbatim.** Found on live data during the U8 exercise. A paginated index links to itself (every Legistar calendar does), so the store holds a capture of the target whose `first_retrieved` and `last_retrieved` both equal the source document's own retrieval instant. The bracket arm needs `observations > 1` and does not fire; the `before && after` arm then matches THE SAME ROW on both sides and returns `undetermined` with the basis "the target changed somewhere between the captures bracketing this document's retrieval", plus a detail naming one capture hash twice: `bracketing captures differ: fc7f4bb39281 last seen 2026-07-30T00:18:02Z, fc7f4bb39281 first seen 2026-07-30T00:18:02Z`. Nothing changed and the two captures are one capture. The verdict value is defensible; the BASIS is false, and because the UI shows the plane's basis in the plane's own words rather than paraphrasing it, a member reads a fabricated claim about a source. The fix belongs in the plane, not the viewer: `resolveLinks` should recognise `pick.capture_sha === sourceCapture` and report a self-reference, which is a fourth basis rather than a fourth verdict. The UI already counts self-references apart from connections (see below), so the count is right while the sentence is wrong. | M3 · open |
| D-58 | gap | 2026-07-30 | **`captured_locators` and `links` are written ONLY when `op=acquire` is called with `subresources:true`, so an ordinary capture files no address and can never be a link target.** Found while trying to establish a `contemporaneous` verdict live: a plain `op=acquire` of a page that other captures link to left the verdict at `undetermined` because nothing had recorded that the store now held that address. Both `recordcapturedlocator` and `recordlinks` sit inside the subresource branch in `index.mjs` (the block guarded by reading the primary back out for the parser), which means the address index reflects only the subset of captures that asked for support material. Consequences: a link to a plainly-captured document resolves as `offsite` when the record demonstrably holds it; the reverse index (`op=links&address=`) under-reports what points at a document; and no verdict can ever be established for a target captured the ordinary way. `recordcapturedlocator` needs nothing from the parser and belongs beside the register write, unconditionally. The links write legitimately needs the parse and can stay where it is. RESOLVED 2026-07-30: moved to the capture body, unconditional, with `stLim` hoisted. Regression test drives `op=acquire` and asserts both directions (a PDF and a plainly-captured page resolve as `linked` naming their real hashes; an address the record genuinely lacks is still `offsite`). Negative control confirms five assertions fail against the unfixed code. | resolved 2026-07-30 |
| D-59 | unverified | 2026-07-30 | **`contemporaneous` has never been observed on real data, and the strongest verdict arm may be unreachable for the sources BIO actually captures.** The arm requires one set of bytes observed on both sides of a document's retrieval, `observations > 1` on a single `captured_locators` row. Measured 2026-07-30 on `oakland.legistar.com`: two captures of `Legislation.aspx` twelve minutes apart produced two DIFFERENT hashes (`7f1083d74f30`, `8691808a30ab`), because ASP.NET pages carry per-response viewstate and timestamps. A page whose bytes differ on every request can never produce an `observations > 1` row, so identical-byte bracketing cannot fire for it regardless of whether the content changed. That makes `undetermined` not merely the common case but the ONLY reachable verdict for a large class of municipal sources, which is honest and is also weaker than the design assumed. The other establishing routes named in LINK-FIDELITY.md (a timestamp token over the target's hash, a third-party archive, monitoring across the interval with no change detected) do not depend on byte identity and are now the load-bearing ones rather than the fallbacks. Worth measuring across more hosts before deciding whether the bracket arm earns its complexity. | M3 · open |
| D-60 | design | 2026-07-30 | **A capture has no STABLE DIGEST, so change detection, duplicate detection and contemporaneity all run on a hash that moves for reasons that are not changes.** MEASURED 2026-07-30, two fetches of `oakland.legistar.com/Calendar.aspx` three seconds apart: identical length, **114,177 differing bytes, 31% of the document**, every differing byte inside exactly two hidden fields, `__VIEWSTATE` (115,096 bytes) and `__EVENTVALIDATION` (876 bytes). Normalise those two and the remaining **252,948 bytes, 68.6%, are byte-identical**. Nothing about the calendar changed. ASP.NET reserialises its control tree and reissues its anti-forgery list on every response, and municipal publishing runs on this class of software. Three separate mechanisms are wrong because of it: **monitoring** reports a change on every tick and therefore reports nothing; **duplicate detection** does not fire on re-captures of the pages that get re-captured most, so the record grows a second bundle for one document while C-18.3 and `op=audit` both stay silent because the hashes genuinely differ; and **contemporaneity's strongest arm** can never fire (see D-59). The fix is one thing: beside its raw identity hash, a capture carries a derived digest computed with known-volatile regions normalised to a placeholder. Identity stays raw and raw bytes are never rewritten; comparison uses the stable digest. Volatile regions are RECORDED with their field names, offsets and lengths, and the fact that two fetches differed only there is itself a dated observation worth keeping. Classification is per-family, heuristic, and reclassifiable exactly as chrome detection is, discovered by measurement rather than declared. A volatile region is never treated as evidence. Design written into LINK-FIDELITY.md. **PARTLY DONE 2026-07-30:** the volatile machinery is NOT at `civicos-ui/volatile.mjs` (that path does not exist; the row was wrong). It lives in `docprofile/index.mjs` (the `digests` and `compare` exports) with per-family handlers in `docprofile/handlers/` (`aspnet-webforms.mjs` is the Legistar/viewstate case), validated both directions against two live captures of one Legistar page. The PLANE has not adopted it: monitoring, `op=audit`'s duplicate sweep, and `resolveLinks`' bracket arm all still compare raw hashes, so the plane should import `digests`/`compare` rather than grow a second copy. **ADOPTION FEASIBILITY MEASURED 2026-07-30:** docprofile is 37KB of pure JS, zero external deps, zero node builtins, only internal cross-imports. Bundled through the plane's own esbuild target it tree-shakes to **5.3KB** against 2.4MB of headroom under the 3MB Free limit, so the bundle-size concern that applies to unpdf (D-91) does NOT apply here. docprofile sits at repo root OUTSIDE `bio-plane/`; the import path from `src/` is `../../docprofile/index.mjs`, and because `build` inlines all imports into one self-contained `dist/bio-plane.bundled.mjs`, the deployed artifact and the installer are unaffected by the cross-directory location as long as `docprofile/` is present at BUILD time (it is, in a clean checkout). Adoption is a build task, not an investigation. | M3 · open — **PARTLY STALE, corrected 2026-08-06: the PLANE HAS ADOPTED docprofile** (`bio-plane/src/index.mjs:70` imports `identify, doctypeFor, profileRecord, digests, CONFIDENCE, readText` from `docprofile/registry.mjs`), so "the PLANE has not adopted it" is no longer true as written. **CHECKED 2026-08-06 (sub-session). THE ROW IS ONE-THIRD DISCHARGED, and my own 2026-08-06 correction above was half-right — it is true of the WRITE path only.** `compare()` is **never called anywhere in `bio-plane/src/`**; the only caller in the repo outside docprofile itself is the browser (`app.html:13676`). The plane calls `identify()` and `digests()` in exactly one place, `op=acquire` (`index.mjs:3251`, `:3314`). Site by site: **(a) MONITORING — STILL RAW.** `index.mjs:3834-3838` digests the fetched bytes with `crypto.subtle` and compares `seen === baseline` against `provenance.json`'s recorded sha. No digest, no compare. On a Legistar page this reports `modified` every tick, which is this row's original measurement, undischarged. **(b) AUDIT'S DUPLICATE DETECTION — DISCHARGED.** `checkRegisterIntegrity` (`bio-checks.mjs:3553-3558`) buckets on `docs[i].profile.digests.evidentiary` when `determined === true`, beside the raw arm, firing only when ≥2 different raw shas share a digest. **So this row's claim that the duplicate sweep compares raw hashes is STALE, and `DOCUMENT-PROFILES.md:309-310` repeats the stale claim** — delegated to FRAMEWORK in `CLAIMS.md`. But note the scope: it is INTRA-BUNDLE, and no cross-bundle sweep exists at all. **(c) `resolveLinks`' CONTEMPORANEITY BRACKET — STILL RAW.** `store.mjs:14859-14873` brackets over `captured_locators` rows whose identity is raw `capture_sha`, so on a viewstate-churning page every fetch mints a fresh row with `observations = 1`, `bracket` is always null, and the verdict degrades to undetermined. **This is D-59's "strongest arm can never fire", still true.** So what remains on this row is (a) and (c) |
| D-61 | defect | 2026-07-30 | **`op=lease` cannot be taken by a machine credential, so no unattended writer can revise a bundle.** Found live: `leases.actor` is NOT NULL and is stamped from the session, so a member TOKEN (as opposed to a member session) fails with `SQLITE_CONSTRAINT_NOTNULL` on `leases.actor`. The revision path in the UI takes a lease before promoting, which means the capture-refill path is session-only. That collides with Bob's ruling that captures are autonomous jobs a member can walk away from: if the daemon finishes a capture the member started, it cannot write the completed manifest back, because it cannot take a lease. Two candidate shapes, and the choice is a judgment: either the lease grows a machine actor identity (`class:member`, `daemon`) so unattended writers are named rather than anonymous, or the refill path skips the lease and relies on `promote`'s CAS on `base`, which is the actual integrity mechanism and which was used to work around this today. The lease is a courtesy lock against two members editing at once; the CAS is what prevents a lost update. Whichever way it goes, the daemon needs to be able to complete work it was asked to do. **CLOSED 2026-07-31 (REC-2, e425b24):** option (a) — a named machine actor `token:<class>`, server-stamped and unforgeable, no schema change; the store refuses an anonymous lease by name, and promote's CAS on `base` (the real integrity mechanism) is untouched. | M1 · CLOSED (REC-2) |
| D-62 | defect | 2026-07-30 | **The installer's `mdFor` omits `content_hash` even when a document is attached, so any bundle it writes can never be released.** Found live on the first bundle U8 wrote. C-2.7 makes a well-formed `content_hash` an ENTRY REQUIREMENT for `verified`, and the release flow reads the frontmatter and nothing else, so a bundle carrying a perfectly good capture is refused at `collected -> verified` for a field the writing surface never wrote. It also makes the bundle unfindable through the search layer's `hash:` facet, which reads that same field, which in turn defeats the duplicate detection described in D-60. FIXED in civicos-ui's ported `mdFor` (writes `content_hash: sha256:<primary>` whenever a document is present, absent for typed intake where inventing one would be a lie) and the live bundle was repaired by revision. **NOT fixed in `bio-plane/src/setup.mjs`**, which is the installer's own copy and the one a new group gets, so every bundle created through the installer wizard with a captured document has the same hole. RESOLVED 2026-07-31 (REC-3): `bio-plane/src/setup.mjs` `mdFor` now emits `content_hash: sha256:<primary capture sha>` whenever a document is attached (gained an `src` param + one caller update), absent for typed intake, mirroring the civicos-ui port. Verified via `test/setup-honesty.test.mjs`: the served SETUP_HTML carries the emission, and checkBundle (the authority) confirms a document bundle WITH content_hash clears the C-2.7 "verified state requires a well-formed content_hash" finding that WITHOUT it refuses release; negative control RUN (drop the line → 3 fail). | resolved 2026-07-31, REC-3 |
| D-63 | gap | 2026-07-30 | **Only three stacks have been measured, and every unmeasured one is a handler waiting for evidence.** `docprofile/` now has a structure for recognising a document's stack and kind and deciding what is evidentiary, what is furniture and what is per-render machinery, and it is validated against ASP.NET WebForms (Legistar), WordPress (Oaklandside) and a client-rendered app (OpenGov). Everything else in Oakland's orbit falls to the conservative handler, which is safe and noisy: Drupal, plain static HTML, Granicus, CivicPlus, NextRequest, Squarespace, and the county's own sites. The work per stack is small and specific: fetch the same page twice, diff, attribute the differences, decide whether the document has an identifiable boundary, and write the handler with the measurement in its comments. The noise the conservative handler makes on an unmeasured source is the signal that the source needs a handler, so this row is a standing invitation rather than a blocker. Do not add a rule without a measurement: a careless rule hides a real change, which is the only failure mode here that matters. | M2 · open, ongoing |
| D-64 | gap | 2026-07-30 | **A client-rendered page is recognised as a shell and still cannot be captured as evidence.** `docprofile`'s `client_rendered` handler detects the case structurally (a body with no links and no prose) and carries a member-facing warning that what was collected is the frame and not the figures. That is the honest interim and not the requirement. The standing ruling is that JS-rendered content IS the content and must be captured at the SAME GRADE as the rest of the document, because the render happened in the site's own execution environment at capture time while the HTML/CSS rendition is rendered later in the reader's. That needs the Browser Rendering path, which is available on Workers Free at 10 minutes a day. Until it lands, `oaklandca.opengov.com` and every portal like it can be captured and cannot be cited, and the record should say so on the document page rather than only at capture time. Blocked behind D-55 (per-origin authority for a rendered capture) for anything carrying third-party script output. | M2 · BLOCKED on D-55 · open |
| D-65 | gap | 2026-07-30 | **Monitoring contracts exist and nothing consumes them.** `docprofile/monitoring.mjs` decides, per document kind, whether to watch substance, membership, or nothing at all, and diffs an index's entries into removed / altered / added with removal and alteration as events and addition as routine. Validated against real Legistar rows. The PLANE's watcher does not call any of it: monitoring still compares raw hashes on every kind of document alike, so a delisted meeting is invisible and a reissued viewstate reads as a change. Two further pieces belong with the adoption. The daemon's per-bundle monitor FREQUENCY should follow the document kind, because a delisting is time-sensitive and a record rarely moves, and the current one-frequency-fits-all is what makes the noise expensive. And a confirmation needs somewhere to land: "all 18 entries still present and unchanged" is dated first-party evidence that nothing was withdrawn, which is the primary contemporaneity route's raw material, and today it would be computed and discarded. | M3 · open |
| D-66 | gap | 2026-07-30 | **Only one content type is written, and the others are named but unmeasured.** `docprofile/doctypes/` has the layered structure and `meeting_calendar`, written entirely from a page that was fetched, diffed and read. Bob named the others directly and each needs the same treatment before it is written: a regulation or ordinance (what is an amendment versus a renumbering), a staff directory (a title change may matter, a phone extension may not, and departure is the significant one), an agenda (an item added or withdrawn before a meeting, and the relationship to the minutes that follow), meeting minutes (attendance, votes, and continuances), and a budget or dataset. The generic fallback reports change without describing it, which is noisy and honest, and the noise is the prompt. DO NOT write a type from what a document probably looks like: a content type invented rather than measured reassures people about things it has not understood, which is the one failure mode that matters here. | M2 · open, ongoing |
| D-67 | gap | 2026-07-30 | **Connections are emitted and nothing stores, relates or shows them.** `meeting_calendar.connections()` produces referential and temporal edges from a real calendar (measured: 39 referential, 11 temporal from one page), including the valuable form, an absence with a due date, such as minutes not published three weeks after a meeting. Nothing consumes them. Three pieces are missing. STORAGE: the plane has `links_to` in `REL_VOCAB` for source-asserted links, and these are SYSTEM-asserted inferences about meaning and sequence, which is a different `asserted_by` and probably a different vocabulary. AGEING: a temporal absence with an `expected_by` needs something to notice when the date passes, which is monitoring's job and not the parser's. PRESENTATION: Bob was explicit that the two kinds are understood and processed differently by people and their assistants and must be presented differently in the UI, and the document page currently has one link surface built for source-asserted links only. | M4 · STORAGE DONE (FW-8, fe81904): connections now PERSIST (connections table, op=connect/connections). Relating/showing (ageing + presentation) remain — slice B + Steps 6/8. |
| D-68 | design | 2026-07-30 | **Seven vocabularies were invented in one day and at least six of them overlap; reconcile before building further.** Named and evidenced in `docs/architecture/CONSTRUCTS.md`. The specific collisions: `CONFIDENCE` and `TYPE_CONFIDENCE` are one idea duplicated because the content axis was split off after the stack axis already had a ladder; `monitor()`, `compare()` and `assess()` are three public entry points to one question, of which `assess()` is the complete one; `diffMembers` and `diffEntities` are the same function written twice, hours apart, the second strictly better because it names which fact moved; `CONTRACT` is derived from document kind and so belongs to the content type rather than the stack handler; event type strings (`delisted`, `minutes_replaced`, ...) are ad hoc inside one content type with no catalogue, which is the lesson the check catalogue already taught the plane; and `meaningful` is a boolean derivable from `SIGNIFICANCE` rather than a second fact. This is Step 0 of the plan and it is the only step that deletes more than it adds. Doing it after the next capability means building that capability twice. | M3 · CLOSED 2026-07-31 (FW-2, commit 6f1a70f): reconciled to one confidence ladder + one recogniser/registry engine + assess() as sole entry; monitoring.mjs, diffMembers and the duplicate loops deleted; docprofile net -27 lines; civicos-ui + full battery green |
| D-69 | gap | 2026-07-30 | **1,463 lines of validated architecture have zero consumers, and that ratio is the real measure of the diffusion.** `docprofile/` (stack handlers, three digests, fidelity levels, monitoring contracts, content types, the layered pipeline, connections) runs only inside the UI's flattened copy. No plane file imports any of it; monitoring still compares raw hashes on every document alike; `profileRecord` is never written to a capture; confirmations are computed in three places and stored in none; connections are emitted and discarded. The plan in CONSTRUCTS.md exists to fix this and its governing rule is that no step is done until something consumes its output. The single cheapest and most load-bearing step is Step 1: `op=acquire` calling `identify()` and `doctypeFor()` and writing the profile onto the capture, roughly twenty lines, without which no verdict computed later can be re-evaluated when a handler turns out to have been wrong. | M3 · open, blocks |
| D-70 | design | 2026-07-30 | **The framework treats "which axes of variation exist" as fixed at two, and history says a third arrives within days.** `BIO_Content_Framework_v0_10.md` §4 defines a uniform recogniser shape and a registry per axis, and names the two axes in use (host stack, content type) plus candidates already visible: authority class (the issuing body's own publication versus a mirror), access mode (public, paywalled, login-walled, rate-limited), and format (HTML, PDF, dataset, scanned image needing OCR). §11 names the likeliest next one outright: entities that OUTLIVE documents. A person, a body, a parcel, an ordinance are things the record will want to track across documents, while readings currently scope entities to one document. The framework is written so a new axis costs a registry rather than a rewrite, and that claim was untested: no third axis had been added, so the uniformity of the recogniser shape was an assertion rather than a demonstrated property. The first genuine third axis is the test of whether §9's cost table is real. VERDICT (2026-08-03, COFF-1): the FORMAT axis is that third axis and the claim HELD — `bio-plane/src/formats.mjs` (I7, CONFIRMED 1.0.0) is one registry entry per format, both dispatch sites (the acquire-time subresource guard, `op=pdfstructure`) consult only the registry, and `formats.test.mjs` proves a TEST-ONLY stub format registers and becomes reachable through detect→structure with ZERO edits outside the registry file. §9's cost table is real for the FORMAT axis: a new format costs `registerFormat(entry)` and nothing else (COFF-3/4/5 pay exactly that). The §11 candidate (entities that outlive documents) remains future and is its own axis, not this row's residue. | closed 2026-08-03, COFF-1 — the axis-cost claim demonstrated for FORMAT |
| D-71 | gap | 2026-07-30 | **Readings are transient, which silently blocks everything BIO exists to do.** A content type's `parse()` produces entities with stable keys and named facts, and the result is used for one comparison and thrown away. Nothing stores it, so nothing can ask "which documents mention this ordinance", which is the single largest piece of manual work in case development and the thing Bob named when restating BIO's purpose. Entities cannot be resolved ACROSS documents if no document's entities are held. This is Step 3 of the plan and it is small: readings are derived and cheap to recompute, so they need no ceremony, only a place to live and an index by entity reference. It is listed as a gap rather than a defect because nothing is wrong with the code; the capability was simply never built, and its absence was invisible while the only consumer was a two-capture diff. | M4 · open — CORRECTED 2026-08-07 (session BOB, FINDINGS-WORKPLAN B2): readings ARE persisted now (FW-5 done; CONSTRUCTS Step 3 landed the place-to-live and the index by entity reference this row asked for), so the "blocks the entity axis" claim is stale and withdrawn; the row's item text describes the state BEFORE FW-5 |
| D-72 | design | 2026-07-30 | **Connections have no GRADE, so a case cannot show which of its links are the weak ones.** RULED by Bob: BIO's contribution should "improve the grade of connections overall". Framework §8.1 defines the scheme on the model of capture grade: A, the source's own identifier with both ends captured; B, an identifier the source uses matched exactly at both ends; C, correspondence such as a name or a date proximity, plausible and never presented as established; D, asserted with no captured basis, recorded as testimony with an author. Nothing implements it. Two distinctions must survive implementation, because both are easy to lose: grade is NOT credibility, since a Grade D connection from a member who was in the room may be the most valuable thing in a case and is labelled by author rather than discounted; and grade is not `asserted_by`, since the author says who claims it while the grade says what would be needed to check it. The value is that grade is IMPROVABLE, and raising it is work a machine does well and a person does slowly. | M4 · CLOSED for connections (FW-8, fe81904): every connection carries the §8.1 grade = weaker of its two ends; established derives from it structurally. |
| D-73 | design | 2026-07-30 | **The connection table models a pair and the domain needs a chain.** RULED by Bob: the meeting-agenda-minutes sequence is ONE form of connected data, and the system must support many types of happenings and progressions, his example being need, budget request, budget approval, RFP, responses, award, signed contract. Framework §8.2 generalises the table into progressions with stages carrying `after`, cardinality, interval and required-ness. The construct that falls out and did not exist before is the MISSING PREDECESSOR: a later stage present with no earlier one. It is usually sharper than a missing successor, because late minutes are an administrative lapse while an award with no solicitation is a question about how public money was committed, and it is epistemically stronger, since a missing successor may simply not have happened yet while a missing predecessor either exists somewhere or never existed and both answers are worth having. Also needed: exception documents that lawfully discharge a skipped stage, so the question becomes "where is the document saying this may be missing" rather than "why is this missing"; and junction checks as data, which is where a case actually gets made. | M4 · CHAIN DONE (FW-8 rule+2-node, FW-9 f000854 N-stage instances with weakest-grade inheritance). The domain's chain is modelled and threaded; exception/junction refinements remain (slice C). |
| D-74 | gap | 2026-07-30 | **Oakland's shared identifier spaces have not been measured, and they are the cheapest large win available.** Framework §8.3: a progression inside one system can reach Grade A because that system links its own stages, but one crossing systems collapses to Grade C, since a procurement portal, a finance system and a legislative record each keep their own identifier space and none links to the others. That is precisely where the most consequential progressions live. The lever is that institutions DO reuse certain identifiers across systems, and finding which ones is empirical work exactly like measuring a host stack: a contract or purchase order number, a project or CIP number, a resolution or ordinance number, an APN, a fund or account code. Each identifier found in two systems converts an entire progression from Grade C to Grade B. This should be recorded per institution the way stack measurements are recorded per host, and for Oakland none of it has been done. | M4 · open, high value |
| D-75 | design | 2026-07-30 | **The content framework and the object catalogue have been developed independently and have never been connected.** Framework §12, added at Bob's direction that the system must support goals, objectives and aspirations: the record already models intent as `project` (which carries an `objective` field, states `forming → investigating → matured → closed`), `focus` and `problem` (open questions and obstacles), and `action` (steps). The framework was written through eleven sections without touching any of it. Two object types genuinely do not exist and are needed: ASPIRATION, a standing commitment that never closes and sets priority, and GOAL, a bounded pursuit that eventually does. Conflating the three is the ordinary failure: an aspiration written as an objective is never finished and demoralises, and an objective written as an aspiration is never checked and quietly abandons itself. | M4 · open |
| D-76 | design | 2026-07-30 | **Progress on an objective is reported rather than derived, and it should be computed from the record.** Framework §12's load-bearing idea: an objective states its SATISFACTION CONDITION in the framework's own vocabulary, e.g. "hold a Grade B or better procurement progression instance for every contract over $250k on fund 3100 since FY2019". Three things follow and each is worth the work on its own. Progress becomes derived: "41 of 58 at Grade B, 12 Grade C for want of a shared identifier, 5 with no solicitation and no exception document" is computed, so nobody has to be trusted about how it is going. The gaps become the WORK LIST, with the 12 naming exactly what measurement would raise them and the 5 being records requests with the request already specified. And an AI assistant working the objective becomes auditable in the same terms as anyone else, since everything it produces carries provenance and grade. | M4 · open |
| D-77 | doctrine | 2026-07-30 | **Goal-directed work is where a case turns into a brief, and nothing guards against it.** Now invariant 7 of the framework: a goal may direct what is SOUGHT but must never filter what is recorded, retained or shown about what was found, and a finding that cuts against a goal must be surfaced at least as prominently as one that supports it. This is the invariant most likely to be violated BY ACCIDENT, because helpfulness looks exactly like it from the inside: an assistant pursuing an objective, ranking findings by relevance to that objective, is one short step from an assistant that quietly does not mention the inconvenient one. `BIO_Declared_Bias_v0_1.md` is the doctrine this connects to and neither is implemented. The concrete mechanism needed is in Step 8b: every finding takes the same path to a member regardless of which way it cuts, and a deferral is recorded with its reason. | DOCTRINE (Bob) · open |
| D-78 | defect | 2026-07-30 | **Both bundle writers hardcode `surfaced_by: human`, so an assistant cannot honestly surface a focus.** RULED by Bob: an assistant may open a focus unattended, because that support is central to what BIO should offer and a focus is informative and advisory rather than committing. The check catalogue already permits it: C-2.8 accepts `agent` or `human`, which means the doctrine was allowed for at the check level long before any surface could express it. Both writers, `bio-plane/src/setup.mjs` and the ported copy in `civicos-ui/app.html`, emit the literal string `human` for every focus and problem regardless of who is writing. The fix is small and the consequence is not: without it an assistant-surfaced focus is indistinguishable in the record from a member's, which is a false attribution in a system whose whole point is that claims carry their author. RESOLVED 2026-07-31 (REC-3): rather than trust a caller-asserted literal, `surfaced_by` is now SERVER-STAMPED at the trust boundary in `bio-plane/src/index.mjs` op=promote, exactly where author/owner/by/viewer are already stamped — a focus CREATED by a machine credential (viaSession false) records `agent`, one by a member's session `human`, the caller's value overwritten and the bundle.md sha recomputed. This was NOT put in store.mjs's promote: the store byte-trusts bundle.md and 40+ suites depend on that (e.g. projection.test projects an arbitrary surfaced_by verbatim), so the honest decision belongs where the actor class is known — and stamping there fixes BOTH writers (setup.mjs AND civicos-ui/app.html) at once, since the plane, not the surface, is now authoritative. Stamped on the creation act; a revision carries the document's value forward. `test/surfaced-by.test.mjs`: a member-surfaced focus records human, an agent-surfaced one agent (the hardcoded `human` does not survive); negative control RUN (force the stamp to human → 2 fail). The DISPLAY half is D-82, still open. | resolved 2026-07-31, REC-3 |
| D-79 | design | 2026-07-30 | **Unattended surfacing needs aggregation and ageing, neither of which exists.** An assistant that can open a focus without a member can produce hundreds where a person produces one, and framework §12 states the two disciplines that requires. AGGREGATE, DO NOT MULTIPLY: one junction check firing across 58 contracts is one focus with 58 instances, not 58 focuses, because "why do these 58 awards have no solicitation" is one question; getting this wrong does not corrupt the record, it drowns it, which for an advisory object is the same failure. AGE RATHER THAN VANISH: a machine-surfaced focus nobody has acted on after some interval moves to `deferred` with the reason recorded, never silently disappearing, because a finding that disappears is indistinguishable from one that was never made and that rule does not relax because the finder was a machine. Neither the aggregation key nor the ageing job exists. | M4 · open |
| D-80 | design | 2026-07-30 | **Contact between aspirations is not detected, and contradiction is a thing to FIND rather than prevent.** RULED by Bob: contradicting aspirations are welcomed, because we may not realise that they contradict and because we learn from trying to achieve aspirations whether they are achieved or not. That removes the arbiter from the design deliberately rather than leaving it unfinished: no precedence between group, project and member scopes, and no forced resolution, since forcing one would suppress exactly the discovery the ruling is pointing at. What remains to build is detection of CONTACT, not of contradiction, because judging whether two prose commitments contradict is not something this framework can establish and inventing that verdict would violate invariant 5. Contact IS establishable: two aspirations directing work at the same entities, touching the same progressions, or ordering the same queue differently. What counts as contact is named in framework §12.1 and not specified, and the surface that shows it does not exist. | M4 · open |
| D-81 | design | 2026-07-30 | **An aspiration accumulates learning and has nowhere to put it.** Follows from Bob's ruling that we learn from pursuing aspirations whether or not they are achieved. An object that never closes has no natural moment at which anyone writes down what it taught, so framework §12.2 adds a pursuit record: the objectives opened under it, the findings adopted and deferred, the records requests and their answers, the measurements, and the dead ends. Three properties are the whole value and each is easy to lose. A DEAD END IS KEPT, since "we assumed the fund code would appear in the procurement portal and it does not" is the kind of thing every member relearns individually and nobody writes down, and it is exactly what makes the next member's work cheaper. It is NOT A PROGRESS BAR, since an aspiration has no completion; objectives beneath it carry satisfaction conditions and derived progress, the aspiration itself does not. And it SURVIVES ABANDONMENT: retiring an aspiration after two years should be an act that captures what it taught rather than one that archives a folder. | M4 · open |
| D-82 | design | 2026-07-30 | **An assistant-surfaced focus must be visually distinguishable, and no surface does it.** RULED by Bob: the appearance of an assistant-surfaced focus should communicate that it is one. The record already carries the distinction once D-78 is fixed; this is the display half. The framing matters for how it is built: this is NOT a discount applied to the question, since a good question stands on its merits whoever asked it. What a member needs to know is that nobody has yet judged it worth asking, which is exactly the difference between a machine noticing a pattern and a member deciding it matters, and the marking is what lets them give it the reading it deserves rather than assuming a colleague already thought it through. Applies to the focus list, the review queue, and anywhere a focus appears beside a project. | M7 · open — the DISPLAY doctrine is REALIZED for the PROPOSAL by UI-5 (2026-07-31, ui-agent-5): the proposal surface (`civicos-ui/app.html`, `proposalDerivedBadgeHtml`) renders a derived finding VISIBLY as one — "Surfaced by the record — not yet judged / nobody has yet decided it is worth pursuing" — and never as an established fact, with the negative control (neuter the marker → reads as established) RUN in `act-proposal.test.mjs`. That is the display half for a proposal, which is what QUEUE UI-5 targeted. What REMAINS open under this row is the SAME marking on a `surfaced_by: agent` FOCUS wherever a focus is listed (the focus list, the review queue, beside a project) — those surfaces still do not distinguish an assistant-surfaced focus. |
| D-83 | design | 2026-07-30 | **The bias doctrine's SUBJECT REGISTRY and the framework's entity axis are the same construct, and are on course to be built twice.** `BIO_Declared_Bias_v0_1.md` safeguard 4 requires a subject registry: a bundle of entries for sources, institutions, offices and movements, each with aliases, plus declared relations between them (`proxy_for`, `member_of`, `overlaps`), each justified and citable. That is an entity registry. Step 4 of the CONSTRUCTS plan must produce the registry the bias doctrine needs, and the requirement runs both ways. The doctrine CONSTRAINS the entity axis: aliases and member-declared relations must be first-class and citable, so an entity model deriving identity only from source identifiers would leave safeguard 4 unbuildable. And the entity axis SERVES the doctrine's stated prerequisite, that "evidence items need source attribution the system can match", since resolving a reference in a reading to a registered entity IS that attribution. One caution: a declared relation is CONSTITUTIVE rather than evidentiary, the group fixing what its own statements mean rather than claiming something checkable, so it sits outside the A-to-D connection grade and grading it Grade D would be a category error. | M4 · SLICE A LANDED (FW-6): the registry itself is built ONCE — `entities`/`entity_aliases`/`entity_relations` (I5 1.2.0), a union kind vocabulary reconciling safeguard 4's four subject kinds with the framework's entity kinds (DEC-6 leaves the bias-statement-reach question for Bob), aliases first-class, declared relations carrying justification+citation and structurally NO grade column (the category error is prevented by shape, asserted in entityregistry.test.mjs). What REMAINS open: RESOLVING a `reading_refs` reference (FW-5) to a registry entity, declaring the resolution method as the §8.1 connection grade — the recognisers, the NEXT slice. The "built twice" risk this row named is closed; the construct is one. |
| D-84 | defect | 2026-07-30 | **`object_type: bias` does not exist in the check catalogue, so a bias bundle cannot be written.** The catalogue carries information, focus, project, action and problem. `BIO_Declared_Bias_v0_1.md` specifies bias sets as bundles of `object_type: bias` precisely so they inherit append-only history, member-authored transitions, convergent promotion and conformance checks without inventing governance. The doctrine is three days old, fully worked, and unimplementable at the first step. Small: a type, its states, its heading set, and the statement-anatomy checks (kind in the closed set of three, subject referencing the registry, justification required, citation required for kind=pattern, and the malformedness refusal). **SCOPE ADDED 2026-08-04 by CONDUCT, enacting DEC-54 (Bob) — whoever closes this row must build to these four, and they are here rather than only in the register because a design recorded where the decision was made reaches only the person who already knows it.** (a) An INHALED external policy **SPLITS** into bars (`required_strength`) and bias statements (scrutiny / inference / pattern): one document contains both, and conflating them breaks the gate/disclose distinction DEC-20 rests on. (b) **The UNENFORCEABLE RESIDUE is a first-class published output, not a log line** — a case saying "held to AP's standards" must ALSO say which of those standards this system does not check. The evidence is in `research/SEARCH-COMPLETENESS.md`: the extractable rules are the countable ones, and in four of five documented verification failures **the countable rules were formally satisfied while the uncountable properties failed** — so enforcing the extractable half silently would deliver enforcement of precisely the part that does not protect, wearing the authority of the whole policy. (c) **INHALE MEANS PROPOSE FOR ADOPTION, NEVER INSTALL** — adoption is an authored, attributed act (DEC-46, D-90, D-82), because otherwise adopting a policy LAUNDERS a standard: "we follow BBC standards" with nobody in the group having authored anything, which is the never-prefill violation wearing a compliance badge. (d) **The adopted policy is PINNED — source, retrieval date, content hash — and a published case names the VERSION it was held to.** An external policy moves; a case published under it must stay checkable after it moves. That is DEC-12's edition pattern and D-183's calibration pattern at a third altitude, and it is what makes REGRADE meaningful across organisations: two lenses, both pinned, both re-runnable. | M4 · open |
| D-85 | design | 2026-07-30 | **An assistant-surfaced focus must carry the bias manifest in force when it was surfaced, and nothing does.** Follows from two facts that met this session. Bob ruled an assistant may open a focus unattended; the bias doctrine says any BIO work done under bias carries the fully declared bias as part of that work's evidentiary record. The assistant is doing BIO work, the effective statement set shaped what it scrutinised and which inferences it could draw, and unlike a member it will not remember. Without the manifest a focus surfaced under one lens is indistinguishable later from one surfaced under another, and bias debt cannot be computed against it. This also answers a question the doctrine leaves open: an assistant has no bias of its own to declare, it has the effective set it was run under plus the objective it was pursuing, and recording both is the whole of its obligation. | M4 · open |
| D-86 | design | 2026-07-30 | **Bias debt and temporal expectations are the same shape and should share one mechanism.** Bias debt: the lens changed, so this analysis owes a re-run, and it may not advance its workproduct_state or be ratified until the debt is settled; tracking continuous, settlement batchable. Temporal expectations from framework §8.2: this progression stage is due and has not arrived. Both are an obligation with a clock, attached to an object, blocking a state transition, settleable in batches. Two schedulers would be two sets of bugs about the same thing. Step 7 of the CONSTRUCTS plan is where the ageing mechanism gets built and is the moment to decide it once. **PARTLY SETTLED 2026-07-31 (REC-8, Step 7): the TEMPORAL half is built and the mechanism is decided — an `overdue-scan` consumer on REC-1's reconciling DO alarm detects an overdue required successor (derived on read against an injectable clock, no stored flag), surfacing an `overdue_successor` proposal. The bias-debt half is DEFERRED and rides the SAME consumer shape (an obligation-with-a-clock, batch-settleable) with a different producer — a bias-debt sweep would register beside `overdue-scan` and inherit the reconcile/self-termination, so the "one mechanism, not two" ruling holds. What remains open under D-86 is building that bias-debt producer.** | M4 · open |
| D-87 | design | 2026-07-30 | **A pattern statement's evidence is cited once and never revisited, so a bias can outlive its justification unremarked.** Bob's reframing: a side-effect of this framework's work should be that evidentiary comments accrue to bias records, measuring the extent to which the bias is JUSTIFIED. The doctrine requires a `pattern` statement to cite evidence and refuses to let one leave draft without a citation; what it lacks is TIME. A statement declared in March citing three instances is a different object by August, either because those three became forty or because they stayed three while sixty went the other way, and nothing notices either. This is the INVERSE of bias debt: bias debt says the lens changed so the analysis owes a re-run, this says the evidence changed so the lens owes a re-examination. The second is the one nobody builds because it is uncomfortable, and a group may keep a statement whose grounds have evaporated provided it says why, but the decay must not go unremarked, since a bias whose grounds have quietly evaporated is undeclared in the way that matters. | M4 · open |
| D-88 | design | 2026-07-30 | **A bias statement should be able to carry a MEASURABLE FORM, and it is the same construct as an objective's satisfaction condition.** Framework §13.1. Most pattern statements are not mechanically measurable and must stay analysis; some are, and those should say so, with the measure expressed in the framework's own vocabulary. Three properties keep it honest and each is easy to lose. The measure NEVER EDITS the statement: text and justification stay a member's authored act and the standing measure is a derived dated attachment, because a machine that could rewrite doctrine would be worse than the problem it solves. The scope is REGISTRY-DEFINED and never hand-picked, since a cherry-picked denominator would let a group manufacture authority for a bias, which is the distortion the malformedness rule fights arriving by the back door. And it is reported WHICHEVER WAY IT CUTS, which is the case where invariant 7 actually costs something, because the value of the feature is entirely in the measures that refute. Build it once with §12's satisfaction-condition evaluator. | M4 · open |
| D-89 | doctrine | 2026-07-30 | **Bias must never shape what is captured or monitored, only how conclusions are weighed.** Framework §13.1 settles a question v0.8 asked badly. A pattern statement's evidence comes from monitoring; if monitoring configuration were itself shaped by a lens, a measure computed over that collection would prove only that the bias had been thorough. The fix is to forbid the coupling rather than document it: a scrutiny statement must not cause the system to collect less from a source, or more. This is invariant 7 applied to the bias doctrine itself, and it is what keeps the whole of §13.1 from being circular. Nothing enforces it today because neither bias binding nor per-source monitoring configuration exists, which makes now the cheap moment to write the constraint down rather than discover it later. | DOCTRINE (Bob) · open |
| D-90 | doctrine | 2026-07-30 | **Measure decay is loud and never blocking, which is the general rule that derived things inform and authored acts bind.** RULED by Bob, by pointing at the principle already written: the measure never edits the statement, so a measure that may not edit a statement certainly may not block work resting on one. Now invariant 8 of the framework, and it runs through more than bias: an assistant's proposal, a connection grade, a recogniser's confidence all report and none decides. The asymmetry with bias debt is exact rather than arbitrary. Bias debt blocks because the GROUP changed the lens by an authored act and the block is what makes that amendment mean something; a decayed measure means the WORLD changed and nobody decided anything, and an institution improving its behaviour must not be able to freeze a group's in-flight work. The path from decay to a block runs through a person: the measure reports, a member amends or retires the statement, that amendment is authored, and it generates bias debt in the ordinary way. A group that reads a collapsed measure and keeps the statement anyway has done something legitimate and recorded. | DOCTRINE (Bob) · open |
| D-91 | gap | 2026-07-30 | **A PDF is captured as bytes only: no content, no links, no structure, so the document class this project exists for cannot participate in the citation graph.** RULED by Bob: content should be identified and processed in other document types exactly as it is in HTML. Today `op=acquire` hits the `NOT_HTML` guard at `index.mjs` and a PDF yields a hash and nothing else. Agenda packets, staff reports, budget exhibits and ACFRs are PDFs, and an agenda PDF linking to a dozen item PDFs is precisely the graph CAPTURE exists to build. Mapping onto machinery that already exists: `/Annots` with `/Subtype /Link` and a `/URI` action are the `<a href>` equivalent; named destinations and `/Dest` are the `anchor` partition; embedded files are `intra`; page number plus annotation rectangle is the element reference. Link annotations are cheap and need no font handling; Workers has `DecompressionStream("deflate")` natively so FlateDecode costs no bundled code. Text extraction is the expensive half (glyph-to-Unicode through embedded CMaps) and Bob is right that libraries solve it: `unpdf` is a serverless pdf.js build targeting Workers. The real constraint is bundle size against the 3MB Free worker limit and CPU against the measured ceiling, both MEASURABLE rather than arguable, and an earlier claim here that it was "expensive and failure-prone" was a guess and is withdrawn. DECIDED: CAPTURE extracts structure and hands FRAMEWORK a parsed object tree; `docprofile` decides what the content is, because content identification is FRAMEWORK's subject regardless of container. **PHASE 1 LANDED 2026-07-31** (area CONTENT-PDF, commit 0b1c6ce): `bio-plane/src/pdfstructure.mjs` extracts the outbound-link graph — `/Annots` link annotations classified into the SAME anchor/intra/deferred/refused partitions HTML uses (wrappers byte-identical to subresources.mjs, asserted; plus a first-class `undetermined`), resolving classic objects, FlateDecode, and object streams, and reading a page's `/Rect` + index as the element reference. 46-assertion suite with a run negative control; producer-side shape registered as I2 (provisional) in INTERFACES.md. NOT DONE: text extraction (glyph→Unicode via `unpdf`) is phase 2 and gated on measuring bundle size vs the 3MB limit and CPU vs the ceiling; and live-verify awaits `op=pdfstructure`, a recorded DELEGATION to CAPTURE. **OP WIRED 2026-07-31** (CAP-1, commit 2ab62f4): `op=pdfstructure` serves the structure over the `op=capture` byte path, read-only, same auth. **PHASE-2 MEASURED → GO 2026-07-31** (CPDF-1, MEASUREMENTS.md): `unpdf` bundles to ~2.9MB raw / 0.71MB gzip — FITS the 3MB Free limit with 2.29MB gzip headroom; node-proxy extraction ~0.7–0.9 ms/page. Text extraction (CPDF-2) now in progress. STILL OPEN: unpdf/pdf.js wants the whole document in memory — exactly what I1's range reads avoid for large PDFs — so the authoritative large-PDF Worker-CPU/memory ceiling needs a deployed probe; oversized PDFs are recorded text-undetermined. | M2 · open; phase-1 + op wiring landed, phase-2 GO, text extraction in progress |
| D-92 | defect | 2026-07-30 | **`op=file` with a member token returns intermittent 403 under sequential load.** Found reading the live instance: three bundles failed in one pass and succeeded in the next, with different bundles failing each time, so it is transient rather than a permission boundary. A 403 rather than a 429 makes it read as a refusal instead of a rate limit, which cost a full re-run and nearly produced a false finding that eleven bundles had no recorded source. Whatever the cause, the response should name it. | M7 · open |
| D-93 | defect | 2026-07-30 | **The suite CRASHES rather than skips when `ssh-keygen` is absent, and `sshsig` silently runs two tests short.** `ratify.test.mjs` spawns `ssh-keygen` and dies with an unhandled spawn error, and because `npm test` chains with `&&`, everything after it never runs. Separately `sshsig` reports 16 passes without OpenSSH and 18 with it, so a machine lacking it gets a green-looking partial. The signing path is the one place a false green matters most: every negative control against stock OpenSSH is exactly what is lost. The suite should detect the binary and either skip loudly with a named reason or fail loudly, never quietly do less. | M0 · CLOSED 2026-07-31 (M0-4, 0e2c4f0): npm test IS the discovering runner (no suite hides behind a crash); ratify/sshsig/reuse-ratify/signpage skip LOUDLY by name when ssh-keygen is absent |
| D-94 | defect | 2026-07-30 | **The plane's user-agent was illegible and was the sole cause of the only source-access failure this project has, and the current fix is not durable.** `bio-acquire` and `bio-monitor`, two bare tokens across three call sites that did not agree, both refused with 403 by `www.oaklandca.gov`. MEASURED 2026-07-30, one network, one URL, agent varied alone, eight repeats per string: the honest `CivicOS/<version> (+<url>; instance <name>; <purpose>)` string returns 200 and the bare tokens return 403. Three sessions of reasoning about Cloudflare egress reputation were wrong. FIXED in `userAgent()`, one place, asserted through the op by reading what the source received, with a test that it never impersonates a browser. **WHY THIS STAYS OPEN:** we pass because Akamai does not recognise CivicOS. When the string enters its bot directory it will be categorised and refused exactly as `archive.org_bot`, `Googlebot`, `Bingbot`, `GPTBot` and `ia_archiver` already are on that host. Passing by being unknown is not a position. Needs (a) an allowlist request to the City, (b) the mechanical UA-component probe Bob asked for, so that when admission stops the components can be varied systematically and the pattern MEASURED rather than guessed, and (c) a resolvable contact URL, since the one advertised names a path that does not exist on a domain whose registrar transfer is pending. UPDATE 2026-07-30, same day: (b) is BUILT and validated (scripts/ua-probe.mjs), and its validation run measured the discriminator within our component space: purpose and instance are droppable, REMOVING THE CONTACT URL flips admission 200 to 403 uniformly, second-path confirmed (MEASUREMENTS.md). (c) is FIXED in 0.46.0: the agent advertises the repo URL, which resolves, and the ladder shows that fix was load-bearing. (a) remains, and now has a measured, specific ask attached. | CLOSED 2026-07-31 (DEC-1): the allowlist ARM is closed — Bob ruled the request is not a viable mechanism (blocking the `CivicOS/` token is ONE action for the City and total for us; allowlisting is one request per instance, forever, so the mitigation gets dearer exactly as the project succeeds). The UA exposure REMAINS, unmitigated on that axis; the resilience that SCALES is the archive fallback (CAP-3, now the primary resilience item) and member-driven egress diversity (D-120). Standing assumption raised to "we expect to be seen as hostile to the administration". See `SOURCE-ACCESS.md`. |
| D-120 | design | 2026-07-31 | **Member-driven capture for egress diversity is named nowhere in the ledger.** Raised by DEC-1's enactment: with the allowlist arm closed (D-94), admission on the plane's own egress rests on the CivicOS UA not yet being recognised — not a durable position. The mitigation that SCALES with the project (unlike an allowlist request, one ask per instance forever) is capturing through MEMBERS' own networks: diverse egress that does not all present as one recognisable origin. Needs a design for how a member contributes a capture and how its provenance/grade is recorded (a member's first-party fetch is still evidence at capture time), and how it composes with the archive fallback (CAP-3) as the two scaling resilience mechanisms. | M1 · open (DEC-1) — member-driven egress diversity; placed under M1 in MILESTONES, sibling to CAP-3 as a scaling capture-resilience mechanism |
| D-121 | defect | 2026-07-31 | **A revision can carry a stale `surfaced_by` forward, so an agent-opened focus could later be relabelled `human`.** Raised by REC-3's D-78 fix: `surfaced_by` is now server-stamped at the CREATION act (op=promote, actor class → `agent`/`human`), which is right for the surfacing act. But a later REVISION carries the document's existing `surfaced_by` forward unchanged — the store byte-trusts bundle.md by contract (40+ suites depend on it) — so a reviser could in principle overwrite an agent focus's honest `agent` with `human`. Narrow: outside the surfacing act, requires a revision, and enforcing revision-immutability of the field is a separate store-side change that touches the byte-trust contract. Sibling to D-82 (the DISPLAY half of `surfaced_by`). Not queued; recorded so the next session on the surfacing chain sees it. | M7 · open — revision-integrity of surfaced_by; store-side, sibling to D-82 |
| D-122 | gap | 2026-07-31 | **Connections are only derived by an explicit `op=connect`, and nothing calls it automatically.** FW-8 made op=connect derive+persist a graded connection from the shared-entity collapse, but it is a `contribute` mutation a caller must invoke; no resolve-time hook and no scheduled sweep runs it. So the UI-4 subject view (and the whole connection/progression machinery) shows connections ONLY for subjects someone has manually connected — honestly rendered as "none derived yet", but usually empty. Needs either derive-on-resolve (when a resolution is written, derive connections for that entity) or a scheduled op=connect sweep on the REC-1 DO-alarm (the same walking-task FW-9/FW-10 deferred). Raised by UI-4. | M4 · CLOSED 2026-07-31 (REC-5, 6fdb763): connections auto-derive via a scheduled sweep on REC-1's DO alarm (a `connection_dirty` watermark stamped on new/raised resolutions; bounded, self-terminating, idempotent). The entity axis self-populates — UI-4's subject view now fills without a manual `op=connect`. |
| D-95 | gap | 2026-07-30 | **No per-host request governor, and no request pacing that resembles a human.** Bob: reduce requests to a rate and pattern that feels to the other side like a person, which costs nothing but latency. Belongs in the Durable Object, which serialises and therefore gives a correct global token bucket for the instance for free; a bucket in Worker memory governs nothing because every invocation is independent. Design follows the subrequest-ceiling pattern already proven: our APPETITE is a configured constant because it is ours, their CAPACITY is discovered by being refused and recorded. Config is a table keyed by host with a cautious default, not a constant named for one site. A 429 overrides the bucket entirely and the cooling-off mirrors the counterparty's own escalation. Cannot solve the shared-egress problem: our governor governs our instance only. RESOLVED 2026-07-30, 0.47.0: host_governor table in the DO, appetite configured (12/min default, per-host override, instance binding), capacity by refusal, 429 overrides the bucket honouring Retry-After, escalation doubles per consecutive refusal and resets on success, jittered gaps with a three-token burst, all three outbound sites wired, refusal named HOST_COOLING_OFF. test/governor.test.mjs, 30 assertions both ways. Chosen constants in MEASUREMENTS.md. | resolved 2026-07-30, 0.47.0 |
| D-96 | gap | 2026-07-30 | **`captured_locators` has no `via` column, so observations from different sources are indistinguishable.** Once an alternative source counts as a re-fetch for monitoring (RULED, see `AUTHORITY-AND-TRUST.md`), the contemporaneity bracket arm will compare archive bytes against live bytes as one stream and report a change that is a provenance difference. Two sources agreeing is STRONGER evidence than one source repeating; two sources disagreeing is not evidence of change at all. The arm cannot tell those apart without knowing which is which. Also needed: a capture carries one locator today and needs two, the document address (for an archive capture, the CDX `original` field through our own normaliser) and the retrieval locator actually fetched. RESOLVED 2026-07-30, 0.47.0: via is part of captured_locators' KEY plus the address/retrieval split; derived, reshaped per the links precedent; the bracket arm reads via='direct' only. Live reshape verified: op=audit 31/31 clean after deploy. | resolved 2026-07-30, 0.47.0 |
| D-97 | gap | 2026-07-30 | **`capture.authority` is asserted by the caller and determined by nothing.** `op=acquire` requires `body.authority` as a non-empty string and files it verbatim; nothing derives it, checks it, or records how it was reached. All of Bob's authority rulings therefore build on an empty foundation rather than refine an existing mechanism. Needs `authority_state` (`determined`/`undetermined`), `authority_basis` recorded in BOTH cases, and `provenance_chain` for delegated hops. Design in `AUTHORITY-AND-TRUST.md`. Blocks the publication gate, which cannot refuse an undetermined authority while no capture has one. RESOLVED 2026-07-30, 0.47.0: acquire records authority_state and a dated authority_basis in BOTH cases plus a single-hop provenance_chain; absence of an assertion is honestly undetermined instead of refused (the old NO_AUTHORITY gate forced callers to invent an authority, which was the false assertion the ruling exists to prevent); C-18.1 learned the three-valued contract with legacy documents staying conformant; C-18.9 implements the RULED publication fence, tested both ways. Verified live from Cloudflare egress. The task list the undetermined state routes to is D-98 and remains open. | resolved 2026-07-30, 0.47.0 |
| D-98 | gap | 2026-07-30 | **There is no task or inbox mechanism, so the authority-determination ruling has nowhere to land.** RULED: undetermined authority becomes a task for the project manager, falling back to a group admin where a project has no manager, forwardable to a member better placed to attest. Every user has a todo list behaving like an inbox, and Bob has flagged that its transport and UI surface might later BE email. Consequences: field bounds must be enforced at WRITE time rather than at render time, because an email renders in a client we do not control where a plausible instruction is what phishing looks like; and the record must be transport-agnostic. Build on the gathering queue's shape, reusing C-18.5's grammar bounds and the quoted-data exporter, since the F5 threat is identical. `member_expertise` already supplies routing. Same underlying gap as D-52, which records that BIO has no notification channel at all. GROUNDWORK 2026-07-30: docs/development/INBOX-GRAMMAR.md fixes the field grammar and routing as a CONTRACT, deriving every bound from settled precedent (C-18.5 field-for-field, the RULED routing order, member_expertise's real columns), so this is now a build task rather than a design task. RULED 2026-07-30: an undetermined capture creates a task automatically at capture, through a PRODUCER/CONSUMER QUEUE. The capture path only enqueues (carrying refers_to, F5-bounded subject, timestamp); a separate consumer holds the sole write path to the inbox, applies routing and the C-19.1 grammar, and dedups idempotently on (refers_to, kind). This stops the daemon credential's blast radius at the queue boundary: a leaked capture token can enqueue noise but cannot write a task, set an assignee, or forge history. Transport (Cloudflare Queue vs durable table drained on schedule) is the consumer's choice at build; table-as-queue is the default for staying in the DO and the audit model. Contract in INBOX-GRAMMAR.md updated. RESOLVED 2026-07-31, 0.49.0. Built against INBOX-GRAMMAR.md as a BUILD task: C-19.1 added to `bio-checks` as a sibling of C-18.5, `refers_to` reusing the exported BUNDLE_ID_RE and the locators reusing `isPublicHttpsLocator` rather than restating either, exported for the write path exactly as `checkGatheringGrammar` is, and run at the gate beside it. Catalog 1.16.5 to 1.17.0; `ratify.test.mjs` CORRECTED rather than loosened to a pattern, because the point of that assertion is that a ratification records WHICH catalog judged it. The producer/consumer split is enforced structurally: the capture path reaches only `task_queue`, keyed on (kind, capture_sha) so a re-capture loop dedups against itself, and there is deliberately NO control-plane op for enqueue at all, which is tighter than the contract asked. `taskdrain` is the sole writer. `actor` on every member action is stamped server-side. THREE PLACES THE CONTRACT WAS UNDERSPECIFIED, resolved in the direction its own typing demanded rather than by redesign: (1) `refers_to` is typed as a bundle id but AT CAPTURE NO BUNDLE EXISTS, since op=acquire returns a document that op=promote later files, so the queue event carries a capture_sha and the consumer resolves it through the register; an event whose capture is never promoted WAITS rather than inventing a subject. (2) 'the referred bundle's project' is meaningless for an Information bundle, which is what these captures are, so a project that CITES the bundle supplies the manager, and with no manager and no active admin the task is honestly `unassigned` rather than addressed to a phantom. (3) the dedup is on LIVE tasks, open OR forwarded, not open alone: a forwarded task is still somebody's work and excluding it would let a re-capture spawn a second task for a subject already in flight, which is the flood the dedup exists to prevent. `test/inbox.test.mjs`, 71 assertions, grammar tested both ways with TASK fixtures. A NEGATIVE CONTROL EARNED ITS KEEP: neutering the write-path grammar check left all 67 original assertions passing, because every task the consumer builds from ordinary input is well-formed by construction, so nothing actually proved the write gate fired; an assertion driving it with a capture registered against a non-canonical bundle id was added and the control now fails 3. Live-verified on biosmoke7 0.49.0 in the SCRATCH namespace end to end: a real undetermined acquire enqueued one event and wrote no task, a drain reported it waiting while unfiled, a promote made it resolvable, the next drain created the task, and the task read back off the live instance is CLEAN under the catalog that shipped with it. NOT DONE: nothing drains on a schedule, so a task appears only when op=taskdrain is called. See D-109. | resolved 2026-07-31, 0.49.0 |
| D-99 | gap | 2026-07-30 | **BIO speaks none of the standard web-archive formats, and discards provenance it can never recover.** The archives use WARC (ISO 28500), CDX, SURT and Memento (RFC 7089); BIO uses home-grown equivalents for all four and has no Memento interface, so nothing can query BIO the way BIO wants to query an archive. PARTLY FIXED 2026-07-30: the capture now records every response header in order with duplicates preserved, the requested and resolved locators, the redirect flag and the status. Previously only `content-type` survived, so `Last-Modified` and `ETag` were discarded at the only moment they existed despite both being named in `LINK-FIDELITY.md` as recordable evidence. `peer_address` is recorded as null with a named reason, because the Workers runtime does not expose the peer address of an outbound fetch and an unobtainable value differs from a missing one. STILL OPEN: WARC import and export as INTERCHANGE (the internal store stays content-addressed), and a Memento interface. Note the record is now non-uniform, since captures taken before this carry less, and provenance documents are first-party material and are never reshaped. | M6 · open |
| D-100 | drift | 2026-07-30 | **The Apps Script data plane was deleted, and it was the only component that could reach `www.oaklandca.gov`.** Recorded as a lesson rather than a defect, because the cause is now known and is D-94 rather than the substrate. Six documents exist in the record ONLY because that plane ran on 2026-07-19; nothing else could have taken them, and the loss stayed invisible for five months until a capture was attempted. Two things to carry forward: a migration can remove an access path nobody has identified as load-bearing, and the natural experiment it left behind (Google egress succeeding where Cloudflare egress failed) was read for three sessions as evidence for a network theory that the 2026-07-30 measurement disproved. Two clients differing in TWO variables is not evidence about either one. | WATCH, no task · open as a lesson, no task |
| D-101 | defect | 2026-07-30 | **The template-literal class struck a third time, in schema.mjs, and node --check waved it through.** A D-96 comment quoting a column name in backticks terminated the SCHEMA literal; the stray pair happened to re-balance so `node --check` passed, and only miniflare's parser refused the module. Same class as D-24 (setup.mjs, twice). GUARDED 0.47.0: hygiene.test.mjs now scans schema.mjs exactly as it scans setup.mjs, asserting zero unescaped backticks, zero interpolations, and that the loaded literal ends where the file says it does. The lesson sharpened: a balanced pair of stray backticks defeats a syntax check, so the guard counts ticks rather than parsing. SQL comments quote nothing in backticks. | guarded 0.47.0 |
| D-102 | defect | 2026-07-30 | **The installer wizard binds no INSTANCE_NAME, so every wizard-installed instance identifies as "instance unnamed" in its user-agent.** Found live: biosmoke7's agent said `instance unnamed` through 0.46.0 and 0.47.0 because the binding never existed; grep confirms `newgroup/src` never writes it. The instance component exists so a third party can throttle one operator instead of a provider, and "unnamed" defeats that. The deploy path is FIXED (deploy.mjs binds INSTANCE_NAME from the slug, pushed to biosmoke7, verified live in the provenance chain); the WIZARD path is not, which is the one a new group gets, the same split as D-62. Admission is unaffected: the 2026-07-30 ladder measured instance as droppable. RULED 2026-07-30: the installer wizard must PROMPT the operator for an installation name at setup, and that name becomes the INSTANCE_NAME component of the UA string. This is not just binding a slug the deploy already knows; it is asking a new group what to call themselves, so a third party (e.g. Akamai, IA) can throttle or allowlist one operator by name. The name needs the same write-time bounds as any UA-bound field, since it ends up in a header a counterparty parses. Until the wizard is updated, the reference instance biosmoke7 is bound as 'development' (deploy.mjs binds it; the live agent already carries whatever the last deploy set, currently biosmoke7 as the slug). SIMPLIFIED AND RESOLVED 2026-07-30 (Bob): the instance name IS the worker name. The wizard already collects it as `slug` (validated by slugOk, used for the script and the subdomain), so there was never a prompt to add: it just had to BIND INSTANCE_NAME from the slug it already holds. Done in newgroup/src/index.mjs on BOTH paths, install and update; binding it on update is what retro-names every copy installed before this existed, which are advertising 'unnamed' today, with no operator action. wizard suite +2, 92 passing. deploy.mjs already binds the same way, so plane and wizard now agree and there is ONE source of truth. CONSEQUENCE NEEDING BOB: the reference instance was hand-bound to 'development' earlier this session, which under this rule is a second name that DRIFTS: the next deploy.mjs run overwrites it with the slug 'biosmoke7'. Either the worker is renamed, or the instance is simply called biosmoke7, or an explicit override is added and the one-source-of-truth rule is knowingly relaxed. Not decided here. RULED and DONE 2026-07-30: the interim 'development' override is REMOVED and the reference instance is bound to its worker name, biosmoke7, verified live in the provenance chain. The rule is uniform with no exceptions: instance name IS worker name, on the plane and in the wizard, for this instance and every one that follows. There is no second name and nothing to drift. | resolved 2026-07-30 |
| D-103 | gap | 2026-07-30 | **The governor's state surface is Durable-Object-internal; no control-plane op exposes it.** governorstate and governorconfig exist as store routes for the plane's own use and for suites, but an operator or the UI cannot ask "which hosts are held and why" or set a per-host appetite without a deploy. Small: an OPS entry each, admin/member read for state, admin write for config. Deliberately not folded into 0.47.0, which was already a schema release. RESOLVED 2026-07-30, staged as 0.48.0 (signed 6946361…, NOT yet deployed, awaiting a grant): op=governorstate (admin/member/probe, read) reports which hosts are held and why, so a member watching a capture stall sees the governor is the reason rather than a broken source; op=governorconfig (admin/probe, mutating) sets a host's appetite and REQUIRES a host so no fat-fingered global change is possible, with an omitted appetite resetting that host to the instance default. governorconfig is a null NEEDS entry (bounded by SESSION_OPS.admin like the roster ops, not a section-5 capability), which the capability completeness test confirms. Both ways in the governor suite: a member reads state and is refused config by class. DEPLOYED 0.48.0 2026-07-30, byte-identical to the signed asset, op=audit 31/31 clean. Verified on the live instance: governorconfig wrote web.archive.org at 24/min and governorstate read it back; a member token was refused config by CLASS and the refused write left the value unchanged. | resolved 2026-07-30, 0.48.0 |
| D-104 | design | 2026-07-30 | **A governor-refused monitor tick and a source-unreachable monitor tick look the same to the fallback logic that does not exist yet, and they must not.** When the per-host governor holds a monitor tick (D-95), `op=monitor` returns HTTP 429 `HOST_COOLING_OFF` and writes no tick: honest, because the check did not run. But the archive-fallback ruling shipping next (AUTHORITY-AND-TRUST.md: an alternative source counts as a re-fetch for monitoring after three consecutive failures or fourteen days) counts CONSECUTIVE FAILURES, and a governor refusal is OUR OWN politeness declining, not the source being unreachable. If the fallback counts a governed refusal as a failure, sustained self-throttling would trip the archive fetch: we would fetch from IA because WE paced ourselves, which is backwards and also loads IA for no reason. The distinction is exactly the one the user-agent measurement already taught for a different mechanism: unreachable is a property of a host AND an egress, and "we chose not to ask" is neither. FIX WHEN THE FALLBACK IS BUILT: a governed refusal must not increment the source-failure counter; only an outcome the SOURCE produced (a real 4xx/5xx from the origin, a network failure reaching it) counts toward the threshold. Nothing is wrong in the shipped code, because nothing consumes the counter yet; this exists so the counter is built to exclude governed refusals from the first line rather than discovered to include them after a spurious fallback. RESOLVED 2026-07-31, 0.50.0, and built BEFORE the fallback exactly as this item asked, so the exclusion is designed in rather than discovered after a spurious fallback. New table `source_reachability`, keyed on the normalised DOCUMENT address rather than a host, because one page can be gone while the rest of a site answers. The outcome vocabulary is CLOSED (`success`, `source_refused`, `fetch_failed`, `governed`) on the reasoning that the entire value of the table is telling kinds of not-getting-the-bytes apart, and a free string would let a caller collapse that distinction by accident; `failure` is deliberately not a word it accepts. All four sites in the acquire path are wired: the governor's refusal, the network throw, a non-ok origin response, and success. A governed refusal is counted in its OWN column and moves neither `consecutive_failures` nor the staleness clock nor even `attempts`, because the source was never asked. It is counted rather than dropped so the exclusion can be audited: a number excluded from a decision must stay visible, or a reader cannot tell a source nobody could reach from a source nobody asked, and the verdict's `basis` string says which case it is in words. Bob's ruling 2026-07-31 is the reason this matters more than it looks: the governor is meant to keep traffic low enough that being banned is not a concern, so governed refusals are expected to be COMMON, and a counter that mistook them for source failures would fire the fallback hardest exactly when we were being most polite. One design question the ruling left open and this answers explicitly: the fourteen-day arm measures the length of the CURRENT FAILING RUN, not time since last success, so a document nobody has asked about in a fortnight is not eligible while a document that has been failing since a fortnight ago is; thirty days of nothing but governed refusals is asserted not to age a document into eligibility. `test/reachability.test.mjs`, 34 assertions, every read time-pinned through a `now` parameter so the suite tests the rule and not the day it runs. Negative control: letting governed fall through to the failure path breaks 17 of 34. Live-verified through the real op=acquire path on 0.50.0 for both a 200 and an origin 404. | resolved 2026-07-31, 0.50.0 |
| D-105 | unverified | 2026-07-30 | **The Wayback CDX claims the archive-fallback design rests on could not be checked from this session's egress, so they remain a vendor description rather than a measurement.** ARCHIVE-FALLBACK.md asserts the CDX record shape (SURT urlkey, 14-digit timestamp, original, mime, status, base32 SHA-1 digest, length), the `id_` raw-bytes replay suffix, `warc/revisit` dedup records as dated identical-bytes observations, and the 24-to-60/min rate ceilings. `web.archive.org` returned `hostname_blocked` from the Anthropic container egress on 2026-07-30, so none of it was verified here. The plane's OWN egress is Cloudflare and CAN reach IA (it reaches oaklandca.gov), so the archive session should measure these through the deployed plane before building on them, exactly as the host-stack and user-agent facts were measured rather than assumed. Treat every number and field shape in ARCHIVE-FALLBACK.md as unconfirmed until then. MEASURED AND CLOSED 2026-07-31 through the deployed plane at 0.49.0, store=scratch, three requests to archive.org in total. The container block was reconfirmed first (`Blocked by egress policy`), so the plane's egress is the one that was instrumented, which is also the one that will run the fallback. Record shape CONFIRMED field for field: SURT urlkey, 14-digit timestamp, original, mimetype, statuscode, base32 SHA-1 digest, length; and the `id_` suffix returns raw bytes (HTTP 200, 32,564 bytes, text/html, no redirect). Two things came free that the design wanted: the Memento headers (`Memento-Datetime`, `Link` with rel="original" and a timemap) are ALREADY on the replay response, so 'build to Memento not Wayback' is the lower-effort path rather than only the principled one, and `x-archive-src` names the source WARC. THREE CORRECTIONS, all now written into ARCHIVE-FALLBACK.md and MEASUREMENTS.md: (1) CDX `length` is the compressed WARC record size and NOT the body length, measured 6255 declared against 32,564 received, so it is neither a fixity nor a size check on our bytes; (2) a shared digest can mean an EMPTY BODY, since `3I42H3S6NNFQ2MSVX7XZKYAYSCX5QBYJ` is base32(SHA-1(empty)) and appeared twice in a five-row sample on two 301s, which constrains the best idea in the design: reading a `warc/revisit` as a dated identical-bytes observation holds ONLY for a real 200 with a non-empty body, and any contemporaneity claim built on archive digests must exclude the empty digest explicitly; (3) most rows are not 200, three of five sampled were usable and two were empty redirects, so a fallback taking the most recent row without filtering on statuscode and digest will record a redirect as the document. THE RATE FIGURES REMAIN UNVERIFIED ON PURPOSE and that half of this item stays open as D-111: establishing them requires provoking a 429, and the documented consequence of mishandling one is an hour-long block on Cloudflare's SHARED egress falling on unrelated people, which is not a cost this project gets to impose on strangers to satisfy its own curiosity. | measured 2026-07-31; rate figures remain unverified, see D-111 |
| D-106 | defect | 2026-07-30 | **The installer ships a plane thirteen releases behind the live one, so a new group installs 0.35.0.** `newgroup/src/release.mjs` embeds `RELEASE_VERSION = "0.35.0"` while the plane is at 0.48.0. Everything this session shipped is therefore absent from a fresh install: the legible user-agent (D-94, and with it the ability to reach oaklandca.gov at all), the per-host governor (D-95), three-valued authority and the provenance chain (D-97), the observation-source split (D-96), and the governor's operator surface (D-103). A group installing today gets an instance that CANNOT capture from the City and identifies itself as `bio-acquire`, which is the exact failure this session spent three releases fixing. Found incidentally: `npm run build` regenerates the embedded blob, so the artifact showed as modified in the working tree while the declared version did not move. TWO THINGS TO FIX, and they are separable: (a) cut a new installer release embedding 0.48.0, and (b) the embed step should REFUSE when the embedded version does not match the plane's `package.json`, because a silently stale installer is the failure mode that let this drift thirteen releases without anyone noticing. (b) matters more than (a): (a) is a one-time catch-up and (b) is why it will not recur. Sovereign instances are the whole distribution model, so this is not cosmetic. RESOLVED 2026-07-31, and the mechanism was NOT a forgotten build step: `embed-release.mjs` read the embedded version out of `bio-plane/wrangler.jsonc`, and a tree-wide search found that field has exactly ONE reader, this script. `scripts/deploy.mjs` binds `VERSION` from its command line, so every release from 0.36.0 to 0.48.0 deployed correctly while the config literal sat at 0.35.0 untouched. Re-running the old embed today would still have produced 0.35.0, so (a) was not reachable without (b). Fixed: `package.json` is now the declared authority, `resolveVersion` refuses on ANY disagreement in either direction (a wrangler config ahead of package.json is the same drift mirrored, not a newer release), the refusal names both files and prints the exact edit, and it runs BEFORE the esbuild so a mismatch costs a second and leaves no stale artifact. `wrangler.jsonc` bumped to 0.48.0. TWO FURTHER STALE ARTIFACTS surfaced while fixing this, both consistent with the same pattern and both now corrected: `bio-plane/package-lock.json` declared 0.45.0 (three releases behind, self-healed by `npm install`, which means no clean install had been run in the plane tree since 0.45.0), and the CHECKED-IN `bio-plane/dist/bio-plane.bundled.mjs` hashed `ef23a492…`, matching neither 0.48.0 nor the signed asset. So the version lived in four places and three of them had rotted. Only `package.json` and the deploy command line were right, which is precisely why the live plane was correct while everything built from the tree was not. `package-lock.json` is deliberately NOT gated in hygiene: npm rewrites it on any install, so a check there would fail on a stale checkout rather than on a real defect. Two guards for one invariant, deliberately: the embed refusal cannot be bypassed, and a new check in `bio-plane/test/hygiene.test.mjs` fires cheaply at the moment a release bump creates the drift, in the thread that created it, rather than weeks later in whichever thread next builds an installer. `newgroup/test/embed.test.mjs`, 15 assertions, including a guard that resolves the REAL tree; negative control confirms hygiene fails when the drift is reintroduced. VERIFIED: the rebuilt plane bundle is byte-identical to the signed 0.48.0 asset (`69463617…`), so the installer's built-in copy and the repository copy are now the same bytes and both paths install the same plane; deployed installer read back from the account embeds 0.48.0, contains no occurrence of 0.35.0, and still carries `bindings: []`, the structural no-token guarantee. ONE CORRECTION TO THIS ITEM AS WRITTEN: the blast radius was overstated. `selectRelease` prefers a NEWER repository release over the built-in, and `release/RELEASE.json` has carried a correctly signed 0.48.0 throughout (verified this session against stock `ssh-keygen -Y verify`, with altered-bytes and wrong-namespace negative controls both rejecting), so an install with working network and a verifying signature was already getting 0.48.0. The built-in 0.35.0 landed only when the repository was unreachable or its copy failed integrity or signature. The defect was real and worth the priority it was given, but what it actually removed was the fallback, not the common path. | resolved 2026-07-31 |
| D-107 | gap | 2026-07-31 | **The installer has no scripted deploy, and that is part of why D-106 went unnoticed for thirteen releases.** `newgroup/DEPLOY.md` documents deployment as: open the dashboard, select all in the editor, delete, paste `dist/newgroup.bundled.mjs`, Deploy. A manual paste produces no record of what was deployed, no read-back verification, and no place for a version assertion to live, which is exactly the blind spot the plane does NOT have: `bio-plane/scripts/deploy.mjs` refuses to report success until it reads the script back from the account and hashes it against the signed asset. This session deployed the installer with `wrangler deploy` and verified by reading the script back through the API (version, absence of the old version, and `bindings: []`), which worked but is not written down anywhere. The installer deserves the plane's discipline: a script that deploys, reads back, and asserts both the embedded `RELEASE_VERSION` and the empty binding set, since the no-bindings property is a structural security guarantee that a hand-paste into the wrong Worker could silently break. Not urgent while installer releases are rare, but the cost of the manual path is now measured rather than assumed.  REINFORCED 2026-07-31 by a finding that makes this sharper than when it was written: a fresh checkout on Bob's machine was authenticated to a DIFFERENT Cloudflare account (`neologic`, `2f4cabcc...`) than the project's, because `wrangler` takes its identity from an OAuth session stored on the machine and a machine is often logged in for unrelated work. An installer deploy from that state would have SUCCEEDED and put the installer in the wrong account with no error to notice, which is precisely the class of silent wrong outcome the plane's own deploy path refuses to permit. PARTIAL FIX SHIPPED: both wrangler.jsonc files now pin `account_id`, so the repository rather than the machine decides where a Worker goes. `scripts/deploy.mjs` was never exposed, because it takes the account from CF_ACCT and talks to the REST API directly; the gap was always the installer's `wrangler deploy`. The rest of this item still stands, and the account discovery is an argument for it: the installer deserves the plane's discipline, meaning a script that deploys, reads the Worker back, and asserts the embedded RELEASE_VERSION, the empty binding set, AND the account it actually landed in. | M7 · open |
| D-108 | defect | 2026-07-31 | **`deploy.mjs` verifies the deployed BYTES, but a live Durable Object can keep serving the previous code after the verification passes.** Found immediately after deploying 0.49.0: `op=tasks` answered `unknown op: tasks` from the live instance while the deployed script demonstrably contained the op, and the same call succeeded minutes later with no redeploy in between. The error format is the DURABLE OBJECT's (`unknown op: <op>`), not the control plane's (`unknown op`), which locates it precisely: a fresh Worker isolate accepted the op and forwarded it to a DO still running the 0.48.0 route map. The deploy script's discipline is right about what it checks and silent about what it does not: it reads the script back and hashes it, which proves the BYTES landed, and `/version` is served by the Worker rather than the DO, so a post-deploy check of the version endpoint reports success while every DO-routed op still answers with the old map. That window is invisible to both checks. This is not the deploy script being wrong, it is the verification being narrower than the claim it appears to make. The fix is a post-deploy probe that reaches an op THROUGH the Durable Object and asserts the new surface answers, retrying briefly rather than once, the same fail-closed patience `deployed()` already applies to the upload. Until then, a session that verifies a release in the first seconds after deploy can conclude a shipped op is missing when it is merely not yet cycled, which is exactly the wrong conclusion to act on. CORRECTED AND WIDENED 2026-07-31 while deploying 0.52.0: the original diagnosis, a Durable Object lagging a fresh Worker isolate, was too narrow. The WORKER itself was stale too. Four calls seconds apart in one command got `/version` 0.51.0, two answers from the previous build, and one from the new one, so the rollout is PER-ISOLATE AND NOT ATOMIC and a verification in that window can receive a mix and reach opposite conclusions about the same property. This nearly produced a false security finding: a forged-locator probe appeared to show the new code honouring a caller-supplied replay locator, when the old code was answering. The requirement is therefore stronger than 'retry until the new op appears': a verification must CONFIRM IT IS TALKING TO THE NEW BUILD before believing any answer, including a failure. `/version` is the cheapest such confirmation, it was already available, and it was already answering wrong when first asked. Nothing here is a fault in deploy.mjs, which verified the bytes correctly every time; the whole gap is in what is done after it returns.  RESOLVED 2026-07-31, 0.53.0: `scripts/deploy.mjs` now waits for the instance to SERVE the version before reporting done, and says loudly when it does not. It is not a pass/fail on the deploy, because the deploy succeeded; it is a gate on believing anything measured afterwards. It earned its keep on its first real use: deploying 0.53.0 it printed ROLLOUT NOT CONFIRMED after 60s, which was correct, and the version appeared shortly after. On success it still says that only the WORKER is confirmed and Durable-Object-routed ops may lag, because that half is not something the deploy can check without an instance credential it deliberately does not hold. | resolved 2026-07-31, 0.53.0 |
| D-109 | gap | 2026-07-31 | **Nothing drains the task queue on a schedule, so an auto-created task appears only when somebody calls `op=taskdrain`.** D-98 shipped the producer, the consumer and the split; what it did not ship is a trigger. The RULED behaviour is that an undetermined capture creates a task AUTOMATICALLY at capture, and it now does exactly that up to the queue boundary, but the task itself waits for a drain that nothing issues. Draining from the capture path was considered and rejected: it would put the daemon credential back in the position of causing the inbox write, which is the coupling the producer/consumer split exists to break, even though the consumer would still be deciding every field. The natural home is a Durable Object alarm, the mechanism `selections` already uses (`#armSweep`, self-terminating when the table empties), armed on enqueue and re-armed while `task_queue` is non-empty. That keeps the trigger inside the DO and inside the audit model, and it costs an idle instance no timer. Until it exists the inbox is correct but not yet automatic, and the word 'automatically' in the ruling is not fully honoured. | M1 · open |
| D-110 | drift | 2026-07-31 | **`setup.mjs` still explains a refusal the plane no longer makes.** `acquireWhy` maps `NO_AUTHORITY` to "Say who issued the document." D-97 (0.47.0) made authority three-valued and REMOVED that refusal deliberately: a caller who cannot name the issuing party now leaves the capture honestly undetermined rather than being forced to invent an authority to get past the door, which was the false-precision failure the ruling names. The string is unreachable, so it costs nothing today, but it is a copy of a rule that was overturned sitting in the surface a member reads, and the next person to touch that file will read it as current. Small, and worth removing with the D-98 inbox surface when the UI grows one, since a member who sees an undetermined capture will want the task beside it. RESOLVED 2026-07-31 (REC-3): the stale mapping was deleted from `acquireWhy` in `bio-plane/src/setup.mjs`, and the dead reason's token now appears NOWHERE in the served surface (SETUP_HTML) — not in code, not even in the replacing comment, since carrying the name of an overturned rule into the page a member reads is the drift this row names. `test/setup-honesty.test.mjs` asserts SETUP_HTML no longer contains it while the kept refusals (BAD_LOCATOR) still are explained; negative control RUN (re-add the branch → 1 fail). | resolved 2026-07-31, REC-3 |
| D-111 | unverified | 2026-07-31 | **The Wayback rate ceilings are still third-party description and should stay that way until ordinary use tests them.** The 24-to-60 requests per minute figures, the 429 behaviour, and the claim that ignored 429s escalate to an hour-long firewall block are the remaining unverified half of D-105. They were NOT measured on 2026-07-31 and the decision not to measure them is the point of this entry rather than an omission from it: the only way to establish a wall is to hit it, and the documented consequence lands on Cloudflare's shared egress, which is to say on people who have never heard of this project and get no say. Our appetite stays the conservative 24/min because it is OURS to choose; their capacity gets discovered the way D-95 already discovers capacity, by a refusal arriving in the course of polite use and being recorded with its date and instrument. If a figure is ever needed sooner, the honest route is to ask the Internet Archive, not to probe them. Anyone tempted to close this by testing should read the outreach note at the foot of ARCHIVE-FALLBACK.md first. | NOT OURS TO MEASURE · open, and deliberately not measurable by us |
| D-112 | gap | 2026-07-31 | **The archive fallback's decision half shipped in 0.51.0; the capture half is composed by the CALLER rather than by the plane.** `op=archivelookup` applies the fence, governs the CDX fetch, parses, selects and returns `capture_with: {op: acquire, locator, via, documentAddress}`; `op=acquire` now honours `via: 'archive.org'` and the document-address/retrieval-locator split, files on the same `captured_locators` row as a direct capture, and grades the result C. So both halves exist and each is tested, but nothing composes them into one act, which has two consequences worth naming. First, the CDX evidence does not reach the stored provenance chain: `archivelookup` returns the second, unsigned hop and `acquire` does not receive it, so a capture filed this way today carries the one-hop direct chain with `via: archive.org` on the locator rather than the two-hop disclosed chain the ruling requires. Transitive trust is accepted WHERE DISCLOSED, and right now the disclosure is in the lookup response rather than in the record. Second, a caller could in principle call acquire with `via: archive.org` without having passed the eligibility fence at all. Neither is a hole an attacker gains from (the fence protects the Internet Archive from us, not the record from anyone, and grade C is the conservative direction), but the chain gap is a correctness matter and should close before anything files an archive capture into the real record. The fix is one op that runs the lookup and the capture together and threads the hop through, NOT a caller-supplied provenance parameter: a chain hop a caller can hand us is a chain hop a caller can invent. RESOLVED 2026-07-31, 0.52.0. Composed into ONE act rather than two: `op=acquire` with `via: 'archive.org'` and an `address` now runs the eligibility fence and the CDX lookup ITSELF, through the shared `archiveSelect` helper, and builds the second provenance hop from the CDX record that same call fetched. The stored `provenance_chain` is therefore two hops for an archive capture: ours, honest that we fetched the replay address rather than the publisher, and theirs, carrying the CDX evidence with `bound: false` and the reason it is unsigned. A caller supplies no hop, no replay locator, no document address and no grade; the only field written back onto the request body is the locator, and `documentAddress` no longer exists on a request body at all. Guarded at SOURCE in `hygiene.test.mjs` rather than only at runtime, because a runtime test can show one forged request was ignored while a source check shows the field cannot be read: five assertions that nothing reads a documentAddress, a hop, a chain or a grade off the body, and that the hop and address come from `archiveSelect`. Live on 0.52.0: a request carrying `locator: https://evil.example.com/x` and a forged `documentAddress` answers NOT_ELIGIBLE, the fence having run before anything was fetched, stable across three rounds. Authority is deliberately NOT auto-set to the Internet Archive: doing so would make an archive capture 'determined' and publishable without any member ever asserting who issued the document, when the publisher is reached by delegation and disclosed in the chain rather than asserted by us. | resolved 2026-07-31, 0.52.0 |
| D-113 | defect | 2026-07-31 | **`op=purge` reported scope ALL while leaving the tables added since it was written.** Found during close-out: a scratch purge cleared five bundles and answered `scope: "ALL"`, and an open task pointing at one of the purged bundles survived it. That task then referred to a bundle that no longer existed, which C-19.1 refuses as unresolvable, so the purge had produced exactly the inconsistent state the checker exists to catch. A purge that reports ALL and leaves rows behind is worse than one reporting a narrower scope, because the caller believes the store is empty. FIXED in 0.53.0: the whole-store arm now takes `tasks`, `task_queue` and `source_reachability`, `stats()` reports all three so the removal counts can be proved rather than asserted, and the per-bundle arm deliberately touches none of them for the same reason it leaves selections alone. Verified live: the second sweep removed 1 task and 3 reachability rows and left scratch genuinely empty. THE STANDING HAZARD, which is the part worth carrying forward: this list is maintained by hand and there is nothing that fails when a new derived table is added and not added here. D-98's tables were three releases old before anyone noticed. A hygiene-style check that every CREATE TABLE in schema.mjs is either named in purge or explicitly exempted would close the class rather than this instance. | M0 · open as a class; the instance is fixed |
| D-114 | design | 2026-07-31 | **C-18.9 may be gating publication on the wrong field, and the fence was NOT changed because loosening it is irreversible.** Bob, 2026-07-31: "A capture can be published if there are no undetermined authority links in the provenance. So recording that the capture was done through IA is proper, even if the content authority is still undetermined." That distinguishes two things this codebase currently treats as one. The PROVENANCE authority is who served each hop and is always known: we know we fetched it, and we know the Internet Archive served us. The CONTENT authority is who ISSUED the document and is frequently undetermined. C-18.9 as shipped refuses a bundle at or past verified when any provenance document carries `authority_state: 'undetermined'`, which is the CONTENT axis, so on the reading above it is gating on the wrong one. WHAT WAS DONE IN 0.54.0: only the part that was unambiguous and cheap, namely recording `capture.authority: 'Internet Archive'` on an archive capture, which was Bob's actual correction and removes the objection that doing so would smuggle a document into publishability. WHAT WAS DELIBERATELY NOT DONE: C-18.9 was left exactly as it is. Publication is the one act in this system that cannot be undone, because a published hash answers forever by design, so a fence around it is the worst possible place to apply a session's reading of one sentence. If the reading is right, the correct change is to gate on the provenance chain having no undetermined attestor and to let content-undetermined material publish WITH its undetermined state stated, which is a real doctrine shift and belongs in AUTHORITY-AND-TRUST.md over Bob's signature rather than in a commit message. NEEDS BOB.  RESOLVED 2026-07-31, 0.55.0, catalog 1.18.0, after Bob delegated the determination: "You do what you know to be right." The fence now sits on the PROVENANCE chain rather than the content axis. Refused at or past verified: a captured document with no provenance chain, any hop naming no attestor, content authority undetermined AND silent (no dated basis), and authority_state 'determined' with no authority named. Content-undetermined material WITH a dated basis may now be published. The reasoning is the project's own and is written into AUTHORITY-AND-TRUST.md: refusing publication on a missing attribution recreates at the publication gate the pressure to invent an authority that D-97 removed at the intake gate, and moving that pressure later makes it worse rather than better, because by then a member has done the work and wants it out. What a published hash claims is bytes, address, date and route, not that the document is authentic municipal record. The conformance suite was CORRECTED rather than exempted, both ways, and now asserts that a two-hop archive chain passes, which is the point of disclosure. Ratification remains a member's signed act and an undetermined capture still raises an inbox task, so the question still reaches a person; it simply no longer stops the record saying what it honestly holds while the question is open. | resolved 2026-07-31, 0.55.0 |
| D-115 | gap | 2026-07-31 | **The installer installs ONE Worker, and the topology now has a fleet.** Bob's 2026-07-31 decision moves heavy, dependency-laden functions into dedicated single-purpose Workers the plane calls over service bindings, `pdf-worker` first. `newgroup` installs the plane and nothing else, so a group installing after the fleet lands gets an instance whose PDFs silently do less than every description of it — which is D-106's failure exactly, a quietly different install that nobody notices because nothing errors. The installer must install the fleet, bind the services, and verify each member's version on read-back, and the wizard must say what a member being absent costs. | M7 · open |
| D-116 | design | 2026-07-31 | **Version authority must span the fleet, or D-106's drift class returns multiplied.** A version lived in four places and had rotted in three; the fix made `bio-plane/package.json` the single declared authority with `resolveVersion` refusing on ANY disagreement in either direction. A fleet member is a fifth, sixth and seventh place for a version to live, each with its own deploy and its own rollout, and a plane calling a stale sibling over a service binding is a NEW face of D-108: the plane can verify itself current while the member still answers from the previous build, invisibly to both. Needs the authority rule extended to every member and a post-deploy probe that establishes which build ANSWERED on the far side of the binding, not just on this one. | M7 · open |
| D-117 | gap | 2026-07-31 | resolved 2026-08-07 (BOB sweep) — **the claim is STALE: the instrument is NOT blind to the fleet.** Verified in source: `coverage.mjs:145-187` discovers members from `fleet-member.json`, reads each member's SURFACE table, checks every surface op is reached and a negative control declared, and `fleetUnreached`/`fleetUncontrolled` are gated under `--strict` (`coverage.mjs:186-187`, reported at `:243-244`). Consequence carried to the IS design: IS-9's fleet member must ship `fleet-member.json` + a SURFACE table + suites, or `--strict` fails from its first commit |
| D-118 | unverified | 2026-07-31 | **Service bindings and multi-script accounts on Workers Free are UNMEASURED, and the tiering rests on them.** Workers Paid is an optimisation and never a requirement (RULED), and the sovereign installer puts instances into other groups' accounts, most of which will be Free. Whether a Free account can run a second script, whether service bindings are available on it, and what they cost against the request and CPU budgets are all currently assumptions — the same category as the subrequest ceiling before it was calibrated and the Wayback rate figures that stay third-party. If a Free instance cannot reach `pdf-worker`, Tier 1 is not an optimisation of PDF text extraction, it is the ONLY PDF text extraction such an instance has, which changes Tier 1 from a sizing exercise into a floor. Measure through the plane's own egress before building the Tier 2 path, exactly as D-105 was measured rather than believed. CLOSED 2026-07-31 (CPDF-7), measured on the project's own account (`MEASUREMENTS.md`, the CPDF-7 section): the account is on Workers FREE (the API refused `limits.cpu_ms` with "not supported for the Free plan"), and on that Free plan a SECOND script deploys, a SERVICE BINDING between two Workers deploys and RESOLVES, and a cross-Worker call executes end-to-end for ~1 ms wall-clock / one subrequest (25 binding calls in one Free invocation succeeded). So the conditional does NOT fire: `pdf-worker` (CPDF-6) is viable on Free and central, and Tier 1 (CPDF-4) is not forced to be the floor by any binding limitation. NARROWED, not carried: the one residual Free concern is pdf.js against the 10 ms Worker-CPU/invocation ceiling (measured non-raisable here) — a DIFFERENT axis that already lives as CPDF-1's gated Worker-CPU follow-on and constrains an in-plane Tier 1 equally, so it is not a differentiator. | M2 · closed 2026-07-31 (CPDF-7); the residual CPU-ceiling question stays with CPDF-1's follow-on |
| D-119 | gap | 2026-07-31 | **A reused subresource is never verified against its source, and a ratified bundle can carry one silently.** Asset reuse (0.38.0 onward) spends zero subrequests on furniture the record already holds, which is what makes the 50-request ceiling survivable, and the manifest honestly records `fetched_this_capture: false`. What does not exist is the other half: nothing detects post-hoc that a reused asset later changed, and nothing re-fetches reused parts when a bundle is ratified — the moment its claim changes from working material to evidence. The reuse path's own manifest note TELLS a reader that a ratified capture must re-fetch, and nothing does, so the note describes an obligation the code does not honour. DECIDED 2026-07-31 by session BOB under Bob's delegation, after Bob confirmed the adjacent ruling (a reused asset IS allowed in evidence) and did not recall the mandatory-re-fetch one, which had been recorded once by a session and contradicted by its own document's footer: (a) post-hoc detection from `site_assets` is unconditional and free; (b) at ratification the re-fetch is mandatory as an ATTEMPT AND A RECORD, never as agreement, so `confirmed`/`changed`/`unavailable` all ratify and only silence is forbidden — which is what keeps a source that has gone dark ratifiable, the circumstance the record exists for; (c) a PLAIN GET rather than `If-None-Match`, reversing the conditional-GET suggestion in `CAPTURE-SCALING.md`, because both cost one subrequest and a 304 is the origin's assertion where a hash is our own evidence; (d) a fourth outcome `not_attempted`, recorded with its reason, for parts the invocation budget could not reach, because a ratification that quietly checked 40 of 60 parts claims more than it verified. Full reasoning in `CAPTURE-SCALING.md` item 6. | M2 · queued CAP-4 |
| D-121 | gap | 2026-07-31 | **Spreadsheets, word-processing and presentation documents are captured as bytes only, and the format dispatch cannot absorb them.** Bob's direction: BIO must inspect these as it inspects HTML and PDF, cleanly. Research in `OFFICE-FORMATS.md`. The shape of the finding: `.docx`/`.xlsx`/`.pptx` are ONE container (OOXML = ZIP of XML parts) with three part-maps, and `.odt`/`.ods`/`.odp` are the same shape — so this is not three parsers. MEASURED 2026-07-31 in workerd via miniflare: `DecompressionStream` supports `deflate-raw` and round-trips it (480 raw / 29 compressed / intact), so the container is readable in the plane with ZERO dependency, exactly as `FlateDecode` made PDF phase 1 dependency-free. Outbound links live uniformly in `_rels/*.rels` with `TargetMode="External"` and map onto I2's existing partitions unchanged; embedded files are `intra`. Text is EASIER than PDF, not harder — XML text nodes, no glyph/CMap problem, so there is no Tier 2 and no fleet member for office text. Two element references are BETTER than PDF's page+rect: `Sheet1!B14` and slide+shape are stable and human-meaningful, the first time the record can cite finer than a document without inventing an anchor scheme. THE ARCHITECTURAL POINT: today's dispatch is already TWO mechanisms that do not scale — a hardcoded `HTML_CT` array guarding the acquire-time subresource branch (everything else refused `NOT_HTML`) and a separate read-time `op=pdfstructure`. Three more formats makes five special cases across two mechanisms. The framework already specifies the fix and has never exercised it: §4's uniform recogniser plus a registry per axis, with FORMAT named outright as a candidate axis. So the work is stand up the FORMAT registry and MOVE HTML AND PDF ONTO IT, then add formats as registry entries. | M2 · open |
| D-124 | design | 2026-07-31 | (renumbered from a colliding D-122 by CONDUCT 2026-07-31) **Office formats carry latent evidence HTML and PDF do not, and the same artefacts carry personal data the publisher did not knowingly publish.** From `OFFICE-FORMATS.md`. The evidentiary half is substantial and is why this is more than parity: an XLSX stores the FORMULA (`<f>`) beside its cached value (`<v>`), and for accountability work the derivation is frequently the finding — a PDF or printed copy destroys it permanently; DOCX tracked changes and comments record who changed what and what a reviewer said, which a published PDF is designed to remove; PPTX speaker notes are routinely more candid than the slide; hidden rows, columns and sheets are first-class findings invisible in every rendered form; and `docProps/core.xml` carries creator, `lastModifiedBy`, revision count and instants. The risk is the same artefacts: `lastModifiedBy` NAMES A MEMBER OF STAFF, a tracked change attributes an edit to a person, a comment may be candid about a named individual — none knowingly published, all inside a document the body did knowingly publish. CAPTURING is not in question (the record holds what was served and stripping bytes would break the hash and the premise). SURFACING is a different act with effects on people outside the project, which is the D-77 / invariant-7 neighbourhood and Bob's to rule. RULED 2026-08-01 (DEC-5), and the ruling INVERTS the framing: these are public documents, there is no reason to redact anything from a public record, and who edited a document and when IS evidence — it demonstrates the actions of people and departments. So this is not a disclosure risk to be managed but an EVIDENCE SOURCE to be extracted: creator, lastModifiedBy, revision count and instants, tracked changes with author/date/superseded wording, comments with author and date, speaker notes, hidden sheets — structured, projected, indexed and searchable like any other content. The recommendation this row originally carried ("present and not promoted", kept off indexes and facets) was self-defeating: an analyst can only decide what matters if they can find it, and the patterns that make this material valuable are only visible ACROSS documents. Scope is public records; restricted material is D-124. | M2 · open, ruled |
| D-123 | gap | 2026-07-31 | **I2's element reference is PDF-shaped and office formats need a per-container form, which is `INTERFACE-CHANGES.md`'s first real use.** I2 records `source: {page, rect} | null`. A spreadsheet's reference is `{sheet, cell}` and a deck's is `{slide, shape}`. That is a change to a shape FRAMEWORK builds against, so it goes through the change protocol — and `INTERFACE-CHANGES.md` DOES NOT EXIST, deliberately, because `PARALLELISM.md` says writing it before it is needed gets it wrong. This is the moment it is needed, and a session should expect to write the protocol file as part of the work rather than discovering halfway that there is nowhere to file the proposal. CONCRETELY, today's shape is `source: { page: 3, rect: [x0,y0,x1,y1] } | null`; a sheet needs `{ sheet: "Sheet1", cell: "B14" }` and a slide `{ slide: 7, shape: 3 }`. **AND IT IS NOT SAFELY ADDITIVE, which the first reading of this got wrong:** a consumer written as `if (source) usePage(source.page)` does not break loudly on a sheet-cell source, it reads `undefined` and carries on — so the change REQUIRES consumers to discriminate on a kind tag, which is precisely the "shape another area builds against" case the protocol exists for. A genuine fork the proposal must settle: whether the canonical reference is a STRUCTURED union or a per-container STRING (`"Sheet1!B14"`, `"slide7#shape3"`, `"p7#rect(...)"`) with structured fields beside it. The string has a real argument — `Sheet1!B14` is already the form a person recognises, and the citation surface will want to show it — while the structure is easier to validate. ONE WRINKLE WORTH KNOWING BEFORE PROPOSING: FRAMEWORK owns I2, is dormant, and has never consumed it, so nothing breaks TODAY; the whole reason to use the protocol is that registering an interface means not changing it casually later. CONDUCT answers on FRAMEWORK's behalf IN WRITING (protocol step 3), and that answer must be recorded as CONDUCT answering for a dormant area rather than as FRAMEWORK agreeing — the distinction matters when FRAMEWORK wakes and disagrees. AND A REAL RISK OF DECIDING EARLY: M4 brings readings and the entity axis (D-71, D-83), and an element reference may want to BE the same construct as an entity locator rather than a sibling of it. RESOLVED 2026-08-01 as a PROPOSAL, not a unilateral edit, after Bob delegated it as plumbing. THE FACTS CHANGED WHILE IT WAS BEING REASONED, which is why the answer is not the one drafted an hour earlier: I2 went from PROVISIONAL-and-unconsumed to 1.0.0 STABLE, confirmed by its owner FRAMEWORK (FW-1, framework-agent-2, 2026-07-31). A draft decision that it was provisional and unconsumed, so producer-side revision rather than protocol, was obsolete before it was written -- and this row's original claim, that this is INTERFACE-CHANGES.md's first use, turns out RIGHT for a different reason than it gave. So the protocol applies, INTERFACE-CHANGES.md is written (it deliberately did not exist), and the shape is filed as IC-1 PROPOSED rather than edited into a stable interface by a non-owner: `source` becomes a tagged union with a REQUIRED `kind` discriminator and a REQUIRED human-readable `ref`, with `pdf-page` keeping `page`/`rect` byte-for-byte. Not safely additive, and that is exactly why it is a protocol case: `if (source) usePage(source.page)` reads undefined on a sheet-cell source and carries on. Pure-string was rejected (pushes per-container parsing onto every consumer, where silent misreads live) and pure-structure was rejected (forces every display surface to rebuild the human form). D-121 steps 1-2 proceed meanwhile; neither emits a `source`. | M2 - PROPOSED as IC-1, awaiting FRAMEWORK | M2 · open |
| D-124 | design | 2026-08-01 | **DEC-5 settles public records and deliberately does not settle restricted material.** Bob's ruling — surface everything, internal metadata included, because who edited a document and when is evidence — was given for PUBLIC DOCUMENTS, and he scoped it himself: "there may be other situations where they're not and there are restrictions on what should be on the record or redacted." Three classes are visibly outside it and none is answered: a public-records-request response carrying STATUTORY redactions (where the redaction is itself a fact about the body, and re-deriving redacted content from document metadata would defeat a legal protection the record has no standing to override); MEMBER-ORIGIN or confidential-source material, which C-18.1's custody block already treats differently from a public fetch; and material a body published in error and withdrew, where holding it is settled but surfacing its authorship is not. Recorded now, with a TRIGGER rather than a date, so nobody over-applies DEC-5 to a class it never covered and nobody re-raises it before there is anything to reason about. | DOCTRINE (Bob) · deferred — trigger: the first capture that is NOT a straightforward public publication (a records-request response, member-origin material, or a withdrawn document) |
| D-125 | gap | 2026-08-01 | **A member has no notification preferences, and DEC-10's ruling requires them.** RULED 2026-08-01: an overdue progression stage notifies the member who AUTHORED the connection between that instance and a Focus or Project, and the notification must offer three things -- remind me again at a further increment overdue, stop notifying me about this one, stop notifying me about that GROUP. The plane has no state for any of it: `tasks` carries `open`/`forwarded`/`resolved` and nothing per-member and nothing with a snooze clock. Needed: a per-member preference keyed on the thing muted, where the mute key is expressible on the axes the CONNECTION was made on (progression type, body, or Focus/Project) so a member mutes what they mean rather than a bucket the system chose; a snooze whose increment is THE STAGE'S OWN DECLARED INTERVAL rather than a new constant, since the expectation already carries the number and a second one would drift from it. **THE HAZARD, and it is the one to build against first: MUTING IS PERSONAL, DISMISSING IS A RECORD ACT, and they must never be one control.** If 'stop notifying me' also dismisses the finding, one member's inbox hygiene erases a finding for the whole group -- which is the silent disappearance the discovery-loop doctrine forbids (D-79, Step 8b). The finding stands; only that member's notification stops. NOTE the channel itself already exists: D-98's task inbox is the in-app notification channel, so D-52 ('there is no notification channel in this system at all', raised for export notification) is substantially answered by the same mechanism -- one channel, three consumers, which is the D-86 lesson applied rather than a second scheduler's worth of bugs. AMENDED 2026-08-01 (DEC-10 amendment): notifications AGGREGATE PER CASE, so the preference state is needed at TWO levels, not one -- per item and per (member, Focus/Project) group. The group entry is STANDING and ACCUMULATING rather than a stream, which removes any digest-cadence machinery: one live entry per (member, case) while it holds unhandled events, re-notifying only on a snooze increment or when something new lands after the member last looked. AND THE GROUP MUTE CARRIES ITS OWN HAZARD: it must be scoped to the KINDS PRESENT WHEN IT WAS MADE, so a genuinely new kind of event on that case surfaces again -- otherwise 'mute this case' is a permanent blindfold, which is the same silent-disappearance failure as conflating mute with dismiss, one level up. Grouping itself needs no new stored relation: `tasks.refers_to` names a bundle and the case comes from the citation edge that already exists, so the group is DERIVED from the authored connection rather than duplicated beside it. | M4 - open; the inbox half M8 |
| D-126 | design | 2026-08-01 | **About thirty notification generators exist or are designed, with no catalogue, no classes and no item contract.** Inventoried 2026-08-01 in `NOTIFICATIONS.md` at Bob's direction: clock-driven (overdue successor, temporal expectation, bias debt, recheck sweeps, fallback eligibility, capture-session TTL), data-flow driven (undetermined authority, source modified/removed, governed-vs-unreachable, duplicates, link verdicts, post-hoc reuse divergence, completed walked-away captures, partial captures, text-undetermined, client-rendered shells), governance (endorsements, expertise confirmations, the doorbell, export notification, owner rescue, spent invitations) and analysis (assistant-surfaced focuses, missing predecessors, improvable connection grades, satisfaction gaps, measure decay). THE SIZE IS THE ARGUMENT: thirty ad-hoc sites means thirty invented wordings, severities and button sets, and the drift is guaranteed rather than likely. This is the lesson `CONSTRUCTS.md` item 5 already recorded — ad hoc event strings with no registry, 'the same lesson the check catalogue already taught the plane' — so kinds take stable `N-` ids beside the `C-` ids, and this closes that half of D-68. CLASSES ARE THREE, NOT FOUR SEVERITIES: FINDING (true about the world/record, may become evidence), OBLIGATION (a named person must act for the record to proceed), CONDITION (a fact about our own machinery). A severity ladder was rejected because it encodes how LOUD rather than what it MEANS and rots in one direction — everything becomes a warning; warning/error are severity, which is orthogonal and mostly derivable. FINDING and CONDITION must not merge, or our plumbing dilutes the record's findings and the standing rule that a technical complication is never surfaced as a choice is broken. NEEDED: the catalogue with ids, the item contract (summary, detail, BASIS, producer-published options — never a surface-side map, which is the `op=searchfields` drift), and the `per-item` application weight beside `refuse`/`report`, where a retained item carries its reason. | M4 - open; the surface half M8 |
| D-127 | design | 2026-08-01 | **CASE-MAKING is undesigned, and it is what the whole system is for.** Bob, 2026-08-01: "the CivicOS system is about developing grounded, justified, and meaningful analysis and conclusions. These are built upon evidence and findings from that evidence. We haven't dove into that process of case making yet. But it will certainly involve organizing and building upon this substrate and their derivatives." Everything built to date is SUBSTRATE for this: capture produces bytes and provenance, content produces structure, the framework produces meaning, retrieval finds things, and findings accumulate — and nothing composes them into a justified conclusion a reader can check. The pieces that exist and are unconnected to it: `project` carries an `objective` and a state machine; `focus` holds an open question; citations link Information into a Project with `cites`/`corroborates`; the published surface (UI-PLAN U12) describes a case file as a NARRATIVE with print as first-class, and names the sewer case as the proof piece — but that is a RENDERING of a case, not the process of making one. Missing entirely: how a finding is adopted into an argument, how competing or contradicting findings are held together (D-80 rules that contradiction is a thing to FIND rather than prevent), how a conclusion states what it rests on and what would falsify it, how the grade of a case is derived from the weakest link in its chain (D-72's connection grades are the input and nothing consumes them), and how invariant 7 is enforced in practice — a finding that cuts against the case must take the same path as one that supports it. This is not a milestone gap to be filled quickly; it is the layer the roadmap calls ANALYSIS and it deserves its own design pass with Bob, not a session's guess. **RESTATED BY BOB 2026-08-01 AS THE SYSTEM'S RAISON D'ETRE, which makes it the most important open item in the repository:** CivicOS is about telling a story, or equivalently making a case — capturing the data, rigorously extracting meaning, understanding the legal/regulatory/policy framework, making connections, and figuring out how {information, money, responsibility, relationships, decisions, power} is SUPPOSED to flow and how it ACTUALLY does, where it works and where it does not; and a meta-level above all of that, answering questions, making a case, telling a story and TAKING ACTION to affect the living civic system. "There's nothing about CivicOS and how it works that's more important than supporting users traverse that winding path of questioning, exploring, discovering, documenting, and impacting the civic system." **A STRUCTURAL CONSEQUENCE IT EXPOSES IMMEDIATELY: the milestone ladder has no rung for the thing the system is for.** M0-M8 are substrate and surfaces — running unattended, every document class as evidence, knowing and connecting what it holds, search, leaving, installing, reaching. Not one is "a member can make a case." Read against this statement the ladder is upside down: case-making is not M9 appended after the substrate, it is what the other eight SERVE, and the ladder should say so. Fixing that belongs INSIDE the design pass rather than before it, because the rung's shape is what the pass produces. **And note the fifth verb, which no milestone and no construct touches at all: IMPACTING.** Questioning, exploring, discovering and documenting all have substrate; taking action to affect the civic system has none. **THE PASS IS OPEN as `architecture/BIO_Case_Making_v0_1.md`** (2026-08-01), recording Bob's framing as he gives it plus the observations it raises; nothing in it is settled and nothing builds from it yet. FOUR THINGS ALREADY SURFACED THERE. (a) The constructs CONFLATE WORK WITH OUTPUT while the substrate already draws that line: focus / project / action describe work, a case file is an output, and the two-bucket fence plus ratification is exactly that distinction load-bearing in the plane and unexpressed in the member vocabulary. (b) **A PREVIOUS CLAIM HERE THAT IMPACTING HAS NO SUBSTRATE WAS WRONG and is corrected: `action` IS it** — states planned/active/awaiting_response/resolved/abandoned, headings including `## Correspondence`, fields `action_kind`, `risk_tier`, `counterparty`. Unmistakably outward-facing and in the catalogue throughout. What is missing is not the object but its CONNECTIONS: nothing links an action to the findings that justified it, the case it advances, or the outcome it produced, so the record can hold 'we wrote to the city' and cannot say 'because of these findings, and here is what came back'. The loop from evidence to action to CONSEQUENCE is unbuilt, and the consequence half is itself new evidence about how the system responds — D-128's declared-versus-actual flow measured on our own intervention. (c) Audience difference is probably RENDERING rather than four case types, and the sharp form of it is CLAIM STRENGTH: a journalist can publish 'records suggest', a lawyer needs 'the record establishes', so a case carries claims with derived strength and a rendering selects what clears the audience's threshold and says what it excluded. That gives D-72's unconsumed connection grades a consumer and gives invariant 7 a MECHANISM rather than an exhortation. (d) **~~The government-administrator archetype INVERTS the threat model~~ — STRUCK 2026-08-01, and the row's own correction is recorded here because this is the row a case-making session reads first.** Bob ruled the same day that BIO's doctrine contains nothing resembling "stick it to the man", the objective is BETTER GOVERNMENT, and all stakeholders are assumed to want better outcomes in the public interest — bad actors being real and identified BY EVIDENCE rather than assumed by role. So an administrator is a stakeholder like any other and the question was malformed. THE THREAT MODEL IS SYMMETRIC AND ITS MORE DANGEROUS HALF IS OURS: someone may damage a publisher, and someone may claim far beyond what the evidence supports — and almost every hard-won rule in this repository defends against the second. What remains open is narrower: in an administrator's instance, what does the two-bucket fence protect and from whom? Full reasoning in `BIO_Case_Making_v0_1.md` section 4. The stale claim was — every doctrinal assumption here is outsider-looking-in, and for an administrator the subject and the user are the same institution, so the adversarial fence protects nothing while the impact available is larger. Raised, not answered; it is doctrine. | DOCTRINE (Bob) · open — the design pass, and it is the TOP of the ladder |
| D-128 | design | 2026-08-01 | **The analytic product of progressions is the DELTA between declared and observed flow, and nothing names it as such.** Bob, 2026-08-01: "Obligations are the flows (edges) that go on in a living civic system. Understanding those flows, evolving that understanding, and differentiating between how things are supposed to flow and how they really flow (and the implications of those differences)." This corrects how the OBLIGATION class was first written in NOTIFICATIONS.md — as "a named person must act for the record to proceed", which is a MEMBER TASK and conflated two objects. The CIVIC obligation (the body's declared duty: minutes within N days, an award follows a solicitation) is the primary thing and is a model of how the institution is SUPPOSED to work; the member task is downstream plumbing that frequently arises BECAUSE a civic obligation was observed unmet. Naming them one thing models our inbox and calls it the institution. WHAT FOLLOWS: a progression definition is the DECLARED flow, its instances are the OBSERVED flow, and the divergence has three shapes — missing predecessor (D-73's sharper case), overdue successor (DEC-10), and out-of-order stage (named here for the first time; nothing models it). The delta is not a by-product of progressions, it is the reason to have them, and it needs somewhere to live: a flow model per institution that EVOLVES as understanding does, since a group's picture of how a body is supposed to work is itself a claim that gets refined and should carry its own basis. D-74's shared-identifier measurement is what makes cross-system flows observable at better than Grade C. | M4 · open |
| D-129 | design | 2026-08-01 | **`undetermined` conflates two different claims: WE DO NOT KNOW, and THERE IS POSITIVELY NONE.** Found by the practice survey (`PRACTICE-SURVEY.md`): Wikidata distinguishes `somevalue` (unknown) from `novalue` (positively no value) as separate ASSERTED statements, and BIO blurs them under one word. The conflation is real in our own code, not theoretical — a scanned PDF with no text layer is `novalue` (there is no text to extract, permanently, a property of the document) while a PDF over the Worker envelope is `somevalue` (there IS text and we did not get it, a limitation of our run that a later run could lift). Both currently surface as text-undetermined with the difference only in a reason string. The same split is latent elsewhere: an authority nobody can establish versus a document that genuinely has no issuing authority; a link whose verdict cannot be established versus one established to have no relationship. WHY IT MATTERS BEYOND TIDINESS: the standing rule is that `undetermined` is first-class and must be STATED, and its value to a reader is that it says what we do not know — but 'we could not read this' and 'there is nothing here to read' are different claims about the world, and a member deciding whether to re-capture, escalate or move on needs to tell them apart. One is worth retrying; the other never will be. Cheapest correct shape is probably a second field beside the reason rather than a new vocabulary, but that is a design call for whoever takes it. | M2 - open **WIDENED 2026-08-04 by the archival-systems survey (`STORE-AS-CACHE.md`): the useful state set is LARGER than two, and `partial` is the state BIO most obviously lacks.** Software Heritage stores crawl outcomes explicitly as `OriginVisitStatus.status` in {`created`, `not_found`, `ongoing`, `failed`, `partial`, `full`} — so *we went to this origin at time T and it was not there* is stored DATA, never an inference from missing rows (docs.softwareheritage.org/devel/apidoc/swh.storage.interface.html). Memento (RFC 7089) goes further and makes an archived 404 a CITABLE Memento, MANDATING that a replayed 4XX/5XX carry the same status code, with CDX indexing it in `statuscode` beside the 200s. **`partial` is not hypothetical for us: it is CPDF-5's measured Tier-1-at-88% case and every over-envelope document**, both of which today collapse into the same `undetermined` as a document we never read at all. The governing finding of that survey bears directly on this row — *systems built as ARCHIVES record absence; systems built as CACHES treat absence as a transient failure to be retried* — so this row is load-bearing for the read-through-cache framing rather than tidiness. **SHARPENED AGAIN 2026-08-04 by the caching-systems survey: the principled set is FOUR states with GRADED RETENTION, and there is an AUTHORITY rule BIO already believes in another vocabulary.** RFC 2308 (*Negative Caching of DNS Queries*) separates: the name does not exist (NXDOMAIN, SOA-derived retention, *"one to three hours… work well"*, over a day *"problematic"*); exists-but-not-this-record (NODATA); **we asked and could not tell (SERVFAIL — MUST NOT be cached longer than five minutes)**; and the server was unreachable (same five-minute bound). So: `NEVER_LOOKED` / `LOOKED_ABSENT` / `LOOKED_INDETERMINATE` / `PRESENT`, plus SWH's `partial`. **An indeterminate result must never inherit a definitive lifetime** — CloudFront's error floor is 10 s, Google Cloud CDN publishes 120 s for 404/410/451 and 60 s for 405/501. HTTP encodes the same confidence split more weakly and has NO wire representation for *we have not looked*, which is exactly this row's conflation. **AND RFC 2308 §5 IS THIS PROJECT'S OWN DOCTRINE, ARRIVED AT INDEPENDENTLY IN 1998: *"Negative responses without SOA records SHOULD NOT be cached"*** — the SOA is the proof of AUTHORITY that makes a negative answer a FINDING rather than an absence of finding, so an absence with no authority attached is not recordable at all. That is *an equality that costs nothing to produce is not evidence*, applied to absence. |
| D-130 | defect | 2026-08-01 | **The intake surface writes `counterparty: to be named` on every action, which passes C-2.10 and makes the record assert a counterparty it does not have.** `civicos-ui/app.html:1752` emits `action_kind: other`, `risk_tier: 1`, `counterparty: to be named` as defaults; C-2.10 refuses an EMPTY counterparty and accepts a non-empty one, so the placeholder satisfies the check by being a string. This is the overclaiming class — a field the record fills so a gate will pass — and it lands in the one construct that carries IMPACT, where the counterparty is the whole point of the object. It is also the exact pressure D-97 removed at the intake gate when it made authority three-valued rather than forcing a caller to invent one: `undetermined` is first-class and must be STATED, and "to be named" is a placeholder pretending to be an assertion rather than an honest undetermined. Fix has two halves and the second is the real one: stop writing the placeholder, and give C-2.10 a way to say the counterparty is not yet determined so a member is not forced to invent one to save a draft. Found by the process inventory, 2026-08-01, not by any suite. | M7 · CLOSED 2026-08-04 (REC-23): counterparty three-valued on source's shape; the placeholder refused by name at BOTH emission sites (app.html + setup.mjs — D-130 had named one); coherence rule added; NC reproduced D-130 verbatim before the fix. UI half moves to UI-19 (rendering) + UI-15 (ADD_TYPES absence). |
| D-131 | defect | 2026-08-01 | **A raw NUL byte in `store.mjs` makes the repo's largest source file INVISIBLE to the shell's `grep`, silently.** Found while verifying a research pass. `bio-plane/src/store.mjs:3833` builds a composite dedup key `sk + <NUL> + cs` and writes the separator as a RAW BYTE (offset 230,100) rather than the escape `\u0000`. The intent is sound (NUL cannot occur in either component) and the runtime value is identical; the damage is entirely in tooling. `grep` in this environment wraps ugrep with `-I` (skip binary), one NUL makes the whole file binary, and every grep against the store core returns **exit 1, no output, no warning** — a false negative indistinguishable from 'no matches'. Confirmed by differential: the shell's `grep -c alarm` returns nothing while `/usr/bin/grep -c` and `command grep -c` both return 45. THIS IS THE PROJECT'S FAVOURITE DEFECT CLASS, third instance: `node --check` does not catch it, every test passes, and a verification tool silently stops verifying (siblings D-24, D-101 — a stray backtick terminating a template literal, a balanced pair defeating `node --check`). It matters because sessions verify claims about `store.mjs` with grep constantly: the instrument said the code was not there and the code was there, nearly causing a CORRECT research finding to be discarded. FIX: write the separator as the escape, and add a hygiene assertion that no file under `bio-plane/src/` contains a raw control byte outside tab/newline/CR — same shape as the existing backtick guard, closing the class. | M0 - closed 2026-08-03 (REC-27) — the separator at store.mjs:3833 is now the `\u0000` ESCAPE, value-identical, with a comment stating why; plain `grep -c "CREATE TABLE" bio-plane/src/store.mjs` returns 9 WITHOUT `-a`; and the class is closed as the row asked: hygiene.test.mjs scans every file under `bio-plane/src/` for a raw control byte outside tab/LF/CR, the same shape as the backtick guards. |
| D-132 | defect | 2026-08-01 | **`ADD_TICKS` is used twice in the Add surface and declared nowhere, so a ceilinged capture shows a member a raw ReferenceError.** VERIFIED: `civicos-ui/app.html` references `ADD_TICKS` at lines 6725 and 6825 and no `const`/`let`/`var`/`function` declares it anywhere in the file. Both sites are on the continuation path — the path a member reaches precisely when a capture hits the runtime's outbound-request ceiling and needs another tick, which is the ordinary case for a heavy first capture on the free tier and therefore not an edge case. The member is shown a JavaScript error instead of the choice U8 was built to give them (record it as the unfinished capture it is, or write nothing). This is the one path where the surface's whole job is to keep a technical complication from becoming the member's problem, and it fails by surfacing the rawest possible form of one. | M8 - open |
| D-133 | defect | 2026-08-01 | **Two functions in the Add surface are DECLARED TWICE, so one definition silently wins.** VERIFIED in `civicos-ui/app.html`: `heldMatch` is declared at 6666 and again at 6766; `addCapture` at 6710 and again at 6810. Each is also called once earlier (6570, 6564). Whichever declaration the engine keeps, one body is dead and no tool says which — and the two blocks sit ~100 lines apart in the same continuation region as D-132, which suggests a duplicated edit rather than deliberate overloading. The hazard is not that it crashes: it is that a future session fixes the dead copy, sees no behaviour change, and concludes something else is wrong. Same class as the plane's own duplicate-state-machine finding (three copies of the focus states, with the conformance check binding two of them and leaving the authority unchecked). | M8 - open |
| D-134 | gap | 2026-08-01 | **A complete, tested BALLOT act exists in the UI and nothing can reach it — and zero of the 14 admin-only ops have any UI caller at all.** VERIFIED: `civicos-ui/app.html` declares `canBallot` at 4457 and `openBallotDialog` at 4546, and neither has a call site anywhere in the file. The act has its denominator, divergence handling, pre-flight refusal and receipt — it was built to the ACT construct and exists only for its harness, while the Members screen renders the owner arithmetic with no way to act on it. The process catalogue found the general form: **not one of the fourteen admin-only ops is reachable from the UI**, an axis no earlier inventory measured, which means the entire administrator WRITE surface is absent while the read surface is present. Membership v2's section 4 and 7 governance — seven releases of enforced arithmetic — is operable only by driving the plane directly. | M8 - open |
| D-135 | defect | 2026-08-01 | **The D-15 viewer gate is stamped onto compiled query paths only, so `op=list` shows an uninvited member every project's id, title and state.** VERIFIED at `bio-plane/src/index.mjs:2823-2826`: the `viewer` parameter is set for `search`, `select`, `selection`, `EDGE_ACTIONS` and `STATE_ACTIONS` and for nothing else. `viewerPredicate` exists only in `query.mjs` (121, 607) and is referenced from neither `store.mjs` nor `index.mjs`, so ops that read the `bundles` table DIRECTLY — `list`, `index`, `projection`, `image` — never meet it. `civicos-ui/app.html:952` builds the entire record view from `op=list`. Membership v2 section 7.9 says an uninvited member sees no project AT ALL — not its existence, name, references or participants — and D-15 records that closing it was 'one function, not an audit of every query path'. That claim holds for the compiled paths and does not extend to direct reads. **THE SAFETY COMMENT IS THE SHARP PART.** `index.mjs:2820-2822` states that a viewer the compiler does not recognise compiles to a DENY predicate, 'so the failure mode of a missing stamp is an empty result rather than an unfiltered one'. That fail-closed property is real for ops that reach the compiler and is FALSE for ops that do not: for `list` the failure mode of a missing stamp is unfiltered, which is the opposite of what the comment promises the next reader. SCOPE, stated so this is not over-read: every affected op is member class or above, so this is not a public exposure and not a repeat of D-30 across the two-bucket fence; it is an INTRA-INSTANCE exposure of project existence to a member the project never invited. D-15 also deliberately leaves MACHINE credentials unfiltered, which is sound and is not this. | M7 - open |
| D-136 | defect | 2026-08-01 | **The three ops that implement the section 4.7 ballot and section 4.9 capability edit are unreachable from any administrator's browser.** VERIFIED against `SESSION_OPS` in `bio-plane/src/index.mjs`: `memberadd` and `memberset` are present; `membercaps`, `adminendorse` and `adminremove` are ABSENT. So a signed-in administrator can add a member and change their status but cannot edit capabilities, endorse an addition past the second, or vote on a removal — the governance arithmetic this project spent seven releases enforcing is operable only by driving the plane with a machine credential. This is the missing half of D-134: the UI declares a complete ballot act (`canBallot` at `app.html:4457`, `openBallotDialog` at 4546) with no call site, and there was nothing for it to call THROUGH A SESSION even if it had one. Compounding it, `by` is server-stamped only for `PROJECT_ACTIONS` (`index.mjs:2846`), so on these three ops a machine caller NAMES the voter rather than being identified as them — in a vote whose whole point is who cast it. Intent is unrecorded: the comment at `index.mjs:409-413` explains the token-class choice and is silent on session reach, so this may be an omission rather than a decision, and it should be settled rather than assumed either way. | M7 - open |
| D-137 | defect | 2026-08-01 | **The D-113 class check has a blind spot the size of eight tables, and four of them are silently unpurged.** VERIFIED: `bio-plane/src/schema.mjs` declares 44 tables and `store.mjs` hand-creates EIGHT more — `admin_votes`, `does`, `export_log`, `member_expertise`, `project_owner_votes`, `project_participants`, `selection_items`, `selections` — for 52 in total. Of the eight, FOUR appear in no `DELETE FROM` anywhere in `store.mjs`: `admin_votes`, `does`, `export_log`, `member_expertise`. D-113 was closed by a hygiene check asserting every `CREATE TABLE` is named in purge or explicitly exempted, and that check reads the SCHEMA LITERAL — so tables created outside it are invisible to the guard that exists to catch exactly this. `project_participants` is keyed on a bundle id, which makes it the same silent-leftover shape D-113 was raised for: a whole-store purge reports scope ALL and leaves rows. THE LESSON IS ABOUT THE CHECK, NOT THE TABLES: closing a class by parsing one file closes it only for that file, and the check's own success made the second location invisible. The fix is to enumerate tables from the RUNTIME schema rather than from a source literal. | M0 - closed 2026-08-03 (REC-27) — hygiene.test.mjs now parses store.mjs's CREATE TABLE literals as well as schema.mjs's (the name anchored to the opening paren / USING, so the prose-minted phantom table `does` in this row's own enumeration cannot recur; the eighth table it missed was the FTS5 virtual `bundles_fts`) and reports **52 of 52 tables covered by purge or a stated exemption**. Of the eight: `bundles_fts`/`selections`/`selection_items` were already purged; `project_participants` and `project_owner_votes` (keyed on project_id, a bundle id) added to purge in BOTH arms with stats/removed counters and behavioural assertions in projects.test.mjs; `member_expertise` (roster identity, like members), `admin_votes` (append-only governance record so an ejection stays auditable) and `export_log` (an export can never happen silently, and a purge must not erase the evidence one did) EXEMPT with those reasons stated in the allowlist. Negative control run: removing the project_participants DELETEs fails hygiene NAMING it (51 of 52) and fails 4 behavioural assertions. |
| D-138 | defect | 2026-08-01 | **The UI's state-machine drift guard does not read the authority it claims to check against.** VERIFIED: `civicos-ui/check-semantics.mjs` reads `app.html` and never reads `bio-plane/checks/bio-checks.mjs` (0 occurrences). The UI comment at `app.html:1690-1693` states that this file enforces agreement with the catalogue. It does not — it binds the UI's static copies to each other. So the focus state machine exists in at least three places (the catalogue, the store, the UI) and the check that is supposed to stop them drifting compares two copies while leaving the SOURCE unchecked. A drift in the catalogue — the authority — passes silently. This is the same shape as D-137 one layer up: a guard that appears to close a class and closes only the part it happens to read, with its own green result standing in for the coverage it does not have. | M8 - open |
| D-139 | gap | 2026-08-01 | **Nothing publishes what may be DONE to an object, so every act surface must keep a forbidden surface-side map.** VERIFIED: no `affordances` op and no `options` publication exist in `bio-plane/src/index.mjs`; `op=whoami` publishes capabilities and `op=searchfields` publishes the query vocabulary, and there it stops. The seven `action_kind` values live only in the check catalogue. The interaction constructs make producer-published options a HARD constraint precisely because a surface-side map drifts — and the UI's hand-composed query syntax, drifting from `op=searchfields` which exists to prevent exactly that, is the measured proof. So the ACT construct cannot generalise past one hand-built instance until an op answers: for THIS object in THIS state, what acts exist, at what rung, needing what capability, and what will each refuse. Precedent for the shape is `searchfields`; precedent for producer-declared actions rendered by a surface is GitHub's Checks API (`PRACTICE-SURVEY.md`). This is small and it unblocks the entire M8 surface programme. | M8 - open |
| D-140 | gap | 2026-08-01 | **The queue's two organising axes are not data.** VERIFIED against `CREATE TABLE tasks` in `schema.mjs`: the columns are id, kind, refers_to, capture_sha, subject_text, subject_desc, locators, assignee, assignee_role, status, created, resolved_at, history. There is **no `class`** (FINDING / OBLIGATION / CONDITION, which decides what handling MEANS and whether it is personal or a record act) and **no `case`** (the Focus/Project grouping key Bob ruled on, which is both the relevance filter and the aggregation key). There is also no `queue_state` for the per-member mute, snooze and last-seen that D-125 requires. So the queue as designed — grouped by case, typed by class, with two-level preferences — cannot be built from `tasks` as it stands, and a surface would have to derive class from `kind` by a hardcoded map, which is D-139's forbidden shape wearing different clothes. Note the case key is derivable from the citation edge rather than needing duplication, but it must be RETURNED by the op that lists the queue. | M8 - open |
| D-141 | defect | 2026-08-01 | **The UI rebuilds the project-visibility leak client-side, enumerating every project's projection.** VERIFIED at `civicos-ui/app.html:749-768`: `reverseRefs` calls `loadRecord()` (`op=list`), filters to every `focus`/`problem`/`project`, then calls `getProjection()` on EACH and parses `fm_json` to index their `references`. Its own comment says so — 'walk the projections of every focus and project'. So on top of D-135 (the server never stamps a viewer onto `list`/`projection`), the client actively ENUMERATES what Membership v2 section 7.9 says an uninvited member must not see: not the project's existence, name, references or participants. The two defects compound rather than duplicate — D-135 is the gate that does not fire, this is the caller that walks the whole set anyway, and fixing only the server would leave a client that asks for exactly the thing the server is now refusing. Note the intent is benign and stated ('at the record's scale this is a handful of reads'), which is what makes it the ordinary shape of this failure: a reasonable optimisation at 30 bundles that is a disclosure at 300 and a fence breach at any size. | M7 - open |
| D-142 | defect | 2026-08-01 | **Search silently degrades to a title-substring scan over an unfiltered list, and looks identical when it does.** VERIFIED at `civicos-ui/app.html:4013-4018`: if `op=search` returns no recognisable row shape, the surface falls back to `loadRecord()` — `op=list` — and filters client-side on `title`/`bundle_id` substring, with the comment 'so search always answers'. Three things are wrong and the third is the worst. (1) It bypasses the D-15 viewer gate, since `op=list` carries no viewer stamp (D-135). (2) It answers a WEAKER QUESTION: no full text, no frontmatter, no facets, no relevance — a member searching for a phrase inside a document gets nothing and is shown a confident empty state. (3) **A member cannot tell which engine answered.** The result list, the count line and the empty state are byte-identical either way, so a silent degradation presents as a finished search — which is the 'an outcome that costs nothing to produce is not evidence' family arriving in the UI: a result set that looks like a search result and is not one. A surface that cannot answer must SAY it cannot answer; `undetermined` is first-class here too. | M8 - open |
| D-143 | gap | 2026-08-01 | **A member of the public can verify a hash and list ids, and cannot READ a published case — the surface UI-PLAN calls 'the reason the rest exists' has no data path.** VERIFIED. The unauthenticated (`classes: null`) ops are exactly: `publishedmanifest`, `bootstrap`, `claim`, `login`, `enroll`, `invitelook`, `verify`, `knock`. **Not one of them returns a published bundle's bytes or prose.** And `publishedList` in `store.mjs` selects `bundle_id, bundle_sha, ratified_at, attestor_member, gate_version` — **no title** — which is why the published view renders a bundle id as its heading. So the public surface can today answer 'does this hash match' and 'what ids exist' and cannot answer 'what does it say'. U12 describes the case file as a narrative a member of the public reads start to finish, with print first-class and the sewer case as the proof piece, and names it as the reason the rest of the system exists; it is the one rung with no reachable data beneath it. NEEDED: a `classes: null` op serving a published bundle's prose and its registered bytes from the PUBLISHED bucket only (never the working corpus — the two-bucket fence is what makes an unauthenticated read safe), and a title on the published projection. Note the fence makes this SAFER to build than it looks: that bucket has never held unratified material, which is the same reasoning that already justifies `verify` being unauthenticated. | M6 - open |
| D-144 | defect | 2026-08-01 | **Re-ratifying a bundle DESTROYS the previous attestation — the record can still serve an old published hash and can no longer say who attested it.** VERIFIED at `bio-plane/src/store.mjs:5934-5940`: `published_bundles` is keyed on `bundle_id` alone and the write is `ON CONFLICT(bundle_id) DO UPDATE SET bundle_sha, ratified_at, attestor_key, attestor_member, gate_version, sig_armored` — every field replaced. **ONE CORRECTION TO HOW THIS WAS FIRST REPORTED, and it changes the fix rather than the severity.** The system's headline promise HOLDS: `published_shas` is keyed `(sha256, bundle_id, path)` and is genuinely append-only, so a hash once published stays answerable forever and `op=verify` on an old hash still works — the comment at 5941 saying exactly that is true. What is destroyed is the ATTESTATION PROVENANCE of the earlier publication: who signed it, when, with which key, under which catalogue version. So the record can answer 'yes, these bytes were published' and cannot answer 'by whom, and under what rules' — in a system whose entire product is that claims carry their author, and about the one act it calls irreversible. THE FIX IS NOT IDEMPOTENCE, which is what the first reading would have suggested: re-ratification is legitimate (a bundle revised and re-published SHOULD carry a new attestation). The fix is to make `published_bundles` accumulate like `published_shas` already does — key it `(bundle_id, ratified_at)` or chain each attestation to the one it supersedes — so the history of who stood behind which version is kept rather than replaced. Note a storyboard (`SB-OUTPUT` S16) designs a screen promising exactly that accumulation and cites `published_shas` as its justification without reading the row above it. | M6 - open |
| D-145 | design | 2026-08-01 | **Bundle ids are per-instance sequential, so nothing addressed by id survives leaving the instance that minted it.** VERIFIED: `allocId` (`store.mjs:4401`) mints per-instance sequence numbers, so every sovereign instance independently produces `INFO-2026-0001`. Hashes are global and ids are not. This is a hole under M6's whole promise — a record that can be mirrored, verified and outlived by tooling that already exists — because everything the record says ABOUT a document is addressed by id: `references[].target`, `refs`, `inquiry_basis`, `elevated_into`, every citation edge. An export can be verified byte for byte and its internal graph cannot be re-pointed at anything outside itself, and two instances citing each other would collide on the first id either had ever minted. The design pass asked what GRADE a cross-instance citation carries (`AUDIENCES` row 17) and nobody asked whether it can be ADDRESSED at all. Candidate shapes, none free: an instance-scoped prefix (cheap, changes every id and this record is append-only), a UUID beside the human id (no migration, two identifiers for one thing), or citation by content hash with the id as a comment — which is the shape the record's own doctrine already prefers, since identity is canonical and the address is a comment. | M6 - open |
| D-146 | gap | 2026-08-01 | **Six UI surfaces READ the entity/connection/progression layer and nothing can WRITE it — nine mutating ops landed with no caller anywhere, so the surfaces render empty by construction.** VERIFIED. The UI calls `entity`, `entitybyalias`, `connections`, `concerns`, `resolutions` and `captureprogressions` — all reads. Of the eleven mutating ops in that family, only `taskresolve` and `connect` have any caller: **`entitycreate`, `entityalias`, `relationdeclare`, `resolve`, `resolvetestify`, `progressiondefine`, `thread` and `discharge` have no caller in `civicos-ui/app.html` OR `bio-plane/src/setup.mjs`.** So an entire milestone's write path is unreachable while its read path is shipped and rendering. THIS CORRECTS AN EARLIER CLAIM OF MINE IN THE SHARPEST DIRECTION. I recorded that the A-D grades and the entity axis 'have no consumer until the claim layer exists', and used that to argue the claim layer should be built first so they would acquire one. The literal claim is false and the truth is worse: **they have consumers and no producers.** Six surfaces already depend on this layer and display nothing, and no amount of building above it changes that. The ordering consequence is real: the entity WRITE surface is not a later enrichment, it is what makes six shipped surfaces non-empty AND what produces the earned grades an inquiry's basis legs need (`resolutions` being the A/B/C source). Build the claim layer and the entity write surface together, not one behind the other. | M4 - open |
| D-147 | design | 2026-08-01 | **The `action` object models a records request as one round trip, and the real lifecycle has at least four decision points with their own clocks and outcomes.** `action` runs `planned -> active -> awaiting_response -> resolved / abandoned`, so `awaiting_response` collapses everything between asking and getting. The externally-sourced lifecycle (see `PROBLEM-DOMAIN.md`) has distinct stages that each produce a different fact about the body: the FEE ESTIMATE, the FEE-WAIVER decision, PARTIAL PRODUCTION, the ADMINISTRATIVE APPEAL, and litigation. Each has its own clock and its own outcome vocabulary, and collapsing them loses precisely what an accountability record wants. Federal appeal adjudication averaged 96.91 days in FY2024 [DOJ OIP, government's own statistics] and appeal outcomes vary by an order of magnitude between agencies — complete reversal 9.3% at State, 17.6% at DOJ, 31.4% at DHS [computed from the three agencies' own FY2024 annual reports] — so **a design must not assume a single reversal rate or a single response shape**. This is D-128's declared-versus-actual flow with real numbers attached: the declared flow is the statutory timeline, the actual flow is what these figures describe, and the delta is the finding. | M4 - open |
| D-148 | design | 2026-08-01 | **A fee quote is EVIDENCE about a public body, and the design treats it as an administrative obstacle.** Sourced examples: Colorado's Secretary of State quoted **$2.1M** (69,736 projected labor hours) for election contracts; Texas DCJ quoted **$1,132,024.30** for records the state PREA office supplied for **$551.39** — **a 2,000x spread for identical records**; Nevada DETR quoted $650,000, then **dropped a $1,083 charge entirely and released the records free once challenged**, which is direct evidence the quote was not cost-based; DoD quoted **~$660 million** [MuckRock survey Dec 2023; Las Vegas Review-Journal Sept 2023; Techdirt — press and NGO sources, labelled as such]. Against that, the federal fee system collects **0.34-0.38% of what FOIA costs to run**, about **$1.54 per request**, and the State Department collected **$0.00** in FY2024 [government's own reported statistics] — **fees are a deterrent instrument, not a cost-recovery one**, and the record should be able to say so with its own captured evidence. Consequence for the design: a fee quote should be a first-class captured artifact with a comparison basis, not correspondence prose. Two identical requests priced 2,000x apart is exactly the shape BIO exists to make checkable, and today it would be a paragraph in a `## Correspondence` heading. | M4 - open |
| D-149 | defect | 2026-08-01 | **The design is jurisdiction-blind, and BIO's own jurisdiction differs from federal FOIA on every axis that matters.** Nothing in the record models WHICH access regime an `action` runs under, and the assumptions differ sharply. Federal: fee-shifting is discretionary ('the court MAY assess... substantially prevailed', 5 USC 552(a)(4)(E)), search and review fees are chargeable by requester class, and plaintiffs substantially prevailed in only **112 of 1,672 lawsuits, 6.7%** [GAO-16-667, the government auditing itself]. **California, which is Oakland and therefore BIO's actual regime**: fee-shifting is MANDATORY ('the court SHALL award court costs and reasonable attorney's fees to the requester', Gov. Code 7923.115(a)); fees are **direct duplication cost only** with no search fee and no review fee (7922.530(a)); and **redaction time is not billable at all** — *National Lawyers Guild v. City of Hayward* (2020) 9 Cal.5th 488 threw out a $2,938.58 invoice for 40.2 hours of bodycam redaction, holding that 'extraction' means retrieving responsive data, not omitting unwanted data. So in California 'it will take us 200 hours to redact this' cannot lawfully become a bill — and a design carrying federal assumptions would model a cost that does not exist here and miss a violation when one is charged. A pending 2026 California bill would loosen the fee rules [CalMatters, Mar 2026]; flag as pending, not law. | M4 - open |
| D-150 | design | 2026-08-01 | **The completeness claim is VALIDATED by the strongest external evidence in the study — and the same evidence says an authored field is necessary and not sufficient.** From `PROBLEM-DOMAIN.md`: concealing exculpatory evidence appears in **44% of exonerations involving official misconduct**; Nifong scoped a report to exclude; Fujitsu edited out references to known defects; South Yorkshire amended 116 statements. **Every retained fact was true in every one of those cases** — which is precisely the failure the completeness claim targets and the reason a case is not merely a published finding. That is the clearest validation any BIO construct has received from outside the repo. BUT THE SAME SOURCES CARRY A CORRECTION: in both the SBI and Rolling Stone reviews the catch was **a second person with standing**, not a form field — and in each the person who found the gap was overruled inside the hierarchy. So the authored exclusion statement is necessary and insufficient, and the design should say so rather than let the field stand in for the check. What that implies is not settled here: candidates are a second-member acknowledgement on the exclusion statement before publication, or making the statement the thing a reviewer is specifically asked to attack rather than one field among many. Do not build the field and call the problem solved. | M10 - open · REC-14 (disposition corrected 2026-08-01 by session BOB: this row was written `M6` when the ladder had no rung above M8, and M6 is *the record can be left, mirrored and outlived* — nothing to do with a completeness claim at publication. M10 now exists and is where publication lives.) |

| D-151 | defect | 2026-08-01 | **A machine credential can RESOLVE an unassigned task, so an obligation can be closed with no member act — and the code's own comments say it cannot.** VERIFIED this turn in the source. `Store.#refuseNotYours` (`bio-plane/src/store.mjs:6943-6946`) returns `null` — allow — the moment `row.assignee === "unassigned"`, BEFORE it examines who is calling; and `taskforward` / `taskresolve` both carry `"probe"` in their `classes` (`bio-plane/src/index.mjs:271-272`). So `op=taskresolve` driven by a machine credential closes the task and writes a history entry reading `actor: "token:probe"`. The fence's own comment states the guarantee it makes — *"a daemon cannot close somebody's work"* — and that is exactly true and exactly insufficient: **a daemon cannot close somebody's work; it can close NOBODY'S work, and closing is the act.** Two comments in the same codebase already forbid what the code permits: `taskResolve`'s own header calls it *"Also a member action"*, and `index.mjs:602-604` calls both verbs *"MEMBER actions performed by a PERSON through their session"*. This is the same class as D-137 and D-138 — a guard whose comment describes a stronger property than its code enforces, so a later reader takes the comment as the specification. THE FIX IS A PRECEDENT ALREADY IN THE FILE, not a new rule: `release` (`store.mjs:1857-1861`) refuses any author matching `/^token:/` with `MACHINE_CANNOT_RELEASE`. Refuse at the ACT, not at the fence, so the refusal does not depend on assignment state: `MACHINE_CANNOT_RESOLVE` and `MACHINE_CANNOT_FORWARD`, plus removing `"probe"` from the two ops' `classes` and correcting `#refuseNotYours`'s comment to state what it actually guarantees. The fence STAYS — it answers *is this THIS member's task* and the act refusal answers *is this a person at all*; the second is not derivable from the first, which is why the hole existed. `taskdrain` is deliberately untouched: it is the daemon's path and draining is not resolving. Lines up with REC-13's `MACHINE_CANNOT_CONCLUDE`, so the record ends with ONE rule in three places rather than three judgements — a machine may surface, route and prepare; a member authors, resolves and concludes. NEGATIVE CONTROL for the fixing item: remove the act-level refusal and a `token:probe` credential resolves an unassigned task while every existing task assertion still passes — which is the state today. Raised by DEC-7. | M8 · CLOSED 2026-08-03 (REC-28): act-level refusals by shape before the row is read; probe declassed with the ACL limit stated; fence kept; NC re-run by CONDUCT. |

| D-152 | design | 2026-08-01 | **Image-only PDFs must be OCR'd and investigated, and OCR text must never be indistinguishable from text the publisher wrote.** RULED 2026-08-01 (DEC-4), overturning the recommendation that scanned PDFs stay honestly unreadable. Bob: *"Some PDFs will be released in the public record as image only. The textual contents of these images need to be extracted and investigated for meaningful content."* The refused argument was a volume argument — ~14% of a 14-document sample, skewing to design and scan artifacts — and coverage is not a volume question: a class the record can capture and never read is a hole, and it does not have to be large, only non-empty. The class is also not randomly assigned; scanned-only is what a signed order, an exhibit, a handwritten annotation and a deliberately-imaged release all look like. *"Investigated for meaningful content"* puts OCR output on the READING path (FW-15's L2→L3 wire), not merely in a text field, or the class is captured, readable and still ungraphed. **THE PROVENANCE RULE IS STRUCTURAL AND IS THE THING THAT COULD TURN THIS CAPABILITY INTO AN OVERCLAIMING DEFECT:** extracted text carries `text_source: 'layer'`; OCR carries `text_source: 'ocr'` with engine, version and per-region confidence, and they are distinguishable everywhere the text is shown, cited or indexed. A basis leg resting on OCR'd text carries its IMAGE REGION (page + rect) so a reader checks the claim against the pixels rather than against our transcription — the same discipline as answering by hash. OCR never raises a capture grade, and a low-confidence region reads `undetermined` rather than a best guess, which is the mojibake rule one layer up. **MEASUREMENT GATES DESIGN:** whether OCR runs in-plane at all is unknown — a WASM engine's bundle size against the Worker limit and its CPU against the isolate ceiling (D-36, D-56) — with the pdf-worker fleet member (I6) the first fallback and an external service the second. Nothing is designed before that number exists. | M2 - open · CPDF-9 (measure), CPDF-10 (build) |

| D-153 | design | 2026-08-01 | **`research/AUDIENCES.md` H4 and H6 are not hazards to be avoided; they are the required workflow — corrected by external evidence, and the pass is NOT edited.** RULED 2026-08-01 (DEC-13). AUDIENCES §7 runs `RATIFY -> CASE -> rendering -> action -> consequence` and §9 lists the pre-publication release of material among its HAZARDS — H4 (*the low-threshold rendering that escapes*) and H6 (*the embargo*). The practice literature says the ordering is inverted, and the sources are professional standards rather than opinion: the SPJ code (*"diligently seek out subjects of news stories to give them the opportunity to respond to allegations of wrongdoing"*); GAGAS/Yellow Book, where obtaining the views of responsible officials is a REPORTING REQUIREMENT and GAO's own protocols give an agency **7 to 30 calendar days** on a draft report, expect a single position including the rationale for any disagreement, and print the response beside the finding; and the Columbia Journalism School review of Rolling Stone's *A Rape on Campus*, which identified a comment request made **without specifics** as the central failure. So an unratified rendering carrying specific claims must be able to leave the building, addressed to the subject, under a deadline, before anything is ratified — which is exactly AUDIENCES row 14's *"delivery with no bucket"*, and the outside evidence says that row is the MAIN PATH for any case that names anybody rather than an edge case. **THE PASS IS DELIBERATELY NOT EDITED** — a research pass is a record of what it concluded, and correcting it in place would rewrite history and hide that the evidence arrived later and overturned it; same treatment `RECONCILED.md` gave the storyboard files, with the correction living in a live surface and a pointer going on the pass. What Bob's ruling adds beyond the sources: the GATE is not the contact but the group's DECLARED, JUSTIFIED POSITION on it, carried inside the case as declared bias — which is what makes a tipping-off refusal legitimate and visible instead of quiet. | M10 - open · REC-14, REC-24, UI-17 |

| D-154 | design | 2026-08-01 | **A HUNCH is temporary declared bias with a temporarily-high connection grade, and it is bias debt that must clear before publication.** RULED 2026-08-01 (DEC-15), and it resolves the D1(a)-versus-D1(b) fork by showing it was a false one: an authored connection grade and an earned one are not rivals, they are two phases of the same object. During `open` a member may author a connection grade high enough to make otherwise disconnected evidence traversable — *"the investigator thinks this is true"* — and at `published` no hunch may remain, so every leg is earned or is honest testimony. **THE GATE ALREADY EXISTED AND WAS BUILT FOR SOMETHING ELSE:** `BIO_Declared_Bias_v0_1.md`'s bias-debt section already ends *"a work product carrying bias debt cannot advance its workproduct_state or be ratified for publication until the debt is settled"*, written for a bias statement CHANGING and leaving old analysis owed a re-run. Registering a hunch as what it is reaches that rule with no new machinery. It also settles what CLEARING means without a further ruling: debt is *cleared by re-running the evaluation under the current set*, so the test is neither "delete the leg" nor "leave it in unrated" — **the case must still hold when the hunch is removed from the set**. `grade_source` gains `hunch` beside `resolution` and `testimony`; it is the ONLY authored grade permitted above D, permitted precisely because it cannot survive publication; it carries an author and a date; and it is **visible as a hunch on every surface from the moment it is made**, because the failure mode this construct invites is a hunch quietly ageing into a fact. A hunch is NOT an `undetermined` leg and must never be composed as one — R1 suspends an axis when a grade is ABSENT, while a hunch grade is present and asserted, and treating it as undetermined would destroy the traversability the ruling exists to preserve. `op=publishpreflight` refuses `UNCLEARED_HUNCH` naming every leg, before any signature exists. Manifest registration needs `object_type: bias` (D-84) and is sequenced behind it; the leg-level source and the refusal ship with the claim layer where they bite. | M9 - open · REC-11, REC-12, REC-15, REC-18, UI-11, UI-12 |

| D-155 | design | 2026-08-01 | **A project declares the evidentiary strength its work requires, and the published case carries that declaration beside the strength it reached.** RULED 2026-08-01 (DEC-17). `BIO_Content_Framework_v0_10.md` §12 has recorded since v0.1 that a claim *"needs a standard of proof attached and that is doctrine rather than architecture — the next design conversation"*; this is that conversation. The question was what makes an unsupported case harder to state, and both options this project generated put the standard in the wrong place: a global floor would refuse honest weak findings, and no floor leaves nothing to fall short of. **The standard belongs to the GROUP and to a particular piece of their work** — a project convened to refer something to an auditor needs a different bar from one convened to decide whether a thing is worth looking at, and both are legitimate, so a single system-wide answer was always going to be wrong for one of them. **This is the PROJECT axis and it is not the AUDIENCE axis**, which is why it does not contradict `research/AUDIENCES.md` §5's ruling that per-audience relaxation is *"a structural prior by role — the same defect as a suspicion flag, with the sign reversed"* and that a threshold belongs on a rendering and never on ratification. A project-declared bar is authored by the group, before the work, about its own intentions; nobody's standard is set by who they are. That file is not edited (see D-153's reasoning); its ruling stands and is about a different axis. SHAPE: `project` frontmatter gains `required_strength{capture, connection}` — **a PAIR per R2**, because a scalar would re-collapse the two axes in the one field a reader is most likely to quote. C-2.9 already requires a non-empty `objective` on the same object, so the check has a precedent beside it. `op=publishpreflight` refuses `BELOW_PROJECT_STRENGTH` naming the axis; the escape is amending the project's declared bar, which is an authored, dated, on-the-record act visible in the published case — you can lower your own bar, you cannot do it quietly. A project with no declared strength gates nothing and the case SAYS so rather than rendering a blank, since an absent bar is not a bar of zero. An inquiry outside any project has no bar and inherits none. | M10 - open · REC-14, REC-15, UI-18 |

| D-156 | design | 2026-08-02 | **"Audience" names two different things in this project, and they have different machinery attached.** Named by Bob 2026-08-02 (DEC-17 addendum): in requirements gathering the word refers to **user types** — the people who use BIO; when referring to a published case it means **the readers and consumers of that case** — elements of the public who use the information, analysis and conclusions it contains. VERIFIED in the corpus: `research/AUDIENCES.md` enumerates *"eight audiences"* mixing both senses in one list — an administrator or a lawyer READING A PUBLISHED CASE is the second sense, while *"the publisher's own future members"* and the primary archetype (*a member of a community accountability group*, from `JOURNEY-PRIMARY.md`) are BIO USERS and are the first; the word `archetype` occurs twice in the entire file. **THE CONFLATION IS WHY THAT FILE'S §5 READS AS A WARNING RATHER THAN A DEFINITION.** Its hard constraint — *"a per-audience relaxation is a structural prior by role… if administrators need a lower threshold, that is a threshold on a RENDERING, and it must never be a threshold on RATIFICATION"* — is correct and unchanged, but it had to be written defensively because "administrator" sat in a list mixing a user type with a case reader, which makes a per-role gate look like a natural extension of the list. Separated, the rule is definitional: **renderings and thresholds vary by AUDIENCE; capabilities, journeys and surfaces vary by USER TYPE; neither ever reaches ratification.** DISAMBIGUATION ADOPTED: `AUDIENCE` is reserved for readers/consumers of a published case; the requirements sense is `USER TYPE`, or `ARCHETYPE` where a document already uses that word. This is the D-68 / D-8 vocabulary-drift class caught BEFORE it reached the code — no surface, table or op names either concept today, so the correction costs documents and no migration, which is the whole reason to pay it now. The affected documents are `research/AUDIENCES.md`, `research/JOURNEY-PRIMARY.md`, `UI-PLAN.md` and `research/CAPABILITIES.md`; the research passes take a pointer rather than an edit (D-153's reasoning). **AMENDED 2026-08-02 by Bob, and the correction is larger than the vocabulary split:** the USER TYPE list is not one archetype. Bob: the community-accountability-group member is the PRIMARY archetype, but user types also include *"journalists, lawyers, city administrators, an oversight body, and any other role that involves collecting evidence, investigating, and any other group interested in improving government using the capabilities and processes of the CivicOS."* **The consequence D-156 did not see: the same person can be BOTH.** A lawyer may USE an instance to build a case and may also READ someone else's published case — the two senses are not two populations of people, they are two RELATIONSHIPS to a case, and which one applies depends on the case, not on the person. That is why the machinery differs (capabilities and surfaces versus renderings and thresholds) and why neither ever reaches ratification. | M10 - open · a documentation pass, no code |
| D-157 | defect | 2026-08-02 | **`op=memberlist` hands the cover↔handle PAIRING to ordinary members and to `MEMBER_TOKEN`, and §3 says only administrators see them together.** MEASURED 2026-08-02 (session BOB, `MEASUREMENTS.md`): an ordinary member's session (`administer: false`) and the shared `MEMBER_TOKEN` machine credential each receive `handle = cover` for every member on the LIVE `bio` store, byte-identical to the `ADMIN_TOKEN` view. `BIO_Membership_Architecture_v1.md` §3 and `v2` §3 are identical and unambiguous: *"Pairing. Only administrators see cover and handle together."* The split is the anti-deanonymisation mechanism — `schema.mjs` on `members.cover`: *"the cover-and-handle split exists precisely so that a roster seized or subpoenaed does not deanonymise the group."* **The source contradicts itself in three places:** `index.mjs:407` grants `classes: ["admin","member","probe"]`, the comment eight lines below it says *"All admin-only: memberlist pairs cover with handle and only administrators see those together (section 3)"*, and `store.mjs:5810` says *"Every op that reaches this is admin-only at the control plane."* Neither comment is true. **`PROBE_TOKEN` is NOT exposed** — `scopeFor` (`index.mjs:862`) confines probe class to the `scratch` store, a different DO with its own member table; that half was measured before it was claimed. **The fix is a projection, not a refusal:** §3 also says *"Members and the public see handles"*, so a member legitimately needs the handle roster (project participant lists, authorship) and must not receive `cover`. `test/members.test.mjs:192` asserts *"machine member token reads the roster"* and must be CORRECTED rather than exempted (`CLAUDE.md`), with a comment saying why the old assertion was wrong; the negative control is a non-admin caller receiving a `cover` field at all. | M7 · CLOSED 2026-08-03 (REC-29): the projection withholds cover from every non-administrator, server-stamped, fail-closed; members.test.mjs corrected; NC re-run by CONDUCT. |
| D-158 | defect | 2026-08-02 | **A signing key registered for a member who never ENROLLED reads `active` on `op=signerlist` and is refused by `op=ratify`.** MEASURED 2026-08-02 (session BOB, `MEASUREMENTS.md`, real `ssh-keygen` signatures through the real ratify path). `signerList()` (`store.mjs:5884`) reads the `signers` table alone; `gateFacts()` (`store.mjs:5920`) joins `members` and requires `s.status='active' AND m.status='active'`. The REVOCATION path agrees only because `memberSet` (`store.mjs:5857-5861`) cascades — it deletes the member's sessions and sets their signers to `revoked`. There is no cascade for the other direction: a member is `status='invited'` until enrolment (`store.mjs:5731`) and `signerAdd` checks only that the member EXISTS (`store.mjs:5874`), so an admin may register a key for someone who has never enrolled and `signerlist` reports it `active` while a signature from it returns `SIG_UNKNOWN_KEY`. **Not a blocker for RECONCILED §4 Q11**, which is settled YES on this measurement: the surface's *"you hold no active signing key"* is never a false alarm, because a key `signerlist` does not show active is one `ratify` will not accept. It is wrong only in the optimistic direction. **Two candidate fixes, and the second is preferable:** make `signerList()` join `members` so one predicate serves both, or make `signerAdd` refuse a member who has not enrolled — the join is the smaller change but leaves a row that means nothing; refusing at write keeps the table honest and is the shape `signerAdd`'s existing NO_SUCH_MEMBER check already reaches for. Whichever ships, the other view must be asserted against it. | M10 - open · REC-15/UI-17 pre-flight; scoped in the 2026-08-02 BOB INBOX entry |
| D-159 | design | 2026-08-02 | **An ungraded leg now costs a conclusion nothing, which makes leaving one ungraded the cheapest way to keep an inconvenient leg out of the reckoning.** Created by DEC-18: an ungraded leg is INERT — it does not contribute, does not floor and does not unrate — so a member holding a leg that cuts against their conclusion can decline to grade it and the conclusion stands on its load-bearing legs. This is a laundering path in the OPPOSITE direction from the one R1 closed, and it is named here rather than left for a build session to discover. **Three defences already stand and a fourth should not be invented before use shows one is needed:** every ungraded leg is NAMED, on the case and in the index row (DEC-18's plural clause); the completeness statement is authored, unprefilled and must say what the case leaves out; and invariant 7 makes a leg that cuts against travel the same path as one that supports. **What would settle it is USE, not argument** — whether real groups leave cuts-against legs ungraded is observable once M9/M10 ship, and inventing a counterweight now would be the same mistake as a global strength floor (§4 Q2). | M10 - open · WATCH, no task — re-raise when M10 has run with a real group |
| D-160 | drift | 2026-08-02 | **`RECONCILED.md` says `SUSPEND` 50 times and never says `UNRATED`, which is the canonical word.** `BIO_Case_Making_v0_1.md` R1 renamed it after the reconciliation pass was written and said the rename *"is not cosmetic"* — `SUSPEND` already meant the OPPOSITE thing in `SB-OUTPUT` §5.1, and `BUILD-ORDER`'s REC-12 says *"ship SUSPEND"* citing that file, so a worker following the build order in good faith would build the behaviour R1 forbids while every document appears to agree. That collision is what RECONCILED's own R1-h row identifies as *"the single most dangerous collision in the corpus"* — and RECONCILED then adopted the dangerous word as its canonical one. **Mitigated 2026-08-02** by a dated amendment block at the head of §1.1 declaring UNRATED canonical and every SUSPEND below it to be read as UNRATED; the 50 occurrences are NOT rewritten, because RECONCILED is a reconciliation record and rewriting it in place would hide that the collision happened. **What closing it takes:** REC-12 and any item quoting §1.1 must use UNRATED in the code, the copy and the test names. | M9 - open · REC-12 carries it |
| D-161 | gap | 2026-08-03 | **A connection is anchored to specific passages and then throws the anchor away.** `resolutions` is keyed `(capture_sha, ref, entity_id)` — a SPECIFIC reference inside a capture, with its own grade and method — and `reading_refs` gives each `ref` a kind, key and label. `deriveConnections` (`store.mjs:3499`) then *"collapses them to the STRONGEST grade each capture resolved to the entity at, and forms one connection per PAIR of distinct captures"*, so the connection row carries `a_capture_sha`/`b_capture_sha` and no passage. **Consequence:** following a connection lands a reader on a whole document — a 300-page budget PDF — rather than on the line that grounds it, and a case's citations cannot be checked at the point they are made. Raised by Bob 2026-08-03: *"a connection can also refer to specific content within a document, right?"* The data is not lost — it is in `resolutions` — it is simply not carried onto the connection. **Design question inside it, because one capture may resolve to the same entity at several refs: does a connection carry the ref pair that DETERMINED its grade, or all supporting mentions?** Recommendation is the determining pair, because that is the rule the record already applies everywhere else (name the leg that is why), with the full set reachable behind it. | M4 - open · design agreed in outline, not yet an item |
| D-162 | gap | 2026-08-03 | **A connection runs through a NAMED thing, so two documents about the same IDEA cannot be connected.** `ENTITY_KINDS` (`store.mjs`) is `source, institution, office, movement, person, body, ordinance, parcel, contract, fund` — every one a named entity. There is no `concept`, `topic` or `theme`. So two staff reports both concerning *deferred maintenance*, naming no shared entity, have no connection available to them. Raised by Bob 2026-08-03, who described a connection as running through *"the same entity/idea"* — the second half of which is not built. **Not obviously a defect:** an idea has no identity criteria, and a registry of themes is how a record starts asserting relationships nobody can check, which is the failure mode `BIO_Declared_Bias_v0_1.md`'s malformedness rule exists to prevent. **What it needs is a decision, not a table** — whether a theme can be an entity while staying falsifiable. | M4 - open · DOCTRINE (Bob) — raise as a DEC when M4 reaches the entity axis |
| D-163 | gap | 2026-08-03 | **A citation cannot point INSIDE a document.** `refs` is keyed `(bundle_id, target_id, kind)` — bundle to bundle — so a staff report's §3 referring to its own Appendix B, or to page 14 of another document, has nowhere to live. Bob's definition 2026-08-03: *"a citation is a reference in one piece of content to a different piece of content — whether in the same document or across documents."* The build satisfies only the across-documents half, and only at whole-document granularity. Shares a root with D-161 (no sub-document addressing on any edge) and with D-123 (I2's element reference is PDF-shaped and office formats need a per-container form) — **all three want the same anchor primitive, and solving them separately would build it three times.** | M4 - open · fold into D-123's element-reference work |
| D-164 | design | 2026-08-03 | **L3 CONTENT is a named layer with no object, which is the twin of the L7 finding the study already made.** RULED by DEC-23: content — a piece of information extracted from a document, up to and including the whole document — is the unit the record points at. Extraction EXISTS (`readings` keyed by capture, `reading_refs` giving each in-document reference a kind/key/label, `resolutions` keyed `(capture_sha, ref, entity_id)` and graded) and **no edge in the system can point at any of it**: legs, citations and connections all address a whole capture or bundle. So the record can know a reference sits at a place in a document and cannot let a claim cite it. **The two absences fail together** — a claim that could cite a passage is exactly what neither L3 nor L7 can express. **This SUPERSEDES the framing of D-161, D-163 and D-123, which are the same missing primitive seen three times** (a connection discarding the anchor `resolutions` already holds; a citation unable to point inside a document; I2's element reference being PDF-shaped where office formats need a per-container form). They must be solved ONCE, as an addressable content extent, or the primitive gets built three times and drifts. | M4 - open · the content-extent primitive; D-161/D-163/D-123 fold into it |
| D-165 | design | 2026-08-03 | **What each ACTION KIND requires before it is worth attempting is not modelled, and the backward question cannot be answered mechanically without it.** The backward question — *"given what this project has established, what ELSE would need to be true for action X to be a credible option?"* (DEC-26) — is the most productive capability the action plan offers, because its output is a work list rather than an opinion. Answering it needs a model of each action kind's preconditions, which is procedural, jurisdiction-specific, and nowhere in the record. **DEFERRED 2026-08-03 by Bob** — *"may also [be] too difficult to take on now"* — **with the derivation path named, which is what makes this deferrable rather than open-ended:** *"it could be figured out through some combination of supporting evidence already gathered, mechanical means, and AI support."* Three bounded inputs: what the project has ALREADY established (the gap is relative to a real corpus, not an abstract standard); MECHANICAL checks for the structural preconditions (a filing window against a date, standing against who the group is, exhaustion against actions already recorded); and AI for the procedural knowledge that is neither. **What this does NOT block: the plan itself.** A plan can be authored, ordered, branched, costed and support-labelled with the member supplying what an action requires; only the system answering the backward question on its own waits. So S11 is specifiable now apart from that one affordance. | M10 - open · deferred, trigger: S11 exists and members are answering the backward question by hand |
| D-166 | gap | 2026-08-03 | **The oaklandca.gov finance-document URLs the CPDF-5 corpus rests on now serve HTML — the site was reorganized since 2026-07-31, so `tier1-coverage-probe.mjs`'s Oakland half is no longer reproducible at its recorded URLs, and a capture pointing there today would capture a redirect page.** Found by CPDF-9 (2026-08-03) while sourcing scanned exhibits. Someone should re-locate the finance documents and re-point the probe. The churn is itself evidence for why capture-with-provenance matters: the record's copies remain answerable while the source moved. | M1 - open · re-locate the corpus URLs; the probe's Oakland half is stale until then |
| D-167 | design | 2026-08-03 | **The stack axis mislabels extracted document text: plain PDF Tier-1 text trips `client_rendered`'s no-anchors structural test at `possible` confidence.** Found by FW-15 while wiring readings for PDFs. It never reaches the record (the reading names the doctype reader, and readtext.mjs's doc states the limitation), so this is drift-in-waiting rather than a live defect — but a future FRAMEWORK item should make the stack axis text-provenance-aware (it was designed against fetched HTML, and extracted text is a different provenance class). | M3 - open · fold into the next FRAMEWORK stack-axis item |
| D-168 | design | 2026-08-03 | **`op=cite` is type-only, so the plane permits citing RETIRED information.** Surfaced by REC-19's affordances derivation being honest to the store's guards: retire refuses while cited (`CITED`), but citing an already-retired bundle succeeds — affordances therefore truthfully publishes `cite` on a retired bundle. Whether a claim may REST on retired information is a doctrine question adjacent to REC-11's C-6.3 arm and UI-20's pre-flight; if it should be refused, the rule belongs in the STORE, never on a surface (DEC-8). Provisional: current behaviour stands. | M9 - open · bounds REC-11/UI-20; raise to Bob via DECISIONS if REC-11's worker hits it as a real fork |
| D-169 | gap | 2026-08-03 | **`Store.#setScalar` replaces but cannot ADD a frontmatter key, and `mdFor` intake documents carry no `disposition_reason` line — so an intake-created inquiry that is later disposed becomes non-conformant (C-2.8).** Noticed by REC-10's worker; predates the collapse (same held for focus since 0.35.0). Two candidate fixes: teach #setScalar to insert a missing key, or make intake mint the line empty; either way assert the disposed-intake-doc path in the suite. | M7 - open · small, batch with the next honesty-defect item |
| D-170 | design | 2026-08-04 | **An UNGROUPED condition cannot be muted, and `governor-holding-host` is the first item that routinely is ungrouped.** `queue_state` is keyed `(member, case)` (REC-21), so an item with no home has no case to mute against — a held host whose documents nothing rests on yet produces a condition every member sees and nobody can silence. Provisional, accepted at REC-32's integration: LEAVE IT — a pseudo-case is the invented home REC-20 refuses, and a case-less member-scoped mute is one step from mute-the-kind-everywhere, which is one step from the delete button D-125 forbids. Reversal costs a nullable case_id in queue_state and a second admission arm. | M8 - open · WATCH; re-raise when a real group reports an unsilenceable condition |
| D-171 | gap | 2026-08-04 | **`#revisionKind` (C-20.1's writer lookup) tiebreaks the latest manifest entry by `created DESC, snap_key DESC` — the document's OWN backdatable time and a lexical sort over an opaque caller-chosen key, not a clock.** REC-32's derivation uses the same first key with `rowid DESC` (the store's write order) and documents the difference at the site; whether #revisionKind should take the same tiebreak is a small RECORD chore. Found by REC-32, deliberately not silently fixed (it sits on the catalog path). | M7 - open · batch with the next honesty-defect item (beside D-169) |
| D-172 | design | 2026-08-04 | **The one DO alarm can fire while a tick is still in flight.** A tick calling a control-plane op over `env.SELF` re-enters the same Durable Object; the nested op can arm the drain one second out, and workerd fires the alarm underneath the still-awaiting tick. REC-26's first idempotence-key version was silently wiped by exactly this — the re-entrant run found every subject claimed, closed an epoch the outer run was still working through, and observations inflated WHILE every assertion about the key's own rows passed. REC-26 memory-guards its two ticks and deliberately did not widen the mechanism. The open shape question: a `serial: true` consumer flag in `#schedConsumers` enforced by `onAlarm` — it applies to any future async consumer that calls out over SELF. | M1 - open · decide at the NEXT scheduler consumer; the serial flag is the recommended shape |
| D-173 | gap | 2026-08-04 | **Every intent-layer READ in the UI forgot to open the plane's `{ok, result}` envelope** — against a live plane the Subjects screen answered "no subject known" for subjects IN the registry and UI-9's what-the-record-knows panel rendered blank, while BOTH suites passed because both mocks answered unwrapped: the one shape the plane never sends. The mock-mirrors-the-defect class, live-fire's own lesson one layer up. | M8 · CLOSED 2026-08-04 (UI-13): fixed at one seam (recR, 17 reads rewired); the mock corrected with a dated comment. The CLASS lesson stands: a UI mock must answer the wire shape. CLASS CLOSED STRUCTURALLY 2026-08-04 (UI-23): six instances total (the sixth — signIn — meant password sign-in never worked live); the two-arm envelope guard now fails any unwrapped mock and any transport call outside the seams. |
| D-174 | design | 2026-08-04 | **The plane's refusal `detail` strings carry plane vocabulary (`op=entitycreate`, `capture sha`) and DEC-8 renders them verbatim — in tension with the member-facing vocabulary guard.** UI-13 contains them in dedicated pre-flight containers so UI-4's jargon guard holds, but the wording-ownership question (does the plane learn member-facing refusal wording, or do surfaces get a licensed translation layer?) is real and unruled. | M8 - open · **THE TRIGGER FIRED 2026-08-04 and the row did its job.** REC-41 gave op=login its first refusal SENTENCE and UI-30 renders it under DEC-8 — so the tension left UI-13's dedicated containers and now stands on the SIGN-IN GATE, the first screen a member meets, saying "no active credential", "a salted derivation", "its stored hash". **DEC-49 RAISED** with Bob (does the plane learn member-facing wording, or do surfaces get a licensed translation layer), provisional = as shipped, recommendation = the plane owns it. **UI-31 QUEUED** and deliberately NOT waiting on the answer: no vocabulary guard covers any pre-authentication surface, so this tension was UNMEASURED rather than accepted — **UI-31 LANDED 2026-08-04 and it is now MEASURED**: see `MEASUREMENTS.md`, "2026-08-04, UI-31: the plane vocabulary standing on the PRE-AUTHENTICATION surfaces" (13 terms, 5 surfaces, 56 occurrences a member reads), and the guard REPORTS (a guard that failed on the shipped sentence would force a surface to invent a translation, which DEC-8 forbids). Row stays open until DEC-49 is answered. |
| D-175 | gap | 2026-08-04 | **Some battery suite varies by 2 assertions run-to-run on an unmodified tree** (4693 vs 4695 observed on the same baseline; runs at REC-34's HEAD stable at 4743 across three). Likely a conditional/network-adjacent arm. A count that moves on its own erodes the per-suite-diff instrument every integration relies on. Identify the suite and pin the conditional arm (skip LOUDLY with a name, the D-93 rule, or make it deterministic). | M0 - open · small; the next M0-lane pass takes it |
| D-176 | design | 2026-08-04 | **UI-14's all-clear rests on an INTERPRETED feed-emptiness** (a feed's emptiness = what it contributed to this screen, not its raw rows — else one historical resolved task forbids the all-clear forever). Honest and asserted, but the interpretation is doing real work, and the surface's QUEUE_SEEN receipt map grows for the life of a session. The literal alternative is a narrow plane read — "what left this member's queue since they last looked" — that would make the all-clear rule literal and bound the map. | M8 - open · WATCH; becomes a RECORD delegation if the interpretation bites or the map's growth matters |
| D-177 | design | 2026-08-04 | **The capture axis BELOW the ceiling is still authored.** REC-18's ceiling stops overclaiming (no capture grade above B, grade A structurally unreachable) but B-or-weaker remains a member's statement — the record holds no per-document capture grade. Closing it needs a per-capture fidelity FACT (captured_locators.via, authority state) turned into a graded value — CAPTURE's ground; R2-b's amendment points further, at REC-12's #strengthWalk reading the grade from the target. Raise to Bob only if it survives the CAPTURE-side work. | M9 - open · CAPTURE's next activation takes it |
| D-178 | gap | 2026-08-04 | **op=audit's sweep injects earnedRegistry but not publishedRegistry, so C-21.1/C-21.2 never fire in an audit.** Pre-existing, surfaced by REC-18. Not closed there because adding it could fire C-21.2 across the corpus — a small separable item that should land with a corpus check first. | M10 - open · small; pair with the next M10 item |
| D-179 | gap | 2026-08-04 | **register.capture_sha is a GLOBAL primary key, so registering one sha against a second bundle silently MOVES the row** — it cost REC-18's worker a debugging cycle inside another suite. Whether that is the intended semantics (one capture, one home) or a missed compound key is a store question; either way the silent move deserves a named refusal or an assertion. | M7 - open · batch with the honesty-defect items (beside D-169/D-171) |
| D-180 | gap | 2026-08-04 | **`#migrate` splits SCHEMA on `;`, so a semicolon inside an inline `--` comment on a column line silently truncates the CREATE TABLE** — it fails loudly downstream (SQLITE_ERROR: incomplete input, 72 suites red) but names the wrong thing and cost REC-24 a debug cycle. One hygiene assertion closes the class (no `;` inside a `--` comment within the schema literal). | M0 - open · one-liner; the next M0-lane pass takes it with D-175 |
| D-181 | design | 2026-08-04 | **C-2.8 refuses an ACTION as a leg of a question's basis** ("information or another inquiry, nothing else" — measured against checkBundle), so BUILD-ORDER's "an inquiry whose basis includes this action and its clock" is plane work that does not exist; the overdue→finding loop carries the action as a references[] edge instead, and the surface says which of the two it is. THE TENSION: admitting the leg would rest a claim partly on our own act, which is DEC-14's non-self rule one construct over — the reference may simply be RIGHT. Conservative state shipped. | M10 - open · raise to Bob only if a real non-response case needs the leg; else the reference stands |
| D-184 | gap | 2026-08-04 | **A member's FIRSTHAND OBSERVATION of an event has no home as a basis leg, and the one construct that looks like it is scoped to correspondence only.** Surfaced by Bob's DEC-39 example 2026-08-04: a coroner testifies at trial; the record holds only a NEWSPAPER ACCOUNT of that testimony; a member was in the courtroom and can attest to what was said; the court transcript exists but is unpublished. MEASURED: C-2.8 admits *"information or another inquiry, nothing else"* as a basis leg (D-181), so the member's own observation is neither. `actionCorrespond` (`store.mjs:3015`) DOES implement a capture-or-testify choice with `held_as: capture｜testimony`, `MACHINE_CANNOT_CORRESPOND`, and the rule that *"we have it in writing" and "somebody remembers it" are different kinds of evidence and must not be able to be confused"* — but it is scoped to a correspondence EXCHANGE on an action, not to witnessing an event. `grade_source` admits `testimony` (schema.mjs:1141, DEC-15) at the GRADE level, which is a different thing from the leg's CONTENT. **The honest path today exists and is unnamed:** the member authors a member-original record (a dated, authored witness statement), captures it, and cites THAT — which is right, because it converts a memory into an artifact with an author and a date that a reader can weigh, rather than letting an unrecorded recollection carry a claim. Nothing tells a member to do this, so the likely failure is a member citing the NEWSPAPER for a fact they personally witnessed — silently substituting a secondhand source for firsthand knowledge, which understates the evidence and misattributes it at the same time. Wants the `actionCorrespond` pattern generalised, or the member-original path surfaced and named. | M9 · open — pair with DEC-39's fence wording and REC-11's leg surface |
| D-196 | design | 2026-08-04 | **The completeness statement is PROSE with nothing behind it, which the search-completeness research identifies as the exact claim the field considers worthless.** MEASURED: `completenessFields` (bio-checks.mjs) carries `statement` (authored prose about what the case does NOT cover), `subject_justification`, and `excluded` rows of (target, description, reason). **It records NOTHING about what was SEARCHED**, and `excluded` can only hold DELIBERATE omissions — things someone knew of and left out — so there is no representation at all for what nobody knew existed. The literature's verdict on unbacked completeness claims is Blair & Maron (1985): attorneys stipulated they must reach 75% recall and sincerely believed they had; measured recall was ~20%, after iteration and refinement. **THE FIX IS ALREADY DESIGNED UNDER ANOTHER NAME.** `STORE-AS-CACHE.md` argues for an append-only OBSERVATION LOG — every look, including the ones returning nothing — on provenance grounds. That is also, exactly, the disclosed-process half of a defensible completeness claim: *we searched these sources, at these levels, on these dates, on these leads, and this came back, including the empty results*. That is Rule 26(g)'s *reasonable inquiry* made checkable. **The two designs are ONE design** and neither works alone: an observation log nobody publishes is bookkeeping, and a completeness statement with nothing behind it is Blair & Maron. What must NOT be built: a recall-like number (the denominator is unknowable at BIO's prevalence and Goodhart is invoked by name in the literature) or elusion testing (its inventors have asked the profession to discontinue it; it overstated recall by 16-36 points in live litigation, and its failure mode is assessing one's own work). See `research/SEARCH-COMPLETENESS.md`. | M10 - open · the completeness statement gains a search record from the observation log |
| D-195 | design | 2026-08-04 | **DEC-32's OR-arithmetic takes the MAXIMUM over branches, which is only sound if the branches are INDEPENDENT — and nothing in the design tests independence, which three separate research strands identify as the exact failure that defeats every professional methodology.** DEC-32 (ruled 2026-08-04) sets strength as MIN over AND-related legs and MAX over OR-related branches, with *independently sufficient* an AUTHORED member judgment. **The research says an authored judgment is precisely what fails here, because correlation is invisible from where the author stands.** THREE INDEPENDENT DEMONSTRATIONS: (1) **The New York Times' Iraq WMD post-mortem** — reporters had defector testimony PLUS senior administration confirmation, two sources from different institutional categories, and the editors' note records that the accounts were *"often eagerly confirmed by United States officials"* who had themselves been fed by the same INC/Chalabi pipeline. Multiplicity was real; independence was not. (2) **Steve Buttry's Farragut case (Verification Handbook, p.15)** — twelve team members plus the opposing star and coach, fourteen honest non-colluding eyewitnesses, all consistent, all WRONG the same way; game video showed one foul where all fourteen remembered three or four. His conclusion: source count is not evidence when a shared narrative can generate correlated error. (3) **The Berkeley Protocol names CIRCULAR REPORTING as a hazard (para.187) and supplies no test for it**; the Verification Handbook prescribes triangulation while its own lead case study shows triangulation failing; Bellingcat's peer review is same-institution; ICIJ's journalists all work one corpus. **THE CONSEQUENCE FOR BIO IS EXACT: two OR-branches whose legs trace to captures sharing an origin are not two grounds, and taking the max publishes a strength the finding does not have — the Judith Miller error with arithmetic behind it.** THE FIX BIO IS UNUSUALLY WELL PLACED TO BUILD, and no newsroom could: provenance is CONTENT-ADDRESSED and every capture records its origin and chain, so the system can DERIVE whether two branches share an upstream source and surface it. Derived informs, authored binds (D-90) — the system flags *these two grounds share a source* and the member decides; it must not silently compute independence, and must not refuse the act. Also record what the member asserted: *independently sufficient* should mean independent in ORIGIN, not merely in reasoning, and the entry's own anti-gaming argument (visibility to a reader) only works if the shared origin is visible too. | M9 · open — pair with REC-12's strength derivation; DEC-32's arithmetic is unchanged, the INDEPENDENCE CHECK is what is missing |
| D-194 | gap | 2026-08-04 | **A member's LEAD has no home, and a search that came back EMPTY is not recorded at all — so the most evidentiary product of shoe leather is the one the record drops.** Named by Bob 2026-08-04: *"A reporter faces the same hazard — that corpus silence gets read as evidence of absence. That's why tips and shoe leather are so valuable."* MEASURED: the frontier today is the `deferred` link partition (`subresources.mjs:660`) — URLs DISCOVERED INSIDE DOCUMENTS. **A tip is not a URL**; it is a thing a member has reason to believe exists, sought. Nothing in the model holds *"there should be a contract between X and Y — look for it"*, and nothing holds the outcome when the looking fails. So the frontier is entirely DERIVED, and a derived frontier inherits the corpus's blind spots exactly — the machine analogue of a reporter who only reads clippings. **THE CONSEQUENCE THAT MAKES THIS EVIDENTIARY RATHER THAN ERGONOMIC:** RFC 2308's rule (see `STORE-AS-CACHE.md`) is that a negative answer with no proof of AUTHORITY attached is not recordable at all — the SOA is what makes *it does not exist* a finding rather than a shrug. **A member's authored lead IS that authority.** *We went looking for the document this tip described, across these sources, on this date, and there is none* is a finding with a name behind it; the same empty result with no lead behind it is silence that cost nothing to produce. Sibling to D-184 (a member's firsthand OBSERVATION has no home as a basis leg) — the same member knowledge is unrepresentable both BEFORE the search and AFTER it, and the two should be designed together. | M4 - open · the authored frontier; pair with D-184 |
| D-190 | gap | 2026-08-04 | **A Durable Object's SQLite is capped at 10 GB per object and this ceiling is recorded nowhere in the repository.** VENDOR CLAIM (developers.cloudflare.com/durable-objects/platform/limits, retrieved 2026-08-04): 10 GB per object on Workers Paid, 1 GB on Free; unlimited objects; a soft ~1,000 req/s per object. D-36 records workerd's SQL STATEMENT limits (100 bound params, 5 compound-SELECT terms) and M6 names R2 growth and the free-tier storage ceiling, but the DO's own storage ceiling — the ceiling on THE RECORD, as distinct from the captured bytes in R2 — appears in no row and no measurement. Today that is abstract because every instance is a development instance. **Under the read-through-cache framing (`STORE-AS-CACHE.md`) it stops being abstract, because the store then grows BY DESIGN rather than by member action.** At some corpus size the record must sharded across objects, and the single-threaded ~1,000 req/s per object is a second ceiling on the same decision. Deciding it now is cheap; deciding it after a cache model ships is the D-113/scheduler class — a shape question settled late, with consumers already built on the wrong assumption. NOT MEASURED BY US: the vendor figure is a claim, and what matters is the growth CURVE per captured document, which nothing has measured. | M6 · **PARTLY MEASURED 2026-08-08 (FL-1, riding D-218s measurement — same `MEASUREMENTS.md` entry).** Instrument: the planes own `op=stats` -> `dbBytes`, i.e. workerds `ctx.storage.sql.databaseSize` (`src/store.mjs:11209`), read live on two namespaces. **Empty-schema baseline 925,696 B; loaded store 6,402,048 B at 31 bundles / 142 files / 88 register / 244 history -> 176,657 B per bundle**, which against the vendors 10 GB claim is ~60,800 bundles and against 1 GB ~6,073 (numerator ours, denominator theirs). **ALSO MEASURED, and it is why this row stays OPEN: the platform has NO per-object storage surface at all** — `durableObjectsStorageGroups` carries only date dimensions and returned zero rows on this account, and `durableObjectsPeriodicGroups` is per-object but reports no stored bytes, so `dbBytes` off the object is the only handle there is. **AND THE MEASURED POINT HAS AN EMPTY MEANING LAYER — connections, entities and resolutions are all ZERO — so the slope prices CAPTURE ONLY and is a FLOOR on cost / an OVERSTATEMENT of capacity.** What remains is exactly D-224s measurement: the quadratic `connections` growth on the same object. Do not read 60,800 as headroom until that lands |
| D-191 | design | 2026-08-04 | **A capture assembled from reused subresources is a COMPOSITE with a temporal spread that the record does not state, so it may depict a moment that never existed.** From the archival-systems survey (`STORE-AS-CACHE.md`): Ainsworth, Nelson & Van de Sompel measured that embedded resources in an archived page routinely carry datetimes *"up to several years in the future or past"* relative to the embedding page, and the follow-up study is titled *"Only One Out of Five Archived Web Pages Existed as Presented"* (arxiv.org/abs/1402.0928; HT '15). **BIO builds exactly this shape on purpose**: `CAPTURE-SCALING.md`'s reuse policy fetches evidence and reuses furniture, and the manifest records `reused_from` and `reused_fetched_at` PER PART — so the data to compute the spread exists and nothing computes or states it. CAP-4's re-fetch at ratification NARROWS the spread and does not MEASURE it. The honest shape is the one the record uses everywhere else: state the fact rather than fix it — a composite capture carries its temporal extent (earliest and latest part fetch instant) and a reader weighs it, exactly as they weigh a grade. This matters most for a rendered capture offered as evidence of what a page LOOKED like, which is the class D-64/D-55 covers. | M2 - open · state the spread; the per-part data already exists |
| D-192 | defect | 2026-08-04 | **Integrity of stored bytes is not integrity of what a reader SEES, and archived-page replay is a documented attack surface.** Lerner, Kohno & Roesner, *Rewriting History: Changing the Archived Web from the Present* (ACM CCS 2017, adalerner.com/lerner-rewritingHistory-CCS17.pdf): four demonstrated attacks that alter a reader's view of an archived page — text, images, styling and behaviour — by injecting or abusing script that runs at VIEW time, and the paper's own summary is that *"attackers do not need to compromise the archives in order to compromise users' views of a stored page."* BIO stores captured HTML and renders it, and the whole product is trustworthiness of the record, so a reader shown an altered rendition of bytes we hold intact is the defect class this project cares about most — the hash stays correct while the claim the reader forms is wrong. NOT YET ASSESSED against our own viewer: what BIO renders, with what sandboxing, and whether any captured script executes at view time is UNMEASURED and is the first step. The mitigation vocabulary is standard (sandboxed iframes, CSP, stripping or neutering script at RENDER time while never altering the STORED bytes — the stored/served distinction is the load-bearing one, since altering stored bytes would break the hash and the premise). | M2 - open · measure what the viewer executes before designing the fix |
| D-188 | design | 2026-08-04 | **"Bias debt" reads as "bias", so the publication rule sounds like it forbids what the doctrine requires — one word, two meanings, caught free.** DEC-20 ruled 2026-08-02 that only a HUNCH blocks publication and that ordinary declared bias is disclosed and TRAVELS with every published case (*"bias debt is DISCLOSED; hunch debt is DISQUALIFYING"*). But `BIO_Declared_Bias_v0_1.md`'s bias-debt section still reads *"a work product carrying bias debt cannot advance its workproduct_state or be ratified for publication until the debt is settled"*, and a reader who does not already know that `debt` here means specifically a hunch reads it as *bias must be cleared before publication* — the opposite of the doctrine. **THE EVIDENCE IS THAT BOB HIMSELF RE-READ IT AS A CONTRADICTION on 2026-08-04, and he authored the ruling.** If the person who decided it misreads the text, every later reader will. This is the D-156 class (one term, two senses) and it is free right now: no table, op or surface names either sense. FIX: say HUNCH DEBT wherever the disqualifying rule is meant, reserve *bias debt* for the general disclosed class, and state the DEC-20 principle at the head of the section rather than only in the decision register. | M10 · **CLOSED 2026-08-05 by REC-47** (DEC-46 (d)), and the sweep found more than the row predicted, which is why the closure is worth reading rather than ticking. **The row called this "documentation pass, no code". IT WAS NOT.** Four CODE sites named the disqualifying rule with the disclosed term — `schema.mjs`'s `grade_source` comment, `bio-checks.mjs`'s `GRADE_SOURCES`, `store.mjs`'s hunch-composition comment, and six spellings in `earnedbasis.test.mjs` — all corrected. **AND THE SWEEP FOUND A SECOND, WORSE CLASS THE ROW DID NOT NAME: the pre-DEC-20 BLANKET RULE is still ASSERTED, as live doctrine, in five places.** (1) `bio-plane/src/queuestate.mjs` and (2) `docs/development/NOTIFICATIONS.md` both describe the `bias-debt` obligation kind as *"blocks a transition"* — two copies agreeing with each other on the overruled rule, and the producer is unbuilt (D-86's remaining half), so it would have been BUILT TO THE SENTENCE. (3) `BIO_Content_Framework_v0_10.md` rests a whole section ("Decay is loud and never blocking") on *"bias debt BLOCKS"* as its contrast partner, with a two-row asymmetry table — corrected to three rows, and the argument comes out sharper: what makes something blocking is not that a lens changed but that a member asserted a GRADE the evidence does not support. (4) `BIO_Design_Requirements_v2.md`'s July-27 Bob addendum states the blanket rule in the same breath as the requirement it contradicts — annotated rather than rewritten, because it is a dated record of a direction. (5) `BIO_Intake_Doctrine_v1_1.md`'s ratification clause. Also corrected: `BIO_Declared_Bias_v0_1.md` (the banner D-188 asked for, at the head of the section; the blocking sentence STRUCK rather than deleted so a reader who remembers it can see it is gone; the DEC-15 heading; Bob's own quotation left VERBATIM with the vocabulary reconciled after it rather than edited), `BIO_Case_Making_v0_1.md`, `BIO_Interaction_Constructs_v0_1.md`, `MILESTONES.md` D-154. DELIBERATELY NOT TOUCHED: `newgroup/src/release.mjs` (bundled copy — D-193's rule, refreshes at the next cut), `bio-plane/dist/` (build artifact), and the occurrences in `README.md`, `CONSTRUCTS.md`, `PROCESS-INVENTORY.md` and two further `BIO_Content_Framework` sites, which use *bias debt* in its CORRECT general sense and are the term's legitimate home. |
| D-193 | debt | 2026-08-04 | **`newgroup/src/release.mjs` embeds a bundled copy of `setup.mjs`, so an installer cut before the next release ships the PRE-REC-41 setup panel** — the one whose "Roles with passwords" row described a disclosure the plane no longer makes, and whose paraphrase of the login refusal stated the old two-code distinction more plainly than the plane ever did. Reported by REC-41 and deliberately NOT hand-patched, because hand-editing a bundled copy is how the copy and its source drift apart (D-21). It refreshes for free at the next release cut. THE ROW EXISTS SO THE CUT IS NOT MADE UNKNOWINGLY: whoever cuts the next plane release checks that the bundle was rebuilt from the current source rather than assuming it. | M7 · open — closes at the next DIST release cut; no code change owed before then |
| D-197 | defect | 2026-08-04 | (renumbered by CONDUCT at UI-37's integration — it was written as D-195 and collided with an existing D-195; **the older row keeps the number, as with every collision this session**) **CLOSED 2026-08-04 by UI-37, and it was THREE refusals across THREE surfaces rather than the one this row recorded — see UI-37's landed line; the worst was `pubList`, which rendered a refusal as "This group has not published any case files yet", a claim about the ENTIRE record.** The public verification surface told an uncredentialed reader that the record has no such part, when the plane in fact REFUSED to answer.** `pubVerify` calls `op=verify` through `apiQ`, which — unlike `rec` — does not throw on `ok:false`; a refusal envelope carries no `published` field, so it falls to the else branch and renders *"NOT PUBLISHED. No published part answers to that hash. A hash that was never ratified and a hash that never existed are the same answer here, deliberately."* MEASURED by UI-36 and VERIFIED INDEPENDENTLY BY CONDUCT at both ends: `op=verify` refuses a malformed hash with `{ok:false, error:"verify requires sha256=<64 lowercase hex>"}` at HTTP 400; `api()` never checks status; and the container row composes its hash as `bundle_sha || ""`, so an empty hash reaches this by a CLICK. **This is the worst shape of defect this project recognises — the record asserting something it does not know — and it stands on the one surface whose entire purpose is that a stranger can verify WITHOUT our cooperation.** The sentence is also unusually convincing, because its second clause explains why the two cases are deliberately indistinguishable, which is true of a real not-published answer and false of a refusal. NOT FIXED AT DISCOVERY, deliberately: DEC-49 is open with Bob and the pre-authentication surfaces must not move while its measurement is being taken. UI-36 pinned it with an assertion that says at the site that it pins a DEFECT. | M8 · **CLOSED AT BOTH LAYERS 2026-08-05. UI-37 closed the RENDERING (three refusals across three surfaces); REC-52 closed the LAYER BENEATH IT, and that half was eleven sites rather than the two its item named.** The plane itself converted its own failures into substantive negatives, so a surface that faithfully rendered what it received rendered a lie no surface could correct: `op=verify` and `op=bootstrap` spread an unchecked result into `{ok:true}` at HTTP 200; `op=publishedmanifest` did the same by a DIFFERENT route (`result: undefined`, which `JSON.stringify` DROPS) on the op that fills the published INDEX — so the rendered consequence was the WHOLE record, which is the sentence D-173 and this row turn out to share; `op=publishedbytes` answered THIS ROW'S OWN SENTENCE ("a hash that was never ratified and a hash that never existed are the same answer here, deliberately") over a store that never answered, so the wording this row records as unusually convincing was being minted in the PLANE as well as at the surface; `op=publishedcase` synthesised `NOT_PUBLISHED`, and its `publishedtargets` read made every basis leg of every finding say "what it rests on is not in the published record"; `op=affordances`, `op=queue` and `op=monitor` minted `NO_FACTS`, `NO_QUEUE` and `ABSENT`; `op=knock` reported a store silence to an anonymous member of the public as HTTP 429; and the SESSION lookup reported it to a signed-in member as a verdict on their credential. FIXED AT A CHOKEPOINT (`doAnswer`/`storeSilent` in index.mjs) with a source-level sweep asserting no handler can spread an unchecked result, its reach proved as a DELTA, and `test/plane-envelope.test.mjs` driving a REAL Durable Object failure at each site. Both directions are held: a genuine not-published answer still reads as not-published, with its own arm. TWO SITES REPORTED AND DELIBERATELY NOT FIXED — both inside REC-47's concurrently-held publish/ratify block, both this same class: `do/list`'s result, defaulted to an empty array, hands the gate an EMPTY known-id set so every reference reads as unresolved, and `do/reusedparts`' silence reads as "nothing was reused". Pinned as RELATION assertions, which rule on nothing. |
| D-189 | gap | 2026-08-04 | **No surface can say that a project carries its own bias, and the UI's only notion of declared bias is hunch legs.** MEASURED 2026-08-04: `civicos-ui/app.html:12776` says verbatim *"DECLARED BIAS is the HUNCH legs and nothing else"*, and D-84 records that `object_type: bias` is absent from the check catalogue so a bias bundle cannot be written at all. Meanwhile `BIO_Declared_Bias_v0_1.md` makes project-level bias load-bearing: project managers define it, it may OVERRIDE instance statements, and `effective bias` = instance statements minus project nullifications plus project additions — so which project a piece of work sits in DETERMINES the lens it is evaluated under. Ruled by Bob 2026-08-04 (DEC-46): *"UI surfaces need to indicate that a project contains its own bias."* This is a requirement on UNBUILT work rather than a defect in shipped work, and recording it now is what stops the project surfaces being built bias-blind and retrofitted — the same reason DEC-46's import ruling had to arrive before import. Rides with the bias bundle (D-84 first). | M4 · open — with D-84's bias bundle |
| D-187 | design | 2026-08-04 | **A published case was built as exactly ONE inquiry, and nobody ever chose that — it was assumed by every item in the chain.** MEASURED against source when Bob asked for a fact check of his own definition: `store.mjs:3539` refuses verbatim *"publishing publishes ONE case: pass target=<inquiry id>"*; `published` is a state of an INQUIRY (`STATES.inquiry`); `published_bundles` is keyed `(bundle_id, edition)`; `index.mjs:3667` builds the container as `case: body.bundleId` with its own comment *"the parts listed here ARE the bundle's files"*; `MILESTONES.md:428` says *"a concluded inquiry is published"*. Bob's definition is the opposite and is RULED (DEC-44): a case is one or MORE findings, scoped to the project that gathered them. **THE ESCAPE HATCH IS CLOSED BY DEC-32, ruled the same day** — a parent inquiry citing children would collapse several propositions into one conclusion with one falsifier, which is the overclaim DEC-32 exists to prevent; so the two rulings are consistent only if a case is a CONTAINER over findings. Second measured consequence: a published child *NAMES its parent and siblings, serving neither* (UI-18), so even under composition a stranger holding the zip cannot read the findings it points at without returning to the instance — defeating S9's read-without-our-cooperation premise. Reshapes DONE items (REC-14, REC-22, UI-18) as a CORRECTION not new scope. Nothing is in production, so it is a schema correction rather than a migration. The trap to avoid while fixing it: a case must NEVER derive a single case-level strength — each finding keeps its own pair (R2's forbidden composition at a new altitude). | M10 · open — DEC-44's enactment |
| D-186 | defect | 2026-08-04 | **The test battery leaks one OS temp directory per miniflare instance and never cleans up, and it filled the development machine's disk to ZERO.** MEASURED 2026-08-04 when a planning-surface write failed with ENOSPC: **23,263 `miniflare-*` directories holding 41.0 GB** in the user's `$TMPDIR`, plus ~2,300 more from the suites' own sandboxes (`ratify-`, `sshsig-`, `signpage-`, `attest-`, `reuse-ratify-`, `publish-`, `publishedcase-`, `reeval-`) and 595 MB from the OCR probes (`cpdf9-ocr-`, `cpdf11-moondream-`, which npm-install engines into temp by design). 36 of 42 suites drive the plane through miniflare, CONDUCT runs the FULL battery on every worker landing, and nothing removes the sandbox afterward — so the leak scales with integration throughput and accumulated ~23k directories in days. **THE COST IS NOT DISK, IT IS THE THING THAT STOPPED:** at zero bytes every Bash command failed before it started (the harness could not write its own output file), so the session could not commit, push, or reach CONDUCT at all — a planning surface that is written and unpublishable is exactly the failure `plancheck` exists to prevent, arriving by a route it cannot see. Reclaimed 37.2 GB by deleting only dirs older than the running battery (a worker was mid-`test:battery`; deleting its live sandbox would have broken CONDUCT's verification — the fix must be equally careful). NEEDS: the harness disposes of its miniflare sandbox in a `finally`, the suites dispose of their own, and the battery sweeps ORPHANS older than the current run at start; the OCR probes' npm installs are separately bounded. A negative control exists for free — run the battery twice and assert the temp-dir count does not grow. | M0 · M0-8 CLOSED IT 2026-08-04, and CORRECTED ITS MECHANISM — record the correction, because the intuition in the row above is backwards and the next reader will have it too. **The leak is the SUCCESS path, not the failure path.** `Miniflare#dispose()` (4.20260722.0, `dist/src/index.js:104655`) takes down its own synchronous `process.on("exit")` cleanup and THEN starts removing the sandbox without awaiting it; every suite ends `await mf.dispose(); process.exit(…)` — which `hygiene.test.mjs` REQUIRES, so a lingering handle cannot turn a green run into a hang — and the exit beats the unawaited removal with the safety net already gone. Measured three modes × three runs: succeed-and-exit leaked 3/3, THROW leaked 0/3 (miniflare's exit hook is still armed when a suite never reaches dispose), dispose+250 ms settle leaked 0/3. So a `finally` would have fixed nothing, and 23,263 directories in days is the record of a battery **working**. FIX: `bio-plane/test/sandbox.mjs` owns `$TMPDIR` for the process and `rmSync`s it synchronously on exit and on signal — which covers the suites' own `ratify-`/`sshsig-`/`attest-`/… sandboxes for free, since they all `mkdtempSync(join(tmpdir(),…))`; `hygiene.test.mjs` requires the import of every suite that builds a Miniflare or mkdtemps, so the list cannot fall behind the directory (D-93's lesson); `scripts/battery.mjs` sweeps orphans by OWNER pid where the name carries one and by age-with-2h-grace where it does not, and spares outright anything a live `workerd`/`node` holds open (`lsof`) — which is not decorative: a foreign worktree's suite was found holding a sandbox after 1h28m while this was being written. The battery now ASSERTS it, from a temp directory of its own that it hands to every child, so the count is attributable and a concurrent un-fixed checkout cannot redden the run. Verified: battery green twice, 95/95, 5172 assertions, 0 residue each; NC (remove the exit disposal) → 84 sandboxes leaked and the run fails while all 95 suites still report green. RESIDUAL, deliberately left: the OCR/PDF probes are out of scope by the item, and `tier1-coverage-probe.mjs`'s persistent `$TMPDIR` cache is why they must stay out. |
| D-185 | design | 2026-08-04 | **The free-tier frugality code now defends against limits no SUPPORTED instance runs under, and the old warning about it INVERTS.** `CAPTURE-SCALING.md` warned that developing on Paid would rot the free-tier path, because the 50-subrequest ceiling and the 10 ms CPU discipline would stop being exercised while every unfunded community instance depended on them. DEC-42 removes the dependant: Free is no longer supported and the installer refuses it. **The wrong reading is to delete the frugality** — resumable sessions, the calibrated ceiling, capture-across-ticks and the CPU discipline are cheap to keep, expensive to rebuild, and still the difference between a heavy first capture completing and failing even under Paid's larger budgets. The RIGHT reading is that the project must stop believing it is free-tier-viable while nothing exercises that path: either the installer's refusal is the only free-tier statement the system makes, or a free-tier configuration is deliberately re-supported and TESTED. Pick one in writing rather than drifting. | M7 - open · DEC-42's item 4 ENACTED 2026-08-04: DO NOT DELETE the free-tier frugality code — resumable sessions, the calibrated ceiling and capture-across-ticks are cheap to keep, expensive to rebuild, and still decide whether a heavy first capture completes under Paid. What must be settled in writing is that the project stops believing it is free-tier-viable while nothing exercises that path. |
| D-183 | design | 2026-08-04 | **A derived transcription's GRADE rests on a measurement, and nothing binds the transcription to the measurement that justified it — so an engine that changes silently invalidates grades with no way to find which ones.** Named by Bob 2026-08-04: *"given that web services are occasionally upgraded, and a Moondream revision could affect the trust level."* The chain records WHO derived the text (`ocr(engine, version)`, DEC-4) and CPDF-11 sets the engine's fidelity CAP by measurement — but the cap is a fact about the engine AT A DATE, and the transcription records no reference to it. So when a re-probe returns a different number, the blast radius is unknowable: every transcription ever made by that engine is equally suspect and equally unidentifiable. **THE FIX IS ONE FIELD AND ONE CONSTRUCT: a CALIBRATION** — a dated, identified measurement of a derivation engine's fidelity, which the chain REFERENCES (`ocr(moondream3.1, calibration: CAL-n)`), so a superseded calibration names exactly the transcriptions resting on it. **AND THE DRIFT HANDLER IS ASYMMETRIC, which is the part a build session will not derive**: a calibration that measures WORSE means legs resting on the old cap now overclaim, so it SURFACES a re-evaluation obligation (REC-17's shape, DEC-16's event propagation) and recomputes nothing on the member's behalf; a calibration that measures BETTER raises NOTHING automatically, because a grade rises only by an authored act (DEC-4, no machine mints a grade). Silent re-grading in either direction is the defect. **THIS GENERALISES BEYOND OCR and is the reason to build it once**: Tier-2 pdf.js has a pinned version that will move, an AI post-processor is a second derivation, an external service retrains under an unchanged name (DEC-35's own argument against Textract) — three engines with one problem, the D-164 lesson applied before rather than after. Trigger for the re-probe is the REC-1 alarm (a sixth consumer); a vendor CHANGELOG watch may only ACCELERATE a probe and may never satisfy one, because absence of an announcement is not evidence of no change and a vendor's documentation is a claim, not a measurement. | M2 · open — CPDF-13 (the calibration construct + scheduled re-probe; corrected 2026-08-07: the fixing item is CPDF-13, not CPDF-12 — renumbered at drain), sequenced behind CPDF-11's GO/NO-GO |
| D-182 | design | 2026-08-04 | **risk_tier is D-130's unreached residue**: C-2.10 admits 1/2/3, nothing publishes member words for them, no undetermined value exists — a surface either invents meanings or writes the floor (both writers keep 1, reasoning at each site). Either publish member-facing words for the tiers (REC-38's pattern) or make it three-valued the way authority and counterparty were. | M10 - open · pair with the next action-surface item |
| D-198 | gap | 2026-08-04 | (**renumbered THREE times by CONDUCT** — from a colliding D-184, then a colliding D-185, then a colliding D-188; other sessions' rows hold all three numbers. This row is CLOSED, so it moves rather than the open vocabulary row that now holds D-188: renumbering a closed row costs two pointers, renumbering an open one costs every future reference to it) **op=bootstrap (classes null — unauthenticated) answers `roles`: every credentialled role with the date each password was last set, and NOTHING consumes it** — not setup.mjs, not civicos-ui, not newgroup, no battery assertion (measured by REC-39). An unconsumed disclosure on an anonymous op; REC-39's login-distinguishability reasoning RESTS on it being open, so closing it reopens that decision — the two travel together. | M7 · **CLOSED 2026-08-04 by REC-41** — the `roles` field is gone from the anonymous answer (structural sweep over the whole response, NC (a) restores it and names it by path and value), IC-20 filed, I3 → 6.0.0. The travelling half went with it: the login codes collapsed to one `SIGN_IN_REFUSED` with a sentence, because with the roster closed the distinct codes became an unmetered anonymous oracle — and the collapse was defeated by an eleven-fold timing difference (0.6ms vs 6.7ms) until that was equalised too. Premise correction on the row above: `newgroup` DOES consume the op (src/index.mjs:364, :631), just not this field. |
| D-199 | design | 2026-08-04 | **The `ai` credential class and its declared task scope — DESIGN AHEAD, recorded here by CONDUCT enacting DEC-55 (Bob, 2026-08-04), because nothing reaches the plane today and a scope kept only in the decision register reaches only the person who already knows it.** Whoever builds the assistant (DEC-27) builds to these five. (1) **Mint the `ai` class in `classify()` on the DEC-37 precedent**, with a DECLARED TASK SCOPE enforced at the gate in `scopeFor`'s shape. (2) **The task scope is declared IN THE RECORD, never in a settings row** — DEC-17's reasoning transplanted: a settings row *"would be a way to change the standard with nothing to read afterwards"*, and what an AI credential may reach is exactly the thing that must be amendable only as an authored, dated, on-the-record act. (3) **Minting an AI token is a MEMBER act, never an AI act.** If an agent can request a broader token, the scoping is theatre — the never-prefill pattern applied to capability rather than to text. (4) **The record names the token IDENTITY and the PRINCIPAL behind it** — an organisation-scoped key acts for the group with nobody individual behind it; a member-scoped key is attributable. Both are legitimate, they carry different accountability, so an act must say which; never the token's value, which `tokens.mjs` already denylists on publication. (5) **THE EXISTING MACHINE FENCES GENERALISE TO THE NEW CLASS BY CONSTRUCTION — AND ONLY BECAUSE OF REC-46, WHICH LANDED THE SAME DAY.** Until then `store.mjs` answered *"is this a machine"* for itself ELEVEN times in two hand-typed shapes while the catalog answered it a third way with a word list that knew nothing of the prefix, and REC-45 had measured the cost: the gate accepted `asserted_by: token:member`. REC-46 collapsed them into one predicate over the `token:<class>` stamp, so **a token stamping `token:ai` is caught by every `MACHINE_CANNOT_*` refusal automatically**. Asked a day earlier this would have meant editing eleven sites and probably missing one. **The negative control is therefore cheap and MANDATORY, and ships WITH the class rather than after it:** mint an `ai`-class credential and assert that `MACHINE_CANNOT_PUBLISH`, `_CONCLUDE`, `_RELEASE`, `_REOPEN`, `_CORRESPOND`, `_MOVE_ACTION` and D-151's resolve/forward refusals each fire BY NAME — and that removing the predicate makes them all pass. **SEQUENCE THE CHECK ROLE FIRST**: DEC-24 calls it *"the role most worth building"* because the threat model is self-directed overclaiming, and it is also the safest, since a role that only reads and raises needs the narrowest scope of the four (EXTRACT, PURSUE, FIND, CHECK). | M9 · open — **NO LONGER ONLY WITH THE ASSISTANT (updated 2026-08-05, DEC-60).** The `ai` class now has a SECOND consumer and it is the first to hold a mutating op: the INVESTIGATIVE SESSION (`INVESTIGATIVE-SESSION.md`), whose scope reads across a project and writes only suggestions through one endpoint (IS-3/IS-4). **All five determinations above hold unchanged and IS-4 extends the scope vocabulary rather than revising it** — (2) in particular is what makes an investigative scope declarable in the record rather than in a settings row, and (5)'s negative control now has a second class of fence to fire against. DEC-24's rule still stands over all of it, and DEC-60 is its fullest expression rather than an exception: the machine proposes — now including the reasoned object itself — and the member authors by accepting. Nothing is owed before the assistant (DEC-27) OR before IS-1. Appended 2026-08-07, RESOLVED later the same day (session BOB, under Bob's tactical delegation — `IS-SWEEP-2026-08-07.md` §4b item 7): **CHECK is the investigative session's first DEPLOYED MODE** — the session run with its stated objective against an existing conclusion IS the CHECK role, so the enacted CHECK-first instruction is satisfied by one build, not a separate prior one. Milestone token corrected M4→M9 2026-08-07 per MILESTONES.md's M9 placement note (finding F8) |
| D-200 | defect | 2026-08-04 | **`op=audit` on the live instance is NOT clean: 31 checked, 21 clean, 10 with errors, every one `C-18.9`** — *"provenance documents[0] is at or past verified with no provenance_chain: a published hash claims these bytes came from somewhere by some route, and this document names none"*. MEASURED against the live plane 2026-08-04 immediately after the 0.56.0 deploy. **NOT caused or surfaced by that deploy, and this was established rather than assumed: `C-18.9` appears identically (4 occurrences) in the 0.55.0 release bundle and the 0.56.0 one, so the previous build would have reported the same ten.** It is RECORD STATE, not code: ten bundles sit at or past `verified` while naming no provenance chain, which is the record claiming a route it cannot show — the overclaim class this project exists to refuse, sitting in live data rather than in a code path. DIST's gate step 8 says `op=audit` clean before anything is called done, so **this release is deployed with a KNOWN unclean audit and that is stated rather than quietly passed**. NEEDS: decide per bundle whether the chain can be reconstructed from what was captured, or whether the honest act is to move the document back off `verified` — and note that back-dating a chain nobody recorded would be exactly the invention the gate exists to prevent. | M2 · **ten dispositioned 2026-08-05 by REC-54, mechanism built and tested, APPLICATION TO THE LIVE RECORD PENDING A DEPLOY** (op=provenancechain does not exist in the serving 0.56.0 build; DIST cuts releases and the plane deploy is gated to Bob). All ten reconstruct from evidence the register already held — nine by direct daemon fetch (locator + retrieved + capture.method), one by a member custody block — each hop STAMPED `reconstructed` so a chain derived in 2026-08 cannot be read as one witnessed at capture. Was **OWNED by REC-54** (RECORD re-promoted 2026-08-05 to run it); do not close by weakening C-18.9, and do not back-date a chain nobody recorded |
| D-201 | defect | 2026-08-04 | **`bio-plane/scripts/deploy.mjs` would DESTROY the `civicos` UI worker's only binding, and its own comment invites you to point it there.** That comment reads *"The UI worker carries no version number in the shared repo and contends for nothing, so it is not gated"* — true about the BATON, and it reads as permission to deploy the UI with that script. But its metadata is the PLANE's: it declares `VERSION`, `INSTANCE_NAME` and the two R2 buckets, and its `keep_bindings` is `["secret_text", "durable_object_namespace"]` — **which does not include `service`**. MEASURED 2026-08-04 by reading the live worker's settings before deploying: `civicos` has exactly ONE binding, `service PLANE -> biosmoke7`, and it is what makes `/api` reach the plane at all. Deploying the UI through `deploy.mjs` would drop it and leave the site serving HTML whose every request fails. CLOSED THE SAME TURN by `civicos-ui/deploy-ui.mjs`, which carries the UI's own metadata and the same read-back-and-hash discipline — **and which itself first REFUSED a deploy that had actually succeeded**, because the account returns the module inside a MULTIPART envelope and hashing the response body compares the envelope; `deploy.mjs` already knew that and the new script now does the same extraction. Row kept open until `deploy.mjs` REFUSES the `civicos` slug by name rather than relying on nobody trying it. **CLOSED 2026-08-05 by DIST.** `deploy.mjs` now refuses by name from a `NOT_A_PLANE` table, each entry carrying its own reason and its own deploy path, BEFORE the baton check — and the old `const GATED = slug !== "civicos"` is gone, which is the line that made the UI the one slug reaching the destroying PUT with no gate in front of it at all. **`pdf-worker` is in the same table**, because it is the identical hazard (a fleet member with no `PUBLISHED` binding and no Durable Object, which this script's metadata would bind all three) and leaving it out would have been the same reliance on nobody trying it that kept this row open. An ALLOWLIST was considered and rejected as unavailable: plane slugs are chosen by the groups that install them, so they cannot be enumerated. NEGATIVE CONTROLS RUN 2026-08-05, all three: `civicos` → refused exit 3; `pdf-worker` → refused exit 3; and the other direction, a plane slug (`biosmoke7`) → NOT refused by the new guard, falling through to the baton check and stopping there on the missing `--thread` — so the guard is proved to bite AND proved not to over-refuse. Each was run with NO `--thread`, which is precisely the invocation that under the old code skipped the baton entirely and went straight to the PUT. | M0 · CLOSED 2026-08-05 (DIST) |
| D-202 | defect | 2026-08-05 | **The plane's DECLARED configuration and its DEPLOYED configuration have silently diverged, and `deploy.mjs` is structurally incapable of closing the gap.** `bio-plane/wrangler.jsonc` declares two service bindings — `PDF_WORKER -> pdf-worker` and `SELF -> bio-plane` — and **MEASURED 2026-08-05 against the live account, `biosmoke7` has ZERO service bindings of any kind** (8 bindings live: ADMIN_TOKEN, MEMBER_TOKEN, PROBE_TOKEN, CAPTURES, PUBLISHED, STORE, VERSION, INSTANCE_NAME). The cause is the same one as D-201, pointed at the plane instead of the UI: `deploy.mjs` sends a HARDCODED binding list (VERSION, INSTANCE_NAME, CAPTURES, PUBLISHED) that never had a service entry, and its `keep_bindings` did not include `service` — so a class it neither SENDS nor KEEPS is DELETED on every deploy. `wrangler.jsonc` is the file the repository treats as authoritative and the file every reviewer reads, and **no deploy has ever consulted it**; the two have been free to drift with nothing comparing them. Two live consequences, neither of them theoretical: (1) **REC-26's `SELF` binding never took effect.** The delegation landed it in `wrangler.jsonc` and stated its acceptance as deploy-gated; the deploy then happened (0.56.0, 2026-08-04) and did not carry it, so CAP-3's archive-monitor and REC-26's monitor-cadence are STILL built-and-wired-to-nothing, exactly as `MACHINE-PROCESSES.md` §0 measured — and the landing looks complete in every document. (2) **`PDF_WORKER` is unbound, so `op=pdfstructure` degrades to Tier 1 on the live plane** (D-115's un-fleeted path) — and `pdf-worker` is not deployed to the account at all (**HTTP 404**), so the binding could not have resolved even if sent. PARTIAL FIX APPLIED 2026-08-05 (DIST): `service` added to `keep_bindings`, which is the PROTECTIVE half — it stops a future deploy deleting a service binding that is present, and changes nothing today because none is. **NOT FIXED, and deliberately not by DIST mid-cut:** actually SENDING the declared bindings. That is a behavioural change — it ARMS the two monitoring consumers on a live instance, whose semantics belong to RECORD and CAPTURE, and it cannot work for `PDF_WORKER` until `pdf-worker` is deployed at all. NEEDS: `deploy.mjs` to derive its bindings FROM `wrangler.jsonc` rather than restate them, so the declared config is the deployed config by construction and this class cannot recur; plus a decision on deploying `pdf-worker`. | M1 · open — the protective half landed; deriving bindings from wrangler.jsonc and arming SELF are the remainder |
| D-203 | defect | 2026-08-05 | **Five checks in the catalog advise a repair the state machine refuses, and a sixth check refuses it by name.** MEASURED 2026-08-05 by REC-54: `checks/bio-checks.mjs` carries five repair strings telling an operator to *"return the bundle to collected"* or *"set current_state to collected"* (in `checkAuthorityPublishable` (C-18.9), `checkReleaseAuthority` twice around C-18.1, and two more near C-22 at the signed-release arm). `STATES.information.edges` is `{ collected: ['verified'], verified: ['retired'] }` — **there is no `verified -> collected` edge**, and `checkStates` (C-4.2) fires `transition verified -> collected is not a legal information edge` on anything that tries. So an operator who follows the advice produces a NEW error, and the honest disposition D-200 named — *move the document back off `verified` and say so* — HAS NO ROUTE THROUGH THE PLANE. REC-54 corrected the one inside C-18.9, the only check function it claimed; the other four sit in check functions it did not open, and are reported here rather than edited across a claim boundary. NEEDS: either the four repair strings corrected to name a reachable act, or a decision that Information gains a `verified -> collected` edge (which is DOCTRINE, not a mechanism call — see D-203). | M2 · open — four repair strings, RECORD ground, no doctrine needed to fix the TEXT |
| D-204 | defect | 2026-08-05 | **No op can move an Information document off `verified` except to `retired`, which is terminal and means something else.** MEASURED 2026-08-05 by REC-54 while dispositioning D-200. `op=reopen` takes an INQUIRY target only; `op=retire` moves `verified -> retired` and says in its own refusal *"There is no move back out of retired"*; `op=release` moves `collected -> verified` and is explicitly not repeatable. So when D-200 says a bundle whose chain cannot be reconstructed should be *"moved BACK OFF verified"*, the plane offers no act that does it, and the only available exit permanently withdraws the document — which asserts something quite different from *"we cannot show this route"*. REC-54 did not need the route (all ten of D-200's bundles reconstructed from recorded evidence) but the gap is real and the NEXT such bundle will need it. **This is a DOCTRINE question and is routed as one, not decided by a worker:** retracting a verification is a statement about a group's own past act, and whether the record may un-say it — versus carrying a standing `undetermined` marker at `verified` — is Bob's call. NEEDS: a decision between a `verified -> collected` retraction edge with a required reason, and an `undetermined`-route marker that leaves the state alone. | M2 · open — DOCTRINE (Bob), raised by REC-54 as a decision item |
| D-205 | defect | 2026-08-05 | **`BIO_ADMIN_TOKEN`'s value was printed into a worker session's transcript by a `node` stack trace, and that credential is the root of trust.** Self-reported by the REC-54 worker at hand-off — the right behaviour, and the reason this is on the record rather than lost: it did not repeat the value, and CONDUCT has not printed it either. The token was not COMMITTED, so `tokens.mjs`'s per-value publication denylist does NOT treat it as revoked — that defence only fires on a value published IN THE REPO, and this was an exposure in a transcript, which is precisely the route the denylist cannot see. `ADMIN_TOKEN` is the root-of-trust credential (DEC-43 is open about retiring its fallback), so the exposure is not narrow: it is admin over the live instance. **NEEDS: rotation, which is Bob's** — a credential act, and the machine path is the clipboard route `CLAUDE.md` defines, never a value typed into a conversation. Worth stating because it generalises: **a thrown error's message is a THIRD way a secret escapes, beside printing it and committing it, and nothing in the tooling watches that route.** | M7 · open — **rotation is Bob's**; the generalisable half (a caller that reads a credential must not let it reach an exception message) is RECORD ground and unassigned |
| D-206 | gap | 2026-08-05 | **`op=audit`'s TALLY is keyed by check id, so REC-54's new absent-vs-empty distinction reaches a reader only through the offender detail, which is bounded at 20.** Stated by REC-54 as residual rather than left to be discovered. The three findings C-18.9 now separates — *no chain recorded*, *a chain recorded and empty*, *a chain field that is not a chain* — are genuinely different facts about the record, and **a tally that collapses them re-creates in the REPORT the same conflation the check just removed from the DATA.** | M2 · **CLOSED 2026-08-05 by REC-56** — decided in favour of carrying it: an optional `code` on the finding (minted at the same call site as the finding it describes) and a derived `tallyDetail` beside `tally` on `op=audit`. `tally` does not move by one byte and `tallyDetail` is absent when nothing on the page carried a code. Reasoning at both sites (`f()` in `checks/bio-checks.mjs`, `auditPass()` in `src/store.mjs`); CLAUDE.md's *"saying which of those is true is a first-class obligation, not a diagnostic detail"* is what settled it. |
| D-207 | defect | 2026-08-05 | **Any `op=cite` over a query selection that swapped AT A CONSTANT COUNT, before REC-55 landed, wrote a Session Log entry that is SILENT about the drift — and silence there reads as an assertion.** The entry composes `Trigger: selection <handle> (the set had moved since it was made; …)` and states that clause whenever it applies, so its absence reads as *the set had not moved*. Found by REC-55's sweep and **it is the exposed half**: the refuse gate was latent, but `cite` is report-weight and UI-25 gave `kind:"query"` its first surface the same day. Fixed forward — the clause is now composed once and shared by both arms — but **the record may already hold entries written under the old behaviour**, and the Session Log is APPEND-ONLY. Whether any such entry exists is **mechanically checkable and was not checkable by the worker**: the fix is not in the serving 0.56.0 build and the plane deploy is gated. NEEDS: sweep the live record once the build is deployed. **If an entry IS found, whether an append-only record may be annotated is DOCTRINE and is Bob's** — do not decide it in a worker. | M8 · open — behind the same deploy D-200 waits on; **provisional: do nothing until the record is swept**, the cost of waiting is nil today and grows only while the query lease is live |
| D-208 | design | 2026-08-05 | **One question is now composed in two layers that cannot read each other.** `civicos-ui/app.html`'s `selectionMoved()` composes `moved || digestChanged` on the surface; REC-55's `Store.#answerChanged` composes the same question in the plane. They agree today and neither can see the other — the drift-by-hand-copy shape this project has measured five times, except that here BOTH copies are correct and the risk is only future divergence. **Deliberately NOT collapsed:** REC-55 declined to publish `#answerChanged` as a field speculatively, which was the right call — an unconsumed publication is exactly what REC-41 removed from `op=bootstrap` and what UI-35 is measuring on `op=publishedcase`. TRIGGER: the first surface offering a REFUSE-WEIGHT act over a query lease. At that point publishing the predicate as a field and having the surface read it is an additive I3 change that collapses the two. | M8 · DEFERRED on the trigger above — reviewed when a refuse-weight act reaches a query lease |
| D-209 | gap | 2026-08-05 | **The repair-reachability walk judges MOVE DIRECTIVES, and a repair that names an op WITHOUT directing a state move is checked only for EXISTENCE.** `test/repair-reachability.test.mjs` (REC-56) holds every repair string in the catalogue to five judgements, but A3 — *does the plane offer this act at this state* — only fires where the repair names an edge or a destination. A repair reading `<verb> through op=<x>` on a document whose state `op=<x>` does not apply to would pass. Closing it needs the ARM'S GUARD STATE extracted statically from the enclosing `current_state === '...'`, which is buildable (the pattern is uniform, inline in one function and at the single dispatch site in two others) and was not built here. **Two instances were read by hand in REC-56 and deliberately LEFT STANDING**, because both state where an artefact legitimately comes from rather than directing an act now — `publish through op=publish, which stamps the edition` and `divide through op=inquirydivide, which authors the block`, C-20.1's `re-produce the creation at collected` shape. Reading those as directives would be the instrument measuring the prose. | M2 · open — **provisional: the class is guarded for directives and stated for the rest**; cost of leaving it is one unchecked phrasing family, and closing it is one static-guard extractor plus the re-reading of every `op=` mention in the catalogue |
| D-210 | design | 2026-08-05 | **`op=release` is not repeatable, and nothing in the plane can re-make a release that was made wrongly.** Measured by REC-56 while correcting C-18.1: BOTH of that check's arms fire only on a bundle at or past `verified`, and both advised *a named member re-makes the release decision* / *a named member ratifies and records the collected -> verified transition* — while `release()` refuses anything already at `verified` (`ILLEGAL_TRANSITION`, "release is not repeatable"). So the FIRST repair in each array was as unreachable as the `return the bundle to collected` half D-203 was routed for, which the routing did not predict. The strings now name `op=retire` and raising it. **Whether a wrongly-made release can be re-made at all is the same question as DEC-56** — a re-make needs a route off `verified` — so this row is deliberately NOT decided here and does not pre-empt it. | M2 · **DOCTRINE (Bob)** — rides DEC-56; provisional is the corrected advice, which is honest under either ruling |
| D-211 | defect | 2026-08-05 | **DIST's gate step 8 says `op=audit` clean before anything is called done, and on a record past 200 documents a clean FIRST PAGE satisfies that wording.** MEASURED 2026-08-05 by CONDUCT at REC-57's integration, establishing the consequence rather than filing the finding: in `auditPass()` the answered `ok` is the op's TRANSPORT verdict (it ran) while `checked` is **the size of one page** and `clean`/`withErrors` are counts **over that page** — REC-57's own comment at the site says it plainly: *"`checked` is the size of one page and `ok` is a verdict over it, so 'the audit is clean' and 'the first 200 are clean' published the same shape."* REC-57 made the two DISTINGUISHABLE (a non-null `cursor` means more, `total` is published, and `limit` now states the applied cap), so a careful reader can tell — **the remaining gap is the GATE's wording, which does not say to check them.** **D-200's own reading is UNAFFECTED and this was checked rather than assumed:** the live instance holds 31 documents, well under the 200 cap, so `cursor` was null and 31 checked / 21 clean / 10 with errors is a complete reading of the whole record. This is LATENT today and becomes real the day an instance passes 200 documents — which a group capturing an agenda packet a week reaches inside a year. NEEDS: `kickoffs/DIST.md` step 8 to require `cursor: null` (or `checked === total`) alongside clean, so "the audit is clean" is a claim about the RECORD rather than about a page. **DIST's file, so this is a delegation and not a CONDUCT edit** — raised here because CONDUCT met it, and because the gate is what stands between a known-unclean record and a release. | M0 · open — **DIST's gate wording**; latent until an instance passes 200 documents, and the fix is one clause |
| D-212 | design | 2026-08-05 | **SUPERSEDED 2026-08-05 by the VERSIONS design — the question this row asked turned out not to be a record question at all.** The row went through three drafts and each was wrong in an instructive way, so the history is kept rather than deleted. **Draft 1** said claims must be recordable as agreeing or disagreeing along a range, the hazard being a single collapsed number. Bob refuted the premise with the case the row was built on — an award that skipped competitive bidding plus an emergency exemption: *"I see two pieces that when taken together tell the story of what happened and why - perhaps justifiably, perhaps not."* They do not conflict, and whether either is RELEVANT depends on what the inquiry asks. **Draft 2** replaced that with three stored objects — support, undercut, rebuttal — where an undercut kills the inference from a leg to a claim while the leg stays true. Bob refuted that too, and more fundamentally: *"There may be evidence, even a single sentence of evidence, that supports and undercuts and rebuts a claim - all in the same sentence."* Roles are not types; one sentence plays all three at once against different claims. **THE ANSWER, and it makes the system smaller:** *"the only way to make sense of how the various legs of a claim come together is for an AI to be involved, for it to understand the facts in context and adjust the legs so that they properly tell the story and assign strength values that when calculated are supported by the evidence."* The calculation stays as simple as it is; the intelligence goes into how the legs are formed and weighted, not into relationships the record computes over. What the record holds is the legs, their strengths, and a written account of why they tell this story — carried by a VERSION (`INVESTIGATIVE-SESSION.md` §5, §6). **Nothing is owed on this row.** The lesson worth carrying: two attempts to solve a judgement problem with schema, both by a session doing structure where the question was understanding. | SUPERSEDED by DEC-60's versions design — no work owed; kept for the two refuted drafts |
| D-213 | design | 2026-08-05 | **ANSWERED 2026-08-06 by Bob: discovered evidence not related to the current inquiry is CAPTURED and recorded as an ACTIONABLE NOTIFICATION the member can act on later.** The problem, raised by DEC-60: the investigative session reads ALL of a project's inquiries for context but writes only to the SUBJECT one, so evidence it turns up bearing on inquiry B — the same vendor holding three other contracts, met while investigating whether one was competitively bid — had nowhere to go and was dropped by construction. That is D-194's authored frontier (a member's lead has no home) with a PRODUCER generating them continuously rather than a member noticing one occasionally. **The answer needs no new machinery**: `NOTIFICATIONS.md` already catalogues ~30 generators with stable `N-<n>` ids, written precisely so that thirty sites do not each invent a wording and a severity, and the QUEUE construct already defines what a member LEARNS. This is a new kind in an existing catalogue. **And it composes correctly with the capture rule**: the document is CAPTURED — entry to the cache, not to a leg of any claim — while the OBSERVATION becomes the notification, so nothing enters a claim without a member. **Still open beside it: D-194/D-184**, the same gap for a member's own leads, which this does not close and should now be easier to close by the same route. | M9 · answered 2026-08-06 (DEC-60, Bob) — **close condition CORRECTED 2026-08-07: the code does not use N-ids.** Verified: no N-number exists anywhere in source; the live vocabulary is SLUGS in `queuestate.mjs` (`QUEUE_FINDING_KINDS` :118-134), `classOfKind` is the fence, and the store refuses unknown kinds at the mint. The build is: one FINDING-class slug (FINDING, not CONDITION — a CONDITION is personally mutable, and one member could silently mute a real lead), a producer, a real `basis`, and `options[]` from the producer — **whose natural options are inquiry-grain acts that do not yet exist, the D-222 grain problem one surface over.** `case` derives from inquiry B's ancestors, not A's. Closes when the slug is registered and the session emits it |
| D-214 | design | 2026-08-05 | **ANSWERED THE DAY IT WAS RAISED — Bob, 2026-08-05: `rejected` is BOTH an act AND a state of a suggested leg, and BOTH `considering` and `rejected` are REVERSIBLE.** Raised by DEC-60 and closed in the same session; kept as a row because IS-1 builds to it and the reasoning must reach whoever writes the state machine. What this settles, in the terms the builder needs: (1) the leg REMAINS A SUGGESTION throughout — `rejected` is a state the suggested leg is in, not a separate object and not a deletion, so a rejected leg is still readable, still shows its basis, and still counts toward nothing; (2) the REJECTION ACT is recorded in its own right, which is what makes a PATTERN over rejections queryable — and that pattern is where DEC-60's third argument lives, since a member who rejects every suggestion running against their thesis is visible only if the acts persist; (3) **REVERSIBILITY answers the question that made this hard** — a rejected leg that new evidence would revive can simply be un-rejected, so nothing needs a terminal state and nothing is lost by rejecting early. Note for IS-1: reversibility means the four states are NOT a one-way ladder, so the transition table must be authored rather than assumed, and each reversal is itself a member act that is recorded (an un-rejection is evidence about the member's attention exactly as the rejection was). | M9 · answered 2026-08-05 (DEC-60, Bob) — IS-1 UNBLOCKED on this axis; the row closes when IS-1 lands with the transition table and its negative control |
| D-215 | design | 2026-08-05 | **Two mechanisms proposed inside DEC-60's design and NOT ruled.** *(Rewritten 2026-08-05 in plain terms at Bob's instruction — the first draft called (1) a "congeniality self-report", a phrase invented in the session that wrote it and decodable by nobody else. Term dropped; both are in `INVESTIGATIVE-SESSION.md` §7 and §8, marked proposed.)* **(1) ANSWERED 2026-08-05 — Bob: the AI SKILL's requirements include MINIMISING these effects.** Constrain the skill at the source rather than bolting a report onto the output; DEC-46's lens-may-be-preserved-not-applied is the standard the skill is written against. Recorded as a skill requirement in `INVESTIGATIVE-SESSION.md` §11, not as a mechanism. The original proposal, for the record: The session is given the net bias (project + instance combined). It can use that two ways. Silently — letting the declared position shape what it proposes, which is the lens being APPLIED and is what DEC-46 forbade for import, where a travelling manifest may PRESERVE a lens and may not apply it. Or out loud — attaching to a suggestion: *this agrees with the position the project has declared; here is what I went looking for that would have cut against it, and here is what I found.* The second turns the bias from something that could quietly steer the run into something a member can CHECK, and it is a thing a member reviewing their own work cannot do for themselves: nobody can reliably notice when they are agreeing with themselves. **(2) A SUGGESTION CARRIES THE CONDITIONS IT WAS FORMED UNDER.** Bob, 2026-08-05: *"everything can change at the drop of a hat (bias, standard, claims)"*. So a suggestion is only interpretable against the bias, the evidence standard, and the claim set that were in force when it was written. Carried with it, a member opening a month-old suggestion can see it was formed under a different bar, or before three claims were accepted, or under a position the project has since revised. Not carried, a stale suggestion looks exactly like a current one. This is the shape DEC-54 already uses for a pinned policy — source, date and hash recorded, with cases naming the version. **(2) ANSWERED 2026-08-06 — Bob: a claim version does not need to know the version previously in place, nor the bias, nor the evidence standard.** The reason is internal to the versions design and the session's own argument for recording them was wrong: **a version is a COMPLETE ACCOUNT, not a patch.** Were versions diffs, what preceded them would matter; they are not, so it does not. The same logic disposes of the other two — a version is evaluated against the CURRENT standard, so which bar it was written under tells a reader nothing they would act on; and the bias ought not to have shaped it at all, since minimising that is a requirement ON THE SKILL (see (1)), so recording it would be recording something that should not have mattered. The session's argument was that a stale version reads as a current one: real, but the remedy was wrong and far heavier than needed, since a version carries a date and names its run anyway. **What IS kept, for a different reason: the run's OBSERVATION LOG** — where it searched and where it stopped — which is not about which rules applied but about what evidence was reachable at the time, and that is what a later reader needs to judge whether the account was well-founded. | M4 · closed 2026-08-06 (DEC-60, Bob) — both halves answered; the observation log survives as part of the run object, not as version metadata. Correction 2026-08-07 (`IS-SWEEP-2026-08-07.md` §1.2): the BIAS half of (2) was ALREADY RULED before this row was raised — `Content_Framework:1129-1141`: an assistant-surfaced focus must carry the bias manifest in force when it was surfaced, since unlike a member it will not remember — so the closure stands but its reasoning line must cite the standing ruling rather than dispose of the question fresh |
| D-216 | design | 2026-08-05 | **CHECKED AND ANSWERED 2026-08-08 (W0's slot-free model-check lane). THE ANSWER IS PER-PROJECT: `INVESTIGATIVE-SESSION.md` §7 IS CORRECT AND CLONING IS NOT THE HONEST ANSWER.** The question was whether an inquiry shared across projects has ONE stance every referencing project must share, or a per-project one — §7 ruled per-project, Bob's first instinct was to CLONE the inquiry on divergence, and cloning was rejected because it duplicates the whole evidence trail and the copies immediately drift, so the shared investigation stops being shared. **DRIVEN, NOT READ — the instrument is `bio-plane/test/d216-sharing.probe.mjs` (38/38) with `d216-sharing.control.mjs` (3 arms as declared); the full record is in `MEASUREMENTS.md`, 2026-08-08.** Two projects stood on two different readings of one shared inquiry simultaneously, the plane refused neither, and after divergence each still saw every version and every leg of the other's; the sharing edge is a `cites` row in `refs` (`op=backlinks` reports it, `op=stats.refs` grew by exactly 2, and `op=strengthbarof` composes strictest-per-axis across BOTH citing projects — an answer no single-project walk can produce); the inquiry's own bytes carry NO stance and a read naming no project gets no `current` field at all. **NOTHING IN THE MODEL REQUIRES ONE SHARED STANCE** — the edge is load-bearing (severing it refuses the stance `VERSION_CURRENT_UNRELATED`), the mechanism is not two-only, and the one cross-project composition in the model is the BAR and not the stance. **TWO CORRECTIONS THE CHECK FORCED. (i) §7's and this row's own cited verification was WRONG: `op=linkproject` is link + project-as-a-VERB — it takes a `capture=<sha256>` and projects that capture's resolved links into `links_to` edges — and it neither takes a project bundle nor writes a `cites` edge. The conclusion was right; the evidence was a name collision. (ii) THE FINDING THAT OUTRANKS THE ANSWER: `op=cite` REFUSES an inquiry on the PROJECT arm (`NOT_INFORMATION`), so the edge the whole IS build hangs on has NO CURATED PRODUCER — a project draws on a question only by authoring its `bundle.md` and calling `op=promote`. PL-2's fixture hand-authored exactly that, which is why its suite drove the gate without discovering the gate has no door.** | M9 · measured 2026-08-08 — **IS-3 UNBLOCKED**; PL-13 survives with PL-2's pointer confirmed, its remaining scope the two notification slugs (asserted ABSENT) plus the new `op=cite`/`op=sever` project-arm gap |
| D-217 | design | 2026-08-05 | **BOTH HALVES ANSWERED 2026-08-06 by Bob.** Raised by DEC-60's versions design. **(a) REVIEW BURDEN — versions form a DERIVATION TREE, and accepting one prunes the branch.** The problem: a background run adds a version whenever its output differs from every existing one, so a claim under frequent investigation accumulates versions faster than anyone reads them — each correctly labelled a suggestion, the whole unexamined. Bob: *"make it easy to delete versions, or allow versions to be a tree that expresses how one derives from another, so a user can delete all previous derivations when they accept one as the one they want."* **The tree is already half-built into the design** — `INVESTIGATIVE-SESSION.md` §6.3 has editing a version DERIVE a new one from it — so this makes the derivation edge EXPLICIT and adds the prune. Consequences for the builder: a version records what it was derived from (null for one a run composed fresh); accepting a version offers deletion of the ancestors it descends from, as an offer and not an automatic act, since a member may want a rejected reading kept as the record of what was considered; and deletion is a MEMBER act — the AI cannot delete a version any more than it can accept one. **(b) A REWORDED CLAIM IS USER SELECTABLE** — the member chooses whether rewording a claim produces a new VERSION of it or a NEW CLAIM. **So this is not a schema decision, it is a runtime one, and the schema must support both.** The reason both are legitimate: a tightened wording that leaves the legs meaning what they meant is a version; a reworded claim that changes what the legs are answering is a different claim wearing a similar sentence, and forcing either into the other loses something. It interacts with the question-sharpening suggestion (§8), which can reword an INQUIRY's question and thereby move every claim beneath it — that path should offer the same choice rather than deciding for the member. | M9 · answered 2026-08-06 (DEC-60, Bob) — closes when the derivation edge, the prune offer (HIDE, never delete — `IS-SWEEP-2026-08-07.md` §4b item 6) and the version-or-new-claim choice land with IS-1 |
| D-218 | design | 2026-08-05 | **UNMEASURED: whether a fleet-member Worker can hold a WHOLE investigative run inside the paid CPU ceiling.** Raised by DEC-60's integration architecture (`INVESTIGATIVE-SESSION.md` §14a), where Bob's Claude-account cascade (member, else project, else instance) settles that the agent loop runs in the GROUP'S OWN infrastructure — which preserves the installer's sovereignty promise and points at the fleet-member shape `pdf-worker` already established (I6). **THE REASONING THAT MUST NOT BE TRUSTED UNTIL MEASURED:** an agent loop spends most of its time WAITING on API responses, and Workers bill CPU rather than wall time, so a run of many minutes may sit well inside the ceiling. That is an argument, not a measurement, and this project has a standing rule against exactly this move — three claims the archive design rested on were wrong when finally measured, every one having sat in the design document for weeks. **WHAT IT DECIDES:** whether a fleet member can hold an entire run, or whether a run must be structured as many short invocations with its state carried between them, which is a materially different build. **RELATED MEASURED FACT:** Workers Paid was confirmed on this account 2026-08-04 (`MEASUREMENTS.md`), moving the CPU line from 10 ms to 30 s — so the question is not whether there is headroom but whether a whole run fits in it. NEEDS: a probe running a representative agent loop in a Worker and reporting CPU consumed against wall time, recorded in MEASUREMENTS.md before IS work that assumes either shape. | M9 · **MEASURED 2026-08-08 (FL-1) — the CPU question is ANSWERED and it is not the binding one.** `MEASUREMENTS.md`, FL-1 entry; instrument the Cloudflare GraphQL Analytics API (`workersInvocationsAdaptive.sum.cpuTimeUs`), read off a DEPLOYED probe, never the Workers own clock. **The reasoning this row said not to trust is CONFIRMED: 50.0 s of wall time waiting on subrequests cost 4.29 ms of billed CPU (~0.16 ms per awaited subrequest, independent of wait length).** A 25-turn run-shaped loop over REAL plane payloads costs 24.29 ms; 100 turns 189.28 ms; 400 turns 4,488.9 ms — CPU grows as ~n^1.9 (the transcript re-serialised every turn), putting ~1,100 turns inside the 30 s default and ~3,700 inside the 5-minute maximum (extrapolated from measured points, labelled as such). **BUT MEMORY BINDS FIRST BY AN ORDER OF MAGNITUDE: memoryUsageBytesP99 reaches 120.4 MB of the 128 MB isolate at 200 turns while CPU is still 2.5% of the ceiling.** So a fleet member CAN hold a long run as far as CPU is concerned, and CANNOT hold an unbounded one because of memory — which lands where §14a already put it: **the resumable shape is the one to build, and the number sizes the segment rather than deciding the shape.** OPEN only for FL-3 to consume; nothing further to measure here |
| D-219 | defect | 2026-08-06 | **A stored, member-facing method string says "with no captured basis" and reads as "with no basis", which is FALSE — the basis is REQUIRED.** Found 2026-08-06 while answering Bob's question *"What is testimony with no basis? Where did it come from then? Isn't there always context for how something got into the system?"* — and his instinct was right. `testifyResolution` (`store.mjs:8398`) REFUSES without one: `NO_BASIS` — *"grade D is recorded testimony: it carries the member's stated basis, with an author and a date"*. So a grade-D testimony always carries the member's stated reason, an author and a date. What it lacks is a CAPTURED DOCUMENT supporting the link, which is why it grades weakest. **But the method string written into the record reads** `testimony -- asserted by <who> with no captured basis (framework 8.1 grade D)` **, and a reader meeting "no captured basis" hears "no basis at all" — the record describing its own contents more weakly than they are.** This is the inverse of the usual defect class here (a comment claiming a stronger property than the code enforces); it is a stored string claiming a WEAKER one, and it is worse than a comment because it is member-facing and persisted on every grade-D resolution. NEEDS: the string to say what is actually missing — no captured document — while keeping the grade and the framework reference. Also worth a pass for siblings: any other place where "no basis" is used to mean "no captured basis". | M8 · open — one string plus its assertions; it rides whatever next touches the resolution path |
| D-220 | design | 2026-08-06 | **VERSIONS OF ONE DOCUMENT ARE ALREADY RECORDED AND NOTHING EXPOSES THEM, so the code that needs the version chain reconstructs it by full-text search and gets it wrong.** RULED by Bob 2026-08-06: *"Versions of the same document should be linked so that the system recognizes that reality. Each version should also be indexed by the same url (or id that refers back to the url.) These capabilities should be used in code that would benefit to know about this versioning reality."* **THE INDEX HE IS DESCRIBING ALREADY EXISTS** — verified in `schema.mjs` 2026-08-06: `captured_locators` is keyed `(address_norm, capture_sha, via)` with an index on `(address_norm, first_retrieved)`, carrying `first_retrieved`, `last_retrieved` and `observations`; `register` maps `capture_sha` to `bundle_id` and is indexed both ways. **So "every version at this address, in date order, with its bundle" is ONE INDEXED JOIN over two existing tables.** The versioning reality is recorded; no op answers it. **THEREFORE: DO NOT ADD AN EDGE BETWEEN VERSIONS.** An explicit `supersedes` relation would be a SECOND COPY of a fact the record already holds, which is the mirror-and-drift class this project refuses (D-164's solve-it-once; D-138's guard that guarded nothing). The link Bob asks for is the join, EXPOSED. **WHAT THE UNCHANGED CASE ALREADY DOES RIGHT, so it is not re-litigated:** a re-capture of identical content creates no bundle and no second register entry — it advances `last_retrieved` and increments `observations` on the existing row (Bob's own ruling, 2026-07-30, `LINK-FIDELITY.md`). What has no structural form is the CHANGED case, where a new bundle is created and its only tie to its predecessor is a SENTENCE OF PROSE in the body naming the earlier bundle id. **CODE THAT WOULD BENEFIT (Bob's third clause), in the order the benefit is largest:** (1) `heldMatch` in `civicos-ui/app.html`, which today searches `locator:"<url>"` and is the site of D-221; the join replaces the search and removes that defect by construction rather than patching it. (2) The **investigative session** (DEC-60): a run that reads sixty versions of one calendar as sixty documents has a distorted picture of what the record holds, which is the false-coverage hazard `STORE-AS-CACHE.md` names, arriving at the document level. (3) **Search result rendering** — sixty bundles at one address are sixty versions of one document, and a result list presenting them as sixty documents misrepresents the record. (4) **Monitoring** and (5) **`op=audit`'s cross-bundle duplicate sweep**, both of which D-60 records as comparing raw hashes; that line is partly stale (see D-60's correction) and the join gives either one its candidate set directly. (6) **A reader of a citation** — a case cites a specific bundle and that citation must never drift, but the LIVE record can show that later versions exist (a published case cannot be affected, and must not be). **RESEARCHED IN DEPTH 2026-08-06 (sub-session), and the fix is SMALLER than this row assumed — both halves already exist in code.** (i) **The read method exists and was deliberately left unwired:** `Store#capturedLocators({ addressNorm })` (`store.mjs:14779`, REC-26), whose own docstring says *"Deliberately not wired to a control-plane op."* **Its only caller anywhere in the repo is `test/monitor-cadence.test.mjs:415`** — in production nothing reads it. (ii) **The join already exists:** `#conditionBundlesForHost` (`store.mjs:9991`) already runs `captured_locators JOIN register ON r.capture_sha = cl.capture_sha`, scoped by GLOB to a host prefix and used to pick queue-condition subjects. So D-220 is *generalise an existing join and wire an existing method to an op* — not new machinery. (iii) **One write site, and it is unconditional:** `recordCapturedLocator` (`store.mjs:14747`), called only from `op=acquire` (`index.mjs:2938`) for every capture. New bytes at a known address INSERT a row; the same bytes seen again bump `observations` and widen the `MIN`/`MAX` interval — *"Seeing the same bytes again later is not a duplicate, it is the observation that proves the target held still in between."* `via` is in the key deliberately (D-96): an archive sighting of the same bytes is a different fact. Address normalisation is `normalizeAddress` (`subresources.mjs:520`) and is deliberately conservative — it does NOT strip trailing slashes or case-fold the path, because *"a normalisation MISS looks exactly like 'not captured'."* **CORRECTION TO THIS ROW'S OWN CONSUMER LIST:** consumer (5) named `op=audit`'s *cross-bundle duplicate sweep*. **No such sweep exists.** `auditPass` iterates bundles and calls `checkBundle` per bundle; `checkRegisterIntegrity` (`bio-checks.mjs:3536`) buckets within one bundle's `provenance.json` only. The phrase was inherited from D-60 and is a category error — there is nothing cross-bundle to move onto the join, which makes the join the thing that would CREATE that capability rather than improve it. **A CONSUMER THE ROW MISSED, and it is the largest one:** **MONITORING SELECTS PER BUNDLE, NEVER PER ADDRESS** (`store.mjs:15983` `#monitorCadencePlan`, `:16023` `#monitorCadenceTick`, keyed on `bundles.monitor_enabled/monitor_frequency/monitor_last_checked`). So sixty versions of one calendar are **sixty independent monitor subjects** — the same address fetched sixty times on sixty schedules. Monitoring an ADDRESS is what a member means; monitoring a bundle is what the code does. | M3 · open (Bob 2026-08-06) — design settled, no schema change, and the implementation is smaller than assumed: **generalise `#conditionBundlesForHost`'s join, wire `capturedLocators` to an op**, then consumers — (1) `heldMatch` (closes D-221), (2) monitoring per address, (3) the investigative session, (4) search rendering |
| D-221 | defect | 2026-08-06 | **The "changed from" sentence written into a new capture names the WRONG predecessor — almost certainly the OLDEST version at that address rather than the previous one.** Traced in source 2026-08-06. `heldMatch` (`civicos-ui/app.html:13614`) finds prior captures at an address with the query `locator:"<url>"`. **That is not an address comparison:** `locator` is a full-text-indexed field (`query.mjs:61`, `FTS_COLUMNS`), and a fielded query on a field carrying `fts` compiles to a TEXT ATOM (`query.mjs:499`, `if (f.fts) return textAtom(...)`), not to column equality. A text atom creates a rank arm, so the default order is RELEVANCE (`query.mjs:732-733`, `ctx.sort?.field || (rank ? "relevance" : "updated")`). Every capture at one address carries identical URL text, so the bm25 scores tie and the declared tiebreak — `bundle_id ASC` — decides the order. `heldMatch` then returns on the FIRST comparable row it walks. **So on a calendar captured weekly for a year, the record can state "the source has since published a changed document" against a snapshot twelve months old, and present it to the member as the immediately preceding version.** The sentence is written into the bundle body at `app.html:13485` and is permanent in that bundle. SECOND, SMALLER RISK, mechanism-only and NOT measured: because the match is a text search rather than an equality, two different URLs that tokenise alike — same host and path, different query string — can match each other. FIX: D-220's join, which answers by address and orders by date; ordering the existing search explicitly would work and would leave the fragile lookup in place. **UI's paths, so this is a DELEGATION and not an edit from BOB** — recorded here because BOB traced it. | M3 · open — **delegation to UI**; closed by D-220's consumer (1) rather than separately |
| D-222 | design | 2026-08-06 | **THE MEANING LAYER HAS NO QUERY SURFACE, and the investigative session is the first consumer that genuinely needs one — so this is a PRECONDITION of DEC-60, not an enhancement.** Raised 2026-08-06 by the rigorous architecture pass Bob asked for. **THE MEASUREMENT, from `STORE-AS-CACHE.md` and re-verified:** BIO has two retrieval routes. **Route 1**, the query compiler (`query.mjs`), projects **34 filterable fields and exactly 5 FTS columns** (`title`, `body`, `meta`, `locator`, `authority`). **Route 2**, the meaning tables — `readings`, `reading_refs`, `resolutions`, `connections`, `inquiry_basis` — is **not reachable by the query compiler at all.** **WHY THE HOLE IS INVISIBLE, which is worse than the hole:** route 1 carries SCALAR SUMMARIES of the meaning layer onto the bundle row (`capture` → `inquiry_capture_strength`, `legs` → `inquiry_basis_count`, `connection` → `inquiry_connection_strength`, `resolution` → `action_resolution`), so a caller can filter by a finding's STRENGTH and never reach the legs that produced it. `STORE-AS-CACHE.md`'s own words: *the projection creates a false sense of coverage — the meaning layer is visible as a number and unreachable as a structure.* **WHY IT BLOCKS THE SESSION:** forming a version of a claim's legs (DEC-60) is meaning-layer work end to end. As `INVESTIGATIVE-SESSION.md` §3 is written, the session's read scope is NOT ACHIEVABLE — it can see that a claim scores 0.7 and cannot see what it rests on, which is precisely the work it exists to do. **RELATED AND SAME-SHAPED: D-220**, where sixty versions of one document read as sixty documents is the identical false-coverage failure one axis over. **RESEARCHED IN DEPTH 2026-08-06 (sub-session), and the gap is wider than this row stated.** **(i) THIRTEEN TABLES, and about TWENTY-FOUR ops read them — NONE of which is a query surface.** Every one is a fixed-shape lookup keyed on exactly one of `capture_sha`, `ref`, `entity_id`, `relation_id`, `bundle_id`, `target_id` or `(progression_key, entity_id)`, or an unkeyed whole-table walk with a fixed output shape. **No predicate, no projection choice, no sort, no paging** (the one exception is `documentsNamingEntity`'s clamped limit). So the layer is not under-queried; it is UNQUERIED BY CONSTRUCTION. **(ii) GRADE IS A FILTER NOWHERE.** No op filters on grade at all, and **no grade column is indexed** — there is no `resolutions(grade)` or `connections(grade)`. The consequence with teeth: the C tier is explicitly *"FLAGGED for a member to confirm"* (`schema.mjs:876`) and **nothing can enumerate the flagged set** — `connectionsFor` refuses without a key (`store.mjs:8618`). A queue of work the system creates and cannot list. **(iii) NO CROSS-AXIS JOIN.** *"Every claim whose legs cite documents from authority X"* is two indexed columns and no join. **(iv) TIME IS A PREDICATE NOWHERE** — `at` is on nearly every table and filterable on none, so *"what resolved this week"* is unaskable. **(v) `readings.reading` IS AN OPAQUE JSON BLOB**, so nothing inside a reading is reachable except what `reading_refs`/`reading_ref_terms` already project out. **(vi) SEE D-223** — the sharpest single instance: hunch debt is publication-disqualifying and cannot be listed. NEEDS: a meaning-layer read surface. **Whether it extends the query compiler or is a second addressable surface is NOT a free choice — `query.mjs:701-705` states that a second query path is what the compiler's design exists to prevent, and D-15 gives visibility exactly ONE compilation point, enforced by a throw rather than a convention.** Options are being graded; D-223's question is acceptance on whichever wins. | M3 · open (DEC-60) — **BLOCKS the investigative session's read scope**; nothing else in the IS decomposition depends on it. Appended 2026-08-07: the options were GRADED 2026-08-06 in `INVESTIGATIVE-SESSION.md` §14c — recommendation D, staged as A then C; do not re-grade |
| D-223 | defect | 2026-08-06 | **HUNCH DEBT CANNOT BE ENUMERATED, and the schema itself calls it the one declared bias that DISQUALIFIES PUBLICATION.** Found 2026-08-06 by the meaning-layer research sub-session. `inquiry_basis` (`schema.mjs:1221`) carries `grade_source`, and `schema.mjs:1187` names a `hunch` leg as publication-disqualifying. **No op can answer *"every leg whose `grade_source` is hunch"*, corpus-wide or per project.** It is checked only PER BUNDLE at the two gates (`checkInquiryBasis`), which means the rule is enforced one document at a time and **the outstanding debt across a project is unknowable to the group that carries it**. The same is true of `role`, `ground` and `grade_axis` — so *"every inquiry with a `cuts_against` leg"* and *"every multi-ground basis"* (REC-42's OR branches) are equally unaskable. This is a case of the record enforcing a rule it cannot report on, and it bears directly on doctrine: a group cannot see its own exposure before it publishes, only be refused at the moment it tries. Bob's own vocabulary correction (D-188, *say HUNCH DEBT*) presumes the debt is visible as a quantity; it is not. FIX: falls out of D-222 — this is one predicate over one already-present column, unavailable only because the meaning layer has no query surface. | M3 · open — rides D-222; **name it in D-222's acceptance so the surface is not built without the one question that most obviously needed it** |
| D-224 | design | 2026-08-06 | **`connections` grows QUADRATICALLY in the documents concerning one entity, and nothing caps, comments on, or measures it.** Found 2026-08-06 by the meaning-layer research sub-session. `deriveConnections` (`store.mjs:8548`) collapses `resolutions` to distinct captures per entity and then emits **every unordered pair** — a nested loop at `store.mjs:8566-8568`, so **k documents concerning one entity produce k(k−1)/2 rows**. At 100 documents that is 4,950 rows for one entity; at 1,000 it is 499,500. **There is no cap, no comment acknowledging the growth, and no measurement anywhere in the repo.** Three things make it worse rather than academic: (i) a heavily-covered subject — the entity a group's whole project is about — is exactly where k is largest, so the cost lands on the most important entity rather than a marginal one; (ii) the derivation is idempotent and alarm-driven (`connection-derive`, batch 100 entities per tick), so the write amplification arrives as sustained background work on a Durable Object whose storage ceiling is itself unrecorded debt (D-190); (iii) `stats()` counts `connections` but NOT `readings`, `reading_refs`, `reading_ref_terms` or `inquiry_basis`, so an operator watching for this has one number and no denominator. NEEDS: a measurement first — derive against a synthetic entity at k = 10/100/1000 and record rows, bytes and tick duration in `MEASUREMENTS.md` — then a decision about whether pairs are materialised at all above some k, or derived on read like progression instance grades already are (`#assembleInstance`, which stores nothing). **Do not cap it before measuring; the point of the row is that nobody knows the curve.** | M3 · open — measurement first, and it is cheap; the design question only exists if the numbers say so |
| D-225 | defect | 2026-08-07 | **The meaning-layer reads `concerns`, `resolutions` and `connections` are UNCAPPED.** `store.mjs:8451` (`concerns`, the reverse index), `store.mjs:8438` (`resolutionsForCapture`) and `store.mjs:8605` (`connectionsFor`) all return UNBOUNDED arrays — no limit, no paging, no truncation marker — and that is exactly why they never appeared on REC-57's bounded-ops roster: the roster enumerated ops WITH envelopes, and an op with no envelope at all was invisible to the instrument that would have flagged it. The growth has teeth: `connections` for one entity grows with D-224's k(k−1)/2 curve, so the most important entity produces the largest unbounded response. NEEDS: the three reads join REC-57's envelope discipline BEFORE any new meaning-layer surface (D-222's) lands beside them — an uncapped legacy read next to a capped new surface is the inconsistency a caller will build against. Relation: E4 in `FINDINGS-WORKPLAN.md`. Found by the 2026-08-07 consistency sweep (`IS-SWEEP-2026-08-07.md` §5a, §6). | M3 · open |
| D-226 | design | 2026-08-07 | **VOCABULARY DRIFT across the IS design and the register — D-156's class.** Measured by the decisions audit (`IS-SWEEP-2026-08-07.md` §6): **"finding" now has THREE senses** — the record's unit of truth (DEC-44), a derived progression finding (DEC-9/10), and a sub-session SEARCH RESULT (`INVESTIGATIVE-SESSION.md` §14b.1) — and the worst of the three sits in IS-9's acceptance clause, where a search hit is named after the record's unit of truth. **"version" has SIX senses**, three of them inside the IS doc itself. **"leg" in the IS design is materially THINNER than the register's leg** — no axis, no relationship, no grade_source, no extent — so a builder implementing the design's leg under-builds the register's. **"ground" is simultaneously a DEC-32-banned surface word AND a real schema column** on `inquiry_basis`. Four words that mean different things in the two documents a builder must read together. | M9 · open — resolve the vocabulary BEFORE IS-1 is scheduled **[CORRECTED 2026-08-08 by CONDUCT at PL-1's integration, on PL-1's delegation: THIS ROW IS STALE.** `INVESTIGATIVE-SESSION.md` §0 ENACTS its resolution and says so in terms — *fixed here, binding on every section below* — and **PL-1 built against §0, not against this row.** A debt row reading `open` over a resolution the design already carries is the mirror of the failure this project keeps meeting from the other side: a reader trusts the row, re-opens a settled question, and spends an item on it. Left in place with this note rather than deleted, because the row is what a future sweep will find; whoever next touches D-226 closes or re-scopes it against §0.]** |
| D-227 | defect | 2026-08-07 | **REC-60's suites pin the PUBLISHED ENVELOPE and not the SQL BOUND, so a regression that removes `LIMIT ?` while leaving the slice and the envelope intact passes both of them GREEN.** MEASURED 2026-08-07 by CONDUCT at REC-60's integration, by arming its negative control a DIFFERENT way than the worker did: removing `LIMIT ?, cap + 1` from `resolutionsForCapture`'s query while leaving `const truncated = rows.length > cap`, the `slice(0, cap)` and the published `limit`/`truncated` **left `bounds.test.mjs` at 99/0 and `meaning-bounds.test.mjs` at 51/0 — fully green.** The published answer stays honest and correctly truncated; **the SCAN becomes unbounded again**, which is D-225's actual memory concern rather than its honesty concern — and on `connectionsFor`, whose response grows on D-224's k(k−1)/2 curve, an unbounded scan is precisely the shape that bites. **This is NOT a defect in REC-60's fix, which is correct and shipped**: the fix bounds the SQL. It is a gap in what the suites would CATCH, and it exists because REC-60's walk reads what a method PUBLISHES by design — the walk cannot see this and says so at the site. **It is the same lesson as REC-60's own NC(2) finding from the other direction:** honesty and boundedness are different defects, and an instrument that measures one does not measure the other. NEEDS: an assertion that the row source itself is bounded — driving a store with more rows than the ceiling and asserting the query cannot return them, rather than asserting the answer was sliced. Relation: D-225, D-224, and REC-66's unbounded derivation, which is this defect one step earlier. | M3 · open — rides REC-66, which already owns the derivation-vs-response distinction |
| D-228 | defect | 2026-08-07 | **A QUOTED FIELD VALUE KEEPS ITS QUOTES, so `state:"open"` matches nothing and `title:"two words"` compiles to a mangled FTS5 expression.** Found 2026-08-07 by PL-8 while writing the meaning arm’s over-strictness control — an equally correct alternative phrasing that a member would type, and it did not work. The tokenizer has a branch for exactly this (`query.mjs`, `if (rest === "" && src[i] === a double quote)`, with a comment saying *`field:"two words"` splits at the quote, so back up and read the value properly rather than truncating it at the space*) and **that branch is UNREACHABLE**: the bare reader stops only at whitespace and parentheses, so it has already consumed the opening quote by the time `rest` is tested, and `rest` is never the empty string. MEASURED, through `compile()`: `state:"open"` binds the argument `"open"` WITH the quote characters, so an indexed equality against a corpus that stores `open` returns nothing and says nothing; `title:"two words"` binds the FTS5 expression `({title} : """two" AND "words""")`. **This is PRE-EXISTING and language-wide** — it belongs to every one of the 34 projected fields, not to the meaning arms, which merely inherit it. It is not fixed inside PL-8 because the fix changes what every selector matches (a query that returned nothing may start returning rows) and that is a change every retrieval suite must be re-run against, with its own control. **PINNED rather than left**: `meaningquery.test.mjs` asserts the CURRENT behaviour on both an arm and an ordinary field, so the fix flips a visible assertion instead of landing unnoticed. NEEDS: make the branch reachable (stop the bare reader at a quote, or split on the colon before reading the value), correct the two pins, and re-run the retrieval suites. | M3 · open — small, but it must not ride another item’s battery |
| D-229 | defect | 2026-08-08 | **ELEVEN OF THE TWELVE `MACHINE_CANNOT_*` FENCES ARE NOT DOING THE WORK — an unrelated payload guard is, and that is luck rather than a fence.** MEASURED 2026-08-08 by PL-11, running **the second half of DEC-55.5 (owed control 1) for the first time in this project** — the half that asks not *do the refusals fire* but *does removing the predicate make them all pass*. Neutering `isMachineStamp` disarms all twelve with one edit, and PL-11 recorded **what each act answered INSTEAD**, because "they all pass" is the generous reading: `RELEASE→NO_ACKNOWLEDGMENT`, `CONCLUDE→NO_CONCLUSION`, `REOPEN→NO_REASON`, `PUBLISH→NO_SUBJECT_POSITION`, `MOVE_ACTION→NO_REASON`, `CORRESPOND→NO_TARGET`, `DIVIDE→NO_REASON`, `GROUND→NO_PARTITION`, `MOVE_VERSION→VERSION_ACT_NO_SUCH_VERSION`, `FORWARD→NO_SUCH_TASK`, `RESOLVE→NO_SUCH_TASK`. **Not one is refused AS A MACHINE.** Each falls through to the ordinary payload complaint sitting behind the fence — so the machine identity is doing no work at eleven sites, and a caller who supplies a complete payload meets nothing. **AND THE TWELFTH IS THE PROOF: `MACHINE_CANNOT_DECLARE` FULLY SUCCEEDED — a machine SET THE GROUP'S REQUIRED EVIDENTIARY STRENGTH**, because its payload happened to be complete. The other eleven are one well-formed request away from the same outcome. **This is not a defect PL-11 introduced and not one REC-46 caused** — REC-46's single predicate is what makes the fence *breakable in one edit*, which is what let it be measured at all; before it, the same test would have needed eleven edits and would probably never have been run. **What is wrong is that the fences were believed on the strength of their first half.** NEEDS: each of the twelve asserted to refuse a machine **with a COMPLETE payload**, so the fence is what refuses rather than the complaint behind it — the same shape as an over-strictness arm, one layer down. Relation: DEC-55.5, D-199.5, REC-46. | M4 · **CLOSED 2026-08-08 by REC-73** — `bio-plane/test/machine-fences.test.mjs`, 45 assertions: all twelve driven under payloads that would otherwise SUCCEED, each refused BY NAME, each followed by the SAME payload accepted from a signed-in member (which is what makes it complete, measured rather than asserted). **NOTHING IN THE PLANE WAS CHANGED and REC-46's predicate was not touched** — the defect was in the CONTROLS, and it is the controls that moved. **AND THE MEASUREMENT IS LARGER THAN THIS ROW PREDICTED: under complete payloads the same one-line neutering lets TEN of the twelve acts GO ALL THE WAY THROUGH**, not one — a machine released a document to `verified`, published a case at edition 1, divided a question, accepted a reading, and set the group's required evidentiary strength with `author: token:ai` in the row. **The two that did not are `taskforward`/`taskresolve`, caught by REC-4's `NOT_YOURS`** — the only pair in the family with a second independent fence behind the first, invisible until a payload good enough to reach it was used. Sweep recorded in MEASUREMENTS.md 2026-08-08 and raised as **D-230**. |


| D-230 | defect | 2026-08-08 | **EIGHT IDENTITY REFUSALS SHADOW A PAYLOAD COMPLAINT AND NO SUITE PINS ANY OF THEM — which is worse than D-229's class, because those were believed on half their evidence and these are not measured at all.** MEASURED 2026-08-08 by REC-73's sweep (`bio-plane/test/machine-fences.test.mjs` block 4, figures in MEASUREMENTS.md): a walk over `store.mjs` computing, for every refusal, how many distinct refusals sit BEHIND it in the same method. **380 methods, 111 identity-flavoured refusals, 66 shadowing something outside the twelve, and eight with NO pin at all: `BAD_HANDLE`, `EDITION_NOT_INCREMENTED`, `LEASE_HELD`, `NOT_ACTIVE`, `NOT_AN_OWNER`, `NO_AUTHOR`, `NO_CASE`, `NO_OWNERS`.** Two of them guard OWNERSHIP of a project (`NOT_AN_OWNER`, `NO_OWNERS` in `projectOwnerRemove`/`projectOwnerRescue`) and one guards a PUBLICATION invariant (`EDITION_NOT_INCREMENTED`), so this is not a tail of cosmetic codes. NEEDS: a pin per code, driven under a payload that would otherwise succeed — REC-73's shape, one layer out. **The deepest shadows are separately worth a look and are NOT part of this row: `NOT_THE_OWNER` inside `promote` shadows 18 refusals and `CAS_STALE` 17**, both pinned, neither shown to be what refuses. Relation: D-229, REC-73, REC-4. | M4 · open — the set is pinned AS A SET in REC-73's suite, so it cannot drift in either direction unnoticed |

## Dispositions, added 2026-07-31

Every open row now carries a leading DISPOSITION token in its Status column: a
milestone from `MILESTONES.md`, or `DOCTRINE (Bob)`, `ACCEPTED`, `WATCH, no task`, or
`NOT OURS TO MEASURE`. The reason is that this file had become the de facto backlog
while `QUEUE.md` held six items, so forward work was invisible inside a ledger nobody
could sort. **A row with no disposition is invisible work** — which is how Bob's
ruling that reused parts must be re-fetched at ratification went two design revisions
with nothing scheduling it and no row recording it.

The token says where the work is placed, not that it is scheduled. Only `QUEUE.md`
schedules. A new row takes a disposition at the moment it is written.

Two ledger corrections this pass found, both stale rather than wrong when written:

- **D-48** said `open, S-12 step 6`. It was closed by 0.28.0 (`PLAN.md` S-12 item 6
  names it explicitly) and the residue is D-50, the catalog half.
- **D-53** said it blocks S-11 step 5. That block is STALE: bulk release shipped in
  0.34.0 under Bob's 2026-07-27 ruling, which decoupled the operation from trust
  inheritance entirely. D-53 remains open as doctrine and blocks nothing today.
