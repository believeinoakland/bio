# Moved

This file is now `docs/development/kickoffs/UI.md`.

Kickoffs were split per THREAD on 2026-07-30, after two concurrent sessions both
rewrote this file. There is one kickoff per line of work, and the first line of a
session's prompt names which thread it belongs to:

```
Kickoff: thread UI. Read docs/development/kickoffs/UI.md from
raw.githubusercontent.com/believeinoakland/bio/main and follow it.
```

See `docs/development/kickoffs/README.md` for the register of active threads,
the paths each owns, the append-only rules for shared files, and the rule that
matters most: fetch and rebase before pushing, and never force-push `main`.

This stub stays because links to it exist in older documents and in Bob's notes.
