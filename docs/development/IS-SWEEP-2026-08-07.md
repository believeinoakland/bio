# The consistency sweep — investigative session vs the recorded architecture

**2026-08-07, session BOB, at Bob's direction:** *"engage in another exhaustive multi-session
sweep through the repo… to optimize the opportunity to improve consistency across decisions,
architecture, requirements, and functional completeness"* — the pass that readies the record
for final investigation-AI architecture, integration, and code development.

Five parallel research sessions: the 59 decisions · `docs/architecture/**` (16 files, 12,223
lines) · milestones/queue/debt · the remaining design docs + research corpus · a functional
audit of the plane's 131 ops against the IS design. This file is the synthesis. Findings were
**re-verified against source** before being recorded here; each carries its citation.

The verdict in one line: **the design's doctrine held up; its nouns were wrong in three
places, two of its reads are unbuildable today, one door it assumes does not exist, and the
architecture corpus already answers four questions the design listed as open.**

---

## 1 · CORRECTIONS — the design said something the record contradicts

### 1.1 A CLAIM is not an object, and IS-1's subject was misnamed

`OBJECT_TYPES` (`bio-checks.mjs:26`) is `{information, inquiry, project, action}` — there is
no claim type, no claims table, no claim op. **Case Making ruled it** (2026-08-01): a claim
is a **FIELD of an inquiry**; concluding is the inquiry ADOPTING a claim, carrying the basis
and the falsifier; `focus`/`finding`/`case` are phase names on the one recursive type
`inquiry`.

**So IS-1 versions the BASIS OF AN INQUIRY, not the legs of a free-standing claim object.**
The design's intent survives intact — a version is a complete alternative composition of
`basis[]` with its description — but the build target is the existing inquiry construct:
`bundle.md`'s `basis[]` as the authority, `inquiry_basis` as its projection (one write site,
inside `promote`'s transaction, `store.mjs:7288`; a directly-written version table would be
the second-place-to-state-a-fact D-21 forbids).

**A version must also carry the GROUND partition, not only the legs.** `inquiry_basis`
already has `ground` and `role` columns; DEC-32 (grounds, OR-max) is still open with the
columns already real. A version that is a flat leg set cannot express plurality — the
version IS the composition, and the ground partition is part of the composition.

### 1.2 The bias read: half ruled, half forbidden — and D-215 closes

- **RULED, and this session missed it when raising D-215:** *"An assistant-surfaced focus
  must carry the bias manifest in force when it was surfaced… unlike a member it will not
  remember. Without the manifest… bias debt cannot be computed against it"*
  (`Content_Framework:1129-1141`). The run-carries-its-bias-context question was answered
  before it was asked. **D-215(2) is therefore RULED YES for the bias component.**
- **FORBIDDEN, and the design drifted toward it:** *"bias never shapes what is captured or
  monitored, only how conclusions are weighed"* (`Content_Framework:1283`) — the coupling is
  *forbidden rather than documented*. §3's "net bias" read is legitimate only as the thing
  the run CARRIES and the weighing it discloses — never as an input to what the session
  searches for. This sharpens Bob's own §11 ruling (minimise bias effects in the skill) into
  a structural rule: **the search half of the run must not receive the bias at all.**
- **AND THE OBJECT TO READ DOES NOT EXIST:** `object_type: bias` is absent from the check
  catalogue (D-84); `Declared_Bias_v0_1` is a DRAFT — *"no check exists, no code implements
  it."* The net bias is the design's **second unbuildable read**, beside D-222's meaning
  layer. Until D-84 lands, the run's manifest-carrying obligation is dischargeable only as
  "no manifest was in force," stated.

### 1.3 An AI-suggested leg's grade is EARNED or ABSENT — never assigned

The apparent tension between Bob's §5 ("the AI… assigns strength values that when calculated
are supported by the evidence") and §8.1 (grade states HOW the connection was established,
and nothing else; the ASSISTANT construct forbids *"a grade asserted rather than earned"*)
**dissolves under three standing rulings, and no new ruling is needed:**

- §8.1: a leg's grade is a fact about METHOD — the source's own identifier (A), an identifier
  matched in content (B), correspondence (C), testimony (D).
- DEC-18: an ungraded leg is INERT, contributes nothing, **and every ungraded leg is
  NAMED** — inert must never mean invisible.
- DEC-15: a HUNCH is a member act. The AI may not propose one.

**Therefore: the session COMPOSES legs and the grades arrive from the record** — from the
resolutions the legs rest on (`earnedBasisRegistry`, `store.mjs:8808`), exactly as `op=cite`
already fills them. Where the record has earned nothing, the leg is ungraded, inert, and
named as such in the version's description. "Assign strength values" means composing legs
whose earned grades produce a supported calculation — not minting numbers. This also gives
§10's safe-to-be-wrong property (which recogniser, which version, what confidence, what
signals) a path to every machine-suggested leg.

### 1.4 The state set exists nowhere, and the proposal rules the design rebuilt are already written

`suggested/considering/accepted/rejected` has no analogue anywhere in the plane — not in
`STATES` (five machines, `bio-checks.mjs:127`), not in task states (`open/forwarded/
resolved`), not in proposal dispositions (`deferred/dismissed`, with adoption explicitly "an
authored act, not a disposition," `store.mjs:10730`). IS-2 builds the sixth machine. Three
consequences the design missed:

- **The interaction-construct PROPOSAL rules apply and answer D-217(a) better than dedup
  did:** a machine proposal is **adopted / deferred WITH a recorded reason / dismissed WITH a
  recorded reason**; proposals **AGGREGATE, never multiply**; they **AGE rather than
  vanish**; and they must visibly **LOOK derived** (D-82 — and D-78 records that both
  existing writers hardcode `surfaced_by: human`, so the field the design relies on is
  broken today; Step 8b, which fixes it, is UNSCHEDULED).
- **The member-side transitions are ACTS and owe the four beats** (choose · see what will be
  refused BEFORE it runs · author the reason · receipt). The design treats accept/reject/
  make-current as bare state flips. Rejection at minimum carries a reason (the rejection
  record is the anti-omission instrument — it is worthless without one).
- **The new machine must publish itself through `op=affordances`** (a NEEDS/NON_ACTS row) or
  every surface showing version states holds a second copy of the rule — the drift class
  DEC-8 closed.

### 1.5 The capture-request door does not exist, and opening it crosses two recorded decisions

§4's fence — "the AI requests; the daemon captures" — has **no mechanism on the request
side.** Verified: `taskenqueue` is absent from the OPS table *deliberately, with the
reasoning written into the table itself* (`index.mjs:295-299`: no control-plane route may put
an event in the queue on its own account); `taskEnqueue` refuses any kind outside
`TASK_KINDS = ["authority-undetermined"]`; and the daemon class holds exactly TWO verbs
(`acquire` — archive arm only, direct refused by name at `index.mjs:2687` — and `monitor`),
per DEC-37's own words: *"scope it to exactly the two verbs it needs today and widen by
decision, not by drift."* **→ DECISION ITEM for Bob (§4 below).**

### 1.6 Published case vs pruned versions — R4's disclosure rule reaches here

R4 (division): *a published child names its parent and its siblings*, because division
without disclosure is *"a laundering path with a tidy name."* The versions design lets a
member prune rejected ancestor versions (D-217a) and publishes ONLY the current version
(§13). Structurally identical hazard: **a published case that discloses nothing about
alternative accounts considered and pruned.** The exclusion-statement machinery
(`inquiry_exclusions`, completeness statements scoped to an edition) is the existing idiom.
**→ DECISION ITEM for Bob (§4).**

### 1.7 Smaller definite corrections

| finding | consequence |
| --- | --- |
| **Regrade does not exist as an op.** Grades move only by minting better resolutions then re-writing basis through cite/promote | the "regrade is the second caller" line in DEC-46/IS is a design intent, not a mechanism; and **a frozen version's leg grades can go stale beneath it** — a version freezes the COMPOSITION and the grade-REFERENCES; the effective calculation always uses current earned grades (this follows from 1.3; the description records what was true at composition) |
| **The run object has a proven model:** `capture_sessions` (`schema.mjs:358`) — *"SCRATCH, not record… a work list with an expiry"*, ticks + expires + opaque state, resumable across invocations | IS-6/IS-9 extend this shape; the observation log cannot live in `bundle.md`, which is written only on success |
| **The scheduler has SEVEN live consumers** now (`store.mjs:1452`), ticks may be async and are awaited | IS-9(c) confirmed as one appended entry |
| **`runtime-ceiling-reached` exists in the condition vocabulary with no producer** (`queuestate.mjs:82`) | IS-9(d)'s budget-exhaustion record = build that producer, not a new kind |
| **D-216 resolves toward NO CLONING:** project↔inquiry is a `refs` edge; many projects may cite one inquiry; and the project's own `bundle.md` frontmatter already carries `required_strength` read by `#requiredStrengthFor` (`store.mjs:5154`) with strictest-wins | IS-3's CURRENT pointer is a project-authored, dated frontmatter field beside `required_strength` — never a settings row (DEC-17/D-199.2) |
| **The out-of-inquiry lead is a FINDING-class slug** in `queuestate.mjs`, not an N-id; its `options[]` are inquiry-grain acts that don't exist yet; its `case` set derives from inquiry B's ancestors, not A's | recorded on D-213 2026-08-07 |
| **`derived_from` is already in the closed relationship vocabulary** (State Rules v1.5) — but Case Making verified `supersedes`/`corroborates`/etc. have ZERO producers in `store.mjs`; vocabulary membership means only that C-6.1 won't refuse the string | IS-1's derivation edge should BE `derived_from`, and IS-1 becomes its first real producer |
| **The IS work allocates no C-numbers.** Every refusal the design promises ("refused BY NAME") lives in the check catalogue | IS items name their C-numbers at build, same as every other gate |
| **`suggested` material and the ratification fence:** Intake Doctrine — sweep material lands at `collected`, never higher; fetching is policy-governed, never caller-governed | the run's captures land exactly as the daemon's always have; nothing new to build, one sentence to carry |

## 2 · STALE RECORD CORRECTED BY THIS SWEEP (already pushed, batch 1)

`VERIFICATION.md`'s floor table (0/42 → measured 105/105 suites declaring negative
controls) · D-117 resolved (coverage.mjs gates the fleet under `--strict`) · D-213's close
condition (N-ids → slugs) · IS-2's negative control rewritten (it would have passed
VACUOUSLY under rule 3a — the fence lives in three layers and each must be broken with the
others held open).

Remaining stale, delegated or noted: `OFFICE-FORMATS.md` says "nothing here is built" while
`formats.mjs` registers html/pdf/docx/xlsx/pptx (owner: CONTENT-OFFICE) · Case Making says
`inquiry_basis` "does not exist" — it does now · `Content_Framework §12.2` says a claim is
deliberately not modelled — Case Making superseded it.

## 3 · WHAT THE EVIDENCE-GATHERING HALF INHERITS (from the design-docs sweep, recorded here so it reaches the builder)

- **The UA string is measured, not stylistic** (`SOURCE-ACCESS.md:133-169`): removing the
  contact URL flips admission 200→403 uniformly; `purpose` distinguishes capture from
  monitoring — an investigation fetch introduces or reuses a purpose token deliberately.
- **An observation log that says "source unreachable" when the truth is "our governor held
  us" manufactures a false absence** (D-104; `source-unreachable-governed` exists in the
  vocabulary to keep these apart). The log must make the same distinction.
- **Client-rendered portals produce evidentially empty captures with no signal** —
  `client-rendered-shell` is catalogued with NO producer; `CLIENT-RENDERED.md` is a draft
  whose open question 4 is precisely whether an unattended sweep may capture such a source.
  The run must treat a shell capture as `LOOKED_INDETERMINATE`, not `PRESENT`.
- **Archives are a BACKUP source only** (three consecutive failures or fourteen days), the
  fallback is built and idle, and CDX has three measured traps (compressed-size `length`,
  the empty-body digest, non-200 rows).
- **TREC 2011 measured searchers estimating their own recall and it failed badly** (+95/−87
  points, premature termination from false belief of high recall) — the direct evidential
  case for §14b.4's rule that THE MODEL NEVER DECIDES WHEN THE LOOP STOPS.
- **The two 2026 AI-review cases** (Schulte; James v. Cerebras) — where law imposes nothing,
  parties who care negotiate **prompt-level disclosure with change tracking**. The skill
  version the run records (§14a) is a requirement, not an analogy.
- **The placeholder defect is the machine-speed overclaim**: the UI satisfies a non-empty
  check with the literal `counterparty: to be named` (`PROCESS-INVENTORY`). An AI filling
  required fields to clear a gate is the same defect at scale — a skill-layer prohibition
  and a §14b.5 check (a version whose description or reason field is boilerplate is not
  proposed).
- **The PRACTICE-SURVEY "deliberately violate" list is the skill's prohibition set** — no
  generated justification anywhere (a generated one is a fabricated attribution); the one
  permitted auto-composition is assembling the member's OWN prior words (Zotero's line); no
  single confidence score; no connection-density ranking; machine-proposed connections never
  presented as connections.

## 4 · DECISION ITEMS FOR BOB — accumulated, in the kickoff shape

**(1) The capture-request door.** The AI must request captures; no request mechanism exists,
`taskenqueue` was omitted from the op surface deliberately, and DEC-37 scoped daemon to two
verbs "widened by decision, not by drift." — *Provisional:* nothing built; the IS design
holds. *Options:* (a) a `capture_requests` table + a new scheduler consumer, the producer
being IS-4's endpoint (keeps the no-control-plane-enqueue rule intact; the daemon drains);
(b) a scoped control-plane enqueue op for the `ai` class (crosses the recorded reasoning).
*Recommendation:* (a) — it widens DEC-37 by decision while keeping the door the OPS comment
closed still closed. *Reversal:* cheap before build; the table is scratch-class.

**(2) Published-case disclosure of pruned alternatives (1.6).** Does a published case state
that alternative versions existed and were pruned — as the completeness/exclusion statements
already do for scope? *Provisional:* §13 as written (current version only, named).
*Recommendation:* one sentence in the published bundle, same idiom as the exclusion block:
authored, per edition. *Reversal:* impossible for editions already published — which is the
argument for deciding before IS-8 builds.

**(3) robots.txt and "areas anybody can go through."** Measured: Oakland's robots.txt
carries 82 Disallow rules of which **63 are Public Ethics Commission publications**,
including sixteen years of annual reports. On this instance's most relevant corpus, the
conduct rule DEC-47 deferred decides whether the session can reach transparency publications
at all. *Provisional:* nothing built; no fetch happens. This is doctrine (posture toward
sources, and what "public" means), not build detail.

**(4) DEC-52's remaining yes/no** — unchanged from before the sweep, sharpened by 1.3: the
constitutive acts (entity/alias/relation/progression/testimony/threading, and the expertise
pair hardest) get fences; `resolve` stays open as derived; the sidebar mechanism you ruled
is the approval path.

## 5a · THE LADDER, THE QUEUE, AND THE BUILD ORDER (fourth sweep session, milestones/queue/debt)

**The IS work is filed on the wrong rung.** The DEC-60 debt family carries M4, inherited from
D-199's placement by proximity — but M4's own acceptance is readings/progressions/junction
findings, and **M9's acceptance IS the claim layer** (legs, basis, per-axis strength,
undetermined suspending an axis). By the ladder's own words: IS-1/2/4/7 are M9, IS-8 is M10,
IS-3 and the running-session surface are M8. The D-127 precedent applies — when case-making
had no rung, the answer was new rungs (M9/M10), not filing it under substrate. **And
`MILESTONES.md` is the stalest surface in the repo**: its placement tables stop at D-156,
missing ~50 open rows including the whole IS family, against its own preamble rule. No IS
rung, no DEC-60 mention anywhere in the file.

**Six answered decisions have no enactment path** — DEC-47/49/56/57/58/59 all decided
2026-08-06, no `enacted:` line, no BOB INBOX entry, no queue item. The two with build weight:
DEC-49 (every refusable condition carries a code + canned translation; untranslated codes
fail the harness — touches every fence IS adds) and DEC-59 (bias + definitions become case
ELEMENTS and PDF sections — reshapes IS-8 and the landed case ops).

**Sequencing findings (recorded as findings, CONDUCT gates):**
- Preconditions first, both M3 and both unqueued: D-222 (option A then C) and D-220's join.
- **IS-6 is the one unblocked start** — depends on nothing, discharges D-196, adopts D-129's
  four-state vocabulary (`NEVER_LOOKED / LOOKED_ABSENT / LOOKED_INDETERMINATE / PRESENT` +
  `partial`), and makes §15's instruments computable. IS-1 in parallel once the claim-shape
  question settles.
- **REC-59 before any new IS op** (the bare-array pin allows exactly one exception — the op
  REC-59 fixes). The uncapped meaning reads (`concerns`/`resolutions`/`connections`) are
  RECORD work with no item.
- **UI-38 is the collision risk**: it builds the assistant's surface registry while §14a
  says the running-session surface is designed ONCE for all AI features. Its scope should
  absorb that or two AI features grow two surfaces.
- IS-3 waits on D-216's model check; IS-9's shape waits on D-218's measurement; IS-5 waits
  on D-199 (unqueued); IS-8 waits on D-187's reshape + DEC-59's elements + the DEC-33-blocked
  publication ceremony (REC-15/UI-17 — trigger: "Bob reopens the case-making thread," which
  DEC-60 arguably fired; needs saying, not assuming).
- **D-199's "SEQUENCE THE CHECK ROLE FIRST" is now stale against DEC-60** (the investigative
  session — a mutating scope — lands first). Resolve deliberately, not silently.

**More rows that bind the build, from the sweep:** D-195 (independence check) belongs in
IS-4's pre-write checks — an AI composing OR-branches at volume is *"the Judith Miller error
with arithmetic behind it"*; D-164 bounds legs to whole bundles today and the design must SAY
so until content-extent lands; D-168 (`op=cite` is type-only — a "reachable" leg check would
pass RETIRED information); D-184/C-2.8 bound the leg vocabulary to *information or inquiry,
nothing else*; D-190 (the DO ceiling is unmeasured while three IS mechanisms and D-224's
quadratic table all grow the same object); D-146 (build the entity write surface WITH the
claim layer); D-159 (an ungraded leg costs a conclusion nothing — re-read against a machine
shaping legs at volume).

## 6 · THE DECISIONS AUDIT (fifth session) — conflicts with the answered register

Measured first: **the IS design cites exactly five decisions** (24, 47, 52, 55, 60) and
stands, unknowing, on at least fifteen more. The conflicts, most severe first — each
verified by the auditing session against the register's own words:

**C1 · The prune deletes the design's own evidence.** D-217a's prune-on-accept lets a member
delete ancestor versions at the moment of acceptance — and D-214 says in terms that the
rejection PATTERN is queryable only if the acts persist: *"a member who rejects every
suggestion running against their thesis is visible only if the acts persist."* Prune-on-accept
is exactly the act that deletes the suggestions that ran against them. **Bob's two rulings
collide** (→ §4 item 6). DEC-19/DEC-16's never-vanishes-silently posture points the same way,
and DEC-29(b) requires the offer's WORDING to state the disclosure — unspecified.

**C2 · DEC-32's anti-gaming keystone is bypassed by construction.** The keystone: the default
is AND; independent sufficiency is affirmatively claimed PER BRANCH; *"the structure is
authored BEFORE the strength is shown."* An AI-composed version arrives structure-and-strength
together, and one accept carries the member's name over OR-branches they never claimed.
D-195's independence check is the natural counter and appears nowhere in the design.

**C3/C4 · Two assistant rules are contradicted with no amendment note.** DEC-27 limit 2: the
assistant *"may not add propositions the member did not state. It ASKS; it does not assert."*
DEC-60 licenses claim formulation — the line moved by ruling — but neither DEC-24 nor DEC-27
carries a note, so a builder reading the register gets the opposite instruction. And DEC-24
rule 3: *"a machine-proposed connection is a HUNCH… until earned or attested"* — under which
every AI-composed version is publication-disqualifying (DEC-20), while the design treats
hunch as a member-only marking. Either rule 3 is superseded (needs saying) or the design is
wrong (needs fixing).

**C5 · "Effective strength" is the refused single number.** DEC-21/DEC-44 (four refusals):
strength is a PAIR over two populations, never composed. §12 is singular throughout, and the
design's "the calculation stays simple" omits DEC-32's MIN-over-AND/MAX-over-OR — a version
with no relationship field re-ships the flat-AND basis REC-42 corrected.

**C6 · Attribution.** §4.1 says a requested capture is attributable to the instance's
ordinary path "rather than to an agent" — DEC-27(b) requires the record to state BOTH: *"the
assistant captured this, at Anna's request."* They cannot both be true. DEC-27(b) wins;
design corrected accordingly. Also: the Claude-account principal and the plane-credential
principal are two different principals; the record must name both.

**C7 · The lens rule was demoted from structure to skill** — and §14b.4's own words forbid
that (*"a skill is instructions; a fence is code"*). Sharper: cross-project sharing lets Team
B accept a version composed under Team A's bias with none of the four things DEC-46 required
at exactly this boundary — and DEC-46(3) explicitly REJECTED a notification-grade answer.

**C8 · A projectless inquiry has no CURRENT and no bar** (legal per DEC-17), and "the current
evidence standard" is per-project and a PAIR — §3's singular read is unspecifiable for a
shared inquiry. **C9 ·** §13's singular "published bundle / current version" is DEC-44's
corrected arity error re-entering: a case is a container over MANY findings, each with claims,
each with a current version per project. **C10 ·** the what-if strength presented as colour is
not IN-BAND — DEC-40's control (strip the filter line, harness fails) applies. **C11 ·** the
pre-write checks' side of the wire is unstated: if the fleet member computes them it holds a
copy of the plane's rules (DEC-8 breach); the plane must run them. And DEC-49's not-optional
guard applies to the three new member-facing conditions the design mints with no codes.
**C12 · CHECK vanished.** DEC-55's enactment instructs *"sequence the CHECK role first"*; the
IS build order contradicts an enacted instruction (→ §4 item 7). **C13 ·** DEC-32 withheld
object identity from claims by the citability test; a versioned, named, stateful claim object
rebuilds the multiplicity D-127's collapse removed — OR the design's "claim" means "inquiry"
(→ §4 item 5). **C14 ·** DEC-54's policy version pin must be carried at the published case
even though versions don't carry conditions (reconcilable; §13 must say it). **C15 ·** legs
are document-grain until D-164 lands — a SECOND undeclared precondition beside D-222 — and
DEC-23 requires extent + extraction method on every content leg.

**Vocabulary drift (D-156's class, no rows yet):** *finding* now has three senses and the
worst is in IS-9's acceptance clause (a search hit named after the record's unit of truth);
*version* has six senses, three inside the IS doc itself; *leg* in the design is materially
thinner than the register's (no axis, no relationship, no grade_source, no extent); *ground*
is a DEC-32-banned surface word AND a schema column.

**Register integrity:** `## Answered, awaiting enactment: (none)` is FALSE — seven entries
decided with no enacted line. DEC-8 carries no amendment note from DEC-49. **Two Bob rulings
exist only in this design doc and not in the register:** transcript retention (with the
litigation hold) and the pursue/investigate merge. The empty-run instrument has no object to
count — §9 has no suggestion kind for *"we looked at this level and it is empty,"* while
CLAUDE.md makes saying-which-absence a first-class obligation. The run's READ VIEWER is
unstated (an org-scoped credential composing material a narrower member then accepts crosses
D-15). Seven negative controls owed by name (DEC-55.5's second half, DEC-44's, DEC-40's,
DEC-34's, DEC-46(a)'s, the pre-write checks, and the objective's own control: feed a run an
inquiry the evidence does not support and assert it proposes nothing). Four notifications
minted with no ids. §14c's numbers are not in MEASUREMENTS.md. The uncapped meaning reads
have no debt row. DEC-53's revisit trigger (bulk resolution) HAS FIRED via DEC-52's sidebar
ruling. DEC-50's refusal is bypassed by the version-edit path (§6.7 allows an unattributed
structural edit). DEC-43's stakes rose: on a fallback instance, AI-directed open-internet
fetching runs under the ROOT credential.

## 4a · THREE MORE DECISION ITEMS FOR BOB (from the decisions audit)

**(5) What is a claim?** DEC-32 withheld object identity from claims (citability test);
Case Making ruled claim = field of inquiry; the versions design gives claims names, states,
versions and a CURRENT pointer — object identity in all but name. *Options:* (a) versions
attach to the INQUIRY's basis (claim stays a field; one version-set per inquiry per project);
(b) claims become first-class objects (reverses DEC-32's containment deliberately).
*Recommendation:* (a) — it preserves two standing rulings and loses nothing the design needs,
since §8's question-sharpening already splits an inquiry carrying two propositions.

**(6) The prune vs the rejection record** — your D-217a and D-214 rulings collide (C1).
*Options:* (a) prune never removes REJECTED versions, only superseded-unreviewed ones; (b)
prune hides rather than deletes (the acts persist, the display shrinks); (c) prune as ruled,
accepting the loss of the pattern. *Recommendation:* (b) — it is what "delete" plausibly meant
at the UI altitude, and it keeps both rulings whole.

**(7) Sequencing: CHECK first, or the investigative session first?** DEC-55's enactment
instruction says CHECK; DEC-60's momentum says investigate. The audit notes they may be one
build: the investigative session run with §2's objective against an existing conclusion IS
CHECK. *Recommendation:* rule that CHECK is the investigative session's first DEPLOYED MODE
rather than a separate prior build — satisfying the enacted instruction without a second
architecture.

## 4b · FOUR OF THE SEVEN RESOLVED BY THIS SESSION — Bob, 2026-08-07: *"do not suggest that
I be the one to make decisions that relate to relatively tactical and module-level decisions.
Those are your responsibility to resolve - and resolve correctly with full understanding of
the architectural decisions already established."* Re-examined: four of the seven were
answerable from rulings already recorded, and are now DECIDED by session BOB:

- **(1) The capture-request door → the `capture_requests` table, drained by the daemon.**
  The POLICY was already Bob's own ruling — *"capturing a document is something the daemon
  does (sometimes at the suggestion of an AI)"* — so the widening DEC-37 requires "by
  decision" has its decision; only the mechanism remained, and the table keeps closed the
  door the OPS comment closed (no control-plane enqueue), keeps the daemon the sole
  fetcher, and gives DEC-47's conduct rules one enforcement point at the drain.
- **(5) What is a claim → versions attach to the INQUIRY'S BASIS; claim stays a field.**
  Forced by two standing rulings: DEC-32 withheld object identity from claims by the
  citability test, and Case Making ruled claim = a field of an inquiry. One version-set per
  inquiry per project; an inquiry carrying two distinct propositions is split by §8's
  question-sharpening, which is the machinery the design already has.
- **(6) Prune vs the rejection record → HIDE, never delete.** Forced by DEC-16/DEC-19's
  never-vanishes-silently posture and D-214's the-acts-must-persist: "delete" at the UI
  altitude means the display shrinks while the acts remain queryable. Both of Bob's rulings
  survive whole; no doctrine moved.
- **(7) Sequencing → CHECK is the investigative session's first DEPLOYED MODE.** DEC-55's
  enacted CHECK-first instruction and DEC-60 are satisfied by one build: the session run
  with §2's objective against an existing conclusion IS the CHECK role; deploy that mode
  first. No second architecture.

**What remains genuinely Bob's — three: (2) published-case disclosure of pruned
alternatives (what the record claims to the world), (3) robots.txt vs the Oakland PEC
corpus (posture toward sources, effects on outside parties), (4) DEC-52's remaining yes/no
(the machine/member boundary).**

## 5 · WHAT REMAINS OPEN AFTER THE SWEEP

D-222 (meaning-layer read surface — precondition, options graded in §14c) · D-84 (the bias
object type — now named as the second precondition) · D-218 (CPU probe) · D-224 (connections
quadratic growth — measure) · DEC-32 (grounds; the version design now depends on its answer)
· the four decision items above · two sweep sessions still to report (decisions audit;
milestones/queue/debt audit), whose findings append here.
