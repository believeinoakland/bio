# QUEUE — closed rows, moved by the archiver

**Written only by `node tools/ledger.mjs archive <ID>`**, which moves a closed row (`done` or
`superseded`) out of `docs/development/QUEUE.md` VERBATIM and appends it here, and refuses the
move unless every id occurs exactly as often in the live ledger plus its archive after the move
as before. Rows arrive in the order they were archived. Find any id, live or archived, with
`node tools/ledger.mjs find <ID>`; rulings in these rows stay indexed by `tools/decided.mjs`.
The August roll (`QUEUE-2026-08.md`) is a separate closed file and is never appended to.

### M0-61 · done — landed `1585cec9`, merged by CONDUCT #4, 2026-09-18. **THE ROW'S DIAGNOSIS WAS HALF WRONG AND THE WORKER SAID SO: the newline is not the defect.** Of 50 matches only 3 cross a line break and 2 of the 3 are GENUINE claims wrapped mid-phrase; the false one (`RECONCILED.md`) matches identically on one line — it is false because it sits 187 lines above the table it was credited to. So the literal-space "fix" would have cleared the receipt and lost both true claims (control arm C proves it). The predicate now spans at most one in-paragraph line break, never a blank line, with the reason at the site; the real fix is `statussweep`'s REMOTE-trip flag. Every count unchanged (16 / 10 apart / 6 real); output byte-identical across six `corpuscheck` modes. `undesignedclaims` reads 28, not the row's 27. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **`corpuscheck`'s `UNDESIGNED` PREDICATE IS BUILT FROM `\s`, WHICH MATCHES A NEWLINE — so a sentence broken across two lines fires it, and on one file that dragged in a table of ten unrelated rows (raw 16 against a real 6).** — waits on nothing; the M0 background lane holds no slot.
milestone: M0 (background lane, holds no slot) — a predicate that is harmless only because something else gates it is a defect waiting for its second caller
interface: none — a shared predicate in `tools/corpuscheck.mjs`
design: `docs/architecture/CORPUS-STANDARD.md` §4.8 (the single-authority rule the predicate serves) and §7 (how the checker works), read with M0-58's landed report, which found it and deliberately did not fix it.
depends-on: none
scope: **FOUND BY M0-58 AND REPORTED RATHER THAN WORKED AROUND, which is why it is a row instead of a silent patch.** The predicate's `\s` matches a NEWLINE, so `"…and not\ndesigned once."` satisfies it. On `research/RECONCILED.md` §2.2 one such sentence pulled in a table of **ten `constraint` rows**, which is the whole gap between a raw 16 and a real 6. **THE IMPORTANT PART IS THE CONDITIONAL: it is HARMLESS INSIDE `corpuscheck` TODAY because signal 1 gates it — and NOT harmless ungated.** M0-58's own sweep imports the predicate and had to flag such trips and count their items apart. **A predicate that is safe only because of its current caller is a defect with a fuse in it**, and the second caller already exists. **DO NOT simply swap `\s` for a literal space** without checking what else the predicate must span — tabs and multiple spaces are legitimate; the question is whether a LINE BREAK should be, and the answer needs to be argued at the site rather than assumed from this row.
accepts-when: the predicate no longer matches across a line break, **or it does so DELIBERATELY with the reason written at the site**; **the raw-versus-real discrepancy is re-measured and recorded** — 16 against 6 on the named file is the receipt and must move to agreement or be explained; `corpuscheck` output is BYTE-IDENTICAL for every document that was not mis-matched, proving the fix narrowed nothing else; M0-58's `statussweep` still reports the same findings, since it imports this predicate; `node tools/plancheck.mjs --local` then BARE; `cd bio-plane && npm run test:battery` green own-baseline.
NEGATIVE CONTROL: run and recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon** — a control was invisible to the register for writing `NEGATIVE CONTROL (`) — the fix reverted → the cross-newline sentence matches again and the arm FAILS naming the file and the count. **Over-strictness is the arm that matters: a genuine single-line undesigned claim must STILL be matched**, and `undesignedclaims.mjs` must still report its 27 with 0 unaudited — this row narrows a FALSE match and must not remove a TRUE one. **State how a liar would satisfy this before stating what it checks:** the cheapest green is to tighten the predicate until the one named file passes, which fixes the receipt and leaves every other cross-newline instance unmatched and undetected.
added: 2026-09-18 · CONDUCT #3 (**ENACTED FROM M0-58's REPORT IN THE SAME TURN AS ITS INTEGRATION.** The worker found it, proved it harmless in its current caller, said plainly that it is not harmless in general, and did not touch it — the right call, and the reason this is a row with a receipt rather than a defect someone worked around. Id minted with `node tools/mintid.mjs M0`, run bare.)

### M0-62 · done — landed `bf432916`, merged by CONDUCT #4, 2026-09-18. §18 item 5 now POINTS at `BIO_System_Design.md` §3 construct 10; the ruling kept word for word; construct 10 byte-identical (sha256 before and after). A THIRD copy the row did not name — the front matter's *doctrine, Bob's*, stale since 2026-09-14 — corrected. **The row's control was wrong as written:** `statussweep` never named this pair (M0-58 found it by hand) and exits 0 always; the new suite section is what fails. **Reported for BOB, not fixed:** no §3 construct row states the status of firsthand observation (D-184). **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **TWO GOVERNED DOCUMENTS STATE THE MEMBER'S LEAD'S DESIGN STATUS, AND THE TO-DO LIST RE-ASSERTS IT INSTEAD OF POINTING — the exact shape §4.8 was written to refuse, found by M0-58's sweep.** — waits on nothing; the M0 background lane holds no slot.
milestone: M0 (background lane, holds no slot) — a status restated in a second place is a copy that starts rotting the moment it is written
interface: none — a corpus correction
design: `docs/architecture/CORPUS-STANDARD.md` §4.8, read with `docs/architecture/BIO_System_Design.md` §3 — **which Bob ruled the SINGLE AUTHORITY on design status, 2026-09-17** — and with M0-58's landed report, which reported both sites and deliberately adjudicated neither.
depends-on: none
scope: **BOTH SITES, AS M0-58 REPORTED THEM AND WITHOUT RE-DERIVING THEM.** **Site A** — `BIO_Content_Framework_v0_10.md` §18 item 5, under *pieces to be designed… designed nowhere in this document*: *homes for the member's lead and firsthand observation | D-194 and D-184, designed together. RULED 2026-09-14 … designed in Program B*. **Site B** — `BIO_System_Design.md` §3 construct 10's state cell: *the authored frontier — a member's LEAD — has no home (D-194) [audited 2026-09-17: STILL TRUE, D-194 open]*. **THEY ARE NOT FLATLY CONTRADICTORY — *ruled but homeless* reconciles them — AND THAT IS WHAT MAKES THIS THE DANGEROUS CASE RATHER THAN THE EASY ONE**, because nothing will ever fail on it: M0-58 measured that its own arm fires only once a restatement has gone STALE, so **a second statement still TRUE today passes untouched, and it is the copy that rots tomorrow.** **CONDUCT APPLIES BOB'S EXISTING RULING RATHER THAN MAKING A NEW ONE: the construct map is the single authority on design status, so §3 construct 10 is authoritative and §18 item 5 must POINT at it rather than restate it** — the same act M0-57 performed on §18 item 6, and the same shape as sweeping a surface to a ruling already made. **If the builder finds the two statements CANNOT both be true — if *ruled but homeless* does not in fact reconcile them — that is a different item and it goes to BOB**, because then the question is which is FALSE rather than which is AUTHORITATIVE.
accepts-when: §18 item 5 POINTS at the authority instead of restating its status, in the form items 1–4 and now item 6 already use; **`BIO_System_Design.md` §3 construct 10 is NOT edited** unless it is itself wrong, and if it is edited the reason is stated — the authority does not get quietly rewritten to match its copy; **the substance is preserved**, since *ruled but homeless* is a real and useful fact and this row removes a DUPLICATE, never a FACT; `node tools/corpuscheck.mjs` 0 fail with the front matter of any document whose stated completeness changes moved in the SAME commit; M0-58's `statussweep` re-run and its finding shown CLOSED rather than merely absent; `node tools/plancheck.mjs --local` then BARE; `cd bio-plane && npm run test:battery` green own-baseline.
NEGATIVE CONTROL: run and recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — the pointer reverted to a restatement → `statussweep` names the pair again with both sites and the arm FAILS. **Over-strictness: no other §18 item moves, and every document that was not mis-stating a status is BYTE-IDENTICAL.** **State how a liar would satisfy this before stating what it checks:** the cheapest green is to DELETE §18 item 5 — the duplicate is gone, the sweep is silent, and the corpus has quietly lost a piece it still owes, which is worse than the duplicate it was meant to remove.
added: 2026-09-18 · CONDUCT #3 (**ENACTED FROM M0-58's REPORT, which found both sites and correctly declined to adjudicate — *which copy is authoritative is a judgement*.** CONDUCT did not make a fresh judgement either: **Bob ruled on 2026-09-17 that the construct map is the single authority on design status, and this row applies that ruling** rather than re-opening it. The alternative reading — that the two statements genuinely conflict — is named in the scope and routed to BOB if the builder finds it. Id minted with `node tools/mintid.mjs M0`, run bare.)

### M0-63 · done — **BOB #14 CONFIRMED AT THE CODE, 2026-09-18: THE RULING'S EXAMPLE WAS WRONG AND THE RULING STANDS.** `#frontierContent`'s page comes from `#frontierLatest` with `LIMIT (cap+1)*2`, cap ≤ `FRONTIER_LIMIT_MAX` = 2000; its per-row reads are `LIMIT 1` point lookups — bounded, no unbounded scan amplified. The STAY example had been cited from D-384's prose, not the code. landed `a9ffa456` (claim released `0dd066da`), merged by CONDUCT #4, 2026-09-18. **D-384 CLOSED. The header credit is gone and all 23 members are dispositioned BY NAME: 10 STAY (each with its per-row read written at the site and asserted every run), 13 LEAVE (each with its reason). Ceiling 37 → 24; dispatched ops 13 → 10; `ncLinearInline` OUT; a hoist no longer moves any membership.** **THE ROW WAS STALE BY ONE AND THE WORKER SAID SO: 23, not 22** — the roster also carried REC-96's `#searchedForCase`, which the row's count missed. **AND IT DEPARTED FROM THE BRIEF'S OWN EXAMPLE, WITH EVIDENCE:** `frontier`, `#frontierContent`, `#frontierMeaning` LEAVE — their per-row reads run over a page capped at `FRONTIER_LIMIT_MAX`, which is bounded, and what the walk had counted was a GROUP BY in a loop header that runs once. That is the ruling applied (amplification, not size), not a departure from it. `danglingRefs` was a member only because the method splitter does not recognise a generator (`*eachImage`), rowed as **D-414** rather than fixed across five suites. Negative controls: 5 arms, `node bio-plane/test/nc-m063.mjs`. Battery `223/223 · 13937` on the worker's tree. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **D-384 ENACTED: THE `for`-HEADER CREDIT IN `perRowScan` GOES, AND EACH OF THE 22 HOIST-FRAGILE MEMBERS LEAVES OR STAYS BY NAME — a scan in a loop's own header is its row source and runs ONCE, so crediting it as a scan per row grades where the call is written rather than what it costs.** — waits on nothing; the M0 background lane holds no slot.
milestone: M0 (background lane, holds no slot) — a ceiling whose members are held by a spelling is a ceiling that does not mean what it says
interface: none — `bio-plane/test/derivation-bounds.test.mjs` only
design: `docs/development/VERIFICATION.md` — the test estate's own authority, admitted for the M0 lane BY NAME by `tools/rowdesign.mjs`; read with `DEBT.md` D-384 (the measurement: 35 → 14 with the credit removed, the same 21 names by two readers sharing no code path) and the suite's own header, which states what the walk can and cannot see.
depends-on: none
scope: **THE RULING IS BOB #14's, 2026-09-18, delegated as instrument design and NOT to be re-opened here:** the derivation class keeps its definition — AMPLIFICATION, not size — and the `for`-header credit goes. **Widening the class to a linear pass over an unbounded scan was considered and REJECTED** (it enrols `ncLinear`, which the suite declares out by name, and duplicates REC-60's bounded-read question). **THE MEMBERS DO NOT LEAVE SILENTLY.** `HOIST_FRAGILE_2026_09_16` names 21; REC-116 added `provenanceRoutesMarked`, so the population is **22 on `d3ac7ae9` — RE-MEASURE IT on your own tree before you start, and if it is not 22, say what it is and why before touching anything.** For EACH member, by name: **STAYS** — with its amplification NAMED (a per-row read inside a helper the walk cannot see, the shape REC-93/94/95 admitted `#frontierContent`, `#frontierMeaning`, `frontier` for), in a by-name roster with its reason at the site; **or LEAVES**. **A member that stays on the say-so of its old membership, with no amplification named, is the defect this row exists to remove.** Take ids with `node tools/mintid.mjs <NS>`, never by reading the file.
accepts-when: the header credit is removed from `perRowScan`; every one of the 22 appears in exactly one of two by-name lists — STAYS (each with its named amplification) or LEAVES — and the two lists sum to the measured population; the ceiling moves to the new count with the change stated at the site; `ncLinearInline` is OUT; REC-88's hoist no longer moves any membership (inline and hoisted forms classify identically); `DEBT.md` D-384 moves to closed in the same commit with the final counts; `node tools/plancheck.mjs --local` then BARE; `node scripts/coverage.mjs --strict` exit read UNPIPED; `cd bio-plane && npm run test:battery` green own-baseline, read by its COMPLETION LINE.
NEGATIVE CONTROL: run and recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — re-adding the header credit must turn the suite RED naming the arrivals; `ncLinearInline` flipped back IN must fail by name. **Over-strictness:** `ncLinear` stays OUT, and a genuine per-row scan inside a loop BODY is still IN. **State how a liar would satisfy this before stating what it checks:** the cheapest green is to delete the credit and re-pin the roster at whatever count falls out — every departure unexamined, and the three members REC-93/94/95 admitted for helper-hidden reads silently lost. The by-name disposition is what the row is for; the count is only its receipt.
added: 2026-09-18 · CONDUCT #4 (**ENACTED FROM BOB #14's INBOX ENTRY, 2026-09-18, item 2**, which decided D-384 and handed it here to row. Id minted with `node tools/mintid.mjs M0`, run bare.)

### FW-19 · done — landed `c8d9c6cb`, merged by CONDUCT #4, 2026-09-18. **The `sheet-range` and `doc-table` arms and the `image` reference exist, all six office entries emit them, and the content row admits them with `covers` per arm and a `cited_as` column** — an image cited as its own bytes carries no chain and no cap BY MEANING, and is kept apart from an undetermined transcription. **IC-124 (I2 2.3.0, I1 1.5.0) and IC-125 (I5 1.17.0, I3 23.2.0) RESOLVED ACCEPTED at integration.** Premise check: CONDUCT's pre-spawn grep hits were comments saying the arm WAITS — none of the row was built. **Touched `civicos-ui/app.html` and one UI assertion outside its three areas, because `check-semantics.mjs` fails any landing that adds a kind without that copy** — flagged in `CLAIMS.md` for UI to review the three member-facing words. Debt: **D-415** (defined tables / named ranges), **D-416** (a reading inside a new kind does not credit its connection — under-claims only). UI delegation for `cited_as` rendering stands. Battery `224/224 · 13970` on its tree. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **BREADTH item 3 — the two arms and the image reference: IC-1 gains `sheet-range` and `doc-table` and the `image` reference (I2, additive); the office entries emit them; the content table admits them with `covers` per arm and the `cited_as` column (I5, additive).** — waits on REC-85 (the three-arm `covers` this extends); FRAMEWORK + COFF + RECORD.
milestone: M4 — D-164's extent union grows by the two arms the search index and the workbook unit need
interface: I2 (an IC minted at spawn; FRAMEWORK answers-for by CONDUCT in writing if dormant) and I5 (an IC minted at spawn)
design: `docs/development/EXTRACTION-BREADTH-DESIGN.md` §3.1–3.3 and §7 row 3; IC-1's union
depends-on: REC-85
scope: exactly BREADTH §7 row 3.
accepts-when: the two arms mint on an in-range extent and refuse out-of-range by name (REC-85's shape); an `image` reference is emitted by an office entry and admitted with `cited_as`; `cd bio-plane && npm run test:battery` green own-baseline; `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0, with a control-plane assertion for every new op or check in the same turn; `node civicos-ui/test/run.mjs` from the repo root, unpiped, exit 0; `node tools/plancheck.mjs --local` 0 fail; the home design's front matter moved in the same commit if its stated completeness changes.
NEGATIVE CONTROL: per `EXTRACTION-BREADTH-DESIGN.md` §8 — a new arm's out-of-range refusal neutered → an impossible extent mints and the arm fails by name; over-strictness: REC-85's three arms and REC-82's two byte-identical.
added: 2026-09-14 · CONDUCT #11 (draining the 2026-09-14 BOB #11 inbox entry "Part II §18 pieces 2–4 designed" — one row per decomposition-table line; the TABLE is the scope's authority and this row is the pointer, not a second copy; ids minted with `node tools/mintid.mjs`)

### FW-21 · done — landed `c68c5060`, merged by CONDUCT #4, 2026-09-18. **THE ANSWER: Bob's 2026-09-14 on-point refinement reached NEITHER FW-17 NOR SK-7**, though it was in the design before both landed. **FW-17's gap OVERCLAIMS, measured by driving the ops (M-51): citing the page an ordinance is on and a page that never mentions it return the same definite "outside" → rowed as `REC-120`, top of RECORD.** SK-7's half is REC-86's candidate list and no new gap. The content framework said in four places that the on-point pair was "rowed (REC-86)" — false (REC-86 narrows a LEG) — corrected; D-161 narrowed. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **DID BOB'S 2026-09-14 ON-POINT REFINEMENT EVER REACH `FW-17` AND `SK-7`? THE QUESTION IS ROWED BECAUSE THE ANSWER IS UNDETERMINED, AND UNDETERMINED IS FIRST-CLASS.** — waits on nothing; owner FRAMEWORK.
milestone: M4 — D-164, the content-extent primitive
interface: none expected — a verification of what two landed items actually built
design: `docs/architecture/BIO_Content_Framework_v0_10.md` Part II §14.4 and §18, which carry Bob's 5.4 second pass and its 5.6 amendment; the 2026-09-14 BOB #10 inbox entry is the scope's authority for what the refinement SAID
depends-on: none
scope: **THE HONEST STATE, AND IT IS A MEASUREMENT RATHER THAN A SUSPICION.** Bob's 2026-09-14 second pass on 5.4 named THREE rows to fold at next touch: `REC-86` (a machine-proposed candidate list), `SK-7` (the assistant proposing on-point passages for a document-grain edge, labelled machine work) and `FW-17` (the connection pair is the ON-POINT pair, chosen, not merely the strongest-graded mention). **The entry was never drained.** `REC-86` is still `queued` and CONDUCT #2 folded the refinement into it on 2026-09-17. **`SK-7` and `FW-17` are `done`, and whether the refinement reached them BEFORE they landed is UNDETERMINED:** a grep of both rows' whole blocks — scope, accepts-when and landed line — returns ZERO hits for `on-point`, `candidate list` or `machine-proposed`. **That is evidence and it is not proof: a report may use different words for the same thing, and concluding *not built* from *not mentioned* is precisely the unearned-absence class this queue has paid for repeatedly.** So the row's first act is to READ THE CODE, not the rows. **If the refinement did land, close this row saying so and name where it is in the tree — an absence that costs nothing to produce is not a finding, and neither is a presence.** If it did not, the FW-17 half is the one that matters: a connection pair chosen as the STRONGEST-GRADED mention rather than the ON-POINT one is a record asserting relevance it has not established, which is the overclaim class.
accepts-when: for EACH of `SK-7` and `FW-17`, the answer is stated with the file and symbol that settles it — **built, or not built, and not "no mention found"**; anything found unbuilt is either fixed in place if small or rowed as its own item with what it costs; `cd bio-plane && npm run test:battery` green own-baseline; `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0; `node tools/plancheck.mjs --local`, then BARE after committing.
NEGATIVE CONTROL: run and recorded — if a behaviour is found MISSING and added, neutering it must fail BY NAME; if both are found already built, the row states that no code changed and the control is the READ itself, named as such rather than dressed as a gate.
added: 2026-09-17 · CONDUCT #2 (found while auditing seven undrained BOB INBOX entries at the artifact. **The 2026-09-14 BOB #10 entry "TWO REFINEMENTS FROM BOB ON THE ACT-6 RULINGS" was the ONE of the seven that was NOT enacted**, and it is the receipt for why BOB #12 refused to let me drain the set on its word: draining an entry whose act was never performed loses the act silently and forever, while auditing one that WAS performed costs a grep. **When the two errors cost different amounts, take the cheap one every time.** Id minted with `node tools/mintid.mjs FW`)

### CAP-10 · done — landed `2c5cca74`, merged by CONDUCT #4, 2026-09-18. **Every Google Drive export's chain now carries `convert(google-export, <format>)` AHEAD of `layer`, cap UNDETERMINED — null, never a letter.** IC-122 RESOLVED ACCEPTED, I2 2.3.0 → 2.4.0. **THE ROW WAS INSUFFICIENT AND THE WORKER SAID WHY:** *`captureBound` reads it as any other step* would have SWALLOWED the conversion's null the day a text layer gets a measured letter, and a Drive leg would then claim that letter — so the step declares that its unmeasured state makes the document undetermined, and `derivationCap` reads that (changes no stored value today). A letter without a calibration is refused as new check **C-35.13**. Four-arm control `bio-plane/test/drive-convert.control.mjs`. **What CAP-11 can now calibrate:** one step per format (`engine: "google-export"`, `format` odt/ods/odp), each `cap: null` with a `measured_by` naming CAP-11. No backfill of existing Drive captures — how many are in the live record is not measured. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **DEC-75 ENACTED, act 1 — the `convert(producer, format)` step kind on I2: a Google Drive export's chain carries `convert(google-export, <format>)` AHEAD of `layer` with cap UNDETERMINED (null, stated, never a letter), emitted by CAP-8's `driveHop` path; its own IC on I2 minted at spawn (FRAMEWORK dormant — CONDUCT answers-for in writing); nothing in CAP-8 as landed is undone.** — waits on CAP-9 (the same `op=acquire` path, serialised).
milestone: M2 — the record claims LESS about a conversion than a direct capture until the step is calibrated (Bob's 5.8: a letter now, lowered later under authored legs, is the forbidden move)
interface: I2 — a NEW step kind in the chain grammar (`textchain.mjs` rule 1: a chain, never a token; rule 2: every derivation step weakens); its OWN IC minted at spawn, PROPOSED by the worker, RESOLVED by CONDUCT answering for FRAMEWORK
design: DEC-75 (answered 2026-09-14 by BOB #11 at Bob's delegation; reasoning in `DECISIONS.md` and folded into `BIO_Content_Framework_v0_10.md` Part II §16's Drive paragraph); `bio-plane/src/textchain.mjs`'s header (the four rules); IC-85 (the hop the step sits beside)
depends-on: CAP-9
scope: the step kind admitted by the chain grammar (`textchain.mjs` — the ONE place a step kind is defined, the suite's grammar arm extended, never exempted); `driveHop`'s path emits it ahead of `layer` with `cap: null` for every Drive export; `captureBound` reads it as any other step with no code beyond the kind (so REC-88, when it lands, bounds a Drive-export leg to UNDETERMINED on the capture axis, stated); every existing chain byte-identical (digest pin against a pristine-tree measurement); D-351 keeps its own row.
accepts-when: a Drive export's chain carries the `convert` step ahead of `layer` with cap null, driven through `op=acquire` on a fixture export and read back; a non-Drive capture's chain is byte-identical to the pre-item answer; `cd bio-plane && npm run test:battery` green own-baseline; `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0; UI harness from the repo root exit 0; plancheck --local 0 fail; Part II §16's front matter moved if its stated completeness changes.
NEGATIVE CONTROL: run and recorded — the step emitted with a LETTER cap → refused by TEXT_CHAIN_STRENGTHENS or the grammar arm by name (an unmeasured step may not claim a letter); the step omitted → the chain reads as a direct capture of original bytes and the arm fails by name; over-strictness: an archive capture's two-hop chain unchanged.
added: 2026-09-14 · CONDUCT #11 (DEC-75's "for CONDUCT to enact" converted to rows at the drain; the ruling is BOB #11's at Bob's delegation)

### REC-86 · done — landed `d60fb9a3`, merged by CONDUCT #4, 2026-09-18. **NARROW's PLANE HALF IS BUILT:** `op=narrowcandidates` lists the machine's proposals for one leg (reading positions, EXTRACT proposals, machine-marked rows), EVERY one labelled machine work and the read writing nothing; `op=narrow` is the member's act — a NEW basis version, born `suggested`, one leg STRICTLY narrower on the SAME capture, the old retained byte-for-byte, the narrowed leg ungraded until it earns (`grade_not_carried`). Anything claiming more precision than established is refused BY NAME (C-50: `NARROW_NOT_NARROWER`, `NARROW_OTHER_CAPTURE`, `NARROW_NAME`, `NARROW_NOT_A_MEMBER`). **D-350 CLOSED** (legs carry `content_id`). IC-123 RESOLVED ACCEPTED, I3 23.2.0 → 23.3.0. Eight-arm control `bio-plane/test/nc-rec86.mjs`. **NOT built, and said:** the UI affordance (delegated; also waits on UI-62's page picker, which the row never named), and narrowing a leg of the LIVE `basis:` block — refused with a sentence, because the live basis carries no partition to copy and building one would be the record inventing a partition nobody asserted (a DESIGN GAP in §18.1 / `CONTENT-EXTENT-DESIGN-SPACE.md` §5.3). **Two choices the worker made and named as reversible:** born `suggested`, and the copied partition asserted in the NARROWING member's name. **At merge CONDUCT declined one line of its framework edit** — the connection-pair row claimed the ON-POINT connection choice built; that is REC-120's, not this item's. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **waits on REC-97 (the `op=cite` widening UI-61 found missing) and UI-61 (the selection surface it needs first); its own IC on I3, minted at spawn.** — **Act 6 item 6 — NARROW (Bob's 5.3): a member makes an existing citation more specific — a new basis version against a narrower content row, the old retained; RECORD + UI.**
milestone: M4 — D-164, the content-extent primitive (RECORD)
interface: I3 — its OWN IC, filed at spawn, before building
depends-on: UI-61
scope: **BOB'S 2026-09-14 REFINEMENT IS FOLDED IN HERE BY CONDUCT #2 ON 2026-09-17, THREE DAYS LATE, AND HOW IT WAS LOST IS WORTH MORE THAN THE FOLD.** Bob's second pass on 5.4 ruled that **specificity of reference is WORKED FOR**: where an edge is at document grain, the assistant, a member or another means tries to find the specific passages; where the target mentions the entity more than once, **only the ON-POINT passages — to the point made at the referring end — are referred to.** The scope consequence for THIS row, in the entry's own words: **NARROW gains a machine-proposed candidate list the member chooses from.** The entry said *yours to fold at next touch* and there was no next touch; it sat in the BOB INBOX undrained for three days and **a fold owed at a touch that never comes is an act nobody performs** — the note-is-not-an-item class, arriving through the one channel built to move architecture between sessions. **A machine-proposed list is MACHINE WORK and the member's choice is the ACT**, so the candidate list may never narrow by itself: an uncited machine proposal is a PROPOSAL, never coverage. Original scope follows, unchanged. scope: the inbox entry's item 6 and IC-84's "NOT in this IC": a member act that narrows a leg — a new basis version whose leg targets a narrower content row of the same capture, the old version retained (never re-pointed: Bob's 5.8, an authored edge is never moved without a member's act — this IS the member's act); the UI affordance on the leg; the audit trail names both rows.
accepts-when: narrowing produces a new version with the narrower row and leaves the old version and its row untouched, driven through the op and the UI harness; `cd bio-plane && npm run test:battery` green — measure your own baseline (~187/187 · ~11,343 with all three member installs); `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0, with a control-plane assertion for every new op or check in the same turn; `node civicos-ui/test/run.mjs` from the repo root, unpiped; `op=audit` clean in scratch; plancheck 0 fail
NEGATIVE CONTROL: run and recorded — a narrowing that would move the OLD version's target → refused (5.8 driven); a "narrowing" to a WIDER extent → refused by name; over-strictness: promote without narrowing unchanged.
added: 2026-09-14 · CONDUCT #10 (draining the 2026-09-14 BOB #10 act-6 entry: the nine items scoped there, in its dependency order; the entry's item text stays in the inbox record as the scope's authority beside IC-83/IC-84 and the D-164 study — a notification, not a second copy)

### CPDF-19 · done — landed `ee5dbc4d`, merged at `7a21feee` by CONDUCT #4, 2026-09-18. **Read-time re-extraction to tier 3 is BUILT, opt-in: `op=pdfstructure&ocr=1` re-reads a capture the record already holds, and without the flag the answer is byte-identical.** The acquire path's tier-3, text-unit, reading and entity blocks now live in shared functions so acquire and read obey ONE rule. **D-319 CLOSED; TWO DEFECTS FOUND IN THE MOVED CODE AND CLOSED:** **D-417** — the calibration lookup asked the DO for `?op=calibrations` while the store routes on the PATH, so every OCR chain named no calibration, indistinguishable from a store holding none; **D-418** — `tier3_candidate` was set on ANY non-empty tier-3 note, and since D-252 that note carries the SUCCESS sentence too, so a document a member transcribed page by page stayed on the frontier as still wanting OCR, permanently. IC-126 RESOLVED ACCEPTED, I3 23.3.0 → 23.4.0. Eight-arm control `bio-plane/test/nc-cpdf19.mjs`. **Design §8 said *exactly one observation*; the shared writer writes three** — the suite pins the real delta and the gap is in the design's Incomplete. **Left open, named:** whether a re-read should mint a bundle version; captures already stored with the stale D-418 flag or a null calibration are NOT rewritten. **FW-20 is now unblocked** on this seam. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **BREADTH item 5 — D-319: read-time re-extraction to tier 3, OPT-IN: the seam on the read op behind the flag and the existing predicates; the new chain replacing the text units and staling the content rows; the content-level observation; the frontier read as the candidate list; refused by name with no member bound.** — waits on REC-94 (the writer and the read) and REC-91 (the units it replaces — ships without them if not yet landed, replacing nothing and SAYING so); CONTENT-PDF + RECORD.
milestone: M2 — D-319 closed
interface: I3 — the flag, additive, its OWN IC minted at spawn
design: `docs/development/EXTRACTION-BREADTH-DESIGN.md` §5.1 and §7 row 5; D-319 (`ocr-member-e2e.test.mjs` pins the gap today — CORRECT that pin, never exempt it)
depends-on: REC-94; REC-91 (soft, per the table)
scope: exactly BREADTH §7 row 5; D-319's disposition moves with the commit.
accepts-when: with the flag, a read reaches tier 3 and the chain replaces the units and stales the content rows with the observation written; without the flag, byte-identical to today; a call with no member bound refused by name; `cd bio-plane && npm run test:battery` green own-baseline; `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0, with a control-plane assertion for every new op or check in the same turn; `node civicos-ui/test/run.mjs` from the repo root, unpiped, exit 0; `node tools/plancheck.mjs --local` 0 fail; the home design's front matter moved in the same commit if its stated completeness changes.
**SHARED VOCABULARY, RULED 2026-09-14 by CONDUCT #11 at BOB #11's raising — and it is a MECHANISM, not a convention.** REC-92 (the `passage:` arm's `scope` tally), REC-94 (the content-level writers and the per-capture state) and CPDF-19 (D-319's re-extraction, which moves a capture between states) all write or read ONE content-axis state — `CONTENT-SEARCH-DESIGN.md` §4.4's `indexed_full` / `indexed_partial` / `indexed_none` / `not_extracted` — and their designs say each MAY ship without the others, stating what it cannot yet answer. **Three items spelling one vocabulary in three weeks is the id-collision shape one level up, and the vigilance fix is already known to fail, so: the state is ONE EXPORTED CONSTANT in the plane, beside the observation writer, imported by every reader and writer of it; no item spells a member of that set as a literal, and no suite pins one — the suites pin the CONSTANT, so a divergent spelling is a build error rather than a review finding.** The FIRST of these to reach integration EXPORTS it and names it in its release line; every later one IMPORTS it and says so. **CONDUCT's act at that first integration, stated with its actor:** fold the landed spelling into all three governed designs' own sections in the same turn (`CONTENT-SEARCH-DESIGN.md` §4.4, `OBSERVATION-LOG-DESIGN.md` §4.2, `EXTRACTION-BREADTH-DESIGN.md` §5.1) and amend the other two rows to name the constant rather than the prose — because a spelling that lives only in three documents' prose is the defect BOB #11 raised, and folding it is what closes it.

NEGATIVE CONTROL: per `EXTRACTION-BREADTH-DESIGN.md` §8 — the staling neutered → a re-extracted capture's content rows read fresh and the arm fails by name; the flag ignored → the opt-out arm fails; over-strictness: the default path digest-identical to the pre-item answer.
added: 2026-09-14 · CONDUCT #11 (draining the 2026-09-14 BOB #11 inbox entry "Part II §18 pieces 2–4 designed" — one row per decomposition-table line; the TABLE is the scope's authority and this row is the pointer, not a second copy; ids minted with `node tools/mintid.mjs`)

### REC-104 · done — landed `fb16dd99`, merged by CONDUCT #4, 2026-09-18. **The `content:` arm's `chain` filter answers off `chain_kind`, a VIRTUAL GENERATED column over `chain` with an index on `(chain_kind, bundle_id)` — a stale value is impossible BY CONSTRUCTION (SQLite refuses a write naming it and recomputes it on every `chain` change), and there stays ONE definition of "last step" in the plane.** The read-time parse is gone. **Measured (M-52), same-run proportions because the machine was never quiet:** −37.6% at 5,000 bundles, −43.4% at 20,000, against a 6–7% noise floor — **and the saving is the INDEX; without it the column is no faster than the parse.** REC-90's absolute figures are NOT comparable across days and the worker said so rather than claiming a win against them. Every `content:` answer byte-identical (38-question digest). Eight-arm control `bio-plane/test/nc-rec104.mjs`. **Found:** the row cited REC-90's figures as M-21; they are M-23. **Surfaced for FW-19's owner → `REC-121`.** **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into an isolated `agent-*` worktree (wave 1 of CONDUCT #4). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **THE `chain` FILTER IS A READ-TIME JSON PARSE WHILE §4.1 GIVES `capture_text` A `chain_kind` COLUMN FOR THE IDENTICAL QUESTION — measured as the SLOWEST filter on the table and the one query no index improved.** — waits on REC-91 (the mint path where the column would be written); owner RECORD.
milestone: M3 — the content grain answers its own questions at column speed
interface: none expected — a stored column feeding an existing published filter; the answer shape does not move, and if it does the IC is minted before building
design: `docs/development/CONTENT-SEARCH-DESIGN.md` §4.2 (the `content:` arm, whose Incomplete entry this is) read against its own §4.1, which takes the OTHER choice for the same question and says why
depends-on: REC-91 (SEARCH item 4 — `capture_text` and its mint path; the column is written where the row is minted, and that path is not REC-90's region)
scope: **REC-90's DESIGN GAP, rowed rather than left in a report. The interesting part is that this design already CONTAINS the answer and takes the opposite choice one section over**: §4.1 gives `capture_text` a `chain_kind` COLUMN and states its reasoning; §4.2's `chain` filter parses JSON at read time. **It shipped as the parse because the MINT PATH was outside REC-90's region — a scoping fact, not a judgement that the parse is right**, and saying which of those it was is the whole reason this row exists. Give the content row the same column treatment, written where the row is minted, and **retire the parse rather than leaving both** — two ways to answer one question is the mirror-and-drift class this project refuses everywhere else.
accepts-when: the `chain` filter answers off a column, driven through `op=meaningrows`, with the parse REMOVED rather than kept beside it; **the improvement is MEASURED against REC-90's own recorded figures at BOTH corpus sizes, not one** — M-23 records a filter that reversed sign between 5,000 and 20,000 bundles against a 20.5% noise floor, so a single-size result proves nothing here; a stale `chain_kind` is impossible by construction or is refused by name; `cd bio-plane && npm run test:battery` green own-baseline; `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0; `node civicos-ui/test/run.mjs` from the repo root, unpiped, exit 0; `node tools/plancheck.mjs --local` 0 fail; §4.2's Incomplete entry moves with the commit.
NEGATIVE CONTROL: run and recorded — the column made stale against the chain it describes → a query returns a row whose chain no longer matches and the suite FAILS by name (the arm that proves the column is not merely faster but still TRUE); over-strictness: every `content:` query answers byte-identically to its pre-item answer on the committed fixture, since this changes how the question is answered and never which rows answer it.
added: 2026-09-15 · CONDUCT #11 (REC-90's `DESIGN GAP:` heading, converted to a row at integration. Queued behind REC-91 rather than runnable, and the row says so.)

### REC-120 · done — landed `a531311f` (tip `862f0bdd`), merged by CONDUCT #4, 2026-09-18. **THE OVERCLAIM IS CLOSED: citing page 9 of M-51's document (where the ordinance really is) now answers UNDETERMINED with its reason, and page 7 (which never mentions it) still answers a definite outside** — driven through `op=promote`/`op=resolve`/`op=connect`/`op=connections`, the one-mention document byte-identical to a golden captured on the pristine tree. Act (2): the pair states it is the strongest-graded mention, NOT chosen, with its tie-break made REAL (`ORDER BY capture_sha, ref` — before, ties fell to row order). IC-129 RESOLVED ACCEPTED (I5 1.18.0, I3 23.5.0). **IT WENT ONE STEP PAST ACT (1) AND SAID WHY — the row contradicted itself:** its negative control demanded that flipping the tie-break not move page 9 to *reached*, which act (1) alone cannot satisfy; the rule that survives the flip is *a reach won on a tie is also undetermined*, so page 2 moved from *reaches at A* to UNDETERMINED. Accepted: an answer that flips on one character is an equality that costs nothing. **Cost named:** until act (3) exists, a partial citation of any multi-mention document stays undetermined — honest, and no definite answer. FW-21's probe now reports GROUND BROKEN on page 2 (delegated to FRAMEWORK). `reading_refs` holds one position per reference string, so a reference read on several pages counts once (recorded in D-161, not driven). Act (3) → `REC-122`. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 as a ONE-FOR-ONE REFILL under BOB #14's product-only rule. DEPENDS-ON CHECKED AGAINST THE CODE AT SPAWN, on `23f8df76`: `Store.deriveConnections` and `connectionGradeForContent` present in `bio-plane/src/store.mjs` (FW-17's pair, the thing this row corrects). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **A CONNECTION'S GRADE AT A CITED PART ANSWERS A DEFINITE "OUTSIDE" WHEN THE TRUTH IS UNDETERMINED — measured by FW-21 (M-51): citing the page where an ordinance really is (p.9 of a document that also mentions it on p.2 at the same grade) and citing a page that never mentions it (p.7) return IDENTICAL counts and the same firm *"none of them reaches this citation"*. The record claims more than it can support, on a surface a member reaches today.** — waits on nothing; owner RECORD.
milestone: M4 — D-164, the content-extent primitive (RECORD)
interface: I3 for acts (1)–(2) only if a field is added or a reason string moves — mint an IC with `node tools/mintid.mjs IC` BEFORE building if so; act (3) is OUT of this row's scope (it changes I5 and I3 and needs its own row and IC)
design: `docs/architecture/BIO_Content_Framework_v0_10.md` §14.5 (the connection pair and what it is NOT, corrected by FW-21 2026-09-18) and §17, read with `DEBT.md` D-161 (its open on-point half, narrowed by FW-21 with the measurement and the three acts) and `MEASUREMENTS.md` M-51.
depends-on: none — FW-17 (the pair kept on `connections`) is DONE; verify `Store.deriveConnections` and `connectionGradeForContent` in `bio-plane/src/store.mjs` on your tree before building.
scope: **D-161's open half, acts (1) and (2), and NOT act (3).** (1) **THE OVERCLAIM, first and alone if it must be:** `connectionGradeForContent` answers UNDETERMINED — stated, with its reason — rather than a definite *outside* whenever ANOTHER mention of the same entity in that document falls inside the cited part and the stored pair is not that mention; *outside* stays the answer only when no mention of the entity in the document falls inside the part. (2) **The pair says what it is:** the stored pair states that it is the STRONGEST-GRADED mention (a machine selection by grade, with its tie-break named — today it is row order, which is not a basis) — never presented as the ON-POINT pair. **(3) is a SEPARATE ROW, not written until this lands:** the member's act of CHOOSING the on-point pair (Bob's 2026-09-14 refinement, §5.4) changes I5 and I3 and is RECORD + UI. FW-21's probe `tools/fw21-onpoint-probe.mjs` reproduces the defect; it is the starting fixture. Take ids with `node tools/mintid.mjs <NS>`.
accepts-when: FW-21's two citations (p.9 and p.7 in M-51's fixture) no longer return the same answer — p.9 reads UNDETERMINED with its reason, p.7 still reads *outside*, driven THROUGH THE OP a member reaches (not the store alone); a document with ONE mention is unchanged byte-identically (over-strictness); the pair's own record states it is strongest-by-grade; `DEBT.md` D-161 narrowed again in the same commit; `node tools/plancheck.mjs --local` then BARE; `node scripts/coverage.mjs --strict` exit read UNPIPED; `cd bio-plane && npm run test:battery` green own-baseline, read by its COMPLETION LINE.
NEGATIVE CONTROL: run and recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — the undetermined branch removed → p.9 answers *outside* again and the arm FAILS naming the fixture; FW-21's own control (`>` → `>=` in the pair's tie-break) must NO LONGER move the p.9 answer from undetermined to *reached*, since an answer that flips on a tie-break carries no relevance. **State how a liar would satisfy this before stating what it checks:** the cheapest green is to answer UNDETERMINED for EVERY multi-mention document regardless of where the cited part falls — no overclaim, and a member now gets no answer where the record genuinely knows one (p.7). Both directions are asserted.
added: 2026-09-18 · CONDUCT #4 (**from FW-21's report, which found the overclaim by DRIVING THE OPS rather than reading the rows, and routed it to CONDUCT as "one act owed" in D-161.** It outranks every other open row in the RECORD lane on this project's own rule: a record-overclaim a member can hit today outranks everything else. Id minted with `node tools/mintid.mjs REC`, run bare.)


**HEADING RESTORED 2026-08-10 by CONDUCT — it was DELETED, with four others, by the
closed-item roll at `cc99ec1`.** The roll moved 195 done/superseded items out correctly and
took five AREA headings with them (`RECORD`, `CONTENT-PDF`, `FRAMEWORK`, `CONTENT-HTML`,
`DIST`, `UI` — 11 headings down to 6), because every item under those headings happened to
be closed. **No item was lost and no status changed; what was lost was which AREA each
surviving open item belongs to and which areas hold the two slots** — so `REC-69` and
`REC-15` read as M0 lane items, `DIST-2`/`DIST-3`/`UI-53` read as CAPTURE items, and the
queue could not answer "what is active" at all. Restored verbatim from `c7fc5c3`, the
commit before the roll. **The lesson is the roll's, not the roller's: a bulk move keyed on
ITEM status silently deletes any SECTION whose items are all closed, and a section is not
an item.** Recorded on the class rather than the instance.

Owns the store core and retrieval (`PARALLELISM.md`). Claim it in `CLAIMS.md` before
editing; **`store.mjs` needs `grep -a`, and its size is MEASURED not quoted** — `wc -l <
bio-plane/src/store.mjs` reads **25,861 (2026-08-10)**. The verbatim restore of this
paragraph carried "16,287 lines (MEASURED 2026-08-04)", which was the FOURTH stale instance
of this figure and is exactly what `CLAUDE.md`'s own trap entry now forbids quoting — so the
number is replaced by the command that produces it, and the date stands as a reading rather
than a fact. CAPTURE holds the link/capture/task/reachability functions, so name paths
precisely.

The 2026-08-01 handover run order (REC-10 → REC-19 → REC-11 → REC-13 → REC-12 → REC-14) is
DONE, and the area drained on 2026-08-04 — UI took the freed slot. It is re-promoted
2026-08-05 because **D-200 is RECORD ground and had no owner**: ten live bundles claim a
provenance route they cannot show, which is live record state no other area can touch.

### REC-87 · done — landed `6cd2a450`, merged by CONDUCT #4, 2026-09-18. **TRANSCRIBE's PLANE HALF IS BUILT:** a member types a selected portion's text as a `typed(member)` derivation step, cap UNDETERMINED by declaration; a SECOND member may attest it; **the typist attesting their own typing is refused by name (C-52.9)** and excluded at every read. A typing is raised only by its OWN attestations, never the capture's, and is not staled by a re-read of its capture. IC-127 (I2 2.5.0) and IC-128 (I3 23.6.0) RESOLVED ACCEPTED. **The step kind is spelled `typed`, not the row's `member`** — `member` is already a `content:` query word and the collision made an existing query ambiguous; the worker caught it in its own battery. **A REAL DEFECT FOUND BY A CONTROL ARM AND FIXED:** C-45.2 refused a typing of any capture with no extraction chain — Bob's own case, a scan no engine could read. **At integration CONDUCT found the construct map's `4.transcribe` ABSENT probe BLIND** — it searched for a `member:` step kind and passed while the construct was built; corrected to BUILT with positive probes, plus `4.transcribe-ui` ABSENT. Eleven-arm control `bio-plane/test/nc-rec87.mjs`. **Not built, named:** the UI act (delegated); office portions (cells, paragraphs, shapes) are refused (C-52.5) because the attestation extent grammar is `document | page | region` only; *nothing prefilled* is enforceable only in the UI. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 as a ONE-FOR-ONE REFILL under BOB #14's product-only rule. DEPENDS-ON CHECKED AGAINST THE CODE AT SPAWN, on `23f8df76`: UI-61 done and REC-97's `op=cite` widening present; REC-86 (same `store.mjs` region near `op=cite`, same I3) MERGED at 23.3.0, so this starts from its tree; CAP-10's `convert` step kind is present, so a second new I2 step kind resolves against I2 2.4.0. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **waits on REC-97 (the `op=cite` widening UI-61 found missing) and UI-61; its own IC on I2 (a step kind) and I3, minted at spawn.** — **Act 6 item 7 — TRANSCRIBE (Bob's 5.2): a member selects a portion and types its text — step kind `member(handle)`, cap undetermined and STATED, attestable by a SECOND member, the transcriber's own attestation refused by name; RECORD + UI.**
milestone: M4 — D-164, the content-extent primitive (RECORD)
interface: I2 (a new step kind — FRAMEWORK dormant, CONDUCT answers-for) and I3 — its OWN IC, filed at spawn
design: `docs/development/CONTENT-EXTENT-DESIGN-SPACE.md` §5.2 ("Is a member's transcription a derivation or a verification?" — Bob's 5.2 ruling, which this row builds) and §6 (the mechanism), under `BIO_Content_Framework_v0_10.md` Part II §14.4 and §18 piece 1
depends-on: UI-61
scope: the inbox entry's item 7 and the study §5.2: a transcription is a DERIVATION step of kind `member(handle)` on the content row's chain with cap undetermined and stated; a second member may attest it (raising the ceiling as any attestation does); the transcriber attesting their own transcription is refused by name (the equality-that-costs-nothing rule, one altitude up); the UI act is a portion selection plus a text field, nothing prefilled.
accepts-when: a member transcribes a portion, a second member attests it, the ceiling moves as ruled, and the self-attestation is refused by name — all driven; `cd bio-plane && npm run test:battery` green — measure your own baseline (~187/187 · ~11,343 with all three member installs); `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0, with a control-plane assertion for every new op or check in the same turn; `node civicos-ui/test/run.mjs` from the repo root, unpiped; `op=audit` clean in scratch; plancheck 0 fail
NEGATIVE CONTROL: run and recorded — the self-attestation refusal neutered → the ceiling rises on one member's word (must FAIL); a transcription with no portion selected → refused; over-strictness: existing attestation paths byte-identical.
added: 2026-09-14 · CONDUCT #10 (draining the 2026-09-14 BOB #10 act-6 entry: the nine items scoped there, in its dependency order; the entry's item text stays in the inbox record as the scope's authority beside IC-83/IC-84 and the D-164 study — a notification, not a second copy)

### CPDF-18 · done — landed `84954ce4`, merged by CONDUCT #4, 2026-09-18. **PDF IMAGES ARE CONTENT:** the structure op emits `image {page, rect}` for every image a page PAINTS (a second content-stream walk interpreting `q`/`Q`/`cm`/`Do` and Form `/Matrix`, inline images reported), a row mints from that reference as BYTES, and its crop is a derived rendition (`pdf-worker/src/imagecrop.mjs`), driven acquire → pdfstructure → promote → content. **TABLE RECOGNITION MEASURED NO-GO (M-55)** against criteria written INTO THE PROBE BEFORE RUNNING IT: reproducible 15/15, no false tables on 13 table-free pages — and a RULED 3×3 table NOT FOUND AT ALL. No table reader built, as the row required. Booked as IC-124's addendum, I2 2.6.0. Five-arm control `bio-plane/test/nc-cpdf18.mjs`. **Debt:** D-419 (no op asks for the crop), D-420 (an image row is checked against the page COUNT, not against what the page paints). **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 as a ONE-FOR-ONE REFILL under BOB #14's product-only rule. DEPENDS-ON CHECKED AGAINST THE CODE AT SPAWN, on `23f8df76`: FW-19 MERGED — the `image` reference and the grammar that admits `image {page, rect}` are in the tree (IC-124/IC-125); CPDF-19 finished and its branch moves the acquire path's tier-3 code into shared functions, so if CPDF-18 edits acquire code it rebases onto CPDF-19 once that merges. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **BREADTH item 4 — PDF images as content, and the table measurement: `image` as `{page, rect}` from the structure op; the crop as a derived rendition; and the GO/NO-GO measurement of a table-recognition step on the runtime (reproducibility of the structure across runs, cost) recorded BEFORE any table reader is rowed.** — waits on FW-19.
milestone: M2 — CONTENT-PDF's breadth: images reachable as content; tables measured before designed
interface: none (the `image` reference is FW-19's IC)
design: `docs/development/EXTRACTION-BREADTH-DESIGN.md` §3.3, §3.4 and §7 row 4 (BREADTH's own Incomplete §3.3 is the measurement half)
depends-on: FW-19
scope: exactly BREADTH §7 row 4; the measurement is recorded in MEASUREMENTS.md with its verdict and NO table reader is built here regardless of the verdict.
accepts-when: an `image` content row mints from the structure op with its crop as a derived rendition, driven; the table measurement is in MEASUREMENTS.md with a GO/NO-GO and BREADTH §3.3's front matter moved; `cd bio-plane && npm run test:battery` green own-baseline; `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0, with a control-plane assertion for every new op or check in the same turn; `node civicos-ui/test/run.mjs` from the repo root, unpiped, exit 0; `node tools/plancheck.mjs --local` 0 fail; the home design's front matter moved in the same commit if its stated completeness changes.
NEGATIVE CONTROL: per `EXTRACTION-BREADTH-DESIGN.md` §8 — the rect dropped from the reference → the crop cannot be derived and the arm fails by name; over-strictness: a page with no images yields no rows and says so.
added: 2026-09-14 · CONDUCT #11 (draining the 2026-09-14 BOB #11 inbox entry "Part II §18 pieces 2–4 designed" — one row per decomposition-table line; the TABLE is the scope's authority and this row is the pointer, not a second copy; ids minted with `node tools/mintid.mjs`)

### REC-100 · done — landed on `worktree-agent-a249f66820def3efd-rebased` at `4924babd`, merged by CONDUCT #4, 2026-09-18. **D-366 CLOSED: a rollup's PRESENT now points at the row that makes it PRESENT — the plane computes the referent in the same grouped read as `#aiRunSearchState`, the `run` carve-out is DELETED, and a forged referent (another run's seq, a non-PRESENT row, a LATER seq) is refused by name.** The falsifier held: `#aiRunSearchState` reads PRESENT iff a non-terminal PRESENT row exists, so the referent always exists when needed and the airunclose deadlock cannot arise; the reaper is driven. **THE WORKER FOUND THE CONSEQUENCE NOBODY HAD PRICED AND CONDUCT HELD THE MERGE ON IT:** deleting the carve-out makes `op=airuntick` refuse what `agent-worker`'s `stepLog` sent, and agent-worker never read `refused[]` — after a deploy, model-judged PRESENT steps would have vanished from run logs silently (its suites mock the op). **So IC-130 is MAJOR (I3 24.0.0) and the consumer migration landed WITH it:** `stepLog` records a model-judged PRESENT as `LOOKED_INDETERMINATE` with the judgement kept verbatim (a model-supplied referent would cost nothing to produce; a pointer to *the latest PRESENT row* would claim a basis nobody named), counted in `present_unbacked`; and every refused entry is now reported in `log_refused`, in the plane's own words — **the class closed, not only the instance**, tested against the REAL plane under miniflare. **DEPLOY CONSTRAINT: the plane and agent-worker ship together** (IC-130's resolution). Nine plane arms + two agent-worker arms in `bio-plane/test/nc-rec100.mjs`; the `aw-refused` arm first came back GREEN because the test compared two fields the same code fills (0 = 0) — corrected to count against the trace and the real log. **Found stale in the design and routed to BOB (§4.4 Incomplete):** the reaper writes through `#aiRunTerminate`, not a wake entry; a wake entry is a non-terminal rollup that a later terminal row can point at, forming a chain; a rollup reads `backed` even when its referent is a legacy undetermined row. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 as a ONE-FOR-ONE REFILL under BOB #14's product-only rule. DEPENDS-ON CHECKED AGAINST THE CODE AT SPAWN, on `23f8df76`: `#aiRunSearchState`, `#aiRunTerminate`, `#aiRunReap` and `OBS_PRESENT_NO_REFERENT` present in `store.mjs`; REC-86 (the concurrent I3 change it was sequenced behind) MERGED, I3 at 23.3.0. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **UNBLOCKED 2026-09-18 BY BOB #14's RULING (D-366; `OBSERVATION-LOG-DESIGN.md` §3 and §4.4) AND RE-SCOPED BY CONDUCT #4 TO THE BUILD.** A rollup's `PRESENT` — the terminal entry (`#aiRunTerminate`) and the reaper's wake entry (`#aiRunReap`) — carries `result_kind = observation` with `result_ref` = the `seq` of the LATEST non-terminal `PRESENT` row of the same `(authority_kind, authority)`, computed by the PLANE in the same read as `#aiRunSearchState`, never supplied by a caller. **THE BUILD, four acts:** (1) the two writers take the supporting `seq` and write the `observation` referent; (2) C-22.10 gains an `observation` arm — the ref must resolve to an EARLIER `PRESENT` row of the SAME authority, else refused; (3) the `run` carve-out is DELETED; (4) pre-existing bare `run` rows read back stated UNDETERMINED through REC-113's projection, never filled. **Interface:** an ADDITIVE IC on I3 (`result_kind` gains a value on `op=airunlog`'s read) is minted with `node tools/mintid.mjs IC` BEFORE building. `agent-worker`'s `stepLog` half is unchanged and stays with its delegation in `CLAIMS.md`. **Controls owed:** a rollup naming another run's `seq`, a non-`PRESENT` row, or a LATER `seq` — each refused BY NAME; `op=airunclose` SUCCEEDS with the carve-out gone for a run that observed `PRESENT` (the deadlock arm); the carve-out restored → the suite says so; the REAPER driven, which D-366 records as owed and no arm reaches today. **NOT decided and NOT in scope:** `partial`'s referent stays in the design's incomplete list. **Falsifier — go to it first:** BOB verified at the code that `#aiRunSearchState` reads `PRESENT` iff a non-terminal `PRESENT` row exists; re-verify that on your tree before building, because if it is false the referent can be absent when needed and the deadlock returns. **Prior state, kept as the record: blocked** — **THE CARVE-OUT STANDS, AND THAT REFUSAL IS THE RESULT.** Work landed at `fcd4cf9b` + `e2af2534`, merged at `ca3836f9` by CONDUCT #1, 2026-09-16 — **but the row is NOT `done`, because it was not done as specified and saying otherwise is the wrong-status class this queue keeps paying for.** **EVERY PRECONDITION THE ROW AND D-366 NAMED WAS WRONG, measured on the tree:** REC-95 landed and did NOT close it (its three writers write under `derive`, not `run`, and REC-95 recorded the correction was owed here), and the drain exits were never the blocker — the capture exit has carried `resultRef: r.sha` since REC-93. **The three writers that actually emit a bare `run` PRESENT were named NOWHERE:** `#aiRunTerminate` and `#aiRunReap`, both taking state from `#aiRunSearchState` — **a ROLLUP, which has no referent BY CONSTRUCTION** — and `agent-worker`'s `stepLog`. **AND THE CONSEQUENCE IS DRIVEN, NOT ARGUED: with the carve-out widened, `op=airunclose` answers `terminated: false, ok: false, code: OBS_PRESENT_NO_REFERENT` and the terminal entry is never written — a run that observed anything PRESENT cannot be closed at all.** Deleting the condition today is a LIFECYCLE DEADLOCK, not a tightened fence. **THE MEASUREMENT THE ROW PRESCRIBED WOULD HAVE GIVEN THE WRONG ANSWER, and this is the finding to keep:** the accepts-when required the `overstrict` arm to come back EMPTY, and that arm runs ONE SUITE — it cannot see either rollup writer, nor `agent-worker`, whose suites MOCK `op=airuntick`, so a widening breaks that integration with the whole battery green. **An empty arm would have licensed precisely the wrong conclusion**; reading the writers is what found all three. The blindness is now in the arm's own declaration and `I3` was added so it GRADES the rollup rather than printing it. **TWO ARMS ARE RECORDED AS NOT RUN rather than quietly dropped** — *carve-out restored* and *backfill filled* both presuppose a widening and a backfill deliberately not done. Baseline confirmed CONDUCT #1's exactly at 206/206 · 12,887; `REGISTER_FLOOR` exact and unmoved. **AND A MEASURED FACT THAT CHANGES A ROUTINE ASSUMPTION: a COMMENT-ONLY plane-source change still moves `dist/`**, because the bundle is unminified and retains comments — its `fleetbundles` went 83/4 until it rebuilt. **BLOCKED ON A DESIGN RULING THAT IS NOT A WRITER'S TO MAKE:** what a terminal entry's referent IS, or the stated grounds for exempting a ROLLUP — narrower than today's blanket carve-out. Routed to BOB then; ANSWERED 2026-09-18 (see above). The read half is **REC-113** and the `agent-worker` half is delegated in `CLAIMS.md`. **Prior state, kept as the record: running, then queued.** Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; **if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH (the preamble's falsification rule), and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **D-366 CLOSED BY ITS WRITERS, which is the only way it closes: C-22.10 does not fire on `authority_kind = 'run'`, so the authority that writes the MOST observations is the one exempt from the refusal that says a `PRESENT` must name what it found.** — waits on REC-95 (the meaning-level writers that carry the referents; until they exist the carve-out is load-bearing and deleting it would refuse correct work).
milestone: M3 — the observation log's own refusal reaches the authority that writes most of it
interface: none expected — a refusal widening over rows the plane already writes; if `op=airuntick`'s accepted shape narrows, that is I3 and the IC is minted before building
design: `docs/development/OBSERVATION-LOG-DESIGN.md` §3 (the rule, written unconditionally) and §4.4 (the fold that cannot satisfy it); D-366 in `DEBT.md` carries the measurement, the cost and what closing it takes
depends-on: REC-95 (LOG item 3 — the reader run, the resolution attempt, the connection derivation: the writers that give a run's `PRESENT` something to point at)
scope: **REC-93's own residue, rowed rather than left in its report — the delegation named RECORD as the actor and a later item as the place, and this is that item.** Once no live writer emits a bare `run` `PRESENT`, the carve-out in `checkObservation` becomes ONE DELETED CONDITION; the capture drain exit is already the model (it carries `resultRef: r.sha`). **THE BACKFILL IS THE PART THAT CANNOT BE DONE AND MUST THEREFORE BE SAID:** rows already written have no referent anywhere in the record, so they are STATED as undetermined rather than filled — inventing one to get past a gate is the exact failure `CLAUDE.md` names, and it is the failure this table was built to prevent. **Do not widen the check before the writers land**; the instrument that shows why is `test/nc-rec93.mjs`'s `overstrict` arm, which widens C-22.10 to every authority and names the correct work that would refuse today.
accepts-when: no live writer emits a `run` `PRESENT` with no referent (measured by the `overstrict` arm coming back EMPTY rather than by reading the writers); C-22.10's carve-out deleted and the refusal driven over `authority_kind = 'run'`; pre-existing bare-`run` rows read back through `op=airunlog` with their coverage claim STATED as undetermined, never filled and never dropped; D-366's disposition moves with the commit; `cd bio-plane && npm run test:battery` green own-baseline; `node scripts/coverage.mjs --strict` DIRECTLY, `$?` unpiped, exit 0; `node civicos-ui/test/run.mjs` from the repo root, unpiped, exit 0; `node tools/plancheck.mjs --local` 0 fail; §3's and §4.4's reconciliation moves in the same commit if what they state changes.
NEGATIVE CONTROL: run and recorded — the carve-out restored → a bare `run` `PRESENT` is accepted again and the suite says so BY NAME (the arm that proves the gap was real); **the arm this row exists to prevent** — the backfill filled with a derived referent instead of STATED undetermined must FAIL, because a satisfied gate over an invented fact is the defect wearing the fix's clothes; over-strictness: REC-96's completeness statement over a case whose rows are all referent-carrying is byte-identical to its pre-item answer.
added: 2026-09-15 · CONDUCT #11 (REC-93's DELEGATION act 4 converted to a row during a worker-wave sweep of the CLAIMS register — the delegation named its actor and its dependency and had no row, which is the note-is-not-an-item class this queue has now learned six times; it is queued behind REC-95 rather than runnable, and saying that on the row is the point)

### REC-123 · done — landed `19ff57cb`, merged by CONDUCT #4, 2026-09-18. **THE TRACE FOUND A LIVE TRUST-OF-THE-RECORD DEFECT: an `ai` credential whose scope named `op=ratify` or `op=caseratify`, carrying a registered member's valid signature, PUBLISHED the finding and COMMITTED the case — and the record named the MEMBER (iris) as the one who acted.** Both now refuse it BY NAME at the handler (C-32.12, C-32.13), not only at the scope check a broader scope would pass. Every other attest path was already fenced (`attesttext`, `transcriptionattest` by C-35.10) or out of a machine's reach (C-29.9), and `op=attest` is left open ON THE DESIGN'S OWN TEXT (`BIO_Intake_Doctrine_v1_1.md` §3: automatable, required of the fetch layer). **The suite DISCOVERS the op list three ways rather than trusting the row** and fails if anything found is untraced; it names its own blind spot (an act that vouches under none of those verbs, rung or stamp). `11.machine-fence` UNDETERMINED → BUILT. IC-132 MAJOR, I3 25.0.0. Four-arm control incl. OVER-STRICTNESS (`bio-plane/test/machine-attest.control.mjs`). **D-421 ROUTED TO BOB:** operator ADMIN/MEMBER/PROBE tokens still ratify with a member's signature; rule 4's wording covers them. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 (BOB #14's build order; a one-for-one refill of a freed product slot). DEPENDS-ON CHECKED AGAINST THE CODE at spawn: the `MACHINE_CANNOT_*` family present in `bio-plane/checks/bio-checks.mjs`; `11.machine-fence` reads UNDETERMINED in `construct-status.json`. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **CAN AN `ai` CREDENTIAL ATTEST OR RATIFY? `construct-status.json` 11.machine-fence reads UNDETERMINED because nobody has TRACED it: authored acts are fenced by the `MACHINE_CANNOT_*` family, but the attest and ratify handlers were never followed below the scope check. A machine attesting the record is the trust-of-the-record question itself.** — waits on nothing; owner RECORD. BOB #14's build order, item 1.
milestone: M3 — the record's own refusals reach every authority that writes it
interface: none for the TRACE; I3 if the trace finds an unfenced path and the fix adds a refusal — then mint an IC with `node tools/mintid.mjs IC` BEFORE fixing, against I3 as read at resolution
design: `docs/architecture/BIO_Assistant_and_AI_Roles_v0_1.md` §3 (the rules, stated once — what a machine may and may not author) read with `docs/architecture/BIO_System_Design.md` §3 construct 11 and the claim `11.machine-fence` in `docs/architecture/construct-status.json`.
depends-on: none — the `MACHINE_CANNOT_*` family is on `main` (`bio-plane/checks/bio-checks.mjs`); verify.
scope: **TRACE FIRST, BY DRIVING, NOT BY READING:** mint an `ai` credential scoped to attest (and one scoped to ratify) and drive EVERY op that attests or ratifies — `op=attest`, `op=attesttext`, `op=textattest`, `op=transcriptionattest` (REC-87, new today), `op=ratify` and any case/edition ratification — through the control plane in a scratch store, recording for each: refused by name / refused by the scope check only / ACCEPTED. **Grep for every attest/ratify writer rather than trusting this list.** Then: every path that ACCEPTS a machine attestation or ratification is a defect — fence it with a named `MACHINE_CANNOT_*` refusal at the handler (not only the scope check, which a broader scope would pass), unless the design EXPLICITLY permits it, in which case cite the section and leave it. Move `11.machine-fence` to BUILT or to a named PARTIAL in `construct-status.json` with PROBES (an absence claim needs a search that must stay empty), then `node tools/status.mjs --write`.
accepts-when: a table in the suite and the row naming every attest/ratify op and its measured machine outcome; every op refuses a machine credential BY NAME below the scope check (or is cited as permitted by the design); `construct-status` updated and `--check` 0 drift; `node tools/plancheck.mjs --local` then BARE; `node scripts/coverage.mjs --strict` exit read UNPIPED; `cd bio-plane && npm run test:battery` green own-baseline, read by its COMPLETION LINE.
NEGATIVE CONTROL: recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — each new fence removed → a machine attestation is ACCEPTED and the arm FAILS naming the op; **over-strictness:** a MEMBER attesting through the same op still succeeds. **State how a liar would satisfy this before stating what it checks:** the cheapest green is to test only the ops this row names — the one attest path nobody listed is the one a machine will use.
added: 2026-09-18 · CONDUCT #4 (BOB #14's build order, 8008cde3, item 1 — "trace the attest and ratify handlers FIRST". Id minted with `node tools/mintid.mjs REC`, run bare.)

### REC-121 · done — landed `3b0fe5fd`, merged by CONDUCT #4, 2026-09-18. **`content:chain=undetermined` now returns only TEXT rows; an image cited as its own bytes answers under a new value, `content:chain=does-not-apply`, and `chain_last` says so rather than showing a null that looks undetermined.** Premise held first (three documents, four rows, all returned by `undetermined` on the unedited tree). **Decided at the site: a bytes row IS reachable by the `chain` filter** — otherwise a member looking by chain would never find it, which is the silent drop the row forbids. The row's LIAR was driven as an arm (`dropall`: undetermined checks pass, reachability FAILS 10). IC-131 MINOR, I3 25.1.0. **Found:** the answer digest covers 40 questions, not 38; two harness pristine-copy directories were never gitignored and got staged by `git add -A` — caught and ignored. **Measured and deliberately NOT fixed (out of scope — *no other answer may move*): the SAME mislabel on the CAP axis → `REC-127`.** **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4. BOB #14 RULED it INSIDE the build order and FIRST AMONG EQUALS: *a defect in a just-landed item IS a defect in the substrate; corrections to landed items outrank new items.* DEPENDS-ON CHECKED AGAINST THE CODE at spawn on `6e50b260`: `cited_as` (FW-19) and `chain_kind` (REC-104) both present in `bio-plane/src/schema.mjs`. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** — **A FLIP WITHOUT A SPAWN, CORRECTED 2026-09-18 by CONDUCT #4 and recorded rather than silently undone: this row read `running` on `origin/main` from `3fe0ce81` until this commit with NO worker ever started** — BOB #14's build order landed between the flip and the spawn and the spawn was never made; the verification step (*confirm the act took*) was skipped. It returns to `queued` because it is not on that order; whether a DEFECT CORRECTION to a just-landed item sits outside the order is routed to BOB. — **`content:chain=undetermined` MATCHES AN IMAGE CITED AS ITS OWN BYTES, whose chain is NULL BY MEANING ("does not apply") and NOT undetermined — so a member filtering for undetermined provenance is shown rows whose provenance question has no answer to be undetermined about.** Found by REC-104's worker, which correctly left it alone (its item must not change an answer). — waits on nothing; owner RECORD.
milestone: M4 — D-164, the content-extent primitive (RECORD)
interface: I3 — only if the filter's published vocabulary gains a value (e.g. a `does-not-apply` arm); mint an IC with `node tools/mintid.mjs IC` BEFORE building if so, against I3 as read at resolution
design: `docs/development/EXTRACTION-BREADTH-DESIGN.md` §3.2 (the `image` reference and `cited_as`, IC-125) read with `docs/development/CONTENT-SEARCH-DESIGN.md` §4 (the `content:` arm and its `chain` filter, as amended by REC-104).
depends-on: none — FW-19's `cited_as` column and REC-104's `chain_kind` column are both on `main`; verify both in `bio-plane/src/schema.mjs` before building.
scope: `content:chain=undetermined` stays `chain IS NULL` for TEXT rows and EXCLUDES `cited_as = 'bytes'` rows, which are reported under their own stated answer (does not apply) rather than silently dropped or silently counted as undetermined. **Decide at the site, with the reason written, whether a bytes row is reachable by the `chain` filter at all.** No other `content:` answer may move: REC-104's 38-question digest is the over-strictness pin.
accepts-when: a fixture with one TEXT row whose chain is null and one IMAGE row cited as bytes returns ONLY the text row for `content:chain=undetermined`, driven through `op=meaningrows`; the bytes row is reachable and labelled does-not-apply wherever the design says it should be; REC-104's digest byte-identical for every question not involving a bytes row; `node tools/plancheck.mjs --local` then BARE; `node scripts/coverage.mjs --strict` exit read UNPIPED; `cd bio-plane && npm run test:battery` green own-baseline, read by its COMPLETION LINE.
NEGATIVE CONTROL: run and recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — the exclusion removed → the bytes row reappears under `undetermined` and the arm FAILS naming it. **State how a liar would satisfy this before stating what it checks:** the cheapest green is to drop bytes rows from EVERY `content:` answer — the mislabel disappears and so does the image, which is a record losing a citation to fix a filter.
added: 2026-09-18 · CONDUCT #4 (from REC-104's report. A mislabel of provenance, not an absent feature — so it is a RECORD row, not debt. Id minted with `node tools/mintid.mjs REC`, run bare.)

### REC-125 · done — landed `7eb262e3`, merged by CONDUCT #4, 2026-09-18. **The operator's ADMIN, MEMBER and PROBE tokens are refused at `op=ratify`/`op=caseratify` even carrying a member's valid signature; a signed-in human session ratifies exactly as before.** **THE PRECONDITION WAS MET, WITH THE SEARCH STATED:** no procedure submits either op with a bearer token; the only live submitter is `setup.mjs`'s instance page. The refusal keys on HOW THE CALLER ARRIVED — the suite reads token classes from `classify()`, and a structural check fails if the guard ever names one. Six-arm control incl. the liar (`tokenstring`). IC-137 MAJOR, I3 26.0.0. **It exposed a false sentence of CONDUCT #4's own in IC-132** (corrected by appended note). **BOB #14 RULED the founder question:** a founder's password session stays ALLOWED (a human, and the only live publishing route); D-421's wording corrected to *a HUMAN's own authenticated session*; and the record must state who DELIVERED as well as who AUTHORISED → `REC-128`. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into REC-123's slot — a CORRECTION TO LANDED SUBSTRATE, inside the build order and ahead of new items (BOB #14's rule). DEPENDS-ON CHECKED AGAINST THE CODE at spawn: REC-123's fences (C-32.12, C-32.13) and `machine-attest.test.mjs` present on the gated tree. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** — **D-421 DECIDED BY BOB #14 (BOB lane, applying rule 4, no new doctrine): REFUSE. An ATTESTED act — `op=ratify`, `op=caseratify`, and any future attested op — is performed ONLY by a named member's OWN AUTHENTICATED SESSION; the operator's bearer tokens (ADMIN, MEMBER, PROBE) may no longer deliver one, even carrying a member's valid signature.** *The signature proves who AUTHORISED; the credential that delivers it decides WHEN the record changes, and the record names the actor.* — owner RECORD.
milestone: M3 — the record's own refusals reach every authority that writes it
interface: I3 — MAJOR (a formerly accepted delivery is refused); mint the IC with `node tools/mintid.mjs IC` BEFORE building, base read at resolution (I3 25.0.0 when rowed)
design: `docs/architecture/BIO_Assistant_and_AI_Roles_v0_1.md` §3 rule 4 (*no machine credential performs the attested act*), read with `DEBT.md` D-421 and REC-123's `bio-plane/test/machine-attest.test.mjs`.
depends-on: REC-123 (DONE — merged in the same integration as this row).
scope: **THE ROW'S FIRST ACT, BEFORE ANY CODE — VERIFY AT THE ARTIFACT THAT THE OPERATOR'S CURRENT PUBLISHING ROUTE WORKS THROUGH A MEMBER SESSION.** DEC-33 says publication runs through the operator, and `tools/sign-release.html` only PRODUCES ratification signatures; it does not submit them. **If ANY documented or live operator procedure submits `op=ratify` or `op=caseratify` with a bearer token, STOP AND REPORT IT** — refusing would close the only publishing route, and that is Bob's call. Grep every kickoff, `docs/`, `scripts/`, `tools/`, `newgroup/`, and the DIST procedures, and say what you searched. **Otherwise build it:** both handlers refuse every operator bearer-token class BY NAME below the scope check, a signed-in member's session still ratifies; the three suites that drive the token path (`ratify.test.mjs`, `reuse-ratify.test.mjs`, `ratify-envelope.test.mjs`) re-pointed at a member session with the reason written, never exempted; `11.machine-fence` in `construct-status.json` updated with probes, then `node tools/status.mjs --write`.
accepts-when: the precondition's grep and its result are in the row's report; an ADMIN, a MEMBER and a PROBE token each refused BY NAME at both ops through the control plane; a member session ratifies exactly as before; `status --check` 0 drift; plancheck BARE; coverage --strict exit read UNPIPED; battery green by its COMPLETION LINE.
NEGATIVE CONTROL: recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — ONE ARM PER REFUSED CREDENTIAL CLASS (the fence removed for that class → its ratification is ACCEPTED and the arm FAILS naming the class); **over-strictness:** a member session still ratifies. **State how a liar would satisfy this before stating what it checks:** the cheapest green is to refuse by token STRING rather than by credential CLASS — a fourth operator token would walk straight through.
added: 2026-09-18 · CONDUCT #4 (D-421 raised by REC-123's builder and decided by BOB #14 the same hour. Id minted with `node tools/mintid.mjs REC`, run bare.)

### REC-127 · done — landed `c9117059`, merged by CONDUCT #4, 2026-09-18. **`content:cap=undetermined` no longer matches an image cited as its own bytes; those answer `content:cap=does-not-apply`, and `rows=content` labels their `derivation_cap` with the same word — through a new registry key projecting an EXPRESSION in its own slot, so no row's column set changes and only a bytes row's value does.** The word is spelled once (`CAP_DOES_NOT_APPLY` bound to `CHAIN_DOES_NOT_APPLY`). REC-121's §4 measurement corrected at its site with the reason. Six-arm control incl. the liar (`dropall`) and a pre-item arm; the answer digest identical across every arm that should not move it. IC-138 MINOR, I3 26.1.0. **Left, named:** `op=content`'s own `derivation_cap` on a bytes row still reads null — beside `transcription.applies: false`, which says why. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into the slot REC-121 freed — a correction to landed substrate, ahead of REC-126 (BOB #14's rule). DEPENDS-ON CHECKED AGAINST THE CODE: REC-121 merged in the same integration as this flip, its `rec121-chain-bytes.test.mjs` §4 present; spawned only after that integration is pushed. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **THE SAME MISLABEL ON THE CAP AXIS: `content:cap=undetermined` (`derivation_cap IS NULL`) still MATCHES AN IMAGE CITED AS ITS OWN BYTES, whose cap is NULL BY MEANING (does not apply), and `rows=content`'s `derivation_cap` shows an unlabelled null.** REC-121 measured it (its suite's §4 is the assertion a fix must correct) and left it, correctly, because its own row said no other answer may move. A correction to a just-landed item — inside the build order, ahead of new items (BOB #14). — owner RECORD.
milestone: M4 — D-164, the content-extent primitive (RECORD)
interface: I3 — MINOR if it follows IC-131's shape (a `does-not-apply` value, the narrowing only stops over-reporting); mint with `node tools/mintid.mjs IC` BEFORE building, base read at resolution
design: `docs/development/CONTENT-SEARCH-DESIGN.md` §4 (the `content:` arm, as amended by REC-104 and REC-121) read with `docs/development/EXTRACTION-BREADTH-DESIGN.md` §3.2 (`cited_as`, IC-125).
depends-on: REC-121 (DONE — merged with this row; its `rec121-chain-bytes.test.mjs` §4 pins today's cap behaviour).
scope: mirror REC-121 on the cap axis: `cap:undetermined` excludes `cited_as = 'bytes'` rows, which answer under their own stated value; `rows=content`'s `derivation_cap` is labelled rather than a bare null for them; every bytes row stays reachable. REC-121's §4 assertion is corrected at its site with the reason, never exempted; the answer digest is the over-strictness pin.
accepts-when: through `op=meaningrows`, `content:cap=undetermined` returns only text rows and the bytes rows answer under their own value; every other answer byte-identical (the digest); battery green by its COMPLETION LINE; coverage --strict exit read UNPIPED.
NEGATIVE CONTROL: recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — the exclusion removed → the bytes rows reappear under `cap:undetermined` and the arm FAILS naming them; **the liar** (bytes rows dropped from every answer) driven as an arm, as REC-121 did, so the reachability checks catch it.
added: 2026-09-18 · CONDUCT #4 (from REC-121's report. Id minted with `node tools/mintid.mjs REC`, run bare.)

### MK-1 · done — landed `3a67ddd7`, merged by CONDUCT #4, 2026-09-18. **A MEMBER'S FIRSTHAND OBSERVATION IS A DOCUMENT: `op=testify` files the member's words as an authored INFO bundle — origin and actor `member`, a SERVER-STAMPED author and `observed_at`, no capture grade — whose `authored` flag only the testimony path can set (a module-private Symbol no request body can carry).** **HELD OFF `main` ONCE, ON BOB #14's CONCERN, AND THE HOLD WAS RIGHT:** driven path by path, **an observation COULD be published — after any member PUT its bytes, `op=ratify` put the member's words, the authored provenance document and the observer's HANDLE into the published bucket**, and a finding citing one and a case containing one both ratified. **This landing carries the fence** (C-53.10 observation, C-53.11 a finding reaching one at any depth, C-53.12 a case), after the machine fence and before the signature is weighed, with an OVER-STRICTNESS arm (ordinary documents, findings and cases still ratify); **lifting it is MK-3's act.** And BOB's ruling (1): a CANONICAL HEADER (`bio-testimony/1`, id, observed_at, no author) so identical words from two members are two testimonies — proved by a test. IC-133 (I3 26.2.0), IC-134 (I5 1.19.0). Seventeen-arm control; `pubbundle` re-driven by hand; one of its own assertions found reading a RE-RENDERING instead of the stored file — corrected to read the bucket. **Left for MK-3, named:** an observation's words live inline in the bundle, not the working capture bucket. `2.firsthand` PARTIAL. **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into build-order item 3's slot (BOB #14: MK-1..MK-5 fill items 3 and 6). DEPENDS-ON CHECKED AGAINST THE CODE at spawn: `mintContent` present in `bio-plane/src/store.mjs` and the `register` table in `schema.mjs`; `2.firsthand` ABSENT in `construct-status.json`. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **THE AUTHORED BUNDLE — a member's firsthand observation IS a document: an authored INFO bundle in the register, origin and actor `member`, a new `authored` flag only the testimony path can set, a SERVER-STAMPED author and `observed_at`; content rows over it, so every existing reader works unchanged.** — owner RECORD; surfaces are Program B's and are NOT rowed.
milestone: M3 — the member's own knowledge enters the record as what it is
interface: I5 (the register) and I3 (the op) — each its own IC, minted with `node tools/mintid.mjs IC` BEFORE building, bases read at resolution (I3 24.0.0, I5 1.18.0 when rowed)
design: `docs/development/MEMBER-KNOWLEDGE-DESIGN.md` §2 (the member's words are the document) and §7 (the refusals)
depends-on: none — verify the register and `mintContent` on your tree
scope: build §2's mechanism and §7's refusals that fall to it: a register entry claiming origin other than `member` on an authored bundle, a non-authored bundle claiming `authored`, and a caller-supplied author — each refused by name. Name the op so it does not collide (`op=claim` is taken; grep before naming). Move `2.firsthand` in `construct-status.json` with probes (update `3.census` after reviewing by meaning), then `node tools/status.mjs --write`. **Read the design section at the artifact before building (§8's own condition).**
accepts-when: an observation authored through the op lands as a bundle whose content rows every existing reader serves unchanged; each §7 refusal that falls to MK-1 fires by name through the op; the author is the session's member whatever the caller sends; `status --check` 0 drift; plancheck BARE; coverage --strict exit 0 unpiped; battery green by its COMPLETION LINE.
NEGATIVE CONTROL: recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — one arm per refusal — each fence removed → the forged bundle is ACCEPTED and the arm FAILS naming it; the author stamp removed → a caller-supplied author lands and the arm FAILS. **Liar:** a flag any writer can set — the `authored` bit must be settable ONLY on the testimony path.
added: 2026-09-18 · CONDUCT #4 (from BOB #14's inbox; MEMBER-KNOWLEDGE-DESIGN.md §8, build-order items 3 and 6.)

### MK-4 · done — landed `44e22d62` (branch `mk4-lead-visibility`, pushed under a NEW name because the rebase made the old one non-fast-forward), merged by CONDUCT #4, 2026-09-18. **THE LEAD IS BUILT: `op=lead` writes a lead row (no bundle — a lead is never evidence), `op=leadlook` records a look through the existing `#observe` under `authority_kind 'lead'`, `op=leadread` reads it, `op=leadshare` shares it; a lead cited as ANY leg is refused by name (C-54.1).** Authoring writes only the row — a look entry at authoring would record a look nobody made, which §3 forbids; the builder said the row asked for that and refused it on the design's text. **HELD OFF `main` ONCE for BOB #14's VISIBILITY RULING and landed with it:** the author always; a project's JOINED participants only after the author SHARES (C-54.9 if the author is not joined, C-54.10 if a non-author tries); a machine only through a member-scoped `ai` key (instance tokens and org-scoped keys reach no lead); everyone else the byte-identical answer for a lead that does not exist. Thirteen-arm control incl. `machinewide` (the provisional unfiltered rule restored → 64/5) and over-strictness. The battery caught two defects the builder introduced (a session-list edit that dropped six ops; an unbounded query) before the final run. IC-135 (I5 1.20.0), IC-136 (I3 26.3.0). `10.lead` BUILT; `9.internet` ABSENT with its text corrected. **Its merge onto MK-1 was the session's first real code conflict, resolved keep-both by hand and proved by the full battery.** **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into build-order item 3's slot (BOB #14: MK-1..MK-5 fill items 3 and 6). DEPENDS-ON CHECKED AGAINST THE CODE at spawn: the `observation_log` table and its `authority_kind` column present in `bio-plane/src/schema.mjs`; `10.lead` ABSENT in `construct-status.json`. Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** **Prior state, kept as the record: queued** — **THE LEAD — the same member knowledge BEFORE the search: an authored row that is NEVER evidence, whose writer writes `observation_log` with `authority_kind = 'lead'`, and `LOOKED_ABSENT` recorded against a lead.** — owner RECORD; surfaces are Program B's and are NOT rowed.
milestone: M3 — the member's own knowledge enters the record as what it is
interface: I3 and I5 — each its own IC, minted BEFORE building, bases read at resolution (I3 24.0.0, I5 1.18.0 when rowed)
design: `docs/development/MEMBER-KNOWLEDGE-DESIGN.md` §5 (the lead) read with `docs/development/OBSERVATION-LOG-DESIGN.md` (the log it writes)
depends-on: none — verify `observation_log` and its `authority_kind` vocabulary on your tree
scope: build §5; a lead cited as a basis leg is refused by name (§7). Move `10.lead` in `construct-status.json` with probes (update `3.census` after reviewing by meaning), then `--write`. **Read the design section at the artifact before building (§8's own condition).**
accepts-when: a lead authored through the op writes its row and an `observation_log` entry with `authority_kind = 'lead'`; `LOOKED_ABSENT` against a lead is recorded and read back; citing a lead as a leg is refused by name; `status --check` 0 drift; battery green by its COMPLETION LINE.
NEGATIVE CONTROL: recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — the lead-as-leg refusal removed → a lead lands as a basis leg and the arm FAILS. **Liar:** a lead that is merely an unlabelled observation — it must never be citable as evidence.
added: 2026-09-18 · CONDUCT #4 (from BOB #14's inbox; MEMBER-KNOWLEDGE-DESIGN.md §8, build-order items 3 and 6.)

### LED-1 · done — landed `bff74215`, merged by CONDUCT #4, 2026-09-18. **THE AUDIT SIMULATED THE MIGRATION rather than reading the code:** two clones, one control and one with every closed row moved (QUEUE 1,156,976 → 209,049 bytes, −82%), the same 16 ledger suites run in both. **The move BREAKS:** `planning-hygiene` (live `QUEUED <ID>` references point at `done` rows); `mintid.test` and `plancheck` (the known-collision list goes stale for D-124, CPDF-9, FW-15, M0-16 — mintid's DUPLICATE check never reads `docs/archive/`, though its FLOORS do and CORPUS-STANDARD §6 says it scans the archive); `rowdesign.control` A2b (needs ≥50 closed rows live). **AND IT LOSES OWED WORK SILENTLY:** `owed.mjs` dropped D-330, D-401, D-405, D-407 (BOB 17→13, CONDUCT 15→11) because *no "open" in the last cell* is the WRONG DEFINITION OF CLOSED — and August's archiving used it. `decided.mjs` is safe (1,080 rulings, none lost; 104 change file, regenerating fixes them). 11 of 16 open rows' `depends-on` point at `done` rows. **BROKEN TODAY, reported not fixed:** `rowdesign.control` arm A1 (anchor now finds M0-33, not M0-29); `tools/.ui59/*.mjs` hardcode a path absent on this machine; mintid's duplicate check blind to the archive. Also: `tools/gates.mjs`'s doc-suite list omits `owed.test.mjs`. **The full reader table is M-57.** — **Prior state, kept as the record: queued** — **THE LEDGER READER AUDIT, BEFORE ANYTHING MOVES: 19 instruments read `QUEUE.md` and 14 read `DEBT.md`; for each, what it reads and whether it needs a CLOSED row to exist.** — waits on nothing. **EXEMPT FROM THE M0 HOLD BY NAME (BOB #14, 2026-09-18).**
milestone: M0 (process, Bob's direction 2026-09-18) — the ledgers become small, ordered and always current
interface: none
design: `docs/development/VERIFICATION.md` — the test estate's own authority, admitted for the M0 lane BY NAME by `tools/rowdesign.mjs`; read with the BOB INBOX entry of 2026-09-18 *THE LEDGERS BECOME SMALL, ORDERED AND ALWAYS CURRENT* (enacted into LED-1..LED-5 by CONDUCT #4), which lists the readers.
depends-on: none
scope: every reader BOB's entry names — QUEUE: `tools/{owed,nc-m039,mintid,plancheck,rowdesign}.mjs`, `bio-plane/scripts/op-claims.mjs`, tests `acquire, check-firing, corpuscheck(+control), m041-instrument-census(+control), mintid, op-claims, planning-hygiene, publish, refselectivity.control, rowdesign.control, scheduler`; DEBT: `tools/{decided,owed,mergecarry,mintid,plancheck,nc-m039}.mjs`, `bio-plane/scripts/armdecay.mjs`, tests `caseflip, corpuscheck, dec65-strength-reach.control, derivation-bounds, m025-arm-anchor-witness, mintid, planning-hygiene` — **AND GREP FOR ANY READER THE LIST MISSED** (`tools/status.mjs`, `tools/statussweep.mjs`, `tools/strandedwork.mjs`, `tools/retirable.mjs` and anything under `tools/` or `bio-plane/` that opens either file). For each: what it reads, and whether it needs a CLOSED row to exist (id allocation, a ruling's source line, a pinned count). Verify — do not assume — which already read `docs/archive/ledgers/` (mintid is said to).
accepts-when: a table in this row (and `MEASUREMENTS.md`) naming each reader, what it reads, and its dependency on closed rows; every reader that needs history is named with the change it will need; battery green; **NO row moved.**
NEGATIVE CONTROL: none owed — an audit with no code change; its falsifier is the grep for readers the list missed, stated with its command.
added: 2026-09-18 · CONDUCT #4 (enacted from BOB #14's inbox entry; the LED ids are BOB's names for the five, kept as written.)

### LED-2 · done — landed `4f09d384`, merged by CONDUCT #4, 2026-09-18. **`tools/ledger.mjs` archives ONE closed row verbatim with conservation checked TWICE — on the plan and on what is read back from disk — by id MULTISET and by every live line, and restores both files (sha256-verified) if the read-back fails.** The line-conservation check exists because the first control run showed a move could swallow a `## AREA` heading the id check cannot see. **ONE DEFINITION OF CLOSED, in `tools/owed.mjs`, imported by the archiver:** the old owed test (`CLOSED` anywhere in the cell) called 66 rows closed of which at least six are plainly OPEN (D-376, D-242, D-361, D-300, D-161, D-292) and missed 6 closed ones; and owed read the WRONG CELL for 17 rows whose body contains a `|`, hiding residue on D-285 and D-400. All six M-57 breakages handled (mintid's duplicate check reads the new archive; `QUEUED <ID>` resolves live ∪ archive; rowdesign.control A1/A2b corrected; new archive files, never the August ones; DEBT ≥ 10 kB guarded). `plancheck` §2h live: (a) closed rows in a live ledger — 399, WARN until LED-3; (b) size budget — QUEUE 1,185,305 B vs 150 KiB, WARN until LED-4; (c) depends-on resolution — FAIL now, 0 unresolved. `gates.mjs` now derives doc-facing suites by the TOOLS they call (adds `owed.test`, `fleetbundles.test`). Simulated full migration on a copy: 397 rows, 0 refused, collision register unchanged. **Found, reported, not fixed:** owed's owner pattern now attributes D-127 to BOB and D-283 to CONDUCT by FALSE match (capitalised *BOB'S* the person); D-186 and D-408 need their disposition wording fixed before LED-3 can archive them. — **Prior state, kept as the record: queued** — **THE ARCHIVER AND ITS GATE: `tools/ledger.mjs archive <ID>` moves ONE closed row verbatim to `docs/archive/ledgers/<FILE>-closed.md`, the id set of live ∪ archive conserved EXACTLY; `plancheck` arms for no-closed-row-in-a-live-ledger, a size budget, and depends-on resolution.** — waits on LED-1. **EXEMPT FROM THE M0 HOLD BY NAME.**
milestone: M0 (process, Bob's direction 2026-09-18)
interface: none
design: `docs/development/VERIFICATION.md` (admitted for M0 by name), read with BOB #14's inbox entry of 2026-09-18 (LED-2's paragraph) and LED-1's reader table.
depends-on: LED-1
scope: as BOB's entry states: `archive <ID>` for a `done`/`superseded` QUEUE row or a CLOSED DEBT row with no declared residue; `plancheck` arms (a) no closed row in a live ledger, (b) a size budget per ledger and per open row (start QUEUE ≤ 150 KB, row ≤ 3 KB) — WARN until LED-4 lands, then FAIL, (c) every `depends-on` resolves to an open row, an archived done row, or a `tools/status.mjs` claim reading BUILT.
accepts-when: suite + negative control, one arm per property; **the CONSERVATION arm is the one that matters** — a mover that drops a row is the D-288 loss shape; battery green; plancheck BARE.
NEGATIVE CONTROL: one arm per property, **conservation first**: a mover that drops one row must FAIL naming the id. **State how a liar would satisfy this before stating what it checks:** the cheapest green is a conservation check that counts rows rather than comparing id SETS — a dropped row and a duplicated row cancel.
added: 2026-09-18 · CONDUCT #4 (from BOB #14's inbox entry.)

### REC-130 · done — landed `11cf4aca`, merged by CONDUCT #4, 2026-09-18. **AN UNSIGNED CASE DOCUMENT IS NO LONGER READABLE BY STRANGERS, AND ENUMERATION LEARNS NOTHING: anonymous callers, unknown tokens, members of another project, the probe class and another member's agent all receive the BYTE-IDENTICAL answer a nonexistent case id gets — status, content type and body compared raw against the same op's answer for the same id read before it was minted. A signed case stays public.** Premise confirmed first: the unedited tree returned every authored sentence to an anonymous caller. Standing reuses the existing D-15 viewer predicate against the owning project — no narrower rule invented. **A SECOND disclosure found and closed at the same boundary:** `op=caseratify`'s refusal details told a member of another project that a case existed. `casesign.test.mjs`'s anonymous-read assertion corrected with a DATED comment; `gate-reads` moves `casedocument` to GATED. Controls (e)–(h) incl. the liar (`NOT_PERMITTED` instead of not-found → every byte-for-byte arm fails) and over-strictness. IC-141 MAJOR, I3 27.0.0. **Open on deployed instances until the next DIST cut.** **Prior state, kept as the record: running** — **SPAWNED 2026-09-18 by CONDUCT #4 into REC-126's slot — a correction to landed substrate, ahead of new work (BOB #14). DEPENDS-ON CHECKED AGAINST THE CODE at spawn: `casedocument` present in `bio-plane/src/index.mjs`'s OPS table with no credential class (the exposure this row closes). Falsify rather than believe: a live worker holds an `agent-*` worktree with a claim on the paths its scope names; if none does, this row is UNDETERMINED between `queued` and done-awaiting-integration — READ THE BRANCH, and never conclude `queued` from the absence alone.** — **AN UNSIGNED CASE DOCUMENT — a group's working material — IS READABLE BY ANYONE WITH NO LOGIN, AND CASE IDS ARE SEQUENTIAL, SO A STRANGER CAN WALK THEM.** `op=casedocument` carries no credential class and returns scope, findings, completeness statement, exclusions and bias acknowledgement to any caller. Found by REC-126's builder, checked at the code. **DECIDED by BOB #14, as an application of the publication fence already ruled (no new doctrine):** an UNSIGNED case document answers ONLY to members with standing in the owning project (and, once REC-126 exists, a live review-copy grant holder); **everyone else gets EXACTLY the answer a case that does not exist gets, so enumeration learns nothing;** a SIGNED case document stays public, because strangers need it to verify. CASE-5b's anonymous read was a mechanism choice with no ruling behind it. — owner RECORD.
milestone: M3 — the publication fence holds for working material
interface: I3 — MAJOR (a formerly answered anonymous read is refused); mint with `node tools/mintid.mjs IC` BEFORE building, base read at resolution (I3 26.2.0 when rowed)
design: `docs/architecture/BIO_Publication_v0_1.md` (the publication fence: unratified working material never crosses to the public) read with `bio-plane/src/index.mjs`'s own comment on `op=index` (*a title alone names what the group is looking into*) and BOB #14's ruling as recorded on this row.
depends-on: none
scope: `op=casedocument` on an UNSIGNED case answers only a member with standing in the owning project; any other caller — anonymous, a member of another project, a machine credential outside scope — receives the SAME response, byte for byte, as for a case id that does not exist (the no-existence-leak property, asserted by comparing the two responses). A SIGNED case document stays public. **Correct `bio-plane/test/casesign.test.mjs`'s anonymous-read assertion with a DATED comment saying why the old one was wrong** — never exempt it. Grep every other reader of an unsigned case (and anything that enumerates case ids) and apply the same rule, or name why it does not apply.
accepts-when: anonymous and non-standing callers get the not-found answer for an unsigned case, IDENTICAL to a nonexistent id; a standing member reads it; a signed case reads publicly as before; `status --check` 0 drift; plancheck BARE; coverage --strict exit read UNPIPED; battery green by its COMPLETION LINE.
NEGATIVE CONTROL: recorded in the suite's own `NEGATIVE CONTROL:` line (**with the colon**) — the standing check removed → an anonymous caller reads the unsigned case and the arm FAILS; a DISTINGUISHABLE refusal introduced (e.g. `NOT_PERMITTED` rather than not-found) → the no-existence-leak arm FAILS. **Liar:** a refusal that says *forbidden* — it tells the enumerator the case exists.
added: 2026-09-18 · CONDUCT #4 (REC-126's gap 1, decided by BOB #14 the same hour. Id minted with `node tools/mintid.mjs REC`, run bare.)

