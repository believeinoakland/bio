# DEBT — closed rows, to 2026-08-09

**Rolled out of `docs/development/DEBT.md` on 2026-08-10.** 110 rows whose status is not
`open`, unedited, in their original order. The live file keeps every open row and the
rules. Rulings recorded in these rows are still indexed — `node tools/decided.mjs`.

| id | kind | raised | what | status |
| --- | --- | --- | --- | --- |
| D-1 | design | 2026-07-24 | **Root of trust is unmodelled.** Three parts of the membership design lean on it; the only implementation is ADMIN_TOKEN, a bootstrap credential that became the root of trust by accident. It is a proxy for hosting access, has no custody model (no m-of-n, no split custody), leaves no audit trail, and cannot be rotated without returning the instance to unclaimed. Deciding what a root of trust should BE for a BIO group is doctrine work of the same weight as the membership model. See `architecture/BIO_Membership_Architecture_v1.md` §9. | DOCTRINE · DEFERRED 2026-07-31 (DEC-2) until a BIO instance runs with MULTIPLE MEMBERS, from which what a real group can actually hold in custody can be OBSERVED rather than guessed — a custody model assumed in the abstract fails silently in the group it was built for. No session re-raises it before the trigger. |
| D-2 | defect | 2026-07-24 | **History paths use a non-canonical layout.** `readImage` emits `_history/<snap_key>/<path>`; canonical is `_history/bundle_<snap_key>.md`. All 30 bundles fail C-12.2, 168 errors, no content missing. `schema.mjs` line 3 settles which side moves. | fixed 0.5.0 |
| D-3 | defect | 2026-07-24 | **Promotion records are not projected.** The catalog requires `_history/promotion_<key>.json`; the plane stores the data and emits none of it. Makes `classifyDivergence` and C-20.1 unreachable rather than passing. | fixed 0.5.0 |
| D-4 | drift | 2026-07-24 | **Manifest `kind` vocabulary differs.** Plane writes `creation`/`direct_write`; catalog switches on `promotion`. This is why D-3 did not surface as an error: the check never fired. A divergence that hides another divergence. | fixed 0.5.0 |
| D-5 | gap | 2026-07-24 | **`plane-gate/0.1` implements 4 checks of 49.** No frontmatter contract, state legality, transition edges, canonical headings, append-only, citation register, provenance register, release authority, release signature, or mechanical conformance. This is how D-6 and D-7 shipped invisibly. | S-2 | fixed 0.6.0 |
| D-6 | defect | 2026-07-24 | **Intake UI stamps illegal first states.** A Problem lands at `forming` (legal: surfaced, elevated, deferred, dismissed); an Action lands at `forming` (legal: planned, active, awaiting_response, resolved, abandoned). C-4.1 refuses both. Shipped in 0.4.0 and 0.4.1. | S-3 | fixed 0.5.1 |
| D-7 | defect | 2026-07-24 | **Intake UI writes 4 of 15 core fields**, and `## Summary` for all four types where only Information takes it. C-2.2 fires eleven times per created bundle; C-3.1 on three types. Same root cause as D-6: written against the plane's tolerant store because the catalog was unavailable. | S-3 | fixed 0.5.1 |
| D-8 | drift | 2026-07-24 | **Relationship vocabulary disagrees.** State Rules 5.1 declares a closed six-value vocabulary; the catalog carries a seventh, `corroborates`. Implementation is ahead of a spec that claims to be the closed authority, so the document needs the amendment. | open | amended 2026-07-24 |
| D-9 | unverified | 2026-07-24 | **Unreferenced register rows: MEASURED and then RE-MEASURED, 2026-07-26. All 87 rows are sound.** The entry guessed "superseded bytes plus dropped transport twins", untestable because no op could read the register. `op=registeraudit` (0.22.0) reported 57 live, 30 "dropped", 0 orphan, all 30 being `migration/drive-provenance.json`, one per bundle. **That "dropped" classification was WRONG and so was the conclusion drawn from it.** `registerAudit` looks only in `files` and `history` and never in R2. `migrate/migrate.mjs` line 14 states its intent plainly: the Drive provenance is carried "verbatim as a registered drive-provenance capture, so the Drive era remains inspectable WITHOUT POLLUTING THE LIVE FILE IMAGE". It registers the capture, uploads the bytes, and deliberately keeps the file out of the bundle, which is exactly what the two-bucket architecture is for. Verified against the live plane: `op=capture&sha256=45a07df1…` returns HTTP 200 and the actual provenance JSON. **The migration IS auditable and no register row is broken.** The intermediate claim that it was not, recorded here on 2026-07-26 and now retracted, came from reading `op=file` returning null and never checking whether the bytes existed elsewhere. CONSEQUENCE: `registerAudit`'s `dropped` class is misleading and its output must not be trusted until it distinguishes `captured` (not in the image, bytes retrievable from `bio-captures`, sound) from genuinely `unbacked` (bytes nowhere). No row is known to be unbacked. | M6 · measured, sound; registerAudit needs an R2 check |
| D-46 | defect | 2026-07-26 | **The deployed plane enforces the SUPERSEDED v1.4 rule for project participant removal.** `projectRemove` in `store.mjs` requires an in-app administrator and answers `ADMIN_ONLY`, which is Membership Architecture v1.4 section 7.7. v2 section 7.7 REVERSES it: only a project OWNER removes a participant, and administrators hold no authority over project participation at all (v2 section 4.9), retaining only sight (7.3, 7.8). Confirmed live on biosmoke7 at 0.24.0, which answers `ADMIN_ONLY`. `projects.test.mjs` asserts the old rule and must be CORRECTED rather than exempted, per standing lesson 3. RESOLVED in 0.25.0: `projectRemove` now requires an owner and answers `NOT_THE_OWNER`, `projectInvite` lost its administrator bypass, both run through one `#isProjectOwner` helper, and the four assertions encoding the old rule were corrected. Verified live on biosmoke7. | resolved 2026-07-26, 0.25.0 |
| D-47 | gap | 2026-07-26 | **7.13 is not built: an administrator cannot add an owner to a project whose owners are all inactive.** Membership Architecture v2 7.13 gives administrators exactly one participation power, and only when EVERY owner of that project is inactive. Until it exists, a sole owner who gets deactivated leaves a project that accepts no new participants, cannot be reactivated, and cannot change hands. Not urgent on a development instance with one project and no deactivated members. RESOLVED in 0.30.0 as `op=projectownerrescue`: an administrator adds ONE owner when EVERY owner is inactive, adding rather than replacing, recorded with the acting administrator and a reason. | resolved 2026-07-26, 0.30.0 |
| D-49 | defect | 2026-07-26 | **Reactivating an ejected administrator restored their administrator status.** A section 4.7 removal sets `status='revoked'` and leaves `role='admin'`, so `memberSet(active)` returned them to office with one call by any single administrator, undoing a group vote and defeating 4.7's consensus-on-addition. Found while building section 1.3, not by a report. RESOLVED in 0.27.0: reactivation demotes them to `member` and says so in the response; restoring the office goes through `adminendorse` like any other appointment. Asserted in `membership.test.mjs`, written failing first. | resolved 2026-07-26, 0.27.0 |
| D-51 | drift | 2026-07-26 | **The `expertise` column on `members` is dead.** Section 1.3 moved to the append-only `member_expertise` table in 0.27.0, because a list column cannot carry a confirmation state, a confirmer and a withdrawal per entry. `memberAdd` still accepts and stores an `expertise` argument and `memberList` still returns the column, so two places now answer the same question and one of them is never updated. Left rather than dropped, since nothing here deletes, but the reader and the writer should be retired and `memberlist` should serve the table instead. RESOLVED in 0.30.0, and it was not merely drift: v1.4 let an administrator ASSIGN expertise at invitation time, which v2 1.3 forbids. `memberAdd` now refuses the argument rather than ignoring it and `memberList` serves from `member_expertise`. The column stays, unused. | resolved 2026-07-26, 0.30.0 |
| D-10 | defect | 2026-07-24 | **A no-op update reads as success.** The installer reports "Updated to X" whenever the upload succeeds, including when X equals the version already installed. Observed live: a 0.3.10-over-0.3.10 update read as a successful update and the load-bearing line was easy to skim past. | open | fixed, wizard 0.5.1 |
| D-12 | gap | 2026-07-24 | **Scale benchmark unrun.** Conversion Plan step 6: synthesized stores at 5,000 and 20,000 bundles against the prediction table. Real measurements exist and beat the refutation thresholds widely, but the formal benchmark has never run, and it should run with the full gate in the path. | S-8 | measured 2026-07-24 |
| D-13 | unverified | 2026-07-24 | **Source fidelity to Drive is not independently established.** The record is spec-conformant and every referenced capture hashes correctly, both verified this session. Fidelity to the Drive original rests on the migration tool's comparison against its own mirror. Judged sufficient because the corpus is development reference that production will refetch. | ACCEPTED · accepted |
| D-14 | defect | 2026-07-24 | **The enrolment screen is unreachable.** `s-enroll` exists in the markup and nothing ever calls `show("#s-enroll")`, so an invited member has a code and no way to use it. Shipped in 0.4.0 and 0.4.1. The burner URL is what should reach it. | S-3 | fixed 0.5.1 |
| D-15 | gap | 2026-07-24 | **The index must filter derived reverse edges by the viewer's position.** CLOSED 0.23.0. The seam reserved in S-10 held: satisfying it was one function, `viewerPredicate` in `query.mjs`, and not an audit of every query path. An identified session now compiles a predicate over `project_participants`; an uninvited member sees no project at all, not its existence, name, references or participants (7.9). The EVIDENCE CORPUS STAYS SHARED by design: the filter applies to project bundles and nothing else, because compartmenting Information and Problems would fracture the record and stop a member on one project seeing what another had gathered. Machine credentials are deliberately unfiltered: `class:member` is a shared instance token with no participation to check, and anyone holding one already has instance-level access, so filtering it would buy nothing while breaking the operator path. MEASURED, because the bench drives `class:member` and therefore did not exercise the new predicate at all: a member-viewer shape was added and the filter costs 16ms against 17ms unfiltered at 20,000 bundles, the `EXISTS` arms short-circuiting on `object_type <> 'project'` for almost every row. `test/projects.test.mjs`, 25 assertions. | closed 0.23.0 |
| D-17 | defect | 2026-07-24 | **Manifest `created` was the server's wall clock**, where C-12.1 compares live `last_updated` against earlier entries' `created`. A signed ratification backdates `last_updated` to the transition instant, so an honest document read as history running backwards. The catalog's own comments record this exact deadlock freezing a bundle on 2026-07-22: satisfying C-12.1 meant editing bundle.md and destroying the ratification. Now stamped from the revision's own time. Found by the conformance suite on its first run. | fixed 0.5.0 |
| D-21 | drift | 2026-07-24 | **References live in two places and only one is checked.** The plane's `refs` table is populated from the promote payload; the catalog reads `references` out of `bundle.md` frontmatter. A caller can set them inconsistently and nothing notices, because C-6 only ever sees the frontmatter. Either promote should parse references from the document, or the payload field should go. | open | fixed 0.6.2 |
| D-22 | gap | 2026-07-24 | **The revise path writes no Session Log entry.** C-13.2 requires one on any bundle whose `last_updated` moves past `created`, and C-5.1 requires prior entries be retained. A member editing through the browser can therefore produce a bundle the gate refuses, with a message about the Session Log that will mean nothing to them. The editor should append an entry on save. | open | fixed 0.6.1 |
| D-23 | defect | 2026-07-24 | **The revise path had never worked.** The save handler read `lease.result.baseSha`; the lease returns `base`. So every revision sent `base: undefined`, the store correctly refused it as a stale write, and the page reported the raw code `CAS_STALE` because its friendly message matched on `STALE`. It also overwrote `created` with the save time, destroying when the bundle was actually created, and never moved `last_updated` in the document the catalog reads. Found while fixing D-22, which was one of four things wrong in the same twenty lines. | fixed 0.6.1 |
| D-24 | defect | 2026-07-24 | **A comment broke the served page, for the second time.** setup.mjs is one template literal, so an unescaped backtick terminates it and a dollar-brace starts an interpolation. A comment quoting two field names in backticks made the module unparseable. The 0.3.8 hang was the same class. Both were found by accident; `hygiene.test.mjs` now scans the template for unescaped backticks, counts interpolations, loads the module, and parses the script it serves. | guarded 0.6.1 |
| D-25 | design | 2026-07-24 | **`promote` writes a whole image, so a partial writer deletes what it does not mention.** This is correct for a content-addressed store with a single writer, and it is a trap: the monitor's first tick supplied only `bundle.md` and removed the provenance register from every bundle it touched, taking the monitoring baseline with it. Fixed at the call site, and the image now carries a blob's size so callers CAN pass the rest back, but the sharp edge is still there for the next writer. Options: a patch-style promote that merges, or a refusal when a promotion drops a file that the previous revision had and the writer did not name. The second is probably right, since silent deletion has no legitimate use. | open | fixed 0.13.1 |
| D-26 | design | 2026-07-24 | **A whole-store conformance pass pays one round trip per bundle.** Measured: 63s locally for 20,000 bundles, about 2,060s on the deployed plane sequentially, roughly 97% of it fixed per-call latency. The catalog is a pure function over an injected filesystem and the images already live inside the Durable Object, so the pass belongs inside the object: one call in, a findings summary out. Store and checks are not the constraint; the shape of the API is. | open | fixed 0.12.0 |
| D-27 | gap | 2026-07-24 | **`list` returns every bundle and is linear.** 81ms at 5,000, 434ms at 20,000, roughly 2s at 100,000. Fine now, and it returns everything because nothing has needed less. Pagination or a narrower projection is the answer, and the benchmark above says when. | open | fixed 0.14.0 |
| D-29 | defect | 2026-07-24 | **The browser's revise path deleted every file but bundle.md.** Identical to the monitor's first tick, in a different place: editing a bundle with a captured document removed its provenance register and every capture. Found by closing D-25, which refused the write instead of obeying it. Both call sites now carry files forward. | fixed 0.13.1 |
| D-31 | design | 2026-07-25 | **The `public` token class was an attractive nuisance and is retired.** A credential handed to the public is not a credential: to be public it must be widely distributed, and once distributed it bounds nothing. It granted `selftest` and `publishedlist`, was never issued by the installer (which creates exactly boot, member, and probe), and was bound on no instance, yet its existence is what invited `op=index` onto its list and produced D-30. The published surface is protected structurally by the `classes: null` ops that read only the published projection. When a public directory of published material is wanted, it is an unauthenticated op on that projection, like `verify`, never a token class. | fixed 0.14.2, held by test/fence.test.mjs |
| D-30 | defect | 2026-07-25 | **`op=index` served working-corpus metadata to the public token class.** The control plane's own header says the public class is published-scope reads only, and its note on `verify` justifies that op being unauthenticated precisely because it answers from the published projection. `op=index` reads the `bundles` table, so it met neither condition, and a public-class credential received every bundle's id, title, current state, last-updated time, and image hash. A title names what a group is investigating and the state says how far along it is, both before there is anything to answer, which is the disclosure the two-bucket fence exists to prevent. No caller depended on the grant. Found while settling the retrieval design, since the same fence governs search. | fixed 0.14.1, held by test/fence.test.mjs |
| D-20 | gap | 2026-07-24 | **The gate needs byte-complete images.** C-12.2 checks history-snapshot presence with `files.has` rather than the elided-aware helper, so a tier-scoped read fails it even when the bytes exist in R2. Correct for a byte-complete gate, but it means gating a capture-heavy bundle pulls its history blobs: 148MB for the whole record. Measure in S-8 and consider whether R2 `head` can satisfy presence without a fetch. | S-8 | fixed 0.6.0, catalog 1.16.5 |
| D-18 | gap | 2026-07-24 | **The conformance suite asserts only the C-12 family.** Other families are counted and printed but not failed, because the intake path owes D-7. Tighten to "zero findings" when S-3 lands. | M0 · S-3 |
| D-16 | gap | 2026-07-24 | **The cover field must be labelled as a label**, not a name, wherever it appears. A field that reads as "identity" invites an administrator to type a legal name, which is the whole exposure the cover-and-handle split exists to avoid. Naming it is the cheapest part of the obligation and the only part the software can enforce. | open | fixed 0.14.0 |
| D-34 | design | 2026-07-25 | **A server-side selection has no lifecycle and no owner.** Step 5 of the retrieval build order is not in 0.16.0. `mode=ids` returns the whole set in the page's order, which satisfies "select-all is a distinct operation", but the snapshot-by-handle construct is absent and with it every resource question it raises. A Worker holds no connection, so the plane cannot observe a closed tab and can only require proof of life: ownership by session so a logout or a revocation collects, a TTL refreshed on read, a Durable Object alarm to sweep, and caps by BYTES before count, since a 20,000-id selection is 400 to 800KB in the same SQLite that holds the record. Above the byte cap, store the query plus the SHA-256 of the ordered id list rather than the ids: drift detection stays exact and storage becomes O(1), at the cost of being able to say a large selection drifted without saying which rows moved. Note also that a selection is the first thing in an append-only store that is legitimately collectable, and that exception needs writing into doctrine or a later session will read the sweep as a violation. | fixed 0.17.0 |
| D-35 | design | 2026-07-25 | **Drift in a selection must be detected and must not be absorbed.** When the corpus moves under a selection, five things can have happened: an item was revised, an item was purged, an item stopped matching the query, a new bundle started matching, or the viewer's visibility changed. The first two are one join if `bundle_sha` is stored per item; the next two are one re-run of the stored query; the last is the gate applied at action time, which is obligatory anyway. Auto-updating the set is rejected: a selection records an operator's INTENT, and an action landing on rows the operator never saw is an accountability failure in a record whose whole purpose is attribution. The monitor complicates this honestly, since mechanical revisions will drift a long-lived selection with no human involved, so drift should be CLASSIFIED using the `writer` and `operation` already on the manifest rather than absorbed. | fixed 0.17.0, held by test/selection.test.mjs |
| D-37 | gap | 2026-07-25 | **The 300-second selection keep-alive.** CLOSED by Bob's decision, 2026-07-25, on the grounds that a selection timeout is not a failure mode but a term of the contract, the same way an idle logout is: intrinsic to the relationship rather than an error, and its consequences the client's to decide. The client may re-select against a possibly different record set, tell the user the window closed, or warn as expiry approaches and offer to hold. The contract is already expressed on the wire and needs no new machinery: `op=select` returns `expires` and `ttlSeconds`, `op=selection` returns a refreshed `expires`, and `NO_SUCH_SELECTION` IS the timeout signal. `op=selection` is non-mutating and defaults to weight `report`, so a client polls it both to hold the lease and to learn what drifted, which is what a UI wants to show anyway. Reclamation is alarm-driven: `#armSweep` on creation, `alarm()` sweeping and re-arming while any selection remains, self-terminating when the table empties. 300 seconds stands. Three claims made against this item during the 0.21.1 session were WRONG and are recorded so they are not made again: that the sweep was opportunistic-only and needed an alarm (it has one), that expired and unknown needed distinguishing server-side (the client knows its own creation time and TTL and can distinguish locally), and that no way existed for a client to say it was still present (`op=selection` is exactly that). | closed by decision |
| D-39 | defect | 2026-07-25 | **Every op returns a worker exception on a POST with an empty body.** `store.mjs`'s `fetch` calls `await req.json()` unconditionally when the method is POST, so an empty body throws before any op is dispatched and the caller sees Cloudflare error 1101 rather than a BIO refusal. Verified on the deployed 0.18.0 against `stats`, `index`, `dangling`, `searchfields` and `selectionlist`, so it is general and pre-dates the citing work; it was found while exercising `op=cite` live. Harmless to correctness, since every op that matters is reachable by GET or by POST with `{}`, but it turns a client bug into an opaque platform error instead of a named reason. One-line fix, deliberately not folded into the 0.18.0 release. RESOLVED 2026-07-31 (REC-3): the guard already existed at the Durable Object (store.mjs `fetch`, commit 6ac72d0a — an empty POST body is `null`, a present-but-invalid one is refused `BAD_JSON` by name), so an empty POST already returns a named JSON reason rather than a 1101; the debt stayed open only because NO suite locked it in. `test/empty-body.test.mjs` now drives an empty POST to the five debt-named ops (stats/index/dangling/searchfields/selectionlist) and to mutating ops (promote→NO_BODY etc.), with the negative control RUN (break the guard → the five ops return a non-JSON 500, 8 fail; restored → 10 pass). | resolved 2026-07-31, REC-3 |
| D-41 | gap | 2026-07-25 | **The Section 4 governance write path had never run against a deployed instance.** CLOSED 2026-07-25 on the deployed 0.21.1, once Bob issued a throwaway ADMIN_TOKEN. 27 of 27 assertions passed live: consensus refusing an addition past the second and naming who must still endorse, the last endorsement issuing the invitation, one removal vote falling short with the tally reported, the target refused their own vote, a second vote carrying with deciders and reason recorded, `IMPOSSIBLE_AT_TWO` at two administrators, `ROOT_OF_TRUST` refusing to remove the founder, `ADMIN_REQUIRES_VOTE` refusing a direct revocation, and the capability rules. So 0.20.0 carries no D-43-shaped hole. The record was untouched throughout: 30 bundles, 137 files, 239 history rows, audit 30 clean. NOTE: the verification ran against the `bio` store rather than `scratch`, because D-44 made the member lifecycle uncompletable in `scratch`. Two probe member rows and some admin_votes rows remain in the biosmoke7 roster as a result; the RECORD is unaffected and the instance is disposable. | closed |
| D-44 | defect | 2026-07-25 | **Unauthenticated ops ignored the `store` parameter, making an invitation created in `scratch` unredeemable.** `enroll` and `invitelook` are `classes: null` and the control plane pinned every such op to `idFromName("bio")`. So `op=memberadd&store=scratch` answered `ok` and handed over a token that could never be redeemed, because enrolment always reached `bio`. A silent dead end, and it made the scratch namespace useless for rehearsing the member surface, which is exactly what an operator would want to do before touching a live roster. Pinning is CORRECT for `claim` and `login`, since an instance has one identity and there is nothing to claim in a scratch namespace, so the fix scopes only the invitation ops: the token is the authority and exists in exactly one store, so naming a store grants an unauthenticated caller nothing. Fixed 0.21.2, asserted in `fence.test.mjs`. Found against the deployed plane while closing D-41, not by 1280 green assertions. | fixed 0.21.2 |
| D-43 | defect | 2026-07-25 | **A `classes: null` op tested only at the Durable Object is untested where it is used.** `op=invitelook` shipped in 0.21.0 with a ReferenceError in its control-plane branch, so every live call answered a Cloudflare 1101 while 1276 assertions passed: the burner suite drove the store directly, as every store-level suite does, and for an unauthenticated op the control plane is the ONLY route a real caller has. Fixed in 0.21.1. THE CLASS IS NOW CLOSED TOO: `fence.test.mjs` reads the `classes: null` set out of the module and calls every one of them through the control plane with input that should produce a structured refusal, asserting only that a BIO answer comes back rather than a worker exception or `unknown op`. Reachability, not success. The probe catches a thrown parse, because a worker exception returns a non-JSON body and a verifier that dies on the failure it exists to detect reports nothing. Reading the set from the module means an op added later cannot pass by not being listed. | closed 0.22.1 |
| D-58 | gap | 2026-07-30 | **`captured_locators` and `links` are written ONLY when `op=acquire` is called with `subresources:true`, so an ordinary capture files no address and can never be a link target.** Found while trying to establish a `contemporaneous` verdict live: a plain `op=acquire` of a page that other captures link to left the verdict at `undetermined` because nothing had recorded that the store now held that address. Both `recordcapturedlocator` and `recordlinks` sit inside the subresource branch in `index.mjs` (the block guarded by reading the primary back out for the parser), which means the address index reflects only the subset of captures that asked for support material. Consequences: a link to a plainly-captured document resolves as `offsite` when the record demonstrably holds it; the reverse index (`op=links&address=`) under-reports what points at a document; and no verdict can ever be established for a target captured the ordinary way. `recordcapturedlocator` needs nothing from the parser and belongs beside the register write, unconditionally. The links write legitimately needs the parse and can stay where it is. RESOLVED 2026-07-30: moved to the capture body, unconditional, with `stLim` hoisted. Regression test drives `op=acquire` and asserts both directions (a PDF and a plainly-captured page resolve as `linked` naming their real hashes; an address the record genuinely lacks is still `offsite`). Negative control confirms five assertions fail against the unfixed code. | resolved 2026-07-30 |
| D-61 | defect | 2026-07-30 | **`op=lease` cannot be taken by a machine credential, so no unattended writer can revise a bundle.** Found live: `leases.actor` is NOT NULL and is stamped from the session, so a member TOKEN (as opposed to a member session) fails with `SQLITE_CONSTRAINT_NOTNULL` on `leases.actor`. The revision path in the UI takes a lease before promoting, which means the capture-refill path is session-only. That collides with Bob's ruling that captures are autonomous jobs a member can walk away from: if the daemon finishes a capture the member started, it cannot write the completed manifest back, because it cannot take a lease. Two candidate shapes, and the choice is a judgment: either the lease grows a machine actor identity (`class:member`, `daemon`) so unattended writers are named rather than anonymous, or the refill path skips the lease and relies on `promote`'s CAS on `base`, which is the actual integrity mechanism and which was used to work around this today. The lease is a courtesy lock against two members editing at once; the CAS is what prevents a lost update. Whichever way it goes, the daemon needs to be able to complete work it was asked to do. **CLOSED 2026-07-31 (REC-2, e425b24):** option (a) — a named machine actor `token:<class>`, server-stamped and unforgeable, no schema change; the store refuses an anonymous lease by name, and promote's CAS on `base` (the real integrity mechanism) is untouched. | M1 · CLOSED (REC-2) |
| D-62 | defect | 2026-07-30 | **The installer's `mdFor` omits `content_hash` even when a document is attached, so any bundle it writes can never be released.** Found live on the first bundle U8 wrote. C-2.7 makes a well-formed `content_hash` an ENTRY REQUIREMENT for `verified`, and the release flow reads the frontmatter and nothing else, so a bundle carrying a perfectly good capture is refused at `collected -> verified` for a field the writing surface never wrote. It also makes the bundle unfindable through the search layer's `hash:` facet, which reads that same field, which in turn defeats the duplicate detection described in D-60. FIXED in civicos-ui's ported `mdFor` (writes `content_hash: sha256:<primary>` whenever a document is present, absent for typed intake where inventing one would be a lie) and the live bundle was repaired by revision. **NOT fixed in `bio-plane/src/setup.mjs`**, which is the installer's own copy and the one a new group gets, so every bundle created through the installer wizard with a captured document has the same hole. RESOLVED 2026-07-31 (REC-3): `bio-plane/src/setup.mjs` `mdFor` now emits `content_hash: sha256:<primary capture sha>` whenever a document is attached (gained an `src` param + one caller update), absent for typed intake, mirroring the civicos-ui port. Verified via `test/setup-honesty.test.mjs`: the served SETUP_HTML carries the emission, and checkBundle (the authority) confirms a document bundle WITH content_hash clears the C-2.7 "verified state requires a well-formed content_hash" finding that WITHOUT it refuses release; negative control RUN (drop the line → 3 fail). | resolved 2026-07-31, REC-3 |
| D-67 | gap | 2026-07-30 | **Connections are emitted and nothing stores, relates or shows them.** `meeting_calendar.connections()` produces referential and temporal edges from a real calendar (measured: 39 referential, 11 temporal from one page), including the valuable form, an absence with a due date, such as minutes not published three weeks after a meeting. Nothing consumes them. Three pieces are missing. STORAGE: the plane has `links_to` in `REL_VOCAB` for source-asserted links, and these are SYSTEM-asserted inferences about meaning and sequence, which is a different `asserted_by` and probably a different vocabulary. AGEING: a temporal absence with an `expected_by` needs something to notice when the date passes, which is monitoring's job and not the parser's. PRESENTATION: Bob was explicit that the two kinds are understood and processed differently by people and their assistants and must be presented differently in the UI, and the document page currently has one link surface built for source-asserted links only. | M4 · STORAGE DONE (FW-8, fe81904): connections now PERSIST (connections table, op=connect/connections). Relating/showing (ageing + presentation) remain — slice B + Steps 6/8. |
| D-68 | design | 2026-07-30 | **Seven vocabularies were invented in one day and at least six of them overlap; reconcile before building further.** Named and evidenced in `docs/architecture/CONSTRUCTS.md`. The specific collisions: `CONFIDENCE` and `TYPE_CONFIDENCE` are one idea duplicated because the content axis was split off after the stack axis already had a ladder; `monitor()`, `compare()` and `assess()` are three public entry points to one question, of which `assess()` is the complete one; `diffMembers` and `diffEntities` are the same function written twice, hours apart, the second strictly better because it names which fact moved; `CONTRACT` is derived from document kind and so belongs to the content type rather than the stack handler; event type strings (`delisted`, `minutes_replaced`, ...) are ad hoc inside one content type with no catalogue, which is the lesson the check catalogue already taught the plane; and `meaningful` is a boolean derivable from `SIGNIFICANCE` rather than a second fact. This is Step 0 of the plan and it is the only step that deletes more than it adds. Doing it after the next capability means building that capability twice. | M3 · CLOSED 2026-07-31 (FW-2, commit 6f1a70f): reconciled to one confidence ladder + one recogniser/registry engine + assess() as sole entry; monitoring.mjs, diffMembers and the duplicate loops deleted; docprofile net -27 lines; civicos-ui + full battery green |
| D-70 | design | 2026-07-30 | **The framework treats "which axes of variation exist" as fixed at two, and history says a third arrives within days.** `BIO_Content_Framework_v0_10.md` §4 defines a uniform recogniser shape and a registry per axis, and names the two axes in use (host stack, content type) plus candidates already visible: authority class (the issuing body's own publication versus a mirror), access mode (public, paywalled, login-walled, rate-limited), and format (HTML, PDF, dataset, scanned image needing OCR). §11 names the likeliest next one outright: entities that OUTLIVE documents. A person, a body, a parcel, an ordinance are things the record will want to track across documents, while readings currently scope entities to one document. The framework is written so a new axis costs a registry rather than a rewrite, and that claim was untested: no third axis had been added, so the uniformity of the recogniser shape was an assertion rather than a demonstrated property. The first genuine third axis is the test of whether §9's cost table is real. VERDICT (2026-08-03, COFF-1): the FORMAT axis is that third axis and the claim HELD — `bio-plane/src/formats.mjs` (I7, CONFIRMED 1.0.0) is one registry entry per format, both dispatch sites (the acquire-time subresource guard, `op=pdfstructure`) consult only the registry, and `formats.test.mjs` proves a TEST-ONLY stub format registers and becomes reachable through detect→structure with ZERO edits outside the registry file. §9's cost table is real for the FORMAT axis: a new format costs `registerFormat(entry)` and nothing else (COFF-3/4/5 pay exactly that). The §11 candidate (entities that outlive documents) remains future and is its own axis, not this row's residue. | closed 2026-08-03, COFF-1 — the axis-cost claim demonstrated for FORMAT |
| D-72 | design | 2026-07-30 | **Connections have no GRADE, so a case cannot show which of its links are the weak ones.** RULED by Bob: BIO's contribution should "improve the grade of connections overall". Framework §8.1 defines the scheme on the model of capture grade: A, the source's own identifier with both ends captured; B, an identifier the source uses matched exactly at both ends; C, correspondence such as a name or a date proximity, plausible and never presented as established; D, asserted with no captured basis, recorded as testimony with an author. Nothing implements it. Two distinctions must survive implementation, because both are easy to lose: grade is NOT credibility, since a Grade D connection from a member who was in the room may be the most valuable thing in a case and is labelled by author rather than discounted; and grade is not `asserted_by`, since the author says who claims it while the grade says what would be needed to check it. The value is that grade is IMPROVABLE, and raising it is work a machine does well and a person does slowly. | M4 · CLOSED for connections (FW-8, fe81904): every connection carries the §8.1 grade = weaker of its two ends; established derives from it structurally. |
| D-73 | design | 2026-07-30 | **The connection table models a pair and the domain needs a chain.** RULED by Bob: the meeting-agenda-minutes sequence is ONE form of connected data, and the system must support many types of happenings and progressions, his example being need, budget request, budget approval, RFP, responses, award, signed contract. Framework §8.2 generalises the table into progressions with stages carrying `after`, cardinality, interval and required-ness. The construct that falls out and did not exist before is the MISSING PREDECESSOR: a later stage present with no earlier one. It is usually sharper than a missing successor, because late minutes are an administrative lapse while an award with no solicitation is a question about how public money was committed, and it is epistemically stronger, since a missing successor may simply not have happened yet while a missing predecessor either exists somewhere or never existed and both answers are worth having. Also needed: exception documents that lawfully discharge a skipped stage, so the question becomes "where is the document saying this may be missing" rather than "why is this missing"; and junction checks as data, which is where a case actually gets made. | M4 · CHAIN DONE (FW-8 rule+2-node, FW-9 f000854 N-stage instances with weakest-grade inheritance). The domain's chain is modelled and threaded; exception/junction refinements remain (slice C). |
| D-78 | defect | 2026-07-30 | **Both bundle writers hardcode `surfaced_by: human`, so an assistant cannot honestly surface a focus.** RULED by Bob: an assistant may open a focus unattended, because that support is central to what BIO should offer and a focus is informative and advisory rather than committing. The check catalogue already permits it: C-2.8 accepts `agent` or `human`, which means the doctrine was allowed for at the check level long before any surface could express it. Both writers, `bio-plane/src/setup.mjs` and the ported copy in `civicos-ui/app.html`, emit the literal string `human` for every focus and problem regardless of who is writing. The fix is small and the consequence is not: without it an assistant-surfaced focus is indistinguishable in the record from a member's, which is a false attribution in a system whose whole point is that claims carry their author. RESOLVED 2026-07-31 (REC-3): rather than trust a caller-asserted literal, `surfaced_by` is now SERVER-STAMPED at the trust boundary in `bio-plane/src/index.mjs` op=promote, exactly where author/owner/by/viewer are already stamped — a focus CREATED by a machine credential (viaSession false) records `agent`, one by a member's session `human`, the caller's value overwritten and the bundle.md sha recomputed. This was NOT put in store.mjs's promote: the store byte-trusts bundle.md and 40+ suites depend on that (e.g. projection.test projects an arbitrary surfaced_by verbatim), so the honest decision belongs where the actor class is known — and stamping there fixes BOTH writers (setup.mjs AND civicos-ui/app.html) at once, since the plane, not the surface, is now authoritative. Stamped on the creation act; a revision carries the document's value forward. `test/surfaced-by.test.mjs`: a member-surfaced focus records human, an agent-surfaced one agent (the hardcoded `human` does not survive); negative control RUN (force the stamp to human → 2 fail). The DISPLAY half is D-82, still open. | resolved 2026-07-31, REC-3 |
| D-93 | defect | 2026-07-30 | **The suite CRASHES rather than skips when `ssh-keygen` is absent, and `sshsig` silently runs two tests short.** `ratify.test.mjs` spawns `ssh-keygen` and dies with an unhandled spawn error, and because `npm test` chains with `&&`, everything after it never runs. Separately `sshsig` reports 16 passes without OpenSSH and 18 with it, so a machine lacking it gets a green-looking partial. The signing path is the one place a false green matters most: every negative control against stock OpenSSH is exactly what is lost. The suite should detect the binary and either skip loudly with a named reason or fail loudly, never quietly do less. | M0 · CLOSED 2026-07-31 (M0-4, 0e2c4f0): npm test IS the discovering runner (no suite hides behind a crash); ratify/sshsig/reuse-ratify/signpage skip LOUDLY by name when ssh-keygen is absent |
| D-94 | defect | 2026-07-30 | **The plane's user-agent was illegible and was the sole cause of the only source-access failure this project has, and the current fix is not durable.** `bio-acquire` and `bio-monitor`, two bare tokens across three call sites that did not agree, both refused with 403 by `www.oaklandca.gov`. MEASURED 2026-07-30, one network, one URL, agent varied alone, eight repeats per string: the honest `CivicOS/<version> (+<url>; instance <name>; <purpose>)` string returns 200 and the bare tokens return 403. Three sessions of reasoning about Cloudflare egress reputation were wrong. FIXED in `userAgent()`, one place, asserted through the op by reading what the source received, with a test that it never impersonates a browser. **WHY THIS STAYS OPEN:** we pass because Akamai does not recognise CivicOS. When the string enters its bot directory it will be categorised and refused exactly as `archive.org_bot`, `Googlebot`, `Bingbot`, `GPTBot` and `ia_archiver` already are on that host. Passing by being unknown is not a position. Needs (a) an allowlist request to the City, (b) the mechanical UA-component probe Bob asked for, so that when admission stops the components can be varied systematically and the pattern MEASURED rather than guessed, and (c) a resolvable contact URL, since the one advertised names a path that does not exist on a domain whose registrar transfer is pending. UPDATE 2026-07-30, same day: (b) is BUILT and validated (scripts/ua-probe.mjs), and its validation run measured the discriminator within our component space: purpose and instance are droppable, REMOVING THE CONTACT URL flips admission 200 to 403 uniformly, second-path confirmed (MEASUREMENTS.md). (c) is FIXED in 0.46.0: the agent advertises the repo URL, which resolves, and the ladder shows that fix was load-bearing. (a) remains, and now has a measured, specific ask attached. | CLOSED 2026-07-31 (DEC-1): the allowlist ARM is closed — Bob ruled the request is not a viable mechanism (blocking the `CivicOS/` token is ONE action for the City and total for us; allowlisting is one request per instance, forever, so the mitigation gets dearer exactly as the project succeeds). The UA exposure REMAINS, unmitigated on that axis; the resilience that SCALES is the archive fallback (CAP-3, now the primary resilience item) and member-driven egress diversity (D-120). Standing assumption raised to "we expect to be seen as hostile to the administration". See `SOURCE-ACCESS.md`. |
| D-122 | gap | 2026-07-31 | **Connections are only derived by an explicit `op=connect`, and nothing calls it automatically.** FW-8 made op=connect derive+persist a graded connection from the shared-entity collapse, but it is a `contribute` mutation a caller must invoke; no resolve-time hook and no scheduled sweep runs it. So the UI-4 subject view (and the whole connection/progression machinery) shows connections ONLY for subjects someone has manually connected — honestly rendered as "none derived yet", but usually empty. Needs either derive-on-resolve (when a resolution is written, derive connections for that entity) or a scheduled op=connect sweep on the REC-1 DO-alarm (the same walking-task FW-9/FW-10 deferred). Raised by UI-4. | M4 · CLOSED 2026-07-31 (REC-5, 6fdb763): connections auto-derive via a scheduled sweep on REC-1's DO alarm (a `connection_dirty` watermark stamped on new/raised resolutions; bounded, self-terminating, idempotent). The entity axis self-populates — UI-4's subject view now fills without a manual `op=connect`. |
| D-95 | gap | 2026-07-30 | **No per-host request governor, and no request pacing that resembles a human.** Bob: reduce requests to a rate and pattern that feels to the other side like a person, which costs nothing but latency. Belongs in the Durable Object, which serialises and therefore gives a correct global token bucket for the instance for free; a bucket in Worker memory governs nothing because every invocation is independent. Design follows the subrequest-ceiling pattern already proven: our APPETITE is a configured constant because it is ours, their CAPACITY is discovered by being refused and recorded. Config is a table keyed by host with a cautious default, not a constant named for one site. A 429 overrides the bucket entirely and the cooling-off mirrors the counterparty's own escalation. Cannot solve the shared-egress problem: our governor governs our instance only. RESOLVED 2026-07-30, 0.47.0: host_governor table in the DO, appetite configured (12/min default, per-host override, instance binding), capacity by refusal, 429 overrides the bucket honouring Retry-After, escalation doubles per consecutive refusal and resets on success, jittered gaps with a three-token burst, all three outbound sites wired, refusal named HOST_COOLING_OFF. test/governor.test.mjs, 30 assertions both ways. Chosen constants in MEASUREMENTS.md. | resolved 2026-07-30, 0.47.0 |
| D-96 | gap | 2026-07-30 | **`captured_locators` has no `via` column, so observations from different sources are indistinguishable.** Once an alternative source counts as a re-fetch for monitoring (RULED, see `AUTHORITY-AND-TRUST.md`), the contemporaneity bracket arm will compare archive bytes against live bytes as one stream and report a change that is a provenance difference. Two sources agreeing is STRONGER evidence than one source repeating; two sources disagreeing is not evidence of change at all. The arm cannot tell those apart without knowing which is which. Also needed: a capture carries one locator today and needs two, the document address (for an archive capture, the CDX `original` field through our own normaliser) and the retrieval locator actually fetched. RESOLVED 2026-07-30, 0.47.0: via is part of captured_locators' KEY plus the address/retrieval split; derived, reshaped per the links precedent; the bracket arm reads via='direct' only. Live reshape verified: op=audit 31/31 clean after deploy. | resolved 2026-07-30, 0.47.0 |
| D-97 | gap | 2026-07-30 | **`capture.authority` is asserted by the caller and determined by nothing.** `op=acquire` requires `body.authority` as a non-empty string and files it verbatim; nothing derives it, checks it, or records how it was reached. All of Bob's authority rulings therefore build on an empty foundation rather than refine an existing mechanism. Needs `authority_state` (`determined`/`undetermined`), `authority_basis` recorded in BOTH cases, and `provenance_chain` for delegated hops. Design in `AUTHORITY-AND-TRUST.md`. Blocks the publication gate, which cannot refuse an undetermined authority while no capture has one. RESOLVED 2026-07-30, 0.47.0: acquire records authority_state and a dated authority_basis in BOTH cases plus a single-hop provenance_chain; absence of an assertion is honestly undetermined instead of refused (the old NO_AUTHORITY gate forced callers to invent an authority, which was the false assertion the ruling exists to prevent); C-18.1 learned the three-valued contract with legacy documents staying conformant; C-18.9 implements the RULED publication fence, tested both ways. Verified live from Cloudflare egress. The task list the undetermined state routes to is D-98 and remains open. | resolved 2026-07-30, 0.47.0 |
| D-98 | gap | 2026-07-30 | **There is no task or inbox mechanism, so the authority-determination ruling has nowhere to land.** RULED: undetermined authority becomes a task for the project manager, falling back to a group admin where a project has no manager, forwardable to a member better placed to attest. Every user has a todo list behaving like an inbox, and Bob has flagged that its transport and UI surface might later BE email. Consequences: field bounds must be enforced at WRITE time rather than at render time, because an email renders in a client we do not control where a plausible instruction is what phishing looks like; and the record must be transport-agnostic. Build on the gathering queue's shape, reusing C-18.5's grammar bounds and the quoted-data exporter, since the F5 threat is identical. `member_expertise` already supplies routing. Same underlying gap as D-52, which records that BIO has no notification channel at all. GROUNDWORK 2026-07-30: docs/development/INBOX-GRAMMAR.md fixes the field grammar and routing as a CONTRACT, deriving every bound from settled precedent (C-18.5 field-for-field, the RULED routing order, member_expertise's real columns), so this is now a build task rather than a design task. RULED 2026-07-30: an undetermined capture creates a task automatically at capture, through a PRODUCER/CONSUMER QUEUE. The capture path only enqueues (carrying refers_to, F5-bounded subject, timestamp); a separate consumer holds the sole write path to the inbox, applies routing and the C-19.1 grammar, and dedups idempotently on (refers_to, kind). This stops the daemon credential's blast radius at the queue boundary: a leaked capture token can enqueue noise but cannot write a task, set an assignee, or forge history. Transport (Cloudflare Queue vs durable table drained on schedule) is the consumer's choice at build; table-as-queue is the default for staying in the DO and the audit model. Contract in INBOX-GRAMMAR.md updated. RESOLVED 2026-07-31, 0.49.0. Built against INBOX-GRAMMAR.md as a BUILD task: C-19.1 added to `bio-checks` as a sibling of C-18.5, `refers_to` reusing the exported BUNDLE_ID_RE and the locators reusing `isPublicHttpsLocator` rather than restating either, exported for the write path exactly as `checkGatheringGrammar` is, and run at the gate beside it. Catalog 1.16.5 to 1.17.0; `ratify.test.mjs` CORRECTED rather than loosened to a pattern, because the point of that assertion is that a ratification records WHICH catalog judged it. The producer/consumer split is enforced structurally: the capture path reaches only `task_queue`, keyed on (kind, capture_sha) so a re-capture loop dedups against itself, and there is deliberately NO control-plane op for enqueue at all, which is tighter than the contract asked. `taskdrain` is the sole writer. `actor` on every member action is stamped server-side. THREE PLACES THE CONTRACT WAS UNDERSPECIFIED, resolved in the direction its own typing demanded rather than by redesign: (1) `refers_to` is typed as a bundle id but AT CAPTURE NO BUNDLE EXISTS, since op=acquire returns a document that op=promote later files, so the queue event carries a capture_sha and the consumer resolves it through the register; an event whose capture is never promoted WAITS rather than inventing a subject. (2) 'the referred bundle's project' is meaningless for an Information bundle, which is what these captures are, so a project that CITES the bundle supplies the manager, and with no manager and no active admin the task is honestly `unassigned` rather than addressed to a phantom. (3) the dedup is on LIVE tasks, open OR forwarded, not open alone: a forwarded task is still somebody's work and excluding it would let a re-capture spawn a second task for a subject already in flight, which is the flood the dedup exists to prevent. `test/inbox.test.mjs`, 71 assertions, grammar tested both ways with TASK fixtures. A NEGATIVE CONTROL EARNED ITS KEEP: neutering the write-path grammar check left all 67 original assertions passing, because every task the consumer builds from ordinary input is well-formed by construction, so nothing actually proved the write gate fired; an assertion driving it with a capture registered against a non-canonical bundle id was added and the control now fails 3. Live-verified on biosmoke7 0.49.0 in the SCRATCH namespace end to end: a real undetermined acquire enqueued one event and wrote no task, a drain reported it waiting while unfiled, a promote made it resolvable, the next drain created the task, and the task read back off the live instance is CLEAN under the catalog that shipped with it. NOT DONE: nothing drains on a schedule, so a task appears only when op=taskdrain is called. See D-109. | resolved 2026-07-31, 0.49.0 |
| D-101 | defect | 2026-07-30 | **The template-literal class struck a third time, in schema.mjs, and node --check waved it through.** A D-96 comment quoting a column name in backticks terminated the SCHEMA literal; the stray pair happened to re-balance so `node --check` passed, and only miniflare's parser refused the module. Same class as D-24 (setup.mjs, twice). GUARDED 0.47.0: hygiene.test.mjs now scans schema.mjs exactly as it scans setup.mjs, asserting zero unescaped backticks, zero interpolations, and that the loaded literal ends where the file says it does. The lesson sharpened: a balanced pair of stray backticks defeats a syntax check, so the guard counts ticks rather than parsing. SQL comments quote nothing in backticks. | guarded 0.47.0 |
| D-102 | defect | 2026-07-30 | **The installer wizard binds no INSTANCE_NAME, so every wizard-installed instance identifies as "instance unnamed" in its user-agent.** Found live: biosmoke7's agent said `instance unnamed` through 0.46.0 and 0.47.0 because the binding never existed; grep confirms `newgroup/src` never writes it. The instance component exists so a third party can throttle one operator instead of a provider, and "unnamed" defeats that. The deploy path is FIXED (deploy.mjs binds INSTANCE_NAME from the slug, pushed to biosmoke7, verified live in the provenance chain); the WIZARD path is not, which is the one a new group gets, the same split as D-62. Admission is unaffected: the 2026-07-30 ladder measured instance as droppable. RULED 2026-07-30: the installer wizard must PROMPT the operator for an installation name at setup, and that name becomes the INSTANCE_NAME component of the UA string. This is not just binding a slug the deploy already knows; it is asking a new group what to call themselves, so a third party (e.g. Akamai, IA) can throttle or allowlist one operator by name. The name needs the same write-time bounds as any UA-bound field, since it ends up in a header a counterparty parses. Until the wizard is updated, the reference instance biosmoke7 is bound as 'development' (deploy.mjs binds it; the live agent already carries whatever the last deploy set, currently biosmoke7 as the slug). SIMPLIFIED AND RESOLVED 2026-07-30 (Bob): the instance name IS the worker name. The wizard already collects it as `slug` (validated by slugOk, used for the script and the subdomain), so there was never a prompt to add: it just had to BIND INSTANCE_NAME from the slug it already holds. Done in newgroup/src/index.mjs on BOTH paths, install and update; binding it on update is what retro-names every copy installed before this existed, which are advertising 'unnamed' today, with no operator action. wizard suite +2, 92 passing. deploy.mjs already binds the same way, so plane and wizard now agree and there is ONE source of truth. CONSEQUENCE NEEDING BOB: the reference instance was hand-bound to 'development' earlier this session, which under this rule is a second name that DRIFTS: the next deploy.mjs run overwrites it with the slug 'biosmoke7'. Either the worker is renamed, or the instance is simply called biosmoke7, or an explicit override is added and the one-source-of-truth rule is knowingly relaxed. Not decided here. RULED and DONE 2026-07-30: the interim 'development' override is REMOVED and the reference instance is bound to its worker name, biosmoke7, verified live in the provenance chain. The rule is uniform with no exceptions: instance name IS worker name, on the plane and in the wizard, for this instance and every one that follows. There is no second name and nothing to drift. | resolved 2026-07-30 |
| D-103 | gap | 2026-07-30 | **The governor's state surface is Durable-Object-internal; no control-plane op exposes it.** governorstate and governorconfig exist as store routes for the plane's own use and for suites, but an operator or the UI cannot ask "which hosts are held and why" or set a per-host appetite without a deploy. Small: an OPS entry each, admin/member read for state, admin write for config. Deliberately not folded into 0.47.0, which was already a schema release. RESOLVED 2026-07-30, staged as 0.48.0 (signed 6946361…, NOT yet deployed, awaiting a grant): op=governorstate (admin/member/probe, read) reports which hosts are held and why, so a member watching a capture stall sees the governor is the reason rather than a broken source; op=governorconfig (admin/probe, mutating) sets a host's appetite and REQUIRES a host so no fat-fingered global change is possible, with an omitted appetite resetting that host to the instance default. governorconfig is a null NEEDS entry (bounded by SESSION_OPS.admin like the roster ops, not a section-5 capability), which the capability completeness test confirms. Both ways in the governor suite: a member reads state and is refused config by class. DEPLOYED 0.48.0 2026-07-30, byte-identical to the signed asset, op=audit 31/31 clean. Verified on the live instance: governorconfig wrote web.archive.org at 24/min and governorstate read it back; a member token was refused config by CLASS and the refused write left the value unchanged. | resolved 2026-07-30, 0.48.0 |
| D-104 | design | 2026-07-30 | **A governor-refused monitor tick and a source-unreachable monitor tick look the same to the fallback logic that does not exist yet, and they must not.** When the per-host governor holds a monitor tick (D-95), `op=monitor` returns HTTP 429 `HOST_COOLING_OFF` and writes no tick: honest, because the check did not run. But the archive-fallback ruling shipping next (AUTHORITY-AND-TRUST.md: an alternative source counts as a re-fetch for monitoring after three consecutive failures or fourteen days) counts CONSECUTIVE FAILURES, and a governor refusal is OUR OWN politeness declining, not the source being unreachable. If the fallback counts a governed refusal as a failure, sustained self-throttling would trip the archive fetch: we would fetch from IA because WE paced ourselves, which is backwards and also loads IA for no reason. The distinction is exactly the one the user-agent measurement already taught for a different mechanism: unreachable is a property of a host AND an egress, and "we chose not to ask" is neither. FIX WHEN THE FALLBACK IS BUILT: a governed refusal must not increment the source-failure counter; only an outcome the SOURCE produced (a real 4xx/5xx from the origin, a network failure reaching it) counts toward the threshold. Nothing is wrong in the shipped code, because nothing consumes the counter yet; this exists so the counter is built to exclude governed refusals from the first line rather than discovered to include them after a spurious fallback. RESOLVED 2026-07-31, 0.50.0, and built BEFORE the fallback exactly as this item asked, so the exclusion is designed in rather than discovered after a spurious fallback. New table `source_reachability`, keyed on the normalised DOCUMENT address rather than a host, because one page can be gone while the rest of a site answers. The outcome vocabulary is CLOSED (`success`, `source_refused`, `fetch_failed`, `governed`) on the reasoning that the entire value of the table is telling kinds of not-getting-the-bytes apart, and a free string would let a caller collapse that distinction by accident; `failure` is deliberately not a word it accepts. All four sites in the acquire path are wired: the governor's refusal, the network throw, a non-ok origin response, and success. A governed refusal is counted in its OWN column and moves neither `consecutive_failures` nor the staleness clock nor even `attempts`, because the source was never asked. It is counted rather than dropped so the exclusion can be audited: a number excluded from a decision must stay visible, or a reader cannot tell a source nobody could reach from a source nobody asked, and the verdict's `basis` string says which case it is in words. Bob's ruling 2026-07-31 is the reason this matters more than it looks: the governor is meant to keep traffic low enough that being banned is not a concern, so governed refusals are expected to be COMMON, and a counter that mistook them for source failures would fire the fallback hardest exactly when we were being most polite. One design question the ruling left open and this answers explicitly: the fourteen-day arm measures the length of the CURRENT FAILING RUN, not time since last success, so a document nobody has asked about in a fortnight is not eligible while a document that has been failing since a fortnight ago is; thirty days of nothing but governed refusals is asserted not to age a document into eligibility. `test/reachability.test.mjs`, 34 assertions, every read time-pinned through a `now` parameter so the suite tests the rule and not the day it runs. Negative control: letting governed fall through to the failure path breaks 17 of 34. Live-verified through the real op=acquire path on 0.50.0 for both a 200 and an origin 404. | resolved 2026-07-31, 0.50.0 |
| D-105 | unverified | 2026-07-30 | **The Wayback CDX claims the archive-fallback design rests on could not be checked from this session's egress, so they remain a vendor description rather than a measurement.** ARCHIVE-FALLBACK.md asserts the CDX record shape (SURT urlkey, 14-digit timestamp, original, mime, status, base32 SHA-1 digest, length), the `id_` raw-bytes replay suffix, `warc/revisit` dedup records as dated identical-bytes observations, and the 24-to-60/min rate ceilings. `web.archive.org` returned `hostname_blocked` from the Anthropic container egress on 2026-07-30, so none of it was verified here. The plane's OWN egress is Cloudflare and CAN reach IA (it reaches oaklandca.gov), so the archive session should measure these through the deployed plane before building on them, exactly as the host-stack and user-agent facts were measured rather than assumed. Treat every number and field shape in ARCHIVE-FALLBACK.md as unconfirmed until then. MEASURED AND CLOSED 2026-07-31 through the deployed plane at 0.49.0, store=scratch, three requests to archive.org in total. The container block was reconfirmed first (`Blocked by egress policy`), so the plane's egress is the one that was instrumented, which is also the one that will run the fallback. Record shape CONFIRMED field for field: SURT urlkey, 14-digit timestamp, original, mimetype, statuscode, base32 SHA-1 digest, length; and the `id_` suffix returns raw bytes (HTTP 200, 32,564 bytes, text/html, no redirect). Two things came free that the design wanted: the Memento headers (`Memento-Datetime`, `Link` with rel="original" and a timemap) are ALREADY on the replay response, so 'build to Memento not Wayback' is the lower-effort path rather than only the principled one, and `x-archive-src` names the source WARC. THREE CORRECTIONS, all now written into ARCHIVE-FALLBACK.md and MEASUREMENTS.md: (1) CDX `length` is the compressed WARC record size and NOT the body length, measured 6255 declared against 32,564 received, so it is neither a fixity nor a size check on our bytes; (2) a shared digest can mean an EMPTY BODY, since `3I42H3S6NNFQ2MSVX7XZKYAYSCX5QBYJ` is base32(SHA-1(empty)) and appeared twice in a five-row sample on two 301s, which constrains the best idea in the design: reading a `warc/revisit` as a dated identical-bytes observation holds ONLY for a real 200 with a non-empty body, and any contemporaneity claim built on archive digests must exclude the empty digest explicitly; (3) most rows are not 200, three of five sampled were usable and two were empty redirects, so a fallback taking the most recent row without filtering on statuscode and digest will record a redirect as the document. THE RATE FIGURES REMAIN UNVERIFIED ON PURPOSE and that half of this item stays open as D-111: establishing them requires provoking a 429, and the documented consequence of mishandling one is an hour-long block on Cloudflare's SHARED egress falling on unrelated people, which is not a cost this project gets to impose on strangers to satisfy its own curiosity. | measured 2026-07-31; rate figures remain unverified, see D-111 |
| D-106 | defect | 2026-07-30 | **The installer ships a plane thirteen releases behind the live one, so a new group installs 0.35.0.** `newgroup/src/release.mjs` embeds `RELEASE_VERSION = "0.35.0"` while the plane is at 0.48.0. Everything this session shipped is therefore absent from a fresh install: the legible user-agent (D-94, and with it the ability to reach oaklandca.gov at all), the per-host governor (D-95), three-valued authority and the provenance chain (D-97), the observation-source split (D-96), and the governor's operator surface (D-103). A group installing today gets an instance that CANNOT capture from the City and identifies itself as `bio-acquire`, which is the exact failure this session spent three releases fixing. Found incidentally: `npm run build` regenerates the embedded blob, so the artifact showed as modified in the working tree while the declared version did not move. TWO THINGS TO FIX, and they are separable: (a) cut a new installer release embedding 0.48.0, and (b) the embed step should REFUSE when the embedded version does not match the plane's `package.json`, because a silently stale installer is the failure mode that let this drift thirteen releases without anyone noticing. (b) matters more than (a): (a) is a one-time catch-up and (b) is why it will not recur. Sovereign instances are the whole distribution model, so this is not cosmetic. RESOLVED 2026-07-31, and the mechanism was NOT a forgotten build step: `embed-release.mjs` read the embedded version out of `bio-plane/wrangler.jsonc`, and a tree-wide search found that field has exactly ONE reader, this script. `scripts/deploy.mjs` binds `VERSION` from its command line, so every release from 0.36.0 to 0.48.0 deployed correctly while the config literal sat at 0.35.0 untouched. Re-running the old embed today would still have produced 0.35.0, so (a) was not reachable without (b). Fixed: `package.json` is now the declared authority, `resolveVersion` refuses on ANY disagreement in either direction (a wrangler config ahead of package.json is the same drift mirrored, not a newer release), the refusal names both files and prints the exact edit, and it runs BEFORE the esbuild so a mismatch costs a second and leaves no stale artifact. `wrangler.jsonc` bumped to 0.48.0. TWO FURTHER STALE ARTIFACTS surfaced while fixing this, both consistent with the same pattern and both now corrected: `bio-plane/package-lock.json` declared 0.45.0 (three releases behind, self-healed by `npm install`, which means no clean install had been run in the plane tree since 0.45.0), and the CHECKED-IN `bio-plane/dist/bio-plane.bundled.mjs` hashed `ef23a492…`, matching neither 0.48.0 nor the signed asset. So the version lived in four places and three of them had rotted. Only `package.json` and the deploy command line were right, which is precisely why the live plane was correct while everything built from the tree was not. `package-lock.json` is deliberately NOT gated in hygiene: npm rewrites it on any install, so a check there would fail on a stale checkout rather than on a real defect. Two guards for one invariant, deliberately: the embed refusal cannot be bypassed, and a new check in `bio-plane/test/hygiene.test.mjs` fires cheaply at the moment a release bump creates the drift, in the thread that created it, rather than weeks later in whichever thread next builds an installer. `newgroup/test/embed.test.mjs`, 15 assertions, including a guard that resolves the REAL tree; negative control confirms hygiene fails when the drift is reintroduced. VERIFIED: the rebuilt plane bundle is byte-identical to the signed 0.48.0 asset (`69463617…`), so the installer's built-in copy and the repository copy are now the same bytes and both paths install the same plane; deployed installer read back from the account embeds 0.48.0, contains no occurrence of 0.35.0, and still carries `bindings: []`, the structural no-token guarantee. ONE CORRECTION TO THIS ITEM AS WRITTEN: the blast radius was overstated. `selectRelease` prefers a NEWER repository release over the built-in, and `release/RELEASE.json` has carried a correctly signed 0.48.0 throughout (verified this session against stock `ssh-keygen -Y verify`, with altered-bytes and wrong-namespace negative controls both rejecting), so an install with working network and a verifying signature was already getting 0.48.0. The built-in 0.35.0 landed only when the repository was unreachable or its copy failed integrity or signature. The defect was real and worth the priority it was given, but what it actually removed was the fallback, not the common path. | resolved 2026-07-31 |
| D-108 | defect | 2026-07-31 | **`deploy.mjs` verifies the deployed BYTES, but a live Durable Object can keep serving the previous code after the verification passes.** Found immediately after deploying 0.49.0: `op=tasks` answered `unknown op: tasks` from the live instance while the deployed script demonstrably contained the op, and the same call succeeded minutes later with no redeploy in between. The error format is the DURABLE OBJECT's (`unknown op: <op>`), not the control plane's (`unknown op`), which locates it precisely: a fresh Worker isolate accepted the op and forwarded it to a DO still running the 0.48.0 route map. The deploy script's discipline is right about what it checks and silent about what it does not: it reads the script back and hashes it, which proves the BYTES landed, and `/version` is served by the Worker rather than the DO, so a post-deploy check of the version endpoint reports success while every DO-routed op still answers with the old map. That window is invisible to both checks. This is not the deploy script being wrong, it is the verification being narrower than the claim it appears to make. The fix is a post-deploy probe that reaches an op THROUGH the Durable Object and asserts the new surface answers, retrying briefly rather than once, the same fail-closed patience `deployed()` already applies to the upload. Until then, a session that verifies a release in the first seconds after deploy can conclude a shipped op is missing when it is merely not yet cycled, which is exactly the wrong conclusion to act on. CORRECTED AND WIDENED 2026-07-31 while deploying 0.52.0: the original diagnosis, a Durable Object lagging a fresh Worker isolate, was too narrow. The WORKER itself was stale too. Four calls seconds apart in one command got `/version` 0.51.0, two answers from the previous build, and one from the new one, so the rollout is PER-ISOLATE AND NOT ATOMIC and a verification in that window can receive a mix and reach opposite conclusions about the same property. This nearly produced a false security finding: a forged-locator probe appeared to show the new code honouring a caller-supplied replay locator, when the old code was answering. The requirement is therefore stronger than 'retry until the new op appears': a verification must CONFIRM IT IS TALKING TO THE NEW BUILD before believing any answer, including a failure. `/version` is the cheapest such confirmation, it was already available, and it was already answering wrong when first asked. Nothing here is a fault in deploy.mjs, which verified the bytes correctly every time; the whole gap is in what is done after it returns.  RESOLVED 2026-07-31, 0.53.0: `scripts/deploy.mjs` now waits for the instance to SERVE the version before reporting done, and says loudly when it does not. It is not a pass/fail on the deploy, because the deploy succeeded; it is a gate on believing anything measured afterwards. It earned its keep on its first real use: deploying 0.53.0 it printed ROLLOUT NOT CONFIRMED after 60s, which was correct, and the version appeared shortly after. On success it still says that only the WORKER is confirmed and Durable-Object-routed ops may lag, because that half is not something the deploy can check without an instance credential it deliberately does not hold. | resolved 2026-07-31, 0.53.0 |
| D-110 | drift | 2026-07-31 | **`setup.mjs` still explains a refusal the plane no longer makes.** `acquireWhy` maps `NO_AUTHORITY` to "Say who issued the document." D-97 (0.47.0) made authority three-valued and REMOVED that refusal deliberately: a caller who cannot name the issuing party now leaves the capture honestly undetermined rather than being forced to invent an authority to get past the door, which was the false-precision failure the ruling names. The string is unreachable, so it costs nothing today, but it is a copy of a rule that was overturned sitting in the surface a member reads, and the next person to touch that file will read it as current. Small, and worth removing with the D-98 inbox surface when the UI grows one, since a member who sees an undetermined capture will want the task beside it. RESOLVED 2026-07-31 (REC-3): the stale mapping was deleted from `acquireWhy` in `bio-plane/src/setup.mjs`, and the dead reason's token now appears NOWHERE in the served surface (SETUP_HTML) — not in code, not even in the replacing comment, since carrying the name of an overturned rule into the page a member reads is the drift this row names. `test/setup-honesty.test.mjs` asserts SETUP_HTML no longer contains it while the kept refusals (BAD_LOCATOR) still are explained; negative control RUN (re-add the branch → 1 fail). | resolved 2026-07-31, REC-3 |
| D-112 | gap | 2026-07-31 | **The archive fallback's decision half shipped in 0.51.0; the capture half is composed by the CALLER rather than by the plane.** `op=archivelookup` applies the fence, governs the CDX fetch, parses, selects and returns `capture_with: {op: acquire, locator, via, documentAddress}`; `op=acquire` now honours `via: 'archive.org'` and the document-address/retrieval-locator split, files on the same `captured_locators` row as a direct capture, and grades the result C. So both halves exist and each is tested, but nothing composes them into one act, which has two consequences worth naming. First, the CDX evidence does not reach the stored provenance chain: `archivelookup` returns the second, unsigned hop and `acquire` does not receive it, so a capture filed this way today carries the one-hop direct chain with `via: archive.org` on the locator rather than the two-hop disclosed chain the ruling requires. Transitive trust is accepted WHERE DISCLOSED, and right now the disclosure is in the lookup response rather than in the record. Second, a caller could in principle call acquire with `via: archive.org` without having passed the eligibility fence at all. Neither is a hole an attacker gains from (the fence protects the Internet Archive from us, not the record from anyone, and grade C is the conservative direction), but the chain gap is a correctness matter and should close before anything files an archive capture into the real record. The fix is one op that runs the lookup and the capture together and threads the hop through, NOT a caller-supplied provenance parameter: a chain hop a caller can hand us is a chain hop a caller can invent. RESOLVED 2026-07-31, 0.52.0. Composed into ONE act rather than two: `op=acquire` with `via: 'archive.org'` and an `address` now runs the eligibility fence and the CDX lookup ITSELF, through the shared `archiveSelect` helper, and builds the second provenance hop from the CDX record that same call fetched. The stored `provenance_chain` is therefore two hops for an archive capture: ours, honest that we fetched the replay address rather than the publisher, and theirs, carrying the CDX evidence with `bound: false` and the reason it is unsigned. A caller supplies no hop, no replay locator, no document address and no grade; the only field written back onto the request body is the locator, and `documentAddress` no longer exists on a request body at all. Guarded at SOURCE in `hygiene.test.mjs` rather than only at runtime, because a runtime test can show one forged request was ignored while a source check shows the field cannot be read: five assertions that nothing reads a documentAddress, a hop, a chain or a grade off the body, and that the hop and address come from `archiveSelect`. Live on 0.52.0: a request carrying `locator: https://evil.example.com/x` and a forged `documentAddress` answers NOT_ELIGIBLE, the fence having run before anything was fetched, stable across three rounds. Authority is deliberately NOT auto-set to the Internet Archive: doing so would make an archive capture 'determined' and publishable without any member ever asserting who issued the document, when the publisher is reached by delegation and disclosed in the chain rather than asserted by us. | resolved 2026-07-31, 0.52.0 |
| D-114 | design | 2026-07-31 | **C-18.9 may be gating publication on the wrong field, and the fence was NOT changed because loosening it is irreversible.** Bob, 2026-07-31: "A capture can be published if there are no undetermined authority links in the provenance. So recording that the capture was done through IA is proper, even if the content authority is still undetermined." That distinguishes two things this codebase currently treats as one. The PROVENANCE authority is who served each hop and is always known: we know we fetched it, and we know the Internet Archive served us. The CONTENT authority is who ISSUED the document and is frequently undetermined. C-18.9 as shipped refuses a bundle at or past verified when any provenance document carries `authority_state: 'undetermined'`, which is the CONTENT axis, so on the reading above it is gating on the wrong one. WHAT WAS DONE IN 0.54.0: only the part that was unambiguous and cheap, namely recording `capture.authority: 'Internet Archive'` on an archive capture, which was Bob's actual correction and removes the objection that doing so would smuggle a document into publishability. WHAT WAS DELIBERATELY NOT DONE: C-18.9 was left exactly as it is. Publication is the one act in this system that cannot be undone, because a published hash answers forever by design, so a fence around it is the worst possible place to apply a session's reading of one sentence. If the reading is right, the correct change is to gate on the provenance chain having no undetermined attestor and to let content-undetermined material publish WITH its undetermined state stated, which is a real doctrine shift and belongs in AUTHORITY-AND-TRUST.md over Bob's signature rather than in a commit message. NEEDS BOB.  RESOLVED 2026-07-31, 0.55.0, catalog 1.18.0, after Bob delegated the determination: "You do what you know to be right." The fence now sits on the PROVENANCE chain rather than the content axis. Refused at or past verified: a captured document with no provenance chain, any hop naming no attestor, content authority undetermined AND silent (no dated basis), and authority_state 'determined' with no authority named. Content-undetermined material WITH a dated basis may now be published. The reasoning is the project's own and is written into AUTHORITY-AND-TRUST.md: refusing publication on a missing attribution recreates at the publication gate the pressure to invent an authority that D-97 removed at the intake gate, and moving that pressure later makes it worse rather than better, because by then a member has done the work and wants it out. What a published hash claims is bytes, address, date and route, not that the document is authentic municipal record. The conformance suite was CORRECTED rather than exempted, both ways, and now asserts that a two-hop archive chain passes, which is the point of disclosure. Ratification remains a member's signed act and an undetermined capture still raises an inbox task, so the question still reaches a person; it simply no longer stops the record saying what it honestly holds while the question is open. | resolved 2026-07-31, 0.55.0 |
| D-117 | gap | 2026-07-31 | resolved 2026-08-07 (BOB sweep) — **the claim is STALE: the instrument is NOT blind to the fleet.** Verified in source: `coverage.mjs:145-187` discovers members from `fleet-member.json`, reads each member's SURFACE table, checks every surface op is reached and a negative control declared, and `fleetUnreached`/`fleetUncontrolled` are gated under `--strict` (`coverage.mjs:186-187`, reported at `:243-244`). Consequence carried to the IS design: IS-9's fleet member must ship `fleet-member.json` + a SURFACE table + suites, or `--strict` fails from its first commit |
| D-118 | unverified | 2026-07-31 | **Service bindings and multi-script accounts on Workers Free are UNMEASURED, and the tiering rests on them.** Workers Paid is an optimisation and never a requirement (RULED), and the sovereign installer puts instances into other groups' accounts, most of which will be Free. Whether a Free account can run a second script, whether service bindings are available on it, and what they cost against the request and CPU budgets are all currently assumptions — the same category as the subrequest ceiling before it was calibrated and the Wayback rate figures that stay third-party. If a Free instance cannot reach `pdf-worker`, Tier 1 is not an optimisation of PDF text extraction, it is the ONLY PDF text extraction such an instance has, which changes Tier 1 from a sizing exercise into a floor. Measure through the plane's own egress before building the Tier 2 path, exactly as D-105 was measured rather than believed. CLOSED 2026-07-31 (CPDF-7), measured on the project's own account (`MEASUREMENTS.md`, the CPDF-7 section): the account is on Workers FREE (the API refused `limits.cpu_ms` with "not supported for the Free plan"), and on that Free plan a SECOND script deploys, a SERVICE BINDING between two Workers deploys and RESOLVES, and a cross-Worker call executes end-to-end for ~1 ms wall-clock / one subrequest (25 binding calls in one Free invocation succeeded). So the conditional does NOT fire: `pdf-worker` (CPDF-6) is viable on Free and central, and Tier 1 (CPDF-4) is not forced to be the floor by any binding limitation. NARROWED, not carried: the one residual Free concern is pdf.js against the 10 ms Worker-CPU/invocation ceiling (measured non-raisable here) — a DIFFERENT axis that already lives as CPDF-1's gated Worker-CPU follow-on and constrains an in-plane Tier 1 equally, so it is not a differentiator. | M2 · closed 2026-07-31 (CPDF-7); the residual CPU-ceiling question stays with CPDF-1's follow-on |
| D-119 | gap | 2026-07-31 | **A reused subresource is never verified against its source, and a ratified bundle can carry one silently.** Asset reuse (0.38.0 onward) spends zero subrequests on furniture the record already holds, which is what makes the 50-request ceiling survivable, and the manifest honestly records `fetched_this_capture: false`. What does not exist is the other half: nothing detects post-hoc that a reused asset later changed, and nothing re-fetches reused parts when a bundle is ratified — the moment its claim changes from working material to evidence. The reuse path's own manifest note TELLS a reader that a ratified capture must re-fetch, and nothing does, so the note describes an obligation the code does not honour. DECIDED 2026-07-31 by session BOB under Bob's delegation, after Bob confirmed the adjacent ruling (a reused asset IS allowed in evidence) and did not recall the mandatory-re-fetch one, which had been recorded once by a session and contradicted by its own document's footer: (a) post-hoc detection from `site_assets` is unconditional and free; (b) at ratification the re-fetch is mandatory as an ATTEMPT AND A RECORD, never as agreement, so `confirmed`/`changed`/`unavailable` all ratify and only silence is forbidden — which is what keeps a source that has gone dark ratifiable, the circumstance the record exists for; (c) a PLAIN GET rather than `If-None-Match`, reversing the conditional-GET suggestion in `CAPTURE-SCALING.md`, because both cost one subrequest and a 304 is the origin's assertion where a hash is our own evidence; (d) a fourth outcome `not_attempted`, recorded with its reason, for parts the invocation budget could not reach, because a ratification that quietly checked 40 of 60 parts claims more than it verified. Full reasoning in `CAPTURE-SCALING.md` item 6. | M2 · queued CAP-4 |
| D-130 | defect | 2026-08-01 | **The intake surface writes `counterparty: to be named` on every action, which passes C-2.10 and makes the record assert a counterparty it does not have.** `civicos-ui/app.html:1752` emits `action_kind: other`, `risk_tier: 1`, `counterparty: to be named` as defaults; C-2.10 refuses an EMPTY counterparty and accepts a non-empty one, so the placeholder satisfies the check by being a string. This is the overclaiming class — a field the record fills so a gate will pass — and it lands in the one construct that carries IMPACT, where the counterparty is the whole point of the object. It is also the exact pressure D-97 removed at the intake gate when it made authority three-valued rather than forcing a caller to invent one: `undetermined` is first-class and must be STATED, and "to be named" is a placeholder pretending to be an assertion rather than an honest undetermined. Fix has two halves and the second is the real one: stop writing the placeholder, and give C-2.10 a way to say the counterparty is not yet determined so a member is not forced to invent one to save a draft. Found by the process inventory, 2026-08-01, not by any suite. | M7 · CLOSED 2026-08-04 (REC-23): counterparty three-valued on source's shape; the placeholder refused by name at BOTH emission sites (app.html + setup.mjs — D-130 had named one); coherence rule added; NC reproduced D-130 verbatim before the fix. UI half moves to UI-19 (rendering) + UI-15 (ADD_TYPES absence). |
| D-131 | defect | 2026-08-01 | **A raw NUL byte in `store.mjs` makes the repo's largest source file INVISIBLE to the shell's `grep`, silently.** Found while verifying a research pass. `bio-plane/src/store.mjs:3833` builds a composite dedup key `sk + <NUL> + cs` and writes the separator as a RAW BYTE (offset 230,100) rather than the escape `\u0000`. The intent is sound (NUL cannot occur in either component) and the runtime value is identical; the damage is entirely in tooling. `grep` in this environment wraps ugrep with `-I` (skip binary), one NUL makes the whole file binary, and every grep against the store core returns **exit 1, no output, no warning** — a false negative indistinguishable from 'no matches'. Confirmed by differential: the shell's `grep -c alarm` returns nothing while `/usr/bin/grep -c` and `command grep -c` both return 45. THIS IS THE PROJECT'S FAVOURITE DEFECT CLASS, third instance: `node --check` does not catch it, every test passes, and a verification tool silently stops verifying (siblings D-24, D-101 — a stray backtick terminating a template literal, a balanced pair defeating `node --check`). It matters because sessions verify claims about `store.mjs` with grep constantly: the instrument said the code was not there and the code was there, nearly causing a CORRECT research finding to be discarded. FIX: write the separator as the escape, and add a hygiene assertion that no file under `bio-plane/src/` contains a raw control byte outside tab/newline/CR — same shape as the existing backtick guard, closing the class. | M0 - closed 2026-08-03 (REC-27) — the separator at store.mjs:3833 is now the `\u0000` ESCAPE, value-identical, with a comment stating why; plain `grep -c "CREATE TABLE" bio-plane/src/store.mjs` returns 9 WITHOUT `-a`; and the class is closed as the row asked: hygiene.test.mjs scans every file under `bio-plane/src/` for a raw control byte outside tab/LF/CR, the same shape as the backtick guards. |
| D-137 | defect | 2026-08-01 | **The D-113 class check has a blind spot the size of eight tables, and four of them are silently unpurged.** VERIFIED: `bio-plane/src/schema.mjs` declares 44 tables and `store.mjs` hand-creates EIGHT more — `admin_votes`, `does`, `export_log`, `member_expertise`, `project_owner_votes`, `project_participants`, `selection_items`, `selections` — for 52 in total. Of the eight, FOUR appear in no `DELETE FROM` anywhere in `store.mjs`: `admin_votes`, `does`, `export_log`, `member_expertise`. D-113 was closed by a hygiene check asserting every `CREATE TABLE` is named in purge or explicitly exempted, and that check reads the SCHEMA LITERAL — so tables created outside it are invisible to the guard that exists to catch exactly this. `project_participants` is keyed on a bundle id, which makes it the same silent-leftover shape D-113 was raised for: a whole-store purge reports scope ALL and leaves rows. THE LESSON IS ABOUT THE CHECK, NOT THE TABLES: closing a class by parsing one file closes it only for that file, and the check's own success made the second location invisible. The fix is to enumerate tables from the RUNTIME schema rather than from a source literal. | M0 - closed 2026-08-03 (REC-27) — hygiene.test.mjs now parses store.mjs's CREATE TABLE literals as well as schema.mjs's (the name anchored to the opening paren / USING, so the prose-minted phantom table `does` in this row's own enumeration cannot recur; the eighth table it missed was the FTS5 virtual `bundles_fts`) and reports **52 of 52 tables covered by purge or a stated exemption**. Of the eight: `bundles_fts`/`selections`/`selection_items` were already purged; `project_participants` and `project_owner_votes` (keyed on project_id, a bundle id) added to purge in BOTH arms with stats/removed counters and behavioural assertions in projects.test.mjs; `member_expertise` (roster identity, like members), `admin_votes` (append-only governance record so an ejection stays auditable) and `export_log` (an export can never happen silently, and a purge must not erase the evidence one did) EXEMPT with those reasons stated in the allowlist. Negative control run: removing the project_participants DELETEs fails hygiene NAMING it (51 of 52) and fails 4 behavioural assertions. |
| D-151 | defect | 2026-08-01 | **A machine credential can RESOLVE an unassigned task, so an obligation can be closed with no member act — and the code's own comments say it cannot.** VERIFIED this turn in the source. `Store.#refuseNotYours` (`bio-plane/src/store.mjs:6943-6946`) returns `null` — allow — the moment `row.assignee === "unassigned"`, BEFORE it examines who is calling; and `taskforward` / `taskresolve` both carry `"probe"` in their `classes` (`bio-plane/src/index.mjs:271-272`). So `op=taskresolve` driven by a machine credential closes the task and writes a history entry reading `actor: "token:probe"`. The fence's own comment states the guarantee it makes — *"a daemon cannot close somebody's work"* — and that is exactly true and exactly insufficient: **a daemon cannot close somebody's work; it can close NOBODY'S work, and closing is the act.** Two comments in the same codebase already forbid what the code permits: `taskResolve`'s own header calls it *"Also a member action"*, and `index.mjs:602-604` calls both verbs *"MEMBER actions performed by a PERSON through their session"*. This is the same class as D-137 and D-138 — a guard whose comment describes a stronger property than its code enforces, so a later reader takes the comment as the specification. THE FIX IS A PRECEDENT ALREADY IN THE FILE, not a new rule: `release` (`store.mjs:1857-1861`) refuses any author matching `/^token:/` with `MACHINE_CANNOT_RELEASE`. Refuse at the ACT, not at the fence, so the refusal does not depend on assignment state: `MACHINE_CANNOT_RESOLVE` and `MACHINE_CANNOT_FORWARD`, plus removing `"probe"` from the two ops' `classes` and correcting `#refuseNotYours`'s comment to state what it actually guarantees. The fence STAYS — it answers *is this THIS member's task* and the act refusal answers *is this a person at all*; the second is not derivable from the first, which is why the hole existed. `taskdrain` is deliberately untouched: it is the daemon's path and draining is not resolving. Lines up with REC-13's `MACHINE_CANNOT_CONCLUDE`, so the record ends with ONE rule in three places rather than three judgements — a machine may surface, route and prepare; a member authors, resolves and concludes. NEGATIVE CONTROL for the fixing item: remove the act-level refusal and a `token:probe` credential resolves an unassigned task while every existing task assertion still passes — which is the state today. Raised by DEC-7. | M8 · CLOSED 2026-08-03 (REC-28): act-level refusals by shape before the row is read; probe declassed with the ACL limit stated; fence kept; NC re-run by CONDUCT. |
| D-157 | defect | 2026-08-02 | **`op=memberlist` hands the cover↔handle PAIRING to ordinary members and to `MEMBER_TOKEN`, and §3 says only administrators see them together.** MEASURED 2026-08-02 (session BOB, `MEASUREMENTS.md`): an ordinary member's session (`administer: false`) and the shared `MEMBER_TOKEN` machine credential each receive `handle = cover` for every member on the LIVE `bio` store, byte-identical to the `ADMIN_TOKEN` view. `BIO_Membership_Architecture_v1.md` §3 and `v2` §3 are identical and unambiguous: *"Pairing. Only administrators see cover and handle together."* The split is the anti-deanonymisation mechanism — `schema.mjs` on `members.cover`: *"the cover-and-handle split exists precisely so that a roster seized or subpoenaed does not deanonymise the group."* **The source contradicts itself in three places:** `index.mjs:407` grants `classes: ["admin","member","probe"]`, the comment eight lines below it says *"All admin-only: memberlist pairs cover with handle and only administrators see those together (section 3)"*, and `store.mjs:5810` says *"Every op that reaches this is admin-only at the control plane."* Neither comment is true. **`PROBE_TOKEN` is NOT exposed** — `scopeFor` (`index.mjs:862`) confines probe class to the `scratch` store, a different DO with its own member table; that half was measured before it was claimed. **The fix is a projection, not a refusal:** §3 also says *"Members and the public see handles"*, so a member legitimately needs the handle roster (project participant lists, authorship) and must not receive `cover`. `test/members.test.mjs:192` asserts *"machine member token reads the roster"* and must be CORRECTED rather than exempted (`CLAUDE.md`), with a comment saying why the old assertion was wrong; the negative control is a non-admin caller receiving a `cover` field at all. | M7 · CLOSED 2026-08-03 (REC-29): the projection withholds cover from every non-administrator, server-stamped, fail-closed; members.test.mjs corrected; NC re-run by CONDUCT. |
| D-173 | gap | 2026-08-04 | **Every intent-layer READ in the UI forgot to open the plane's `{ok, result}` envelope** — against a live plane the Subjects screen answered "no subject known" for subjects IN the registry and UI-9's what-the-record-knows panel rendered blank, while BOTH suites passed because both mocks answered unwrapped: the one shape the plane never sends. The mock-mirrors-the-defect class, live-fire's own lesson one layer up. | M8 · CLOSED 2026-08-04 (UI-13): fixed at one seam (recR, 17 reads rewired); the mock corrected with a dated comment. The CLASS lesson stands: a UI mock must answer the wire shape. CLASS CLOSED STRUCTURALLY 2026-08-04 (UI-23): six instances total (the sixth — signIn — meant password sign-in never worked live); the two-arm envelope guard now fails any unwrapped mock and any transport call outside the seams. |
| D-188 | design | 2026-08-04 | **"Bias debt" reads as "bias", so the publication rule sounds like it forbids what the doctrine requires — one word, two meanings, caught free.** DEC-20 ruled 2026-08-02 that only a HUNCH blocks publication and that ordinary declared bias is disclosed and TRAVELS with every published case (*"bias debt is DISCLOSED; hunch debt is DISQUALIFYING"*). But `BIO_Declared_Bias_v0_1.md`'s bias-debt section still reads *"a work product carrying bias debt cannot advance its workproduct_state or be ratified for publication until the debt is settled"*, and a reader who does not already know that `debt` here means specifically a hunch reads it as *bias must be cleared before publication* — the opposite of the doctrine. **THE EVIDENCE IS THAT BOB HIMSELF RE-READ IT AS A CONTRADICTION on 2026-08-04, and he authored the ruling.** If the person who decided it misreads the text, every later reader will. This is the D-156 class (one term, two senses) and it is free right now: no table, op or surface names either sense. FIX: say HUNCH DEBT wherever the disqualifying rule is meant, reserve *bias debt* for the general disclosed class, and state the DEC-20 principle at the head of the section rather than only in the decision register. | M10 · **CLOSED 2026-08-05 by REC-47** (DEC-46 (d)), and the sweep found more than the row predicted, which is why the closure is worth reading rather than ticking. **The row called this "documentation pass, no code". IT WAS NOT.** Four CODE sites named the disqualifying rule with the disclosed term — `schema.mjs`'s `grade_source` comment, `bio-checks.mjs`'s `GRADE_SOURCES`, `store.mjs`'s hunch-composition comment, and six spellings in `earnedbasis.test.mjs` — all corrected. **AND THE SWEEP FOUND A SECOND, WORSE CLASS THE ROW DID NOT NAME: the pre-DEC-20 BLANKET RULE is still ASSERTED, as live doctrine, in five places.** (1) `bio-plane/src/queuestate.mjs` and (2) `docs/development/NOTIFICATIONS.md` both describe the `bias-debt` obligation kind as *"blocks a transition"* — two copies agreeing with each other on the overruled rule, and the producer is unbuilt (D-86's remaining half), so it would have been BUILT TO THE SENTENCE. (3) `BIO_Content_Framework_v0_10.md` rests a whole section ("Decay is loud and never blocking") on *"bias debt BLOCKS"* as its contrast partner, with a two-row asymmetry table — corrected to three rows, and the argument comes out sharper: what makes something blocking is not that a lens changed but that a member asserted a GRADE the evidence does not support. (4) `BIO_Design_Requirements_v2.md`'s July-27 Bob addendum states the blanket rule in the same breath as the requirement it contradicts — annotated rather than rewritten, because it is a dated record of a direction. (5) `BIO_Intake_Doctrine_v1_1.md`'s ratification clause. Also corrected: `BIO_Declared_Bias_v0_1.md` (the banner D-188 asked for, at the head of the section; the blocking sentence STRUCK rather than deleted so a reader who remembers it can see it is gone; the DEC-15 heading; Bob's own quotation left VERBATIM with the vocabulary reconciled after it rather than edited), `BIO_Case_Making_v0_1.md`, `BIO_Interaction_Constructs_v0_1.md`, `MILESTONES.md` D-154. DELIBERATELY NOT TOUCHED: `newgroup/src/release.mjs` (bundled copy — D-193's rule, refreshes at the next cut), `bio-plane/dist/` (build artifact), and the occurrences in `README.md`, `CONSTRUCTS.md`, `PROCESS-INVENTORY.md` and two further `BIO_Content_Framework` sites, which use *bias debt* in its CORRECT general sense and are the term's legitimate home. |
| D-197 | defect | 2026-08-04 | (renumbered by CONDUCT at UI-37's integration — it was written as D-195 and collided with an existing D-195; **the older row keeps the number, as with every collision this session**) **CLOSED 2026-08-04 by UI-37, and it was THREE refusals across THREE surfaces rather than the one this row recorded — see UI-37's landed line; the worst was `pubList`, which rendered a refusal as "This group has not published any case files yet", a claim about the ENTIRE record.** The public verification surface told an uncredentialed reader that the record has no such part, when the plane in fact REFUSED to answer.** `pubVerify` calls `op=verify` through `apiQ`, which — unlike `rec` — does not throw on `ok:false`; a refusal envelope carries no `published` field, so it falls to the else branch and renders *"NOT PUBLISHED. No published part answers to that hash. A hash that was never ratified and a hash that never existed are the same answer here, deliberately."* MEASURED by UI-36 and VERIFIED INDEPENDENTLY BY CONDUCT at both ends: `op=verify` refuses a malformed hash with `{ok:false, error:"verify requires sha256=<64 lowercase hex>"}` at HTTP 400; `api()` never checks status; and the container row composes its hash as `bundle_sha || ""`, so an empty hash reaches this by a CLICK. **This is the worst shape of defect this project recognises — the record asserting something it does not know — and it stands on the one surface whose entire purpose is that a stranger can verify WITHOUT our cooperation.** The sentence is also unusually convincing, because its second clause explains why the two cases are deliberately indistinguishable, which is true of a real not-published answer and false of a refusal. NOT FIXED AT DISCOVERY, deliberately: DEC-49 is open with Bob and the pre-authentication surfaces must not move while its measurement is being taken. UI-36 pinned it with an assertion that says at the site that it pins a DEFECT. | M8 · **CLOSED AT BOTH LAYERS 2026-08-05. UI-37 closed the RENDERING (three refusals across three surfaces); REC-52 closed the LAYER BENEATH IT, and that half was eleven sites rather than the two its item named.** The plane itself converted its own failures into substantive negatives, so a surface that faithfully rendered what it received rendered a lie no surface could correct: `op=verify` and `op=bootstrap` spread an unchecked result into `{ok:true}` at HTTP 200; `op=publishedmanifest` did the same by a DIFFERENT route (`result: undefined`, which `JSON.stringify` DROPS) on the op that fills the published INDEX — so the rendered consequence was the WHOLE record, which is the sentence D-173 and this row turn out to share; `op=publishedbytes` answered THIS ROW'S OWN SENTENCE ("a hash that was never ratified and a hash that never existed are the same answer here, deliberately") over a store that never answered, so the wording this row records as unusually convincing was being minted in the PLANE as well as at the surface; `op=publishedcase` synthesised `NOT_PUBLISHED`, and its `publishedtargets` read made every basis leg of every finding say "what it rests on is not in the published record"; `op=affordances`, `op=queue` and `op=monitor` minted `NO_FACTS`, `NO_QUEUE` and `ABSENT`; `op=knock` reported a store silence to an anonymous member of the public as HTTP 429; and the SESSION lookup reported it to a signed-in member as a verdict on their credential. FIXED AT A CHOKEPOINT (`doAnswer`/`storeSilent` in index.mjs) with a source-level sweep asserting no handler can spread an unchecked result, its reach proved as a DELTA, and `test/plane-envelope.test.mjs` driving a REAL Durable Object failure at each site. Both directions are held: a genuine not-published answer still reads as not-published, with its own arm. TWO SITES REPORTED AND DELIBERATELY NOT FIXED — both inside REC-47's concurrently-held publish/ratify block, both this same class: `do/list`'s result, defaulted to an empty array, hands the gate an EMPTY known-id set so every reference reads as unresolved, and `do/reusedparts`' silence reads as "nothing was reused". Pinned as RELATION assertions, which rule on nothing. |
| D-198 | gap | 2026-08-04 | (**renumbered THREE times by CONDUCT** — from a colliding D-184, then a colliding D-185, then a colliding D-188; other sessions' rows hold all three numbers. This row is CLOSED, so it moves rather than the open vocabulary row that now holds D-188: renumbering a closed row costs two pointers, renumbering an open one costs every future reference to it) **op=bootstrap (classes null — unauthenticated) answers `roles`: every credentialled role with the date each password was last set, and NOTHING consumes it** — not setup.mjs, not civicos-ui, not newgroup, no battery assertion (measured by REC-39). An unconsumed disclosure on an anonymous op; REC-39's login-distinguishability reasoning RESTS on it being open, so closing it reopens that decision — the two travel together. | M7 · **CLOSED 2026-08-04 by REC-41** — the `roles` field is gone from the anonymous answer (structural sweep over the whole response, NC (a) restores it and names it by path and value), IC-20 filed, I3 → 6.0.0. The travelling half went with it: the login codes collapsed to one `SIGN_IN_REFUSED` with a sentence, because with the roster closed the distinct codes became an unmetered anonymous oracle — and the collapse was defeated by an eleven-fold timing difference (0.6ms vs 6.7ms) until that was equalised too. Premise correction on the row above: `newgroup` DOES consume the op (src/index.mjs:364, :631), just not this field. |
| D-200 | defect | 2026-08-04 | **`op=audit` on the live instance is NOT clean: 31 checked, 21 clean, 10 with errors, every one `C-18.9`** — *"provenance documents[0] is at or past verified with no provenance_chain: a published hash claims these bytes came from somewhere by some route, and this document names none"*. MEASURED against the live plane 2026-08-04 immediately after the 0.56.0 deploy. **NOT caused or surfaced by that deploy, and this was established rather than assumed: `C-18.9` appears identically (4 occurrences) in the 0.55.0 release bundle and the 0.56.0 one, so the previous build would have reported the same ten.** It is RECORD STATE, not code: ten bundles sit at or past `verified` while naming no provenance chain, which is the record claiming a route it cannot show — the overclaim class this project exists to refuse, sitting in live data rather than in a code path. DIST's gate step 8 says `op=audit` clean before anything is called done, so **this release is deployed with a KNOWN unclean audit and that is stated rather than quietly passed**. NEEDS: decide per bundle whether the chain can be reconstructed from what was captured, or whether the honest act is to move the document back off `verified` — and note that back-dating a chain nobody recorded would be exactly the invention the gate exists to prevent. | M2 · **ten dispositioned 2026-08-05 by REC-54, mechanism built and tested, APPLICATION TO THE LIVE RECORD PENDING A DEPLOY** (op=provenancechain does not exist in the serving 0.56.0 build; DIST cuts releases and the plane deploy is gated to Bob). All ten reconstruct from evidence the register already held — nine by direct daemon fetch (locator + retrieved + capture.method), one by a member custody block — each hop STAMPED `reconstructed` so a chain derived in 2026-08 cannot be read as one witnessed at capture. Was **OWNED by REC-54** (RECORD re-promoted 2026-08-05 to run it); do not close by weakening C-18.9, and do not back-date a chain nobody recorded |
| D-201 | defect | 2026-08-04 | **`bio-plane/scripts/deploy.mjs` would DESTROY the `civicos` UI worker's only binding, and its own comment invites you to point it there.** That comment reads *"The UI worker carries no version number in the shared repo and contends for nothing, so it is not gated"* — true about the BATON, and it reads as permission to deploy the UI with that script. But its metadata is the PLANE's: it declares `VERSION`, `INSTANCE_NAME` and the two R2 buckets, and its `keep_bindings` is `["secret_text", "durable_object_namespace"]` — **which does not include `service`**. MEASURED 2026-08-04 by reading the live worker's settings before deploying: `civicos` has exactly ONE binding, `service PLANE -> biosmoke7`, and it is what makes `/api` reach the plane at all. Deploying the UI through `deploy.mjs` would drop it and leave the site serving HTML whose every request fails. CLOSED THE SAME TURN by `civicos-ui/deploy-ui.mjs`, which carries the UI's own metadata and the same read-back-and-hash discipline — **and which itself first REFUSED a deploy that had actually succeeded**, because the account returns the module inside a MULTIPART envelope and hashing the response body compares the envelope; `deploy.mjs` already knew that and the new script now does the same extraction. Row kept open until `deploy.mjs` REFUSES the `civicos` slug by name rather than relying on nobody trying it. **CLOSED 2026-08-05 by DIST.** `deploy.mjs` now refuses by name from a `NOT_A_PLANE` table, each entry carrying its own reason and its own deploy path, BEFORE the baton check — and the old `const GATED = slug !== "civicos"` is gone, which is the line that made the UI the one slug reaching the destroying PUT with no gate in front of it at all. **`pdf-worker` is in the same table**, because it is the identical hazard (a fleet member with no `PUBLISHED` binding and no Durable Object, which this script's metadata would bind all three) and leaving it out would have been the same reliance on nobody trying it that kept this row open. An ALLOWLIST was considered and rejected as unavailable: plane slugs are chosen by the groups that install them, so they cannot be enumerated. NEGATIVE CONTROLS RUN 2026-08-05, all three: `civicos` → refused exit 3; `pdf-worker` → refused exit 3; and the other direction, a plane slug (`biosmoke7`) → NOT refused by the new guard, falling through to the baton check and stopping there on the missing `--thread` — so the guard is proved to bite AND proved not to over-refuse. Each was run with NO `--thread`, which is precisely the invocation that under the old code skipped the baton entirely and went straight to the PUT. | M0 · CLOSED 2026-08-05 (DIST) |
| D-206 | gap | 2026-08-05 | **`op=audit`'s TALLY is keyed by check id, so REC-54's new absent-vs-empty distinction reaches a reader only through the offender detail, which is bounded at 20.** Stated by REC-54 as residual rather than left to be discovered. The three findings C-18.9 now separates — *no chain recorded*, *a chain recorded and empty*, *a chain field that is not a chain* — are genuinely different facts about the record, and **a tally that collapses them re-creates in the REPORT the same conflation the check just removed from the DATA.** | M2 · **CLOSED 2026-08-05 by REC-56** — decided in favour of carrying it: an optional `code` on the finding (minted at the same call site as the finding it describes) and a derived `tallyDetail` beside `tally` on `op=audit`. `tally` does not move by one byte and `tallyDetail` is absent when nothing on the page carried a code. Reasoning at both sites (`f()` in `checks/bio-checks.mjs`, `auditPass()` in `src/store.mjs`); CLAUDE.md's *"saying which of those is true is a first-class obligation, not a diagnostic detail"* is what settled it. |
| D-208 | design | 2026-08-05 | **One question is now composed in two layers that cannot read each other.** `civicos-ui/app.html`'s `selectionMoved()` composes `moved || digestChanged` on the surface; REC-55's `Store.#answerChanged` composes the same question in the plane. They agree today and neither can see the other — the drift-by-hand-copy shape this project has measured five times, except that here BOTH copies are correct and the risk is only future divergence. **Deliberately NOT collapsed:** REC-55 declined to publish `#answerChanged` as a field speculatively, which was the right call — an unconsumed publication is exactly what REC-41 removed from `op=bootstrap` and what UI-35 is measuring on `op=publishedcase`. TRIGGER: the first surface offering a REFUSE-WEIGHT act over a query lease. At that point publishing the predicate as a field and having the surface read it is an additive I3 change that collapses the two. | M8 · DEFERRED on the trigger above — reviewed when a refuse-weight act reaches a query lease |
| D-210 | design | 2026-08-05 | **`op=release` is not repeatable, and nothing in the plane can re-make a release that was made wrongly.** Measured by REC-56 while correcting C-18.1: BOTH of that check's arms fire only on a bundle at or past `verified`, and both advised *a named member re-makes the release decision* / *a named member ratifies and records the collected -> verified transition* — while `release()` refuses anything already at `verified` (`ILLEGAL_TRANSITION`, "release is not repeatable"). So the FIRST repair in each array was as unreachable as the `return the bundle to collected` half D-203 was routed for, which the routing did not predict. The strings now name `op=retire` and raising it. **Whether a wrongly-made release can be re-made at all is the same question as DEC-56** — a re-make needs a route off `verified` — so this row is deliberately NOT decided here and does not pre-empt it. | M2 · **DOCTRINE (Bob)** — rides DEC-56; provisional is the corrected advice, which is honest under either ruling |
| D-212 | design | 2026-08-05 | **SUPERSEDED 2026-08-05 by the VERSIONS design — the question this row asked turned out not to be a record question at all.** The row went through three drafts and each was wrong in an instructive way, so the history is kept rather than deleted. **Draft 1** said claims must be recordable as agreeing or disagreeing along a range, the hazard being a single collapsed number. Bob refuted the premise with the case the row was built on — an award that skipped competitive bidding plus an emergency exemption: *"I see two pieces that when taken together tell the story of what happened and why - perhaps justifiably, perhaps not."* They do not conflict, and whether either is RELEVANT depends on what the inquiry asks. **Draft 2** replaced that with three stored objects — support, undercut, rebuttal — where an undercut kills the inference from a leg to a claim while the leg stays true. Bob refuted that too, and more fundamentally: *"There may be evidence, even a single sentence of evidence, that supports and undercuts and rebuts a claim - all in the same sentence."* Roles are not types; one sentence plays all three at once against different claims. **THE ANSWER, and it makes the system smaller:** *"the only way to make sense of how the various legs of a claim come together is for an AI to be involved, for it to understand the facts in context and adjust the legs so that they properly tell the story and assign strength values that when calculated are supported by the evidence."* The calculation stays as simple as it is; the intelligence goes into how the legs are formed and weighted, not into relationships the record computes over. What the record holds is the legs, their strengths, and a written account of why they tell this story — carried by a VERSION (`INVESTIGATIVE-SESSION.md` §5, §6). **Nothing is owed on this row.** The lesson worth carrying: two attempts to solve a judgement problem with schema, both by a session doing structure where the question was understanding. | SUPERSEDED by DEC-60's versions design — no work owed; kept for the two refuted drafts |
| D-214 | design | 2026-08-05 | **ANSWERED THE DAY IT WAS RAISED — Bob, 2026-08-05: `rejected` is BOTH an act AND a state of a suggested leg, and BOTH `considering` and `rejected` are REVERSIBLE.** Raised by DEC-60 and closed in the same session; kept as a row because IS-1 builds to it and the reasoning must reach whoever writes the state machine. What this settles, in the terms the builder needs: (1) the leg REMAINS A SUGGESTION throughout — `rejected` is a state the suggested leg is in, not a separate object and not a deletion, so a rejected leg is still readable, still shows its basis, and still counts toward nothing; (2) the REJECTION ACT is recorded in its own right, which is what makes a PATTERN over rejections queryable — and that pattern is where DEC-60's third argument lives, since a member who rejects every suggestion running against their thesis is visible only if the acts persist; (3) **REVERSIBILITY answers the question that made this hard** — a rejected leg that new evidence would revive can simply be un-rejected, so nothing needs a terminal state and nothing is lost by rejecting early. Note for IS-1: reversibility means the four states are NOT a one-way ladder, so the transition table must be authored rather than assumed, and each reversal is itself a member act that is recorded (an un-rejection is evidence about the member's attention exactly as the rejection was). | M9 · answered 2026-08-05 (DEC-60, Bob) — IS-1 UNBLOCKED on this axis; the row closes when IS-1 lands with the transition table and its negative control |
| D-215 | design | 2026-08-05 | **Two mechanisms proposed inside DEC-60's design and NOT ruled.** *(Rewritten 2026-08-05 in plain terms at Bob's instruction — the first draft called (1) a "congeniality self-report", a phrase invented in the session that wrote it and decodable by nobody else. Term dropped; both are in `INVESTIGATIVE-SESSION.md` §7 and §8, marked proposed.)* **(1) ANSWERED 2026-08-05 — Bob: the AI SKILL's requirements include MINIMISING these effects.** Constrain the skill at the source rather than bolting a report onto the output; DEC-46's lens-may-be-preserved-not-applied is the standard the skill is written against. Recorded as a skill requirement in `INVESTIGATIVE-SESSION.md` §11, not as a mechanism. The original proposal, for the record: The session is given the net bias (project + instance combined). It can use that two ways. Silently — letting the declared position shape what it proposes, which is the lens being APPLIED and is what DEC-46 forbade for import, where a travelling manifest may PRESERVE a lens and may not apply it. Or out loud — attaching to a suggestion: *this agrees with the position the project has declared; here is what I went looking for that would have cut against it, and here is what I found.* The second turns the bias from something that could quietly steer the run into something a member can CHECK, and it is a thing a member reviewing their own work cannot do for themselves: nobody can reliably notice when they are agreeing with themselves. **(2) A SUGGESTION CARRIES THE CONDITIONS IT WAS FORMED UNDER.** Bob, 2026-08-05: *"everything can change at the drop of a hat (bias, standard, claims)"*. So a suggestion is only interpretable against the bias, the evidence standard, and the claim set that were in force when it was written. Carried with it, a member opening a month-old suggestion can see it was formed under a different bar, or before three claims were accepted, or under a position the project has since revised. Not carried, a stale suggestion looks exactly like a current one. This is the shape DEC-54 already uses for a pinned policy — source, date and hash recorded, with cases naming the version. **(2) ANSWERED 2026-08-06 — Bob: a claim version does not need to know the version previously in place, nor the bias, nor the evidence standard.** The reason is internal to the versions design and the session's own argument for recording them was wrong: **a version is a COMPLETE ACCOUNT, not a patch.** Were versions diffs, what preceded them would matter; they are not, so it does not. The same logic disposes of the other two — a version is evaluated against the CURRENT standard, so which bar it was written under tells a reader nothing they would act on; and the bias ought not to have shaped it at all, since minimising that is a requirement ON THE SKILL (see (1)), so recording it would be recording something that should not have mattered. The session's argument was that a stale version reads as a current one: real, but the remedy was wrong and far heavier than needed, since a version carries a date and names its run anyway. **What IS kept, for a different reason: the run's OBSERVATION LOG** — where it searched and where it stopped — which is not about which rules applied but about what evidence was reachable at the time, and that is what a later reader needs to judge whether the account was well-founded. | M4 · closed 2026-08-06 (DEC-60, Bob) — both halves answered; the observation log survives as part of the run object, not as version metadata. Correction 2026-08-07 (`IS-SWEEP-2026-08-07.md` §1.2): the BIAS half of (2) was ALREADY RULED before this row was raised — `Content_Framework:1129-1141`: an assistant-surfaced focus must carry the bias manifest in force when it was surfaced, since unlike a member it will not remember — so the closure stands but its reasoning line must cite the standing ruling rather than dispose of the question fresh |
| D-216 | design | 2026-08-05 | **CHECKED AND ANSWERED 2026-08-08 (W0's slot-free model-check lane). THE ANSWER IS PER-PROJECT: `INVESTIGATIVE-SESSION.md` §7 IS CORRECT AND CLONING IS NOT THE HONEST ANSWER.** The question was whether an inquiry shared across projects has ONE stance every referencing project must share, or a per-project one — §7 ruled per-project, Bob's first instinct was to CLONE the inquiry on divergence, and cloning was rejected because it duplicates the whole evidence trail and the copies immediately drift, so the shared investigation stops being shared. **DRIVEN, NOT READ — the instrument is `bio-plane/test/d216-sharing.probe.mjs` (38/38) with `d216-sharing.control.mjs` (3 arms as declared); the full record is in `MEASUREMENTS.md`, 2026-08-08.** Two projects stood on two different readings of one shared inquiry simultaneously, the plane refused neither, and after divergence each still saw every version and every leg of the other's; the sharing edge is a `cites` row in `refs` (`op=backlinks` reports it, `op=stats.refs` grew by exactly 2, and `op=strengthbarof` composes strictest-per-axis across BOTH citing projects — an answer no single-project walk can produce); the inquiry's own bytes carry NO stance and a read naming no project gets no `current` field at all. **NOTHING IN THE MODEL REQUIRES ONE SHARED STANCE** — the edge is load-bearing (severing it refuses the stance `VERSION_CURRENT_UNRELATED`), the mechanism is not two-only, and the one cross-project composition in the model is the BAR and not the stance. **TWO CORRECTIONS THE CHECK FORCED. (i) §7's and this row's own cited verification was WRONG: `op=linkproject` is link + project-as-a-VERB — it takes a `capture=<sha256>` and projects that capture's resolved links into `links_to` edges — and it neither takes a project bundle nor writes a `cites` edge. The conclusion was right; the evidence was a name collision. (ii) THE FINDING THAT OUTRANKS THE ANSWER: `op=cite` REFUSES an inquiry on the PROJECT arm (`NOT_INFORMATION`), so the edge the whole IS build hangs on has NO CURATED PRODUCER — a project draws on a question only by authoring its `bundle.md` and calling `op=promote`. PL-2's fixture hand-authored exactly that, which is why its suite drove the gate without discovering the gate has no door.** | M9 · measured 2026-08-08 — **IS-3 UNBLOCKED**; PL-13 survives with PL-2's pointer confirmed, its remaining scope the two notification slugs (asserted ABSENT) plus the new `op=cite`/`op=sever` project-arm gap |
| D-217 | design | 2026-08-05 | **BOTH HALVES ANSWERED 2026-08-06 by Bob.** Raised by DEC-60's versions design. **(a) REVIEW BURDEN — versions form a DERIVATION TREE, and accepting one prunes the branch.** The problem: a background run adds a version whenever its output differs from every existing one, so a claim under frequent investigation accumulates versions faster than anyone reads them — each correctly labelled a suggestion, the whole unexamined. Bob: *"make it easy to delete versions, or allow versions to be a tree that expresses how one derives from another, so a user can delete all previous derivations when they accept one as the one they want."* **The tree is already half-built into the design** — `INVESTIGATIVE-SESSION.md` §6.3 has editing a version DERIVE a new one from it — so this makes the derivation edge EXPLICIT and adds the prune. Consequences for the builder: a version records what it was derived from (null for one a run composed fresh); accepting a version offers deletion of the ancestors it descends from, as an offer and not an automatic act, since a member may want a rejected reading kept as the record of what was considered; and deletion is a MEMBER act — the AI cannot delete a version any more than it can accept one. **(b) A REWORDED CLAIM IS USER SELECTABLE** — the member chooses whether rewording a claim produces a new VERSION of it or a NEW CLAIM. **So this is not a schema decision, it is a runtime one, and the schema must support both.** The reason both are legitimate: a tightened wording that leaves the legs meaning what they meant is a version; a reworded claim that changes what the legs are answering is a different claim wearing a similar sentence, and forcing either into the other loses something. It interacts with the question-sharpening suggestion (§8), which can reword an INQUIRY's question and thereby move every claim beneath it — that path should offer the same choice rather than deciding for the member. | M9 · answered 2026-08-06 (DEC-60, Bob) — closes when the derivation edge, the prune offer (HIDE, never delete — `IS-SWEEP-2026-08-07.md` §4b item 6) and the version-or-new-claim choice land with IS-1 |
| D-221 | defect | 2026-08-06 | **The "changed from" sentence written into a new capture names the WRONG predecessor — almost certainly the OLDEST version at that address rather than the previous one.** Traced in source 2026-08-06. `heldMatch` (`civicos-ui/app.html:13614`) finds prior captures at an address with the query `locator:"<url>"`. **That is not an address comparison:** `locator` is a full-text-indexed field (`query.mjs:61`, `FTS_COLUMNS`), and a fielded query on a field carrying `fts` compiles to a TEXT ATOM (`query.mjs:499`, `if (f.fts) return textAtom(...)`), not to column equality. A text atom creates a rank arm, so the default order is RELEVANCE (`query.mjs:732-733`, `ctx.sort?.field || (rank ? "relevance" : "updated")`). Every capture at one address carries identical URL text, so the bm25 scores tie and the declared tiebreak — `bundle_id ASC` — decides the order. `heldMatch` then returns on the FIRST comparable row it walks. **So on a calendar captured weekly for a year, the record can state "the source has since published a changed document" against a snapshot twelve months old, and present it to the member as the immediately preceding version.** The sentence is written into the bundle body at `app.html:13485` and is permanent in that bundle. SECOND, SMALLER RISK, mechanism-only and NOT measured: because the match is a text search rather than an equality, two different URLs that tokenise alike — same host and path, different query string — can match each other. FIX: D-220's join, which answers by address and orders by date; ordering the existing search explicitly would work and would leave the fragile lookup in place. **UI's paths, so this is a DELEGATION and not an edit from BOB** — recorded here because BOB traced it. | M3 · **CLOSED 2026-08-08 by UI-50**, exactly as this row said it would be — by D-220's consumer rather than separately. `heldMatch`'s address arm asks `op=versionchain` (PL-10, I3 10.2.0) for the chain's `total` and seeks the LAST version at `offset = total - 1`; that version is the predecessor of a capture not yet written, and it is what the sentence names. **`at=<sha>` is NOT used and the reason is measured rather than preferred:** the chain joins `register`, whose row is written by `op=promote`, so at the moment this check runs the fresh capture is not IN the chain and cannot be an anchor. `predecessor: null` at index 0 has its analogue in `total: 0` and renders as an honest absence. Proved in `civicos-ui/test/version-predecessor.test.mjs` over SIXTY chain lengths with ground truth computed in JavaScript and never read back from the op, with the trap proved armed on every subset. The row's SECOND, unmeasured risk — two URLs that tokenise alike matching each other — **goes with it**: the chain is keyed on `address_norm` equality and consults no text index at all. **What does NOT go with it is the sentences ALREADY WRITTEN; see D-236.** |
| D-227 | defect | 2026-08-07 | **REC-60's suites pin the PUBLISHED ENVELOPE and not the SQL BOUND, so a regression that removes `LIMIT ?` while leaving the slice and the envelope intact passes both of them GREEN.** MEASURED 2026-08-07 by CONDUCT at REC-60's integration, by arming its negative control a DIFFERENT way than the worker did: removing `LIMIT ?, cap + 1` from `resolutionsForCapture`'s query while leaving `const truncated = rows.length > cap`, the `slice(0, cap)` and the published `limit`/`truncated` **left `bounds.test.mjs` at 99/0 and `meaning-bounds.test.mjs` at 51/0 — fully green.** The published answer stays honest and correctly truncated; **the SCAN becomes unbounded again**, which is D-225's actual memory concern rather than its honesty concern — and on `connectionsFor`, whose response grows on D-224's k(k−1)/2 curve, an unbounded scan is precisely the shape that bites. **This is NOT a defect in REC-60's fix, which is correct and shipped**: the fix bounds the SQL. It is a gap in what the suites would CATCH, and it exists because REC-60's walk reads what a method PUBLISHES by design — the walk cannot see this and says so at the site. **It is the same lesson as REC-60's own NC(2) finding from the other direction:** honesty and boundedness are different defects, and an instrument that measures one does not measure the other. NEEDS: an assertion that the row source itself is bounded — driving a store with more rows than the ceiling and asserting the query cannot return them, rather than asserting the answer was sliced. Relation: D-225, D-224, and REC-66's unbounded derivation, which is this defect one step earlier. | M3 · **PARTLY CLOSED 2026-08-08 (REC-66), and the technique this row asked for now exists and is driven.** `bio-plane/test/derivation-bounds.test.mjs` asserts the ROW SOURCE itself — a store driven with 40 documents on one subject while the bound admits 32, and the answer publishes `resolution_rows: 33`, so the query provably CANNOT return them — beside a text pin on the SQL off the method's own comment-stripped segment. **AND THE ROW'S OWN CLAIM WAS RE-MEASURED AND HELD, TWICE:** REC-66's negative control (1) restored the unbounded derivation and (2) planted the naive answer-only fix, and under BOTH arms `bounds.test.mjs` (151/0) and `meaning-bounds.test.mjs` (85/0) stayed **FULLY GREEN** — the published envelope is honest over an unbounded scan, exactly as this row says. **WHAT REMAINS:** the same LIVE row-source assertion for REC-60's three reads (`concerns`, `resolutions`, `connections`), which today are pinned by TEXT only (`op=airunlog` gained a text pin at REC-70; the three have none). A text pin dies the day the SQL is reworded; the live one does not |
| D-228 | defect | 2026-08-07 | **A QUOTED FIELD VALUE KEEPS ITS QUOTES, so `state:"open"` matches nothing and `title:"two words"` compiles to a mangled FTS5 expression.** Found 2026-08-07 by PL-8 while writing the meaning arm’s over-strictness control — an equally correct alternative phrasing that a member would type, and it did not work. The tokenizer has a branch for exactly this (`query.mjs`, `if (rest === "" && src[i] === a double quote)`, with a comment saying *`field:"two words"` splits at the quote, so back up and read the value properly rather than truncating it at the space*) and **that branch is UNREACHABLE**: the bare reader stops only at whitespace and parentheses, so it has already consumed the opening quote by the time `rest` is tested, and `rest` is never the empty string. MEASURED, through `compile()`: `state:"open"` binds the argument `"open"` WITH the quote characters, so an indexed equality against a corpus that stores `open` returns nothing and says nothing; `title:"two words"` binds the FTS5 expression `({title} : """two" AND "words""")`. **This is PRE-EXISTING and language-wide** — it belongs to every one of the 34 projected fields, not to the meaning arms, which merely inherit it. It is not fixed inside PL-8 because the fix changes what every selector matches (a query that returned nothing may start returning rows) and that is a change every retrieval suite must be re-run against, with its own control. **PINNED rather than left**: `meaningquery.test.mjs` asserts the CURRENT behaviour on both an arm and an ordinary field, so the fix flips a visible assertion instead of landing unnoticed. NEEDS: make the branch reachable (stop the bare reader at a quote, or split on the colon before reading the value), correct the two pins, and re-run the retrieval suites. | M3 · **CLOSED 2026-08-08 by REC-68.** THE MECHANISM, because the symptom was never the finding: the guard `rest === "" && src[i] === '"'` is UNSATISFIABLE against the reader that produces its inputs — the bare reader stopped only at whitespace and parens, so `src[i]` after it was whitespace, a paren or undefined and NEVER a quote; and a quote after the colon was CONSUMED, so `rest` began with `"` and was never empty. Each half falsified the other. THE FIX IS ONE TERMINATOR: the bare reader now stops at a quote, the rewind that undid its over-consumption is gone (its absence is what makes the branch reachable), and the branch is GENERALISED so a comparison lead (`created:>"2026-01-01"`, `fm:a.b="c"`) is not fixed-adjacent-but-broken. **MEASURED, and the two failure modes separate:** 28 of the 34 projected fields, the 3 meaning arms and `fm:` MATCHED NOTHING, which fails honestly; the 3 FTS columns MATCHED THE WRONG THING when the value was multi-word — `title:"Fund general"` returned a real row for a phrase in no title, because FTS5 binds a column filter to the phrase that FOLLOWS it, so only the first word was scoped. **And the sharpest, found by driving rather than by reading: a nothing-matching selector UNDER NEGATION returns THE WHOLE CORPUS** (`-state:"collected"` gave 3 of 3), so an honest empty answer became a confident complete one; `has:`/`sort:` dropped their arm with a warning, which widens the same way. PL-10's finding that the defect did not bite the FTS columns' SINGLE-word case is RE-MEASURED here rather than inherited and holds. PL-8's two pins CORRECTED with dated reasons, never exempted. Branch reachability is now asserted STRUCTURALLY as well as behaviourally, because an equivalent-mechanism revert round-trips every single-word value and only a structural pin can see it (NC arm d). Battery 123/123 at 7,805, +53 attributed across four suites. Rider done: `schema.mjs`'s grade_source comment named three sources against a catalogue of five and is now DRIVEN against `GRADE_SOURCES` by `hygiene.test.mjs`, in both directions and with a REACH delta. |
| D-229 | defect | 2026-08-08 | **ELEVEN OF THE TWELVE `MACHINE_CANNOT_*` FENCES ARE NOT DOING THE WORK — an unrelated payload guard is, and that is luck rather than a fence.** MEASURED 2026-08-08 by PL-11, running **the second half of DEC-55.5 (owed control 1) for the first time in this project** — the half that asks not *do the refusals fire* but *does removing the predicate make them all pass*. Neutering `isMachineStamp` disarms all twelve with one edit, and PL-11 recorded **what each act answered INSTEAD**, because "they all pass" is the generous reading: `RELEASE→NO_ACKNOWLEDGMENT`, `CONCLUDE→NO_CONCLUSION`, `REOPEN→NO_REASON`, `PUBLISH→NO_SUBJECT_POSITION`, `MOVE_ACTION→NO_REASON`, `CORRESPOND→NO_TARGET`, `DIVIDE→NO_REASON`, `GROUND→NO_PARTITION`, `MOVE_VERSION→VERSION_ACT_NO_SUCH_VERSION`, `FORWARD→NO_SUCH_TASK`, `RESOLVE→NO_SUCH_TASK`. **Not one is refused AS A MACHINE.** Each falls through to the ordinary payload complaint sitting behind the fence — so the machine identity is doing no work at eleven sites, and a caller who supplies a complete payload meets nothing. **AND THE TWELFTH IS THE PROOF: `MACHINE_CANNOT_DECLARE` FULLY SUCCEEDED — a machine SET THE GROUP'S REQUIRED EVIDENTIARY STRENGTH**, because its payload happened to be complete. The other eleven are one well-formed request away from the same outcome. **This is not a defect PL-11 introduced and not one REC-46 caused** — REC-46's single predicate is what makes the fence *breakable in one edit*, which is what let it be measured at all; before it, the same test would have needed eleven edits and would probably never have been run. **What is wrong is that the fences were believed on the strength of their first half.** NEEDS: each of the twelve asserted to refuse a machine **with a COMPLETE payload**, so the fence is what refuses rather than the complaint behind it — the same shape as an over-strictness arm, one layer down. Relation: DEC-55.5, D-199.5, REC-46. | M4 · **CLOSED 2026-08-08 by REC-73** — `bio-plane/test/machine-fences.test.mjs`, 45 assertions: all twelve driven under payloads that would otherwise SUCCEED, each refused BY NAME, each followed by the SAME payload accepted from a signed-in member (which is what makes it complete, measured rather than asserted). **NOTHING IN THE PLANE WAS CHANGED and REC-46's predicate was not touched** — the defect was in the CONTROLS, and it is the controls that moved. **AND THE MEASUREMENT IS LARGER THAN THIS ROW PREDICTED: under complete payloads the same one-line neutering lets TEN of the twelve acts GO ALL THE WAY THROUGH**, not one — a machine released a document to `verified`, published a case at edition 1, divided a question, accepted a reading, and set the group's required evidentiary strength with `author: token:ai` in the row. **The two that did not are `taskforward`/`taskresolve`, caught by REC-4's `NOT_YOURS`** — the only pair in the family with a second independent fence behind the first, invisible until a payload good enough to reach it was used. Sweep recorded in MEASUREMENTS.md 2026-08-08 and raised as **D-230**. |
| D-231 | defect | 2026-08-08 | **`bio-plane/test/suggest.test.mjs` IS INTERMITTENTLY RED UNDER THE BATTERY AND GREEN STANDALONE — observed twice, by two sessions that could not see each other.** PL-14 saw 60/1 once and 61/0 on a re-run plus three standalone runs, on a tree carrying none of its files. CONDUCT saw **59/2 under the battery** at PL-14's integration and then **61/0 on three consecutive standalone runs and 0 failures on the next full battery**, on a tree carrying PL-11, SK-1 and PL-14. The two failing arms both times were CHECK 3 (a reading identical in substance to one already held is REFUSED by C-number and the reading it matches is NAMED) and the structural arm behind it (`ONE refusal row exists for three submissions`) — **F10's verbatim-resubmit key, which is keyed on the canonical submission BYTE FOR BYTE plus the inquiry's `bundle_sha`.** That key is the obvious suspect: it is the one thing in the suite whose value depends on state that a concurrently-running suite could move, and a stale-or-fresh key is exactly the difference between a stored refusal returned and the checks re-run. **NOT DIAGNOSED — that is the item, and it must not be closed by re-running until it is green.** WHY IT OUTRANKS ITS SIZE: **an intermittently red suite is one whose GREEN carries no information**, and a battery that is intermittently red trains every session to re-run rather than investigate — which is how a real regression gets attributed to the flake. This project's whole verification posture rests on the battery being believed. **SWEEP FOR THE CLASS**, do not fix the one arm: which other suites hold state across submissions, and which of those could a concurrent suite move? NEEDS: the cause NAMED (not the symptom suppressed), the arm made deterministic, and a control proving the arm still fails when the subject is broken — a flaky arm silenced is a rule nobody is enforcing. Relation: D-93 (a suite dying early hiding the arms behind it), D-186 (sandbox ownership per process). | M0 · **CLOSED 2026-08-08 (M0-13). THE CAUSE IS A WALL CLOCK INSIDE A COMPARISON THAT HAD TO BE TIME-FREE, AND THE SUSPECT IN THIS ROW WAS WRONG — F10's key is CLEAN.** `submission` is built out of caller-derived fields TWENTY-ONE LINES BEFORE `nowIso` exists, and `nowIso` reaches only the `first_at`/`last_at` columns; nothing a concurrent suite could move was ever involved, and the battery runs its suites SEQUENTIALLY so there was no concurrency to move anything. **THE ACTUAL CAUSE, in `suggestVersion`: `substanceOf` stripped `name` and `derived_from` from PL-1's canonical composition before comparing a candidate against every held reading — but a composition's ground rows are `ground\t<ground>\t<asserted_by>\t<at>\t<statement>`, and `#suggestionFrontmatter` stamps that `at` from the SERVER'S clock at second resolution.** The candidate is stamped NOW; every held reading was stamped when it was written. So CHECK 3's duplicate gate fired ONLY when both landed inside the SAME ONE-SECOND BUCKET. DRIVEN, not inferred: identical readings 0ms apart were refused, the same pair 1,200ms apart LANDED as a second version. **THE FLAKE WAS THE CHEAP HALF — the expensive half is that §6 rule 8 WAS UNENFORCED IN THE PLANE.** *A run adds its output as a new version ONLY IF it differs in substance from every existing one*, and any retry loop pausing a second between attempts wrote unbounded identical readings into a member's inquiry. The suite runs ~510ms and usually stayed inside one second standalone; under a loaded battery it did not, and a duplicate that LANDS makes the version count 7 where block 3 asserts 6 — which is exactly the 59/2 CONDUCT saw, while a boundary crossed only by the later renamed submission is the 60/1 PL-14 saw. **FIX: one expression, in the COMPARISON only** — the stamp is blanked out of the ground rows before comparing. The composition itself is untouched, because the freeze compares document-derived bytes against stored bytes and both carry the same authored stamp there; removing it from the composition would have changed every version PL-1 already froze. **NOT CLOSED BY RE-RUNNING AND THE ARM WAS NOT SILENCED:** the suite gained a boundary arm that crosses a second ON PURPOSE and asserts the crossing, plus an over-strictness arm proving the exclusion is exactly one field wide. Controls (D-231a) re-arms the clock and REPRODUCES the historical 2-failure signature deterministically; (D-231b) widens the exclusion by one field and the over-strictness arm fails instead. 12 arms run, 0 behaved other than declared. Residual raised as **D-234** (renumbered from D-232 at integration — see that row) and D-233. 
| D-232 | defect | 2026-08-08 | **`pdf-worker`'s SUITE HAS BEEN RUN BY NOTHING SINCE 2026-07-31, WHILE `coverage.mjs` CREDITED ITS SURFACE AS REACHED FROM A SOURCE READ.** MEASURED 2026-08-08 by FL-2/VF-3's class sweep, which went looking for the class rather than for the one member it was adding. `scripts/battery.mjs` discovered only `bio-plane/test/`, so `pdf-worker/test/pdf-worker.test.mjs` — a real suite, committed and green when it was written — **executed nowhere for eight days while the fleet figure read 1/1 ops reached and 1/1 declaring a control.** That is **D-93's class one directory out** (a runner whose reach is smaller than its report) and it is **D-117's own failure mode arriving inside D-117's instrument**: a component going dark while the figure holds still, which is wrong in the GENEROUS direction, the direction this project treats as worse. **Half is CLOSED and half is OPEN, and they are different defects.** CLOSED: `battery.mjs` now discovers fleet members BY MANIFEST and RUNS their suites, printing a `fleet:` line that NAMES any dark member on every run — so darkness is now stated rather than absent, and `coverage.mjs` gained an accounted-for-Workers gate (a directory with a `wrangler.jsonc` and no `fleet-member.json` fails) plus a fleet FLOOR, so **hiding a manifest now FAILS instead of quietly shrinking the fleet.** OPEN: the member is STILL DARK — its suite cannot resolve `miniflare`, so it is SKIPPED (named) rather than run, and the battery currently prints `DARK: pdf-worker` every time. NEEDS: `pdf-worker`'s suite resolving its dependency the way `agent-worker`'s does — through the plane's install rather than its own — so a fresh checkout runs it. **NOT DONE BY FL-2 on purpose:** `pdf-worker/**` is CONTENT-PDF's path and reaching into it is the mistake REC-71 exists to correct. Delegated in `CLAIMS.md`. **RIDER, same sweep, same file:** `pdf-worker`'s line-comment stripper idiom (`//` to end of line) **deletes `"http://…"` and the rest of its line** — FL-2 copied it, and its own only-one-absolute-URL arm then read 6,029 characters of a 17,265-character file and came back green over a source truncated by two thirds. Any walk that copied that idiom is suspect. **AND I6 HAS NO `GET /version`**, so fleet rule 4 (*a verification must establish which build ANSWERED*) is unverifiable for this member — filed at IC-32 so DS-4's rollout gate does not discover it at deploy time. Relation: D-93, D-117, D-108, IC-32. | M0 · **CLOSED 2026-08-08 (CPDF-9).** First half by FL-2/VF-3 (discovery, the fleet line, the accounted-for gate and the floor); **second half by CPDF-9: the member's suite now resolves `miniflare` through the PLANE's install when its own directory has none — `agent-worker`'s mechanism COPIED, not a second one — so it RUNS on a fresh checkout.** MEASURED: the battery went 121/122 green + 1 named skip · 7,456 assertions · `DARK: pdf-worker`, to 122/122 green · 7,504 assertions · `fleet: 2 members … 2 member(s) actually RAN` with no `DARK:` name; the member's own suite reports **48 pass, 0 fail** with `pdf-worker/node_modules` ABSENT. Proved by control arm (A2): break one assertion in the member's suite and the battery EXITS 1 naming it — before this item the identical edit changed nothing anywhere. Arm (A3) re-armed the dependency failure and the member is NAMED as dark both ways rather than silently counted green. **BOTH RIDERS CLOSED TOO:** the line-comment stripper is anchored on a non-`:` and asserted in BOTH directions over a fixture that carries a URL, with the corpus size PRINTED and a floor asserted (a walk over a truncated corpus now FAILS instead of reporting clean); and **I6 GAINED `GET /version`** (IC-33 filed PROPOSED; the bump is CONDUCT's), reading `env.VERSION` from the binding so what it reports is the build that is RUNNING. `battery.mjs`'s `fleetDepSkip` is now UNREACHABLE for every member that exists and SAYS SO in its own header rather than being deleted silently; the residual it leaves — a named skip still leaves the battery GREEN while a component is dark — is recorded there and is the fleet's to weigh · **RECOVERED 2026-08-10 by CONDUCT (D-264): CPDF-9's closure was written and then DROPPED by its own merge `1c5d96a`, so this row asked for finished work for two days. Restored VERBATIM from `1c5d96a^2:docs/development/DEBT.md` — not paraphrased — and its live claim RE-CHECKED rather than trusted: the battery on `eb367d1` prints `fleet: 2 members beside the plane · 5 suite(s) discovered · 2 member(s) actually RAN` with NO `DARK:` name, so the sentence is still true. Restoring a false sentence would be worse than the gap.** |
| D-233 | gap | 2026-08-08 | **THE NEGATIVE-CONTROL REGISTER'S "ARMS STATED" TALLY CANNOT SEE A DECLARATION WRITTEN IN THE NUMBERED STYLE, AND SCORES FOUR SUITES AT ZERO ARMS WHILE THEY DECLARE DOZENS.** MEASURED 2026-08-08 by M0-13 with the instrument itself (`scripts/control-register.mjs`, `readControl` over all 119 suites): **`suggest.test.mjs` declares TWELVE arms, every one of them run by `suggest.control.mjs`, and the register scores it 0.** `countArms` counts occurrences of ` -> ` or ` → ` (`const ARM = /\s(?:->|→)\s/g`), which is the grammar `VERIFICATION.md` documents — but several suites write their arms as `(1) … (2) …` prose instead, and those are invisible. Four of 119 suites score zero. **NOTHING IS RED AND THAT IS THE POINT:** the arms count is REPORTED and never gated, so the published figure (388 on this tree) is an undercount nobody is alerted to, and it is exactly the class `VERIFICATION.md`'s own M0-9 section records — a detector wrong in the GENEROUS direction, found only by measuring. This one is wrong in the STINGY direction instead, which is safer but still makes the figure mean less than it appears to. NEEDS: either the counter taught the numbered style, or the four suites converted to the arrow grammar, or the figure reported as a floor with the uncountable suites NAMED — but not silently left at a number that is neither. Whichever way, `VERIFICATION.md`'s row moves in the same turn, from what the instrument PRINTS. | M0 · **CLOSED 2026-08-08 (M0-14). THE ROW'S DIAGNOSIS WAS ONE OF FOUR CAUSES, AND THE NUMBERED STYLE WAS NOT EVEN THE COMMONEST.** Measured on this tree: 120 suites, **395 arms before and 462 after with no suite's declaration touched — the +67 is INSTRUMENT, not estate.** The four zeros had four DIFFERENT causes: `bias.test.mjs` states thirteen arms **in the arrow grammar the detector already understood**, in enumerated paragraphs the EXTENT rule cut off; `case-opened.test.mjs` heads its fuller block with a DASH instead of a colon, so **one character of punctuation hid a whole declaration**; `suggest.test.mjs` and `strengthpair.test.mjs` state ordinal lists with **no arrow anywhere** (`strengthpair` says *THE SEVENTEEN ARMS* and the corrected matcher counts exactly 17). **AND A FIFTH SUITE NOBODY WAS LOOKING AT SCORED 1 AGAINST A REAL 10:** `versionstate.test.mjs`'s single counted arm was a FALSE POSITIVE — an `->` inside a quoted CODE EDIT. A count that is merely WRONG is harder to notice than one that is zero. **FIXED BY INVERTING THE TEST, NOT BY LENGTHENING A LIST** (REC-70's precedent): an arm is one MARKED item of a list, marked either as a TRANSITION or as an ENUMERATION, counted as **max(transitions, enumerations) and never the sum**; and — the half that is the whole defect — **a declaration carrying no marking is `null`, NEVER `0`, and is NAMED as UNCLASSIFIED.** `case-opened.test.mjs` is the one that remains, named and pinned. **THE FIGURE IS NOW FALSIFIABLE:** `REGISTER_FLOOR` gates `--strict` on arms, on classified declarations and on the CORPUS the register reads, because a ceiling could never have caught this — the tally only ever rises on its own. Six controls run, **0 behaved other than declared**, each armed alone, every restore verified against a pristine pre-arm copy by sha256 AND content (`test/register.control.mjs`). VERIFICATION.md's row and section moved in the same turn from what the instrument PRINTED. **Residual, stated rather than hidden: the tally is an explicit FLOOR on arms stated** — a LABELLED arm (`(D-231a)`) is not counted, so `suggest.test.mjs` reads 8 against a real 10, and widening the ordinal to any bracketed token would count every `(D-113)` in this prose. |
| D-234 | defect | 2026-08-08 | **THE SAME DUPLICATE GATE IS ALSO DEFEATED BY A QUOTATION MARK, WITH NO CLOCK INVOLVED AND NO FLAKINESS — `suggestVersion`'s substance comparison builds its candidate from RAW ARGS while the bytes it compares against went through `#fmSafe`.** Found 2026-08-08 by M0-13's class sweep and MEASURED by driving the plane, not by reading it. `#fmSafe` rewrites `"` and `\` to `'`, folds newlines to spaces and trims — so the composition derived from what was WRITTEN can never equal the composition derived from what was SUBMITTED whenever a description, claim, ground label or statement contains any of those. Driven, all inside one second so the D-231 clock cannot be what fails them: a duplicate whose description carries a quote LANDED; one whose ground statement carries a backslash LANDED; one whose description had trailing spaces LANDED. There is a second, narrower half: `gRows` emits `statement:` only when non-empty while `#suggestionFrontmatter` passes `statement: g.statement ?? null` unconditionally, so an EMPTY-STRING statement composes two ways as well. **THIS IS THE SAME WRITE GATE (§6 rule 8) FAILING FOR A SECOND, INDEPENDENT REASON, and it is arguably worse than D-231 because it is DETERMINISTIC** — a member who types a quotation mark simply does not get the duplicate check, always, and nothing goes red to say so. **DELIBERATELY NOT FIXED BY M0-13**, which was a test-estate lane item that had already opened `src/store.mjs` once for D-231's cause: the honest fix normalises the candidate the way the write does, which also changes the `composition` the endpoint PUBLISHES on its success path (today that published value can differ from what the record actually holds — worth checking as part of the same item). NEEDS: the candidate composed from the values that WILL be persisted rather than from raw args, an arm per lossy transform (`"`, `\`, newline, trim, empty statement), and a decision on whether the published `composition` should match the stored one. | M3 · **CLOSED 2026-08-08 (REC-75), DRIVEN THROUGH THE CONTROL PLANE AND NOT ASSERTED AT THE HELPER.** **THE FIX IS ONE NORMALISER AND NOT A LIST OF TRANSFORMS:** `Store.#suggestionPersisted` says ONCE what a submitted value becomes in the document — `#fmSafe` on every value, `null` for every field the write would emit no line for, and the grounds the write drops dropped — and BOTH the write's row composers AND `#suggestionFrontmatter`'s candidate read it. There is deliberately no second computation to drift: a candidate *taught* the write's transforms would have agreed with it only until somebody changed one. Arms driven through `op=suggest` for every lossy transform — quote, backslash, newline, leading/trailing space, a leg note's punctuation, and a field the write OMITS. **THE ROW WAS ONE CASE TOO WIDE AND THE MEASUREMENT SAYS SO:** the EMPTY-STRING statement was NEVER broken (`#canon` maps `null` and `""` to the same empty field), and the WHITESPACE-ONLY statement never reaches CHECK 3 at all because `isBoilerplate` refuses it several screens earlier — the blank-field divergence is real but a leg's `date` is the field that exposes it, and both findings are kept as arms rather than smoothed. **THE PUBLICATION QUESTION IS SETTLED: `composition` PUBLISHES THE RECORD'S BYTES**, read back from `inquiry_basis_versions` after the promotion, with `composition_of: "record"` on the answer so a consumer can tell which bytes it holds; asserted equal to what `op=basisversions` publishes for the same version. That divergence ALREADY EXISTED and was invisible — it is closed, not introduced. **THE CLASS SWEEP FOUND ONE MORE LIVE INSTANCE IN THE SAME FUNCTION:** `SUGGEST_NAME_TAKEN` compared a caller-derived name against stored names, and although `VERSION_NAME_RE` excludes every character `#fmSafe` rewrites, a NEWLINE folds to a SPACE the grammar allows — so a reading named `the ledger<newline>account` walked past this endpoint and was refused by `promote` as VERSION_NAME_NOT_UNIQUE, in another family's words over a document already composed. Fixed the same way. **THREE OTHER `#fmSafe` WRITERS SWEPT AND CLOSED, each by a different mechanism** — `groundInquiry` (refuses `["\\\r\n]` outright rather than sanitising; `GROUND_LABEL_RE` excludes them; both sides trim), `publishCase` (every sanitised value is DERIVED, and the one authored-bytes comparison is signed-document vs signed-document), `divide` (nothing it sanitises is ever compared). The FREEZE is untouched, for PL-1's reason. **D-231's fix is untouched and its two arms re-run unchanged**, which is what says the two halves are independent. 5 new control arms (D-234a-e); 17 arms run, 0 behaved other than declared. Residual raised as **D-235**. |
| D-236 | defect | 2026-08-08 | **THE DEC-49 GUARD'S TEETH ARE BLIND TO EIGHT REFUSAL OBJECTS IN THE PLANE, BECAUSE ARM C MATCHES ONE SPELLING OF "THIS IS A REFUSAL" AND THE PLANE HAS THREE.** Found 2026-08-08 by REC-64, and found the way this class always gets found — **by REC-64's OWN new governed site coming back unfalsifiable**, not by reading the guard. `civicos-ui/check-refusal-codes.mjs` arm C collects refusals with `/\bok\s*:\s*false\b/`. MEASURED over `bio-plane/src`: **704 `ok: false`, 5 `started: false`, 3 computed `ok: !<expr>`.** So a CODELESS refusal — the exact thing arm C exists to fail on — passes silently in any of those eight, and two of the five `started: false` live in `store.mjs aiRunOpen`, where **one of them WAS codeless until REC-64 gave it a code by hand.** The guard reported that site as `92L (0 judged, 0 code(s) checked)`: read in full, asserting nothing, and green. **This is the same shape as VF-2's own two self-defects** (a parameter list read as a body; a character floor failing a good 19-character sentence) — the instrument being the thing that is wrong — and as REC-71's `refuse(key, …)` finding, one field over rather than one call over. It is ALSO why `selectionResolve`'s `SET_MOVED` could not be given a region `where` by REC-64: it refuses through `ok: !stopped`, so a region around it would judge zero refusals and fail as a drifted marker, and the code is therefore still untranslated. NEEDS: arm C's refusal matcher widened to the measured set, driven in BOTH directions (a codeless `started: false` planted at a governed site must go RED, and the widening must not start matching non-refusals); then the sites it newly sees checked, since none of them has ever been judged. **RENUMBERED D-235 -> D-236 AT INTEGRATION 2026-08-08 by CONDUCT. THREE ITEMS TOOK D-235 IN THE SAME HOURS** — REC-75 for `op=suggest`'s two-source answer, REC-64 for this, and M0-10 for battery residue outside `$TMPDIR` — **each having measured D-235 as the next free number over the real file, and each right when it looked.** REC-75 pushed first so its number stands; this is the second and M0-10's is the third. **FOUR collisions of one shape in one day now (a C-number family, an IC number, and twice a debt number), which is the measured price of the raised concurrency budget — paid by the integrator, which is the correct place for it. What would end the class is a MINTED id rather than a read-the-file-and-add-one convention**, and that is worth an item rather than another note. REC-64's report and claim name D-235. Relation: REC-64, REC-71, VF-2, D-231's class. | M7 · **CLOSED 2026-08-08 (REC-76).** **THE FIX INVERTS RATHER THAN LENGTHENING A LIST** (REC-70's lesson, fourth outing): arm C now takes every object literal in RETURN POSITION at a governed site and reads its VERDICT — the FIRST BOOLEAN-SHAPED top-level property — then grades by whether the outcome DECLARES ITSELF A SUCCESS. `true` is a success and is not judged; the literal `false` or a COMPUTED boolean (`!x`, a comparison, `Boolean(x)`) is a refusal and owes a code; **no boolean-shaped property at all is UNCLASSIFIED, NAMED every run and ceilinged, never silently scored zero** (M0-14's and CPDF-9's shape). The field-name half is what goes stale — `ok`, `started`, `found`, `proposed`, `preview` are five today — and the boolean half does not; what remains fixed is JavaScript's own set of boolean-producing operators, not a project vocabulary. **MEASURED, NOT ASSUMED: the widening LOST NOTHING** — every `ok:false` the old matcher judged at a governed site is inside a return-position object (0 of 60 sites build a refusal into a variable). **WHAT IT IMMEDIATELY FOUND: `aiRunOpen` went `92L (0 judged, 0 code(s) checked)` -> 4 judged, and TWO of its four refusals were CODELESS at a governed site** — they now carry C-33.30 `AI_RUN_NO_CONTEXT` and C-33.31 `AI_RUN_ALREADY_OPEN`. **AND `SET_MOVED` HAS ITS REGION AND ITS TRANSLATION** (C-33.32, `src/store.mjs selectionResolve > is-selection-moved`, 21 lines, 1 refusal judged and 2 codes COMPARED), which is the concrete debt this row named: `reachGap` 42 -> 41. **ONE FIGURE FELL FOR A REASON THAT IS NOT SLACK AND IS RECORDED AT THE SITE:** `codesChecked` read 119 old / 118 new on the SAME tree, because `captureRequestArm`'s NESTED refusal envelope (`{ok:false, silent:false, refusal:{ok:false, reason, code}}`) was graded TWICE by the old walk — `2 judged, 4 checked` over ONE return. It now reads 1/2, which is what is there. **WHAT THE NEW WALK STILL CANNOT SEE, stated at the site rather than discovered later:** a refusal built into a VARIABLE and returned later (`subresources.mjs` writes refusals that way; 0 governed sites do), and a NEGATIVE-POLARITY verdict (`failed: true`) — for which the cross-check is that a declared success carrying a refusal CODE FAILS, gated at zero. **3 unclassified outcomes are NAMED on every run** (one nested-callback return in `promote > bias-set-refusal`, two spread-an-outcome returns in `#moveVersionState`) under a ceiling that may only fall. Six real-tree control arms, and the pair (n1)/(n2) is the receipt: the same planted codeless `started:false` FAILS the new classifier and PASSES the old one. Relation: D-240 (the class sweep's two other instances) |
| D-238 | defect | 2026-08-08 | **`git stash` IS REPOSITORY-WIDE ACROSS EVERY WORKTREE, SO ONE WORKER'S UNCOMMITTED WORK — INCLUDING A WHOLE UNTRACKED `.test.mjs` — LANDS IN ANOTHER WORKER'S TREE AND IS RUN BY ITS BATTERY.** This is the named cause of REC-68's phantom suite (M0-15): its first baseline discovered 124 suites including `machinefences-dec49.test.mjs` (57 pass), another worker's file, in no commit, gone by the next run, and it could not determine how the file arrived. **MEASURED, not inferred:** `refs/stash` is NOT one of git's per-worktree refs (those are `HEAD`, `refs/bisect/*`, `refs/worktree/*`, `refs/rewritten/*`), so a `git stash push` run inside worktree `agent-a0e79024273135242` wrote `refs/stash` and `logs/refs/stash` into the COMMON git directory `bio/.git/` while that worktree's own `refs/` stayed EMPTY. **Sixty checkouts share ONE stash stack**, so `stash@{0}` means "what any of the sixty pushed last", and `git stash push -u` carries UNTRACKED files. **THE INSTANCE IS IN THE OBJECT DATABASE:** stash commit `8706832`, headed `On worktree-agent-a773e28c7c7d0fb8b: RESTORED BY UI-50 SESSION: another session's D-228 work, accidentally popped from stash by ui50`, contains a DIFFERENT worker's `src/query.mjs`, `test/search.test.mjs` and `test/meaningquery.test.mjs` — one worker's files deposited in a worktree that never wrote them. **RE-ARMED AND REPRODUCED** in a scratch repository (`bio-plane/test/battery-provenance.control.sh arm2`): A writes an untracked suite, A pushes, B pops, the suite appears in B byte-identical (sha256 `a10e94a7…`). **WHY IT OUTRANKS ITS SIZE:** every worker here is briefed to MEASURE ITS OWN BASELINE AND TRUST IT OVER ITS BRIEF, and the recipe most of them use to get a true baseline is `git stash` — so the practice that defends against stale figures is itself the delivery mechanism for a phantom, and a contaminated baseline is a wrong number carrying full confidence. **UI-50's reading that "another session is working in this worktree" is NOT required by the evidence and is probably wrong:** an interleaved push between its own push and pop produces exactly that appearance from one cause. | M0 · **HALF CLOSED 2026-08-08 (M0-15), and the half that is closed is DETECTION, not prevention.** `scripts/battery.mjs` now checks every suite it RAN and every fleet manifest that enrolled one against `git ls-tree HEAD`, NAMES anything not in a commit (UNTRACKED vs staged-not-committed), prints beside the contaminated total the total another checkout reproduces, and says UNVERIFIED rather than clean when git cannot answer. Driven end to end by `test/battery-provenance.test.mjs` over 7 scratch repositories. **NOT closed: the deposit itself.** The runner cannot stop a `pop`; what it can do is stop a phantom being counted silently. **THE REMAINING WORK IS A CHANGE TO THE WORKER RECIPE, NOT TO THE RUNNER**, and it is written into `ORCHESTRATION.md`'s failure table in the same turn: a worker that must re-measure at HEAD should not use the shared stack at all, and if it does, must push with `-m <agent-id>` and restore BY THE STASH COMMIT SHA it captured, never by `stash@{0}`. Two other discovery walks over the same uncontrolled directory remain unguarded and are NAMED rather than left silent: `scripts/coverage.mjs` (suites and fleet manifests) and `test/hygiene.test.mjs` (three corpus walks) — a phantom inflates the register corpus, and a `REGISTER_FLOOR` moved while one is present would be permanently too high. **DETECTION FULLY CLOSED 2026-08-08 (M0-16), AND THE RULE MOVED RATHER THAN BEING COPIED.** M0-15's check now lives in `bio-plane/scripts/provenance.mjs` and the walks are its CALLERS — four statements of one rule is how the next goes stale, REC-46's and REC-35's argument — so `battery.mjs` prints exactly what it printed before and `battery-provenance.test.mjs` is 23 pass on both sides of the move. Guarded: `battery.mjs` 2 walks, `coverage.mjs` **3** (the item said 2: the `wrangler.jsonc` walk that decides whether a directory IS a Worker is a third), `hygiene.test.mjs` 3 over `test/` **plus its 5 over `src/` and `checks/`, which the item did not name and which are the same class** — a `pop` deposits an untracked `src/*.mjs` as readily as a suite. **THE PAYLOAD WAS THE FLOOR AND IT IS NOW CLOSED AT THE FLOOR:** `REGISTER_FLOOR` is compared against, and the report tells the reader to move it to, the REPRODUCIBLE figure — and this fired during the build rather than in theory, printing `arms 534` beside a reproducible `526` and refusing the higher one until the commit landed. **A SECOND DEFECT FOUND WHILE GUARDING:** `coverage.mjs`'s fleet walk carried NO dotfile filter while the battery's always has, so it could ENROL A MEMBER THE RUNNER IT REPORTS ON WOULD NEVER RUN — coverage credited from a source read while nothing executed it, which is D-117 one directory out and in the generous direction; the two walks now agree. **CLASS SWEPT AND RATCHETED, not judged:** `hygiene.test.mjs` counts every `readdirSync` site in the estate — **21 files walk, 3 GUARDED, 18 NAMED** — and a new one fails by name; what the matcher cannot see is stated at the site and in `VERIFICATION.md`. **ONE MEMBER OF THE CLASS IS WORTH AN ITEM AND IS DELEGATED IN `CLAIMS.md`: `civicos-ui/test/run.mjs`**, the UI battery's runner, which discovers over an uncontrolled directory and whose printed total IS a baseline sessions quote — `battery.mjs` before M0-15, one estate over. **STILL NOT CLOSED: the deposit itself, and CONTENT.** `ls-tree HEAD --name-only` answers about a PATH, so a tracked file whose CONTENT was replaced reads as in-the-commit; this detects an ARRIVAL, never a MODIFICATION. |
| D-240 | defect | 2026-08-08 | **TWO MORE INSTRUMENTS IN THIS REPOSITORY GRADE A RETURN BY ONE LITERAL, AND ONE OF THEM IS THE FILE REC-70 FIXED — ON THE HALF IT DID NOT TOUCH.** Found 2026-08-08 by REC-76's class sweep, which is the question D-236 asked one altitude up: *which other instruments grade a return by one literal?* **CORPUS SWEPT: 232 instrument-candidate files across 8 directories (bio-plane/scripts, bio-plane/test, civicos-ui, civicos-ui/test, tools, agent-worker, pdf-worker, newgroup), 6,309,940 chars, 214 of them reading or matching source text.** **REACH: 36 regex literals naming a verdict-shaped field beside a boolean literal; hand-verified down to TWO real classifiers** — the rest are single-site PINS (a regex anchoring one named return, which is a different and legitimate thing) or NEGATIVE CONTROL prose. **(1) `bio-plane/test/meaning-bounds.test.mjs:361`, `const REFUSAL_RETURN = /\bok\s*:\s*false/`.** REC-70 INVERTED the success side of this walk — *grade every return that does not declare itself a refusal* — and the EXCLUDER that decides what a refusal is stayed one literal. Its own comment states the claim: *refusals in this plane are `ok: false` with a `reason` … they are the only shape excluded.* **MEASURED over `store.mjs`: 489 return-object literals are excluded as refusals and 72 more are refusal-shaped and NOT excluded, across 37 distinct verdict fields** — `found: false` x16, `started: false` x4, `terminated: false` x4, `recorded: false` x4, `configured: false` x4, `ok: !s`, and thirty more. So a refusal in any of those spellings is handed to a walk whose whole subject is UNBOUNDED COLLECTION READS, and would be graded as one if it published a collection key. **The direction is the generous one — it inflates the BARE roster, which REC-70 ratcheted — so a false positive there is a ratchet nobody can hold.** **(2) `bio-plane/test/plane-envelope.test.mjs:180`, DETECTOR A: `if (!/^\s*\{\s*ok:\s*true\b/.test(c.arg)) continue;`.** **MEASURED over `index.mjs`: 152 `json()` calls, 26 GRADED, 126 SKIPPED**, and of the skipped **4 spread a Durable Object `.result`** — the exact thing the detector exists to judge. Three are `ok: false` refusal envelopes and legitimately outside its subject; **the fourth, `index.mjs:4587`, is `json({ ok: !!promoted.result?.ok, …, ...promoted.result… })` — a COMPUTED verdict spreading a store result, which is D-236's own shape arriving in a second instrument.** Whether that site needs an `.answered` check is for that suite's owner; what is certain is that the detector cannot see it. **WHAT THE SWEEP'S OWN MATCHER CANNOT SEE, stated rather than implied:** a classifier built from a STRING concatenated into a `RegExp` at runtime, one whose verdict field is held in a variable, and one that tests a PARSED value rather than source text — the sweep reads regex LITERALS in source. It also cannot tell a classifier from a pin without a human reading it, which is why the 36 were hand-verified rather than reported as the answer. NEEDS: each one inverted the way D-236's was — grade by whether the return DECLARES ITSELF a success/refusal, and NAME what cannot be classified. **NOT DONE HERE: `bio-plane/test/**` is not REC-76's, both files carry RATCHETED figures that only their owner can move honestly, and widening (1) would move REC-70's bare roster — a measurement, not an edit.** Delegated in `CLAIMS.md`. Relation: D-236, REC-70, REC-76, D-233's class. | M7 · **CLOSED 2026-08-08 (D-240's own item). BOTH CLASSIFIERS INVERTED OVER ONE SHARED READER** — REC-76's, imported rather than re-derived (`bio-plane/test/verdict-reader.mjs`, byte-identical to `civicos-ui/check-refusal-codes.mjs` function by function, with a DRIFT PIN both suites assert so a third reader cannot appear by drift). **THIS ROW'S FIGURES WERE RE-MEASURED AND SEVERAL ARE STALE — reported because the practice is to trust the measurement:** `store.mjs` is 23,523 lines here and dispatches **179** ops (the file's own comments still say 156), it holds **843** return-object literals, and `index.mjs` has **117** `json()` call sites and not 152 — that last figure predates REC-67's anchor correction, which took a 19%-inflated corpus off this very walk. **(1) `meaning-bounds.test.mjs`** — the excluder reads the VERDICT: 487 excluded by the old literal, **520 by the declared-refusal rule, +33 in 14 verdict spellings the old one could not see** (`found: false` x10, `recorded`/`started: false` x4 each, `admitted: false` x3, plus eight singletons), **0 lost by the widening**, 23 more carrying a COMPUTED verdict and 115 carrying none, both GRADED on purpose. **THE POLICY DIVERGES FROM REC-76's, AND IT WAS MEASURED RATHER THAN REASONED: REC-76 reads a COMPUTED verdict as a refusal, and applying that here took the suite 85/0 -> 82/3 and BOUNDED 20 -> 16 — four correctly-bounded reads losing their clean bill — because a COMPARISON is how this plane spells a CURSOR (`bundles.length === cap ? …`) and a TRUNCATION FLAG (`found.length > cap`), not only a verdict.** One reader, two stated polarities, each with its reason at its own site. **NO FLOOR MOVED, AND THAT IS THE MEASUREMENT: BARE stayed 38, OPAQUE 10, every roster identical.** The correction is a HARDENING whose value is prospective, and its receipt is control arm (5)/(5b): the SAME planted `found: false` refusal leaves the ratchet green under the new rule and **breaks it at 39 of 38 over a NON-DEFECT under the old one.** **WHAT IT COSTS, NAMED AND PINNED BY NAME rather than absorbed: REC-76's ordering rule — the first boolean-shaped property is the verdict, true over its SIXTY governed sites — DOES NOT HOLD over 843 returns.** Two SUCCESS returns lead with a boolean DATUM and are now excluded (`#sessionRights` `rootOfTrust: false`, `#conditionHomes` `ungrouped: false`); both are pinned by name beside an arm asserting NEITHER is reached by a dispatched op, so no ratchet moves on their account. **(2) `plane-envelope.test.mjs` DETECTOR A** — the gate grades a declared success AND a COMPUTED verdict (23 -> 25 sites), still refuses a declared refusal, and NAMES its **14 UNCLASSIFIED** sites (11 hand `json()` a VARIABLE, which is REC-76's own recorded blind spot; 3 are literals with no boolean property) under a ceiling whose cross-check is that **not one of them spreads a store `.result`, gated at zero.** **VIOLATIONS 0 -> 0 WHILE THE REACH DELTA GOES 9 -> 15: this row's `index.mjs:4587` (now 4608) WAS correctly guarded all along and NOTHING WAS CHECKING THAT IT STAYED SO** — a mechanism believed on the strength of its existence rather than its behaviour. Control (4)/(4b) is the receipt: removing that real guard fires DETECTOR A naming `promoted` under the new gate and reports **ZERO violations** under the old one. **THE CLASS SWEEP WAS RE-RUN INDEPENDENTLY AND 2 IS THE NUMBER: 280 instrument-candidate files across 10 roots, 8,674,710 chars, 251 of them reading source or answer text, 33 regex literals naming a verdict-shaped field beside a boolean literal and 30 in classifier position** — every one of the 31 that is not these two is either a SINGLE-SITE PIN or the `mutating:` DECLARATION-TABLE family (13 of the 33, across `coverage.mjs`, `op-claims.mjs`, `capability.test.mjs` and six suites), **which is the same SHAPE closed BY CONSTRUCTION and is named rather than counted as an instance**: 159 of 159 `OPS` rows write the literal, nothing computes it, and `capability.test.mjs` holds the table total. A separate non-regex sweep (`includes`/`indexOf`/`startsWith`/`new RegExp` over a verdict) found two further hits, both single-site anchors. **WHAT NEITHER SWEEP CAN SEE, stated rather than implied: a classifier assembled from a string at runtime, one whose verdict field is held in a variable, and one testing a PARSED value rather than source text** — and `newgroup/src/release.mjs` is excluded by name, because it holds a whole copy of the bundled plane inside a string (FW-13's receipt). 10 control arms in `test/verdict-excluder.control.mjs`, **10 of 10 as declared**, each armed alone, every restore verified by sha256 AND by content against uniquely-named per-arm copies with printed byte counts under a floor, every arm reading the suite's own FOOT rather than a tally. **TWO INSTRUMENT DEFECTS THE CONTROL FOUND, RECORDED RATHER THAN SMOOTHED:** both new censuses first read `verdictOf` DIRECTLY instead of asking the gate/excluder in use, so arm (1) left every printed figure UNCHANGED over a REVERTED classifier — a report derived from a copy of its own subject; and the harness's register first compared declared SENTENCES and reported all ten arms as NOT AS DECLARED. Battery **134/134 at 8,400** against a re-run true baseline of **134/134 at 8,387** on the SHIPPED (rebased) tree, +13 attributed per suite (meaning-bounds 85 -> 92, plane-envelope 54 -> 60) with no other suite moving; the same +13 was measured before the rebase at 133/133 8,319 -> 8,332, and both baselines were RE-RUN rather than subtracted; `--strict` exit 0 read unpiped; UI harness exit 0. **`REGISTER_FLOOR` NOT MOVED, measured rather than assumed: the register records the FULLEST SINGLE declaration per suite and both files already declare fuller ones — 570/570 before and after, classified 129, corpus 130.** Residual: **D-254** — the reader is a COPY held by a drift pin, and the single-home import is one line in VERIFY's file, delegated. |
| D-243 | gap | 2026-08-08 | **NOTHING DETECTS AN ID THAT WAS ALLOCATED WITHOUT THE ALLOCATOR.** M0-17 closes the race; it does not make itself compulsory. A worker that reads `DEBT.md`, adds one and writes the next number gets exactly the behaviour of 2026-08-08 and no instrument anywhere says a word — the suite proves the tool works and `kickoffs/CONDUCT.md` puts it in every brief, and **that is a convention holding up a mechanism, which is the shape this project has been bitten by repeatedly** (REC-48 kept a marker line self-contained so a detector would read it; M0-9 removed that convention and read the block instead). **WHY IT IS NOT FIXED HERE, and the reasoning is worth keeping: a check that an id was minted is not writable after the fact.** The ledger is not committed (a committed ledger races, which is why it is not), so a reviewer at integration cannot ask it whether a given number was taken properly — the claim file is in CONDUCT's `.git`, not in the worker's commit, and the honest answer for every id allocated before 2026-08-08 is "unknown". **A gate that answers `unknown` for the whole corpus and `yes` only for ids taken since is a gate that fails honest runs, which is `VERIFICATION.md`'s own stated reason for not making `--strict` the gate yet.** NEEDS: the cheap half is a detector at INTEGRATION rather than in the battery — CONDUCT can ask `node tools/mintid.mjs --list` whether each NEW id in a merged branch is held in the ledger, and treat a miss as a question rather than a failure. That is a step in CONDUCT's loop, not a suite, and it is owed as an item rather than smuggled in here. Relation: M0-17, D-242. **CLOSED 2026-08-08, AND THE REASONING ABOVE WAS TESTED RATHER THAN INHERITED — IT IS HALF RIGHT, AND THE HALF IT MISSED IS THE ONE THAT COULD BE INSTRUMENTED.** The structural argument stands exactly as written: *was this id minted?* needs the ledger, the ledger is in no commit, so no suite can ask it and the honest answer for every id allocated before 2026-08-08 is `unknown`. **What it missed is that the HARM is not the question.** What an un-minted id COSTS is a collision — two things wearing one id — and **a collision is entirely in the text of the commit: it needs no ledger, and it answers yes or no rather than unknown.** So the class splits and gets two instruments instead of one loop step. **THE EFFECT, as a FAILURE:** `allocations`/`collisions` in `tools/mintid.mjs` read every registered namespace's ALLOCATION SITES (593 across 18 namespaces; `C` and `M` NAMED as ungradable rather than scored clean) and a duplicate fails **both `plancheck` — which CONDUCT runs before every push — and the battery**, which is the cheap-and-early copy plus the cannot-be-bypassed copy. **ON ITS FIRST RUN IT FOUND SIX COLLISIONS ALREADY IN `origin/main`, known to nobody; they are D-248.** **THE CAUSE, as a QUESTION:** `node tools/mintid.mjs --audit --base <ref>` classifies every id a branch introduces as HELD / NOT HELD / PRE-LEDGER against the ledger and the per-namespace watermark, and reports an allocation sitting above the ledger's high-water. **A question never fails the run** — that is this row's own reasoning about `unknown`, kept. `kickoffs/CONDUCT.md` step 2 names the command after the merge and before the push, and `mintid.test.mjs` ASSERTS that it does, so the step cannot silently leave the loop. **The "not owed yet, the ledger holds 2 ids" reservation expired by measurement:** the ledger held 9 ids in `D` alone when this landed, and `--base` already classifies M0-17's own `D-242`/`D-243` as HELD against older ids as PRE-LEDGER. **What is still NOT detected, stated so a clean run is not read as more than it is:** an un-minted id that has not collided yet and sits below its namespace's watermark; a collision inside `C` (dotted members repeat a family number by design) or `M` (declares no allocation site); and a collision between two branches nobody has merged — which is the one the merge creates, and the reason the command runs AFTER merging. | M0 · closed 2026-08-08 — **two instruments, split by what needs the ledger**: a duplicate FAILS `plancheck` and the battery; an un-held id is a QUESTION in CONDUCT's step 2. Residual renumber is D-248 |
| D-249 | gap | 2026-08-08 | **A FIXED PORT IS THE SAME CLASS AS A HARDCODED TEMP PATH AND NO INSTRUMENT IN THIS ESTATE CAN SEE IT — NEITHER THE ONE THAT CHECKS CONTAINMENT NOR THE ONE THAT REPORTS RESIDUE.** M0-10 named this blind spot at the site of its own containment check; D-237's reporting half **DECIDED IT BELONGS IN A ROW OF ITS OWN RATHER THAN IN THAT ITEM, and the reason is a category difference rather than a scoping preference. A PORT LEAVES NOTHING BEHIND.** The residue report is an after-the-fact filesystem walk — it asks what is sitting in a shared root, what grew while the run ran, and what a live pid chain has open. A bound port has no "after": the moment the process exits there is no artifact to find, no bytes to size, no mtime to compare. So the instrument that closed the reporting half is structurally incapable of covering this, and folding the port in would have made that item's sweep claim reach it does not have. **MEASURED 2026-08-08, over `bio-plane/test`, `bio-plane/scripts`, `bio-plane/migrate`, `civicos-ui/test`, `agent-worker/test`, `pdf-worker/test` and `tools`, matching `port: <4 digits>`, `.listen(<4 digits>`, `localhost:<4 digits>` and `127.0.0.1:<4 digits>`: EXACTLY ONE SITE, `bio-plane/migrate/local-plane.mjs:10` (`port: 8787`), which is a hand-run dev plane and NOT a suite — so ZERO suites pin a port today and this is LATENT, not shipped.** That is why it is a `gap` and not a `defect`, and why it is not urgent. WHAT IT WOULD COST TO CLOSE: a source-level matcher in `hygiene.test.mjs` beside M0-10's `GROUND` regex (a different corpus, a different fixture, its own reach and over-strictness arms), or a runtime bind probe. WHAT MAKES IT WORTH A ROW ANYWAY: the failure mode is D-237's exactly — two suites sharing one fixed port fail with different signatures on every run, kill each other rather than failing an assertion, and read as flake. D-231 and D-237 each cost a diagnosis to a defect of that shape. Relation: D-237 (the filesystem half, closed), D-186, D-231. | M0 · **CLOSED 2026-08-09 — AND THE GAP WAS MEASURED EMPTY BEFORE IT WAS PINNED, WHICH IS THE RESULT RATHER THAN A PREAMBLE TO ONE.** D-249's reach was reproduced exactly: over the seven roots it named, **ONE site pins a port and it is not a suite** (`migrate/local-plane.mjs:10`, `port: 8787`), and **ZERO suites do**. But a source measurement cannot answer the question the row actually asks, and D-249 said so itself — the blind spot it names last is "a port a DEPENDENCY chooses", and **131 of the 151 suites construct a Miniflare, every one of which binds**. So the question was taken to the OS instead of to the source, with `test/d249-port.probe.mjs`. **THE ESTATE PINS NOTHING AT RUNTIME EITHER:** three runs of a real suite listened on 60239/60254/60306, 60323/60396/60440 and 60473/60573/60733 — **zero ports in common**, bound by node AND by workerd (the entry socket belongs to the CHILD process, so sampling the node pid alone reports nothing and reads as "nothing binds" — the false negative the probe was built to avoid). **EIGHT CONCURRENT COPIES OF ONE SUITE: 8 of 8 exit 0 on ONE signature (18/0), no `EADDRINUSE`, no divergence** — which is the D-237 collision shape not occurring, driven rather than argued. Miniflare's ephemeral choice was MEASURED, not read from its documentation, which would have been its claim and not ours. **THE PIN LANDED ANYWAY AND IT FAILS RATHER THAN REPORTS**, beside M0-10's `GROUND` regex in `hygiene.test.mjs`: no suite may name a port in a numeric LITERAL. Inverted rather than listed (REC-70) — what makes a port safe is not which number it is but that the process ASKS THE KERNEL instead of naming one, so `port: 0` is the single excluded number and everything else is a pin. **THAT EXCLUSION IS LOAD-BEARING TODAY, NOT HYPOTHETICAL: `migrate.test.mjs:147` really does write `port: 0`,** and the `zero` control arm shows the check false-positives on that real, correct line the moment the exclusion is removed. **FIVE CONTROL ARMS, EACH ARMED ALONE, ALL FIVE AS DECLARED** (`test/d249-port.control.mjs`): baseline 594/0; `plant` (a real `port: 8787` into `bootstrap.test.mjs`) 593/1 naming the file and the number; `neuter` 593/1 failing the REACH arm while the corpus arm stays GREEN — which is the whole reason a reach arm exists; `widen` (grade any 4-digit number) 592/2 with **5,158 false findings from dates and ids**, the over-strictness direction; `zero` 592/2. Every restore verified by sha256 AND `cmp` against a uniquely-named per-arm pristine copy. **WHAT THE CHECK CANNOT SEE, and it is named at the site:** a port built by concatenation or held in a variable, a port from an environment variable, a port passed as a bare positional argument, and **a port a DEPENDENCY chooses** — so this check reading CLEAN is a statement about the SOURCE and never about what the estate binds. That second question has no standing instrument and is answered only by re-running the probe. **THE SWEEP FOUND A DIFFERENT PORT HAZARD THAT IS REAL, and it is NOT this row's and is not folded in: D-281.** hygiene 591 -> 594 pass, 0 fail. |
| D-252 | gap | 2026-08-08 | **TIER-3 ROUTING IS PER DOCUMENT, AND THE THING IT ROUTES IS PER PAGE.** `needsTier3` (`index.mjs`) answers TRUE when ANY page carries a `no_text_layer` marker, so a MIXED document — a text-layer report with three scanned exhibits stapled to the back, which is an ordinary shape for a Council packet — routes the WHOLE document to OCR. Today that costs nothing real, because no OCR member exists and the branch is untaken; the day one does, the cost is transcribing pages that already have perfectly good text and, worse, **REPLACING** them: the wire assigns `i2text = built.text` wholesale, so a good text layer would be overwritten by an OCR pass at cap `C` when the layer was better. **STATED AT THE PREDICATE rather than left to be discovered**, and it is a real limit rather than a hypothetical one: CPDF-9's exhibit was wholly image-only, so the mixed case was never in the measured corpus and nobody has counted how common it is. NEEDS: per-page routing — the member takes a PAGE LIST, and the wire merges its answer page-wise into the Tier-1/Tier-2 text rather than replacing the document. That needs a producer that works a page at a time, which is CPDF-12's to define, so the merge is written in the turn the member lands rather than guessed at now. Relation: CPDF-10 (the seam), CPDF-12 (the producer), I2's per-page text shape (which already has the right grain — `text.pages[]` — so nothing new is needed on the contract side). **RESOLVED 2026-08-09.** The row said the merge wanted writing in the turn that lands the member; establishing whether that turn had arrived was the first question and the answer is NO — CPDF-12 landed the RENDERER (`pdf-worker/src/pagepixels.mjs`), there is no `/transcribe` producer and no `OCR_WORKER` binding anywhere outside this suite's stub (grepped: the only two occurrences in the repository are the wire's own call and the test stub). **But the merge is not speculative and did not need one**, and that is the correction to the row: the two things being merged are BOTH landed shapes — the Tier-1/Tier-2 I2 text in hand, and `ocrTextFromMember`'s output, whose contract this file already fixes completely. So what was written is the CONSUMER-side rule, driven end to end through the op against the stub member that already existed. **WHAT LANDED: (1)** `tier3Pages` — the per-page grain, read from the same marker vocabulary as the routing predicate; `needsTier3` is DELIBERATELY UNCHANGED because a pageless `no_text_layer` marker must still select the document. **(2)** the page list travels in the `/transcribe` request as a HINT (the cost half — a member need not transcribe pages that already have text) and is re-checked on the way back, because a hint the consumer trusts is a contract enforced at the wrong end. **(3)** `mergeTier3Text` — an OCR page may FILL a page, never REPLACE one, on TWO conditions: the page carries the marker AND its base text is empty. The second is what makes the guarantee unconditional rather than dependent on a producer's own claim about its output. **(4)** the doctrine half, which is the part worth reading: a mixed document's chain is now the CONCATENATION of its parts, each derivation step carrying `extent: {kind:'pages', pages:[…]}`, and **a mixed document's `derivation_cap` is UNDETERMINED rather than the engine's letter** — because a text layer's fidelity is `null` and letting an OCR pass's `C` stand for the whole document would RESOLVE THAT NULL INTO A LETTER. That is not cosmetic: `gradeCeiling` reads null as "undetermined, which is a statement, not a permission" and `C` as permission up to C, so the merge would have handed a leg citing an unmeasured text layer a ceiling nobody measured. A leg citing a PAGE still gets that page's own cap (`derivationCap(chain, target)`), so the mixed document stays usable without the document-level claim being inflated. **(5)** a document that READ is now allowed to say what it could not read: `ocrNote` reached only the FAILED branch, so a mixed document read off its text layer said nothing at all about its exhibits. **A DOCUMENT WITH ONE PROVENANCE IS UNTOUCHED** — `mergedChain` of one part returns that part's chain unscoped, which is the over-strictness arm and is why every landed CPDF-10 assertion still passes unchanged. NEGATIVE CONTROLS: five new arms in `test/nc-cpdf10.mjs`, each armed ALONE; arm (g) restores the wholesale assignment and the suite fails 11 arms with THE HEADLINE first (the good page's references gone). Sweep: D-283, D-284. | M2 · **CLOSED 2026-08-09 (D-252's own turn, not CPDF-12's — see the resolution appended to the item).** The MERGE landed and is driven through `op=acquire`; the PRODUCER still does not exist and is still CPDF-12's. |
| D-263 | defect | 2026-08-09 | **REC-68'S MERGE (`0ca7640`) DROPPED ITS EDIT TO `VERIFICATION.md`, AND WHAT WENT WITH THE NUMBER WAS THE PROVENANCE.** FOUND 2026-08-09 by M0-20's class sweep — `node tools/mergecarry.mjs --range origin/main`, 182 merges, the second of three drops it found and one nobody knew about. The merge message states the resolution plainly: *"VERIFICATION.md kept OURS (127/127 · 526 arms) over REC-68's 120/120; ours is the later measurement"*. **The number was the cheap half.** REC-68's branch also rewrote the row's PROVENANCE SENTENCE, recording that the arms figure moved `471 -> 482` in the turn that invalidated it, taken from what `coverage.mjs` PRINTED, and that it moved TWICE — because writing a control's results into its own declaration raises the tally again. That sentence is in the merged tree nowhere, and the row it was replaced by explains how the figure moves using a superseded example. **NOT REPAIRABLE BY EDITING A NUMBER** — today's printed figure is `134 of 134 · 621 arms` against the row's `127 of 127 · 526`, which is ordinary drift and NOT attributable to this drop; what is attributable is the missing explanation of the double-move. NEEDS: whoever next moves that row re-states how the figure moves, from `0ca7640^2:docs/development/VERIFICATION.md` which still holds REC-68's text. Relation: M0-20, D-264. | M0 · **CLOSED 2026-08-09 (D-263). RECOVERED FROM `0ca7640^2` AND AMENDED, NOT RESTORED — AND THE AMENDMENT IS THE ITEM.** REC-68's sentence opened *"THE CURRENT PRINTED FIGURE IS `482 arms`"*. It was RIGHT WHEN WRITTEN and is FALSE TODAY (`--strict` prints 741), so restoring it verbatim would have traded a missing explanation for a false one — a second defect wearing the first one's clothes. **The perishable half stays in the register ROW where the integrator moves it; the durable half — HOW the figure moves — now sits beside the grammar it follows from**, in VERIFICATION.md's `D-263 PROVENANCE` span, where no measurement can age it out. **THE MECHANISM WAS DRIVEN, NOT BELIEVED, AND REC-68'S FORM TURNED OUT INCOMPLETE:** writing a control's results back into its declaration raises the tally because a result is written as a transition and transitions are what the matcher counts — but the count is `max(transitions, enumerations)`, so an ENUMERATED declaration absorbs prose results for FREE (measured 3 -> 3) and moves only once arrows OUTNUMBER ordinals (3 ordinals + 4 arrow results -> 4). **REC-68's unconditional form is true of arrow-grammar declarations and only sometimes true of enumerated ones**; a worker taking it on trust would be right most of the time, which is the worst way for a rule to be wrong. That boundary is the amendment and is stated at the site. PINNED by `bio-plane/test/register-grammar.test.mjs` (15 assertions, 10 declared arms): the span is bounded at BOTH ends per DEC-49's smallest-span rule, its history is compared against the REC-68 branch blob git still holds rather than a hand copy, and one arm FAILS if the block re-acquires a present-tense figure. **7 of 7 control arms armed and run, 7 as declared — after TWO came back wrong and one REFUSED TO ARM, all three recorded at the site.** Battery 152/152 · 9,704 (true baseline 151/151 · 9,688 measured at `19745ad`, delta +16 attributed by DIFFING two full runs). `REGISTER_FLOOR` 731/145/146 -> 741/146/147 from the printed reproducible figures, read TWICE because this item's own subject applied to itself. Relation: M0-20, D-264 — **which CONDUCT CLOSED at this merge, 2026-08-10, so the parenthetical this row landed with is corrected here rather than left to read as a live obligation** |
| D-264 | defect | 2026-08-09 | **CPDF-9'S MERGE (`1c5d96a`) DROPPED ITS CLOSURE OF D-232, SO THE LEDGER STILL ASKS FOR WORK THAT WAS DONE — AND THE CONSEQUENCE IS LIVE TODAY.** FOUND 2026-08-09 by M0-20's class sweep, the third of three drops over 182 merges and the second nobody knew about. CPDF-9's branch rewrote D-232's disposition from `HALF CLOSED ... Open half: the member's dependency resolution` to `CLOSED 2026-08-08 (CPDF-9)`, with the measurement in the row (`121/122 green + 1 named skip · DARK: pdf-worker` -> `122/122 green · 2 member(s) actually RAN`, the member's own suite 48 pass / 0 fail with `pdf-worker/node_modules` ABSENT) and two control arms. The merge kept main's row. **`git log -S "CLOSED 2026-08-08 (CPDF-9)" origin/main -- docs/development/DEBT.md` IS EMPTY: the closure never landed anywhere.** MEASURED 2026-08-09 on `65a2ea1`: D-232 still reads *"the battery names it dark on every run until it is done"* while the battery prints `fleet: 2 members beside the plane · 4 suite(s) discovered · 2 member(s) actually RAN` and **no `DARK:` at all**. So the row asks a future worker to do work that is finished, and the evidence that it is finished was written and thrown away. **THIS IS THE COST OF THE DEFECT M0-20 EXISTS FOR, IN A ROW RATHER THAN IN A FLOOR**, and it is why the check fails rather than warns. NEEDS: D-232's disposition corrected from `1c5d96a^2:docs/development/DEBT.md`, which still holds CPDF-9's text verbatim — CONDUCT's, because the ledger is CONDUCT's and a worker restoring another item's closure claim is the wrong shape. **AND THE REGISTER DOES NOT TRACK THIS — SAID PLAINLY BECAUSE THE FIRST DRAFT OF THIS ROW CLAIMED IT DID.** `KNOWN_HISTORICAL_DROPS` in `tools/mergecarry.mjs` grades HISTORY, and `1c5d96a`'s drop is true of that commit for ever; the row there is permanent and closing THIS row does not retire it. The repair is a fact about the current tree, which is why it is a debt row and not a register entry. Relation: M0-20, D-263, D-232. | M0 · **CLOSED 2026-08-10 by CONDUCT.** D-232's disposition restored VERBATIM from `1c5d96a^2:docs/development/DEBT.md`, the two bodies first compared byte for byte so the restore could not smuggle in a drifted body — they were IDENTICAL, and only the disposition field had been lost. **The recovered sentence was RE-CHECKED against a live run rather than trusted:** the battery on `eb367d1` prints `fleet: 2 members beside the plane · 5 suite(s) discovered · 2 member(s) actually RAN` with no `DARK:` name, so CPDF-9's claim is still true today. That check is D-263's rule applied — restoring a false sentence is worse than the gap — and it is the half a verbatim `git show` cannot do for you. `mergecarry`'s history row for `1c5d96a` stands and is not retired by this. |
| D-269 | defect | 2026-08-09 | **DEC-32 CLAUSE 1 WAS BEING BROKEN ON A LIVE MEMBER-FACING SURFACE — the plane's SUCCESS prose for a strength axis, not its refusals.** `Store.#axisResult` composes the `detail` sentence for all three axis states, and every fragment REC-42 added for the STRUCTURED case carried the analyst's vocabulary the decision forbids (*"NEVER show AND / OR / disjunction / grounds — not even as tooltips"*). **MEASURED at D-269 by driving the composer's real bytes over all three states: 18 of 30 composed sentences carried it, 42 hits.** The FLAT (unstructured) sentences were and are clean, so the defect entered with REC-42's structured extension and never touched the pre-DEC-32 answer. **IT WAS LIVE**: four sites in `civicos-ui/app.html` render `detail` verbatim off `op=inquirystrength`, a FIFTH channel exists because `#strengthWalk` embeds the whole sentence into a leg's `why`, `axisPanel` serves BOTH the inquiry page and the PUBLISHED SIGNED case, and `#freezeStrength` writes the sentence into `bundle.md`'s frozen frontmatter — so it was also being frozen into published bytes. It fired exactly when a member had structured their basis through UI-27's elicitation, the flow built to keep that vocabulary away from them. Found by UI-43, which correctly declined to pick the fix because it is a judgement about what a member sees. **Why nobody saw it: the REFUSAL side was swept (PL-14's canned translations, and the suite asserts the ban of every one) and the SUCCESS side is a different string that nothing was looking at.** The three hand-written BANNED lists in `civicos-ui/test/` do not agree with each other and NONE would have caught `independently sufficient`. | M9 · closed 2026-08-09 by D-269 — the PLANE was re-worded, not the surfaces silenced: DEC-32 bans the VOCABULARY, not the INFORMATION, clause 3's own remedy is *plain words* rather than suppression, and clause 7 makes *which set carried this* the thing a READER must be able to test — deleting the sentence would have removed that at every consumer for ever. Five structured-only fragments moved onto UI-27's existing member vocabulary (`sets of reasons`, `carries it on its own`); NO arithmetic line, no key and no flat sentence changed. `bio-plane/test/analystvocab.test.mjs` is the standing guard and is NOT a list of spellings: it DRIVES `#axisResult`'s real bytes and classifies the RENDERED string (a source grep is defeated by concatenation — that is how the first measurement read 2 of 3), and its ban family is DERIVED from the derivation's own comments and identifiers MINUS the words the member's flow already ships, so a FOURTH spelling joins the family in the commit that invents it. Six control arms as declared, including an OVER-STRICTNESS arm proving a comment, an identifier and a fixture id may use the words. Interface: **IC-50**. **WHAT IS NOT CLOSED and is the rest of the class: `MACHINE_FENCE_CHECKS.MACHINE_CANNOT_GROUND`'s canned translation (1 of 166 rows, 16 families) and 8 refusal `detail`s in `groundInquiry` — PL-14's ground, delegated with figures, NOT edited here.** |
| D-262 | defect | 2026-08-09 | **ELEVEN OF THE TWELVE `MACHINE_CANNOT_*` FENCES CARRIED NEITHER THEIR C-NUMBER NOR THEIR CANNED TRANSLATION TO THE CALLER — AND NO INSTRUMENT COULD SEE IT, WHICH IS THE HALF THAT MATTERS.** MEASURED 2026-08-09 by driving the plane's own op surface and reading the WIRE rather than the site. All twelve fences fire (REC-73 proved that under complete payloads); all twelve carry a catalogued C-number and a canned translation (REC-64 wrote eleven of them); **exactly ONE put either on the wire** — `MACHINE_CANNOT_MOVE_VERSION`, and it is the only site in the family that refuses through a helper that READS the catalogue row. The other eleven build `{ ok: false, reason: "MACHINE_CANNOT_…", detail: … }` by hand, and a hand cannot carry a row it does not read. **THE COUNTER-READING WAS ANSWERED RATHER THAN IGNORED:** a consumer holding the catalogue can resolve a bare code, so the wire arguably need not carry the sentence — except that the one consumer built since DEC-49 deliberately holds NO catalogue. `agent-worker/src/index.mjs` says so three times in its own words (*"the plane's refusal carries its C-number and its DEC-49 canned translation; this member passes it through UNCHANGED"*), and it is right to: a component that paraphrases a refusal is the thirteen-surfaces drift DEC-49 exists to close. **A pass-through that receives nothing passes nothing through.** So the sentence belongs on the wire, and the code belongs there too — BOTH, because they answer different questions (what happened, and where the rule is written). **WHY THIS IS AN INSTRUMENT ITEM RATHER THAN ELEVEN EDITS: nothing graded the RESPONSE.** DEC-49's guard (`civicos-ui/check-refusal-codes.mjs`) arm C grades the SITE against the CATALOGUE and says in its own header that it *"says nothing about a LIVE plane"*; `machinefences-dec49.test.mjs` grades the CATALOGUE against its RENDERER; REC-73 grades the FENCE against the ACT. **Not one grades what a caller receives.** So a fence could be perfectly declared, perfectly catalogued, pass every guard, refuse by name under a complete payload — and still hand a member a bare machine word, which is the exact failure DEC-49 exists to prevent surviving inside the mechanism built to prevent it. This project's most-repeated defect is a mechanism believed on its EXISTENCE rather than its BEHAVIOUR; here the mechanism was a checker. Relation: DEC-49, DEC-8, REC-64, REC-73, D-229, SK-1. | M4 · **CLOSED 2026-08-09** — **ONE control-plane decoration, not eleven site edits, and the choice is a measurement rather than a preference.** `dec49Attach()` in `src/index.mjs` attaches the catalogue row (`code`, `check`, `translation`) to any refusal leaving through `json()` — the file's own chokepoint doctrine, which `doAnswer`'s header already states: *"the fix is a CHOKEPOINT, not twenty-four remembered checks."* MEASURED: 118 of this file's 125 response returns pass through `json()` (the other 7 are a 204, a version string, two HTML pages and three byte bodies, none a refusal carrier), and that covers the generic store forward **AND the 36 `doAnswer` handlers that never reach it** — eleven site edits would have closed the forward's eleven and left all 36 exactly as they were. **IT NEVER OVERWRITES AND NEVER INVENTS:** a field already present is left as the site wrote it (so a divergent sentence is still catchable, and the new instrument's equality check can still fail), and a code with no row is left bare and reported as census (untranslated codes are REC-64's sweep — **that is the boundary between the two items, and the instrument draws it rather than an agreement**). No refusal site, no DEC-49 REGION marker, no code literal and no `OPS` row was touched, so the guard's arm C still COMPARES every code. **AND THE INSTRUMENT THAT WOULD HAVE CAUGHT IT NOW EXISTS: `bio-plane/test/refusal-wire.test.mjs`, 23 assertions** — it drives every member-reachable op with an empty payload under a signed-in member AND under an `ai` credential whose scope is HARVESTED from the OPS table, then grades every refusal RECEIVED against the catalogue: 274 calls, 137 ops, 176 refusal envelopes, 60 distinct codes, 30 graded and 30 in the census. **AND THE CLASS SWEEP FOUND THE DEFECT IS FOUR TIMES THE SIZE OF THE ELEVEN:** the same suite reports the STATIC class on every run — **47 sites across `store.mjs` and `index.mjs` name a CATALOGUED code and carry no translation of their own, 45 distinct codes**, against 16 sites that carry the row themselves. The eleven fences are 11 of 47, and the figure grows on its own every time a refusal is written by hand — which is the argument for one attach rather than N edits, stated as a number rather than a preference. What that matcher cannot see is stated at its site: a refusal assembled across statements, one built by a helper, or one whose code is in a variable — all of which leave through `json()` anyway, so the decoration covers what the walk cannot. Corpus floored and printed on every run; what it cannot classify is printed BY NAME. SEVEN NEGATIVE-CONTROL ARMS via `test/refusal-wire.control.mjs`, and **ARM D came back NOT AS DECLARED on its first run and found this item's own instrument wrong** (a blind catalogue threw a `TypeError` and the tally never printed) — corrected and re-run, all seven as declared. Raised **D-270** in the same sweep. |
| D-267 | defect | 2026-08-09 | **THE QUEUE'S ANCESTOR WALK IS BLIND TO `status: severed`, SO A PROJECT THAT WITHDREW FROM A QUESTION IS STILL A HOME FOR EVERY ITEM FILED UNDER IT.** Found by PL-13 by DRIVING it — a fixture project citing a shared inquiry with `status: severed` appeared in the `case.ancestors` of items about that inquiry — and asserted AS THE WALK ACTUALLY ANSWERS in `bio-plane/test/current.test.mjs` §4 and §5 rather than smoothed into the shape the arm first expected. **THE MECHANISM IS ONE LINE AND IT IS A PROJECTION GAP, NOT A LOGIC ERROR:** `promote` projects `references[]` into `refs(bundle_id, target_id, kind)` carrying `rel` and DROPPING `status` (`store.mjs`, the refs projection), so `#queueAncestorEdges`' `SELECT ... FROM refs WHERE target_id=? AND kind='cites'` cannot see a severance that the document itself records. **IT IS CLASS-WIDE, not PL-13's**: every producer whose `case` set comes from `#queueAncestors` inherits it — `out-of-inquiry-lead` and the two proposal kinds identically — and the same blindness would affect anything else reading `refs` for a live citation. **THE PLANE ALREADY HAS THE OPPOSITE RULE ONE OP OVER, WHICH IS WHAT MAKES THIS A DEFECT RATHER THAN AN UNDECIDED QUESTION:** `versionAct` refuses `VERSION_CURRENT_UNRELATED` for a project whose reference is `severed`, reading the project's OWN frontmatter, and PL-13's `#projectsDrawingOn` reproduces that predicate deliberately so the feed and the act agree about who is in the conversation. So today the act says a severed project has no stance to move while the feed still routes it that question's notifications. **PL-13 DID NOT FIX IT AND THE REASON IS THE PROJECT'S OWN RULE:** one producer filtering its own homes would be a SECOND implementation of the homes rule, and a rule with two implementations is the shape that has already absorbed a control here. The fix belongs at the walk or at the projection. NEEDS: either `refs` gains a `status` column (a projection change touching `promote` and every reader) or `#queueAncestorEdges` confirms against the citing document's frontmatter as `#projectsDrawingOn` does (cheaper, one method, one extra read per candidate edge). Whoever takes it should sweep the OTHER `refs kind='cites'` readers in the same turn. Relation: D-216 (the sharing edge), PL-13, PL-15, REC-20/DEC-16. **CLOSED 2026-08-09, AT THE WALK, AND THE PROJECTION WAS NOT TOUCHED.** The fix is ONE predicate — `#refEdgeSevered(citingId, targetId, rel)`, *is this edge recorded as withdrawn in the CITING DOCUMENT's own frontmatter* — CONSUMED by `#citesInto`, `#restsOnLive` and `#queueAncestorEdges`, so the rule now has one implementation and three callers where it had four inline copies and a fifth reader that did not know it existed. Teaching `refs` a `status` column was REJECTED on a measurement rather than a preference: fifteen reverse-edge reads plus the export manifest build against that shape, and only the document can be authoritative about a status anyway (D-21). **NO INTERFACE CHANGED, so no IC row is owed.** **THE ROW DID NOT PREDICT THE OTHER HALF AND IT IS THE SAME DEFECT:** `inquiry_basis` is the identical kind of projection, drops `status` identically, and `#queueAncestorEdges` walks it too — so an inquiry that WITHDREW the reference under its basis leg was a home in exactly the same way, and nothing had named it. Both halves are confirmed and OR-ed, so a bundle reached by two edge kinds keeps its home while either is live; both are armed APART in the control harness, which is what proves neither is covering for the other. Severance narrows ONLY on a positive recorded withdrawal — unreadable is live, unrecorded is live, an unrecognised `status:` spelling is live — because a fence tighter than its rule drops homes nobody gave up. PL-13's three pins in `current.test.mjs` §4/§5 went red exactly as designed and were CORRECTED with a dated reason at each site, never exempted. Landed with `bio-plane/test/severedhomes.test.mjs` (14 arms, driven through `op=queue` and `op=backlinks`) and `bio-plane/test/severedhomes.control.mjs` (7 arms, 0 came back wrong). The class sweep that found the basis half also found SIX MORE reverse-edge reads that do not confirm — raised as **D-280**, one of them DRIVEN. | M9 · CLOSED 2026-08-09 — fixed at the walk through one shared predicate, PL-13's pins corrected rather than exempted, sweep residue raised as D-280 |
| D-277 | defect | 2026-08-09 | **THE CHECK CATALOG WAS HARVESTED FROM RAW SOURCE, COMMENTS INCLUDED, SO WRITING AN ORDINARY EXPLANATORY COMMENT FAILED THE GATE.** `bio-plane/scripts/coverage.mjs` built the conformance catalog by matching a check-id shape over the bytes of `checks/bio-checks.mjs`. A numeral written in PROSE — a comment explaining a rule, a note recording which numeral an allocation deliberately skipped and why — was harvested as a CATALOGUED CHECK, nothing then named it, and `node scripts/coverage.mjs --strict` exited 1 on a family that was complete. **REPRODUCED BEFORE ANYTHING WAS CHANGED: one comment of that kind took the catalog to 225 against 224 and the gate to exit 1.** **THIS IS THE DOCUMENTATION-POISONS-ITS-OWN-CORPUS CLASS FOR AT LEAST THE FIFTH TIME** (`tools/mintid.mjs` caught its own debt row poisoning its own floor; `hygiene.test.mjs`'s walk census caught its own correction because the comment explaining the removal spelled the token; UI-43 found it again; `walkfloor.mjs`'s first draft blanked strings as well as comments and reported zero cross-file floors over an estate with four) **AND THE FIRST ONE THAT GATES** — the others report. The catalogue had already bent ITSELF around the instrument: its renumbering note says in its own words that it deleted its own worked example because this harvester read it as code, which is the documentation being edited to suit the tool. **THE FIX IS AN INVERSION, NOT A LIST OF PROSE SPELLINGS TO SKIP:** the corpus is now DECLARED CODE (`bio-plane/scripts/declared-source.mjs`), which keeps a real declaration in ANY spelling — measured, the catalogue writes an id in three, and a matcher built from the two obvious ones would have lost the retired ids the retirements table requires to keep being counted. **THREE MORE INSTANCES OF THE SAME CLASS WERE FOUND IN THE SAME INSTRUMENT AND ALL THREE POINTED THE GENEROUS WAY.** (a) The CREDIT side read suite comments, so three checks read as covered on the strength of a SENTENCE — one of them a sentence in the very suite that proves checks fire. (b) The credit matcher had no word boundary, so a check was credited because a SIBLING was named, twelve ids matching that way and one credited only that way. (c) A DECISION id of the same dotted form ended in a check id's bytes and was catalogued as a check, reading as covered because suites discussing that decision contain the same bytes — two errors cancelling. All four closed; the three exposed checks now have real FIRES / absent-on-clean proofs in `check-firing.test.mjs`. The op-reach matcher had the identical shape and was closed too, measured at 0 of 163 ops affected today, so that half is a hazard shut rather than a figure corrected. Relation: D-271 (which met this), D-238, D-233, M0-18. | M0 · **CLOSED 2026-08-09** by this item — instrument, suite (`test/declared-corpus.test.mjs`) and a seven-arm control driving the real gate (`test/declared-corpus.control.mjs`), with the arms shown to BITE by re-running them against the old rules |
| D-258 | defect | 2026-08-09 | **`plan.meaning.columns` AND `plan.meaning.refs` ARE COMPUTED AND READ BY NOBODY — D-255's exact class, one field along, found by the instrument D-255 built rather than by reading.** `compile()`'s meaning descriptor (`query.mjs`, the `meaning:` block of the returned plan) carries EIGHT fields. `op=meaningrows` reads six of them at `store.mjs:1330-1336` — `arm`, `table`, `grain`, `identity`, `limit`, `offset` — and publishes all six. It never reads `columns` (`MEANING[rowArm].row`) or `refs` (`MEANING[rowArm].refs`), and neither does anything else. **MEASURED 2026-08-09, and the two halves are named apart rather than reported as one:** `columns` BEHAVIOURALLY — armed as a getter that THROWS, the WHOLE battery stayed **138/138 green** (`fieldread.control.mjs` ARM C); `refs` by the read sweep (written 6,930 times, read 0) plus reading its only plausible consumer, `store.mjs:1330-1336`, which does not mention it. **`refs` therefore has NO tripwire arm yet and is the weaker of the two claims** — one line in that file arms it, and whoever closes this row should run it rather than inherit this sentence. **THE DISTINCTION THAT MAKES THIS A REAL FINDING RATHER THAN A GREP RESULT: `MEANING[arm].row` and `MEANING[arm].refs` ARE live** — read inside `compile()` itself (the `refs` presence columns at `query.mjs:1306/1317`, the `searchFields` vocabulary at `:332`) and by `meaningread.test.mjs`. It is the COPIES placed on the plan that nothing asks for, which is why a name-based sweep reports both names as read and finds nothing. **NOT FIXED WITH D-255, and the reason is a choice rather than caution.** Unlike `atom.phrase`, these sit on a descriptor whose every SIBLING is published by an op, so "give them a reader" is a live alternative that deleting them forecloses — a surface building a table over meaning rows wants to be told which columns a row carries, and `grain`/`identity` are published for exactly that reason. Adding two fields to a published envelope is an interface change belonging to whoever owns `op=meaningrows` (PL-9's), which a D-255 worker may not take on its behalf. **Closing it is one of two one-liners:** delete both fields from the plan, or publish them in the op's envelope and file the IC. Whoever takes it should decide which and say so — and the arm is already written: `bio-plane/test/fieldread.control.mjs` ARM C. | M3 · **CLOSED 2026-08-09 — BOTH FIELDS DELETED, and the delete-or-publish decision is recorded AT THE SITE in `compile()` rather than only here.** **WHAT DECIDED IT, and it is a measurement rather than a preference: the fact `columns`/`refs` carry is ALREADY PUBLISHED, per arm, by `op=searchfields`** — `meaningVocabulary()` emits `rows: { grain, identity, columns, refs }` off the same registry, and that is the vocabulary route surfaces actually compose from. **MEASURED CONSUMERS of `op=meaningrows`, repo-wide 2026-08-09: `civicos-ui` NONE (zero occurrences); `agent-worker/src/index.mjs` ONE call site, which reads `.reached` and `.rows` and nothing else.** So publishing them would have added two fields to an envelope no surface reads, on behalf of a consumer that does not exist — and `columns` restates the keys the returned rows already carry, whereas `grain`/`identity` say what a row MEANS and how it is ADDRESSED, which the rows cannot say about themselves. That is the line the row asked for between a field a member can act on and a value published because it had already been computed. **NO IC ROW IS OWED — no published envelope changed**, pinned key-by-key through the op in `meaningread.test.mjs`; the PUBLISH alternative would have owed one, and reversing costs those two lines plus that row. **`refs` GOT THE TRIPWIRE ARM THE ROW SAID IT LACKED, and it was not inherited: ARM R2** (`fieldread.control.mjs`), plus its own positive control **ARM P3** — the tripwire on `refs` WITH a read injected into `store.mjs` in the unanticipated spelling `plan.meaning["ref"+"s"]`, which takes `bounds` and `meaningread` red and both REPRODUCE alone, so R2s green is not free. **THE CLASS WAS DECIDED, NOT LISTED: `--tripwire-sweep` gives every one of the descriptors fields a VERDICT** over a whole battery each — `arm`/`table`/`grain`/`identity`/`limit`/`offset` **LIVE** (the five D-255 could only assert are now measured), `columns`/`refs` **DEAD** — with a BASELINE row aiming the identical mechanism at a field that cannot exist. **TWO INSTRUMENT DEFECTS FOUND BEFORE ANY SUBJECT DEFECT AND BOTH RECORDED RATHER THAN SMOOTHED:** (i) the first version of the mode used a recording Proxy, whose BASELINE row failed `meaningread.test.mjs` — not workerd, but `meaningread.test.mjs:100` pinning the LITERAL SOURCE TEXT the Proxy wrap rewrites; that mattered because the same suite drives `grain`/`identity`, so a live published field could have been scored DEAD behind its own contamination, and the mechanism was replaced with D-255s throwing getter; (ii) `refs` came back **LIVE** on its first pass because `daemon-token.test.mjs` failed under SIX concurrent batteries — driven alone against the identical armed tree it passed 56/0 with the tripwire never firing, and a whole-battery re-run of the same arm was 141/142 with only the ratchet, so **every failing suite is now RE-DRIVEN ALONE before it counts as a read**. A red battery is not evidence of a read. **Evidence no answer moved: the answer-determining digest is `66593efe4289784d…` over 3,680 compilations, byte-identical either side** (and it is a `--digest` MODE now; D-255s was a one-off that left nothing re-runnable). **TWELVE ARMS, all AS DECLARED, tree PRISTINE by sha256 AND `cmp` on BOTH subjects.** The ratchet is three structural pins in `query.test.mjs` plus the envelope key-set pin in `meaningread.test.mjs`. **D-255s ARM C was CORRECTED, not exempted** — its anchor no longer occurs, so it re-adds the field first and declares the exact suite set. The sweep also turned up an unrelated live defect in the one real consumer: **D-276**. |
| D-271 | defect | 2026-08-09 | **A CHECK DESCRIBED A GUARANTEE IT DID NOT PROVIDE, AND THE ACCEPT CEREMONY WAS BUILT ON TOP OF IT — both halves MEASURED by driving the ops, not by reading them.** Found from the surface side by UI-43 while building DEC-32 §12's accept ceremony; verified from the plane by this item, and **UI-43's reading was right on both halves and NARROWER than the truth on one.** (a) `op=suggest`'s CHECK 4 carried the sentence *"the derivation is PUBLISHED on every version that passes, so the member affirming independence at §12's accept ceremony is affirming it against what the record can see."* `shared_origins`/`origins_complete` went out on `op=suggest`'s WRITE answer and on NO read — not `op=basisversions`, not `op=versionstrength` — so a member at the ceremony, who is reading, affirmed against nothing. **AND THE WRITE HALF CARRIED NO INFORMATION EITHER**, which UI-43 did not claim and this item measured: CHECK 4 REFUSES every non-empty result, so a version that passes necessarily carries `[]`, and a one-part reading (where the origin walk never runs) published byte-identical bytes to a two-part reading that was genuinely walked — `[]` conflating *looked and found nothing* with *there was nothing to look for*, which is D-129 at the field grain. (b) DEC-32 clause 4 requires independent sufficiency be affirmatively claimed PER PART by an act carrying a member's name; **no version act had anywhere to put it**, so the record could not distinguish an accept made after affirming each part from one made by a surface that never asked. RESOLVED: `#independenceOf` is now ONE implementation with TWO consumers — CHECK 4 (which refuses on it) and `op=versionstrength` (which publishes it to the ceremony), **recomputed against the record rather than replayed from the write**, so a shared origin recorded AFTER a version landed is visible at the moment it matters and `checked:false` says when nothing was walked; and `op=versionaccept` takes `affirmed`, refuses an incomplete or unknown naming of the parts (C-25.33) on readings declaring more than one, stores the PART NAMES (never a bare boolean) on `inquiry_basis_versions.affirmed_parts`, and `op=basisversions` publishes them. The CHECK's sentence is CORRECTED at its site with the measurement, never exempted. IC-52. | resolved 2026-08-09 (D-271) · IC-52 filed, version bump and RESOLUTION are CONDUCT's |
