# REC-173 — worker report (CONDUCT #15), relayed for CONDUCT #16. INTEGRATE IT.

Branch `land/worker/REC-173` @ a6f5c815 (base includes b41d1edb; a13667ee merged, no markers). GREEN FULL `292/292 suites
green · 17475 assertions passing` on tree 414dd20b; coverage --strict and UI harnesses green.
BUILT: `index.mjs` op=promote's stamp block + new `migrationReplayOf`: a REPLAY iff (1) ADMIN deploy token (not a session)
and (2) the named `provenanceCapture` is registered in the same creation at `migration/drive-provenance.json`, its bytes
re-read from R2 and hashed, a preserved Drive record has target = this bundle id listing bundle.md at the SHA-256 COMPUTED
from the text, and the file's own sha256 equals that computed value. A replay: no rule-2 run check (REC-171's stamp line
unchanged, one line after removes it), D-78 does not restamp (Drive-era surfaced_by kept), a row in NEW TABLE
`inquiry_migration_replays`, op=projection reads `surfaced_in: not recorded (migrated from the Drive era)`. Everything else
is REC-171's ordinary creation; `migrationReplay` is a server stamp deleted first for every caller. migrate.mjs REORDERED:
the creation registers and names the capture (was registered only on the LAST revision, after the creation); admin-only.
migrate.test (Drive-era question, admin token): dies SURFACE_NO_RUN on b41d1edb → 48/0. New rec173-migration-replay.test:
12/7 → 19/0. Control 11 arms AS DECLARED: no-sha (N3 altered bytes), liars caller-made capture N5a-c, stale SHA N3b,
member/probe token N4/N4b, forged stamp N6, no run-skip N7, over-strictness R2 admitted; migrate.test control arm 5 fails by
name. REC-171's (9) and D-85's controls re-run: AS DECLARED. Census moved 197/102 (one table), §3 re-rendered.
IC-190 PROPOSED I3 + I5 additive (proposal 58.0.0 → 58.1.0, 1.26.0 → 1.27.0) — RESOLVE ON THE BASE AT LANDING.
OWED AT INTEGRATION: re-read the promote spans (DEC-49 regions/ratchets) and REGISTER_FLOOR on the merged tree; re-run
REC-173's and REC-171's controls (authority path).
DESIGN GAP (to BOB): "a capture already in the record" — promote is the register's only writer and a register row names its
bundle, so it is built as bytes held before the creation and registered BY the creation; a separate prior act is BOB's call.
DEFECT (RECORD-INTEGRITY, to SCHEDULER): op=promote keeps a caller-supplied bundle.md `sha256` as the bundle's head without
checking it against the text — driven: sha `fff…` stored with ok:true. FIX: promote computes each inline text file's SHA-256
and refuses a mismatch (a new refusal code + translation).
