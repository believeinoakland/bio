# extraction — requirements

**Status** · DRAFT by a drafting worker for BOB #42, 2026-09-26 (P18), from a reading of the code; for Bob's approval (a product module, P17). Layer 4. Code today: its own files `bio-plane/src/readingprov.mjs` (218 lines), `calibration.mjs` (429), `extractrun.mjs` (347); and inside the legacy modules, measured at `tranche/T3` @ `91933d75`. `bio-plane/src/index.mjs`: the acquire wire's text budget (357–418); `reextractRow` (4123–4129); the tier ladder, merges, chain and reading composers `needsTier2` … `ocrTextFromMember` (4624–5803); `op=pdfstructure` with its `ocr=1` re-read (7614–7957); the reading and text block of `op=acquire` (9225–9928; K49's 9250–9932). `bio-plane/src/store.mjs`: the reading writer, re-extraction, history, text-source and text-index writers `#writeReadings` … `#writeCaptureText` (19969–20626); `#backfillRefTerms`, `readingTermsClear`, `readingHistoryClear`, `reindexNames`, `readingFor`, `transcribedDocuments`, the calibration region and `documentsByReference` (24487–25201); the term helpers (25570–25629); the text-index constants (779–800); their dispatch arms (52392–52479, in part). `schema.mjs`: `readings`, `reading_refs`, `reading_ref_terms`, `reading_text_source`, `reading_history`, `capture_text` (and `capture_text_fts`), `calibrations`, `calibration_subjects`, `calibration_signals`. `bio-checks.mjs`: `CALIBRATION_CHECKS` (C-42), `REEXTRACT_CHECKS` (C-51). The map is `build/extraction/extraction.md`. Not yet met: R1 (K49), R3 (D-684, D-593), R8 (D-635, D-713), R9 (D-665, D-697), R10 (D-614), R13 (D-694), R16 (D-685, D-724), R18 (N21), R20 and R24 (K31), R21 (Open for Bob 3), R22 (N28, D-724), R35 (D-616), R36 (the index state), R43 (D-587, D-668), R46 (D-668), R56 (K23). Old-plan rows carried here: D-593, D-694, D-724 (K49), D-587, D-614, D-616, D-635, D-665, D-668, D-684, D-685, D-697, D-713.

**Size (P6).** About 5,450 lines move (about 2,250 without comment-only lines): the three files 994, `index.mjs` 2,297, `store.mjs` about 1,570 with its migrations, `schema.mjs` about 400, the two check families 146. That is over the 4,000 at which BOB reports a module. One session can still read it with the public parts of its uses, since 60% of it is comment, but see Open for Bob 2: split calibration (about 1,140 lines) into its own module before this one. Not counted, because they go elsewhere: the attestation of a transcription and the citation context (`attestText`, `attestationsFor`, `contentContextFor` and its readers, `text_attestations`, about 470 lines) go to `content`, whose draft also claims them; the AI proposal store (`extractPropose`, `extractProposals`, `proposed_readings`, about 330 lines) goes to `ai-runs` (map §2–§3).

## Public

### Purpose

Makes readings from captured bytes. It produces a capture's text through the tier ladder (the format entry in the plane, the `pdf-worker` member, the `ocr-worker` member), composes the transcription chain as it goes, runs the content type's reader over the text, and records the reading with its provenance, its references as they appear, its text units and its text-source projection. It keeps every distinct reading of a capture and attributes a re-read that disagrees; re-reads a capture at a member's request; holds the calibrations of derivation engines and derives the drift they reveal. It mints no content (`content`'s), resolves no reference to an entity (`entities`'), fetches nothing from outside, and writes no bundle.

### Provides

Terms. **I2 text** is the text shape format entries and the two members return (`format-registry`, `text-chain`). **Tier** 1 is the format entry in the plane, 2 the `pdf-worker` member (binding `PDF_WORKER`), 3 the `ocr-worker` member (binding `OCR_WORKER`). A **glyph** is a non-whitespace code point (`text-chain.glyphCount`). A **failed reading** is `found: false` with no entities and a basis saying why; it is never an emptied document. The **viewer** is the control plane's stamp; a bundle the viewer may not see is withheld as `membership` answers (R43–R44 there).

**read(document, env) → `{reading, text_units?, text_units_over_bound?}`** Reads a stored capture. `document` is `capture`'s acquire answer's document (digest, parts, profile, transport headers, document address, provenance chain). Never throws; every failure is a failed reading.
- **R1** The bytes are read from the evidence store under the document's digest (`record-core.evidenceStore`, R38 there), never from the caller; no field the caller sends supplies text. With no store bound or the object absent, the reading is a failed reading saying so. `op=acquire`'s wire answer still carries `reading` and `text_units` (Open for Bob 1). *(not yet met: K49 — read inside acquire, from the fetch's own read-back)*
- **R2** Text read as text (a textual type, single part, within the profile's text bound) with no format entry that reads it is handed to the content type's reader (`docprofile.doctypeFor`'s `parse`): the reading is `{content_type, reader_version, read_from_text: true, found, entities, facts, at, basis, position_parts, position_why}`, `found` exactly when an entity was read. A reader that throws gives a failed reading naming the error; a type with no reader gives `read_from_text: false` and says so.
- **R3** A capture whose detected format has an entry with `text` or `structure` is read through that entry over the stored bytes (tier 1), including delimited text read as text at intake and a multi-part capture within the entry's bound; a CSV's text is the entry's own decode, never the intake's lossy one. *(not yet met: D-684, D-593 — a `text/csv` read at intake never reaches its entry, its text is the lossy decode, and a multi-part capture is not read)*
- **R4** Tier 2: a PDF whose tier-1 undetermined markers outnumber the glyphs of its text, unless every marker is `no_text_layer`, is sent to `pdf-worker` when bound and merged page by page by `text-chain.mergeTier2Text`. The tier-1 producer marker is carried when the member supplies none. The wired tier is 2 only when a page was replaced; a document with pages from both tiers gets a chain of two page-scoped parts; the merge's per-page statement is kept for R7. A member that fails, refuses or is unbound leaves tier 1 standing, and the reason is carried.
- **R5** Tier 3 selection: a document is a candidate when a page carries `no_text_layer` and none carries `encrypted`; the pages asked for are those pages, ascending. With `ocr-worker` bound, each page the member defers is asked for again, one page per call, in order, up to 24 calls per request. A page past that budget, the pages after a call that throws, a page the member declines, a page answered under another engine build (`engine`, `version`, `cap`, `measured_by` or `confidence_floor` differ from the first answer) and a page it was not asked for are not merged, and the note says which. With no member bound the document stays unread and the note says no OCR engine is installed.
- **R6** A member's answer is taken only when it names its engine and version and a measured `cap` with `measured_by`. A region with no checkable image anchor is dropped and counted; a region below the member's confidence floor reads undetermined and its text is discarded; a page no region anchors is not merged. The OCR part's chain is `pixels → ocr(engine, version)`, both at the member's cap, naming the live calibration of that engine and version (R42), or none when there is none or it cannot be read.
- **R7** The tier-3 merge fills only asked-for pages whose text holds no glyph; any other page the member returns is refused. A base with no per-page text refuses OCR when it holds a glyph and takes it whole when it holds none. The merged chain's parts are the tier-1 and tier-2 pages as R4's merge stated them, any other layer page at the wired tier, and the filled pages under R6's chain; a page with no glyph is in no part. The wired tier is 3 when a page was filled. `tier3_candidate` is true exactly when the document was selected and nothing was filled or a selected page is still unread.
- **R8** A page routed to OCR whose layer text is only a folio keeps that text, gains the transcription appended, and is listed in both parts; a later re-read never appends it again. *(not yet met: D-635, D-713)*
- **R9** A page tier 2 wins keeps a still-true `image_unread` marker; a page OCR fills, by replacement or append, discharges it. *(not yet met: D-665, D-697)*
- **R10** The tier-3 note gives each refused page its own reason (no such page, not asked for, carries glyphs), and says a page held text only when it did. *(not yet met: D-614)*
- **R11** The chain is null until a text surface answers. A layer's chain is `text-chain.layerChain` at the wired tier with its cap undetermined and the reason stated; a layer whose producer marker names OCR software gains `ocr(<product>)` uncapped. A Drive export (the document's chain carries Google's export hop) gets `convert(google-export, <format>)` at the head, cap undetermined, after every other part is settled; if that prepend is refused, no chain is recorded and the reading fails, naming the refusal.
- **R12** Final text goes to `docprofile.readText` with the headers, document address, content type and retrieval instant. A determined reading carries `text_source` (the chain), `text_tier`, `text_container`, and a basis naming the reader, the chain, the tier, the tier-2 and tier-3 notes and where references were read (all, some with the reason, or none with the producer's reason). An undetermined one is a failed reading whose basis gives the tier notes and the entry's reason.
- **R13** `page_count` is the structure reader's count when positive, else null, never zero. `container_extent` names the levels the entry emitted (sheets, paragraphs, slides by list; tables, images by key): a sheet's `rows`, `cols`, `usedRows`, `usedCols` as integers or null; slides keyed by slide number, as long as the deck's `deckLength`; for a PDF, the images its pages paint as `{page, rect}`, or `images_why`. Absent when no entry answered; present and null when one answered and itemised nothing. A delimited-text reading carries its sheet list. *(not yet met: D-694)*
- **R14** `reading.dialect` is absent when no entry answered a decoding choice, null when one answered and could not state it, else `format-registry.readingDialect`'s `{delimiter, encoding}`; a dialect signature that throws leaves it absent.
- **R15** `reading.provenance` is `readingProvenance`'s (R25), composed at one site for every branch, over exactly the text the reader was handed; text read as text at intake names the plane with tier null.
- **R16** Text units: one per page, paragraph or slide holding a glyph, its extent in the producer's own numbering and `seq` its position. The wire carries at most 512 KiB, each unit charged its UTF-8 bytes plus 128. A unit that does not fit is carried as its capped prefix marked `truncated`, and every unit left out is named. An absent unit list and an empty one are different answers. *(not yet met: D-685, D-724 — a unit that does not fit is dropped and only counted)*
- **R17** Nothing about the source is fetched: tiers 2 and 3 are reached only through their bindings, with the capture's digest and store.
- **R18** Every recogniser runs over the instance's jurisdiction view (`jurisdictions.combine` of record-core's `jurisdiction_profiles`), never a default. *(not yet met: N21)*

**Writing a reading: the promote projection and `writeReading({bundleId, captureSha, reading, textUnits, textUnitsOverBound, profileFormat, author}) → {kept, indexed, listeners}`**
- **R19** One writer serves promote and the re-read (R34), inside one `record-core.transact`. In order: keeps the history (R23); replaces the `readings` row (content type, reader version, found, entity count, the reading, `at`, and the capture format from the document's profile, kept when none is given); replaces the references, one row per distinct place (the entity's `source` first, then its `occurrences`, at most 256, with one unplaced row standing for any beyond), each position re-normalised by `text-chain.readingSource` (a position that does not normalise is stored as none), each reference raw `kind:key` and unresolved; replaces the name terms per source (reference, key, label); replaces the text-source row (transcribed, terminal step, engines, derivation cap, step count, chain, calibration ids named), writing none for an absent or malformed chain; replaces the text index (R22).
- **R20** Registered with `promotion` (R39 there): for each document in the bundle's `data/provenance.json` with a capture digest and a reading object, R19 runs, except that a stored re-read is not replaced by a reading that is not one and carries the same `at`. *(not yet met: K31 — `promote` calls the writer inline)*
- **R21** A reading carried in `data/provenance.json` that this instance did not compose is recorded as the caller's assertion, not as a reading (Open for Bob 3). *(not yet met: Open for Bob 3)*
- **R22** The text index: the capture's rows are deleted first, even when no unit is offered. Blank units are dropped; the rest are ordered by `seq`; each is capped at 128 KiB (`truncated`); a capture holds at most 4,096 units and 2 MiB of UTF-8, the rest counted over bound. A unit with no extent kind, or an address already written, is counted unaddressable. The chain kind is `text-chain.chainKindFor`. The answer gives `offered`, `written`, `truncated`, `over_bound` (with the wire's own count), `unaddressable` and the chain kind, and names each unit skipped. *(not yet met: N28 — the kind is `terminalStep(chain) || "layer"`; D-724 — skipped units are only counted)*
- **R23** Every distinct reading of a capture is kept in arrival order, keyed by the digest of its JSON, before the `readings` row is replaced. A reading equal to the latest kept one is not kept again. A capture whose one reading predates the history has it kept first. Each kept reading stores `compareProvenance` against the one before it (R26).
- **R24** `onReading(module, fn)`: a later module registers once (a second registration by the same module is refused `LISTENER_DECLARED`). After each write, every `fn` runs in the same transaction, in the modules' total order, with `{bundleId, captureSha, reading, chainBefore, chainAfter, indexed, author}`. A listener that throws fails the whole write, because a replaced reading whose cited passages were not marked stale would move an authored edge's target unseen. *(not yet met: K31 — the writer marks content stale and writes the observation rows itself)*

**The pure rules (`readingprov.mjs`)**
- **R25** `readingProvenance({text, chain, tier, container, planeVersion, member})` → scheme `reading-provenance/1`, `text_tier`, `container`, `text_sha256` of `docprofile.flattenText`'s text (null for empty or absent text, with `why`), `text_chars`, `text_from`, `producers` and `pages`. A page's tier comes from the chain first (a covering `pixels` step is 3, naming the next `ocr` step's engine; else the covering `layer` step's tier), then the page's own stamp, then the document's tier; the member is 1 → plane (with its version), 2 → `pdf-worker`, 3 → `ocr-worker`, else null. A page with no text is listed and neither digested nor credited. Text with no page grain is credited to one producer, and `pages_why` says so.
- **R26** `compareProvenance(prior, next)` → `agrees` (equal digests), `differs` (with `changed[]`: pages grouped by the tier and member before and now, and a sentence in the shape *tier 3 on ocr-worker returned different text for page 1*), `undetermined` (either side carries no provenance: never inferred from the tier or the chain) or `no_text` (neither digested text). `describePages` numbers pages from 1 and folds runs.

**Reading the record**
- **R27** `readingFor(captureSha, viewer)` (`op=reading`): `NO_SHA` without a digest; `found: false` for a capture not read; else the reading, reader, `at`, the bundle withheld from a viewer who may not see it, `text_provenance` spelled out by `describeChain` or `{recorded: false, why}`, and `reading_history` (at most 16, newest first, with `kept` and `truncated`; a reading without provenance reads it undetermined).
- **R28** `documentsByReference(ref, viewer)` (`op=readingref`): every captured document whose reading carries `ref` exactly, one entry per document with its first position and, when read at several places, every `occurrences` entry in reading order; the bundle withheld as in R27; an empty `ref` answers `count: 0`.
- **R29** `transcribedDocuments({terminalStep, transcribed, limit, viewer})` (`op=textprovenance`): the text-source rows filtered as asked, ordered by bundle and capture, at most `limit` (default 200, maximum 5,000) with `truncated` measured by reading one more; bundles withheld as in R27.
- **R30** `readingOf(captureSha)` → `{reading, chain, pageCount, containerExtent, textContainer, captureFormat}` or null: the persisted reading's facts that `content` bounds a citation by (content R12–R13). `pageCount` is the stored `page_count` when a positive integer, else null, never zero; `containerExtent` is as R13 stored it, absent when never stored and null when stored null; `captureFormat` is the profile's format key the reading was written with. It withholds nothing, being about the document's own bytes; callers gate the bundle.

**Re-reading (`op=pdfstructure`)**
- **R31** Without `ocr` it is a read: `sha256` must be 64 lowercase hex (required-argument refusal); no evidence store is the storage-absent refusal; an absent object is `NOT_FOUND` (404); no registered `pdf` entry is `FORMAT_UNREGISTERED` (501); a non-PDF is the entry's own answer (422). Otherwise the structure with tier 2 per R4 (notes `tier2_no_improvement`, `tier2_unavailable`), its `tier` and `provenance` (R25). Nothing is written, and the answer is byte-identical to the read before `ocr=1` existed.
- **R32** With `ocr` present, before any byte is read or engine called: a value other than `1` is `REEXTRACT_FLAG_MALFORMED` (C-51.1, 400); an `ai` credential `REEXTRACT_AGENT_REFUSED` (C-51.2, 403); a session without `contribute` `REEXTRACT_NOT_CAPABLE` (C-51.3, 403); no OCR member bound `REEXTRACT_NO_OCR_MEMBER` (C-51.4, 501); no reading of the capture the caller can see `REEXTRACT_NOT_READ` (C-51.5, 409), answered alike for one never filed.
- **R33** Tier 3 then runs per R5–R7. With no page filled the answer's `reextraction` is `{performed: false, written: false, cost, candidate, why}` and nothing is written.
- **R34** With pages filled, the reading is composed by R12's rule with the stored reading's content type and reader version, `at` kept as the capture instant, `page_count` and `container_extent` carried, R25's provenance, and `reextracted: {at, by, engine, version, calibration, pages, via: "op=pdfstructure&ocr=1"}`. It is written by R19, `by` stamped by the control plane. The answer's `reextraction` gives `performed`, `written`, the pages, engine, chain, a reading summary, and what the listeners and index reported (`staled`, `units`, `observed`, `compared`). No bundle version is minted.
- **R35** A re-read asks the OCR member only for the pages the stored reading still leaves unread, so repeated re-reads of a long scan reach its tail. *(not yet met: D-616 — it asks for the same first pages again)*

**The text index and the name terms**
- **R36** `unitsOf(captureSha)` → the capture's indexed units in `seq` order (extent, reference, text, `truncated`), at most 4,096, and the index's own state from its last write: `whole` (every offered unit written), `partial` (with the over-bound, skipped and unaddressable counts, R22), `none` (nothing was offered), or null (never indexed). The four are never read alike. `content` reads it (its R35). *(not yet met: the state is read from `observation-log`'s rows, `#indexStateOf`, which a layer-4 module cannot use)*
- **R37** `reindexNames({limit})` writes the name terms (R19) for up to `limit` stored references that have none, and answers how many it wrote and how many remain; the instance runs it once at start with 500. `readingTermsClear` and `readingHistoryClear` are test seams no op reaches.

**Calibration** (`calibration.mjs`, and the store)
- **R38** `checkCalibration(cal)`: an object (`CAL_SHAPE`, C-42.1) naming engine and version (`CAL_UNNAMED`, C-42.2), dated (`CAL_UNDATED`, C-42.3), a cap that is a `BASIS_GRADES` letter or null (C-42.1), and a probe run: `probe_id`, non-empty `probe_inputs` and `scores` (`CAL_NO_PROBE`, C-42.4). A null cap is legal.
- **R39** `compare(next, prev)` is on caps: no previous is `same`; a malformed calibration or another engine is `incomparable`; a lost bound is `worse`; a gained bound is `better`; two nulls are `same`. `drifted(verdict)` answers `raises_obligation` only for `worse`, and `regrades: false` on every branch.
- **R40** `nextProbeDue({lastAt, signals, cadenceMs})` is never later than `lastAt` + cadence (30 days, `CALIBRATION_CADENCE_MS`); a never-probed engine is due at once; a signal can only bring the probe forward. `checkSignal` refuses a malformed signal (`CAL_SIGNAL_SHAPE`, C-42.5) and one carrying a cap or scores (`CAL_SIGNAL_CLAIMS_MEASUREMENT`, C-42.6).
- **R41** `driftObligations(supersessions, bound)` names exactly the transcriptions bound to a calibration that a `worse` one superseded, each with both caps, `reeval: {flag, since, source: "calibration"}` and `regraded: false`, sorted by id; nothing bound to a `better`, unsuperseded or absent calibration.
- **R42** `calibrationRecord(pkg)` (`op=calibrate`): a request to re-grade (`regrade`, `apply_to_transcriptions`) is `CAL_CANNOT_REGRADE` (C-42.7); R38's refusals apply. It mints `CAL-<n>`, marks the engine's live calibration `replaced_by` with R39's verdict, registers or updates the engine's subject with this probe as its last, consumes the engine's pending signals, and answers the drift, the obligations raised (R41's, only when worse) and `regraded: 0`. It never writes a reading, a chain or a grade.
- **R43** `measured_by` is the control plane's stamp of the caller's principal, never the body's; a calibration with none is refused `CAL_UNATTRIBUTED`. *(not yet met: D-587 — a caller-supplied string; D-668 — refused `CAL_NO_PROBE`)*
- **R44** `calibrationDrift({engine, viewer})` (`op=calibrationdrift`) derives R41's obligations on read, never stores them, withholds rows in bundles the viewer may not see, reads at most 5,000 text-source rows with `truncated`, and answers `regraded: 0`. `calibrations({engine, limit})` (`op=calibrations`) lists calibrations newest first, bounded as R29, with the subjects and each one's next probe.
- **R45** `calibrationSignalRecord(pkg)` (`op=calibrationsignal`) records a signal R40 accepts and answers the subject's next probe, `changed_grades: 0`, `stood_in_for_probe: false`.
- **R46** `calibrationSubjectRegister(pkg)` (`op=calibrationsubject`) needs an engine and a probe id, refused by codes naming a subject's conditions; registering claims no fidelity. *(not yet met: D-668 — refused with the measurement codes `CAL_UNNAMED`, `CAL_NO_PROBE`)*
- **R47** For the scheduler: `calibrationDue(now)` counts subjects due; `calibrationWake(now)` is null with no enabled subject, otherwise the earliest next probe (a past one moved to now plus the scheduler's grace); `calibrationTick(now)` lists the due subjects and runs no probe and writes no calibration.

**The EXTRACT role's vocabulary (`extractrun.mjs`)** (the act that uses it is `ai-runs`')
- **R48** `EXTRACT_RUN_MODE` is `"extract"`; `EXTRACT_FUNCTIONS` holds exactly the functions something emits (`propose-reading`). `checkExtractFunction` refuses `NO_FUNCTION` and `UNKNOWN_FUNCTION`, `checkExtractVersion` `NO_FUNCTION_VERSION`.
- **R49** `proposedReadingGrade(entry)` is computed, never taken: a kind and key earn B, a label alone C, nothing earns null; the sentence is composed from the letter. `checkProposedRef` refuses `PROPOSAL_SHAPE`, `GRADE_OFFERED` (a caller-offered grade), `PROPOSAL_NO_REF`, `PROPOSAL_NAMES_NOTHING`, `PROPOSAL_ABOVE_CEILING` (stronger than B) and `PROPOSAL_POSITION` (a position present and unreadable).
- **R50** `proposalChain(captureChain, {fn, version, cap})` appends `ai(fn, version)` through `text-chain.appendStep` (its refusals returned unchanged), refusing `NO_CAPTURE_CHAIN` for a capture with no valid chain and `CAP_NOT_A_GRADE`; an absent cap is undetermined. `mintRatio({minted, cited})` answers `ratio: null` when nothing was minted, else cited over minted with the uncited count.

## Private

### Uses

- `jurisdictions`: `combine` (R18).
- `legacy-checks`: `BASIS_GRADES`, `CALIBRATION_CHECKS` (C-42) and `REEXTRACT_CHECKS` (C-51) until they move here (map §1), `canonicalExtent`, `describeExtent`, `sha256HexSync`, `requiredArgument`.
- `text-chain`: `appendStep`, `checkChain`, `derivationCap`, `layerChain`, `mergedChain`, `mergeTier2Text`, `tier2Note`, `glyphCount`, `describeChain`, `readingSource`, `readingSourceJson`, `readingOccurrenceKey`, `applyConfidenceFloor`, `checkAnchor`, `calibrationsOf`, `isTranscribed`, `terminalStep`, `chainKindFor`, `STEP_KINDS`.
- `format-registry`: `getFormat`, `readingDialect`.
- `docprofile`: `doctypeFor`, `readText`, `flattenText`.
- `pdf-worker`, `ocr-worker`: through their bindings only (R4–R6).
- `record-core`: `recordOf`, `transact`, `evidenceStore`, `declarePurge`, `readFile` (the provenance document a re-read reads its locator from), `getSetting`.
- `membership`: the viewer gate (R43–R44 there).
- `capture`: its acquire answer's document, the input of `read` (R1).
- `promotion`: `registerStep` (R20). Not in `modules.json`'s `uses` today (map §3).

### Invariants

- **R51** No machine mints a grade: nothing here raises or lowers one; a chain only weakens (`text-chain`); a calibration re-grades nothing in either direction; a proposal's grade is computed (DEC-4).
- **R52** A reading that finds nothing is a failed reading, recorded with its reason, never backfilled; absent, empty and null stay three facts (page count, container extent, units, provenance, dialect).
- **R53** A reference is carried as it appears, never resolved here.
- **R54** Each refusal carries its catalogue row: C-42.1–C-42.7, C-51.1–C-51.5.
- **R55** Every list read is bounded and says when it was cut (`limit`, `truncated`).
- **R56** Purge: the reading tables are declared keyed to their bundle (`readings`, `reading_refs`, `reading_ref_terms`, `reading_text_source`, `reading_history`, `capture_text`; `capture_text_fts` whole-store only); the calibration tables are declared exempt, being measurements of engines, not of the record. *(not yet met: K23 — `legacy-store` declares the reading tables, and nothing declares the calibration tables)*
- **R57** No place is named in this module's behaviour or outward text (R18).

### Satisfies

- `docs/architecture/BIO_Content_Framework_v0_10.md` Part I §2 (invariants), §7 (a failed reader, never an emptied document); Part II §14.2 (a derivation never raises a cap), §16 whole (the path, the tiers, reading provenance, the chain's four rules, the promote-time projection).
- `docs/development/EXTRACTION-BREADTH-DESIGN.md` §4 (the EXTRACT productions' vocabulary), §5.1 (the re-read, as built), §5.2 (the per-page rule and the order of two merges), §6.
- `docs/development/CONTENT-SEARCH-DESIGN.md` §3, §4.1 (one unit per element reference), §4.3 (the bounds, truncation stated).
- `docs/development/OBSERVATION-LOG-DESIGN.md` §4.2 (the re-read's observation, through R24).
- `docs/architecture/BIO_Assistant_and_AI_Roles_v0_1.md` §7.3.
- `docs/development/SCHEDULER.md`, "The eleventh consumer" (the calibration cadence and its cost).
- `docs/development/OFFICE-FORMATS.md`, the format axis as ruled; `docs/development/DOCUMENT-PROFILES.md`.
- `docs/architecture/BIO_Membership_Architecture_v2.md` §7 (item 7.9: a hidden bundle answers as an absent one).
- DEC-4 (no machine mints a grade), DEC-24, DEC-74, DEC-75 (the Drive conversion step).

### Suggestions

- **Two halves.** The pipeline (R1–R18, R31–R35) runs in the Worker with the fleet bindings; the store half (R19–R30, R36–R47) is `extractionOf(ctx)` (K61). The pipeline reaches the store half as `index.mjs` reaches the store today; its one store read (R6's live calibration) should become a named service rather than an `op=calibrations` fetch.
- **Listeners.** `content` registers its stale mark (REC-82), `observation-log` its extraction, reader-run and indexed rows, through R24, as provenance's `onReceipt` serves observation-log (provenance R47). Until they are extracted, `legacy-store` registers them.
- **Read contract.** Later modules join `readings`, `reading_refs`, `reading_text_source` and `capture_text` in SQL (connections, progressions, the frontier, search). State them as a read contract, as record-core R37 does for `bundles`, rather than wrap each join (map §3).
- `documentsByReference` is unbounded today; R55 asks for a bound, rowed when BOB confirms it.
- The closing table of Content Framework §16 still lists the re-read and the EXTRACT role as ABSENT; both are built (EXTRACTION-BREADTH §5.1, §4). BOB corrects the table.
- Tests: each C-42 and C-51 refusal gets a negative control; R3, R16 and R22 get over-strictness arms (a PDF and an office capture read byte for byte as before); R31 keeps the pinned digest of the plain read. Built work: D-616, D-635, D-665 on `land/worker/D-6xx`; D-684, D-685, D-724 stacked on `land/worker/D-724` over D-672 and D-415 (map §4); D-668 on `land/worker/D-668`; judged at the job.

## Open for Bob

(Its first two draft points were ruled by BOB: K72 (8) and K73.)

1. **A caller can author a reading.** At promote, the reading persisted is whatever `data/provenance.json` carries (Content Framework §16, "What it does not do" (1)), so a caller can file entities, a chain naming an engine and calibration, or a provenance it invented. Intake Doctrine's rule that a fact a caller can hand in is one a caller can invent (capture R35) does not reach readings today. *Recommendation:* extraction keeps each reading it composes (R23's history already holds it, keyed by capture), and the promote projection records a carried reading this instance did not compose as the caller's assertion, with provenance undetermined and no position, rather than refusing it. Refusing would fail every bundle promoted from an older acquire. This is R21.
