# T2 · docprofile — job record

Session: `session_01QLCYkFnXjtMweB8KWin4FT` (DOCPROFILE #1). BOB at start: `session_01JwTyEmUPSgvwiHEwzCzoxK` (BOB #41).

**Status** · WORKING, 2026-09-26. Job for module `docprofile`, tranche T2, branch `job/T2/docprofile` (merged `origin/tranche/T2` @ 79e6ccd829). Entries: T2-11 (tests for every live id), N3 (local vocabulary into the profile). Building against `jurisdictions`' Provides until its branch merges. One open question (Q1), carrying on on my best reading.

## Questions

### Q1 · 2026-09-26 · The vocabulary's shape: my R6 against jurisdictions R6

**The conflict.** My R6 says each content type takes its vocabulary from "`ctx.view`'s recogniser-vocabulary section, keyed by the type's own key", covering masthead/self-naming phrases, furniture, operative voice and caption words, code citations, report sections, reference-line shapes, directory floors, practice thresholds and publishing-system address shapes. The provider's contract, `jurisdictions` R6–R7, defines a CLOSED `vocabulary` set that is not keyed by content type: `furniture`, `bodies`, `member_titles`, `enactment_markers`, `codes`, `file_numbers`, `report_titles`, `report_sections`, `recommendation_openers`, `template_blanks`, plus `practice.minutes_due_days`, `spaces.enactment` (kinds and forms) and `systems`. It has no key for: minutes/agenda masthead words, the operative voice (ordain/resolve, WHEREAS), memorandum labels (TO/FROM/SUBJECT/DATE), the directory floors (5 addresses, 0.8 at one domain) and self-naming words, or the furniture-recurrence floor (3). Its `validate` refuses any other key (`UNKNOWN_VOCABULARY`), so I cannot read facts the profile may not hold.

**My best reading, which I am building now.** The provider's closed set is the interface. Each content type reads every LOCAL fact from the key of that set that holds it: furniture (office and jurisdiction names, record link labels), bodies, member titles, enactment kinds/forms/markers, codes, file numbers, report titles/sections/recommendation openers/template blanks, and `minutes_due_days` (absent: the "minutes not yet published" connection carries no `expected_by` and says why, never a default 21). What stays in code is place-free: English and parliamentary words any jurisdiction's clerks use (Minutes, Agenda, ordain/resolve, WHEREAS, Aye/Noe, TO/FROM/SUBJECT/DATE, Section/Chapter, directory/staff/roster/contacts), Legistar's own page and link shapes (a vendor's technology used by many jurisdictions, like the stack handlers), and the measured structural floors (masthead recurrence 3, directory floor 5 and 0.8), which are properties of documents, not of a place. So no place is named in code (R30) and no local fact is held there, but R6's wording ("keyed by the type's own key", and the non-local kinds it lists) is not met literally.

**Recommendation.** Reword my R6 and R30 to name `jurisdictions` R6–R7's keys and to exempt place-free language and structural floors, as above. The alternative, adding keys to `jurisdictions` R6, changes a provided service (Bob's) and gains nothing: those facts are the same in every jurisdiction.

**Also for BOB, same root (callers).** Once N3 lands, a caller that passes no `view` gets no local recognition (jurisdictions R16: undetermined). `legacy-index` (`bio-plane/src/index.mjs`: `doctypeFor` @3090, `assess` @3108, `readText` @7864 and @9771, `doctypeFor` @9112) passes none today, so the old battery's docprofile-dependent tests (for example `bio-plane/test/profile.test.mjs`, `reading.test.mjs`, `pipeline-e2e.test.mjs`) will read no file numbers, bodies or code citations. The fix is in `legacy-index`: pass `view: jurisdictions.combine(<the instance's active profiles>).view`. Not mine to make; flagged now so it can be routed.
