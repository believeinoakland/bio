# Rulings

**Status** · Every ruling, one line each (PROCESS-MECHANICS §9; P15): `K<n> · date · module or layer · the ruling · why`. A ruling is changed only by a new line naming its evidence and the one it replaces. Where a ruling has a fuller home, the line names it. Started by BOB #38, 2026-09-26, from the rulings made during the transition.

- K1 · 2026-09-25 · all · No jurisdiction in the product: local facts are data in jurisdiction profiles held by `jurisdictions`; outward text names CivicOS and the group · Bob's concern that Oakland references tell other groups CivicOS is not for them (`layers.md`, "No jurisdiction in the product")
- K2 · 2026-09-25 · all · No module size limit; size is a watched metric, reported near 4,000 lines · Bob (`layers.md`, rulings on the draft, 1)
- K3 · 2026-09-25 · all · Ops move with their construct at extraction; `control-plane` keeps routing, authentication and the response envelope · Bob (ibid., 2)
- K4 · 2026-09-25 · all · Each module owns its tables; `schema.mjs` is divided at extraction · Bob (ibid., 3)
- K5 · 2026-09-25 · legacy-ui · The UI is a placeholder worked on elsewhere; no tranche plans work on it · Bob (ibid., 4)
- K6 · 2026-09-25 · all · Every check in `bio-checks.mjs` moves into one module as an invariant with its own id and test; none is dropped without a ruling · Bob ("very important to keep")
- K7 · 2026-09-25 · layer 7 · Understanding (intent, reevaluation) is a layer; Discovery is a candidate beyond MVP · Bob (`layers.md`, layer 7)
- K8 · 2026-09-25 · requirements · The conventions of `build/requirements/README.md` govern every requirements file · Bob delegated their organisation to BOB (BOB #37)
- K9 · 2026-09-25 · signatures · The signing page's source and generator move into `signatures` (entry N7) · BOB #37, so the module's tests can check the page it serves
- K10 · 2026-09-25 · promotion · Check C-18.8 moves to `promotion` and verifies through `signatures`; the duplicate verifier retires (N8) · BOB #37
- K11 · 2026-09-26 · layer 9 · The Action layer (standards, conformance, consequences, actions, filings, escalation) sits between Publication and Operations · Bob (`layers.md`, layer 9)
- K12 · 2026-09-26 · consequences · Consequences are computed from the record where possible, otherwise undetermined and settled by members from the record and their own assessment; significance is the members' judgment · Bob (ibid.)
- K13 · 2026-09-26 · filings · Tier 3 gets a counsel packet for named counsel, never published or fileable as it stands · Bob; amends Design Requirement 8
- K14 · 2026-09-26 · escalation · Stage 7, political accountability; policy advocacy and candidate support stay out; Operational Principle 1 stands · Bob; amends Design Requirement 7
- K15 · 2026-09-26 · jurisdictions · The profile carries the action sections (R23–R30), built with the module (N11) · Bob: no reason to defer
- K16 · 2026-09-26 · process · P17: Bob decides policy and doctrine, requirements, architecture and UX; every lower-level decision is BOB's, recorded and reported · Bob (`civicos-process/PROCESS-DESIGN.md`, P17)
- K17 · 2026-09-26 · all · Helper modules `test-support` and `bundler` (layer 1) and legacy module `legacy-tests` (last) added; the uses the code already had declared; `skills` follows `ai-runs` · BOB #38 under P17, from the architecture check's first run (`layers.md`, "Helper modules")
- K18 · 2026-09-26 · checks · Owner of a file is the module with the most specific matching path; a requirement id is defined where written in bold `**Rn**`; a relative import naming no tracked file is counted, not judged · BOB #38 (`civicos-process/checks/README.md`)
- K19 · 2026-09-26 · process · The full regression runs only by hand, at a release or when Bob asks; nothing runs on a push to `main` · P11; Bob asked that the old push-to-main gate go
- K20 · 2026-09-26 · requirements · Requirements of a helper or legacy module are technical detail and BOB's under P17; requirements of a product module are Bob's · BOB #38's reading of P17
- K21 · 2026-09-26 · process · Outside a tranche, BOB commits the build state to `main` directly; while a tranche runs, `main` does not change and BOB writes on the tranche branch · P12 and mechanics §4
- K22 · 2026-09-26 · process · What goes into a tranche and when it opens are BOB's; Bob is told when one opens and asked for his meter reading, never asked to approve. Replaces the tranche-approval step of the earlier mechanics · Bob, correcting BOB #38's transcription of P17
- K23 · 2026-09-26 · record-core · `purge` clears only tables their owning modules declare to it (the legacy store declares the rest until extraction); instance settings, including the active jurisdiction profiles (N10), live in record-core, exempt from purge; `register` goes to provenance and `refs` to connections, as their code comments say; D-629 (a stack trace returned to callers) moves to control-plane, which owns the response envelope (K3) · BOB #38, reviewing the layer-2 draft (`build/extraction/record-core.md`)
- K24 · 2026-09-26 · process · Every session is named by its lane in capitals and its iteration: `BOB #38`; a module job's lane is its module (`PDF-READER #1`) · Bob (`civicos-process` mechanics §2)
- K25 · 2026-09-26 · process · BOB and a job speak by a one-time routine into the other's session at the next whole minute; BOB's address is the Status line of the tranche's `plan/current.md`; state drives and the message only accelerates · measured in the dry runs D1–D3 (`civicos-process` mechanics §13)
- K26 · 2026-09-26 · process · The revised mechanics are certified for use (P3) after the dry runs D1–D3; T1 carries four named checks (many jobs at once, a job out of context, a question for Bob, preparation beside running jobs) and one more (a tool fix reaching running jobs) · `civicos-process/dryrun/CERTIFICATION.md`

- K27 · 2026-09-26 · pdf-worker · R21 reads `/Rotate` as inherited (own, then nearest ancestor, else 0), a non-multiple of 90 answering `PAGE_UNREADABLE`; R17 and R18 count the images a page paints (`pdf-reader.pdfPageImages`), never those its `/Resources` lists · R21 had stated the D-671 defect as the requirement; R18 already said "painting", and counting shared resources refused single-scan pages (pdf-worker Q1, Q2). Wording, BOB's under P17
- K28 · 2026-09-26 · pdf-reader · N9: `openPdf` (R30), `PdfDoc.pageCount` (R19), `PdfDoc.pageDict` (R31) and `imagePlacementSource` (R32) are the named services; `.objects`, `._pageOrder`, `_stream` and `_ctm` are private · pdf-reader's proposal at e405d41; an interface detail, BOB's under P17
- K29 · 2026-09-26 · process · The product's generated artifacts (the plane's and three workers' bundles), with the command that regenerates each and the one check that verifies all, are listed in `build/manifest.md` · mechanics §14; handoff §6 step 2
- K30 · 2026-09-26 · pdf-reader · R26's figures are M-178's (image-only at most 4 glyphs and share at least 0.18; undetermined below 22 glyphs otherwise; none at 22 or more); R25 records trailing bytes as the note `flate_trailing_bytes:<n>` and marks undecodable or unresolvable content per page · measured on `land/worker/D-627` @ 056d3092 (M-178, 1,788 pages); PDF-READER #1's Q1, Q2; detail, BOB's under P17
- K31 · 2026-09-26 · record-core, promotion, membership · Later modules join a promotion by registering checks, projections and facts with `promotion` (R39–R40), all inside `record-core.transact`; until a module is extracted, `legacy-store` registers its share. `record-core` provides `transact`, `commit`, `bundleInfo` and `listBundles` and takes the caller's visibility predicate in `auditPass`. `forkProject` and project name uniqueness go to `promotion`; export goes to `publication` · the promotion and membership reviews: `promote` writes and checks later layers' tables inside one transaction, and record-core stated no write or bundle read service; function moves and interfaces are BOB's under P17 (layers.md ruling 5)
- K32 · 2026-09-26 · pdf-reader · R26 applies only to a page that shows text: a page carrying `no_text_layer` carries neither `image_content_unread` nor `image_content_undetermined` · the rule K30's rewording dropped: D-627 (BOB #35) is about a page whose only text is a folio, and a no-text page already routes to OCR; PDF-READER #1's Q3
- K33 · 2026-09-26 · signatures · N7: `signatures` owns `bio-plane/src/sign-release.html` (the signing page's source, moved from `tools/`) and `bio-plane/scripts/embed-signpage.mjs` (its generator, taken from `legacy-index`'s `bio-plane/scripts/`); BOB removes `tools/sign-release.html` at T2's layer close, since a job writes only its own paths · the job must own both files to test that the page served is the current render; file ownership is BOB's under P17
- K34 · 2026-09-26 · odf-reader, ooxml · R28 now states only "each part not read", R29 (D-346) says what is read (meta.xml as `core-properties`; each embedded manifest member, excluding the package's own parts, `Pictures/` and `svg:font-face-uri` fonts, as a `sha256` `intra` link, absent, encrypted or unreadable ones undetermined), R3 and R30 drop the unconditional markers; `IMAGE_MIME_BY_EXT` is named in ooxml R20 and odf-reader's Uses · R3, R28 and R30 contradicted R29 once met; ODF-READER #1's Q1; wording, BOB's under P17
- K35 · 2026-09-26 · id-spaces · N2 keeps `legacy-store`'s `op=idmatch` working through a temporary, marked adapter in `id-spaces` under the old names (R26), building its view from every non-test profile `jurisdictions` holds; it retires with N6 · no `legacy-store` job runs in T2, and `main` must stay correct when T2 merges (P1, P12); ID-SPACES #1's Q1
- K36 · 2026-09-26 · office-readers · R9 states D-415's met behaviour: `xlsxEntry.text()` carries `rangeUnits` (one `sheet-range` unit per defined name or table that is one rectangle on one sheet) and `rangeUnitsSkipped` (every other, with its why), over the size guard too · R9 stated only the gap; OFFICE-READERS #1's Q1; an interface detail, BOB's under P17
- K37 · 2026-09-26 · signatures · R30 (the served page is the current render), R31 (its script's signatures verify with `ssh-keygen` and `verifySshsig`) and R32 (its visible text names CivicOS, wire formats unchanged) are added; the Suggestion that these are checked elsewhere is dropped · N7 brings the page and its generator into the module (K33); R32 applies `layers.md` rule 4, already Bob's; SIGNATURES #1's Q1
- K38 · 2026-09-26 · text-chain · R77 carries the base page's `image_content_*` `undetermined` entries (and fields) unchanged, never re-graded; R81 is `chainKindFor` over each covering part's last derivation step, and R22 stamps `part` only when two parts share a page · pdf-reader R26 writes the markers as `undetermined` entries; R81's literal reading could not tell parts apart (D-723); TEXT-CHAIN #1's Q1, Q2; wording, BOB's under P17
- K39 · 2026-09-26 · docprofile · R6 and R30 name `jurisdictions`' own keys and keep place-free language, a system's own page shapes and measured floors in code; with no `ctx.view` (legacy-index today) the view is `jurisdictions.combine` of every non-test held profile, until N21 · jurisdictions' vocabulary set is closed, so R6's per-type keys did not exist; `main` must keep recognising local documents when T2 merges (P1); DOCPROFILE #1's Q1
- K40 · 2026-09-26 · ocr-worker · R19 checks at run time what a member can observe (a compiled core; the model's length) and pins the core's exact bytes before deploy through the bundle manifest's hash; the committed assets are `WASM_BYTES`/`MODEL_BYTES` long · a compiled `WebAssembly.Module` has no byte length; OCR-WORKER #1's Q1; wording, BOB's under P17
- K41 · 2026-09-26 · bundler · R8 lists static `import`/`export … from` specifiers only; dynamic `import()` is checked by `verifyFresh` from the metafile · a lexical scan cannot tell a string in dead code from a statement; BUNDLER #1's Q1; wording, BOB's under P17
- K42 · 2026-09-26 · test-support · R9 holds while the exit sweep renames the sandbox to `<SANDBOX>-swept` beside it: the same directory moved, never a second one · TEST-SUPPORT #1's reading, confirmed; a stricter reading would move where legacy-tests looks for leaks for no gain
- K43 · 2026-09-26 · pdf-worker · R25's JBIG2 scope is at least generic and generic-refinement, symbol-dictionary and text regions (MMR/Huffman and arithmetic, with `JBIG2Globals`); pattern and halftone where small; an undecoded segment type answers `UNSUPPORTED_FILTER` naming it · the held pages D-622 is for (e.g. Ordinance 13035 C.M.S.) are text regions over a global symbol dictionary, which R25 as worded would refuse; PDF-WORKER #2's Q1; the scope of an entry is BOB's under P17
- K44 · 2026-09-26 · jurisdictions · R25's tier and venue are optional (absent is undetermined, R27); R2 states the basis grammar; R12 the combine-list errors; R13 the view's `id` and `name`; R14 `bases` on merged facts; R29 how labels, citations, laws and extensions combine. The CPRA 14-day extension is held with basis `UNMEASURED` · R25 contradicted Bob's R30 (a tier only where Roadmap §8 names one); the rest is detail left unstated; JURISDICTIONS #1's Q1; wording, BOB's under P17
- K45 · 2026-09-26 · id-spaces · R26's adapter also exports `CMS_FLOOR` (the old battery imports it); R21's `referent.by` is exactly "the caller's reading" · ID-SPACES #1's REPORT; wording, BOB's under P17
- K46 · 2026-09-26 · signatures · `tools/sign-release.html` stays until N24 moves the two legacy tests that read it; replaces K33's removal at T2's close. The installer's bundle `newgroup/dist/newgroup.bundled.mjs` joins the generated artifacts in `build/manifest.md` · removing it now would break legacy-tests, which no T2 job owns (P1); SIGNATURES #1's REPORT
- K47 · 2026-09-26 · host-governor · The `host_governor` table is exempt from purge; a negative appetite (R3) and an unchecked stored appetite (R12) are fixed by the extraction job; an unreachable governor lets the fetch go ahead ungoverned, stated as a requirement; `governedFetch` and `op=governorstate`/`op=governorconfig` are the module's services, moved out of `index.mjs` by entry N25 (a job may remove code only from its one `from` legacy module) · the layer-3 draft's Q1–Q4; BOB's under P17
- K48 · 2026-09-26 · capture-sources · A D-570 render records a new `settled` fired class with `completeness: "settled_with_open_requests"`, grade unchanged, the rule off until N is measured; the render locale comes from the active profiles, `en-US` only as fallback; Memento stays here, not yet met and unscheduled; R36 (the CDX `urlkey`) is fixed in the first job; the unused `runtime-limits` edge is removed · the draft's Q1–Q5; the locale follows `layers.md`'s "No jurisdiction in the product"; BOB's under P17
- K49 · 2026-09-26 · provenance, capture, record-core · provenance uses `promotion` (`registerStep`, `promote`), owns `captured_locators` and the version chain, `testify`, D-580 and the C-18 register arms (18.1, .3, .4, .6, .9; 18.5 to monitoring; 18.7 with 18.8 in promotion), and offers `onReceipt` for observation-log; record-core provides the evidence bucket (added to its requirements before T3); capture's reading block moves to `extraction` with D-593, D-694 and D-724; capture keeps the event queue and the render allowance, the tasks inbox going to a later module; capture's continuation is fixed per R11 · the layer-3 drafts' questions; function moves and interfaces are BOB's under P17 (layers.md ruling 5)
- K50 · 2026-09-26 · text-chain · R78's `document` joins the non-empty pages' texts, as the code always has · TEXT-CHAIN #1's Q3; wording, BOB's under P17
- K51 · 2026-09-26 · office-readers · R9's anchor link is emitted for every defined name, multi-area ones included, as the code and its test do; the met markers of T2's entries are dropped at the layer close · OFFICE-READERS #1's note (4); wording, BOB's under P17
- K52 · 2026-09-26 · odf-reader, legacy-tests · R10's empty lists are the body-derived links and items only; R29's meta.xml and manifest output is still emitted over the guard. `formats-odf.test.mjs` stays red on its 13 D-346 assertions until N29 (legacy-tests is layer 11, no T2 job owns it), as N15 · ODF-READER #1's Q1 addendum and report; K19 runs the full regression only at a release
- K53 · 2026-09-26 · process · A tranche may include a job for a module of any higher layer; its layers still run one at a time, lowest first, skipping those with no entries (P10). `legacy-tests` (layer 11) therefore runs in T3 after layer 2, carrying every legacy-tests entry, first N32; until then the old battery is red on `main` where T2 made it so, the full regression running only at a release (K19) · DOCPROFILE #1's report: 39 harnesses break on T2's new `jurisdictions/`, and no legacy-tests entry could otherwise run before a layer-11 tranche; sequencing, BOB's under P17
- K54 · 2026-09-26 · pdf-worker · R20's success shape lists `jbig2` and `jpx` detail beside `ccitt` and `dct`; R24 states `/ImageMask` polarity: a mask sample 0 paints (PDF §8.9.6.2) · PDF-WORKER #2's reports 3 and 4, the flaw it fixed and tested; wording, BOB's under P17
- K55 · 2026-09-26 · process · At T2's close `fleetbundles.test.mjs` gives 95 pass, 1 fail: the fail is a legacy assertion pinning ocr-worker's input list (N33), while every verifying arm (input hashes, byte identity, externals) passes; the tranche closes on that, as T1 closed with N15 · the pinned list is legacy-tests', no T2 job owns it (K53)
- K56 · 2026-09-26 · record-core, membership, promotion · Bob approved the three modules' requirements; organisation-wide AI keys are minted by administrators only (membership R62); an owner's reason for removing a participant stays in the record, readable by every participant (R63) · Bob, 2026-09-26, answering the layer-2 page
- K57 · 2026-09-26 · membership, record-core, promotion, query-language · The membership map's open points (`build/extraction/membership.md` §3): R2's inactive-member refusal moves from the `login` dispatch arm into `login()`; record-core adds `listByType` (R36), states `bundles.bundle_id`/`object_type` as a read contract later SQL may join (R37, keeping D-497's SQL-bounded directory) and provides `evidenceStore()` (R38, K49); membership states the facts later code reads (R64–R69), `attestingKeys()` (R70; the pinned three-reader test changes with it), `projectCreated` for promotion's project creation (R71, so `promote()` stops writing membership's rows), and the `bootstrap` and `setpassword` ops (R72, R73); `inviteLook` returns `expertise` (R15) and the vestigial `members.expertise` column is dropped at extraction; `viewerPredicate` takes `GATE_MARK` with it, `query-language` re-exporting it and using `membership`; `PROJECT_NAMING_READS` stays with the dispatcher; the group-identity cluster (C-64) stays in `legacy-store` through T3, its owner decided with layer 3's maps; each extracted module keeps its own copy of the small `#rows`/`#one` helpers · the map's §3–§4, whose options are all BOB's under P17 (interfaces, function moves, uses edges)
- K58 · 2026-09-26 · capture, capture-requests · The AI capture requests (C-28; `captureRequest`, its drain, conduct, attribution, expiry; `capture_requests`; ops `capturerequest*`) become their own product module `capture-requests`, layer 6 after `ai-runs`, extracted from `legacy-store`; `capture` keeps a trusted in-process arm and `op=acquire` stops accepting `via: "capture-request"` from outside · Bob, 2026-09-26 ~18:05 UTC, in BOB #41's session (capture.md Open for Bob 1), relayed by BOB #41; its requirements are drafted before its layer's tranche (P18)
- K59 · 2026-09-26 · provenance · An instance signing key for its own receipts (R34): one key per instance, held as a secret, replaceable by the operator; receipts signed before a replacement stay verifiable against the old public key · Bob, ibid. (provenance.md Open for Bob); co-attestation at capture (capture R20) stays open: Bob asked for an explanation, given by BOB #41, and will rule
- K60 · 2026-09-26 · capture · Co-attestation at capture (R20): every capture requests both a co-archive of the locator and a trusted timestamp over the capture hash, wherever the source permits, as Intake Doctrine §3 requires; the co-archive is no longer opt-in · Bob, ~18:11 UTC, in BOB #41's session, relayed by BOB #41; all three layer-3 questions are now answered, the four layer-3 requirement sets await his approval
- K61 · 2026-09-26 · record-core, membership, promotion · How extracted modules reach each other: each exports a factory answering its one instance per Durable Object storage (record-core's `recordOf(ctx)`, R39), and reaches a module it uses through that module's factory on the same `ctx`; `legacy-store` calls the factories with `this.ctx` and delegates to them. A test may pass its own instance. record-core gains, for its users' needs found in T3: R39 the factory, R40 `seedMintLedger`, R41–R43 `head`, `manifestEntry`, `livePaths`, R44 `commit`'s row fields and stated time, R45 `auditPass`'s `context`, R46 `declarePurge`'s keyed-table form · RECORD-CORE #1's stated shape and Q1, MEMBERSHIP #1 Q1, PROMOTION #1 Q2; interface detail serving approved behaviour, BOB's under P17
- K62 · 2026-09-26 · promotion, record-core, membership, legacy-checks · (1) `promote()` and `reopen()` are wholly promotion's to move and rewire; record-core and membership provide `commit` and their services and do not edit those bodies. (2) R39's order and projection keys as now written. (3) `signatures` joins promotion's `uses` (K10); promotion meets R31 with its own check through `verifySshsig`, run by the gate; removing C-18.8 and the hand-written verifier from `bio-checks.mjs` waits for an entry against `legacy-checks` (N36), the double check being harmless meanwhile. (4) `forkProject` moves unchanged; the job writes its behaviour in its record, from which BOB states it as promotion R41. (5) N17: `CAS_STALE` carries its row; `EXISTS` and `ABSENT` stay not yet met until `legacy-checks` gains rows (N36) · PROMOTION #1 Q1–Q6
- K63 · 2026-09-26 · membership, query-language · membership owns `viewerPredicate` and `GATE_MARK` (R-text per K57); `query.mjs` keeps its copy until a `query-language` entry makes it a re-export of membership's (N37); the old battery is untouched and reaches membership through `legacy-store`'s delegating methods · MEMBERSHIP #1 Q1
- K64 · 2026-09-26 · promotion, legacy-checks, process · The checks behind promotion's R30 (C-20.1, C-17.2), R31 (C-18.8) and R32 (C-4.2) sit in the check catalogue (`legacy-checks`), not the store: promotion is extracted from both (`from` a list, mechanics §12.2 as revised and certified by D5), removes those checks from `bio-checks.mjs` adding nothing there, runs them in the gate after `checkBundle`, and `legacy-store`'s audit wrapper runs them in the audit by an import from promotion; this replaces K62 (3)'s deferral of C-18.8. R18 is met against the catalogue as it stands; its D-695 and D-717 arms come with `actions` · PROMOTION #1 Q7; the deferral would have left four live ids untestable
- K65 · 2026-09-26 · record-core, promotion · R16 in the D-700 form: the manifest document keeps its entries sorted by key (C-12.1 holds), each entry carrying `seq`, its write-order rank; write order is read from `seq` (promotion R30 walks it). Wording of R16, not its meaning (write order is never lost) · RECORD-CORE #1 Q2; the literal reading would have broken C-12.1 (cite.test.mjs, 3 failures)
- K66 · 2026-09-26 · promotion, process · `promotion`'s `from` is `["legacy-checks", "legacy-store"]` (K64), the revision certified by D5 (civicos-process `2c99230`); an extraction's net removal is judged over all its legacy modules together (D5's finding, `383f070`). Every running job pulls civicos-process `main` before its next check run · BOB #42
- K67 · 2026-09-26 · host-governor, capture-sources, provenance, capture · Bob approved the four layer-3 requirement sets, with his rulings K58–K60 folded in · Bob, in BOB #42's session, ~18:50 UTC
- K68 · 2026-09-26 · promotion, membership · `forkProject` stated as promotion R41–R44 from PROMOTION #1's description of the moved code (Q5, K62 (4)); membership provides `participation(projectId, memberId)` (R74) for it (PROMOTION #1 Q8). MEMBERSHIP #1's decisions in its record (the `membershipOf(ctx, {record})` shape, R10, R11, R19, R29/R62's check ids, R40, R71, the owner-vote rows) are accepted as readings of its requirements · wording and interface detail, BOB's under P17
- K69 · 2026-09-26 · instance-setup, promotion, record-core · The group-identity cluster (C-64, `INSTANCE_GROUP_CHECKS`: `groupIdentity`, `#stampGroup`, `#checkGroupDomain`, the tables `instance_group`, `group_identity_history`, `group_domain_checks`, their ops) goes to `instance-setup` (layer 11), which may use `host-governor` and `scheduler`, both earlier; no new module. `promotion` reads the producing group (R13) as a fact registered with it (R40), supplied by `legacy-store` until `instance-setup`'s extraction. Its requirements are written into `instance-setup`'s before that extraction (N38) · K57 left it open for layer 3's maps; record-core may not use host-governor or scheduler, and membership's requirements do not claim it
- K70 · 2026-09-26 · pdf-worker, image-codecs, pdf-pixels, ocr-worker, content · N34's split, as `build/extraction/pdf-worker-split.md` recommends: `image-codecs` (the pure decoders, the CCITT decoder moved out of `pagepixels.mjs` into its own file), `pdf-pixels` (page pixels, filter chains, crop, their refusal vocabularies) and `pdf-worker` (the tier-2 Worker and its bundle), in that order after `docprofile`; files stay in `pdf-worker/`, owned file by file; `ocr-worker` and `content` use `pdf-pixels`; `make-pdf.mjs` goes to `test-support`; each requirement id is assigned as the proposal's table says, with the rewordings it lists; which `REFUSALS` reason the JPX memory refusal maps to is the job's. BOB applies it to `modules.json` and the requirement files when T4 opens · P6 (4,075 lines past the 4,000 mark). Read as BOB's under P17: it adds and removes no product capability, only redistributes one module's among three
- K71 · 2026-09-26 · capture-requests, scheduler, observation-log, ai-runs · capture-requests' draft requirements (R1–R36) and map committed for review; `host-governor` joins its `uses` and `capture-requests` joins `scheduler`'s. `observation-log` and `ai-runs`, earlier in the order, read `capture_requests` directly today: each offers a registration this module fills (the K31 pattern), at their extractions (N39). What "configured" tests once the drain calls `capture` in process, how purge clears `lead_inquiry` without the row (record-core R46 cannot say it), and which of the 31 old-battery files follow the module are its extraction job's, through BOB · BOB #42 under P17; its three questions of meaning go to Bob
- K72 · 2026-09-26 · host-governor, provenance, capture, capture-sources, record-core, legacy-tests · The layer-3 maps' open points: (1) `from`: host-governor `legacy-store`+`legacy-index`; provenance `legacy-checks`+`legacy-store`+`legacy-index`; capture `legacy-store`+`legacy-index` (its refusal families stay in `legacy-checks`, read as today). (2) `governedFetch` takes the governor as `{admit, report}` (R15): in process in the Durable Object, a thin adapter over the Durable Object from a Worker. (3) `host_governor`'s DDL moves out of `schema.mjs` (as `RECORD_SCHEMA` did); the seven old tests that read that text are `legacy-tests`' in T4. (4) C-18's synchronous arms register with `promotion.registerStep` (R42–R46); C-18.6's asynchronous byte hashing stays in the catalogue, run by the gate before the transaction, as today. (5) Provenance's refusal families stay in `legacy-checks` (its Uses); the Suggestion to move them is withdrawn; `TESTIMONY_CHECKS` is not split. (6) `register` and `captured_locators` are a stated read contract (provenance R48); the 48 outside reads stay as SQL joins, every write provenance's. (7) Testify's producing group is read as promotion's registered fact (K69). (8) acquire's reading block (index.mjs 9225–9928) leaves `acquire`: the `op=acquire` handler in `legacy-index` calls `capture.acquire` and then reads, until `extraction` takes the block (K49); R42 is met in T4. (9) capture never calls `scheduler`: listeners registered with it (R44), `legacy-store` registering the scheduler's arming until then; `monitoring` reads R8's figures through R43. (10) Co-attestation (R20) goes through `provenance.attest`; `signatures` stays out of capture's uses. (11) `acquire` is a service inside the Durable Object (K61); the Worker's op forwards to it. (12) The archive's 24-a-minute appetite is a figure `capture-sources` offers and `capture` applies through `host-governor`; `digestCensus` and `snapKeyCensus` read record-core's own tables and go to `record-core` (N40) · the four maps (drafting worker, reviewed by BOB #42); interface and ownership detail, BOB's under P17
- K73 · 2026-09-26 · extraction, calibration, content, ai-runs, reevaluation, inquiry, basis-versions, entities · Layer 4's drafts: (1) `attestText`, `attestationsFor`, `text_attestations` and `contentContextFor` are content's; extraction provides `readingOf` (R30) and `unitsOf` (R36) to it. (2) `extractPropose` and `proposed_readings` go to `ai-runs`, which gains a use of `extraction`; extraction uses `promotion` for its promote projection. (3) `from`: extraction `legacy-checks`+`legacy-store`+`legacy-index` (C-42, C-51 move with it, as K64); content `legacy-store`+`legacy-checks` (the extent grammar, C-45, C-52, C-80.3). (4) content's uses gain `text-chain` and drop `id-spaces` and `capture` (nothing calls them); `pdf-worker` becomes `pdf-pixels` when K70 applies. (5) `op=versionnotice`'s question arm (it reads `inquiry_basis`) with C-80.1–C-80.2 goes to `reevaluation`; the leg code with C-45.7–C-45.10 and C-50 to `inquiry` and `basis-versions`; `contentAxis` to `retrieval`; `idMatch` to `entities` (N6). (6) Where reading happens is K72 (8): `op=acquire` calls `capture.acquire`, then `extraction.read`, answering both as today. (7) Calibration becomes its own layer-4 module before `extraction` (about 1,140 lines: `calibration.mjs`, its store region, C-42, three tables; extraction's R38–R40, R42–R43, R45–R47), the drift obligations staying in extraction; read as BOB's under P17 as K70 was (a split for P6, no capability added or removed); its requirements file is split from extraction's before Bob approves either (N41) · the drafting workers for extraction and content, reviewed by BOB #42
