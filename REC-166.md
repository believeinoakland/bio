# REC-166 — the worker's stand-down report, verbatim in substance (CONDUCT #13, 2026-09-22 ~16:53Z)

Stood down at Bob's order, relayed by BOB #27 at 16:49Z. REC-166 is NOT BUILT: no source or test file was edited and no gate
or battery ran. The working tree was clean, so nothing new was committed or pushed.

**Branch:** `worktree-agent-a1707ddf948cd5c29` @ `cd046a8ec092429731276c05e5ee537455707ed7` (as `git ls-remote` printed it).
It is ONE commit on `origin/main` `3bbcb6e1`: the claim block at the end of `CLAIMS.md`, left UNRELEASED.

## Done
- The row read `running` on `origin/main` `3bbcb6e1`, and the branch starts there. `npm ci` exited 0 in all three
  directories, each `node_modules` a real directory. Disk free went from 7.2 GiB to 4.9 GiB (on the Mac).
- Read in full: `CLAUDE.md`, `WORKER.md`, `RECORD.md`, INVESTIGATIVE-SESSION §7's ruling, §7.1 and the front matter.
- **IC-175 is minted** (`node tools/mintid.mjs IC`, in the Mac's `.git/bio-idalloc`, which does NOT travel to a fresh
  clone) but not yet written into `INTERFACE-CHANGES.md`. REUSE IC-175 for this item.
- Baselines on the untouched tree, each run alone, exit 0: `case-edition-conclusion` 36/0, `current` 63/0,
  `versionstate` 89/0.

## Measured at the code (line numbers as of `3bbcb6e1`)
- **The site.** In `#moveVersionState` (store.mjs ~33884–34281), make-current writes the inquiry after the preview
  return: `last_updated`, then a Session Log entry `| Version current |` reading "reading '<v>' is what <P> stands on",
  plus a `Reason:` line if a reason was given. It promotes the inquiry at ~34260 and only then calls
  `#setProjectCurrentVersion` (~34277).
- **What the project already gets.** `#setProjectCurrentVersion` (~34285) already writes the project's pointer, its
  `last_updated` and its own entry: `| Stands on |`, `Trigger: op=versioncurrent on <inquiry>`, "this project now stands
  on reading '<v>' of <inquiry>". It writes no reason.
- Every make-current names a project; there is no no-project path (refused `VERSION_CURRENT_NO_PROJECT`).

## Next act: the planned edit
- **Store.** Right after the preview return, add
  `if (act === "current") { const p = this.#setProjectCurrentVersion(projectRow, target, vname, who, when, why); if (!p.ok) return { ...p, act, target, version: vname, project: projectId }; return receipt; }`.
- Then drop the "is what … stands on" branch from the inquiry's `Changes:` line, and drop the second write at the end of
  the function.
- `#setProjectCurrentVersion` takes `why` and appends a `Reason:` line after its `Changes:` line.
- **Do not touch three lines** in `#setProjectCurrentVersion`: the `#setScalar(text, "last_updated", …)` line, the
  `#appendSessionLog(text,` line and the `### Session ${when} | Stands on | ${who}\n` line. `current.control.mjs` arm 1
  anchors on them, and `m025-arm-anchor-witness.test.mjs` checks that anchor.
- **The trap in testing the receipt.** The project already carried a "stands on" line before this change, so asserting
  that line alone passes on the untouched plane AND for the liar. The receipt arm must make the project current WITH A
  REASON, then assert the `Reason:` line inside the project's `| Stands on |` entry — matching on facts, not wording.

## Suites the fix will make wrong (correct each with a dated reason, never exempt)
- `case-edition-conclusion.test.mjs` section 9 (~523–542) asserts the old route, where the make-current moved the
  finding's bytes. After the fix those bytes stay at the pin and the publish takes the conclusion-comparison route:
  edition 2, the answer carrying `edition_warranted`. Its header and comments (~16–30, 66–68, 345–349) state the old
  write as present fact.
- `case-edition-conclusion.control.mjs`: the `S9_ROUTE` label fragment (line 122) must follow the new label, or the
  driver refuses to run any arm (exit 4). Arm (a) moves S9 from must-not-fail to must-fail; re-run the driver and record
  the result at its foot.
- `current.test.mjs` block 3's header claims the inquiry's bytes don't move, but nothing asserts it; true after the fix
  (adding the assertion is optional).
- No suite or UI reads the inquiry's "stands on" line. The UI's make-current sends no reason and re-reads
  `op=basisversions&project=` afterwards.

## The planned new suite: `current-shared-question.test.mjs` and its `.control.mjs`
- **Fixture:** from `case-edition-conclusion.test.mjs` (one admin member with an ssh-keygen key). Projects A and B both
  cite question Q, which has two accepted readings. A stands on the first, concludes, publishes and ratifies case X.
  Then B, and separately A, make the second reading current with a reason.
- **Assertions:** Q's `bundle_sha` still equals the pin (the row's control arm); Q's `last_updated` and `bundle.md`
  byte-identical; `op=caseflags` for Q and for case X names nothing; the case's fences hold (`op=versionreject&preview=1`
  refused `PUBLISHED_CANNOT_MOVE_VERSION`; `op=affordances` offers neither `inquirydivide` nor `inquiryground`; A
  publishing again unchanged refused `ALREADY_A_CASE_MEMBER`); B's `Stands on` entry carries Q, the reading and the
  reason, and B's pointer reads the new reading; on an UNPUBLISHED question `op=versionaccept` and each sibling still
  move that question's `bundle_sha`.
- **Control arms:** (a) restore the inquiry promotion — the `bundle_sha` arm fails by name; (b) the liar, no `Reason:`
  in the project — the receipt arm fails; (c) the refused fix (b), inquiry promoted but the flag exempted — `caseflags`
  passes, the pin and fence arms fail; (d) over-strictness, the project's wording changed — the suite stays green.
- **Housekeeping:** the pen in `bio-plane/.nc-rec166/` with its own `.gitignore` line. The new suite moves `hygiene` and
  `REGISTER_FLOOR` (take the figures from `coverage.mjs`'s own print).

## Still owed after the code
`npm run build`; the design document's front matter (the §7 bullet and gap (d)) plus a dated "Built" paragraph under §7;
`construct-status.json` `8.claim` (text, a `hit` probe, a `file` probe; then `status.mjs --write` and `--check`); IC-175
written with measured consumers; a `MEASUREMENTS.md` entry with a minted id; the committed-tree gate, `plancheck`,
`mintid --audit`, and the claim's `released:` line.

## Class sweep: begun, not finished
- store.mjs has 23 `this.promote(` sites. Besides this one, the project-scoped acts that promote a bundle other than the
  project: `conclude` (~5516: verify it is the no-project path only); `publishCase` (~7939: stamps case fields into each
  member finding's bytes — the known remainder recorded in IC-66); `forkProject` (unchecked); the generic `op=promote`.
- **A LEAD, NOT MEASURED:** when a second project publishes a finding that another project's case already pins, the
  finding's bytes move — that would unpin the first project's case and raise a revision flag on it, the same class. Drive
  it through the ops before anything is routed.
- Not yet seen by the sweep: direct SQL writes to `bundles`/`files`, `index.mjs`, `agent-worker`.

## Could not do
The build, the REC-157 suite's correction, any control arm, the gate, the docs, filing IC-175, releasing the claim. No
floors moved, no DELEGATION raised, no DESIGN GAP, no decision for Bob.

## For the cloud resumer
1. Fetch, re-read the REC-166 row, and fast-forward the branch to `origin/main` if it is behind.
2. `npm ci` in the three directories.
3. Append your own session line to the claim block (its machine details are Sparky-Air's).
4. Make the store edit above.
The worker's sweep script and run logs were in a scratch folder outside the repository and did not survive.
