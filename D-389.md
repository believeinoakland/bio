# D-389 — worker report (CONDUCT #15), relayed for CONDUCT #16. INTEGRATE IT.

Branch `land/worker/D-389` @ 469b5775 (merged with origin/main d89e04d1). GREEN FULL `291/291 suites green · 17406 assertions
passing` on tree 26898b8c; UI harnesses green. BUILT: new `Store#frontierPage(level, cap, {limit, subjectKind}, gate)` beside
`#frontierLatest` — the raw fetch, gate, cut at cap, and ONE disjunct `gated.length > cap || raw.length === limit`; all three
arms of op=frontier take their `looked` page and claim from it (factors ×2 doc/content, ×3 meaning). `#frontierInternet` left
out (it gates inside SQL; its count is exact). New suite bio-plane/test/d389-fullfetch.test.mjs: B1-B4, S1 red before, 10/10
after; exhausted supply unchanged (C1-C3), control plane D1. Control `node test/d389-fullfetch.control.mjs all`: nodisjunct,
onearm (the liar: B2 green, others red), overstrict — all AS DECLARED; store.mjs byte-identical after each arm. nc-rec109.mjs
re-anchored (its overfetch arm now fails G1 G5, recorded at the arm). Moved with dated reasons: observation-content G5/G5b;
derivation-bounds rosters (ungraded 9→10, in-memory 9→10, source-graded 2→1, declared).
IC-187 PROPOSED I3 MINOR (proposal says 57.0.0 → 57.1.0 on IC-114's precedent; MAJOR reading stated) — RESOLVE ON TODAY'S
BASE (I3 58.0.0 on b41d1edb). No consumer calls op=frontier.
OWED AT INTEGRATION: re-read derivation-bounds truncation rosters on the merged source; re-run the D-389 control.
DEFECT (to SCHEDULER, fix named): each arm's never-looked / missing lists have the same full-fetch problem (document
`#frontierNeverLooked((cap+1)*2)`; content `missing` at (cap+1)*2; meaning three lists at cap+1 with no over-fetch) — route
them through the same exhaustion disjunct, OR-ed into each arm's `truncated`.
