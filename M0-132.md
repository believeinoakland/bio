# M0-132 — worker report (CONDUCT #15), relayed for CONDUCT #16. INTEGRATE IT.

Branch `land/worker/M0-132` @ 082a1e75 (merged with origin/main e62e08e1). Measurement M-115. Gate on the pushed tree
e451a5e7: `130/130 suites green · 9164 assertions passing`, GREEN class FULLREUSE (211 units reused; every changed unit re-ran;
NOT a backstop). Pre-merge FULL on 8377388a: `291/291 suites green · 17445 assertions passing`. civicos-ui green.
WIRE SUITE: `ai-session-fixture.mjs` gains WIRE_RANGES (FETCH_CONSUMED 1007–1499, FETCH_ALLOWED 3001–3999, SUBS_CONSUMED
4003–4499, OTHER_CONSUMED 6001–6999, OTHER_ALLOWED 7001–8999, SUBS_ALLOWED 9001–9973, RUNTIME 2–5) — the brief's example
overlapped (OTHER_ALLOWED = FETCH_ALLOWED + 1000..2000 hits SUBS_CONSUMED). `wireRangeProof()` checks every derived interval
against every published band (incl. ticks, raw-substring arms W7b/W7c, the clock's year band). D-286's module REUSED
(`drawFrom` / `overlapFaults`); ai-session-context still 80/80. New ARM D0c; D0b, W7b, W7c unchanged. Old draw: 1 in 912 red
over 2,000,000 draws; new: 0 in 2,000,000. 500 consecutive whole-suite runs (fresh node, real plane): 500/500 at 86/0.
SNAP KEYS: one sweep over 816 files; Math.random snap keys 19 → 0, per-suite `snapKeySeq` counters with dated comments in 18
suites (action-loop, aicredential ×2, capturerequests, caseobject, cite-scale, cite, dec65-single-part, edges, fence-e2e,
grounds, independence, inquiryground, machine-fences, shadowed-refusals, skillprohibitions, suggest, versions, versionstate)
plus selection:80 (a bare Date.now()). Cannot see helper-built keys or non-Math.random/Date.now randomness.
CONTROLS (10a–d): forced collision → D0b + D1 by name; old FETCH range → D0c(i) 4 faults; liar constant fixture → D0c(iii);
over-strictness 86/86 — AS DECLARED.
DEFECT (RECORD-INTEGRITY, to SCHEDULER/BOB): `promote` writes INSERT OR REPLACE, so a repeated (bundle_id, snap_key) SILENTLY
OVERWRITES a version's manifest rows (the old collisions were silent, not red — frozen counters kept all 18 suites green).
FIX: promote refuses a snap key already present for that bundle unless it is an explicit replay (RECORD designs it).
DESIGN GAP: D0c cannot tell whether the suite's own DRAW was pinned while drawWire stays random.
