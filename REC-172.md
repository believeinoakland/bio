# REC-172 — worker report (CONDUCT #15), relayed for CONDUCT #16. INTEGRATE IT (authority-adjacent: re-run BOTH controls).

Branch `land/worker/REC-172` @ f24751d1 (base origin/main a13667ee). GREEN FULL `292/292 suites green · 17484 assertions
passing` on tree 097c522e; coverage --strict, civicos-ui, DEC-49 guard green.
BUILT (airun.mjs, store.mjs, bio-checks.mjs): all checks in `checkConsume` (new modes `map` for tick consume, `list` for
open bounds). Tick: an array/non-object consume and an unknown key (incl. __proto__, constructor) are REFUSED (were skipped
with ticked:true). Open: non-list bounds / non-object or unknown-bound entry refused (C-22.15; before, `fetchs: 3` gave a run
with NO fetch ceiling); `allowed` gets consumed's shape check. `lease` refused as a consumable at both ops for any figure
(new PLANE_DECIDED_BOUNDS, apart from PLANE_COUNTED_BOUNDS so REC-169's census holds). vf4-live-scratch sends {fetches:1}
and reads the result's `ticked`. Codes: NEW AI_RUN_BOUND_UNKNOWN C-22.15; C-22.13 and C-22.14 translations widened.
rec172-bounds.test 7/29 on 91913d6b → 36/0; controls 8 arms AS DECLARED (restore-continue 29/7; liar non-map ignored 29/7;
lease-spendable 31/5; allowance-coerced 29/7; bounds-map-ignored 35/1; vf4-array 35/1; overstrict 36/0); REC-169's control
re-run on the same sources: 9 arms AS DECLARED. airun.test ARM D1 corrected 14 → 15 C-22 numbers (dated).
IC-188 PROPOSED I3 MAJOR (proposal says 57.0.0 → 58.0.0) — RESOLVE ON TODAY'S BASE (I3 is 58.0.0 on main already → 59.0.0 or
later). OWED AT INTEGRATION: resolve IC-188; re-run REC-172's AND REC-169's controls on the merged tree; REGISTER_FLOOR re-read
(worker's merged print: 1774/283/284/245); DEC-49 keys re-read (worker moved: rows 301, census 611, reach 354, codesChecked
338, refusalsJudged 341 — UI-81 moves some of the same keys: take the merged print).
DESIGN GAP (to BOB): an absent `allowed` opens a bound at 0, which `finishedBound` reads as NO ceiling; whether a declared
bound may omit its allowance is BOB's ruling (fix if so: refuse an absent `allowed` in checkConsume's list branch).
Leftover: four gate logs at .claude/worktrees/rec172-gate{,2,3,4}.log (outside the repo tree; the worker's rm was denied).
