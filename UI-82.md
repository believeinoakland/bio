# UI-82 — worker report (CONDUCT #15), relayed for CONDUCT #16. INTEGRATE IT.

Branch `land/worker/UI-82` @ 4b1775ab (base 91913d6b, d89e04d1 merged; `git merge-tree` clean against b41d1edb). GREEN FULL
`290/290 suites green · 17393 assertions passing` on tree da3663c0; SKIP 0; civicos-ui all green.
BUILT (civicos-ui/app.html): new `pubCasePairOf` + `pubCasePairNoteHtml` above `pubList`; only pubList's member-block map
changed; `pubOpen` (UI-81's) untouched. Each case row reads the `strengthByCase` entry matching its case_id and edition and
draws that case's pair naming case and edition; no whole pair → UNDETERMINED with its reason; never the no-pair sentence,
never another case's pair. Single-pair and no-pair rows render as before. No plane code spelled in app.html (DEC-49 unchanged,
353 in reach). New suite civicos-ui/test/published-index-pair.test.mjs (REC-170's fixture, real plane, real SSHSIG): 12/8 on
91913d6b's app.html → 20/0. Control published-index-pair.control.mjs: (A) scalar again 12/8; (B) liar, first case's pair
under every case 16/4 at "NEVER ONE CASE'S PAIR UNDER ANOTHER"; (C) over-strictness 20/0 — all AS DECLARED.
Docs: CIVICOS_UI_STATE `v101` (UI-81 holds v100 — keep unless UI-81 lands without it); BIO_Publication front matter + rule
12 "As built"; construct-status 13.member-untouched (+2 probes); §3 re-rendered.
OWED AT INTEGRATION: `node tools/status.mjs --check` on the merged tree (main changed both status files after its merge).
No IC (reads IC-183). No floors moved. No plane files.
GAP (plane's, already stated in IC-183/REC-170): a legacy `/1` case document is left out of the comparison, so a finding
pinned by one /1 and one /2 case with different pairs is served one scalar pair; fix in `#frozenPairsByCase` (read a /1
member's pair from its own bytes). Unreachable today (no /1 can be minted); count of legacy documents unknown.
ENV: the scratchpad is shared across sessions; a peer's text overwrote its scratch file — it moved to its own mktemp dir.
