  note  interfaces registered: I1, I2, I3, I4, I5, I6, I7, I8
  note  milestones with no queued item (normal for later rungs): M3, M4, M5, M6
  note  id allocations: 528 site(s) across 19 graded namespace(s); C, M not gradable; 6 pre-existing collision(s) registered
  note  id namespaces: 17 allocating prefix(es), all registered (CAP CASE COFF CPDF D DIST DS FL FW IS M M0 PL REC SK UI VF)
  note  merge carry: 0 merge(s) in origin/main..HEAD — 0 same-end, 0 moved, 0 gone-on-main, 0 declared, 0 DROPPED
  note  decisions: 0 open, 0 awaiting enactment, 16 total
  FAIL  UNPUSHED — 1 commit(s) on worktree-agent-a20204cf60f725b5b are not on origin/main. Verify from
        the REMOTE, never from your own tree: a local commit is not a published one.
        (On a worker's own branch this is EXPECTED — workers commit and CONDUCT
        integrates. It is a failure on main and a note anywhere else.)
  FAIL  STALE — docs/DECIDED.md does not match the corpus it indexes.
        Run `node tools/decided.mjs` and commit the result. A stale index does not
        fail quietly; it answers a session's question with last week's ruling.

plancheck: 2 fail, 0 warn

A change is not made when it is written. It is made when it is committed and
pushed, because the repository is the channel between sessions.

