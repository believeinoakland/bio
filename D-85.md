# D-85 — worker report (CONDUCT #14, wave 7), relayed for CONDUCT #15. INTEGRATE IT (AUTHORITY: tell DIST).

Branch `worktree-agent-a09e49cc337fc2200` @ c5032e51 (merged origin/main 619dfa65; 0 markers). GREEN FULL on tree 94836c52:
`284/284 suites green · 17154 assertions passing`; coverage --strict and civicos-ui green; mintid --audit 0 breaks.
RULE 2: index.mjs stamps an `ai` credential's op=promote with `assistantPrincipal` (deleted first for every caller);
store.mjs `#surfacingGate` on an inquiry creation carrying it: sight SURFACE_NO_RUN (C-66.1, also no-run) → position
AI_RUN_NOT_PRINCIPAL (C-22.12) → status SURFACE_RUN_NOT_RUNNING (C-66.2) → bound SURFACE_NO_BOUND / SURFACE_BOUND_REACHED
(C-66.3/.4; `surfaces` is a new RUN_BOUNDS row). A landing writes `inquiry_run_surfacings` (purged, counted by op=stats) and
spends one `surfaces` in the same transaction; op=projection gains `surfaced_in` (run + lens, "not recorded" when none).
A member's own creation is untouched. RULE 3: aiRunOpen stores `lens_at_open`; bias block gains at_open, hand
(in_force|stale), moved_basis; `moved` compares open vs now; unreadable reads `unreadable`.
Suite d85-surface-run.test.mjs 47/0 (8/39 on f05c1efd's sources); control 16 arms AS DECLARED; the row's (drop the run
check) 27/20 failing O1 by name; both liars caught. 4 suites corrected with dated comments. C-66 and IC-181 minted:
IC-181 PROPOSED I3 MAJOR 54.0.0 → 55.0.0, I5 ADDITIVE 1.25.0 → 1.26.0 — resolve against the bases read at integration.
AT INTEGRATION: re-read REGISTER_FLOOR (printed arms 1722, classified 275, corpus 276) and the DEC-49 guard's regionLines
(region `#surfacingGate > is-surface-run`, 32 lines) on the merged tree. construct 11.run-production → BUILT.
FINDINGS: (SCHEDULER) op=airuntick's `consume` accepts a NEGATIVE delta — a principal can refund spent mints/surfaces (read,
not driven); fix: refuse negative deltas and consumption of plane-counted bounds. (BOB, design gap) rule 2 names the `ai`
credential; admin/member/probe machine tokens still create questions stamped surfaced_by: agent outside any run — does rule
2 reach them? (UI) bound-sweep.test.mjs's method-head regex misses `async` methods; fix: admit `async\s+` in methodBodies.
No MEASUREMENTS entry written.
