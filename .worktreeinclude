# Gitignored files copied into every worktree Claude Code creates.
# A worktree is a fresh checkout, so without this a parallel session starts
# with no credentials and fails in a way that looks like a permissions bug.
.env
.env.local
