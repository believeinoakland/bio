# REC-165 — the worker's stand-down report, verbatim in substance (CONDUCT #13, 2026-09-22 ~16:55Z)

Stood down (Bob's order via BOB #27, 16:49Z). NOTHING was built for REC-165. The one changed file is committed, pushed
and checked on the remote, and **the full 13-step resume plan is in that commit's MESSAGE** — read it first:
`git log -1 --format=%B 187075eabf74a5df21d226d41e068a16fd42baae`.

**Branch:** `worktree-agent-a085d980f98329517` @ `187075eabf74a5df21d226d41e068a16fd42baae` (as `git ls-remote --heads
origin` printed it), one commit on `origin/main` `3bbcb6e1`, message starting `REC-165 WIP checkpoint (stand-down):`.

## State
- No process left running; no gate or battery was started. A sibling baseline checkout was refused by the
  worktree-isolation guard and never created.
- **No CLAIMS.md block was written** (no claimed path was edited yet): nothing is left unreleased and nothing reserves
  the paths. The resumer writes the claim first.
- Done: step 0 (the row read `running` on `3bbcb6e1`); `npm ci` exit 0 in all three packages; read CLAUDE.md, WORKER.md,
  RECORD.md and INVESTIGATIVE-SESSION.md §11 with its front matter, whole; every site the fix touches TRACED in
  `index.mjs`, `store.mjs`, `airun.mjs` and the catalogue (line numbers in the commit message).
- The only tracked change: two pen lines in `.gitignore`, whose comment names a control driver
  `bio-plane/test/nc-rec165.mjs` that is NOT YET WRITTEN.
- Not done: no code or test edits, no IC minted, no check number taken, no suite, baseline, battery or gate run.

## Next, in order (the commit message has all 13 steps)
1. The CLAIMS.md block; mint the IC with `node tools/mintid.mjs IC`.
2. `index.mjs`: add `RUN_PRODUCTION_ACTIONS = ["suggest", "extractpropose"]` and extend the ONE `principal` stamp
   condition with it. Do NOT extend the `actor` stamp that `RUN_VERB_ACTIONS` also drives.
3. The `store.mjs` routes: both ops take `caller` from that stamp. On `suggest`, set it AFTER the body spread, or a
   caller could send their own.
4. `suggestVersion`, inside the `is-suggest-shape` region and before the stored-refusal lookup, three checks in order:
   a run whose context the caller cannot see answers the same `SUGGEST_NO_RUN` as a run that never existed; another
   principal's run gets `runPrincipalGate`'s refusal, passed through; a run that has ended gets a new row,
   `SUGGEST_RUN_NOT_RUNNING` (proposed C-27.18 — the tool leaves numbers within a family to its owner; C-27.18 appears
   nowhere in the tree).
5. `extractPropose`: the same sight check, then the principal gate, then the existing `RUN_NOT_RUNNING` check.
6. `runPrincipalGate` gains an optional `act` argument so its message fits all four ops; tick and close keep their wording.
7. A new suite and its control driver covering both ops and both caller kinds (a member session, and an AI credential
   that member minted).
8. Correct existing suites that assumed the old behaviour, with dated comments, never exempted: `dec65-single-part.test.mjs`
   (suggests under Ruth's run as the member-class token and as the member none-so-blind) WILL break; ~15 others to check,
   listed in the commit message.
9. `airun-principal.control.mjs` anchors on the exact stamp line step 2 changes: re-anchor it and re-run it.
10. Move the refusal-code guard's floors and the coverage floors to the figures the tools print; file the IC; update
    `construct-status.json` and the design's front matter; rebuild; gate; push.

Measured for the IC: `civicos-ui` calls neither op; `agent-worker` calls `suggest` with the same credential it ticks
with, and REC-152 already requires that credential to be the run's principal.

## Found by reading, NOT yet driven
- **`op=capturerequest` has the same defect and rule 1 does not name it**: it accepts any running run and copies that
  run's principals into the request row, so one member can file a capture request the record credits to another member.
  Same fix (the stamp, the sight check, the principal gate). To SCHEDULER once someone has DRIVEN it through the op.
- **Possible DESIGN GAP, INVESTIGATIVE-SESSION.md §11 item 5:** rule 1 does not say whether a suggestion's target
  question must be inside its run's context; as written, a run over one question can suggest onto another. BOB's.

The worker's run logs and suite runner were in the ignored `.rec165-runs/` folder on the Mac only; not needed.
