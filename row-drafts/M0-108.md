### M0-108 · queued — **`bio-plane/test/retirable.control.mjs` DECLARES FEWER ARMS THAN IT RUNS: its head says *"eight arms, M0-83's six"* and `ARMS` holds fifteen, A6b among them.** The census reads a driver's declared count from its head (`readDeclaredArms`), so the declaration it reads is not the driver's (re-read on `7c967f09`; CONDUCT #11's route). — owner M0.
order: behind the product rows, in the instrument cluster after M0-93, the same class: a control's declaration disagreeing with what it runs (Bob, 2026-09-22: *process is overhead*; SCHEDULER #12)
milestone: M0
interface: none
design: `docs/development/VERIFICATION.md` (admitted for M0 by name), with the census's `readDeclaredArms` (`m025-arm-anchor-witness.test.mjs`) as the reader the phrase must satisfy.
depends-on: none.
scope: the head states fifteen arms (D-402's eight, A6b and M0-83's six) in a phrase `readDeclaredArms` reads, dated at the site; nothing else in the driver moves.
accepts-when: the census reads fifteen declared arms for the driver, equal to the length of `ARMS`. How a liar passes it: a phrase the reader cannot parse, which reads as no declaration, so the arm asserts the parsed figure. NEGATIVE CONTROL: restore "eight arms", and the equality fails naming the driver.
added: 2026-09-22 · SCHEDULER #12 (CONDUCT #11's route, verified at the code; `node tools/mintid.mjs M0`).
