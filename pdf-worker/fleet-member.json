{
  "name": "pdf-worker",
  "entry": "src/index.mjs",
  "surface": "SURFACE",
  "testDir": "test",
  "bundle": {
    "entry": "src/index.mjs",
    "outfile": "dist/pdf-worker.bundled.mjs",
    "manifest": "dist/pdf-worker.bundle.json",
    "external": ["cloudflare:workers", "node:*"],
    "note": "FL-9's GUARD (BOB, 2026-09-10). This member has committed a bundle since 2026-07-31 and had NOTHING asserting the bundle matched its source — DIST measured that gap. The recipe lives in bio-plane/scripts/fleet-bundle.mjs, the committed manifest beside the artifact states its sha256 and the sha256 of every input, and bio-plane/test/fleetbundles.test.mjs FAILS on a stale artifact rather than letting it ship. THREE of this member's six build inputs are ../bio-plane/src/* (pdfstructure, subresources, cpu), so a change to the PLANE stales this artifact."
  },
  "note": "The fleet-coverage marker (D-117). scripts/coverage.mjs discovers a fleet member by this file, reads its `surface` table from `entry`, and holds it to the same three surfaces the plane is held to: each surface op reached by a suite in `testDir`, and a declared negative control. A second Worker shipping with flat coverage is the failure this closes."
}
