# `node_modules` WITHOUT the trailing slash (c18-batch7fix, 2026-09-24): `node_modules/` matches only a DIRECTORY,
# so a SYMLINKED node_modules was committed on c17-batch7 (daf0c729) and replaced installed modules with a dangling link.
node_modules
